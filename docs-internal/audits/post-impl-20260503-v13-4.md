# Post-Implementation Audit — 2026-05-03 (v13.4 close gate)

> **Scope:** v13.4 implementation surface from Phases 225, 226, 227, 228, 230, and 231, plus the Phase 229 inline audit fixes. The audit resolves evidence against committed phase artifacts and current committed code. Pre-existing uncommitted catalog maps, embed-token, frontend, and backend-test edits were not credited as shipped work.

> **Close status:** **MILESTONE CLOSE VERIFIED** for the committed v13.4 scope. No unresolved P1 findings remain. Two CI-blocking audit findings were fixed inline: format drift in Phase 230 architecture files (`f71fffb7`) and stale reupload test patch targets after CatalogPort migration (`75c32019`).

## Boundary

Boundary Integrity grades **A+**.

- Phase 219 OAuth IdP dual-layer gate still holds at schema and service layers.
- The false share/embed-token "(enterprise only)" API copy found on 2026-05-02 is absent in committed HEAD; committed field descriptions now describe the behavior without an unbacked edition claim.
- No new enterprise-only feature scaffolding was introduced by Phases 225, 226, 227, 228, 230, or 231.
- `standards/` remains hard-free by inspection from the prior audit and is not touched by v13.4 phase work.

Dirty-worktree note: uncommitted embed-token and map-share changes appear to add edition gating and alter related tests. Those edits predate Phase 229 and are outside the committed close-gate evidence; they are listed as residual risk, not as shipped boundary evidence.

## Coupling

Coupling Health grades **A-**.

- Phase 225 closed the `processing -> catalog` module-level direction with `ProcessingPort` and `test_no_processing_imports_catalog`.
- Phase 230 closed the symmetric `catalog -> processing` module-level direction with `CatalogPort` and `test_no_catalog_imports_processing`.
- Focused greps returned zero forbidden module-level imports in both directions.
- `log_action()` remains behind the AuditSink chokepoint. The only non-chokepoint hit is a registry docstring in `backend/app/platform/extensions/__init__.py`.
- The known large modules `catalog/maps/service.py` and `catalog/search/service.py` remain P2 follow-ups from the 2026-05-02 audit, not P1 close blockers.

## Seam

Seam Quality grades **A-**.

- Phase 226 promotes AI chat/completion dispatch to `AIProviderExtension`, `DefaultAnthropicProvider`, `DefaultOpenAICompatibleProvider`, and `get_ai_provider(name)`.
- Phase 231 promotes embeddings to `EmbeddingProviderExtension`, `DefaultOpenAIEmbeddingProvider`, and `get_embedding_provider(name)`.
- Static provider-SDK import checks return zero module-level `anthropic` or `openai` imports under `backend/app/processing/`.
- `test_no_hardcoded_ai_provider_branches` and the expanded provider SDK guard are part of the architecture marker suite.
- Deferred AI structured-output paths called out in prior artifacts remain explicitly out of v13.4 scope.

## OSS Surface

OSS Surface grades **A** for the v13.4 close gate.

- Phase 228 completed the cold publish path: `geolens==1.0.0`, `geolens-cli==1.0.0`, and `@geolens/sdk==1.0.0` are visible from public package registries.
- PyPI publishes use Trusted Publishing; `NPM_TOKEN` presence and `PYPI_TOKEN` absence are documented in `228-VERIFICATION.md`.
- The clean-machine `verify-published.yml` workflow passed during Phase 228.
- Remaining OSS adoption items (`geolens.yaml`, `geolens-schemas`, Helm/AMI/SBOM) stay future-scope P2/P3 items.

## Findings

| ID | Severity | Area | Evidence | Disposition |
|---|---|---|---|---|
| F-01 | P1 | CI format gate | `ruff format --check` wanted changes in `backend/app/core/catalog_port.py` and `backend/tests/test_layering.py`. | Fixed inline in `f71fffb7` (`style(229): format audit architecture surfaces`). |
| F-02 | P1 | Test compatibility after CatalogPort | Full backend run stopped at `tests/test_defer_orphan_guard.py::TestReuploadOrphanGuard::test_reupload_service_defer_failure_marks_job_failed`; tests patched removed `router_reupload.reupload_service` instead of the CatalogPort task accessor. | Fixed inline in `75c32019` (`test(229): patch reupload task tests through catalog port`). Focused reupload tests now pass. |
| R-01 | Residual risk | Dirty worktree | Full backend rerun after F-02 reached 418 passed, then failed in `tests/test_embed_tokens.py::TestTileEmbedTokenAccess::test_tile_access_expired_token` with 422 vs 201 while embed-token schemas/services/tests are dirty before Phase 229. | Not a Phase 229 fix. Treat as unrelated dirty-worktree residual risk for the owner of those changes. |
| R-02 | Residual risk | Local DB provisioning | Running DB-backed tests without the documented Compose env hits `CREATE EXTENSION IF NOT EXISTS vector` because the host PostgreSQL lacks `vector.control`. | Documented. CI-style `POSTGRES_PORT=5434` path progressed into real tests; DB-independent checks use `POSTGRES_PORT=1`. |

No unresolved P0 or P1 findings remain for the committed v13.4 scope.

## Grades

| Dimension | Prior relevant grade | Post-audit grade | Target | Result |
|---|---:|---:|---:|---|
| Boundary Integrity | A after 2026-05-02, A+ after false-copy cleanup | **A+** | A+ | PASS |
| Coupling Health | B+ on 2026-05-02 before Phase 230 | **A-** | A- | PASS |
| Seam Quality | A- on 2026-05-02 before Phase 231 | **A-** | A- | PASS |
| OSS Surface | A- on 2026-05-02 before Phase 228 close | **A** | evidence only | PASS |

## Verification

Passed:

- `node /Users/ishiland/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/229-post-impl-audit-v13-4/229-01-PLAN.md` — valid, no warnings.
- `cd backend && POSTGRES_PORT=1 uv run pytest tests/test_layering.py -m architecture -q` — 13 passed.
- `cd backend && POSTGRES_PORT=1 uv run pytest tests/test_ai_provider_extension.py tests/test_embedding_provider_extension.py -q` — 6 passed.
- `cd backend && POSTGRES_PORT=1 uv run pytest tests/test_layering.py -m architecture tests/test_ai_provider_extension.py tests/test_embedding_provider_extension.py tests/test_embedding_service.py -q` — 13 passed, 11 deselected.
- `cd backend && env ... POSTGRES_PORT=5434 ... uv run pytest tests/test_defer_orphan_guard.py::TestReuploadOrphanGuard tests/test_reupload_service.py::TestServiceReuploadCommitDispatch -q` — 4 passed.
- `cd backend && uv run ruff check ...` on audit-touched architecture and reupload test files — passed.
- `cd backend && uv run ruff format --check ...` on audit-touched architecture and reupload test files — passed after `f71fffb7`.
- `python -m pip index versions geolens` — latest visible `1.0.0`.
- `python -m pip index versions geolens-cli` — latest visible `1.0.0`.
- `npm view @geolens/sdk version --json` — `"1.0.0"`.

Expected zero-output checks:

- `git grep -n -E '^(from|import) (backend\.)?app\.modules\.catalog|^(from|import) app\.modules\.catalog' -- backend/app/processing/` — no output.
- `git grep -n -E '^(from|import) (backend\.)?app\.processing|^(from|import) app\.processing' -- backend/app/modules/catalog/` — no output.
- `git grep -n -E '^(from|import) (anthropic|openai)( |$)' -- backend/app/processing/` — no output.
- `git grep -n -P "if\s+.*provider\s*==\s*['\"](?:anthropic|openai_compatible)" -- backend/app/processing/ ':!backend/app/processing/ai/streaming.py' ':!backend/app/processing/ai/metadata_service.py'` — no output.

Blocked or limited:

- `cd backend && uv run pytest tests/test_embedding_service.py -q` and `cd backend && uv run pytest tests/test_saml_overlay.py -q` without the documented local Compose DB env fail at `CREATE EXTENSION IF NOT EXISTS vector` because the host PostgreSQL installation lacks `/usr/share/postgresql/17/extension/vector.control`.
- The full backend CI-style run with `POSTGRES_PORT=5434` is not claimed green because the dirty worktree's embed-token changes caused `tests/test_embed_tokens.py::TestTileEmbedTokenAccess::test_tile_access_expired_token` to fail with 422 vs 201 after 418 passes. This is outside the committed Phase 229 audit scope.

## Milestone Close Status

**MILESTONE CLOSE VERIFIED — v13.4 can close.**

PIAUDIT-01 is satisfied by this dated report. PIAUDIT-02 is satisfied because both P1 findings were fixed inline and no unresolved P1 remains. PIAUDIT-03 is satisfied because Boundary Integrity A+, Coupling Health A-, and Seam Quality A- meet the roadmap targets.
