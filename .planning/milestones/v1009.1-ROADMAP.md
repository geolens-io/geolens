# Milestone Archive: v1009.1 Builder Smoke Polish

**Shipped:** 2026-05-15
**Phases:** 1045 (single phase, hygiene pattern)
**Requirements:** 18 (SP-01..SP-18); 16 PASS + 1 PARTIAL + 1 ESCALATE + 1 SKIPPED-with-rationale
**Commits:** 24 between `cf40075b..1168e91a`
**Tag:** `v1009.1`

## Goal

Close out the 17 non-B-01 findings from the 2026-05-15 Map Builder Playwright smoke check (`.planning/quick/260515-cej-docker-rebuild-builder-smoke/FINDINGS.md`). B-01 itself (live-add maplibre sync regression) shipped ahead of milestone open at commit `85738f1c`.

## Phase

- ✅ **Phase 1045: builder-smoke-polish** — completed 2026-05-15. Three sequential plans grouped by severity (A: BLOCKER+MAJORs, B: MINORs, C: POLISH+HOUSEKEEPING), single CTRL-01 batch gate at phase end.

## Outcomes

### Resolved (16 PASS)

| Req | Severity | Outcome |
|---|---|---|
| SP-01 | BLOCKER | BulkActionBar Group/Ungroup/Delete reachable via Lucide `MoreHorizontal` overflow popover |
| SP-02 | MAJOR | Coord readout subscribes to MapLibre `move` event; lat/lng tracks `map.getCenter()` |
| SP-04 | MAJOR | Shift-click extends layer selection as range via `computeNextSelection` pure helper with `useRef` anchor |
| SP-05 | MAJOR | "Pending style preview" banner gated on `hasUnsavedStyleChanges(draft, layer)` deep-equal helper |
| SP-06 | MINOR | Duplicate "Saved" badge removed; Save button carries Saved/Saving…/Save state alone |
| SP-08 | MINOR | `use-ai-availability` wrapped in TanStack `useQuery` with `staleTime: 60_000` |
| SP-09 | MINOR | Module-level `inflightRefresh` mutex in `client.ts` + routed `use-auth.ts` proactive timer through `tryRefresh` |
| SP-10 | MINOR | `aria-pressed={layer.visible}` on every visibility toggle button |
| SP-11 | MINOR | `/auth/login` decorator changed to no-trailing-slash (200 not 307) |
| SP-13 | POLISH | Basemap eye is `<span>` glyph with tooltip; disabled `<button>` removed |
| SP-14 | POLISH | StackRow row container has `cursor-pointer` + `hover:bg-[var(--surface-2)]` |
| SP-15 | POLISH | BulkActionBar gated on `!isSettingsOpen`; selection persists across scene switch |
| SP-16 | POLISH | `captureThumbnail` wrapped in 500ms trailing-edge debounce keyed by mapId |
| SP-17 | POLISH | Full-width "＋" U+FF0B stripped from all 4 locales; Lucide `<Plus />` icon only |
| SP-18 | HOUSEKEEPING | Test maps `a00b7e96-…` + `58149b92-…` deleted; GET returns 404 |
| (review) | — | WR-02/WR-03/IN-02/IN-05 from cross-cutting code review applied inline at commit `9031742f` |

### Deferred / Escalated

| Req | Severity | Disposition | Followup |
|---|---|---|---|
| SP-03 | MAJOR | **ESCALATE** | B-01 fix at `85738f1c` did NOT fully close M-02. Live Playwright re-check showed fresh-add still doesn't push source/layer into maplibre style until reload. Suspected `syncInputs` memo closure on `structuralKey`-only key, OR `mapReady` timing on second effect re-fire when tokens land. Workaround: refresh after add. **Followup quick task required.** |
| SP-07 | MINOR | **PARTIAL-PASS** | Frontend `quicklook-cache.ts` caches 404 responses so the search page no longer re-requests. First request per session still 404s. Honest fix is backend `has_quicklook` predicate. **Backend followup quick task required.** |
| SP-12 | POLISH | **SKIPPED-with-rationale** | MapLibre `ScaleControl` bar already on screen (`BuilderMap.tsx:854`, `ViewerMap.tsx:746`). Smoke checker's p-01 actually asked for a "1:N" representative-fraction pane — a small feature (requires meters-per-pixel calculation with cos-lat factor, new UI slot in MapCoordReadout, i18n for "1:N" formatting). Out of polish scope. **New feature ticket if requested.** |

### Pre-existing main-branch issues (out of scope, not regressed)

- 5 ESLint errors on `main` (EmptyStackState.tsx × 4, UnifiedStackPanel.test.tsx × 1) — documented in `.planning/milestones/v1009.1-phases/1045-builder-smoke-polish/deferred-items.md`

## Verification gates

- `npx tsc --noEmit` clean
- `npx eslint src/` clean (no new errors from this milestone)
- vitest 516+/516+ across Plan A/B/C surfaces (174 + 174 + 168)
- `uv run ruff check` clean
- Backend pytest auth 37/37
- i18n 773 keys × 4 locales (en/de/es/fr)
- Live Playwright spot-checks confirmed SP-01, 02, 10, 11, 13, 17, 18
- Live `curl POST /auth/login` confirmed 200 (no 307) for SP-11
- Live API `DELETE` + `GET` confirmed 204/404 for SP-18

## Reviewer findings (inline-fixed)

3 self-review fixes during execution (Plan A: Radix portal selection bug; Plan B: misleading test name; Plan C: unused state field) + 4 cross-cutting reviewer fixes after (WR-02 inflightRefresh microtask race, WR-03 defensive Set copy, IN-02 redundant zoomend handler, IN-05 SP-11 docstring).

## Files touched (16 total)

**Frontend (13):**
- `frontend/src/components/builder/BulkActionBar.tsx` (SP-01)
- `frontend/src/components/builder/UnifiedStackPanel.tsx` (SP-01/04/10/13/14/15/17)
- `frontend/src/components/builder/StackRow.tsx` (SP-04/10/14)
- `frontend/src/components/builder/BasemapGroupRow.tsx` (SP-13)
- `frontend/src/components/builder/SidebarRail.tsx` (SP-17)
- `frontend/src/components/builder/LayerStyleEditor.tsx` (SP-05)
- `frontend/src/components/builder/BuilderMap.tsx` (SP-16)
- `frontend/src/components/builder/selection-utils.ts` (SP-04 new helper)
- `frontend/src/components/map/MapCoordReadout.tsx` (SP-02 + IN-02 cleanup)
- `frontend/src/pages/MapBuilderPage.tsx` (SP-02/04/06/15 + WR-03)
- `frontend/src/hooks/use-ai-availability.ts` (SP-08)
- `frontend/src/hooks/use-auth.ts` (SP-09 root)
- `frontend/src/api/client.ts` (SP-09 mutex + WR-02)
- `frontend/src/lib/quicklook-cache.ts` (SP-07 new)
- `frontend/src/components/builder/DatasetSearchPanel.tsx` (SP-07 extended)
- `frontend/src/components/search/SearchResultCard.tsx` (SP-07)
- i18n locales `en/de/es/fr/builder.json` (SP-17)

**Backend (1):**
- `backend/app/modules/auth/router.py` (SP-11 + IN-05 docstring)

## Open followups

1. **B-01 / SP-03 quick task** — investigate residual maplibre sync race; suspected `syncInputs` memo closure or `mapReady` timing
2. **SP-07 backend quick task** — add `has_quicklook` predicate to skip quicklook serving for datasets without a generated preview
3. **SP-12 feature ticket** — representative-fraction "1:N" pane next to MapCoordReadout (requires cos-lat factor + i18n)
