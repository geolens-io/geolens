---
phase: 1045-builder-smoke-polish
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/hooks/use-ai-availability.ts
  - frontend/src/api/client.ts
  - frontend/src/pages/MapBuilderPage.tsx
  - frontend/src/components/builder/StackRow.tsx
  - frontend/src/components/builder/UnifiedStackPanel.tsx
  - frontend/src/pages/SearchPage.tsx
  - backend/app/modules/auth/router.py
autonomous: false
requirements:
  - SP-06
  - SP-07
  - SP-08
  - SP-09
  - SP-10
  - SP-11

must_haves:
  truths:
    - "Header shows exactly one save-state element (badge OR button, not both)"
    - "Search page loads with zero console errors"
    - "/api/admin/ai-status/ is called at most twice in any 3-minute idle window"
    - "Concurrent 401s trigger exactly one /api/auth/refresh/ POST per refresh cycle"
    - "Visibility toggle buttons expose aria-pressed reflecting layer.visible"
    - "POST /auth/login (no trailing slash) returns 200/422 instead of 307"
  artifacts:
    - path: ".planning/phases/1045-builder-smoke-polish/VERIFICATION.md"
      provides: "Per-SP pass record (6 lines after Plan B)"
      contains: "SP-06"
---

<objective>
Plan B of Phase 1045 — close the 6 MINOR findings from the 2026-05-15 Map Builder smoke check. Network hygiene (ai-status + auth refresh), a11y (aria-pressed), header dedup, search-page console cleanup, and login trailing-slash.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/quick/260515-cej-docker-rebuild-builder-smoke/FINDINGS.md
</context>

## Goal

Close the 6 MINOR findings. None are user-blocking but each is a tracked irritant: poll storm, refresh storm, duplicate save badge, 404 console noise, missing aria-pressed, login 307.

## Per-plan done definition

1. All 6 tasks committed (conventional commits, no AI/bot attribution, ≤72 char subjects)
2. `cd frontend && npx tsc --noEmit` — clean
3. `cd frontend && npx eslint src/` — clean
4. `cd frontend && npm test -- --run` passes for affected files
5. `cd backend && uv run ruff check .` clean (for SP-11 only)
6. `/gsd-code-review --fix` run; every finding addressed inline
7. Commit chain pushed

## Sequencing rationale

SP-08 / SP-09 are network-hygiene fixes on shared infra (TanStack Query + auth interceptor) — do them early so they don't perturb later UAT. SP-06 + SP-10 are both in MapBuilderPage header / StackRow respectively. SP-07 is SearchPage-only. SP-11 is the only backend task — runs last so frontend ruff/typecheck don't blur with uv/ruff calls.

<tasks>

<task type="auto" tdd="true">
  <name>Task B.1 — SP-08: Cache /api/admin/ai-status/ via TanStack Query staleTime</name>
  <files>frontend/src/hooks/use-ai-availability.ts</files>
  <behavior>
    - Hook calls ai-status at most once per 60s per session
    - Multiple consumers share a single in-flight query (queryKey: ['admin', 'ai-status'])
    - Result is cached for 60s minimum
  </behavior>
  <action>Smoke showed 11+ hits in a 3-minute idle window because the hook re-fires on every consumer mount. Wrap the fetch in TanStack `useQuery` with `queryKey: ['admin', 'ai-status']`, `staleTime: 60_000`, `gcTime: 5 * 60_000`. Keep the existing return shape (callers expect `{available, model, ...}`). If multiple files use ai-status, hoist via the shared hook — do not duplicate the queryKey. If the existing file is `use-ai-availability.ts` and another `useAiStatus`-like hook also exists, consolidate to one.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npx eslint src/hooks/use-ai-availability.ts &amp;&amp; npm test -- --run use-ai</automated>
    <human-check>DevTools network tab: keep builder idle 3 minutes; count `/api/admin/ai-status/` calls; should be ≤ 2.</human-check>
  </verify>
  <done>ai-status fires at most 2× per 3-min idle window across all consumers.</done>
  <commit>perf(frontend): cache ai-status query with 60s staleTime</commit>
</task>

<task type="auto" tdd="true">
  <name>Task B.2 — SP-09: De-dupe concurrent /api/auth/refresh/ via in-flight promise</name>
  <files>frontend/src/api/client.ts</files>
  <behavior>
    - 3 concurrent 401 responses trigger exactly 1 POST /auth/refresh/
    - All 3 callers resolve with the same new token
    - On refresh failure all 3 reject and clear the in-flight promise
  </behavior>
  <action>Add a module-level `let inflightRefresh: Promise&lt;...&gt; | null = null` singleton in client.ts. The 401 interceptor checks the singleton: if null, kick off the refresh POST and assign the promise; otherwise await the existing one. On resolve/reject, clear `inflightRefresh = null` so subsequent expirations start fresh. Keep existing token-rotation logic intact. Pure de-duplication, no new behavior.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npx eslint src/api/client.ts &amp;&amp; npm test -- --run client</automated>
    <human-check>DevTools: open builder, observe page that triggers ≥3 concurrent tile-token 401s; network tab shows exactly 1 POST /auth/refresh/.</human-check>
  </verify>
  <done>Concurrent 401s collapse to a single refresh POST. No regression in single-401 path.</done>
  <commit>fix(api): de-dupe concurrent auth refresh via in-flight promise</commit>
</task>

<task type="auto" tdd="false">
  <name>Task B.3 — SP-06: Remove duplicate Saved badge from builder header</name>
  <files>frontend/src/pages/MapBuilderPage.tsx</files>
  <action>Header today renders BOTH `[✓ Saved]` badge AND `[Save] Saved` button. Remove the badge; let the Save button carry the state (Save / Saving… / Saved label + dirty indicator). Keep the existing dirty-detection wiring; only delete the badge element and its surrounding wrapper. If the badge was used to convey timestamp ("Last saved 2 min ago"), preserve that info via a tooltip on the button.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npx eslint src/pages/MapBuilderPage.tsx</automated>
    <human-check>DOM check: exactly one element matching `Saved`/`Unsaved` text in the header per state.</human-check>
  </verify>
  <done>Header has one save-state element (the Save button) carrying all three states.</done>
  <commit>fix(builder): remove duplicate Saved badge from header</commit>
</task>

<task type="auto" tdd="true">
  <name>Task B.4 — SP-10: Add aria-pressed to visibility toggle buttons</name>
  <files>frontend/src/components/builder/StackRow.tsx, frontend/src/components/builder/UnifiedStackPanel.tsx</files>
  <behavior>
    - Visibility toggle button exposes `aria-pressed={layer.visible}`
    - Basemap row eye glyph (until SP-13 lands) ALSO exposes aria-pressed for the visible state
    - Vitest a11y assertion: `getByRole('button', {name: /toggle visibility/i, pressed: true|false})` matches
  </behavior>
  <action>Add `aria-pressed={layer.visible}` (or `!layer.hidden` — match the existing visibility prop name) to every `<button aria-label="Toggle visibility for …">` in StackRow.tsx and the basemap row in UnifiedStackPanel.tsx. Pure attribute addition.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npx eslint src/components/builder/StackRow.tsx src/components/builder/UnifiedStackPanel.tsx &amp;&amp; npm test -- --run StackRow</automated>
    <human-check>DOM query for `[aria-pressed]` on visibility toggles returns ≥ N elements (one per row).</human-check>
  </verify>
  <done>Every visibility toggle has aria-pressed reflecting layer.visible.</done>
  <commit>fix(a11y): aria-pressed on visibility toggle buttons</commit>
</task>

<task type="auto" tdd="false">
  <name>Task B.5 — SP-07: Skip quicklook GET when thumbnail_status != 'ready'</name>
  <files>frontend/src/pages/SearchPage.tsx</files>
  <action>Smoke shows 3 of 6 seeded MultiPoint datasets 404 on `/api/datasets/&lt;id&gt;/quicklook?size=256`. Per REQUIREMENTS.md SP-07, the frontend-skip path is lower-risk than a backend Celery backfill. In SearchPage's quicklook fetch (or its underlying hook), conditionally skip the GET when the dataset record's `thumbnail_status != 'ready'`. Card placeholder logic already handles missing quicklooks gracefully — just don't issue the request. If the field name differs (e.g. `quicklook_status`, `preview_status`), use whatever the existing dataset shape exposes.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npx eslint src/pages/SearchPage.tsx</automated>
    <human-check>Reload SearchPage with DevTools open; console errors = 0 (the 3 quicklook 404s must be gone).</human-check>
  </verify>
  <done>SearchPage console clean. Quicklook GET issued only when thumbnail_status === 'ready'.</done>
  <commit>fix(search): skip quicklook fetch when thumbnail_status not ready</commit>
</task>

<task type="auto" tdd="false">
  <name>Task B.6 — SP-11: Define /auth/login without trailing slash</name>
  <files>backend/app/modules/auth/router.py</files>
  <action>Today `POST /auth/login` returns 307 redirect to `/auth/login/`, which drops the form body for HTTP clients that don't replay on 307. Per the CLAUDE.md FastAPI trailing-slash guidance both shapes are legal — pick the no-trailing-slash form for external integrators. Locate the login route decorator (likely `@router.post("/login/", …)`) and change to `@router.post("/login", …)`. Verify no other internal callers rely on the trailing-slash form (grep for `/auth/login/` in backend/frontend). The frontend already hits `/api/auth/login/` through the proxy — that path is separately mounted at the API gateway and unaffected by this internal route shape change. If frontend hits the trailing-slash path directly, update to no-slash for parity.</action>
  <verify>
    <automated>cd backend &amp;&amp; uv run ruff check app/modules/auth/router.py &amp;&amp; uv run ruff format --check app/modules/auth/router.py</automated>
    <human-check>`curl -X POST http://localhost:8001/auth/login -H "content-type: application/x-www-form-urlencoded" -d 'username=admin&amp;password=admin'` returns 200 (or 422 with wrong creds) — NOT 307.</human-check>
  </verify>
  <done>POST /auth/login returns body-preserving 200/422 without redirect. Browser auth flow unaffected.</done>
  <commit>fix(auth): drop trailing slash on /auth/login route</commit>
</task>

</tasks>

<verification>
- Frontend typecheck + lint clean
- Backend ruff clean (auth router only)
- Vitest passes on affected files
- DevTools network audit confirms ai-status ≤ 2/3min and auth-refresh dedupe
- SearchPage console clean (3 quicklook 404s gone)
</verification>

<success_criteria>
- 6/6 tasks complete
- All B-plan must_haves true
- /gsd-code-review --fix run with findings addressed inline
</success_criteria>

<output>
Create `.planning/phases/1045-builder-smoke-polish/1045-02-SUMMARY.md` when done.
</output>
