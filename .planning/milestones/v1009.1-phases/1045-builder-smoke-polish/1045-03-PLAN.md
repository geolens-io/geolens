---
phase: 1045-builder-smoke-polish
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - frontend/src/components/builder/StackRow.tsx
  - frontend/src/components/builder/UnifiedStackPanel.tsx
  - frontend/src/pages/MapBuilderPage.tsx
  - frontend/src/components/builder/BuilderMap.tsx
autonomous: false
requirements:
  - SP-12
  - SP-13
  - SP-14
  - SP-15
  - SP-16
  - SP-17
  - SP-18

must_haves:
  truths:
    - "Basemap visibility slot renders as a non-interactive glyph (not a disabled <button>)"
    - "Hovering a layer row body shows cursor:pointer and a subtle hover background"
    - "Opening the global Settings scene hides the BulkActionBar"
    - "Layer add fires at most 1 PUT /thumbnail/ within a 500ms window"
    - "'+ Add data' button uses Lucide <Plus /> icon"
    - "Map UUID a00b7e96-95b7-48d6-a911-57b97b767ebc returns 404 from /api/maps/"
  artifacts:
    - path: ".planning/phases/1045-builder-smoke-polish/VERIFICATION.md"
      provides: "Per-SP pass record (7 lines after Plan C)"
      contains: "SP-12"
---

<objective>
Plan C of Phase 1045 — close the 6 POLISH findings plus 1 HOUSEKEEPING item from the 2026-05-15 Map Builder smoke check. Visual nits + scene-aware bulk bar + thumbnail debounce + delete the test map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/quick/260515-cej-docker-rebuild-builder-smoke/FINDINGS.md
</context>

## Goal

Close the visual + scene-aware polish items. SP-12 (scale pane) is OPTIONAL — skip with justification if non-trivial. SP-18 deletes the test map left over from the smoke run.

## Per-plan done definition

1. All 5 tasks committed or operationally completed (C.5 is no-commit)
2. `cd frontend && npx tsc --noEmit` — clean
3. `cd frontend && npx eslint src/` — clean
4. `cd frontend && npm test -- --run` passes for affected files
5. `/gsd-code-review --fix` run; every finding addressed inline
6. Commit chain pushed

## Sequencing rationale

SP-13 + SP-14 + SP-17 all touch StackRow.tsx + UnifiedStackPanel.tsx — grouped into one task to minimize churn. SP-15 is in MapBuilderPage selection/scene logic. SP-16 is in the builder save path. SP-12 is OPTIONAL — last. SP-18 is operational — very last.

<tasks>

<task type="auto" tdd="false">
  <name>Task C.1 — SP-13 + SP-14 + SP-17: StackRow + basemap row + Add data icon polish</name>
  <files>frontend/src/components/builder/StackRow.tsx, frontend/src/components/builder/UnifiedStackPanel.tsx</files>
  <action>Three small DOM/style changes grouped because they collide on the same two files: (1) SP-13: Replace the disabled `<button aria-label="Toggle visibility for Basemap · Positron">` in the basemap row with a non-interactive `<span title="Basemap is always visible — use Remove basemap to hide.">` containing a muted eye glyph (or small lock icon). Keep the layout slot footprint identical. (2) SP-14: Add `hover:bg-[var(--surface-2)]` and `cursor-pointer` to the StackRow row-container Tailwind classes so hovering the row body shows a visible affordance; do not change click behavior. (3) SP-17: Replace the `"＋"` (U+FF0B full-width plus) literal in the "Add data" button with Lucide `<Plus className="h-4 w-4" />`. Import `Plus` from `lucide-react`.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npx eslint src/components/builder/StackRow.tsx src/components/builder/UnifiedStackPanel.tsx &amp;&amp; npm test -- --run StackRow UnifiedStackPanel</automated>
    <human-check>Visual: basemap row eye is non-interactive glyph with tooltip; row hover shows surface-2 background + pointer cursor; "+ Add data" button shows Lucide plus icon (not full-width character).</human-check>
  </verify>
  <done>Basemap visibility slot is a glyph not a disabled button. Row hover shows pointer + background. Add data uses Lucide Plus.</done>
  <commit>fix(builder): polish basemap eye glyph, row hover, and Add data icon</commit>
</task>

<task type="auto" tdd="false">
  <name>Task C.2 — SP-15: Hide BulkActionBar when global Settings scene is active</name>
  <files>frontend/src/pages/MapBuilderPage.tsx</files>
  <action>Today the BulkActionBar stays pinned at the bottom of the sidebar even when the global ⚙ Settings panel is open, showing "2 selected" against unrelated content. Conditionally hide the BulkActionBar when the current builder scene is Settings (check the existing scene-routing state — likely `activeScene === 'settings'` or similar). Selection state can persist behind the scenes; just hide the bar visually. When Settings closes, the bar returns with the selection intact.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npx eslint src/pages/MapBuilderPage.tsx</automated>
    <human-check>Playwright: multi-select 2 layers → open Settings → bar gone; close Settings → bar back with same selection.</human-check>
  </verify>
  <done>BulkActionBar hidden when Settings scene is active; selection preserved across scene switch.</done>
  <commit>fix(builder): hide BulkActionBar during global Settings scene</commit>
</task>

<task type="auto" tdd="true">
  <name>Task C.3 — SP-16: Debounce thumbnail PUT to coalesce back-to-back layer-adds</name>
  <files>frontend/src/components/builder/BuilderMap.tsx</files>
  <behavior>
    - Two layer-adds within 500ms result in exactly 1 PUT /api/maps/&lt;id&gt;/thumbnail/
    - Single layer-add still PUTs once after the debounce window
    - Trailing-edge behavior (not leading) so the capture reflects the final state
  </behavior>
  <action>Locate the thumbnail PUT call in the builder save path (search `thumbnail` in BuilderMap.tsx / MapBuilderPage.tsx). Wrap the capture trigger in a 500ms trailing debounce — either lodash `debounce` (already a frontend dep) or a small `useDebouncedCallback` if one exists in the codebase. Don't add a new dependency. Smoke showed 2 back-to-back PUTs (requests 313, 314) — coalesce to 1.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npx eslint src/components/builder/BuilderMap.tsx &amp;&amp; npm test -- --run BuilderMap</automated>
    <human-check>DevTools network: add a layer; count PUT /thumbnail/ calls within 500ms = 1 (was 2).</human-check>
  </verify>
  <done>At most 1 PUT thumbnail per 500ms regardless of how many layer-adds queue up.</done>
  <commit>perf(builder): debounce thumbnail PUT by 500ms</commit>
</task>

<task type="auto" tdd="false">
  <name>Task C.4 — SP-12 (OPTIONAL): Add scale pane next to coord readout</name>
  <files>frontend/src/pages/MapBuilderPage.tsx, frontend/src/components/builder/BuilderMap.tsx</files>
  <action>OPTIONAL. If non-trivial, skip with a one-line justification in VERIFICATION.md. Simplest path: instantiate MapLibre's built-in `ScaleControl` (`new maplibregl.ScaleControl({maxWidth: 100, unit: 'metric'})`) and add it via `map.addControl(scale, 'top-right')`, OR render its DOM next to the existing coord readout for layout parity. If MapLibre's default position doesn't sit cleanly with the existing header, skip and write `SP-12: SKIPPED — non-trivial layout integration; deferred to follow-up.` to VERIFICATION.md.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npx eslint src/pages/MapBuilderPage.tsx src/components/builder/BuilderMap.tsx</automated>
    <human-check>Visual: small scale bar appears near coord readout (or one-line justification written to VERIFICATION.md if skipped).</human-check>
  </verify>
  <done>Scale pane visible near coord readout OR justified-skip recorded in VERIFICATION.md.</done>
  <commit>feat(builder): add scale control to map header (OR skip with VERIFICATION.md note)</commit>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task C.5 — SP-18: Delete test map Smoke Check 2026-05-15</name>
  <files>.planning/phases/1045-builder-smoke-polish/VERIFICATION.md</files>
  <what-built>Operational housekeeping — no code commit.</what-built>
  <how-to-verify>
    Run one of:
    - `curl -X DELETE http://localhost:8001/api/maps/a00b7e96-95b7-48d6-a911-57b97b767ebc/ -H "Authorization: Bearer $ADMIN_TOKEN"`
    - OR `psql $DATABASE_URL -c "DELETE FROM maps WHERE id = 'a00b7e96-95b7-48d6-a911-57b97b767ebc';"`
    - OR delete via the GeoLens admin UI at /maps

    Then verify:
    - `curl -i http://localhost:8001/api/maps/a00b7e96-95b7-48d6-a911-57b97b767ebc/` returns 404

    Write to VERIFICATION.md: `SP-18: PASS — map a00b7e96-95b7-48d6-a911-57b97b767ebc deleted; GET returns 404.`
  </how-to-verify>
  <resume-signal>Type "approved" after confirming the GET returns 404.</resume-signal>
</task>

</tasks>

<verification>
- Frontend typecheck + lint clean
- Vitest passes for affected files
- Visual checks for polish items (hover, glyph, icon swap)
- DevTools network: 1 thumbnail PUT per layer-add
- Test map UUID returns 404
</verification>

<success_criteria>
- 5/5 tasks complete (4 code + 1 operational)
- All C-plan must_haves true
- /gsd-code-review --fix run with findings addressed inline
- SP-12 either landed or justified-skip recorded
</success_criteria>

<output>
Create `.planning/phases/1045-builder-smoke-polish/1045-03-SUMMARY.md` when done.

## Phase CTRL-01 batch gate (runs after Plan C)

<task type="checkpoint:human-verify" gate="blocking">
  <name>CTRL-01 — 18/18 SP items resolved via Playwright smoke re-run</name>
  <what-built>All three plans (A/B/C) committed and pushed. CTRL-01 verifies the 18-step Playwright smoke check from `.planning/quick/260515-cej-docker-rebuild-builder-smoke/FINDINGS.md` "Reproduction artifacts" table now passes 18/18.</what-built>
  <how-to-verify>
    Run a Playwright re-check against http://localhost:8080 covering the 18 reproduction steps. For each SP item, assert:

    SP-01: Multi-select 2 layers; Group / Ungroup / Delete reachable via overflow menu inside the 340px sidebar at 1440×900. → PASS
    SP-02: Pan map; lat/lng readout updates to match `map.getCenter()`. → PASS
    SP-03: Add vector + DEM to fresh map; both render at auto-fit zoom without reload. → PASS (closed by B-01) OR SKIPPED-with-rationale
    SP-04: Click row A, shift-click row B → range selection. → PASS
    SP-05: Add layer, open editor → no "Pending preview" banner. Change paint → banner appears. → PASS
    SP-06: Header shows ONE save-state element per state. → PASS
    SP-07: SearchPage reload; console errors = 0 (3 quicklook 404s gone). → PASS
    SP-08: Idle builder 3 min; `/api/admin/ai-status/` calls ≤ 2. → PASS
    SP-09: Trigger ≥3 concurrent 401s; exactly 1 `/auth/refresh/` POST. → PASS
    SP-10: `[aria-pressed]` present on every visibility toggle. → PASS
    SP-11: `curl -X POST http://localhost:8001/auth/login` returns 200/422 not 307. → PASS
    SP-12: Scale pane visible near coord readout OR SKIPPED-with-rationale. → PASS or SKIPPED
    SP-13: Basemap visibility slot is non-button glyph with tooltip. → PASS
    SP-14: Hover any layer row body → cursor:pointer + surface-2 background. → PASS
    SP-15: Multi-select 2 layers → open Settings → bar hidden → close Settings → bar back. → PASS
    SP-16: Add a layer; PUT /thumbnail/ count within 500ms = 1. → PASS
    SP-17: "+ Add data" button shows Lucide Plus icon. → PASS
    SP-18: GET /api/maps/a00b7e96-95b7-48d6-a911-57b97b767ebc/ → 404. → PASS

    Console errors during the run = 0 (the 3 quicklook 404s must be gone via SP-07).

    Then run focused vitest:
    - `cd frontend && npm test -- --run BulkActionBar LayerStyleEditor UnifiedStackPanel StackRow BuilderMap MapBuilderPage SearchPage client use-ai`

    Vitest must be 100% pass on affected files.

    Write `.planning/phases/1045-builder-smoke-polish/VERIFICATION.md` with one line per SP item:

    ```
    SP-01: PASS — BulkActionBar overflow menu surfaces Group/Ungroup/Delete inside sidebar
    SP-02: PASS — coord readout subscribes to move events; lat/lng tracks map center
    SP-03: PASS — B-01 fix closes M-02; vector + DEM render on first add
    ...
    SP-18: PASS — test map a00b7e96-… deleted; GET returns 404
    ```

    If any SP fails, escalate before approving.
  </how-to-verify>
  <resume-signal>Type "approved" once VERIFICATION.md shows 18/18 PASS (or SKIPPED-with-rationale for SP-03 / SP-12). Then proceed to tag v1009.1 and archive milestone.</resume-signal>
</task>
</output>
