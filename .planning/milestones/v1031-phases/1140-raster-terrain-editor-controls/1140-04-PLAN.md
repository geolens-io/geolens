---
phase: 1140-raster-terrain-editor-controls
plan: 04
type: execute
wave: 3
depends_on: ["1140-01", "1140-03"]
files_modified:
  - frontend/src/components/builder/layer-adapters/raster-adapter.ts
  - frontend/src/components/builder/map-sync.ts
  - frontend/src/components/builder/LayerStyleEditor/RasterEditor.tsx
  - frontend/src/components/builder/layer-adapters/__tests__/raster-adapter.test.ts
  - frontend/src/components/builder/LayerStyleEditor/__tests__/RasterEditor.test.tsx
  - frontend/src/i18n/locales/en/builder.json
  - frontend/src/i18n/locales/de/builder.json
  - frontend/src/i18n/locales/es/builder.json
  - frontend/src/i18n/locales/fr/builder.json
autonomous: true
requirements: [EDITOR-RASTER-COLORMAP]
tags: [raster, colormap, titiler, builder, editor]

must_haves:
  truths:
    - "A single-band raster layer (band_count === 1) shows a COLORMAP section with colormap + stretch selects"
    - "Multi-band rasters (band_count !== 1) show no colormap section"
    - "Selecting a colormap rebuilds the tile URL and MapLibre re-fetches tiles with the new colormap"
    - "Existing raster brightness/contrast/saturation/hue sliders + Reset remain unaffected"
  artifacts:
    - path: "frontend/src/components/builder/layer-adapters/raster-adapter.ts"
      provides: "buildColormapTileUrl(baseUrl, paint) helper"
      contains: "buildColormapTileUrl"
    - path: "frontend/src/components/builder/LayerStyleEditor/RasterEditor.tsx"
      provides: "COLORMAP section gated on band_count === 1, writing _colormap/_stretch"
      contains: "_colormap"
  key_links:
    - from: "_colormap/_stretch paint keys"
      to: "raster tile URL query params"
      via: "buildColormapTileUrl appends ?colormap_name=&stretch= ; syncRasterLayer URL-diff recreates source"
      pattern: "buildColormapTileUrl"
    - from: "layer.band_count === 1"
      to: "COLORMAP section visibility"
      via: "RasterEditor conditional render"
      pattern: "band_count"
---

<objective>
Add the frontend half of EDITOR-RASTER-COLORMAP: a COLORMAP section in `RasterEditor` (shown only for single-band rasters) with colormap + stretch selects, plus the `buildColormapTileUrl` helper that turns the `_colormap`/`_stretch` paint keys into a Titiler tile URL — letting the existing `syncRasterLayer` URL-diff tear down and recreate the source so MapLibre re-fetches tiles with the new colormap.

Purpose: Completes the colormap feature on top of Plan 01's backend params + `band_count`. The control writes builder-private `_colormap`/`_stretch` keys (never reaching MapLibre paint, RESEARCH Pitfall 6); the raster adapter builds the colormap URL; the existing tile-URL-diff logic in `syncRasterLayer` (map-sync.ts:628 `currentSourceSpec.tiles?.[0] !== desiredTileUrl`) is the re-render trigger — no new mechanism (RESEARCH Pattern 3).

Output: `buildColormapTileUrl` in raster-adapter.ts; the colormap URL applied in syncRasterLayer before the tile-URL diff; the COLORMAP section in RasterEditor; unit tests; en/de/es/fr i18n keys.

Sequenced Wave 3: depends on Plan 01 (band_count on MapLayerResponse type) and co-edits map-sync.ts with Plans 02/03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1140-raster-terrain-editor-controls/1140-RESEARCH.md
@.planning/phases/1140-raster-terrain-editor-controls/1140-UI-SPEC.md
@.planning/phases/1140-raster-terrain-editor-controls/1140-01-SUMMARY.md

<interfaces>
<!-- Source-of-truth contracts. Use directly. -->

frontend/src/components/builder/layer-adapters/raster-adapter.ts:
- `RASTER_OWNED_PAINT_PROPERTIES` (line 26) = ['raster-brightness-min','raster-contrast','raster-saturation','raster-hue-rotate']. Do NOT add _colormap/_stretch here (Pitfall 6 — they'd be passed to setPaintProperty and rejected).
- `RASTER_PAINT_DEFAULTS` (line 5). The adapter's addLayers builds the source with `tiles: ['${window.location.origin}${tileUrl}']` (line 84).

frontend/src/components/builder/map-sync.ts — syncRasterLayer (line 599):
- `adapterInput.tileUrl = token.tile_url` (line 605).
- `const desiredTileUrl = absolutizeTileUrl(token.tile_url)` (line 619).
- The re-render trigger (line 624-633): the source is torn down + recreated when `currentSourceSpec.tiles?.[0] !== desiredTileUrl` (among other diffs). To make a colormap change re-fetch tiles, the colormap query params MUST be part of `token.tile_url` / `desiredTileUrl` BEFORE this comparison.
- `adapterInput.paint` holds the `_colormap`/`_stretch` keys.

frontend/src/components/builder/LayerStyleEditor/RasterEditor.tsx — current (read it):
- Signature `RasterEditor({ layer, paint, onPaintProp, t }: BaseStyleEditorProps)`.
- `layer: MapLayerResponse` now has `band_count?: number | null` (added by Plan 01).
- `onPaintProp(key, value)` routes builder-private keys into paint (LayerStyleEditor handlePaintProp at line 340; underscore keys merge into paint).
- `coalesceFrame(key, fn)` from '@/lib/builder/raf-coalesce' debounces writes (line 12, used for sliders).
- Structure: APPEARANCE section (4 sliders) → Reset Collapsible. Append COLORMAP section AFTER APPEARANCE and BEFORE the Reset Collapsible (UI-SPEC A-08).

shadcn Select: `@/components/ui/select` — `<Select value={...} onValueChange={...}><SelectTrigger className="h-8 text-xs"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="...">...</SelectItem></SelectContent></Select>`. A disabled option is `<SelectItem value="..." disabled>...</SelectItem>` (shadcn forwards `disabled` to Radix; the item is non-selectable and does not fire onValueChange). Label pattern: left span `w-28 shrink-0 text-xs text-muted-foreground` (UI-SPEC).

buildColormapTileUrl contract (RESEARCH Pattern 3 / Code Examples):
```
export function buildColormapTileUrl(baseUrl: string, paint: Record<string, unknown>): string {
  const colormap = paint['_colormap']; const stretch = paint['_stretch'];
  if (!colormap || colormap === 'gray') return baseUrl;          // gray = Titiler single-band default → no param
  const params = new URLSearchParams();
  params.set('colormap_name', colormap as string);
  if (typeof stretch === 'string' && stretch !== 'minmax') params.set('stretch', stretch);
  return `${baseUrl}?${params.toString()}`;
}
```
The 8 colormaps (Titiler names, UI-SPEC + Plan 01 allowlist): gray, viridis, inferno, plasma, magma, ylorrd, bugn, terrain.
Stretch values: minmax (the ONLY active option this phase), percentile, stddev. Per the checker DESIGN DECISION (Option A): minmax is enabled; percentile + stddev render as DISABLED `<SelectItem disabled>` with a "coming soon" suffix so the roadmap is signaled but the control never silently no-ops. Stats-based percentile/stddev computation is the v1032 follow-up (RESEARCH Finding 6 / Assumption A5). buildColormapTileUrl still accepts a `_stretch` value defensively (only minmax can be selected from the UI, but the helper handles any value robustly).

i18n keys (UI-SPEC; all 4 locales must match — resources.test.ts). Under `style.raster`:
sectionColormap="COLORMAP", colormapLabel="Colormap", stretchLabel="Stretch",
stretchMinmax="Min/Max", stretchPercentile="Percentile (2–98%)", stretchStddev="Std Deviation",
stretchComingSoon="coming soon"  // NEW shared suffix; appended in JSX to the disabled percentile/stddev option labels (lowest-churn — one new key, not two rewritten labels),
colormapGray="Grayscale", colormapViridis="Viridis", colormapInferno="Inferno", colormapPlasma="Plasma",
colormapMagma="Magma", colormapYlorrd="Yellow-Red", colormapBugn="Blue-Green", colormapTerrain="Terrain".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildColormapTileUrl helper + apply it in syncRasterLayer before the tile-URL diff</name>
  <read_first>
    - frontend/src/components/builder/layer-adapters/raster-adapter.ts (RASTER_OWNED_PAINT_PROPERTIES line 26; addLayers source build line 84)
    - frontend/src/components/builder/map-sync.ts (syncRasterLayer line 599-644 — token.tile_url assignment line 605, desiredTileUrl line 619, the tile-URL-diff teardown line 624-637)
    - frontend/src/components/builder/layer-adapters/__tests__/raster-adapter.test.ts (existing test patterns)
    - .planning/phases/1140-raster-terrain-editor-controls/1140-RESEARCH.md (Pattern 3; Code Examples "Tile URL builder"; Pitfall 6)
  </read_first>
  <behavior>
    - buildColormapTileUrl(url, { _colormap: 'viridis' }) → `url?colormap_name=viridis`.
    - buildColormapTileUrl(url, { _colormap: 'viridis', _stretch: 'percentile' }) → contains both `colormap_name=viridis` and `stretch=percentile` (helper is robust to any _stretch value, even though the UI only emits minmax this phase).
    - buildColormapTileUrl(url, { _colormap: 'gray' }) → url unchanged (gray is the single-band default; no param).
    - buildColormapTileUrl(url, {}) and { _colormap: undefined } → url unchanged.
    - buildColormapTileUrl(url, { _colormap: 'viridis', _stretch: 'minmax' }) → `colormap_name=viridis` only (minmax is default; no stretch param).
    - In syncRasterLayer: when `_colormap='viridis'` is in paint, the source's tiles[0] reflects the colormap URL, so a change from no-colormap → viridis (or viridis → inferno) triggers the existing source teardown/recreate (re-fetch).
  </behavior>
  <action>
    1. In `raster-adapter.ts`, add and export `buildColormapTileUrl(baseUrl: string, paint: Record<string, unknown>): string` per the contract in <interfaces>. Keep `_colormap`/`_stretch` OUT of `RASTER_OWNED_PAINT_PROPERTIES` and `RASTER_PAINT_DEFAULTS` (Pitfall 6).
    2. In `map-sync.ts` syncRasterLayer, apply the colormap to the tile URL so it participates in the existing diff. At the top where `adapterInput.tileUrl = token.tile_url` is set (line 605), AND where `desiredTileUrl = absolutizeTileUrl(token.tile_url)` is computed (line 619), route the URL through `buildColormapTileUrl(token.tile_url, adapterInput.paint)` FIRST. Cleanest: compute `const colormapTileUrl = buildColormapTileUrl(token.tile_url, adapterInput.paint)` once, set `adapterInput.tileUrl = colormapTileUrl`, and use `absolutizeTileUrl(colormapTileUrl)` for `desiredTileUrl`. This guarantees: (a) the source teardown diff sees the colormap-bearing URL, (b) the rebuilt source's `tiles` array carries the colormap params. Import `buildColormapTileUrl` from the raster-adapter. Only apply for the non-hillshade raster path (the `useHillshade` branch must NOT get colormap params — DEM uses terrainrgb; gate on `!useHillshade`).
    3. Confirm `absolutizeTileUrl` handles a URL that already has a query string (it prefixes origin to a root-relative path; a `?...` suffix is preserved since it only checks `startsWith('http')`). If absolutizeTileUrl would mangle the query, adjust to preserve it. Add a test asserting absolutize+colormap composition yields a single well-formed `origin/path?colormap_name=...` URL.
    4. Add tests to raster-adapter.test.ts: a `describe('buildColormapTileUrl')` block covering every `<behavior>` bullet for the helper. Add a map-sync raster test (in the existing raster sync test file is acceptable, or here) asserting that a paint change adding `_colormap` produces a source whose tiles[0] contains `colormap_name=` (i.e., the recreate path fired).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/builder/layer-adapters/__tests__/raster-adapter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "buildColormapTileUrl" frontend/src/components/builder/layer-adapters/raster-adapter.ts` shows the exported function; `grep -n "buildColormapTileUrl" frontend/src/components/builder/map-sync.ts` shows the import + application in syncRasterLayer.
    - `_colormap`/`_stretch` are absent from `RASTER_OWNED_PAINT_PROPERTIES` and `RASTER_PAINT_DEFAULTS` (grep confirms tuple unchanged — Pitfall 6).
    - Every `<behavior>` bullet has a passing test; gray and empty paint return the base URL unchanged.
    - The colormap URL is applied only on the non-hillshade raster path (DEM/hillshade tile URL is NOT given colormap params) — asserted.
    - `npx tsc -b --noEmit` clean.
  </acceptance_criteria>
  <done>The `_colormap`/`_stretch` paint keys produce a Titiler colormap tile URL that flows through the existing syncRasterLayer URL-diff, recreating the source so MapLibre re-fetches colorized tiles; DEM rendering untouched.</done>
</task>

<task type="auto">
  <name>Task 2: COLORMAP section in RasterEditor (gated on band_count === 1) + i18n</name>
  <read_first>
    - frontend/src/components/builder/LayerStyleEditor/RasterEditor.tsx (full file — APPEARANCE sliders, Reset Collapsible, onPaintProp usage, coalesceFrame)
    - frontend/src/components/builder/LayerStyleEditor/types.ts (BaseStyleEditorProps — layer, paint, onPaintProp, t)
    - frontend/src/types/api.ts (MapLayerResponse with band_count, post-Plan-01)
    - frontend/src/components/ui/select.tsx (SelectItem — confirm `disabled` prop is forwarded to Radix)
    - frontend/src/components/builder/LayerStyleEditor/__tests__/RasterEditor.test.tsx (existing Tests 1-7 — brightness/contrast/saturation/hue/Reset; ALL must stay green)
    - frontend/src/i18n/locales/en/builder.json (style.raster namespace)
    - .planning/phases/1140-raster-terrain-editor-controls/1140-UI-SPEC.md (Component Inventory EDITOR-RASTER-COLORMAP; A-03 band_count===1 gate; A-08 placement above Reset)
  </read_first>
  <action>
    1. In `RasterEditor.tsx`, append a COLORMAP `<section className="border-b">` AFTER the APPEARANCE section and BEFORE the Reset Collapsible (UI-SPEC A-08). Gate the entire section on `layer.band_count === 1` (strict equality — null/undefined/3 → not rendered; RESEARCH Pitfall 5 / UI-SPEC A-03).
    2. Inside the section (same header markup `text-[10px] font-semibold uppercase tracking-[0.08em] text-muted-foreground mb-2`, header `t('style.raster.sectionColormap', { defaultValue: 'COLORMAP' })`), two control rows:
       - Colormap Select: left label span `w-28 shrink-0 text-xs text-muted-foreground` = `t('style.raster.colormapLabel')`; `<Select value={String(paint['_colormap'] ?? 'gray')} onValueChange={(v) => onPaintProp('_colormap', v)}>` with `SelectTrigger className="h-8 text-xs"`; 8 `SelectItem`s (value=Titiler name, label=i18n): gray/viridis/inferno/plasma/magma/ylorrd/bugn/terrain → labels colormapGray/Viridis/Inferno/Plasma/Magma/Ylorrd/Bugn/Terrain.
       - Stretch Select (DESIGN DECISION Option A — minmax is the only ACTIVE stretch this phase; percentile + stddev are signaled-but-disabled so they never silently no-op): label `t('style.raster.stretchLabel')`; `value={String(paint['_stretch'] ?? 'minmax')}`, onValueChange → onPaintProp('_stretch', v); 3 SelectItems —
         * `<SelectItem value="minmax">{t('style.raster.stretchMinmax')}</SelectItem>` — ENABLED (default + only functional).
         * `<SelectItem value="percentile" disabled>{`${t('style.raster.stretchPercentile')} (${t('style.raster.stretchComingSoon')})`}</SelectItem>` — DISABLED with "coming soon" suffix.
         * `<SelectItem value="stddev" disabled>{`${t('style.raster.stretchStddev')} (${t('style.raster.stretchComingSoon')})`}</SelectItem>` — DISABLED with "coming soon" suffix.
         The disabled items are non-selectable (Radix blocks selection) and therefore never fire onValueChange → no silent fallback to minmax behind the user's back. Do NOT add backend percentile/stddev computation (that is the v1032 follow-up).
       - Write through `onPaintProp` directly (no coalesceFrame needed — Selects fire once per change, not continuously like sliders).
    3. Add the 15 `style.raster.*` keys (sectionColormap, colormapLabel, stretchLabel, 3 stretch options, stretchComingSoon, 8 colormap option labels) in ALL FOUR locales. The NEW key `stretchComingSoon` MUST be present in en/de/es/fr (en="coming soon", de="bald verfügbar", es="próximamente", fr="bientôt disponible") so resources.test.ts parity holds. de/es/fr get real translations for the section header + labels; colormap proper names (Viridis, Inferno, Plasma, Magma, Terrain) stay as-is across locales (they are palette proper nouns); translate Grayscale/Yellow-Red/Blue-Green + the stretch labels. Key sets must be identical across locales (resources.test.ts).
    4. Add tests to RasterEditor.test.tsx: COLORMAP section renders when `band_count === 1`; absent when `band_count === 3`; absent when band_count null/undefined; changing the colormap Select fires `onPaintProp('_colormap', <value>)`; selecting minmax fires `onPaintProp('_stretch', 'minmax')`; the 8 colormap options + 3 stretch options are present; the percentile + stddev stretch options are rendered DISABLED and do NOT fire `onPaintProp` when interacted with (assert the item has the disabled state / no onPaintProp('_stretch','percentile') call). Existing Tests 1-7 (the 4 sliders + Reset) must remain green. (Note: there is no longer a "change stretch to percentile" success path — percentile is non-selectable; the stretch-change test asserts minmax selection only.)
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/builder/LayerStyleEditor/__tests__/RasterEditor.test.tsx src/i18n/resources.test.ts && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_colormap\|sectionColormap\|band_count" frontend/src/components/builder/LayerStyleEditor/RasterEditor.tsx` confirms the gated section.
    - COLORMAP section visible iff `layer.band_count === 1` (3 cases asserted: ===1 shows, ===3 hidden, null hidden).
    - colormap Select fires `onPaintProp('_colormap', ...)`; the minmax stretch option fires `onPaintProp('_stretch', 'minmax')` (asserted).
    - [DESIGN DECISION Option A] percentile + stddev stretch SelectItems render `disabled` (with the "coming soon" suffix) and do NOT fire `onPaintProp('_stretch', ...)` — asserted; no silent no-op path exists.
    - [BEHAVIOR PRESERVATION — success criterion 4] Existing RasterEditor Tests 1-7 (brightness, contrast, saturation, hue, Reset) remain green — no assertion removed or weakened.
    - i18n: all 15 `style.raster.*` keys (including the new `stretchComingSoon`) present in en/de/es/fr; resources.test.ts green; `npx tsc -b --noEmit` clean.
  </acceptance_criteria>
  <done>Single-band raster layers show a COLORMAP section with a colormap Select + a stretch Select where minmax is active and percentile/stddev are disabled with a "coming soon" affordance (no silent no-op); multi-band rasters show no section; existing raster controls untouched; 4-locale parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → tile URL → /raster-tiles (nginx) → api | The `_colormap`/`_stretch` paint keys become tile-URL query params sent to the backend. The backend (Plan 01) validates them against the allowlist; the frontend only emits curated values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1140-01 | Tampering | _colormap/_stretch → tile URL → Titiler | mitigate | Frontend emits only the 8 curated colormap names + the single active `minmax` stretch from fixed Select option lists (percentile/stddev are disabled). The authoritative validation is the Plan 01 backend allowlist (422 on out-of-set) — the frontend is defense-in-depth, NOT the trust boundary. (Cross-ref Plan 01 T-1140-01.) |
| T-1140-07 | Tampering | _colormap reaching MapLibre setPaintProperty | mitigate | Keys stay OUT of RASTER_OWNED_PAINT_PROPERTIES (Pitfall 6) — verified by grep in Task 1; they only mutate the tile URL, never a MapLibre paint property. |
| T-1140-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (shadcn Select is pre-existing). N/A. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/builder/layer-adapters/__tests__/raster-adapter.test.ts src/components/builder/LayerStyleEditor/__tests__/RasterEditor.test.tsx src/components/builder/__tests__/map-sync.raster.test.ts src/i18n/resources.test.ts` green.
- `cd frontend && npx tsc -b --noEmit` clean.
- Manual tile re-render verified at Phase 1143 Playwright MCP close-gate on the live builder (headless WebGL can't paint — VALIDATION.md Manual-Only).
</verification>

<success_criteria>
- COLORMAP section in RasterEditor gated on band_count === 1, colormap Select + stretch Select (minmax active; percentile/stddev disabled with a "coming soon" affordance — Option A) writing `_colormap`/`_stretch`.
- buildColormapTileUrl + syncRasterLayer URL-diff cause MapLibre to re-fetch tiles on colormap change.
- Existing raster controls unaffected; 4-locale parity. (Backend params/allowlist + band_count delivered by Plan 01.)
</success_criteria>

<output>
Create `.planning/phases/1140-raster-terrain-editor-controls/1140-04-SUMMARY.md` when done.
Note that minmax is the only active stretch in 1140; percentile/stddev ship as DISABLED Select options with a "coming soon" affordance (DESIGN DECISION Option A — honest, non-misleading UX, no silent no-op) — restate the v1032 follow-up to add stats-based percentile/stddev (backend computation + un-disabling the options).
</output>
</content>
