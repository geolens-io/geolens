---
phase: 1143-quality-sweep-playwright-close-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/openapi.json
  - sdks/python/geolens
  - sdks/typescript/src
  - CHANGELOG.md
autonomous: true
requirements: [QA-02, QA-03]
tags: [close-gate, openapi, sdk, changelog, gates]

must_haves:
  truths:
    - "OpenAPI snapshot + Python/TS SDKs regenerated to include the 1140 raster colormap params + band_count and the 1142 og-image routes + og_image_url"
    - "make openapi-check and make sdks-check pass (no drift) after regeneration"
    - "Frontend typecheck + lint + vitest + i18n parity all green; backend pytest on touched modules green"
    - "CHANGELOG has a v1031 entry summarizing the four render-mode controls + OG social cards + SharePanel typography"
  artifacts:
    - path: "backend/openapi.json"
      provides: "regenerated OpenAPI snapshot with og-image routes + band_count + colormap params"
      contains: "og-image"
    - path: "CHANGELOG.md"
      provides: "v1031 changelog entry"
      contains: "1031"
---

# Plan 1143-01 — Quality Gates + OpenAPI/SDK Refresh + CHANGELOG (QA-02, QA-03)

The automatable half of the v1031 close-gate. (QA-01 live Playwright MCP smoke is run separately by the orchestrator — NOT in this plan, since GSD executors lack `mcp__playwright__*`.)

<threat_model>
No new attack surface — this plan regenerates artifacts, runs gates, and edits CHANGELOG. The regenerated SDKs/OpenAPI reflect already-reviewed routes (1140/1142). Threats N/A. The one care: do not hand-edit generated SDK files (regenerate via `make sdks`), and do not leak secrets into CHANGELOG.
</threat_model>

<task id="1" type="execute">
<objective>QA-03a: regenerate OpenAPI snapshot + Python/TS SDKs for the 1140 + 1142 backend changes.</objective>
<read_first>
- Makefile (the `openapi`, `openapi-check`, `sdks`, `sdks-check` targets — run them as written)
- .planning/phases/1143-quality-sweep-playwright-close-gate/1143-CONTEXT.md (what backend changed: 1140 raster colormap_name/stretch params + band_count on MapLayerResponse; 1142 PUT/GET /maps/{id}/og-image/ + MapResponse.og_image_url; /card is include_in_schema=False)
</read_first>
<action>
Run `make openapi` (regenerates `backend/openapi.json`), then `make sdks` (regenerates `sdks/python/geolens` + `sdks/typescript/src` from the flattened openapi). Then run `make openapi-check` and `make sdks-check` to confirm no remaining drift. Stage and commit the regenerated `backend/openapi.json` + `sdks/python/**` + `sdks/typescript/**` in ONE commit `chore(1143-01): regenerate OpenAPI + SDKs for v1031 (og-image routes, band_count, raster colormap params)`. Do NOT hand-edit generated files. If `make sdks` requires network (uvx/openapi-python-client) and it is unavailable, capture the error and report — do not fake the artifacts.
</action>
<acceptance_criteria>
- `backend/openapi.json` diff includes the og-image paths and `og_image_url` / `band_count` / `colormap_name` additions.
- `make openapi-check` exits 0 (no drift) AND `make sdks-check` exits 0 (no drift) after regeneration.
- Regenerated artifacts committed in one commit; no generated file hand-edited.
</acceptance_criteria>
<verify><automated>make openapi-check && make sdks-check</automated></verify>
<done>OpenAPI + SDKs regenerated, drift-free, committed.</done>
</task>

<task id="2" type="execute">
<objective>QA-02: run all touched-surface quality gates and capture concise pass/fail evidence.</objective>
<read_first>
- frontend/package.json (scripts: typecheck, lint, test, test:i18n)
- .planning/STATE.md (note: v1011 documented some PRE-EXISTING e2e flakes — distinguish them from v1031 regressions)
- repo-root package.json + any e2e/ directory (locate the builder e2e smoke script — it is NOT in frontend/package.json)
</read_first>
<action>
Run, capturing concise results (tail output): (a) `cd frontend && npm run typecheck`; (b) `cd frontend && npm run lint`; (c) `cd frontend && npm run test` (full vitest — report totals); (d) backend `cd backend && set -a && source ../.env.test && set +a && uv run pytest -n 4` on the touched modules (`backend/tests/test_raster_colormap_proxy.py backend/tests/test_maps_og_image.py` at minimum; run broader if quick); (e) locate + run the builder e2e smoke (check repo-root `package.json` scripts or `e2e/`; if it exists run it, else report "e2e smoke script not located in this workspace" — do NOT block); (f) `cd frontend && npm run test:i18n`. For each gate, record PASS/FAIL + counts in the SUMMARY. Any failure that reproduces on a pre-v1031 commit is PRE-EXISTING (note it); a NEW failure introduced by v1031 is a regression to fix before close.
</action>
<acceptance_criteria>
- typecheck: 0 errors. lint: 0 new errors. frontend vitest: all pass (report N/N). backend pytest (touched modules): all pass. i18n parity: pass. e2e smoke: pass OR documented-not-located/pre-existing-flake with rationale.
- Any NEW v1031 regression is fixed (or explicitly escalated); pre-existing failures are labeled as such.
</acceptance_criteria>
<verify><automated>cd frontend && npm run typecheck && npm run test:i18n</automated></verify>
<done>All v1031 gates green (or pre-existing failures documented); results captured in SUMMARY.</done>
</task>

<task id="3" type="execute">
<objective>QA-03b: write the v1031 CHANGELOG entry.</objective>
<read_first>
- CHANGELOG.md (the `## [Unreleased]` section at the top + the existing entry format/style)
- .planning/phases/1140-*/1140-01-SUMMARY.md … 1142-*/1142-02-SUMMARY.md (what shipped — for accurate, measured changelog bullets)
</read_first>
<action>
Add a v1031 entry to CHANGELOG.md (under/replacing `## [Unreleased]` per the repo's convention) summarizing: raster & terrain editor controls (DEM contour overlay, hypsometric tint ramps, single-band raster colormap), the fill-pattern editor control, OG-image/social-card meta on shared map links (+1200×630 capture), and the SharePanel typography cleanup. Match the existing CHANGELOG section style (Added/Changed/Fixed groupings). Keep bullets factual and user-facing. Commit `docs(1143-01): CHANGELOG v1031 entry`.
</action>
<acceptance_criteria>
- CHANGELOG.md contains a v1031 (or dated Unreleased→v1031) section referencing the render-mode controls, fill-pattern, OG social cards, and SharePanel typography.
- Section matches existing CHANGELOG formatting; committed.
</acceptance_criteria>
<verify><automated>grep -n "1031\|contour\|hypsometric\|colormap\|fill-pattern\|og:image\|social card" CHANGELOG.md | head</automated></verify>
<done>v1031 CHANGELOG entry written + committed.</done>
</task>

<verification>
After all tasks: `make openapi-check && make sdks-check` pass; `cd frontend && npm run typecheck && npm run test:i18n` pass; CHANGELOG has the v1031 entry. Capture the full gate results (QA-02) in the SUMMARY for the orchestrator's close-gate verification.
</verification>
