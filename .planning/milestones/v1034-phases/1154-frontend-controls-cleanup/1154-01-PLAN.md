---
phase: 1154-frontend-controls-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/builder/layer-adapters/raster-adapter.ts
  - frontend/src/components/builder/layer-adapters/__tests__/raster-adapter.test.ts
autonomous: true
requirements:
  - RASTER-STRETCH-UI-01
must_haves:
  truths:
    - "buildColormapTileUrl forwards pmin/pmax only for a non-default percentile stretch"
    - "buildColormapTileUrl forwards sigma only for a non-default stddev stretch"
    - "buildColormapTileUrl is byte-identical to today's output at all defaults (no _pmin/_pmax/_sigma, or values at 2/98/2)"
  artifacts:
    - path: "frontend/src/components/builder/layer-adapters/raster-adapter.ts"
      provides: "buildColormapTileUrl pmin/pmax/sigma non-default forwarding"
      contains: "_pmin"
    - path: "frontend/src/components/builder/layer-adapters/__tests__/raster-adapter.test.ts"
      provides: "unit coverage for non-default forwarding + default-unchanged invariant"
      contains: "pmin"
  key_links:
    - from: "frontend/src/components/builder/map-sync.ts:648"
      to: "buildColormapTileUrl"
      via: "tile URL construction in syncRasterLayer"
      pattern: "buildColormapTileUrl"
---

<objective>
Extend `buildColormapTileUrl` in `raster-adapter.ts` to forward the new
builder-private stretch-bound paint keys `_pmin`, `_pmax`, and `_sigma` to the
raster tile URL — strictly under the established "only forward non-default"
discipline so the default-case URL is byte-identical to today.

Purpose: This is the URL contract that Plan 02's RasterEditor controls write
against. Locking it first (with full unit coverage) means Plan 02's UI executor
receives a fixed, tested contract rather than discovering it.

Output: Updated `buildColormapTileUrl` + expanded unit tests proving the
forwarding rules and the default-unchanged invariant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1154-frontend-controls-cleanup/1154-CONTEXT.md

<interfaces>
<!-- The current contract the executor extends. Defaults: percentile=2/98, stddev sigma=2. -->

From frontend/src/components/builder/layer-adapters/raster-adapter.ts:
```typescript
export function buildColormapTileUrl(
  baseUrl: string,
  paint: Record<string, unknown>,
): string
// Reads paint['_colormap'] (forwards colormap_name when non-gray),
// paint['_stretch'] (forwards stretch when !== 'minmax').
// Uses URLSearchParams; returns baseUrl unmodified when no params set.
```

Sole caller — frontend/src/components/builder/map-sync.ts:648:
```typescript
: buildColormapTileUrl(token.tile_url, adapterInput.paint);
```
Param ORDER in the URL is asserted by existing tests via URLSearchParams insertion order
(colormap_name first, then stretch). Append pmin/pmax/sigma AFTER stretch to keep
existing assertions stable.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Forward _pmin/_pmax/_sigma from buildColormapTileUrl (non-default only)</name>
  <files>frontend/src/components/builder/layer-adapters/raster-adapter.ts</files>
  <read_first>
    - frontend/src/components/builder/layer-adapters/raster-adapter.ts (lines 33-73 — the buildColormapTileUrl function + its doc comment)
    - .planning/phases/1154-frontend-controls-cleanup/1154-CONTEXT.md (RASTER-STRETCH-UI-01 forwarding rules, lines 23-26)
  </read_first>
  <behavior>
    - percentile, non-default: paint={_stretch:'percentile', _pmin:5, _pmax:95} → URL contains `stretch=percentile`, `pmin=5`, `pmax=95`
    - percentile, default: paint={_stretch:'percentile', _pmin:2, _pmax:98} → URL contains `stretch=percentile` but NO `pmin`/`pmax`
    - percentile, partial non-default (only pmin moved): paint={_stretch:'percentile', _pmin:5} → URL contains `pmin=5`; pmax omitted (still default 98)
    - stddev, non-default: paint={_stretch:'stddev', _sigma:3} → URL contains `stretch=stddev`, `sigma=3`
    - stddev, default: paint={_stretch:'stddev', _sigma:2} → URL contains `stretch=stddev` but NO `sigma`
    - cross-mode isolation: pmin/pmax are NEVER forwarded when _stretch !== 'percentile'; sigma NEVER forwarded when _stretch !== 'stddev' (even if the keys are present in paint)
    - default invariant: buildColormapTileUrl(BASE, {_colormap:'viridis', _stretch:'percentile'}) === today's output (no bound keys present → no pmin/pmax)
  </behavior>
  <action>
    In `buildColormapTileUrl`, AFTER the existing `stretch` param block (line ~68-70),
    add bound-param forwarding. Read `_pmin`, `_pmax`, `_sigma` from `paint`.

    Define the defaults inline as named constants at module scope (or local consts):
    `STRETCH_PMIN_DEFAULT = 2`, `STRETCH_PMAX_DEFAULT = 98`, `STRETCH_SIGMA_DEFAULT = 2`.

    Percentile branch — only when `stretch === 'percentile'`:
    - Coerce `_pmin`/`_pmax` to number; if it is a finite number AND differs from its
      default, `params.set('pmin', String(value))` / `params.set('pmax', String(value))`.
      Forward each independently (one may be non-default while the other is default).
    Stddev branch — only when `stretch === 'stddev'`:
    - Coerce `_sigma` to number; if finite AND `!== STRETCH_SIGMA_DEFAULT`,
      `params.set('sigma', String(value))`.

    Do NOT clamp/validate range here — client validation lives in Plan 02's RasterEditor;
    this builder forwards whatever non-default numeric value it receives (backend 422s on
    out-of-range as defense in depth). Append AFTER the stretch param so existing
    URLSearchParams insertion-order assertions stay valid.

    Update the function doc comment to document the three new params and the non-default rule.
    Do NOT add `_pmin`/`_pmax`/`_sigma` to `RASTER_OWNED_PAINT_PROPERTIES` — they are
    builder-private URL keys (Pitfall 6), same as `_colormap`/`_stretch`.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` (via `npm run typecheck`) exits 0
    - New vitest cases in raster-adapter.test.ts assert each behavior listed above
    - Existing buildColormapTileUrl tests (colormap_name ordering, gray default, minmax) still pass unchanged
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- src/components/builder/layer-adapters/__tests__/raster-adapter.test.ts --run</automated>
  </verify>
  <done>buildColormapTileUrl forwards pmin/pmax for non-default percentile, sigma for non-default stddev, omits all bound params at defaults, and is byte-identical to prior output when no bound keys are set.</done>
</task>

<task type="auto">
  <name>Task 2: Add raster-adapter unit tests for bound forwarding + default invariant</name>
  <files>frontend/src/components/builder/layer-adapters/__tests__/raster-adapter.test.ts</files>
  <read_first>
    - frontend/src/components/builder/layer-adapters/__tests__/raster-adapter.test.ts (lines 146-220 — the existing `describe('buildColormapTileUrl', ...)` block)
  </read_first>
  <action>
    Inside the existing `describe('buildColormapTileUrl', ...)` block, add cases covering
    the Task 1 behavior list. Use the existing `BASE` constant and `await import('../raster-adapter')`
    pattern already established in the file. Explicit cases (minimum):
    - 'forwards pmin/pmax for non-default percentile' → expects `pmin=5` and `pmax=95`
    - 'omits pmin/pmax for default percentile (2/98)' → URL has `stretch=percentile`, no `pmin`/`pmax`
    - 'forwards only pmin when pmax left at default' → `pmin=5` present, `pmax` absent
    - 'forwards sigma for non-default stddev' → expects `sigma=3`
    - 'omits sigma for default stddev (2)' → URL has `stretch=stddev`, no `sigma`
    - 'never forwards pmin/pmax when stretch is not percentile' → paint={_stretch:'stddev', _pmin:5, _pmax:95} has no `pmin`/`pmax`
    - 'never forwards sigma when stretch is not stddev' → paint={_stretch:'percentile', _sigma:3} has no `sigma`
    - 'default percentile URL is byte-identical to today' → assert exact string equality with the
      no-bound-keys percentile output
    Assert on the returned URL string (substring `.toContain` for presence; `.not.toContain` for absence).
  </action>
  <acceptance_criteria>
    - `npm run test -- src/components/builder/layer-adapters/__tests__/raster-adapter.test.ts --run` passes with the new cases green
    - At least one case asserts the default-unchanged invariant via exact-string equality
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- src/components/builder/layer-adapters/__tests__/raster-adapter.test.ts --run</automated>
  </verify>
  <done>raster-adapter.test.ts contains passing assertions for non-default forwarding (pmin/pmax/sigma), cross-mode isolation, and the byte-identical-at-default invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RasterEditor UI → tile URL → raster-proxy/Titiler | user-entered percentile/sigma values cross into a server-side query param |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1154-01 | Tampering | pmin/pmax/sigma query params on raster tile URL | mitigate | Plan 02 client-validates `0<=pmin<pmax<=100`; backend (phase 1153) 422s on out-of-range before Titiler — defense in depth. This plan's builder forwards only finite numbers. |
| T-1154-02 | Denial of Service | extreme/garbage param values | accept | Backend 422 rejects out-of-range; values are bounded numerics, no unbounded payload; low-value read endpoint. |
| T-1154-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (pure source edit + tests). |
</threat_model>

<verification>
- `cd frontend && npm run typecheck` exits 0
- `cd frontend && npm run test -- src/components/builder/layer-adapters/__tests__/raster-adapter.test.ts --run` green
- No change to `RASTER_OWNED_PAINT_PROPERTIES` (Pitfall 6 — builder-private keys stay out)
</verification>

<success_criteria>
- buildColormapTileUrl forwards pmin/pmax for non-default percentile, sigma for non-default stddev
- Default-case URL byte-identical to prior behavior (proven by exact-string test)
- All existing raster-adapter tests still pass
</success_criteria>

<output>
Create `.planning/phases/1154-frontend-controls-cleanup/1154-01-SUMMARY.md` when done.
</output>
