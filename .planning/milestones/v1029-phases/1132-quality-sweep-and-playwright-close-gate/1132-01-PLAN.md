# Plan 1132-01: Quality Sweep and Playwright Close Gate

**Status:** Complete
**Requirements:** QA-01, QA-02, QA-03, QA-04

## Tasks

- [x] Run focused DCAT/export pytest coverage.
- [x] Run Ruff check and format-check for touched backend standards/export/test files.
- [x] Run OpenAPI and SDK drift checks from a clean worktree.
- [x] Start a runtime API surface with the committed DCAT-US dependency set.
- [x] Use Playwright MCP to verify DCAT-US catalog export, catalog validation, per-dataset export, and per-dataset validation.
- [x] Record runtime network and console hygiene.

## Verification

- `cd backend && uv run ruff check app/standards/dcat_us app/modules/catalog/datasets/api/router_export.py tests/test_dcat.py` -> passed.
- `cd backend && uv run ruff format --check app/standards/dcat_us app/modules/catalog/datasets/api/router_export.py tests/test_dcat.py` -> 6 files already formatted.
- `cd backend && env $(grep -v '^#' ../.env.test.example | grep -v '^$' | xargs) uv run pytest tests/test_dcat.py tests/test_datasets.py::TestDatasetSubRouterRouting::test_dcat_catalog_not_captured_by_dataset_id -q` -> 22 passed.
- Clean worktree: `make openapi-check && make sdks-check` -> passed.
- Playwright MCP: four DCAT-US endpoint requests returned 200 OK.
