---
phase: 1075-conftest-test-db-lifecycle-refactor-baseline-fixes
plan: 05
type: execute
wave: 3
depends_on:
  - 1075-01
  - 1075-02
  - 1075-03
  - 1075-04
files_modified:
  - .planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-05-VERIFICATION.md
autonomous: true
requirements:
  - TI-01
  - TI-02
tags:
  - verification
  - close-out
  - phase-gate

must_haves:
  truths:
    - "Running `uv run pytest backend/tests/ -x` exits 0 (or has documented skips only — no unannotated failures, no `InvalidCatalogNameError` errors)"
    - "Running `uv run pytest backend/tests/ -n auto` exits 0 (same skip discipline, full parallel-safe)"
    - "Zero `asyncpg.exceptions.InvalidCatalogNameError` errors in either run"
    - "Pre-Plan-01 baseline (1636/1647 PASS + 1363 errors) is replaced with post-1075 baseline documented at the phase level"
    - "Pre/post counts captured in 1075-05-VERIFICATION.md for downstream phase comparison (1076, 1077, 1079)"
    - "All 11 v1015 baseline failures (3+3+5) are dispositioned by name with a per-test entry (PASSED or SKIPPED-with-issue)"
    - "Skip count + issue links inventory exists and is auditable"
  artifacts:
    - path: ".planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-05-VERIFICATION.md"
      provides: "Phase-level pre/post verification record — closing TI-01 and TI-02"
      min_lines: 80
  key_links:
    - from: "1075-05-VERIFICATION.md"
      to: "1075-01/02/03/04 SUMMARYs"
      via: "result aggregation"
      pattern: "1075-0[1234]-SUMMARY"
---

<objective>
Run the full backend pytest suite under both sequential (`-x`) and parallel (`-n auto`) modes. Verify the TI-01 (zero `InvalidCatalogNameError`) and TI-02 (11 baseline failures dispositioned) success criteria are met. Capture pre/post counts in a verification record so downstream phases (1076, 1077, 1079) can compare against this baseline.

Purpose: Phase 1075's two requirements are joint gates on every downstream phase's test signal. The plans 01-04 do the work; this plan PROVES the work is complete and records the closing state. Without this verification, "TI-01 done" and "TI-02 done" are claims without evidence.

Output:
- A new `.planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-05-VERIFICATION.md` document with:
  - Pre-1075 baseline (from v1016 Phase 1074 audit: 1636/1647 PASS, 11 FAILED, 1363 errors)
  - Post-1075 baseline (run NOW): exact PASSED / SKIPPED / FAILED / ERROR counts
  - Per-skip-test inventory: test_name → issue URL or todo-filepath
  - Per-file disposition summary (test_defer_orphan_guard.py, test_ingest.py, test_maps_style_json.py)
  - Pre/post `grep -c "InvalidCatalogNameError"` count (target: 1363 → 0)
- A git commit grouping the phase's work
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-CONTEXT.md
@.planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-01-SUMMARY.md
@.planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-02-SUMMARY.md
@.planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-03-SUMMARY.md
@.planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-04-SUMMARY.md

<interfaces>
<!-- Pre-1075 baseline (from .planning/milestones/v1016-MILESTONE-AUDIT.md): -->

Tests run via `uv run pytest` in `backend/`:
- PASSED: 1636 (of 1647 expected)
- FAILED: 11 (test_defer_orphan_guard.py ×3, test_ingest.py ×3, test_maps_style_json.py ×5)
- ERROR: 1363 (`asyncpg.exceptions.InvalidCatalogNameError`)

TI-01 target: 0 errors (all 1363 InvalidCatalogNameError eliminated)
TI-02 target: 0 failed (all 11 fixed OR skipped-with-issue; SKIPPED is allowed)

Acceptance for phase close-out:
- PASSED count: <= 1647 (some tests may now be SKIPPED)
- FAILED count: 0
- ERROR count: 0
- SKIPPED count: 0-11 (with documented issues)
- Net: 1647 = PASSED + SKIPPED (every test accounted for)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full backend pytest suite (sequential) + parallel + collect raw counts</name>
  <read_first>
    - .planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-01-SUMMARY.md (Plan 01's notes)
    - .planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-02-SUMMARY.md
    - .planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-03-SUMMARY.md
    - .planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-04-SUMMARY.md
    - backend/tests/conftest.py (the refactored version from Plan 01)
  </read_first>
  <action>
    **Step 1: Sequential run.**
    ```
    cd backend && uv run pytest tests/ -v --tb=short 2>&1 | tee /tmp/1075_full_seq.log
    ```
    This will take several minutes. Do NOT use `-x` — we want every test outcome, even after the first failure, for accurate accounting.

    **Step 2: Parallel run.**
    ```
    cd backend && uv run pytest tests/ -v --tb=short -n auto 2>&1 | tee /tmp/1075_full_par.log
    ```

    **Step 3: Tally per-log:**
    For each log, extract:
    - `grep -c "^PASSED" <log>` → PASSED count
    - `grep -c "^FAILED" <log>` → FAILED count (must be 0)
    - `grep -c "^ERROR" <log>` → ERROR count (must be 0)
    - `grep -c "^SKIPPED" <log>` → SKIPPED count (matches sum of skips across Plans 02-04)
    - `grep -c "InvalidCatalogNameError" <log>` → must be 0
    - `grep -E "^=+ .*passed.*failed.* in " <log> | tail -1` → pytest's own summary line

    **Step 4:** Cross-check counts between the two runs. They must agree on PASSED, SKIPPED, FAILED, ERROR. (Different runs of the same suite shouldn't produce different outcomes — if they do, that's a parallelism bug.) If counts diverge, flag as a phase-level concern and document.

    Write a scratch file `/tmp/1075-05-counts.md`:
    ```
    # Full pytest run counts

    ## Sequential (`pytest -x` style without -x for full coverage)
    PASSED:  <N>
    FAILED:  <must be 0>
    ERROR:   <must be 0>
    SKIPPED: <M>
    InvalidCatalogNameError: <must be 0>
    pytest summary line: <copy from log>

    ## Parallel (`pytest -n auto`)
    PASSED:  <N>
    FAILED:  <must be 0>
    ERROR:   <must be 0>
    SKIPPED: <M>
    InvalidCatalogNameError: <must be 0>
    pytest summary line: <copy from log>

    ## Cross-check
    Sequential PASSED + SKIPPED: <N + M>
    Parallel PASSED + SKIPPED: <N + M>
    Match: <YES / NO + details>
    ```
  </action>
  <acceptance_criteria>
    - `/tmp/1075_full_seq.log` exists, > 100 KB (full run produces large output)
    - `/tmp/1075_full_par.log` exists, > 100 KB
    - `/tmp/1075-05-counts.md` exists with the counts table filled in
    - `grep -c "InvalidCatalogNameError" /tmp/1075_full_seq.log` returns 0
    - `grep -c "InvalidCatalogNameError" /tmp/1075_full_par.log` returns 0
    - `grep -c "^FAILED" /tmp/1075_full_seq.log` returns 0
    - `grep -c "^FAILED" /tmp/1075_full_par.log` returns 0
    - PASSED + SKIPPED count matches between sequential and parallel runs (within 0 — must be EXACTLY equal)
  </acceptance_criteria>
  <verify>
    <automated>echo "Sequential errors:" && grep -c "InvalidCatalogNameError" /tmp/1075_full_seq.log && echo "Parallel errors:" && grep -c "InvalidCatalogNameError" /tmp/1075_full_par.log && echo "Sequential failures:" && grep -c "^FAILED" /tmp/1075_full_seq.log && echo "Parallel failures:" && grep -c "^FAILED" /tmp/1075_full_par.log</automated>
  </verify>
  <done>Both runs complete with zero failures and zero InvalidCatalogNameError errors; counts captured in /tmp/1075-05-counts.md.</done>
</task>

<task type="auto">
  <name>Task 2: Aggregate per-file dispositions + write 1075-05-VERIFICATION.md</name>
  <read_first>
    - /tmp/1075-05-counts.md
    - 1075-01-SUMMARY.md, 1075-02-SUMMARY.md, 1075-03-SUMMARY.md, 1075-04-SUMMARY.md (the prior plans' summaries)
    - /tmp/dog_final.log, /tmp/ingest_seq.log, /tmp/maps_seq.log (per-file Task 4 logs from Plans 02/03/04)
    - .planning/milestones/v1016-MILESTONE-AUDIT.md (for pre-Plan-01 baseline reference numbers)
  </read_first>
  <action>
    Create `.planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-05-VERIFICATION.md` with this structure:

    ```markdown
    # Phase 1075 Verification: Conftest Test-DB Lifecycle Refactor + Baseline Fixes

    **Phase:** 1075
    **Requirements closed:** TI-01, TI-02
    **Verified:** <YYYY-MM-DD>
    **Verifier:** Plan 1075-05

    ## Baseline Comparison

    | Metric | Pre-1075 (v1016 Phase 1074 audit) | Post-1075 (Plan 05 verification) | Delta |
    |--------|-----------------------------------|----------------------------------|-------|
    | PASSED | 1636 | <N> | <±> |
    | FAILED | 11 | 0 | -11 |
    | ERROR (InvalidCatalogNameError) | 1363 | 0 | -1363 |
    | SKIPPED | <unknown — included in audit's "11"> | <M> | (re-categorized from FAILED) |
    | TOTAL | 1647 | <N + M> | <0 if test count unchanged> |

    ## TI-01 Closure Evidence (Conftest test-DB lifecycle refactor)

    **Target:** Zero `asyncpg.exceptions.InvalidCatalogNameError` errors across the full backend/tests/ tree.

    **Verification:**
    - Sequential run (`pytest -x` without -x for full coverage): `grep -c "InvalidCatalogNameError" /tmp/1075_full_seq.log` = **0**
    - Parallel run (`pytest -n auto`): `grep -c "InvalidCatalogNameError" /tmp/1075_full_par.log` = **0**
    - Regression test `tests/test_conftest_lifecycle.py` (6 tests) all PASSED

    **Evidence files:**
    - /tmp/1075_full_seq.log (full sequential run)
    - /tmp/1075_full_par.log (full parallel run)
    - backend/tests/test_conftest_lifecycle.py (committed regression net)
    - backend/tests/conftest.py (the refactored fixture; see 1075-01-SUMMARY for details)

    **Implementation:** See 1075-01-SUMMARY.md

    ## TI-02 Closure Evidence (Fix 11 v1015 baseline failures)

    **Target:** All 11 baseline failures (`test_defer_orphan_guard.py` ×3, `test_ingest.py` ×3, `test_maps_style_json.py` ×5) dispositioned — either FIXED or SKIPPED-with-issue.

    ### test_defer_orphan_guard.py (3 failures)

    | Test | Pre-1075 | Post-1075 | Disposition |
    |------|----------|-----------|-------------|
    | <test_name_1> | FAILED | <PASSED / SKIPPED> | <fix description OR issue URL> |
    | <test_name_2> | FAILED | <PASSED / SKIPPED> | <...> |
    | <test_name_3> | FAILED | <PASSED / SKIPPED> | <...> |

    Source: 1075-02-SUMMARY.md

    ### test_ingest.py (3 failures)

    | Test | Pre-1075 | Post-1075 | Disposition |
    |------|----------|-----------|-------------|
    | test_upload_success | FAILED | <...> | <...> |
    | test_csv_upload_success | FAILED | <...> | <...> |
    | test_service_job_commits_with_service_body | FAILED | <...> | <...> |

    Source: 1075-03-SUMMARY.md

    ### test_maps_style_json.py (5 failures)

    | Test | Pre-1075 | Post-1075 | Disposition |
    |------|----------|-----------|-------------|
    | <test_name_1> | FAILED | <...> | <...> |
    | ... 5 rows ... | | | |

    Source: 1075-04-SUMMARY.md

    ### Skip-with-issue inventory

    All SKIPPED tests have a `pytest.mark.skip(reason=...)` annotation pointing to a tracked artifact:

    | Test | Skip Reason | Tracking Artifact |
    |------|-------------|-------------------|
    | <test_name> | <one-line summary> | <github issue URL OR .planning/todos/pending/...md filepath> |
    | ... | | |

    Count: <M> SKIPPED total across the 3 files.

    ## Phase-Level Health Check

    - [x] TI-01: Zero InvalidCatalogNameError errors (down from 1363) — verified in /tmp/1075_full_seq.log + /tmp/1075_full_par.log
    - [x] TI-02: All 11 v1015 baseline failures dispositioned — every named test is PASSED or SKIPPED with documented rationale
    - [x] Parallel run cleanly: pytest -n auto exits 0 — proves worker isolation via Plan 01's conftest refactor
    - [x] No new failures introduced: PASSED count >= 1636 (pre-baseline)
    - [x] Every SKIPPED test has a tracked artifact (issue or todo file)

    ## Downstream Phase Readiness

    Phase 1075 produces a clean pytest baseline that downstream phases (1076, 1077, 1079) consume:

    - **Phase 1076 (Backend Ingest P2):** Will add a regression test for the metadata.py P2-02 internal-commit-subversion contract. That test runs against the per-worker DB established in Plan 1075-01.
    - **Phase 1077 (Frontend Ingest P2):** Frontend-only; doesn't consume backend pytest baseline directly but trusts the green signal from Phase 1075.
    - **Phase 1079 (Close Gate + Hygiene):** Will capture the TI-03 post-v1017 pytest baseline doc at `.planning/audits/PYTEST-BASELINE-2026-05-21.md`. This Plan 05 doc is the precursor — TI-03's doc compares the post-v1017 close-gate run against the Phase 1075 post-fix baseline.

    ## Decision Log

    <Document any deviation from the plan: e.g., "Plan 04 found that 4 of 5 failures shared the _layer Pydantic schema drift; 1 required a per-test fix because it exercised a v1011 paint._height_column field that wasn't in the helper.">

    ## Sign-off

    Phase 1075 closes when:
    - [x] This VERIFICATION.md exists and is complete
    - [x] All 5 plans committed (1075-01 through 1075-05)
    - [x] STATE.md updated by Plan 05 to mark Phase 1075 complete
    - [x] ROADMAP.md plan checkboxes ticked for all 5 plans
    ```

    Fill in every `<placeholder>` from the count files and the prior plan SUMMARYs. Reuse the per-file Task 4 logs from Plans 02/03/04 to confirm per-test dispositions match.
  </action>
  <acceptance_criteria>
    - `.planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-05-VERIFICATION.md` exists
    - File is at least 80 lines
    - Every `<placeholder>` is filled with concrete data (no `TBD`, no `<value>`)
    - The Baseline Comparison table has post-1075 numbers (PASSED, FAILED=0, ERROR=0, SKIPPED, TOTAL)
    - The Skip-with-issue inventory table has one row per SKIPPED test, each with a non-empty Tracking Artifact column
    - The Decision Log section has at least 2-3 sentences documenting actual decisions made
    - `grep -c "TBD\|<value>\|<placeholder>" .planning/phases/1075-*/1075-05-VERIFICATION.md` returns 0
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-05-VERIFICATION.md && wc -l .planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-05-VERIFICATION.md | awk '{print $1}' | head -1 && grep -c "TBD\|<value>\|<placeholder>" .planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-05-VERIFICATION.md</automated>
  </verify>
  <done>1075-05-VERIFICATION.md exists, complete, no placeholders; phase-level closure evidence is auditable.</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md + ROADMAP.md to reflect Phase 1075 completion</name>
  <read_first>
    - .planning/STATE.md (current state — verify current position, deferred items)
    - .planning/ROADMAP.md (verify current Phase 1075 checkbox + Plans list)
    - .planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-05-VERIFICATION.md (the verification record from Task 2)
  </read_first>
  <action>
    **Step 1:** Update `.planning/STATE.md`:
    - Change `Phase: 1075 (next — not started)` to `Phase: 1075 (complete) → 1076/1077/1078 (next, parallel-eligible)`
    - Update `progress.completed_phases` from 0 to 1
    - Update `progress.completed_plans` to 5 (the 5 plans of Phase 1075)
    - Update `progress.percent` to 20 (1/5 phases done)
    - Update `last_activity` to today's date
    - Append to `Decisions` section:
      ```
      - <YYYY-MM-DD> (v1017 Phase 1075): TI-01 conftest refactor adds pytest-xdist to dev deps and switches the per-test DB naming to incorporate PYTEST_XDIST_WORKER. Test parallelism via `-n auto` is now first-class supported and pinned by tests/test_conftest_lifecycle.py.
      - <YYYY-MM-DD> (v1017 Phase 1075): TI-02 dispositioned 11 baseline failures. <K> auto-resolved after Plan 01's conftest fix. <L> fixed at root cause via test-side updates. <M> skipped with GitHub issue links pinned to v1018 follow-up.
      ```
    - If any Plan 02/03/04 surfaced production-code drift requiring fixes, note that in Decisions (with file + rationale)

    **Step 2:** Update `.planning/ROADMAP.md` `Progress` table — change Phase 1075 row from `0/0 | Not started | -` to `5/5 | Complete | <YYYY-MM-DD>`.

    **Step 3:** Update `.planning/ROADMAP.md` v1017 Phase Details section — mark Phase 1075's plan list:
    ```
    Plans:
    - [x] 1075-01-PLAN.md — Conftest test-DB lifecycle refactor (TI-01)
    - [x] 1075-02-PLAN.md — Fix test_defer_orphan_guard.py 3 failures (TI-02 partial)
    - [x] 1075-03-PLAN.md — Fix test_ingest.py 3 failures (TI-02 partial)
    - [x] 1075-04-PLAN.md — Fix test_maps_style_json.py 5 failures (TI-02 partial)
    - [x] 1075-05-PLAN.md — Full-suite verification + commit (TI-01 + TI-02 close)
    ```

    **Step 4:** Add Phase 1075 to the `## Phases` archive section in ROADMAP.md (similar to existing closed-phase entries — keep the existing v1016 section as the template).
  </action>
  <acceptance_criteria>
    - `grep "Phase: 1075 (complete)" .planning/STATE.md` returns at least 1 line
    - `grep "completed_phases: 1" .planning/STATE.md` returns 1 line
    - `grep "completed_plans: 5" .planning/STATE.md` returns 1 line
    - All 5 Plan checkboxes for Phase 1075 are `[x]` (verify via `grep -c "1075-0[12345]-PLAN.md" .planning/ROADMAP.md` — should be >= 5)
    - ROADMAP.md Progress table shows `5/5 | Complete` for Phase 1075
    - STATE.md `last_activity` is today's date
    - Decisions section has new entries for TI-01 and TI-02 closure
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Phase: 1075 (complete)" /Users/ishiland/Code/geolens/.planning/STATE.md && grep -c "5/5 | Complete" /Users/ishiland/Code/geolens/.planning/ROADMAP.md</automated>
  </verify>
  <done>STATE.md and ROADMAP.md reflect Phase 1075 closure; downstream phases (1076/1077/1078) are ready to start.</done>
</task>

<task type="auto">
  <name>Task 4: Commit phase work in a single grouped commit</name>
  <read_first>
    - All modified files from Plans 01-05 (verify what's tracked via `git status` and `git diff --stat`)
  </read_first>
  <action>
    **Step 1:** Run `git status` and `git diff --stat` to confirm the changes match expectations. Expected files:
    - backend/pyproject.toml (Plan 01)
    - backend/uv.lock (Plan 01)
    - backend/tests/conftest.py (Plan 01)
    - backend/tests/test_conftest_lifecycle.py (NEW, Plan 01)
    - backend/tests/test_defer_orphan_guard.py (Plan 02)
    - backend/tests/test_ingest.py (Plan 03)
    - backend/tests/test_maps_style_json.py (Plan 04)
    - .planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-01-PLAN.md (NEW, this plan creation)
    - .planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-02-PLAN.md (NEW)
    - .planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-03-PLAN.md (NEW)
    - .planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-04-PLAN.md (NEW)
    - .planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-05-PLAN.md (NEW)
    - .planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-0[12345]-SUMMARY.md (NEW)
    - .planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-05-VERIFICATION.md (NEW)
    - .planning/STATE.md (Plan 05)
    - .planning/ROADMAP.md (Plan 05)
    - any .planning/todos/pending/2026-05-21-1075-0[234]-skipped-test-*.md (Plan 02/03/04 fallback if gh failed)

    **Step 2:** Stage the files explicitly (NOT `git add -A`):
    ```
    git add \
      backend/pyproject.toml \
      backend/uv.lock \
      backend/tests/conftest.py \
      backend/tests/test_conftest_lifecycle.py \
      backend/tests/test_defer_orphan_guard.py \
      backend/tests/test_ingest.py \
      backend/tests/test_maps_style_json.py \
      .planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/ \
      .planning/STATE.md \
      .planning/ROADMAP.md
    ```

    If any .planning/todos/pending/2026-05-21-1075-0*-skipped-test-*.md files exist (issue fallback), add them too.

    **Step 3:** Create the commit with this message (use HEREDOC):
    ```
    git commit -m "$(cat <<'EOF'
    Phase 1075: conftest test-DB lifecycle refactor + baseline failure dispositions

    Closes TI-01 + TI-02. Net effect: 1363 InvalidCatalogNameError errors → 0,
    and 11 v1015 baseline failures → 0 FAILED (split between fixed and
    skipped-with-issue).

    TI-01 (conftest refactor):
    - Add pytest-xdist>=3.6.0 to dev deps so -n auto is first-class
    - Switch test-DB naming to {base}_{worker_id}_{uuid8}; PYTEST_XDIST_WORKER
      env var selects per-worker isolation under -n auto, defaults to "master"
    - Order teardown: pg_terminate_backend → 50ms sleep → DROP DATABASE
    - Pin invariants via new tests/test_conftest_lifecycle.py (6 tests)

    TI-02 (11 baseline failures dispositioned):
    - test_defer_orphan_guard.py (3): see 1075-02-SUMMARY
    - test_ingest.py (3): see 1075-03-SUMMARY
    - test_maps_style_json.py (5): see 1075-04-SUMMARY
    - All SKIPPED tests carry pytest.mark.skip(reason=<issue-or-todo-url>)

    Verification: 1075-05-VERIFICATION.md captures pre/post counts. Sequential
    and parallel (-n auto) runs both exit 0 with 0 FAILED, 0 errors.

    Downstream readiness: Phase 1076/1077/1078 can now consume a trustworthy
    pytest signal. Phase 1079 TI-03 will produce the post-v1017 baseline doc
    on top of this Phase 1075 baseline.
    EOF
    )"
    ```

    **Step 4:** Verify the commit landed cleanly:
    ```
    git status   # must show clean working tree
    git log -1 --stat   # confirms the commit
    ```
  </action>
  <acceptance_criteria>
    - `git status --porcelain` returns 0 lines after commit (clean working tree)
    - `git log -1 --pretty=format:"%s"` matches "Phase 1075: conftest test-DB lifecycle refactor + baseline failure dispositions"
    - `git log -1 --stat | grep -c "tests/conftest.py"` returns at least 1
    - `git log -1 --stat | grep -c "1075-05-VERIFICATION.md"` returns 1
    - `git log -1 --stat | grep -c "STATE.md\|ROADMAP.md"` returns at least 2
    - No `.env`, no credentials files in the diff (verify with `git log -1 --stat | grep -E "\.env|credentials|secrets"` returns 0)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && git status --porcelain | wc -l | head -1 && git log -1 --pretty=format:"%s"</automated>
  </verify>
  <done>Phase 1075 work committed in a single grouped commit; working tree is clean; commit message documents both TI-01 and TI-02 closure with downstream-phase implications.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest runner → full test surface | runs every test file in backend/tests/; touches real DB (per-worker) + real ASGI app under test |
| git → remote | THIS PLAN DOES NOT PUSH. The commit lands on local main only. Pushing is a manual user action. |
| Phase 1075 → Phase 1076/1077/1078/1079 | downstream phases inherit the post-1075 baseline; if 1075's verification is wrong, downstream signal is corrupted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1075-05-01 | Tampering | VERIFICATION.md contains fabricated numbers | mitigate | All numbers in the verification doc are sourced from /tmp/1075-05-counts.md which itself comes from /tmp/1075_full_seq.log and /tmp/1075_full_par.log. The logs are evidence trails; if VERIFICATION.md numbers don't match the logs, the doc is invalid. Future audit (Phase 1079) re-runs the suite as part of close-gate. |
| T-1075-05-02 | Repudiation | Skipped tests without trackable artifacts | mitigate | Task 2 acceptance gate requires every SKIPPED test row in the inventory table to have a non-empty "Tracking Artifact" column with a verifiable URL or filepath. Empty rows fail acceptance. |
| T-1075-05-03 | Denial of Service | Parallel run -n auto consumes excess test-DB resources | accept | Each xdist worker creates its own DB; for an 8-core machine, that's 8 DBs simultaneously. Postgres can handle this easily; the existing CREATE/DROP DATABASE pattern reaps them on teardown. If a developer machine is under-resourced, they can run with `-n 2` instead — the conftest fix supports any N. |
| T-1075-05-04 | Repudiation | Commit message claims closure without evidence | mitigate | Task 4 commit message references the VERIFICATION.md document; SUMMARYs from Plans 01-04 are also committed; the evidence chain is auditable |
| T-1075-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan — only pytest-xdist from Plan 01, which has already passed legitimacy review there |
</threat_model>

<verification>
1. `cd backend && uv run pytest tests/` exits 0
2. `cd backend && uv run pytest tests/ -n auto` exits 0
3. Zero `InvalidCatalogNameError` errors across both runs (down from 1363)
4. Zero FAILED tests across both runs (down from 11)
5. SKIPPED tests all have annotated reasons pointing to issues or todo files
6. PASSED + SKIPPED count matches between sequential and parallel runs
7. 1075-05-VERIFICATION.md exists and is complete (no placeholders)
8. STATE.md + ROADMAP.md updated; phase shown as complete
9. Single git commit groups all phase work; clean working tree post-commit
</verification>

<success_criteria>
- [ ] TI-01 closed: zero InvalidCatalogNameError errors; sequential + parallel runs both exit 0
- [ ] TI-02 closed: all 11 v1015 baseline failures dispositioned (PASSED or SKIPPED with documented issue)
- [ ] 1075-05-VERIFICATION.md created with complete per-test inventory
- [ ] STATE.md updated (progress 1/5 phases complete; 5/5 plans for Phase 1075)
- [ ] ROADMAP.md Progress table + plans list updated
- [ ] Single git commit groups all Phase 1075 work
- [ ] Downstream phases (1076, 1077, 1078, 1079) have a trustworthy baseline to build on
</success_criteria>

<output>
Create `.planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-05-SUMMARY.md` documenting:
- Task-level results (each of the 4 tasks completed)
- Pointer to 1075-05-VERIFICATION.md as the substantive evidence
- Pre/post baseline summary (one-paragraph)
- Phase commit hash
- Any deviations from the plan
</output>
