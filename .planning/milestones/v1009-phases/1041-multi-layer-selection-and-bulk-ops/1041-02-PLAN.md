---
phase: 1041
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - frontend/src/components/builder/BulkActionBar.tsx
  - frontend/src/i18n/locales/en/builder.json
  - frontend/src/components/builder/UnifiedStackPanel.tsx
autonomous: true
requirements:
  - POL-08
tags:
  - mapbuilder
  - bulk-actions
  - i18n
  - shadcn

must_haves:
  truths:
    - "User who has 2+ rows multi-selected sees a sticky-footer bulk action bar inside the unified stack panel showing: '{N} selected | Visibility | Opacity slider | Group | Ungroup | Delete'."
    - "User who has only 0 or 1 rows selected does NOT see the bulk action bar (it is unmounted, not merely hidden)."
    - "User clicking 'Delete' inside the bulk action bar sees the bar transition to a confirmation row: 'Delete {N} layers? This cannot be undone.' with [Cancel] [Delete {N} layers] buttons; Cancel is autoFocused."
    - "User pressing Escape while in the confirmation state OR clicking Cancel returns the bar to its normal multi-action state with selection preserved."
    - "User sees the Group button disabled when any selected row is already inside a folder-group OR is itself a folder-group row OR is a DEM/raster layer (with a tooltip explaining why)."
    - "User sees the Ungroup button disabled when any selected row is NOT a folder-group row."
    - "User sees Visibility/Opacity buttons take the average opacity / majority visibility across selected layers as their initial display."
  artifacts:
    - path: "frontend/src/components/builder/BulkActionBar.tsx"
      provides: "BulkActionBar component — sticky footer, confirmation state machine, disable rules, ARIA toolbar + aria-live, optimistic-state previews"
      min_lines: 150
      contains: "role=\"toolbar\""
    - path: "frontend/src/i18n/locales/en/builder.json"
      provides: "bulkActions namespace key with all 18 strings from UI-SPEC Copywriting Contract"
      contains: "bulkActions"
    - path: "frontend/src/components/builder/UnifiedStackPanel.tsx"
      provides: "Conditional render <BulkActionBar selectedIds=... layers=... onClearSelection=... onBulk*={...} /> when selectedIds.size >= 2; bulk handler props received from parent (lifted in Plan 01) but with no-op defaults until Plan 03 wires the real handlers"
      contains: "BulkActionBar"
  key_links:
    - from: "frontend/src/components/builder/BulkActionBar.tsx"
      to: "frontend/src/i18n/locales/en/builder.json (bulkActions namespace)"
      via: "useTranslation('builder') + t('bulkActions.*') keys"
      pattern: "t\\('bulkActions\\."
    - from: "frontend/src/components/builder/UnifiedStackPanel.tsx"
      to: "frontend/src/components/builder/BulkActionBar.tsx"
      via: "Conditional JSX render based on selectedIds.size >= 2"
      pattern: "<BulkActionBar"
    - from: "frontend/src/components/builder/BulkActionBar.tsx"
      to: "shadcn ui (Button, Slider, Tooltip, Checkbox)"
      via: "imports from @/components/ui/*"
      pattern: "@/components/ui"
---

<objective>
Ship the `BulkActionBar` component (sticky footer of `UnifiedStackPanel`), add the `bulkActions` i18n namespace to `en/builder.json` (placed BEFORE the duplicate-key block at line 859 — see PATTERNS.md key architecture note #4), and conditionally render the bar in `UnifiedStackPanel` when `selectedIds.size >= 2`. Closes POL-08 (bulk action bar exists with visibility/opacity/group/ungroup/delete + correct enable/disable rules).

Purpose: With Plan 01 landing the selection model, this plan adds the visible action surface. The bar's handlers wire to the (yet-to-be-implemented) bulk op functions in `use-builder-layers.ts` — for this plan, the handler props arrive as no-op stubs from `MapBuilderPage` (with `console.warn('Bulk handler not wired yet — Plan 03')` so the executor can verify wiring without firing real ops). Plan 03 swaps the no-ops for real implementations.

Output: A self-contained `BulkActionBar` component with a confirmation state machine, the `bulkActions` i18n keys for English, and the render condition wired into `UnifiedStackPanel`. The bar visibly appears/disappears based on selection size in the running app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-CONTEXT.md
@.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-PATTERNS.md
@.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-UI-SPEC.md
@.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-01-SUMMARY.md
@.claude/skills/sketch-findings-geolens/SKILL.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase 2026-05-14. -->

BulkActionBar props contract (define in BulkActionBar.tsx — this is what UnifiedStackPanel must pass):
```typescript
import type { GroupedLayer } from '@/components/builder/types';   // or wherever GroupedLayer lives

interface BulkActionBarProps {
  selectedIds: Set<string>;            // current multi-selection
  layers: GroupedLayer[];              // all layers in the stack — used to compute avgOpacity, majorityVisible, canGroup, canUngroup
  onClearSelection: () => void;        // called on ESC, outside-click, post-bulk-op (POL-10)
  onBulkVisibility: (ids: Set<string>) => void;
  onBulkOpacity: (ids: Set<string>, opacity: number) => void;   // opacity 0..1
  onBulkGroup: (ids: Set<string>) => void;
  onBulkUngroup: (ids: Set<string>) => void;
  onBulkDelete: (ids: Set<string>) => void;
}
```

From frontend/src/components/builder/FolderGroupRow.tsx (analog for inline confirm — lines 311-346):
- The `confirmingDelete` inline alertdialog pattern with role="alertdialog", autoFocus on Cancel, two-button layout (Delete/Cancel).
- Use this EXACT pattern for the BulkActionBar confirmation state — only the copy and the action bound to "Delete" change.

From frontend/src/components/ui/button.tsx: `Button` variants are `default | secondary | ghost | destructive | outline | link`; sizes `default | sm | icon`.

From frontend/src/components/ui/slider.tsx: shadcn Radix Slider; props `value: number[]`, `onValueChange: (v: number[]) => void`, `min`, `max`, `step`. Use `value={[Math.round(avgOpacity * 100)]}`, `min={0}`, `max={100}`, `step={1}`, `onValueChange={(v) => onBulkOpacity(selectedIds, v[0] / 100)}`.

From frontend/src/components/ui/tooltip.tsx: requires `<TooltipProvider>` ancestor (already mounted in MapBuilderPage). Use `<Tooltip><TooltipTrigger asChild>...</TooltipTrigger><TooltipContent side="top">...</TooltipContent></Tooltip>`.

Disable pattern (UI-SPEC §3 + PATTERNS.md "Disable pattern"):
```typescript
className={cn('h-8 gap-1.5 px-2', !canGroup && 'opacity-40 cursor-not-allowed pointer-events-none')}
```

Bar container CSS (UI-SPEC §"Bar container"):
- `position: sticky; bottom: 0;`
- `height: h-12 (48px)` — fixed
- `display: flex; align-items: center; gap: 1 (4px)` (Tailwind `gap-1`)
- `padding: 0 px-3 (12px horizontal)`
- `background: bg-[var(--surface-2)]`
- `border-top: 1px solid var(--border)` → Tailwind `border-t border-[var(--border)]`
- `border-bottom-{l,r}-radius: var(--radius-md)` → Tailwind `rounded-bl-[var(--radius-md)] rounded-br-[var(--radius-md)]`
- Enter animation: `transition-all duration-150` + initial `translate-y-2 opacity-0` → `translate-y-0 opacity-100` (use `data-[state=...]` attrs OR a simple Boolean-driven className swap; UI-SPEC says "Tailwind transition-all duration-150" — keep simple).

Existing en/builder.json structure (lines 715-859 = first block; 860-end = duplicate block). The `bulkActions` key MUST go INSIDE the first block (between line 858 closing brace and line 859 — i.e., as the LAST top-level key of the first block, OR appended INSIDE one of the existing namespaces? No — `bulkActions` is its own top-level namespace). Place it as the LAST top-level key inside the first object block, immediately before the line 859 boundary that ends the first block. Confirm by re-reading lines 715-870 of `en/builder.json` before editing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bulkActions i18n namespace to en/builder.json</name>
  <files>frontend/src/i18n/locales/en/builder.json</files>
  <read_first>
    - frontend/src/i18n/locales/en/builder.json lines 700-880 (identify the first block boundary at line ~859, the duplicate block start at line 860, and pick a safe insertion point INSIDE the first block)
    - .planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-PATTERNS.md lines 502-543 (i18n bulkActions key spec)
    - .planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-UI-SPEC.md "Copywriting Contract" section (canonical 18-string list)
  </read_first>
  <behavior>
    - Test 1: `JSON.parse(en/builder.json)` succeeds (no syntax error).
    - Test 2: `parsed.bulkActions.selectedCount === '{{count}} selected'`.
    - Test 3: All 18 keys from UI-SPEC Copywriting Contract are present under `bulkActions`.
    - Test 4: `bulkActions` appears EXACTLY ONCE in the file (not duplicated into the second block).
    - Test 5: The duplicate-block bug (existing top-level keys at lines 715 and 860) is NOT made worse — line 860+ block is left untouched in this task.
  </behavior>
  <action>
Add the `bulkActions` namespace key to `en/builder.json` per UI-SPEC Copywriting Contract and PATTERNS.md i18n spec. Implementation steps:

1. **Locate insertion point.** Re-read `en/builder.json` lines 700-880. The file has a duplicate-key bug — top-level keys appear at line ~715 (first block) and again at line ~860 (second block). The first block ends just before line 859/860. Find the EXACT line where the first block's closing `}` for its last namespace sits (likely the `"layerEditor"` namespace ending). Insert `bulkActions` as the LAST key inside the first block, immediately before that closing namespace's `}` boundary.

2. **Use the Edit tool, not Write.** Find the unique anchor "the last `}` of the FIRST occurrence of `"layerEditor"`" (or whichever is the last namespace inside the first block) and insert the new key after it with a comma. Do NOT touch the second block (lines 860+).

3. **Add per D-01 (additional_constraints):** the exact 18 keys from UI-SPEC Copywriting Contract under a `bulkActions` parent. Use the literal copy strings (English source). Each key value uses i18next interpolation `{{count}}` and `{{name}}` where applicable. Per PATTERNS.md spec, the namespace shape is:

```
"bulkActions": {
  "selectedCount": "{{count}} selected",
  "toolbarLabel": "Bulk actions for {{count}} selected layers",
  "liveAnnouncement": "{{count}} layers selected.",
  "visibility": "Visibility",
  "visibilityAriaLabel": "Toggle visibility for {{count}} selected layers",
  "opacity": "Opacity",
  "opacityAriaLabel": "Set opacity for {{count}} selected layers",
  "group": "Group",
  "groupAriaLabel": "Group {{count}} selected layers",
  "groupDisabledTooltip": "Select only loose layers to group",
  "ungroup": "Ungroup",
  "ungroupAriaLabel": "Ungroup {{count}} selected groups",
  "ungroupDisabledTooltip": "Select only groups to ungroup",
  "delete": "Delete",
  "deleteAriaLabel": "Delete {{count}} selected layers",
  "deleteConfirmLabel": "Delete {{count}} layers? This cannot be undone.",
  "deleteConfirmAction": "Delete {{count}} layers",
  "deleteConfirmCancel": "Cancel",
  "errorUpdateRolledBack": "Failed to update layers — changes rolled back.",
  "errorDeleteRolledBack": "Failed to delete {{count}} layers — no changes made.",
  "selectRow": "Select {{name}}",
  "selectGroup": "Select {{name}}"
}
```

4. **Note on extra keys:** Plan 01 references `bulkActions.selectRow` and `bulkActions.selectGroup` for the Checkbox aria-labels — both are included above (the UI-SPEC spec listed 18 strings; this adds 2 more for the checkbox aria-labels = 22 total). Document the addition in the SUMMARY so Phase 1044 knows to translate all 22 strings to de/fr/es.

5. **i18n dedup is OUT OF SCOPE.** The duplicate-key bug at line 860+ stays in this phase. PATTERNS.md key architecture note #4: cleanup is "a bonus" but not required, and `additional_constraints` says it is Phase 1042 polish territory.

6. **Validate JSON.** After the edit, run `node -e "JSON.parse(require('fs').readFileSync('frontend/src/i18n/locales/en/builder.json', 'utf8'))"` — must exit 0.
  </action>
  <verify>
    <automated>node -e "const d=JSON.parse(require('fs').readFileSync('/Users/ishiland/Code/geolens/frontend/src/i18n/locales/en/builder.json','utf8')); if(!d.bulkActions||!d.bulkActions.selectedCount||!d.bulkActions.deleteConfirmAction||!d.bulkActions.errorUpdateRolledBack||!d.bulkActions.selectRow){process.exit(1)} console.log('bulkActions key count:', Object.keys(d.bulkActions).length)" &amp;&amp; grep -c '"bulkActions":' /Users/ishiland/Code/geolens/frontend/src/i18n/locales/en/builder.json</automated>
  </verify>
  <acceptance_criteria>
    - JSON.parse succeeds.
    - `data.bulkActions` is an object containing at least these keys: `selectedCount`, `toolbarLabel`, `liveAnnouncement`, `visibility`, `visibilityAriaLabel`, `opacity`, `opacityAriaLabel`, `group`, `groupAriaLabel`, `groupDisabledTooltip`, `ungroup`, `ungroupAriaLabel`, `ungroupDisabledTooltip`, `delete`, `deleteAriaLabel`, `deleteConfirmLabel`, `deleteConfirmAction`, `deleteConfirmCancel`, `errorUpdateRolledBack`, `errorDeleteRolledBack`, `selectRow`, `selectGroup` (22 keys total).
    - `grep -c '"bulkActions":' en/builder.json` returns EXACTLY 1 (no accidental duplication into the line 860+ block).
    - The duplicate-key bug is not made worse: `grep -c '"unifiedStack":' en/builder.json` still returns 2 (status quo — Phase 1042's problem).
    - File is valid JSON; loaded by i18next at runtime without console error.
  </acceptance_criteria>
  <done>
    - All acceptance criteria pass.
    - `bulkActions` namespace inserted ONCE in the first block of the file.
    - Existing keys untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create BulkActionBar component with confirmation state machine</name>
  <files>frontend/src/components/builder/BulkActionBar.tsx, frontend/src/components/builder/UnifiedStackPanel.tsx, frontend/src/pages/MapBuilderPage.tsx</files>
  <read_first>
    - frontend/src/components/builder/FolderGroupRow.tsx lines 1-50, 311-346 (confirmingDelete inline alertdialog pattern — autoFocus on Cancel, role="alertdialog", two-button layout)
    - frontend/src/components/builder/StackRow.tsx lines 1-50, 357-389 (single-layer delete dialog — for compare-and-contrast on copy)
    - frontend/src/components/ui/button.tsx (Button variants/sizes available)
    - frontend/src/components/ui/slider.tsx (Radix Slider props — value: number[], onValueChange)
    - frontend/src/components/ui/tooltip.tsx (Tooltip component shape)
    - frontend/src/components/builder/UnifiedStackPanel.tsx lines 685-730 (existing Tooltip usage analog at the Settings/AddData buttons), lines 720-730 (listbox element + scrollable list closing div — find the right insertion point for the bar)
    - .planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-PATTERNS.md "BulkActionBar.tsx" section (skeleton + container CSS + Tooltip pattern)
    - .planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-UI-SPEC.md sections "Decision A — Bulk action bar position", "3. Bulk action bar conditions", "5. Bulk delete confirmation", "Copywriting Contract", "Color disambiguation"
  </read_first>
  <behavior>
    - Test 1: BulkActionBar with `selectedIds=new Set(['a','b'])` and 2 matching layers renders `role="toolbar"`, `aria-label="Bulk actions for 2 selected layers"`, `aria-live="polite"`, the count "2 selected", and 5 action buttons (Visibility, Opacity, Group, Ungroup, Delete).
    - Test 2: BulkActionBar with `selectedIds.size === 1` does NOT render (the parent UnifiedStackPanel guards on `>= 2`; this test asserts the parent guard).
    - Test 3: Clicking the Delete button transitions the bar to confirmation state — the 5 action buttons hide, the confirmation label "Delete 2 layers? This cannot be undone." renders in `text-destructive`, two buttons appear (Cancel + "Delete 2 layers"), Cancel has autoFocus.
    - Test 4: In confirmation state, clicking Cancel returns the bar to its 5-button state without firing onBulkDelete; selection is preserved.
    - Test 5: In confirmation state, pressing Escape returns the bar to its 5-button state.
    - Test 6: In confirmation state, clicking "Delete 2 layers" calls `onBulkDelete(selectedIds)` exactly once.
    - Test 7: Group button is disabled when ANY selected layer has a `parent_group_id` set (already in a group) OR `dataset_record_type !== 'vector_dataset'` (raster/DEM/basemap can't be in folder-group). The disabled tooltip reads "Select only loose layers to group".
    - Test 8: Ungroup button is disabled when NOT all selected rows are folder-group rows (`layer_type !== 'group:folder'` for any selected). Tooltip: "Select only groups to ungroup".
    - Test 9: Visibility button calls `onBulkVisibility(selectedIds)` once on click; the icon shows `EyeOff` when majority of selected layers are visible (toggle-to-hide cue), else `Eye`.
    - Test 10: Opacity Slider initial value is `Math.round(avgOpacity * 100)` (rounded average); changing the slider calls `onBulkOpacity(selectedIds, v[0] / 100)`.
    - Test 11: All control onClick / onPointerDown handlers stopPropagation so they do not bubble to the listbox row click handlers.
  </behavior>
  <action>
Create `BulkActionBar.tsx` per PATTERNS.md skeleton + UI-SPEC Decision A and §5 confirmation state. Implementation steps:

1. **File layout.** Create `frontend/src/components/builder/BulkActionBar.tsx`. Use `memo` from React (analog: `frontend/src/components/builder/StackRow.tsx` is memo'd). Export as named `BulkActionBar`.

2. **Imports.** lucide-react: `Eye`, `EyeOff`, `FolderPlus`, `FolderMinus`, `Trash2`. shadcn ui: `Button`, `Slider`, `Tooltip`, `TooltipContent`, `TooltipTrigger` (no `TooltipProvider` — already mounted at MapBuilderPage scope). Project: `cn` from `@/lib/utils`, `useTranslation` from `react-i18next`, `useState` from `react`. Type: `GroupedLayer` from the same location StackRow imports it.

3. **Props interface.** Use the BulkActionBarProps from the `<interfaces>` block above verbatim.

4. **Derived values.** Inside the component, compute these once per render:
   - `const N = selectedIds.size;`
   - `const selectedLayers = useMemo(() => layers.filter((l) => selectedIds.has(l.id)), [layers, selectedIds]);`
   - `const avgOpacity = N > 0 ? selectedLayers.reduce((sum, l) => sum + (l.opacity ?? 1), 0) / N : 1;`
   - `const visibleCount = selectedLayers.filter((l) => l.visible !== false).length;`  // default-true for legacy layers
   - `const majorityVisible = visibleCount > N / 2;`
   - `const canGroup = selectedLayers.every((l) => !l.parent_group_id && l.layer_type !== 'group:folder' && l.dataset_record_type === 'vector_dataset');`
   - `const canUngroup = N > 0 && selectedLayers.every((l) => l.layer_type === 'group:folder');`

5. **State machine.** `const [confirmingDelete, setConfirmingDelete] = useState(false);`. Reset to false whenever `selectedIds` changes externally (via `useEffect` keyed on `selectedIds.size === 0`).

6. **Bar container.** Outer element: a `<div>` with the exact classes per UI-SPEC "Bar container":
   - `'sticky bottom-0 flex items-center gap-1 px-3 h-12 bg-[var(--surface-2)] border-t border-[var(--border)] rounded-bl-[var(--radius-md)] rounded-br-[var(--radius-md)] transition-all duration-150'`
   - ARIA: `role="toolbar"`, `aria-label={t('bulkActions.toolbarLabel', { count: N })}`, `aria-live="polite"`.
   - Stop propagation on `onPointerDown` and `onClick` at the container level so clicks inside don't bubble to the listbox parent.

7. **Normal state (5-button mode) JSX.** When `!confirmingDelete`:
   - Left segment: `<span className="text-[13px] font-medium text-muted-foreground" aria-live="polite">{t('bulkActions.selectedCount', { count: N })}</span>`. Width target ~80px (no fixed width — let flex distribute).
   - Visibility button (`Button variant="ghost" size="sm" className="h-8 gap-1.5 px-2"`): Eye / EyeOff icon based on `majorityVisible`, label "Visibility" hidden on narrow widths via `<span className="hidden xl:inline">`. aria-label=`t('bulkActions.visibilityAriaLabel', { count: N })`. onClick: `onBulkVisibility(selectedIds)`.
   - Opacity: a flex group with a small "Opacity" label + a `<Slider value={[Math.round(avgOpacity * 100)]} min={0} max={100} step={1} onValueChange={(v) => onBulkOpacity(selectedIds, v[0] / 100)} className="w-20" aria-label={t('bulkActions.opacityAriaLabel', { count: N })} />`.
   - Group button (Button ghost sm): FolderPlus icon, label "Group", `disabled={!canGroup}`, classes `cn('h-8 gap-1.5 px-2', !canGroup && 'opacity-40 cursor-not-allowed pointer-events-none')`. Wrap in Tooltip when disabled showing `t('bulkActions.groupDisabledTooltip')`. onClick: `onBulkGroup(selectedIds)`.
   - Ungroup button: FolderMinus icon, label "Ungroup", `disabled={!canUngroup}`, parallel Tooltip with `t('bulkActions.ungroupDisabledTooltip')`. onClick: `onBulkUngroup(selectedIds)`.
   - Delete button (Button ghost sm with `text-destructive` class): Trash2 icon, label "Delete", aria-label=`t('bulkActions.deleteAriaLabel', { count: N })`. onClick: `setConfirmingDelete(true)`.
   - Each button must `onPointerDown={(e) => e.stopPropagation()}` to avoid drag/selection interference.

8. **Confirmation state JSX.** When `confirmingDelete`:
   - Replace bar contents with a single row: `<p className="text-sm text-destructive flex-1 text-center">{t('bulkActions.deleteConfirmLabel', { count: N })}</p>` + two buttons.
   - Cancel button: `<Button variant="secondary" size="sm" autoFocus onClick={() => setConfirmingDelete(false)}>{t('bulkActions.deleteConfirmCancel')}</Button>`. Per AUD-09, `autoFocus` is on the safe option (Cancel), NOT the destructive option.
   - Confirm button: `<Button variant="ghost" size="sm" className="text-destructive" onClick={() => { onBulkDelete(selectedIds); setConfirmingDelete(false); }}>{t('bulkActions.deleteConfirmAction', { count: N })}</Button>`.
   - Wrap in `role="alertdialog"`, `aria-labelledby={confirmId}` where `confirmId` is a unique-per-mount React useId().
   - Add a keydown listener on the container: when Escape, `setConfirmingDelete(false)` and stop propagation so it doesn't reach the parent's selection-clearing Escape handler (PATTERNS.md note: confirmation Escape preserves selection).

9. **Wire BulkActionBar into UnifiedStackPanel.** In `UnifiedStackPanel.tsx`, immediately before the closing `</div>` of the scrollable list (around line 864 per PATTERNS.md), add:
   - `{selectedIds.size >= 2 && (<BulkActionBar selectedIds={selectedIds} layers={layers} onClearSelection={onClearSelection} onBulkVisibility={onBulkVisibility} onBulkOpacity={onBulkOpacity} onBulkGroup={onBulkGroup} onBulkUngroup={onBulkUngroup} onBulkDelete={onBulkDelete} />)}`.
   - Add the matching props to `UnifiedStackPanelProps` interface: `onClearSelection: () => void`, `onBulkVisibility: (ids: Set<string>) => void`, `onBulkOpacity: (ids, opacity) => void`, `onBulkGroup`, `onBulkUngroup`, `onBulkDelete`.
   - Add the import: `import { BulkActionBar } from './BulkActionBar';`

10. **Stub bulk handlers in MapBuilderPage.** In `MapBuilderPage.tsx`, declare 5 no-op stub handlers and the clear-selection handler:
    ```
    const handleClearSelection = useCallback(() => { setSelectedIds(new Set()); }, []);
    const handleBulkVisibility = useCallback((ids: Set<string>) => { console.warn('[Phase 1041 Plan 03] handleBulkVisibility not wired yet:', ids); }, []);
    const handleBulkOpacity = useCallback((ids: Set<string>, opacity: number) => { console.warn('[Phase 1041 Plan 03] handleBulkOpacity not wired yet:', ids, opacity); }, []);
    const handleBulkGroup = useCallback((ids: Set<string>) => { console.warn('[Phase 1041 Plan 03] handleBulkGroup not wired yet:', ids); }, []);
    const handleBulkUngroup = useCallback((ids: Set<string>) => { console.warn('[Phase 1041 Plan 03] handleBulkUngroup not wired yet:', ids); }, []);
    const handleBulkDelete = useCallback((ids: Set<string>) => { console.warn('[Phase 1041 Plan 03] handleBulkDelete not wired yet:', ids); }, []);
    ```
    Pass all 6 to `<UnifiedStackPanel>` JSX. The `console.warn` is intentional — Plan 03 replaces these with real implementations and the warn becomes silent. The warn prefix tells reviewers exactly which plan owns the wiring.

11. **No new tokens.** Per D-01 hard constraint: BulkActionBar uses `var(--surface-2)`, `var(--border)`, `var(--radius-md)`, `var(--primary)`, `text-destructive` (existing token), all from sketch-findings-geolens / index.css. No new color/spacing/motion.

12. **No DnD interaction.** BulkActionBar must NOT interact with `useDndContext` or `useDraggable` — it sits inside the DndContext but is not draggable. Stop propagation on pointerDown is the safety net.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend &amp;&amp; npx tsc -b --noEmit 2>&amp;1 | tail -10 &amp;&amp; test -f /Users/ishiland/Code/geolens/frontend/src/components/builder/BulkActionBar.tsx &amp;&amp; grep -c "role=\"toolbar\"" /Users/ishiland/Code/geolens/frontend/src/components/builder/BulkActionBar.tsx &amp;&amp; grep -c "confirmingDelete" /Users/ishiland/Code/geolens/frontend/src/components/builder/BulkActionBar.tsx &amp;&amp; grep -c "autoFocus" /Users/ishiland/Code/geolens/frontend/src/components/builder/BulkActionBar.tsx &amp;&amp; grep -c "onBulkVisibility\|onBulkOpacity\|onBulkGroup\|onBulkUngroup\|onBulkDelete" /Users/ishiland/Code/geolens/frontend/src/components/builder/BulkActionBar.tsx &amp;&amp; grep -c "BulkActionBar" /Users/ishiland/Code/geolens/frontend/src/components/builder/UnifiedStackPanel.tsx &amp;&amp; grep -c "handleBulk" /Users/ishiland/Code/geolens/frontend/src/pages/MapBuilderPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tsc 0 errors.
    - `BulkActionBar.tsx` exists, exports `BulkActionBar` as a named export.
    - `role="toolbar"` literal present (≥1 match).
    - `aria-live="polite"` literal present.
    - `confirmingDelete` state machine present (`useState(false)` + setter calls in click handlers).
    - `autoFocus` present on a Button (Cancel button per UI-SPEC §5 / AUD-09).
    - All 5 onBulk* handler props present in the component (each grep ≥1).
    - `<BulkActionBar` JSX present in `UnifiedStackPanel.tsx` with `selectedIds.size >= 2` guard (grep `selectedIds.size >= 2` returns ≥1).
    - 5 stub handlers + handleClearSelection present in `MapBuilderPage.tsx` (grep `handleBulk` ≥5 occurrences accounting for declarations + JSX use).
    - No new tokens added: `git diff frontend/src/index.css` returns empty for this task.
    - Build green: `cd frontend && npm run build 2>&1 | tail -5` exit 0.
  </acceptance_criteria>
  <done>
    - All acceptance criteria pass.
    - When the executor runs the dev server and selects 2 rows, the BulkActionBar appears at the bottom of the stack panel with all 5 action buttons.
    - Clicking Delete shows the inline confirmation; Cancel returns to action mode.
    - Clicking action buttons in normal state logs the `[Phase 1041 Plan 03]` warning (proves wiring is complete; Plan 03 swaps the warns for real ops).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → Confirmation state | Users may double-click Delete; the autoFocus on Cancel + the state machine prevent accidental destruction |
| i18n placeholder substitution | `{{count}}` and `{{name}}` interpolated via i18next — no raw user input concatenation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1041-05 | Tampering | BulkActionBar disable rules | mitigate | `canGroup` and `canUngroup` derived in the component each render — the underlying handler in Plan 03 will ALSO guard before mutating. The disable rule is the primary line of defense; the handler guard is defense-in-depth. |
| T-1041-06 | Denial of Service | Confirmation state machine | mitigate | Escape inside confirmation state stops propagation so it does NOT clear the multi-selection — preserves user's ability to retry. Verified by acceptance criterion test 5. |
| T-1041-07 | Information Disclosure | i18n keys with `{{count}}` | accept | Count is the integer size of selectedIds; no PII. |
| T-1041-08 | Repudiation | Bulk delete confirmation | mitigate | Two-step confirmation (click Delete → click "Delete N layers") + autoFocus on Cancel makes accidental deletion measurably harder than single-click. The eventual delete fires per-layer DELETE which is audited by the existing endpoint. |
</threat_model>

<verification>
End-of-plan checks:

```bash
cd /Users/ishiland/Code/geolens/frontend
npx tsc -b --noEmit                      # 0 errors
node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en/builder.json','utf8'))"   # 0 exit
npx vitest run src/components/builder/UnifiedStackPanel.test.tsx 2>&1 | tail -10   # existing tests still green (Plan 04 adds BulkActionBar-specific tests)
npm run build 2>&1 | tail -5             # 0 errors
```

Manual smoke (optional):
1. Run dev server, open Map Builder with 3+ overlay layers.
2. Cmd-click 2 rows → BulkActionBar appears at the bottom of the stack.
3. Click Delete → confirmation row replaces the action row; Cancel is autoFocused.
4. Press Escape → returns to action mode; selection preserved.
5. Click Cancel → same.
6. Click Visibility / Opacity / Group / Ungroup → console.warn fires from MapBuilderPage stubs.
</verification>

<success_criteria>
- POL-08 fully shipped: bulk action bar exists, anchored to footer, with all 5 actions (visibility, opacity, group, ungroup, delete).
- Enable/disable rules per UI-SPEC §3 enforced in the component.
- Inline delete confirmation per UI-SPEC §5 (no modal).
- ARIA: role="toolbar", aria-label, aria-live="polite" on the bar; role="alertdialog" + autoFocus on Cancel for confirmation state.
- bulkActions i18n namespace present in en/builder.json (22 keys); duplicate-block bug not made worse.
- BulkActionBar wired into UnifiedStackPanel with `selectedIds.size >= 2` guard.
- 5 stub bulk handlers + handleClearSelection present in MapBuilderPage; ready for Plan 03 to replace stubs with real implementations.
- No new design tokens introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-02-SUMMARY.md`. Document:
- The exact line where `bulkActions` was inserted into `en/builder.json` (so Phase 1042 dedup task can find it).
- The two extra keys added beyond UI-SPEC's 18 (`selectRow`, `selectGroup` for checkbox aria-labels) — Phase 1044 i18n must translate all 22.
- The 5 stub handlers in MapBuilderPage — note that Plan 03 replaces them; the `[Phase 1041 Plan 03]` console.warn prefix tells reviewers when stubs fire.
</output>
