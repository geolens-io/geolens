---
phase: 1044
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/builder/__tests__/UnifiedStackPanel.a11y.test.tsx
  - frontend/src/components/builder/__tests__/MapBuilderPage.a11y.test.tsx
  - .planning/phases/1044-cross-cutting-closeout/1044-A11Y-WALKTHROUGH.md
autonomous: true
requirements:
  - POL-23
tags:
  - a11y
  - accessibility
  - keyboard
  - aria
  - mapbuilder

must_haves:
  truths:
    - "Stack listbox advertises `role='listbox'` + `aria-multiselectable='true'` + a localized `aria-label` (`unifiedStack.listboxLabel`)."
    - "Each interactive stack row carries `data-row-id` so the document-level Shift+Arrow handler can resolve focus → row identity."
    - "Drag-from-catalog supports keyboard-only path: focus grip handle → Space pick up → Arrow keys traverse drop targets → Enter/Space drop, Escape cancels — backed by aria-live announcements (`a11y.dragPickup`, `a11y.dragPosition`, `a11y.dragDropped`, `a11y.dragCancelled`)."
    - "Multi-select supports Shift+ArrowUp / Shift+ArrowDown to extend selection, Space to toggle, Escape to clear."
    - "Destructive confirmations (BulkActionBar delete, layer-editor confirm-delete, basemap reset, folder-group delete) auto-focus their Cancel/Keep button so a stray Enter doesn't destroy state."
    - "A repeatable keyboard-only walkthrough document exists for both new features so a future reviewer can re-verify after refactors."
  artifacts:
    - path: frontend/src/components/builder/__tests__/UnifiedStackPanel.a11y.test.tsx
      provides: "Vitest a11y contract for listbox + multi-select keyboard semantics"
      min_lines: 60
    - path: frontend/src/components/builder/__tests__/MapBuilderPage.a11y.test.tsx
      provides: "Vitest a11y contract for drag-from-catalog aria-live region + announcement re-fire"
      min_lines: 50
    - path: .planning/phases/1044-cross-cutting-closeout/1044-A11Y-WALKTHROUGH.md
      provides: "Step-by-step keyboard walkthrough for drag-from-catalog and multi-select bulk delete; reusable as manual UAT script"
      contains: "drag-from-catalog"
  key_links:
    - from: frontend/src/components/builder/UnifiedStackPanel.tsx
      to: frontend/src/components/builder/__tests__/UnifiedStackPanel.a11y.test.tsx
      via: "aria-multiselectable + listboxLabel + Shift+Arrow document handler assertions"
      pattern: "aria-multiselectable.*true"
    - from: frontend/src/pages/MapBuilderPage.tsx
      to: frontend/src/components/builder/__tests__/MapBuilderPage.a11y.test.tsx
      via: "data-testid='dnd-announcement' aria-live region assertions"
      pattern: "data-testid=\\\"dnd-announcement\\\""
---

<objective>
Verify and pin the accessibility contract for the two v1.5 features (drag-from-catalog + multi-select bulk ops) by adding two focused vitest a11y test files and producing a repeatable keyboard-only walkthrough document. The implementation is already shipped (Phases 1040-04 and 1041-01) — this plan asserts the contract holds and documents how a screen-reader user actually completes each flow.

Purpose: POL-23 closes the milestone's a11y contract. The wiring is in place (verified by `grep`: `aria-multiselectable="true"` at UnifiedStackPanel.tsx:859, `data-row-id={layer.id}` at lines 193 + 372, aria-live region at MapBuilderPage.tsx:879-886, destructive-confirm autoFocus from Phase 1043-01). What's missing is (a) a test file that pins the listbox+keyboard contract independently of the larger UnifiedStackPanel suite, and (b) a written keyboard walkthrough that any reviewer (including a non-screen-reader user) can execute to verify the contract holds.

Output: Two new vitest files + one walkthrough markdown; existing builder vitest baseline maintained (no test count regression).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1044-cross-cutting-closeout/1044-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/1040-drag-from-catalog-into-stack/1040-04-SUMMARY.md
@.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-01-SUMMARY.md
@.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-04-SUMMARY.md
@.planning/phases/1043-error-empty-states-and-ia-cleanup/1043-01-SUMMARY.md

@frontend/src/components/builder/UnifiedStackPanel.tsx
@frontend/src/components/builder/BulkActionBar.tsx
@frontend/src/pages/MapBuilderPage.tsx

<interfaces>
<!-- The a11y surface to assert against. Do NOT modify these — assertions only. -->

UnifiedStackPanel.tsx (lines 750–910 region):
- The basemap dock div carries `data-testid="basemap-dock"` (line 750).
- The main listbox container carries `role="listbox"`, `aria-label={t('unifiedStack.listboxLabel', { defaultValue: 'Map layers' })}`, `aria-multiselectable="true"` (lines 857–859).
- Each row wrapper carries `data-row-id={layer.id}` (lines 193 + 372).
- A document-level keydown listener (lines 670–700) resolves Shift+Arrow keys against `[data-row-id]` and calls `onShiftClick(adjacentId)`; clamped at top/bottom of `selectableRowIds`.
- The Escape clearing path lives in the SAME effect: `onClearSelection()` fires when Escape is pressed AND `selectedIds.size > 0`.
- Outside-mousedown clearing: an effect listens for `mousedown` outside `stackPanelRef.current` and fires `onClearSelection()`.

MapBuilderPage.tsx (lines 95–110 + 875–890 region):
- `dragAnnouncement` state holds the latest screen-reader message; `announce(text)` calls `setDragAnnouncement(text + ZWS + Date.now())` to force aria-live re-fire.
- The sr-only div: `<div className="sr-only" role="status" aria-live="polite" aria-atomic="true" data-testid="dnd-announcement">{dragAnnouncement}</div>`.
- handleDragStart (catalog source) calls `announce(t('a11y.dragPickup', { name }))`.
- handleDragOver calls `announce(t('a11y.dragPosition', { n, total }))` only when over-id changes (lastOverIdRef debounce).
- handleDragEnd success path: `announce(t('a11y.dragDropped', { name, n }))`. Cancel/no-over: `announce(t('a11y.dragCancelled'))`.
- handleDragCancel: `announce(t('a11y.dragCancelled'))`.

BulkActionBar.tsx + LayerEditorPanel + FolderGroupRow destructive confirms (Phase 1043-01):
- BulkActionBar `role="alertdialog"` confirm dialog has `<button autoFocus>{t('bulkActions.deleteConfirmCancel')}</button>` so Cancel is the default action.
- LayerEditorPanel confirm-delete: `autoFocus` on `t('layerEditor.confirmDelete.keep')`.
- FolderGroupRow confirm-delete: `autoFocus` on `t('folderGroup.deleteConfirmCancel')`.
- BasemapSublayer reset: `autoFocus` on `t('basemapSublayer.resetConfirmCancel')`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the listbox + multi-select a11y contract (vitest)</name>
  <files>frontend/src/components/builder/__tests__/UnifiedStackPanel.a11y.test.tsx</files>
  <read_first>
    - frontend/src/components/builder/UnifiedStackPanel.tsx (lines 600–910 — the a11y wiring under test)
    - frontend/src/components/builder/__tests__/UnifiedStackPanel.multi-select.test.tsx (existing test pattern — props shape, makeLayer helper, document.addEventListener handling)
    - frontend/src/components/builder/__tests__/UnifiedStackPanel.test.tsx (existing render harness — i18n mock pattern, vi.mock pitfalls per PATTERNS.md)
    - .planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-04-SUMMARY.md (Test 8 + Tests 16–18 are the closest existing analogs — do NOT duplicate them; this file pins the *a11y* contract specifically, not the selection-model contract)
  </read_first>
  <behavior>
    - Test 1 — listbox role and label: rendering UnifiedStackPanel produces an element with `role="listbox"` whose `aria-label` is the localized listbox label (matches the i18n mock's resolution of `unifiedStack.listboxLabel`).
    - Test 2 — aria-multiselectable: same listbox element has `aria-multiselectable="true"`.
    - Test 3 — data-row-id presence: every layer row has `data-row-id` matching its layer id (smoke for the Shift+Arrow handler resolution path).
    - Test 4 — Shift+ArrowDown extension: with rows ['a','b','c'] selectable and 'a' focused (Tab simulated to set document.activeElement to a row), `keydown` Shift+ArrowDown on `document` fires `onShiftClick('b')`. Mirror the test 16 pattern from `UnifiedStackPanel.multi-select.test.tsx` but assert from the a11y angle (the contract is "Shift+Arrow extends selection through the listbox").
    - Test 5 — Shift+ArrowUp clamp: at top of selectable rows ('a' focused), Shift+ArrowUp does NOT fire `onShiftClick`. (Boundary safety.)
    - Test 6 — Escape clears: pressing Escape with `selectedIds=Set(['a','b'])` on the panel fires `onClearSelection`.
    - Test 7 — Outside mousedown clears: `mousedown` on document.body (outside stackPanelRef) with `selectedIds=Set(['a'])` fires `onClearSelection`.
    - Test 8 — basemap-group is non-selectable from keyboard: with `isMultiSelectionActive=true`, the basemap group row has `cursor-not-allowed` (visual a11y signal) and Cmd-click on its row is wired NOT to call `onCmdClick` (defense-in-depth for POL-11). This duplicates a multi-select test only if assertion target differs — instead, assert the *aria* contract: basemap row does NOT carry `aria-selected="true"` even when overlay rows do.
  </behavior>
  <action>
    Create `frontend/src/components/builder/__tests__/UnifiedStackPanel.a11y.test.tsx` following the conventions of the existing `UnifiedStackPanel.multi-select.test.tsx`:

    - Use the same i18n mock pattern (see `multi-select.test.tsx` top-of-file). The mock's `t()` MUST resolve `unifiedStack.listboxLabel` to a deterministic string (e.g., `'Map layers'`) so Test 1 can assert `getByRole('listbox', { name: 'Map layers' })`.
    - Use the same `makeLayer(id, opts)` helper or import it if extracted; otherwise inline a minimal copy. NO file-level `vi.mock('@dnd-kit/core', ...)` — per `1040-PATTERNS.md` and `1041-04-SUMMARY.md`, file-level dnd-kit mocks cause `Worker exited unexpectedly` regressions.
    - Render UnifiedStackPanel directly with controlled props (callbacks as `vi.fn()`); do not bring up MapBuilderPage.
    - Wrap renders in QueryClientProvider via the project's shared `renderHook`/`render` test utilities; check `frontend/src/test/test-utils.tsx` (or wherever the shared wrapper lives) for the canonical pattern.
    - For Tests 4 + 5 + 6 + 7, fire events on `document` (not the panel div) per the wiring at UnifiedStackPanel.tsx lines 670–700 — the existing handler attaches to document.
    - For Test 1, the listbox name comes from the resolved i18n label, NOT a `data-testid`. Use `screen.getByRole('listbox', { name: ... })` to assert the name; this is the assertion that catches both missing `aria-label` and broken i18n resolution.
    - Use `afterEach` `vi.clearAllMocks()` AND `cleanup()` per the bulk-ops test cleanup chain (1041-04-SUMMARY.md decisions §4) to avoid mock-call-count leaks.

    The file should be ~150-200 lines (8 tests, shared describe block, helpers reused or stubbed minimally).

    Do NOT modify any source file. If a test fails because a runtime contract is broken (e.g., aria-multiselectable was removed), STOP and surface the regression — do not "fix" the test to match the broken implementation.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/builder/__tests__/UnifiedStackPanel.a11y.test.tsx --reporter=basic 2>&amp;1 | tail -10 | grep -E "(Tests\s+8 passed|✓\s+Test [1-8])"</automated>
  </verify>
  <done>
    `UnifiedStackPanel.a11y.test.tsx` exists with 8 passing tests in 1 describe block. `npx vitest run src/components/builder/` reports test count strictly greater than the prior baseline (was 764 after Phase 1041-04 per its SUMMARY) and 0 failures, 0 worker errors. No source file modified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the drag-from-catalog aria-live announcement contract (vitest)</name>
  <files>frontend/src/components/builder/__tests__/MapBuilderPage.a11y.test.tsx</files>
  <read_first>
    - frontend/src/pages/MapBuilderPage.tsx (lines 95–115 + 875–890 — announce() + aria-live region)
    - frontend/src/components/builder/__tests__/DatasetSearchPanel.dragdrop.test.tsx (existing dnd-test pattern from Phase 1040-04 — vi.spyOn(dndCore, 'useDraggable'), no file-level dnd mock)
    - .planning/phases/1040-drag-from-catalog-into-stack/1040-04-SUMMARY.md (architecture diagram for announce() flow + ZWS+timestamp re-fire pattern)
    - frontend/src/i18n/locales/en/builder.json (a11y.* keys — Test assertions use the resolved English copy via the i18n mock)
  </read_first>
  <behavior>
    - Test 1 — aria-live region present: rendering MapBuilderPage produces an element matching `[data-testid="dnd-announcement"]` with `role="status"`, `aria-live="polite"`, `aria-atomic="true"`, and `className` containing `sr-only`.
    - Test 2 — initial state empty: the region's text content is the empty string (or just ZWS) before any drag.
    - Test 3 — pickup announces: simulating a `handleDragStart` event with a catalog-source draggable (e.g., `active.data.current = { source: 'catalog', name: 'Test Layer', recordType: 'vector_dataset' }`) populates the region with text containing the layer name AND the canonical pickup phrase from `a11y.dragPickup`.
    - Test 4 — re-fire on identical message: triggering pickup twice with the same name produces TWO distinct text values in the region (because of the ZWS+timestamp suffix); the second value is NOT identical to the first.
    - Test 5 — drop success announces: triggering `handleDragEnd` with a valid catalog-over event populates the region with text matching `a11y.dragDropped` (containing the layer name).
    - Test 6 — cancel announces: triggering `handleDragCancel` populates the region with the `a11y.dragCancelled` text.

    Note on test infrastructure: MapBuilderPage uses many providers (router, query client, auth, toast, etc.). The minimum viable approach is to render with the existing test wrapper used by MapBuilderPage's existing tests (`frontend/src/pages/__tests__/MapBuilderPage.test.tsx` — read it first to copy the wrapper pattern). If MapBuilderPage has no existing test, render only the relevant subtree by extracting/exporting a smaller component, OR render the full page with stubbed router (`MemoryRouter`) and a mock `useParams()` returning a stub map id, plus mocked API hooks (`use-builder-layers` etc. that return empty layer arrays).
  </behavior>
  <action>
    Create `frontend/src/components/builder/__tests__/MapBuilderPage.a11y.test.tsx`. If MapBuilderPage's render surface is too heavy to host in vitest (more than ~5 mocks needed), you have TWO options:

    **Option A (preferred — render the live region directly):** If `MapBuilderPage.tsx` already renders the aria-live region as inline JSX, extract just the `dragAnnouncement` state + announce() into a small shared hook `useDragAnnouncement` (read first to confirm structure — DO NOT extract if it would require source changes for testability; we are NOT modifying source in this plan). Failing extraction, render the full MapBuilderPage with a router + minimal mock chain.

    **Option B (full render — fallback):** Wrap with MemoryRouter, mock `useParams` to return `{ mapId: 'test-map-id' }`, mock the auth store to return a stub user, mock `useMap` / `useBuilderLayers` to return empty arrays, mock `useToast` to no-op. Then call the exposed handlers via the page's onDragStart/End/Cancel props (these flow into the inner DndContext).

    Choose the lighter option that succeeds. Both must satisfy the 6 behavior assertions above.

    Same hygiene rules as Task 1:
    - No file-level `vi.mock('@dnd-kit/core', ...)` — use `vi.spyOn(dndCore, 'useDraggable')` if needed or simulate by calling the handlers directly via test-only handles.
    - i18n mock must return resolved English strings for `a11y.dragPickup`, `a11y.dragDropped`, `a11y.dragCancelled`, `a11y.dragPosition` so assertions can match against substrings ("Picked up", "Dropped", "Drop cancelled").
    - `afterEach`: `vi.clearAllMocks()` + `cleanup()`.

    If full MapBuilderPage rendering proves too brittle (>30 minutes of mock plumbing), STOP after a partial implementation, document the blocker in the SUMMARY, and reduce Task 2 to Tests 1+2 (region presence + initial empty state) — Tests 3-6 become a deferred item to Phase 1044 Plan 03's e2e spec, which exercises the same flow at a higher level. Do NOT skip the test file entirely — Tests 1+2 alone still pin the most regression-prone surface (someone deleting the region).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/builder/__tests__/MapBuilderPage.a11y.test.tsx --reporter=basic 2>&amp;1 | tail -10 | grep -E "(Tests\s+[1-6] passed|✓\s+Test [1-6])"</automated>
  </verify>
  <done>
    `MapBuilderPage.a11y.test.tsx` exists with at least Tests 1+2 passing (Tests 3-6 if full-render is feasible). The full builder vitest run (`npx vitest run src/components/builder/`) shows 0 failures, 0 worker errors, and a test count strictly greater than the post-Task-1 baseline. Any deferred behavior tests are recorded in the SUMMARY as known coverage gaps with the e2e spec (Plan 03) named as the alternate verification path.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author the keyboard-only walkthrough document</name>
  <files>.planning/phases/1044-cross-cutting-closeout/1044-A11Y-WALKTHROUGH.md</files>
  <read_first>
    - frontend/src/components/builder/UnifiedStackPanel.tsx (selection model + Shift+Arrow handler)
    - frontend/src/pages/MapBuilderPage.tsx (drag handlers + announce flow)
    - frontend/src/components/builder/DatasetSearchPanel.tsx (catalog row drag handle aria-label)
    - frontend/src/components/builder/BulkActionBar.tsx (confirmation flow)
    - frontend/src/i18n/locales/en/builder.json (a11y.* keys — the actual screen-reader announcement text)
  </read_first>
  <action>
    Create `.planning/phases/1044-cross-cutting-closeout/1044-A11Y-WALKTHROUGH.md` with three numbered walkthroughs and a "How to verify" header at the top stating: VoiceOver (macOS Cmd+F5) or NVDA (Windows) recommended; the walkthrough is also runnable without a screen-reader to verify focus management and visible UI behaviour.

    **Walkthrough A: Drag-from-catalog (keyboard only)**
    1. Open a map in the builder (URL `/maps/{id}`); wait for the layer stack to render.
    2. Open the Add Dataset modal via `Tab` to the "+ Add data" button → Enter.
    3. Inside the modal, `Tab` to the first dataset row's grip handle (focusable, `aria-label="Drag to add to map"` per `search.dragHandle`).
    4. Press `Space` → screen-reader announces `"Picked up <dataset name>. Use arrow keys to choose a position, Enter to drop, Escape to cancel."` (`a11y.dragPickup`).
    5. Press `ArrowDown` repeatedly → screen-reader announces `"Current position: 1 of N"`, `"Current position: 2 of N"` (`a11y.dragPosition`).
    6. Press `Enter` → screen-reader announces `"Dropped. <dataset name> added at position N."` (`a11y.dragDropped`); the modal stays open (POL-05) and a toast confirms.
    7. To cancel mid-drag instead: press `Escape` → screen-reader announces `"Drop cancelled."` (`a11y.dragCancelled`); the modal stays open with no layer added.

    **Walkthrough B: Multi-select bulk delete (keyboard only)**
    1. With 3+ overlay layers in the stack, `Tab` into the listbox (`role="listbox"`, `aria-label="Map layers"`, `aria-multiselectable="true"`).
    2. Focus the first row → screen-reader announces row content + "selected, 1 of N, listbox".
    3. Press `Space` to add the row to multi-selection (toggles `aria-selected=true`).
    4. Press `Shift+ArrowDown` to extend selection to the next row (`onShiftClick` from the document handler at UnifiedStackPanel.tsx:670-700).
    5. Press `Tab` (or move focus to the bulk action bar) → focus reaches the toolbar (`role="toolbar"`, `aria-label="Bulk actions for {{count}} selected layers"`).
    6. `Tab` to "Delete" → press `Space` → confirmation dialog appears (`role="alertdialog"`); focus auto-lands on Cancel (per Phase 1043-01 destructive-confirm autoFocus).
    7. Press `Tab` once → focus moves to "Delete N layers" → press `Space` to confirm; or press `Escape` to cancel and close the dialog.
    8. To clear selection without deleting: press `Escape` anywhere in the listbox (`onClearSelection` fires when `selectedIds.size > 0`).
    9. Verify basemap-group boundary (POL-11): with overlay rows selected, `Tab` to the basemap group row in the basemap dock → it should NOT be cmd-clickable (cursor-not-allowed visual cue; `onCmdClick` not wired on the row). Selection cannot mix overlays + basemap.

    **Walkthrough C: Section transitions preserve focus + scroll (POL-18 carry-over)**
    1. With a basemap group in the stack, `Tab` to the basemap group row → press `Enter` to drill into the LayerEditorPanel basemap-group scene.
    2. Tab through sublayer chips → press `Enter` on a sublayer to drill into the basemap-sublayer scene.
    3. Press `Esc` (or click the "Back to basemap" footer button) → focus returns to the originating sublayer chip.
    4. Press `Esc` again → focus returns to the basemap group row in the listbox; vertical scroll position of the listbox is preserved.

    **Source-of-truth references (cite at the bottom):**
    - `frontend/src/components/builder/UnifiedStackPanel.tsx:780` — listbox role
    - `frontend/src/components/builder/UnifiedStackPanel.tsx:857-859` — listbox + aria-multiselectable
    - `frontend/src/components/builder/UnifiedStackPanel.tsx:670-700` — Shift+Arrow handler + Escape clear
    - `frontend/src/pages/MapBuilderPage.tsx:879-886` — aria-live announcement region
    - `frontend/src/components/builder/BulkActionBar.tsx:140` — alertdialog confirmation
    - `frontend/src/i18n/locales/en/builder.json` — `a11y.*` keys

    **Known limitations (record honestly):**
    - dnd-kit's KeyboardSensor uses Space+Arrow+Enter on macOS; some screen readers may intercept Space — note that VoiceOver users can press `Ctrl+Option+Space` to forward Space to the application.
    - Mobile (<800px) drill-down is via Sheet overlay; a separate gesture pass is deferred (per Phase 1038 Test 4 mobile-defer comment in `e2e/builder-unified-stack.spec.ts:376-384`).
  </action>
  <verify>
    <automated>test -f .planning/phases/1044-cross-cutting-closeout/1044-A11Y-WALKTHROUGH.md &amp;&amp; grep -c "^## Walkthrough\|^## Source-of-truth\|^## Known limitations" .planning/phases/1044-cross-cutting-closeout/1044-A11Y-WALKTHROUGH.md | awk '{ if ($1 &gt;= 4) exit 0; else { print "ERROR: found " $1 " required sections; expected at least 4"; exit 1 } }'</automated>
  </verify>
  <done>
    1044-A11Y-WALKTHROUGH.md exists with three numbered walkthroughs (drag-from-catalog, multi-select bulk delete, section transitions), explicit screen-reader announcement strings quoted from `a11y.*` keys, source-of-truth file:line references, and a known-limitations section. Reviewer can execute the walkthrough end-to-end with only the document + a running builder.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AT (assistive technology) → DOM | Screen readers consume aria attributes; broken contract = silent feature loss for AT users. |
| keyboard input → drag handler | Keyboard sensor triggers same handlers as pointer; an Escape that doesn't fire could leave a drag stuck. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1044-05 | Repudiation | screen-reader announcement loss | mitigate | Tests 1-2 in Task 2 pin the aria-live region presence + attributes; the ZWS+timestamp re-fire pattern (Test 4) ensures duplicate messages are not silently swallowed. |
| T-1044-06 | Tampering (refactor breaks listbox semantics) | UnifiedStackPanel.tsx aria-multiselectable | mitigate | Tests 1+2 in Task 1 pin role + aria-multiselectable + aria-label; any future PR that drops these breaks the test, not just behavior. |
| T-1044-07 | Denial of service (selection cannot be cleared) | Escape handler | mitigate | Test 6 in Task 1 asserts Escape clears selection when active; outside-mousedown clearing (Test 7) provides a second escape hatch. |
| T-1044-08 | Elevation of privilege (destructive action mis-fired) | bulk-delete confirmation | mitigate | Walkthrough B step 6 documents the autoFocus-on-Cancel contract from Phase 1043-01. The Cancel default protects against stray Enter presses. |
</threat_model>

<verification>
- `npx vitest run src/components/builder/__tests__/UnifiedStackPanel.a11y.test.tsx` reports 8 passing tests, 0 failures.
- `npx vitest run src/components/builder/__tests__/MapBuilderPage.a11y.test.tsx` reports at least 2 passing tests (Tests 1+2), 0 failures; Tests 3-6 pass if full-render is feasible.
- `npx vitest run src/components/builder/` shows 0 failures, 0 worker errors, count strictly greater than post-Phase-1041 baseline of 764.
- `npx tsc -b --noEmit` (frontend) reports 0 errors.
- `1044-A11Y-WALKTHROUGH.md` exists and contains the three walkthroughs + source-of-truth references.
</verification>

<success_criteria>
1. Two new vitest a11y test files exist and pass.
2. Listbox role, aria-multiselectable, aria-label, data-row-id, Shift+Arrow handler, Escape-clear, outside-mousedown-clear all pinned by tests.
3. aria-live drag-announcement region presence pinned by tests; pickup/drop/cancel announcement content pinned where feasible.
4. Keyboard walkthrough document exists and is reusable as manual UAT script for the next milestone reviewer.
5. Builder vitest baseline maintained (no test count regression; 0 worker errors).
</success_criteria>

<output>
After completion, create `.planning/phases/1044-cross-cutting-closeout/1044-02-SUMMARY.md` documenting:
- Test counts added (target: 8 + 6 = 14 new tests).
- Whether Task 2 used Option A (extracted hook) or Option B (full-render with mocks); if Option B with deferred Tests 3-6, name the e2e spec from Plan 03 as the alternate coverage path.
- Confirmation that walkthrough document was authored and is reusable.
- Any contract regressions discovered during test authoring (would be a serious finding — surface immediately, do not paper over).
</output>
