---
phase: 1042-spacing-density-states-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
autonomous: true
requirements:
  - POL-13
  - POL-14
tags:
  - builder
  - polish
  - tokens
  - motion

must_haves:
  truths:
    - "frontend/src/index.css :root defines --motion-fast: 150ms and --motion-base: 250ms"
    - "Insertion line ([data-dnd-over=true]) renders with 25% primary-color bloom shadow and full radius"
    - "Expanded folder-group children container shows a primary-tint wash when its parent row has data-group-drop-target=true"
    - "Pre-existing .dragging-active .kebab { opacity: 0 !important } rule remains intact (verified, not duplicated)"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "Motion timing tokens + insertion-line bloom + group-children wash CSS rules"
      contains: "--motion-fast"
  key_links:
    - from: "frontend/src/index.css :root"
      to: "All Tailwind duration-[--motion-fast] / duration-[--motion-base] consumers across builder"
      via: "CSS custom property cascade"
      pattern: "--motion-fast:\\s*150ms"
    - from: "frontend/src/index.css [data-dnd-over=true] block"
      to: "InsertionLine drop affordance during catalog->stack drag"
      via: "attribute selector applied by @dnd-kit useDroppable"
      pattern: "\\[data-dnd-over=\"true\"\\]"
    - from: "frontend/src/index.css [data-group-drop-target=true]"
      to: ".folder-group-children container in UnifiedStackPanel.tsx"
      via: "sibling/descendant selector + oklch wash"
      pattern: "\\.folder-group-children"
---

<objective>
Add the motion timing tokens (`--motion-fast`, `--motion-base`) to `index.css :root` and bake the two carry-over CSS-only fixes from Phase 1040's UI review into the existing drag-polish blocks: insertion-line bloom shadow + radius, and folder-group-children wash when the parent is a drop target.

Purpose: This plan is the prerequisite for Plans 02/03/04. Every `duration-[--motion-fast]` Tailwind class those plans add resolves to nothing if these tokens are missing. Landing the CSS first lets the parallel Wave 2 plans reference the tokens safely. The two carry-over fixes are also CSS-only, share the same file, and shipping them in the same PR keeps the index.css surface change atomic and reviewable.

Output: A patched `frontend/src/index.css` with two new tokens, an extended `[data-dnd-over="true"]` rule, and a new `[data-group-drop-target="true"] .folder-group-children` rule. No JSX or TS files touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/1042-spacing-density-states-polish/1042-CONTEXT.md
@.planning/phases/1042-spacing-density-states-polish/1042-PATTERNS.md
@.planning/phases/1042-spacing-density-states-polish/1042-UI-SPEC.md
@.planning/phases/1039-ux-audit-and-test-debt-closeout/BUILDER-UX-AUDIT.md

# Source file:
@frontend/src/index.css

<interfaces>
<!-- Existing CSS landmarks the executor must preserve / extend -->

From frontend/src/index.css (per PATTERNS.md scan):
- `:root` token block (light mode); `.dark` token block (dark mode). Both around lines 28-209.
- Existing drag-polish block: lines 512-547. Contains `[data-dnd-over="true"]` (lines 514-517), `.dragging-active .kebab { opacity: 0 !important }` (line 527 — already present, do NOT re-add), and `[data-group-drop-target="true"]` (lines 542-546).
- `tw-animate-css` import at line 8 (provides `animate-in`, `fade-in` utilities used by Plan 04).

Confirmed absent (must add this plan):
- `--motion-fast` (no grep match in :root)
- `--motion-base` (no grep match in :root)
- `--radius-full` (use literal `9999px` per UI-SPEC §Spacing exception)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add motion timing tokens to :root (AUD-08)</name>
  <files>frontend/src/index.css</files>
  <action>Add `--motion-fast: 150ms;` and `--motion-base: 250ms;` to the `:root` selector in frontend/src/index.css per AUD-08 from BUILDER-UX-AUDIT.md and per UI-SPEC §Motion Timing Reference. Insert as a new comment-prefixed block (`/* Phase 1042 AUD-08: motion timing tokens */`) immediately after the existing token group in `:root`. Do NOT add to `.dark` — these are universal timing values and CSS custom properties cascade; duplicating in `.dark` invites drift. Do NOT introduce any other tokens (the `--motion-*` pair is the only authorized addition per CONTEXT.md hard constraint "no new tokens EXCEPT --motion-fast / --motion-base"). After insertion, grep verifies both tokens exist exactly once at `:root` scope.</action>
  <verify>
    <automated>cd frontend && grep -E "^\s*--motion-(fast|base):" src/index.css | wc -l | grep -q "^2$" && grep -c "^\s*--motion-fast:\s*150ms" src/index.css | grep -q "^1$"</automated>
  </verify>
  <done>`--motion-fast: 150ms;` and `--motion-base: 250ms;` appear exactly once each in `:root`; `.dark` block unchanged; file still parses (next task's verify covers this).</done>
</task>

<task type="auto">
  <name>Task 2: Extend insertion-line block with bloom + radius (1040 carry-over)</name>
  <files>frontend/src/index.css</files>
  <action>Locate the existing `[data-dnd-over="true"]` block (around lines 514-517 per PATTERNS.md). Currently it sets only `border-top: 2px solid var(--primary)`. Add two declarations to the same rule: `border-radius: 9999px;` and `box-shadow: 0 0 0 2px oklch(0.55 0.18 250 / 25%);`. The `9999px` literal is required because `--radius-full` is confirmed absent from index.css per PATTERNS.md §Key Confirmations (UI-SPEC §Spacing exception authorizes the literal). The bloom color matches the `--primary` OKLCH coordinates at 25% alpha — do not substitute a different hue or open-code a hex value. While editing this block, confirm the `.dragging-active .kebab { opacity: 0 !important; }` rule (line 527 per PATTERNS.md) is still present; if absent, add it (AUD-03 verification — PATTERNS.md states it is already present, but the verify step ensures we did not accidentally delete it).</action>
  <verify>
    <automated>cd frontend && grep -A4 'data-dnd-over="true"' src/index.css | grep -q 'box-shadow:.*oklch.*0.55 0.18 250.*25%' && grep -A4 'data-dnd-over="true"' src/index.css | grep -q 'border-radius:\s*9999px' && grep -q 'dragging-active .kebab' src/index.css</automated>
  </verify>
  <done>The `[data-dnd-over="true"]` rule contains all three declarations (border-top, border-radius, box-shadow); `.dragging-active .kebab` rule still present; visual smoke (opening builder, dragging a catalog row over the stack) shows a soft glow around the insertion line.</done>
</task>

<task type="auto">
  <name>Task 3: Add folder-group children wash rule (1040 carry-over)</name>
  <files>frontend/src/index.css</files>
  <action>Before writing the new rule, inspect `frontend/src/components/builder/UnifiedStackPanel.tsx` around lines 362-390 to confirm whether `.folder-group-children` is a next sibling (`+`) or a descendant of `[data-group-drop-target="true"]`. PATTERNS.md §Cluster A flags this as the only structural ambiguity: the executor MUST verify the actual DOM relation before choosing the combinator. After verification, add a new rule immediately after the existing `[data-group-drop-target="true"]` block (around line 547 per PATTERNS.md) using the verified combinator. Target declarations: `background: oklch(0.97 0.02 250 / 60%);` and `border-radius: var(--radius-md);`. Both selectors (sibling and descendant) may be safely combined into a single rule with a comma — that is the recommended belt-and-braces approach if the DOM relation is ambiguous. Reference: UI-SPEC §Color reserved-accent #4 (group-drop-target fill). Do NOT modify the existing `[data-group-drop-target="true"]` block (lines 542-546) — that styles the parent row itself; this new rule styles the children container.</action>
  <verify>
    <automated>cd frontend && grep -E '\[data-group-drop-target="true"\].*\.folder-group-children|\.folder-group-children.*data-group-drop-target' src/index.css | wc -l | awk '$1 >= 1 {exit 0} {exit 1}' && grep -A3 'folder-group-children' src/index.css | grep -q 'oklch(0\.97 0\.02 250 / 60%)'</automated>
  </verify>
  <done>A new CSS rule targets `.folder-group-children` when its parent (or self ancestor) carries `[data-group-drop-target="true"]`; declarations include the OKLCH wash and `var(--radius-md)`; the original `[data-group-drop-target="true"]` row-styling block is untouched; visual smoke (drag a catalog row over an expanded folder group) tints the children container.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time CSS author -> runtime browser | All edits are static CSS. No runtime input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1042-01-01 | T (Tampering) | frontend/src/index.css | accept | Static stylesheet bundled at build time; tampering would require source-tree write access which is outside this plan's threat surface. No user-controlled values flow into CSS. |
| T-1042-01-02 | I (Information Disclosure) | new motion tokens | accept | `--motion-fast`/`--motion-base` are public timing values; no PII or secret material. |
| T-1042-01-03 | D (Denial of Service) | extended `[data-dnd-over="true"]` selector | accept | Selector is a single attribute match on a single element type; box-shadow paints are GPU-accelerated and bounded; no measurable perf risk for the worst-case (one drop target visible at a time). |
</threat_model>

<verification>
- `cd frontend && npm run build` completes without CSS-related errors.
- `cd frontend && npx vitest run src/components/builder/__tests__/EmptyStackState.test.tsx --reporter=basic` passes (smoke that index.css is still parseable; this test imports the stylesheet via the component tree).
- Manual visual smoke (recommended but not gating): open builder at <http://localhost:8080/builder>, drag a Add-Dataset modal row over the stack — insertion line shows a soft glow; drag over an expanded folder group — children container is tinted.
</verification>

<success_criteria>
- [x] POL-13 (spacing/density/typography token normalization): two new motion tokens land WITHIN the locked sketch-findings token contract per CONTEXT.md carve-out, enabling the rest of Phase 1042's normalization to reference consistent timing.
- [x] POL-14 (state vocabulary unification): all subsequent `duration-[--motion-fast]` consumers in Plans 02/03/04 resolve to 150ms; insertion-line bloom and group-children wash unify the visual drop-target state vocabulary across catalog->stack drags.
- [x] No new tokens beyond the two motion timing constants explicitly authorized by CONTEXT.md ("no new design tokens EXCEPT --motion-fast / --motion-base").
- [x] No JSX, TSX, or test files touched in this plan (file ownership locked to `frontend/src/index.css` to keep Wave 2 plans truly parallel).
- [x] No regressions to existing `[data-dnd-over="true"]` border-top, `.dragging-active .kebab` opacity rule, or original `[data-group-drop-target="true"]` row styling.
</success_criteria>

<output>
After completion, create `.planning/phases/1042-spacing-density-states-polish/1042-01-SUMMARY.md` per the template, naming the exact line numbers added, the verified DOM relation chosen for the children wash combinator, and the grep results from each task's verify step.
</output>
