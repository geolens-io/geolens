---
phase: 1042-spacing-density-states-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 1042-01
files_modified:
  - frontend/src/components/builder/BulkActionBar.tsx
  - frontend/src/components/builder/__tests__/BulkActionBar.test.tsx
  - frontend/src/components/builder/DatasetSearchPanel.tsx
  - frontend/src/components/builder/__tests__/DatasetSearchPanel.test.tsx
autonomous: true
requirements:
  - POL-14
  - POL-15
tags:
  - builder
  - bulk-actions
  - catalog
  - loading
  - polish

must_haves:
  truths:
    - "BulkActionBar element gap is 8px (gap-2) not 4px (gap-1)"
    - "BulkActionBar Cancel button in delete-confirm state uses variant=ghost not variant=secondary"
    - "BulkActionBar mounts with a real translate-y + opacity transition (initial state class -> mounted state class via useEffect/requestAnimationFrame)"
    - "BulkActionBar enabled-button labels are visible at 340px sidebar width via either a working sm:inline breakpoint OR a Tooltip wrapper that mirrors the disabled-button tooltip pattern"
    - "DatasetSearchPanel results list shows 5 skeleton placeholders during isLoading (first fetch); shows a 0.5px primary progress band during isFetching && !isLoading (refetch)"
    - "DatasetSearchPanel filter chips and ToggleGroupItems are uniform h-7 (28px); search Input stays h-9; basemap RecordTypeBadge stays h-5"
    - "DatasetSearchPanel result-row disclosure caret uses ChevronRight + rotate-90 (no ChevronDown swap); transition-transform with --motion-fast"
    - "DatasetSearchPanel draggable row body uses cursor-grab when not dragging and cursor-grabbing while dragging (full row body, not just handle)"
  artifacts:
    - path: "frontend/src/components/builder/BulkActionBar.tsx"
      provides: "Polished BulkActionBar with visible labels, ghost Cancel, mount transition, gap-2 spacing"
      contains: "useState"
    - path: "frontend/src/components/builder/DatasetSearchPanel.tsx"
      provides: "Skeleton-loading first fetch, progress-band refetch, normalized filter heights, ChevronRight rotate-90, cursor-grab on full row body"
      contains: "Skeleton"
    - path: "frontend/src/components/builder/__tests__/BulkActionBar.test.tsx"
      provides: "Vitest coverage for the four BulkActionBar polish fixes"
      contains: "BulkActionBar"
    - path: "frontend/src/components/builder/__tests__/DatasetSearchPanel.test.tsx"
      provides: "Vitest coverage for skeleton render + cursor-grab + chevron rotate"
      contains: "DatasetSearchPanel"
  key_links:
    - from: "BulkActionBar.tsx mount effect"
      to: "container className conditional ('translate-y-2 opacity-0' vs 'translate-y-0 opacity-100')"
      via: "useState(false) + useEffect(requestAnimationFrame(setMounted(true)))"
      pattern: "requestAnimationFrame\\(\\s*\\(\\s*\\)\\s*=>\\s*setMounted"
    - from: "DatasetSearchPanel.tsx isLoading branch"
      to: "5 Skeleton h-[58px] placeholders"
      via: "Array.from({ length: 5 }).map render"
      pattern: "Skeleton.*h-\\[58px\\]"
    - from: "DatasetSearchPanel.tsx DraggableDatasetRow outer div"
      to: "cursor-grab / cursor-grabbing class toggle"
      via: "cn() conditional with !isDragging / isDragging"
      pattern: "cursor-grab"
---

<objective>
Close Cluster B (BulkActionBar Phase 1041 carry-over fixes) and Cluster C (DatasetSearchPanel AUD-10/12/13/15 + cursor-grab carry-over) in a single plan. These two surfaces own no shared files with Plans 03/04, so this plan can ship in parallel with them once Plan 01's motion tokens are in `:root`.

Purpose: Phase 1041's UI review surfaced four BulkActionBar regressions (invisible labels at 340px, secondary Cancel variant, no-op mount animation, gap-1 instead of gap-2) that POL-14 (state unification) and POL-08-trace cleanup must close. POL-15 (loading affordances) requires the DatasetSearchPanel results list to swap its 16px spinner for skeleton rows and add a refetch progress band — this is the single P0 finding (AUD-10) owned by Phase 1042. The cursor-grab carry-over from Phase 1040's review and AUD-12/15 also live here because they share the DatasetSearchPanel file.

Output: A polished BulkActionBar that mounts with a real transition, shows labels (via sm:inline or Tooltip wrap), ghost-styles Cancel, and uses gap-2 spacing. A polished DatasetSearchPanel with skeleton-loading on first fetch, a progress band on refetch, normalized filter heights, ChevronRight rotation, and cursor-grab on the full row body. Both updates land with vitest coverage co-located in the existing __tests__ folder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/1042-spacing-density-states-polish/1042-CONTEXT.md
@.planning/phases/1042-spacing-density-states-polish/1042-PATTERNS.md
@.planning/phases/1042-spacing-density-states-polish/1042-UI-SPEC.md
@.planning/phases/1039-ux-audit-and-test-debt-closeout/BUILDER-UX-AUDIT.md

# Source files in scope:
@frontend/src/components/builder/BulkActionBar.tsx
@frontend/src/components/builder/DatasetSearchPanel.tsx
@frontend/src/components/builder/__tests__/BulkActionBar.test.tsx
@frontend/src/components/builder/__tests__/DatasetSearchPanel.test.tsx

# Patterns to copy:
@frontend/src/components/maps/MapCardSkeleton.tsx
@frontend/src/components/ui/skeleton.tsx

<interfaces>
<!-- Key landmarks per PATTERNS.md §Cluster B and §Cluster C. -->

BulkActionBar.tsx (existing, 345 LOC):
- export interface BulkActionBarProps (line 14)
- export const BulkActionBar = memo(function BulkActionBar(...)) (line 38)
- Container at lines 107-120 — currently uses `gap-1 px-3 transition-all duration-150` with NO initial-state class (mount animation no-op).
- Cancel button (delete-confirm state) at line 145 — currently `variant="secondary" autoFocus`.
- Button label spans at lines 203, 213, 244, 264, 291, 311, 337 — all use `hidden xl:inline text-xs` which never triggers in a 340px sidebar context.
- Existing Tooltip pattern for DISABLED buttons around lines 254-268 — copy this shape for enabled buttons if `sm:inline` still hides the label.

DatasetSearchPanel.tsx (existing, 719 LOC):
- Loading single-spinner block at lines 640-644 (per PATTERNS.md) using `<Loader2 className="h-4 w-4 animate-spin text-muted-foreground" />`.
- DraggableDatasetRow outer div at line 231 — currently no cursor classes on outer div.
- DraggableBasemapRow outer div at line 322 — same fix needed.
- Disclosure caret at line 258 — currently `{expanded ? <ChevronDown /> : <ChevronRight />}` swap.
- Filter chip buttons at lines 585, 597, 608, 620 — currently `h-6` (24px); ToggleGroupItem at lines 564-574 already correct at `h-7`.
- isLoading / isFetching from useQuery — both available as variables already in scope per PATTERNS.md.

Skeleton import path:
- `import { Skeleton } from '@/components/ui/skeleton';` (per MapCardSkeleton.tsx:2). Component uses `animate-shimmer` defined at index.css:355 — already shipped.

Existing tests:
- frontend/src/components/builder/__tests__/BulkActionBar.test.tsx — created in Phase 1041; extend with new test cases.
- frontend/src/components/builder/__tests__/DatasetSearchPanel.test.tsx — pre-existing; extend with skeleton + cursor + chevron tests.
- frontend/src/components/builder/__tests__/DatasetSearchPanel.dragdrop.test.tsx — Phase 1040; do NOT modify (separate concern).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix BulkActionBar (gap, Cancel variant, mount animation, label visibility)</name>
  <files>frontend/src/components/builder/BulkActionBar.tsx, frontend/src/components/builder/__tests__/BulkActionBar.test.tsx</files>
  <behavior>
    - Test 1: Container element has `gap-2` class (not `gap-1`).
    - Test 2: Initial render (before useEffect resolves) carries the initial-state classes (`translate-y-2 opacity-0`); after `act(() => Promise.resolve())` flushes the rAF callback the container carries the mounted classes (`translate-y-0 opacity-100`).
    - Test 3: When the component is in delete-confirm state, the rendered Cancel button receives ghost styling (assert by querying for the variant prop or by snapshotting the className contains the ghost-button Tailwind classes — check Button component's class output for variant="ghost").
    - Test 4: For each enabled action button (Visibility, Opacity, Group, Ungroup, Delete), the accessible name is non-empty AND visible to assistive tech — either via a visible label span (when sm/md viewport) or via a Tooltip wrapper that exposes the label as `TooltipContent`. Use `getByRole('button', { name: /Visibility|Toggle visibility/i })` style queries.
  </behavior>
  <action>Apply all four BulkActionBar carry-over fixes from Phase 1041's UI review per UI-SPEC §Carry-over Fixes from Phase 1041 UI Review and PATTERNS.md §Cluster B. Step-by-step: (1) change container className `gap-1` to `gap-2` (UI-SPEC table row 4 of the carry-over section); (2) add `const [mounted, setMounted] = useState(false); useEffect(() => { const id = requestAnimationFrame(() => setMounted(true)); return () => cancelAnimationFrame(id); }, []);` then update container className to include conditional `mounted ? 'translate-y-0 opacity-100' : 'translate-y-2 opacity-0'` and switch the duration utility to `duration-[--motion-fast]` (Plan 01 ships the token); (3) change the Cancel button at line 145 from `variant="secondary"` to `variant="ghost"` while preserving `autoFocus` and `size="sm"`; (4) for label visibility: first lower every `hidden xl:inline` label-span class to `hidden sm:inline`, THEN wrap each ENABLED action button (Visibility, Opacity, Group, Ungroup, Delete) in a `<Tooltip><TooltipTrigger asChild>...</TooltipTrigger><TooltipContent>{label}</TooltipContent></Tooltip>` matching the existing disabled-button tooltip shape (PATTERNS.md §Cluster B "Pattern for Tooltip wrapping"). The Tooltip becomes the always-on safety net so the label is reachable by AT even when the visible span hides at narrow viewports. Tooltip text == button label copy already in the file. Do NOT introduce new i18n keys (Phase 1044 owns locale fill); reuse existing t() calls. Add to BulkActionBar.test.tsx the four test cases described in <behavior>; vitest mocks for tooltip behavior should rely on the existing TooltipProvider wrapper used in Phase 1041 tests. Do NOT touch the underlying handlers, the per-row write logic, or the basemap-boundary guard — those are Phase 1041 contracts and out of scope here.</action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/builder/__tests__/BulkActionBar.test.tsx --reporter=basic</automated>
  </verify>
  <done>All BulkActionBar tests pass (existing + 4 new); manual smoke shows the bar slides up on appear, Cancel renders as a ghost button, labels are reachable on hover/focus at 340px sidebar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DatasetSearchPanel — skeleton + progress band + filter heights + chevron + cursor-grab</name>
  <files>frontend/src/components/builder/DatasetSearchPanel.tsx, frontend/src/components/builder/__tests__/DatasetSearchPanel.test.tsx</files>
  <behavior>
    - Test 1 (AUD-10): When the React Query hook is in `isLoading=true` (first fetch), the rendered tree contains exactly 5 elements with role="presentation" or with a class containing 'animate-pulse'/'animate-shimmer' AND height matching `h-[58px]`. Use the test wrapper that mocks useQuery to control isLoading/isFetching.
    - Test 2 (AUD-13): When `isFetching=true && isLoading=false`, the tree contains a thin progress band element (h-0.5 with animate-pulse + bg-[var(--primary)]) above the dimmed list.
    - Test 3 (AUD-15): The first dataset result row's disclosure caret is a single ChevronRight element (not a conditional ChevronRight/ChevronDown swap); its className contains both `transition-transform` and either `duration-[--motion-fast]` or — until token lands in test env — assert it does NOT use `duration-150` literal anymore. After expanding (simulate click), the same node's className contains `rotate-90`.
    - Test 4 (cursor-grab carry-over): The DraggableDatasetRow outer div has className containing `cursor-grab` when its drag state is idle. Use a render that does not invoke an active drag (default `useDraggable` mock state).
  </behavior>
  <action>Apply Cluster C fixes from PATTERNS.md and UI-SPEC §Findings to Address. Step-by-step:
1. **AUD-10 + AUD-13 (PRIORITY — POL-15 P0):** Replace the existing single-spinner block (lines 640-644 per PATTERNS.md) with the conditional split from PATTERNS.md §Cluster C: when `isLoading` (first fetch), render `<div className="mt-3 space-y-1 px-1">{Array.from({ length: 5 }).map((_, i) => <Skeleton key={i} className="h-[58px] w-full rounded-md" />)}</div>`; when `isFetching && !isLoading` (refetch), render `<div className="h-0.5 w-full bg-[var(--primary)] animate-pulse" />` above the existing dimmed list. Import Skeleton via `import { Skeleton } from '@/components/ui/skeleton';` per PATTERNS.md §Shared Patterns. Keep the existing `pointer-events-none opacity-50` on the dimmed list at line 687 (UI-SPEC §Loading Affordance Matrix calls this out as the "stale" signal pair).
2. **AUD-12 (filter heights):** Update filter chip buttons at lines 585, 597, 608, 620 from `h-6` to `h-7` (UI-SPEC §Findings AUD-12). ToggleGroupItem at lines 564-574 stays `h-7`. Search Input stays `h-9`. Basemap RecordTypeBadge stays `h-5` (UI-SPEC: badge not button).
3. **AUD-15 (disclosure icon):** Replace the `expanded ? <ChevronDown /> : <ChevronRight />` swap at line 258 with a single `<ChevronRight className={cn('h-3.5 w-3.5 transition-transform duration-[--motion-fast]', expanded && 'rotate-90')} />` per PATTERNS.md §Cluster C and UI-SPEC §Findings AUD-15. Remove the now-unused `ChevronDown` import (run a final grep to confirm no other reference remains).
4. **cursor-grab carry-over (1040 review):** On the DraggableDatasetRow outer div (line 231) AND the DraggableBasemapRow outer div (line 322), append cursor classes to the existing `cn(...)` call: `!isDragging && 'cursor-grab', isDragging && 'cursor-grabbing'` (UI-SPEC §Carry-over Fixes from Phase 1040 UI Review). Do NOT remove cursor classes from the inner handle button — both layers carrying cursor-grab is intentional so the visual affordance never disappears between handle and body.

Add to DatasetSearchPanel.test.tsx the four test cases described in <behavior>. The existing test file uses MSW (or query-mock) — reuse its existing setup; do not introduce a new mocking framework. Per CONTEXT.md anti-pattern note: do NOT add `vi.mock('@dnd-kit/core', ...)` at file top — work with the live module and rely on its public API.

Do NOT touch the search query, result mapping, basemap tab logic, or the existing dragdrop test file — those are out of scope. Do NOT add new i18n strings (Phase 1044 owns locale fill).</action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/builder/__tests__/DatasetSearchPanel.test.tsx --reporter=basic</automated>
  </verify>
  <done>All DatasetSearchPanel tests pass (existing + 4 new); first-fetch shows 5 skeleton rows; refetch shows progress band over dimmed list; filter chips render at 28px height; disclosure caret rotates instead of swapping icons; full row body shows grab cursor on hover.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) <- catalog API | DatasetSearchPanel results list renders attacker-influenced dataset titles, descriptions, IDs through React (auto-escaped). No new sink in this plan. |
| user mouse/keyboard input -> @dnd-kit registration | useDraggable / useDroppable handlers process pointer events; no new payload validation surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1042-02-01 | T (Tampering) | BulkActionBar Tooltip wrappers | accept | Tooltip content is hard-coded label string from t() — same source as the visible span; no new injection vector. |
| T-1042-02-02 | I (Information Disclosure) | DatasetSearchPanel skeleton placeholders | accept | Skeletons render no real data; they are static divs. |
| T-1042-02-03 | D (Denial of Service) | progress band animate-pulse during refetch | accept | Single 0.5px element with CSS animation; no JS loop, no expensive layout. |
| T-1042-02-04 | E (Elevation of Privilege) | BulkActionBar mount animation useEffect | accept | useEffect side-effect is `setState`; no privileged-API call; cleanup function cancels the rAF on unmount to avoid setState after unmount. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/builder/__tests__/BulkActionBar.test.tsx --reporter=basic` — passes.
- `cd frontend && npx vitest run src/components/builder/__tests__/DatasetSearchPanel.test.tsx --reporter=basic` — passes.
- `cd frontend && npx vitest run src/components/builder/__tests__/DatasetSearchPanel.dragdrop.test.tsx --reporter=basic` — still passes (regression gate; this plan must not break the Phase 1040 dragdrop coverage).
- `cd frontend && npm run lint -- src/components/builder/BulkActionBar.tsx src/components/builder/DatasetSearchPanel.tsx` — clean.
- Manual smoke (recommended): open the Add Dataset modal at <http://localhost:8080/builder>, observe (a) 5 skeleton rows on first open, (b) hovering a result row shows grab cursor on the full row, (c) opening a row's expand caret rotates rather than swaps; trigger a multi-select flow in the stack and observe BulkActionBar slides up, Cancel renders as ghost, gap visibly widens vs the pre-fix shipped bar.
</verification>

<success_criteria>
- [x] POL-14 (state vocabulary unification): BulkActionBar Cancel variant aligns with the destructive-confirm pattern used elsewhere in builder; element gap aligns with sketch sm=8px; mount transition no longer a no-op; label visibility reachable at every viewport.
- [x] POL-15 (loading affordances): DatasetSearchPanel results list shows skeleton rows on first fetch and a progress band on refetch — closes the only P0 owned by Phase 1042 (AUD-10).
- [x] AUD-10 / AUD-12 / AUD-13 / AUD-15 from BUILDER-UX-AUDIT.md addressed.
- [x] cursor-grab carry-over from Phase 1040 UI review applied to both DraggableDatasetRow and DraggableBasemapRow.
- [x] No new i18n keys, no new shadcn components, no backend touches, no @dnd-kit/core file-mock anti-pattern.
- [x] Existing tests in BulkActionBar.test.tsx, DatasetSearchPanel.test.tsx, and DatasetSearchPanel.dragdrop.test.tsx remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/1042-spacing-density-states-polish/1042-02-SUMMARY.md` per the template, naming the exact file lines edited, the chosen label-visibility strategy (sm:inline alone vs. + Tooltip wrappers), the verified ChevronDown removal, and the per-test result count.
</output>
