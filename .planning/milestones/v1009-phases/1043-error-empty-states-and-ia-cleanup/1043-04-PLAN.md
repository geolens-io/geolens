---
phase: 1043-error-empty-states-and-ia-cleanup
plan: 04
type: execute
wave: 4
depends_on:
  - 1043-03
files_modified:
  - frontend/src/components/builder/LayerEditorPanel.tsx
  - frontend/src/components/builder/StackRow.tsx
  - frontend/src/components/builder/DatasetSearchPanel.tsx
  - frontend/src/components/builder/BasemapGroupRow.tsx
  - frontend/src/components/builder/FolderGroupRow.tsx
  - frontend/src/components/builder/SettingsEditorScene.tsx
  - frontend/src/components/builder/hooks/use-builder-layers.ts
  - frontend/src/i18n/locales/en/builder.json
autonomous: true
requirements:
  - POL-17
  - POL-18
tags:
  - builder
  - polish
  - tokens
  - i18n
  - carry-over

must_haves:
  truths:
    - "Zero remaining `hover:bg-accent` instances in the seven 1042 carry-over call sites (LayerEditorPanel close + pin, StackRow kebab, DatasetSearchPanel ×2, BasemapGroupRow kebab, FolderGroupRow kebab) — replaced with `hover:bg-[var(--surface-2)]`"
    - "SettingsEditorScene three section eyebrows (TERRAIN / WIDGETS / PROJECTION at lines ~104, 155, 213) import and use the canonical `eyebrowClassName` constant from EmptyStackState (single source of truth for tracking-wide)"
    - "Five new English i18n keys exist: basemapGroup.toggleExpand, basemapSublayer.strokeColor, basemapSublayer.strokeWidth, basemapSublayer.casingColor, basemapSublayer.casingWidth"
    - "use-builder-layers freshLayerTimeoutRef is verified to self-null when the timeout fires (already shipped in Phase 1042 per UI-REVIEW WR-03 fix; verify-only here)"
  artifacts:
    - path: "frontend/src/components/builder/LayerEditorPanel.tsx"
      provides: "hover:bg-accent → hover:bg-[var(--surface-2)] at lines 234 + 278"
      contains: "hover:bg-[var(--surface-2)]"
    - path: "frontend/src/components/builder/SettingsEditorScene.tsx"
      provides: "eyebrowClassName imported and used at three section headers"
      contains: "eyebrowClassName"
    - path: "frontend/src/i18n/locales/en/builder.json"
      provides: "Five carry-over English copy keys"
      contains: "toggleExpand"
  key_links:
    - from: "frontend/src/components/builder/SettingsEditorScene.tsx"
      to: "frontend/src/components/builder/EmptyStackState.tsx"
      via: "import { eyebrowClassName } from './EmptyStackState'"
      pattern: "import.*eyebrowClassName"
    - from: "frontend/src/components/builder/StackRow.tsx, BasemapGroupRow.tsx, FolderGroupRow.tsx, LayerEditorPanel.tsx, DatasetSearchPanel.tsx"
      to: "--surface-2 hover token"
      via: "Tailwind arbitrary value class hover:bg-[var(--surface-2)]"
      pattern: "hover:bg-\\[var\\(--surface-2\\)\\]"
---

<objective>
Close the three Phase 1042 UI-REVIEW carry-overs explicitly assigned to Phase 1043 by CONTEXT.md §"Carry-overs from Phase 1042 UI review": (1) the `hover:bg-accent` 7-site token sweep flagged by 1042-UI-REVIEW Top Fix #1, (2) the SettingsEditorScene 3-eyebrow migration to the canonical `eyebrowClassName` flagged by Top Fix #2 / WARNING under Pillar 4, and (3) the missing i18n key fill-in for the 5 keys flagged across Pillar 1 INFO findings (1× basemapGroup.toggleExpand + 4× basemapSublayer.*). Also verifies the WR-03 freshLayerTimeoutRef self-null fix already shipped in Phase 1042.

Purpose: These three items are the residual cleanup needed to close the Phase 1042 polish sweep with full token-contract enforcement. They were deferred from Phase 1042 to Phase 1043 because they cluster naturally with the IA cleanup work and preserve the file-overlap-driven sequential wave structure. The hover-token sweep enforces the AUD-21 state vocabulary across all 7 deviating call sites; the eyebrow migration eliminates the tracking-[0.08em] vs tracking-wide divergence; the i18n keys close the i18n-check warning surface for English (de/es/fr remain Phase 1044's scope).
Output: Seven `hover:bg-accent` → `hover:bg-[var(--surface-2)]` substitutions; three `<span className=...>` → `<span className={eyebrowClassName}>` substitutions plus a new import; five new English keys in builder.json; freshLayerTimeoutRef self-null line verified present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1043-error-empty-states-and-ia-cleanup/1043-CONTEXT.md
@.planning/phases/1043-error-empty-states-and-ia-cleanup/1043-UI-SPEC.md
@.planning/phases/1042-spacing-density-states-polish/1042-UI-REVIEW.md
@frontend/src/components/builder/EmptyStackState.tsx
@frontend/src/components/builder/SettingsEditorScene.tsx
@frontend/src/components/builder/LayerEditorPanel.tsx
@frontend/src/components/builder/StackRow.tsx
@frontend/src/components/builder/DatasetSearchPanel.tsx
@frontend/src/components/builder/BasemapGroupRow.tsx
@frontend/src/components/builder/FolderGroupRow.tsx
@frontend/src/components/builder/hooks/use-builder-layers.ts

<interfaces>
<!-- The 7 hover:bg-accent call sites confirmed by grep at planning time: -->
<!--   LayerEditorPanel.tsx:234 — close/back button wrapper -->
<!--   LayerEditorPanel.tsx:278 — pin/popout button wrapper -->
<!--   StackRow.tsx:337 — kebab trigger button -->
<!--   DatasetSearchPanel.tsx:253 — DraggableDatasetRow expand button -->
<!--   DatasetSearchPanel.tsx:343 — DraggableBasemapRow expand button -->
<!--   BasemapGroupRow.tsx:203 — kebab trigger button -->
<!--   FolderGroupRow.tsx:314 — kebab trigger button -->

<!-- All 7 must become hover:bg-[var(--surface-2)] per AUD-21 state vocabulary established by Phase 1042 (and previously applied at SidebarRail.tsx:72 + :121). -->

<!-- The canonical eyebrowClassName constant is exported from EmptyStackState.tsx:17: -->
<!--   export const eyebrowClassName = 'block text-[10px] font-semibold tracking-wide text-muted-foreground uppercase px-1'; -->

<!-- SettingsEditorScene.tsx eyebrow span sites are at approximately lines 104, 155, 213 (per Phase 1042 UI-REVIEW). -->
<!-- Confirm exact line numbers via grep before editing — the file may have shifted by ±1-2 lines since the review. -->

<!-- freshLayerTimeoutRef self-null is at use-builder-layers.ts:655-657 per Phase 1042 grep findings. Verify-only — no code change unless missing. -->

<!-- Existing builder.json sections that hold the missing keys: -->
<!--   basemapGroup object at line 802 — add toggleExpand here -->
<!--   basemapSublayer object at line 813 — add strokeColor / strokeWidth / casingColor / casingWidth here -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: hover:bg-accent → hover:bg-[var(--surface-2)] sweep on 7 builder call sites (1042 carry-over Top Fix #1)</name>
  <files>frontend/src/components/builder/LayerEditorPanel.tsx, frontend/src/components/builder/StackRow.tsx, frontend/src/components/builder/DatasetSearchPanel.tsx, frontend/src/components/builder/BasemapGroupRow.tsx, frontend/src/components/builder/FolderGroupRow.tsx</files>
  <read_first>
    - .planning/phases/1043-error-empty-states-and-ia-cleanup/1043-UI-SPEC.md §"hover:bg-accent token sweep" lines 103-112 for the canonical 7-site list
    - .planning/phases/1042-spacing-density-states-polish/1042-UI-REVIEW.md §Pillar 3 Color findings for the WARNING context
    - Re-grep current state via `grep -n "hover:bg-accent" frontend/src/components/builder/{LayerEditorPanel,StackRow,DatasetSearchPanel,BasemapGroupRow,FolderGroupRow}.tsx` to confirm 7 hits before editing — line numbers may have shifted ±1-2 from prior plans
  </read_first>
  <action>
    Apply the AUD-21 state-vocabulary hover-token sweep per UI-SPEC §"hover:bg-accent token sweep" (carry-over from 1042 UI-REVIEW Top Fix #1). For each of the 7 listed call sites, replace `hover:bg-accent` with `hover:bg-[var(--surface-2)]`. Important: only replace the hover-token portion of the className. Other classes on the same element (e.g. `hover:text-foreground`) stay unchanged. Process each file in turn: (a) `LayerEditorPanel.tsx` lines 234 (close/back button wrapper) and 278 (pin/popout button wrapper) — replace inline; (b) `StackRow.tsx` line 337 (kebab trigger button); (c) `DatasetSearchPanel.tsx` lines 253 (DraggableDatasetRow expand button) and 343 (DraggableBasemapRow expand button); (d) `BasemapGroupRow.tsx` line 203 (kebab trigger button); (e) `FolderGroupRow.tsx` line 314 (kebab trigger button). After all 7 edits, re-grep with `grep -rn "hover:bg-accent" frontend/src/components/builder/ | grep -v test` and confirm zero matches in the builder source set. If any unexpected new occurrence appears in a file NOT on the canonical 7-site list, leave it alone and surface it in the SUMMARY — only the 7 sites flagged by the audit are in scope. Do not touch SidebarRail.tsx (already correct per Phase 1042 Plan 03) or BulkActionBar / EmptyStackState (also already correct).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -rn "hover:bg-accent" src/components/builder/ | grep -v test | grep -v __tests__ | wc -l</automated>
    Expected: 0 matches in non-test builder source files. Also: `grep -c "hover:bg-\[var(--surface-2)\]" src/components/builder/StackRow.tsx` returns ≥1; same for BasemapGroupRow.tsx, FolderGroupRow.tsx; `grep -c "hover:bg-\[var(--surface-2)\]" src/components/builder/LayerEditorPanel.tsx` returns ≥2; `grep -c "hover:bg-\[var(--surface-2)\]" src/components/builder/DatasetSearchPanel.tsx` returns ≥2; `npx tsc --noEmit -p tsconfig.app.json 2>&1 | head` returns no errors.
  </verify>
  <done>
    Zero `hover:bg-accent` matches in non-test files under src/components/builder/. All 7 sites use `hover:bg-[var(--surface-2)]`. Hover behavior on kebab triggers, panel close/pin buttons, and dataset expand buttons matches the surface-2 token (visually unchanged at the current palette since accent and surface-2 are ~1 chroma apart, but the token contract is now enforced).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SettingsEditorScene eyebrow migration to eyebrowClassName (1042 carry-over Top Fix #2)</name>
  <files>frontend/src/components/builder/SettingsEditorScene.tsx</files>
  <read_first>
    - .planning/phases/1043-error-empty-states-and-ia-cleanup/1043-UI-SPEC.md §"SettingsEditorScene Eyebrow Migration" lines 458-473 for the import + replace pattern
    - .planning/phases/1042-spacing-density-states-polish/1042-UI-REVIEW.md §Pillar 4 Typography WARNING for the divergence context
    - frontend/src/components/builder/EmptyStackState.tsx line 17 for the canonical eyebrowClassName export
    - frontend/src/components/builder/SettingsEditorScene.tsx — grep for `tracking-\[0.08em\]` to find the actual current line numbers (review documented lines 104/155/213 but the file may have shifted)
  </read_first>
  <action>
    Apply the eyebrow canonicalization per UI-SPEC §"SettingsEditorScene Eyebrow Migration" (carry-over from 1042 UI-REVIEW Top Fix #2). In SettingsEditorScene.tsx: (1) at the top of the file with the other imports, add `import { eyebrowClassName } from './EmptyStackState';` (use the relative path since both files are sibling components in builder/; preserve existing import group ordering); (2) grep within the file for `tracking-\[0.08em\]` to locate the three `<span>` eyebrow labels (documented as TERRAIN / WIDGETS / PROJECTION around lines 104, 155, 213 — verify before editing); (3) for each of the three sites, replace the inline className `"text-[10px] font-semibold uppercase tracking-[0.08em] text-muted-foreground"` (or whatever order the literal classes are in) with the canonical `eyebrowClassName` constant: `<span className={eyebrowClassName}>TERRAIN</span>`. Important: only modify the className attribute — do not change the text content (TERRAIN / WIDGETS / PROJECTION) and do not change any other props on the span (e.g. aria-hidden if present). The constant uses `tracking-wide` (0.1em) instead of `tracking-[0.08em]` — this is the intended unification per UI-SPEC §"SettingsEditorScene Eyebrow Migration" line 472. After editing, re-grep `grep -n "tracking-\[0.08em\]" frontend/src/components/builder/SettingsEditorScene.tsx` and confirm zero matches.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -c "tracking-\[0.08em\]" src/components/builder/SettingsEditorScene.tsx</automated>
    Expected: 0. Also: `grep -c "eyebrowClassName" src/components/builder/SettingsEditorScene.tsx` returns ≥4 (1 import + 3 usages); `grep -c "import.*eyebrowClassName" src/components/builder/SettingsEditorScene.tsx` returns 1; `npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep SettingsEditorScene | head` returns no errors.
  </verify>
  <done>
    SettingsEditorScene imports eyebrowClassName from ./EmptyStackState. The three section headers (TERRAIN / WIDGETS / PROJECTION) use `<span className={eyebrowClassName}>`. No remaining `tracking-[0.08em]` literal in the file. Tracking visually shifts from 0.08em to 0.1em (within 0.2px per character at 10px font-size — the intended canonicalization per UI-SPEC). SettingsEditorScene tests pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Five new English i18n keys + freshLayerTimeoutRef WR-03 verify (1042 carry-over INFO)</name>
  <files>frontend/src/i18n/locales/en/builder.json, frontend/src/components/builder/hooks/use-builder-layers.ts</files>
  <read_first>
    - .planning/phases/1043-error-empty-states-and-ia-cleanup/1043-UI-SPEC.md §"Missing i18n Keys" lines 476-493 for the canonical key list and English values
    - .planning/phases/1042-spacing-density-states-polish/1042-UI-REVIEW.md §Pillar 1 INFO findings for the carried-from-1042 context
    - .planning/phases/1042-spacing-density-states-polish/1042-UI-REVIEW.md §Pillar 6 Experience Design WARNING (WR-03) for the freshLayerTimeoutRef self-null context
    - frontend/src/i18n/locales/en/builder.json — confirm the basemapGroup object exists at line 802 and basemapSublayer object at line 813
    - frontend/src/components/builder/hooks/use-builder-layers.ts lines 650-660 for the freshLayerTimeoutRef state
  </read_first>
  <action>
    Apply the i18n key fill-in per UI-SPEC §"Missing i18n Keys" (carry-over from 1042 UI-REVIEW). In en/builder.json: (1) under the existing `"basemapGroup"` object (around line 802), add `"toggleExpand": "Toggle basemap group"` (alphabetical placement within the basemapGroup keys); (2) under the existing `"basemapSublayer"` object (around line 813), add four new keys: `"strokeColor": "Stroke color"`, `"strokeWidth": "Stroke width"`, `"casingColor": "Casing color"`, `"casingWidth": "Casing width"` (alphabetical placement within basemapSublayer). Use a JSON-aware edit pattern (Read the file, Edit/Write the modified file) — do not hand-craft a heredoc. Validate JSON validity after editing with `node -e "JSON.parse(require('fs').readFileSync('frontend/src/i18n/locales/en/builder.json'))"`. Do NOT modify any other locale file (de.json / es.json / fr.json) — Phase 1044 owns those (per CONTEXT.md §Scope Discipline). Note: keys `search.retry` (Plan 01) and `unifiedStack.emptyHelpBody` + `unifiedStack.browseAllShort` + `search.browseCatalogCta` + `layerEditor.source.noColumns` (Plan 02) and `basemapGroup.railLabel` (Plan 03) are added by earlier plans — do not duplicate them here. (4) VERIFY-ONLY for use-builder-layers.ts WR-03 fix: read lines 650-660 and confirm the freshLayerTimeoutRef setTimeout callback contains `freshLayerTimeoutRef.current = null;` after `setFreshLayerId(null);` per Phase 1042 UI-REVIEW Top Fix at WR-03 (this self-null was shipped in Phase 1042 per the planning-time grep showing line 657). If the self-null line is MISSING, add it; if present, no edit is needed and the SUMMARY records the verification.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; node -e "const j = JSON.parse(require('fs').readFileSync('src/i18n/locales/en/builder.json')); const must = ['basemapGroup.toggleExpand','basemapSublayer.strokeColor','basemapSublayer.strokeWidth','basemapSublayer.casingColor','basemapSublayer.casingWidth']; const miss = must.filter(p => { const [a,b] = p.split('.'); return !j[a] || j[a][b] === undefined; }); if (miss.length) { console.error('MISSING:', miss); process.exit(1); } console.log('OK')"</automated>
    Expected: prints "OK", exit 0. Also: `grep -A2 "setFreshLayerId(null)" src/components/builder/hooks/use-builder-layers.ts | grep -c "freshLayerTimeoutRef.current = null"` returns ≥1 (verifies WR-03 self-null is present); `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en/builder.json'))"` exits 0.
  </verify>
  <done>
    en/builder.json has the five new English keys (basemapGroup.toggleExpand, 4× basemapSublayer.*). JSON parses cleanly. de/es/fr locale files are untouched. use-builder-layers.ts freshLayerTimeoutRef self-nulls when the timeout fires (verified — already shipped in Phase 1042; no edit needed). All builder vitest tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tailwind className edits ↔ rendered DOM | Pure className substitutions; no behavior change, no event handler change, no new prop surface. |
| Static i18n key additions ↔ i18n.t resolution | Five new English-only entries; no template interpolation, no user-supplied content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1043-14 | Tampering | Hover-token className substitution | accept | Identical-syntax substitution; arbitrary-value Tailwind class is statically generated. No new attack surface. |
| T-1043-15 | Tampering | eyebrowClassName import | accept | Cross-component constant import within same package; constant value is a static string. No runtime indirection beyond standard ES module resolution. |
| T-1043-16 | Tampering | i18n key additions | accept | Five static-string entries with hard-coded English values; no user-controlled paths or interpolation. JSON validation enforced post-edit. |
| T-1043-17 | Information Disclosure | use-builder-layers verify-only inspection | accept | Read-only verification of an existing private React ref; no information leaves the inspected scope. |
</threat_model>

<verification>
1. `cd frontend && npx vitest run src/components/builder/ 2>&1 | tail -10` — all builder vitest passes (broad sweep since edits touch many files).
2. `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | head` — no new TS errors.
3. `cd frontend && npx eslint src/components/builder/ --max-warnings 0 2>&1 | tail` — clean across the builder folder.
4. `node -e "JSON.parse(require('fs').readFileSync('frontend/src/i18n/locales/en/builder.json'))"` — JSON valid.
5. `cd frontend && grep -rn "hover:bg-accent" src/components/builder/ | grep -v __tests__ | wc -l` — returns 0.
6. `cd frontend && grep -rn "tracking-\[0.08em\]" src/components/builder/SettingsEditorScene.tsx | wc -l` — returns 0.
7. `cd frontend && node -e "const j = JSON.parse(require('fs').readFileSync('src/i18n/locales/en/builder.json')); ['toggleExpand','strokeColor','strokeWidth','casingColor','casingWidth','railLabel'].forEach(k => { const present = JSON.stringify(j).includes('\"' + k + '\"'); console.log(k, present ? 'OK' : 'MISSING'); })"` — all six keys (5 from this plan + railLabel from Plan 03) marked OK.
</verification>

<success_criteria>
- Zero `hover:bg-accent` instances in non-test builder source files; all 7 sites converted to `hover:bg-[var(--surface-2)]`.
- SettingsEditorScene three eyebrow labels use `eyebrowClassName` from EmptyStackState (single source of truth); zero residual `tracking-[0.08em]` in that file.
- Five new English-only keys present in en/builder.json (basemapGroup.toggleExpand + 4× basemapSublayer.*); de/es/fr untouched.
- WR-03 freshLayerTimeoutRef self-null verified present (no edit if already shipped; SUMMARY records verification result).
- Builder vitest passes; TS clean; ESLint clean across src/components/builder/.
- POL-17 + POL-18 closure complete: all six audit findings owned by Phase 1043 (AUD-09/11/14/18/20/22) addressed across Plans 01/02/03/04; all three Phase 1042 carry-overs closed.
</success_criteria>

<output>
Create `.planning/phases/1043-error-empty-states-and-ia-cleanup/1043-04-SUMMARY.md` when done.
</output>
