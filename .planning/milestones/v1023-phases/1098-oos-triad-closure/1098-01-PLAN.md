---
phase: 1098
plan: 01
plan_id: 1098-01
name: OOS Triad Closure
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/modules/catalog/maps/router.py
  - backend/tests/test_layering.py
  - backend/tests/test_phase_275_readme_accuracy.py
  - backend/tests/test_ssrf_redirect.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/1098-oos-triad-closure/1098-01-SUMMARY.md
autonomous: true
requirements: [OOS-01, OOS-02, OOS-03]
requirements_addressed: [OOS-01, OOS-02, OOS-03]

must_haves:
  truths:
    - "[D-16] Sequential `pytest tests/` returns `failed == 0` literal (no OOS rows in the failure summary — not '0 NEW failed')"
    - "[D-16] `pytest -n 4 tests/` returns `failed == 0` literal on a single run (the 3 OOS targets are retired; only OAUTH-01/02 flakes remain — Phase 1099 scope)"
    - "[D-16] `pytest -n auto tests/` 3-run sequence shows ≤30 distinct (failed+errors) per run AND 0 `InvalidCatalogNameError` cascade frames (PARA-01 invariant preserved)"
    - "[D-01 trim-first / D-02 cap-raise fallback / D-03 no facade decomposition] `test_router_orchestrator_modules_stay_within_loc_cap` passes — either via trim to ≤1799 LOC (preferred) or via cap-raise to 1850 with documented backlog promotion"
    - "[D-04 delete / D-05 rationale in SUMMARY not test / D-06 no README change / D-07 no sibling sweep] `test_readme_signature_maps_list_intact` no longer exists (deleted in same commit batch as SUMMARY.md)"
    - "[D-08 behavioral / D-09 contract not wiring / D-10 no leaker hunt / D-11 keep constructor-arg checks] `test_make_safe_client_has_event_hook` rewritten as a behavioral SSRF-contract test that exercises `_revalidate_redirect` end-to-end (immune to module-level mock.patch contamination)"
    - "[D-12 single plan / D-13 file-disjoint surfaces / D-14 single shared verify gate / D-15 autonomous-mode-safe choices / D-17 REQ citation pinning / D-18 atomic flip with SUMMARY] REQUIREMENTS.md OOS-01/OOS-02/OOS-03 rows show `[x]` + `Complete` in the SAME commit as 1098-01-SUMMARY.md (v1019 TD-13 traceability rule)"
  artifacts:
    - path: ".planning/phases/1098-oos-triad-closure/1098-01-SUMMARY.md"
      provides: "Per-OOS root-cause + fix description + verify-gate measurement table"
      contains: "OOS-01"
    - path: "backend/tests/test_ssrf_redirect.py"
      provides: "Behavioral SSRF redirect test (replaces brittle identity check at former line 100)"
      contains: "make_safe_client"
    - path: "backend/app/modules/catalog/maps/router.py"
      provides: "Maps router — trimmed to ≤1799 LOC OR cap raised in test_layering.py allowlist to 1850"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table with OOS-01/02/03 flipped to `[x]` + `Complete`"
      contains: "| OOS-01 | Phase 1098 | Complete |"
  key_links:
    - from: "1098-01-SUMMARY.md commit"
      to: ".planning/REQUIREMENTS.md OOS-01/02/03 traceability rows"
      via: "atomic commit grouping (v1019 TD-13 traceability_flip rule)"
      pattern: "git log -1 --name-only | grep -E '(REQUIREMENTS\\.md|1098-01-SUMMARY\\.md)'"
    - from: "backend/tests/test_ssrf_redirect.py (rewritten test)"
      to: "backend/app/modules/catalog/sources/security.py:70-112 (_revalidate_redirect + make_safe_client)"
      via: "behavioral end-to-end invocation through client._event_hooks['response'] pipeline"
      pattern: "pytest.raises\\(SSRFError\\)"
    - from: "test_layering.py:865 allowlist entry"
      to: "backend/app/modules/catalog/maps/router.py LOC count"
      via: "len(path.read_text().splitlines())"
      pattern: "maps/router\\.py.*: 1(800|850)"
---

<objective>
Retire the 3 long-carried sequential pytest failures (OOS-01 / OOS-02 / OOS-03) so the post-v1023 invariant becomes `sequential failed == 0` **literal** (no OOS rows in the failure summary, not "0 NEW failed"). Test-infra hygiene only — smallest-milestone charter applies per v1023 REQUIREMENTS.md.

Purpose: Close v1019/v1020/v1021/v1022's "0 NEW failures" carry-forward debt by retiring all 3 OOS pins with file-disjoint atomic edits + single shared verify gate (Phase 1096 precedent). Sets up Phase 1099 OAuth parallel-mode stabilization to run against a zero-OOS baseline.

Output:
- 3 retired test-pin failures (1 trim/cap-raise, 1 delete, 1 behavioral rewrite)
- 1098-01-SUMMARY.md with per-OOS root cause + verify-gate measurement table
- Traceability flip in REQUIREMENTS.md (OOS-01/02/03 → `[x]` + `Complete`) — same commit as SUMMARY.md
- Optional: ROADMAP.md backlog promotion (`Phase 999.x: maps/router.py decomposition`) ONLY if OOS-01 fallback path taken
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/1098-oos-triad-closure/1098-CONTEXT.md
@.planning/milestones/v1022-phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md
@.planning/audits/PYTEST-XDIST-PERF-v1020.md
@backend/tests/test_layering.py
@backend/tests/test_phase_275_readme_accuracy.py
@backend/tests/test_ssrf_redirect.py
@backend/app/modules/catalog/sources/security.py
@backend/app/modules/catalog/maps/router.py

<interfaces>
<!-- Confirmed via `git grep -n` + `wc -l` at plan time (2026-05-24). -->
<!-- D-17 REQ citation pinning satisfied — executor reads these directly. -->

OOS-01 pin: backend/tests/test_layering.py:833 — `def test_router_orchestrator_modules_stay_within_loc_cap() -> None:`
OOS-01 target: backend/app/modules/catalog/maps/router.py — currently **1807 LOC** (cap 1800 → need ≤1799 to pass; or raise cap to 1850 via fallback)
OOS-01 allowlist site: backend/tests/test_layering.py:851-867 (the per-file allowlist dict — entry for `maps/router.py` is at line 865)

OOS-02 pin: backend/tests/test_phase_275_readme_accuracy.py:116-134 — `def test_readme_signature_maps_list_intact() -> None:` (entire 19-line function block to delete)
OOS-02 sibling tests (8 total, ALL PASS — do NOT touch): test_readme_natural_earth_count_matches_seed_script, test_readme_api_reference_link_is_external, test_readme_surfaces_examples_manifests_directory, test_readme_documents_cold_build_time, test_readme_python_badge_widened, test_code_of_conduct_has_inline_pledge, test_all_readmes_are_utf8, test_readme_fr_has_accent_marks

OOS-03 pin: backend/tests/test_ssrf_redirect.py:100-108 — `def test_make_safe_client_has_event_hook():` (function block to rewrite)
OOS-03 behavioral pattern to mirror: backend/tests/test_ssrf_redirect.py:22-30 (`test_redirect_to_private_ip_blocked`)
OOS-03 production code (read-only — DO NOT MODIFY): backend/app/modules/catalog/sources/security.py:70-112 (`_revalidate_redirect` + `make_safe_client`)

REQUIREMENTS.md traceability table site: .planning/REQUIREMENTS.md:76-83 (rows for OOS-01/02/03 are at lines 80/81/82 — confirm via `grep -n "^| OOS-" .planning/REQUIREMENTS.md` before edit)

Verify gate commands (CONTEXT.md `<code_context>` verbatim):
  Sequential: `cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/`
  Parallel-4: `cd backend && set -a && source ../.env.test && set +a && uv run pytest -n 4 tests/`
  Parallel-auto (3-run with stale-DB cleanup between runs):
    `docker compose exec -T db psql -U geolens -d geolens -At -c "SELECT datname FROM pg_database WHERE datname LIKE 'geolens%test_gw%' OR datname LIKE 'geolens%test_master%';" | xargs -I{} docker compose exec -T db psql -U geolens -d geolens -c "DROP DATABASE IF EXISTS \"{}\" WITH (FORCE);"`
    `cd backend && set -a && source ../.env.test && set +a && uv run pytest -n auto tests/`  # ×3
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — re-validate pin line numbers + stack health</name>
  <files>(none modified — read/verify only)</files>
  <read_first>
    - backend/tests/test_layering.py (lines 820-885 — confirm `test_router_orchestrator_modules_stay_within_loc_cap` at line 833 + allowlist at lines 851-867)
    - backend/tests/test_phase_275_readme_accuracy.py (full file — confirm `test_readme_signature_maps_list_intact` at line 116-134)
    - backend/tests/test_ssrf_redirect.py (lines 1-108 — confirm `test_make_safe_client_has_event_hook` at line 100-108 + behavioral pattern at lines 22-97)
    - backend/app/modules/catalog/sources/security.py (lines 70-112 — read-only context; do not modify)
  </read_first>
  <action>
Re-validate the 3 OOS test pins via `git grep -n` and confirm `maps/router.py` LOC count (D-17 REQ citation pinning). Run all 4 commands below sequentially and record outputs in scratch — they become evidence rows in T6 SUMMARY.md.

1. `git grep -n "def test_router_orchestrator_modules_stay_within_loc_cap" backend/tests/` — MUST return `backend/tests/test_layering.py:833:`
2. `git grep -n "def test_readme_signature_maps_list_intact" backend/tests/` — MUST return `backend/tests/test_phase_275_readme_accuracy.py:116:`
3. `git grep -n "def test_make_safe_client_has_event_hook" backend/tests/` — MUST return `backend/tests/test_ssrf_redirect.py:100:`
4. `wc -l backend/app/modules/catalog/maps/router.py` — MUST return `1807` (the documented 7-LOC overrun + 1 line spec margin for T2 trim target).

If ANY pin shifted (different line number) OR `maps/router.py` LOC ≠ 1807: STOP and report — context drifted since plan write, planner must re-validate (D-17).

Stack health spot-check:
5. `docker compose ps` — confirm 5 services healthy (api, db, frontend, titiler, worker). If any service is down or unhealthy, attempt `docker compose up -d` ONCE; if still unhealthy after 30s, report and stop (verify gate in T5 needs a live DB).
6. `docker compose exec -T db psql -U geolens -d geolens -c "SELECT 1;"` — confirm DB connectivity (the `.env.test` host-port mapping at `localhost:5434` per MEMORY.md known issue).

Record all 6 outputs verbatim in a scratch buffer; T6 SUMMARY.md will quote them in the "Pre-flight evidence" subsection.
  </action>
  <verify>
    <automated>git grep -n "def test_router_orchestrator_modules_stay_within_loc_cap" backend/tests/ | grep -c "test_layering.py:833"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "def test_router_orchestrator_modules_stay_within_loc_cap" backend/tests/` returns exactly `backend/tests/test_layering.py:833:def test_router_orchestrator_modules_stay_within_loc_cap() -> None:`
    - `git grep -n "def test_readme_signature_maps_list_intact" backend/tests/` returns exactly `backend/tests/test_phase_275_readme_accuracy.py:116:def test_readme_signature_maps_list_intact() -> None:`
    - `git grep -n "def test_make_safe_client_has_event_hook" backend/tests/` returns exactly `backend/tests/test_ssrf_redirect.py:100:def test_make_safe_client_has_event_hook():`
    - `wc -l backend/app/modules/catalog/maps/router.py` returns `1807 backend/app/modules/catalog/maps/router.py`
    - `docker compose ps` shows 5 services in `(healthy)` state
    - `docker compose exec -T db psql -U geolens -d geolens -c "SELECT 1;"` returns a row (exit 0)
  </acceptance_criteria>
  <done>All 6 pre-flight evidence rows captured verbatim in scratch; pin/LOC values match D-17 expectations exactly; 5 docker services healthy.</done>
</task>

<task type="auto">
  <name>Task 2: OOS-01 — trim maps/router.py to ≤1799 LOC (fallback: cap-raise to 1850 + backlog promotion)</name>
  <files>backend/app/modules/catalog/maps/router.py, backend/tests/test_layering.py (only if fallback), .planning/ROADMAP.md (only if fallback)</files>
  <read_first>
    - backend/app/modules/catalog/maps/router.py (full file — survey for trim candidates)
    - backend/tests/test_layering.py:851-867 (allowlist dict — only edited if fallback path)
    - .planning/ROADMAP.md (lines 180-247 — backlog section, only edited if fallback path)
  </read_first>
  <action>
Per D-01 / D-02 / D-03: attempt mechanical trim of `backend/app/modules/catalog/maps/router.py` from 1807 LOC to **≤1799 LOC** (8-line reduction, lands cleanly under the 1800 cap). NO behavior change, NO endpoint changes, NO signature changes, NO import removals that change runtime behavior — purely textual reduction. Per D-03, full facade decomposition is OUT OF SCOPE.

**Trim hunting ground** (per CONTEXT.md `<specifics>`, in lowest-risk order):
1. **Multi-line `from X import (...)` blocks that fit on one line.** Inspect the top imports (lines 8-100). `from fastapi import (APIRouter, Body, Depends, File, HTTPException, Query, Request, Response, UploadFile, status,)` currently spans 12 lines — collapsing to `from fastapi import APIRouter, Body, Depends, File, HTTPException, Query, Request, Response, UploadFile, status` saves ~10 lines if it fits the project line-length budget. **Check ruff/black config first**: `grep -E "line-length|line_length" backend/pyproject.toml backend/ruff.toml 2>/dev/null` to confirm the max-line-length budget (project uses 100 — verify before committing). If a single-line collapse violates the budget, prefer a 3-4 line block form instead of 12-line form.
2. **Long inline docstrings on private helpers.** Grep `^    """` in `maps/router.py` to find function-level docstrings; compress 4-6 line docstrings on internal/private functions to 1-2 lines. PUBLIC endpoint docstrings (the ones that show up in OpenAPI) MUST be preserved unchanged.
3. **Tombstone comments** explaining "Phase XXX did Y" that the code itself already conveys. Search for `# Phase \d+` patterns; remove only comments that restate the surrounding code (NOT comments that document non-obvious invariants — when in doubt, keep).
4. **Trailing-blank-line / leading-blank cleanup** at top/bottom of class methods (very last resort — usually only saves 1-2 lines total).

**Validation loop:**
- After each trim batch, run `wc -l backend/app/modules/catalog/maps/router.py`. Target: ≤1799.
- Once at ≤1799, run `cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/test_layering.py::test_router_orchestrator_modules_stay_within_loc_cap -xvs` to confirm the test passes.
- Then run `cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/test_layering.py -x` to confirm sibling layering tests still pass (no regression on `test_decomposed_service_modules_stay_within_size_budgets` at the file's adjacent test surface).

**Fallback path (D-02) — ONLY if trim cannot land ≤1799 without behavior risk:**
1. Edit `backend/tests/test_layering.py:865` to raise the `maps/router.py` cap from `1800` to `1850`. Replace the existing carve-out comment block (lines 855-864) with this new block ABOVE the allowlist entry:
   ```python
       # Phase 1098 OOS-01 close-gate carve-out (v1023, 2026-05-24):
       #   maps/router.py = 1807 LOC at v1022 close-gate; trim attempt
       #   landed at <NN> LOC (see commit SHA in .planning/phases/
       #   1098-oos-triad-closure/1098-01-SUMMARY.md). Raising cap to 1850
       #   with ~2.4% headroom. THIS IS THE LAST RAISE — if maps/router.py
       #   regrows past 1850, v1024+ MUST decompose the module per
       #   Phase 226 / 238 / 252 facade-pattern (see backlog entry
       #   Phase 999.x in .planning/ROADMAP.md). Test was DESIGNED to force
       #   decomposition; this is the 4th raise (1500→1700→1800→1850).
   ```
2. Promote backlog entry to `.planning/ROADMAP.md`. Insert AFTER the existing `### Phase 999.16:` entry (line 237+ region) and BEFORE the closing horizontal rule. Use this EXACT shape (matches existing backlog formatting at lines 200-246):
   ```markdown
   ### Phase 999.x: maps/router.py decomposition (BACKLOG — P2)

   **Goal:** Decompose backend/app/modules/catalog/maps/router.py (currently ~1850 LOC, last cap raise at v1023 Phase 1098 OOS-01) into facade + cohesive sub-routers per Phase 226 / 238 / 252 facade patterns. Likely split candidates: `maps_crud.py` (create/read/update/delete), `maps_layers.py` (layer CRUD + reordering), `maps_thumbnails.py` (capture/serve), `maps_public.py` (share-link + embed-token surfaces).
   **Source:** v1023 Phase 1098 OOS-01 fallback (test_layering.py:865 cap raised 1800→1850). The test was DESIGNED to force decomposition; the cap has now been raised 4× (1500→1700→1800→1850), eroding test intent.
   **Estimated effort:** 0.5–1 day (refactor + OpenAPI snapshot regen + frontend SDK regen + smoke).
   **Triggered when:** maps/router.py crosses 1850 LOC OR before next major Map Builder phase.

   Plans:

   - [ ] TBD

   ---
   ```

**Decision documentation:** Record in scratch (will go into T6 SUMMARY.md):
- Final LOC count of `maps/router.py` after T2
- Trim technique(s) used (which of #1-4 above)
- Path taken: TRIM-SUCCESS (≤1799) or CAP-RAISE-FALLBACK (1850 + backlog promoted)

Per D-15 autonomous-mode-safe: if trim attempt makes <3 LOC of progress after exhausting #1-2 (the cheapest hunting grounds), abandon trim and take the cap-raise fallback. Do NOT spend more than 30 min in trim mode — cap-raise is the documented fallback for a reason.
  </action>
  <verify>
    <automated>cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/test_layering.py::test_router_orchestrator_modules_stay_within_loc_cap -x 2>&1 | tail -5 | grep -E "(1 passed|PASSED)"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_layering.py::test_router_orchestrator_modules_stay_within_loc_cap -x` exits 0 with "1 passed" in summary
    - EITHER (TRIM path): `wc -l backend/app/modules/catalog/maps/router.py` returns ≤1799 AND `git diff backend/tests/test_layering.py` is empty AND `git diff .planning/ROADMAP.md` is empty
    - OR (CAP-RAISE path): `wc -l backend/app/modules/catalog/maps/router.py` is unchanged from pre-T2 (1807) AND `grep '"backend/app/modules/catalog/maps/router.py": 1850' backend/tests/test_layering.py` returns 1 match AND `grep -c "^### Phase 999.x: maps/router.py decomposition" .planning/ROADMAP.md` returns 1
    - `cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/test_layering.py -x` exits 0 with NO failures in the layering suite (no sibling-test regression)
    - If router.py was modified: `python3 -c "import ast; ast.parse(open('backend/app/modules/catalog/maps/router.py').read())"` returns no syntax errors
  </acceptance_criteria>
  <done>OOS-01 closed — `test_router_orchestrator_modules_stay_within_loc_cap` passes. Path taken (TRIM vs CAP-RAISE) recorded in scratch for SUMMARY.md.</done>
</task>

<task type="auto">
  <name>Task 3: OOS-02 — delete test_readme_signature_maps_list_intact from test_phase_275_readme_accuracy.py</name>
  <files>backend/tests/test_phase_275_readme_accuracy.py</files>
  <read_first>
    - backend/tests/test_phase_275_readme_accuracy.py (full file — confirm function spans lines 116-134 and identify the exact deletion bounds including any leading blank lines)
  </read_first>
  <action>
Per D-04 / D-05 / D-06 / D-07: **Delete** the entire `test_readme_signature_maps_list_intact` function from `backend/tests/test_phase_275_readme_accuracy.py`. The function spans lines 116-134 (19 lines including the `def` line and its body, plus the preceding blank line at 115 if it groups with this function — confirm via Read before editing).

**Exact deletion bounds** (verify via Read first):
- Start: the blank line BEFORE line 116 (the one separating this function from `test_readme_fr_has_accent_marks` above) — typically line 115
- End: the last line of the function body (line 134, which is `f"Signature-map list shrunk: expected >=9 bullets, found {len(bullets)}"` immediately followed by closing `)`)
- Inclusive: 19-20 lines total

Use the Edit tool. Match the exact text from the source (Read it first to ensure exact whitespace + quote characters).

**Critical constraints:**
- D-05: NO residual comment in the test file explaining the removal. The rationale lives ONLY in 1098-01-SUMMARY.md (T6). Do NOT leave a comment like `# Removed 2026-05-24 — signature-stories section retired in commit 4a7d1a29`.
- D-06: NO README content change. Do NOT touch `README.md`, `README.es.md`, `README.fr.md`, or `README.de.md`. The signature-stories section was intentionally removed in commit `4a7d1a29` ("chore: remove demo overlay apparatus", 2026-05-22) — restoring it would be a doc-lying regression.
- D-07: NO sibling-test sweep. The 8 other tests in this file (`test_readme_natural_earth_count_matches_seed_script`, `test_readme_api_reference_link_is_external`, `test_readme_surfaces_examples_manifests_directory`, `test_readme_documents_cold_build_time`, `test_readme_python_badge_widened`, `test_code_of_conduct_has_inline_pledge`, `test_all_readmes_are_utf8`, `test_readme_fr_has_accent_marks`) ALL PASS per discuss-time spot-check. Do NOT touch them.

**Validation:**
- Post-delete file length should be ≤115 lines (was 134 lines; 19-line function removed; final newline character counted as a line by `wc -l`).
- Run `cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/test_phase_275_readme_accuracy.py -x` — MUST report 8 passed, 0 failed (the 8 sibling tests). The deleted test is no longer collected.
- Run `git grep -c "def test_readme_signature_maps_list_intact" backend/tests/` — MUST return 0.
- Verify `import re` and `from pathlib import Path` are still used by sibling tests (they ARE — sibling tests use both); do NOT remove imports as a side effect.
  </action>
  <verify>
    <automated>cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/test_phase_275_readme_accuracy.py -x 2>&1 | tail -5 | grep -E "8 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -c "def test_readme_signature_maps_list_intact" backend/tests/` returns 0
    - `git grep -c "def test_readme" backend/tests/test_phase_275_readme_accuracy.py` returns 6 (the 6 remaining `test_readme_*` functions; original count was 7)
    - `grep -c "^def test_" backend/tests/test_phase_275_readme_accuracy.py` returns 8 (total tests in file: 7 `test_readme_*` minus 1 deleted = 6, plus `test_code_of_conduct_has_inline_pledge` + `test_all_readmes_are_utf8` = 8)
    - `cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/test_phase_275_readme_accuracy.py -x` exits 0 with "8 passed" in summary
    - `git diff README.md README.es.md README.fr.md README.de.md` returns empty (no README content edits per D-06)
    - `python3 -c "import ast; ast.parse(open('backend/tests/test_phase_275_readme_accuracy.py').read())"` returns no syntax errors
  </acceptance_criteria>
  <done>OOS-02 closed — `test_readme_signature_maps_list_intact` removed; 8 sibling tests in the file still pass; READMEs untouched.</done>
</task>

<task type="auto">
  <name>Task 4: OOS-03 — rewrite test_make_safe_client_has_event_hook as behavioral SSRF-contract test</name>
  <files>backend/tests/test_ssrf_redirect.py</files>
  <read_first>
    - backend/tests/test_ssrf_redirect.py (lines 1-108 full — must reuse existing imports + understand sibling `test_redirect_to_private_ip_blocked` pattern at lines 22-30)
    - backend/app/modules/catalog/sources/security.py (lines 70-112 — read-only context for production contract semantics)
  </read_first>
  <action>
Per D-08 / D-09 / D-10 / D-11: Replace the identity-check test at `backend/tests/test_ssrf_redirect.py:100-108` with a **behavioral SSRF-contract test** that exercises `make_safe_client()`'s `_event_hooks["response"]` pipeline end-to-end. The new test is immune to module-level `mock.patch` contamination because it tests BEHAVIOR (does an SSRF redirect get rejected?) instead of WIRING (is the function identity in a list?).

**Exact replacement** — use the Edit tool to swap lines 100-108 with this block:

```python
@pytest.mark.anyio
async def test_make_safe_client_blocks_private_ip_redirect():
    """make_safe_client returns a client whose hook pipeline rejects SSRF redirects.

    Behavioral test for the SSRF revalidation contract — supersedes the
    pre-2026-05-24 identity check (which was brittle to module-level
    mock.patch contamination from sibling tests). v1023 Phase 1098 OOS-03.
    """
    client = make_safe_client()
    # Sanity: the response-hook pipeline is wired (non-empty)
    response_hooks = client._event_hooks.get("response", [])
    assert response_hooks, "make_safe_client must wire at least one response hook"
    # Behavioral: a 302 -> 127.0.0.1 redirect must be rejected end-to-end
    response = httpx.Response(
        302,
        headers={"Location": "http://127.0.0.1/internal"},
        request=httpx.Request("GET", "https://attacker.example/redirect"),
    )
    with pytest.raises(SSRFError):
        for hook in response_hooks:
            await hook(response)
    # Constructor-arg checks (not patchable — safe to retain per D-11)
    assert client.follow_redirects is True
    assert client.max_redirects == 5
```

**Why this shape:**
- Mirrors the existing behavioral pattern at lines 22-30 (`test_redirect_to_private_ip_blocked`) — uses the same `httpx.Response(302, headers={"Location": "..."}, request=httpx.Request("GET", "..."))` construction.
- Iterates `client._event_hooks["response"]` and awaits each hook against the constructed response — this is the SAME path that production `httpx.AsyncClient` takes when receiving a 3xx response, so we exercise the contract end-to-end.
- D-09: Tests CONTRACT (refuses 3xx → private IP), not WIRING (specific function in list). Immune to any sibling test that does `mock.patch("app.modules.catalog.sources.security.validate_url_for_ssrf", ...)` and forgets to restore — even if `_revalidate_redirect` is monkey-patched, the test verifies the hook still blocks SSRF.
- D-11: Constructor-arg assertions (`follow_redirects`, `max_redirects`) RETAINED — these check kwargs passed to `httpx.AsyncClient.__init__`, which are not patchable post-construction, so they're not flake-prone.
- D-10: Do NOT hunt for the leaker — the defensive rewrite makes the leaker irrelevant.

**Constraints:**
- Imports at lines 11-18 (`httpx`, `pytest`, `from app.modules.catalog.sources.security import SSRFError, _revalidate_redirect, make_safe_client`) — KEEP all 3 (sibling tests at lines 22-97 still use `_revalidate_redirect` directly). Do NOT remove `_revalidate_redirect` from the import block.
- The `@pytest.mark.anyio` marker is REQUIRED (sibling tests have it; the test uses `await`).
- Function name CHANGES: `test_make_safe_client_has_event_hook` → `test_make_safe_client_blocks_private_ip_redirect`. This is intentional — the new name reflects the behavioral contract, not the wiring detail. Phase 1099/1100 close-gate language ("OOS-03 closed") references the OLD pin name in evidence chains; 1098-01-SUMMARY.md MUST cross-reference both the old name (deleted) and the new name (added) to preserve traceability.

**Validation:**
- Run `cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/test_ssrf_redirect.py -xvs` — MUST report 7 passed (the 6 original behavioral tests at lines 22-97 + the 1 new behavioral test). The original `test_make_safe_client_has_event_hook` no longer exists.
- Run `git grep -c "def test_make_safe_client_has_event_hook" backend/tests/` — MUST return 0.
- Run `git grep -c "def test_make_safe_client_blocks_private_ip_redirect" backend/tests/` — MUST return 1.
- Cross-check that the 6 sibling tests at lines 22-97 are UNCHANGED: `git diff backend/tests/test_ssrf_redirect.py | grep -E "^-.*def test_(redirect_to_private_ip_blocked|redirect_to_link_local_blocked|redirect_to_public_allowed|relative_redirect_resolution|non_http_scheme_redirect_blocked|non_redirect_status_passthrough)"` returns NO matches (none of the 6 siblings was touched).
  </action>
  <verify>
    <automated>cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/test_ssrf_redirect.py -x 2>&1 | tail -5 | grep -E "7 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -c "def test_make_safe_client_has_event_hook" backend/tests/` returns 0 (old pin deleted)
    - `git grep -c "def test_make_safe_client_blocks_private_ip_redirect" backend/tests/` returns 1 (new pin added)
    - `grep -c "^def test_" backend/tests/test_ssrf_redirect.py` returns 7 (6 original behavioral + 1 new behavioral)
    - `cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/test_ssrf_redirect.py -x` exits 0 with "7 passed" in summary
    - `python3 -c "import ast; ast.parse(open('backend/tests/test_ssrf_redirect.py').read())"` returns no syntax errors
    - `git diff backend/tests/test_ssrf_redirect.py | grep -E "^-.*def test_(redirect_to_private_ip_blocked|redirect_to_link_local_blocked|redirect_to_public_allowed|relative_redirect_resolution|non_http_scheme_redirect_blocked|non_redirect_status_passthrough)"` returns 0 matches (sibling tests untouched)
    - `git diff backend/app/modules/catalog/sources/security.py` returns empty (NO production-code change per D-08; SSRF posture is correct)
  </acceptance_criteria>
  <done>OOS-03 closed — old identity-check test replaced with behavioral SSRF-contract test; 6 sibling tests untouched; production security.py unmodified.</done>
</task>

<task type="auto">
  <name>Task 5: Verify gate — sequential + -n 4 + -n auto 3-run (literal failed == 0)</name>
  <files>(none modified — measurement only; outputs captured for T6 SUMMARY.md)</files>
  <read_first>
    - .planning/audits/PYTEST-XDIST-PERF-v1020.md (Section 1: stale-DB cleanup recipe; Section 2: flake taxonomy for `-n auto` cascade expectations)
    - .planning/milestones/v1022-phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md (the v1022 baseline format — measurement table shape for SUMMARY.md)
  </read_first>
  <action>
Single shared measurement gate per D-12 / D-13 / D-14. Wallclock budget: ~9 min sequential + ~6 min `-n 4` + ~22 min `-n auto` 3-run ≈ **~37 min total** (single-gate amortized; alternative 3× per-OOS gate cost would be ~111 min).

**Pre-condition:** T2 (OOS-01), T3 (OOS-02), T4 (OOS-03) have all completed. Do NOT proceed if any of those tasks reported a non-zero exit on their per-target validation. The verify gate is the GROUP measurement.

**Run 1: Sequential pytest** (expect `3063+ passed / 0 failed / 38 skipped`)

```bash
cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/ 2>&1 | tee /tmp/1098-verify-seq.log | tail -10
```

Capture the final summary line (e.g., `===== 3063 passed, 38 skipped in 532.18s (0:08:52) =====`). MUST contain `0 failed` LITERAL — no OOS rows. If `failed != 0`, STOP and investigate which test failed (likely a sibling-regression from T2/T3/T4). Per D-16: sequential `failed == 0` is NON-NEGOTIABLE.

**Run 2: -n 4 parallel** (expect `3063+ passed / 0 failed / 38 skipped` — note: OAUTH-01/02 flakes are Phase 1099 scope; if they surface here, they're OAUTH carry-forward not OOS regression)

```bash
cd backend && set -a && source ../.env.test && set +a && uv run pytest -n 4 tests/ 2>&1 | tee /tmp/1098-verify-n4.log | tail -10
```

Capture summary line. Expected: `0 failed` LITERAL (because OOS triad retired). If OAUTH-01 or OAUTH-02 surface as `-n 4` flakes (per v1022 baseline 3059/4 OOS+oauth/38), record them as carry-forward to Phase 1099 — they're NOT OOS regressions. The KEY check: ZERO failures that match the OOS pin names (`test_router_orchestrator_modules_stay_within_loc_cap`, `test_readme_signature_maps_list_intact`, `test_make_safe_client_has_event_hook`).

**Run 3: -n auto 3-run sequence** (PARA-01 invariant preservation — ≤30 distinct (failed+errors) per run + 0 `InvalidCatalogNameError` cascade frames)

Stale-DB cleanup before EACH of the 3 runs (per PYTEST-XDIST-PERF-v1020.md §1):

```bash
# Cleanup script (run before each -n auto invocation):
docker compose exec -T db psql -U geolens -d geolens -At -c \
  "SELECT datname FROM pg_database WHERE datname LIKE 'geolens%test_gw%' OR datname LIKE 'geolens%test_master%';" \
  | xargs -I{} docker compose exec -T db psql -U geolens -d geolens -c "DROP DATABASE IF EXISTS \"{}\" WITH (FORCE);"
```

Then run pytest 3× with cleanup between:

```bash
# Run A
<cleanup script>
cd backend && set -a && source ../.env.test && set +a && uv run pytest -n auto tests/ 2>&1 | tee /tmp/1098-verify-auto-A.log | tail -10

# Run B
<cleanup script>
cd backend && set -a && source ../.env.test && set +a && uv run pytest -n auto tests/ 2>&1 | tee /tmp/1098-verify-auto-B.log | tail -10

# Run C
<cleanup script>
cd backend && set -a && source ../.env.test && set +a && uv run pytest -n auto tests/ 2>&1 | tee /tmp/1098-verify-auto-C.log | tail -10
```

For each run, extract:
- Summary line (total passed/failed/errors/skipped)
- Count of `InvalidCatalogNameError` lines: `grep -c "InvalidCatalogNameError" /tmp/1098-verify-auto-{A,B,C}.log` — MUST be 0 for each (PARA-01 invariant preserved per v1022 Phase 1097)
- Distinct (failed+errors) per run: this is the GATE — ≤30 per run is the PARA-01 acceptance criterion

Build a measurement table (will go into T6 SUMMARY.md):

```
| Run | Mode | Passed | Failed | Errors | Skipped | Distinct (F+E) | ICN frames |
|-----|------|--------|--------|--------|---------|----------------|------------|
| Seq | pytest | 3063+ | 0 | 0 | 38 | 0 | n/a |
| P4 | -n 4 | 3063+ | 0 (or 0..4 OAUTH carryforward) | 0 | 38 | 0..4 | n/a |
| Auto-A | -n auto | ?? | ?? | ?? | ?? | ≤30 | 0 |
| Auto-B | -n auto | ?? | ?? | ?? | ?? | ≤30 | 0 |
| Auto-C | -n auto | ?? | ?? | ?? | ?? | ≤30 | 0 |
```

**Hard gates** (per D-16 + must_haves):
- Sequential `failed == 0` LITERAL — no exceptions, no waivers, no "0 NEW"
- `-n 4` failed count = 0 OR only OAUTH-01/02 (Phase 1099 scope; OOS retired)
- `-n auto` ≤30 distinct per run AND `InvalidCatalogNameError` count = 0 per run

If ANY hard gate fails, STOP and investigate. Do NOT proceed to T6 until all gates pass. Per D-16, sequential `failed == 0` is non-negotiable.

**Note on `.env.test` host-port mapping** (MEMORY.md known issue): `backend/tests` requires `set -a && source ../.env.test && set +a` BEFORE `uv run pytest`; otherwise tests fail with `InvalidCatalogNameError: database "geolens_test_master_..." does not exist`. The verify-gate commands above include this prefix verbatim — do NOT drop it.
  </action>
  <verify>
    <automated>grep -E "(0 failed|passed.*0 failed)" /tmp/1098-verify-seq.log | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/1098-verify-seq.log` final summary line contains `0 failed` LITERAL (sequential — non-negotiable per D-16)
    - `/tmp/1098-verify-n4.log` final summary line contains EITHER `0 failed` OR only `test_callback_missing_state_returns_error` / `test_callback_invalid_code_returns_error` in the failure list (OAUTH carry-forward to Phase 1099 — not OOS regression)
    - Neither `/tmp/1098-verify-n4.log` nor `/tmp/1098-verify-seq.log` contains any of the 3 OOS pin names in failure summaries: `grep -E "(test_router_orchestrator_modules_stay_within_loc_cap|test_readme_signature_maps_list_intact|test_make_safe_client_has_event_hook)" /tmp/1098-verify-{seq,n4}.log | grep -i fail | wc -l` returns 0
    - For each of `/tmp/1098-verify-auto-A.log`, `/tmp/1098-verify-auto-B.log`, `/tmp/1098-verify-auto-C.log`: `grep -c "InvalidCatalogNameError" {file}` returns 0 (PARA-01 cascade invariant preserved)
    - For each of `/tmp/1098-verify-auto-A.log`, `/tmp/1098-verify-auto-B.log`, `/tmp/1098-verify-auto-C.log`: distinct (failed+errors) ≤ 30 per run (extracted from summary line)
    - Measurement table assembled in scratch with all 5 rows populated; ready for T6 SUMMARY.md write
  </acceptance_criteria>
  <done>Verify gate passed — sequential `failed == 0` literal + `-n 4` failed count is 0 or OAUTH-carry-forward-only + `-n auto` 3-run shows ≤30 distinct per run + 0 ICN frames. Measurement table assembled.</done>
</task>

<task type="auto">
  <name>Task 6: SUMMARY.md write + REQUIREMENTS.md traceability flip + atomic commit (+ optional ROADMAP backlog if fallback)</name>
  <files>.planning/phases/1098-oos-triad-closure/1098-01-SUMMARY.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md (only if T2 fallback taken)</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 76-83 — Traceability table; locate the OOS-01/02/03 rows)
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY template shape — if absent, use the structure below)
    - Scratch buffers from T1 (pre-flight evidence), T2 (OOS-01 path taken), T3 (OOS-02 delete confirmation), T4 (OOS-03 rewrite confirmation), T5 (measurement table)
  </read_first>
  <action>
Write `1098-01-SUMMARY.md` and flip REQUIREMENTS.md traceability rows + (conditional) promote ROADMAP backlog entry, then commit ALL planning + production diffs in a SINGLE atomic commit per D-18 / v1019 TD-13 `requirements_traceability_flip` rule.

**Step 1: Write `.planning/phases/1098-oos-triad-closure/1098-01-SUMMARY.md`**

Structure (use this exact section layout):

```markdown
# Phase 1098 Plan 01 — OOS Triad Closure SUMMARY

**Completed:** 2026-05-24
**Phase:** 1098 OOS Triad Closure (v1023)
**Plan:** 1098-01
**Status:** Complete
**Requirements closed:** OOS-01, OOS-02, OOS-03

## Goal Achieved

Sequential pytest baseline now `failed == 0` **literal** (no OOS rows). v1022's
"0 NEW failed" invariant retired in favor of the strict literal-zero state per
v1023 HARD INVARIANT.

## What Shipped

### OOS-01 — `test_router_orchestrator_modules_stay_within_loc_cap` (test_layering.py:833)

[Describe the path taken — TRIM or CAP-RAISE. If TRIM: list trim techniques used,
final LOC count, behavior-preservation confirmation. If CAP-RAISE: document the
1800→1850 raise, the carve-out comment block at test_layering.py:865, the
backlog promotion to ROADMAP.md Phase 999.x.]

**Path:** [TRIM-SUCCESS at NN LOC | CAP-RAISE-FALLBACK to 1850 + backlog promoted]

### OOS-02 — `test_readme_signature_maps_list_intact` (test_phase_275_readme_accuracy.py:116)

Removed 2026-05-24 — the API-04 / M-23 invariant this pinned (themed-demo
signature-maps section in README) was retired in commit `4a7d1a29` ("chore:
remove demo overlay apparatus", 2026-05-22) along with the themed-demo docker
overlay, 9 themed map fixtures, and the `GEOLENS_DEMO_MODE` flag. The signature-
stories README section is gone by design — restoring it would be a doc-lying
regression. 8 sibling tests in the same file still pin load-bearing README
invariants (utf8, FR accents, badge, cold-build-time, examples-manifests
directory, natural-earth count, api-reference link, code-of-conduct pledge)
and all 8 still pass.

### OOS-03 — `test_make_safe_client_has_event_hook` (test_ssrf_redirect.py:100)

Replaced the identity check (`_revalidate_redirect in client._event_hooks["response"]`)
with a behavioral SSRF-contract test
(`test_make_safe_client_blocks_private_ip_redirect`). The new test exercises the
client's response-hook pipeline end-to-end by driving a 302 → 127.0.0.1 redirect
through `client._event_hooks["response"]` and asserting `SSRFError` is raised.

**Root-cause** (documented for posterity): the original test checked WIRING
(function identity in a list) instead of BEHAVIOR (does an SSRF redirect get
rejected?). Identity checks are brittle to module-level `mock.patch` of
`app.modules.catalog.sources.security.*` from sibling test files — under
sequential pytest the contamination accumulates, under `-n 4` the order is
nondeterministic ("did not fire" flake-class per PYTEST-XDIST-PERF-v1020.md §2).
Production code at `backend/app/modules/catalog/sources/security.py:70-112` is
correct — `make_safe_client()` still wires `_revalidate_redirect` into
`event_hooks`. SSRF posture has NOT changed; no production-code modification.

**Leaker hunt deferred** (D-10): bisecting 70+ test files to find the
contaminating sibling is high-effort, low-value now that the defensive rewrite
addresses the symptom permanently. Parked indefinitely.

## Pre-flight Evidence (T1)

[Quote verbatim outputs from T1's `git grep` + `wc -l` + `docker compose ps`]

## Verify Gate Evidence (T5)

| Run | Mode | Passed | Failed | Errors | Skipped | Distinct (F+E) | ICN frames |
|-----|------|--------|--------|--------|---------|----------------|------------|
| Seq | pytest | NN | 0 | 0 | 38 | 0 | n/a |
| P4 | -n 4 | NN | N (oauth carryforward to Phase 1099 only) | 0 | 38 | N | n/a |
| Auto-A | -n auto | NN | NN | NN | NN | ≤30 | 0 |
| Auto-B | -n auto | NN | NN | NN | NN | ≤30 | 0 |
| Auto-C | -n auto | NN | NN | NN | NN | ≤30 | 0 |

Sequential summary line (verbatim from /tmp/1098-verify-seq.log):
```
[paste final summary line]
```

-n 4 summary line (verbatim):
```
[paste final summary line]
```

## Files Touched

| File | Change | LOC delta |
|------|--------|-----------|
| backend/app/modules/catalog/maps/router.py | [TRIM: ${N} lines removed via ${technique}] OR [unchanged - cap-raise path] | ${delta} |
| backend/tests/test_layering.py | [unchanged - trim path] OR [cap raised 1800→1850 + carve-out comment] | [0 or +N] |
| backend/tests/test_phase_275_readme_accuracy.py | test_readme_signature_maps_list_intact deleted | -19 |
| backend/tests/test_ssrf_redirect.py | test_make_safe_client_has_event_hook rewritten as test_make_safe_client_blocks_private_ip_redirect | +12 (approx; 9 old → ~21 new) |
| backend/app/modules/catalog/sources/security.py | unchanged (read-only context per D-08) | 0 |
| .planning/REQUIREMENTS.md | OOS-01/02/03 traceability rows flipped Pending → Complete | 0 (cell updates) |
| .planning/ROADMAP.md | [unchanged - trim path] OR [Phase 999.x backlog entry promoted] | [0 or +9] |
| .planning/phases/1098-oos-triad-closure/1098-01-SUMMARY.md | new | +${N} |

## Hard Gates Met

- [x] D-16 Sequential pytest `failed == 0` **literal** (no OOS rows, not "0 NEW")
- [x] PARA-01 invariant preserved: `-n auto` 3-run ≤30 distinct (F+E) per run + 0 ICN frames
- [x] D-17 REQ citation pinning re-validated at T1 (all 3 OOS pin paths confirmed)
- [x] D-18 Traceability flip atomic with this SUMMARY.md write

## Carry-forward to Phase 1099

- OAUTH-01: `test_callback_missing_state_returns_error` `-n 4` flake
- OAUTH-02: `test_callback_invalid_code_returns_error` `-n 4` flake

These surfaced in T5's `-n 4` run (if applicable) as expected per v1022 close-gate baseline; they are Phase 1099 scope, not OOS regressions.

## Patterns Established / Reinforced

- **Single-plan / N-task / 1-verify-gate shape** for hygiene-milestone closures (matches Phase 1096 precedent). 37 min single gate vs ~111 min 3× split gate.
- **Behavioral over identity checks** for SSRF test family — wiring assertions (`X in list`) are brittle to module-level monkey-patching; behavioral assertions (`raises SSRFError`) are not.
- **Carve-out cap-raise allowlist pattern** (D-02 fallback) — `test_layering.py:851-867` style, with HARD ceiling comment + backlog promotion. Pre-committed last-raise language to force decomposition next time.
- **D-10 leaker-hunt-deferred** — defensive rewrites can permanently retire brittle tests without requiring root-cause investigation, when the cost/value math favors symptom removal.
```

Fill in all `[bracketed]` placeholders with actual values from T1-T5 scratch outputs.

**Step 2: Flip REQUIREMENTS.md traceability rows**

Edit `.planning/REQUIREMENTS.md` lines 80-82 (the Traceability table). For each of OOS-01, OOS-02, OOS-03:
- Find the row: `| OOS-01 | Phase 1098 | Pending |`
- Replace with: `| OOS-01 | Phase 1098 | Complete |`
- Repeat for OOS-02 + OOS-03.

ALSO flip the unchecked boxes in the requirement definitions:
- Line 28 (OOS-01): `- [ ] **OOS-01**:` → `- [x] **OOS-01**:`
- Line 30 (OOS-02): `- [ ] **OOS-02**:` → `- [x] **OOS-02**:`
- Line 32 (OOS-03): `- [ ] **OOS-03**:` → `- [x] **OOS-03**:`

(Verify exact line numbers via `grep -n "^- \[ \] \*\*OOS-" .planning/REQUIREMENTS.md` before edit — lines may shift if other edits landed since plan write.)

ALSO flip the unchecked boxes in ROADMAP.md Phase 1098 row at line 93:
- `- [ ] **Phase 1098: OOS Triad Closure**` → `- [x] **Phase 1098: OOS Triad Closure**`
AND flip the plan checkbox at line 111:
- `- [ ] 1098-01: Diagnose + fix OOS triad (test_layering / test_phase_275_readme_accuracy / test_ssrf_redirect)` → `- [x] 1098-01: ...`

**Step 3: Atomic commit**

Per D-18 + v1019 TD-13: SUMMARY.md write + REQUIREMENTS.md flip + (conditional) ROADMAP edit + all 4 backend file changes (router.py, test_layering.py if fallback, test_phase_275_readme_accuracy.py, test_ssrf_redirect.py) MUST land in ONE commit.

Commit message (use HEREDOC):
```bash
git add backend/app/modules/catalog/maps/router.py \
        backend/tests/test_layering.py \
        backend/tests/test_phase_275_readme_accuracy.py \
        backend/tests/test_ssrf_redirect.py \
        .planning/REQUIREMENTS.md \
        .planning/ROADMAP.md \
        .planning/phases/1098-oos-triad-closure/1098-01-SUMMARY.md

git commit -m "$(cat <<'EOF'
docs(1098): close OOS-01/02/03 — sequential failed == 0 literal

OOS-01 (test_layering.py:833): [TRIM | CAP-RAISE] — maps/router.py [trimmed to NN LOC | cap raised 1800→1850 + Phase 999.x backlog promoted]
OOS-02 (test_phase_275_readme_accuracy.py:116): test_readme_signature_maps_list_intact removed (signature-stories README section retired in commit 4a7d1a29, 2026-05-22)
OOS-03 (test_ssrf_redirect.py:100): test_make_safe_client_has_event_hook rewritten as test_make_safe_client_blocks_private_ip_redirect — behavioral SSRF-contract test immune to module-level mock.patch contamination

Verify gate (T5):
- Sequential: NN passed / 0 failed / 38 skipped — failed == 0 LITERAL
- -n 4: NN passed / [0 | N OAUTH-carry-forward] / 38 skipped
- -n auto 3-run: ≤30 distinct (F+E) per run + 0 ICN frames (PARA-01 preserved)

Closes: OOS-01, OOS-02, OOS-03 (v1023 REQUIREMENTS.md traceability rows flipped to Complete)
EOF
)"
```

Replace `[TRIM | CAP-RAISE]` and `[trimmed to NN LOC | cap raised 1800→1850 + Phase 999.x backlog promoted]` and `[0 | N OAUTH-carry-forward]` with actual values from scratch.

**Step 4: Verify atomic-commit invariant**

```bash
git log -1 --name-only
```

Output MUST include ALL of:
- `.planning/phases/1098-oos-triad-closure/1098-01-SUMMARY.md`
- `.planning/REQUIREMENTS.md`
- `backend/tests/test_phase_275_readme_accuracy.py`
- `backend/tests/test_ssrf_redirect.py`
- `backend/app/modules/catalog/maps/router.py` (if trim path) OR `backend/tests/test_layering.py` (if cap-raise path)
- `.planning/ROADMAP.md` (always — Phase 1098 checkbox flip; entry added if cap-raise path)

If any required file is missing from the commit, this VIOLATES v1019 TD-13 + D-18 atomic-flip rule. Per the rule, this requires a NEW commit (NOT an amend) — see global GSD skill files. Amend is only valid when no hook fails AND no work would be lost.

**No CHANGELOG entry per CONTEXT.md `<specifics>` out-of-scope** — that's Phase 1100 CLOSE-01 scope.
  </action>
  <verify>
    <automated>git log -1 --name-only --pretty=format: | grep -E "(1098-01-SUMMARY|REQUIREMENTS|test_phase_275_readme_accuracy|test_ssrf_redirect|ROADMAP)" | wc -l | xargs -I{} test {} -ge 5 && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/phases/1098-oos-triad-closure/1098-01-SUMMARY.md`
    - `grep -c "^| OOS-01 | Phase 1098 | Complete |" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^| OOS-02 | Phase 1098 | Complete |" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^| OOS-03 | Phase 1098 | Complete |" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[x\] \*\*OOS-01\*\*:" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[x\] \*\*OOS-02\*\*:" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[x\] \*\*OOS-03\*\*:" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[x\] \*\*Phase 1098: OOS Triad Closure\*\*" .planning/ROADMAP.md` returns 1
    - `git log -1 --name-only --pretty=format:` includes BOTH `1098-01-SUMMARY.md` AND `REQUIREMENTS.md` in the same commit (D-18 atomic-flip invariant)
    - If T2 took CAP-RAISE path: `grep -c "^### Phase 999.x: maps/router.py decomposition" .planning/ROADMAP.md` returns 1 AND the same commit touches `.planning/ROADMAP.md` (backlog promoted atomically)
    - Commit message contains the substring `Closes: OOS-01, OOS-02, OOS-03`
  </acceptance_criteria>
  <done>SUMMARY.md written with all evidence; REQUIREMENTS.md traceability rows + checkboxes flipped; ROADMAP.md Phase 1098 checkbox flipped; all changes committed atomically; D-18 atomic-flip invariant verified.</done>
</task>

</tasks>

<verification>
## Phase-Level Verification

After all 6 tasks complete, the following hold:

1. **Sequential `failed == 0` literal** — `pytest tests/` summary line shows `0 failed` (NO OOS rows). Non-negotiable per D-16.
2. **`-n 4` failed count is 0 OR only OAUTH-01/02 carry-forward** — the 3 OOS pins are gone from the failure summary; OAUTH flakes (if present) are Phase 1099 scope.
3. **`-n auto` 3-run shows ≤30 distinct (F+E) per run + 0 `InvalidCatalogNameError` frames** — PARA-01 invariant preserved (v1022 close-gate carry).
4. **All 3 OOS test pins resolved**:
   - OOS-01: `test_router_orchestrator_modules_stay_within_loc_cap` passes (trim OR cap-raise path)
   - OOS-02: `test_readme_signature_maps_list_intact` no longer exists (deleted)
   - OOS-03: `test_make_safe_client_has_event_hook` no longer exists; `test_make_safe_client_blocks_private_ip_redirect` passes (behavioral rewrite)
5. **REQUIREMENTS.md traceability + checkboxes flipped atomically with 1098-01-SUMMARY.md** — D-18 / v1019 TD-13 satisfied.
6. **ROADMAP.md Phase 1098 + plan checkboxes flipped** — same commit (D-18 atomic-flip extends to ROADMAP).
7. **Optional ROADMAP backlog promotion** if cap-raise path — `Phase 999.x: maps/router.py decomposition` entry exists.
8. **Zero production-code surface change** beyond OOS-01's optional trim — `git diff backend/app/modules/catalog/sources/` returns empty (D-08 SSRF posture unchanged).
9. **Zero README content change** — `git diff README.md README.es.md README.fr.md README.de.md` empty (D-06).
10. **8 sibling tests in test_phase_275_readme_accuracy.py still pass** (D-07 no sibling sweep).
11. **6 sibling tests in test_ssrf_redirect.py (lines 22-97) still pass** (no behavioral-test regression).
</verification>

<success_criteria>
- Plan 1098-01 closes OOS-01, OOS-02, OOS-03 atomically per D-12 / D-13 / D-14
- Sequential `pytest tests/` returns `failed == 0` literal (D-16 hard invariant)
- `-n 4` and `-n auto` measurements captured in 1098-01-SUMMARY.md per PARA-01 invariant preservation requirement
- 1098-01-SUMMARY.md + REQUIREMENTS.md flip + (conditional) ROADMAP backlog land in ONE commit (D-18)
- Zero scope creep beyond CONTEXT.md `<specifics>` out-of-scope list (no OAuth fix, no CI live-verify, no security.py refactor, no README edits, no sibling test sweep, no CHANGELOG [1.5.8] entry)
- Phase 1099 starts against a zero-OOS sequential baseline (so its OAuth `-n 4` measurements are unambiguous)
</success_criteria>

<output>
Create `.planning/phases/1098-oos-triad-closure/1098-01-SUMMARY.md` when done (per T6).
</output>
