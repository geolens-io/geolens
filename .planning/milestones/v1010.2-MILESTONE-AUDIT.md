---
milestone: v1010.2
milestone_name: Builder Smoke Carryover
audited: 2026-05-17
status: passed
scores:
  requirements: 5/5
  phases: 1/1
  integration: N/A (single-phase hygiene close)
  flows: N/A (no new user flows; only existing builder surface)
gaps: {}
tech_debt:
  - phase: 1050-builder-smoke-carryover
    items:
      - "IN-01 (Info, deferred per scope): `__resetThumbnailDebounceForTests` name no longer scoped to just the debounce; cosmetic"
      - "IN-02 (Info, deferred per scope): no DEM-keeps-per-layer-source test assertion (cluster + raster paths still correct, just under-tested)"
      - "IN-03 (Info, deferred per scope): misleading reference-count comment in `use-builder-layers.ts`"
      - "Pre-existing: `LayerEditorPanel.tsx:413,694` 2 typecheck errors (carry-forward from before v1010.2; out of scope)"
      - "Pre-existing: 4 TS6133 unused-var warnings in test files (carry-forward; out of scope)"
nyquist:
  compliant_phases: 0
  partial_phases: 0
  missing_phases: 1
  overall: not_applicable
  rationale: "Hygiene-close milestone — no new behavior to validate. Nyquist-style validation strategy was explicitly scoped out per CONTEXT.md (`No research dimension. No AI dimension. No UI design contract. This is a backend/frontend bug-fix close-out — code-level only`)."
---

# Milestone v1010.2 Audit — Builder Smoke Carryover

**Status:** ✅ PASSED
**Phases:** 1/1 complete (Phase 1050)
**Requirements:** 5/5 satisfied (SMOKE-08..12)
**Audited:** 2026-05-17

## Summary

v1010.2 closes the 5 carried-forward smoke findings from v1010.1's 2026-05-17 Playwright MCP smoke check. Single-phase hygiene close, sequential plans, single CTRL-01 batch gate. All 5 SF closures shipped, plus 7 secondary code-review findings (3 BLOCKER + 4 WARNING) caught and fixed inline before this audit.

## Phase Coverage

| Phase | Plans | Verification | Notes |
|-------|-------|--------------|-------|
| 1050 builder-smoke-carryover | 6/6 complete | ✅ passed (11/11 must-haves) | 5 SF closures + 1 CTRL-01 close gate |

## Requirements Coverage (5/5)

| REQ-ID | Plan | Status | Evidence |
|--------|------|--------|----------|
| SMOKE-08 (SF-04 dedupe MapLibre sources) | 1050-01 | ✅ satisfied | `getSourceIdForLayer()` helper at `map-sync.ts:374`; 24 unique tile URLs for 2-dataset map confirmed via Playwright MCP; cluster sources preserved per-layer. Commits `cab57a32`, `c1c84cc7`. Post-shipping leak fixes: `516c9ae5` (CR-01), `fd149688` (CR-02), `8b791a08` (WR-01). |
| SMOKE-09 (SF-05 blob revoke timing) | 1050-02 | ✅ satisfied | `useEffect` cleanup at `use-map-thumbnail.ts:45-50` revokes on `[src]` change; 0 blob errors on post-login redirect confirmed via Playwright MCP. Commit `4473d21e`. |
| SMOKE-10 (SF-06 anonymous pre-auth probes) | 1050-03 | ✅ satisfied | `useSavedSearches` + `useAIStatus` + `useEmbeddingStats` all consumer-gated on `!!token` (plus `&& isAdmin` for admin probes); 0 401-noise on anonymous `/login` confirmed via Playwright MCP. Commits `912458e8`, `aca42c99`, `d6b0b9c6` (post-shipping CR-03+WR-04 fix for `useEmbeddingStats`). |
| SMOKE-11 (SF-07 thumbnail PUT count) | 1050-04 | ✅ satisfied | Module-level `autoCapturedMapIds` Set + `shouldAutoCapture()` predicate at `use-builder-save.ts:155,184` survives StrictMode remount; ≤1 PUT on initial mount confirmed via Playwright MCP. Commit `37fee435`. |
| SMOKE-12 (SF-08 false-positive basemap toast) | 1050-05 | ✅ satisfied | `basemapLoadedAtRef` latch at `BuilderMap.tsx:91`; suppression narrowed to 3000ms save-flow window post-WR-02 fix at `BuilderMap.tsx:417` (no permanent suppression of real outages). vitest 3/3. Commits `9fe0b4ec`, `0f0290ba`. |

## Integration

N/A — single-phase milestone. No cross-phase wiring. The 5 SF closures are independent bug fixes that share only the existing builder surface; integration coverage rolls forward from v1010.1's tagged state.

## E2E Flows

N/A — no new user flows introduced. v1010.2 closes regressions in existing flows (open map, login redirect, anonymous landing, save map). All re-verified via Playwright MCP (5/5 SF + 3/3 v1010.1 regression checks).

## Smoke Gate

| Gate | Result |
|------|--------|
| Frontend typecheck (`tsc --noEmit`) | 0 new errors (pre-existing carry-forward acknowledged) |
| Frontend vitest (full) | 1913/1913 PASS (1909 baseline + 4 regression tests from code-fix loop) |
| `e2e:smoke:builder` | 26/26 PASS (matches v1010.1 baseline) |
| Playwright MCP re-verify | 5/5 SF surfaces ✅ + 3/3 v1010.1 regressions ✅ |
| Code review | 3 BLOCKER + 4 WARNING — all closed inline (status: fixed in REVIEW.md) |

## Code Review Outcomes

Post-implementation review at Phase 1050 surfaced 7 secondary findings. Per `feedback_review_findings_inline.md`, all were fixed inline before this audit:

| Finding | Severity | Resolution |
|---------|----------|------------|
| CR-01: `waitForVisibleLayerSources` legacy lookup post-dedupe | BLOCKER | Fixed `516c9ae5` (route through `getSourceIdForLayer`) |
| CR-02: token-refresh `setTiles` legacy lookup post-dedupe | BLOCKER | Fixed `fd149688` (route through `getSourceIdForLayer`) |
| CR-03 + WR-04: `useEmbeddingStats` ungated | BLOCKER + WARNING | Fixed `d6b0b9c6` (consumer-gate same as `useAIStatus`) |
| WR-01: `removeStaleSourcesAndLayers` companion-layer leak | WARNING | Fixed `8b791a08` (derive companion ids from layer stack) |
| WR-02: SF-08 latch permanent-suppression too wide | WARNING | Fixed `0f0290ba` (3000ms save-flow window) |
| WR-03: `autoCapturedMapIds` never clears on navigation | WARNING | Fixed `0451657f` (corrected misleading comment per reviewer's safer option) |

## Tech Debt (3 Info findings, deferred per scope)

- IN-01: `__resetThumbnailDebounceForTests` name no longer accurately scoped (also resets `autoCapturedMapIds`)
- IN-02: No explicit DEM-keeps-per-layer-source test assertion (paths still correct, just under-tested)
- IN-03: Misleading reference-count comment in `use-builder-layers.ts`

## Verdict

**PASSED — ready for `/gsd-complete-milestone v1010.2`.**

All 5 v1010.1 carryforward smoke findings are closed with measured before/after evidence. Phase verification: 11/11 must-haves. Code review: 7/7 findings closed inline. Smoke gate: 4/4 green. Live Playwright MCP re-verify: 8/8 surfaces (5 SF + 3 regressions).
