---
phase: 1137-sharing-and-embed-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/viewer/ViewerMap.tsx
  - frontend/src/pages/PublicViewerPage.tsx
  - frontend/src/components/viewer/__tests__/ViewerMap.branding.test.tsx
  - frontend/src/pages/__tests__/PublicViewerPage.test.tsx
  - frontend/src/components/builder/hooks/__tests__/use-builder-save.test.ts
  - frontend/src/i18n/locales/en/common.json
  - frontend/src/i18n/locales/de/common.json
  - frontend/src/i18n/locales/es/common.json
  - frontend/src/i18n/locales/fr/common.json
autonomous: true
requirements:
  - SHARE-07
  - SHARE-09

must_haves:
  truths:
    - "Shared/embed viewer (PublicViewerPage) renders 'Powered by GeoLens' branding in BOTH embed mode (isEmbed=true) and shared-link mode (isEmbed=false) when useEdition() returns community"
    - "Branding is SUPPRESSED in the viewer when useEdition() returns enterprise AND branding.show_badge === false (matching the existing AppLayout/AppFooter contract at AppLayout.tsx:16)"
    - "Embed-mode branding overlay anchors to bottom-left of the map canvas (absolute bottom-2 left-2), z-10, semi-transparent paper background, text-xs text-muted-foreground — UI-SPEC Surface 3A spec"
    - "Non-embed shared-link mode continues to render AppFooter at bottom (existing v1014 contract preserved — no regression)"
    - "ViewerMap.tsx imports useEdition (new) and renders a {!isEnterprise || branding?.show_badge !== false} overlay inside its return JSX — but ONLY when a prop showInlineBranding={true} is passed by PublicViewerPage in embed mode"
    - "PublicViewerPage passes showInlineBranding={isEmbed} to ViewerMap; non-embed mode keeps AppFooter for branding"
    - "use-builder-save.ts handleExportPNG composition (existing implementation at lines 534-665) is verified by 4 new regression pins in use-builder-save.test.ts: (1) title block renders when localName non-empty, (2) legend block renders when legendLayers.length > 0, (3) branding footer renders when isEnterprise=false, (4) branding footer ABSENT when isEnterprise=true — pinned via Canvas2DContext.fillText spy"
    - "i18n key 'export.poweredBy' = 'Powered by GeoLens' exists in en/de/es/fr common.json (4-language parity, same key used by both viewer overlay and export PNG)"
    - "PublicMapViewerPage is NOT modified — it is the authenticated owner-view; branding applies to shared/embed only per UI-SPEC Surface 3A wording"
  artifacts:
    - path: "frontend/src/components/viewer/ViewerMap.tsx"
      provides: "useEdition() integration + conditional 'Powered by GeoLens' branding overlay rendered when showInlineBranding prop is true and community edition (or enterprise with show_badge !== false)"
      contains: "useEdition"
    - path: "frontend/src/components/viewer/__tests__/ViewerMap.branding.test.tsx"
      provides: "Regression pin: 4 cases (community shows; enterprise show_badge=false hides; enterprise show_badge=true shows; showInlineBranding=false hides regardless)"
      min_lines: 80
    - path: "frontend/src/pages/PublicViewerPage.tsx"
      provides: "Pass showInlineBranding={isEmbed} to ViewerMap; AppFooter remains in non-embed mode unchanged"
      contains: "showInlineBranding"
    - path: "frontend/src/pages/__tests__/PublicViewerPage.test.tsx"
      provides: "Regression pin: embed mode renders ViewerMap with showInlineBranding=true; non-embed mode renders AppFooter (existing behavior preserved)"
    - path: "frontend/src/components/builder/hooks/__tests__/use-builder-save.test.ts"
      provides: "SHARE-09 export PNG composition pins: title block, legend block, branding footer (4 new it() blocks asserting fillText calls on the canvas ctx mock)"
  key_links:
    - from: "frontend/src/components/viewer/ViewerMap.tsx"
      to: "frontend/src/hooks/use-edition.ts"
      via: "import { useEdition } from '@/hooks/use-edition'"
      pattern: "useEdition"
    - from: "frontend/src/pages/PublicViewerPage.tsx"
      to: "frontend/src/components/viewer/ViewerMap.tsx"
      via: "showInlineBranding={isEmbed} prop"
      pattern: "showInlineBranding"
---

<objective>
Add "Powered by GeoLens" branding to the embed-mode viewer canvas (currently AppFooter is suppressed when `isEmbed=true` at PublicViewerPage.tsx:181 — the existing `!isEmbed` gate). Embed-mode viewers today render zero branding regardless of edition; SHARE-07 fixes that gap.

Verify that `use-builder-save.ts` export PNG composition (already implemented at lines 534-665 per UI-SPEC scout — title block, legend block, branding footer all stacked) is pinned by regression tests (SHARE-09). Add 4 new pins.

Add `useEdition()` integration to ViewerMap so the branding overlay reads the same edition state the rest of the app uses; rely on PublicViewerPage to also read `useBranding().show_badge` so the gate matches the existing AppFooter contract (AppLayout.tsx:16) — branding overlay shows when `!isEnterprise || branding?.show_badge !== false`.

PublicMapViewerPage (authenticated owner view) is NOT modified — branding applies to the public shared/embed viewer surface only, matching the UI-SPEC Surface 3A scope.

Output: ViewerMap renders a `<div>` branding overlay when `showInlineBranding` prop is true; PublicViewerPage passes `showInlineBranding={isEmbed}` so embed iframes finally surface attribution. 4 new export PNG regression pins in use-builder-save.test.ts. 4-language i18n parity for `export.poweredBy`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1137-sharing-and-embed-polish/1137-CONTEXT.md
@.planning/phases/1137-sharing-and-embed-polish/1137-UI-SPEC.md

@frontend/src/components/viewer/ViewerMap.tsx
@frontend/src/pages/PublicViewerPage.tsx
@frontend/src/hooks/use-edition.ts
@frontend/src/components/layout/AppLayout.tsx
@frontend/src/components/layout/AppFooter.tsx
@frontend/src/components/builder/hooks/use-builder-save.ts
@frontend/src/components/builder/hooks/__tests__/use-builder-save.test.ts

<interfaces>
From frontend/src/hooks/use-edition.ts:
```typescript
export function useEdition(): {
  edition: 'community' | 'enterprise';
  features: string[];
  isEnterprise: boolean;
  isLoading: boolean;
};
```

From frontend/src/components/layout/AppLayout.tsx:15-16 (the contract this plan mirrors):
```typescript
const { data: branding } = useBranding();
const showFooterBranding = !isEnterprise || branding?.show_badge !== false;
```

From frontend/src/components/builder/hooks/use-builder-save.ts:534-665 (existing implementation — verify-only):
- `handleExportPNG` composes an offscreen canvas
- Title block: `if (title) { ctx.font = '700...'; ctx.fillText(title, ...); }` at lines 589-598
- Legend block: `if (legendLayers.length > 0) { ctx.fillText(legendHeader, ...); for (const layer of legendLayers) { ctx.fillRect(...swatch...); ctx.fillText(layerName, ...); } }` at lines 604-629
- Branding footer: `if (showBranding) { ctx.fillText('Powered by GeoLens', ...); }` at lines 631-638 with `showBranding = !isEnterprise` at line 568

From frontend/src/pages/PublicViewerPage.tsx (lines 45-50, 181-187):
```typescript
const { isEnterprise } = useEdition();
const { data: branding } = useBranding();
const showFooterBranding = !isEnterprise || branding?.show_badge !== false;
const isEmbed = searchParams.get('embed') === 'true';
// ...
{!isEmbed && <AppFooter showBranding={showFooterBranding} ... />}
```
This plan keeps the AppFooter gate but ALSO passes branding into ViewerMap via a new prop for the embed-mode case.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add showInlineBranding prop + branding overlay to ViewerMap</name>
  <files>frontend/src/components/viewer/ViewerMap.tsx, frontend/src/components/viewer/__tests__/ViewerMap.branding.test.tsx</files>
  <read_first>
    - frontend/src/components/viewer/ViewerMap.tsx (lines 53-70: ViewerMapProps; lines 760-803: return JSX where overlay goes)
    - frontend/src/hooks/use-edition.ts
    - frontend/src/hooks/use-settings.ts (useBranding shape)
    - .planning/phases/1137-sharing-and-embed-polish/1137-UI-SPEC.md (Surface 3A lines 274-305)
  </read_first>
  <behavior>
    - With `showInlineBranding={true}` and `useEdition` mocked to `{ isEnterprise: false }`: branding `<span>Powered by GeoLens</span>` is in the DOM
    - With `showInlineBranding={true}` and `useEdition` mocked to `{ isEnterprise: true }` AND `useBranding` mocked to `{ data: { show_badge: false } }`: branding text NOT in DOM
    - With `showInlineBranding={true}` and `useEdition` mocked to `{ isEnterprise: true }` AND `useBranding` mocked to `{ data: { show_badge: true } }`: branding text IS in DOM (enterprise opted in)
    - With `showInlineBranding={false}` (default): branding text NEVER in DOM regardless of edition (PublicMapViewerPage and any other non-embed caller stays clean)
    - Branding overlay has class `absolute bottom-2 left-2 z-10 text-xs text-muted-foreground bg-background/70 rounded px-2 py-1` (UI-SPEC Surface 3A spec)
  </behavior>
  <action>
    Step 1 — write the regression test file `frontend/src/components/viewer/__tests__/ViewerMap.branding.test.tsx`. Mock `useEdition` and `useBranding` via `vi.mock`. Mock `@vis.gl/react-maplibre` at module-level to a minimal `<div data-testid="map">` stub (the `LayerLegend.test.tsx` and `ViewerMap.basemap-config.test.tsx` siblings show the pattern — read them first to copy the mocking shape exactly so the test does not try to instantiate MapLibre GL).

    Render `<ViewerMap ... showInlineBranding={true} />` with minimal props (layers=[], basemapStyle='positron', initialViewState={center_lng:0,center_lat:0,zoom:1,bearing:0,pitch:0}, visibleLayers=new Set()). Pin the 4 cases in `<behavior>`.

    Step 2 — modify `frontend/src/components/viewer/ViewerMap.tsx`:

    (a) Add `showInlineBranding?: boolean` to `ViewerMapProps` (default false — non-breaking for existing callers).
    (b) Destructure `showInlineBranding = false` in the function signature.
    (c) Import `useEdition` from `@/hooks/use-edition` AND `useBranding` from `@/hooks/use-settings`.
    (d) Inside the component (before return JSX): `const { isEnterprise } = useEdition(); const { data: branding } = useBranding(); const showBranding = !isEnterprise || branding?.show_badge !== false;`
    (e) Inside the return `<div className="relative h-full w-full ...">` wrapper, AFTER `<MapCoordReadout map={...}>`, add a sibling element:

       ```
       {showInlineBranding && showBranding && (
         <span
           data-testid="viewer-branding-overlay"
           className="absolute bottom-2 left-2 z-10 text-xs text-muted-foreground bg-background/70 rounded px-2 py-1 pointer-events-none"
         >
           {t('export.poweredBy', { defaultValue: 'Powered by GeoLens' })}
         </span>
       )}
       ```

       Use the `t` from the already-imported `useTranslation('common')` near line 139. Add `'common'` namespace if not already loaded (it is — see line 139 `const { t } = useTranslation('common')`).

       Apply `pointer-events-none` so the overlay never blocks map gestures (UI-SPEC inheritance — branding does not interfere with interaction).

    Do NOT touch the existing ScaleControl, NavigationControl, FullscreenControl, AttributionControl positioning — UI-SPEC Surface 3A locks `bottom-2 left-2` for the branding only.
  </action>
  <verify>
    <automated>cd frontend && npm test -- ViewerMap.branding --run 2>&1 | tail -20</automated>
  </verify>
  <done>
    `cd frontend && npm test -- ViewerMap.branding --run` passes 4 tests. `grep -n "useEdition\|showInlineBranding\|viewer-branding-overlay" frontend/src/components/viewer/ViewerMap.tsx` returns at least 3 matches. `grep -c "import.*useEdition" frontend/src/components/viewer/ViewerMap.tsx` returns 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire PublicViewerPage to pass showInlineBranding={isEmbed}</name>
  <files>frontend/src/pages/PublicViewerPage.tsx, frontend/src/pages/__tests__/PublicViewerPage.test.tsx</files>
  <read_first>
    - frontend/src/pages/PublicViewerPage.tsx (entire file, lines 1-191)
    - frontend/src/pages/__tests__/PublicViewerPage.test.tsx (existing test patterns — match its mocking shape for hooks and ViewerMap)
  </read_first>
  <behavior>
    - When PublicViewerPage mounts with searchParams `?embed=true` → ViewerMap receives prop `showInlineBranding={true}`
    - When PublicViewerPage mounts WITHOUT `embed=true` (shared-link mode) → ViewerMap receives `showInlineBranding={false}` AND AppFooter renders at bottom (existing v1014 behavior preserved — no regression)
    - When in embed mode AppFooter still does NOT render (existing `!isEmbed && <AppFooter />` gate unchanged)
  </behavior>
  <action>
    Step 1 — Modify `frontend/src/pages/PublicViewerPage.tsx`. At the `<ViewerMap ... />` callsite around lines 144-154, add a new prop:

    ```
    showInlineBranding={isEmbed}
    ```

    Place it adjacent to the existing `embedToken={embedToken}` prop so the diff is minimal.

    Do NOT change the `{!isEmbed && <AppFooter ... />}` block at lines 181-187 — non-embed branding still rides through AppFooter; embed branding now rides through ViewerMap's new overlay.

    Step 2 — Extend `frontend/src/pages/__tests__/PublicViewerPage.test.tsx` (do NOT create a new file). Append a new `describe('SHARE-07 branding overlay routing')` block with 2 tests:

    (a) `test_embed_mode_passes_showInlineBranding_true` — render with `?embed=true`, mock `ViewerMap` via `vi.mock('@/components/viewer/ViewerMap', ...)` to capture props; assert the mock was called with `showInlineBranding: true`.

    (b) `test_non_embed_mode_passes_showInlineBranding_false` — render WITHOUT `?embed=true`, assert ViewerMap received `showInlineBranding: false` AND AppFooter is in the DOM.

    Use the existing test file's mocking shape for `useSharedMap`, `useEdition`, `useBranding`. Read lines 1-80 first to copy the setup.
  </action>
  <verify>
    <automated>cd frontend && npm test -- PublicViewerPage --run 2>&1 | tail -20</automated>
  </verify>
  <done>
    `cd frontend && npm test -- PublicViewerPage --run` passes all existing tests + 2 new tests. `grep -n "showInlineBranding={isEmbed}" frontend/src/pages/PublicViewerPage.tsx` returns exactly 1 line.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SHARE-09 — verify export PNG composition with 4 regression pins</name>
  <files>frontend/src/components/builder/hooks/__tests__/use-builder-save.test.ts</files>
  <read_first>
    - frontend/src/components/builder/hooks/use-builder-save.ts:534-665 (existing handleExportPNG implementation)
    - frontend/src/components/builder/hooks/__tests__/use-builder-save.test.ts (existing describe('useBuilderSave') block — read 60 lines around line 299 to mirror the harness)
    - .planning/phases/1137-sharing-and-embed-polish/1137-UI-SPEC.md Surface 4 lines 324-374
  </read_first>
  <behavior>
    - With `localName = 'My Map'` and `localDescription = 'desc'` and `isEnterprise = false`: handleExportPNG produces an offscreen canvas where ctx.fillText is called with title text 'My Map', description text 'desc', the legend header text matching i18n key 'export.legendHeader', and the branding text matching i18n key 'export.poweredBy'
    - With `localName = ''` (empty): fillText is NOT called with any title-block content (titleBlockH = 0 short-circuit at line 556)
    - With `localLayers = []`: fillText is NOT called for any legend row (legendLayers filter at line 558 returns empty)
    - With `isEnterprise = true` AND `branding?.show_badge !== false`: fillText is NOT called with 'Powered by GeoLens' (showBranding = !isEnterprise = false at line 568 — note: use-builder-save reads isEnterprise from useEdition() at line 397, see existing setup pattern)
  </behavior>
  <action>
    Append `describe('SHARE-09 export PNG composition')` to `frontend/src/components/builder/hooks/__tests__/use-builder-save.test.ts`.

    Setup pattern: the existing tests at lines 299-705 already mock the hook environment. Mirror that setup. Also mock `HTMLCanvasElement.prototype.getContext` to return a stub Canvas2DContext where `fillText`, `fillRect`, `drawImage`, `measureText` are vi.fn() spies, and `toBlob` (on the offscreen canvas) calls its callback with a `new Blob([])`.

    Mock `state.mapInstanceRef.current.getCanvas()` to return a stub HTMLCanvasElement (use `document.createElement('canvas')`).

    The 4 new `it()` blocks pin the existing implementation:

    (a) `it('renders title and description in the title block when localName non-empty')`:
       - Setup with `localName='My Map'`, `localDescription='Test desc'`, `isEnterprise=false`, `localLayers=[]`.
       - Trigger handleExportPNG (resolve the deferred render registration — use `vi.useFakeTimers` if the existing render-event tick path requires it, otherwise dispatch the render event the implementation listens for; read use-builder-save.ts:534-660 carefully to discover the render-frame trigger pattern).
       - Assert `ctx.fillText` was called with first arg matching `/^My Map$/`.
       - Assert `ctx.fillText` was called with first arg matching `/^Test desc$/`.

    (b) `it('renders legend header and one row per visible legend layer')`:
       - Setup with `localLayers=[{visible:true, show_in_legend:true, display_name:'Streets'}, {visible:true, show_in_legend:false, ...}, {visible:false, ...}]` — only the first qualifies.
       - Assert `ctx.fillText` was called with first arg matching `/Legend/i` (header).
       - Assert `ctx.fillText` was called with first arg matching `/^Streets$/`.
       - Assert `ctx.fillRect` was called at least once (swatch rendering).

    (c) `it('renders Powered by GeoLens footer when isEnterprise is false')`:
       - Setup with `isEnterprise=false`.
       - Assert `ctx.fillText` was called with first arg matching `/Powered by GeoLens/`.

    (d) `it('suppresses Powered by GeoLens footer when isEnterprise is true')`:
       - Setup with `isEnterprise=true`.
       - Assert `ctx.fillText` was NOT called with any string matching `/Powered by GeoLens/` — use `expect(fillTextSpy).not.toHaveBeenCalledWith(expect.stringMatching(/Powered by GeoLens/), ...)`.

    These 4 pins lock the SHARE-09 contract that the existing implementation already satisfies. Implementation does NOT change — only tests are added.
  </action>
  <verify>
    <automated>cd frontend && npm test -- use-builder-save --run 2>&1 | tail -25</automated>
  </verify>
  <done>
    `cd frontend && npm test -- use-builder-save --run` passes all existing tests + 4 new SHARE-09 pins. `grep -n "SHARE-09" frontend/src/components/builder/hooks/__tests__/use-builder-save.test.ts` returns at least 1 line (describe block).
  </done>
</task>

<task type="auto">
  <name>Task 4: i18n parity — add export.poweredBy to en/de/es/fr common.json</name>
  <files>frontend/src/i18n/locales/en/common.json, frontend/src/i18n/locales/de/common.json, frontend/src/i18n/locales/es/common.json, frontend/src/i18n/locales/fr/common.json</files>
  <read_first>
    - frontend/src/i18n/locales/en/common.json (find the `export` key block or top-level)
    - frontend/src/i18n/locales/de/common.json (mirror the structure)
    - .planning/phases/1137-sharing-and-embed-polish/1137-UI-SPEC.md (line 562: `export.poweredBy = "Powered by GeoLens"`)
  </read_first>
  <action>
    For each of the 4 locale files: if a top-level `"export"` object exists, add or update `"poweredBy": "Powered by GeoLens"` inside it. If no `"export"` block exists, add one: `"export": { "poweredBy": "Powered by GeoLens", "legendHeader": "Legend" }`.

    Check whether `export.legendHeader` already exists (it is consumed by use-builder-save.ts:608) and add it ONLY if missing — do not duplicate.

    German (de): "Powered by GeoLens" stays in English (brand name is the same in all 4 locales — common practice for product branding). However for `legendHeader`: en=Legend, de=Legende, es=Leyenda, fr=Légende.

    Do NOT touch other keys; do NOT reformat the JSON file. Use minimal-diff edits.

    Run `npm run i18n` (the parity check script) after editing to confirm all 4 locales have matching key trees.
  </action>
  <verify>
    <automated>cd frontend && npm run i18n 2>&1 | tail -10</automated>
    <automated>grep -c "poweredBy" frontend/src/i18n/locales/en/common.json frontend/src/i18n/locales/de/common.json frontend/src/i18n/locales/es/common.json frontend/src/i18n/locales/fr/common.json</automated>
  </verify>
  <done>
    All 4 locale files contain `export.poweredBy`. `cd frontend && npm run i18n` exits 0. The 4 grep counts all equal 1.
  </done>
</task>

</tasks>

<verification>
  <automated>cd frontend && npm test -- ViewerMap.branding PublicViewerPage use-builder-save --run</automated>
  <automated>grep -c "showInlineBranding" frontend/src/pages/PublicViewerPage.tsx frontend/src/components/viewer/ViewerMap.tsx</automated>
  <automated>cd frontend && npm run i18n</automated>
  <automated>cd frontend && npm run typecheck 2>&1 | tail -5</automated>
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public embed iframe → branding overlay | overlay DOM injected into a sandboxed iframe — must not bypass sandbox security |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1137-03-01 | Tampering | branding overlay DOM | accept | Overlay is static i18n text + Tailwind classes; no user-controlled content; no XSS vector |
| T-1137-03-02 | Information Disclosure | useEdition response in iframe | accept | Edition info is already public (community vs enterprise); no PII; no new endpoint exposure |
| T-1137-03-03 | Repudiation | Branding visibility in enterprise | mitigate | `branding?.show_badge !== false` gate matches AppLayout.tsx:16; enterprise customers who hide branding via the existing /admin/branding flow stay hidden — no double-display |
</threat_model>

<success_criteria>
- ViewerMap.tsx renders an inline branding overlay when `showInlineBranding={true}` AND (`!isEnterprise` OR `branding?.show_badge !== false`).
- PublicViewerPage passes `showInlineBranding={isEmbed}` to ViewerMap; non-embed flow unchanged (AppFooter still renders).
- 4 new regression pins in `ViewerMap.branding.test.tsx` (community show, enterprise hide via show_badge=false, enterprise show via show_badge=true, showInlineBranding=false gate).
- 2 new regression pins in `PublicViewerPage.test.tsx` (embed routes prop true; non-embed routes prop false).
- 4 new SHARE-09 export PNG pins in `use-builder-save.test.ts` (title rendered, legend rendered, branding rendered community, branding suppressed enterprise).
- `cd frontend && npm run i18n` exits 0 with `export.poweredBy` present in all 4 locales (en/de/es/fr).
- `cd frontend && npm run typecheck` exits 0.
- `grep -n "BuilderActionSource" frontend/src/components/viewer/ViewerMap.tsx` returns 0 lines (HARD INVARIANT preserved — no widening).
- `grep -n "SHARE-08\|og_image_uri\|og:image" frontend/src/components/viewer/ViewerMap.tsx` returns 0 lines (SHARE-08 DEFER respected — no OG-card work).
</success_criteria>

<output>
Create `.planning/phases/1137-sharing-and-embed-polish/1137-03-SUMMARY.md` when done. Include:
- Files modified (ViewerMap.tsx, PublicViewerPage.tsx, 3 test files, 4 i18n files)
- New ViewerMap prop signature: `showInlineBranding?: boolean` (default false)
- Branding gate condition: `showInlineBranding && (!isEnterprise || branding?.show_badge !== false)`
- 4 SHARE-09 export PNG pins added (verify-only — no implementation change in use-builder-save.ts)
- i18n parity confirmed via `npm run i18n` exit 0
- PublicMapViewerPage NOT modified (out of scope per UI-SPEC)
</output>
