---
phase: 1137-sharing-and-embed-polish
plan: 07
type: execute
wave: 5
depends_on:
  - 1137-01
  - 1137-02
  - 1137-03
  - 1137-04
  - 1137-05
  - 1137-06
files_modified:
  - .planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md
autonomous: false
mcp_driver: orchestrator
requirements: []
tags:
  - share
  - mcp-smoke
  - close-gate

must_haves:
  truths:
    - "1137-MCP-SMOKE.md exists at .planning/phases/1137-sharing-and-embed-polish/ and contains a numbered checklist of 7 MCP smoke surfaces (one per SHARE REQ ID) plus the cross-cutting HARD-INVARIANT regression checks"
    - "Each checklist item explicitly states which mcp__playwright__* tool the orchestrator calls — NOT a delegation to gsd-executor (per feedback_playwright_mcp_orchestrator_only memory)"
    - "Each checklist item has an explicit PASS / PARTIAL / FAIL disposition column AND a 'observed' column the orchestrator fills in live"
    - "HARD-INVARIANT regressions are explicit: sandbox=allow-scripts grep, BuilderActionSource unchanged, SHARE-08 not touched, frame-ancestors no-*"
    - "Each checklist item names a representative map ID and a starting URL"
    - "Orchestrator-driven MCP — this plan does NOT spawn an executor; it produces a checklist artifact the orchestrator follows directly"
  artifacts:
    - path: ".planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md"
      provides: "Live Playwright MCP smoke checklist for Phase 1137 close-gate"
      min_lines: 100
  key_links:
    - from: ".planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md"
      to: "orchestrator mcp__playwright__* tool calls"
      via: "checklist names tool + selector + expected behavior + observed column"
      pattern: "mcp__playwright"
---

<objective>
Produce `1137-MCP-SMOKE.md` — a checklist the **orchestrator** (NOT a gsd-executor sub-agent) follows directly using its own `mcp__playwright__*` tools. The checklist must cover every Phase 1137 success criterion via a live walkthrough on `localhost:8080` against representative maps, with explicit PASS / PARTIAL / FAIL dispositions per surface.

Purpose: the v1011 / v1020 / v1021 / v1028 pattern shows that orchestrator-driven MCP catches regressions vitest cannot reach (cross-context offset contracts, cross-origin iframe sandbox behavior, optimistic-UI rollback flicker, dnd-kit collision regressions). This plan packages that smoke discipline for Phase 1137's 6 SHARE REQs.

Critical: this is NOT an executor plan. The orchestrator spawns gsd-executor for the 6 implementation plans (01-06); for Plan 07 the orchestrator calls `mcp__playwright__*` tools itself in its own turn. The deliverable is a markdown checklist the orchestrator can execute as a script.

Output: `1137-MCP-SMOKE.md` with 7 numbered surfaces + 4 HARD-INVARIANT checks + a final sign-off block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

NOTE: this plan creates a MARKDOWN CHECKLIST, not code. The orchestrator follows the checklist directly using its own mcp__playwright__* tools. No executor is spawned for live browser interaction.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1137-sharing-and-embed-polish/1137-CONTEXT.md
@.planning/phases/1137-sharing-and-embed-polish/1137-UI-SPEC.md
@.planning/phases/1137-sharing-and-embed-polish/1137-01-SUMMARY.md
@.planning/phases/1137-sharing-and-embed-polish/1137-02-SUMMARY.md
@.planning/phases/1137-sharing-and-embed-polish/1137-03-SUMMARY.md
@.planning/phases/1137-sharing-and-embed-polish/1137-04-SUMMARY.md
@.planning/phases/1137-sharing-and-embed-polish/1137-05-SUMMARY.md
@.planning/phases/1137-sharing-and-embed-polish/1137-06-SUMMARY.md

<reference>
Prior MCP smoke artifact for shape reference (NOT for content reuse):
- .planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/1135-MCP-SMOKE.md
- .planning/phases/1136-per-render-mode-editor-polish/1136-07-* (Plan 1136-07 has MCP checklist embedded)

Canonical map for sharing smoke: c39be324-6815-40e5-8143-00a2723827b2 (ADK High Peaks — has public visibility paths exercised by prior milestones).
</reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the orchestrator-driven MCP smoke checklist</name>
  <files>.planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md</files>
  <read_first>
    - .planning/phases/1137-sharing-and-embed-polish/1137-UI-SPEC.md (all 5 surfaces)
    - .planning/phases/1137-sharing-and-embed-polish/1137-CONTEXT.md (hard invariants + pitfalls)
    - .planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/1135-MCP-SMOKE.md (prior smoke artifact shape — IF it exists; check via ls first; if not, follow the structure below from scratch)
  </read_first>
  <action>
    Create `.planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md`. The document is a CHECKLIST the orchestrator follows directly — every item explicitly names a `mcp__playwright__*` tool. The structure:

    Header section:
    - Title: `# Phase 1137 — Live MCP Smoke Checklist (orchestrator-driven)`
    - Operator note: "This is NOT an executor task. The orchestrator calls mcp__playwright__* tools directly per item below. Do NOT spawn gsd-executor for live browser steps — that violates feedback_playwright_mcp_orchestrator_only memory."
    - Pre-flight: 1 paragraph confirming `docker compose ps` shows healthy api / worker / frontend; admin credentials available (`GEOLENS_ADMIN_USERNAME` / `GEOLENS_ADMIN_PASSWORD`).
    - Canonical map: `c39be324-6815-40e5-8143-00a2723827b2` (ADK High Peaks).
    - Base URL: `http://localhost:8080`.

    Section 1: SHARE-02 / SHARE-06 chip-based allowed-origins
    A numbered checklist with these MCP steps:
    1. `mcp__playwright__browser_navigate` → `http://localhost:8080/maps/c39be324-.../builder`
    2. Sign in via mcp__playwright__browser_evaluate or browser_type on the login form if needed
    3. Open Share dialog via `mcp__playwright__browser_click` on the Share button
    4. Set visibility to public via radio click
    5. Click "Generate share link"
    6. Click "Link Settings" disclosure
    7. Toggle "Restrict to domains" switch ON
    8. Type 'Example.com' + Enter in the chip input → screenshot via `mcp__playwright__browser_take_screenshot`; expected: chip 'https://example.com' visible; input cleared
    9. Type '*' + Enter → expected: inline error 'Wildcard origin not allowed' visible; no new chip; no PATCH fired (check Network panel via mcp__playwright__browser_evaluate inspecting performance.getEntries or via browser_console_messages)
    10. Click chip remove X → expected: chip disappears; PATCH fires
    Disposition column: PASS / PARTIAL / FAIL.

    Section 2: SHARE-04 expiration presets
    1. With dialog still open, locate the expiration Select (above the domains chip block)
    2. `mcp__playwright__browser_click` to open Select
    3. Choose "7 days" → expected: no extra Save click required; toast 'share.expirationUpdated' visible; Select trigger now displays '7 days'
    4. Choose "Custom date…" → expected: date Input + Save button appear
    5. Pitfall #6 verification: take screenshot showing Copy Link button STILL present after step 3 (rawShareToken survived)
    Disposition column.

    Section 3: SHARE-07 community-edition viewer branding
    1. `mcp__playwright__browser_navigate` to `http://localhost:8080/m/<share-token-from-Section-1>` (shared-link mode, NOT embed)
    2. Expected: AppFooter visible at bottom with "Powered by GeoLens" (existing behavior — regression check)
    3. Navigate to `http://localhost:8080/m/<share-token>?embed=true&et=<embed-token>` (embed mode)
    4. Expected: NEW overlay 'Powered by GeoLens' visible at bottom-left inside the ViewerMap canvas (Plan 03 surface); AppFooter is NOT visible (embed gate)
    5. Screenshot for visual diff
    Disposition column.

    Section 4: SHARE-09 legend + title in shared viewer
    1. Already on the shared-link URL from Section 3
    2. Expected: MapTitlePill visible at top with map name + description (existing behavior — regression check)
    3. Expected: LayerLegend visible (existing behavior — regression check)
    4. Add `&legend=false` to URL → expected: legend hidden
    5. Test export PNG: navigate back to builder, click Export PNG → open downloaded file → expected: title block + legend block + 'Powered by GeoLens' footer all present in the PNG (visual verify)
    Disposition column.

    Section 5: SHARE-03 iframe embed-preview pane
    1. Navigate back to builder, open Share dialog (share + embed tokens still active)
    2. Locate the new "Preview" disclosure below the embed code textarea
    3. Click Preview → expected: iframe appears with loading spinner
    4. Wait for iframe onLoad → expected: live embed view of the map renders inside the pane
    5. `mcp__playwright__browser_evaluate` to read iframe.sandbox.value → expected: exactly 'allow-scripts' (NOT 'allow-scripts allow-same-origin')
    6. `mcp__playwright__browser_evaluate` to read iframe.title → expected: 'Map embed preview'
    7. Verify security indicator footer below iframe: text "sandbox=\"allow-scripts\" only — SEC-07 contract" visible with Shield icon
    Disposition column.

    Section 6: Pitfall #7 inflightEmbedCreate race (best-effort UI smoke)
    1. Reset by revoking the share token (Revoke button) OR navigate to a fresh public map without share token
    2. Click "Generate share link" TWICE rapidly (within 100ms — use `mcp__playwright__browser_evaluate` with `(async () => { document.querySelectorAll('button')[...].click(); document.querySelectorAll('button')[...].click(); })()` to fire both before the first promise resolves)
    3. Observe Network: expected EXACTLY 1 POST /maps/{id}/embed-tokens fired (use browser_console_messages or browser_evaluate against performance.getEntriesByType('resource'))
    4. Note: this is a best-effort UI smoke — the vitest pin in SharePanel.test.tsx is the authoritative regression. MCP smoke confirms the production code path behaves the same as the test.
    Disposition column.

    Section 7: HARD-INVARIANT regression sweep (cross-cutting)
    Each is a quick grep check the orchestrator runs via `Bash`, not MCP:
    1. `grep -cE 'sandbox="allow-scripts"' frontend/src/components/builder/SharePanel.tsx` → expected: 2 (snippet + iframe pane)
    2. `grep -cE 'allow-same-origin' frontend/src/components/builder/SharePanel.tsx` → expected: 0
    3. `grep -c "BuilderActionSource" frontend/src/components/builder/SharePanel.tsx frontend/src/components/viewer/ViewerMap.tsx` → expected: 0 (neither file references the action source)
    4. `grep -c "SHARE-08\|og_image_uri\|og:image" frontend/src/components/builder/SharePanel.tsx frontend/src/components/viewer/ViewerMap.tsx frontend/src/pages/PublicViewerPage.tsx` → expected: 0
    5. `grep -c "frame-ancestors '\\*'\|frame-ancestors \\*" backend/app/modules/catalog/maps/router.py backend/app/api/middleware/security.py` → expected: 0
    Disposition column.

    Section 8: Final sign-off block
    A markdown table:
    | Surface | REQ ID | Disposition | Observed | Date |
    | SHARE-02 chip add/remove/canonicalize | SHARE-02 | | | |
    | SHARE-06 wildcard reject + dedupe | SHARE-06 | | | |
    | SHARE-04 expiration presets | SHARE-04 | | | |
    | SHARE-07 viewer branding (community + enterprise) | SHARE-07 | | | |
    | SHARE-09 legend + title + export PNG | SHARE-09 | | | |
    | SHARE-03 iframe preview pane | SHARE-03 | | | |
    | Pitfall #6 rawShareToken survival | (Pitfall) | | | |
    | Pitfall #7 race dedupe | (Pitfall) | | | |
    | Pitfall #8 canonical form | (Pitfall) | | | |
    | HARD-INVARIANT grep sweep | (Invariant) | | | |

    Final block: "Sign-off: orchestrator records overall PASS / PARTIAL / FAIL here on completion."

    Reference materials section: links to UI-SPEC.md sections per surface, links to Plan 01-06 SUMMARYs.

    The checklist MUST be self-contained — the orchestrator should be able to execute it without re-reading the implementation plans. Each MCP step names the EXACT tool (mcp__playwright__browser_navigate, mcp__playwright__browser_click, mcp__playwright__browser_evaluate, mcp__playwright__browser_take_screenshot, mcp__playwright__browser_console_messages) so the call sites are obvious.
  </action>
  <verify>
    <automated>test -f .planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md && wc -l .planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md | awk '{ print ($1 > 100 ? "OK" : "TOO SHORT: " $1 " lines") }'</automated>
    <automated>grep -c "mcp__playwright" .planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md</automated>
    <automated>grep -cE "^Section [0-9]|^## Section [0-9]" .planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md</automated>
    <automated>grep -c "PASS / PARTIAL / FAIL\|Disposition" .planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md</automated>
  </verify>
  <done>
    `.planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md` exists with at least 100 lines, references mcp__playwright tools at least 15 times (multiple per section), has 8 sections, and includes Disposition column references.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Hand off MCP smoke checklist to orchestrator (DO NOT delegate to gsd-executor)</name>
  <files>.planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md</files>
  <action>
    STOP at this task. Do NOT execute the MCP smoke from the gsd-executor context — the executor sub-agent does not have `mcp__playwright__*` tools and would fail the smoke silently. Instead, emit the following hand-off block in the executor's summary message and exit cleanly:

    "Phase 1137 implementation complete (Plans 01-06 shipped). Checklist artifact `1137-MCP-SMOKE.md` is now ready. Per feedback_playwright_mcp_orchestrator_only memory, the orchestrator drives MCP smoke. Resume to /gsd:execute-phase orchestrator and request live MCP execution against the checklist. Do not spawn another executor for the smoke."

    Then write `.planning/phases/1137-sharing-and-embed-polish/1137-07-SUMMARY.md` recording the artifact path and the hand-off contract, and exit. The orchestrator resumes with the checklist directly.
  </action>
  <what-built>
    Phase 1137 implementation complete (Plans 01-06 shipped):
    - url-normalize.ts canonical-form helper (Pitfall #8)
    - Backend CSP no-* defensive pin + canonical round-trip
    - ViewerMap branding overlay + export PNG composition pins
    - SharePanel chip-based allowed-origins UI (SHARE-02 / SHARE-06)
    - SharePanel expiration preset Select + Pitfall #6 docstring (SHARE-04)
    - SharePanel iframe embed-preview pane + inflightEmbedCreate race ref (SHARE-03 / Pitfall #7)
    - 7 SHARE i18n key clusters across en/de/es/fr (Plans 03/04/05/06)

    Live MCP smoke checklist `.planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md` is now ready for orchestrator-driven execution.
  </what-built>
  <how-to-verify>
    **ORCHESTRATOR DRIVES THIS CHECKPOINT — NOT THE EXECUTOR.**

    Per feedback_playwright_mcp_orchestrator_only memory:
    - The orchestrator has access to `mcp__playwright__*` tools.
    - Executors (gsd-executor sub-agents) do NOT have those tools.
    - This checkpoint is a HAND-OFF to the orchestrator. The executor running Plan 07 has already produced the checklist artifact `1137-MCP-SMOKE.md`. The orchestrator now follows the checklist on its own turn.

    Orchestrator actions on resume:
    1. Open `.planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md`.
    2. Pre-flight: confirm `docker compose ps` shows healthy services. If not, surface the issue to operator.
    3. For each Section (1-7) in the checklist, call the named `mcp__playwright__*` tools in sequence, record disposition + observed values.
    4. Run the Section 7 HARD-INVARIANT grep checks via `Bash`.
    5. Fill in the Section 8 sign-off table.
    6. Surface aggregate result to operator: "X/9 PASS, Y PARTIAL, Z FAIL" and propose either Phase 1137 close or a v1137.1 carry-forward plan.

    If the orchestrator does NOT have `mcp__playwright__*` tools available (rare: orchestrator running without the playwright MCP), the orchestrator instead surfaces a message to the operator: "Live MCP smoke must run on a workstation with the playwright MCP available. Checklist is at .planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md."

    NEVER delegate the MCP execution to a gsd-executor spawn — the executor sub-agent lacks the mcp__playwright tools and cannot run the smoke. This violates feedback_playwright_mcp_orchestrator_only.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md</automated>
    <automated>test -f .planning/phases/1137-sharing-and-embed-polish/1137-07-SUMMARY.md</automated>
  </verify>
  <done>
    Checklist artifact exists at `.planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md`. Executor summary explicitly hands off MCP execution to the orchestrator. SUMMARY.md is written. Executor exits cleanly without attempting browser interaction.
  </done>
  <resume-signal>
    Orchestrator records the aggregate disposition in the operator-facing summary. Type "smoke-passed" to close Phase 1137 OR describe issues to file v1137.1 carry-forward plan.
  </resume-signal>
</task>

</tasks>

<verification>
  <automated>test -f .planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md</automated>
  <automated>wc -l .planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md</automated>
  <automated>grep -c "mcp__playwright" .planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md</automated>
</verification>

<success_criteria>
- `.planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md` exists with at least 100 lines.
- Checklist contains 7 surface sections + 1 HARD-INVARIANT grep section + 1 final sign-off table.
- Every MCP step explicitly names the `mcp__playwright__*` tool (browser_navigate, browser_click, browser_evaluate, browser_take_screenshot, browser_console_messages).
- Checklist explicitly states "orchestrator-driven, NOT executor" at the top.
- The checkpoint task hands off to the orchestrator without delegating live browser steps to a sub-agent.
- All 6 SHARE REQ IDs + 3 Pitfalls + HARD-INVARIANT grep are covered in the sign-off table.
</success_criteria>

<output>
Create `.planning/phases/1137-sharing-and-embed-polish/1137-07-SUMMARY.md` when the checklist file is written (before the human-verify checkpoint resumes). Include:
- Path to checklist artifact
- Sections produced (8 in total)
- Hand-off note: "Orchestrator follows 1137-MCP-SMOKE.md on resume; gsd-executor sub-agents do NOT have mcp__playwright tools and MUST NOT be delegated this work."

The orchestrator updates the SUMMARY with the smoke-result aggregate AFTER it runs the checklist.
</output>
