---
phase: 1137-sharing-and-embed-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/builder/url-normalize.ts
  - frontend/src/lib/builder/__tests__/url-normalize.test.ts
autonomous: true
requirements:
  - SHARE-06
tags:
  - share
  - normalization
  - pitfall-8

must_haves:
  truths:
    - "normalizeOrigin('Example.com') returns 'https://example.com' (adds scheme + lowercases host)"
    - "normalizeOrigin('https://Example.com/') returns 'https://example.com' (lowercases host + strips trailing slash)"
    - "normalizeOrigin('http://localhost:3000/') returns 'http://localhost:3000' (preserves explicit non-default port + strips trailing slash)"
    - "normalizeOrigin('https://example.com:443') returns 'https://example.com' (strips default port for scheme)"
    - "normalizeOrigin('http://example.com:80') returns 'http://example.com' (strips default port for scheme)"
    - "normalizeOrigin('*') throws WildcardOriginError so callers can surface 'Wildcard origin not allowed' inline (Pitfall #8 + CSP no-* invariant)"
    - "normalizeOrigin('  ') (whitespace only) throws InvalidOriginError"
    - "dedupeOrigins(['https://example.com', 'HTTPS://Example.com/']) returns ['https://example.com'] (canonical-form dedupe)"
    - "Module exports normalizeOrigin, dedupeOrigins, WildcardOriginError, InvalidOriginError — frontend callers in SharePanel chip input (Plan 04) consume these directly without re-implementing the contract"
  artifacts:
    - path: "frontend/src/lib/builder/url-normalize.ts"
      provides: "Canonical-form origin normalization (Pitfall #8) + dedupe helper consumed by SharePanel chip input"
      exports: ["normalizeOrigin", "dedupeOrigins", "WildcardOriginError", "InvalidOriginError"]
      min_lines: 60
    - path: "frontend/src/lib/builder/__tests__/url-normalize.test.ts"
      provides: "Unit tests pinning canonical-form rules (Pitfall #8) — case/port/trailing-slash + wildcard rejection + dedupe"
      min_lines: 80
  key_links:
    - from: "frontend/src/lib/builder/url-normalize.ts"
      to: "frontend/src/components/builder/SharePanel.tsx (Plan 04)"
      via: "import { normalizeOrigin, dedupeOrigins, WildcardOriginError } from '@/lib/builder/url-normalize'"
      pattern: "import.*from.*url-normalize"
---

<objective>
Create the `normalizeOrigin` canonical-form helper that the SharePanel chip input (Plan 04) consumes. Canonical-form rules match the existing backend `_normalize_origin` in `backend/app/modules/embed_tokens/schemas.py:14-31` so frontend optimistic chips render the same string the backend persists (eliminating Pitfall #8 drift between optimistic UI and saved state).

Purpose: lock the Pitfall #8 normalization contract behind a tested unit boundary BEFORE the chip-input UI consumes it. Backend already normalizes server-side via `_normalize_origin` (scheme add then lowercase then strip trailing slash then strip default port). This plan mirrors that contract on the frontend so chip rendering is canonical the moment the user presses Enter — no flash-of-non-canonical-text between optimistic render and PATCH response.

Output: `frontend/src/lib/builder/url-normalize.ts` exporting `normalizeOrigin`, `dedupeOrigins`, and two named errors. Unit tests pin every rule in the contract plus wildcard rejection (CSP no-`*` invariant — locked decision from CONTEXT.md and ROADMAP SC #1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1137-sharing-and-embed-polish/1137-CONTEXT.md
@.planning/phases/1137-sharing-and-embed-polish/1137-UI-SPEC.md

@backend/app/modules/embed_tokens/schemas.py
@frontend/src/components/builder/SharePanel.tsx

<interfaces>
<!-- Mirror this backend contract exactly. Frontend chips must show the same canonical form the backend persists. -->

From backend/app/modules/embed_tokens/schemas.py:14-31 (_normalize_origin):
```python
def _normalize_origin(origin: str) -> str:
    normalized = origin.strip().lower().rstrip("/")
    if not normalized.startswith(("http://", "https://")):
        normalized = f"https://{normalized}"

    parsed = urlparse(normalized)
    if not parsed.hostname:
        raise ValueError(f"Invalid origin: {origin}")

    scheme = parsed.scheme or "https"
    host = parsed.hostname
    port = parsed.port
    if (scheme == "http" and port == 80) or (scheme == "https" and port == 443):
        port = None

    if port:
        return f"{scheme}://{host}:{port}"
    return f"{scheme}://{host}"
```

Frontend equivalent uses `new URL()` (browser DOM API) instead of `urlparse` — same semantics for ports + hostname + scheme.

Existing parseOrigins in SharePanel.tsx:24-31 (will be REMOVED in Plan 04):
```typescript
function parseOrigins(input: string): string[] {
  return input
    .split(',')
    .map((s) => s.trim())
    .filter(Boolean)
    .map((s) => s.replace(/\/+$/, ''))
    .map((s) => (s.includes('://') ? s : `https://${s}`));
}
```
This is the buggy comma-split path that drops case-normalization and port-stripping. Plan 04 replaces it; Plan 01 creates its tested replacement.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit tests for normalizeOrigin and dedupeOrigins</name>
  <files>frontend/src/lib/builder/__tests__/url-normalize.test.ts</files>
  <read_first>
    - backend/app/modules/embed_tokens/schemas.py (lines 14-43: _normalize_origin and _validate_origins)
    - .planning/phases/1137-sharing-and-embed-polish/1137-UI-SPEC.md (Surface 1 Normalization Contract, lines 162-179)
  </read_first>
  <behavior>
    - normalizeOrigin('Example.com') === 'https://example.com'
    - normalizeOrigin('https://Example.com/') === 'https://example.com'
    - normalizeOrigin('HTTPS://example.com//') === 'https://example.com' (multiple trailing slashes stripped)
    - normalizeOrigin('http://localhost:3000/') === 'http://localhost:3000'
    - normalizeOrigin('https://example.com:443') === 'https://example.com' (default port stripped)
    - normalizeOrigin('http://example.com:80/') === 'http://example.com' (default port plus trailing slash both stripped)
    - normalizeOrigin('https://Example.COM:8080/foo') === 'https://example.com:8080' (path discarded — origin only)
    - normalizeOrigin('*') throws WildcardOriginError with message containing 'Wildcard origin not allowed'
    - normalizeOrigin('   ') (whitespace only) throws InvalidOriginError
    - normalizeOrigin('') throws InvalidOriginError
    - normalizeOrigin('not a url at all') throws InvalidOriginError (URL constructor fails)
    - dedupeOrigins(['https://example.com', 'HTTPS://Example.com/']) returns ['https://example.com'] (one entry)
    - dedupeOrigins(['https://a.com', 'https://b.com', 'https://a.com']) returns ['https://a.com', 'https://b.com'] (order preserved, dupes dropped)
    - dedupeOrigins([]) returns []
    - dedupeOrigins(['*', 'https://example.com']) — wildcard entries are filtered out (NOT thrown) and the rest are deduped; returns ['https://example.com']
  </behavior>
  <action>
    Create `frontend/src/lib/builder/__tests__/url-normalize.test.ts`. Import from `'../url-normalize'` (file does NOT exist yet — tests must fail with module-resolution error per RED step).

    Use vitest `describe`/`it`/`expect`. Two `describe` blocks:
    (1) `describe('normalizeOrigin')` covering scheme-add, lowercase, trailing-slash strip, default-port strip, path discard, wildcard throw, empty/whitespace throw, malformed throw.
    (2) `describe('dedupeOrigins')` covering canonical-form dedupe, order preservation, wildcard filter, empty input.

    Each `it()` block uses exactly one `expect()` so failures are pinpointed. Wildcard test uses `expect(() => normalizeOrigin('*')).toThrow(WildcardOriginError)` AND `.toThrow(/Wildcard origin not allowed/)`.

    Do NOT inline implementation in the test file. NEVER skip a wildcard test — the CSP no-`*` invariant is the load-bearing security pin in this phase (ROADMAP SC #1, locked decision in CONTEXT.md).
  </action>
  <verify>
    <automated>cd frontend && npm test -- url-normalize --run 2>&1 | tail -30</automated>
  </verify>
  <done>Test file exists. `cd frontend && npm test -- url-normalize --run` fails with "Cannot find module '../url-normalize'" (or equivalent module-not-found error) — RED state confirmed before Task 2 implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement normalizeOrigin + dedupeOrigins + error classes</name>
  <files>frontend/src/lib/builder/url-normalize.ts</files>
  <read_first>
    - frontend/src/lib/builder/__tests__/url-normalize.test.ts (just created in Task 1)
    - backend/app/modules/embed_tokens/schemas.py:14-31 (mirror the contract)
  </read_first>
  <action>
    Create `frontend/src/lib/builder/url-normalize.ts`. Implement (per CONTEXT.md locked decision: Pitfall #8 normalization at the chip-input boundary):

    1. Two named error classes that extend `Error`:
       - `WildcardOriginError` with message "Wildcard origin not allowed"
       - `InvalidOriginError` with the malformed input echoed in the message

    2. `export function normalizeOrigin(input: string): string`:
       - Trim whitespace.
       - If trimmed equals '*' or starts with '*' → throw WildcardOriginError (caller surfaces inline UI error per UI-SPEC Normalization Contract).
       - If trimmed is empty after trim → throw InvalidOriginError.
       - If no scheme (no '://' prefix), prepend 'https://'.
       - Construct `new URL(prepended)`. If the URL constructor throws (malformed input), rethrow as InvalidOriginError preserving original input in message.
       - Extract `protocol` (drop trailing colon → 'http' or 'https'), `hostname` (lowercase via toLowerCase()), `port`.
       - If port is default for scheme (80 for http, 443 for https), drop it. URL.port is already '' in those cases via the WHATWG URL parser — but verify with an explicit check to defend against future parser changes.
       - Return `${scheme}://${host}` or `${scheme}://${host}:${port}` if port is non-empty.

    3. `export function dedupeOrigins(inputs: string[]): string[]`:
       - Iterate inputs. For each, try `normalizeOrigin(input)`. Catch WildcardOriginError → skip silently. Catch InvalidOriginError → skip silently. Any other error → rethrow.
       - Use a Set keyed on the canonical form to dedupe while preserving first-seen order: maintain `seen = new Set<string>()` and `result: string[] = []`; push only when not in seen.

    No fenced code blocks inside this action — directive prose only. No regex hacks; use the WHATWG URL parser via `new URL(...)`. Use real try/catch.

    Mirror backend semantics exactly so the chip the user sees post-Enter equals the chip the backend would persist post-PATCH (eliminates flash-of-non-canonical-render).
  </action>
  <verify>
    <automated>cd frontend && npm test -- url-normalize --run 2>&1 | tail -20</automated>
  </verify>
  <done>
    All unit tests in `frontend/src/lib/builder/__tests__/url-normalize.test.ts` pass GREEN. `grep -E '^export (function|class) (normalizeOrigin|dedupeOrigins|WildcardOriginError|InvalidOriginError)' frontend/src/lib/builder/url-normalize.ts` returns exactly 4 lines (one per export). Module is importable as `@/lib/builder/url-normalize`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Parity-check frontend canonical form against backend _normalize_origin</name>
  <files>frontend/src/lib/builder/__tests__/url-normalize.test.ts</files>
  <read_first>
    - backend/app/modules/embed_tokens/schemas.py:14-31
    - frontend/src/lib/builder/url-normalize.ts (just implemented in Task 2)
  </read_first>
  <action>
    Append a third `describe('parity with backend _normalize_origin')` block to the test file. Include EXACT input/output pairs the backend already accepts (verified by reading `backend/tests/test_embed_tokens.py` patterns for `allowed_origins=[...]` lists). Pin at least these 5 cases:

    - 'http://localhost:3000' → 'http://localhost:3000'
    - 'https://dashboard.example.com' → 'https://dashboard.example.com'
    - 'https://partner.example' → 'https://partner.example'
    - 'HTTPS://EXAMPLE.COM:443/' → 'https://example.com' (case + default-port + trailing-slash all stripped)
    - 'http://example.com:8080' → 'http://example.com:8080' (non-default port preserved)

    This is a regression guard. If the frontend ever drifts from backend semantics, this test fails and the executor knows to either fix frontend OR open a coordinated backend+frontend change.

    Do NOT run the backend here — this is a pure frontend-side pin against known backend behavior. The backend tests in `backend/tests/test_embed_tokens.py` and `backend/tests/test_embed_framing_csp.py` already cover backend semantics. This test only pins that the frontend matches.
  </action>
  <verify>
    <automated>cd frontend && npm test -- url-normalize --run 2>&1 | tail -10</automated>
  </verify>
  <done>All tests in url-normalize.test.ts pass GREEN. Backend-parity describe block is present with at least 5 explicit case pairs.</done>
</task>

</tasks>

<verification>
  <automated>cd frontend && npm test -- url-normalize --run</automated>
  <automated>grep -c 'export' frontend/src/lib/builder/url-normalize.ts</automated>
  <automated>grep -E 'WildcardOriginError|InvalidOriginError' frontend/src/lib/builder/url-normalize.ts | wc -l</automated>
</verification>

<success_criteria>
- `frontend/src/lib/builder/url-normalize.ts` exists and exports `normalizeOrigin`, `dedupeOrigins`, `WildcardOriginError`, `InvalidOriginError` (4 named exports).
- `cd frontend && npm test -- url-normalize --run` passes all tests in `url-normalize.test.ts` (at least 14 cases across 3 describe blocks).
- `normalizeOrigin('*')` throws `WildcardOriginError` (CSP no-`*` invariant locked at unit boundary).
- No regression in existing frontend tests: `cd frontend && npm test -- --run 2>&1 | tail -5` shows green.
- `grep -n "parseOrigins" frontend/src/components/builder/SharePanel.tsx` still returns 2 matches (legacy helper unchanged in this plan — Plan 04 removes it).
</success_criteria>

<output>
Create `.planning/phases/1137-sharing-and-embed-polish/1137-01-SUMMARY.md` when done. Include:
- Files created (url-normalize.ts + test file)
- Exported names (4 named exports)
- Test count (number of `it()` blocks per describe)
- Backend parity verified: yes/no with citation to `backend/app/modules/embed_tokens/schemas.py:14-31`
- Hand-off contract for Plan 04: import path `@/lib/builder/url-normalize`, consumed exports
</output>
