---
phase: 1139-quality-sweep-and-playwright-close-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-QUALITY-GATES.md
autonomous: true
requirements: [QA-03]

must_haves:
  truths:
    - "typecheck exits 0"
    - "vitest full suite is green"
    - "lint exits 0"
    - "e2e:smoke:builder is green"
    - "i18n parity (resources test + changed-namespace check) passes"
    - "Any failure is classified as pre-existing (allowlisted) or new-regression with evidence"
  artifacts:
    - path: ".planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-QUALITY-GATES.md"
      provides: "Recorded pass/fail for all five gates with exact commands + exit codes"
      contains: "typecheck"
  key_links:
    - from: "1139-QUALITY-GATES.md"
      to: "frontend/package.json + root package.json scripts"
      via: "exact npm commands run with recorded exit codes"
      pattern: "npm run (typecheck|test|lint)"
---

<objective>
Run all five v1030 quality gates and record verbatim results: frontend `typecheck`, frontend `vitest` (full suite), frontend `lint`, root `e2e:smoke:builder`, and the i18n pair (`test:i18n` + `check:i18n:changed`). Produce `1139-QUALITY-GATES.md` documenting each gate's exact command, exit code, and pass/fail.

Purpose: Satisfy QA-03. This is the deterministic, headless half of the close-gate — it must be green (or every failure explicitly allowlisted as a known non-regression) before the orchestrator runs the live MCP smoke in Plan 03.

Output: `.planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-QUALITY-GATES.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<script_locations>
The npm scripts for this phase live in TWO package.json files. Do NOT assume one location.

`frontend/package.json` (run with `cd frontend && ...`):
- `typecheck`  → `tsc -b --noEmit`
- `test`       → `vitest run --passWithNoTests`  (this is the FULL vitest suite)
- `lint`       → `eslint .`
- `test:i18n`  → `vitest run src/i18n/resources.test.ts`
- `check:i18n:changed` → `node ./scripts/check-i18n-changed-namespaces.mjs`

ROOT `package.json` (run from repo root, NO `cd`):
- `e2e:smoke:builder` → `npx playwright test e2e/builder.spec.ts e2e/builder-styling.spec.ts e2e/builder-v1-5.spec.ts --project=chromium`
</script_locations>

<pre_existing_failure_allowlist>
Per project memory (`project_v1011_e2e_pre_existing_failures.md`), these two failures are NOT regressions — they reproduce on baseline commit `736cffca`:
- `e2e/accessibility.spec.ts:151` (axe `aria-required-children` + `nested-interactive`)
- `e2e/builder-unified-stack.spec.ts:193` (drag-reorder picks basemap-group, blocked by `disabled.droppable`)

IMPORTANT: NEITHER spec is in the `e2e:smoke:builder` subset (which runs only `builder.spec.ts`, `builder-styling.spec.ts`, `builder-v1-5.spec.ts`). Per project memory, these two pre-existing failures "slip past the e2e:smoke:builder subset." Therefore `e2e:smoke:builder` is expected to be FULLY GREEN. If it is NOT green, that is a NEW regression and must be reported as a blocker — do not allowlist it under the names above.
</pre_existing_failure_allowlist>

<i18n_note>
The "i18n 2/2" success criterion = two checks, both in `frontend/package.json`:
1. `npm run test:i18n` — the de/es/fr/en resource parity vitest (`src/i18n/resources.test.ts`).
2. `npm run check:i18n:changed` — namespace-parity check across changed translation files.
Run both. "2/2" means both pass.
</i18n_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run frontend gates (typecheck, vitest, lint) and capture results</name>
  <files>(no source edits — this task only runs commands and records output)</files>
  <action>
    Run the three frontend gates from the `frontend/` directory, each as a separate command so individual exit codes are unambiguous:
    1. `cd frontend && npm run typecheck` — capture exit code.
    2. `cd frontend && npm run test` — the full vitest suite; capture exit code AND the final pass/fail tally line (e.g. "Tests N passed (N)").
    3. `cd frontend && npm run lint` — capture exit code.

    For each gate, record the exact command, the exit code, and (for vitest) the test-count summary. If any gate fails, capture the failing file paths and the first relevant error lines (do NOT paste hundreds of lines — capture the failing test names / rule IDs / file:line).

    Do NOT attempt to fix failures in this task. Classification + reporting happens in Task 3. If vitest reveals a genuinely broken test from v1030 work, note it precisely; the orchestrator decides whether to spawn a gap-closure plan.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck && npm run lint && npm run test</automated>
  </verify>
  <done>typecheck exit 0, lint exit 0, vitest exit 0 with a recorded pass tally — OR each non-zero exit captured with failing identifiers for Task 3 classification.</done>
</task>

<task type="auto">
  <name>Task 2: Run e2e:smoke:builder and the i18n pair, capture results</name>
  <files>(no source edits — this task only runs commands and records output)</files>
  <action>
    Run from the correct locations (e2e is ROOT, i18n is frontend):
    1. `npm run e2e:smoke:builder` (repo root, NO cd) — capture exit code + per-spec pass/fail. Expected FULLY GREEN per the allowlist note (the two known pre-existing failures are NOT in this subset).
    2. `cd frontend && npm run test:i18n` — capture exit code.
    3. `cd frontend && npm run check:i18n:changed` — capture exit code.

    The e2e smoke requires the live stack at `localhost:8080` (already running and healthy: api/db/frontend/titiler/worker all up). If Playwright reports an auth/storage-state expiry, note it — the root `e2e:smoke:builder-hardening` config runs an auth-setup project first, but the plain `e2e:smoke:builder` relies on existing storage state; a stale-token failure is an environment issue, NOT a code regression, and should be recorded as such (re-run after auth refresh if needed).

    Record exact command + exit code for all three.
  </action>
  <verify>
    <automated>npm run e2e:smoke:builder</automated>
  </verify>
  <done>e2e:smoke:builder green (exit 0); test:i18n exit 0; check:i18n:changed exit 0 — OR each failure captured with spec/file identifiers and an environment-vs-regression note.</done>
</task>

<task type="auto">
  <name>Task 3: Write 1139-QUALITY-GATES.md with classified results</name>
  <files>.planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-QUALITY-GATES.md</files>
  <action>
    Create `1139-QUALITY-GATES.md` with a results table: one row per gate (typecheck / vitest / lint / e2e:smoke:builder / test:i18n / check:i18n:changed), columns = Command, Exit Code, Verdict (PASS / FAIL), Notes.

    Add a "Failure Classification" section. For each FAIL:
    - If it matches the pre-existing allowlist (`accessibility.spec.ts:151` or `builder-unified-stack.spec.ts:193`), mark `PRE-EXISTING (non-regression, reproduces on 736cffca)` AND note it should NOT have surfaced via `e2e:smoke:builder` (those specs are out of that subset) — so if it appears there, re-classify as NEW.
    - If it is a stale-auth/storage-state environment issue, mark `ENVIRONMENT (not a code regression)` with the re-run result after refresh.
    - Otherwise mark `NEW REGRESSION` with file:line + the failing identifier, and flag for orchestrator gap-closure.

    End with an explicit "QA-03 verdict: GREEN | GREEN-WITH-ALLOWLISTED | BLOCKED" line. GREEN means all six checks exit 0. GREEN-WITH-ALLOWLISTED is only valid if every failure is environment or pre-existing-allowlisted. Any NEW REGRESSION → BLOCKED.

    Do not invent results — transcribe the actual exit codes and tallies observed in Tasks 1-2.
  </action>
  <verify>
    <automated>test -f .planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-QUALITY-GATES.md && grep -q "QA-03 verdict:" .planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-QUALITY-GATES.md</automated>
  </verify>
  <done>1139-QUALITY-GATES.md exists with a per-gate results table, a Failure Classification section, and an explicit "QA-03 verdict:" line reflecting the real observed exit codes.</done>
</task>

</tasks>

<verification>
- All five gate categories run from their correct package.json location (frontend vs root).
- e2e:smoke:builder treated as expected-green (pre-existing failures are out of subset).
- i18n counted as 2 checks (test:i18n + check:i18n:changed).
- Every failure classified: PRE-EXISTING / ENVIRONMENT / NEW REGRESSION.
- `1139-QUALITY-GATES.md` carries an explicit QA-03 verdict.
</verification>

<success_criteria>
QA-03 satisfied: typecheck 0, vitest green, lint 0, e2e:smoke:builder green, i18n 2/2 — recorded in `1139-QUALITY-GATES.md` with any failure explicitly allowlisted (pre-existing/environment) or escalated (new regression).
</success_criteria>

<output>
Create `.planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-01-SUMMARY.md` when done.
</output>
