---
phase: 1139-quality-sweep-and-playwright-close-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - backend/openapi.json
  - .planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-OPENAPI-DECISION.md
autonomous: true
requirements: [QA-04]

must_haves:
  truths:
    - "CHANGELOG [Unreleased] documents v1030 builder-polish work with measured numbers"
    - "make openapi was run; the resulting backend/openapi.json diff is recorded (empty or non-empty)"
    - "Pitfall #15 contract holds: if the OpenAPI/SDK diff is non-empty, CHANGELOG mentions it; if empty, that is stated explicitly"
    - "make sdks-check passes (SDK matches the committed OpenAPI snapshot)"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "v1030 [Unreleased] entries with measured numbers"
      contains: "RasterEditor"
    - path: ".planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-OPENAPI-DECISION.md"
      provides: "Proof of the Pitfall #15 check: make openapi/openapi-check/sdks-check output + diff verdict"
      contains: "openapi-check"
  key_links:
    - from: "CHANGELOG.md [Unreleased]"
      to: "v1030 phases 1135/1136/1137/1138"
      via: "measured-number bullets (RasterEditor controls, line-cap/join, AI Shape B, share polish set)"
      pattern: "line-cap|RasterEditor|Powered by GeoLens"
---

<objective>
Populate CHANGELOG `[Unreleased]` for v1030 builder-polish work with measured numbers, then satisfy the Pitfall #15 OpenAPI/SDK contract: run `make openapi` (dual-snapshot order: geolens FIRST), `make openapi-check`, and `make sdks-check`, and record whether the backend surface changed. Document the verdict in `1139-OPENAPI-DECISION.md`.

Purpose: Satisfy QA-04. Pitfall #15 is a HARD blocker — an OpenAPI/types diff that is non-empty while CHANGELOG is silent fails the gate. The inverse (asserting a diff exists when it does not) is also wrong. This plan PROVES the actual state rather than assuming it.

Output: updated `CHANGELOG.md`; `1139-OPENAPI-DECISION.md` with the regeneration evidence; `backend/openapi.json` only if regeneration produced a real change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@CHANGELOG.md

<backend_change_inventory>
v1030 backend commits that touched Python (verified via git log against `8fd5f104~1..HEAD -- backend/**`):
- `4b643bde` fix(1135): AI-08 — `chat_actions.py` emits `show_query_result` `rows` for non-spatial queries. This adds a key to a STREAMED dict payload inside the chat SSE action stream — NOT a Pydantic `response_model` field. The `/ai/chat/` route's `ChatResponse` model is unchanged.
- `e7a0f4fc` + `88eb2f3d` (1135-05): docstring + test only on `_validate_chat_layers` / `_schema_cache_key`. Docstrings do NOT affect the OpenAPI schema.
- `8e5587ac` + `acf52303` + `63ce7a8a` (1137): `embed_tokens/schemas.py` `_validate_origins` rejects `"*"` (a pydantic validator — runtime validation, not a field-shape change) + `maps/router.py` CSP `frame-ancestors` (a response HEADER, not a schema). Validators and headers do NOT change the OpenAPI schema surface.

EXPECTATION (to be PROVEN, not assumed): `git diff --stat 8fd5f104~1..HEAD -- backend/openapi.json` is currently EMPTY, which is consistent with the above (docstring + dict-payload + validator + header changes do not move the schema). `make openapi` should regenerate to a no-op diff. If it does NOT — if regeneration produces a real diff — that is a genuine drift that MUST be committed AND called out in CHANGELOG. PROVE which case is true via Task 2.
</backend_change_inventory>

<measured_numbers>
The CHANGELOG measured numbers are pre-decided in CONTEXT.md (verified against Phase MCP-VERIFY docs):
- RasterEditor stub closed → **7 controls** (Brightness min, Brightness max, Contrast, Saturation, Hue, Fade, Opacity) + a Reset button (1136 MCP-BACKFILL confirms "7 sliders visible" + Reset).
- LineEditor: **line-cap** (butt/round/square) + **line-join** (bevel/round/miter) added as MapLibre LAYOUT properties.
- FillEditor: 3D-extrusion "Range: X–Y, N features" hint from `dataset_sample_values`.
- BasemapEditor: "No basemap" preset (first card); DETAIL LEVEL stays gone (v1011 INV-01 holds).
- AI confirm-before-apply = **Shape B** (`pendingLayers` staging buffer in `chat-action-staging.ts`, above `dispatchLayerAction`) + action preview chips + inline data-analysis card + viewport-aware suggestion chips.
- Share polish set: allowed-origins **chips** + expiration **presets** (1d/7d/30d/1y/Never) + "Powered by GeoLens" community **branding** + **legend+title** in shared/embed/export + conditional **iframe preview** (all enterprise-gated; community edition keeps simplified dialog).
- Easy wins: Cmd/Ctrl+S save (with preventDefault + dialog-open no-op), popup URL auto-linkify + media render + `{column}` token doc, empty-layer "0 features — check your filter" hint with Clear-filter.
- MAP tier-1 fixes: delete-layer across all render modes (no orphans), visibility-toggle per-adapter, rename-group rAF focus, ≤800px sidebar/coord/Sheet layout fixes, scroll containment, filter-pill overflow, Notes presence dot.
</measured_numbers>

<dual_snapshot_order>
Per project memory (`project_openapi_dual_snapshot_refresh_order.md`): ALWAYS `make openapi` (geolens) BEFORE `npm run fetch-openapi` (sibling docs repo). The `fetch-openapi` step lives in the SIBLING repo `~/Code/getgeolens.com` — it is OUT of the geolens working tree and OUT of this plan's scope. This plan owns only the geolens half: `make openapi` + `make openapi-check` + `make sdks-check`. If the geolens diff is non-empty, note in `1139-OPENAPI-DECISION.md` that the sibling docs `npm run fetch-openapi` is a required downstream manual follow-up (record it; do not attempt it from here).
</dual_snapshot_order>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate CHANGELOG [Unreleased] with v1030 measured numbers</name>
  <files>CHANGELOG.md</files>
  <action>
    Edit the `[Unreleased]` section of `CHANGELOG.md`. The existing entries are v1029 DCAT-US content; ADD v1030 builder-polish entries — do NOT delete or rewrite the existing v1029 bullets (this milestone has not shipped yet, so [Unreleased] accumulates both).

    Add user-facing bullets under the existing `### Added`, `### Changed`, and `### Fixed` subsections using the measured numbers from `<measured_numbers>`. Group sensibly:
    - Added: RasterEditor 7 controls + Reset; line-cap/line-join; FillEditor 3D extrusion range hint; "No basemap" basemap preset; AI confirm-before-apply staging (Shape B) with preview chips + inline data-analysis card + viewport-aware suggestions; allowed-origins chips; expiration presets; "Powered by GeoLens" community branding; legend+title in shared/embed/export; conditional embed iframe preview; Cmd/Ctrl+S save shortcut; popup URL/media auto-render with `{column}` token docs; empty-layer "0 features" hint with Clear-filter.
    - Fixed: delete-layer orphan-source cleanup across all render modes; visibility-toggle per-adapter immediacy; rename-group focus race (rAF-deferred); ≤800px sidebar/NavigationControl/coord-readout/Sheet layout collisions; map-body scroll containment; filter-pill overflow; Notes presence indicator.

    Write plain user-facing language (this file is the GitHub release-note source of truth). No internal REQ IDs in the bullets. Be specific with the numbers (e.g., "seven raster adjustment controls", "line-cap and line-join", "five expiration presets (1 day / 7 days / 30 days / 1 year / Never)").

    Do NOT add an OpenAPI/SDK bullet yet — that depends on Task 2's diff verdict. Task 3 adds it conditionally.
  </action>
  <verify>
    <automated>grep -qi "RasterEditor\|raster adjustment" CHANGELOG.md && grep -qi "line-cap" CHANGELOG.md && grep -qi "Powered by GeoLens" CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG [Unreleased] contains v1030 builder-polish bullets with measured numbers (raster controls, line-cap/join, share polish set, AI staging) alongside the preserved v1029 entries.</done>
</task>

<task type="auto">
  <name>Task 2: Run make openapi / openapi-check / sdks-check and record the diff verdict</name>
  <files>backend/openapi.json (only if regeneration changes it), .planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-OPENAPI-DECISION.md</files>
  <action>
    Prove the Pitfall #15 state. Run in order from repo root:
    1. `git diff --stat HEAD -- backend/openapi.json sdks/` — confirm clean starting tree (record it).
    2. `make openapi` — regenerates `backend/openapi.json` from the runtime FastAPI app (dual-snapshot order: geolens first). NOTE: `make openapi` runs `cd backend && PYTHONPATH=. uv run python scripts/dump_openapi.py` and needs backend deps available; if it requires the `.env.test` DB recipe to import the app, apply `set -a && source .env.test && set +a` first (per project memory, backend tooling needs `POSTGRES_HOST=localhost POSTGRES_PORT=5434`). If `make openapi` cannot run locally, fall back to `make openapi-check` (which only compares, no DB write) and record the blocker.
    3. `git diff --stat -- backend/openapi.json` — capture whether regeneration produced a real change.
    4. `make openapi-check` — must exit 0 (snapshot matches runtime).
    5. `make sdks-check` — regenerates both SDKs and `git diff --exit-code`s them; must exit 0 (SDK matches the committed snapshot, excluding hand-written wrappers).

    Write `1139-OPENAPI-DECISION.md` recording: the starting-tree state, each command's exit code, the `backend/openapi.json` post-regeneration diff (EMPTY or the changed paths), and an explicit verdict line:
    - "OpenAPI surface verdict: UNCHANGED" if the diff is empty AND openapi-check + sdks-check pass.
    - "OpenAPI surface verdict: CHANGED" if regeneration produced a real diff — list the changed schema paths.

    If the diff is CHANGED, the regenerated `backend/openapi.json` (and any `sdks/` changes) become part of this plan's output and MUST be committed. If UNCHANGED, leave `backend/openapi.json` as-is.

    Cross-check against the `<backend_change_inventory>` reasoning: the expectation is UNCHANGED (docstring/dict-payload/validator/header changes don't move the schema). If the actual result is CHANGED, that contradicts the inventory and the decision doc must explain which backend change caused it.
  </action>
  <verify>
    <automated>make openapi-check && make sdks-check && test -f .planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-OPENAPI-DECISION.md && grep -q "OpenAPI surface verdict:" .planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-OPENAPI-DECISION.md</automated>
  </verify>
  <done>make openapi-check exit 0, make sdks-check exit 0, and 1139-OPENAPI-DECISION.md records the regeneration diff with an explicit UNCHANGED/CHANGED verdict. If CHANGED, backend/openapi.json + sdks/ updates are staged for commit.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile CHANGELOG against the OpenAPI verdict (Pitfall #15 closure)</name>
  <files>CHANGELOG.md, .planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-OPENAPI-DECISION.md</files>
  <action>
    Close the Pitfall #15 loop based on Task 2's verdict:
    - If verdict is CHANGED: add a CHANGELOG bullet describing the API change in user-facing terms (e.g., under Changed: "AI chat data-analysis responses now include row data for non-spatial queries"), and note in `1139-OPENAPI-DECISION.md` that the sibling docs repo (`~/Code/getgeolens.com`) needs `npm run fetch-openapi` as a manual downstream follow-up.
    - If verdict is UNCHANGED: add a single line to the CHANGELOG `### Verification` subsection stating the OpenAPI snapshot and SDKs were regenerated and confirmed drift-free for v1030 (so the close-gate record shows the check was performed and intentionally produced no schema change — NOT silence). Example: "OpenAPI snapshot (`make openapi-check`) and generated SDKs (`make sdks-check`) regenerated clean for v1030 — backend docstring/validation/header changes did not alter the schema surface."

    This guarantees the Pitfall #15 invariant holds in BOTH directions: a non-empty diff is never silent, and an empty diff is explicitly recorded rather than ambiguous.
  </action>
  <verify>
    <automated>grep -qi "openapi\|sdk" CHANGELOG.md && grep -q "OpenAPI surface verdict:" .planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-OPENAPI-DECISION.md</automated>
  </verify>
  <done>CHANGELOG reflects the OpenAPI verdict (a Changed bullet if CHANGED, or a Verification line confirming drift-free regeneration if UNCHANGED); Pitfall #15 closed in both directions.</done>
</task>

</tasks>

<verification>
- CHANGELOG [Unreleased] carries v1030 measured numbers alongside preserved v1029 entries.
- `make openapi-check` and `make sdks-check` both exit 0.
- `1139-OPENAPI-DECISION.md` records the regeneration diff with an UNCHANGED/CHANGED verdict.
- Pitfall #15 satisfied both ways: non-empty diff → CHANGELOG bullet + sibling-repo follow-up note; empty diff → explicit Verification line.
</verification>

<success_criteria>
QA-04 satisfied: CHANGELOG [Unreleased] populated with measured v1030 numbers; OpenAPI snapshot + SDKs proven in-sync via `make openapi-check`/`make sdks-check`; the OpenAPI verdict is recorded and reconciled with CHANGELOG so Pitfall #15 cannot be violated in either direction.
</success_criteria>

<output>
Create `.planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-02-SUMMARY.md` when done.
</output>
