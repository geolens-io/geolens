---
phase: 1139-quality-sweep-and-playwright-close-gate
plan: 03
type: execute
wave: 2
depends_on: ["1139-01", "1139-02"]
files_modified:
  - .planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-CLOSE-GATE-SMOKE.md
autonomous: false
requirements: [QA-01, QA-02]

must_haves:
  truths:
    - "A 3-viewport (1440x900 / 800x600 / 414x896) live-MCP checklist exists covering every render mode, layer op, save-persist, and shared/embed parity"
    - "An AI_ENABLED=false checklist exists with the exact env-override + restart + restore procedure"
    - "Each checklist item names the canonical map, the exact surface, and the expected observation"
    - "The orchestrator (not the executor) is the designated MCP driver per feedback_playwright_mcp_orchestrator_only"
  artifacts:
    - path: ".planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-CLOSE-GATE-SMOKE.md"
      provides: "Orchestrator-runnable 3-viewport + disabled-AI MCP smoke checklist with verdict columns"
      contains: "414x896"
  key_links:
    - from: "1139-CLOSE-GATE-SMOKE.md"
      to: "canonical map c39be324-6815-40e5-8143-00a2723827b2 + carry-forward items from MCP-BACKFILL-260527.md"
      via: "per-viewport checklist rows mapping each carried-forward surface to a live observation"
      pattern: "c39be324"
---

<objective>
Author `1139-CLOSE-GATE-SMOKE.md`: a precise, orchestrator-runnable checklist for the v1030 close-gate live Playwright MCP smoke. It must cover (a) all three viewports (1440x900, 800x600, 414x896) across every render mode + layer ops + save-persist + shared/embed parity with ZERO console errors, and (b) the AI_ENABLED=false disabled-AI smoke with the exact env-override/restart/restore procedure.

Purpose: Satisfy QA-01 and QA-02. The executor cannot run live MCP (the gsd-executor subagent lacks mcp__playwright__* — confirmed in MCP-BACKFILL-260527.md). This plan's executor deliverable is the WRITTEN CHECKLIST ONLY. The orchestrator then drives the live MCP smoke directly against the checklist and records verdicts.

Output: `.planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-CLOSE-GATE-SMOKE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/MCP-BACKFILL-260527.md
@.planning/phases/1137-sharing-and-embed-polish/1137-MCP-VERIFY.md
@.planning/phases/1138-easy-win-sweep/1138-MCP-VERIFY.md

<carry_forward_inventory>
These surfaces were unit-pinned (or test-pinned behind the enterprise gate) in prior phases and explicitly deferred to the 1139 close-gate for LIVE interactive verification. The checklist MUST cover each:

From MCP-BACKFILL-260527.md "What's Carried Forward to Phase 1139 Close-Gate":
1. Trigger a destructive AI action ("add NHD lakes layer") -> staging tray appears with Accept/Reject chips -> Reject -> map state byte-equal to pre-prompt (AI-01, AI-09).
2. Send an analysis query -> inline data-analysis card renders (AI-08). NOTE: backend AI-08 fix 4b643bde emits show_query_result rows for non-spatial queries — this is the live confirmation that the SF-MCP-01 carry-forward is closed.
3. Simulate/observe a 403/503 -> recoverable error banner with Retry (AI-03).
4. Delete-layer for EVERY render mode in the stack (fill / line / circle / symbol / heatmap / cluster / raster) — no orphan sources, no stack drift (MAP-17).
5. Toggle visibility for each render mode -> canvas updates immediately (MAP-18).
6. Enter rename mode on a group -> rAF-deferred focus lands (MAP-16).
7. Pan/zoom on canvas -> body scrollY stays 0 (MAP-19).
8. Activate 4+ filter chips -> overflow scrolls within the 40vh cap, no measure-widget collision (MAP-20).
9. Add notes to a map -> presence dot appears on the Notes button (MAP-22).
10. Open LineEditor + FillEditor -> line-cap/line-join Selects + extrusion "Range: X-Y, N features" hint render live (EDITOR-LINE-01/02, EDITOR-FILL-04).

From 1138-MCP-VERIFY.md "What Wasn't Verified Live":
11. Open a popup with an embedded URL/media attribute -> auto-linkify + image/video render (EASY-11).
12. Set a filter that returns 0 features -> "0 features — check your filter" hint + Clear-filter button works (EASY-18).

From 1137-MCP-VERIFY.md "What Wasn't Verified Live (Enterprise Gate)":
13. Share/embed parity: shared viewer + embed viewer render the map, "Powered by GeoLens" branding shows (community edition), title renders, sandbox stays allow-scripts only. F3: confirm legend renders on a map WITH categorical/graduated symbology (the canonical ADK map had no legend-eligible layer — pick or style a layer that triggers a legend entry, OR note that no legend-eligible layer exists in the current map set and the legend-suppression behavior is correct).
NOTE: chips/presets/iframe-preview are enterprise-gated (community stack hides them) — these stay unit-pinned and are OUT of the community-edition live smoke. Record them as "enterprise-gated, unit-pinned" rather than failing them.
</carry_forward_inventory>

<environment_facts>
- Canonical map: c39be324-6815-40e5-8143-00a2723827b2 (Adirondack High Peaks — Terrain & Trails). Confirm it still has the full render-mode spread (DEM hillshade + raster + lines + polygons). If a render mode (e.g. circle/cluster/heatmap/symbol) is NOT present on the canonical map, the checklist must name a representative second map per mode OR instruct the orchestrator to add a layer of that mode to a throwaway copy for the delete/visibility sweep.
- Stack: frontend localhost:8080, api localhost:8001; api/db/frontend/titiler/worker all healthy. Auth: admin/admin.
- AI is CURRENTLY ENABLED (both ANTHROPIC_API_KEY and OPENAI_API_KEY are set in .env) — correct for QA-01.
- AI_ENABLED=false toggle: GeoLens has NO literal AI_ENABLED env var. AI availability is gated by provider keys in backend/app/core/config.py (anthropic_api_key / openai_api_key). "AI_ENABLED=false" = comment out BOTH ANTHROPIC_API_KEY and OPENAI_API_KEY in .env, restart api+worker, smoke, then RESTORE both keys and restart again. The frontend reads availability via frontend/src/hooks/use-ai-availability.ts; the disabled state must surface an actionable panel (no inert dead-end button) with zero /ai/* console errors. Relevant /ai/* routes: /ai/chat/, /ai/generate-map/, /ai/metadata/*.
</environment_facts>

<orchestrator_driver_note>
Per feedback_playwright_mcp_orchestrator_only: the gsd-executor subagent grants Read, Write, Edit, Bash, Grep, Glob, mcp__context7__* — NO mcp__playwright__*. The executor for THIS plan ONLY writes the checklist file. It must NOT attempt to call mcp__playwright__*, and must NOT fall back to authoring .spec.ts files in place of live MCP (the exact anti-pattern that necessitated the backfill). The checklist is structured so the ORCHESTRATOR executes it directly after this plan returns.
</orchestrator_driver_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the 3-viewport live-MCP smoke checklist (QA-01)</name>
  <files>.planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-CLOSE-GATE-SMOKE.md</files>
  <action>
    Create 1139-CLOSE-GATE-SMOKE.md. Open with a header block naming: driver = orchestrator (mcp__playwright__*), canonical map c39be324-..., viewports [1440x900, 800x600, 414x896], stack URLs, auth admin/admin.

    Add a "QA-01 — 3-Viewport Live MCP Smoke" section as a checklist table. Columns: # | Surface / Action | Viewport(s) | Expected observation | Verdict | Notes. One row per carried-forward item 1-13 from carry_forward_inventory, mapped to the viewport(s) where it is most meaningful:
    - Render-mode renders + delete-layer + visibility-toggle (items 4, 5): run at 1440x900 primary, spot-check the layout at 800x600 + 414x896. Enumerate the render modes explicitly (fill / line / circle / symbol / heatmap / cluster / raster) so the orchestrator ticks each.
    - Layout-sensitive items (rename focus, scroll containment, filter overflow, Notes dot, popup, empty-filter — items 6, 7, 8, 9, 11, 12): include the 800x600 + 414x896 columns since these are the regression-risk viewports.
    - Save-persist: explicit row — make a styling change -> Save -> reload -> change persists.
    - Shared/embed parity (item 13): shared viewer + embed viewer render, branding shows, title renders, sandbox allow-scripts only, legend behavior confirmed (or correctly suppressed).
    - AI items (1, 2, 3): destructive-action staging tray + Reject byte-equal; analysis inline card; 403/503 error banner with Retry — all at 1440x900 (AI rail is widest there).

    Add an explicit "ZERO console errors" gate row per viewport — the orchestrator must capture browser console state at each viewport and confirm no errors (the known non-blocking MapLibre terrain maxzoom warning is allowlisted per v1028 close-gate precedent; record it if seen but it is NOT a failure).

    Where the canonical map lacks a render mode, give the orchestrator a concrete fallback instruction (name a representative map, or "add a {mode} layer to a throwaway copy, exercise delete/visibility, then delete the copy"). Do NOT leave any render mode unaddressed.

    For enterprise-gated share surfaces (chips/presets/iframe-preview), add a single row marking them "enterprise-gated — unit-pinned in Phase 1137 Plans 04/05/06; OUT of community-edition live smoke" so the orchestrator does not mistake their absence for a failure.
  </action>
  <verify>
    <automated>test -f .planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-CLOSE-GATE-SMOKE.md && grep -q "1440x900" .planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-CLOSE-GATE-SMOKE.md && grep -q "800x600" .planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-CLOSE-GATE-SMOKE.md && grep -q "414x896" .planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-CLOSE-GATE-SMOKE.md</automated>
  </verify>
  <done>The QA-01 section exists with a per-item checklist table covering all 13 carried-forward surfaces, all three viewports represented, every render mode enumerated for the delete/visibility sweep, a save-persist row, a shared/embed parity row, and a per-viewport zero-console-errors gate. Enterprise-gated share surfaces are marked out-of-scope-for-community rather than as failures.</done>
</task>

<task type="auto">
  <name>Task 2: Write the AI_ENABLED=false disabled-AI smoke procedure (QA-02)</name>
  <files>.planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-CLOSE-GATE-SMOKE.md</files>
  <action>
    Append a "QA-02 — Disabled-AI Smoke (AI_ENABLED=false)" section to the same file. Give the orchestrator the EXACT procedure (since there is no literal AI_ENABLED var):
    1. Back up .env (or note the two key lines to restore): comment out ANTHROPIC_API_KEY and OPENAI_API_KEY in .env.
    2. Restart the api + worker services so config re-reads (docker compose restart api worker per the project memory stack-restart decision tree — frontend/Vite needs no rebuild; this is a config-only change, NOT a migration).
    3. Reload localhost:8080, open the builder on the canonical map, open the AI rail.
    4. Confirm: the AI rail surfaces an ACTIONABLE disabled state (an explanatory panel — NOT an inert dead-end disabled button); there is NO inert button; the browser console shows ZERO /ai/* errors (no failed /ai/chat/, /ai/generate-map/, /ai/metadata/* network errors surfaced as console errors); the map canvas is NOT broken (renders normally).
    5. RESTORE: uncomment both keys in .env, restart api + worker again, reload, confirm AI rail returns to enabled state.

    Add a checklist table with rows: actionable disabled panel | no inert button | zero /ai/* console errors | canvas intact | restore-to-enabled confirmed. Columns: Check | Expected | Verdict | Notes.

    Include a hard reminder: the RESTORE step is mandatory — leaving AI disabled corrupts the stack state for any subsequent QA-01 re-run. The orchestrator must verify AI is re-enabled before declaring QA-02 complete.
  </action>
  <verify>
    <automated>grep -q "AI_ENABLED=false" .planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-CLOSE-GATE-SMOKE.md && grep -qi "restore" .planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-CLOSE-GATE-SMOKE.md && grep -q "ANTHROPIC_API_KEY" .planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-CLOSE-GATE-SMOKE.md</automated>
  </verify>
  <done>The QA-02 section gives the exact comment-out-keys + restart + smoke + restore procedure, names the /ai/* routes to watch, requires the actionable-disabled-panel + zero-console-error + canvas-intact checks, and makes the restore-to-enabled step a mandatory completion gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Orchestrator-driven live MCP close-gate (QA-01 + QA-02)</name>
  <files>.planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-CLOSE-GATE-SMOKE.md</files>
  <action>
    HANDOFF TO ORCHESTRATOR — the gsd-executor does NOT run this task (it lacks mcp__playwright__*). The executor's deliverable for this plan is the written checklist (Tasks 1-2 above). This checkpoint hands the completed 1139-CLOSE-GATE-SMOKE.md to the orchestrator, which drives the live Playwright MCP smoke directly and records every Verdict cell back into the file. The executor must NOT author .spec.ts files in place of live MCP and must NOT mark this task done on its own.
  </action>
  <what-built>1139-CLOSE-GATE-SMOKE.md — the orchestrator-runnable QA-01 (3-viewport) + QA-02 (disabled-AI) live MCP smoke checklist. The executor authored the checklist; live MCP execution is performed by the ORCHESTRATOR (the gsd-executor subagent cannot call mcp__playwright__*).</what-built>
  <how-to-verify>
    The orchestrator drives the live Playwright MCP smoke directly against 1139-CLOSE-GATE-SMOKE.md:
    1. QA-01: For each of the three viewports (1440x900, 800x600, 414x896), walk the checklist rows on the canonical map c39be324-6815-40e5-8143-00a2723827b2 (and any per-mode fallback maps the checklist names). Tick each Verdict cell. Capture browser console state per viewport and confirm zero errors (MapLibre terrain maxzoom warning is the only allowlisted noise).
    2. QA-02: Execute the disabled-AI procedure (comment out both provider keys -> restart api+worker -> smoke the AI rail disabled state + zero /ai/* console errors + canvas intact -> RESTORE both keys -> restart -> confirm AI re-enabled).
    3. Record all verdicts back into 1139-CLOSE-GATE-SMOKE.md. Any FAIL becomes a v1030 close blocker (spawn gap-closure) — every render mode must render, layer ops must work, save must persist, shared/embed parity must hold, and the disabled-AI state must be clean.
    Expected outcome: all QA-01 rows PASS across all three viewports with zero console errors; QA-02 disabled state is actionable with zero /ai/* errors and AI is restored to enabled.
  </how-to-verify>
  <verify>
    <automated>grep -qi "verdict" .planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-CLOSE-GATE-SMOKE.md</automated>
  </verify>
  <done>All QA-01 + QA-02 Verdict cells in 1139-CLOSE-GATE-SMOKE.md are recorded as PASS by the orchestrator (with AI restored to enabled), or failing surfaces are documented and routed to gap-closure.</done>
  <resume-signal>Type "approved" once all QA-01 + QA-02 verdicts are recorded as PASS in 1139-CLOSE-GATE-SMOKE.md (with AI restored to enabled), or describe the failing surfaces.</resume-signal>
</task>

</tasks>

<verification>
- 1139-CLOSE-GATE-SMOKE.md exists with both a QA-01 (3-viewport) and a QA-02 (disabled-AI) section.
- QA-01 covers all 13 carried-forward surfaces, every render mode for delete/visibility, save-persist, shared/embed parity, and a per-viewport zero-console-errors gate.
- QA-02 gives the exact key-comment-out + restart + restore procedure and names the /ai/* routes.
- The checkpoint records orchestrator-driven verdicts; the executor never calls mcp__playwright__*.
</verification>

<success_criteria>
QA-01 and QA-02 satisfied: the orchestrator-runnable checklist exists and, after the checkpoint, carries recorded PASS verdicts for the 3-viewport live MCP smoke (every render mode renders, layer ops work, save persists, shared/embed parity, zero console errors) and the disabled-AI smoke (actionable disabled state, no inert button, zero /ai/* errors, canvas intact, AI restored).
</success_criteria>

<output>
Create `.planning/phases/1139-quality-sweep-and-playwright-close-gate/1139-03-SUMMARY.md` when done.
</output>
