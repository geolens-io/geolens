---
phase: 1134-map-functionality-and-smaller-screen-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/builder/__tests__/UnifiedStackPanel.test.tsx
  - frontend/src/components/builder/__tests__/FolderGroupRow.test.tsx
autonomous: true
requirements:
  - MAP-16
tags:
  - builder
  - rename-focus
  - dnd-kit
  - radix

must_haves:
  truths:
    - "Entering rename-group mode (via kebab menu or double-click) gives focus to the rename text input on first paint via one rAF deferral"
    - "Synchronous focus is NOT used (negative-control pin so a regression would re-introduce the v1011 BUG-03 dnd-kit/Radix focus race)"
    - "UnifiedStackPanel integration test surfaces the FolderGroupRow rename path end-to-end (kebab → Rename group → input focused)"
  artifacts:
    - path: "frontend/src/components/builder/__tests__/UnifiedStackPanel.test.tsx"
      provides: "Extended with MAP-16 rename-focus integration test routed through the panel surface"
    - path: "frontend/src/components/builder/__tests__/FolderGroupRow.test.tsx"
      provides: "Extended with MAP-16 negative-control pin (synchronous focus path NOT used) — defends BUG-03 fix from accidental revert"
  key_links:
    - from: "FolderGroupRow editing useEffect"
      to: "requestAnimationFrame(() => inputRef.current?.focus())"
      via: "line 89-94 — rAF-deferred focus"
      pattern: "requestAnimationFrame\\("
---

<objective>
Pin the v1011 BUG-03 rename-group rAF-deferred focus contract via two regression tests, satisfying MAP-16's ROADMAP success criterion that names `UnifiedStackPanel.test.tsx` as the regression site.

Purpose: Phase 1133 audit classified `todo.md L145` ("rename group does not focus appropriately") as `closed-in-prior-milestone` (v1011 BUG-03 commit `80bddc14`). The current source at `FolderGroupRow.tsx:89-97` uses the canonical rAF-deferred pattern. The `FolderGroupRow.test.tsx` BUG-03 pin (Test 19 + Test 25 at lines 356+, 444+) covers the row in isolation. MAP-16 needs:
1. An integration test at the `UnifiedStackPanel.test.tsx` surface (per ROADMAP's named path) that proves the rename path works end-to-end through the panel.
2. A negative-control pin asserting synchronous focus is NOT used — protects against a future revert that drops the rAF deferral.

Output:
- Extended `UnifiedStackPanel.test.tsx`: 2 new tests, one integration + one a11y assertion on rename input.
- Extended `FolderGroupRow.test.tsx`: 1 new negative-control test that fails if synchronous focus is reintroduced.

This plan does NOT modify any source code under `frontend/src/components/` — the v1011 fix is already shipped. Pure test-pin work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-CONTEXT.md
@.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-UI-SPEC.md
@.planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md

@frontend/src/components/builder/UnifiedStackPanel.tsx
@frontend/src/components/builder/FolderGroupRow.tsx
@frontend/src/components/builder/__tests__/UnifiedStackPanel.test.tsx
@frontend/src/components/builder/__tests__/FolderGroupRow.test.tsx

<interfaces>
<!-- The rename-focus contract that ships today (v1011 BUG-03 PASS) -->

From `frontend/src/components/builder/FolderGroupRow.tsx:73-100` (current source — DO NOT MODIFY):
```tsx
const inputRef = useRef<HTMLInputElement | null>(null);
// editing useEffect lost the focus race to Radix DropdownMenu's
// restoreFocus on the trigger. Use rAF defer (one tick) so the
// editing input mounts and focuses AFTER Radix's restoreFocus.
// restoreFocus is skipped specifically when the rename item was just
// selected; other dismiss paths (Escape, outside click) still restore focus
// to the trigger.

// Auto-focus + select input text when entering edit mode. The rAF defer is
// load-bearing — see BUG-03 commit 80bddc14.
useEffect(() => {
  if (editing) {
    requestAnimationFrame(() => {
      if (inputRef.current) {
        inputRef.current.focus();
        inputRef.current.select();
      }
    });
  }
}, [editing]);
```

UI-SPEC IC-02 contract (from `1134-UI-SPEC.md`):
- One rAF deferral. NOT setTimeout(fn, 0) and NOT synchronous focus.
- Regression test: rename input `.focus()` asserted after `act(() => {})` wrapping the trigger.

From `frontend/src/components/builder/__tests__/FolderGroupRow.test.tsx:347-475` (existing BUG-03 pins):
- Test 19 (line 356): "BUG-03 — entering rename mode focuses the rename input (rAF-deferred focus wins focus race)" — already pins the positive contract via `requestAnimationFrame` flush.
- Test 25 (line 444): "BUG-03 — the editing useEffect schedules its focus call via requestAnimationFrame (source assertion)" — pins source text.

From `frontend/src/components/builder/__tests__/UnifiedStackPanel.test.tsx` (existing — extend, do not rewrite):
- Default props already include `onRenameGroup: vi.fn()` (line 188).
- No rename-focus test today.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UnifiedStackPanel.test.tsx — integration test for rename-group focus</name>
  <files>frontend/src/components/builder/__tests__/UnifiedStackPanel.test.tsx</files>
  <read_first>
    - frontend/src/components/builder/__tests__/UnifiedStackPanel.test.tsx (existing harness — note defaultProps shape and how groups are fixture'd)
    - frontend/src/components/builder/__tests__/FolderGroupRow.test.tsx Test 19 (lines 356-388 — canonical assertion pattern for the rAF flush)
    - frontend/src/components/builder/UnifiedStackPanel.tsx (lines 818, 994 — how onRenameGroup flows through to FolderGroupRow)
  </read_first>
  <behavior>
    - Rendering UnifiedStackPanel with a folder-group layer in the fixture and triggering the kebab → Rename group menu item ends with `document.activeElement` being the rename text input (after the rAF flush).
    - Asserts the integration surface explicitly: this is NOT a unit test of FolderGroupRow — it goes through the panel's group-row rendering path.
  </behavior>
  <action>Append a new describe block to `frontend/src/components/builder/__tests__/UnifiedStackPanel.test.tsx` titled `MAP-16 — rename-group rAF-deferred focus integration`. Add EXACTLY these two tests inside it:

**Test 1: `rename-group via kebab → Rename group menu item focuses the rename input (rAF flush)`**

1. Build a fixture with at least one folder-group layer using the existing fixture helpers from this file. The group needs `layer_type: 'group:folder'` and `display_name: 'Old Group Name'`.
2. Render `UnifiedStackPanel` with the fixture and `onRenameGroup: vi.fn()`.
3. Locate the kebab trigger on the group row (use `getByRole('button', { name: /more|menu|options/i })` or `getByTestId` depending on the row's existing affordance — read the row source to confirm).
4. Click kebab to open menu; click the "Rename group" menu item.
5. Wrap a `requestAnimationFrame` flush in `act(() => { ... })` — pattern from `FolderGroupRow.test.tsx` Test 19: use `vi.useFakeTimers()` if needed, or call the rAF callback directly via the `globalThis.requestAnimationFrame` mock pattern.
6. Assert `document.activeElement` is the input element matching `getByRole('textbox')` AND that its `value` equals 'Old Group Name'.

**Test 2: `rename-group input has the BUG-03 docstring contract — autofocus is NOT a React prop`**

A source-text assertion that catches a regression where a future engineer adds `autoFocus` (the React prop) to the rename input instead of using the rAF-deferred pattern. This is a static defense-in-depth check.

1. Read `frontend/src/components/builder/FolderGroupRow.tsx` via Node `fs.readFileSync` inside the test (mirroring `FolderGroupRow.test.tsx` Test 25 pattern at line 444).
2. Assert the file source contains `requestAnimationFrame` near the `editing` useEffect.
3. Assert the file source does NOT contain `autoFocus={true}` or `autoFocus={editing}` as a prop on the rename input — only the `eslint-disable-next-line jsx-a11y/no-autofocus` comment paired with `autoFocus` AS-IS (the existing code uses autoFocus + the rAF pattern as belt-and-suspenders; we pin the belt — rAF — and accept the suspenders — autoFocus — as long as the rAF stays). The exact pin: `expect(fileSrc).toMatch(/requestAnimationFrame\([\s\S]{0,200}?inputRef\.current\?\.focus/)` — proves the rAF wraps the focus call.

Pre-step: confirm the fixture-construction pattern by running `grep -nE "layer_type: 'group:folder'" frontend/src/components/builder/__tests__/UnifiedStackPanel.test.tsx` to see if a group fixture helper already exists. If not, follow the shape from `FolderGroupRow.test.tsx:67`'s `defaultProps`.</action>
  <acceptance_criteria>
    - `cd frontend && npm test -- "components/builder/__tests__/UnifiedStackPanel.test.tsx" -- --run` exits 0 with the new 2 tests passing alongside the existing tests in the file.
    - `grep -nE "describe.*MAP-16" frontend/src/components/builder/__tests__/UnifiedStackPanel.test.tsx` returns ≥1 match.
    - `cd frontend && npm run typecheck` exits 0.
    - No changes to any file under `frontend/src/components/builder/*.tsx` (test-only plan).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- "components/builder/__tests__/UnifiedStackPanel.test.tsx" -- --run</automated>
  </verify>
  <done>UnifiedStackPanel.test.tsx contains 2 new MAP-16 tests: integration test for kebab → Rename group → focus on input; source-text pin for the rAF wrap around the focus call.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FolderGroupRow.test.tsx — negative-control pin for synchronous focus</name>
  <files>frontend/src/components/builder/__tests__/FolderGroupRow.test.tsx</files>
  <read_first>
    - frontend/src/components/builder/__tests__/FolderGroupRow.test.tsx Test 19 + Test 25 (existing BUG-03 positive pins)
    - frontend/src/components/builder/FolderGroupRow.tsx lines 73-100 (the editing useEffect)
  </read_first>
  <behavior>
    - A test that fails if the rAF deferral is removed (synchronous focus reintroduced) — this is the negative control.
    - Specifically: when `editing` flips to true, the input is NOT immediately focused in the same synchronous tick as the state change. Focus only arrives AFTER a rAF flush.
  </behavior>
  <action>Append one test to the existing `BUG-03: rename-group autofocus` describe block in `frontend/src/components/builder/__tests__/FolderGroupRow.test.tsx`. The test name: `BUG-03 negative control — input is NOT focused synchronously when editing flips to true (MAP-16 regression guard)`.

Implementation pattern:
1. Stub `requestAnimationFrame` to NOT auto-invoke its callback (capture it instead):
   ```ts
   const rafCallbacks: FrameRequestCallback[] = [];
   vi.spyOn(globalThis, 'requestAnimationFrame').mockImplementation((cb) => {
     rafCallbacks.push(cb);
     return 0 as unknown as number;
   });
   ```
2. Render FolderGroupRow with the standard fixture.
3. Trigger the rename mode (double-click the group name span, mirroring Test 7's interaction at line 168).
4. **Without** flushing the rAF queue, assert `document.activeElement` is NOT the rename input. The `<input>` should be mounted (`screen.getByRole('textbox')` succeeds) but not focused yet.
5. Flush the queued rAF: `act(() => { rafCallbacks.forEach((cb) => cb(performance.now())); })`.
6. Assert `document.activeElement` IS the rename input now.

Comment block explaining the contract: "If this test ever fails, the rAF deferral was likely removed from FolderGroupRow.tsx editing useEffect — that re-introduces the v1011 BUG-03 dnd-kit/Radix focus race. Revert the synchronous-focus change before merging."</action>
  <acceptance_criteria>
    - `cd frontend && npm test -- "components/builder/__tests__/FolderGroupRow.test.tsx" -- --run` exits 0 with the new test plus all existing tests passing.
    - `grep -nE "negative control.*synchronously" frontend/src/components/builder/__tests__/FolderGroupRow.test.tsx` returns ≥1 match.
    - The test must clean up its `requestAnimationFrame` spy in afterEach (`vi.restoreAllMocks()`).
    - `cd frontend && npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- "components/builder/__tests__/FolderGroupRow.test.tsx" -- --run</automated>
  </verify>
  <done>FolderGroupRow.test.tsx contains a new negative-control test inside the BUG-03 describe block; the test fails if synchronous focus is reintroduced.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm test -- "UnifiedStackPanel" "FolderGroupRow" -- --run` exits 0.
- No source file under `frontend/src/components/builder/*.tsx` (excluding `__tests__/`) was modified — `git diff --name-only frontend/src/components/builder/ | grep -v __tests__` returns empty for this plan.
- BuilderLayerAction union UNCHANGED.
</verification>

<success_criteria>
- ROADMAP MAP-16 success criterion satisfied: rAF-deferred focus pinned at `UnifiedStackPanel.test.tsx` (integration) + `FolderGroupRow.test.tsx` (negative control).
- v1011 BUG-03 fix protected against accidental revert.
- Pure test-pin plan — zero source code changes.
</success_criteria>

<output>
Create `.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-03-SUMMARY.md` documenting:
- The 2 new tests in UnifiedStackPanel.test.tsx (integration + source-text pin).
- The 1 new test in FolderGroupRow.test.tsx (negative control).
- Cross-reference v1011 BUG-03 commit `80bddc14`.
- Confirmation that no source code was modified.
</output>
