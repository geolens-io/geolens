---
phase: 1134-map-functionality-and-smaller-screen-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/MapBuilderPage.tsx
  - frontend/src/components/builder/ActiveFilterChips.tsx
  - frontend/src/components/map/__tests__/MapCoordReadout.test.tsx
  - frontend/src/components/builder/__tests__/MapBuilderPage.sheet-close-button.test.tsx
  - frontend/src/components/builder/__tests__/ActiveFilterChips.test.tsx
autonomous: true
requirements:
  - MAP-07
  - MAP-08
  - MAP-09
  - MAP-10
  - MAP-20
tags:
  - builder
  - smaller-screen
  - responsive

must_haves:
  truths:
    - "At 800×600 the right-sidebar Sheet's chrome does not vertically overlap the top-left NavigationControl"
    - "MapCoordReadout pill renders at the load-bearing right-14 (56px) offset — protected by a positive-form queryBy* assertion"
    - "Every <SheetContent> in the builder canvas (MapBuilderPage.tsx) declares showCloseButton={false} — covered by an exhaustive grep guard"
    - "Filter pills container limits its height with max-h-[40vh] and overflow-y-auto so the column can never reach the bottom-left measure widget at ≤800px"
  artifacts:
    - path: "frontend/src/pages/MapBuilderPage.tsx"
      provides: "Sidebar Sheet inset adjustment for ≤800px (vertical offset below MapTitleBar)"
      contains: "mt-12"
    - path: "frontend/src/components/builder/ActiveFilterChips.tsx"
      provides: "max-h-[40vh] overflow-y-auto on chip container per UI-SPEC §Filter-Pill vs Measure-Widget Collision Avoidance"
      contains: "max-h-\\[40vh\\]"
    - path: "frontend/src/components/map/__tests__/MapCoordReadout.test.tsx"
      provides: "Positive-form regression pin — `right-14` class present on rendered pill (MAP-08)"
    - path: "frontend/src/components/builder/__tests__/MapBuilderPage.sheet-close-button.test.tsx"
      provides: "Extended with MAP-10 exhaustive grep-guard test — every <SheetContent in MapBuilderPage.tsx has showCloseButton={false}"
    - path: "frontend/src/components/builder/__tests__/ActiveFilterChips.test.tsx"
      provides: "MAP-20 regression — chip container has max-h-[40vh] + overflow-y-auto classes"
  key_links:
    - from: "ActiveFilterChips chip container"
      to: "container max-height limit"
      via: "max-h-[40vh] tailwind class"
      pattern: "max-h-\\[40vh\\]"
    - from: "MapBuilderPage.tsx ≤800px Sheet"
      to: "MapTitleBar vertical offset"
      via: "mt-12 class on SheetContent"
      pattern: "mt-12"
---

<objective>
Close the four ≤800px layout collisions named in ROADMAP MAP-07/08/09/10/20:
1. **MAP-07** — Right-sidebar Sheet's chrome does not visually overlap the `top-left` NavigationControl at 800×600 (sidebar collapse trigger fix; NavigationControl stays `top-left` per Pitfall #10).
2. **MAP-08** — Lat/long readout pill positioning protected by a positive-form regression test.
3. **MAP-09** — Basemap selector renders single close button (already PASS per WALK-SS-03; re-pinned via MAP-10 sweep).
4. **MAP-10** — Every `<SheetContent>` in the builder canvas opts out of duplicate-X via `showCloseButton={false}` — exhaustive grep guard ensures the contract holds across all current and future Sheet wrappers in MapBuilderPage.
5. **MAP-20** — Filter pills cannot collide with the bottom-left MeasurementWidget — `max-h-[40vh]` + `overflow-y-auto` on the chip container.

Purpose: Stabilize the smaller-screen layout so Phase 1135 AI staging work and the Phase 1139 close-gate Playwright MCP smoke do not surface layout regressions. NavigationControl position stays `top-left` (Pitfall #10 — non-negotiable). Sheet wrappers stay opt-in via the existing `showCloseButton={false}` prop (Pitfall #11 — non-negotiable).

Output:
- Two small source edits: a `mt-12` to push the ≤800px Sheet below the MapTitleBar; `max-h-[40vh] overflow-y-auto` on the filter chip container.
- Three test extensions/creations pinning the contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-CONTEXT.md
@.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-UI-SPEC.md
@.planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md

@frontend/src/pages/MapBuilderPage.tsx
@frontend/src/components/builder/ActiveFilterChips.tsx
@frontend/src/components/map/MapCoordReadout.tsx
@frontend/src/components/builder/BuilderMap.tsx
@frontend/src/components/ui/sheet.tsx
@frontend/src/components/builder/__tests__/MapBuilderPage.sheet-close-button.test.tsx
@frontend/src/components/map/__tests__/MapCoordReadout.test.tsx

<interfaces>
<!-- The two SheetContent surfaces in the builder canvas (already showCloseButton={false}) -->

From `frontend/src/pages/MapBuilderPage.tsx`:
- Line 1252 — `<SheetContent side="right" showCloseButton={false} className="w-full max-w-[380px] p-0 flex flex-col">` (LayerEditorPanel wrap — ≤800px drill-down)
- Line 1364 — `<SheetContent side="right" showCloseButton={false} className="w-[22rem] max-w-[calc(100vw-5rem)] p-0 flex flex-col">` (BuilderRail mobile sheet)

UI-SPEC §Smaller-Screen ≤800px Contract item 4:
> "The fix is in the sidebar Sheet's width/offset — NOT in NavigationControl position. The Sheet already has `className=\"w-[22rem] max-w-[calc(100vw-5rem)]\"` (MapBuilderPage.tsx:1371) which limits the overlay to `100vw - 80px`. At 800px, max-width = 720px. The navigation controls sit at `top-left` so the overlap surface is vertical (height of the Sheet vs. top of the NavigationControl). If vertical overlap is confirmed live via MCP, add a `mt-12` (48px) to the Sheet's `inset-y-0 top-0` to shift it below the MapTitleBar row."

From `frontend/src/components/map/MapCoordReadout.tsx:124` (current):
```tsx
<div className="absolute top-2 right-14 z-10 pointer-events-none">
```

UI-SPEC §Filter-Pill vs Measure-Widget Collision Avoidance (MAP-20):
> "Add `max-h` + `overflow-y: auto` to the filter chip container so that at ≤800px the chip column does not grow past the midpoint of the canvas (`max-h-[40vh]`)."

Current ActiveFilterChips container (line 120-121):
```tsx
<div className="flex flex-wrap gap-1.5 pointer-events-none">
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MAP-07 sidebar offset + MAP-10 exhaustive SheetContent grep guard</name>
  <files>frontend/src/pages/MapBuilderPage.tsx, frontend/src/components/builder/__tests__/MapBuilderPage.sheet-close-button.test.tsx</files>
  <read_first>
    - frontend/src/pages/MapBuilderPage.tsx lines 1246-1380 (the 2 SheetContent surfaces in the builder canvas)
    - frontend/src/components/builder/__tests__/MapBuilderPage.sheet-close-button.test.tsx (existing 8 tests cover the negative control + positive sheet shape — extend with the new exhaustive grep guard)
    - frontend/src/components/builder/BuilderMap.tsx lines 1042-1054 (`data-builder-canvas` marker — important: do NOT touch this)
    - frontend/src/index.css lines 467-475 (the `[data-builder-canvas]` CSS rule that already pushes the top-left control down 32px)
  </read_first>
  <behavior>
    - At 800×600 the layer-editor SheetContent (currently `w-full max-w-[380px]`) renders with a vertical offset that places its top edge below the MapTitleBar (~48px below the viewport top) so the Sheet does not overlap the NavigationControl group.
    - The mobile-rail SheetContent (right side, `w-[22rem]`) gets the same vertical offset.
    - The exhaustive grep guard test asserts: every `<SheetContent>` JSX node in `frontend/src/pages/MapBuilderPage.tsx` declares `showCloseButton={false}`.
    - NO change to NavigationControl position (stays `top-left`) — Pitfall #10 non-negotiable.
  </behavior>
  <action>**Source change in `frontend/src/pages/MapBuilderPage.tsx`:**

Edit the className on the two `<SheetContent>` elements (lines 1252-1263 and 1364-1372) by adding `mt-12` to the className string:
- Line 1262: change `className="w-full max-w-[380px] p-0 flex flex-col"` → `className="mt-12 h-[calc(100%-3rem)] w-full max-w-[380px] p-0 flex flex-col"` (the height adjustment compensates for the offset so the Sheet content area still fills the remaining canvas).
- Line 1371: change `className="w-[22rem] max-w-[calc(100vw-5rem)] p-0 flex flex-col"` → `className="mt-12 h-[calc(100%-3rem)] w-[22rem] max-w-[calc(100vw-5rem)] p-0 flex flex-col"`.

Add a `// MAP-07` comment above each modified className referencing the v1011 RESP-01/02 contract: "// MAP-07: 48px top offset clears the MapTitleBar so the right-sidebar Sheet does not visually overlap the top-left NavigationControl at ≤800px. NavigationControl stays `top-left` per Pitfall #10; this is the sidebar-side fix."

**Test extension in `frontend/src/components/builder/__tests__/MapBuilderPage.sheet-close-button.test.tsx`:**

Append a new describe block titled `MAP-10 — exhaustive SheetContent grep guard`. Add EXACTLY this test:

**Test 9: `every <SheetContent in MapBuilderPage.tsx declares showCloseButton={false} (exhaustive)`**

1. Use Node's `fs.readFileSync` to read `frontend/src/pages/MapBuilderPage.tsx` as a string.
2. Find every line index where the text `<SheetContent` appears. Use a regex like `/<SheetContent\b[\s\S]*?>/g` to extract each opening tag (multi-line).
3. Assert the number of matches is ≥1 (sanity check that this test is actually finding tags).
4. For each match, assert it contains `showCloseButton={false}`. If any match is missing the opt-out, the test fails with a clear message that includes the line number and snippet (so the next executor can fix the new wrapper).
5. Add a second sanity assertion: also check that no `<Sheet ` tag opens a sheet inside the file without a child `<SheetContent showCloseButton={false}>` — this is belt-and-suspenders.

Example test body shape:
```ts
import fs from 'node:fs';
import path from 'node:path';

it('MAP-10: every <SheetContent in MapBuilderPage.tsx declares showCloseButton={false}', () => {
  const filePath = path.resolve(__dirname, '../../../pages/MapBuilderPage.tsx');
  const src = fs.readFileSync(filePath, 'utf8');
  const matches = src.match(/<SheetContent\b[\s\S]*?>/g) ?? [];
  expect(matches.length).toBeGreaterThan(0);
  for (const match of matches) {
    expect(match, `SheetContent missing showCloseButton={false}: ${match.slice(0, 200)}`).toContain('showCloseButton={false}');
  }
});
```

Add a docstring comment block above the new test explaining: this is the v1011 Pitfall #11 enforcement gate. If a future engineer adds a new `<SheetContent>` to MapBuilderPage.tsx without the opt-out, this test fails. The negative-control test (Test 8) in this file proves the bug-shape if the opt-out is removed.</action>
  <acceptance_criteria>
    - `grep -nE "mt-12 h-\[calc\(100%-3rem\)\]" frontend/src/pages/MapBuilderPage.tsx` returns 2 matches.
    - `grep -cE "showCloseButton=\{false\}" frontend/src/pages/MapBuilderPage.tsx` returns 2 (both surfaces preserved).
    - `cd frontend && npm test -- "MapBuilderPage.sheet-close-button" -- --run` exits 0 with all 8 original + 1 new tests passing.
    - `grep -nE "describe.*MAP-10" frontend/src/components/builder/__tests__/MapBuilderPage.sheet-close-button.test.tsx` returns ≥1 match.
    - `cd frontend && npm run typecheck` exits 0.
    - NavigationControl position not changed — `grep -nE "NavigationControl position" frontend/src/components/builder/BuilderMap.tsx` still returns `top-left` (single match).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- "MapBuilderPage.sheet-close-button" -- --run</automated>
  </verify>
  <done>Both ≤800px Sheets shifted down 48px via `mt-12 h-[calc(100%-3rem)]`; exhaustive grep guard test asserts every SheetContent in MapBuilderPage.tsx declares showCloseButton={false}.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MAP-08 MapCoordReadout positive-form regression + MAP-09 basemap sheet pin</name>
  <files>frontend/src/components/map/__tests__/MapCoordReadout.test.tsx</files>
  <read_first>
    - frontend/src/components/map/__tests__/MapCoordReadout.test.tsx (existing tests cover SP-02 move-event subscription + SP-12 representative-fraction — extend with the MAP-08 positive-form pin)
    - frontend/src/components/map/MapCoordReadout.tsx line 124 (the `top-2 right-14 z-10` className)
    - frontend/src/components/builder/__tests__/MapBuilderPage.sheet-close-button.test.tsx (the BasemapPicker call site for MAP-09 — already covered by Test 1 indirectly; we cross-reference it in MAP-09)
  </read_first>
  <behavior>
    - MapCoordReadout renders with the load-bearing `right-14` className (positive-form `queryBy*` assertion against the rendered DOM).
    - The component renders the `top-2 z-10` positioning classes — keeps the cross-context contract honest.
    - A docstring assertion guards against future moves: the test text mentions the load-bearing 56px offset and references the docstring at MapCoordReadout.tsx:26-37.
  </behavior>
  <action>Append a new describe block to `frontend/src/components/map/__tests__/MapCoordReadout.test.tsx` titled `MAP-08 — load-bearing positioning regression pin`. Add EXACTLY these tests:

**Test 1: `renders with the right-14 load-bearing offset class (RESP-02 / MAP-08 regression)`**

1. Use the existing test harness (`makeMockMap` or equivalent — read the file to confirm helper name).
2. Render the component.
3. Locate the rendered pill via its existing class-based selector. The pill is the first child div with the readout content.
4. Assert via `toHaveClass('right-14')` and `toHaveClass('top-2')` and `toHaveClass('z-10')`. This is the positive-form assertion ROADMAP MAP-08 names.

**Test 2: `RESP-02 docstring still cross-references both Builder + Viewer contexts (source-text pin)`**

1. Read `frontend/src/components/map/MapCoordReadout.tsx` via `fs.readFileSync`.
2. Assert the file contains the substring `RESP-02 — Phase 1051 Plan 09` (or equivalent) — the docstring contract.
3. Assert it mentions both `BuilderMap` AND `ViewerMap` in the docstring block (the cross-context contract — see UI-SPEC INV-02).
4. Assert no `right-12`, `right-10`, `right-8` appears next to a `top-2` on the readout pill className — guards against future "px optimization" PRs that reduce the offset.

Cross-reference MAP-09 in a comment above Test 1: "MAP-09 (basemap sheet single close button) is covered by `MapBuilderPage.sheet-close-button.test.tsx` Tests 1-7. The basemap sheet is the same `<SheetContent showCloseButton={false}>` wrapper pattern verified there; no additional test needed here."</action>
  <acceptance_criteria>
    - `cd frontend && npm test -- "components/map/__tests__/MapCoordReadout.test.tsx" -- --run` exits 0 with all existing + new 2 tests passing.
    - `grep -nE "describe.*MAP-08" frontend/src/components/map/__tests__/MapCoordReadout.test.tsx` returns ≥1 match.
    - `grep -nE "toHaveClass\(.right-14" frontend/src/components/map/__tests__/MapCoordReadout.test.tsx` returns ≥1 match.
    - `cd frontend && npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- "components/map/__tests__/MapCoordReadout.test.tsx" -- --run</automated>
  </verify>
  <done>MapCoordReadout.test.tsx contains 2 new MAP-08 tests pinning the right-14/top-2/z-10 positioning classes and the docstring cross-context contract. MAP-09 is cross-referenced via comment to the existing sheet-close-button.test.tsx coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MAP-20 ActiveFilterChips max-height + overflow + regression test</name>
  <files>frontend/src/components/builder/ActiveFilterChips.tsx, frontend/src/components/builder/__tests__/ActiveFilterChips.test.tsx</files>
  <read_first>
    - frontend/src/components/builder/ActiveFilterChips.tsx (current container at line 120-121 — `flex flex-wrap gap-1.5 pointer-events-none`)
    - frontend/src/pages/MapBuilderPage.tsx line 1349 (the consumer — confirms `<ActiveFilterChips layers={...} onClearFilter={...} />` is mounted in topLeftSlot of WidgetHost)
    - UI-SPEC §Filter-Pill vs Measure-Widget Collision Avoidance (the `max-h-[40vh]` decision)
  </read_first>
  <behavior>
    - Chip container has `max-h-[40vh] overflow-y-auto` — at ≤800px height (600px) the column cannot grow past 240px tall, which is well above the bottom-left MeasurementWidget's vertical band.
    - When there are 0 chips, the container does not render (`return null` preserved).
    - The chip container retains its `pointer-events-none` (chips themselves have `pointer-events-auto` via their span).
  </behavior>
  <action>**Source change in `frontend/src/components/builder/ActiveFilterChips.tsx`:**

Edit line 121 className from:
```tsx
<div className="flex flex-wrap gap-1.5 pointer-events-none">
```
to:
```tsx
<div className="flex flex-wrap gap-1.5 max-h-[40vh] overflow-y-auto pointer-events-none">
```

Add a comment above the line: `// MAP-20: max-h-[40vh] + overflow-y-auto prevents the chip column from growing into the bottom-left MeasurementWidget at ≤800px. See UI-SPEC §Filter-Pill vs Measure-Widget Collision Avoidance.`

**Create `frontend/src/components/builder/__tests__/ActiveFilterChips.test.tsx`** with EXACTLY these tests:

Setup: standard React Testing Library render harness. Mock `react-i18next` like other tests in this directory.

- Test 1: `MAP-20 — chip container has max-h-[40vh] + overflow-y-auto classes (regression pin)`
  1. Build a fixture with 1 visible layer with a non-null filter (`['==', ['get', 'class'], 'highway']`).
  2. Render `<ActiveFilterChips layers={layers} onClearFilter={vi.fn()} />`.
  3. Locate the container div (the flex-wrap parent of the chip spans).
  4. Assert `toHaveClass('max-h-[40vh]')` and `toHaveClass('overflow-y-auto')` and `toHaveClass('pointer-events-none')`.

- Test 2: `zero chips renders null (preserves current behavior)`
  1. Render with `layers={[]}`.
  2. Assert the container is not in the document (`queryByText` returns null for any chip label).

- Test 3: `1 chip renders the chip label and the layer name`
  1. Build a fixture with 1 visible layer named "Roads" and filter `['==', ['get', 'class'], 'highway']`.
  2. Render.
  3. Assert `getByText('class == "highway"')` exists.
  4. Assert `getByText('ROADS')` or `/roads/i` exists (the layer name uppercase via the existing font-mono tracking-wider class).

- Test 4: `chip click calls onClearFilter with the layer id`
  1. Same fixture as Test 3.
  2. Click the X button (`getByLabelText(/clear/i)` or `getByRole('button', { name: /clear/i })`).
  3. Assert `onClearFilter` mock called with the layer id.

- Test 5: `MAP-20 negative control — without the max-h class, the container would have no height limit (bug-shape pin)`
  1. Read the source file via `fs.readFileSync`.
  2. Assert `max-h-[40vh]` and `overflow-y-auto` both appear in the source. This catches a future "CSS cleanup" PR that removes one or both classes.</action>
  <acceptance_criteria>
    - `grep -nE "max-h-\[40vh\]" frontend/src/components/builder/ActiveFilterChips.tsx` returns 1 match.
    - `grep -nE "overflow-y-auto" frontend/src/components/builder/ActiveFilterChips.tsx` returns 1 match.
    - `cd frontend && npm test -- "components/builder/__tests__/ActiveFilterChips.test.tsx" -- --run` exits 0 with all 5 tests passing.
    - `cd frontend && npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- "components/builder/__tests__/ActiveFilterChips.test.tsx" -- --run</automated>
  </verify>
  <done>ActiveFilterChips container has max-h-[40vh] + overflow-y-auto; new ActiveFilterChips.test.tsx pins the classes + chip rendering + clear callback + negative-control source pin.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm test -- "MapBuilderPage.sheet-close-button" "MapCoordReadout" "ActiveFilterChips" -- --run` exits 0.
- `npm run typecheck` exits 0.
- `grep -cE "showCloseButton=\{false\}" frontend/src/pages/MapBuilderPage.tsx` returns ≥2 (both builder-canvas Sheets remain opt-out).
- NavigationControl `position` in BuilderMap.tsx is still `top-left` (Pitfall #10 honored).
- BuilderLayerAction union UNCHANGED.
</verification>

<success_criteria>
- ROADMAP MAP-07 satisfied: ≤800px Sheet has 48px top offset; NavigationControl stays top-left.
- ROADMAP MAP-08 satisfied: positive-form `right-14` regression pin in MapCoordReadout.test.tsx.
- ROADMAP MAP-09 satisfied: existing sheet-close-button.test.tsx Tests 1-7 cover the single-close-button contract; cross-referenced in MAP-08 test.
- ROADMAP MAP-10 satisfied: exhaustive grep guard test at MapBuilderPage.sheet-close-button.test.tsx.
- ROADMAP MAP-20 satisfied: max-h + overflow-y-auto on chip container; regression pinned in ActiveFilterChips.test.tsx.
</success_criteria>

<output>
Create `.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-04-SUMMARY.md` documenting:
- The 2 className edits in MapBuilderPage.tsx (line 1262, 1371) with before/after.
- The 1 className edit in ActiveFilterChips.tsx (line 121).
- 3 test files extended/created: sheet-close-button.test.tsx (+1 test), MapCoordReadout.test.tsx (+2 tests), ActiveFilterChips.test.tsx (NEW, 5 tests).
- Confirmation Pitfall #10 + #11 honored (NavigationControl position unchanged; showCloseButton={false} preserved on both Sheets).
</output>
