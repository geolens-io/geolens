---
phase: 1134-map-functionality-and-smaller-screen-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/builder/layer-adapters/raster-adapter.ts
  - frontend/src/components/builder/layer-adapters/symbol-adapter.ts
  - frontend/src/components/builder/layer-adapters/__tests__/raster-adapter.test.ts
  - frontend/src/components/builder/layer-adapters/__tests__/symbol-adapter.test.ts
  - frontend/src/components/builder/layer-adapters/__tests__/circle-adapter.test.ts
  - frontend/src/components/builder/layer-adapters/__tests__/heatmap-adapter.test.ts
  - frontend/src/components/builder/layer-adapters/__tests__/fill-adapter.test.ts
  - frontend/src/components/builder/layer-adapters/__tests__/line-adapter.test.ts
  - frontend/src/components/builder/layer-adapters/__tests__/cluster-adapter.test.ts
autonomous: true
requirements:
  - MAP-18
tags:
  - builder
  - map-sync
  - layer-adapter

must_haves:
  truths:
    - "Toggling visibility off then on for a circle, heatmap, raster, symbol, fill, line, or cluster layer immediately reflects on the MapLibre canvas"
    - "Reloading a raster basemap (style swap) keeps existing raster layers visible — no blank tiles caused by an early-return source guard"
    - "Setting a filter on a symbol layer after paint changes still applies the filter on the canvas"
  artifacts:
    - path: "frontend/src/components/builder/layer-adapters/raster-adapter.ts"
      provides: "Split source-guard / layer-guard in addLayers (WALK-R-05 fix); idempotent layer re-add when source exists but layer was removed"
      contains: "if (!map.getSource(sourceId))"
    - path: "frontend/src/components/builder/layer-adapters/symbol-adapter.ts"
      provides: "Replaces raw map.setFilter with shared syncLayerFilter helper to match v1026 owned-property contract"
      contains: "syncLayerFilter"
    - path: "frontend/src/components/builder/layer-adapters/__tests__/raster-adapter.test.ts"
      provides: "WALK-R-05 regression — source-exists/layer-missing path re-adds layer only"
    - path: "frontend/src/components/builder/layer-adapters/__tests__/symbol-adapter.test.ts"
      provides: "Filter sync on syncPaint via syncLayerFilter helper"
    - path: "frontend/src/components/builder/layer-adapters/__tests__/circle-adapter.test.ts"
      provides: "MAP-18 BUG-01 regression pin — addLayers honors visible=false; syncPaint calls syncLayerFilter; syncVisibility uses syncSingleLayerVisibility"
    - path: "frontend/src/components/builder/layer-adapters/__tests__/heatmap-adapter.test.ts"
      provides: "Extended with v1011 BUG-01 + syncLayerFilter pin (file already exists)"
    - path: "frontend/src/components/builder/layer-adapters/__tests__/fill-adapter.test.ts"
      provides: "BUG-01 regression — initial visibility honored across layer/outline/extrusion companions"
    - path: "frontend/src/components/builder/layer-adapters/__tests__/line-adapter.test.ts"
      provides: "BUG-01 regression — addLayers initial visibility honored; arrowLayerId guard PASS"
    - path: "frontend/src/components/builder/layer-adapters/__tests__/cluster-adapter.test.ts"
      provides: "BUG-01 regression — all 3 cluster sub-layers honor visible at add-time"
  key_links:
    - from: "raster-adapter.ts addLayers"
      to: "map.addSource + map.addLayer"
      via: "split guards"
      pattern: "if \\(!map\\.getSource"
    - from: "symbol-adapter.ts syncPaint"
      to: "shared.ts syncLayerFilter"
      via: "import + call site"
      pattern: "syncLayerFilter\\(map, input\\.layerId, input\\.filter"
---

<objective>
Close the per-render-mode visibility + filter + raster source-guard regressions that Phase 1133 named: WALK-R-05 (raster early-return), WALK-S (symbol uses raw `setFilter` rather than the shared helper), and the audit's MAP-18 contract that every adapter pin `syncVisibility` at add-time (v1011 BUG-01).

Purpose: Stabilize the `dispatchLayerAction` boundary's downstream side-effects before Phase 1135 AI staging tests pin against it. Every adapter must follow the same `syncVisibility(input.visible)` + `syncLayerFilter(map, id, filter)` contract used in `fill-adapter.ts` and `line-adapter.ts`.

Output:
- One bug-fix in `raster-adapter.ts` (split source guard from layer guard).
- One pattern-consistency fix in `symbol-adapter.ts` (use `syncLayerFilter` helper).
- Seven adapter regression test files (one per render mode: circle, heatmap, raster, symbol, fill, line, cluster) pinning the BUG-01 + filter-sync contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-CONTEXT.md
@.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-UI-SPEC.md
@.planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md

@frontend/src/components/builder/layer-adapters/fill-adapter.ts
@frontend/src/components/builder/layer-adapters/line-adapter.ts
@frontend/src/components/builder/layer-adapters/circle-adapter.ts
@frontend/src/components/builder/layer-adapters/heatmap-adapter.ts
@frontend/src/components/builder/layer-adapters/raster-adapter.ts
@frontend/src/components/builder/layer-adapters/symbol-adapter.ts
@frontend/src/components/builder/layer-adapters/cluster-adapter.ts
@frontend/src/components/builder/layer-adapters/shared.ts
@frontend/src/components/builder/layer-adapters/types.ts
@frontend/src/components/builder/layer-adapters/__tests__/heatmap-adapter.test.ts
@frontend/src/components/builder/layer-adapters/__tests__/shared.test.ts

<interfaces>
<!-- Key contracts the executor must follow. All exist today; no widening. -->

From `frontend/src/components/builder/layer-adapters/shared.ts`:
- `syncLayerFilter(map, layerId, filter)` — calls `map.setFilter(id, filter ?? null)` once `map.getLayer(id)` is truthy. Idempotent. Used by fill/line/circle/heatmap. Symbol and cluster use raw `map.setFilter` today — symbol gets migrated, cluster does not (its filter is the `unclusteredFilter` compound which intentionally bypasses the helper).
- `syncSingleLayerVisibility(map, layerId, visible)` — guard-wrapped `map.setLayoutProperty(id, 'visibility', visible ? 'visible' : 'none')`. Used by circle/heatmap/cluster.
- `syncOwnedPaintProperties(map, layerId, paint, { geomType?, ownedProperties })` — paint-property reconciliation against owned-list; the v1026 contract.

From `frontend/src/components/builder/layer-adapters/types.ts`:
```ts
export interface LayerAdapter {
  type: string;
  addLayers(map: MaplibreMap, input: AdapterLayerInput): void;
  syncPaint(map: MaplibreMap, input: AdapterLayerInput): void;
  syncVisibility(map: MaplibreMap, input: AdapterLayerInput): void;
  getLayerIds(layerId: string): string[];
}
```

From `frontend/src/components/builder/layer-adapters/raster-adapter.ts` (current — bug at line 61):
```
addLayers(map, input) {
  if (map.getSource(sourceId)) return;   // <-- WALK-R-05: gates whole addLayers, not just addSource
  map.addSource(sourceId, { ... });
  map.addLayer({ id: layerId, type: 'raster', source: sourceId, paint: buildRasterPaint(input) });
  if (!visible) map.setLayoutProperty(layerId, 'visibility', 'none');
}
```

From `frontend/src/components/builder/layer-adapters/symbol-adapter.ts` (current — uses raw setFilter at lines 132-134 + 151):
```
addLayers: ... if (input.filter && Array.isArray(input.filter) && input.filter.length > 0) {
  map.setFilter(input.layerId, input.filter);
}
syncPaint: ... map.setFilter(input.layerId, input.filter ?? null);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Raster adapter — split source-guard from layer-guard (WALK-R-05)</name>
  <files>frontend/src/components/builder/layer-adapters/raster-adapter.ts, frontend/src/components/builder/layer-adapters/__tests__/raster-adapter.test.ts</files>
  <read_first>
    - frontend/src/components/builder/layer-adapters/raster-adapter.ts (current early-return bug at line 61)
    - frontend/src/components/builder/layer-adapters/hillshade-adapter.ts (canonical pattern — guards source AND layer separately)
    - frontend/src/components/builder/__tests__/map-sync.raster.test.ts (existing raster test harness for fixture shape)
  </read_first>
  <behavior>
    - addLayers(map, input) with source not existing AND layer not existing → addSource then addLayer.
    - addLayers(map, input) with source existing AND layer NOT existing → SKIP addSource, addLayer only.
    - addLayers(map, input) with source existing AND layer existing → no-op (idempotent).
    - addLayers honors `visible === false` at add-time by passing `{ layout: { visibility: 'none' } }` in the addLayer call (BUG-01 contract; existing post-add `setLayoutProperty` survives but is no longer the only signal).
  </behavior>
  <action>Refactor `addLayers` in `frontend/src/components/builder/layer-adapters/raster-adapter.ts` (around line 59-79):

1. Replace the single guard `if (map.getSource(sourceId)) return;` with split guards: `if (!map.getSource(sourceId)) map.addSource(...);` then `if (map.getLayer(layerId)) return;` then `map.addLayer({...})`.
2. Move the `visible === false` initial visibility into the `map.addLayer` call's `layout` block (`...(visible === false ? { layout: { visibility: 'none' as const } } : {})`) per WALK-R BUG-01 PASS pattern. Keep the post-add `if (!visible) map.setLayoutProperty(layerId, 'visibility', 'none');` line as defense-in-depth — both must work.

Create `frontend/src/components/builder/layer-adapters/__tests__/raster-adapter.test.ts` with mock MapLibre instance (`getSource`/`getLayer`/`addSource`/`addLayer` as `vi.fn`) covering:
- Test 1: cold add → both `addSource` and `addLayer` called.
- Test 2: WALK-R-05 source-exists/layer-missing path → `addSource` NOT called, `addLayer` IS called.
- Test 3: source-exists/layer-exists → neither called (no-op).
- Test 4: `visible: false` → `addLayer` receives `{ layout: { visibility: 'none' } }`.
- Test 5: `syncPaint` after source+layer exist routes through `RASTER_PAINT_PROPERTIES` (smoke-only — full coverage is in existing `map-sync.raster.test.ts`).
- Test 6: `syncVisibility(visible=false)` calls `setLayoutProperty(layerId, 'visibility', 'none')`.
- Test 7: `getLayerIds` returns `[layerId]`.

Use the `getSource: vi.fn(() => null)` / `mockImplementation` pattern from `map-sync.raster.test.ts:21` to drive each branch. **Do NOT modify the `RASTER_PAINT_PROPERTIES` list, `RASTER_PAINT_DEFAULTS`, or `buildRasterPaint`** — pure addLayers/guard refactor.</action>
  <acceptance_criteria>
    - `cd frontend && npm test -- layer-adapters/__tests__/raster-adapter.test.ts -- --run` exits 0 with all 7 tests passing.
    - `grep -n "if (map.getSource(sourceId)) return" frontend/src/components/builder/layer-adapters/raster-adapter.ts` returns ZERO matches (the early-return is removed).
    - `grep -nE "if \(!map\.getSource\(sourceId\)\)" frontend/src/components/builder/layer-adapters/raster-adapter.ts` returns ≥1 match (split source guard present).
    - `grep -nE "if \(map\.getLayer\(layerId\)\) return" frontend/src/components/builder/layer-adapters/raster-adapter.ts` returns ≥1 match (split layer guard present).
    - `cd frontend && npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- layer-adapters/__tests__/raster-adapter.test.ts -- --run &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>raster-adapter.ts has split source/layer guards; the source-exists/layer-missing path re-adds the layer; visible=false honored at add-time; new test file pins all 4 guard branches plus visibility/getLayerIds smoke.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Symbol adapter — migrate raw setFilter to syncLayerFilter helper</name>
  <files>frontend/src/components/builder/layer-adapters/symbol-adapter.ts, frontend/src/components/builder/layer-adapters/__tests__/symbol-adapter.test.ts</files>
  <read_first>
    - frontend/src/components/builder/layer-adapters/symbol-adapter.ts (lines 132-134 + 151 — raw setFilter)
    - frontend/src/components/builder/layer-adapters/circle-adapter.ts (canonical syncLayerFilter usage at line 69)
    - frontend/src/components/builder/layer-adapters/shared.ts (syncLayerFilter helper at line 195)
  </read_first>
  <behavior>
    - addLayers with non-empty filter calls `syncLayerFilter(map, layerId, filter)` (not raw `setFilter`).
    - addLayers with `filter == null` does NOT call `setFilter` (matches existing behavior: only called when array+length>0).
    - syncPaint always calls `syncLayerFilter(map, layerId, filter)` (NOT raw `setFilter`); helper internally guards `getLayer`.
  </behavior>
  <action>Modify `frontend/src/components/builder/layer-adapters/symbol-adapter.ts`:
1. Import `syncLayerFilter` from `./shared` (add to existing `import { syncOwnedLayoutProperties, syncOwnedPaintProperties, syncSingleLayerVisibility } from './shared';` line).
2. In `addLayers` (around line 132-134), replace the raw `map.setFilter(input.layerId, input.filter)` call with `syncLayerFilter(map, input.layerId, input.filter)`. Keep the existing guard `if (input.filter && Array.isArray(input.filter) && input.filter.length > 0)` — `syncLayerFilter` will no-op when there is no real filter, but preserving the guard keeps the symbol-adapter behavior identical for empty/null filters.
3. In `syncPaint` (line 151), replace `map.setFilter(input.layerId, input.filter ?? null)` with `syncLayerFilter(map, input.layerId, input.filter)`.

Create `frontend/src/components/builder/layer-adapters/__tests__/symbol-adapter.test.ts` covering:
- Test 1: addLayers with `visible: true` calls `addLayer` with `layout.visibility === 'visible'`.
- Test 2: addLayers with `visible: false` calls `addLayer` with `layout.visibility === 'none'` (BUG-01 PASS regression).
- Test 3: syncPaint with a filter `['==', ['get', 'class'], 'highway']` ends up calling `setFilter(layerId, filter)` (via syncLayerFilter — verify by stubbing `getLayer` to return truthy and asserting `setFilter` was called with the expected args).
- Test 4: syncPaint with `filter: null` calls `setFilter(layerId, null)` (syncLayerFilter helper passes null through).
- Test 5: syncVisibility(visible=false) calls `setLayoutProperty(layerId, 'visibility', 'none')`.
- Test 6: getLayerIds returns `[layerId]`.

Mock MapLibre instance via `vi.fn()` for `addLayer`, `setFilter`, `getLayer`, `setLayoutProperty`, `setPaintProperty` — same harness shape as `heatmap-adapter.test.ts`.</action>
  <acceptance_criteria>
    - `grep -nE "syncLayerFilter" frontend/src/components/builder/layer-adapters/symbol-adapter.ts` returns ≥2 matches (one import + at least one call site in syncPaint).
    - `grep -nE "map\.setFilter\(input\.layerId" frontend/src/components/builder/layer-adapters/symbol-adapter.ts` returns ZERO matches (raw setFilter callsite gone — note: any addLayers raw-setFilter line may be replaced or wrapped, what matters is that syncPaint no longer uses raw setFilter).
    - `cd frontend && npm test -- layer-adapters/__tests__/symbol-adapter.test.ts -- --run` exits 0 with all 6 tests passing.
    - `cd frontend && npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- layer-adapters/__tests__/symbol-adapter.test.ts -- --run &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>symbol-adapter.ts uses the shared `syncLayerFilter` helper in syncPaint (and in addLayers' positive-filter branch); new test file pins addLayers initial visibility, syncPaint filter sync (via helper), syncVisibility, and getLayerIds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Adapter regression pin sweep — circle, heatmap, fill, line, cluster</name>
  <files>frontend/src/components/builder/layer-adapters/__tests__/circle-adapter.test.ts, frontend/src/components/builder/layer-adapters/__tests__/heatmap-adapter.test.ts, frontend/src/components/builder/layer-adapters/__tests__/fill-adapter.test.ts, frontend/src/components/builder/layer-adapters/__tests__/line-adapter.test.ts, frontend/src/components/builder/layer-adapters/__tests__/cluster-adapter.test.ts</files>
  <read_first>
    - frontend/src/components/builder/layer-adapters/__tests__/heatmap-adapter.test.ts (existing harness shape — extend, do not rewrite)
    - frontend/src/components/builder/layer-adapters/circle-adapter.ts (already calls syncLayerFilter at line 69 + honors initialLayout at line 39 — pin both)
    - frontend/src/components/builder/layer-adapters/fill-adapter.ts (initialLayout at line 93 — extends across outline/extrusion companions)
    - frontend/src/components/builder/layer-adapters/line-adapter.ts (BUG-01 PASS comment at audit line 47)
    - frontend/src/components/builder/layer-adapters/cluster-adapter.ts (3 sub-layers all honor visibility at lines 139, 157, 184)
  </read_first>
  <behavior>
    For each adapter, the regression pin asserts (regardless of whether the current source already implements it correctly):
    - `addLayers` with `visible: false` produces `layout.visibility === 'none'` in the `addLayer` call (v1011 BUG-01 PASS pin — guards against future regression).
    - `syncPaint` calls `syncLayerFilter` (or equivalent contract for cluster's compound filter).
    - `syncVisibility(input.visible)` sets `visibility` for every layer in `getLayerIds()`.
    - `getLayerIds(layerId)` shape matches the documented IDs (e.g. fill: `[layerId, outline, extrusion]`; cluster: `[clusterCircleLayerId, clusterCountLayerId, layerId]`).
  </behavior>
  <action>**Each adapter test file must be created or extended with EXACTLY these assertions. Do NOT add coverage outside this contract — this is a regression-pin plan, not an exhaustive test suite.**

For each of the 5 files (circle-adapter.test.ts, heatmap-adapter.test.ts, fill-adapter.test.ts, line-adapter.test.ts, cluster-adapter.test.ts) use the mock-MapLibre harness from the existing `heatmap-adapter.test.ts`. **For `heatmap-adapter.test.ts`:** EXTEND the existing file with the missing pins (do NOT overwrite).

Required tests per file (test name `[adapter] — [contract]` to make grep audits clean):

- **circle-adapter.test.ts** (NEW):
  - Test 1: `circle adapter — addLayers honors visible=false at add-time (BUG-01 PASS pin)` — `addLayer` mock called with `layout.visibility === 'none'`.
  - Test 2: `circle adapter — syncPaint calls syncLayerFilter` — assert via a `setFilter` spy that filter is pushed to the canvas on syncPaint.
  - Test 3: `circle adapter — syncVisibility uses syncSingleLayerVisibility helper` — `setLayoutProperty(layerId, 'visibility', ...)` called.
  - Test 4: `circle adapter — getLayerIds returns [layerId]`.

- **heatmap-adapter.test.ts** (EXTEND existing): add the same 4 tests as circle (currently the file tests only color expression / opacity compounding per WALK-H PASS).

- **fill-adapter.test.ts** (NEW): same 4 tests, BUT Test 1 covers all 3 companion layers — `addLayer` mock invoked 3× with `layerId`, `outline`, `extrusion` (when height column set); each must receive `layout.visibility === 'none'` when `input.visible === false`. Test 4: `getLayerIds` returns exactly `[layerId, ${layerId}-outline, ${layerId}-extrusion]`.

- **line-adapter.test.ts** (NEW): same 4 tests. Test 4: `getLayerIds` returns the documented line-adapter shape (include arrow id only in arrow render mode — assert canonical PASS PATH from audit line 47).

- **cluster-adapter.test.ts** (NEW): same 4 tests adapted for cluster. Test 1: all 3 sub-layers (`-cluster`, `-cluster-count`, base) honor `visible=false` at add-time. Test 2: syncPaint calls `setFilter` on all 3 sub-layers (the cluster compound filter does NOT use `syncLayerFilter` helper — pin the raw `setFilter` call shape instead; document inline in the test that this is the intentional v1026 exception for cluster's compound `combineFilter(unclusteredFilter, input.filter)` shape from `cluster-adapter.ts:71-73`).

DO NOT alter adapter source code in this task. **This task ONLY creates tests; if a test exposes a real regression, file it in PATTERNS.md, do not fix in this plan.**</action>
  <acceptance_criteria>
    - 5 test files exist at the listed paths (circle/heatmap/fill/line/cluster) — `ls frontend/src/components/builder/layer-adapters/__tests__/*-adapter.test.ts | wc -l` returns 6 (the 5 new/extended + raster from Task 1).
    - `cd frontend && npm test -- "frontend/src/components/builder/layer-adapters/__tests__" -- --run` exits 0 with all tests passing.
    - Each test file contains at least one test name matching `/honors visible=false at add-time/i` (the BUG-01 pin).
    - `cd frontend && npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- "components/builder/layer-adapters/__tests__" -- --run &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>5 adapter regression test files exist and pass; each pins addLayers initial visibility (BUG-01), syncPaint filter sync, syncVisibility companion sweep, and getLayerIds shape. No adapter source code modified in this task.</done>
</task>

</tasks>

<verification>
- All 6 adapter regression test files exist and pass (circle, heatmap, fill, line, cluster, raster — symbol's tests are in Task 2).
- `raster-adapter.ts` split-guard refactor lands; `symbol-adapter.ts` uses `syncLayerFilter` helper.
- `BuilderLayerAction` union UNCHANGED — `git diff -- frontend/src/components/builder/builder-action-contract.ts` returns empty.
- `cd frontend && npm test -- layer-adapters -- --run` and `npm run typecheck` both green.
</verification>

<success_criteria>
- Symbol filter migration commits clean (one import, one syncPaint call, one optional addLayers helper).
- Raster split-guard refactor preserves existing test passes in `map-sync.raster.test.ts` (no regression).
- 5 new + 1 extended adapter test files pin BUG-01 + filter-sync + visibility + getLayerIds contracts.
- ROADMAP MAP-18 success criterion satisfied: "per-adapter regression pin in `layer-adapters/__tests__/` covers the v1011 BUG-01 `syncVisibility` initial-layout pattern across ALL adapters, not just the named one."
</success_criteria>

<output>
Create `.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-01-SUMMARY.md` documenting:
- Raster split-guard before/after diff with line numbers.
- Symbol setFilter→syncLayerFilter migration call sites.
- Test count per adapter file (RED count went to GREEN count).
- Cross-reference WALK-R-05, WALK-S-01 if relevant, and the audit's MAP-18 PASS pins.
</output>
