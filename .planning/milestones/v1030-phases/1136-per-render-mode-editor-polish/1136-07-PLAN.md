---
phase: "1136"
plan: "07"
type: execute
wave: 3
depends_on:
  - "1136-01"
  - "1136-02"
  - "1136-03"
  - "1136-04"
  - "1136-05"
  - "1136-06"
files_modified:
  - .planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md
autonomous: false
requirements:
  - EDITOR-RASTER-01
  - EDITOR-RASTER-02
  - EDITOR-RASTER-03
  - EDITOR-RASTER-04
  - EDITOR-LINE-01
  - EDITOR-LINE-02
  - EDITOR-FILL-04
  - EDITOR-BASEMAP-02
  - EDITOR-BASEMAP-03
tags: [builder, mcp, live-smoke, playwright, verification]
must_haves:
  truths:
    - "Live Playwright MCP smoke confirms: 4 raster sliders move on a live raster layer and the canvas reflects the brightness/contrast/saturation/hue change"
    - "Live Playwright MCP smoke confirms: line-cap + line-join Selects render in LineEditor on a live line layer; selecting an option mutates the line cap/join visually"
    - "Live Playwright MCP smoke confirms: FillEditor range hint renders on a 3D-extrusion fill layer with sample data"
    - "Live Playwright MCP smoke confirms: 'No basemap' preset card is clickable from BasemapGroupEditorScene; clicking it removes the basemap layers from the canvas"
    - "Live Playwright MCP smoke confirms: DETAIL LEVEL UI is ABSENT from BasemapSublayerEditorScene (visual confirmation of v1011 INV-01)"
    - "Live Playwright MCP smoke confirms: zero browser-console errors during the 5-surface walk"
  artifacts:
    - path: ".planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md"
      provides: "Per-surface MCP smoke report with pass/fail per requirement + console-error log + Playwright MCP timestamps"
      contains: "MCP smoke"
  key_links:
    - from: "1136-MCP-SMOKE.md report"
      to: "5 editor surfaces from Plans 01-04 + 1 absence pin from Plan 05"
      via: "Live walkthrough at localhost:8080 using canonical ADK map"
      pattern: "EDITOR-"
---

<objective>
Run a live Playwright MCP smoke test against `localhost:8080` (the running Vite dev proxy) covering all 5 editor surfaces shipped by Plans 01-04 + the DETAIL LEVEL stays-gone visual confirmation from Plan 05. Produce a single report file `1136-MCP-SMOKE.md` listing pass/fail per requirement, console-error counts per surface, and Playwright MCP timestamps.

This is a CHECKPOINT plan — Claude drives the Playwright MCP browser autonomously through the 5-surface script, captures the outcome, writes the report, then surfaces the human-verify checkpoint at the end ONLY if any surface fails or console errors are non-zero. If all 5 surfaces pass clean, the report is sufficient — no human gate beyond the developer's review of the report file.

Purpose: This phase ships per-render-mode editor polish — the v1011/v1028 hard precedent is "live MCP smoke before milestone close-gate". ROADMAP Phase 1139 will re-verify across 3 viewports at milestone close, but each editor-touching phase must self-verify its own surfaces FIRST.

Canonical test map: `c39be324-6815-40e5-8143-00a2723827b2` (ADK High Peaks map, used by Phase 1133 audit + Phase 1134 MCP smoke + Phase 1135 MCP smoke). This map contains layers covering every render mode (fill + line + circle + raster + DEM/terrain + basemap), making it the single-map fixture that exercises all 5 Phase 1136 surfaces.

Output:
- `1136-MCP-SMOKE.md` containing:
  - Stack health snapshot (5/5 docker containers up)
  - Browser console-error baseline (before navigation)
  - 5 surface walks (one per surface) with pass/fail + console-error delta per surface
  - Final disposition: PASS (all 5 clean) / FAIL (one or more issues)
- If FAIL: a checkpoint:human-verify task pauses execution so the developer can decide whether to (a) fix inline and re-run, or (b) carry to v1031.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1136-per-render-mode-editor-polish/1136-CONTEXT.md
@.planning/phases/1136-per-render-mode-editor-polish/1136-UI-SPEC.md

@.planning/phases/1136-per-render-mode-editor-polish/1136-01-SUMMARY.md
@.planning/phases/1136-per-render-mode-editor-polish/1136-02-SUMMARY.md
@.planning/phases/1136-per-render-mode-editor-polish/1136-03-SUMMARY.md
@.planning/phases/1136-per-render-mode-editor-polish/1136-04-SUMMARY.md
@.planning/phases/1136-per-render-mode-editor-polish/1136-05-SUMMARY.md
@.planning/phases/1136-per-render-mode-editor-polish/1136-06-SUMMARY.md

# Phase 1134 MCP smoke template — the format this report should follow:
@.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-06-SUMMARY.md

<interfaces>
<!-- Key contracts. -->

Canonical map ID: `c39be324-6815-40e5-8143-00a2723827b2` — ADK High Peaks; verified to contain:
- 1+ raster layer (NY 2023 orthos) — exercises Plans 01 (RasterEditor sliders)
- 1+ line layer (trails / roads) — exercises Plan 02 (LineEditor cap/join)
- 1+ fill layer with `paint._height_column` (urban areas building footprints OR adding one for the test) — exercises Plan 03 (FillEditor range hint)
- Default basemap (Positron or similar) — exercises Plan 04 ("No basemap" preset)
- BasemapSublayerEditorScene reachable via Settings → drill into a basemap sublayer — exercises Plan 05 (DETAIL LEVEL visual absence)

Stack health command (verify 5/5 containers up before MCP smoke begins):
```bash
docker compose ps --format json | grep -c '"State":"running"'
# Must return 5 (frontend + api + worker + db + titiler)
```

Vite dev proxy URL: `http://localhost:8080` (per MEMORY.md "Credentials" section).

Per-surface contract (sourced from REQUIREMENTS.md):
- **EDITOR-RASTER-01..04 visual:** Move each of the 4 sliders; canvas reflects the change within ~250ms (matching coalesceFrame debounce). Click Reset; all 4 sliders return to 0.
- **EDITOR-LINE-01/02 visual:** Open the Cap Select; pick "Square"; line caps update on canvas. Open Join Select; pick "Bevel"; line joins update.
- **EDITOR-FILL-04 visual:** Open a fill layer with a height column set; range hint text reads "Range: X–Y, N features" with actual numeric values.
- **EDITOR-BASEMAP-02 visual:** Click "No basemap" card; basemap layers disappear from canvas; switch back to a real preset; basemap reappears.
- **EDITOR-BASEMAP-03 visual:** Open a basemap sublayer's editor; verify NO "Detail Level" heading / pill strip / customized hint visible (confirms test pin matches live render).

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stack health + baseline console snapshot</name>
  <files>
    .planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md
  </files>
  <read_first>
    - .planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-06-SUMMARY.md (existing MCP smoke report format — mirror its structure)
  </read_first>
  <action>
    1. Run `docker compose ps --format json | grep -c '"State":"running"'`. Expected: `5`. If less, run `docker compose up -d --build` and re-check.

    2. Open Playwright MCP at `http://localhost:8080`. Log into the admin account (use the env-var-set credentials per MEMORY.md "Credentials" section).

    3. Capture initial browser console snapshot via Playwright MCP browser_console_messages — record error count (should be 0).

    4. Navigate to the canonical map: `http://localhost:8080/builder/c39be324-6815-40e5-8143-00a2723827b2`. Wait for the LayerStack to render (visual confirmation: layer list visible in the left sidebar).

    5. Re-snapshot console — record any errors during initial load (should be 0 or only known-acceptable "noise" warnings that Phase 1133 audit catalogued; do NOT carry-forward unknown errors).

    6. Write the opening section of `1136-MCP-SMOKE.md`:
       ```markdown
       # Phase 1136 MCP Smoke Report

       **Date:** {ISO 2026-05-2X}
       **Stack:** 5/5 docker containers running
       **Test URL:** http://localhost:8080
       **Test map:** c39be324-6815-40e5-8143-00a2723827b2 (ADK High Peaks)
       **Baseline console errors:** 0 (or N — listed in detail)

       ## Surface Coverage

       | Surface | Requirement(s) | Plan | Status |
       |---|---|---|---|
       | RasterEditor 4 sliders + Reset | EDITOR-RASTER-01..04 | 01 | (filled in by Tasks 2-5) |
       | LineEditor Cap + Join Selects | EDITOR-LINE-01, EDITOR-LINE-02 | 02 | … |
       | FillEditor 3D extrusion range hint | EDITOR-FILL-04 | 03 | … |
       | BasemapGroupEditorScene "No basemap" preset | EDITOR-BASEMAP-02 | 04 | … |
       | BasemapSublayerEditorScene DETAIL LEVEL absent | EDITOR-BASEMAP-03 | 05 | … |
       ```
  </action>
  <acceptance_criteria>
    - `docker compose ps` shows 5 running containers.
    - Playwright MCP session is open at `http://localhost:8080/builder/c39be324-...` with the LayerStack visible.
    - `1136-MCP-SMOKE.md` exists with the header section populated.
    - Console error baseline is recorded (0 or explicitly listed).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md && echo "REPORT_EXISTS"; docker compose ps --format json 2>/dev/null | grep -c '"State":"running"'</automated>
  </verify>
  <done>
    Stack healthy, browser at canonical map, report file scaffolded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Walk Surface 1 — RasterEditor 4 sliders + Reset (EDITOR-RASTER-01..04)</name>
  <files>
    .planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md
  </files>
  <read_first>
    - .planning/phases/1136-per-render-mode-editor-polish/1136-01-SUMMARY.md (confirm what shipped)
    - .planning/phases/1136-per-render-mode-editor-polish/1136-UI-SPEC.md (Surface 1 contract)
  </read_first>
  <action>
    Via Playwright MCP, on the canonical map:

    1. Locate a raster layer in the LayerStack (NY 2023 orthos — the visible raster layer name should be in the layer list).
    2. Click the raster layer row → LayerEditorPanel opens (380px flyout per sketch-findings-geolens).
    3. Confirm RasterEditor renders 4 sliders with labels: "Brightness min", "Contrast", "Saturation", "Hue".
    4. Drag the Brightness slider to ~0.5; capture screenshot; verify canvas brightens.
    5. Drag the Contrast slider to ~0.3; verify canvas contrast increase.
    6. Drag the Saturation slider to ~-0.3; verify canvas desaturates.
    7. Drag the Hue slider to ~60; verify canvas hue shifts.
    8. Expand the Reset Collapsible; click "Reset to defaults"; verify all 4 sliders return to 0 visually + canvas returns to original raster appearance.
    9. Re-snapshot browser console; record any new errors as a delta.
    10. Append Surface 1 details to `1136-MCP-SMOKE.md`:
        ```markdown
        ## Surface 1: RasterEditor (EDITOR-RASTER-01..04)

        - **PASS / FAIL:** PASS
        - **Brightness slider:** PASS — slider moved to 0.5, canvas brightened
        - **Contrast slider:** PASS — slider moved to 0.3, canvas contrast increased
        - **Saturation slider:** PASS — slider moved to -0.3, canvas desaturated
        - **Hue-rotate slider:** PASS — slider moved to 60, canvas hue shifted
        - **Reset button:** PASS — all 4 sliders returned to 0 + canvas restored
        - **Console errors:** 0 delta from baseline
        ```
  </action>
  <acceptance_criteria>
    - All 5 slider interactions trigger a visible canvas change.
    - Reset returns all 4 to defaults.
    - Zero new console errors during Surface 1 walk.
    - `1136-MCP-SMOKE.md` Surface 1 section is populated.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "Surface 1.*RasterEditor" .planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md 2>&1 | head -3</automated>
  </verify>
  <done>
    Surface 1 walk complete and recorded.
  </done>
</task>

<task type="auto">
  <name>Task 3: Walk Surface 2 — LineEditor Cap + Join Selects (EDITOR-LINE-01/02)</name>
  <files>
    .planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md
  </files>
  <read_first>
    - .planning/phases/1136-per-render-mode-editor-polish/1136-02-SUMMARY.md
  </read_first>
  <action>
    Via Playwright MCP:

    1. Locate a line layer in the LayerStack (trails / roads — pick any visible line layer in the ADK map).
    2. Click the line layer row → LayerEditorPanel opens.
    3. Scroll to the bottom of LineEditor; confirm "Line ends" heading present + Cap Select + Join Select visible.
    4. Open the Cap Select; verify 3 options: Butt, Round, Square. Pick "Square".
    5. Wait ~100ms (rAF tick); inspect the line caps on canvas — expect flat square ends (vs the default rounded ends).
    6. Open the Join Select; verify 3 options: Bevel, Round, Miter. Pick "Bevel".
    7. Wait; inspect line joints — expect angular bevels (vs default round).
    8. Pick "Round" again from both Selects; confirm visual return to default.
    9. Re-snapshot console; record delta.
    10. Append Surface 2 section:
        ```markdown
        ## Surface 2: LineEditor (EDITOR-LINE-01, EDITOR-LINE-02)

        - **PASS / FAIL:** PASS
        - **line-cap Select:** PASS — 3 options, "Square" applied + visual confirmed
        - **line-join Select:** PASS — 3 options, "Bevel" applied + visual confirmed
        - **Round restoration:** PASS — both return to default round shape
        - **Console errors:** 0 delta
        ```
  </action>
  <acceptance_criteria>
    - Both Selects render with 3 options each.
    - Selecting "Square" / "Bevel" produces visible canvas change.
    - Selecting "Round" restores defaults.
    - Zero new console errors.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "Surface 2.*LineEditor" .planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md 2>&1 | head -3</automated>
  </verify>
  <done>
    Surface 2 walk complete and recorded.
  </done>
</task>

<task type="auto">
  <name>Task 4: Walk Surface 3 — FillEditor 3D extrusion range hint (EDITOR-FILL-04)</name>
  <files>
    .planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md
  </files>
  <read_first>
    - .planning/phases/1136-per-render-mode-editor-polish/1136-03-SUMMARY.md
  </read_first>
  <action>
    Via Playwright MCP:

    1. Locate a fill layer in the ADK map that has 3D extrusion enabled (urban areas / building footprints). If none, briefly add a height column to an existing fill layer for the test (set heightColumn dropdown to a numeric column).
    2. Click the fill layer row → LayerEditorPanel opens.
    3. Scroll to the Height column Select; confirm a numeric column is selected (not "None").
    4. Verify the range hint renders below the Height column row: text matches `/Range: [\d.,]+–[\d.,]+, [\d,]+ features/` (en-dash separator). Screenshot for the report.
    5. Change the Height column to "None"; verify the range hint disappears (no placeholder text — UI-SPEC § Surface 3 "hidden silently").
    6. Re-select the numeric column; verify the hint reappears.
    7. Console snapshot.
    8. Append Surface 3 section:
        ```markdown
        ## Surface 3: FillEditor 3D extrusion range hint (EDITOR-FILL-04)

        - **PASS / FAIL:** PASS
        - **Range hint renders:** PASS — text "Range: 12–340, 1,247 features" (sample values from the column)
        - **Hidden when no column:** PASS — picking "None" removes the hint with no placeholder
        - **Reappears on re-select:** PASS
        - **En-dash separator (U+2013):** PASS — confirmed via visual inspection
        - **Console errors:** 0 delta
        ```
  </action>
  <acceptance_criteria>
    - Range hint renders when height column is set + sample data exists.
    - Hint disappears (silently) when column is cleared.
    - En-dash is the separator character.
    - Zero new console errors.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "Surface 3.*FillEditor" .planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md 2>&1 | head -3</automated>
  </verify>
  <done>
    Surface 3 walk complete and recorded.
  </done>
</task>

<task type="auto">
  <name>Task 5: Walk Surface 4 — BasemapGroupEditorScene "No basemap" preset (EDITOR-BASEMAP-02)</name>
  <files>
    .planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md
  </files>
  <read_first>
    - .planning/phases/1136-per-render-mode-editor-polish/1136-04-SUMMARY.md
  </read_first>
  <action>
    Via Playwright MCP:

    1. In the LayerStack sidebar, click the basemap row at the top → BasemapGroupEditorScene opens in the LayerEditorPanel flyout.
    2. Confirm the preset card grid is visible. Verify the FIRST card is the "No basemap" entry with the checkered-pattern thumbnail.
    3. Click the "No basemap" card. Wait for the canvas to re-render. Verify:
       - The basemap layers disappear (no Positron tiles visible)
       - Data layers (raster, line, fill) remain visible against a transparent / background-color canvas
       - The "No basemap" card now displays the active ring class
    4. Click a real preset card (e.g., "Positron"). Verify basemap reappears + active ring transfers.
    5. Toggle once more: click "No basemap" again; verify basemap disappears.
    6. SAVE the map (Cmd/Ctrl+S or the Save button). RELOAD the page. Verify:
       - `basemap_id` round-trips: the "No basemap" card is active on reload (proves persistence per UI-SPEC § Surface 4 "Persistence Round-Trip" contract).
    7. Console snapshot.
    8. Append Surface 4 section:
        ```markdown
        ## Surface 4: BasemapGroupEditorScene "No basemap" preset (EDITOR-BASEMAP-02)

        - **PASS / FAIL:** PASS
        - **"No basemap" card position:** PASS — FIRST entry in preset grid
        - **Click → basemap disappears:** PASS
        - **Data layers remain visible:** PASS
        - **Active ring on "No basemap":** PASS
        - **Switch back to Positron:** PASS — basemap reappears
        - **Persistence round-trip:** PASS — saved + reloaded; "No basemap" still active
        - **Console errors:** 0 delta
        ```
  </action>
  <acceptance_criteria>
    - "No basemap" is the first card in the grid.
    - Clicking it removes basemap; data layers persist.
    - Active ring transfers correctly.
    - SAVE → RELOAD restores "No basemap" selection.
    - Zero new console errors.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "Surface 4.*No basemap|EDITOR-BASEMAP-02" .planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md 2>&1 | head -5</automated>
  </verify>
  <done>
    Surface 4 walk complete and recorded; persistence round-trip verified live.
  </done>
</task>

<task type="auto">
  <name>Task 6: Walk Surface 5 — BasemapSublayerEditorScene DETAIL LEVEL absence (EDITOR-BASEMAP-03)</name>
  <files>
    .planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md
  </files>
  <read_first>
    - .planning/phases/1136-per-render-mode-editor-polish/1136-05-SUMMARY.md
  </read_first>
  <action>
    Via Playwright MCP:

    1. First, restore a real basemap (e.g., Positron) if Task 5 left the map in "No basemap" state.
    2. In the LayerStack, expand the basemap group (click the disclosure caret). The basemap sublayers (Roads / Labels / Buildings / Boundaries) become visible.
    3. Click a basemap sublayer row (e.g., "Roads") → BasemapSublayerEditorScene opens in the LayerEditorPanel flyout.
    4. Visually inspect the entire scene. Confirm the following are ABSENT:
       - NO "DETAIL LEVEL" heading or text
       - NO pill strip / radiogroup with detail-level options
       - NO "currently customized" status hint
    5. Confirm the following ARE present (existing surfaces — sanity-check the test environment is correct):
       - STROKE section (Color + Width)
       - CASING section (Casing color + Casing width)
       - ZOOM RANGE section (Min zoom + Max zoom)
       - Visibility / Opacity section
       - RESET collapsible
    6. Console snapshot.
    7. Append Surface 5 section:
        ```markdown
        ## Surface 5: BasemapSublayerEditorScene DETAIL LEVEL absence (EDITOR-BASEMAP-03)

        - **PASS / FAIL:** PASS
        - **DETAIL LEVEL heading absent:** PASS
        - **DETAIL LEVEL pill strip / radiogroup absent:** PASS
        - **"currently customized" hint absent:** PASS
        - **STROKE / CASING / ZOOM RANGE / Visibility / RESET sections still present:** PASS — full editor surface visually correct
        - **Console errors:** 0 delta
        ```
  </action>
  <acceptance_criteria>
    - DETAIL LEVEL surface is visually confirmed absent.
    - All other editor sections (STROKE/CASING/ZOOM/Visibility/RESET) still present.
    - Zero new console errors.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "Surface 5.*DETAIL LEVEL|EDITOR-BASEMAP-03" .planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md 2>&1 | head -5</automated>
  </verify>
  <done>
    Surface 5 walk complete and recorded; live visual confirmation matches the test-pin from Plan 05.
  </done>
</task>

<task type="auto">
  <name>Task 7: Finalize report with overall disposition</name>
  <files>
    .planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md
  </files>
  <read_first>
    - .planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md (the in-progress report from Tasks 1-6)
  </read_first>
  <action>
    Append the final disposition section to `1136-MCP-SMOKE.md`:

    ```markdown
    ## Overall Disposition

    | Surface | Requirement(s) | Status |
    |---|---|---|
    | RasterEditor | EDITOR-RASTER-01..04 | PASS |
    | LineEditor | EDITOR-LINE-01, EDITOR-LINE-02 | PASS |
    | FillEditor range hint | EDITOR-FILL-04 | PASS |
    | "No basemap" preset | EDITOR-BASEMAP-02 | PASS |
    | DETAIL LEVEL absence | EDITOR-BASEMAP-03 | PASS |

    **Final:** PASS — all 5 surfaces clean, all 9 requirements satisfied live, 0 console errors across the walkthrough.

    ## Console Error Log

    Baseline: 0
    After Surface 1: 0 delta
    After Surface 2: 0 delta
    After Surface 3: 0 delta
    After Surface 4: 0 delta
    After Surface 5: 0 delta

    Total session console errors: 0

    ## Carry-Forward Findings

    None.

    ## Cross-References

    - REQUIREMENTS.md EDITOR-RASTER-01..04, EDITOR-LINE-01, EDITOR-LINE-02, EDITOR-FILL-04, EDITOR-BASEMAP-02, EDITOR-BASEMAP-03
    - Phase 1133 audit rows WALK-R-01..04, WALK-L-01, WALK-L-02, WALK-F-03, WALK-B-01, WALK-B-02
    - v1011 INV-01 DETAIL LEVEL disposition
    - Phase 1059 D-18 disposition reaffirmation
    ```

    If any surface returned FAIL or non-zero console errors, replace the above with the actual findings AND add a `## Issues` section listing each finding with: surface, requirement(s) affected, observed behavior, expected behavior, screenshot reference, proposed disposition (fix-inline vs carry-to-v1031).
  </action>
  <acceptance_criteria>
    - `1136-MCP-SMOKE.md` final section populated with Overall Disposition table.
    - Console error log present.
    - Carry-Forward Findings section explicitly states "None" OR lists findings.
    - Cross-References section cites REQUIREMENTS.md REQ IDs + Phase 1133 audit rows.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "Overall Disposition|Final:.*(PASS|FAIL)" .planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md 2>&1 | head -5</automated>
  </verify>
  <done>
    MCP smoke report is complete, signed off, committed to git.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Developer reviews 1136-MCP-SMOKE.md</name>
  <files>
    .planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md
  </files>
  <what-built>
    Live Playwright MCP smoke walked all 5 Phase 1136 editor surfaces against `localhost:8080` on the canonical ADK map. Results are recorded in `.planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md`.
  </what-built>
  <action>
    Pause execution and wait for the developer to review the report. No code or file mutations performed by Claude in this task — purely a gate for human sign-off after the autonomous MCP walk in Tasks 1-7. The developer either approves (resume token "approved") or surfaces findings that route either to inline fixes or v1031 carry-forward.
  </action>
  <how-to-verify>
    1. Open `.planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md`.
    2. Verify the Overall Disposition table shows PASS for all 5 surfaces.
    3. Verify the Console Error Log shows 0 errors per surface.
    4. Verify the Carry-Forward Findings section is "None" OR lists items you accept.
    5. Spot-check at least 1 surface live by re-opening the canonical map at `http://localhost:8080/builder/c39be324-6815-40e5-8143-00a2723827b2` and clicking through to confirm the surface still works.

    If you spot a real-world regression that the headless walk missed, type the specific issue(s) and we will route to v1031 or fix inline.
  </how-to-verify>
  <verify>
    <human-check>Developer reads 1136-MCP-SMOKE.md and either types "approved" or surfaces a finding. No automated gate at this step — the report itself is the artifact under review.</human-check>
  </verify>
  <done>
    Developer typed "approved" OR every surfaced finding has a routed disposition (inline fix vs v1031 carry-forward).
  </done>
  <resume-signal>Type "approved" to close Phase 1136, or describe issues found.</resume-signal>
</task>

</tasks>

<verification>

**Local self-check before SUMMARY:**

1. `1136-MCP-SMOKE.md` exists with all 5 surface sections + Overall Disposition table.
2. Each surface section contains pass/fail per requirement + console error delta.
3. `grep -nE "PASS|FAIL" .planning/phases/1136-per-render-mode-editor-polish/1136-MCP-SMOKE.md | head -10` returns at least 10 hits.

</verification>

<success_criteria>
- Live MCP smoke verified all 5 editor surfaces against `localhost:8080` on the canonical ADK map.
- All 9 EDITOR requirements satisfied live (visual + interaction + persistence where applicable).
- Zero browser-console errors across the walkthrough.
- Report file `1136-MCP-SMOKE.md` exists with full pass/fail breakdown + console log + carry-forward findings.
- Developer checkpoint reviewed and approved.
</success_criteria>

<output>
Create `.planning/phases/1136-per-render-mode-editor-polish/1136-07-SUMMARY.md` summarizing:
- 5 surfaces walked live via Playwright MCP
- 9 requirements verified PASS (or list FAILs)
- Console error log (0 across walkthrough OR specifics)
- Cross-reference to `1136-MCP-SMOKE.md` for the per-surface detail
</output>