---
phase: 1133-audit-first-builder-walkthrough
plan: 02
type: execute
wave: 2
depends_on:
  - 1133-01
files_modified:
  - .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md
autonomous: true
requirements:
  - WALK-02
user_setup: []

must_haves:
  truths:
    - "Every `/ai/*` endpoint defined in `backend/app/processing/ai/router.py` appears in the AI Consumer-Gating Matrix as its own row."
    - "Every row identifies the frontend hook (or `(no frontend consumer)`) that calls the endpoint."
    - "Every row records the live `enabled` gate expression as it exists in the source today (e.g. `enabled: !!token && isAdmin` from `use-ai-availability.ts:21`), OR `(no gate — direct fetch)` if the call site bypasses TanStack Query."
    - "Every row records the 403 distinct surface AND the 503 distinct surface (banner / toast / silent-fail / etc.) — these are the live behaviors today, not aspirational."
    - "Every row records Pitfall #4 status as PASS / FAIL / N/A with a one-line rationale."
    - "Findings that result from this matrix (AI hook missing a gate, 403/503 surface absent or broken) are appended to the Phase 1134-1138 Routing Table under Phase 1135 (AI-*) REQ IDs."
  artifacts:
    - path: ".planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md"
      provides: "AI Consumer-Gating Matrix section replaces the Plan 01 stub."
      contains: "## AI Consumer-Gating Matrix"
  key_links:
    - from: ".planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md"
      to: "backend/app/processing/ai/router.py"
      via: "every matrix row cites an endpoint defined in `router.py`"
      pattern: "/ai/(generate-map|chat|metadata)"
    - from: ".planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md"
      to: "frontend/src/hooks/use-ai-availability.ts + frontend/src/components/builder/ChatPanel.tsx"
      via: "matrix Frontend Hook column points to the call-site file + line"
      pattern: "use-ai-availability|ChatPanel|use-admin|ai-metadata"
---

<objective>
Replace the `## AI Consumer-Gating Matrix` stub from Plan 01 with a complete cross-reference table: every `/ai/*` endpoint × frontend hook, with explicit columns for `enabled` gating, 403 distinct surface, and 503 distinct surface. This is the Pitfall #4 (v1010.2 SF-06 recurrence guard) audit — same auth gap, same fix pattern: when a planner names ONE hook (e.g. `useAIStatus`), greppers must catch sibling-shape hooks (`useEmbeddingStats`) so v1030's AI work doesn't ship the same defect.

Purpose: Phase 1135 (AI chat polish) reads this matrix as its work list. Every row with `enabled` = `(no gate — direct fetch)` or `(missing)` is a Phase 1135 task. Every row with broken 403/503 surface is a Phase 1135 task.

Output: `## AI Consumer-Gating Matrix` section in `1133-BUILDER-WALKTHROUGH-AUDIT.md` populated end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1133-audit-first-builder-walkthrough/1133-CONTEXT.md
@.planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md
@backend/app/processing/ai/router.py
@frontend/src/hooks/use-ai-availability.ts
@frontend/src/hooks/use-admin.ts
@frontend/src/components/builder/ChatPanel.tsx
@frontend/src/api/ai-metadata.ts
@frontend/src/api/maps.ts

<interfaces>
<!-- Anchor: live state on `main` as of 2026-05-27 -->

Backend `/ai/*` endpoints (from `backend/app/processing/ai/router.py`):
- `POST /ai/generate-map/`              (line 178)
- `POST /ai/generate-map/stream/`       (line 205, SSE)
- `POST /ai/chat/`                      (line 268)
- `POST /ai/chat/stream/`                (line 304, SSE)
- `POST /ai/metadata/summary/`           (line 454)
- `POST /ai/metadata/keywords/`          (line 471)
- `POST /ai/metadata/lineage/`           (line 488)
- `POST /ai/metadata/{...}`              (line 505 — verify exact path during plan execution)

All routes:
- gated by `Depends(require_permission("use_ai_chat"))` at FastAPI layer
- gated by `await _check_ai_available(db)` at endpoint top (returns 503 when AI disabled in admin settings)

Frontend consumers (per `grep -rln "useAIStatus\\|useEmbeddingStats" frontend/src`):
- `frontend/src/hooks/use-ai-availability.ts:21` — `useAIStatus({ enabled: !!token && isAdmin })` — admin-only gate
- `frontend/src/hooks/use-admin.ts` — defines `useAIStatus`
- `frontend/src/components/admin/AIStatusCard.tsx` — admin panel
- `frontend/src/components/admin/settings/SettingsAITab.tsx` — admin panel
- `frontend/src/components/builder/ChatPanel.tsx` — `/ai/chat/` SSE consumer (Builder AI rail)
- `frontend/src/api/ai-metadata.ts` — `/ai/metadata/*` consumers (dataset/record metadata drafting)
- `frontend/src/api/maps.ts` — `/ai/generate-map/` map-from-prompt consumer

Composite gate (live today): `useAIAvailability()` returns `isAIAvailable = aiStatus.data?.enabled && aiStatus.data?.configured && can('use_ai_chat')`. This is the canonical "is AI available right now" predicate.

Pitfall #4 (per CONTEXT.md): Every `/ai/*` consumer hook MUST have `enabled: !!token && aiEnabled` semantics (where `aiEnabled` is the live `useAIAvailability().isAIAvailable` for non-admin consumers, or `!!token && isAdmin` for admin-only consumers).

v1010.2 SF-06 precedent: when adding ONE gate, audit sibling hooks for the same shape. Same auth gap, same fix pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate `/ai/*` endpoints from backend router + every frontend call-site</name>
  <files>.planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md</files>
  <read_first>
    - backend/app/processing/ai/router.py (full file — every `@router.post(...)` decorator is one row)
    - frontend/src/hooks/use-ai-availability.ts (canonical composite gate)
    - frontend/src/hooks/use-admin.ts (sibling hooks — useAIStatus, useEmbeddingStats, etc.)
    - frontend/src/components/builder/ChatPanel.tsx (builder AI rail SSE consumer)
    - frontend/src/api/ai-metadata.ts (metadata drafting consumers)
    - frontend/src/api/maps.ts (generate-map consumer)
  </read_first>
  <action>
    Replace the `## AI Consumer-Gating Matrix` stub section (placeholder note from Plan 01) with the populated matrix.

    Matrix table header (use exactly):
    `| Endpoint | Method | Frontend Hook / Call Site | `enabled` Gate (live) | 403 Surface | 503 Surface | Pitfall #4 Status | Owning Phase | REQ ID |`

    Populate one row per `@router.post` in `backend/app/processing/ai/router.py`. For each row:

    1. **Endpoint column** — exact path including trailing slash if present, e.g. `/ai/chat/`.
    2. **Method column** — `POST` (or `POST (SSE)` for streaming endpoints).
    3. **Frontend Hook / Call Site** — file + line of the consumer. Multiple consumers → one row per consumer. If no frontend caller exists today, write `(no frontend consumer)` and Pitfall #4 = `N/A`.
    4. **`enabled` Gate (live)** — verbatim from the source. Examples:
       - `enabled: !!token && isAdmin` from `use-ai-availability.ts:21`
       - `enabled: !!token && aiEnabled` (target pattern; flag any divergence)
       - `(no gate — direct fetch via apiFetch in mutation)` if the call site is a TanStack Mutation or a raw fetch (mutations don't have an `enabled` predicate; record their call-trigger gate instead — e.g. button-click handler guarded by `isAIAvailable` from `useAIAvailability()`).
    5. **403 Surface** — what the user sees when the backend returns 403 (`require_permission` failure). Inspect call-site error handling. Examples: `react-error-boundary toast "AI is unavailable"`, `silent fail (caught and logged only)`, `error banner in ChatPanel`. Test live in MCP if unclear — DO NOT guess.
    6. **503 Surface** — what the user sees when `_check_ai_available` raises (AI disabled). Examples: `disabled-state panel (v1028 closure)`, `inert button`, `console.error only`.
    7. **Pitfall #4 Status** — PASS / FAIL / N/A:
       - PASS = gate exists and matches the `!!token && aiEnabled`-shape OR `!!token && isAdmin`-shape for admin-only consumers.
       - FAIL = gate missing or mis-shaped (e.g. only `!!token` without `aiEnabled` / `isAdmin`; or mutation handler not guarded by `isAIAvailable`).
       - N/A = no frontend consumer exists.
    8. **Owning Phase** — `1135` for any FAIL row (AI chat polish closes them). `—` for PASS / N/A rows.
    9. **REQ ID** — the v1030 REQ this row closes: `AI-02` (provider-disabled UAT), `AI-03` (provider-error UAT), `AI-08` (analysis card), `AI-04` (visibility-filter docstring), etc. For FAIL rows that don't fit an existing REQ → `(unmapped)` and add a Notes line.

    Sibling-hook sweep (Pitfall #4 / v1010.2 SF-06): after the primary endpoints are tabulated, add a follow-up subsection `### Sibling-Hook Sweep` listing every admin hook in `use-admin.ts` that touches an admin-only endpoint (not just AI). Confirm each has a `!!token && isAdmin` gate. If a hook is missing the gate, it's a FAIL row in the matrix even if it's not `/ai/*` — the precedent is "same auth gap, same fix pattern". Owning phase for sibling-hook FAILs that touch non-AI endpoints → `(carry-forward to v1031)` UNLESS the hook is consumed in the builder, in which case → `1138`.

    After the matrix, route every FAIL row into `## Phase 1134-1138 Routing Table` (the existing table from Plan 01). Use the same row shape as Plan 01 routing.
  </action>
  <verify>
    <automated>grep -A 200 "^## AI Consumer-Gating Matrix" .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md | grep -B 200 -m 1 "^## " | grep -cE "^\| /ai/" | awk '{ if ($1 &gt;= 6) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - The `## AI Consumer-Gating Matrix` section contains a table with the agreed 9 columns.
    - At least 6 rows correspond to backend endpoints in `backend/app/processing/ai/router.py` (`/ai/generate-map/`, `/ai/generate-map/stream/`, `/ai/chat/`, `/ai/chat/stream/`, `/ai/metadata/summary/`, `/ai/metadata/keywords/`, `/ai/metadata/lineage/`, plus any 8th metadata route at line 505+).
    - The "Pitfall #4 Status" column on every row is PASS / FAIL / N/A — not blank.
    - At least one row cites `use-ai-availability.ts:21` (verifying the canonical admin gate was read).
    - Sibling-Hook Sweep subsection exists and either documents 0 sibling gaps OR lists each gap with a "v1010.2 SF-06" cross-reference.
    - Every FAIL row has a corresponding row appended to `## Phase 1134-1138 Routing Table` (in the same doc, populated by Plan 01).
  </acceptance_criteria>
  <done>Matrix is the canonical Phase 1135 work list; Pitfall #4 audit is complete and traceable.</done>
</task>

</tasks>

<verification>
- `## AI Consumer-Gating Matrix` is populated end-to-end (no stub text remains).
- Every backend `/ai/*` endpoint defined in `router.py` has at least one row.
- Sibling-Hook Sweep subsection exists.
- Every FAIL row is routed into the Phase 1134-1138 Routing Table under Phase 1135.
</verification>

<success_criteria>
- WALK-02 satisfied: Pitfall #4 audit complete; v1010.2 SF-06 recurrence is now guarded by the sibling-hook sweep.
- Phase 1135 planner has a concrete work list for AI chat polish.
</success_criteria>

<output>
Create `.planning/phases/1133-audit-first-builder-walkthrough/1133-02-SUMMARY.md` when done.
</output>
