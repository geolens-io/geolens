---
phase: 1133-audit-first-builder-walkthrough
plan: 04
type: execute
wave: 4
depends_on:
  - 1133-01
  - 1133-02
  - 1133-03
files_modified:
  - .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md
autonomous: true
requirements:
  - WALK-04
user_setup: []

must_haves:
  truths:
    - "Grep guard 1: `grep -rnE 'map\\.setPaintProperty|map\\.setLayoutProperty' frontend/src --include='*.ts*'` is run live; the full output is captured in the audit doc."
    - "Every grep hit is classified as (a) inside `frontend/src/components/builder/layer-adapters/`, (b) inside `frontend/src/components/builder/map-sync.ts`, or (c) outside — i.e., a documented exception that requires explicit rationale."
    - "Known existing exceptions (`ViewerMap.tsx:699`, `label-layer-utils.ts:78-94`) are recorded with rationale; if any NEW exception exists (not in layer-adapters AND not in map-sync AND not label-layer-utils AND not ViewerMap), it is filed as a P1 finding routed to Phase 1134 or 1136 with REQ ID."
    - "BuilderLayerAction union is the only typed mutation entry point: a grep against `dispatchBuilderLayerAction` callers OR against `BuilderLayerAction` consumers proves no bypass exists (e.g., no component constructs ad-hoc layer-mutation objects outside the union)."
    - "v1008 unified-stack contract verified: drag-orderable stack with basemap-as-group still uses the `disabled.droppable` per-drag-source pattern from v1011 — no regression of basemap-group becoming a collision target during catalog drags."
  artifacts:
    - path: ".planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md"
      provides: "Invariant Grep Checks section replaces the Plan 01 stub."
      contains: "## Invariant Grep Checks"
  key_links:
    - from: ".planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md"
      to: "frontend/src/components/builder/layer-adapters/"
      via: "every grep hit row records the file + line; non-adapter / non-map-sync hits are flagged"
      pattern: "layer-adapters/|map-sync\\.ts"
    - from: ".planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md"
      to: "frontend/src/components/builder/builder-action-contract.ts"
      via: "BuilderLayerAction union check cites the contract file + line"
      pattern: "BuilderLayerAction|dispatchBuilderLayerAction"
---

<objective>
Replace the `## Invariant Grep Checks` stub from Plan 01 with verified live grep results that prove v1008 unified-stack + v1026 reconciler + v1027 typed action-boundary contracts hold on clean `main` post-`3ed5ceb3`. This is WALK-04 explicit: `grep -nE 'map\.setPaintProperty|map\.setLayoutProperty' frontend/src --include='*.ts*' -r` must return only `layer-adapters/` + `map-sync.ts` hits (plus the two known-good exceptions), AND `BuilderLayerAction` union remains the only mutation entry point.

Purpose: Phase 1136 (per-render-mode editor polish) Pitfall #9 watch — every new slider/picker must route through layer-adapters, not direct `map.setPaintProperty`. The grep guard is the canonical contract proof. Without this audit, Phase 1136 might ship a RasterEditor slider that bypasses RasterAdapter and breaks the v1026 reconciler invariant.

Output: `## Invariant Grep Checks` section in `1133-BUILDER-WALKTHROUGH-AUDIT.md` populated end-to-end with live grep output and exception annotations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1133-audit-first-builder-walkthrough/1133-CONTEXT.md
@.planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md
@frontend/src/components/builder/builder-action-contract.ts
@frontend/src/components/builder/map-sync.ts
@frontend/src/components/builder/layer-adapters/shared.ts
@frontend/src/components/builder/label-layer-utils.ts
@frontend/src/components/viewer/ViewerMap.tsx
@.planning/milestones/v1011-MILESTONE-AUDIT.md
@.planning/milestones/v1027-MILESTONE-AUDIT.md

<interfaces>
<!-- Known live grep output as of 2026-05-27 (captured during planning — re-run live in execution to catch drift) -->

`grep -rnE "map\.setPaintProperty|map\.setLayoutProperty" frontend/src --include="*.ts*"` returned 29+ hits on planning day:

Allowed (inside layer-adapters/ OR map-sync.ts):
- map-sync.ts:258, 261, 345, 355, 798, 808
- layer-adapters/line-adapter.ts:217
- layer-adapters/shared.ts:136, 154, 178, 180, 251, 321, 331, 366
- (more in other adapters — re-grep to enumerate)

Documented exceptions (NOT inside adapters/map-sync — must be recorded with rationale):
- frontend/src/components/viewer/ViewerMap.tsx:699 — `map.setLayoutProperty(labelId, 'visibility', ...)` — Viewer is read-only / no Builder action boundary applies; label visibility sync mirrors builder label-layer-utils.
- frontend/src/components/builder/label-layer-utils.ts:78-94 — label sublayer mutations (`text-field`, `text-size`, `symbol-placement`, etc.); this is the LABEL companion-layer surface that v1026 reconciler delegates to. v1026 SUMMARY should confirm this is the intended boundary (verify during plan execution).

If any hit is OUTSIDE adapters/map-sync AND not in the two documented-exception files → P1 finding routed to Phase 1134 or 1136.

BuilderLayerAction union (from `frontend/src/components/builder/builder-action-contract.ts:10`):
- `export type BuilderLayerAction = ...` — sum type of all allowed layer mutations.
- `export function dispatchBuilderLayerAction(action, handlers)` at line 62 — single entry point.
- Verify: grep for non-test callers of layer-mutation primitives (`setLayer*`, `addLayer*`, `removeLayer*`) to confirm none bypass dispatchBuilderLayerAction. If a caller bypasses the dispatch (e.g., direct mutation in a component effect), it's a v1027 boundary violation → P1 finding routed to Phase 1134.

v1008 unified-stack contract (from project memory + v1011 audit):
- UnifiedStackPanel uses dnd-kit `useSortable` with the `disabled.droppable` per-drag-source contract (v1011 CTRL-01 closure) — basemap-group is NOT a `closestCenter` collision target during catalog drags.
- Grep for `disabled:.*droppable` or `useDndContext` patterns in UnifiedStackPanel-related files to confirm the contract is in place.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run live grep guards and tabulate every hit with classification</name>
  <files>.planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md</files>
  <read_first>
    - frontend/src/components/builder/builder-action-contract.ts (lines 10-65 — union + dispatcher)
    - frontend/src/components/builder/map-sync.ts (lines 250-360 + 790-820 — known setPaintProperty / setLayoutProperty hits)
    - frontend/src/components/builder/layer-adapters/shared.ts (lines 130-370 — shared helpers for adapter mutations)
    - frontend/src/components/builder/label-layer-utils.ts (lines 78-94 — documented exception)
    - frontend/src/components/viewer/ViewerMap.tsx (line 699 — documented exception)
  </read_first>
  <action>
    Replace the `## Invariant Grep Checks` stub section with the populated invariant table.

    Section structure:

    ### Grep Guard 1: Direct `setPaintProperty` / `setLayoutProperty`
    Command run:
    `grep -rnE "map\.setPaintProperty|map\.setLayoutProperty" frontend/src --include="*.ts*"`

    Capture the exact output. Then build a table:
    `| File:Line | Inside layer-adapters/ ? | Inside map-sync.ts ? | Documented Exception ? | Disposition |`

    For every hit:
    - If inside `frontend/src/components/builder/layer-adapters/` → `Inside layer-adapters/: yes`, Disposition `PASS — adapter boundary`.
    - Else if inside `frontend/src/components/builder/map-sync.ts` → `Inside map-sync.ts: yes`, Disposition `PASS — reconciler boundary`.
    - Else if inside `frontend/src/components/builder/label-layer-utils.ts` → `Documented Exception: yes (label companion-layer surface; v1026 reconciler delegation point)`, Disposition `PASS — documented exception`.
    - Else if inside `frontend/src/components/viewer/ViewerMap.tsx` → `Documented Exception: yes (viewer is read-only; no Builder action boundary)`, Disposition `PASS — documented exception`.
    - Else → `Disposition: FAIL — undocumented exception; P1 finding, route to Phase 1134 (boundary violation)`. Add a corresponding routing-table row.

    ### Grep Guard 2: `BuilderLayerAction` union is the sole mutation entry point
    Commands run (use both):
    1. `grep -n "^export type BuilderLayerAction" frontend/src/components/builder/builder-action-contract.ts` — confirm union exists.
    2. `grep -rn "dispatchBuilderLayerAction" frontend/src --include="*.ts*"` — list every dispatch caller.
    3. `grep -rn "BuilderLayerAction" frontend/src --include="*.ts*"` — every reference.

    Record:
    - Union still exists at `builder-action-contract.ts:10` (or current line — verify).
    - Dispatch helpers (file + line for each non-test caller).
    - Any layer-mutation that constructs a state outside the union shape → FAIL row + route to Phase 1134.

    ### Grep Guard 3: v1008 unified-stack `disabled.droppable` contract still in place
    Command run:
    `grep -rn "disabled:.*droppable\|useDndContext" frontend/src/components/builder --include="*.ts*"`

    Confirm the per-drag-source disabled.droppable gating from v1011 CTRL-01 is present on the basemap-group in UnifiedStackPanel (or wherever the basemap row registers as sortable). If the pattern is missing → P1 finding routed to Phase 1134 (regression of v1011 CTRL-01).

    ### Grep Guard 4: v1027 typed action-boundary callers do not bypass `dispatchBuilderLayerAction`
    Commands run:
    1. `grep -rn "map\.removeLayer\|map\.removeSource" frontend/src/components/builder --include="*.ts*"` — direct removal must go through adapters or reconciler.
    2. `grep -rn "map\.addLayer\|map\.addSource" frontend/src/components/builder --include="*.ts*"` — direct addition same constraint.

    For every hit outside `layer-adapters/` or `map-sync.ts` (or documented v1026 exceptions in label-layer-utils / BuilderMap initialization), record a FAIL row + route.

    ### Summary
    After the four guards, write a one-paragraph summary: how many hits total, how many classified PASS, how many FAIL. The summary line `Grep guards passed: N/4` is the canonical sign-off — if N < 4, list the specific FAILs.
  </action>
  <verify>
    <automated>grep -A 300 "^## Invariant Grep Checks" .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md | grep -B 300 -m 1 "^## " | grep -cE "^### Grep Guard" | awk '{ if ($1 &gt;= 4) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - The `## Invariant Grep Checks` section contains exactly 4 subsections (one per grep guard).
    - Grep Guard 1 table lists every hit from the live grep output; every hit has a non-blank Disposition column.
    - Documented exceptions for `label-layer-utils.ts` and `ViewerMap.tsx` are present with rationale; no other exception is allowed without a Notes entry citing a prior milestone closure.
    - Every FAIL row (any of the 4 guards) has a corresponding entry in `## Phase 1134-1138 Routing Table` routed to Phase 1134 or 1136.
    - The Summary line records `Grep guards passed: N/4` — N is honest; if 4/4 the next sentence is "WALK-04 invariants verified clean."
  </acceptance_criteria>
  <done>Invariant audit is the canonical contract proof: Phase 1136 cannot ship adapter-bypass code without the FAIL surfacing here first.</done>
</task>

</tasks>

<verification>
- All 4 grep guards run live and tabulated.
- Every hit classified PASS / FAIL with disposition.
- FAIL rows routed to downstream phases via the Phase 1134-1138 Routing Table.
</verification>

<success_criteria>
- WALK-04 satisfied: v1008 / v1026 / v1027 contracts verified clean on `main` post-`3ed5ceb3` (or FAILs routed for Phase 1134/1136 closure).
</success_criteria>

<output>
Create `.planning/phases/1133-audit-first-builder-walkthrough/1133-04-SUMMARY.md` when done.
</output>
