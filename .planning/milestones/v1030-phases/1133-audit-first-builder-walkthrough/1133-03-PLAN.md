---
phase: 1133-audit-first-builder-walkthrough
plan: 03
type: execute
wave: 3
depends_on:
  - 1133-01
  - 1133-02
files_modified:
  - .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md
autonomous: true
requirements:
  - WALK-03
user_setup: []

must_haves:
  truths:
    - "Every line in `todo.md` between lines 96 and 171 inclusive that names an actionable item has a row in the staleness table."
    - "Every row classifies the item as `closed-in-prior-milestone` / `live-regression` / `genuine-new-gap` / `out-of-scope-anti-feature`."
    - "Every `closed-in-prior-milestone` row cites the specific milestone (v1011 / v1027 / v1028 / v1029 / v13.11 / etc.) and a commit SHA or REQ ID from that milestone — paraphrase is not citation."
    - "Every `live-regression` row routes to Phase 1134-1138 via a finding in the routing table; every `genuine-new-gap` row either routes or is flagged for v1031 carry-forward."
    - "Items already pre-marked ✅ in todo.md (lines 105/106/107) are recorded as `closed-in-prior-milestone` with the SHA already in the line preserved as the citation."
  artifacts:
    - path: ".planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md"
      provides: "todo.md Staleness Pass section replaces the Plan 01 stub."
      contains: "## todo.md Staleness Pass"
  key_links:
    - from: ".planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md"
      to: "todo.md lines 96-171"
      via: "every row records `Source: todo.md L{NN}` mapping back to the exact source line"
      pattern: "todo\\.md L[0-9]+"
    - from: ".planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md"
      to: ".planning/milestones/v1011-MILESTONE-AUDIT.md + .planning/milestones/v1028-MILESTONE-AUDIT.md"
      via: "every `closed-in-prior-milestone` row cites a milestone audit file or commit SHA"
      pattern: "v1011|v1027|v1028|v1029|v13\\.|[0-9a-f]{7,}"
---

<objective>
Replace the `## todo.md Staleness Pass` stub from Plan 01 with a classification table that prevents already-shipped items from being re-scheduled in v1030 phases. Pitfall #13 explicitly: each item in `todo.md` lines 96-171 must cite v1011 / v1027 / v1028 / v1029 milestone summary if closed; the rest are either live regressions (closed earlier but broke again) or genuine new gaps.

Purpose: Phases 1134-1138 planners read this section to know which `todo.md` items are noise vs. which are real work. Without this audit, Phase 1134 might re-implement BUG-01 visibility-toggle (already shipped in v1011 by commit `8c6de63`) or Phase 1136 might re-add DETAIL LEVEL (intentionally removed in v1011 INV-01).

Output: `## todo.md Staleness Pass` section in `1133-BUILDER-WALKTHROUGH-AUDIT.md` populated end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1133-audit-first-builder-walkthrough/1133-CONTEXT.md
@.planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md
@todo.md
@.planning/milestones/v1011-MILESTONE-AUDIT.md
@.planning/milestones/v1028-MILESTONE-AUDIT.md
@.planning/milestones/v1027-MILESTONE-AUDIT.md
@.planning/milestones/v1029-MILESTONE-AUDIT.md

<interfaces>
<!-- Pre-classified hints (verify each in execution, do not trust blindly) -->

Already-marked ✅ in todo.md (preserve the existing citation):
- L105: "save button orange indicator when not saved — DONE in v13.11 QUALITY-01 (dd90b64b)"
- L106: "review any warnings or errors in the map builder console — DONE in v13.11 QUALITY-03 (dd90b64b)"
- L107: "public map has zoom controls in a different location — DONE in v13.11 QUALITY-04 (dd90b64b)"

Already-marked ✅ for #101 (L117): "Fix gh #101 (tmpfs upload cap) — DONE 2026-05-08 by 260508-rr5 (commit 220a2052)" — `closed-in-prior-milestone` (v13.11 era).

L142-149 are the v1011-shape items from project memory:
- L142 "expand carrot for layer groups is too small" → v1011 UX-01 closure (24×24 caret via `-mx-1`)
- L143 "regular layer toggle does not work (Layer 1 map c868cc3a-...)" → v1011 BUG-01 closure (adapter contract fix at fill/line/circle/heatmap addLayers + defense-in-depth syncVisibility)
- L144 "Map Settings - are widgets necessary here?" → NEW question (no v1011 closure cited; treat as `genuine-new-gap` if confirmed live; OR `out-of-scope` if a v1030 polish boundary excludes it)
- L145 "rename group does not focus" → v1011 BUG-03 closure (rAF-deferred focus)
- L146 "delete layer does not work" → v1011 BUG-02 closure (handleRemove optimistic + rollback)
- L147 "right sidebar collapse overtop of zoom controls" → v1011 RESP-01 closure (NavigationControl top-left + data-builder-canvas margin)
- L148 "lat/long pill overlays map widget container" → v1011 RESP-02 closure (MapCoordReadout right-14 + showScale)
- L149 "basemap selector has 2 X close buttons" → v1011 RESP-03 closure (SheetContent showCloseButton={false} opt-out)
- L141 "DETAIL LEVEL toggle? doesnt work" → v1011 INV-01 closure (REMOVED — comment at `BasemapSublayerEditorScene.tsx:16-18`)
- L140 "instead of opacity slider on sublayers, indication of high-impact layer configs" → v1011 UX-02 closure (SublayerConfigIndicators)
- L142b "Basemap should be draggable in layer order" → v1011 UX-03 closure (basemap_position jsonb-additive + reorderBasemapAboveData)

L96-103 + L108-114 anti-features / out-of-scope (per REQUIREMENTS.md "Out of Scope"):
- L96 popup URL/media → maps to v1030 EASY-11 (already a REQ — `live-regression-or-new-gap` depending on builder state)
- L98 legend docking → significant layout change, deferred
- L99 "make basemap optional" → maps to v1030 EDITOR-BASEMAP-02
- L100 annotation layer → explicit `out-of-scope-anti-feature` per REQUIREMENTS.md
- L101 indicator on notes icon → maps to v1030 MAP-22
- L103 filter pills conflict with measure widget → maps to v1030 MAP-20
- L108 AI confirm before applying changes → maps to v1030 AI-01
- L109 AI Skills Repo → explicit `out-of-scope-anti-feature`
- L110 Connect functionality (S3/duckdb/etc) → explicit `out-of-scope-anti-feature`
- L111 enterprise deactivate → covered by v13.2 → `closed-in-prior-milestone`
- L112 demo maps → explicit `out-of-scope-anti-feature`
- L120 gh #100 worker MissingGreenlet → separate `/gsd-debug` track → `out-of-scope-anti-feature` for v1030
- L124 DEM sizing → separate quick-task track → `out-of-scope-anti-feature` for v1030

L150-171 mostly anti-features per CONTEXT.md:
- L151 "powered by geolens / legend / title in export" → maps to v1030 SHARE-07 + SHARE-09
- L154 LiDAR → `out-of-scope-anti-feature`
- L157-158 "Pending style preview / Reflects this layer before save" → genuine candidate for `live-regression` audit during Plan 01 MCP walk
- L160 "Render as Text" → `out-of-scope-anti-feature` (v2 deferred)
- L162 draw/add text to map → `out-of-scope-anti-feature`
- L163 popup URL/media → DUP of L96 → EASY-11
- L164 dcat 3.0 → v1029 closure → `closed-in-prior-milestone`
- L166 Download DEM/aerial/trail → `out-of-scope-anti-feature` (quick-task track)
- L168 Review builder-audit command → `out-of-scope-anti-feature` (tooling, not feature)
- L170 label layer → `out-of-scope-anti-feature` (defer to layer-type expansion)
- L171 AI chat for layer creation / data analysis → maps to v1030 AI-08

These are HINTS — the executor MUST verify each by reading the source artifact (milestone audit file or live code at the cited symbol) before recording the classification. Do NOT propagate stale hints if the source has shifted.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate every actionable item in todo.md lines 96-171 and classify</name>
  <files>.planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md</files>
  <read_first>
    - todo.md lines 96-171 (the staleness pass source — every actionable item needs a row)
    - .planning/milestones/v1011-MILESTONE-AUDIT.md (BUG-01..03, UX-01..04, RESP-01..03, INV-01 — confirm SHAs)
    - .planning/milestones/v1027-MILESTONE-AUDIT.md (typed action boundary closures)
    - .planning/milestones/v1028-MILESTONE-AUDIT.md (AI unavailable state + notes clear closures)
    - .planning/milestones/v1029-MILESTONE-AUDIT.md (DCAT 3.0 closure)
    - .planning/REQUIREMENTS.md "Out of Scope" table (lines 102-123) — confirms anti-feature designation
  </read_first>
  <action>
    Replace the `## todo.md Staleness Pass` stub section with the classification table.

    Table header (use exactly):
    `| Source Line | Item (verbatim or paraphrase ≤80 chars) | Classification | Citation | Owning Phase / Disposition |`

    Iterate `todo.md` lines 96-171. Skip pure whitespace lines and lines that are continuation prose (already-✅ items keep their existing SHA as Citation). For each actionable item, populate one row:

    1. **Source Line** — `L{NN}` matching the actual line number in `todo.md` as it exists today (verify by reading the file fresh; do not trust the hint block).
    2. **Item** — verbatim text from the line OR a paraphrase ≤80 chars if the line is long (mark paraphrases with a trailing `…`).
    3. **Classification** — one of:
       - `closed-in-prior-milestone` — the surface is fixed and shipped in a prior milestone; verify live via grep / file-read / commit SHA.
       - `live-regression` — the surface was previously shipped but broke again (Plan 01 MCP walk should have caught this; cross-reference its finding).
       - `genuine-new-gap` — not previously fixed; maps to a v1030 REQ ID.
       - `out-of-scope-anti-feature` — explicitly excluded per REQUIREMENTS.md "Out of Scope" or "v2 Requirements".
    4. **Citation** — REQUIRED for the first two classifications:
       - For `closed-in-prior-milestone`: cite the milestone tag (e.g. `v1011 BUG-01`) AND a commit SHA (e.g. `8c6de63`) AND the exact REQ ID from the milestone REQUIREMENTS.md (or in-line `✅` SHA if already present). Paraphrase is NOT citation — must be a specific symbol the future verifier can grep.
       - For `live-regression`: cite the Plan 01 Finding ID that captured the regression (e.g. `WALK-F-01`).
       - For `genuine-new-gap` / `out-of-scope-anti-feature`: cite the v1030 REQ ID (e.g. `EASY-11`) OR the REQUIREMENTS.md "Out of Scope" row.
    5. **Owning Phase / Disposition**:
       - `closed-in-prior-milestone` → `— (no action; preserve as historical reference)`
       - `live-regression` → `1134` / `1135` / `1136` / `1137` / `1138` (per the routing table)
       - `genuine-new-gap` → owning phase per the REQ ID
       - `out-of-scope-anti-feature` → `OOS — v1031 carry-forward` OR `OOS — out-of-scope-permanent` (the latter for items in REQUIREMENTS.md "Out of Scope" with rationale).

    Verification step (per row): for every `closed-in-prior-milestone` row, run `grep -l "{REQ ID}" .planning/milestones/` to confirm the cited milestone audit exists; if the grep returns 0, the citation is broken — reclassify the row as `live-regression` or `genuine-new-gap` and route accordingly.

    After the main table, add a `### Summary` subsection counting:
    - Total items classified: N
    - Closed in prior milestones: N
    - Live regressions: N
    - Genuine new gaps: N
    - Out-of-scope: N

    For every `live-regression` and `genuine-new-gap` row, append a corresponding row to `## Phase 1134-1138 Routing Table` (the existing table from Plan 01). Do NOT duplicate rows that Plan 01 / Plan 02 already routed — instead add a Notes column entry "todo.md L{NN}" to the existing routing row so the routing table cross-references todo.md AND the MCP finding.
  </action>
  <verify>
    <automated>grep -A 500 "^## todo.md Staleness Pass" .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md | grep -B 500 -m 1 "^## " | grep -cE "^\| L[0-9]+" | awk '{ if ($1 &gt;= 15) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - The `## todo.md Staleness Pass` section is populated; no stub text remains.
    - At least 15 rows correspond to actionable items in `todo.md` lines 96-171 (file has roughly 30+ actionable lines in that range; some will collapse into the same row when items duplicate).
    - Every row has a non-empty Classification column matching the 4 allowed values.
    - Every `closed-in-prior-milestone` row has a Citation column with both a milestone tag AND either a commit SHA OR a REQ ID. Paraphrase-only citations fail the gate.
    - Every `live-regression` and `genuine-new-gap` row has a corresponding entry in `## Phase 1134-1138 Routing Table` (either a new row OR a Notes annotation on a Plan 01 / Plan 02 row).
    - The `### Summary` subsection counts roll up to the total row count.
  </acceptance_criteria>
  <done>Staleness pass is the canonical reference: Phase 1134-1138 planners know which todo.md items are noise vs. real work, with milestone-audit-grade citations.</done>
</task>

</tasks>

<verification>
- Every actionable item in `todo.md` lines 96-171 has a row in the staleness table.
- Every `closed-in-prior-milestone` row cites a specific commit SHA or REQ ID, not a paraphrase.
- Routing table from Plan 01 has been cross-referenced (no duplicate finding rows).
</verification>

<success_criteria>
- WALK-03 satisfied: Pitfall #13 (todo.md staleness) is audited; downstream planners cannot accidentally re-implement closed work.
</success_criteria>

<output>
Create `.planning/phases/1133-audit-first-builder-walkthrough/1133-03-SUMMARY.md` when done.
</output>
