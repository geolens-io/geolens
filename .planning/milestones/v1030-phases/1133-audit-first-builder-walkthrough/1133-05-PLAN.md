---
phase: 1133-audit-first-builder-walkthrough
plan: 05
type: execute
wave: 5
depends_on:
  - 1133-01
  - 1133-02
  - 1133-03
  - 1133-04
files_modified:
  - .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - WALK-05
user_setup: []

must_haves:
  truths:
    - "The audit doc records the live thumbnail-capture sizing as found in `frontend/src/components/builder/hooks/use-builder-save.ts` (currently `thumbW = 400`, `thumbH = 250` JPEG)."
    - "The disposition explicitly states whether a 1200×630 variant exists OR can be produced — `400×250` is NOT a 1200×630 variant."
    - "If no 1200×630 variant exists: SHARE-08 is flagged to v1031 with rationale, AND a Future Requirements entry is added to `.planning/REQUIREMENTS.md` under a `## Future Requirements` section."
    - "If a 1200×630 variant CAN be produced from the existing capture pipeline (e.g., capture at native canvas size and resize on backend, or add a second `doCapture` invocation at 1200×630), the audit doc records the feasibility note + estimated effort + which downstream phase would close it."
    - "SHARE-03 (iframe preview) sandbox feasibility is also recorded here as a secondary deliverable: `SharePanel.tsx` already pins `sandbox=\"allow-scripts\"` (no `allow-same-origin` — SEC-07 / M-70 contract) — confirm the iframe-preview pane in Phase 1137 can mirror this contract without `allow-same-origin` widening."
  artifacts:
    - path: ".planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md"
      provides: "SHARE-08 Disposition section replaces the Plan 01 stub."
      contains: "## SHARE-08 Disposition"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Future Requirements entry for SHARE-08 if the 1200×630 thumbnail variant is not available (or confirmation that it IS available and SHARE-08 stays in v1031 v2 list as-is)."
      contains: "Future Requirements OR SHARE-08"
  key_links:
    - from: ".planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md"
      to: "frontend/src/components/builder/hooks/use-builder-save.ts"
      via: "disposition cites the live `thumbW`/`thumbH` constants and the file path"
      pattern: "use-builder-save\\.ts|thumbW|thumbH|400|250"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md"
      via: "if Future Requirements entry is added, it links back to the audit doc disposition"
      pattern: "1133-BUILDER-WALKTHROUGH-AUDIT|SHARE-08"
---

<objective>
Replace the `## SHARE-08 Disposition` stub from Plan 01 with a concrete go/no-go ruling for SHARE-08 (OG-cards / 1200×630 thumbnail variant for social cards on shared links). This is the WALK-05 success criterion: if no 1200×630 variant exists or can be produced cheaply, SHARE-08 is officially flagged to v1031 in `REQUIREMENTS.md` Future Requirements. The current state (verified during planning): thumbnails are 400×250 JPEG at `frontend/src/components/builder/hooks/use-builder-save.ts:33-34` (`thumbW = 400`, `thumbH = 250`). 400×250 ≠ 1200×630.

Purpose: Phase 1137 (sharing/embed polish) needs a definitive ruling on SHARE-08 BEFORE its plans are written. If SHARE-08 stays in v1030 scope (1200×630 variant feasible), Phase 1137 picks it up; if it goes to v1031, Phase 1137 omits it cleanly. No ambiguity propagates into Phase 1137's plan.

This plan ALSO addresses SHARE-03 (iframe-preview sandbox feasibility) as a secondary deliverable — Phase 1137 needs to know whether the iframe-preview pane can mirror the live SEC-07 / M-70 contract (`sandbox="allow-scripts"` only, no `allow-same-origin`) without breaking.

Output: `## SHARE-08 Disposition` section in `1133-BUILDER-WALKTHROUGH-AUDIT.md` populated with feasibility + ruling + (if no) Future Requirements entry in `REQUIREMENTS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1133-audit-first-builder-walkthrough/1133-CONTEXT.md
@.planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md
@frontend/src/components/builder/hooks/use-builder-save.ts
@frontend/src/components/builder/SharePanel.tsx
@frontend/src/components/maps/hooks/use-map-thumbnail.ts
@backend/app/modules/catalog/maps/router.py
@backend/app/modules/catalog/maps/models.py
@backend/app/modules/catalog/maps/schemas.py

<interfaces>
<!-- Known live state as of 2026-05-27 -->

Thumbnail capture pipeline (verified during planning):
- `frontend/src/components/builder/hooks/use-builder-save.ts:23-69` defines `doCapture(map, mapId, queryClient)`:
  - `thumbW = 400` (line 33)
  - `thumbH = 250` (line 34)
  - Crops + resizes source canvas to 400×250 with JPEG quality 0.7
  - Uploads via `uploadThumbnail(mapId, dataUri)` (a PUT to `/maps/{id}/thumbnail/`).
  - Quality budget noted in PERF-08 (Phase 274): no permanent `preserveDrawingBuffer`.
- `frontend/src/components/builder/hooks/use-builder-save.ts:165` defines the debounced wrapper `captureThumbnail` (500ms trailing edge per SP-16).
- `frontend/src/components/maps/hooks/use-map-thumbnail.ts` consumes the stored thumbnail; URL: `/maps/{id}/thumbnail/`.

Backend storage:
- `backend/app/modules/catalog/maps/models.py:100`: `thumbnail_uri: Mapped[str | None]` — single URI, no variants.
- `backend/app/modules/catalog/maps/router.py:1579`: `map_obj.thumbnail_uri = storage_key` — single key, no second variant.
- `router.py:1611`: media type detection by extension (`.jpg` → `image/jpeg`).

Feasibility paths for 1200×630 variant:
- Path A: Add a SECOND `doCapture` invocation at 1200×630 alongside the existing 400×250. Two PUTs, two storage keys (or one second-variant key like `thumbnail_uri_og`). Backend needs a second nullable column (`og_image_uri`) + a second route `/maps/{id}/og-image/`. Effort: ~1 day (mostly backend column + route + storage wiring).
- Path B: Capture at native canvas size (typically 1440×900 in builder) and let backend resize to both 400×250 AND 1200×630 on upload. Avoids dual capture. Effort: ~1.5 days (backend resize pipeline).
- Path C: Defer to v1031 — record SHARE-08 in Future Requirements; no v1030 work.

SHARE-03 iframe preview sandbox feasibility:
- `frontend/src/components/builder/SharePanel.tsx:63`: live embed string uses `sandbox="allow-scripts"` only. SEC-07 / M-70 contract holds; no `allow-same-origin`.
- For an iframe-preview pane inside SharePanel, the preview iframe can either point to the same live shared-viewer URL with the same sandbox, OR use `srcdoc=` with an embedded HTML snippet (also `allow-scripts` only). Both feasible without breaking SEC-07.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record SHARE-08 + SHARE-03 disposition and update REQUIREMENTS.md if SHARE-08 defers</name>
  <files>
    .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - frontend/src/components/builder/hooks/use-builder-save.ts lines 20-180 (capture pipeline + debounce + sizing constants)
    - frontend/src/components/builder/SharePanel.tsx lines 1-100 + 320-475 (rawShareToken / persistedShareTokenHint separation + embed iframe sandbox at line 63)
    - backend/app/modules/catalog/maps/router.py lines 1570-1612 (thumbnail upload + serve)
    - backend/app/modules/catalog/maps/models.py line 100 (single `thumbnail_uri` column)
    - .planning/REQUIREMENTS.md lines 79-100 (v2 Requirements section + the existing SHARE-08 entry at line 89)
  </read_first>
  <action>
    Replace the `## SHARE-08 Disposition` stub section in the audit doc.

    Section structure:

    ### Current State (verified)
    - Thumbnail capture: 400×250 JPEG (cite `use-builder-save.ts:33-34`).
    - Backend storage: single `thumbnail_uri` column; no second-variant column (cite `models.py:100`).
    - Consumer: `useMapThumbnail` blob URL via `/maps/{id}/thumbnail/` (cite `use-map-thumbnail.ts:33`).

    ### Feasibility Assessment
    Tabulate Path A / Path B / Path C with columns:
    `| Path | Description | Effort (context cost) | Backend changes? | Frontend changes? | Recommendation |`

    Path A: Dual capture (400×250 + 1200×630). Backend: new nullable column + new route. Frontend: second doCapture call.
    Path B: Native capture + backend resize. Backend: resize pipeline + dual storage. Frontend: send larger image.
    Path C: Defer to v1031.

    Pick a recommendation. The audit doc must EXPLICITLY name the recommendation as one of A / B / C.

    ### Ruling
    A single bolded sentence: `**SHARE-08 disposition: DEFER to v1031** (cite Path C)` OR `**SHARE-08 disposition: KEEP in v1030 via Path A/B** — route to Phase 1137 as REQ ID SHARE-08-NEW`.

    Default recommendation (use this unless feasibility says otherwise): **DEFER to v1031** — Path C — because (1) the v1030 milestone is polish-not-feature per CONTEXT.md, (2) backend column + route work expands scope, (3) `@vercel/og`-class libraries are on the explicit do-NOT-add list per STACK invariants in ROADMAP line 11, (4) the 400×250 thumbnail already satisfies the `useMapThumbnail` consumer (no v1030 REQ depends on OG-cards).

    If you recommend DEFER:
    - Add a Future Requirements entry to `.planning/REQUIREMENTS.md`. Place it at the bottom of the file (or in an existing `## Future Requirements` section if present; create the section if missing). Format:
      ```
      ## Future Requirements (v1031+)

      ### SHARE-08: OG-image / social-card meta on shared links

      **Deferred from:** v1030 (Phase 1133 WALK-05 disposition)
      **Why deferred:** The live thumbnail pipeline produces 400×250 JPEG only (`use-builder-save.ts:33-34`). A 1200×630 variant requires either dual capture (Path A: backend column + route) or backend resize pipeline (Path B). Both expand scope beyond the v1030 polish boundary.
      **What's required to ship:** Pick Path A or Path B in a v1031 audit; backend gets a second `og_image_uri` column + serve route; frontend either captures dual or sends larger source.
      **Out-of-scope libraries:** `@vercel/og`, `satori` (STACK do-NOT-add list).
      **Cross-reference:** `.planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md#share-08-disposition`
      ```

    If you recommend KEEP (Path A or B): do NOT modify REQUIREMENTS.md beyond the existing SHARE-08 entry; instead add a row to `## Phase 1134-1138 Routing Table` under Phase 1137 with REQ ID `SHARE-08-NEW` and effort estimate.

    ### SHARE-03 (Iframe Preview Sandbox) Feasibility
    Append a subsection covering SHARE-03 separately (this is a secondary deliverable):
    - Cite `SharePanel.tsx:63` — `sandbox="allow-scripts"` only (SEC-07 / M-70 contract).
    - Confirm a preview iframe inside SharePanel can mirror this contract without `allow-same-origin`.
    - Ruling: KEEP SHARE-03 in v1030 Phase 1137 (default) OR DEFER if iframe-preview cannot be implemented without widening the sandbox.

    If KEEP — no REQUIREMENTS.md change. If DEFER — flag SHARE-03 to v1031 with the same Future Requirements pattern.
  </action>
  <verify>
    <automated>grep -q "^## SHARE-08 Disposition" .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md &amp;&amp; grep -qE "SHARE-08 disposition:\s*(DEFER|KEEP)" .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md &amp;&amp; grep -qE "SHARE-03|iframe.*preview|SEC-07" .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `## SHARE-08 Disposition` section is populated; stub note from Plan 01 is removed.
    - The section names `400×250` and `1200×630` literally — these numbers are the canonical input/output for the disposition.
    - A single bolded "Ruling" sentence picks DEFER or KEEP with the path letter (A / B / C).
    - If DEFER: `.planning/REQUIREMENTS.md` has a new `## Future Requirements (v1031+)` section (or appends to an existing one) with the SHARE-08 entry, cross-referencing the audit doc.
    - If KEEP: `## Phase 1134-1138 Routing Table` has a row for Phase 1137 SHARE-08-NEW with effort estimate.
    - A SHARE-03 (iframe-preview) feasibility subsection exists with a KEEP / DEFER ruling.
  </acceptance_criteria>
  <done>SHARE-08 has a binding disposition; Phase 1137 planner reads it as scope input. SHARE-03 feasibility also resolved so Phase 1137 knows whether to plan the iframe-preview pane.</done>
</task>

<task type="auto">
  <name>Task 2: Final audit-doc lint pass — verify cross-references hold</name>
  <files>.planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md</files>
  <read_first>
    - .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md (the now-complete doc)
    - .planning/REQUIREMENTS.md traceability table (lines 128-173) for v1030 REQ ID validation
  </read_first>
  <action>
    Run a final consistency sweep on the audit doc:

    1. Every Finding ID referenced in the Phase 1134-1138 Routing Table also appears in its source section (render-mode table, smaller-screen table, AI matrix, todo.md staleness pass, or invariant grep checks). No orphan IDs.
    2. Every REQ ID cited in the audit doc exists in `.planning/REQUIREMENTS.md` traceability table OR is suffixed `-NEW` (newly-created during this audit) OR is the literal `(unmapped)`. Any REQ ID that fails this check is a typo — fix in place.
    3. Every `closed-in-prior-milestone` row in the todo.md staleness pass cites a milestone tag matching `v1011|v1027|v1028|v1029|v13\.1[0-9]|[0-9a-f]{7,}` (the SHA pattern catches inline `dd90b64b`-style citations from the existing ✅ items).
    4. Every routing-row's Owning Phase is in the set {1134, 1135, 1136, 1137, 1138, 1139} OR the row is marked OOS.
    5. Add a final `## Audit Sign-Off` section at the bottom with:
       - Plans 01-05 completion check (Plan 01: render-mode walk + routing table; Plan 02: AI matrix; Plan 03: todo.md staleness; Plan 04: invariant grep; Plan 05: SHARE-08 + SHARE-03).
       - Total findings count.
       - Plan 1134-1138 work-list size (count of routing-table rows per phase).
       - WALK-01..05 PASS/FAIL signoff (one line each).

    If any check FAILs and cannot be repaired in place (e.g., a Finding ID is referenced in the routing table but the source row was deleted), record the inconsistency in the Sign-Off section as a known gap and route it to Plan 1133-XX-PLAN.md replanning (this should not happen if Plans 01-04 ran cleanly).
  </action>
  <verify>
    <automated>grep -q "^## Audit Sign-Off" .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md &amp;&amp; grep -cE "WALK-0[1-5].*(PASS|FAIL)" .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md | awk '{ if ($1 &gt;= 5) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `## Audit Sign-Off` section exists at the bottom of the doc.
    - WALK-01, WALK-02, WALK-03, WALK-04, WALK-05 each have a PASS or FAIL line in the Sign-Off.
    - No orphan Finding IDs (every ID in the routing table also appears in a source section).
    - Every REQ ID cited matches the REQUIREMENTS.md traceability OR is suffixed `-NEW` OR is `(unmapped)`.
    - Counts in Sign-Off (total findings, per-phase work-list size) are non-zero where expected and roll up consistently.
  </acceptance_criteria>
  <done>Audit doc is internally consistent; WALK-01..05 all have explicit PASS/FAIL signoff; downstream phases can begin planning.</done>
</task>

</tasks>

<verification>
- `## SHARE-08 Disposition` has a binding KEEP/DEFER ruling with path letter.
- `.planning/REQUIREMENTS.md` updated only if DEFER ruling needs a Future Requirements entry.
- `## Audit Sign-Off` records WALK-01..05 PASS/FAIL.
- No orphan Finding IDs or unmapped REQ IDs (except the literal `(unmapped)` placeholder).
</verification>

<success_criteria>
- WALK-05 satisfied: SHARE-08 disposition is binding (DEFER → v1031 with rationale, OR KEEP → Phase 1137 work-list).
- Audit doc is internally consistent and Phase 1134-1138 planners can begin.
</success_criteria>

<output>
Create `.planning/phases/1133-audit-first-builder-walkthrough/1133-05-SUMMARY.md` when done.
</output>
