---
phase: 1138-easy-win-sweep
plan: 04
type: execute
wave: 2
depends_on:
  - 1138-01
  - 1138-02
  - 1138-03
files_modified:
  - .planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md
autonomous: false
mcp_driver: orchestrator
requirements: []
tags:
  - easy-win
  - mcp-smoke
  - close-gate
  - pitfall-14

must_haves:
  truths:
    - "1138-MCP-SMOKE.md exists at .planning/phases/1138-easy-win-sweep/ and contains a numbered checklist of 3 MCP smoke surfaces (one per EASY REQ ID: EASY-02 keyboard shortcut, EASY-11 popup URL/media, EASY-18 empty-state hint) plus the cross-cutting HARD-INVARIANT regression checks plus the Pitfall #14 ≤800px layout-regression sweep."
    - "Each checklist item explicitly states which mcp__playwright__* tool the orchestrator calls — NOT a delegation to gsd-executor (per feedback_playwright_mcp_orchestrator_only memory; v1030 Phases 1134/1135/1136 had executors fake MCP output)."
    - "Each checklist item has an explicit PASS / PARTIAL / FAIL disposition column AND a 'observed' column the orchestrator fills in live."
    - "HARD-INVARIANT regressions are explicit: git diff against builder-action-contract.ts returns 0 lines; Pitfall #9 grep for setPaintProperty / setLayoutProperty outside layer-adapters/ + map-sync.ts returns clean; BuilderActionSource not widened."
    - "Pitfall #14 ≤800px regression sweep: navigate to a representative map, resize to 800x600 viewport, verify NO regression in Phase 1134 MAP-07/MAP-08/MAP-10 fixes (NavigationControl stays at top-left without sidebar overlap; lat/long readout no overlap; SheetContent showCloseButton={false} pin still holds) AND Phase 1136 layout fixes (RasterEditor sliders, LineEditor cap/join Selects, FillEditor extrusion hint, BasemapEditor No-basemap preset still render correctly at 800px)."
    - "Each checklist item names a representative map ID and a starting URL (default canonical ADK map: c39be324-6815-40e5-8143-00a2723827b2 — known to have multiple visible vector layers and at least one filterable layer; popup config is editor-toggleable per layer)."
    - "Orchestrator-driven MCP — this plan does NOT spawn an executor for live MCP; the executor's job is ONLY to write the checklist artifact. The orchestrator follows the checklist directly during its own turn (per feedback_playwright_mcp_orchestrator_only)."
    - "Final hand-off block in the executor's summary message tells the orchestrator to resume MCP smoke at the orchestrator level."
  artifacts:
    - path: ".planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md"
      provides: "Live Playwright MCP smoke checklist for Phase 1138 close-gate at 800x600 viewport (Pitfall #14)"
      min_lines: 120
  key_links:
    - from: ".planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md"
      to: "orchestrator mcp__playwright__* tool calls"
      via: "checklist names tool + selector + expected behavior + observed column"
      pattern: "mcp__playwright"
---

<objective>
Produce `1138-MCP-SMOKE.md` — a checklist the **orchestrator** (NOT a gsd-executor sub-agent) follows directly using its own `mcp__playwright__*` tools. The checklist must cover every Phase 1138 success criterion via a live walkthrough on `localhost:8080` against the canonical ADK map at 800x600 viewport, with explicit PASS / PARTIAL / FAIL dispositions per surface.

Purpose: per CONTEXT.md Pre-Decided Anchors and ROADMAP SC #4 ("Live Playwright MCP at 800px viewport verifies all three easy-wins do not regress any Phase 1134/1136 layout fix"). Per `feedback_playwright_mcp_orchestrator_only` memory, the orchestrator MUST drive MCP smoke directly — executors silently fabricate "5/5 PASS" reports without driving a browser. The executor's job is to write the checklist; the orchestrator runs it.

Critical: this is NOT an executor plan. The orchestrator spawns gsd-executor for the 3 implementation plans (01-03); for Plan 04 the orchestrator calls `mcp__playwright__*` tools itself in its own turn. The executor deliverable is a markdown checklist the orchestrator executes as a script.

Output: `1138-MCP-SMOKE.md` with 3 EASY-surface sections + 1 Pitfall #14 ≤800px regression sweep + 1 HARD-INVARIANT grep section + 1 final sign-off block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

NOTE: this plan creates a MARKDOWN CHECKLIST, not code. The orchestrator follows the checklist directly using its own mcp__playwright__* tools. No executor is spawned for live browser interaction.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1138-easy-win-sweep/1138-CONTEXT.md

<reference>
Prior MCP smoke artifact for shape reference:
- .planning/phases/1137-sharing-and-embed-polish/1137-07-PLAN.md (the canonical orchestrator-driven MCP smoke plan shape — adopt the same structure)
- .planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/1135-MCP-SMOKE.md (if exists; per STATE.md v1030 Phase 1135 archive)

Canonical map for smoke: `c39be324-6815-40e5-8143-00a2723827b2` (ADK High Peaks). Known properties:
- Multiple visible vector layers (point, line, polygon)
- Pre-existing popup_config on at least one layer (useful for EASY-11 verification of existing behavior preservation)
- Filterable vector layers (curated 46er peaks layer is a strong candidate for EASY-18 — apply a filter that returns 0 features in the current viewport)

Admin credentials per project memory: `GEOLENS_ADMIN_USERNAME` / `GEOLENS_ADMIN_PASSWORD` env vars (defaults `admin`/`admin`).

Base URL: `http://localhost:8080` (Vite dev proxy; backend on `api:8000` from docker compose).
</reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the orchestrator-driven MCP smoke checklist</name>
  <files>.planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md</files>
  <read_first>
    - .planning/phases/1138-easy-win-sweep/1138-CONTEXT.md (hard invariants + ROADMAP SC bullets)
    - .planning/phases/1138-easy-win-sweep/1138-01-PLAN.md (EASY-02 contract — must_haves block)
    - .planning/phases/1138-easy-win-sweep/1138-02-PLAN.md (EASY-11 contract)
    - .planning/phases/1138-easy-win-sweep/1138-03-PLAN.md (EASY-18 contract)
    - .planning/phases/1137-sharing-and-embed-polish/1137-07-PLAN.md (canonical orchestrator-driven MCP plan shape — adopt the same section structure; if the corresponding 1137-MCP-SMOKE.md file exists at .planning/phases/1137-sharing-and-embed-polish/1137-MCP-SMOKE.md, read it as a structural reference)
  </read_first>
  <action>
    Create `.planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md`. The document is a CHECKLIST the orchestrator follows directly — every step explicitly names a `mcp__playwright__*` tool. The structure mirrors 1137-07-PLAN's checklist shape.

    HEADER SECTION:
    - Title: `# Phase 1138 — Live MCP Smoke Checklist (orchestrator-driven)`
    - Operator note: "This is NOT an executor task. The orchestrator calls mcp__playwright__* tools directly per item below. Do NOT spawn gsd-executor for live browser steps — that violates feedback_playwright_mcp_orchestrator_only memory. v1030 Phases 1134/1135/1136 had executors fabricate PASS/FAIL reports without a real browser; that pattern is forbidden here."
    - Pre-flight: 1 paragraph confirming `docker compose ps` shows healthy api / worker / frontend; admin credentials available (`GEOLENS_ADMIN_USERNAME` / `GEOLENS_ADMIN_PASSWORD`); browser viewport will be set to **800x600** for ALL sections (Pitfall #14: "even trivial CSS-only changes must be MCP-verified at 800px").
    - Canonical map: `c39be324-6815-40e5-8143-00a2723827b2` (ADK High Peaks).
    - Base URL: `http://localhost:8080`.

    SECTION 1: EASY-02 — Cmd/Ctrl+S keyboard shortcut
    A numbered checklist:
    1. `mcp__playwright__browser_resize` → 800x600 (set viewport for all subsequent steps).
    2. `mcp__playwright__browser_navigate` → `http://localhost:8080/maps/c39be324-.../builder`.
    3. Authenticate via the login flow IF the page redirects to `/login`. Use `mcp__playwright__browser_evaluate` to read `window.location.pathname` first; if `/login`, fill the form with admin creds via `mcp__playwright__browser_type` and click submit via `mcp__playwright__browser_click`. Otherwise skip auth.
    4. Wait for the map canvas to render (`mcp__playwright__browser_snapshot` and confirm a `canvas` accessibility-tree entry exists OR `mcp__playwright__browser_evaluate` with `document.querySelector('canvas.maplibregl-canvas') !== null`).
    5. Trigger Cmd+S via `mcp__playwright__browser_press_key` with `Meta+s` (on macOS) OR `Control+s` (on Linux runners). Expected: toast 'toasts.mapSaved' appears within ~1s.
    6. `mcp__playwright__browser_snapshot` to confirm the toast region (`role="status"` or `role="alert"`) contains "Map saved" or the de/es/fr equivalent (per active locale).
    7. NEGATIVE CASE A — Open a modal: click the Share button via `mcp__playwright__browser_click`; verify a Radix dialog opens (`document.querySelector('[role="dialog"][data-state="open"]')` is non-null via `mcp__playwright__browser_evaluate`).
    8. Trigger Cmd+S while the dialog is open. Expected: NO new toast (the existing handleSave is a no-op due to the new dialog-open gate). Verify by counting toasts before/after via `mcp__playwright__browser_evaluate` against `document.querySelectorAll('[data-sonner-toast]').length` OR by polling for ~1s and confirming no new "Map saved" appears.
    9. Close the Share dialog via `mcp__playwright__browser_press_key` Escape OR by clicking the X. Verify Cmd+S now works again.
    10. NEGATIVE CASE B — verify preventDefault: dispatch Cmd+S; the browser's "Save Page As..." dialog must NOT appear (impossible to assert directly via MCP; instead, assert via `mcp__playwright__browser_evaluate` that the page is still at the builder URL, no `beforeunload` fired, no native dialog visible — best-effort).
    Disposition column: PASS / PARTIAL / FAIL with observed.

    SECTION 2: EASY-11 — Popup URL / media handling + token doc
    1. With the builder still open on the canonical ADK map at 800x600, expand a layer that has popup_config enabled (e.g., the curated 46er peaks layer if it has a popup; otherwise pick any vector layer and ENABLE its popup via the Popup tab + Save).
    2. To verify the {column} token doc: open the layer's Popup tab via `mcp__playwright__browser_click` on the Popup tab; verify the expression help text contains "Use {column_name} placeholders. Example: {city}, {state}" (i18n key `popup.expressionHelp` — existing). Verify the NEW media hint paragraph "URLs and images in field values render as links and previews automatically." appears above the Visible Fields heading.
    3. To verify the live render: configure a layer property to contain a URL (one of the existing properties on the 46er peaks layer is `wiki_url` or similar — verify by `mcp__playwright__browser_evaluate` reading `layer.dataset_column_info`). If no existing URL column exists, pick a layer that DOES have one, OR document this as "live data lacks URL column — verified via vitest at 1138-02 instead."
    4. Click a feature on the map to open the popup. Expected: the URL-valued property renders as a clickable anchor (existing behavior — regression check) AND inline text with embedded URLs (if any) auto-linkifies.
    5. For media: if any layer has an image-URL column (.jpg/.png), verify the popup renders an `<img loading="lazy">`. If the canonical map has no media column, document as "media render verified via vitest at 1138-02; live data lacks media column on canonical ADK map."
    6. XSS regression: NEGATIVE CASE — use `mcp__playwright__browser_evaluate` to introspect that no `<a href="javascript:...">` anchor appears in the popup DOM for any property value (defense-in-depth gate). Confirm by querying `document.querySelectorAll('a[href^="javascript:"]').length` === 0 inside the popup.
    Disposition column.

    SECTION 3: EASY-18 — Empty-layer state + Clear filter
    1. With builder still on the canonical ADK map at 800x600, pick a vector layer with filter capability (e.g., the curated 46er peaks layer).
    2. Open the Filter tab via `mcp__playwright__browser_click` on the Filter tab.
    3. Add a filter condition that is guaranteed to return 0 features (e.g., `name == 'ZZZZ_DOES_NOT_EXIST_ZZZZ'`) via the conditions UI. Use a combination of Select clicks + Input typing via mcp tools.
    4. Wait for the map idle event (`mcp__playwright__browser_wait_for` with a short selector poll, OR `mcp__playwright__browser_evaluate` against `document.querySelector('[role="status"]')` to detect the hint appearing).
    5. EXPECTED: empty-state hint with title "0 features — check your filter" (or de/es/fr equivalent) is visible inside the Filter tab body, with a Clear filter button.
    6. Click the Clear filter button via `mcp__playwright__browser_click`. EXPECTED: the filter is cleared (the conditions list returns to empty) AND the hint disappears AND ActiveFilterChips (above the map) updates accordingly (no chip for this layer anymore).
    7. NEGATIVE CASE — set a filter that returns N > 0 features (e.g., `elevation >= 0`). Expected: hint is NOT visible (count > 0).
    Disposition column.

    SECTION 4: Pitfall #14 — ≤800px layout regression sweep
    This section verifies that Plans 01/02/03 did NOT regress any Phase 1134 or Phase 1136 layout fix. ALL steps run at 800x600 viewport.
    1. NavigationControl placement (MAP-07): `mcp__playwright__browser_evaluate` to read the bounding rect of `.maplibregl-ctrl-top-left`. Expected: top offset around 32px (the data-builder-canvas top-left margin), left near 0. Verify it does NOT overlap any right-sidebar Sheet content (if sidebar is open, the Sheet should be right-positioned and the NavigationControl stays at top-left). Verify the sidebar `<aside>` does NOT visually overlap the NavigationControl's bounding box.
    2. Lat/long readout (MAP-08): hover the map (`mcp__playwright__browser_evaluate` to trigger a mousemove on the canvas) → MapCoordReadout pill renders bottom-right of the canvas. Expected: no overlap with widget container chrome. Verify via `getBoundingClientRect` that the pill's right edge is at least 14px (`right-14` Tailwind class) from the canvas right edge.
    3. Sheet showCloseButton={false} (MAP-09/MAP-10): open the Add Data sheet via the empty-stack CTA or the Add (+) button. Expected: exactly ONE close (X) button in the sheet header (the panel's own X — not the shadcn Sheet's built-in auto-close X). Verify by counting `[aria-label*="lose"]` buttons inside the Sheet container.
    4. RasterEditor (1136-01): if any raster layer exists on the canonical map (DEM / NY ortho overlay), open its editor; verify brightness/contrast/saturation/hue sliders + Reset button are visible AND not clipped at 800px width.
    5. LineEditor cap/join (1136-02): pick a line layer; open its editor; verify line-cap and line-join Selects are visible and clickable.
    6. FillEditor 3D extrusion hint (1136-03): if any fill layer has `paint._height_column` set, open its editor and verify the "Range: X–Y, N features" hint is visible (regression — existing).
    7. BasemapEditor No-basemap preset (1136-04): open the basemap group editor; verify the "No basemap" preset card is still visible and clickable. Switching to it produces a transparent / single-color background.
    8. ActiveFilterChips overflow (MAP-20): apply a filter on a layer to spawn a chip; verify the chip column is `max-h-[40vh]` (no growth into MeasurementWidget chrome).
    Disposition column for each sub-step.

    SECTION 5: HARD-INVARIANT regression sweep (cross-cutting, Bash not MCP)
    Each step is a quick grep / git diff check the orchestrator runs via its own `Bash` tool:
    1. `git diff -- frontend/src/components/builder/builder-action-contract.ts` → expected: empty output (BuilderLayerAction / BuilderActionSource UNCHANGED — HARD INVARIANT)
    2. `grep -nE "map\.setPaintProperty|map\.setLayoutProperty|map\.setFilter" frontend/src/components/builder/hooks/use-filtered-feature-count.ts frontend/src/components/builder/hooks/use-builder-save.ts frontend/src/components/builder/LayerFilterEditor.tsx frontend/src/components/map/FeaturePopup.tsx frontend/src/lib/popup-rich-text.ts frontend/src/components/builder/PopupConfigEditor.tsx frontend/src/components/builder/LayerEditorPanel.tsx | wc -l` → expected: 0 (Pitfall #9 clean across all 1138 modified files)
    3. `grep -c "EASY-02" frontend/src/components/builder/hooks/__tests__/use-builder-save.test.ts` → expected: >= 5
    4. `grep -c "EASY-11" frontend/src/lib/__tests__/popup-rich-text.test.ts frontend/src/components/map/__tests__/FeaturePopup.test.tsx frontend/src/components/builder/__tests__/PopupConfigEditor.test.tsx` → expected: >= 24
    5. `grep -c "EASY-18" frontend/src/components/builder/hooks/__tests__/use-filtered-feature-count.test.ts frontend/src/components/builder/__tests__/LayerFilterEditor.test.ts frontend/src/components/builder/__tests__/LayerEditorPanel.test.tsx` → expected: >= 14
    6. `grep -c "dangerouslySetInnerHTML" frontend/src/lib/popup-rich-text.ts frontend/src/components/map/FeaturePopup.tsx` → expected: 0 (XSS defense-in-depth)
    7. `cd frontend && npm run i18n 2>&1 | tail -3` → expected: parity script exits 0
    8. `cd frontend && npm run typecheck 2>&1 | tail -3` → expected: exits 0
    9. `cd frontend && npm test -- --run 2>&1 | tail -5` → expected: full vitest suite green (no regressions)
    Disposition column.

    SECTION 6: Final sign-off block
    A markdown table:
    | Surface | REQ ID | Disposition | Observed | Date |
    | EASY-02 keyboard shortcut + preventDefault + dialog-open no-op | EASY-02 | | | |
    | EASY-11 popup URL auto-linkify + media render + token doc | EASY-11 | | | |
    | EASY-18 empty-state hint + Clear filter dispatcher | EASY-18 | | | |
    | Pitfall #14 ≤800px layout regression sweep | (Pitfall) | | | |
    | HARD-INVARIANT grep sweep | (Invariant) | | | |

    Final block: "Sign-off: orchestrator records overall PASS / PARTIAL / FAIL here on completion. If any FAIL, surface to operator with a proposed 1138.1 carry-forward plan or 1138-FU-XX hygiene close shape."

    Reference materials section: links to Plan 01-03 SUMMARYs (which will exist by the time this checklist runs), Phase 1133 audit doc, and the feedback_playwright_mcp_orchestrator_only memory.

    The checklist MUST be self-contained — the orchestrator should be able to execute it without re-reading the implementation plans. Each MCP step names the EXACT tool (mcp__playwright__browser_navigate, mcp__playwright__browser_click, mcp__playwright__browser_evaluate, mcp__playwright__browser_take_screenshot, mcp__playwright__browser_console_messages, mcp__playwright__browser_resize, mcp__playwright__browser_press_key, mcp__playwright__browser_type, mcp__playwright__browser_snapshot, mcp__playwright__browser_wait_for) so the call sites are obvious.
  </action>
  <verify>
    <automated>test -f .planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md && wc -l .planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md | awk '{ print ($1 > 120 ? "OK" : "TOO SHORT: " $1 " lines") }'</automated>
    <automated>grep -c "mcp__playwright" .planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md</automated>
    <automated>grep -cE "^Section [0-9]|^## Section [0-9]" .planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md</automated>
    <automated>grep -c "PASS / PARTIAL / FAIL\|Disposition" .planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md</automated>
    <automated>grep -c "800x600\|800 x 600\|Pitfall #14" .planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md</automated>
    <automated>grep -c "EASY-02\|EASY-11\|EASY-18" .planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md</automated>
  </verify>
  <done>
    `.planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md` exists with at least 120 lines, references mcp__playwright tools at least 25 times (multiple per section), has 6 sections, references all 3 EASY REQ IDs (>= 3 occurrences combined), references 800x600 viewport / Pitfall #14, and includes Disposition column references.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Hand off MCP smoke checklist to orchestrator (DO NOT delegate to gsd-executor)</name>
  <files>.planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md</files>
  <action>
    STOP at this task. Do NOT execute the MCP smoke from the gsd-executor context — the executor sub-agent does not have `mcp__playwright__*` tools and would fail the smoke silently or fabricate PASS/FAIL output. Instead, emit the following hand-off block in the executor's summary message and exit cleanly:

    "Phase 1138 implementation complete (Plans 01-03 shipped). Checklist artifact `1138-MCP-SMOKE.md` is now ready. Per feedback_playwright_mcp_orchestrator_only memory (v1030 Phases 1134/1135/1136 fabricated MCP output via executors — that is forbidden here), the orchestrator drives MCP smoke directly. Resume to /gsd:execute-phase orchestrator and request live MCP execution against the checklist at 800x600 viewport. Do not spawn another executor for the smoke."

    Then write `.planning/phases/1138-easy-win-sweep/1138-04-SUMMARY.md` recording the artifact path and the hand-off contract, and exit. The orchestrator resumes with the checklist directly.
  </action>
  <what-built>
    Phase 1138 implementation complete (Plans 01-03 shipped):
    - EASY-02: Cmd/Ctrl+S keyboard shortcut with preventDefault + dialog-open no-op gate, regression-pinned at use-builder-save.test.ts
    - EASY-11: popup-rich-text.ts pure helper + FeaturePopup ValueDisplay extension (auto-linkify text + media render + YouTube iframe sandbox) + PopupConfigEditor media hint + 4-locale i18n
    - EASY-18: useFilteredFeatureCount hook (read-only queryRenderedFeatures + idle-debounce + null-safety) + LayerFilterEditor empty-state hint + Clear-filter button (routes through existing onFilterChange(null) → dispatchLayerAction set_filter pathway) + 4-locale i18n
    - Zero changes to BuilderLayerAction / BuilderActionSource (HARD INVARIANT)
    - Zero new map.setPaintProperty / map.setLayoutProperty / map.setFilter calls (Pitfall #9 clean)

    Live MCP smoke checklist `.planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md` is now ready for orchestrator-driven execution at 800x600 viewport (Pitfall #14).
  </what-built>
  <how-to-verify>
    **ORCHESTRATOR DRIVES THIS CHECKPOINT — NOT THE EXECUTOR.**

    Per feedback_playwright_mcp_orchestrator_only memory:
    - The orchestrator has access to `mcp__playwright__*` tools.
    - Executors (gsd-executor sub-agents) do NOT have those tools.
    - This checkpoint is a HAND-OFF to the orchestrator. The executor running Plan 04 has already produced the checklist artifact `1138-MCP-SMOKE.md`. The orchestrator now follows the checklist on its own turn.

    Orchestrator actions on resume:
    1. Open `.planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md`.
    2. Pre-flight: confirm `docker compose ps` shows healthy services. If not, surface the issue to operator.
    3. Resize browser to 800x600 via `mcp__playwright__browser_resize` (Pitfall #14 viewport).
    4. For each Section (1-4) in the checklist, call the named `mcp__playwright__*` tools in sequence, record disposition + observed values.
    5. Run the Section 5 HARD-INVARIANT grep checks via `Bash`.
    6. Fill in the Section 6 sign-off table.
    7. Surface aggregate result to operator: "X/N PASS, Y PARTIAL, Z FAIL" and propose either Phase 1138 close or a 1138.1 carry-forward plan / 1138-FU-XX inline close.

    If the orchestrator does NOT have `mcp__playwright__*` tools available (rare: orchestrator running without the playwright MCP), the orchestrator instead surfaces a message to the operator: "Live MCP smoke must run on a workstation with the playwright MCP available. Checklist is at .planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md."

    NEVER delegate the MCP execution to a gsd-executor spawn — the executor sub-agent lacks the mcp__playwright tools and cannot run the smoke. This violates feedback_playwright_mcp_orchestrator_only.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md</automated>
    <automated>test -f .planning/phases/1138-easy-win-sweep/1138-04-SUMMARY.md</automated>
  </verify>
  <done>
    Checklist artifact exists at `.planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md`. Executor summary explicitly hands off MCP execution to the orchestrator. SUMMARY.md is written. Executor exits cleanly without attempting browser interaction.
  </done>
  <resume-signal>
    Orchestrator records the aggregate disposition in the operator-facing summary. Type "smoke-passed" to close Phase 1138 OR describe issues to file a 1138.1 / 1138-FU-XX carry-forward plan.
  </resume-signal>
</task>

</tasks>

<verification>
- `test -f .planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md` exits 0
- `wc -l .planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md` >= 120 lines
- `grep -c "mcp__playwright" .planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md` >= 25
- `grep -cE "^## Section [0-9]|^Section [0-9]" .planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md` >= 6
- `grep -c "PASS / PARTIAL / FAIL\|Disposition" .planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md` >= 5
- `grep -c "800x600\|Pitfall #14" .planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md` >= 4
- `grep -c "EASY-02\|EASY-11\|EASY-18" .planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md` >= 6 (each REQ at least twice — header + sign-off table)
- The checkpoint task explicitly hands off to the orchestrator and does NOT delegate live browser steps to a sub-agent.
</verification>

<success_criteria>
- `.planning/phases/1138-easy-win-sweep/1138-MCP-SMOKE.md` exists with at least 120 lines.
- Checklist contains 4 surface/regression sections (EASY-02 / EASY-11 / EASY-18 / Pitfall #14 ≤800px sweep) + 1 HARD-INVARIANT grep section + 1 final sign-off table.
- Every MCP step explicitly names the `mcp__playwright__*` tool (browser_resize, browser_navigate, browser_click, browser_evaluate, browser_take_screenshot, browser_console_messages, browser_press_key, browser_type, browser_snapshot, browser_wait_for).
- Checklist explicitly states "orchestrator-driven, NOT executor" at the top.
- The checkpoint task hands off to the orchestrator without delegating live browser steps to a sub-agent.
- All 3 EASY REQ IDs + Pitfall #14 + HARD-INVARIANT grep are covered in the sign-off table.
- Canonical ADK map ID `c39be324-6815-40e5-8143-00a2723827b2` referenced as the smoke target.
</success_criteria>

<output>
Create `.planning/phases/1138-easy-win-sweep/1138-04-SUMMARY.md` when the checklist file is written (before the human-verify checkpoint resumes). Include:
- Path to checklist artifact
- Sections produced (6 in total)
- Hand-off note: "Orchestrator follows 1138-MCP-SMOKE.md on resume; gsd-executor sub-agents do NOT have mcp__playwright tools and MUST NOT be delegated this work. Pitfall #14 applies — 800x600 viewport is the canonical close-gate viewport for Phase 1138 (the broader 3-viewport sweep lives at Phase 1139)."

The orchestrator updates the SUMMARY with the smoke-result aggregate AFTER it runs the checklist.
</output>
