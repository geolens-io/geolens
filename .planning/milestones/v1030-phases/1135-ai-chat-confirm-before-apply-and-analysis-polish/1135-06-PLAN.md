---
phase: 1135-ai-chat-confirm-before-apply-and-analysis-polish
plan: 06
type: execute
wave: 5
depends_on:
  - 1135-01
  - 1135-02
  - 1135-03
  - 1135-04
  - 1135-05
files_modified:
  - .planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/1135-MCP-SMOKE.md
autonomous: false
requirements:
  - AI-01
  - AI-02
  - AI-03
  - AI-05
  - AI-08
  - AI-09
tags:
  - smoke
  - playwright-mcp

must_haves:
  truths:
    - "Live Playwright MCP run produces a 1135-MCP-SMOKE.md report at .planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/ recording PASS/FAIL per AI-01..05/08/09 surface"
    - "AI_ENABLED=true smoke covers all 5 UI-SPEC surfaces against http://localhost:8080 on canonical map c39be324-6815-40e5-8143-00a2723827b2: action chip tray (Surface 1) + inline data card (Surface 2) + viewport-aware chips (Surface 5) + error banner if naturally reproducible (Surface 4)"
    - "AI_ENABLED=false smoke covers the disabled-state rail (Surface 3) on the same canonical map after restarting the api container with AI_ENABLED=false env override — confirms env_disabled reason renders 'AI is disabled' title + 'Go to Settings' CTA (admin user)"
    - "No browser-console error events fire during any of the smoke surfaces (per existing Playwright MCP `console_messages` capture pattern from Phase 1133)"
    - "Final smoke report carries a CHECKPOINT for the human reviewer with disposition (CLEAR-TO-MERGE / FOLLOW-UPS) and a numbered list of any deferred findings"
  artifacts:
    - path: ".planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/1135-MCP-SMOKE.md"
      provides: "Live MCP smoke report covering 5 surfaces in AI_ENABLED=true mode + the disabled-state in AI_ENABLED=false mode; PASS/FAIL per surface + console-error capture + checkpoint disposition"
  key_links:
    - from: "Playwright MCP browser_navigate"
      to: "http://localhost:8080/builder/c39be324-6815-40e5-8143-00a2723827b2"
      via: "live stack smoke (admin/admin)"
      pattern: "c39be324-6815-40e5-8143-00a2723827b2"
    - from: "AI_ENABLED=false restart"
      to: "BuilderRail AIDisabledState env_disabled branch"
      via: "stack restart with env override to reproduce ROADMAP AI-02 disabled-state path"
      pattern: "AI_ENABLED=false"
---

<objective>
Run a live Playwright MCP smoke against `http://localhost:8080` on canonical map `c39be324-6815-40e5-8143-00a2723827b2` covering every Phase 1135 ROADMAP AI requirement: AI-01 (staging tray accept/reject byte-equal), AI-02 (disabled-state with AI_ENABLED=false), AI-03 (error banner if naturally reproducible), AI-05 (viewport-aware chips), AI-08 (inline data card), AI-09 (chip tray rendering).

Purpose: Bridge the unit/vitest evidence (Plans 01-05) with live-canvas evidence the Phase 1133 audit ground-truth pattern requires. Closes the v1030 hard invariant: "live MCP smoke is a justified pre-tag gate for builder milestones" (per v1011 lesson).

Output:
- `1135-MCP-SMOKE.md` — a structured smoke report with one section per surface, screenshot/quote evidence, and a final checkpoint.
- A blocking `checkpoint:human-verify` for the operator after the report is filed (the operator confirms CLEAR-TO-MERGE or files follow-ups).

**Dependency:** All Plans 01-05 must be merged before this plan runs — the smoke verifies the integrated end-to-end behavior, not individual unit pieces.

**Out of scope:** No new tests are added in this plan. No additional UI changes. This is a pure verification plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/1135-CONTEXT.md
@.planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/1135-UI-SPEC.md
@.planning/phases/1135-01-SUMMARY.md
@.planning/phases/1135-02-SUMMARY.md
@.planning/phases/1135-03-SUMMARY.md
@.planning/phases/1135-04-SUMMARY.md
@.planning/phases/1135-05-SUMMARY.md
@.planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md
@.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-06-PLAN.md

<interfaces>
<!-- Smoke harness contracts. -->

**MCP tools available (per planning_context):** `mcp__playwright__browser_navigate`, `mcp__playwright__browser_resize`, `mcp__playwright__browser_snapshot`, `mcp__playwright__browser_click`, `mcp__playwright__browser_type`, `mcp__playwright__browser_evaluate`, `mcp__playwright__browser_console_messages`, `mcp__playwright__browser_take_screenshot`.

**Canonical map:** `c39be324-6815-40e5-8143-00a2723827b2` (ADK High Peaks). Login as `admin/admin`. Builder URL: `http://localhost:8080/builder/c39be324-6815-40e5-8143-00a2723827b2`.

**Phase 1133 smoke precedent:** The Phase 1133 audit captured findings per surface in a single markdown table. This plan mirrors that pattern but the table is PASS/FAIL per AI requirement.

**AI_ENABLED toggle:** From `backend/app/core/config.py` (or similar) — the `AI_ENABLED` env var is read at FastAPI app startup. To flip it from `true` (default) to `false`:
1. `docker compose down api` (or `docker compose restart api` after env edit).
2. Edit `.env` to set `AI_ENABLED=false`.
3. `docker compose up -d api`.
4. Wait for `/api/healthz` to return 200.
5. Reload the builder page; confirm `useAIStatus` returns `data.enabled === false`.
6. After the smoke completes, restore `.env` (`AI_ENABLED=true`) and re-restart.

**Live stack assumption:** The reviewer assumes the stack is already running (`docker compose ps` shows api/worker/frontend healthy). If not, the operator brings it up first per MEMORY.md instructions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AI_ENABLED=true smoke — Surfaces 1, 2, 5 + happy-path checks</name>
  <files>.planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/1135-MCP-SMOKE.md</files>
  <read_first>
    - .planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/1135-UI-SPEC.md (all 5 surfaces — drives the smoke checks)
    - .planning/phases/1135-02-SUMMARY.md, 1135-03-SUMMARY.md, 1135-04-SUMMARY.md (post-Plan integration recap so the smoke runner knows what to expect)
    - .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md (smoke-report shape precedent)
  </read_first>
  <behavior>
    - Open `http://localhost:8080`. Confirm logged in as admin (or log in).
    - Navigate to `/builder/c39be324-6815-40e5-8143-00a2723827b2`.
    - Open the AI rail panel (click the Sparkles icon).
    - Verify the rail opens and a ChatPanel is present (not the disabled-state — `data.enabled === true`).
    - For each surface, capture: a Playwright snapshot/text quote, the console-messages output, and a PASS/FAIL annotation.
    - Stitch the per-surface results into `.planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/1135-MCP-SMOKE.md`.
  </behavior>
  <action>1. **Setup.** Confirm the live stack is up: `docker compose ps` shows `api`, `worker`, `frontend` healthy. If not, request the operator bring the stack up before continuing.

2. **Navigate + auth.** Use `browser_navigate('http://localhost:8080/')`. If the login form appears, type `admin` into the username field, `admin` into the password field, submit. Then navigate to `http://localhost:8080/builder/c39be324-6815-40e5-8143-00a2723827b2`.

3. **Set viewport to 1440×900** via `browser_resize(1440, 900)`. (The smoke checks are primarily desktop-viewport; the AI rail does not change shape at smaller viewports per UI-SPEC.)

4. **Open the AI rail panel.** Use `browser_snapshot` to locate the Sparkles icon button in the right-side rail (`aria-label` includes "Ask AI" when AI is available). `browser_click` it.

5. **Surface 1 — Action Preview Chip Tray (AI-01 / AI-09).**
   - Click into the chat input. Type a prompt that requests a destructive action, e.g. `"Add the NY counties dataset to this map"` (use a dataset name that exists in the catalog and will succeed). Press Enter.
   - Wait for the AI streaming response to complete. The model should return an `add_layer` action.
   - **Capture:** browser_snapshot — confirm the staging tray renders with:
     - Header text matching `pending change` (singular) or `pending changes` (plural).
     - At least one row chip with `Add "..."` text.
     - "Accept all" + "Reject all" buttons visible.
     - Per-chip "Accept" + "Reject" buttons.
   - **Test reject path:** Click "Reject all". Verify the tray disappears AND the map's layer count is unchanged (no new layer added). Use `browser_evaluate(() => document.querySelectorAll('[data-testid="layer-row"]').length)` (or equivalent — adjust to the actual layer-stack DOM selector) BEFORE and AFTER to verify byte-equal layer state.
   - **Test accept path:** Send the same prompt again; this time click "Accept all". Verify the tray disappears AND a new layer appears in the layer stack.
   - **Test accept-one path:** Send a prompt that returns 2 destructive actions if possible (e.g., `"Add NY counties and remove the urban areas layer"`). Click "Accept" on one chip. Verify one action commits, one chip remains.
   - Record PASS/FAIL in the smoke report per sub-check.
   - Capture `browser_console_messages` and confirm zero `error` events.

6. **Surface 2 — Inline Data-Analysis Card (AI-08).**
   - In the same builder session, type a prompt that triggers a data-analysis query, e.g. `"List the 5 largest counties by area"` or `"How many features are in the Counties layer?"`. Press Enter.
   - The AI returns a `show_query_result` action with `rows` populated.
   - **Capture:** browser_snapshot — confirm:
     - The assistant bubble contains a table region with `role="region"` and aria-label "Query result table".
     - Column headers visible.
     - At least 1 data row visible.
     - The row-count footer reads `"{N} rows"` or similar.
   - **Test 5-column cap:** if the query returns >5 columns, confirm the `…` indicator appears in the header.
   - **Test empty state:** type a prompt that should return zero rows (e.g., a filter that matches nothing). Confirm the empty-state copy `"The AI query returned no rows."` + secondary `"Try a broader area or different filter."` renders.
   - Record PASS/FAIL per sub-check.

7. **Surface 5 — Viewport-Aware Suggestion Chips (AI-05).**
   - Clear the chat history (close + reopen the AI rail, OR navigate to a fresh map session).
   - On the empty-state suggestion chip row, capture the initial chip list at default zoom.
   - Zoom in via map controls or programmatic `browser_evaluate(() => window.map.setZoom(14))` (if accessible) until zoom >= 12. Wait 1s for the 500ms debounce + idle event.
   - Click a layer row in the layer stack to mark it selected (this fires the `selectedLayerName` update).
   - Close + reopen the AI rail (the chip list re-renders on empty-state mount). Capture the new chip list.
   - **Expected:** The chip list should now include a "Summarize @[<layer name>] attributes" leading suggestion AND a "Show nearby features in this area" suggestion.
   - If the chips DO NOT change, this is a FAIL — file as finding SF-MCP-01 in the report.
   - Record PASS/FAIL.

8. **Surface 4 — Recoverable Error Banner (AI-03) — opportunistic.**
   - The 503 / 403 banner is hard to reproduce on a healthy stack. **Skip if not naturally reproducible.** Document the skip with rationale in the report.
   - **OPTIONAL reproduction path:** If the operator wants to force a 503 — temporarily set `AI_PROVIDER_API_KEY=invalid` in `.env`, restart api, send a chat message. The first response should fire the 503 banner. Restore the env after.
   - This is a "best effort" sub-check; record SKIPPED or PASS depending on whether the operator chose to attempt it.

9. **Compile the report.** Write `1135-MCP-SMOKE.md` with this structure:

```markdown
# Phase 1135 — Live Playwright MCP Smoke

**Date:** YYYY-MM-DD
**Stack URL:** http://localhost:8080
**Canonical map:** c39be324-6815-40e5-8143-00a2723827b2
**Viewport:** 1440×900
**MCP driver:** /gsd-autonomous (or operator-driven)
**Auth:** admin / admin

## AI_ENABLED=true Pass

| REQ | Surface | Sub-check | PASS / FAIL | Evidence |
|-----|---------|-----------|-------------|----------|
| AI-01 / AI-09 | Staging tray render | Tray appears below assistant bubble after add_layer action | PASS | `snapshot.txt` quote ... |
| AI-01 | Reject-all byte-equal | Layer count BEFORE = AFTER reject; map unchanged | PASS | ... |
| AI-01 | Accept-all dispatches | New layer appears in stack after Accept | PASS | ... |
| AI-01 | Accept-one partial | One chip commits, one remains | PASS / SKIPPED-if-single-action-response | ... |
| AI-09 | Chip text format | Chip reads `Add "..."` or `Remove "..." (N features)` | PASS | ... |
| AI-08 | Inline card render | Table region with rows + row-count footer | PASS | ... |
| AI-08 | 5-column cap | `…` indicator visible when >5 columns | PASS / SKIPPED-if-≤5-columns | ... |
| AI-08 | Empty-rows state | Copy "The AI query returned no rows." renders | PASS | ... |
| AI-05 | Viewport-aware chips | Chip list contains layer-summarize + nearby-features after zoom-in + select | PASS / FAIL | ... |
| AI-03 | Error banner | 503 reproduction → sticky banner + Retry button | SKIPPED — not naturally reproducible without env override | ... |

## Console-Error Capture (AI_ENABLED=true)

Number of `error`-level browser-console events captured during the run: **N**
(Acceptance: N === 0 except for any known-good warnings; any error event is a FAIL row above.)
```

The report is created in the same MCP session — use `browser_evaluate` to capture DOM state and assemble the report content as you go. Write the final file via the regular Write tool at the end.</action>
  <acceptance_criteria>
    - `.planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/1135-MCP-SMOKE.md` exists with the `AI_ENABLED=true Pass` table.
    - At least 8 sub-checks recorded (the 8 named rows; the 9th AI-03 banner row may be SKIPPED with rationale).
    - Console-error capture row has a count + a PASS/FAIL based on the count.
    - Every PASS row has at least one evidence quote OR screenshot reference; FAIL rows have a "finding ID + reproducer" entry.
    - If any FAIL is recorded, the report contains a `## Findings (carry-forward)` section listing each FAIL with disposition.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/1135-MCP-SMOKE.md &amp;&amp; grep -c "AI-0" .planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/1135-MCP-SMOKE.md</automated>
  </verify>
  <done>1135-MCP-SMOKE.md contains the AI_ENABLED=true smoke results table covering 8+ sub-checks across AI-01/AI-05/AI-08/AI-09 plus console-error capture; FAILs (if any) routed to a Findings section.</done>
</task>

<task type="auto">
  <name>Task 2: AI_ENABLED=false smoke — Surface 3 disabled-state taxonomy</name>
  <files>.planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/1135-MCP-SMOKE.md</files>
  <read_first>
    - .planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/1135-UI-SPEC.md (lines 212-272 — Surface 3 spec)
    - .planning/phases/1135-03-SUMMARY.md (BuilderRail AIDisabledState integration recap)
    - .env.example or .env (locate the `AI_ENABLED` variable — confirm the exact name)
  </read_first>
  <behavior>
    - Stop the api container, edit `.env` to set `AI_ENABLED=false`, restart api.
    - Wait for `/api/healthz` to return 200 AND `/api/admin/ai-status/` to return `{ enabled: false, ... }`.
    - Reload the builder page.
    - Open the AI rail.
    - Verify the disabled-state renders with `env_disabled` taxonomy: BotOff icon, title "AI is disabled", body "An administrator has disabled AI for this instance.", "Go to Settings" CTA (admin user).
    - Capture browser_console_messages — confirm zero `/ai/*` 401/403/503 errors fire on page load.
    - Restore `.env` (`AI_ENABLED=true`), restart api, verify normal state returns.
  </behavior>
  <action>1. **Pre-state.** Record the current `.env` AI_ENABLED value. Note the line number.

2. **Flip AI_ENABLED to false.**
   ```bash
   # Edit .env via the Edit tool (preserve all other env vars)
   ```
   Then restart api:
   ```bash
   docker compose restart api
   ```
   Wait until `curl -s http://localhost:8080/api/healthz` returns `{"ok": true}` (or equivalent).

3. **Verify disabled state via API.** `curl -s -u admin:admin http://localhost:8080/api/admin/ai-status/` should return JSON with `"enabled": false`. (This is a sanity check before opening the UI.)

4. **Navigate to the builder.** Re-use the same canonical map URL. Use `browser_navigate` after `browser_evaluate(() => window.location.reload())` if the page is still open from Task 1.

5. **Open the AI rail.** Click the Sparkles icon. Confirm the panel header reads "AI unavailable" (or similar — the `unavailable` label from BuilderRail) NOT "Ask AI".

6. **Verify Surface 3.** Capture browser_snapshot. Confirm:
   - The BotOff icon is rendered (selector: `svg[aria-hidden="true"]` next to the title; or use `getByRole` semantics).
   - The title reads "AI is disabled".
   - The body reads "An administrator has disabled AI for this instance."
   - A "Go to Settings" link is present (admin user).
   - `data-ai-reason="env_disabled"` attribute is on the container (per Plan 03 Task 1 action — this attribute is the regression hook for the smoke).

7. **Console-error check.** `browser_console_messages` — capture all `error` and `warning` level events. The acceptance criterion: ZERO `/ai/*`-related 401/403/503 errors fire (because the composite `enabled: !!token && isAdmin` gate + `aiStatus.data.enabled === false` should suppress all `/ai/*` requests). Any such error is a Pitfall #4 regression — file as SF-MCP-DISABLED-N.

8. **Restore the stack.** Edit `.env` back to `AI_ENABLED=true`. Restart api. Verify `/api/admin/ai-status/` returns `"enabled": true`. Reload the builder page; verify the AI rail returns to the live chat surface.

9. **Append to the smoke report:** Add a new section to `1135-MCP-SMOKE.md`:

```markdown
## AI_ENABLED=false Pass

**Stack mutation:** `AI_ENABLED=true` → `AI_ENABLED=false`; `docker compose restart api`; wait for `/api/admin/ai-status/` to return `{ enabled: false, ... }`. Stack restored to `AI_ENABLED=true` after smoke.

| REQ | Surface | Sub-check | PASS / FAIL | Evidence |
|-----|---------|-----------|-------------|----------|
| AI-02 | Disabled-state title | "AI is disabled" rendered | PASS | snapshot quote |
| AI-02 | Disabled-state body | "An administrator has disabled AI for this instance." | PASS | snapshot quote |
| AI-02 | BotOff icon | `svg` icon visible next to title | PASS | snapshot quote |
| AI-02 | Settings CTA (admin) | "Go to Settings" link rendered for admin user | PASS | snapshot quote |
| AI-02 | data-ai-reason hook | container has `data-ai-reason="env_disabled"` | PASS | DOM attribute query |
| Pitfall #4 | Console silence | Zero `/ai/*` 4xx/5xx errors on page load | PASS | console_messages count: 0 |
```

If any FAIL is recorded, append to the same `## Findings (carry-forward)` section started in Task 1.</action>
  <acceptance_criteria>
    - `1135-MCP-SMOKE.md` contains a `## AI_ENABLED=false Pass` section with at least 6 sub-checks recorded.
    - All sub-checks have evidence quotes.
    - The smoke report explicitly confirms `.env` was restored to `AI_ENABLED=true` post-smoke.
    - Pitfall #4 console-silence check is included and PASS/FAIL recorded.
    - If `data-ai-reason` attribute is missing from the BuilderRail AIDisabledState component (Plan 03 Task 1 action), file a Plan-03 regression finding and route the operator to fix it before merging.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "AI_ENABLED=false" .planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/1135-MCP-SMOKE.md</automated>
  </verify>
  <done>Surface 3 disabled-state pinned live; Pitfall #4 console silence confirmed; stack restored to default state; report appended.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator sign-off on 1135-MCP-SMOKE.md</name>
  <what-built>
    - Plans 01-05 merged + integrated.
    - Live MCP smoke report at `.planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/1135-MCP-SMOKE.md` covering AI_ENABLED=true (Surfaces 1, 2, 5; opportunistic Surface 4) + AI_ENABLED=false (Surface 3 + Pitfall #4).
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/1135-MCP-SMOKE.md` end-to-end.
    2. Confirm every PASS row has evidence (snapshot quote or DOM-query result).
    3. Confirm any FAIL rows have a finding ID + reproducer + a disposition (FIX-NOW vs CARRY-FORWARD-TO-PHASE-1139).
    4. Confirm the `.env` was restored to `AI_ENABLED=true` post-smoke (no lingering stack mutation).
    5. If satisfied: type `approved` to release Phase 1135.
    6. If unsatisfied: describe the gap; the executor will re-plan or extend the report.
  </how-to-verify>
  <resume-signal>Type "approved" to CLEAR-TO-MERGE Phase 1135, or describe issues to defer or fix.</resume-signal>
</task>

</tasks>

<verification>
- `.planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/1135-MCP-SMOKE.md` exists and is read-end-to-end.
- Both AI_ENABLED=true and AI_ENABLED=false pass tables are present.
- Final operator sign-off recorded in the SUMMARY for this plan.
</verification>

<success_criteria>
- ROADMAP AI-01 + AI-02 + AI-03 + AI-05 + AI-08 + AI-09: all verified live (AI-03 may be SKIPPED with rationale if naturally unreproducible).
- v1030 hard invariant: live MCP smoke is a justified pre-tag gate — satisfied for the AI chat polish surface area.
- No lingering stack mutation: `.env` confirmed restored.
- Final operator checkpoint produces CLEAR-TO-MERGE OR a numbered list of follow-ups routed to Phase 1139's close-gate smoke.
</success_criteria>

<output>
Create `.planning/phases/1135-ai-chat-confirm-before-apply-and-analysis-polish/1135-06-SUMMARY.md` documenting:
- Smoke-report file path + total PASS/FAIL count.
- Stack mutation timeline (when AI_ENABLED was flipped, when it was restored).
- Any FAIL findings + their disposition (FIX-NOW vs carry-forward).
- Operator sign-off signal (approved / issues described).
- Cross-reference: ROADMAP AI-01..05/08/09, UI-SPEC Surfaces 1-5, Pitfall #4 console-silence verification.
</output>
</content>
</invoke>