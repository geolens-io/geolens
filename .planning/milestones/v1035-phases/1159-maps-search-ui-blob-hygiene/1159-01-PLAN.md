---
phase: 1159-maps-search-ui-blob-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/main.tsx
  - frontend/src/components/maps/hooks/use-quicklook.ts
  - frontend/src/components/maps/hooks/use-map-thumbnail.ts
autonomous: true
requirements: [MAPS-01, HYG-01]

must_haves:
  truths:
    - "A Vite HMR re-execution of main.tsx reuses the existing React root instead of calling createRoot() a second time on the #root container"
    - "<React.StrictMode> still wraps the app tree (StrictMode preserved, not the cause of the warning)"
    - "registerBlobUrlRevocation(queryClient) runs from a useEffect, not during the render body, in both blob hooks"
    - "Blob-revocation behavior is unchanged: the registration is still wired before the React Query cache can evict an entry (existing use-quicklook Tests 6/7 stay green)"
  artifacts:
    - path: "frontend/src/main.tsx"
      provides: "Cached-root guard on the #root container (HMR-safe single createRoot)"
      contains: "__glRoot"
    - path: "frontend/src/components/maps/hooks/use-quicklook.ts"
      provides: "registerBlobUrlRevocation moved into useEffect keyed on queryClient"
      contains: "useEffect"
    - path: "frontend/src/components/maps/hooks/use-map-thumbnail.ts"
      provides: "registerBlobUrlRevocation moved into useEffect keyed on queryClient"
      contains: "useEffect"
  key_links:
    - from: "frontend/src/main.tsx"
      to: "document.getElementById('root').__glRoot"
      via: "nullish-coalescing cache read + assignment before render"
      pattern: "__glRoot \\?\\? ReactDOM.createRoot"
    - from: "frontend/src/components/maps/hooks/use-quicklook.ts"
      to: "registerBlobUrlRevocation"
      via: "useEffect([queryClient])"
      pattern: "useEffect\\("
---

<objective>
Two source-only frontend hygiene fixes, existing files only, no new deps:

- **MAPS-01 (#122):** Eliminate the duplicate `ReactDOMClient.createRoot() on a container that has already been passed to createRoot()` console error. Root cause is validated (CONTEXT.md `<decisions>`): `main.tsx` is the single app entry; Vite HMR re-executes it with no `import.meta.hot` guard, so `ReactDOM.createRoot(#root)` runs again on a container React already rooted. It is a DEV-ONLY HMR artifact (a cold load and any production build emit 0 warnings). Fix = cache/reuse the root on the container so HMR re-exec reuses it.
- **HYG-01 (QZ-LP-04):** Move `registerBlobUrlRevocation(queryClient)` out of the hook render body and into a `useEffect` in `use-quicklook.ts` and `use-map-thumbnail.ts`. The function is idempotent (module-level `WeakSet` in `blob-url-cache.ts:23,33`), so behavior is unchanged — this only removes the side-effect-in-render smell.

Purpose: Close the app-wide dev-console error (#122) and a self-reported code nit before the Phase 1160 live MCP close-gate.
Output: HMR-safe single-root mount; blob-revocation registration wired via effect.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1159-maps-search-ui-blob-hygiene/1159-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live files. No codebase exploration required. -->

frontend/src/main.tsx — current mount (line 30, inside bootstrap(), inside <React.StrictMode>):
```
ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode> ... </React.StrictMode>,
);
```
`ReactDOM` is `import ReactDOM from 'react-dom/client'` (line 2). `bootstrap()` is `await`-ed via `void bootstrap()` at the bottom (line 46). The full render tree (AppErrorBoundary → QueryClientProvider → ThemeProvider → TooltipProvider → RouterProvider + ThemedToaster) is UNCHANGED — only the `createRoot` call is wrapped.

frontend/src/lib/blob-url-cache.ts — the registration is idempotent:
```
const registered = new WeakSet<QueryClient>();
export function registerBlobUrlRevocation(queryClient: QueryClient): void {
  if (registered.has(queryClient)) return;
  registered.add(queryClient);
  ...subscribe to queryClient.getQueryCache()...
}
```
Calling it 0..n times for the same QueryClient is safe. The `queryClient` from `useQueryClient()` is the SAME instance for the app's lifetime, so an effect keyed on `[queryClient]` fires once.

frontend/src/components/maps/hooks/use-quicklook.ts:52-54 — current render-body call (inside useQuicklookQuery):
```
const queryClient = useQueryClient();
registerBlobUrlRevocation(queryClient);   // <-- side-effect in render
```
NOTE: `useQuicklook` early-returns BEFORE calling `useQuicklookQuery` when datasetId is null or known-missing (lines 37-47), so the effect lives in the inner `useQuicklookQuery` hook (which only runs on the fetch path). Keep it there.

frontend/src/components/maps/hooks/use-map-thumbnail.ts:35-36 — current render-body call:
```
const queryClient = useQueryClient();
registerBlobUrlRevocation(queryClient);   // <-- side-effect in render
```
This hook has no early-return; the effect goes at the top level of useMapThumbnail.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MAPS-01 — cached-root guard in main.tsx (preserve StrictMode)</name>
  <files>frontend/src/main.tsx</files>
  <read_first>
    - frontend/src/main.tsx (the whole file, 47 lines — the only createRoot in the app)
    - .planning/phases/1159-maps-search-ui-blob-hygiene/1159-CONTEXT.md (`<decisions>` MAPS-01 root cause + validated Option A)
  </read_first>
  <action>
    In `bootstrap()` (around line 30), replace the single inline `ReactDOM.createRoot(document.getElementById('root')!).render(...)` statement with a cached-root guard that reuses any root already stored on the container (per CONTEXT.md MAPS-01 validated Option A):
    1. Hoist the container to a local: get `document.getElementById('root')!` into a `container` const.
    2. Read-or-create the root: `const root = (container as unknown as { __glRoot?: ReturnType<typeof ReactDOM.createRoot> }).__glRoot ?? ReactDOM.createRoot(container);` then assign it back: `(container as unknown as { __glRoot?: ReturnType<typeof ReactDOM.createRoot> }).__glRoot = root;`. (You may instead declare a small `interface RootContainer extends HTMLElement { __glRoot?: Root }` with `import { type Root } from 'react-dom/client'` if you prefer typed access over the inline cast — either is acceptable as long as no `any` leaks and `npm run typecheck` is 0.)
    3. Call `root.render(<React.StrictMode> ... </React.StrictMode>)` with the EXISTING tree verbatim (AppErrorBoundary → QueryClientProvider → ThemeProvider → TooltipProvider → RouterProvider + ThemedToaster).
    The container-property name MUST be `__glRoot` (the e2e regression test in Plan 02 keys off the absence of the createRoot warning, and CONTEXT pins this name).
    Gotchas (CONTEXT): PRESERVE `<React.StrictMode>` — it double-invokes render/effects but calls createRoot once; it is NOT the cause. Do NOT add `root.unmount()` then re-`createRoot` (causes a teardown/remount flash every HMR tick). Do NOT add an `import.meta.hot` block — the cached-root guard is the chosen fix and is self-contained. Production mount path is unchanged (cold container has no `__glRoot`, so the first load calls createRoot exactly once as before).
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` (from `frontend/`) exits 0.
    - `grep -nE "__glRoot \?\? ReactDOM\.createRoot" frontend/src/main.tsx` returns exactly 1 match (the read-or-create line).
    - `grep -c "ReactDOM.createRoot(" frontend/src/main.tsx` returns exactly 1 (no second createRoot call introduced).
    - `grep -c "React.StrictMode" frontend/src/main.tsx` returns >= 1 (StrictMode preserved).
    - `grep -c "root.unmount" frontend/src/main.tsx` returns 0 (no unmount/re-root teardown introduced).
    - No `: any` or ` as any` token introduced in main.tsx: `grep -c "as any\|: any" frontend/src/main.tsx` returns 0.
  </acceptance_criteria>
  <done>main.tsx caches the React root on `container.__glRoot` and reuses it on re-exec; StrictMode and the full render tree are unchanged; typecheck is 0.</done>
</task>

<task type="auto">
  <name>Task 2: HYG-01 — move registerBlobUrlRevocation into useEffect in both blob hooks</name>
  <files>frontend/src/components/maps/hooks/use-quicklook.ts, frontend/src/components/maps/hooks/use-map-thumbnail.ts</files>
  <read_first>
    - frontend/src/components/maps/hooks/use-quicklook.ts (whole file, 91 lines — note the early-return split into useQuicklookQuery)
    - frontend/src/components/maps/hooks/use-map-thumbnail.ts (whole file, 51 lines)
    - frontend/src/lib/blob-url-cache.ts (registerBlobUrlRevocation is idempotent via the WeakSet at lines 23,33)
    - frontend/src/components/maps/hooks/__tests__/use-quicklook.test.ts (Tests 6 and 7 — these depend on the registration being wired; they must stay green)
    - .planning/phases/1159-maps-search-ui-blob-hygiene/1159-CONTEXT.md (`<decisions>` HYG-01)
  </read_first>
  <action>
    In BOTH hooks, move the bare render-body call `registerBlobUrlRevocation(queryClient);` into a `useEffect`. Add `useEffect` to the existing `import { useQuery, useQueryClient } from '@tanstack/react-query'` line's neighbor — `useEffect` comes from `'react'`, so add `import { useEffect } from 'react';` at the top of each file (React 19 auto-JSX runtime; do not add a default React import).
    - `use-quicklook.ts`: inside `useQuicklookQuery` (line 52), keep `const queryClient = useQueryClient();`, then replace the bare `registerBlobUrlRevocation(queryClient);` (line 54) with `useEffect(() => { registerBlobUrlRevocation(queryClient); }, [queryClient]);`. Do NOT move it into the outer `useQuicklook` — the early-return guards (datasetId null / known-missing) must run before any hook, so the effect stays inside `useQuicklookQuery` exactly where the call is today.
    - `use-map-thumbnail.ts`: in `useMapThumbnail` (line 30), keep `const queryClient = useQueryClient();` (line 35), then replace the bare `registerBlobUrlRevocation(queryClient);` (line 36) with `useEffect(() => { registerBlobUrlRevocation(queryClient); }, [queryClient]);`.
    Behavior is identical: the effect fires after the first render (and `queryClient` is a stable singleton, so the dep never changes → the effect body runs once), which is before React Query can evict an entry. The WeakSet keeps it idempotent regardless. No signature, return shape, or query-key change. Do NOT touch the `useQuery(...)` blocks, the status-derivation logic, or `withThumbnailVersion`.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck && npm test -- src/components/maps/hooks/__tests__/use-quicklook.test.ts src/components/maps/hooks/__tests__/use-map-thumbnail.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` (from `frontend/`) exits 0.
    - The two existing hook test files pass unchanged — including `use-quicklook.test.ts` Test 6 ("does NOT revoke blob URL on unmount") and Test 7 ("revokes blob URL when React Query evicts the cached entry"). Run: `npm test -- src/components/maps/hooks/__tests__/use-quicklook.test.ts src/components/maps/hooks/__tests__/use-map-thumbnail.test.ts` reports 0 failures.
    - In `use-quicklook.ts`: `grep -c "useEffect(() => {" frontend/src/components/maps/hooks/use-quicklook.ts` returns >= 1, and the line `registerBlobUrlRevocation(queryClient);` is NOT at the bare top level of `useQuicklookQuery` (it is inside the effect body) — verify with `grep -nB1 "registerBlobUrlRevocation(queryClient)" frontend/src/components/maps/hooks/use-quicklook.ts` showing the preceding line contains `useEffect`.
    - In `use-map-thumbnail.ts`: `grep -c "useEffect(() => {" frontend/src/components/maps/hooks/use-map-thumbnail.ts` returns >= 1, and `grep -nB1 "registerBlobUrlRevocation(queryClient)" frontend/src/components/maps/hooks/use-map-thumbnail.ts` shows the preceding line contains `useEffect`.
    - Both files import `useEffect` from `'react'`: `grep -c "import { useEffect } from 'react'" frontend/src/components/maps/hooks/use-quicklook.ts frontend/src/components/maps/hooks/use-map-thumbnail.ts` (combined) returns 2.
  </acceptance_criteria>
  <done>Both hooks call `registerBlobUrlRevocation` from a `useEffect([queryClient])`, not the render body; the existing eviction/no-unmount-revoke tests stay green; typecheck is 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is crossed by this plan. `main.tsx` is the client bootstrap; the blob hooks operate on already-authenticated `apiFetchBlob` responses inside the browser. No request/response shape, auth path, or data-egress surface changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1159-01 | (none) | frontend/src/main.tsx, blob hooks | accept | No new attack surface. MAPS-01 is a DEV-ONLY HMR console artifact (0 in production); HYG-01 reorders an idempotent in-browser call. No package installs, no network/auth/storage changes. |
</threat_model>

<verification>
- `cd frontend && npm run typecheck` → 0 errors.
- `cd frontend && npm test -- src/components/maps/hooks/__tests__/use-quicklook.test.ts src/components/maps/hooks/__tests__/use-map-thumbnail.test.ts` → all pass (Tests 6/7 prove blob-revocation behavior is preserved after the effect move).
- `grep -nE "__glRoot \?\? ReactDOM\.createRoot" frontend/src/main.tsx` → exactly 1 match.
- StrictMode preserved: `grep -c "React.StrictMode" frontend/src/main.tsx` >= 1.
</verification>

<success_criteria>
- main.tsx mounts via a cached `container.__glRoot` (HMR-safe single createRoot), StrictMode intact, production path unchanged.
- `registerBlobUrlRevocation` is invoked from a `useEffect` in both `use-quicklook.ts` and `use-map-thumbnail.ts` with identical behavior.
- typecheck 0; existing hook vitest green.
</success_criteria>

<output>
Create `.planning/phases/1159-maps-search-ui-blob-hygiene/1159-01-SUMMARY.md` when done.
</output>
