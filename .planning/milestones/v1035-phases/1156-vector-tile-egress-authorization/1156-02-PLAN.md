---
phase: 1156-vector-tile-egress-authorization
plan: 02
type: execute
wave: 2
depends_on: ["1156-01"]
files_modified:
  - backend/tests/test_vector_tile_auth.py
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "A failing-before / passing-after regression test pins the anonymous denial of public-unpublished vector tile tokens"
    - "The test asserts the positive over-gating guard: public+published still serves to anon"
    - "The test covers single token, batch token, and cluster-tile denial paths"
  artifacts:
    - path: "backend/tests/test_vector_tile_auth.py"
      provides: "Regression coverage for SEC-01 (vector-tile egress authorization)"
      contains: "record_status"
      min_lines: 80
  key_links:
    - from: "test_vector_tile_auth.py"
      to: "GET /tiles/token/{id}/ and POST /tiles/tokens/ and GET /tiles/clusters/.../*.pbf"
      via: "AsyncClient requests with no auth header (anonymous)"
      pattern: "/tiles/token/"
---

<objective>
Pin SEC-01 with a regression test so the anonymous vector-tile egress leak cannot return. This is success criterion 5 of the phase and the Nyquist behavioral gate for Plan 01.

The test proves: (a) anonymous single token + (b) anonymous batch token + (c) anonymous cluster tile are DENIED for a `public` + non-`published` vector dataset (today all serve / mint); and (d) the POSITIVE over-gating guard — a `public` + `published` vector dataset STILL serves a token to anon.

Purpose: Lock the fix; prevent silent regression of an anonymous data leak.
Output: New file `backend/tests/test_vector_tile_auth.py`, modeled exactly on `backend/tests/test_raster_tiles.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/1156-vector-tile-egress-authorization/1156-CONTEXT.md
@.planning/phases/1156-vector-tile-egress-authorization/1156-PATTERNS.md

<interfaces>
<!-- Test analog + fixtures. Extracted from the codebase 2026-05-30. Copy the factory verbatim; only the assertions change. -->

Analog file (copy structure, imports, and the vector factory): backend/tests/test_raster_tiles.py
  - `_get_admin_id(session)` (line 32) — returns the admin user's UUID
  - `_create_vector_dataset(session, *, created_by, visibility="public", record_status="published")` (line 87)
       -> (Record, Dataset); creates a vector Record + Dataset (NO PostGIS geometry seeded)
  - `_get_auth_header(client, username, password)` (line 151) — OAuth2 form login -> {"Authorization": "Bearer ..."}
  - Positive-case shape to mirror: `test_public_dataset_accessible_by_both_paths` (line 374)
  - Denial shape to mirror: `test_auth_check_blocks_unpublished_for_non_owner` (line 288)

conftest fixtures (backend/tests/conftest.py):
  - `client: AsyncClient`               (line 1274 / used throughout) — test HTTP client; base path already under /api router mount as in test_raster_tiles
  - `test_db_session`                   (line 1503) — async session sharing the client's transaction (use for _create_vector_dataset)
  - `admin_auth_header: dict`           (line 1475) — admin JWT header

Endpoints under test (anonymous = NO Authorization header, NO X-Embed-Token):
  - GET  /tiles/token/{dataset_id}/        single token (returns 200 + {kind:"vector", sig, exp, scope} when allowed)
  - POST /tiles/tokens/                    batch; body {"dataset_ids": [...]}; returns {"tokens": {id: token_or_error}}
       response_model TileTokenBatchResponse; denied entries are {"error": "..."} (NOT raised)
  - GET  /tiles/clusters/data.{table_name}/{z}/{x}/{y}.pbf   cluster tile; anon+unpublished must 404

IMPORTANT — why the token endpoint is the primary regression surface (not the raw `.pbf`):
  `_create_vector_dataset` seeds NO PostGIS geometry, so the raw `/tiles/{table}/{z}/{x}/{y}.pbf`
  ST_AsMVT query would 500/404 on missing data regardless of auth — it cannot cleanly prove the
  auth fix. The HMAC TOKEN is the capability that unlocks the `.pbf`; denying the token (401/404)
  closes the leak. The cluster route shares `_authorize_vector_tile_request` and IS asserted (it 404s
  on the auth guard before any geometry query for the unpublished case).

Status values: "published" (allowed for anon), and a non-published value — use "internal" (the live
repro flip) or "draft". Both are non-published; the gate keys on `!= "published"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_vector_tile_auth.py — denial matrix + positive over-gating guard</name>
  <files>backend/tests/test_vector_tile_auth.py</files>
  <read_first>
    - backend/tests/test_raster_tiles.py lines 1-117 (module docstring, imports, `_get_admin_id`, `_create_vector_dataset`, `_create_raster_dataset`)
    - backend/tests/test_raster_tiles.py lines 145-160 (`_get_auth_header`)
    - backend/tests/test_raster_tiles.py lines 288-317 (denial pattern `test_auth_check_blocks_unpublished_for_non_owner`)
    - backend/tests/test_raster_tiles.py lines 374-395 (positive pattern `test_public_dataset_accessible_by_both_paths`)
    - .planning/phases/1156-vector-tile-egress-authorization/1156-PATTERNS.md "Test Pattern" section (fixtures, factory, denial + positive shapes, run recipe)
  </read_first>
  <action>
    Create `backend/tests/test_vector_tile_auth.py`. Copy the import block and the `_get_admin_id`, `_create_vector_dataset`, and `_get_auth_header` helpers verbatim from `test_raster_tiles.py` (drop the raster-only `_create_raster_dataset` and `RasterAsset` import — keep `Record`, `Dataset`, `User`, `settings`, `uuid`, `pytest`, `AsyncClient`, `select`). Write one test class `TestVectorTileEgressAuthorization` with these async test methods (all use the `client` + `test_db_session` fixtures; mark with `pytestmark = pytest.mark.asyncio` if the analog file does, otherwise follow the analog's collection convention exactly):

    1. `test_anon_single_token_denied_for_public_unpublished`: create a vector dataset with `visibility="public", record_status="internal"`; anonymous `GET /tiles/token/{dataset.id}/` (NO headers); assert `resp.status_code in (401, 404)`. (Today this returns 200 with a minted HMAC sig — the leak.)

    2. `test_anon_batch_token_denied_for_public_unpublished`: same dataset shape; anonymous `POST /tiles/tokens/` with json `{"dataset_ids": [str(dataset.id)]}`; assert `resp.status_code == 200` (batch never fails the request) AND `resp.json()["tokens"][str(dataset.id)]` contains an `"error"` key and NO `"sig"` key (no HMAC minted for the unpublished dataset).

    3. `test_anon_cluster_tile_denied_for_public_unpublished`: create the dataset as above; anonymous `GET /tiles/clusters/data.{dataset.table_name}/2/0/0.pbf` (NO headers); assert `resp.status_code == 404`. The auth guard runs before any geometry query, so the unpublished case 404s on auth. (If the route instead 400s on the non-point clusterable check before auth, assert `resp.status_code in (400, 404)` — but the auth 404 is expected for the unpublished-status path; prefer the tighter `== 404` and only widen if the clusterable check fires first. Read `_ensure_clusterable_dataset` ordering at `tiles/router.py:1126-1130` to confirm: `_resolve_dataset_meta` -> `_ensure_clusterable_dataset` -> `_authorize_vector_tile_request`; since the test factory creates no geometry_type, `_ensure_clusterable_dataset` may 400 first — therefore assert `resp.status_code in (400, 404)` and add an inline comment that 400 = clusterable-gate, 404 = auth-gate, both prove anon does NOT get tile bytes.)

    4. `test_anon_single_token_allowed_for_public_published` (POSITIVE / over-gating guard): create a vector dataset with `visibility="public", record_status="published"`; anonymous `GET /tiles/token/{dataset.id}/`; assert `resp.status_code == 200` and `resp.json()["sig"]` is present (a valid token IS minted for the legitimate public-published case). This is the guard that proves the fix does not over-gate.

    Each test resolves `admin_id = await _get_admin_id(test_db_session)` and passes `created_by=admin_id` to the factory. Do NOT seed PostGIS geometry. Do NOT hit the raw `/tiles/{table}/.../*.pbf` route (no geometry → unrelated 500/404). Keep assertions on exact status codes / JSON keys as specified; no subjective checks.
  </action>
  <verify>
    <automated>cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/test_vector_tile_auth.py -v</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/tests/test_vector_tile_auth.py` exists with class `TestVectorTileEgressAuthorization` containing the four named test methods.
    - All four tests PASS against the Plan-01 fix: `cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/test_vector_tile_auth.py -v` exits 0 with 4 passed.
    - The single-token denial asserts `status_code in (401, 404)`; the positive case asserts `status_code == 200` AND `"sig"` present in the JSON.
    - The batch test asserts the response is 200 with a per-key `"error"` and NO `"sig"` for the unpublished dataset.
    - The cluster test asserts the anon caller receives no tile bytes (status in (400, 404)) for the unpublished dataset.
    - No use of `_create_raster_dataset` / `RasterAsset`; no request to the raw `.pbf` data route.
  </acceptance_criteria>
  <done>`test_vector_tile_auth.py` pins SEC-01: anon single + batch token + cluster tile are denied for public-unpublished vector datasets, and public+published still mints a token for anon (no over-gating); all four tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regression test → vector-tile HTTP API | Test exercises the anonymous trust boundary (no auth header) to prove denial of public-unpublished egress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1156-05 | Information Disclosure (regression prevention) | `test_vector_tile_auth.py` | mitigate | The test pins all three anon denial surfaces (single token, batch token, cluster tile) plus the positive over-gating guard, so a future change that re-opens the leak OR over-gates public+published fails CI. Directly mitigates the recurrence of T-1156-01/02/04 from Plan 01. |
| T-1156-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies — `pytest`, `httpx`, `AsyncClient` already in the test toolchain (used by the `test_raster_tiles.py` analog). No install tasks; Package Legitimacy Gate N/A. |
</threat_model>

<verification>
```bash
cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/test_vector_tile_auth.py -v
```

Expected: 4 passed. (`.env.test` sets `POSTGRES_HOST=localhost` + `POSTGRES_PORT=5434`; without it, `InvalidCatalogNameError`.)
</verification>

<success_criteria>
- `backend/tests/test_vector_tile_auth.py` exists, modeled on `test_raster_tiles.py`.
- Anon single token for public-unpublished → 401/404 (asserted).
- Anon batch token for public-unpublished → 200 with per-key error, no `sig` (asserted).
- Anon cluster tile for public-unpublished → no tile bytes (400/404) (asserted).
- Anon single token for public-published → 200 with `sig` present (asserted; over-gating guard).
- All four tests pass via the documented run recipe.
</success_criteria>

<output>
Create `.planning/phases/1156-vector-tile-egress-authorization/1156-02-SUMMARY.md` when done.
</output>
