---
phase: 1157-backend-export-access-route-hygiene
plan: 02
type: execute
wave: 2
depends_on: ["1157-01"]
files_modified:
  - backend/tests/test_export_access.py
autonomous: true
requirements: [EXP-02, API-01]

must_haves:
  truths:
    - "An anonymous export of a public+published dataset returns a real export body (the EXP-01 positive guard)"
    - "An anonymous export of a public-unpublished dataset is denied (401/403/404)"
    - "An anonymous export of a private/restricted dataset is denied (401/403/404)"
    - "A non-owner authenticated export of a private dataset is denied (401/403/404)"
    - "GET /collections/{id}/items/ (trailing slash) returns the same status as the no-slash form"
  artifacts:
    - path: "backend/tests/test_export_access.py"
      provides: "Regression coverage pinning EXP-01 (allow/deny matrix) and API-01 (dual-shape parity)"
      contains: "def test_"
  key_links:
    - from: "backend/tests/test_export_access.py"
      to: "tests.factories.create_dataset"
      via: "seeds public/private datasets with explicit visibility + record_status"
      pattern: "create_dataset"
    - from: "backend/tests/test_export_access.py"
      to: "app.processing.export.router.export_dataset"
      via: "monkeypatch fake export so the positive case asserts the gate, not OGR"
      pattern: "monkeypatch.setattr"
---

<objective>
EXP-02 (QZ-LP-02) + API-01 pin: a new regression test file that proves the Plan 01 behavior holds.

- EXP-02: anonymous AND non-owner export of private/restricted/unpublished datasets stays denied ({401,403,404}); anonymous export of a public+published dataset is allowed (the EXP-01 positive over-gating guard). No draft/ready vector dataset exists in the dev DB — seed datasets in the test via the `tests.factories.create_dataset` factory.
- API-01: assert `GET /collections/{id}/items/` (trailing slash) resolves to the same status as `/collections/{id}/items` (no-slash) — pins the Plan 01 dual-shape alias.

Purpose: lock the access-control gate and the route-shape alias against regression. EXP-01 + EXP-02 are coupled (the test pins the implementation), so this plan runs in Wave 2 after Plan 01.

Output: `backend/tests/test_export_access.py`. No production code changes, no new deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1157-backend-export-access-route-hygiene/1157-CONTEXT.md
@.planning/phases/1157-backend-export-access-route-hygiene/1157-PATTERNS.md
@.planning/phases/1157-backend-export-access-route-hygiene/1157-01-PLAN.md

<interfaces>
<!-- Test scaffolding confirmed present in the codebase. Use these directly; do not re-derive. -->

Dataset factory — backend/tests/factories.py:
  async def create_dataset(session, *, created_by, name=..., visibility="public",
                           record_status="published", ...) -> Dataset       # :32
      # set record_status to "draft" / "internal" / "ready" for UNPUBLISHED
      # set visibility to "private" / "restricted" for non-public
  async def get_user_id(session, username) -> uuid.UUID                      # :25

Conftest fixtures (no manual login needed for these roles) — backend/tests/conftest.py:
  admin_auth_header   -> dict[str, str]   # :1475  (admin Bearer header)
  viewer_auth_header  -> dict[str, str]   # :1494  (viewer Bearer header — distinct, non-owner user)
  client              -> AsyncClient      (httpx async client fixture)
  test_db_session     -> AsyncSession     (db session fixture)

Mock export service (so the allow-case asserts the GATE, not OGR/GDAL) —
  backend/tests/test_export_hardening.py:181-220:
    monkeypatch.setattr("app.processing.export.router.export_dataset", _fake_export)
    # _fake_export writes b"mock export data" to a temp file and returns (file_path, filename, media_type)
    # FORMAT_MAP imported from app.processing.export.ogr

Export endpoint under test:  GET /datasets/{dataset_id}/export?format={gpkg|geojson|shp|csv}
OGC items endpoint under test: GET /collections/{dataset_id}/items  and  /collections/{dataset_id}/items/

Denied-assertion contract (from PATTERNS.md + 1157-CONTEXT.md):
  check_dataset_access_or_anonymous raises 404 for anonymous denials (hides existence);
  check_dataset_access raises 404 for non-owner authenticated denials; require_permission paths raise 401/403.
  => denied assertions accept the SET {401, 403, 404}, not a single pinned code.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EXP-02 — anon/non-owner export allow+deny matrix</name>
  <files>backend/tests/test_export_access.py</files>
  <read_first>
    - backend/tests/test_export_hardening.py lines 181-220 (the `mock_export_service_for_known05` monkeypatch fixture to mirror) and lines 277-279 (use of the `admin_auth_header` / `viewer_auth_header` fixtures)
    - backend/tests/test_vector_tile_auth.py lines 14-18 (run-recipe header), 176-196 (anon-denied assertion shape), 296-321 (positive over-gating guard shape)
    - backend/tests/factories.py lines 25-85 (`get_user_id` + the `create_dataset` factory signature, including `visibility` and `record_status` kwargs)
    - backend/app/processing/export/router.py (the Plan 01 result — confirm the endpoint path `/{dataset_id}/export`, the `format` query param, and the anon-vs-authenticated branch so assertions match the implemented behavior)
    - .planning/phases/1157-backend-export-access-route-hygiene/1157-PATTERNS.md ("backend/tests/test_export_access.py (EXP-02)" section — verified fixture + assertion shapes + the {401,403,404} denied-set rule)
  </read_first>
  <action>
    Create `backend/tests/test_export_access.py` (new file). Add the module docstring run-recipe header mirroring `test_vector_tile_auth.py:14-18` (docker db up, alembic applied, `set -a && source ../.env.test && set +a` then `uv run pytest tests/test_export_access.py -v`). Standard imports per PATTERNS.md ("Standard imports for a new integration test file"): `os`, `shutil`, `tempfile`, `uuid`, `pytest`, `httpx.AsyncClient`, `from app.processing.export.ogr import FORMAT_MAP`, `from tests.factories import create_dataset, get_user_id`. Mark async tests for the project's async runner consistent with `test_export_hardening.py` (mirror its `pytestmark` / `@pytest.mark.asyncio` usage — confirm while reading).

    Add a `mock_export_service` monkeypatch fixture mirroring `test_export_hardening.py:181-220`: it `monkeypatch.setattr("app.processing.export.router.export_dataset", _fake_export)` where `_fake_export` writes `b"mock export data"` to a tempdir file and returns `(file_path, filename, media_type)` using `FORMAT_MAP[format_key]` for ext/media; clean the tempdir on teardown. This makes the allow-case assert the GATE, not real OGR/GDAL export.

    Seed datasets via `create_dataset` (the admin user is the owner; resolve `admin_id` via `get_user_id(test_db_session, "admin")`):
    - `public_published`: `visibility="public", record_status="published"`
    - `public_unpublished`: `visibility="public", record_status="internal"` (unpublished — the gap with no dev-DB fixture)
    - `private_published`: `visibility="private", record_status="published"`
    - `restricted_published`: `visibility="restricted", record_status="published"`

    Write these tests (use the `client`, `test_db_session`, `admin_auth_header`, `viewer_auth_header` conftest fixtures):
    1. `test_anon_export_public_published_allowed` — with the `mock_export_service` fixture active, anonymous (no Authorization header) `GET /datasets/{public_published.id}/export?format=geojson` returns `200` and a non-empty body. This is the EXP-01 positive over-gating guard.
    2. `test_anon_export_all_formats_public_published_allowed` — parametrize over `gpkg, geojson, shp, csv`; anonymous export of `public_published` returns `200` for each (mock fixture active). (If the project's parametrize style on async tests differs, mirror whatever `test_export_hardening.py` does.)
    3. `test_anon_export_public_unpublished_denied` — anonymous export of `public_unpublished` returns a status in `{401, 403, 404}`.
    4. `test_anon_export_private_denied` — anonymous export of `private_published` returns a status in `{401, 403, 404}`.
    5. `test_anon_export_restricted_denied` — anonymous export of `restricted_published` returns a status in `{401, 403, 404}`.
    6. `test_non_owner_export_private_denied` — authenticated as a NON-owner (use `viewer_auth_header`, a different user than the admin owner) exporting `private_published` returns a status in `{401, 403, 404}`.

    For each denied assertion, include an informative message containing the actual status and `resp.text` (mirror `test_vector_tile_auth.py:191-195`). Use the `{401, 403, 404}` membership set per the PATTERNS.md denied-set rule — do NOT pin a single code (anonymous/visibility denials raise 404; capability denials raise 401/403).

    No fenced code: write the test file via the Write tool; do not paste code into this plan.
  </action>
  <verify>
    <automated>cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/test_export_access.py -v -k "export" -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_export_access.py` exists and `pytest tests/test_export_access.py` collects without import errors.
    - A `mock_export_service` fixture patches `app.processing.export.router.export_dataset` (string `monkeypatch.setattr` with that target present in the file).
    - At least one test asserts anonymous `GET /datasets/{id}/export` for a public+published dataset returns `200` with a non-empty body (positive guard).
    - There are explicit deny tests for: anon+public-unpublished, anon+private, anon+restricted, and non-owner-authenticated+private — each asserting `resp.status_code in {401, 403, 404}`.
    - The non-owner deny test authenticates as a different user than the dataset owner (uses `viewer_auth_header`, not `admin_auth_header`).
    - All tests in the file PASS under the documented run recipe.
  </acceptance_criteria>
  <done>`test_export_access.py` proves EXP-01's gate end-to-end: anonymous export of public+published datasets succeeds, while anonymous and non-owner export of unpublished/private/restricted datasets is denied with a status in {401,403,404}.</done>
</task>

<task type="auto">
  <name>Task 2: API-01 — trailing-slash items route parity test</name>
  <files>backend/tests/test_export_access.py</files>
  <read_first>
    - backend/tests/test_export_access.py (the file from Task 1 — append to it; reuse its imports, fixtures, and the `create_dataset` / `get_user_id` seeding helpers)
    - backend/app/standards/ogc/router.py lines 244-258 (the Plan 01 dual-shape decorators on `get_collection_items` — confirm both `/collections/{id}/items` and `/collections/{id}/items/` are registered)
    - backend/tests/test_vector_tile_auth.py lines 137-142 (login helper shape, if a token is needed; the items route uses `get_optional_user`, so a public+published dataset is anonymously readable)
  </read_first>
  <action>
    Append a test to `backend/tests/test_export_access.py` pinning the API-01 dual-shape alias. Reuse the `public_published` dataset seeding (via `create_dataset`, owner = admin) so the OGC items route is anonymously readable.

    `test_collection_items_trailing_slash_matches_no_slash` — issue `GET /collections/{public_published.id}/items` (no slash) and `GET /collections/{public_published.id}/items/` (trailing slash) as the same caller (anonymous is fine for a public+published dataset). Assert: (a) neither response is `404`; (b) `resp_slash.status_code == resp_noslash.status_code` (the alias resolves identically to the canonical form); (c) `resp_noslash.status_code == 200` (sanity — the canonical form serves). Include the actual statuses + both URLs in the assertion message.

    If the items route is mounted under an `/api` prefix or an OGC sub-prefix in the test client, mirror the exact path the existing OGC tests use (confirm the real mounted path while reading `test_vector_tile_auth.py` / any existing OGC items test) so both requests target the live route — the relative shapes `/collections/{id}/items` vs `/collections/{id}/items/` must differ only by the trailing slash.

    No fenced code: edit the test file via the Edit/Write tool; do not paste code into this plan.
  </action>
  <verify>
    <automated>cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/test_export_access.py -v -k "trailing_slash" -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_export_access.py` contains a test named with `trailing_slash` that requests BOTH `/collections/{id}/items` and `/collections/{id}/items/`.
    - The test asserts neither shape returns `404`, the two statuses are equal, and the no-slash canonical form returns `200`.
    - The test PASSES under the documented run recipe (proving the Plan 01 alias resolves).
  </acceptance_criteria>
  <done>The trailing-slash and no-slash forms of `/collections/{id}/items` are proven to resolve to the same handler with identical status, pinning the API-01 alias against regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → export endpoint (as anon / as non-owner / as owner) | The test exercises the production access-control gate across caller identities; it is the verification surface for the EXP-01 trust boundaries |
| test harness → OGC items route (both URL shapes) | Confirms the alias does not introduce a divergent (e.g. unauthenticated-wider) code path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1157-T1 | Information Disclosure (regression) | `export_dataset_endpoint` anon/non-owner deny paths | mitigate | This plan IS the mitigation for T-1157-01/02/03 from Plan 01: deny tests pin anon+unpublished, anon+private, anon+restricted, non-owner+private; the positive test pins the over-gating guard. Denied assertions use the `{401,403,404}` set so a future change that flips a 404→200 (leak) fails the suite. |
| T-1157-T2 | Tampering (false-green) | `mock_export_service` fixture | accept | The mock patches only `export_dataset` (the OGR call) so the allow-case asserts the GATE, not file generation; the gate (`get_optional_user` + `check_dataset_access_or_anonymous`) runs unmocked. Risk that mocking hides a real gate failure is low because the deny tests do NOT need the export to run (denial happens before export). |
| T-1157-T3 | Information Disclosure | API-01 alias parity | mitigate | The parity test asserts the trailing-slash alias returns the SAME status as the canonical form, so the alias cannot silently become an unauthenticated-wider or differently-gated path. |
| T-1157-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs (existing-files-only; test uses already-present `tests.factories`, `pytest`, `httpx`). No `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
Run from `backend/` after sourcing the test env:

```
cd backend && set -a && source ../.env.test && set +a
uv run pytest tests/test_export_access.py -v -p no:cacheprovider
```

All tests in the file must pass: the anonymous allow case (public+published), the four deny cases (anon+unpublished, anon+private, anon+restricted, non-owner+private), and the API-01 trailing-slash parity case. A failure in any deny case indicates the EXP-01 gate regressed into a leak; a failure in the allow case indicates over-gating.
</verification>

<success_criteria>
- `backend/tests/test_export_access.py` exists, collects cleanly, and all its tests pass under the documented recipe.
- Anonymous export of public+published returns 200 with a body; anon+unpublished, anon+private, anon+restricted, and non-owner+private all return a status in {401,403,404}.
- `/collections/{id}/items/` and `/collections/{id}/items` return identical statuses (neither 404; canonical = 200).
</success_criteria>

<output>
Create `.planning/phases/1157-backend-export-access-route-hygiene/1157-02-SUMMARY.md` when done.
</output>
