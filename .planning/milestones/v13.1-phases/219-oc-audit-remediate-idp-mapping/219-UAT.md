---
status: complete
phase: 219-oc-audit-remediate-idp-mapping
source: [219-01-SUMMARY.md]
started: 2026-04-29T00:00:00Z
updated: 2026-04-29T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Schema-gate tests pass (TestIdpRoleMappingGate)
expected: Running `pytest backend/tests/test_oauth.py::TestIdpRoleMappingGate -v` shows 6/6 PASS for the schema validator gate tests.
result: pass
evidence: |
  `cd backend && uv run pytest tests/test_oauth.py::TestIdpRoleMappingGate -v` → 6 passed in 1.33s.
  All six tests pass: rejects_group_claim_in_community, rejects_group_role_mapping_in_community,
  accepts_group_mapping_in_enterprise, update_rejects_group_role_mapping_in_community,
  empty_mapping_allowed_in_community, update_with_none_group_claim_allowed_in_community.

### 2. Community API rejects group_claim with verbatim error
expected: POST to OAuth provider create endpoint in community edition with `group_claim: "groups"` returns 422 ValidationError containing verbatim string "Group-based role mapping requires the GeoLens Enterprise overlay".
result: pass
evidence: |
  Verified by code inspection + Test 1. FastAPI invokes the same Pydantic validator at request-body parse time.
  schemas.py:173-188 (Create) and 285-300 (Update) raise ValueError with the verbatim string when
  not is_enterprise() and group_claim is set or group_role_mapping is non-empty.
  test_create_rejects_group_claim_in_community asserts the verbatim error string and PASSES.

### 3. Community API rejects non-empty group_role_mapping
expected: POST to OAuth provider create endpoint in community with `group_role_mapping: {"admins": "admin"}` returns 422 with the same verbatim Enterprise-overlay error.
result: pass
evidence: |
  test_create_rejects_group_role_mapping_in_community PASSES (asserts verbatim error string).
  test_update_rejects_group_role_mapping_in_community PASSES (Update path also gated).

### 4. Community API accepts empty mapping carve-out
expected: POST with `group_role_mapping: {}` or `group_claim: null` succeeds in community (D-02 carve-out — empty dict / None are not gated).
result: pass
evidence: |
  Guard logic at schemas.py:181-188 uses `isinstance(self.group_role_mapping, dict) and len(self.group_role_mapping) > 0`.
  test_create_with_empty_mapping_allowed_in_community PASSES.
  test_update_with_none_group_claim_allowed_in_community PASSES.

### 5. Audit doc shows VERIFIED banner
expected: Top of `docs-internal/audits/oc-separation-audit-v13.1-close.md` reads "## ✅ MILESTONE CLOSE VERIFIED — Phase 219 closed boundary gap". Original BLOCKED narrative preserved as `### Pre-remediation state` subsection. Section 1 OAuth IdP rows show 🟢. Section 8 grade-delta shows Boundary Integrity reached A.
result: pass
evidence: |
  Line 20: `## ✅ MILESTONE CLOSE VERIFIED — Phase 219 closed boundary gap`.
  Line 33: `### Pre-remediation state (2026-04-29)` subsection preserved.
  Lines 68-70: All three Section 1 OAuth IdP rows show 🟢 with "Closed by Phase 219" annotations.
  Line 387 (Section 8 grade-delta): `Boundary Integrity | B | B− | **A** | ↑ ... | A− | ✅ YES`.
  Line 429: P1 Residual Triage row 1 marked **Closed by Phase 219 (2026-04-29)**.

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — all tests passed]

## Verification Notes

UAT executed autonomously by Claude (assistant-driven self-verification) on 2026-04-29 per user directive
"go ahead and verify all the checkpoints yourself." Phase 219 is backend-only; no UI surface area, so
Playwright was not exercised. HTTP-layer behaviors (tests 2-4) are functionally identical to the
in-process Pydantic validator path — FastAPI parses request bodies through the same `OAuthProviderCreate`
/ `OAuthProviderUpdate` schemas, so passing the schema-gate tests + verifying the validator wiring at
schemas.py:173-188 / 285-300 covers the API behavior.
