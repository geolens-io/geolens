---
phase: 1086-process-tightening-close-gate
plan: 02
type: execute
wave: 2
depends_on:
  - 1086-01
files_modified:
  - CHANGELOG.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md
autonomous: false
requirements:
  - TD-14

must_haves:
  truths:
    - "docker compose up -d --build api worker rebuilds both images cleanly from the current source tree (so v1018 Phase 1080-02's ssl=False line is baked into the running images, eliminating the 8-hour drift from v1018 audit time)."
    - "docker exec geolens-api-1 grep -n 'ssl=False' app/core/config.py returns the expected line (target: line 309 with `connect_args[\"ssl\"] = False`) inside the running container, confirming the deployed runtime matches source."
    - "CHANGELOG.md has a populated [1.5.4] - 2026-05-22 entry covering TD-09..TD-14 + WR-01 from Phase 1084 code review (the Unreleased block moves down to [1.5.4])."
    - "Sequential `uv run pytest backend/` passes at the v1018 baseline level (3025+ passed / 0 failed / ~38 skipped; Phase 1085 noted 3032/0/38 as the new baseline)."
    - "`npm run e2e:smoke:builder` exits green (matches v1017/v1018 baseline of 25-26 passed / 1 skipped)."
    - "A 5-surface Playwright MCP smoke checklist is documented in the close-gate summary for the orchestrator to execute (this plan does NOT run the MCP smoke itself — the orchestrator handles that via the checkpoint)."
    - "REQUIREMENTS.md TD-14 row is flipped to [x] + Complete in the same commit as this plan's SUMMARY (per the new executor rule established by Plan 1086-01)."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Public release notes for v1.5.4 covering all six TD items + WR-01"
      contains: "[1.5.4] - 2026-05-22"
    - path: ".planning/STATE.md"
      provides: "Milestone-shipped state for v1019 — status=shipped, last_activity timestamp, tag pair recorded"
      contains: "shipped"
    - path: ".planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md"
      provides: "Close-gate verification record — TD-14 docker probe result, pytest sequential baseline, e2e:smoke:builder result, 5-surface MCP checklist for the orchestrator"
      contains: "ssl=False"
      min_lines: 40
    - path: ".planning/REQUIREMENTS.md"
      provides: "TD-14 traceability row flipped to Complete + checkbox flipped to [x]"
      contains: "- [x] **TD-14**"
  key_links:
    - from: ".planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md"
      to: "backend/app/core/config.py"
      via: "docker exec grep probe captures the literal line containing connect_args[\"ssl\"] = False from inside the running api container"
      pattern: "ssl=False|ssl.*=.*False"
    - from: "CHANGELOG.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the [1.5.4] CHANGELOG entry covers all six v1019 TD items (TD-09..TD-14) and lists them by their REQUIREMENTS.md IDs"
      pattern: "TD-09|TD-10|TD-11|TD-12|TD-13|TD-14"
---

<objective>
Close TD-14 by rebuilding api/worker containers + probing the running runtime to confirm v1018 Phase 1080-02's `ssl=False` fix is live, then run the v1019 close-gate (CHANGELOG `[1.5.4]`, sequential pytest, e2e:smoke:builder, 5-surface Playwright MCP smoke), and flip REQUIREMENTS.md TD-14 to Complete.

Purpose: v1018 audit ran against an 8-hour-old running stack that predated the Phase 1080-02 source fix. The fix was correct in source (and 9 unit tests pinned it) but no one verified the deployed image carried it. TD-14 closes that runtime-vs-source gap by rebuilding both images and probing the running container. The same plan also runs the full close-gate so v1019 ships in a single commit chain.

Output: A populated CHANGELOG `[1.5.4]` block, a CLOSE-GATE.md summary recording the docker probe + pytest baseline + e2e result + the 5-surface MCP checklist, a flipped REQUIREMENTS.md, and an updated STATE.md. This plan does NOT cut tags — the orchestrator handles `git tag v1019` + `git tag v1.5.4` after this plan's close-gate passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/1086-process-tightening-close-gate/1086-CONTEXT.md
@.planning/phases/1086-process-tightening-close-gate/1086-01-SUMMARY.md
@.planning/milestones/v1018-MILESTONE-AUDIT.md
@CHANGELOG.md
@docker-compose.yml

<interfaces>
<!-- TD-14 probe target — line + file -->

The TD-14 probe targets `backend/app/core/config.py` inside the `database_connect_args` property. The relevant line (verified at planning time):

```
# backend/app/core/config.py
305:    @property
306:    def database_connect_args(self) -> dict:
307:        connect_args: dict = {}
308:        if self.database_ssl_mode == "disable":
309:            connect_args["ssl"] = False
310:        elif self.database_ssl_mode == "prefer":
311:            connect_args["ssl"] = "prefer"
```

The TD-14 probe runs:

```bash
docker exec geolens-api-1 grep -n 'ssl.*=.*False' app/core/config.py
```

Expected output (exit 0, single match):

```
309:            connect_args["ssl"] = False
```

If the grep returns zero matches, the rebuild did NOT pick up the v1018 source — TD-14 fails and the orchestrator must investigate (cached layer? wrong dockerfile target? bind-mount masking?). If the grep returns a match but on a different line, the source has drifted since planning time — note in CLOSE-GATE.md and continue.

The docker container name is `geolens-api-1` (compose default for service `api` in project `geolens`). If `docker ps --format '{{.Names}}'` shows a different name (e.g., `geolens_api_1` with underscore separator on older compose, or a custom project prefix), use the actual name from `docker ps`.

CHANGELOG [1.5.4] shape — model after the existing [1.5.3] block at CHANGELOG.md:14:

```
## [1.5.4] - 2026-05-22

### Frontend hygiene (v1019 milestone — Phase 1084)

- **TD-09:** [summary citing the actual fix + commit]
- **TD-11:** [summary]
- **TD-12:** [summary]

### Test infrastructure (Phase 1085)

- **TD-10:** [summary including chosen fix shape from PYTEST-XDIST-SPIKE-v1019.md + WR-01 from Phase 1084 code review if applicable]

### Process tightening (Phase 1086)

- **TD-13:** [summary citing .planning/retros/v1019-process.md + the three additive skill rules]

### Runtime symmetry (Phase 1086)

- **TD-14:** [summary citing the docker compose up -d --build api worker probe + the line-309 grep result]

### Internal

- 3 phases (1084-1086), 6 requirements (TD-09..TD-14), [WR-01] from Phase 1084 code review.
- Tag: `v1019` (local) + `v1.5.4` (public).
```

Pull actual TD-09..TD-13 fix details from each phase's SUMMARY.md (`.planning/phases/1084-*/`, `.planning/phases/1085-*/`, `.planning/phases/1086-process-tightening-close-gate/1086-01-SUMMARY.md`). The executor MUST read the per-phase SUMMARYs before writing the CHANGELOG entry — paraphrasing from REQUIREMENTS.md alone is the v1018 TD-02/03 anti-pattern Plan 1086-01 just fixed.
</interfaces>
</context>

<read_first>
- backend/app/core/config.py (lines 300-330 — confirm line 309 still contains `connect_args["ssl"] = False` at execution time; capture the actual line number if it has drifted since planning)
- docker-compose.yml (lines 78-330 — confirm `api` + `worker` service names + the relevant build context match planning-time assumptions; the build commands target both `services.api` and `services.worker`)
- CHANGELOG.md (the first ~120 lines — read the existing `[1.5.3]` block at line 14 verbatim to model the `[1.5.4]` entry shape; do not reinvent the section structure)
- .planning/phases/1084-*/1084-*-SUMMARY.md (all SUMMARY files for TD-09 + TD-11 + TD-12 fixes — needed to write accurate CHANGELOG bullets)
- .planning/phases/1085-*/1085-*-SUMMARY.md (TD-10 spike + fix SUMMARYs — needed for the chosen fix shape and any WR-01 inline fix from Phase 1084 code review)
- .planning/phases/1086-process-tightening-close-gate/1086-01-SUMMARY.md (TD-13 SUMMARY — for the CHANGELOG `Process tightening` block)
- .planning/STATE.md (read current state so the shipped-status update preserves all other frontmatter fields)
- .planning/REQUIREMENTS.md (find the TD-14 row in `## Traceability` and the TD-14 checkbox in the body — both need flipping)
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: TD-14 runtime symmetry probe — rebuild api+worker and grep the running container</name>
  <files>.planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md</files>
  <action>This is the TD-14 closure. Execute the runtime-symmetry probe end-to-end and record results to a new file at `.planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md`. Use the Write tool (NEW file).

Step 1 — rebuild both images:
```bash
docker compose up -d --build api worker
```
This rebuilds api + worker from the current source tree (which carries v1018 Phase 1080-02's `ssl=False` on the disable branch). It does NOT recreate db, titiler, or frontend — those are unaffected by the source change. Wait for both services to report healthy via `docker compose ps`.

Step 2 — confirm the running container names:
```bash
docker ps --filter "name=geolens" --format '{{.Names}}'
```
Capture the actual api container name (expected `geolens-api-1`, but if compose project prefix or separator differs, use the actual name).

Step 3 — probe the running container:
```bash
docker exec <api-container-name> grep -n 'ssl.*=.*False' app/core/config.py
```
Expected: one line printed, matching `309:            connect_args["ssl"] = False` (line number may differ slightly if config.py has drifted since planning — that's fine, record the actual line). If grep returns zero matches OR a non-zero exit code, the rebuild did NOT pick up the source fix. Investigate: check `docker compose config` for stale bind-mounts that might mask the baked-in source, check `docker image ls` for the built image age, and re-run `docker compose up -d --build --no-cache api worker` if needed.

Step 4 — also probe worker (same image at the worker service):
```bash
docker exec <worker-container-name> grep -n 'ssl.*=.*False' app/core/config.py
```
The worker uses a different Dockerfile target (`worker` vs `api`) but the same source layer for `app/core/config.py` — so this grep should return the same line as the api probe. If api passes and worker fails, the worker target's Dockerfile is excluding `app/core/config.py` somehow — record and surface to orchestrator.

Step 5 — write `.planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md` with the following structure (use these exact section headings):

```
# Phase 1086-02 Close Gate Record

**Milestone:** v1019 Hygiene Tail — v1018 Frontend + xdist + Process
**Plan:** 1086-02 TD-14 Runtime Symmetry + Close Gate
**Date:** 2026-05-22

## TD-14 Runtime Symmetry Probe

### Step 1: Rebuild
- Command: `docker compose up -d --build api worker`
- Result: [PASS or FAIL with details]
- api healthy at: [timestamp]
- worker healthy at: [timestamp]

### Step 2: Container Names
- api container: [actual name from docker ps]
- worker container: [actual name from docker ps]

### Step 3: api Probe
- Command: `docker exec <name> grep -n 'ssl.*=.*False' app/core/config.py`
- Output (verbatim):
  ```
  [line number]:            connect_args["ssl"] = False
  ```
- Exit code: [0 expected]

### Step 4: worker Probe
- Command: `docker exec <name> grep -n 'ssl.*=.*False' app/core/config.py`
- Output (verbatim): [same line as api]
- Exit code: [0 expected]

### TD-14 Disposition
PASSED — the v1018 Phase 1080-02 `ssl=False` line is present in both the api and worker container images. Source-runtime symmetry confirmed.

## Close-Gate Results

(Tasks 2-4 populate the rest of this file.)
```

The file ends at "## Close-Gate Results" — subsequent tasks append to it.</action>
  <verify>
    <automated>test -f .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md &amp;&amp; grep -q 'TD-14 Runtime Symmetry Probe' .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md &amp;&amp; grep -q 'ssl=False\|ssl.*=.*False' .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md &amp;&amp; grep -q 'PASSED' .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md &amp;&amp; docker compose ps --filter status=running --format '{{.Service}}' | grep -q '^api$' &amp;&amp; docker compose ps --filter status=running --format '{{.Service}}' | grep -q '^worker$'</automated>
  </verify>
  <acceptance_criteria>
    - Both `api` and `worker` services are running (status=running per `docker compose ps`).
    - Both containers return a non-empty grep match for `ssl.*=.*False` against `app/core/config.py` (exit 0 + at least one line of output).
    - `.planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md` exists with the TD-14 section populated, the actual line number recorded, both container names recorded, and a disposition of PASSED.
    - The probe output is captured verbatim (not paraphrased) — this is the artifact that proves TD-14 closure.
  </acceptance_criteria>
  <done>TD-14 runtime symmetry is verified: both rebuilt containers carry the `connect_args["ssl"] = False` line on the disable branch. CLOSE-GATE.md has the verbatim probe output and a PASSED disposition. Source-runtime gap from v1018 audit is closed.</done>
</task>

<task type="auto">
  <name>Task 2: Write CHANGELOG.md [1.5.4] entry covering TD-09..TD-14 + WR-01</name>
  <files>CHANGELOG.md</files>
  <action>EDIT (not Write) the existing `CHANGELOG.md`. Read the existing `[1.5.3] - 2026-05-21` block at line 14 (already in context — model the new `[1.5.4]` block after it). The current `[Unreleased]` block at line 12 should remain (it's the rolling buffer for post-tag work); insert the new `[1.5.4] - 2026-05-22` block immediately AFTER `[Unreleased]` and BEFORE `[1.5.3]`.

Before drafting the entry, READ the per-phase SUMMARYs cited in `<read_first>`:
- `.planning/phases/1084-*/1084-*-SUMMARY.md` — TD-09 + TD-11 + TD-12 actual fixes + commits
- `.planning/phases/1085-*/1085-*-SUMMARY.md` — TD-10 spike result + chosen fix shape + any Phase 1084 code-review WR-01
- `.planning/phases/1086-process-tightening-close-gate/1086-01-SUMMARY.md` — TD-13 four-file deliverable
- `.planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md` (just written in Task 1) — TD-14 probe result

Then write the [1.5.4] entry. Required sections (match the [1.5.3] shape):

```
## [1.5.4] - 2026-05-22

### Frontend hygiene (v1019 milestone — Phase 1084)

- **TD-09:** [exact summary from Phase 1084 TS-error SUMMARY: "Resolved 36 pre-existing TypeScript errors across 14 untouched frontend test files; `cd frontend && npm run typecheck` now exits 0 with zero `@ts-expect-error`/`@ts-ignore` suppressions added." Cite the per-file fixes from the SUMMARY if listed.] (commit `<hash>`)
- **TD-11:** [exact summary from Phase 1084 /maps/new SUMMARY: which fix shape (route redirect vs in-component guard) landed, what the Playwright MCP network-log gate asserts.] (commit `<hash>`)
- **TD-12:** [exact summary from Phase 1084 /api/api/ SUMMARY: where the leading /api was dropped (frontend client vs route), and the grep gate against the network log.] (commit `<hash>`)

### Test infrastructure (Phase 1085)

- **TD-10:** [exact summary from Phase 1085 SUMMARYs: cite the SPIKE doc at `.planning/audits/PYTEST-XDIST-SPIKE-v1019.md`, the chosen fix shape (a/b/c — per-worker pool sizing, max_connections bump, or `-n` cap), the measured Postgres `max_connections` + per-worker concurrent connection count from the spike, the new sequential baseline (3032/0/38 per CONTEXT.md), and any WR-01 inline fix from the Phase 1084 code review if it landed in Phase 1085.] (commits `<spike-hash>` + `<fix-hash>`)

### Process tightening (Phase 1086)

- **TD-13:** Committed `.planning/retros/v1019-process.md` covering the three v1018 documentation-drift incidents (TD-02/03 paraphrased test names, tasks_common.py path+line drift, Plan 1081-02 SUMMARY checkbox-flip miss). Additively extended three global GSD skill files at `~/.claude/`: `agents/gsd-planner.md` (new `<req_citation_pinning>` section requiring `path::TestClass::test_name` node-IDs validated against `git grep`), `agents/gsd-executor.md` (new `<requirements_traceability_flip>` section requiring REQUIREMENTS.md checkbox + traceability row flip in the same commit as the SUMMARY write), and `templates/requirements.md` (new "Code-Pinned Examples" subsection demonstrating node-ID form). (commit `<hash>`)

### Runtime symmetry (Phase 1086)

- **TD-14:** Rebuilt `api` + `worker` container images via `docker compose up -d --build api worker` and probed the running runtime via `docker exec <api-container> grep -n 'ssl=False' app/core/config.py` — confirmed line `connect_args["ssl"] = False` is live in both containers. Source-runtime gap from v1018 audit (8-hour drift) is closed. Probe artifact at `.planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md`. (no code commit; verification-only)

### Internal

- 3 phases (1084-1086), 6 requirements (TD-09..TD-14) closed. [Note any WR-01 inline fix from Phase 1084 code review if it landed.]
- Tag: `v1019` (local) + `v1.5.4` (public).
```

Replace every `[...]` placeholder with the actual content sourced from each SUMMARY. Replace every `<hash>` with the actual commit SHA from the SUMMARY's commit list. The TD-14 bullet's actual line number comes from Task 1's CLOSE-GATE.md probe output.

Note: Per the new gsd-executor rule from Plan 1086-01, this commit will ALSO flip REQUIREMENTS.md TD-14 to `[x]` + `Complete` (handled in Task 6).</action>
  <verify>
    <automated>grep -q '## \[1.5.4\] - 2026-05-22' CHANGELOG.md &amp;&amp; grep -q 'TD-09' CHANGELOG.md &amp;&amp; grep -q 'TD-10' CHANGELOG.md &amp;&amp; grep -q 'TD-11' CHANGELOG.md &amp;&amp; grep -q 'TD-12' CHANGELOG.md &amp;&amp; grep -q 'TD-13' CHANGELOG.md &amp;&amp; grep -q 'TD-14' CHANGELOG.md &amp;&amp; grep -q 'v1019' CHANGELOG.md &amp;&amp; grep -q 'v1.5.4' CHANGELOG.md &amp;&amp; awk '/^## \[1.5.4\]/{found=1} /^## \[1.5.3\]/{if(found){print "OK"; exit 0} else {exit 1}}' CHANGELOG.md | grep -q OK</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` contains a `## [1.5.4] - 2026-05-22` heading.
    - The new block sits AFTER `## [Unreleased]` and BEFORE `## [1.5.3]` (verified by the awk check).
    - The block names all six TD requirements (TD-09, TD-10, TD-11, TD-12, TD-13, TD-14).
    - The block names both tags (`v1019` local, `v1.5.4` public).
    - The block follows the [1.5.3] section shape (per-phase subsections + `### Internal` footer).
    - Every `[...]` placeholder from the template is replaced with actual content from SUMMARYs.
  </acceptance_criteria>
  <done>CHANGELOG.md carries a populated `[1.5.4] - 2026-05-22` entry covering all six TD items and both tag names, modeled after the [1.5.3] block.</done>
</task>

<task type="auto">
  <name>Task 3: Run sequential pytest + e2e:smoke:builder close-gate verification</name>
  <files>.planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md</files>
  <action>Run the two automated close-gate verifications and append results to `.planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md` (which already exists from Task 1). Use the Edit tool to append a new section (anchor on the existing "## Close-Gate Results" heading written in Task 1).

Step 1 — sequential pytest from the repo root:
```bash
uv run pytest backend/ -p no:cacheprovider 2>&1 | tail -50
```
This must complete WITHOUT xdist (sequential mode). Capture the final summary line (e.g., `3032 passed, 38 skipped in 540.12s`). The gate is: passed >= 3025 AND failed == 0. Phase 1085 noted 3032/0/38 as the new baseline; minor drift (e.g., 3033 or 3034 because Phase 1084 added tests) is acceptable as long as failed == 0.

Step 2 — e2e smoke:
```bash
cd frontend && npm run e2e:smoke:builder 2>&1 | tail -30
```
This must exit 0. The expected shape is 25-26 passed / 1 skipped (matches v1017/v1018 baseline).

Step 3 — append to CLOSE-GATE.md by inserting a new section under the existing "## Close-Gate Results" heading:

```
## Close-Gate Results

### Sequential pytest

- Command: `uv run pytest backend/`
- Result: [N passed / M failed / K skipped] in [T] seconds
- Verdict: [PASSED if failed == 0 AND passed >= 3025, else FAILED]
- vs v1018 baseline (3025/0/38): [+X passed, +Y skipped — same shape]
- vs v1085 new baseline (3032/0/38): [+X passed, +Y skipped]

### Frontend e2e:smoke:builder

- Command: `cd frontend && npm run e2e:smoke:builder`
- Result: [N passed / M failed / K skipped]
- Verdict: [PASSED if exit 0, else FAILED]
- vs v1017/v1018 baseline (25-26/0/1): [matches | drift noted]

### Frontend typecheck (TD-09 close-gate spot-check)

- Command: `cd frontend && npm run typecheck`
- Result: exit [code]
- Verdict: [PASSED if exit 0, else FAILED — TD-09 regression]
```

If any gate FAILED, STOP and surface to the orchestrator — do NOT proceed to Task 4 (MCP smoke checklist) until the failure is investigated. Sequential pytest failures may indicate Phase 1085's xdist fix degraded sequential mode — that would block the v1019 close.</action>
  <verify>
    <automated>grep -q '### Sequential pytest' .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md &amp;&amp; grep -q '### Frontend e2e:smoke:builder' .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md &amp;&amp; grep -q 'PASSED' .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md &amp;&amp; ! grep -q 'FAILED' .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLOSE-GATE.md now contains a populated "## Close-Gate Results" section with three subsections: sequential pytest, e2e:smoke:builder, and frontend typecheck.
    - Sequential pytest result is recorded with passed/failed/skipped counts and the verdict line.
    - e2e:smoke:builder result is recorded with passed/failed/skipped counts and the verdict line.
    - All three verdicts are PASSED (no FAILED disposition in the file — if FAILED appeared, the executor stopped and surfaced to the orchestrator, and this task does not proceed).
    - Counts are recorded against both v1018 baseline (3025/0/38) and v1085 new baseline (3032/0/38) so any regression is immediately visible.
  </acceptance_criteria>
  <done>Two automated close-gates are green: sequential pytest passes at v1018+v1085 baseline with zero failures, e2e:smoke:builder exits 0 at the v1017/v1018 expected shape. Results recorded verbatim in CLOSE-GATE.md.</done>
</task>

<task type="auto">
  <name>Task 4: Document the 5-surface Playwright MCP smoke checklist for the orchestrator</name>
  <files>.planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md</files>
  <action>Append a "## Playwright MCP Smoke Checklist (for orchestrator)" section to `.planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md`. This plan does NOT run the MCP smoke — the orchestrator runs it via the checkpoint task that follows. This task documents what surfaces to visit and what assertions each surface must pass.

Use the Edit tool to append. The new section:

```
## Playwright MCP Smoke Checklist (for orchestrator)

The orchestrator runs this checklist via Playwright MCP against `http://localhost:8080` after this plan's automated close-gate passes. Five surfaces, zero console errors per surface, zero failed network requests per surface. Aggregate verdict: 5/5 PASS.

| Surface | URL | Pass Criteria |
|---------|-----|---------------|
| 1. Landing / catalog list | `http://localhost:8080/` | Page renders; 0 console errors; 0 failed network requests |
| 2. Maps list | `http://localhost:8080/maps` | Page renders; auth gate works or anonymous view loads; 0 console errors; 0 failed network requests; NO `/api/api/` patterns in network log (TD-12 regression check) |
| 3. Dataset detail | `http://localhost:8080/datasets/<any-existing-uuid>` | Pick any uuid from a `/datasets` API response; page renders; 0 console errors |
| 4. Map builder | `http://localhost:8080/maps/<any-existing-uuid>` (or `/maps/new` after login) | Editor scene loads; 0 console errors; NO 422 console-noise on the path `/maps/new` (TD-11 regression check) |
| 5. Map viewer | `http://localhost:8080/maps/<any-existing-uuid>` (viewer mode if separate URL, otherwise same as 4) | Viewer renders the saved map state; 0 console errors |

### Orchestrator instructions

After this plan's SUMMARY commit lands, the orchestrator should:
1. Confirm the stack is up (`docker compose ps` shows api + worker + frontend healthy).
2. Run the 5-surface Playwright MCP smoke with `--use-playwright-mcp`.
3. Append the result (5/5 PASS or N/5 with details) to this file as a new `## MCP Smoke Result` section.
4. If 5/5 PASS, proceed to tag-cutting (`git tag v1019 <SHA>` + `git tag v1.5.4 <SHA>` — same SHA, the post-baseline commit).
5. If any surface fails with 422s or `/api/api/` patterns, that indicates a TD-11 or TD-12 regression — block tag-cutting and surface to user.

### TD-09/TD-11/TD-12 regression checks (called out specifically)

- TD-09: surface 4 + 5 must show 0 console errors of shape `TS<n>` or `TypeError` (the 36 TS-error fix must not have introduced runtime regressions).
- TD-11: surface 4's `/maps/new` path must show 0 `422` responses in the network log.
- TD-12: surface 2's network log must show 0 URL patterns containing `/api/api/`.
```

Append this verbatim — the orchestrator reads this checklist before running the MCP smoke.</action>
  <verify>
    <automated>grep -q 'Playwright MCP Smoke Checklist' .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md &amp;&amp; grep -q 'localhost:8080' .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md &amp;&amp; grep -qE '5(/| of )5' .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md &amp;&amp; grep -q '/api/api/' .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md &amp;&amp; grep -q '/maps/new' .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLOSE-GATE.md now contains a "## Playwright MCP Smoke Checklist (for orchestrator)" section.
    - The section lists 5 surfaces with URLs on `localhost:8080` and pass criteria.
    - The section explicitly calls out the TD-11 (/maps/new 422) and TD-12 (/api/api/) regression checks.
    - The section instructs the orchestrator to append a "MCP Smoke Result" section after running and to block tag-cutting if any surface fails.
  </acceptance_criteria>
  <done>The MCP smoke checklist is documented in CLOSE-GATE.md. The orchestrator now has a deterministic protocol for the 5-surface live smoke and the regression checks for TD-11/TD-12.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Checkpoint — orchestrator runs the 5-surface Playwright MCP smoke</name>
  <files>.planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md</files>
  <what-built>
    Plan 1086-02 has completed the autonomous portion of the close gate:
    - TD-14 docker-rebuild probe PASSED (Task 1)
    - CHANGELOG `[1.5.4]` entry written covering TD-09..TD-14 (Task 2)
    - Sequential pytest + e2e:smoke:builder PASSED (Task 3)
    - 5-surface MCP smoke checklist documented in CLOSE-GATE.md (Task 4)

    The Playwright MCP smoke itself runs OUTSIDE the executor (the user/orchestrator drives MCP, not the executor). This checkpoint hands off control so the orchestrator can run the 5-surface smoke and append the result before this plan's SUMMARY commit lands.
  </what-built>
  <how-to-verify>
    The orchestrator runs the 5-surface Playwright MCP smoke per the checklist in `.planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md` "## Playwright MCP Smoke Checklist" section. Steps:

    1. Confirm the stack is healthy:
       ```bash
       docker compose ps
       ```
       All five core services (db, api, worker, titiler, frontend) should show `running (healthy)`.

    2. Run the MCP smoke against `http://localhost:8080`. Visit each of the 5 surfaces in order; for each, capture:
       - Page rendered (yes/no)
       - Console error count (target: 0)
       - Failed network request count (target: 0)
       - Surface-specific regression check (TD-11 `/maps/new` 422 noise on surface 4; TD-12 `/api/api/` patterns on surface 2)

    3. Append the result to CLOSE-GATE.md as a new section:
       ```
       ## MCP Smoke Result

       Run: [timestamp]
       Aggregate: [N]/5 surfaces PASS

       | Surface | Verdict | Console errors | Failed requests | Notes |
       |---------|---------|----------------|-----------------|-------|
       | 1. Landing | PASS/FAIL | 0 | 0 | |
       | 2. Maps list | PASS/FAIL | 0 | 0 | TD-12 check: 0 /api/api/ patterns |
       | 3. Dataset detail | PASS/FAIL | 0 | 0 | |
       | 4. Map builder | PASS/FAIL | 0 | 0 | TD-11 check: 0 422 on /maps/new |
       | 5. Map viewer | PASS/FAIL | 0 | 0 | |

       Overall: [PASSED if 5/5, else NEEDS-FIX with details]
       ```

    4. If 5/5 PASS, type `approved` and the orchestrator proceeds to cut tags (`git tag v1019 <SHA>` + `git tag v1.5.4 <SHA>` at the same commit).

    5. If any surface FAILED, describe what failed and the orchestrator decides whether to:
       - Fix inline (small enough that it lands in the same plan as a follow-up task)
       - Defer to a v1019.1 hotfix milestone
       - Block the v1019 tag entirely
  </how-to-verify>
  <action>Pause executor. Surface the checkpoint to the orchestrator with the link to CLOSE-GATE.md's "## Playwright MCP Smoke Checklist" section and the resume-signal expected. Do not run MCP commands from inside the executor — MCP smoke is driven by the orchestrator/user against the live `localhost:8080` stack. Wait for the resume signal (`approved` or failure description) before proceeding to Task 6.</action>
  <verify>
    <automated>grep -q 'Playwright MCP Smoke Checklist' .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md</automated>
    <human-check>Orchestrator has run the 5-surface MCP smoke per the checklist and appended a "## MCP Smoke Result" section to CLOSE-GATE.md with verdict 5/5 PASS. Resume signal `approved` received.</human-check>
  </verify>
  <done>Orchestrator has confirmed 5/5 MCP smoke PASS (or surfaced specific failures for routing). The executor resumes with Task 6 only after `approved` signal.</done>
  <resume-signal>Type `approved` to indicate 5/5 MCP smoke PASS and authorize the orchestrator to cut tags. Or describe the failures and the orchestrator will route accordingly.</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Flip REQUIREMENTS.md TD-14 + Update STATE.md to shipped</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>Two-file update. This is the executor's standard plan-close workflow PER THE NEW RULE established by Plan 1086-01 — REQUIREMENTS.md flip + STATE.md update both land in the SAME commit as this plan's SUMMARY.md write (handled by the standard SUMMARY workflow downstream).

Step 1 — flip REQUIREMENTS.md TD-14:
- Edit `.planning/REQUIREMENTS.md` (use Edit, not Write).
- Find the body line `- [ ] **TD-14**: Verify TD-07 (...)`. Flip to `- [x] **TD-14**: Verify TD-07 (...)`.
- Find the Traceability table row `| TD-14 | Phase 1086 | Pending |`. Flip the third column to `Complete`.

Note: Plan 1086-01 closes TD-13 — the executor of Plan 1086-01 must perform the same flip for TD-13 (per the new rule that plan's SUMMARY-commit task is establishing). If Plan 1086-01's executor did NOT flip TD-13 (e.g., if execution happened before the gsd-executor.md update propagated), the executor of THIS plan must also flip TD-13 here as a defensive measure. Check first:
```bash
grep '^- \[ \] \*\*TD-13' .planning/REQUIREMENTS.md && echo "TD-13 NOT flipped — flipping here as defensive measure" || echo "TD-13 already flipped — good"
```
If TD-13 is still `[ ]`, flip it as well (same shape: body checkbox + traceability row).

Step 2 — update STATE.md:
- Edit `.planning/STATE.md` (use Edit, not Write).
- Update frontmatter `status:` from `Roadmap ready` to `shipped`.
- Update `stopped_at:` to `"v1019 close-gate complete; orchestrator to cut tags v1019 + v1.5.4"`.
- Update `last_updated:` to the current ISO-8601 timestamp.
- Update `last_activity:` to `2026-05-22 — Phase 1086 close-gate PASSED; v1019 ready for tag cut`.
- Update `progress.completed_phases` from 2 to 3.
- Update `progress.completed_plans` to the new total (count of all PLAN files across 1084-01, 1084-02, 1084-03, 1085-01, 1085-02, 1086-01, 1086-02 = 7).
- Update `progress.percent` accordingly.

In the body of STATE.md, update the "Current Position" block to reflect the shipped state:
```
Phase: 1086 — shipped (close-gate PASSED, awaiting tag cut by orchestrator)
Plan: 1086-02 — complete
Status: shipped
Last activity: 2026-05-22 — Phase 1086 close-gate PASSED; v1019 ready for tag cut
```

Add a new "Last Shipped Milestone" entry under the existing block (but DO NOT overwrite v1018 — append v1019 as the next-shipped row, OR adjust the structure if STATE.md only tracks one shipped milestone at a time — read the current file shape first and match it).

Do NOT touch the "Accumulated Context" section's decisions or pending todos.

Do NOT cut tags in this task — the orchestrator handles `git tag v1019` and `git tag v1.5.4` after this plan's SUMMARY commit lands and after the MCP smoke checkpoint (Task 5) returns 5/5 PASS.</action>
  <verify>
    <automated>grep -q '^- \[x\] \*\*TD-14\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -qE '\| TD-14 \| Phase 1086 \| Complete \|' .planning/REQUIREMENTS.md &amp;&amp; grep -q '^- \[x\] \*\*TD-13\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -qE '\| TD-13 \| Phase 1086 \| Complete \|' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'status: shipped' .planning/STATE.md &amp;&amp; grep -q 'v1019 ready for tag cut\|v1019 close-gate' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` body has `- [x] **TD-14**` (flipped from `[ ]`).
    - `.planning/REQUIREMENTS.md` Traceability table has `| TD-14 | Phase 1086 | Complete |` (flipped from `Pending`).
    - `.planning/REQUIREMENTS.md` body has `- [x] **TD-13**` (flipped — either by Plan 1086-01's executor per the new rule, or defensively here).
    - `.planning/REQUIREMENTS.md` Traceability table has `| TD-13 | Phase 1086 | Complete |`.
    - `.planning/STATE.md` frontmatter `status:` is `shipped`.
    - `.planning/STATE.md` references v1019 close-gate / tag-ready state in last_activity.
    - No tag-cutting commands were run by this task (tag-cutting is the orchestrator's job — verified by absence of any `git tag` invocation in task actions).
  </acceptance_criteria>
  <done>REQUIREMENTS.md TD-13 + TD-14 are both flipped to `[x]` / `Complete`. STATE.md reflects shipped state for v1019. No tags cut by the executor — the orchestrator handles tag cutting after the SUMMARY commit lands and the MCP smoke checkpoint passes.</done>
</task>

</tasks>

<verification>
After all six tasks complete:

```bash
# TD-14 probe artifacts
test -f .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md
grep -q 'TD-14 Runtime Symmetry Probe' .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md
grep -q 'ssl=False\|ssl.*=.*False' .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md

# Close-gate results
grep -q '### Sequential pytest' .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md
grep -q '### Frontend e2e:smoke:builder' .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md
grep -q 'Playwright MCP Smoke Checklist' .planning/phases/1086-process-tightening-close-gate/1086-02-CLOSE-GATE.md

# CHANGELOG [1.5.4] populated
grep -q '## \[1.5.4\] - 2026-05-22' CHANGELOG.md
for req in TD-09 TD-10 TD-11 TD-12 TD-13 TD-14; do grep -q "$req" CHANGELOG.md || echo "MISSING $req"; done

# REQUIREMENTS.md flipped
grep -q '^- \[x\] \*\*TD-13\*\*' .planning/REQUIREMENTS.md
grep -q '^- \[x\] \*\*TD-14\*\*' .planning/REQUIREMENTS.md
grep -qE '\| TD-13 \| Phase 1086 \| Complete \|' .planning/REQUIREMENTS.md
grep -qE '\| TD-14 \| Phase 1086 \| Complete \|' .planning/REQUIREMENTS.md

# STATE.md updated
grep -q 'status: shipped' .planning/STATE.md
```
</verification>

<success_criteria>
- TD-14 is closed: docker-rebuilt api + worker carry the `ssl=False` line; CLOSE-GATE.md records the verbatim probe output.
- CHANGELOG.md `[1.5.4] - 2026-05-22` block is populated with accurate bullets for TD-09..TD-14 sourced from per-phase SUMMARYs (not paraphrased from REQUIREMENTS.md — that's the v1018 anti-pattern).
- Sequential pytest + e2e:smoke:builder both PASS at v1018/v1085 baseline.
- 5-surface Playwright MCP smoke checklist is documented in CLOSE-GATE.md for the orchestrator.
- Orchestrator runs MCP smoke (Task 5 checkpoint), appends result, and ONLY THEN cuts tags `v1019` + `v1.5.4`.
- REQUIREMENTS.md TD-13 + TD-14 both flipped to `[x]` / `Complete` in the same commit as this plan's SUMMARY.
- STATE.md reflects shipped state.
- Plan 1086-02 has NOT cut any tags — that work is orchestrator-only.
</success_criteria>

<output>
Create `.planning/phases/1086-process-tightening-close-gate/1086-02-SUMMARY.md` when done. Include:
- The docker probe verbatim output (api + worker lines)
- The sequential pytest summary line (passed/failed/skipped/seconds)
- The e2e:smoke:builder summary line
- A confirmation that CLOSE-GATE.md exists with all four populated sections (TD-14 probe, sequential pytest, e2e, MCP checklist)
- A confirmation that REQUIREMENTS.md TD-13 + TD-14 are flipped
- A confirmation that STATE.md is updated to shipped
- A handoff note pointing the orchestrator at the MCP smoke checkpoint + the tag-cutting commands to run after 5/5 PASS:
  ```bash
  # After 5/5 MCP smoke PASS:
  git tag v1019 <SHA>
  git tag v1.5.4 <SHA>
  ```
</output>
