---
phase: 1162-frontend-rename
plan: 02
type: execute
wave: 2
depends_on: ["1162-01"]
files_modified:
  # --- Contract types + client + query keys ---
  - frontend/src/types/api.ts
  - frontend/src/api/settings.ts
  - frontend/src/api/maps.ts
  - frontend/src/api/__tests__/maps.normalize.test.ts
  - frontend/src/lib/query-keys.ts
  - frontend/src/lib/normalize-saved-map.ts
  - frontend/src/lib/__tests__/normalize-saved-map.test.ts
  - frontend/src/hooks/use-settings.ts
  # --- Save payload + its test (owns the `widgets:` body key) ---
  - frontend/src/components/builder/hooks/use-builder-save.ts
  - frontend/src/components/builder/hooks/__tests__/use-builder-save.test.ts
  # --- Type-seam files: bound to the renamed Map field + the renamed hook ---
  - frontend/src/components/builder/map-stack.ts
  # --- Co-edited with plan 01 on DIFFERENT tokens (hook rename + Map-field reads) ---
  - frontend/src/components/builder/MapToolbar.tsx
  - frontend/src/pages/MapBuilderPage.tsx
autonomous: true
requirements: [FE-RENAME-03, FE-RENAME-04, FE-RENAME-05]

must_haves:
  truths:
    - "frontend/src/types/api.ts Map type uses `plugins` (not `widgets`); any settings/feature-toggle type uses `enabled_plugins` (not `enabled_widgets`)"
    - "The settings API client calls /settings/enabled-plugins/ (not enabled-widgets) and exports getEnabledPlugins (not getEnabledWidgets)"
    - "TanStack query keys + the useEnabledPlugins hook + the use-builder-save `plugins:` payload key all use plugin vocabulary"
    - "map-stack.ts uses the renamed Map field (map.plugins) + plugin role/entry ids; MapBuilderPage/MapToolbar call useEnabledPlugins and read mapData.plugins"
    - "PHASE TERMINAL GATE: `cd frontend && npm run typecheck` is 0 AND no widget/Widget/enabled_widgets platform token remains in frontend/src outside i18n locale JSON"
    - "The plugin ID string values `measurement` and `legend` are unchanged"
  artifacts:
    - path: "frontend/src/types/api.ts"
      provides: "Map type `plugins?: string[] | null` matching the Phase-1161 contract"
      contains: "plugins"
    - path: "frontend/src/api/settings.ts"
      provides: "getEnabledPlugins() hitting /settings/enabled-plugins/"
      contains: "enabled-plugins"
    - path: "frontend/src/lib/query-keys.ts"
      provides: "query key enabledPlugins: ['settings','enabled-plugins']"
      contains: "enabled-plugins"
  key_links:
    - from: "frontend/src/api/settings.ts"
      to: "backend /api/settings/enabled-plugins/"
      via: "apiFetch path"
      pattern: "enabled-plugins"
    - from: "frontend/src/hooks/use-settings.ts"
      to: "frontend/src/lib/query-keys.ts"
      via: "useEnabledPlugins query key"
      pattern: "enabledPlugins"
---

<objective>
Flip the frontend's API-contract surface to the renamed `plugins` / `enabled_plugins` vocabulary so it matches the Phase-1161 backend (which cut `maps.widgets`→`maps.plugins`, the Map schema field `widgets`→`plugins`, and route `/settings/enabled-widgets/`→`/settings/enabled-plugins/`). Today the frontend is BROKEN against that backend: `types/api.ts` types `widgets?: string[]` (lines 957 & 1035), the settings client calls the old route + exports `getEnabledWidgets`, the query key is `enabledWidgets: ['settings','enabled-widgets']`, and the save payload sends `widgets:`.

Covers FE-RENAME-03 (api types + settings client + query keys), contract-side FE-RENAME-04 (preserve `measurement`/`legend` literals), and FE-RENAME-05 (the phase terminal gate: whole-frontend `npm run typecheck` 0 + zero non-i18n widget tokens + renamed suites green). Runs wave 2, after 1162-01.

This plan also owns the **type-seam** that 1162-01 deliberately left: `map-stack.ts` (its `MapStackMapInput.widgets` field, `'interaction-widgets'` role, `'interactions:widgets'` id, and `map.widgets` read) and the `mapData.widgets`/`map.widgets` reads + the `useEnabledWidgets`→`useEnabledPlugins` hook rename in `MapToolbar.tsx` and `MapBuilderPage.tsx`. Renaming those together with the `types/api.ts` field keeps every commit green.
Output: contract types/client/keys/normalize/save-payload on the plugins contract; map-stack + seam reads flipped; `npm run typecheck` 0; renamed suites green; whole-frontend grep-clean of platform widget tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1161-backend-rename-and-contract/1161-02-SUMMARY.md
@.planning/phases/1162-frontend-rename/1162-01-PLAN.md

<critical_constraints>
1. **Depends on 1162-01** (wave 2). That plan renamed the `map-plugins/` module + `map-plugin-store` + component consumers, and DELIBERATELY left: the `useEnabledWidgets` hook name, the `mapData.widgets`/`map.widgets` Map-response reads, and `map-stack.ts`. THIS plan owns and finishes exactly those. **Co-edit precision:** `MapToolbar.tsx` and `MapBuilderPage.tsx` are touched by BOTH plans on DIFFERENT tokens. Plan 01 already renamed the store/registry imports + `Plugin*` locals there. THIS plan touches ONLY: (a) `useEnabledWidgets`→`useEnabledPlugins` import + call, (b) the residual `enabledWidgets`-derived local that wraps the hook result if plan 01 left it spelled with the hook's vocabulary, (c) `mapData.widgets`→`mapData.plugins` reads. Re-grep both files first; if plan 01 already renamed a token, don't double-edit.

2. **HARD breaking cut — match the backend EXACTLY** (from 1161-02 SUMMARY, verified green):
   - Map request/response field: `plugins` (was `widgets`) — NO alias.
   - Settings feature-toggle field/key: `enabled_plugins`.
   - Public settings route: `/settings/enabled-plugins/` (dual-shape; keep the SAME trailing-slash shape the client uses today — the existing call is `'/settings/enabled-widgets/'` WITH trailing slash → `'/settings/enabled-plugins/'`).

3. **PRESERVE `'measurement'`/`'legend'` ID string literals.** They are array VALUES of the plugins field — they appear in `normalize-saved-map.test.ts`, `maps.normalize.test.ts`, `use-builder-save.test.ts` (`useWidgetStore.getState().open('legend')` → note: plan 01 renamed `useWidgetStore`→`usePluginStore`; THIS plan does not re-touch that, but the `'legend'` literal stays). Rename the FIELD, never the VALUES. Re-grep after.

4. **i18n locale JSON OUT OF SCOPE** (Phase 1163). Do not edit `frontend/src/i18n/locales/` or rename `widgets.*` i18n key strings.

5. **`types/api.ts` provenance:** confirm hand-maintained vs generated before editing — `grep -n "DO NOT EDIT\|@generated\|openapi-typescript\|This file is auto" frontend/src/types/api.ts | head`. The `widgets?: string[] | null` lines (957, 1035) with no banner indicate hand-maintained; edit directly. If a generated banner exists, regenerate via the project's documented command and record it in the SUMMARY (do not hand-edit a generated file). NOTE: the generated SDK lives at `sdks/typescript/` and was already regenerated in Phase 1161 — that is NOT this file.
</critical_constraints>

<interfaces>
<!-- VERIFIED at HEAD da8fccb8. Re-grep before editing. -->

types/api.ts:
  957 : `widgets?: string[] | null;`   (Map response/base type)  → `plugins?: string[] | null;`
  1035: `widgets?: string[] | null;`   (Map create/update type)  → `plugins?: string[] | null;`
  (grep for any `enabled_widgets` on a settings type here too → `enabled_plugins`.)

api/settings.ts:
  60-62: `export async function getEnabledWidgets(): Promise<string[] | null> { return apiFetch<string[] | null>('/settings/enabled-widgets/'); }`
         → `getEnabledPlugins` + `'/settings/enabled-plugins/'`

lib/query-keys.ts:
  137: `enabledWidgets: ['settings', 'enabled-widgets'] as const,`  → `enabledPlugins: ['settings', 'enabled-plugins'] as const,`

hooks/use-settings.ts:
  ~50-55: a `useEnabledWidgets` hook using `queryKey: queryKeys.settings.enabledWidgets` + `getEnabledWidgets`
         → `useEnabledPlugins` + `queryKeys.settings.enabledPlugins` + `getEnabledPlugins`
  (This hook is imported by MapToolbar, MapBuilderPage, use-builder-save, and map-plugins/PluginHost.tsx. Rename ALL call sites — see below.)

api/maps.ts:
  64 : comment `widgets is string[]|null` ; 72: `resp.widgets = mapNorm.widgets;` ; 213-214: comment about SharedMapResponse widgets
         → `resp.plugins = mapNorm.plugins;` + comments to plugins

lib/normalize-saved-map.ts:
  43-44: `/** Widget list... */ widgets: string[] | null;` ; 68: input type `widgets`; 80/133-135/160: `widgets` field + `rawWidgets`/`const widgets`
         → rename field + locals to `plugins`/`rawPlugins`. KEEP any `'measurement'`/`'legend'` defaults if present.

components/builder/hooks/use-builder-save.ts:
  12: `import { useEnabledWidgets } from '@/hooks/use-settings'`  → useEnabledPlugins
  18: `import { useWidgetStore } from '@/stores/map-plugin-store'`  ← NOTE: plan 01 already changed the PATH to map-plugin-store AND useWidgetStore→usePluginStore. Re-grep: it should read `usePluginStore` already. Do NOT re-edit if so.
  20: `import { getDefaultWidgetIds, resolveAvailableWidgetIds, sameWidgetIds } from '@/components/map-plugins'` ← plan 01 already renamed these to getDefaultPluginIds/resolveAvailablePluginIds/samePluginIds. Re-grep; don't double-edit.
  235: `function resolveWidgetsPayload(` → `resolvePluginsPayload` ; 238 param `enabledWidgetIds`→`enabledPluginIds` ; 240-246 body uses usePluginStore.getState().activePlugins (plan 01) — verify; 246: `cached?.widgets`→`cached?.plugins`
  422-425: `enabledWidgetsQuery`/`enabledWidgetIds` wrapping useEnabledPlugins → rename
  494: `widgets: resolveWidgetsPayload(...)` → `plugins: resolvePluginsPayload(...)`
  ⇒ The `widgets:` body KEY at 494 and `cached?.widgets` at 246 are the CONTRACT payload — those are THIS plan's core. The store/registry symbols are plan 01's; re-grep to avoid double-edits.

map-stack.ts (type-seam, THIS plan):
  33: role `'interaction-widgets'` → `'interaction-plugins'`
  93: `widgets?: string[];` (MapStackMapInput-adjacent metadata type) → `plugins?: string[];`
  125: `widgets?: string[] | null;` (MapStackMapInput) → `plugins?: string[] | null;`
  160: description string "Popup and widget affordances..." → "plugin affordances" (cosmetic, safe)
  663-681: `const widgets = map.widgets ?? []` + entry id `'interactions:widgets'` + role `'interaction-plugins'` + title `'Map widgets'` + orderLabel `'Widgets'` + badge `${widgets.length} widget${...}` + metadata `widgets,`
         → rename all to plugins. The user-facing title/badge strings ('Map widgets'/'Widgets'/'widget') are hardcoded English here (NOT i18n keys) → change to 'Map plugins'/'Plugins'/'plugin'.

MapToolbar.tsx + MapBuilderPage.tsx (co-edit, hook + seam only):
  - rename `useEnabledWidgets`→`useEnabledPlugins` import + call
  - if plan 01 left `enabledWidgetsQuery`/`enabledWidgetKey`/`savedWidgetKey` or `mapData.widgets` spelled widget, rename now (re-grep — plan 01's interfaces said it would rename the Plugin* locals but KEEP the hook + `mapData.widgets`). Specifically flip `mapData.widgets`→`mapData.plugins` (MapBuilderPage ~244/251/253) now that types/api.ts has `plugins`.

test files (THIS plan): maps.normalize.test.ts (3 refs), normalize-saved-map.test.ts (14 refs), use-builder-save.test.ts (21 refs — but many are `usePluginStore`/`'legend'` already handled by plan 01's store rename; THIS plan updates the `payload.data.widgets`→`payload.data.plugins` assertions + `mockEnabledWidgets`→`mockEnabledPlugins` + `useEnabledWidgets`→`useEnabledPlugins` mock). KEEP `'legend'`/`'measurement'` literals.
</interfaces>

<identifier_mapping>
| Old | New |
|-----|-----|
| Map type field `widgets?: string[]` | `plugins?: string[]` |
| settings field/key `enabled_widgets` | `enabled_plugins` |
| route `'/settings/enabled-widgets/'` | `'/settings/enabled-plugins/'` |
| client fn `getEnabledWidgets` | `getEnabledPlugins` |
| hook `useEnabledWidgets` | `useEnabledPlugins` |
| query key `enabledWidgets: ['settings','enabled-widgets']` | `enabledPlugins: ['settings','enabled-plugins']` |
| save fn `resolveWidgetsPayload` + body key `widgets:` | `resolvePluginsPayload` + `plugins:` |
| normalize field/locals `widgets`/`rawWidgets` | `plugins`/`rawPlugins` |
| maps.ts `resp.widgets`/`mapNorm.widgets` | `resp.plugins`/`mapNorm.plugins` |
| map-stack role `'interaction-widgets'`, id `'interactions:widgets'`, locals/field `widgets`, EN strings 'Map widgets'/'Widgets'/'widget' | `'interaction-plugins'`, `'interactions:plugins'`, `plugins`, 'Map plugins'/'Plugins'/'plugin' |
| Map-response reads `mapData.widgets`/`map.widgets` | `mapData.plugins`/`map.plugins` |
| test mocks `mockEnabledWidgets`, `payload.data.widgets` | `mockEnabledPlugins`, `payload.data.plugins` |

DO NOT rename: string VALUES `'measurement'`/`'legend'`; i18n key strings `widgets.*`; anything already renamed by plan 01 (re-grep before each edit).
</identifier_mapping>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename contract types + settings client + query keys + use-settings hook</name>
  <files>frontend/src/types/api.ts, frontend/src/api/settings.ts, frontend/src/lib/query-keys.ts, frontend/src/hooks/use-settings.ts</files>
  <action>
    1. types/api.ts: confirm provenance (grep banner). If hand-maintained: change both `widgets?: string[] | null;` (957, 1035 — re-grep) → `plugins?: string[] | null;`; rename any `enabled_widgets` settings field → `enabled_plugins`. `grep -nE 'widget|Widget' frontend/src/types/api.ts` → 0 after.
    2. api/settings.ts: `getEnabledWidgets`→`getEnabledPlugins`; `'/settings/enabled-widgets/'`→`'/settings/enabled-plugins/'`.
    3. lib/query-keys.ts: `enabledWidgets: ['settings','enabled-widgets']`→`enabledPlugins: ['settings','enabled-plugins']`.
    4. hooks/use-settings.ts: `useEnabledWidgets`→`useEnabledPlugins`; `queryKeys.settings.enabledWidgets`→`.enabledPlugins`; import `getEnabledWidgets`→`getEnabledPlugins`.
    Do NOT yet edit call sites in other files (next tasks). Typecheck WILL be red here (call sites stale) — that's fine until Task 3.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && ! grep -rqE 'widget|Widget|enabled_widgets|enabled-widgets' frontend/src/types/api.ts frontend/src/api/settings.ts frontend/src/lib/query-keys.ts && grep -q "useEnabledPlugins" frontend/src/hooks/use-settings.ts && grep -q "enabled-plugins" frontend/src/api/settings.ts && grep -qE 'plugins\?: string\[\]' frontend/src/types/api.ts && echo CONTRACT_CORE_OK</automated>
  </verify>
  <done>api.ts/settings.ts/query-keys.ts/use-settings.ts on plugin vocabulary; route is enabled-plugins; hook is useEnabledPlugins. (Call sites still stale — Task 3.)</done>
</task>

<task type="auto">
  <name>Task 2: Rename maps normalize/transform + save payload + their tests</name>
  <files>frontend/src/api/maps.ts, frontend/src/lib/normalize-saved-map.ts, frontend/src/api/__tests__/maps.normalize.test.ts, frontend/src/lib/__tests__/normalize-saved-map.test.ts, frontend/src/components/builder/hooks/use-builder-save.ts, frontend/src/components/builder/hooks/__tests__/use-builder-save.test.ts</files>
  <action>
    Re-grep each file first (some symbols were already renamed by plan 01 — never double-edit).
    1. lib/normalize-saved-map.ts: field `widgets: string[] | null`→`plugins`; locals `rawWidgets`/`const widgets`→`rawPlugins`/`plugins`; JSDoc. KEEP measurement/legend default values if any.
    2. api/maps.ts: `resp.widgets = mapNorm.widgets`→`resp.plugins = mapNorm.plugins`; the SharedMapResponse comments → plugins.
    3. use-builder-save.ts: `useEnabledWidgets`→`useEnabledPlugins` (import+call); `resolveWidgetsPayload`→`resolvePluginsPayload`; param `enabledWidgetIds`→`enabledPluginIds`; `cached?.widgets`→`cached?.plugins`; the body key `widgets:`→`plugins:` (~494); `enabledWidgetsQuery`/`enabledWidgetIds` locals. The store/registry symbols (`usePluginStore`, `getDefaultPluginIds`, `resolveAvailablePluginIds`, `samePluginIds`, `activePlugins`) should ALREADY be plugin-spelled from plan 01 — verify, don't re-edit.
    4. Tests: normalize-saved-map.test.ts (`widgets`→`plugins` field assertions + inputs, KEEP literals); maps.normalize.test.ts (`widgets`→`plugins`); use-builder-save.test.ts (`mockEnabledWidgets`→`mockEnabledPlugins`, `useEnabledWidgets`→`useEnabledPlugins` mock, `payload.data.widgets`→`payload.data.plugins`; KEEP `usePluginStore`/`'legend'`).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && ! grep -rqE 'widgets|Widget|enabledWidget' frontend/src/api/maps.ts frontend/src/lib/normalize-saved-map.ts frontend/src/components/builder/hooks/use-builder-save.ts && grep -q 'plugins:' frontend/src/components/builder/hooks/use-builder-save.ts && grep -rhoE "'(measurement|legend)'" frontend/src/components/builder/hooks/__tests__/use-builder-save.test.ts | sort -u && echo MAPS_SAVE_OK</automated>
  </verify>
  <done>normalize/maps/save-payload + tests on plugins contract; `plugins:` body key set; measurement/legend literals intact.</done>
</task>

<task type="auto">
  <name>Task 3: Flip the type-seam (map-stack + MapToolbar/MapBuilderPage reads), then PHASE GATE typecheck-0 + grep-clean + commit</name>
  <files>frontend/src/components/builder/map-stack.ts, frontend/src/components/builder/MapToolbar.tsx, frontend/src/pages/MapBuilderPage.tsx</files>
  <action>
    Re-grep each file first (plan 01 already renamed the Plugin* locals + store/registry imports in MapToolbar/MapBuilderPage — touch ONLY the residual hook + Map-field tokens).
    1. map-stack.ts: role `'interaction-widgets'`→`'interaction-plugins'`; field `widgets?`→`plugins?` (both ~93 + ~125); `const widgets = map.widgets ?? []`→`const plugins = map.plugins ?? []`; entry id `'interactions:widgets'`→`'interactions:plugins'`; title/badge/orderLabel/metadata + the description string → plugin vocabulary (these are hardcoded EN, not i18n keys).
    2. MapToolbar.tsx: `useEnabledWidgets`→`useEnabledPlugins` import+call; any residual `enabledWidgetsQuery`/`enabledWidgetIds` wrapping the hook result (if plan 01 left them widget-spelled because they derive from the hook) → plugin. KEEP `.has('measurement')/'legend'` + `t('widgets.*')`.
    3. MapBuilderPage.tsx: `useEnabledWidgets`→`useEnabledPlugins`; residual `enabledWidgetsQuery`/`enabledWidgetKey`/`savedWidgetKey` if widget-spelled → plugin; `mapData.widgets`→`mapData.plugins` (~244/251/253 — now that types/api.ts has `plugins`).

    THEN run the PHASE TERMINAL GATE:
    - `cd frontend && npm run typecheck` → MUST be 0.
    - Whole-frontend grep-clean (excluding i18n locales): `grep -rnE 'widget|Widget|enabled_widgets|enabled-widgets' frontend/src --include='*.ts' --include='*.tsx' | grep -v '/i18n/locales/'` → MUST return ONLY i18n KEY-string references inside `t(...)`/`labelKey:` (the `'widgets.*'` keys Phase 1163 owns) and NOTHING else. If any identifier/import/field/type token remains, fix it.
    - Run the affected vitest suites (contract + builder save + map-stack consumers) + a broad smoke.
    Commit only when typecheck is 0 and the grep-clean shows only i18n key strings.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npm run typecheck 2>&1 | tail -5 && echo "--- residual widget tokens (expect ONLY i18n 'widgets.*' key strings) ---" && grep -rnE 'widget|Widget|enabled_widgets|enabled-widgets' src --include='*.ts' --include='*.tsx' | grep -v "/i18n/locales/" | grep -vE "widgets\.[a-zA-Z]" | head -20 ; echo "--- (above should be EMPTY) ---" && npm run test -- src/lib/__tests__/normalize-saved-map.test.ts src/api/__tests__/maps.normalize.test.ts src/components/builder/hooks/__tests__/use-builder-save.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>`npm run typecheck` 0; the only widget-spelled tokens left in frontend/src (excluding i18n locales) are the `widgets.*` i18n KEY strings (Phase 1163); contract + save + normalize suites pass; the frontend now consumes the Phase-1161 `plugins`/`enabled_plugins` contract end-to-end.</done>
</task>

</tasks>

<verification>
- `grep -rnE "widgets\?:|enabled_widgets|enabled-widgets|getEnabledWidgets|useEnabledWidgets|enabledWidgets|resolveWidgetsPayload|interaction-widgets|mapData\.widgets|map\.widgets" frontend/src` → 0.
- `grep -rn "enabled-plugins" frontend/src/api/settings.ts` and `grep -rn "plugins?: string\[\]" frontend/src/types/api.ts` → present.
- `cd frontend && npm run typecheck` → 0 (THE phase gate; FE-RENAME-05).
- `'measurement'`/`'legend'` literals preserved.
- Residual `widget` tokens in frontend/src are ONLY the `widgets.*` i18n key strings (Phase 1163 scope) — confirmed by the grep that excludes `widgets\.[a-zA-Z]`.
</verification>

<success_criteria>
- FE-RENAME-03: types/api.ts `plugins`, settings client `/settings/enabled-plugins/` + `getEnabledPlugins`, query keys + hook + save payload + map-stack on plugin vocabulary.
- FE-RENAME-04: `measurement`/`legend` ID literals unchanged.
- FE-RENAME-05: `npm run typecheck` 0; renamed vitest suites green; whole-frontend widget grep-clean (only i18n keys remain).
</success_criteria>

<output>
Create `.planning/phases/1162-frontend-rename/1162-02-SUMMARY.md` when done.
</output>
