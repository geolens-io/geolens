---
phase: 1164-tooling-docs-audit-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sketch-findings-geolens/
  - e2e/admin.spec.ts
  - e2e/builder-unified-stack.spec.ts
  - e2e/mcp-verify-1134-06.spec.ts
  - CHANGELOG.md
autonomous: true
requirements: [TOOL-02, TOOL-03, DOCS-02]
must_haves:
  truths:
    - "The platform 'widget' vocabulary in .claude/skills/sketch-findings-geolens/ (5 design-sketch files: SKILL.md, references/sidebar-structure.md, references/layer-editor-flyout.md, sources/001-unified-stack/README.md + index.html) is renamed to 'plugin' — these describe the legend/measure platform tools that became plugins. (.claude/agents/ does not exist; the brief's '.agents/skills' path was wrong.)"
    - "The 3 EXISTING e2e specs that reference the widget platform (e2e/admin.spec.ts, e2e/builder-unified-stack.spec.ts, e2e/mcp-verify-1134-06.spec.ts) have their widget references updated to plugin vocabulary matching the post-1163 UI"
    - "e2e/admin.spec.ts asserts the post-1163 settings heading text (the i18n string the Map settings section now renders — 'Plugins', not 'Map Widgets')"
    - "No 'widget' references remain in e2e/ (preserved measurement/legend IDs in any retained testid excepted)"
    - "e2e/admin.spec.ts (which is in the e2e:smoke:core gate) and the touched builder specs remain green-ready: every selector/text asserted matches the real post-1163 UI"
    - "CHANGELOG.md has a ## [2.0.0] section documenting the breaking widgets→plugins rename across DB, API, frontend, i18n, noting measurement/legend IDs unchanged and the migration path (0025 / alembic upgrade head)"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "## [2.0.0] breaking-rename changelog entry"
      contains: "## [2.0.0]"
  key_links:
    - from: "e2e/admin.spec.ts"
      to: "post-1163 Map settings section"
      via: "heading-text assertion (renamed from 'Map Widgets')"
      pattern: "Plugins"
    - from: "CHANGELOG.md ## [2.0.0]"
      to: "migration 0025"
      via: "operator migration-path note"
      pattern: "0025|alembic upgrade head"
---

<objective>
Close TOOL-02 (skills/agents vocabulary), TOOL-03 (widget e2e references), and DOCS-02 (CHANGELOG [2.0.0]).

⚠ SCOPE CORRECTION (discovered at plan time — the brief's TOOL-02/TOOL-03 premises were inaccurate; see <interfaces>):
- **TOOL-02:** The brief said "2 skills" reference widgets and named a `.agents/skills` path. Reality (verified at plan time): `.claude/agents/` DOES NOT EXIST, and the widget references live in ONE skill — `.claude/skills/sketch-findings-geolens/` — across 5 design-sketch files (SKILL.md, references/sidebar-structure.md, references/layer-editor-flyout.md, sources/001-unified-stack/README.md, sources/001-unified-stack/index.html). They are platform-vocabulary labels ("Map widgets" toast, `<span>Widgets</span>` in an HTML mock, "terrain/widgets/projection" sidebar notes) describing the legend/measure tools that became plugins → rename to "plugin". NOTE most of these 5 files are git-UNTRACKED (only `references/layer-rows-and-groups.md` in that skill is tracked, and it has no widget ref); commit the renames with `git add -f` for any that are untracked-but-should-be-tracked, OR leave untracked files edited-in-place if that matches how the skill is maintained — see the commit note in Task 1.
- **TOOL-03:** The brief named 3 widget-named e2e specs (widgets.spec.ts / builder-widgets.spec.ts / widgets-persistence.spec.ts). NONE exist — never tracked, no such files anywhere. The REAL widget references in e2e live inside 3 EXISTING, non-widget-named specs: `e2e/admin.spec.ts`, `e2e/builder-unified-stack.spec.ts`, `e2e/mcp-verify-1134-06.spec.ts`. So TOOL-03 is "update widget references inside these 3 existing specs to plugin vocabulary" — NOT a file rename. The success criterion ("widget e2e specs renamed and selectors/strings updated; no widget refs remain; smoke green") is met by updating the contents of the real referencing specs and confirming no widget-named spec exists.
- The e2e harness lives at repo ROOT (`/Users/ishiland/Code/geolens/e2e/`, `playwright.config.ts`, scripts in ROOT `package.json`) — NOT under `frontend/`.

Purpose: finish the tooling/docs side of the v1036 rename so e2e specs assert the post-1163 plugin UI and the changelog records the breaking rename.
Output: (TOOL-02) confirmation/rename of `.claude/skills`+`.claude/agents`; (TOOL-03) updated `e2e/admin.spec.ts`, `e2e/builder-unified-stack.spec.ts`, `e2e/mcp-verify-1134-06.spec.ts`; (DOCS-02) `## [2.0.0]` in `CHANGELOG.md`.

This plan makes NO production-source changes to the application — only `.claude/` tooling, e2e specs, and the changelog. Independent of Plan 01 (disjoint file set), same wave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Changelog (read structure before inserting [2.0.0])
@CHANGELOG.md

# The 3 EXISTING e2e specs that reference the widget platform (the real TOOL-03 targets)
@e2e/admin.spec.ts
@e2e/builder-unified-stack.spec.ts
@e2e/mcp-verify-1134-06.spec.ts

# Post-1163 settings i18n (to confirm the real Map-settings heading text admin.spec.ts must assert)
@frontend/src/components/admin/settings/SettingsMapTab.tsx

<interfaces>
<!-- VERIFIED at plan time. Confirm with grep at execution. -->

TOOL-02 real state (verified):
  - `.claude/agents/` DOES NOT EXIST (brief's ".agents/skills" path was wrong).
  - Widget refs live ONLY in `.claude/skills/sketch-findings-geolens/` — 5 files:
      SKILL.md (line ~32 "...widgets, projection..."),
      references/sidebar-structure.md (line ~28 "...map widgets (legend, measure, etc.)..."),
      references/layer-editor-flyout.md (line ~328 "compass widget" — NOTE this is a literal
        UI-control noun "compass widget", arguably incidental; classify at execution — if it
        means a non-plugin UI affordance, leave it),
      sources/001-unified-stack/README.md (lines ~14/26/30/35/47 "terrain/widgets/projection"),
      sources/001-unified-stack/index.html (lines ~331/497/499/608/610 toast+`<span>Widgets</span>`).
  - These are DESIGN-SKETCH fixtures (HTML mocks + sidebar notes) describing the legend/measure
    platform tools that became plugins → platform vocabulary → rename to "plugin".
  - Git-tracking: in that skill ONLY `references/layer-rows-and-groups.md` is tracked (no widget
    ref). The 5 widget-referencing files are UNTRACKED. Decide commit handling per Task 1.

Repo e2e layout (ROOT, not frontend/):
  - specs: /Users/ishiland/Code/geolens/e2e/*.spec.ts
  - config: playwright.config.ts + playwright.builder-hardening.config.ts (repo root)
  - scripts: ROOT package.json. Frontend package.json has only vitest/typecheck (no e2e).
  - e2e:smoke:builder = `npx playwright test e2e/builder.spec.ts e2e/builder-styling.spec.ts e2e/builder-v1-5.spec.ts --project=chromium`
      → does NOT include any of the 3 widget-referencing specs.
  - e2e:smoke:core    = includes `e2e/admin.spec.ts` (among others). So admin.spec.ts IS gated by smoke:core.
  - builder-unified-stack.spec.ts and mcp-verify-1134-06.spec.ts are in NO smoke script (full-suite only).
  - There is NO @smoke tag convention — smoke sets are explicit file lists in package.json.

The 3 widget-referencing specs and their EXACT widget mentions (verified by grep):
  e2e/admin.spec.ts:165   → `await expect(page.getByText('Map Widgets')).toBeVisible();`
      This asserts the OLD settings heading. Post-1163 the i18n renamed it. CONFIRM the real
      current heading by reading SettingsMapTab.tsx + the en admin.json key it renders (the i18n
      was renamed settings.widgets→settings.plugins in 1163-01). Update the assertion to the REAL
      rendered text (expected 'Plugins' / 'Map Plugins' — verify which). Since admin.spec.ts is in
      smoke:core, this assertion MUST match the live UI or smoke:core breaks.
  e2e/builder-unified-stack.spec.ts:439,443,444 → a comment "Terrain or Widgets or Projection",
      a regex `/terrain|widgets|projection/i`, and an assert message. Update "widgets"→"plugins"
      in the comment, the regex alternative, and the message. (Confirm the Settings panel now
      renders a "Plugins" affordance — the regex must still match real post-1163 text.)
  e2e/mcp-verify-1134-06.spec.ts: multiple COMMENT mentions of "MeasurementWidget"/"measure-widget"/
      "Close widget" + one CODE line 586: `page.locator('[class*="WidgetHost"], [data-testid*="widget"]')`.
      Update comments to plugin vocabulary (MeasurementPlugin, "Close plugin", measure-plugin) and
      the line-586 locator to the post-1163 equivalent. ⚠ VERIFY the real selector: the production
      components emit plugin-* class/testids now — grep `data-testid=`/`class*=` in
      frontend/src/components/map-plugins/ + builder to find what PluginHost/MeasurementPlugin
      actually render, and target THAT (e.g. `[class*="PluginHost"], [data-testid*="plugin"]`). If
      the exact attribute can't be confirmed, retarget to a stable existing selector and note the
      substitution. (This line is "informational" per its own comment + this spec is not in any
      smoke gate, so low risk — but still make it correct.)

Migration revision for the changelog: backend/alembic/versions/0025_widgets_to_plugins_rename.py → revision 0025 (verified).

CHANGELOG version headers (descending, verified): [Unreleased] (line 12), [1.8.0] (line 14), [1.7.0], [1.6.0], ...
  No [2.0.0] yet. Insert [2.0.0] BETWEEN [Unreleased] and [1.8.0].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename widget vocabulary to plugin in the sketch-findings skill (TOOL-02)</name>
  <files>.claude/skills/sketch-findings-geolens/SKILL.md, .claude/skills/sketch-findings-geolens/references/sidebar-structure.md, .claude/skills/sketch-findings-geolens/references/layer-editor-flyout.md, .claude/skills/sketch-findings-geolens/sources/001-unified-stack/README.md, .claude/skills/sketch-findings-geolens/sources/001-unified-stack/index.html</files>
  <action>
RE-DISCOVER the real widget-referencing set first (the brief's "2 skills" + ".agents/skills" path were both wrong; verified at plan time that `.claude/agents/` does not exist and the refs are all in one skill):
  `ls -d .claude/agents 2>/dev/null || echo "no .claude/agents dir (expected)"`
  `grep -rniE 'widget' .claude/skills/`

Expected set (5 files under `.claude/skills/sketch-findings-geolens/`): `SKILL.md`, `references/sidebar-structure.md`, `references/layer-editor-flyout.md`, `sources/001-unified-stack/README.md`, `sources/001-unified-stack/index.html`. These are design-sketch fixtures describing the original builder sidebar; the "widget" mentions name the legend/measure platform tools that became plugins.

For each match, classify then rename PLATFORM vocabulary to plugin (minimal Edits):
  - "map widgets (legend, measure, etc.)" → "map plugins (legend, measure, etc.)".
  - "terrain, projection, widgets" / "Terrain · Widgets · Projection" / "terrain/widgets/projection" → swap "widgets"/"Widgets" → "plugins"/"Plugins".
  - The toast strings + `<span>Widgets</span>` in `sources/001-unified-stack/index.html` (lines ~331/497/499/608/610) → "Plugins" / "Map plugins".
  - INCIDENTAL exception to CHECK: `references/layer-editor-flyout.md` line ~328 says "a 90px compass widget" — that is a literal UI-control noun (a compass affordance), NOT the plugin platform. LEAVE it (and note the exception in the SUMMARY). Confirm by reading the line in context before deciding.
  - Preserve `measurement`/`legend` ID literals (none appear in these lines anyway).

COMMIT HANDLING (these files are mostly git-UNTRACKED): only `references/layer-rows-and-groups.md` in this skill is tracked (and it has no widget ref). The 5 files you edit are untracked local skill content. Per project convention `.planning/`-style force-add does NOT apply to `.claude/skills/` — these are local skill working files. Do NOT `git add -f` them unless the user/orchestrator confirms they should be version-controlled; edit them in place so the local skill speaks plugin vocabulary, and RECORD in the SUMMARY that they were edited-but-left-untracked (matching their current tracking state). If any of the 5 turns out to BE tracked at execution time, commit those normally.

⚠ CONCURRENT-EDIT NOTE: a parallel session (`builder-audit-fixes-20260530`) may edit `.claude/` files. Before any Edit, `git status --short <file>` (for tracked ones) and check for merge-conflict markers. If a file is mid-conflict, handle it last with a fresh re-grep + smallest Edit. NEVER touch `builder-audit-*` branches/worktrees. If a clean edit is impossible, STOP and report.

After editing, `grep -rniE 'widget' .claude/skills/` returns only the deliberately-kept incidental "compass widget" line (if you kept it) — every platform "widget" reference is gone.
  </action>
  <verify>
    <automated>test ! -d .claude/agents && ! grep -rqiE 'map widgets|map-widgets|enabled_widgets|WidgetHost|WidgetPanel|<span>Widgets|Terrain . Widgets|terrain/widgets|terrain, projection, widgets' .claude/skills/ && echo OK-TOOL02; echo "Remaining widget mentions (expect at most the incidental compass-widget line):"; grep -rniE 'widget' .claude/skills/ || echo NONE</automated>
  </verify>
  <done>.claude/agents/ confirmed absent; all PLATFORM widget vocabulary in the 5 sketch-findings-geolens files renamed to plugin (any incidental non-platform "compass widget" deliberately kept + noted); measurement/legend untouched; commit/tracking handling recorded in the SUMMARY; no concurrent WIP disturbed.</done>
</task>

<task type="auto">
  <name>Task 2: Update widget references inside the 3 existing e2e specs to plugin vocabulary (TOOL-03)</name>
  <files>e2e/admin.spec.ts, e2e/builder-unified-stack.spec.ts, e2e/mcp-verify-1134-06.spec.ts</files>
  <action>
STEP A — confirm the scope is still accurate (do not trust the brief's spec filenames):
  `ls e2e/*widget* e2e/*plugin* 2>/dev/null || echo "no widget/plugin-named specs (expected)"`
  `grep -rliE 'widget' e2e/`   → expect exactly: e2e/admin.spec.ts, e2e/builder-unified-stack.spec.ts, e2e/mcp-verify-1134-06.spec.ts
  If the set differs, update the real referencing specs found. Do NOT create or rename spec files — the 3 brief-named widget specs do not exist and must NOT be fabricated.

STEP B — `e2e/admin.spec.ts` (line ~165, in e2e:smoke:core → must stay green):
  Current: `await expect(page.getByText('Map Widgets')).toBeVisible();`
  The settings i18n was renamed in 1163-01. CONFIRM the real rendered heading: read `frontend/src/components/admin/settings/SettingsMapTab.tsx` and the en `admin.json` key it uses (the renamed `settings.plugins.*` key), then update the assertion to the EXACT post-1163 text it renders (expected 'Plugins' or 'Map Plugins' — use the real value, do not guess). Re-grep the line before editing.

STEP C — `e2e/builder-unified-stack.spec.ts` (lines ~439/443/444):
  - comment `// Settings panel typically contains Terrain or Widgets or Projection.` → "...Terrain or Plugins or Projection."
  - regex `editor.getByText(/terrain|widgets|projection/i)` → `/terrain|plugins|projection/i`
  - assert message `'Settings panel should show Terrain/Widgets/Projection content'` → "...Terrain/Plugins/Projection content"
  Confirm the Settings panel renders a "Plugins" affordance post-1163 so the regex still matches.

STEP D — `e2e/mcp-verify-1134-06.spec.ts` (comments + line ~586):
  - Update the comment mentions: "MeasurementWidget"→"MeasurementPlugin", "measure-widget"→"measure-plugin", "Close widget"→"Close plugin", "WidgetHost"→"PluginHost" (lines ~15, 226, 227, 528, 567, 583, 584, 585, 587).
  - Line ~586 CODE: `page.locator('[class*="WidgetHost"], [data-testid*="widget"]')`. VERIFY the real post-1163 selector: `grep -rniE 'data-testid=|class' frontend/src/components/map-plugins/PluginHost.tsx frontend/src/components/map-plugins/builtin/` to learn what PluginHost/MeasurementPlugin actually render, then update to the real equivalent (likely `[class*="PluginHost"], [data-testid*="plugin"]`). If the exact attribute can't be confirmed in src, retarget to a stable existing selector and note the substitution in the SUMMARY. (This locator is self-described "informational" and the spec is in no smoke gate, so low risk.)
  - Also rename the local var `widgetHostExists`→`pluginHostExists` for consistency.

STEP E — typecheck the e2e specs. The e2e harness is at repo root; the right check is the playwright/TS compile. Run `npx tsc --noEmit -p tsconfig.json` if a root tsconfig covers e2e, OR `npx playwright test e2e/admin.spec.ts e2e/builder-unified-stack.spec.ts e2e/mcp-verify-1134-06.spec.ts --list` (the `--list` flag compiles + lists tests without running them or needing a live stack). Confirm no compile errors. Do NOT run the full suites here (the deterministic gate incl. e2e:smoke:builder/core is run in Phase 1165 / QA-01).

STEP F — full sanity: `grep -rniE 'widget' e2e/` returns nothing (except any retained measurement/legend ID substrings, which contain no "widget").

INVARIANT: never rename measurement/legend substrings inside any retained testid. Touch ONLY these 3 e2e specs — no production code, no playwright config, no other specs.
  </action>
  <verify>
    <automated>{ ls e2e/*widget*.spec.ts e2e/*plugin*.spec.ts 2>/dev/null && { echo "FAIL: unexpected widget/plugin-named spec"; exit 1; } || true; } && [ "$(grep -rniE 'widget' e2e/ | wc -l | tr -d ' ')" = "0" ] && npx playwright test e2e/admin.spec.ts e2e/builder-unified-stack.spec.ts e2e/mcp-verify-1134-06.spec.ts --list >/dev/null 2>&1 && echo OK-TOOL03</automated>
  </verify>
  <done>The 3 brief-named widget specs confirmed absent (none fabricated); the 3 real widget-referencing specs (admin/builder-unified-stack/mcp-verify-1134-06) updated to plugin vocabulary with the admin.spec.ts heading assertion matching the real post-1163 i18n text and the mcp-verify locator retargeted to a verified real selector; zero "widget" tokens in e2e/; the specs compile (playwright --list clean). Full smoke run deferred to Phase 1165. No production code or config touched.</done>
</task>

<task type="auto">
  <name>Task 3: Add ## [2.0.0] breaking-rename entry to CHANGELOG.md (DOCS-02)</name>
  <files>CHANGELOG.md</files>
  <action>
Insert a new `## [2.0.0]` section into `CHANGELOG.md` (per DOCS-02). RE-READ the head first: `grep -nE '^## \[' CHANGELOG.md | head`. At plan time: `## [Unreleased]` (line 12), `## [1.8.0] - 2026-05-29` (line 14), then `[1.7.0]`, `[1.6.0]`, … No `## [2.0.0]` exists (confirm `grep -c '^## \[2\.0\.0\]' CHANGELOG.md` is 0 before adding).

Placement: insert `## [2.0.0]` (with today's date) directly BETWEEN the `## [Unreleased]` block and `## [1.8.0]`. Do not disturb `## [Unreleased]`. Read a couple of existing version sections first to mirror the exact heading/bullet/subsection style (`### Changed`/`### Removed`, bullet glyphs, date format `## [X.Y.Z] - YYYY-MM-DD`).

Content — frame as BREAKING; cover all four surfaces + preserved IDs + migration path:
- **BREAKING: the map "widget" platform was renamed to "plugin" across the stack** (v1036). Hard cut — no back-compat alias/deprecation shim; old `widgets`/`enabled_widgets` names removed.
- **Database:** column `maps.widgets` → `maps.plugins`; config key `enabled_widgets` → `enabled_plugins` in `catalog.app_settings` (Alembic migration `0025`, file `0025_widgets_to_plugins_rename.py`). Operators MUST run `alembic upgrade head`. Row values (arrays of ID strings) preserved.
- **API:** map field `widgets` → `plugins`; settings route `/settings/enabled-widgets/` → `/settings/enabled-plugins/`. External API clients MUST update field names and the route path.
- **Frontend:** directory `map-widgets/` → `map-plugins/`; platform identifiers (host/panel/registry/availability) and component test-ids renamed `widget*` → `plugin*`.
- **i18n:** widget-platform translation keys renamed to plugin keys across en/de/es/fr (parity maintained).
- **Preserved (NOT changed):** built-in plugin IDs `measurement` and `legend`; maplibre layer-ids.

Keep it concise but complete enough that an external client knows exactly what to update.
  </action>
  <verify>
    <automated>grep -qE '^## \[2\.0\.0\]' CHANGELOG.md && grep -A40 '## \[2\.0\.0\]' CHANGELOG.md | grep -qiE 'breaking' && grep -A40 '## \[2\.0\.0\]' CHANGELOG.md | grep -q 'enabled-plugins' && grep -A40 '## \[2\.0\.0\]' CHANGELOG.md | grep -qE 'measurement|legend' && grep -A40 '## \[2\.0\.0\]' CHANGELOG.md | grep -qiE 'alembic upgrade head' && grep -A40 '## \[2\.0\.0\]' CHANGELOG.md | grep -q '0025' && echo OK-DOCS02</automated>
  </verify>
  <done>CHANGELOG.md has a ## [2.0.0] section (between [Unreleased] and [1.8.0], dated) framed as BREAKING, documenting the DB column+config (migration 0025 / alembic upgrade head), API field+route (/settings/enabled-plugins/), frontend dir/identifiers, and i18n keys, explicitly noting measurement/legend IDs unchanged. Matches existing file formatting.</done>
</task>

</tasks>

<verification>
- TOOL-02: `.claude/agents/` absent; `grep -rniE 'widget' .claude/skills/` returns at most the deliberately-kept incidental "compass widget" line — all platform widget vocabulary in the 5 sketch-findings-geolens files renamed to plugin.
- TOOL-03: no widget/plugin-named spec file in e2e/; `grep -rniE 'widget' e2e/` returns nothing; the 3 real specs compile via `playwright test --list`; admin.spec.ts heading assertion matches real post-1163 i18n.
- DOCS-02: `## [2.0.0]` exists, framed BREAKING, covers DB/API/frontend/i18n + migration `0025` + `alembic upgrade head` + measurement/legend preserved.
</verification>

<success_criteria>
- Platform "widget" vocabulary in the `.claude/skills/sketch-findings-geolens/` design-sketch files renamed to "plugin" (brief's "2 skills" + ".agents/skills" path inaccuracy recorded; `.claude/agents/` does not exist).
- The 3 existing widget-referencing e2e specs updated to plugin vocabulary + verified real selectors; admin.spec.ts stays green-ready for smoke:core; specs compile.
- `CHANGELOG.md` has a complete `## [2.0.0]` breaking-rename section.
- No production-source files touched.
</success_criteria>

<output>
Create `.planning/phases/1164-tooling-docs-audit-fixes/1164-02-SUMMARY.md` when done.
Record: (1) TOOL-02 outcome — the brief's ".agents/skills"/"2 skills" claims were wrong; `.claude/agents/` does not exist and the real refs were 5 design-sketch files in `.claude/skills/sketch-findings-geolens/` (list which were renamed, which incidental "compass widget" line was kept, and their git-tracking state / whether anything was committed); (2) TOOL-03 scope correction — the 3 brief-named widget specs do not exist; the real referencing specs were admin/builder-unified-stack/mcp-verify-1134-06; the EXACT post-1163 heading text used in admin.spec.ts; any mcp-verify-1134-06 selector substitution made because the assumed attribute wasn't confirmed in src; (3) confirmation the 3 specs compile (`playwright --list`); (4) which smoke script(s) cover the touched specs (admin.spec.ts → e2e:smoke:core; the other two → none); (5) the date used on the `## [2.0.0]` header.
</output>
