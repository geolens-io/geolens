---
phase: 1164-tooling-docs-audit-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/plugin-development.md
  - .claude/commands/widget-audit.md
  - .claude/commands/plugin-audit.md
  - .claude/commands/map-audit.md
  - .claude/commands/builder-audit.md
autonomous: true
requirements: [DOCS-01, TOOL-01, TOOL-04]
must_haves:
  truths:
    - "docs/plugin-development.md exists and accurately describes the post-rename plugin platform (registry, built-ins, availability gating, host/panel/error-boundary contract, the PluginDefinition shape from types.ts, and how to register a new plugin)"
    - "Every symbol named in docs/plugin-development.md exists in frontend/src/components/map-plugins/ (no dangling symbol references)"
    - ".claude/commands/widget-audit.md no longer exists; .claude/commands/plugin-audit.md exists with the same audit logic in plugin vocabulary"
    - "plugin-audit.md Step 2 read list points only at REAL post-1163 files (types.ts, registry.ts, register-plugins.ts, plugin-availability.ts, PluginHost.tsx, PluginPanel.tsx, PluginErrorBoundary.tsx, builtin/MeasurementPlugin.tsx, builtin/LegendPlugin.tsx, and the real __tests__ files) and includes plugin-availability.ts"
    - "plugin-audit.md references docs/plugin-development.md (now exists), not docs/widget-development.md"
    - "plugin-audit.md instructs deriving the built-in plugin set from register-plugins.ts rather than hardcoding 'measurement and legend'"
    - "Both /widget-audit cross-references (map-audit.md line ~317 and builder-audit.md line ~1132) read /plugin-audit"
    - "No platform 'widget' vocabulary remains in plugin-audit.md (preserved IDs measurement/legend and deliberate git-history mentions excepted)"
  artifacts:
    - path: "docs/plugin-development.md"
      provides: "Plugin authoring guide accurate to post-rename code"
      min_lines: 60
      contains: "PluginDefinition"
    - path: ".claude/commands/plugin-audit.md"
      provides: "Renamed deep audit command for the plugin platform"
      contains: "plugin-availability.ts"
  key_links:
    - from: ".claude/commands/plugin-audit.md"
      to: "docs/plugin-development.md"
      via: "docs cross-check reference"
      pattern: "docs/plugin-development\\.md"
    - from: ".claude/commands/plugin-audit.md"
      to: "frontend/src/components/map-plugins/register-plugins.ts"
      via: "Step 2 read list + derive-built-ins instruction"
      pattern: "register-plugins\\.ts"
    - from: ".claude/commands/map-audit.md"
      to: "/plugin-audit"
      via: "cross-reference line ~317"
      pattern: "/plugin-audit"
    - from: ".claude/commands/builder-audit.md"
      to: "/plugin-audit"
      via: "cross-reference line ~1132"
      pattern: "/plugin-audit"
---

<objective>
Create the plugin authoring guide (DOCS-01), rename the widget-audit slash-command to plugin-audit (TOOL-01), and fix the 3 audit review findings inside the renamed command (TOOL-04).

Grouped in one plan because TOOL-04 fix #1 depends on DOCS-01: the audit cross-checks built-ins against `docs/plugin-development.md`, which does not exist (the audit points at the dangling `docs/widget-development.md`). Create the doc first, then point the renamed audit at it.

Purpose: Tooling and docs must speak post-rename "plugin" vocabulary and reference real files.
Output: `docs/plugin-development.md` (new), `.claude/commands/plugin-audit.md` (renamed + fixed), updated cross-refs in `map-audit.md` and `builder-audit.md`.

This plan makes NO production-source changes to the application. It touches only docs and `.claude/` tooling.

⚠ DISCOVERY-DRIFT WARNING — the phase brief's symbol names and paths drifted from the renamed code. The `<interfaces>` block below is VERIFIED against the real `map-plugins/` dir; trust it over the brief, but still re-read each file before editing because a concurrent session (`builder-audit-fixes-20260530`) is active in this working tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Brief defining the 3 TOOL-04 fixes (the "widget-audit.md review findings" section)
@RENAME-widgets-to-plugins-BRIEF.md

# The file to rename + fix (current name; rename to plugin-audit.md in Task 2)
@.claude/commands/widget-audit.md

# Source of truth for DOCS-01 accuracy AND the audit's "derive built-ins from registry" fix
@frontend/src/components/map-plugins/types.ts
@frontend/src/components/map-plugins/registry.ts
@frontend/src/components/map-plugins/register-plugins.ts
@frontend/src/components/map-plugins/plugin-availability.ts
@frontend/src/components/map-plugins/PluginPanel.tsx
@frontend/src/components/map-plugins/PluginHost.tsx
@frontend/src/components/map-plugins/PluginErrorBoundary.tsx
@frontend/src/components/map-plugins/index.ts

<interfaces>
<!-- VERIFIED post-1163 plugin-platform contract (read directly from the dir during planning). -->
<!-- These supersede the brief. The TYPE lives in types.ts — there is NO PluginDefinition.ts file. -->
<!-- Re-read each file before writing; a concurrent session is editing this tree. -->

map-plugins/ dir actually contains: index.ts, types.ts, registry.ts, register-plugins.ts,
  plugin-availability.ts, PluginHost.tsx, PluginPanel.tsx, PluginErrorBoundary.tsx,
  builtin/ (MeasurementPlugin.tsx, LegendPlugin.tsx),
  __tests__/ (registry.test.ts, map-plugin-store.test.ts, PluginHost.test.tsx, WidgetHost.test.tsx).
  NOTE there is NO map-plugin-store.ts inside map-plugins/ — the store source lives at
  frontend/src/components/builder/state/map-plugin-store.ts.
  NOTE __tests__/WidgetHost.test.tsx is a STALE widget-named test file that imports PluginHost
  (production-adjacent leftover) — OUT OF SCOPE for this docs/tooling phase. Do NOT rename/edit it
  here; flag it in the SUMMARY as found-but-deferred.

From registry.ts:
  registerPlugin(def: PluginDefinition): void   // DEV-warns + overwrites on duplicate id
  getPlugins(): PluginDefinition[]              // memoized; returns all registered plugins
  getPlugin(id: string): PluginDefinition | undefined
  NOTE registry.ts:9 still has a stale console.warn string `Widget "${def.id}" already registered` —
  PRODUCTION CODE leftover, OUT OF SCOPE here. Flag in SUMMARY; do NOT edit.

From register-plugins.ts — THE registry source of truth for built-ins:
  registers id 'measurement' (component MeasurementPlugin, labelKey 'plugins.measurement.label', floating top-left, defaultVisible:false)
  registers id 'legend'      (component LegendPlugin,      labelKey 'plugins.legend.label',      floating bottom-left, defaultVisible:true)

From plugin-availability.ts:
  getEnabledPluginDefinitions(enabledPluginIds: string[]|null|undefined): PluginDefinition[]   // null => no restriction
  isPluginIdAvailable(id, enabledPluginIds): boolean
  resolveAvailablePluginIds(pluginIds, enabledPluginIds): string[]
  getDefaultPluginIds(enabledPluginIds): string[]
  samePluginIds(a, b): boolean

From types.ts: PluginDefinition interface (id, labelKey, icon, placement {mode, anchor}, component, defaultVisible) and PluginContext. READ types.ts to transcribe the exact field set — do not invent fields.

From PluginHost.tsx / PluginPanel.tsx / PluginErrorBoundary.tsx: host renders enabled plugins, each panel wrapped in PluginErrorBoundary. READ these to transcribe exact prop/context shapes before documenting.

From index.ts: the public barrel exports. READ to list them accurately.

Built-in plugin IDs (PRESERVED, never rename): measurement, legend
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/plugin-development.md authoring guide (DOCS-01)</name>
  <files>docs/plugin-development.md</files>
  <action>
First confirm the doc does not already exist: `ls docs/plugin-development.md docs/widget-development.md` (neither should exist).

READ these to transcribe exact symbol names/signatures before writing: `frontend/src/components/map-plugins/types.ts`, `registry.ts`, `register-plugins.ts`, `plugin-availability.ts`, `PluginHost.tsx`, `PluginPanel.tsx`, `PluginErrorBoundary.tsx`, `index.ts`, `builtin/MeasurementPlugin.tsx`, `builtin/LegendPlugin.tsx`. Every type/function name in the doc MUST match a symbol that exists. Do not invent APIs. In particular: the type is `PluginDefinition` and lives in `types.ts`; the registry getter is `getPlugins()` NOT `getAllPlugins()`; availability uses `getEnabledPluginDefinitions()`/`isPluginIdAvailable()` NOT `isPluginAvailable()`.

Create `docs/plugin-development.md` covering, in order:
1. **Overview** — the plugin platform is the pluggable map-tool system (registry + host/panel + availability gating). Built-in plugins today are `measurement` and `legend`; state explicitly that `register-plugins.ts` is the source of truth and any built-in list in prose is informational only.
2. **The registry** (`registry.ts`) — `registerPlugin`, `getPlugins`, `getPlugin`. Note overwrite-with-DEV-warning on duplicate id and memoized getter.
3. **Registering built-ins** (`register-plugins.ts`) — calls `registerPlugin(...)` for `measurement` (component `MeasurementPlugin`) and `legend` (component `LegendPlugin`). IDs `measurement`/`legend` are preserved literals (v1036 invariant).
4. **The PluginDefinition shape** (`types.ts`) — document each field as it actually appears: `id`, `labelKey`, `icon`, `placement` (`{ mode, anchor }`), `component`, `defaultVisible`. Document `PluginContext` too (transcribe its fields).
5. **Availability gating** (`plugin-availability.ts`) — `getEnabledPluginDefinitions(enabledPluginIds)` (null = no restriction), `isPluginIdAvailable`, `resolveAvailablePluginIds`, `getDefaultPluginIds`, `samePluginIds`. Explain `enabled_plugins: null` = all, `[]` = none, array = restrict.
6. **The host/panel contract** — `PluginHost` renders enabled plugins, wrapping each panel in `PluginErrorBoundary` (class component, fallback on throw). Document `PluginPanel`'s contract as transcribed.
7. **How to register a new plugin** — (a) author a panel receiving `PluginContext`; (b) build a `PluginDefinition` with a unique `id`, `labelKey`, `icon`, `placement`, `component`, `defaultVisible`; (c) call `registerPlugin(def)` (typically via `register-plugins.ts` for a built-in); (d) add the `labelKey` translation across en/de/es/fr; (e) the host surfaces it once availability passes.
8. **Public exports** — list the barrel exports from `index.ts` (transcribe exactly).

Short illustrative fenced code snippets (a `PluginDefinition` literal, a `registerPlugin` call) ARE appropriate in this doc file. Do NOT create `docs/widget-development.md`.
  </action>
  <verify>
    <automated>test -f docs/plugin-development.md && grep -q 'PluginDefinition' docs/plugin-development.md && grep -q 'register-plugins.ts' docs/plugin-development.md && grep -q 'plugin-availability.ts' docs/plugin-development.md && grep -q 'PluginErrorBoundary' docs/plugin-development.md && grep -q 'getPlugins' docs/plugin-development.md && grep -Eq 'measurement.*legend|legend.*measurement' docs/plugin-development.md && ! grep -q 'getAllPlugins' docs/plugin-development.md && ! grep -q 'PluginDefinition.ts' docs/plugin-development.md && echo OK-DOCS01</automated>
  </verify>
  <done>docs/plugin-development.md exists, names types.ts/registry.ts/register-plugins.ts/plugin-availability.ts and the PluginHost/PluginPanel/PluginErrorBoundary contract using REAL symbol names (getPlugins, getEnabledPluginDefinitions, PluginDefinition in types.ts), documents measurement+legend as preserved built-in IDs, and explains how to register a new plugin. Every symbol it names exists in the dir. No getAllPlugins / PluginDefinition.ts references.</done>
</task>

<task type="auto">
  <name>Task 2: Rename widget-audit.md → plugin-audit.md and apply the 3 audit fixes (TOOL-01 + TOOL-04)</name>
  <files>.claude/commands/widget-audit.md, .claude/commands/plugin-audit.md</files>
  <action>
Rename with git so history is preserved:
`git mv .claude/commands/widget-audit.md .claude/commands/plugin-audit.md`

This is a 349-line file with ~106 "widget" occurrences and STALE pre-rename file paths in its read lists. RE-GREP first: `grep -niE 'widget|map-widgets|register-widgets|docs/widget-development|measurement and legend|getWidgets' .claude/commands/plugin-audit.md`. Read the whole file, then rewrite it in plugin vocabulary preserving structure.

Vocabulary + path renames (TOOL-01, throughout):
- Heading `# /widget-audit - Map Widget Platform Audit` → `# /plugin-audit - Map Plugin Platform Audit`.
- Every `/widget-audit` token → `/plugin-audit` (usage line, Step-1 case/echo).
- Every prose "widget"/"Widget"/"widgets" → "plugin"/"Plugin"/"plugins" EXCEPT preserved IDs and deliberate git-history notes.
- Path `frontend/src/components/map-widgets/**` → `frontend/src/components/map-plugins/**`.
- Component identifiers in prose/invariants: `WidgetHost`→`PluginHost`, `WidgetPanel`→`PluginPanel`, `WidgetErrorBoundary`→`PluginErrorBoundary`, `WidgetContext`→`PluginContext`, `WidgetDefinition`→`PluginDefinition`, `useWidgetStore`→`usePluginStore`, `usePartitionedWidgets`→(grep map-plugins/ for the real hook name; if it no longer exists, describe behavior generically instead of naming a dead hook).
- Config/route tokens: `enabled_widgets`→`enabled_plugins`, `GET /settings/enabled-widgets/`→`GET /settings/enabled-plugins/`, `getWidgets()`→`getPlugins()`, `widgets?: string[] | null`→`plugins?: string[] | null`.
- DELIVERY output path: `docs-internal/audits/widget-audit-{YYYYMMDD}.md` → `docs-internal/audits/plugin-audit-{YYYYMMDD}.md` (preserve the real `docs-internal/audits/` dir; just rename the file prefix).

Step-2 read list — fix STALE filenames AND complete it (TOOL-04 fix #2). The current Step-2 list names pre-rename files that no longer exist. Replace the ENTIRE list with the REAL post-1163 files (CONFIRM each exists with `ls frontend/src/components/map-plugins/ frontend/src/components/map-plugins/builtin/ frontend/src/components/map-plugins/__tests__/` before committing):
  - frontend/src/components/map-plugins/index.ts
  - frontend/src/components/map-plugins/types.ts
  - frontend/src/components/map-plugins/registry.ts
  - frontend/src/components/map-plugins/register-plugins.ts
  - frontend/src/components/map-plugins/plugin-availability.ts   ← the documented TOOL-04 addition
  - frontend/src/components/builder/state/map-plugin-store.ts    ← store source lives under builder/state, NOT map-plugins/ (verified)
  - frontend/src/components/map-plugins/PluginHost.tsx
  - frontend/src/components/map-plugins/PluginPanel.tsx
  - frontend/src/components/map-plugins/PluginErrorBoundary.tsx
  - frontend/src/components/map-plugins/builtin/MeasurementPlugin.tsx
  - frontend/src/components/map-plugins/builtin/LegendPlugin.tsx
  - frontend/src/components/map-plugins/__tests__/registry.test.ts
  - frontend/src/components/map-plugins/__tests__/map-plugin-store.test.ts
  - frontend/src/components/map-plugins/__tests__/PluginHost.test.tsx
  (Do NOT list __tests__/WidgetHost.test.tsx — it is a stale leftover; note it in the SUMMARY but keep it out of the read list.)

TOOL-04 fix #1 — every `docs/widget-development.md` mention (brief cited Step 5, an invariant line, an area-7 line) → `docs/plugin-development.md` (created in Task 1).

TOOL-04 fix #3 — the spots hardcoding "measurement and legend" (around the `builtins` scope desc, the Built-in invariants/areas, and the Synthesis scorecard row): reword so the auditor DERIVES the built-in set from `register-plugins.ts` — e.g. "derive the current built-in plugin set from `register-plugins.ts` (the registry is the source of truth) — do not assume a hardcoded list." Mentioning measurement/legend parenthetically as "at time of writing" is fine. Keep the existing principle "MUST NOT maintain stale hardcoded ID lists; they should derive from the registry" and make these spots consistent with it. The existing Step 6 already derives IDs from the registry — keep it, just fix its `register-widgets.ts`→`register-plugins.ts` and its rg pattern (`registerWidget`→`registerPlugin`, `map-widgets`→`map-plugins`, `enabled_widgets`→`enabled_plugins`).

After editing: `grep -ivE 'measurement|legend' .claude/commands/plugin-audit.md | grep -iE 'widget'` returns nothing, and `grep -E 'map-widgets|register-widgets|widget-development|getWidgets|enabled-widgets|enabled_widgets' .claude/commands/plugin-audit.md` returns nothing.

INVARIANT: do not change measurement/legend ID literals. Do NOT touch any file under `frontend/src/` (production code) — only the audit command.
  </action>
  <verify>
    <automated>test ! -f .claude/commands/widget-audit.md && test -f .claude/commands/plugin-audit.md && grep -q 'register-plugins.ts' .claude/commands/plugin-audit.md && grep -q 'plugin-availability.ts' .claude/commands/plugin-audit.md && grep -q 'docs/plugin-development.md' .claude/commands/plugin-audit.md && ! grep -qE 'map-widgets|register-widgets|docs/widget-development|getWidgets|enabled-widgets|enabled_widgets' .claude/commands/plugin-audit.md && [ "$(grep -ivE 'measurement|legend' .claude/commands/plugin-audit.md | grep -ciE 'widget')" = "0" ] && echo OK-TOOL01-04</automated>
  </verify>
  <done>widget-audit.md gone (git mv); plugin-audit.md exists; Step 2 read list points only at real files and includes plugin-availability.ts (+ the builder/state map-plugin-store.ts path); all docs/widget-development.md refs now read docs/plugin-development.md; hardcoded-built-in spots instruct deriving from register-plugins.ts; zero stale widget paths/tokens; zero platform "widget" vocabulary (measurement/legend excepted). No production code touched.</done>
</task>

<task type="auto">
  <name>Task 3: Update the 2 cross-references to /plugin-audit (TOOL-01)</name>
  <files>.claude/commands/map-audit.md, .claude/commands/builder-audit.md</files>
  <action>
Update the two slash-command cross-references that point at the old `/widget-audit`. RE-GREP both first (line numbers drift):
  `grep -n 'widget-audit' .claude/commands/map-audit.md`
  `grep -n 'widget-audit' .claude/commands/builder-audit.md`

1. `.claude/commands/map-audit.md` — at plan time line 317: ``- `/widget-audit` — deep-dive the widget platform (registry, host/panel, availability gating, built-in widgets)``. Change `/widget-audit` → `/plugin-audit` and "the widget platform"/"built-in widgets" → "the plugin platform"/"built-in plugins". ALSO line ~318 has "enabled widgets" (``...the runtime effect of enabled widgets on a saved map``) → "enabled plugins". Re-grep to confirm exact current lines, then Edit.

2. `.claude/commands/builder-audit.md` — at plan time line 1132 (NOT the brief's 1190), clean working tree, already half-rename-aware: ``- `/widget-audit` (→ `/plugin-audit` after the v1036 rename) — the plugin/widget **host** platform (registry, availability, host/panel contract, built-in widgets). This command audits only the builder-side surfacing of plugins/widgets (Settings "Widgets" section, viewer widget availability); host internals are out of scope here.``. Replace this line so it reads cleanly as plugin vocabulary, e.g.: ``- `/plugin-audit` — the plugin **host** platform (registry, availability, host/panel contract, built-in plugins). This command audits only the builder-side surfacing of plugins (Settings "Plugins" section, viewer plugin availability); host internals are out of scope here.`` Re-grep the current line first; make the smallest Edit that removes the `/widget-audit` token and the platform "widget" words on that line.

⚠⚠ CONCURRENT-EDIT CONTENTION — `.claude/commands/builder-audit.md` is under active edit by a parallel session (`builder-audit-fixes-20260530`) that switches branches and writes builder-audit reports. At plan time the working tree was clean and the line had no conflict markers, but state WILL drift. Before editing:
  - Re-grep `grep -n 'widget-audit' .claude/commands/builder-audit.md` for the CURRENT line (do not trust 1132/1190).
  - `git status --short .claude/commands/builder-audit.md` and check for merge-conflict markers (`<<<<<<<`, `=======`, `>>>>>>>`) or foreign WIP. If present, do map-audit.md FIRST, then handle builder-audit.md LAST with a fresh re-grep and an Edit on the exact matched single line (never bulk rewrite).
  - NEVER touch `builder-audit-*` branches/worktrees. Edit only the file on the current `main` working tree.
  - If a clean single-line Edit is impossible due to a foreign in-progress conflict, STOP and report rather than corrupting concurrent work.
  </action>
  <verify>
    <automated>! grep -rq 'widget-audit' .claude/commands/map-audit.md && ! grep -rq 'widget-audit' .claude/commands/builder-audit.md && grep -q 'plugin-audit' .claude/commands/map-audit.md && grep -q 'plugin-audit' .claude/commands/builder-audit.md && echo OK-CROSSREFS</automated>
  </verify>
  <done>Neither map-audit.md nor builder-audit.md contains "widget-audit"; both contain "plugin-audit"; the builder-audit.md line-~1132 cross-ref reads as clean plugin vocabulary and map-audit.md line ~317-318 references plugin platform / enabled plugins. The builder-audit.md edit was made on a freshly re-grepped line with no merge-conflict markers introduced, and concurrent WIP was not disturbed.</done>
</task>

</tasks>

<verification>
- DOCS-01: `test -f docs/plugin-development.md`; spot-grep that every symbol named exists in the dir (e.g. `grep -rl getPlugins frontend/src/components/map-plugins/`).
- TOOL-01: `widget-audit.md` gone, `plugin-audit.md` present, no `/widget-audit` in map-audit.md/builder-audit.md, no platform "widget" vocabulary in plugin-audit.md (excluding measurement/legend).
- TOOL-04: plugin-audit.md contains `plugin-availability.ts` + `register-plugins.ts`, references `docs/plugin-development.md`, instructs deriving built-ins from `register-plugins.ts`, zero stale `map-widgets`/`register-widgets`/`getWidgets`/`enabled-widgets` tokens.
</verification>

<success_criteria>
- `docs/plugin-development.md` exists and is accurate to the post-rename code (real symbol names).
- `.claude/commands/widget-audit.md` gone; `.claude/commands/plugin-audit.md` exists and is internally consistent with the renamed code (real file paths).
- The 3 TOOL-04 review findings are fixed inside plugin-audit.md; stale pre-rename paths corrected.
- Both `/widget-audit` cross-references now read `/plugin-audit`, builder-audit.md edited safely.
- No production-source files touched.
</success_criteria>

<output>
Create `.planning/phases/1164-tooling-docs-audit-fixes/1164-01-SUMMARY.md` when done.
Record: (1) whether builder-audit.md had merge-conflict markers / foreign WIP at edit time and the actual line the cross-ref was on (vs plan-time 1132); (2) confirmation map-audit.md line ~317 + ~318 were both updated; (3) the two found-but-deferred production-adjacent leftovers — the stale `Widget "${def.id}"` console.warn at `frontend/src/components/map-plugins/registry.ts:9` and the stale widget-named test file `frontend/src/components/map-plugins/__tests__/WidgetHost.test.tsx` (both out of scope for this docs/tooling phase; flag for a later cleanup).
</output>
