---
phase: 1161-backend-rename-and-contract
plan: 02
type: execute
wave: 2
depends_on: [1161-01]
files_modified:
  - backend/app/modules/catalog/maps/schemas.py
  - backend/app/modules/catalog/maps/service_crud.py
  - backend/app/modules/catalog/maps/_router_helpers.py
  - backend/app/modules/catalog/maps/router.py
  - backend/app/modules/settings/schemas.py
  - backend/app/modules/settings/router.py
  - backend/tests/test_maps.py
  - backend/tests/test_persistent_config.py
  - backend/openapi.json
autonomous: true
requirements: [BE-RENAME-04, BE-RENAME-05, BE-RENAME-06]
must_haves:
  truths:
    - "The Map API request/response schema uses plugins (no widgets key accepted or emitted)"
    - "The maps service/router/helpers carry the plugins kwarg+attribute (app.api.main imports cleanly)"
    - "The Settings validate_enabled_plugins function + SETTING_VALIDATORS key are renamed; validator logic preserved; measurement/legend stay valid IDs"
    - "The settings config endpoint is /settings/enabled-plugins/ and returns the ENABLED_PLUGINS value"
    - "All backend tests pass under the new plugins/enabled_plugins names (incl. the renamed route path)"
    - "backend/openapi.json is regenerated; make openapi-check and make sdks-check are clean (no drift)"
    - "A backend-wide grep for widget returns only measurement/legend ID values and unrelated matches — no widgets/enabled_widgets container references remain"
  artifacts:
    - path: "backend/app/modules/catalog/maps/schemas.py"
      provides: "Map API plugins field (base/response + create/update)"
      contains: "plugins"
    - path: "backend/app/modules/settings/schemas.py"
      provides: "validate_enabled_plugins + enabled_plugins SETTING_VALIDATORS key"
      contains: "enabled_plugins"
    - path: "backend/app/modules/settings/router.py"
      provides: "/enabled-plugins endpoint + ENABLED_PLUGINS consumer"
      contains: "enabled-plugins"
    - path: "backend/openapi.json"
      provides: "Regenerated OpenAPI snapshot reflecting the plugins contract"
      contains: "plugins"
  key_links:
    - from: "backend/app/modules/catalog/maps/schemas.py"
      to: "Map.plugins ORM column (plan 01)"
      via: "Pydantic field name matching ORM attribute for from_attributes serialization"
      pattern: "plugins:\\s*list"
    - from: "backend/app/modules/settings/router.py"
      to: "ENABLED_PLUGINS config object (plan 01)"
      via: "import + .get(db) in the enabled-plugins endpoint"
      pattern: "ENABLED_PLUGINS"
---

<objective>
Complete the backend rename at the API contract + consumer layer and lock it into the generated
artifacts. Rename: the Map API schema field `widgets`->`plugins` (base/response + create/update), the
maps service/router/helpers that read/write the column, the Settings validator function +
`SETTING_VALIDATORS` key + config endpoint/object consumer, then sweep every remaining backend test
and regenerate `backend/openapi.json` so `make openapi-check` and `make sdks-check` are clean. Hard
breaking cut — no `widgets` alias accepted or emitted.

Purpose: After this plan the entire backend persists, serves, validates, and tests the plugin platform
under the new vocabulary, and the committed contract (openapi.json + SDKs) matches — satisfying
BE-RENAME-04/05/06 and the phase success criteria.
Output: Renamed API contract + consumers, full backend suite green under the new names, a regenerated
clean OpenAPI snapshot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Depends on plan 01: the 0025 migration + Map.plugins ORM column + ENABLED_PLUGINS config object are
# already in place. Pydantic field names here MUST match the ORM attribute name (plugins) for
# from_attributes serialization. This plan ALSO completes the consumer renames (settings/router.py
# imports ENABLED_PLUGINS) that make `import app.api.main` succeed again — plan 01 deliberately left
# that import broken.
@.planning/phases/1161-backend-rename-and-contract/1161-01-SUMMARY.md

<interfaces>
<!-- VALIDATED 2026-05-30 via git grep. The settings layer differs from the brief/REQUIREMENTS:
     there is NO Pydantic `enabled_widgets` field and NO `@field_validator` and NO FeatureTogglesUpdate
     class. The settings surface is a plain validator FUNCTION + a SETTING_VALIDATORS dict + a config
     ENDPOINT + the ENABLED_WIDGETS config object. Use the real targets below, not the brief's. -->

Map API schema — TWO widgets fields (backend/app/modules/catalog/maps/schemas.py):
```python
# :669-673  base/response + create/update shared schema field
    widgets: list[str] | None = Field(
        default=None,
        max_length=50,
        description="Enabled widget IDs, e.g. ['measurement']",
    )
# :751  the lighter create/update schema field
    widgets: list[str] | None = None
```
Rename BOTH to `plugins`. Update the :672 description to "Enabled plugin IDs, e.g. ['measurement']"
(KEEP the `measurement` example — it is an ID value). Hard cut: do NOT add a
validation_alias/serialization_alias for `widgets`.

Maps consumers that read/write the column (MUST rename so app imports after plan 01's model rename):
```python
# service_crud.py:242  update kwarg
    widgets: list[str] | None | object = _UNSET,
# service_crud.py:283-284  apply
    if widgets is not _UNSET:
        map_obj.widgets = cast(list[str] | None, widgets)
# service_crud.py:388  duplicate/fork copy
        widgets=source.widgets,
# _router_helpers.py:292  response builder kwarg
        widgets=map_obj.widgets,
# service_crud.py:260-261 and router.py:774  explanatory comments mentioning "widgets=null"/"widget"
```
Rename every `widgets` kwarg/attr -> `plugins` and update the comments to plugin wording. The
`body.model_dump(exclude_unset=True)` flow in router.py auto-passes the renamed `plugins` key once the
schema field is renamed — confirm the kwarg name in the update call matches the schema field.

Settings validator FUNCTION + dict key (backend/app/modules/settings/schemas.py):
```python
# :359  function def
def validate_enabled_widgets(v: Any) -> list[str] | None:
    if v is None:
        return None
    if not isinstance(v, list):
# :363  error string
        raise ValueError("enabled_widgets must be a list or null")
    for item in v:
        if not isinstance(item, str) or not item.strip():
# :366  error string
            raise ValueError("Each widget ID must be a non-empty string")
    return [item.strip() for item in v]
...
# :414  SETTING_VALIDATORS dict entry — the KEY string is the persisted config key
    "enabled_widgets": validate_enabled_widgets,
```
Rename: function `validate_enabled_widgets` -> `validate_enabled_plugins`, the dict KEY
`"enabled_widgets"` -> `"enabled_plugins"` (must match the migrated DB key + the config object key),
the dict VALUE to the renamed function, and the two error strings to "enabled_plugins must be a list
or null" / "Each plugin ID must be a non-empty string". The validator LOGIC is preserved exactly
(None passthrough, list check, per-item non-empty-string check, strip) — BE-RENAME-05.

Settings router (backend/app/modules/settings/router.py):
```python
# :25 (import block)   ENABLED_WIDGETS,
# :818-826  dual-shape endpoint
@router.get("/enabled-widgets", response_model=list[str] | None, include_in_schema=False)
@router.get("/enabled-widgets/", response_model=list[str] | None)
async def get_enabled_widgets(db: AsyncSession = Depends(get_db)) -> list[str] | None:
    """Return enabled widget IDs. ..."""
    return await ENABLED_WIDGETS.get(db)
```
Rename: import `ENABLED_WIDGETS` -> `ENABLED_PLUGINS`, both route paths `/enabled-widgets` ->
`/enabled-plugins`, function `get_enabled_widgets` -> `get_enabled_plugins`, docstring wording, and
`ENABLED_WIDGETS.get(db)` -> `ENABLED_PLUGINS.get(db)`. This route-path change is BREAKING and the
test in test_persistent_config.py exercises the OLD path — update it (Task 2).

Tests to sweep (NOTE: there is NO test_widgets.py / test_migrations.py — do not invent one;
the migration test was added in plan 01):
```text
backend/tests/test_maps.py            ~20 widget refs (lines 1341-1401): test names, JSON bodies
                                      {"widgets": ...}, and resp.json()["widgets"] assertions.
                                      KEEP the ["legend","measurement"] / ["measurement"] ID values.
backend/tests/test_persistent_config.py ~24 widget refs (lines 517-596): test names, the route path
                                      "/settings/enabled-widgets/" -> "/settings/enabled-plugins/",
                                      and the {"settings": {"enabled_widgets": ...}} bodies ->
                                      "enabled_plugins". KEEP the ["legend","measurement"] ID values.
```

OpenAPI / SDK regeneration (Makefile — VALIDATED targets):
```make
openapi:        cd backend && PYTHONPATH=. uv run python scripts/dump_openapi.py        # writes backend/openapi.json
openapi-check:  cd backend && PYTHONPATH=. uv run python scripts/dump_openapi.py --check # fails on drift
sdks-check:     regenerates SDKs and fails if anything changed (line 155)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename Map schema + maps consumers + Settings validator/router/endpoint to plugins</name>
  <files>backend/app/modules/catalog/maps/schemas.py, backend/app/modules/catalog/maps/service_crud.py, backend/app/modules/catalog/maps/_router_helpers.py, backend/app/modules/catalog/maps/router.py, backend/app/modules/settings/schemas.py, backend/app/modules/settings/router.py</files>
  <behavior>
    - `import app.api.main` succeeds (this plan completes the ENABLED_PLUGINS consumer rename that plan 01 left broken).
    - Map API: a create/update body with `{"plugins": [...]}` is accepted and the response carries `plugins`; a `widgets` key is NOT specially handled (hard cut — unknown/ignored, no alias).
    - Settings: `validate_enabled_plugins(["legend","measurement"])` returns the list; `validate_enabled_plugins("x")` raises "enabled_plugins must be a list or null"; `validate_enabled_plugins(["",])` raises "Each plugin ID must be a non-empty string". `SETTING_VALIDATORS["enabled_plugins"]` is that function and `"enabled_widgets"` is absent.
    - The settings router exposes `/settings/enabled-plugins/` (the old `/enabled-widgets/` 404s) and returns `ENABLED_PLUGINS.get(db)`.
  </behavior>
  <action>
    maps/schemas.py: rename BOTH `widgets` fields (:669 and :751) to `plugins` (BE-RENAME-04); change
    the :672 description to "Enabled plugin IDs, e.g. ['measurement']" (KEEP the measurement example).
    No `widgets` alias.

    maps consumers (BE-RENAME-04 wiring so the app imports): in service_crud.py rename the `widgets`
    kwarg (:242), the apply block (:283-284 `if widgets ... map_obj.widgets = cast(...)`), and the
    duplicate/fork copy (:388 `widgets=source.widgets`); in _router_helpers.py rename the response
    kwarg (:292 `widgets=map_obj.widgets`); update the explanatory comments mentioning "widgets=null"
    in service_crud.py:260-261 and router.py:774 to plugin wording. Confirm the kwarg passed by
    router.py's `model_dump(exclude_unset=True)` flow now reads `plugins` (it follows the schema field
    name automatically).

    settings/schemas.py (BE-RENAME-05): rename function `validate_enabled_widgets` ->
    `validate_enabled_plugins` (:359), error strings (:363, :366) to plugin wording, and the
    `SETTING_VALIDATORS` entry (:414) — both the `"enabled_widgets"` KEY -> `"enabled_plugins"` and the
    VALUE to the renamed function. Preserve the validator logic EXACTLY. The allowed-ID semantics are
    unchanged.

    settings/router.py (BE-RENAME-05): rename the `ENABLED_WIDGETS` import (:25) -> `ENABLED_PLUGINS`,
    both `@router.get` route paths `/enabled-widgets`(+slash) -> `/enabled-plugins`(+slash), function
    `get_enabled_widgets` -> `get_enabled_plugins`, the docstring, and `ENABLED_WIDGETS.get(db)` ->
    `ENABLED_PLUGINS.get(db)`.

    Do NOT rename `measurement`/`legend` anywhere. After editing, grep all six files
    case-insensitively for `widget`: the ONLY allowed matches are `legend`/`measurement` ID strings
    (there are none of those in these files) — i.e. expect ZERO matches.
  </action>
  <verify>
    <automated>cd backend && set -a && source ../.env.test && set +a && uv run python -c "import app.api.main; from app.modules.settings.schemas import validate_enabled_plugins, SETTING_VALIDATORS; assert validate_enabled_plugins(['legend','measurement'])==['legend','measurement']; assert 'enabled_plugins' in SETTING_VALIDATORS and 'enabled_widgets' not in SETTING_VALIDATORS; import pytest; pytest.raises(ValueError, validate_enabled_plugins, 'x'); pytest.raises(ValueError, validate_enabled_plugins, ['']); print('schema+validator+import OK')" && { if grep -riq "widget" app/modules/catalog/maps/schemas.py app/modules/catalog/maps/service_crud.py app/modules/catalog/maps/_router_helpers.py app/modules/catalog/maps/router.py app/modules/settings/schemas.py app/modules/settings/router.py; then echo "FAIL: widget refs remain"; grep -rni "widget" app/modules/catalog/maps/schemas.py app/modules/catalog/maps/service_crud.py app/modules/catalog/maps/_router_helpers.py app/modules/catalog/maps/router.py app/modules/settings/schemas.py app/modules/settings/router.py; exit 1; else echo "grep clean"; fi; }</automated>
  </verify>
  <done>
    `app.api.main` imports; both Map schema fields + all maps consumers use `plugins`;
    `validate_enabled_plugins` + `SETTING_VALIDATORS["enabled_plugins"]` exist with logic intact and
    `enabled_widgets` gone; the settings endpoint is `/enabled-plugins/` returning `ENABLED_PLUGINS`;
    zero `widget` matches across the six files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep backend tests to plugins and run the backend suite green</name>
  <files>backend/tests/test_maps.py, backend/tests/test_persistent_config.py</files>
  <action>
    Update the two test files that reference widgets (BE-RENAME-06). There is NO test_widgets.py to
    rename — do not create one.

    test_maps.py (~20 refs, lines 1341-1401): rename test method names
    (`test_duplicate_preserves_widgets` -> `..._plugins`, `test_update_map_widgets` -> `..._plugins`),
    the request JSON keys `{"widgets": ...}` -> `{"plugins": ...}`, the `resp.json()["widgets"]`
    assertions -> `["plugins"]`, the local `widget_ids` var -> `plugin_ids`, and the docstrings/
    comments. KEEP the ID VALUES `["legend","measurement"]` and `["measurement"]` exactly.

    test_persistent_config.py (~24 refs, lines 517-596): rename test method names
    (`test_enabled_widgets_*` -> `test_enabled_plugins_*`), the route path
    `"/settings/enabled-widgets/"` -> `"/settings/enabled-plugins/"` (matches Task 1's renamed
    endpoint), the request bodies `{"settings": {"enabled_widgets": ...}}` -> `enabled_plugins`, the
    `widget_ids` local -> `plugin_ids`, and docstrings/comments. KEEP the ID VALUES
    `["legend","measurement"]`.

    Then run the focused suites plus a broad import/smoke to catch any straggler reference. Finally
    grep the WHOLE backend (app + tests + alembic) case-insensitively for `widget`: the only allowed
    matches are `measurement`/`legend` ID strings (there should be none of those bare-word matches) —
    i.e. zero `widget`/`widgets`/`enabled_widgets`/`Widget` container matches remain.
  </action>
  <verify>
    <automated>cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/test_maps.py tests/test_persistent_config.py -q && { if grep -rniq "widget" app/ tests/ alembic/versions/ ; then echo "FAIL: widget refs remain"; grep -rni "widget" app/ tests/ alembic/versions/ | grep -vi "legend\|measurement"; exit 1; else echo "grep clean: no container widget refs in backend"; fi; }</automated>
  </verify>
  <done>
    `test_maps.py` and `test_persistent_config.py` pass under the renamed names + route path; a
    backend-wide grep finds no `widget` container references (only `measurement`/`legend` ID values,
    if any, are tolerated). ID values are preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Regenerate OpenAPI + SDK snapshots and assert no drift</name>
  <files>backend/openapi.json</files>
  <action>
    Regenerate the committed contract so it reflects the plugin rename (BE-RENAME-06). From the repo
    root run `make openapi` (writes `backend/openapi.json` via `scripts/dump_openapi.py`). Then verify
    cleanliness with `make openapi-check` (must report no drift) and `make sdks-check` (regenerates the
    TS/Python SDKs and fails if anything changed — so it will incorporate the `plugins` /
    `enabled_plugins` field + the `/settings/enabled-plugins/` path). If `make sdks-check` reports
    changes, run `make sdks` first to regenerate the SDK sources, commit those, then re-run
    `make sdks-check` until clean.

    Sanity-grep the regenerated `backend/openapi.json` for `widget` (case-insensitive): expect ZERO
    container matches (the `plugins` field + `enabled-plugins` path must be present, `widgets` /
    `enabled-widgets` absent). Do NOT hand-edit openapi.json — it is generated.

    Out of scope (do NOT do here): the sibling getgeolens.com docs-site `npm run fetch-openapi` — that
    is a cross-repo post-merge follow-up per REQUIREMENTS "Out of Scope".
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && make openapi >/dev/null && make openapi-check && { if grep -iq "widget" backend/openapi.json; then echo "FAIL: widget in openapi.json"; exit 1; fi; } && grep -q "\"plugins\"" backend/openapi.json && echo "openapi regenerated + clean + has plugins"</automated>
  </verify>
  <done>
    `make openapi` regenerated `backend/openapi.json`; `make openapi-check` and `make sdks-check`
    report no drift; the snapshot contains `plugins` / `enabled-plugins` and no `widget` container
    references; SDK sources regenerated if needed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external API client → Map/Settings API | clients send `plugins`/`enabled_plugins` JSON; the breaking rename removes the old keys |
| public (unauth) → /settings/enabled-plugins/ | the enabled-plugins endpoint is public (no auth) — same as the old enabled-widgets route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1161-04 | Tampering | Settings `validate_enabled_plugins` input | mitigate | Validator preserved exactly: rejects non-list and empty/non-string items with 422 before persistence (covered by the renamed rejects_* tests). |
| T-1161-05 | Information Disclosure | public `/settings/enabled-plugins/` endpoint | accept | Returns only the enabled-plugin ID list (`measurement`/`legend`) — non-sensitive UI config; was already public as `/enabled-widgets/`. |
| T-1161-06 | Denial of Service | Map `plugins` field unbounded list | mitigate | `max_length=50` retained on the base schema field (unchanged by the rename), capping the array size. |
| T-1161-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies; `make sdks` regenerates from the existing toolchain. No package-install task; legitimacy gate N/A. |
</threat_model>

<verification>
- `import app.api.main` succeeds; Map API + all maps consumers + settings validator/router use the plugin vocabulary.
- `/settings/enabled-plugins/` returns the list; old `/settings/enabled-widgets/` 404s.
- `pytest tests/test_maps.py tests/test_persistent_config.py` green; backend-wide grep finds no `widget` container references.
- `make openapi-check` and `make sdks-check` report no drift; `backend/openapi.json` contains `plugins`/`enabled-plugins`, no `widget`.
</verification>

<success_criteria>
- Map API request/response uses `plugins` (BE-RENAME-04); Settings validator + endpoint + config object use `enabled_plugins` with logic preserved and `measurement`/`legend` still valid (BE-RENAME-05).
- All backend tests pass under the new names; `backend/openapi.json` regenerated and `make openapi-check`/`sdks-check` clean (BE-RENAME-06).
- No `widgets`/`enabled_widgets` container references remain anywhere in `backend/`.
</success_criteria>

<output>
Create `.planning/phases/1161-backend-rename-and-contract/1161-02-SUMMARY.md` when done.
</output>
