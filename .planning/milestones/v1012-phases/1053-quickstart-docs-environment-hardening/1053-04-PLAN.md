---
phase: 1053-quickstart-docs-environment-hardening
plan: 04
type: execute
wave: 3
depends_on:
  - 1053-03
files_modified:
  - ~/Code/getgeolens.com/docs/src/content/docs/guides/quickstart/index.mdx
autonomous: true
requirements:
  - DOC-04
  - BU-03

must_haves:
  truths:
    - "The quickstart no longer makes a brittle '1-2 minutes' startup-time claim. Either the claim is removed entirely (deferring to `install.sh`'s wait-for-health output which now blocks on actual readiness per shipped EW-03) or it states a measured range that survives lab variance."
    - "Apple Silicon users running `docker compose up` see a documented note that the `linux/amd64` platform-mismatch warning emitted by Docker is expected and harmless — they don't worry it's a setup error."
  artifacts:
    - path: "~/Code/getgeolens.com/docs/src/content/docs/guides/quickstart/index.mdx"
      provides: "Realistic startup-time language + Apple Silicon platform warning note"
      contains: "Apple Silicon\\|arm64\\|platform"
  key_links:
    - from: "quickstart/index.mdx Step 1"
      to: "geolens scripts/install.sh wait-for-health output"
      via: "removed/qualified startup-time claim"
      pattern: "1-2 minutes|wait-for-health|install.sh"
---

<objective>
Close two small quickstart-doc requirements that touch different sections of `index.mdx`:

- **DOC-04:** The current quickstart claims "Initial startup takes 1-2 minutes" (line ~41 of `index.mdx`). The audit measured ~65s on a tuned host but the literal new-user path fails entirely without the BU-01 fix; even after the BU-01 fix shipped (commit `7b168bde`) and the install.sh wait-for-health shipped (commit `b4ad03d9` for EW-03), the claim is doc-fragile because lab variance and cold-build first runs can produce 3-4 minute startup times. Per CONTEXT.md DOC-04 decision space, option (b) is recommended: defer to `install.sh` output. We'll combine — qualify the claim AND mention that `install.sh` blocks on health so the user has a precise signal.

- **BU-03:** Apple Silicon hosts running `docker compose up` see a warning: `The requested image's platform (linux/amd64) does not match the detected host platform (linux/arm64/v8)`. Per CONTEXT.md BU-03 decision space, option (b) is recommended: document the warning as expected, do NOT add `platform: linux/amd64` declarations to `docker-compose.yml` (cheaper, no perf cost on amd64 hosts).

Both changes are surgical edits inside `index.mdx` — different sections, single commit.

Sequencing: depends on Plan 03 so the quickstart-index edit lineage is clean (each plan = one commit; Plan 02 → Plan 03 → Plan 04). Wave 3.

Purpose: Close DOC-04, BU-03.

Output: One commit on `~/Code/getgeolens.com/main`. Single file edited.

Repo: SIBLING repo. Cross-repo: yes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/1053-quickstart-docs-environment-hardening/1053-CONTEXT.md
@.planning/phases/1053-quickstart-docs-environment-hardening/1053-02-PLAN.md
@.planning/phases/1053-quickstart-docs-environment-hardening/1053-03-PLAN.md

# Source-of-truth: audit's DOC-04 + BU-03 sections
@.planning/M001-7n8vpc-dry-run-audit.md

# File being modified (sibling repo); read AFTER Plan 03 lands
@~/Code/getgeolens.com/docs/src/content/docs/guides/quickstart/index.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Qualify the '1-2 minutes' startup-time claim (DOC-04)</name>
  <files>~/Code/getgeolens.com/docs/src/content/docs/guides/quickstart/index.mdx</files>
  <action>Locate the line in Step 1 that currently reads:

  > The first run takes 1-2 minutes to build images and run database migrations. Subsequent starts use the cached images.

  Replace it with a more accurate, doc-fidelity-robust version that:
  1. Mentions a range, not a point estimate.
  2. References `install.sh`'s wait-for-health gate (shipped at commit `b4ad03d9` per EW-03) so the user knows the script blocks until services are actually healthy.
  3. Calls out the cold-build first-run case explicitly.

  Suggested replacement:

  > Bring-up time depends on host CPU and whether images are already pulled.
  > Expect roughly **1-2 minutes on a tuned host with cached images**, and
  > **3-4 minutes for a cold-build first run** (image build + database
  > migration + service health-check startup). `scripts/install.sh` polls
  > service health before returning, so the script only exits successfully
  > once the stack is actually ready — no need to guess.

  Notes:
  - The "cached images" framing is from the existing prose; preserve the user mental model.
  - Do NOT mention specific BU-01 bypass workarounds or `DATABASE_SSL_MODE=disable` overrides — BU-01 is shipped-fixed; the new-user path no longer needs that workaround.
  - If `install.sh` does NOT currently print a "stack ready" line, soften the second sentence to "`scripts/install.sh` blocks until each service reports healthy". The README/install.sh in this repo (commit `b4ad03d9`) is the source-of-truth for what the script actually prints. The executor can `grep -A5 "wait_for_health\|service_healthy\|all healthy" /Users/ishiland/Code/geolens/scripts/install.sh` to confirm shape before writing the docs claim.
  - This is a 1-2 sentence edit. Do not over-write.
  </action>
  <verify>
    <automated>cd ~/Code/getgeolens.com && ! grep -q "Initial startup takes 1-2 minutes\." docs/src/content/docs/guides/quickstart/index.mdx && grep -q "cold-build\|cached images\|health" docs/src/content/docs/guides/quickstart/index.mdx</automated>
    <human-check>The brittle "1-2 minutes" point claim is gone. The new prose mentions a range, calls out cold-build as a separate case, and references the install.sh wait-for-health behavior.</human-check>
  </verify>
  <done>DOC-04 closed: the quickstart's startup-time language survives lab variance and points users at `install.sh`'s wait-for-health signal as the source of truth.</done>
</task>

<task type="auto">
  <name>Task 2: Add Apple Silicon platform-mismatch note (BU-03)</name>
  <files>~/Code/getgeolens.com/docs/src/content/docs/guides/quickstart/index.mdx</files>
  <action>The current quickstart has no architecture-specific section. Two reasonable placement options — pick (a) based on the audit's recommended Low-friction approach:

  **Option (a) — RECOMMENDED:** Add a small Aside callout at the END of `## 2. Verify services` (currently around lines 60-66). The user has just run `docker compose ps` and may have seen the Apple Silicon warning during the preceding `docker compose up -d`. The note fits naturally here.

  ```mdx
  <Aside type="note" title="Apple Silicon (arm64) hosts">

  On Apple Silicon Macs (M1 / M2 / M3 / M4), `docker compose up` emits this
  warning during the first image pull:

  ```
  The requested image's platform (linux/amd64) does not match the detected
  host platform (linux/arm64/v8) and no specific platform was requested
  ```

  This is **expected and harmless**. The `db` image (Postgres + PostGIS) and
  a couple of supporting images are amd64-only; Docker Desktop runs them
  under Rosetta 2 emulation. The stack works correctly — you may see slightly
  slower postgres startup compared to a native amd64 host. No action needed.

  </Aside>
  ```

  **Option (b) — fallback:** Add it as a bullet in the existing Troubleshooting section of `install.mdx`. Use this only if the quickstart-page word budget is tight after Plans 02-03 expanded it.

  Either option closes BU-03. Default to (a). Do NOT add `platform: linux/amd64` declarations to `docker-compose.yml` in this repo (CONTEXT.md decision space — option (a) is rejected because it forces emulation on amd64 hosts too).
  </action>
  <verify>
    <automated>cd ~/Code/getgeolens.com && grep -q "Apple Silicon\|arm64\|platform.*amd64\|amd64.*platform" docs/src/content/docs/guides/quickstart/index.mdx</automated>
    <human-check>Either the quickstart `index.mdx` (preferred) or `install.mdx` contains an Aside or troubleshooting bullet that names the Apple Silicon platform-mismatch warning verbatim, states it is expected/harmless, and explains the Rosetta emulation cause. No changes to `docker-compose.yml` in the geolens repo.</human-check>
  </verify>
  <done>BU-03 closed via docs path. Apple Silicon users see a clear "expected and harmless" callout for the platform-mismatch warning. No docker-compose.yml changes in this repo.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the cross-repo change</name>
  <files>~/Code/getgeolens.com/docs/src/content/docs/guides/quickstart/index.mdx</files>
  <action>From the sibling repo, stage and commit:

  ```bash
  cd ~/Code/getgeolens.com
  git status -s
  git diff docs/src/content/docs/guides/quickstart/index.mdx
  git add docs/src/content/docs/guides/quickstart/index.mdx
  git commit -m "docs(quickstart): qualify startup-time claim, add Apple Silicon platform warning note (DOC-04, BU-03)" -m "Closes DOC-04 and BU-03 from geolens v1012 / Phase 1053. Replaces the brittle '1-2 minutes' point claim with a range (1-2 min tuned / 3-4 min cold build) and references install.sh's wait-for-health gate (shipped at b4ad03d9). Adds an Apple Silicon arm64 platform-mismatch Aside in Step 2 explaining the docker amd64 image warning is expected and harmless (Rosetta emulation; no action needed)." -m "Cross-repo plan: geolens .planning/phases/1053-quickstart-docs-environment-hardening/1053-04-PLAN.md"
  ```

  Do NOT push.

  Verify:
  ```bash
  cd ~/Code/getgeolens.com
  git log -3 --oneline
  git log -1 --stat
  ```

  At this point there should be three Plan-scoped commits on `main` in the sibling repo: Plan 02 (DOC-01+EW-01) → Plan 03 (DOC-02+DOC-03+DOC-05) → Plan 04 (DOC-04+BU-03).
  </action>
  <verify>
    <automated>cd ~/Code/getgeolens.com && git log -1 --pretty=format:'%s' | grep -q "DOC-04\|BU-03" && git log -1 --stat | grep -q "quickstart/index.mdx" && [ "$(cd ~/Code/getgeolens.com && git log --oneline main -3 | grep -c 'DOC\|EW\|BU')" = "3" ]</automated>
    <human-check>`git log -3 --oneline` in the sibling repo shows three commits in sequence — Plan 02, Plan 03, Plan 04 — each scoped to `index.mdx`. The final commit references DOC-04 + BU-03 in the subject.</human-check>
  </verify>
  <done>One commit landed on `main` of `~/Code/getgeolens.com` with message referencing DOC-04 + BU-03. Single-file change. NOT pushed. The Phase 1053 cross-repo edit lineage is complete (3 commits on the sibling repo + 1 commit in this repo from Plan 01).</done>
</task>

</tasks>

<verification>
Cross-repo acceptance:
- `cd ~/Code/getgeolens.com && git log -3 --oneline` shows three Plan-scoped commits in sequence.
- The full Phase 1053 cross-repo diff is `git diff main~3..main -- docs/src/content/docs/guides/quickstart/index.mdx` — reviewing this single diff shows the full quickstart rewrite for v1012.
- MDX still parses (`npm run build` or `npm run dev` quick check).

This-repo acceptance:
- `git status` in this repo shows ZERO changes (only Plan 01's `.env.example` edit landed in this repo).

No tests required.
</verification>

<success_criteria>
DOC-04 closed:
1. The "1-2 minutes" point claim is replaced with a range (1-2 min cached / 3-4 min cold).
2. `install.sh`'s wait-for-health behavior is mentioned as the source of truth for "stack is ready".

BU-03 closed:
3. Apple Silicon platform-mismatch warning is documented as expected and harmless. Rosetta emulation is named as the cause.
4. NO `platform: linux/amd64` declarations were added to `docker-compose.yml` in this repo (decision space b honored).

Phase 1053 cross-repo lineage complete:
5. Three commits on `~/Code/getgeolens.com/main` (Plan 02 → Plan 03 → Plan 04), each scoped to `docs/src/content/docs/guides/quickstart/index.mdx`. Plan 01's commit lives in this repo, on `.env.example`. Together, all 8 Phase 1053 requirements are closed across the two repos.
</success_criteria>

<output>
Create `.planning/phases/1053-quickstart-docs-environment-hardening/1053-04-SUMMARY.md` when done — record:
- The cross-repo commit SHA.
- Confirmation `git log -3 --oneline` shows Plans 02→03→04 in sequence with no other commits interleaved.
- One-line credit: "DOC-04 + BU-03 closed via cross-repo commit `<sha>`. Phase 1053 cross-repo edit lineage complete."

After this plan's SUMMARY lands, all 8 Phase 1053 requirements are closed:
- This repo: EW-04 (`.env.example`, Plan 01).
- Sibling repo (`~/Code/getgeolens.com`): DOC-01 + EW-01 (Plan 02), DOC-02 + DOC-03 + DOC-05 (Plan 03), DOC-04 + BU-03 (Plan 04).
</output>
