---
phase: 1055
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/DatasetPage.tsx
  - frontend/src/components/dataset/DatasetDetailHeader.tsx
  - frontend/src/components/dataset/__tests__/DatasetDetailHeader.test.tsx
  - frontend/src/i18n/locales/en/dataset.json
  - frontend/src/i18n/locales/de/dataset.json
  - frontend/src/i18n/locales/es/dataset.json
  - frontend/src/i18n/locales/fr/dataset.json
  - e2e/dataset-detail.spec.ts
autonomous: true
requirements:
  - IMPORT-04
must_haves:
  truths:
    - "The 'More actions' overflow trigger has an accessible name AND a visible label/tooltip so audit-style DOM snapshots can find it without expanding the menu"
    - "The 'Re-Upload' menu item carries a stable `data-testid='dataset-header-overflow-reupload'` (already generated dynamically by DatasetDetailHeader) AND an explicit tooltip describing the action"
    - "An e2e regression test asserts the M001-audit-style reachability: navigate to a dataset detail page as admin, find an affordance whose accessible name contains 'Replace' or 'Re-Upload' or 'Reupload', click it, see the reupload dialog open"
    - "The new e2e test runs in the `e2e:smoke:builder` adjacent surface (dataset-detail.spec.ts already runs) and gates v1.3.0 close"
    - "All 4 locale dataset.json files retain string parity (no en-only additions)"
  artifacts:
    - path: "frontend/src/pages/DatasetPage.tsx"
      provides: "Increased reupload affordance priority OR addition of accessible title attribute to make it discoverable"
      contains: "actions.reupload"
    - path: "e2e/dataset-detail.spec.ts"
      provides: "M001-audit-replay test that proves IMPORT-04 affordance is reachable"
      contains: "M001 audit replay"
    - path: "frontend/src/components/dataset/__tests__/DatasetDetailHeader.test.tsx"
      provides: "Unit test asserting the overflow trigger has accessible name and at least one overflow item"
      contains: "overflow trigger accessible name"
  key_links:
    - from: "DatasetPage.tsx headerActions"
      to: "DatasetDetailHeader partitionActions"
      via: "priority sort puts reupload in overflow"
      pattern: "priority: \\d+"
    - from: "e2e/dataset-detail.spec.ts M001 replay test"
      to: "DatasetDetailHeader overflow trigger"
      via: "getByRole('button', { name: 'More actions' })"
      pattern: "More actions"
---

<objective>
Close the M001-7n8vpc audit's "no kebab/overflow menu observed in the snapshot" finding by hardening reupload discoverability and pinning a Playwright regression test that mirrors the auditor's path.

Purpose: The reupload affordance is SHIPPED and works (DatasetPage.tsx:391-398 adds it as a header action with `Upload` icon and `t('actions.reupload')` label; DatasetDetailHeader.tsx partitions actions into 1 primary + N overflow). But the M001 audit (ran 2026-05-19 as admin/admin via Playwright MCP) couldn't find it — the audit explicitly states "No kebab/overflow menu observed in the snapshot." Either the snapshot timing was off, the overflow trigger was visually too small, or the auditor missed a 32×32 icon button with no visible label.

The fix is NOT to surface reupload as a primary button (CONTEXT.md LOCKED: "Use existing action-menu styling, not a primary button — reupload is a deliberate, rare action"). The fix IS to make the overflow trigger more discoverable (visible "More" label OR tooltip), pin the test that asserts reachability via the audit's exact selector pattern, and add a tooltip to the reupload menu item itself so future audits surface it even without expanding the menu.

Output: A reachable overflow trigger with a discoverable accessible name + visible label/tooltip, a new e2e regression test that proves the M001-style auditor path now works, and 4-locale i18n parity for any new strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1055-reupload-feature/1055-CONTEXT.md
@.planning/M001-7n8vpc-dry-run-audit.md
@frontend/src/pages/DatasetPage.tsx
@frontend/src/components/dataset/DatasetDetailHeader.tsx
@frontend/src/components/dataset/__tests__/DatasetDetailHeader.test.tsx
@e2e/dataset-detail.spec.ts
@frontend/src/i18n/locales/en/dataset.json

<interfaces>
<!-- Key existing patterns the executor needs. Do not explore the codebase; use these. -->

DatasetDetailHeader partition (`frontend/src/components/dataset/DatasetDetailHeader.tsx:57-74`):
- `DESKTOP_PRIMARY_ACTION_LIMIT = 1` — only the lowest-priority visible action becomes a primary button
- All other visible actions go into the overflow menu
- The overflow trigger renders only when `overflow.length > 0` (line 153)
- Trigger is a `<Button>` with `<MoreHorizontal />` icon and `aria-label={t('header.moreActions', { defaultValue: 'More actions' })}`
- Trigger has `data-testid="dataset-header-overflow-trigger"`

DatasetPage headerActions (`frontend/src/pages/DatasetPage.tsx:380-416`):
- 4 actions: publish (priority 5), reupload (priority 10), create-vrt (priority 11), delete (priority 20)
- Sort puts publish as primary; reupload + create-vrt (raster only) + delete go to overflow
- `reupload` action: `visible: isEditor && !isVrt`, icon: `Upload`, label: `t('actions.reupload')` = "Re-Upload"

Existing i18n keys (`frontend/src/i18n/locales/en/dataset.json:85`):
- `actions.reupload` = "Re-Upload" (de: "Erneut hochladen", fr: "Recharger", es: "Volver a subir")
- `header.moreActions` = "More actions"

Existing e2e test (`e2e/dataset-detail.spec.ts:62-70`):
- Already asserts overflow trigger button by name 'More actions'
- Already asserts 'Re-Upload' menu item is visible after clicking it
- This proves the affordance is REACHABLE — the M001 audit's "missing" finding is a discoverability problem (the auditor didn't think to click the kebab), not an absence problem

i18n parity check command (must pass after edits): `cd frontend && pnpm run check:i18n` (or equivalent — see `frontend/package.json` scripts)

Note about test infrastructure:
- The auth file at `playwright/.auth/user.json` is created by `e2e/auth.spec.ts` setup
- `e2e/dataset-detail.spec.ts:31-47` already finds a dataset dynamically — reuse this pattern
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add visible "More" label to overflow trigger + tooltip on Re-Upload item</name>
  <files>frontend/src/components/dataset/DatasetDetailHeader.tsx, frontend/src/components/dataset/__tests__/DatasetDetailHeader.test.tsx, frontend/src/i18n/locales/en/dataset.json, frontend/src/i18n/locales/de/dataset.json, frontend/src/i18n/locales/es/dataset.json, frontend/src/i18n/locales/fr/dataset.json</files>
  <behavior>
    The overflow trigger button must become discoverable to a Playwright DOM snapshot at the `<button>` element level — not requiring the auditor to click the kebab first:

    1. The trigger button renders a visible text label `t('header.more', { defaultValue: 'More' })` next to the `<MoreHorizontal />` icon (use a `hidden sm:inline` Tailwind pattern so mobile remains icon-only; on desktop the word "More" is visible)
    2. The trigger keeps its existing `aria-label="More actions"` (more descriptive than the visible label)
    3. The trigger keeps `data-testid="dataset-header-overflow-trigger"`
    4. The Re-Upload menu item (and the other overflow items: create-vrt, delete) get a `title` attribute (HTML tooltip) carrying `t('actions.{id}Tooltip', ...)` so a Playwright snapshot that captures `title` attributes (axe/RTL `accessibleName` falls back to title) surfaces "Replace this dataset's source file" even when the menu is closed

    For (4), the tooltip strings:
    - reupload: "Replace this dataset's source file"
    - create-vrt: "Create a VRT mosaic from this raster"
    - delete: "Delete this dataset"
    - publish/unpublish: skip (those are primary, not in overflow)

    Add i18n keys to all 4 locales:
    - en: `header.more` = "More", `actions.reuploadTooltip` = "Replace this dataset's source file", `actions.createVrtTooltip` = "Create a VRT mosaic from this raster", `actions.deleteTooltip` = "Delete this dataset"
    - de/es/fr: equivalent translations (use existing translation style for the locale — `actions.reupload` is already translated in each, so match its tone)

    Unit test additions in `DatasetDetailHeader.test.tsx`:
    - Test that when overflow.length > 0, the trigger button is queryable by `getByRole('button', { name: 'More actions' })` AND by visible text 'More'
    - Test that overflow menu items render with `title` attribute (use `getByTitle` or check `.getAttribute('title')`)
  </behavior>
  <action>
    Modify `DatasetDetailHeader.tsx` lines 153-180:
    - Change the trigger button to render `<MoreHorizontal />` + `<span className="hidden sm:inline">{t('header.more', { defaultValue: 'More' })}</span>` (icon + label on desktop, icon-only on mobile)
    - Keep the existing `aria-label` and `data-testid`
    - For each `DropdownMenuItem` inside the overflow, pass a `title={action.tooltip}` HTML attribute. To make this work, extend the `DatasetDetailHeaderAction` interface (lines 27-36) with an optional `tooltip?: string` field.

    Modify `DatasetPage.tsx` lines 380-416 to populate the `tooltip` field on each action object using `t('actions.reuploadTooltip')`, `t('actions.createVrtTooltip')`, `t('actions.deleteTooltip')`. Do NOT add a tooltip on the publish/unpublish action since those become primary buttons where tooltips would clutter.

    Modify the 4 locale `dataset.json` files. Find the existing `actions` object (around line 85 in en, similar in others) and add the three new `*Tooltip` keys alongside the existing labels. Add the `header.more` key to the existing `header` object (around line 89 in en).

    Translations:
    - de: `header.more` = "Mehr", `actions.reuploadTooltip` = "Quelldatei dieses Datensatzes ersetzen", `actions.createVrtTooltip` = "VRT-Mosaik aus diesem Raster erstellen", `actions.deleteTooltip` = "Diesen Datensatz löschen"
    - es: `header.more` = "Más", `actions.reuploadTooltip` = "Reemplazar el archivo fuente de este conjunto de datos", `actions.createVrtTooltip` = "Crear un mosaico VRT a partir de este ráster", `actions.deleteTooltip` = "Eliminar este conjunto de datos"
    - fr: `header.more` = "Plus", `actions.reuploadTooltip` = "Remplacer le fichier source de ce jeu de données", `actions.createVrtTooltip` = "Créer une mosaïque VRT à partir de ce raster", `actions.deleteTooltip` = "Supprimer ce jeu de données"

    Modify `DatasetDetailHeader.test.tsx` to add:
    - A test `'overflow trigger renders with visible More label and aria-label'` asserting both the aria-label and the visible text content
    - A test `'overflow menu items carry tooltip title attributes'` asserting the `title` attribute is present on the rendered `DropdownMenuItem`

    Per project conventions (existing test file patterns), follow the same render/userEvent setup the existing tests use. Do not re-render existing test cases.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest --run src/components/dataset/__tests__/DatasetDetailHeader.test.tsx && pnpm run check:i18n</automated>
  </verify>
  <done>
    DatasetDetailHeader.test.tsx passes (existing tests still pass + 2 new tests pass). i18n parity check passes for all 4 locales. The new "More" label is visible on desktop builds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add M001-audit-replay e2e regression test</name>
  <files>e2e/dataset-detail.spec.ts</files>
  <behavior>
    Add a new test inside the existing `test.describe('Dataset Detail', ...)` block titled `'IMPORT-04: M001 audit replay — reupload affordance is reachable via accessible name'`. The test must:

    1. Navigate to a vector dataset detail page (use the existing `openAdminCountriesDataset(page)` helper — already in the file)
    2. Find an affordance whose accessible name OR visible text contains 'Re-Upload' OR 'Replace' OR 'Reupload' OR the word 'More' (this mirrors what an audit-style DOM scan would find by reading button text or `accessibleName`)
    3. Click it (if it's the "More" trigger, that opens the overflow; if it's a future primary button, that opens the dialog directly)
    4. After click, assert the reupload dialog is open by finding `getByRole('dialog')` with name matching /Re-Upload Dataset/i (the dialog title is `t('reupload.title')` = "Re-Upload Dataset")
    5. Close the dialog (press Escape) and assert it's gone

    The test must NOT depend on a specific overflow-vs-primary placement so future UX iterations remain free to promote reupload to primary without breaking the test.

    Importantly, the test must FAIL if reupload is unreachable (which is the exact regression that M001 reported). Without this test, future audits will silently miss the affordance again.
  </behavior>
  <action>
    Open `e2e/dataset-detail.spec.ts`. Inside the `test.describe('Dataset Detail', ...)` block, after the existing test on line 49 (`'map renders, attribute table loads, export triggers download'`), add a new test:

    ```
    test('IMPORT-04: M001 audit replay — reupload affordance is reachable via accessible name', async ({ page }) => {
      await openAdminCountriesDataset(page);

      // Mirror M001 audit method: scan the page for any affordance whose accessible name
      // mentions Replace / Re-Upload / Reupload / More. If reupload is hidden behind
      // an unlabeled kebab again, this locator will resolve to the affordance OR fail.
      const reuploadAffordance = page.getByRole('button', {
        name: /Re-Upload|Replace|Reupload|More/i,
      });
      // First() because there may be both a "More" trigger and a "Re-Upload" item
      // visible (when overflow is expanded by a previous step).
      await expect(reuploadAffordance.first()).toBeVisible({ timeout: 5_000 });
      await reuploadAffordance.first().click();

      // If the click opened the overflow menu, click the Re-Upload menuitem inside it.
      const reuploadMenuItem = page.getByRole('menuitem', { name: /Re-Upload/i });
      if (await reuploadMenuItem.isVisible().catch(() => false)) {
        await reuploadMenuItem.click();
      }

      // Either path leads to the reupload dialog.
      await expect(page.getByRole('dialog', { name: /Re-Upload Dataset/i })).toBeVisible({
        timeout: 5_000,
      });

      // Close dialog and confirm it's gone (regression for SF-style snapshot leakage).
      await page.keyboard.press('Escape');
      await expect(page.getByRole('dialog', { name: /Re-Upload Dataset/i })).toHaveCount(0);
    });
    ```

    Do NOT modify the existing test on line 49 — it asserts the current overflow placement specifically (which is a valid regression test for the LOCKED CONTEXT.md decision that reupload stays in overflow). The new test is more flexible (accepts either primary OR overflow) and is the audit-shape regression.

    Verify the spec runs in the existing dataset-detail e2e job. Check `playwright.config.ts` or the package.json `e2e:*` scripts: if `dataset-detail.spec.ts` is already in the smoke set, you're done; if not, ensure it's added to the smoke matrix (most likely it already is, since the existing test asserts reupload reachability via the old path).
  </action>
  <verify>
    <automated>cd frontend && pnpm playwright test e2e/dataset-detail.spec.ts -g "M001 audit replay" --reporter=line</automated>
  </verify>
  <done>
    The new test passes against a live stack with seeded datasets. The existing test on line 49 still passes. The test is in the smoke set (runs as part of the v1056 close gate).
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `cd frontend && pnpm vitest --run src/components/dataset/__tests__/DatasetDetailHeader.test.tsx` — unit tests pass
2. `cd frontend && pnpm run check:i18n` — all 4 locales have parity
3. `cd frontend && pnpm playwright test e2e/dataset-detail.spec.ts` — both tests pass (existing + new)
4. Optional sanity: open `localhost:8080/datasets/{any-vector-dataset-id}` in a browser and confirm the "More" label is visible next to the kebab icon
</verification>

<success_criteria>
- The overflow trigger has BOTH an `aria-label="More actions"` AND a visible `More` label (responsive: hidden on mobile, visible on desktop)
- Each overflow menu item has a `title` HTML tooltip so audit-style DOM snapshots can surface the action even without expanding the menu
- 4-locale i18n parity for the 4 new keys (`header.more`, `actions.reuploadTooltip`, `actions.createVrtTooltip`, `actions.deleteTooltip`)
- A new e2e test `IMPORT-04: M001 audit replay` pins reupload reachability via the audit-style auditor path
- All existing tests continue to pass
</success_criteria>

<output>
Create `.planning/phases/1055-reupload-feature/1055-02-SUMMARY.md` when done. Note:
- The audit's missed-finding root cause (small unlabeled kebab) and the fix (visible "More" label + tooltips on overflow items)
- Why reupload is NOT promoted to primary (CONTEXT.md locked decision: "deliberate, rare action")
- The new e2e regression test that prevents the next audit from missing the affordance
</output>