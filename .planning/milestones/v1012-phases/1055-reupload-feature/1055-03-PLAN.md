---
phase: 1055
plan: 03
type: execute
wave: 2
depends_on:
  - 1055-01
  - 1055-02
files_modified:
  - .planning/phases/1055-reupload-feature/1055-VERIFY.md
autonomous: false
requirements:
  - IMPORT-04
must_haves:
  truths:
    - "A fresh-stack live-MCP run reproduces the M001 audit's path (admin/admin login → search → upload audit-test.geojson → open dataset detail) and confirms the Re-Upload affordance is visible"
    - "Clicking the affordance opens the reupload dialog, file selection works, commit returns 202, and the dataset detail page reflects the new file without a page reload"
    - "Audit log contains a new `reupload.commit` entry with the original + new filename in details"
    - "Dataset ID and slug are unchanged after the reupload"
    - "VERIFY.md captures the live run as evidence the next M001-style audit would PASS this surface"
  artifacts:
    - path: ".planning/phases/1055-reupload-feature/1055-VERIFY.md"
      provides: "Live-MCP verification trace + snapshot references + PASS verdict for ROADMAP success criteria 1-3"
      contains: "## Live MCP Verification"
  key_links:
    - from: "1055-VERIFY.md"
      to: "ROADMAP Phase 1055 Success Criteria"
      via: "explicit numbered PASS/FAIL per criterion"
      pattern: "Success Criterion #\\d:"
---

<objective>
Verify Phase 1055 ROADMAP success criteria end-to-end against a live `localhost:8080` stack via Playwright MCP, reproducing the M001-7n8vpc audit's exact path and confirming the audit's IMPORT-04 finding is closed.

Purpose: Plans 01 and 02 close the technical gaps (cross-record-type guard, discoverability hardening, regression test). This plan provides the **evidence** that the v1.3.0 close gate (Phase 1056 CTRL-01) needs to mark IMPORT-04 as PASS. CTRL-01 explicitly requires "live verification on a fresh `localhost:8080` stack against the new-user flow (login → search → import → register table → seed catalog → dataset detail)". This plan delivers the dataset-detail → reupload slice of that verification.

The M001 audit ran on 2026-05-19 and missed reupload entirely. The audit's recommendation said: "Surface a 'Reupload' button in the dataset detail action row (or in a kebab) that opens an in-place file-picker." Plan 02 hardened discoverability + added a regression test. Plan 03 proves the surface is actually discoverable to a live auditor running the same Playwright MCP shape.

This is a checkpoint plan — the orchestrator (operating Playwright MCP) executes it. There is no Claude executor for live MCP because MCP is orchestrator-scoped (per v1010.1/v1011 lessons learned).

Output: A `1055-VERIFY.md` evidence document with per-success-criterion PASS/FAIL verdicts, snapshot timestamps, and any inline-fixable findings closed within the checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1055-reupload-feature/1055-CONTEXT.md
@.planning/M001-7n8vpc-dry-run-audit.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live Playwright MCP verification of M001 audit replay</name>
  <files>.planning/phases/1055-reupload-feature/1055-VERIFY.md</files>
  <action>
    The orchestrator drives Playwright MCP through the full M001 audit replay. There is no Claude executor.

    Summary of what Plans 01 + 02 just shipped (and what this checkpoint verifies is reachable + functional in a live stack):

    Plan 01 — Backend: `_assert_compatible_record_type` guard added to `router_reupload.py` at both `reupload_dataset` and `request_presigned_reupload` entry points; cross-record-type swaps now return 400 with a record_type-aware error message; 3 new pinned pytest cases in `TestReuploadUpload`.

    Plan 02 — Frontend: Overflow trigger now renders a visible "More" label next to the kebab icon on desktop (icon-only on mobile); overflow items carry HTML `title` tooltips; e2e regression test `IMPORT-04: M001 audit replay` added to `dataset-detail.spec.ts`; 4-locale i18n parity for 4 new keys (`header.more`, `actions.reuploadTooltip`, `actions.createVrtTooltip`, `actions.deleteTooltip`).

    Pre-existing (verified during planning, NOT modified by this phase): `backend/app/modules/catalog/datasets/api/router_reupload.py` (613 LOC), `backend/app/processing/ingest/tasks_reupload.py` with `_apply_reupload_swap`, `frontend/src/components/dataset/ReuploadDialog.tsx` (743 LOC) with file dropzone + service-URL probe + schema diff preview, `DatasetPage.tsx` lines 391-398 wiring reupload into header overflow, audit emission at `tasks_common.py:955` with `action="reupload.commit"` (pinned by `test_provenance_attribution.py:339`), 30+ backend pytest cases in `test_reupload.py` + `test_reupload_service.py`.

    LIVE MCP STEPS — Orchestrator executes these in order:

    PRE — Fresh stack:
    - `docker compose down -v && docker compose up -d --build` (clean PG + workers)
    - Wait for healthchecks: `docker compose ps` shows all services Healthy
    - Seed minimal data: `python scripts/seed-natural-earth.py --username admin --password admin` — only need 1-2 datasets to seed the catalog; the audit-test dataset is uploaded fresh in Step 2
    - Confirm Plans 01+02 committed: `git log --oneline -5` shows commits for plans 01 + 02

    STEP 1 (login, mirrors M001 R24):
    - Playwright MCP: navigate to `http://localhost:8080/login`
    - Fill username = admin, password = admin
    - Submit; assert redirect to `/`

    STEP 2 (upload audit-test.geojson, mirrors M001 I01):
    - Navigate to `/import` (or click Import nav)
    - Choose File tab; upload a small GeoJSON. Use `.playwright-mcp/audit-fixtures/audit-test.geojson` if present (226 bytes, 1 Point feature, EPSG:4326). If absent, create one inline with payload `{"type":"FeatureCollection","features":[{"type":"Feature","properties":{"name":"audit-test"},"geometry":{"type":"Point","coordinates":[-73.9857,40.7484]}}]}`
    - Commit upload; capture the resulting dataset ID

    STEP 3 (open dataset detail, mirrors M001 IMPORT-04 path):
    - Navigate to `/datasets/{audit-test-id}`
    - Capture Playwright MCP page snapshot — SAVE to `.playwright-mcp/1055-verify-snapshot-1.yml`

    STEP 4 (validate Success Criterion #1 — reupload affordance visible):
    - In the snapshot from Step 3, look for ONE of:
      - A primary button with text Re-Upload / Replace / Reupload (would mean reupload was promoted to primary — should NOT be the case per CONTEXT.md LOCK)
      - A button labeled "More actions" OR with visible text "More" (overflow trigger per Plan 02)
    - If NEITHER found, FAIL Success Criterion #1; capture details for inline fix
    - If "More" trigger found, click it; capture snapshot 2 to `.playwright-mcp/1055-verify-snapshot-2.yml`; assert a menu item labeled "Re-Upload" is visible

    STEP 5 (validate Success Criterion #2 — reupload flow works):
    - Click the Re-Upload menu item; assert reupload dialog opens (modal title contains "Re-Upload Dataset")
    - Choose "File" source; upload a modified GeoJSON with 2 features (add a second feature so the swap is observable): `{"type":"FeatureCollection","features":[{"type":"Feature","properties":{"name":"audit-test-v2"},"geometry":{"type":"Point","coordinates":[-73.9857,40.7484]}},{"type":"Feature","properties":{"name":"audit-test-v2-second"},"geometry":{"type":"Point","coordinates":[-122.4194,37.7749]}}]}`
    - Wait for preview step to populate (schema diff view shows row_count_delta = +1)
    - Click "Confirm Re-Upload"; assert toast or status "queued" / "running"
    - Wait for job completion. Poll `/api/datasets/{id}` until `feature_count` reflects new file (expect 2 features, was 1). Timeout 60s.
    - Capture snapshot 3 to `.playwright-mcp/1055-verify-snapshot-3.yml`

    STEP 6 (validate Success Criterion #3 — detail page reflects update without page reload):
    - Assert WITHOUT a page reload:
      - `feature_count` on the page now shows 2
      - `last_edited_at` is recent
      - Map renders the second feature
    - If a manual reload was required, FAIL Success Criterion #3 (TanStack Query invalidation may be broken — investigate ReuploadDialog.tsx lines 162-167)

    STEP 7 (validate audit log, mirrors CONTEXT.md `<decisions>` LOCKED):
    - Open `/admin/audit`
    - Filter for action `reupload.commit` for the audit-test dataset
    - Confirm entry exists with: `dataset_id` = audit-test-id, original + new `source_filename` in details, plus `source_format` and `version_number`

    STEP 8 (validate identity preservation):
    - Dataset ID at `/datasets/{audit-test-id}` is unchanged after reupload (URL stability confirms)
    - Slug/table_name unchanged: GET `/api/datasets/{audit-test-id}` BEFORE and AFTER; assert `id` and `table_name` fields equal

    STEP 9 (write VERIFY.md):
    - Create `.planning/phases/1055-reupload-feature/1055-VERIFY.md` with sections:
      - `## Live MCP Verification` (date, stack version, MCP version)
      - `## Pre-requisites verified` (stack healthy, dataset seeded, plans 01+02 committed)
      - `## Step-by-step trace` (Steps 1-8 with snapshot filenames + verdicts)
      - `## Success Criterion #1 — Re-Upload visible: PASS|FAIL` with snapshot evidence
      - `## Success Criterion #2 — File picker + re-ingest preserves ID/slug + audit log: PASS|FAIL`
      - `## Success Criterion #3 — Detail page reflects update without page reload: PASS|FAIL`
      - `## Audit log entry verified` (audit row dumped or screenshot)
      - `## M001 audit gap closed verdict` (one-sentence: "IMPORT-04 closed — reupload reachable, functional, audit-logged, identity-preserved")
      - `## Findings discovered during verification` (any inline-fixed issues OR new defects needing follow-up)

    SUB-CHECKPOINT — If any of Steps 4-8 FAIL, do NOT write VERIFY.md as PASS:
    - FAIL on Success Criterion #1: backtrack to Plan 02 — discoverability not yet sufficient
    - FAIL on Success Criterion #2: reupload pipeline broken — needs investigation
    - FAIL on Success Criterion #3: TanStack Query invalidation needs investigation in ReuploadDialog.tsx (already has invalidation at lines 162-167 — verify it fires)
    - Capture the FAIL in VERIFY.md with reproduction steps and propose follow-up plan
  </action>
  <verify>
    <automated>test -f .planning/phases/1055-reupload-feature/1055-VERIFY.md && grep -q "Success Criterion #1" .planning/phases/1055-reupload-feature/1055-VERIFY.md && grep -q "Success Criterion #2" .planning/phases/1055-reupload-feature/1055-VERIFY.md && grep -q "Success Criterion #3" .planning/phases/1055-reupload-feature/1055-VERIFY.md && grep -q "PASS\|FAIL" .planning/phases/1055-reupload-feature/1055-VERIFY.md</automated>
    <human-check>
      Operator confirms via Playwright MCP that the M001 audit replay PASSes all 3 ROADMAP success criteria, the audit log emits `reupload.commit`, and the dataset's ID/slug are unchanged. VERIFY.md is committed.
    </human-check>
  </verify>
  <done>
    `.planning/phases/1055-reupload-feature/1055-VERIFY.md` exists with explicit PASS verdicts for all 3 ROADMAP success criteria. M001 audit's IMPORT-04 finding is closed (the next M001-style audit running the same MCP path would now find the affordance). Any findings discovered during live verification are either inline-fixed or captured as follow-ups.
  </done>
  <resume-signal>
    After running the live MCP verification, respond with one of:
    - `approved` — all 3 success criteria PASS, VERIFY.md written, IMPORT-04 closed
    - `approved with findings: <description>` — criteria PASS but new defects found and tracked
    - `failed: <criterion> — <reason>` — verification failed; propose follow-up
  </resume-signal>
</task>

</tasks>

<verification>
The checkpoint output IS the verification. VERIFY.md must exist and contain explicit PASS/FAIL per ROADMAP success criterion. The grep gate in `<automated>` blocks the plan from completing if VERIFY.md is missing or malformed.
</verification>

<success_criteria>
- ROADMAP Phase 1055 Success Criterion #1 verified PASS by live MCP: "Replace file" affordance visible on dataset detail page
- ROADMAP Phase 1055 Success Criterion #2 verified PASS by live MCP: file picker → re-ingest preserves ID/slug, regenerates tiles + thumbnail, writes audit-log entry
- ROADMAP Phase 1055 Success Criterion #3 verified PASS by live MCP: dataset detail reflects update without page reload
- `1055-VERIFY.md` exists with snapshot references and per-criterion verdicts
- M001 audit's IMPORT-04 finding is closed (the next M001-style audit running the same MCP path would PASS this surface)
</success_criteria>

<output>
Create `.planning/phases/1055-reupload-feature/1055-03-SUMMARY.md` when done. The 1055-VERIFY.md is the primary evidence artifact; the SUMMARY captures plan-level completion + any followups.
</output>