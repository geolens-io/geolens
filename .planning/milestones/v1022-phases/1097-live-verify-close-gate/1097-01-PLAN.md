---
phase: 1097-live-verify-close-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - .planning/phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md
  - .planning/phases/1097-live-verify-close-gate/1097-01-SUMMARY.md
autonomous: true
requirements:
  - CLOSE-01
user_setup: []

must_haves:
  truths:
    - "Sequential `cd backend && uv run pytest tests/` produces 3060 passed / 3 pre-existing OOS / 38 skipped (0 NEW failures vs Phase 1096 close)"
    - "`cd backend && uv run pytest -n 4 tests/` produces 3057 passed / 6 OOS (3 pre-existing + ~3 oauth/flake) / 38 skipped (0 NEW failures vs Phase 1096 close)"
    - "3 consecutive `pytest -n auto` runs with stale-DB cleanup between runs each produce ≤30 distinct (failed + errors) deterministically (Phase 1096 floor: 5/2/2)"
    - "All 9 retry-pin family tests + 2 pool-sizing invariants PASS in spot-check (3 init_tile_pool + 4 engine_retry + 1 yields_event_loop + 1 do_connect = 9; xdist_nullpool + sequential_queuepool = 2)"
    - "`docker compose ps` shows 5 services healthy (api, db, frontend, titiler, worker) AND `curl -sI http://localhost:8080/api/health/` returns 200"
    - "CHANGELOG.md `[1.5.7]` block exists listing PARA-01, PARA-02, HYG-01, CI-01 (placeholder GREEN/RED) with test pin names + line numbers"
    - "1097-01-CLOSE-GATE.md exists with all three baseline tables + docker health spot-check evidence + log file pointers"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "`[1.5.7]` block with per-requirement evidence per CLOSE-01 (e)"
      contains: "## [1.5.7] - 2026-05-24"
    - path: ".planning/phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md"
      provides: "Close-gate evidence document for CLOSE-01 (a)/(b)/(c)/(d); CI-01 live-verify section appended in Plan 02"
      min_lines: 80
    - path: ".planning/phases/1097-live-verify-close-gate/1097-01-SUMMARY.md"
      provides: "Plan 01 rollup with verification gate matrix"
      min_lines: 40
  key_links:
    - from: ".planning/phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md"
      to: "/tmp/v1022-1097-close-gate-sequential.log + /tmp/v1022-1097-close-gate-n4.log + /tmp/v1022-1097-close-gate-nauto-run{1,2,3}.{log,xml}"
      via: "log-file pointer block (CLOSE-01 (a)+(b)+(c) evidence)"
      pattern: "/tmp/v1022-1097-close-gate-"
    - from: "CHANGELOG.md `[1.5.7]` block"
      to: "test pins (test_init_tile_pool_retries_on_transient_too_many_clients line 1144; test_engine_retry_yields_event_loop_during_backoff line 1253; test_engine_retry_do_connect_event_handler_retries_on_transient_error line 1391; test_init_tile_pool_catches_raw_asyncpg_too_many_connections line 1557; test_init_tile_pool_propagates_non_transient_error line 1666)"
      via: "literal pin-name + line-number citation per CLOSE-01 (e)"
      pattern: "test_fixture_isolation_v1020.py:(1144|1253|1391|1557|1666)"
---

<objective>
Close-gate evidence capture for CLOSE-01. Re-confirm the three Phase-1096 baselines (sequential / `-n 4` / `-n auto` 3-run), spot-check live docker stack health, write the CHANGELOG `[1.5.7]` block, and produce the CLOSE-GATE.md document with verbatim quotes from all three runs.

Purpose: Pin the milestone-tip test invariant numbers to a known-good local state BEFORE pushing to remote (Plan 02). The close-gate doc + CHANGELOG provide the "before" snapshot that Plan 02 augments with the CI-01 live-verify log + tag-cut commit SHA. No code changes, no tag cuts, no remote push in this plan — the v1022 close-gate commit SHA captured by Plan 02 will be the parent of Plan 01's close commit.

Output: CHANGELOG.md `[1.5.7]` block + 1097-01-CLOSE-GATE.md + 1097-01-SUMMARY.md committed atomically as a 3-file commit per CONTEXT.md decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/1097-live-verify-close-gate/1097-CONTEXT.md
@.planning/phases/1096-hygiene-tail/1096-VERIFICATION.md
@.planning/audits/PYTEST-XDIST-PERF-v1020.md
@CHANGELOG.md

<interfaces>
<!-- Source-of-record line numbers verified via `git grep -n` at plan-time (2026-05-24). -->
<!-- Executor should cite these EXACT line numbers in CHANGELOG + CLOSE-GATE.md. -->

CI gate (read-only — no edits in this phase):
- .github/workflows/ci.yml:499 — `pytest-parallel-isolation:` (job definition)
- .github/workflows/ci.yml:500 — `name: Pytest Parallel Isolation`
- .github/workflows/ci.yml:590 — last `run:` line (`uv run pytest -n 4 -v --tb=short -m 'not perf'`)
- .github/workflows/ci.yml:593 — next job (`frontend-lint:`)
- Job range: lines 499-590 (inclusive); plan02 will cite this range when quoting the gh run log.

Regression pins (all in backend/tests/test_fixture_isolation_v1020.py — verified via `git grep -n`):
- L1144 — test_init_tile_pool_retries_on_transient_too_many_clients (PARA-01 / Phase 1095-01)
- L1253 — test_engine_retry_yields_event_loop_during_backoff (PARA-02 / Phase 1095-02 — Shape Y2 token-assertion pin)
- L1391 — test_engine_retry_do_connect_event_handler_retries_on_transient_error (HYG-01 WR-01 / Phase 1096-01)
- L1557 — test_init_tile_pool_catches_raw_asyncpg_too_many_connections (HYG-01 WR-01-1095 carry / Phase 1096-01)
- L1666 — test_init_tile_pool_propagates_non_transient_error (HYG-01 WR-01-1095 carry / Phase 1096-01)

Existing engine-retry family (4 pins — Phase 1093 / TEST-01):
- test_engine_retry_succeeds_on_transient_too_many_clients
- test_engine_retry_catches_raw_asyncpg_too_many_connections
- test_engine_retry_propagates_non_transient_operational_error
- test_engine_retry_exhausts_budget_then_fails_loudly

Pool-sizing invariants (in backend/tests/test_conftest_pool_sizing.py):
- L245 — test_xdist_engine_uses_nullpool
- L271 — test_sequential_engine_uses_queuepool

Wrapper invariant tokens (in backend/tests/conftest.py — single-definition):
- L333 — `_SETUP_PHASE_RETRY_BACKOFFS = (1.0, 2.0, 4.0)`
- L352 — `_TRANSIENT_CONTENTION_EXCEPTIONS = (...)`
- L842 — `except (TypeError, AttributeError, InvalidRequestError):` (WR-03)
- L958-962 — `event.remove(self._sync_engine, "do_connect", self._do_connect_handler)` (WR-04)

Stale-DB cleanup recipe (from PYTEST-XDIST-PERF-v1020.md Section 1 Step 1b):
- Pattern: `docker compose exec -T db psql -U geolens -d geolens -At -c "SELECT datname FROM pg_database WHERE datname LIKE 'geolens%test_gw%' OR datname LIKE 'geolens%test_master%';"`
- Then loop: `xargs -I{} docker compose exec -T db psql -U geolens -d geolens -c "DROP DATABASE IF EXISTS {} WITH (FORCE);"`

Test-environment activation recipe (MEMORY.md — `backend/tests` host-port mapping footgun):
- `cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/`
- The `.env.test` sets `POSTGRES_HOST=localhost` + `POSTGRES_PORT=5434` (docker-compose host-port map for db service).
- WITHOUT this prefix, tests fail with `InvalidCatalogNameError: database "geolens_test_master_..." does not exist`.

Phase 1096 close baselines (from 1096-VERIFICATION.md, the numbers Plan 01 must re-confirm):
- Sequential: 3060 passed / 3 OOS / 38 skipped (540s) — OOS = test_layering, test_phase_275, test_ssrf_redirect
- `-n 4`: 3057 passed / 6 OOS / 38 skipped (328s) — 3 pre-existing OOS + ~3 oauth/flake-class
- `-n auto` 3-run: distinct 5/2/2 deterministic (453s + 452s + 454s), 0 ICN frames in all 3 runs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — stack health + working tree + retry-pin spot-check</name>
  <files>(read-only checks; no files modified)</files>
  <action>
Confirm baseline preconditions before executing the close-gate measurement runs. Three checks in sequence:

(1) Working tree clean: run `git status --short`. Expect zero output. If any modified/untracked file appears, stash or abort — close-gate measurements MUST land on a clean tree so the captured baselines are attributable to the milestone-tip commit and nothing else.

(2) Docker stack 5-services-healthy: run `docker compose ps`. Expect five rows (api, db, frontend, titiler, worker), all `Up ... (healthy)`. If any service is unhealthy or missing, run `docker compose up -d` and re-check. The stack must be hot because Task 2-4's pytest runs use the live `db` container (port 5434).

(3) 37-test retry-pin spot-check: run `cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/test_fixture_isolation_v1020.py tests/test_conftest_pool_sizing.py::test_xdist_engine_uses_nullpool tests/test_conftest_pool_sizing.py::test_sequential_engine_uses_queuepool -q 2>&1 | tail -20`. The retry-pin subset (32 v1020 + 5 v1022 = 37) is the smallest gate that proves the engine wrapper invariants are intact. If the count differs from 37 or any test fails, do NOT proceed to Task 2 — the milestone-tip state is not what we believe it is.

Tooling note (MEMORY.md): if you skip the `set -a && source ../.env.test && set +a` prefix, pytest will fail with `InvalidCatalogNameError: database "geolens_test_master_..." does not exist` because POSTGRES_HOST/POSTGRES_PORT default to api-internal values. ALWAYS include the prefix for backend tests.
  </action>
  <verify>
    <automated>git status --short | wc -l | grep -qx 0 && docker compose ps --format "{{.State}}" | grep -c healthy | grep -qx 5 && cd backend &amp;&amp; set -a &amp;&amp; source ../.env.test &amp;&amp; set +a &amp;&amp; uv run pytest tests/test_fixture_isolation_v1020.py tests/test_conftest_pool_sizing.py::test_xdist_engine_uses_nullpool tests/test_conftest_pool_sizing.py::test_sequential_engine_uses_queuepool -q 2>&amp;1 | tail -5 | grep -E "passed.*in [0-9]+\.[0-9]+s"</automated>
  </verify>
  <done>
Working tree shows zero modified/untracked files (`git status --short` exit 0 with empty stdout). `docker compose ps` shows 5 healthy services. Retry-pin spot-check shows 37 tests in the `passed` line (e.g., `37 passed in X.Xs`) with zero failures. If any of the three checks fails, halt and surface the failure shape — do NOT proceed to Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-confirm sequential baseline (Phase 1096 floor: 3060/3 OOS/38)</name>
  <files>/tmp/v1022-1097-close-gate-sequential.log (capture file; not committed)</files>
  <action>
Run the full sequential pytest suite at milestone-tip and capture the result for verbatim quoting in CLOSE-GATE.md.

Command: `cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/ 2>&1 | tee /tmp/v1022-1097-close-gate-sequential.log`.

Expected result (per Phase 1096 VERIFICATION.md): `3 failed, 3060 passed, 38 skipped in ~540s`. The 3 failures are pre-existing OOS rows documented in REQUIREMENTS.md (Out of Scope table):
- `tests/test_layering.py::test_layering_LOC_cap_decomposition` — LOC-cap decomposition tech-debt (deferred to future hygiene)
- `tests/test_phase_275.py::test_phase_275_readme_sync` — README sync drift (deferred)
- `tests/test_ssrf_redirect.py::test_ssrf_redirect` — flake-class (documented OOS)

HARD INVARIANT (v1019 TD-13): `0 NEW failures attributable to v1022`. The 3 pre-existing failures MAY remain. If the log shows `4+ failed` or any NEW failure name not in the OOS table above, halt and surface the failure shape — CLOSE-01 (a) is not satisfied.

Runtime budget: ~540s (~9 min); set timeout accordingly via Bash tool's `timeout` parameter (660000 ms = 11 min ceiling).

Do NOT commit the log file. Plan 01's atomic-3-file commit covers CHANGELOG.md + CLOSE-GATE.md + SUMMARY.md only; the log lives at `/tmp/` and gets referenced via path in CLOSE-GATE.md.
  </action>
  <verify>
    <automated>tail -3 /tmp/v1022-1097-close-gate-sequential.log | grep -E "3 failed.*3060 passed.*38 skipped" && grep -v "^#" /tmp/v1022-1097-close-gate-sequential.log | grep -cE "(^FAILED|^ERROR)" | grep -qxE "(3|0)"</automated>
  </verify>
  <done>
Log file `/tmp/v1022-1097-close-gate-sequential.log` exists and final summary line matches `3 failed, 3060 passed, 38 skipped in ...s`. The 3 failures are the documented OOS triad (test_layering + test_phase_275 + test_ssrf_redirect) — verify by inspecting `FAILED` lines in the log. ZERO NEW failures outside the OOS triad. HARD INVARIANT preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-confirm `-n 4` baseline (Phase 1096 floor: 3057/6 OOS/38)</name>
  <files>/tmp/v1022-1097-close-gate-n4.log (capture file; not committed)</files>
  <action>
Run pytest with `-n 4` (CI default per PERF-01 + project_pytest_n4_ci_default.md) and capture for CLOSE-GATE.md.

Stale-DB cleanup FIRST (per PYTEST-XDIST-PERF-v1020.md Section 1 Step 1b — required before parallel runs to avoid prior-run perturbation):
```
docker compose exec -T db psql -U geolens -d geolens -At -c \
  "SELECT datname FROM pg_database WHERE datname LIKE 'geolens%test_gw%' OR datname LIKE 'geolens%test_master%';" \
  | xargs -I{} docker compose exec -T db psql -U geolens -d geolens -c "DROP DATABASE IF EXISTS \"{}\" WITH (FORCE);"
```

Then run: `cd backend && set -a && source ../.env.test && set +a && uv run pytest -n 4 tests/ 2>&1 | tee /tmp/v1022-1097-close-gate-n4.log`.

Expected result (per Phase 1096 VERIFICATION.md): `6 failed, 3057 passed, 38 skipped in ~328s`. The 6 failures are 3 pre-existing OOS + ~3 oauth flake-class (documented in PYTEST-XDIST-PERF-v1020.md Section 2 n=4 row + REQUIREMENTS.md PARA-02 closure). Tolerance: 4-7 failures acceptable per CLOSE-01 (b) "0 NEW failed" language — the oauth flake distribution has documented jitter, but the 3 pre-existing OOS rows MUST be present.

HARD INVARIANT: 0 NEW failures attributable to v1022. NEW = not in the documented OOS triad + not in the oauth-flake class documented at PYTEST-XDIST-PERF-v1020.md Section 2.

Runtime budget: ~330s (~5.5 min); set timeout to 480000 ms (8 min ceiling).
  </action>
  <verify>
    <automated>tail -3 /tmp/v1022-1097-close-gate-n4.log | grep -E "([4-7]) failed.*30(5[5-9]|60) passed.*38 skipped"</automated>
  </verify>
  <done>
Log file `/tmp/v1022-1097-close-gate-n4.log` exists and final summary line matches the `-n 4` shape: 4-7 failures (3 pre-existing OOS + up to 3 oauth flake-class), 3055-3060 passed, 38 skipped. ZERO NEW failures outside the OOS + documented oauth flake-class. HARD INVARIANT preserved.
  </done>
</task>

<task type="auto">
  <name>Task 4: Re-confirm `-n auto` 3-run baseline (Phase 1096 floor: 5/2/2 distinct deterministic ≤30)</name>
  <files>/tmp/v1022-1097-close-gate-nauto-run{1,2,3}.{log,xml} (6 capture files; not committed)</files>
  <action>
Run pytest with `-n auto` THREE consecutive times with stale-DB cleanup between each run. This is the PARA-01 acceptance gate per CLOSE-01 (c) — must produce ≤30 distinct (failed+errors) per run deterministically.

For each RUN ∈ {1, 2, 3}:

(1) Stale-DB cleanup (same recipe as Task 3 — required between every -n auto run; Step 1b explicitly mandates it for representative cascade timing).

(2) Run: `cd backend && set -a && source ../.env.test && set +a && uv run pytest -n auto tests/ --junit-xml=/tmp/v1022-1097-close-gate-nauto-run${RUN}.xml 2>&1 | tee /tmp/v1022-1097-close-gate-nauto-run${RUN}.log`.

(3) Extract distinct (failed + errors) count from junit XML: `python3 -c "import xml.etree.ElementTree as ET; r=ET.parse('/tmp/v1022-1097-close-gate-nauto-run${RUN}.xml').getroot(); t=r.find('.//testsuite'); print(int(t.get('failures',0))+int(t.get('errors',0)))"`. Append to a tracker.

Expected result (Phase 1096 close floor): each run distinct ≤30 (target ≤10 to match the 5/2/2 floor). Acceptance per CLOSE-01 (c): ≤30 per run, deterministic across all 3 runs. The PARA-01 acceptance gate is `each run ≤30 distinct`, not `sum ≤30` — each run is independently measured.

If ANY of the 3 runs exceeds 30 distinct, the PARA-01 invariant is broken. Halt and surface the failure shape — close-gate cannot ship.

Cascade-frame check (defense-in-depth): `grep -c "InvalidCatalogNameError" /tmp/v1022-1097-close-gate-nauto-run${RUN}.log` should be 0 for each run (Phase 1096 close had ZERO ICN frames). Non-zero ICN frames indicate Category 4.1 cascade has returned and CLOSE-01 (c) is broken.

Runtime budget: ~450s × 3 runs = ~1350s (~22.5 min) PLUS stale-DB cleanup overhead. Total: ~25 min wall-clock; set per-run timeout to 600000 ms (10 min) and accept that this task is the longest single task in the phase.
  </action>
  <verify>
    <automated>for r in 1 2 3; do python3 -c "import xml.etree.ElementTree as ET; root=ET.parse('/tmp/v1022-1097-close-gate-nauto-run${r}.xml').getroot(); ts=root.find('.//testsuite'); d=int(ts.get('failures','0'))+int(ts.get('errors','0')); icn=__import__('subprocess').check_output(['grep','-c','InvalidCatalogNameError','/tmp/v1022-1097-close-gate-nauto-run${r}.log']).strip().decode(); print(f'run${r}: distinct={d} icn={icn}'); assert d <= 30, f'run${r} distinct {d} > 30 PARA-01 acceptance gate'; assert icn == '0', f'run${r} ICN {icn} != 0 cascade returned'"; done</automated>
  </verify>
  <done>
All 3 `-n auto` runs produced ≤30 distinct (failed + errors) per CLOSE-01 (c) PARA-01 acceptance gate. All 3 runs produced 0 `InvalidCatalogNameError` cascade frames. The 6 capture files exist at `/tmp/v1022-1097-close-gate-nauto-run{1,2,3}.{log,xml}` for CLOSE-GATE.md citation.
  </done>
</task>

<task type="auto">
  <name>Task 5: Live docker stack health spot-check + optional Playwright MCP visit</name>
  <files>(read-only checks; no files modified)</files>
  <action>
Two checks per CLOSE-01 (d), plus one optional defense-in-depth check per CONTEXT.md `--use-playwright-mcp` directive:

(1) Re-run `docker compose ps` (Task 1's check is now ~30+ min stale after Tasks 2-4's parallel pytest hammering — re-verify nothing crashed under the parallel test load). Expect 5 services still healthy.

(2) `curl -sI http://localhost:8080/api/health/ | head -1` → expect `HTTP/1.1 200 OK` exactly. The trailing slash is intentional — both shapes work post-v1021 ROUTE-01 (`redirect_slashes=False` at `backend/app/api/main.py:484` + `_add_trailing_slash_aliases` at `backend/app/api/main.py:642`), but the trailing-slash shape matches CLOSE-01 (d)'s literal acceptance criterion language.

(3) OPTIONAL Playwright MCP browser visit to `http://localhost:8080`: navigate, wait for catalog page, check console for 0 errors, snapshot. Per CONTEXT.md Phase 1097 `<decisions>` Playwright MCP usage section — NOT required for CLOSE-01 acceptance, but adds defense-in-depth signal that v1022's test-infra-only changes did not somehow regress production app behavior. Skip if MCP tool not available or if time-budget pressure (3 `-n auto` runs already consumed ~25 min). If you do run it, capture a one-line "MCP catalog page render: OK / N console errors" status for CLOSE-GATE.md.

Defense-in-depth rationale for (3): v1022 is test-infra-hygiene-only — all changes live in `backend/tests/conftest.py` + `backend/tests/test_fixture_isolation_v1020.py` + `.planning/`. There is no plausible production-code regression path from v1022 changes. The MCP check is symbolic confirmation, not load-bearing evidence.
  </action>
  <verify>
    <automated>docker compose ps --format "{{.State}}" | grep -c healthy | grep -qx 5 && curl -sI http://localhost:8080/api/health/ | head -1 | grep -qE "HTTP/[12](\.[01])? 200"</automated>
  </verify>
  <done>
`docker compose ps` shows 5 healthy services. `curl -sI http://localhost:8080/api/health/` returns `HTTP/1.1 200 OK` (or `HTTP/2 200`). If Playwright MCP visit was performed, console-error count captured for CLOSE-GATE.md citation. CLOSE-01 (d) satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 6: Write CHANGELOG `[1.5.7]` + CLOSE-GATE.md + SUMMARY.md + atomic-3-file commit</name>
  <files>
CHANGELOG.md,
.planning/phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md,
.planning/phases/1097-live-verify-close-gate/1097-01-SUMMARY.md
  </files>
  <action>
Three documents, ONE atomic commit. Per CONTEXT.md `<decisions>` "Atomic-N-file commit per plan" rule — Plan 01's commit modifies exactly 3 files; no other files in the same commit.

(1) **CHANGELOG.md** — Replace the placeholder under `## [Unreleased]` (line 12-14 — currently reads `_No entries yet — open the next milestone with /gsd:new-milestone to begin v1022._`) with the `[1.5.7]` block per CONTEXT.md `<decisions>` template:

```markdown
## [Unreleased]

_No entries yet — open the next milestone with `/gsd:new-milestone` to begin v1023._

## [1.5.7] - 2026-05-24

Test-infrastructure hygiene — closes v1021 carry-forward: per-worker DB lifecycle parallel-mode cascade (PARA-01) + WR-02 sleep footgun (PARA-02) + WR-01/03/04 engine-retry envelope hygiene (HYG-01) + `pytest-parallel-isolation` CI gate live-verify (CI-01).

### Test Infra

- **PARA-01 (Phase 1095-01):** Wrap `_init_tile_pool_for_tests` `asyncpg.create_pool` in existing `_run_with_too_many_clients_retry` envelope at 3 sibling call sites (`backend/tests/test_tiles.py:152`, `backend/tests/test_embed_tokens.py:57`, `backend/tests/test_tile_signing.py:108`) — closes the per-worker DB lifecycle parallel-mode cascade. `pytest -n auto` distinct failures: pre-fix 126-383 per run (v1021 close) → 20/8/16 (Plan 1095-01) → 3/2/3 (Plan 1095-02) → **5/2/2 (Phase 1096 close)**. Regression pin: `backend/tests/test_fixture_isolation_v1020.py::test_init_tile_pool_retries_on_transient_too_many_clients` at line 1144.

- **PARA-02 (Phase 1095-02):** Close WR-02 `_invoke_sleep_in_sync_context` loop-starvation footgun via Shape Y2 load-bearing rationale (greenlet context cannot yield via `asyncio.run` — Shape Y1 attempt produced 658 `RuntimeError: asyncio.run() cannot be called from a running event loop` cascade failures at Plan 1095-02 Task 5 Run 1; the production caller path via SQLAlchemy `greenlet_spawn` has a running loop in the calling thread). The structural mitigation lives at PARA-01's 3 fixture-site wraps. Regression pin: `backend/tests/test_fixture_isolation_v1020.py::test_engine_retry_yields_event_loop_during_backoff` at line 1253 (Shape Y2 static-text token assertion — silent removal breaks CI).

- **HYG-01 (Phase 1096-01):**
  - **WR-03:** Narrowed `except Exception:` at `backend/tests/conftest.py:842` to `except (TypeError, AttributeError, InvalidRequestError):` (3 documented SQLAlchemy event-API failure shapes; tuple expanded from plan-spec 2 classes to 3 when MagicMock test doubles surfaced `sqlalchemy.exc.InvalidRequestError` under SQLAlchemy 2.x — Rule 1 documented deviation).
  - **WR-04:** Added `event.remove(self._sync_engine, "do_connect", self._do_connect_handler)` teardown call in `_RetryingAsyncEngine.dispose()` override at `backend/tests/conftest.py:958-962` (idempotent ref reset; None-guarded for test doubles).
  - **WR-01 + WR-01-1095 carry-forward:** 3 new regression pins in `backend/tests/test_fixture_isolation_v1020.py` — `test_engine_retry_do_connect_event_handler_retries_on_transient_error` at line 1391 (exercises the load-bearing `do_connect` event-handler retry path via `engine.dialect.dispatch.do_connect` — Rule 3 documented deviation from `engine.dispatch.do_connect` per SQLAlchemy DialectEvents surface), `test_init_tile_pool_catches_raw_asyncpg_too_many_connections` at line 1557 (fixture-layer parity with engine-layer raw-asyncpg pin), `test_init_tile_pool_propagates_non_transient_error` at line 1666 (fixture-layer parity with engine-layer propagation pin).

### CI

- **CI-01 (Phase 1097-02):** First post-merge live-verify of `pytest-parallel-isolation` CI gate at `.github/workflows/ci.yml:499-590` (added v1020 Phase 1089). Status: **[GREEN/RED — populated by Plan 02 close]** (Plan 1097-02 captures the `gh run watch` output and replaces this placeholder with verbatim job-step log block).

### Known Limitations

- 3 pre-existing OOS sequential failures preserved (out of v1022 scope per REQUIREMENTS.md Out of Scope table): `tests/test_layering.py::test_layering_LOC_cap_decomposition` (LOC-cap decomposition tech-debt); `tests/test_phase_275.py::test_phase_275_readme_sync` (README sync drift); `tests/test_ssrf_redirect.py::test_ssrf_redirect` (flake-class). Closing these is its own future hygiene milestone.

### Migrations

None. All v1.5.7 changes are test-infra hygiene (`backend/tests/conftest.py` + `backend/tests/test_fixture_isolation_v1020.py` + `.planning/`). No API contract changes, no schema changes, no production-code behavior changes beyond the test-fixture engine layer.
```

KEY: keep `[Unreleased]` heading present (Keep-a-Changelog convention — never delete it; just empty out its body and start the next milestone in a new `[1.5.7]` block immediately below). Reword the Unreleased body to point at v1023.

(2) **1097-01-CLOSE-GATE.md** — Create the close-gate evidence document. Structure:

```markdown
---
phase: 1097-live-verify-close-gate
plan: 01
status: in-progress
captured: 2026-05-24
requirements_addressed: [CLOSE-01]
---

# Phase 1097: Close-Gate Evidence Document

**Status:** IN PROGRESS (Plan 01 — baselines captured; CI-01 live-verify pending Plan 02)
**Captured:** 2026-05-24

## CLOSE-01 (a) — Sequential baseline

Verbatim final summary from `/tmp/v1022-1097-close-gate-sequential.log`:

```
<paste last 3 lines of /tmp/v1022-1097-close-gate-sequential.log here — must include the "3 failed, 3060 passed, 38 skipped in Xs" summary>
```

| Metric | Phase 1096 Close | Phase 1097 Close | Delta |
|--------|------------------|------------------|-------|
| passed | 3060 | <paste> | 0 NEW |
| failed | 3 OOS | <paste> | 0 NEW |
| skipped | 38 | <paste> | 0 |
| runtime | 540s | <paste> | — |

Pre-existing OOS failures (documented in REQUIREMENTS.md Out of Scope table):
1. `tests/test_layering.py::test_layering_LOC_cap_decomposition` (LOC-cap decomposition tech-debt)
2. `tests/test_phase_275.py::test_phase_275_readme_sync` (README sync drift)
3. `tests/test_ssrf_redirect.py::test_ssrf_redirect` (flake-class)

**HARD INVARIANT preserved:** 0 NEW failures attributable to v1022. CLOSE-01 (a) SATISFIED.

## CLOSE-01 (b) — `-n 4` baseline

Verbatim final summary from `/tmp/v1022-1097-close-gate-n4.log`:

```
<paste last 3 lines>
```

| Metric | Phase 1096 Close | Phase 1097 Close | Delta |
|--------|------------------|------------------|-------|
| passed | 3057 | <paste> | <delta> |
| failed | 6 (3 OOS + ~3 oauth/flake) | <paste> | <delta> |
| skipped | 38 | <paste> | 0 |
| runtime | 328s | <paste> | — |

Failure breakdown: 3 documented OOS + ~3 oauth-flake-class (per PYTEST-XDIST-PERF-v1020.md Section 2 n=4 row).

**HARD INVARIANT preserved:** 0 NEW failures attributable to v1022. CLOSE-01 (b) SATISFIED.

## CLOSE-01 (c) — `-n auto` 3-run measurement

Captured to `/tmp/v1022-1097-close-gate-nauto-run{1,2,3}.{log,xml}` with stale-DB cleanup between every run (per PYTEST-XDIST-PERF-v1020.md Section 1 Step 1b).

| Run | distinct (failed+errors) | ICN frames | Phase 1096 floor | PARA-01 gate |
|-----|--------------------------|------------|------------------|--------------|
| 1 | <paste> | 0 | 5 | ≤30 PASS |
| 2 | <paste> | 0 | 2 | ≤30 PASS |
| 3 | <paste> | 0 | 2 | ≤30 PASS |

**PARA-01 acceptance gate PASSED:** each run ≤30 distinct deterministically across 3 consecutive runs. Zero `InvalidCatalogNameError` cascade frames across all 3 runs. CLOSE-01 (c) SATISFIED.

## CLOSE-01 (d) — Live docker stack health spot-check

```
$ docker compose ps
<paste output — must show 5 healthy services: api, db, frontend, titiler, worker>

$ curl -sI http://localhost:8080/api/health/ | head -1
HTTP/1.1 200 OK
```

Optional Playwright MCP visit to `http://localhost:8080`: <captured status from Task 5 (3) or "skipped — symbolic confirmation only">.

**CLOSE-01 (d) SATISFIED.**

## CLOSE-01 (e) — CHANGELOG `[1.5.7]` cross-reference

See `CHANGELOG.md` `[1.5.7]` block (this commit). Lists PARA-01 + PARA-02 + HYG-01 closures with the test pin names + line numbers per CLOSE-01 (e). CI-01 status placeholder will be replaced by Plan 02.

**CLOSE-01 (e) SATISFIED (partial — CI-01 status pending Plan 02).**

## CLOSE-01 (f) — CI-01 live-verify

**PENDING Plan 1097-02.** Plan 02 will:
1. AskUserQuestion confirmation before `git push origin main` (this commit is the close-gate commit; Plan 02 push triggers CI).
2. `gh run watch $RUN_ID` for the `pytest-parallel-isolation` job.
3. Embed the relevant log block in a new section appended to this file (CLOSE-01 (f) section).

## CLOSE-01 (g) — Tags cut

**PENDING Plan 1097-02.** Tags `v1022` (local) + `v1.5.7` (public) will be cut at the close-gate commit SHA (this commit) AFTER CI-01 is GREEN. Recorded in `.planning/MILESTONES.md`.

---

_Plan 01 close (this commit): baselines captured, CHANGELOG written, ready for Plan 02 push gate._
```

Fill the `<paste>` placeholders with the actual captured values from Tasks 2-5. The doc gets appended by Plan 02 — don't pre-write Plan 02's sections.

(3) **1097-01-SUMMARY.md** — Plan 01 rollup using the standard SUMMARY template. Required fields:

```markdown
---
phase: 1097-live-verify-close-gate
plan: 01
completed: 2026-05-24
status: success
requirements_completed: []  # CLOSE-01 NOT flipped until Plan 02 close (tags must be cut first)
files_modified:
  - CHANGELOG.md
  - .planning/phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md
  - .planning/phases/1097-live-verify-close-gate/1097-01-SUMMARY.md
---

# Plan 1097-01: Close-Gate Baselines + CHANGELOG — SUMMARY

## Verification Gate Matrix

| Gate | Expected | Actual | Status |
|------|----------|--------|--------|
| Pre-flight: working tree clean | 0 modified | <fill> | PASS/FAIL |
| Pre-flight: docker 5 services healthy | 5 healthy | <fill> | PASS/FAIL |
| Pre-flight: 37-pin spot-check | 37 passed | <fill> | PASS/FAIL |
| Task 2: sequential | 3060/3 OOS/38 | <fill> | PASS/FAIL |
| Task 3: -n 4 | 3057/4-7/38 | <fill> | PASS/FAIL |
| Task 4: -n auto run 1 distinct ≤30 | ≤30 | <fill> | PASS/FAIL |
| Task 4: -n auto run 2 distinct ≤30 | ≤30 | <fill> | PASS/FAIL |
| Task 4: -n auto run 3 distinct ≤30 | ≤30 | <fill> | PASS/FAIL |
| Task 4: ICN frames all runs | 0/0/0 | <fill> | PASS/FAIL |
| Task 5: curl /api/health/ | 200 | <fill> | PASS/FAIL |
| Task 6: CHANGELOG [1.5.7] block | present | <fill> | PASS/FAIL |
| Task 6: CLOSE-GATE.md exists | present | <fill> | PASS/FAIL |

## Deviations from Plan

<list any task-spec deviations; ideally none for a measurement-only plan>

## Next: Plan 1097-02

Plan 02 will: AskUserQuestion before push → `git push origin main` → `gh run watch` for `pytest-parallel-isolation` → embed log block → cut tags v1022 + v1.5.7 → flip CI-01 + CLOSE-01 traceability → atomic-4-file commit.
```

(4) **Atomic-3-file commit** — stage ONLY these 3 files; no other files in the same commit (CONTEXT.md atomic-N rule):

```
git add CHANGELOG.md \
  .planning/phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md \
  .planning/phases/1097-live-verify-close-gate/1097-01-SUMMARY.md
git commit -m "docs(1097-01): CLOSE-01 close-gate baselines + CHANGELOG [1.5.7]"
```

Verify the commit landed cleanly: `git log -1 --stat` should show exactly 3 files. The commit SHA from this step is the close-gate SHA that Plan 02 will use for `git tag v1022 <SHA> && git tag v1.5.7 <SHA>`.

CRITICAL — do NOT flip CLOSE-01 in REQUIREMENTS.md in this plan. The CLOSE-01 `[x]` flip lands in Plan 02's atomic-4-file commit ALONGSIDE the CI-01 flip + tag references. Flipping CLOSE-01 here would split traceability across two commits and violate v1019 TD-13 `requirements_traceability_flip` rule.
  </action>
  <verify>
    <automated>git log -1 --stat --format="%H %s" | head -1 | grep -E "^[0-9a-f]{7,40} docs\(1097-01\): CLOSE-01 close-gate baselines \+ CHANGELOG \[1\.5\.7\]" && git log -1 --name-only --format="" | grep -cE "(CHANGELOG\.md|1097-01-CLOSE-GATE\.md|1097-01-SUMMARY\.md)" | grep -qx 3 && grep -q "## \[1\.5\.7\] - 2026-05-24" CHANGELOG.md && grep -q "CLOSE-01 (a) — Sequential baseline" .planning/phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md && grep -q "Verification Gate Matrix" .planning/phases/1097-live-verify-close-gate/1097-01-SUMMARY.md</automated>
  </verify>
  <done>
Atomic-3-file commit landed with message `docs(1097-01): CLOSE-01 close-gate baselines + CHANGELOG [1.5.7]`. Exactly 3 files in the commit (CHANGELOG.md + 1097-01-CLOSE-GATE.md + 1097-01-SUMMARY.md); ZERO other files. CHANGELOG `[1.5.7]` block exists with PARA-01 + PARA-02 + HYG-01 + CI-01 (placeholder) entries. CLOSE-GATE.md has all 5 sections (a)-(e) populated with verbatim baseline quotes. SUMMARY.md has gate matrix with all rows populated. CLOSE-01 NOT flipped in REQUIREMENTS.md (lands in Plan 02). Ready for Plan 02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local pytest → docker db | Test fixtures connect to `localhost:5434` (mapped from db service); no external surface |
| local plan files → committed history | Plan 01 commits 3 .md files; no executable code path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1097-01 | Tampering | `/tmp/v1022-1097-close-gate-*.log` capture files | accept | Tmp files; no integrity claim — verbatim quoted into CLOSE-GATE.md which IS committed and tamper-evident via git history |
| T-1097-02 | Repudiation | CLOSE-GATE.md baseline numbers | mitigate | All numbers quoted verbatim with raw log path + line range citation; reviewer can re-run the same recipe to reproduce |
| T-1097-03 | Information Disclosure | Test log files at `/tmp/` | accept | Test data only (no PII, no secrets in test fixtures per v1018 SEC-S16 fixture pin); tmp-cleared on reboot |
| T-1097-04 | Denial of Service | 3× `-n auto` runs consuming ~25 min + docker resources | accept | Documented runtime budget; no production stack impact; sequential gating prevents concurrent test load |
| T-1097-SC | Tampering | npm/pip/cargo installs | accept | NO package installs in this plan — measurement-only + .md edits |

No package installs in Plan 01 → no Package Legitimacy Audit required.
</threat_model>

<verification>
Plan 01 verification gates (all MUST PASS for plan to be COMPLETE):

1. Sequential `3060 passed / 3 OOS / 38 skipped` (Task 2) — HARD INVARIANT
2. `-n 4` `3055-3060 passed / 4-7 OOS+flake / 38 skipped` (Task 3) — HARD INVARIANT
3. `-n auto` 3-run each ≤30 distinct deterministic (Task 4) — PARA-01 acceptance
4. `-n auto` 3-run ICN frames 0/0/0 (Task 4) — Category 4.1 cascade gate
5. `docker compose ps` 5 healthy + `curl -sI http://localhost:8080/api/health/` 200 (Task 5) — CLOSE-01 (d)
6. CHANGELOG `[1.5.7]` block written with all 5 pins + line numbers cited (Task 6)
7. CLOSE-GATE.md sections (a)/(b)/(c)/(d)/(e) populated with verbatim quotes (Task 6)
8. Atomic-3-file commit landed (Task 6) — CONTEXT.md atomic-N rule
9. CLOSE-01 NOT yet flipped in REQUIREMENTS.md (lands in Plan 02) — v1019 TD-13 traceability rule
</verification>

<success_criteria>
Plan 1097-01 succeeds when:
- All 6 tasks PASS their <verify> blocks
- The 3-file atomic commit lands with SHA captured for Plan 02's tag-cut
- CLOSE-GATE.md is half-complete (Plan 02 appends CI-01 (f) + (g) sections)
- CHANGELOG `[1.5.7]` block lists every test pin name + line number per CLOSE-01 (e)
- HARD INVARIANT preserved across sequential + `-n 4` baselines (0 NEW failures)
- PARA-01 acceptance preserved across `-n auto` 3 runs (each ≤30 distinct + 0 ICN frames)
- No code changes, no tag cuts, no remote push (those land in Plan 02)
</success_criteria>

<output>
Create `.planning/phases/1097-live-verify-close-gate/1097-01-SUMMARY.md` when done. Commit message: `docs(1097-01): CLOSE-01 close-gate baselines + CHANGELOG [1.5.7]`. Capture commit SHA for Plan 02's tag-cut.
</output>
