---
phase: 1081-test-fixture-assertion-drift
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_phase_279_user_lifecycle.py
autonomous: true
requirements: [TD-02, TD-03]
tags: [pytest, password-policy, SEC-S16, test-fixture-drift, hygiene]

must_haves:
  truths:
    - "After Plan 01 lands, running `pytest backend/tests/test_phase_279_user_lifecycle.py -x` in the per-worker test DB exits 0 — both ADMIN-05 register-audit tests (the actual TD-02 / TD-03 surfaces) pass under SEC-S16 (12-char + 3-of-4 class) password policy."
    - "Both fixed tests still pin their original audit-event contract — the password change is a fixture update, not an assertion or behavior change."
    - "Zero `pytest.mark.skip` decorators are added to `test_phase_279_user_lifecycle.py`."
  artifacts:
    - path: "backend/tests/test_phase_279_user_lifecycle.py"
      provides: "ADMIN-05 register-audit pin + ADMIN-06 user-FK delete-behavior pin"
      contains: "TestPass1234!"  # the strong-password fixture mirror that the conftest already uses
  key_links:
    - from: "backend/tests/test_phase_279_user_lifecycle.py"
      to: "backend/app/modules/auth/password_policy.py"
      via: "POST /auth/register/ body — registration triggers validate_password_from_settings()"
      pattern: "validate_password_complexity"
    - from: "backend/tests/test_phase_279_user_lifecycle.py::test_register_emits_user_register_audit"
      to: "backend/app/modules/audit/models.py:AuditLog"
      via: "SELECT AuditLog WHERE action='user.register' — pinned outcome of a successful registration"
      pattern: "AuditLog.action == 'user.register'"
---

<objective>
Close TD-02 + TD-03 by updating the password fixtures in two `test_phase_279_user_lifecycle.py` tests to satisfy the v1014 SEC-S16 password policy (12-char minimum + 3-of-4 class diversity). The tests' actual contract (ADMIN-05 `user.register` audit emission and the disabled-registration negative case) is unaffected — they fail today only because the inline password literal `"securepass123"` (lowercase + digit = 2 classes, 13 chars) is rejected by `validate_password_complexity` before the audit/registration path runs.

Purpose: restore the ADMIN-05 audit-event pin under the current post-SEC-S16 password policy so future register-route refactors that silently drop `user.register` audit events still trip these tests. Test-only fix; zero production code touched.

Output: one atomic commit updating the two password fixtures, pytest 0 for the named test file.
</objective>

<scope_notes>
**Critical path correction (planner discovery):** The REQUIREMENTS.md / ROADMAP / 1081-CONTEXT.md text names the failing tests as `test_register_password_too_short` and `test_register_password_diversity`. **Those tests do not exist in the codebase.** `grep -rln "test_register_password_too_short\|test_register_password_diversity" backend/` returns zero hits.

The actual failing tests (per Phase 1075-05 VERIFICATION.md NEW-DISCOVERY table rows #2 and #3) are:
- `test_phase_279_user_lifecycle.py::test_register_emits_user_register_audit` (line 131)
- `test_phase_279_user_lifecycle.py::test_register_disabled_does_not_emit_audit` (line 187)

Both fail because they POST a registration with `"password": "securepass123"` — which has only 2 character classes (lowercase + digit), so v1014 SEC-S16's 3-of-4 diversity rule (configurable via `PASSWORD_REQUIRE_CLASSES`, default 3) rejects it at the `/auth/register/` boundary with a `ValueError` re-raised as Pydantic 422.

This plan honours the spirit of TD-02 / TD-03 — "align password tests to SEC-S16" — at the correct file and the correct line numbers. The verification step uses the actual test names, not the misnamed ones from the planning context.

**Production code path (read in `<read_first>`):** `backend/app/modules/auth/password_policy.py` is the canonical validator. Its `validate_password_from_settings` reads `settings.password_min_length` (default 12) and `settings.password_require_classes` (default 3). The `securepass123` literal violates the diversity rule (length 13 OK; classes = 2 < 3).

**Fixture mirror:** `backend/tests/conftest.py:491` already uses `"TestPass1234!"` as the SEC-S16-compliant default for test-created users. Reuse that exact literal in both tests so the project standard stays single-sourced.

**Single atomic commit (mirrors Plan 1075-02 / 1075-04 precedent):** two tests, one root cause (SEC-S16 fixture drift), one commit. The commit message must enumerate both fixed test names + the path-correction note for traceability.
</scope_notes>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/1081-test-fixture-assertion-drift/1081-CONTEXT.md

# Precedent SUMMARYs (root-cause fixture drift pattern)
@.planning/milestones/v1017-phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-02-SUMMARY.md
@.planning/milestones/v1017-phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-04-SUMMARY.md

<interfaces>
<!-- Production code the test pins. Executor must read password_policy.py to confirm the exact ValueError messages so the verification step asserts the actual policy rejection shape if needed. -->

From backend/app/modules/auth/password_policy.py (validate_password_complexity):
- Raises `ValueError("Password must be at least {min_length} characters")` when len(password) < min_length.
- Raises `ValueError("Password must include at least {require_classes} of: lowercase, uppercase, digit, symbol")` when classes_present < require_classes.
- `validate_password_from_settings()` reads `settings.password_min_length` (default 12) + `settings.password_require_classes` (default 3) — invoked at every register / change-password / admin-create entry point.

From backend/tests/conftest.py:491 (project-standard strong password fixture):
- `password = "TestPass1234!"  # SEC-S16: meets 12-char + 3-class policy`
- 13 chars, 4 classes (upper + lower + digit + symbol) — passes the policy with margin.

From backend/tests/test_password_policy.py:37:
- `STRONG_PASSWORD = "TestPass1234!"` — second project precedent for the same literal.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace weak password fixtures in two register-audit tests</name>
  <files>backend/tests/test_phase_279_user_lifecycle.py</files>
  <read_first>
    - backend/tests/test_phase_279_user_lifecycle.py (full file — both failing tests live at lines 131-183 and 186-217)
    - backend/app/modules/auth/password_policy.py (confirm `validate_password_complexity` rejection shape — 3-of-4 class rule + 12-char min)
    - backend/tests/conftest.py:485-500 (confirm `TestPass1234!` is the canonical SEC-S16-compliant fixture in use elsewhere; we mirror it here)
    - backend/tests/test_password_policy.py:30-40 (second precedent for `TestPass1234!` as `STRONG_PASSWORD` module constant)
  </read_first>
  <action>
Update two password literals in `backend/tests/test_phase_279_user_lifecycle.py`:

1. Line 154 (`test_register_emits_user_register_audit`): change `"password": "securepass123"` to `"password": "TestPass1234!"` inside the POST `/auth/register/` body.
2. Line 206 (`test_register_disabled_does_not_emit_audit`): change `"password": "securepass123"` to `"password": "TestPass1234!"` inside the POST `/auth/register/` body.

Add a 4-line comment block above EACH change citing the root cause and the project-standard fixture:

```
# TD-02 (Plan 1081-01): v1014 SEC-S16 enforces 12-char minimum + 3-of-4 class
# diversity at validate_password_complexity. The prior "securepass123" literal
# (lowercase + digit = 2 classes) fails the 3-of-4 rule before the register
# audit path runs. "TestPass1234!" mirrors conftest.py:491 / test_password_policy.py:37.
```

(Use TD-02 for the first comment, TD-03 for the second.)

Constraints (DO NOT cross these):
- DO NOT change anything else in either test body — assertions on `audit_log.action == "user.register"`, `resp.status_code == 201` / `403`, etc. all stay.
- DO NOT add `pytest.mark.skip` decorators (zero skips per Plan 1075-05 protocol).
- DO NOT touch any production code (`backend/app/`). `git diff --stat backend/app/` must be empty when this commit lands.
- DO NOT extract a module-level constant. The two literals stay inline because each test docstring will then read clearly on its own. (Two test fixtures, two literals — symmetric with the existing inline `"shouldfail_{unique}"` username pattern.)
- DO NOT rename the tests. The names from the codebase (`test_register_emits_user_register_audit`, `test_register_disabled_does_not_emit_audit`) are what executes; the misnamed entries in REQUIREMENTS.md are documentation drift to flag in the SUMMARY.

Commit shape (single atomic commit per Plan 1075-04 single-cause precedent):

```
test(1081-01): TD-02/TD-03 align register-audit password fixtures to SEC-S16

Both ADMIN-05 register-audit tests in test_phase_279_user_lifecycle.py
were failing with 422 (Pydantic ValueError "Password must include at
least 3 of: lowercase, uppercase, digit, symbol") because the inline
"securepass123" fixture has only 2 classes — the v1014 SEC-S16 policy
(12-char + 3-of-4 diversity, see validate_password_complexity in
backend/app/modules/auth/password_policy.py) was added after these
tests landed in Phase 279. Updated both fixtures to "TestPass1234!"
mirroring the project-standard literal at conftest.py:491.

Tests fixed:
- test_register_emits_user_register_audit (line 131, fixture at line 154)
- test_register_disabled_does_not_emit_audit (line 187, fixture at line 206)

Note: REQUIREMENTS.md / ROADMAP / 1081-CONTEXT.md misname these tests
as test_register_password_too_short / test_register_password_diversity.
Those names do not exist in the codebase. Path correction surfaced
during planning; SUMMARY will document the rename for the close-gate
plan to reconcile.

Test-only change. No production code touched. No skip decorators added.
```
  </action>
  <verify>
    <automated>cd backend && env $(grep -v '^#' ../.env.test | xargs) uv run pytest tests/test_phase_279_user_lifecycle.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && env $(grep -v '^#' ../.env.test | xargs) uv run pytest tests/test_phase_279_user_lifecycle.py -x` exits 0
    - `cd backend && env $(grep -v '^#' ../.env.test | xargs) uv run pytest tests/test_phase_279_user_lifecycle.py::test_register_emits_user_register_audit -x` exits 0 (named test)
    - `cd backend && env $(grep -v '^#' ../.env.test | xargs) uv run pytest tests/test_phase_279_user_lifecycle.py::test_register_disabled_does_not_emit_audit -x` exits 0 (named test)
    - `grep -c "pytest.mark.skip" backend/tests/test_phase_279_user_lifecycle.py` returns 0
    - `grep -c "securepass123" backend/tests/test_phase_279_user_lifecycle.py` returns 0 (the weak literal is fully gone)
    - `grep -c "TestPass1234!" backend/tests/test_phase_279_user_lifecycle.py` returns 2 (one per fixed test)
    - `git diff --stat backend/app/` is empty (no production code modified)
    - Exactly one commit on local main with subject starting `test(1081-01): TD-02/TD-03`
  </acceptance_criteria>
  <done>
Both ADMIN-05 register-audit tests pass under the current SEC-S16 password policy. The audit-emission contract pin is restored. Zero skip decorators added. Zero production code touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test client → /auth/register/ | The test POSTs an HTTP body that crosses the FastAPI request boundary into the registration handler. Stub data inside a test client is not a real trust boundary in production, but the test's fidelity to the post-SEC-S16 contract IS a security-relevant signal — a stale fixture that lets the test pass on a weak password would mean the policy is no longer enforced (false-green). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1081-01 | Tampering | Test fixture password | mitigate | Use the project-standard `TestPass1234!` literal (4 classes, 13 chars) so the fixture has the MAXIMUM margin over the policy floor. Any future tightening of `PASSWORD_REQUIRE_CLASSES` (e.g., to 4) would still be satisfied without further test edits. |
| T-1081-02 | Tampering | Assertion strength | accept | The fixed tests assert `resp.status_code == 201` (success) or `== 403` (registration disabled). They do NOT independently re-validate that the password was checked — they rely on the unit-tested `validate_password_complexity` (in test_password_policy.py) and the integration-tested `TestRegisterPasswordPolicy::test_register_rejects_weak_password` (line 122) to pin the rejection path. This plan does not weaken those orthogonal pins; verification will run the full `test_phase_279_user_lifecycle.py` file to confirm. |
| T-1081-03 | Repudiation | Audit-event drop regression | mitigate | The whole purpose of `test_register_emits_user_register_audit` is to fire if a future register-route refactor silently drops the `user.register` audit row. Fixing the password fixture restores that pin — until today, the test was BROKEN by the SEC-S16 policy and provided no actual signal. |
| T-1081-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. No legitimacy checkpoint needed (no installs). |
</threat_model>

<verification>
After Task 1 lands:

1. `cd backend && env $(grep -v '^#' ../.env.test | xargs) uv run pytest tests/test_phase_279_user_lifecycle.py -x` exits 0 (all 4 tests pass: the 2 static-analysis FK tests + 2 fixed register-audit tests).
2. `grep -n "TestPass1234!" backend/tests/test_phase_279_user_lifecycle.py` shows 2 hits (one per fixed test).
3. `grep -n "securepass123" backend/tests/test_phase_279_user_lifecycle.py` shows 0 hits (the weak literal is fully removed).
4. `git diff --stat backend/app/` is empty (no production code touched).
5. Plans 02 + 03 may run concurrently in wave 1 — this plan does not modify any file they touch, so no inter-plan coordination is needed.
</verification>

<success_criteria>
- Both register-audit tests pass under SEC-S16
- Zero `pytest.mark.skip` decorators added
- Zero production code modified
- Single atomic commit with the path-correction note in the message body
- SUMMARY.md documents the test-name rename (`test_register_password_too_short` → `test_register_emits_user_register_audit`, `test_register_password_diversity` → `test_register_disabled_does_not_emit_audit`) for the close-gate plan to reconcile REQUIREMENTS.md / ROADMAP if desired
</success_criteria>

<output>
Create `.planning/phases/1081-test-fixture-assertion-drift/1081-01-SUMMARY.md` when done.

In the SUMMARY, include a top-level **Path correction surfaced** section that captures:
- The REQUIREMENTS.md / ROADMAP / CONTEXT names (`test_register_password_too_short`, `test_register_password_diversity`)
- The actual test names in the codebase (`test_register_emits_user_register_audit`, `test_register_disabled_does_not_emit_audit`)
- The recommendation: close-gate plan (Phase 1083, TD-08) updates REQUIREMENTS.md row TD-02 + TD-03 to point at the correct test names, OR add a `## Path corrections from v1018 planning` section to `.planning/v1017-MILESTONE-AUDIT.md` so future audits do not re-discover this drift.
</output>