---
phase: 1093-engine-level-retry-envelope
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md
autonomous: true
requirements:
  - TEST-01
tags:
  - pytest-xdist
  - sqlalchemy
  - asyncpg
  - postgres
  - fixture-isolation
  - engine-retry
  - audit
  - spike
  - v1021

must_haves:
  truths:
    - "An audit document at .planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md consolidates the 1088-04 architectural REPORT findings into a single planning artifact: post-commit failure surface, three candidate fix shapes (RetryingAsyncEngine wrapper class / event.listen(engine, 'connect') shim / NullPool acquire override / async_creator= override), and a chosen shape with named rationale that Plan 1093-02 implements verbatim."
    - "A pre-fix baseline measurement of `pytest -n auto tests/` at v1021 HEAD (3 consecutive runs, post-Phase-1092 SHA) is captured in the audit document — passed/failed/error/skipped per run plus the categorized failure breakdown (category 4.3 in-test post-commit shape vs. 4.5 non-cascade flake) so Plan 1093-02 has a measurable starting point against which to compare the post-fix delta."
    - "The audit document explicitly distinguishes the test-fixture engine factory (in scope: `_make_test_async_engine` at conftest.py:54-77) from the app engine (out of scope per CONTEXT.md `<decisions>` Locked from REQUIREMENTS.md) — naming the file path, line range, and exact contention surface that the chosen wrapper shape must intercept."
    - "Sequential `pytest tests/` baseline is re-verified clean at v1021 HEAD as a precondition for the parallel measurement — `failed == 0` for any NEW failures (pre-existing OOS failures test_phase_275_readme_accuracy + test_ssrf_redirect.py flake do not count) is the HARD INVARIANT the planning_context enforces."
    - "No code changes to backend/tests/conftest.py or any production file land in this plan — pure audit + measurement deliverable."
  artifacts:
    - path: ".planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md"
      provides: "Architectural decision record + pre-fix baseline measurement consolidated for Plan 1093-02"
      min_lines: 150
      contains: "Section 1 (post-commit failure surface from 1088-04), Section 2 (three candidate fix shapes with pros/cons), Section 3 (chosen shape + rationale), Section 4 (pre-fix baseline measurement at v1021 HEAD), Section 5 (reproducibility), pre-fix-baseline frontmatter keys (head_sha, sequential_passed_count, nauto_run_1/2/3_passed/failed/errors_skipped_wallclock, per_category_breakdown_run_1/2/3)"
  key_links:
    - from: ".planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md"
      to: ".planning/milestones/v1020-phases/1088-fixture-isolation-fixes-regression-pins/1088-04-SUMMARY.md"
      via: "Section 1 cross-reference (post-commit failure shape, iter-3 residual analysis)"
      pattern: "1088-04-SUMMARY"
    - from: ".planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md"
      to: "backend/tests/conftest.py"
      via: "Section 3 names file:line for the engine factory the chosen wrapper shape intercepts"
      pattern: "_make_test_async_engine"
    - from: ".planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md"
      to: ".planning/audits/PYTEST-XDIST-PERF-v1020.md"
      via: "Section 4 cross-reference (methodology mirror — same sampling/sequential-precheck/per-run stale-DB cleanup protocol)"
      pattern: "PYTEST-XDIST-PERF-v1020"
---

<objective>
Produce the architectural decision record + pre-fix baseline measurement for the engine-level retry envelope (TEST-01). Spike-first per v1019 Phase 1085 / v1020 Phase 1087 precedent — no code changes in this plan; deliverable is a single audit document that Plan 1093-02 implements verbatim.

Purpose: 1088-04 named the work but did NOT auto-apply (Rule 4 architectural decision required checkpoint per the v1020 deviation rules). This spike consolidates 1088-04's findings into a planning-grade decision record, measures the v1021-HEAD baseline against which the fix is judged, and locks ONE wrapper shape so Plan 1093-02 has a directive implementation prompt rather than another decision surface.

Output: `.planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md` with 5 sections + frontmatter capturing the pre-fix measurement. Single commit using TD-13 atomic-shape (no REQUIREMENTS.md flip — that lives in Plan 1093-02 with the SUMMARY).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1093-engine-level-retry-envelope/1093-CONTEXT.md
@.planning/milestones/v1020-phases/1088-fixture-isolation-fixes-regression-pins/1088-04-SUMMARY.md
@.planning/audits/PYTEST-XDIST-PERF-v1020.md

<interfaces>
<!-- Surfaces the spike must analyze. Extracted from conftest.py — Plan 1093-02 will implement against these contracts. -->
<!-- Executor uses these directly. No codebase exploration needed for the spike. -->

From backend/tests/conftest.py:54-77 (the engine factory the wrapper intercepts):

```python
def _make_test_async_engine(test_database_url: str):
    """Create the async test engine for this worker.

    xdist workers (PYTEST_XDIST_WORKER != 'master') use NullPool — no idle
    connections persist between tests, keeping concurrent connection count within
    max_connections=30 when 16 workers run simultaneously.
    ...
    """
    is_xdist = os.environ.get("PYTEST_XDIST_WORKER", "master") != "master"
    if is_xdist:
        return create_async_engine(test_database_url, poolclass=NullPool, echo=False)
    pool_size, max_overflow = _derive_test_pool_sizing()
    return create_async_engine(
        test_database_url,
        pool_size=pool_size,
        max_overflow=max_overflow,
        pool_timeout=30,
        echo=False,
    )
```

From backend/tests/conftest.py:343-347 (the catch tuple — REUSED, not re-introduced):

```python
_TRANSIENT_CONTENTION_EXCEPTIONS = (
    OperationalError,
    asyncpg.exceptions.TooManyConnectionsError,
    asyncpg.exceptions.CannotConnectNowError,
)
```

From backend/tests/conftest.py:471 (the setup-phase backoff budget the engine wrapper REUSES per CONTEXT.md `<decisions>`):

```python
_SETUP_PHASE_RETRY_BACKOFFS = (1.0, 2.0, 4.0)  # 7s total wait budget
```

From backend/tests/conftest.py:876 (call site that returns the engine that Plan 1093-02's wrapper intercepts):

```python
test_engine = _make_test_async_engine(settings.test_database_url)
```

From backend/tests/conftest.py:959 (the dispose call — second surface the wrapper must protect per CONTEXT.md `<domain>` line 14):

```python
await test_engine.dispose()
```

Post-commit failure shape (from 1088-04 SUMMARY iter-3 residual analysis): the 48 deterministic failures fire on `bind.connect()` (the SQLAlchemy async engine's connection acquisition) after `await session.commit()` releases the warm-up's connection. The session-factory retry envelope (Plan 1088-04) cannot wrap these — they fire inside test bodies on fresh connection acquisitions through paths the fixture helpers do not own. The engine-level wrapper intercepts at the lowest layer (`engine.connect()` itself), catching every connection acquisition regardless of which fixture body or call site invoked it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate 1088-04 REPORT findings into audit Sections 1-3 (chosen fix shape)</name>
  <files>.planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md</files>
  <action>Create the audit document with frontmatter (captured date, milestone=v1021, phase=1093-engine-level-retry-envelope, plan=01, requirement=TEST-01, host=macOS darwin/arm64 Apple M-series 16-core, worker_count_under_n_auto=16, postgres_max_connections=30, head_sha placeholder — fill in Task 3) + Section 1 (post-commit failure surface) + Section 2 (three candidate fix shapes) + Section 3 (chosen shape + rationale).

Section 1 — "Post-Commit Failure Surface (from Plan 1088-04 iter-3 residual analysis)":
- Restate the surface verbatim from 1088-04 SUMMARY: 48 deterministic failures fire on `bind.connect()` calls AFTER `await session.commit()` releases the warm-up's connection. Subsequent session operations (e.g., `await session.refresh(...)`, multiple sequential request handlers within one test body) acquire fresh asyncpg connections through paths the session-factory retry envelope does not own.
- Cite the exact 1088-04 SUMMARY section ("iter-3 residual analysis" + "Deviations from Plan / Rule 4 architectural decision NOT taken").
- State the +173 non-deterministic failures from HYG-02 flake hunt as same-shape (post-commit race-window contention through `bind.connect()` paths outside the session-factory envelope).
- Name the two surfaces the engine wrapper must intercept per CONTEXT.md `<domain>` line 14: `engine.connect()` AND `engine.dispose()`.

Section 2 — "Candidate Fix Shapes":
List three candidates from CONTEXT.md `<decisions>` lines 36-40 verbatim + a fourth `async_creator=` candidate (asyncpg-direct creator hook):
1. **RetryingAsyncEngine wrapper class** — composition wrapper around `AsyncEngine`; intercepts `.connect()` / `.dispose()` (and synchronously `.begin()` if present). Pros: explicit, fully testable in isolation, mirrors v1020 helper API shape, no monkey-patching. Cons: must proxy the full `AsyncEngine` interface used by call sites (sessions, dispose, possibly events).
2. **event.listen(engine, "connect", retry_handler)** — sqlalchemy event hook on the bare engine. Pros: minimal surface change. Cons: SQLAlchemy connection events fire AFTER DBAPI connection succeeds; cannot retry on the asyncpg-raised exception that fires BEFORE the handler runs. Likely unviable — note this in the analysis.
3. **NullPool acquire override** — subclass NullPool, override `_create_connection` / `connect`. Pros: lowest-level interception, catches every acquisition. Cons: deeper sqlalchemy internals, fragile across versions, only works on the xdist NullPool branch (not the sequential QueuePool branch).
4. **async_creator= override on create_async_engine** — pass a `creator=` callable that retries `asyncpg.connect(...)` itself. Pros: simple, asyncpg-native, no SQLAlchemy internals. Cons: bypasses some SQLAlchemy dialect connection-config; sqlalchemy 2.x's async dialect may need the URL anyway for type-codec setup.

For each candidate, note: (a) does it intercept both `engine.connect()` and `engine.dispose()` per CONTEXT.md? (b) does it preserve the NullPool xdist branch + the QueuePool sequential branch at conftest.py:67-77? (c) does it preserve the existing 17/17 `test_conftest_pool_sizing.py` + `test_conftest_lifecycle.py` regression pins? (d) testability — can the retry shape be exercised without a live Postgres host using the same MagicMock pattern as the v1020 pins?

Section 3 — "Chosen Shape + Rationale":
Pick ONE candidate. State the choice explicitly. Justify against the 4 criteria above plus the v1020 helper conventions (catch tuple `_TRANSIENT_CONTENTION_EXCEPTIONS` REUSED verbatim per CONTEXT.md; backoff budget `(1.0, 2.0, 4.0)` REUSED verbatim per CONTEXT.md `<decisions>` Locked from REQUIREMENTS.md; loud-fail-on-exhaust contract preserved; `sleep_fn` injection for testability matching `_create_test_db_with_retry` signature).

The choice MUST be implementable with NO new external dependencies (no new pip installs), NO change to the `_make_test_async_engine` signature (test_conftest_pool_sizing.py:245 + :271 pins lock it), and MUST handle both NullPool (xdist) and QueuePool (sequential) branches.

State the chosen shape at file:line resolution for what Plan 1093-02 will modify:
- conftest.py — wrap the return value of `_make_test_async_engine` at the function's exit (both branches) so the wrapper is returned to the call site at conftest.py:876 unchanged from the caller's perspective.
- Define the wrapper class / helper at conftest.py — place adjacent to `_acquire_test_session_with_retry` (around line 470-605) so the in-test, session-factory, and engine retry helpers are co-located.
- Reuse `_TRANSIENT_CONTENTION_EXCEPTIONS` (line 343-347) verbatim.
- Reuse `_SETUP_PHASE_RETRY_BACKOFFS` (line 324) verbatim — the engine wrapper uses the 7s budget (per CONTEXT.md `<decisions>` "Same `_TRANSIENT_CONTENTION_EXCEPTIONS` tuple v1020 introduced" + "same `(1.0, 2.0, 4.0)` budget as v1020 fixture-layer helpers" per planning_context).

Name the 3-4 regression pin shapes Plan 1093-02 will land (mirroring the v1020 in-test pin family naming convention `test_engine_retry_*`):
- `test_engine_retry_succeeds_on_transient_too_many_clients` — canonical pin for SQLAlchemy-wrapped OperationalError on `engine.connect()`.
- `test_engine_retry_raw_asyncpg_too_many_connections` — critical-contract pin mirroring the v1020 raw-asyncpg pins (locks down `asyncpg.exceptions.TooManyConnectionsError` stays in the catch tuple at engine layer).
- `test_engine_retry_propagates_non_contention_operational_error` — propagation pin (DNS / auth / refused-connection shapes propagate immediately).
- `test_engine_retry_exhausts_budget_then_fails_loudly` — exhaustion pin (1 + 3 = 4 total attempts, loud re-raise after budget).

End Section 3 with a single explicit "Plan 1093-02 implements [chosen shape] in [file:line range] with [N] regression pins in [target_file]." sentence so Plan 1093-02's executor has a directive.</action>
  <verify>
    <automated>test -f /Users/ishiland/Code/geolens/.planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md && grep -c -E "^## Section [0-9]+ — " /Users/ishiland/Code/geolens/.planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md | awk '$1 >= 3 {exit 0} {exit 1}' && grep -q "_TRANSIENT_CONTENTION_EXCEPTIONS" /Users/ishiland/Code/geolens/.planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md && grep -q "(1.0, 2.0, 4.0)" /Users/ishiland/Code/geolens/.planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md && grep -q "1088-04-SUMMARY" /Users/ishiland/Code/geolens/.planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md && grep -q "Plan 1093-02 implements" /Users/ishiland/Code/geolens/.planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md</automated>
  </verify>
  <done>Audit document exists at the named path with ≥3 sections (1: post-commit failure surface, 2: candidates, 3: chosen shape). Catch tuple and backoff budget cited verbatim. 1088-04 cross-reference present. Chosen-shape closing sentence is a directive for Plan 1093-02. Sections 4 + 5 still empty (filled by Tasks 2 + 3).</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify sequential baseline at v1021 HEAD (HARD GATE — non-negotiable per CONTEXT.md)</name>
  <files>.planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md</files>
  <action>Run the sequential pytest baseline at the current v1021 HEAD (post-Phase-1092 close, before any Plan 1093 code changes) and record the result in the audit document Section 4.0 (subsection that precedes the parallel measurements) per CONTEXT.md `<domain>` acceptance criterion (b) "sequential 3047/0/38 baseline preserved" + planning_context HARD INVARIANT "sequential baseline must remain green at `-n 4` AND sequential. Post-Phase-1092 baseline is ~3049 passed / 1-2 pre-existing OUT-OF-SCOPE failures (test_phase_275 + test_ssrf_redirect flake) / 38 skipped + 14 deselected."

Capture `head_sha` via `cd /Users/ishiland/Code/geolens && git rev-parse HEAD` and write it into the audit frontmatter.

Recipe per planning_context constraints — env-vars MUST be loaded from .env.test:
```
set -a && source /Users/ishiland/Code/geolens/.env.test && set +a
cd /Users/ishiland/Code/geolens/backend
uv run pytest tests/ --tb=no -q 2>&1 | tee /tmp/v1021-1093-01-sequential-baseline.log
```

Extract pytest's final summary line verbatim from the log and paste into Section 4.0 under heading "## Section 4.0 — Sequential baseline re-verify (HARD GATE)".

Decision logic per planning_context HARD INVARIANT (pre-existing OUT-OF-SCOPE failures are NOT regressions):
- `failed == 0` → invariant satisfied, proceed to Task 3.
- `failed > 0` AND ALL failures are in the pre-existing OOS set (`test_phase_275_readme_accuracy.py` OR `test_ssrf_redirect.py`) → record explicitly in Section 4.0 with "pre-existing OOS, NOT a regression" annotation, then proceed to Task 3.
- `failed > 0` with ANY NEW failure node-ID (not in the OOS set) → STOP. Do not proceed to Task 3. Surface the new failure node-IDs in Section 4.0 and HALT the plan — the new regression must be triaged before measuring `-n auto` baseline (a broken sequential baseline contaminates the parallel measurement per PYTEST-XDIST-PERF-v1020.md Section 1 Step 2 "HARD GATE: if `failed > 0`, the measurement campaign halts immediately").

Capture the passed-count + skipped-count + deselected-count verbatim into the audit frontmatter (`sequential_passed_count`, `sequential_skipped_count`, `sequential_deselected_count`, `sequential_wallclock_seconds`).</action>
  <verify>
    <automated>grep -q "Section 4.0 — Sequential baseline re-verify" /Users/ishiland/Code/geolens/.planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md && grep -qE "head_sha:.+[0-9a-f]{7,}" /Users/ishiland/Code/geolens/.planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md && grep -qE "sequential_passed_count:.+[0-9]+" /Users/ishiland/Code/geolens/.planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md && test -f /tmp/v1021-1093-01-sequential-baseline.log && grep -qE "passed|failed" /tmp/v1021-1093-01-sequential-baseline.log</automated>
  </verify>
  <done>Sequential baseline captured in Section 4.0 with verbatim pytest summary line. Frontmatter contains head_sha (full 40-char or short 7+ SHA) + sequential_passed_count + sequential_skipped_count + sequential_deselected_count + sequential_wallclock_seconds. EITHER `failed == 0` OR every failure is in the OOS set (test_phase_275 / test_ssrf_redirect) AND annotated as such. If NEW failures surfaced, the plan HALTED and surfaced them.</done>
</task>

<task type="auto">
  <name>Task 3: Measure pre-fix `pytest -n auto` baseline (3 consecutive runs) into audit Sections 4-5</name>
  <files>.planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md</files>
  <action>Run `pytest -n auto tests/` THREE consecutive times and record verbatim summary lines + categorized failure counts in audit Section 4 (per-run measurements) and Section 5 (reproducibility checklist). This establishes the v1021 starting point against which Plan 1093-02's post-fix delta is judged.

Per-run protocol mirrors PYTEST-XDIST-PERF-v1020.md Section 1 Step 4 + Step 1b (stale-DB cleanup between runs to prevent perturbed timing). Recipe — repeat 3 times with RUN ∈ {1, 2, 3}:

```
# Step 1: Drop stale per-worker test DBs (per PYTEST-XDIST-PERF-v1020.md Step 1b)
docker compose exec -T db psql -U geolens -d geolens -At -c \
  "SELECT datname FROM pg_database WHERE datname LIKE 'geolens%test_gw%' OR datname LIKE 'geolens%test_master%';" \
  > /tmp/v1021-1093-01-stale-dbs-run${RUN}.txt
while read -r db; do [ -z "$db" ] && continue; echo "DROP DATABASE IF EXISTS \"$db\";"; done < /tmp/v1021-1093-01-stale-dbs-run${RUN}.txt > /tmp/v1021-1093-01-drop-stale-run${RUN}.sql
docker compose exec -T db psql -U geolens -d geolens < /tmp/v1021-1093-01-drop-stale-run${RUN}.sql

# Step 2: Run pytest -n auto with JUnit XML for categorized analysis
set -a && source /Users/ishiland/Code/geolens/.env.test && set +a
cd /Users/ishiland/Code/geolens/backend
uv run pytest -n auto --junitxml=/tmp/v1021-1093-01-nauto-run${RUN}.xml tests/ 2>&1 | tee /tmp/v1021-1093-01-nauto-run${RUN}.log
```

For each of the 3 runs, extract from the log:
- pytest's final summary line verbatim (passed/failed/errors/skipped/warnings + wall-clock seconds).
- `grep -c "asyncpg.exceptions.TooManyConnectionsError" /tmp/v1021-1093-01-nauto-run${RUN}.log` (raw cascade line-count).
- `grep -c "InvalidCatalogNameError" /tmp/v1021-1093-01-nauto-run${RUN}.log` (category 4.1 — should be 0 post-Phase-1088, document if not).
- Distinct test-case-level failure count from the JUnit XML (`grep -c '<failure ' /tmp/v1021-1093-01-nauto-run${RUN}.xml` + `grep -c '<error ' /tmp/v1021-1093-01-nauto-run${RUN}.xml`).

Write Section 4 — "Pre-fix Baseline Measurement" with three subsections (Section 4.1, 4.2, 4.3 — one per run), each containing:
- Verbatim pytest summary line.
- Raw cascade line-count (asyncpg.TooManyConnectionsError + InvalidCatalogNameError).
- Distinct failure count (JUnit failures + errors).
- Per-category breakdown if the failure shape can be classified using the same taxonomy as PYTEST-XDIST-REMEASURE-AFTER-1088-01.md Section 4 (4.1 per-worker DB lifecycle / 4.2 setup-phase / 4.3 in-test / 4.4 teardown / 4.5 sandbox-non-cascade).

Capture each run's numbers into the audit frontmatter:
- `nauto_run_1_passed`, `nauto_run_1_failed`, `nauto_run_1_errors`, `nauto_run_1_skipped`, `nauto_run_1_wallclock_seconds`, `nauto_run_1_raw_cascade_lines`
- (same for runs 2, 3)

Write Section 4.4 — "Pre-fix Baseline Summary" with a table:

| Run | passed | failed | errors | wallclock (s) | raw cascade lines |
|-----|--------|--------|--------|---------------|-------------------|
| 1   | …      | …      | …      | …             | …                 |
| 2   | …      | …      | …      | …             | …                 |
| 3   | …      | …      | …      | …             | …                 |

Then a single bold conclusion sentence: "Pre-fix v1021-HEAD baseline at `pytest -n auto`: failure-count range across 3 runs is [min]–[max]. TEST-01 acceptance criterion (a) requires ≤10 failed per run across 3 consecutive runs."

Write Section 5 — "Reproducibility" with the ordered protocol (steps a-h):
a. Confirm Docker stack: `docker compose ps db` shows healthy on `127.0.0.1:5434`.
b. Confirm env file: `ls /Users/ishiland/Code/geolens/.env.test` exists.
c. Capture HEAD SHA: `git rev-parse HEAD` (must match frontmatter `head_sha`).
d. Confirm Postgres ceiling: `docker compose exec -T db psql -U geolens -d geolens -c "SHOW max_connections;"` returns 30 (audit Section 1 cross-reference: Postgres max_connections bump is OUT OF SCOPE per REQUIREMENTS.md).
e. Drop stale per-worker test DBs (Step 1 above) — empty set in steady state per PYTEST-XDIST-PERF-v1020.md Section 1 Step 1b "183-DB residual that v1087 observed is now an empty set in steady-state".
f. Run sequential baseline (Task 2 recipe) — assert per planning_context HARD INVARIANT.
g. For each run ∈ {1, 2, 3}: drop stale DBs, run pytest -n auto with JUnit XML, parse summary + cascade lines.
h. Write per-run subsections (4.1, 4.2, 4.3) + summary table (4.4) into the audit document.

After Section 5 is complete, close the audit document with a single trailing footer block citing:
- Phase: 1093-engine-level-retry-envelope
- Plan: 01 (TEST-01 spike — chosen shape + baseline)
- Captured: 2026-05-23
- Plan 1093-02 will implement [chosen shape from Section 3] and re-measure against this baseline.</action>
  <verify>
    <automated>grep -q "Section 4 — Pre-fix Baseline Measurement\|## Section 4 " /Users/ishiland/Code/geolens/.planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md && grep -q "Section 5 — Reproducibility\|## Section 5 " /Users/ishiland/Code/geolens/.planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md && grep -qE "nauto_run_1_passed:.+[0-9]+" /Users/ishiland/Code/geolens/.planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md && grep -qE "nauto_run_2_passed:.+[0-9]+" /Users/ishiland/Code/geolens/.planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md && grep -qE "nauto_run_3_passed:.+[0-9]+" /Users/ishiland/Code/geolens/.planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md && test -f /tmp/v1021-1093-01-nauto-run1.xml && test -f /tmp/v1021-1093-01-nauto-run2.xml && test -f /tmp/v1021-1093-01-nauto-run3.xml</automated>
  </verify>
  <done>Audit document has Sections 4 (with 4.1, 4.2, 4.3 per-run subsections + 4.4 summary table) and 5 (reproducibility protocol). Frontmatter contains nauto_run_{1,2,3}_passed/failed/errors/skipped/wallclock/raw_cascade_lines. JUnit XML artifacts persist at /tmp/v1021-1093-01-nauto-run{1,2,3}.xml. Pre-fix-baseline conclusion sentence states the failure-count range and references TEST-01 acceptance criterion (a). Trailing footer block cites Phase, Plan, capture date, and the Plan 1093-02 follow-on.</done>
</task>

<task type="auto">
  <name>Task 4: Write Plan 1093-01 SUMMARY + atomic commit (TD-13 shape — NO requirements flip)</name>
  <files>
    .planning/phases/1093-engine-level-retry-envelope/1093-01-SUMMARY.md
  </files>
  <action>Create `1093-01-SUMMARY.md` following the standard SUMMARY template at $HOME/.claude/get-shit-done/templates/summary.md. Required content blocks:

1. **Frontmatter** — phase=1093-engine-level-retry-envelope, plan=01, subsystem=testing, tags=[pytest-xdist, sqlalchemy, asyncpg, postgres, fixture-isolation, engine-retry, audit, spike, v1021], requires (parent phase + 1088-04 architectural REPORT), provides (audit document + pre-fix baseline + chosen shape directive for Plan 1093-02), affects (1093-02), tech-stack (no new packages — REUSES v1020 helpers/constants verbatim), key-files (created: audit doc; modified: none), key-decisions (chosen wrapper shape + rationale, why other 3 candidates rejected, audit pre-fix baseline number range), patterns-established (any new — likely none beyond v1020 conventions), requirements-completed=[] (TEST-01 flip lives in Plan 1093-02 per TD-13 — DO NOT FLIP HERE), duration ~50min, completed=2026-05-23.

2. **Body** — single-paragraph headline summarizing the spike's deliverable and the chosen shape. "Plan Execution" section: enumerate Tasks 1-3 outputs (audit Sections 1-5 + frontmatter populated). "Sequential Baseline Preservation HARD GATE" section: verbatim pytest summary line from /tmp/v1021-1093-01-sequential-baseline.log + invariant-satisfied annotation per planning_context. "Pre-fix Parallel Baseline" section: the 3-run summary table from audit Section 4.4 + pre-fix conclusion sentence. "Chosen Shape" section: 2-3 sentence statement of the chosen candidate + the file:line target for Plan 1093-02. "Self-Check" section: confirm audit doc exists, frontmatter populated, Sections 1-5 written, sequential baseline annotated. "Issues Encountered" section: any deviations from the run protocol (stale-DB count per run, NEW pre-existing-OOS failures if any, JUnit parse anomalies). "Deferred to Plan 1093-02": (a) wrapper implementation per chosen shape; (b) regression pins; (c) post-fix `pytest -n auto` re-measure against this baseline; (d) REQUIREMENTS.md TEST-01 traceability flip; (e) ROADMAP.md Phase 1093 row close.

3. **Commit** — Use the gsd-sdk commit recipe (planning files require force-add per planning_context constraint "Force-add `.planning/` files"). Run:

```
git add -f .planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md .planning/phases/1093-engine-level-retry-envelope/1093-01-SUMMARY.md
git commit -m "chore(1093-01): spike + pre-fix baseline for engine-level retry envelope (TEST-01)

- Audit doc at .planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md consolidates Plan 1088-04 architectural REPORT findings
- Chosen shape: [chosen-candidate-name] per audit Section 3 rationale (rejects 3 alternatives with named tradeoffs)
- Pre-fix \`pytest -n auto\` baseline (3 consecutive runs at HEAD): [min]-[max] failed per run
- Sequential baseline preserved: [N] passed / 0 NEW failures / 38 skipped (pre-existing OOS annotated)
- No code changes; Plan 1093-02 implements + measures post-fix delta

Refs TEST-01 (Phase 1093), 1088-04 SUMMARY architectural REPORT, PYTEST-XDIST-PERF-v1020.md methodology mirror"
```

Commit format per planning_context constraint: `chore(1093-0N): <description>` for the spike. NO `--no-verify` (constraint enforced).

Per TD-13 `requirements_traceability_flip` rule, this commit does NOT flip REQUIREMENTS.md or ROADMAP.md — those flips live in Plan 1093-02's atomic close commit alongside 1093-02-SUMMARY.md.</action>
  <verify>
    <automated>test -f /Users/ishiland/Code/geolens/.planning/phases/1093-engine-level-retry-envelope/1093-01-SUMMARY.md && grep -q "TEST-01" /Users/ishiland/Code/geolens/.planning/phases/1093-engine-level-retry-envelope/1093-01-SUMMARY.md && grep -q "ENGINE-RETRY-ENVELOPE-v1021" /Users/ishiland/Code/geolens/.planning/phases/1093-engine-level-retry-envelope/1093-01-SUMMARY.md && cd /Users/ishiland/Code/geolens && git log -1 --pretty=format:%s | grep -q "1093-01" && git log -1 --name-only --pretty=format: | grep -q "1093-01-SUMMARY.md" && git log -1 --name-only --pretty=format: | grep -q "ENGINE-RETRY-ENVELOPE-v1021.md"</automated>
  </verify>
  <done>1093-01-SUMMARY.md written with frontmatter per template + 7 body sections. Commit message references TEST-01, the chosen shape, the baseline range, and the planning_context constraints. The commit changeset contains ONLY the audit doc + the SUMMARY (NOT REQUIREMENTS.md, NOT ROADMAP.md — those land in Plan 1093-02 per TD-13). The commit succeeded WITHOUT --no-verify.</done>
</task>

</tasks>

<verification>
- Audit document at `.planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md` exists with 5 sections + populated frontmatter.
- Pre-fix baseline (3 consecutive `pytest -n auto` runs at v1021 HEAD) captured with verbatim pytest summary lines + raw cascade line-counts + JUnit XML artifacts persisted at `/tmp/v1021-1093-01-nauto-run{1,2,3}.xml`.
- Sequential `pytest tests/` baseline re-verified clean (or pre-existing-OOS-only) at HEAD — `failed == 0` OR all failures in OOS set (`test_phase_275_readme_accuracy` / `test_ssrf_redirect`).
- Chosen wrapper shape named in audit Section 3 with file:line target for Plan 1093-02 + 3-4 regression pin shapes named with `test_engine_retry_*` naming convention mirroring v1020 in-test pin family.
- Plan 1093-01 SUMMARY exists with TD-13-compliant frontmatter (NO requirements flip — that lives in Plan 1093-02).
- Single atomic commit `chore(1093-01): spike + pre-fix baseline for engine-level retry envelope (TEST-01)` with both audit + SUMMARY in the changeset.
- NO production code modified. NO REQUIREMENTS.md / ROADMAP.md edits. NO Makefile / ci.yml touches.
</verification>

<success_criteria>
- `.planning/audits/ENGINE-RETRY-ENVELOPE-v1021.md` exists with ≥3 sections + populated frontmatter.
- Audit Section 3 directs Plan 1093-02 with a single explicit "Plan 1093-02 implements [chosen shape] in [file:line]" sentence.
- Pre-fix `pytest -n auto` baseline captured for 3 consecutive runs.
- Sequential baseline HARD INVARIANT preserved per planning_context.
- Plan 1093-01 SUMMARY + audit doc committed in a single atomic commit, NO --no-verify, NO REQUIREMENTS.md flip, NO ROADMAP.md flip.
</success_criteria>

<output>
Create `.planning/phases/1093-engine-level-retry-envelope/1093-01-SUMMARY.md` when done.
</output>
