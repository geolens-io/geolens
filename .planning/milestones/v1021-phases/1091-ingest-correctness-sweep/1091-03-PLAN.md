---
phase: 1091-ingest-correctness-sweep
plan: 03
type: execute
wave: 3
depends_on: ["1091-02"]
files_modified:
  - scripts/seed-natural-earth.py
  - backend/tests/test_seed_natural_earth_reconciliation.py
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [OPS-01]
must_haves:
  truths:
    - "After the existing polling loop completes, scripts/seed-natural-earth.py calls GET /api/admin/jobs/?status=failed scoped to job_ids enqueued during this run (filtered via captured job_id set OR started_at window)."
    - "When failures exist, the script prints a failed-job table with source_filename, dataset_id, and error_message in the Import Summary block, AND exits non-zero."
    - "When no failures exist, the script preserves its current exit-zero + green-summary behavior — Reconciliation block prints 'GREEN: 0 failed jobs in /api/admin/jobs/' (or equivalent), no behavioral regression on the happy path."
    - "A test at backend/tests/test_seed_natural_earth_reconciliation.py covers the reconciliation function with a stub /api/admin/jobs/ response (known-failed shape + known-empty shape). Node-IDs pinned in REQUIREMENTS.md OPS-01 traceability cell."
    - "The script's invocation contract is preserved — no new required flags, no flag renames, no breaking changes to --base-url / --username / --password / --api-key / --dry-run / --theme / --dataset / --cache-dir."
    - "REQUIREMENTS.md OPS-01 row flips Pending → Complete and [ ] → [x] in the SAME commit as the SUMMARY.md write + ROADMAP.md row update per v1019 TD-13 atomic-flip rule."
  artifacts:
    - path: "scripts/seed-natural-earth.py"
      provides: "Post-loop reconciliation against /api/admin/jobs/?status=failed"
      contains: "def reconcile_failed_jobs"
    - path: "backend/tests/test_seed_natural_earth_reconciliation.py"
      provides: "Unit tests for the reconciliation function with stubbed HTTP responses"
      exports: ["test_reconciliation_surfaces_failed_jobs", "test_reconciliation_clean_when_no_failures"]
    - path: ".planning/REQUIREMENTS.md"
      provides: "OPS-01 traceability row updated"
      contains: "OPS-01"
  key_links:
    - from: "scripts/seed-natural-earth.py"
      to: "/api/admin/jobs/?status=failed"
      via: "HTTP GET via httpx.AsyncClient with admin token"
      pattern: "admin/jobs.*status=failed"
    - from: "backend/tests/test_seed_natural_earth_reconciliation.py"
      to: "scripts/seed-natural-earth.py"
      via: "sys.path manipulation OR importlib (scripts/ is not a package) to import the reconcile function"
      pattern: "import.*seed_natural_earth|reconcile_failed_jobs"
    - from: ".planning/REQUIREMENTS.md"
      to: "backend/tests/test_seed_natural_earth_reconciliation.py"
      via: "OPS-01 traceability cell cites the test node-IDs"
      pattern: "test_seed_natural_earth_reconciliation\\.py::"
---

<objective>
Add post-polling-loop reconciliation to `scripts/seed-natural-earth.py` against `/api/admin/jobs/?status=failed` so the seed script's "Succeeded: N, Failed: M" summary cannot disagree with the persisted worker job-row status. Closes OPS-01.

**Purpose:** Quick task `260523-at1` SUMMARY Issue §1 documented the disagreement: the seed reported "Succeeded: 109, Failed: 0" while `/api/admin/jobs/` showed 1 failed (the urban_areas_landscan quicklook bug closed by Plan 1091-02). Even after the quicklook fix lands, the reconciliation gap remains: any future post-commit failure mode that lands data successfully but flips the job row to `status=failed` will still escape the seed's heuristic. Adding the reconciliation makes the seed honest about the worker's persisted truth.

**Output:** Reconciliation function + Import Summary block extension + unit tests with stubbed `/api/admin/jobs/` responses + TD-13 atomic close commit (REQUIREMENTS.md flip + SUMMARY.md + ROADMAP.md). After this plan, the phase 1091 close-gate sequence runs (assumed handled by `/gsd:complete-phase` or equivalent).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/1091-ingest-correctness-sweep/1091-CONTEXT.md
@.planning/phases/1091-ingest-correctness-sweep/1091-02-SUMMARY.md

<interfaces>
Source surfaces this plan touches. The seed script is the primary edit; the test file is new.

From `scripts/seed-natural-earth.py` (the script being modified):
- `async def main(args, datasets)` at lines 927-1023. The polling loop is the `asyncio.TaskGroup` block at lines 982-997 (each `process_one` invocation awaits its own job to complete via `poll_job`). After the `async with asyncio.TaskGroup()` exits, `results` is fully aggregated. The reconciliation MUST land between the `TaskGroup` exit and the `print_summary(...)` call at line 1009 (or just after `print_summary` — that placement keeps the existing Import Summary block intact and adds a new sibling block).
- `async def process_one(entry, index, total, sem, client, base_url, api_key, existing, cache_dir, results)` at lines 841-919. Captures each ingest job's outcome into `results` with status=succeeded/skipped/failed and a dataset_id when present. Note: `dataset_id` is captured but NOT the `job_id`. The reconciliation strategy can either (a) capture job_ids during enqueue by modifying `process_one` to record `result['job_id'] = job_id` from upload_resp at line 565, OR (b) filter by `started_at` window using a captured `run_start_time` from before the TaskGroup. The simpler approach is (b) since `process_one` already does the work and (a) requires modifying every results.append() call.
- `async def poll_job(client, base_url, api_key, job_id, timeout=300)` at lines 500-533. Already polls `/api/jobs/{job_id}` (different endpoint from the admin endpoint we'll consume). The admin endpoint is `/api/admin/jobs/` (with trailing slash — confirmed in quick task `260523-at1` SUMMARY reproduction commands). Both endpoints return the same job-row shape.
- `def print_summary(total, succeeded, skipped, failed, failures)` at lines 695-714. The current summary prints "Succeeded: N / Skipped: N / Failed: N / Total: N" and a "Failures:" block when failures non-empty. The reconciliation output augments this: after `print_summary(...)`, print a "Reconciliation" block; if reconciliation found failures the script summary missed, mention them and exit non-zero.
- Argument parsing at lines 623-672 — do NOT add required new flags. If a tunable is needed (e.g. `--admin-token` or `--skip-reconciliation`), it MUST be optional with a safe default. Preferred: reuse the bootstrapped admin token (the script already has admin creds via --username/--password); no new flags needed.

From `/api/admin/jobs/` endpoint (already in use during quick task `260523-at1`):
- Supports query params `?status=failed&limit=N` (where `N <= 200` — per quick task Gap 2). Returns a list of job rows with shape `{id, status, source_filename, dataset_id, error_message, started_at, completed_at, ...}` (full shape captured in quick task SUMMARY Errors §1).
- Requires admin role + JWT or admin API key. The seed script already has an admin API key minted via `bootstrap_api_key()` (lines 257-303), which has admin role since admin creds were used. Verify in execution that the admin API key has access to `/api/admin/jobs/` — if it does NOT, the reconciliation falls back to re-logging in for a JWT (the script already has --username/--password for the bootstrap).

From `backend/tests/conftest.py`:
- Test fixtures provided: `test_db_session`, `client`. The reconciliation test does NOT need a real DB — it stubs the httpx response. Use `unittest.mock.AsyncMock` to patch the httpx client (project does not require pytest_httpx).

From existing test patterns:
- `backend/tests/test_quicklook_predicate.py` uses `@pytest.mark.anyio + async def test_...`. Same pattern for any async test in the new file.
- Script-as-module import: `scripts/` is not a Python package. The test imports the function via `importlib.util.spec_from_file_location` to load `scripts/seed-natural-earth.py` as a module named `seed_natural_earth` (hyphen → underscore). The standard Python recipe handles this — do the importlib dance inline in the test (one helper at the top of the file).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reconciliation function and Import Summary integration</name>
  <files>scripts/seed-natural-earth.py, backend/tests/test_seed_natural_earth_reconciliation.py</files>
  <behavior>
    - **Negative-form (script's heuristic was wrong, admin endpoint disagrees):** When the stubbed `/api/admin/jobs/?status=failed` returns a non-empty list of jobs scoped to the run window (e.g., 1 row with `source_filename=ne_10m_urban_areas_landscan.zip` and a MissingGreenlet error_message), the reconciliation function returns a non-empty list of {id, source_filename, dataset_id, error_message, started_at} dicts. The Import Summary's Reconciliation block emits the failed-job table to stdout, AND the script's exit code is non-zero (1).
    - **Positive-form (script's heuristic and admin endpoint agree, all green):** When the stubbed admin endpoint returns an empty list (no failed jobs in the run window), the reconciliation function returns an empty list. The script prints a one-line GREEN reconciliation status and exits zero (preserves existing behavior).
    - **Run-window scoping:** The reconciliation does NOT report failures from before this run. Filter by `started_at > run_start_time` (where `run_start_time` is captured before the TaskGroup). Document the chosen approach in code comments.
    - **Edge case (admin endpoint unreachable):** If the GET to `/api/admin/jobs/` fails (network error, 5xx, 401), the reconciliation logs a warning but does NOT crash the script — the existing summary still prints. Exit code stays at whatever the per-dataset results indicated (zero if all per-dataset polls were green, non-zero if any per-dataset poll already saw a failure). The reconciliation is additive defense, not a sole gate.
  </behavior>
  <action>
1. **Add the reconciliation function** to `scripts/seed-natural-earth.py`. Insert after `print_summary` (~line 714) and before `create_or_get_collection` (~line 722). Function signature: `async def reconcile_failed_jobs(client: httpx.AsyncClient, base_url: str, api_key: str, run_start_time: datetime, limit: int = 200) -> list[dict]`. Implementation requirements:
   - Issues `GET {base_url}/api/admin/jobs/?status=failed&limit={limit}` with `X-Api-Key` header (matches existing script pattern at line 357).
   - Parses the response; the response shape may be `{"jobs": [...]}` or a bare list — be defensive (try `.get("jobs", [])` first, fall back to treating the body as the list).
   - Filters returned rows by `started_at > run_start_time`. Parse `started_at` (ISO-8601 strings like `"2026-05-23T11:59:05.958235Z"`) with `datetime.fromisoformat(s.replace("Z", "+00:00"))` for cross-version safety.
   - Returns a list of dicts with the keys: id, source_filename, dataset_id, error_message, started_at.
   - Wraps the HTTP call in `try/except (httpx.HTTPStatusError, httpx.TransportError)` — on failure, log a warning via the existing `logger` and return `[]` (additive defense, not sole gate).
   - Docstring cites OPS-01 and the quick-task SUMMARY Issue §1.

2. **Add the datetime import** if not present. Insert `from datetime import datetime, timezone` at the top of the file alongside the other imports (the file currently uses `import time` but not `datetime`). Verify via grep before adding.

3. **Capture the run start time** in `main()` before the TaskGroup. Insert just before line 981 (`print(f"\nImporting {total} datasets...")`):
   - `run_start_time = datetime.now(timezone.utc)` — assigned to a local in `main()`.

4. **Invoke reconciliation after print_summary**. Insert between line 1009 (`print_summary(...)`) and line 1011 (`# Assign datasets to collections by theme`). The block prints a `--- Reconciliation ---` header, calls `await reconcile_failed_jobs(client, base_url, api_key, run_start_time)`, and either:
   - Iterates the returned list and prints one line per failure showing `source_filename`, `dataset_id` (or `(no dataset)`), and `error_message` (truncated to 200 chars), then sets a local `reconciliation_exit_nonzero = True`.
   - Or prints `GREEN: 0 failed jobs in /api/admin/jobs/ within run window` and sets `reconciliation_exit_nonzero = False`.

5. **Wire non-zero exit code** at the entry point. The current `asyncio.run(main(args, datasets))` at line 1065 discards any return value. Change `main()` to return `int` (0 or 1, based on `reconciliation_exit_nonzero` OR any per-dataset failures). At the entry point, replace `asyncio.run(main(args, datasets))` with `sys.exit(asyncio.run(main(args, datasets)) or 0)`. Hard failures from exceptions still surface as non-zero via Python's default exit-on-exception path.

6. **Create the test file** `backend/tests/test_seed_natural_earth_reconciliation.py`:
   - Imports: `pytest`, `httpx`, `from unittest.mock import AsyncMock, MagicMock`, `import importlib.util`, `import sys`, `import pathlib`, `from datetime import datetime, timezone, timedelta`.
   - Module-load helper at top of file: load `scripts/seed-natural-earth.py` as a Python module named `seed_natural_earth`. Path: `pathlib.Path(__file__).resolve().parents[2] / "scripts" / "seed-natural-earth.py"`. Use `importlib.util.spec_from_file_location` + `module_from_spec` + `spec.loader.exec_module(...)`. Cache the loaded module in a module-level variable so it's loaded once.
   - Test 1 (`test_reconciliation_surfaces_failed_jobs`): stub `httpx.AsyncClient.get` to return a `MagicMock` whose `.json()` returns `{"jobs": [{"id": "fake-uuid-1", "source_filename": "ne_10m_urban_areas_landscan.zip", "dataset_id": "ffcba726-d61c-48e9-8786-3b41b5fc96f8", "error_message": "MissingGreenlet ...", "started_at": "2026-05-23T12:00:00+00:00"}]}` and `.raise_for_status()` is a no-op. Pass a `run_start_time` BEFORE that started_at. Assert: returned list has exactly 1 entry whose `source_filename` matches.
   - Test 2 (`test_reconciliation_clean_when_no_failures`): stub returns `{"jobs": []}`. Assert: returned list is empty.
   - Test 3 (`test_reconciliation_filters_by_run_window`): stub returns a job whose `started_at` is BEFORE the passed `run_start_time` (e.g. one hour earlier). Assert: returned list is empty (the failure predates this run).
   - Test 4 (`test_reconciliation_handles_admin_endpoint_failure`): stub `client.get` raises `httpx.TransportError("connection refused")`. Assert: returned list is empty AND the test does not raise. Use the `caplog` fixture to assert that a warning containing "reconcile" (or "/admin/jobs") is emitted.
   - All tests use `@pytest.mark.anyio` + `async def test_...` to match the style in `backend/tests/test_quicklook_predicate.py`.

7. **Run the new tests in isolation**: `cd backend && uv run pytest tests/test_seed_natural_earth_reconciliation.py -xvs`. All 4 must pass.

8. **Run full pytest in parallel** to confirm no regression: `cd backend && uv run pytest -n 4 tests/`. Target: 3047 plus the 4 new tests = 3051+ passing, 0 failed, 38 skipped. The HARD INVARIANT from CONTEXT.md is `failed == 0`.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/test_seed_natural_earth_reconciliation.py -xvs &amp;&amp; uv run pytest -n 4 tests/ --tb=no -q</automated>
  </verify>
  <done>
    - `scripts/seed-natural-earth.py` has `reconcile_failed_jobs` function defined.
    - `main()` captures `run_start_time` before the TaskGroup AND invokes reconciliation after `print_summary` AND wires a non-zero exit when reconciliation finds failures.
    - `backend/tests/test_seed_natural_earth_reconciliation.py` has at least 4 tests, all passing in isolation.
    - Full `pytest -n 4 tests/` shows `failed=0` (3051+/0/38 expected; 3047 baseline + 4 new tests).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The seed-natural-earth.py script now reconciles against `/api/admin/jobs/?status=failed` after the polling loop. When failures exist that the per-dataset heuristic missed, they're printed in a Reconciliation block and the script exits non-zero. When no failures exist, behavior is preserved (green summary, exit zero). Unit tests pin both branches.

    Now verify the live behavior on the live docker stack — TWO scenarios:
    1. **Happy path (post-INGEST-01 fix):** A clean rebuild + canonical seed should print GREEN reconciliation and exit zero.
    2. **Failure-surfacing path:** Inject a synthetic failed job row in the running DB and confirm the reconciliation surfaces it correctly.
  </what-built>
  <how-to-verify>
**Scenario 1 — Happy path** (assumes Plan 1091-02 closed INGEST-01 and the stack is clean):

1. Tear down and rebuild: `cd /Users/ishiland/Code/geolens && docker compose down -v && docker compose up -d --build`. Wait ~3 min for healthchecks.
2. Run the seed and capture exit code: `python3 scripts/seed-natural-earth.py --base-url http://localhost:8080 --username admin --password admin; echo "Exit code: $?"`.
3. Expected output:
   - Import Summary shows `Succeeded: 109 / Failed: 0`.
   - A `--- Reconciliation ---` block follows print_summary.
   - Reconciliation line reads `GREEN: 0 failed jobs in /api/admin/jobs/ within run window`.
   - Exit code: `0`.

**Scenario 2 — Failure-surfacing path** (synthetic failure injection):

4. Insert a synthetic failed job row to validate the reconciliation surfaces it. First confirm the schema/table name: `docker compose exec -T db psql -U geolens -d geolens -c "\\dt catalog.*"` (adjust db/user/schema if different). Then insert a fake failed row with `started_at = NOW()`:
   ```
   docker compose exec -T db psql -U geolens -d geolens -c "INSERT INTO catalog.ingest_jobs (id, status, source_filename, error_message, started_at, completed_at, created_by, current_step, progress) VALUES (gen_random_uuid(), 'failed', 'synthetic-test.zip', 'OPS-01 reconciliation test injection', NOW() - INTERVAL '1 minute', NOW(), (SELECT id FROM catalog.users WHERE username='admin'), 'failed', 0.0);"
   ```
   (Adjust the schema/table/column names per your DB if `\\dt` shows different. If the `created_by` column has a NOT NULL constraint and the admin user lookup fails, fall back to a hardcoded UUID from `SELECT id FROM catalog.users LIMIT 1`.)
5. Re-run the seed (all datasets skipped — reconciliation runs against the injected failure): `python3 scripts/seed-natural-earth.py --base-url http://localhost:8080 --username admin --password admin; echo "Exit code: $?"`.
6. Expected output:
   - Import Summary shows mostly `Skipped: 109` (all datasets already imported in Scenario 1).
   - `--- Reconciliation ---` block shows: `⚠ 1 failed job(s) found in /api/admin/jobs/ that the per-dataset poll missed:` followed by `synthetic-test.zip [(no dataset)]: OPS-01 reconciliation test injection`.
   - Exit code: `1` (non-zero).
7. Clean up the synthetic row so the phase-close gate matrix is clean: `docker compose exec -T db psql -U geolens -d geolens -c "DELETE FROM catalog.ingest_jobs WHERE source_filename = 'synthetic-test.zip';"`.
8. Final re-run to confirm cleanup worked + reconciliation is green again: `python3 scripts/seed-natural-earth.py --base-url http://localhost:8080 --username admin --password admin; echo "Exit code: $?"`. Expected: GREEN reconciliation, exit 0.

If all three runs match expectations, type `approved`. If reconciliation didn't surface the synthetic failure correctly, paste the script output so the executor can fix the filter/print logic.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (paste output from failing scenario)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Atomic close — REQUIREMENTS.md flip + plan SUMMARY + phase SUMMARY + ROADMAP row</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/1091-ingest-correctness-sweep/1091-03-SUMMARY.md, .planning/phases/1091-ingest-correctness-sweep/1091-SUMMARY.md, .planning/ROADMAP.md</files>
  <action>
Apply the v1019 TD-13 atomic close protocol in a single commit. Plan 1091-03 closes OPS-01 and the phase (both reqs now complete).

1. **REQUIREMENTS.md update** — edit `.planning/REQUIREMENTS.md`:
   - Flip `- [ ] **OPS-01**` → `- [x] **OPS-01**`.
   - Append a `Closed:` suffix to the OPS-01 bullet citing the test node-IDs. Validate the node-IDs via `git grep -n "def test_reconciliation" backend/tests/test_seed_natural_earth_reconciliation.py` — must return at least 1 hit. Format: `Closed: see backend/tests/test_seed_natural_earth_reconciliation.py::test_reconciliation_surfaces_failed_jobs and ::test_reconciliation_clean_when_no_failures.`
   - Traceability table: flip `| OPS-01 | Phase 1091 | Pending |` → `| OPS-01 | Phase 1091 | Complete |`.

2. **Plan-level SUMMARY.md** — write `.planning/phases/1091-ingest-correctness-sweep/1091-03-SUMMARY.md` using the standard summary template. Required sections:
   - Frontmatter fields: `phase: 1091-ingest-correctness-sweep`, `plan: 03`, `requirements: [OPS-01]`, `status: complete`, `tdd: true`, `verification: live_docker_rebuild + synthetic_injection`.
   - **What shipped:** new `reconcile_failed_jobs` function in seed-natural-earth.py + post-print_summary Reconciliation block + non-zero exit wiring + 4 unit tests in `test_seed_natural_earth_reconciliation.py`.
   - **How verified:** Scenario 1 (happy path: GREEN reconciliation, exit 0) AND Scenario 2 (synthetic failure injection: surfaced, exit 1) both observed. Sequential pytest 3051+/0/38 preserved.
   - **Patterns confirmed:** additive-defense-not-sole-gate pattern — reconciliation logs network errors and returns [] rather than crashing the seed, so the existing per-dataset polling stays the primary signal and the admin-endpoint check is a belt-and-braces secondary.

3. **Phase-level SUMMARY.md** — write `.planning/phases/1091-ingest-correctness-sweep/1091-SUMMARY.md` (NOTE: this is the phase-aggregate summary, NOT a plan summary). Required sections:
   - Frontmatter: `phase: 1091-ingest-correctness-sweep`, `requirements: [INGEST-01, OPS-01]`, `status: complete`, `plans: [01, 02, 03]`, `tags: [ingest, async-context, seed, reconciliation]` (the `tags` field feeds the global-learnings query path when enabled).
   - **Phase goal status:** Restate the phase goal from ROADMAP.md Phase 1091 entry (the docker-rebuild + seed assertion) and confirm all 5 success criteria are met. Cite the close-gate evidence: failed-job count (0), urban_areas_landscan_10m quicklook URI (non-null), regression test node-IDs (test_quicklook_async_context.py + test_seed_natural_earth_reconciliation.py), spike audit doc presence (.planning/audits/INGEST-QUICKLOOK-ASYNC-CONTEXT-v1021.md), reconciliation behavior on both scenarios.
   - **Plans summary:** One paragraph per plan (1091-01 spike, 1091-02 fix+test, 1091-03 reconciliation+test).
   - **Patterns established / reinforced:** Note v1019/v1020 spike-first pattern continues to deliver. Note "additive-defense reconciliation" pattern is now available for future seed scripts (seed-ago-data.py, seed-perf-data.py might want similar treatment in a future milestone — OUT of v1021 scope, but worth recording for cross-phase pattern transfer).
   - **Followups / deferred:** Any pool-sizing concern from 1091-02's audit Section 3, or any reconciliation-pattern transfer-candidates for sibling scripts.

4. **ROADMAP.md update** — edit `.planning/ROADMAP.md`:
   - Phases checklist (top): flip `- [ ] **Phase 1091: Ingest Correctness Sweep** —` to `- [x] **Phase 1091: Ingest Correctness Sweep** —`.
   - Progress table: flip `| 1091. Ingest Correctness Sweep | 0/TBD | Not started | - |` (which Plan 1091-02 already updated to `2/3 | In progress | - |`) to `| 1091. Ingest Correctness Sweep | 3/3 | Complete | 2026-05-23 |` (use today's date).

5. **Single commit** — stage and commit all files in ONE git commit (no --no-verify). Files to stage:
   - Code: `scripts/seed-natural-earth.py`, `backend/tests/test_seed_natural_earth_reconciliation.py`
   - Planning (force-add since `.planning/` is gitignored): `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/phases/1091-ingest-correctness-sweep/1091-03-SUMMARY.md`, `.planning/phases/1091-ingest-correctness-sweep/1091-SUMMARY.md`
   - Commit message — write to a temp file first to avoid HEREDOC quoting issues, then commit with `-F`:
     - Title: `feat(ops): close OPS-01 + Phase 1091 — seed-natural-earth.py reconciliation`
     - Body bullets:
       - `reconcile_failed_jobs queries /api/admin/jobs/?status=failed scoped to run window`
       - `Import Summary gains --- Reconciliation --- block; non-zero exit when failures found`
       - `4 unit tests pin reconciliation branches (failed/clean/window/transport error)`
       - `Phase 1091 closes: INGEST-01 (1091-02) + OPS-01 (1091-03) both Complete`
     - Project convention: no AI/bot mention in the commit message.
   - Execute: `git add scripts/seed-natural-earth.py backend/tests/test_seed_natural_earth_reconciliation.py && git add -f .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/1091-ingest-correctness-sweep/1091-03-SUMMARY.md .planning/phases/1091-ingest-correctness-sweep/1091-SUMMARY.md && git commit -F /tmp/commit-msg-1091-03.txt` (after writing the message to `/tmp/commit-msg-1091-03.txt`).

6. If pre-commit hooks fail, fix the underlying issue and create a NEW commit (do NOT amend per project convention).

7. Run a final `git log -1 --stat` to confirm the commit landed with all 6 files.
  </action>
  <verify>
    <automated>git log -1 --format="%s" | grep -q "close OPS-01" &amp;&amp; grep -qE "^- \[x\] \*\*OPS-01\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "^\| OPS-01 \| Phase 1091 \| Complete \|" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "1091\. Ingest Correctness Sweep \| 3/3 \| Complete" .planning/ROADMAP.md &amp;&amp; test -f .planning/phases/1091-ingest-correctness-sweep/1091-SUMMARY.md &amp;&amp; test -f .planning/phases/1091-ingest-correctness-sweep/1091-03-SUMMARY.md</automated>
  </verify>
  <done>
    - `git log -1` shows a single commit titled "feat(ops): close OPS-01 + Phase 1091 ..." (or close equivalent).
    - REQUIREMENTS.md OPS-01 checkbox is `[x]` and traceability row is `Complete`.
    - 1091-03-SUMMARY.md and 1091-SUMMARY.md both exist with required sections.
    - ROADMAP.md Progress table shows 3/3 plans complete + Phase 1091 checked off.
    - All 6 files committed atomically (no orphaned WIP).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → /api/admin/jobs/ | Script consumes admin endpoint with the bootstrapped admin API key; no untrusted input. |
| seed script → operator shell | Reconciliation output is plain stdout text; no privileged sink. |
| test → file system (importlib load of script) | Test loads `scripts/seed-natural-earth.py` as a Python module via importlib — trusted local file, no remote code execution risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1091-06 | Information disclosure | Reconciliation output | accept | The Reconciliation block prints `error_message` truncated to 200 chars. The `urban_areas_landscan` error_message in the live SUMMARY contains no PII (just a SQLAlchemy URL + greenlet phrase). Truncation prevents accidental log-bombing of secrets if a future failure mode embeds them. |
| T-1091-07 | Denial of service (admin endpoint hammering) | `/api/admin/jobs/?status=failed` | accept | The reconciliation calls the endpoint ONCE per seed run (post-loop), with `limit<=200`. The endpoint already supports this access pattern. No retry loop, no fan-out. |
| T-1091-08 | Tampering (test loads script as code) | importlib spec_from_file_location | accept | The test is local code loading another local file. No path injection, no untrusted input. The test's `pathlib.Path(__file__).resolve().parents[2] / "scripts" / "seed-natural-earth.py"` resolves deterministically from the test file location. |

Security-enforcement gate applies (default-on); no package installs (zero `npm install` / `uv add`), no new external dependencies. The new test reuses existing project test deps (pytest, httpx, unittest.mock).
</threat_model>

<verification>
- `reconcile_failed_jobs` function exists in `scripts/seed-natural-earth.py` with the signature documented in Task 1.
- `main()` captures `run_start_time` before the TaskGroup, invokes reconciliation after `print_summary`, and wires a non-zero exit code path through `sys.exit(asyncio.run(main(...)))`.
- `backend/tests/test_seed_natural_earth_reconciliation.py` exists with at least 4 `@pytest.mark.anyio + async def` test functions covering the surfaces named in Task 1 step 6.
- Sequential pytest baseline preserved: `cd backend && uv run pytest -n 4 tests/` shows `failed=0` and pass count >= 3051 (3047 baseline + 4 new tests).
- Live Scenario 1 (happy path): GREEN reconciliation, exit code 0.
- Live Scenario 2 (synthetic injection): failed-job table printed, exit code 1.
- Live Scenario 2 cleanup verified: post-cleanup run is green again.
- REQUIREMENTS.md OPS-01 row flipped to `[x]` + Complete in the SAME commit as SUMMARY.md and ROADMAP.md (TD-13 atomic-4-file rule).
- ROADMAP.md Phase 1091 marked Complete with today's date.
- Phase-aggregate SUMMARY.md exists at `.planning/phases/1091-ingest-correctness-sweep/1091-SUMMARY.md`.
- Single commit, no `--no-verify`.
</verification>

<success_criteria>
- OPS-01 acceptance criteria (a), (b), (c) all green per REQUIREMENTS.md.
- Phase 1091 closes: INGEST-01 (Plan 1091-02) + OPS-01 (Plan 1091-03) both Complete.
- Sequential pytest 3051+/0/38 preserved (HARD INVARIANT from CONTEXT.md: `failed == 0`).
- TD-13 atomic-N-file close commit lands cleanly.
- Operator running `docker compose down -v && up -d --build && python3 scripts/seed-natural-earth.py --username admin --password admin` sees a GREEN reconciliation block and exit 0 — the phase goal is met end-to-end.
- v1021 Phase 1092 (Routing + Infra Hygiene) can begin without re-touching this work.
</success_criteria>

<output>
Create `.planning/phases/1091-ingest-correctness-sweep/1091-03-SUMMARY.md` AND `.planning/phases/1091-ingest-correctness-sweep/1091-SUMMARY.md` as part of Task 3's atomic close commit (see Task 3 action). Both files are required for phase close.
</output>
