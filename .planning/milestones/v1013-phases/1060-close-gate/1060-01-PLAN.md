---
phase: 1060-close-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/builder-v1-5.spec.ts
  - e2e/builder.spec.ts
autonomous: true
requirements:
  - CTRL-01

must_haves:
  truths:
    - "e2e/builder-v1-5.spec.ts:152 (drag-from-catalog test) either passes green OR is test-pinned with explicit rationale comment documenting the deferred app-level cause"
    - "e2e/builder.spec.ts:338 (duplicates dataset renderings test) either passes green OR is test-pinned with explicit rationale comment documenting the deferred app-level cause"
    - "Each failure has a documented root cause: real bug, test contract drift, or flaky timing — chosen disposition logged in the test file as a code comment above the failing block"
    - "`npm run e2e:smoke:builder` exits 0 (all listed tests pass or are .skip-with-rationale)"
  artifacts:
    - path: "e2e/builder-v1-5.spec.ts"
      provides: "Stable drag-from-catalog test that either really passes or is annotated with the exact deferred surface"
      contains: "test('1. drag-from-catalog happy"
    - path: "e2e/builder.spec.ts"
      provides: "Stable duplicates dataset renderings test that either really passes or is annotated with the exact deferred surface"
      contains: "test('duplicates dataset renderings"
  key_links:
    - from: "e2e/builder-v1-5.spec.ts attachConsoleGate / assertConsoleClean"
      to: "filtered noise allowlist (MapLibre, GL Driver, GPU stall, glyph range, codepoint)"
      via: "warnings.filter at lines 56-63"
      pattern: "filtered.*=.*warnings\\.filter"
    - from: "e2e/builder.spec.ts duplicates test"
      to: "POST /api/maps/{mapId}/layers duplicate response"
      via: "page.waitForResponse at line 358-360 + dataset_id equality check at line 372"
      pattern: "rowDuplicate\\?\\.dataset_id.*toBe\\(originalLayer\\.dataset_id\\)"
---

<objective>
Triage and resolve the 2 headless e2e failures from the prior partial-close session so that `npm run e2e:smoke:builder` exits 0 before tag creation in Plan 04. Each failure gets a documented disposition (real fix OR test-pin with rationale) chosen against the actual root cause observed via live browser repro, not guessed. Both failures must be resolved — no deferring to v1013.1 (A-02 user decision: BOTH must be resolved before tag).

Purpose: CTRL-01 acceptance criterion #2 requires `e2e:smoke:builder` green. The partial close left 2 failures + 15 tests "didn't run" (Playwright halts after max-failures). Until these are resolved, the smoke gate cannot pass.

Output: Two annotated and verified-green e2e spec files; the `e2e:smoke:builder` suite returns exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1060-close-gate/1060-CONTEXT.md
@.planning/phases/1060-close-gate/1060-SUMMARY.md
@e2e/builder-v1-5.spec.ts
@e2e/builder.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage and resolve e2e/builder-v1-5.spec.ts:152 (drag-from-catalog happy)</name>
  <files>e2e/builder-v1-5.spec.ts</files>
  <read_first>
    e2e/builder-v1-5.spec.ts (lines 1-275 — focus: `attachConsoleGate` at line 40, `assertConsoleClean` filter allowlist at lines 56-63 currently allowlists MapLibre, GL Driver, GPU stall, "Unable to load glyph range", "Rendering codepoint"; test '1. drag-from-catalog happy' at line 152; pointer fallback at lines 230-275)
    .planning/phases/1060-close-gate/1060-SUMMARY.md (lines 102-110 — failure hypothesis: "Phase 1057's geometry_type=null at probe time causes a font-tile fetch that previously succeeded with populated geometry_type")
    frontend/src/components/builder/BuilderMap.tsx (grep for `glyph` and `font` to find the font-tile request site)
    frontend/src/lib/builder/map-sync.ts (grep for `glyph` allowlist patterns and any string-concat that could embed `null` into a font URL)
  </read_first>
  <action>
    Triage the test failure via live browser inspect-fix-verify loop per `feedback_playwright_mcp_self_verify.md`.

    Step 1 — Capture exact failure: run `npx playwright test e2e/builder-v1-5.spec.ts:152 --reporter=list` and capture the 6 `pt` console errors verbatim. Confirm via `docker compose ps` that all services are `(healthy)` first; if not, prompt user to bring the stack up — do NOT run `docker compose up -d` autonomously.

    Step 2 — Reproduce live via Playwright MCP. Use `mcp__playwright__browser_navigate` to `http://localhost:8080`, log in as admin, navigate to a map builder page, then open Add Data and drag a vector dataset. Use `mcp__playwright__browser_console_messages` and `mcp__playwright__browser_network_requests` to inspect the exact origin of the `pt` errors. The hypothesis in 1060-SUMMARY.md is that font-tile fetches return `pt` (PBF tile error). Verify by checking the network log for failed glyph URLs of the form `/fonts/{fontstack}/{range}.pbf` and looking for `null` or `undefined` substrings in the URL path.

    Step 3 — Identify root cause. Three possibilities to discriminate between:
    (a) **Real bug**: a request fires that previously succeeded but now fails (e.g. a font URL constructed with `null` geometry_type concatenated into the path, or a layer's `text-font` style array contains `null`). The fix is at the request-construction site in `frontend/src/components/builder/BuilderMap.tsx` or `frontend/src/lib/builder/map-sync.ts` — apply the minimal change that eliminates the error in the live browser.
    (b) **Test contract drift**: the error class is harmless production noise (e.g. MapLibre glyph fetch in test env without font tiles). Extend the `assertConsoleClean` filter allowlist at lines 56-63 to add the new noise pattern. The pattern MUST be specific (NOT a blanket `.includes('pt')`) — match the exact prefix or substring captured from the live console (e.g. `'Failed to load font'` or `'PT-Sans'` or whatever the verbatim text is). Add a comment above the new filter entry: `// 1060 / CTRL-01: <pattern> is environment-only noise in headless test env — production browser does not log this. Live MCP re-verify confirms 0 errors in real browser.`
    (c) **Flaky**: if the test passes on the first re-run after instrumentation, mark it with `test.fixme(...)` and comment `// 1060 / CTRL-01: flaky after Phase 1057 PROBE-05 ogrinfo enrichment removal; deferred to v1013.1 hygiene with reproducible repro steps captured at .planning/phases/1060-close-gate/1060-FLAKY-NOTES.md`. Use this ONLY if Steps (a) and (b) genuinely don't apply — flaky-pin is the last resort.

    Step 4 — Apply the chosen disposition:
    - If (a): commit the production fix at the request site; the test should pass without modification to the test file.
    - If (b): edit `e2e/builder-v1-5.spec.ts` only — extend the warning filter allowlist with the specific pattern + a `// 1060 / CTRL-01:` provenance comment.
    - If (c): wrap the failing test with `test.fixme(...)` + provenance comment; capture repro steps in a new file `1060-FLAKY-NOTES.md` in the phase dir.

    Step 5 — Re-verify: run `npx playwright test e2e/builder-v1-5.spec.ts:152 --reporter=list` and confirm exit 0 (test passes, or test.fixme skipped). Then run the full file `npx playwright test e2e/builder-v1-5.spec.ts --reporter=list` to confirm no neighbouring tests broke (Tests 2, 3, 4 at lines 283, 364, 448).

    Constraints: do NOT add a blanket `.includes('pt')` filter — that would swallow legitimate "Cannot read prop pt of undefined" style errors. Be specific to the observed text. Do NOT modify `attachConsoleGate` at line 40 (it captures correctly; only the filter is allowlist material). Do NOT touch the other 3 tests in this spec file unless they break as a consequence of the fix. The test was authored 2026-05-19 (commit window 11:52); changes since then are the candidates for the breaking change. `git log --oneline -- frontend/src/components/builder/BuilderMap.tsx frontend/src/lib/builder/map-sync.ts` shows the Phase 1057 PROBE-05 / CLASS-07 changes — that's the change window.
  </action>
  <acceptance_criteria>
    - `npx playwright test e2e/builder-v1-5.spec.ts:152 --reporter=list` exits 0 (PASS or skipped via test.fixme).
    - The disposition (real fix / test-pin / flaky-pin) is documented as a code comment in the modified file with the prefix `// 1060 / CTRL-01:`.
    - If a test-pin path was chosen, the allowlist pattern matches a specific substring captured from the live browser, not a generic word like `'pt'`.
    - If a real fix was applied, the production code change is minimal (≤10 LOC delta in any single file) and the test passes without modification.
    - Neighboring tests in `e2e/builder-v1-5.spec.ts` (Tests 2, 3, 4 at lines 283, 364, 448) still pass.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/builder-v1-5.spec.ts --reporter=list</automated>
  </verify>
  <done>Test at line 152 exits PASS; root-cause comment with `// 1060 / CTRL-01:` prefix added at the modification site; remainder of file remains green.</done>
</task>

<task type="auto">
  <name>Task 2: Triage and resolve e2e/builder.spec.ts:338 (duplicates dataset renderings)</name>
  <files>e2e/builder.spec.ts</files>
  <read_first>
    e2e/builder.spec.ts (lines 325-405 — test 'duplicates dataset renderings from row overflow and Add Dataset modal' starts at line 338; key assertions at lines 362 (status 200/201), 367-368 (length+1), 372-375 (dataset_id + paint + layout + style_config equality), 376 (filter+1))
    .planning/phases/1060-close-gate/1060-SUMMARY.md (lines 108-110 — "Less clear v1013 connection; possibly v1058 GPKG-03 fan-out affecting BulkReview state")
    frontend/src/components/import/UploadForm.tsx (grep for `handleIngestAllLayers` and `commitFanOut` — Phase 1058 additions)
    frontend/src/pages/MapBuilderPage.tsx (search for duplicate-layer kebab handler and `another rendering` button)
    backend/app/modules/catalog/maps/router.py (grep for `POST /maps/{id}/layers` duplicate endpoint shape)
  </read_first>
  <action>
    Triage and resolve the duplicates-renderings test via live browser inspect-fix-verify loop.

    Step 1 — Capture exact failure: run `npx playwright test e2e/builder.spec.ts:338 --reporter=list` (full reporter, not dot). Confirm `docker compose ps` shows all services healthy.

    Step 2 — Reproduce live via Playwright MCP. Navigate to a map builder page in `localhost:8080`, add a vector layer, then exercise both duplicate paths from the test:
    (a) Row kebab → Duplicate menu item (test line 357-362)
    (b) Add Data modal → "another rendering" button (test line 387-393)
    Inspect the live behavior at each assertion that fails. The test asserts:
    - Row kebab duplicate returns 200 or 201 (line 362)
    - `afterRowLayers.length === initialLayers.length + 1` (line 367-368)
    - `rowDuplicate.dataset_id === originalLayer.dataset_id` (line 372)
    - `rowDuplicate.paint === originalLayer.paint` (line 373, ?? null fallback)
    - `rowDuplicate.layout === originalLayer.layout` (line 374, ?? null fallback)
    - `rowDuplicate.style_config === originalLayer.style_config` (line 375, ?? null fallback)
    Capture which assertion fails AND what the live behavior actually produces (the actual returned dataset_id, paint, layout, style_config values via `mcp__playwright__browser_network_requests` on the duplicate POST response).

    Step 3 — Identify root cause. Three possibilities to discriminate between:
    (a) **Real bug introduced by v1013**: a v1057/v1058 change broke the duplicate-layer flow. E.g. the fan-out endpoint `POST /ingest/commit-fan-out/{job_id}` (Phase 1058 Plan 04) might have changed shared `IngestJob` behavior the duplicate path depends on; OR Phase 1057's removal of probe enrichment changed `layer.kind` defaults in a way that breaks duplication round-trip. Fix at the offending site.
    (b) **Test contract drift**: the duplicate API now legitimately returns a different shape (e.g. a new field like `kind` is included in the duplicate that wasn't in the original, or `paint` is now `null` instead of `{}`). Update the test assertion to match the new contract — adding a `// 1060 / CTRL-01:` provenance comment explaining what changed and why the new shape is correct.
    (c) **Flaky timing**: `expect.poll(...)` already retries, but if the POST response arrives while the layer-list refetch races the assertion, the count may be off. Fix by widening the poll timeout or adding a stable `waitForResponse` before the poll.

    Step 4 — Apply the chosen disposition:
    - If (a): commit the production fix; expect the test to pass without modification.
    - If (b): edit only the assertion that drifted; add `// 1060 / CTRL-01: <reason>` provenance comment above the edited assertion. Do NOT relax other assertions.
    - If (c): adjust the polling/wait pattern; document with `// 1060 / CTRL-01: timing fix — <reason>`.

    Step 5 — Re-verify: `npx playwright test e2e/builder.spec.ts:338 --reporter=list` exits 0. Then run the full file `npx playwright test e2e/builder.spec.ts --reporter=list` to confirm no neighboring tests broke (this file is ~1000 LOC with many tests; full re-run is the only safe smoke).

    Constraints: do NOT delete or `.skip` the test outright — A-02 says "BOTH must be resolved before tag". `.fixme` is the only acceptable skip variant, and only as last resort. Do NOT relax the assertions wholesale (e.g. don't change `toEqual` to `toBeTruthy`). Each modified assertion must reflect an actual contract change with rationale. The duplicate path spans frontend handleRemove-pattern + backend `POST /api/maps/{id}/layers` — if you trace back to a real backend change, the fix should be at the backend and the test should pass unchanged.
  </action>
  <acceptance_criteria>
    - `npx playwright test e2e/builder.spec.ts:338 --reporter=list` exits 0.
    - If the test file was modified, every modified line has a `// 1060 / CTRL-01:` provenance comment with the disposition reason.
    - If production code was modified, the change is targeted (touches the smallest scope possible) and references the test name in its commit message.
    - Full `e2e/builder.spec.ts` suite passes (no neighboring test regressions).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/builder.spec.ts --reporter=list</automated>
  </verify>
  <done>Test at line 338 exits PASS; disposition documented with `// 1060 / CTRL-01:` prefix at the modification site; full file remains green.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm full e2e:smoke:builder green</name>
  <files></files>
  <read_first>
    package.json (the `scripts` block — confirm the exact command `e2e:smoke:builder` runs)
    .planning/phases/1060-close-gate/1060-SUMMARY.md (Smoke Gates table — current state at partial close was "2 failed, 15 didn't run, 8 passed")
  </read_first>
  <action>
    After Tasks 1+2 land, run the full `e2e:smoke:builder` suite to confirm the 15 "didn't run" tests now execute and pass.

    Step 1 — Confirm stack health via `docker compose ps`. If any service is unhealthy, prompt user to restart via `docker compose up -d --build`; do not assume.

    Step 2 — Run `cd /Users/ishiland/Code/geolens && npm run e2e:smoke:builder` and read the exit code.

    Step 3 — If exit 0: note the final test count (PASS / FAIL / SKIPPED breakdown) in a one-line memo for Plan 04's Task 1 ingestion, plus the wall-clock time. **Memo handling (sequence-safe):** If `1060-01-SUMMARY.md` does not yet exist (which is the expected case — the `<output>` step at plan close creates it), create the file FIRST with valid frontmatter `---\nplan: 1060-01\nstatus: in_progress\n---` and then append the `<!-- Plan 01 Task 3 memo -->` HTML comment block at the bottom. The `<output>` step at plan close will then update the frontmatter to `status: complete` and write the full SUMMARY body above the memo comment block, preserving the Task 3 memo as the final block. This guarantees the memo exists at Plan 04 entry time regardless of whether Task 3 or the `<output>` step writes the SUMMARY first.

    Step 4 — If exit non-zero: re-read the failing test list.
    - If a NEW failure surfaces (not the 2 fixed in Tasks 1+2), it's a regression introduced by the Task 1 or Task 2 fixes. Re-open the corresponding task, revisit the fix scope to avoid breaking the new test, and re-run.
    - If an old "didn't run" test now fails (a test that wasn't in the prior session's 2-failure set), apply the same triage protocol from Task 1: live MCP repro, identify root cause, real-fix or test-pin with rationale. Report back as part of this task — do not silently expand scope.

    Constraints: do NOT scope-creep into fixing tests that were not failing in the prior session unless they fail now as a consequence of Tasks 1+2 changes. If a previously-passing test now fails after Tasks 1+2, that IS the responsibility of this plan — fix it before declaring Wave 1 done.
  </action>
  <acceptance_criteria>
    - `npm run e2e:smoke:builder` exits 0.
    - Full test count (PASS / FAIL / SKIPPED) captured in a `<!-- Plan 01 Task 3 memo -->` HTML comment at the bottom of `1060-01-SUMMARY.md`.
    - No newly-broken tests beyond the 2 named in 1060-SUMMARY.md.
    - Wall-clock time noted for Plan 04 (smoke baseline metric).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && npm run e2e:smoke:builder</automated>
  </verify>
  <done>e2e:smoke:builder exits 0; final test counts captured in 1060-01-SUMMARY.md memo for Plan 04 ingestion.</done>
</task>

</tasks>

<verification>
- `npx playwright test e2e/builder-v1-5.spec.ts` exits 0
- `npx playwright test e2e/builder.spec.ts` exits 0
- `npm run e2e:smoke:builder` exits 0
- All modifications carry `// 1060 / CTRL-01:` provenance comments
- No tests deleted; .fixme used only as last resort with documented repro steps
</verification>

<success_criteria>
ROADMAP success criterion #2 (e2e:smoke:builder green) is satisfied; A-02 user decision (BOTH e2e failures resolved before tag) is satisfied; downstream Plan 04 can run `npm run e2e:smoke:builder` as a re-verification step and expect green.
</success_criteria>

<output>
Create `.planning/phases/1060-close-gate/1060-01-SUMMARY.md` when done. Include:
- Per-failure disposition (real fix vs test-pin vs flaky-pin) with the chosen reason
- File + line number of each modification
- Total e2e:smoke:builder counts (PASS / FAIL / SKIPPED) + wall-clock time
- Any neighboring-test regressions discovered during full-file re-run, plus their resolution
</output>
