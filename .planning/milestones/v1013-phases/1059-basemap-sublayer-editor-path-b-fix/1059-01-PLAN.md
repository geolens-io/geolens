---
phase: 1059-basemap-sublayer-editor-path-b-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/modules/catalog/maps/schemas.py
  - backend/tests/test_basemap_sublayer_overrides.py
autonomous: true
requirements: [BSE-01]

must_haves:
  truths:
    - "POST /maps/ and PATCH /maps/{id} accept basemap_config.sublayer_overrides and persist it round-trip"
    - "GET /maps/{id} returns the persisted sublayer_overrides dict unchanged"
    - "Legacy maps saved without sublayer_overrides deserialize cleanly (sublayer_overrides=None)"
    - "Pydantic clamps stroke_width/casing_width to 0..20, min_zoom/max_zoom to 0..24, opacity to 0..1"
    - "Pydantic rejects malformed hex color strings (anything that isn't #RRGGBB or null)"
  artifacts:
    - path: "backend/app/modules/catalog/maps/schemas.py"
      provides: "SublayerOverride Pydantic model + BasemapConfig.sublayer_overrides field"
      contains: "class SublayerOverride"
    - path: "backend/tests/test_basemap_sublayer_overrides.py"
      provides: "Validation + round-trip + legacy-compat test coverage"
      min_lines: 80
  key_links:
    - from: "BasemapConfig.sublayer_overrides"
      to: "SublayerOverride"
      via: "dict[str, SublayerOverride] | None type annotation"
      pattern: "sublayer_overrides:.*SublayerOverride"
    - from: "_clean_basemap_config in style_json.py"
      to: "BasemapConfig.model_validate"
      via: "existing model_validate path picks up new field automatically"
      pattern: "BasemapConfig.model_validate"
---

<objective>
Extend the backend Pydantic schema to persist per-sublayer styling overrides additively through `MapBasemapConfig.sublayer_overrides` jsonb. Backend validation must clamp numeric ranges, reject malformed hex, and round-trip the dict through every existing save/load path without a DB migration.

Purpose: Implements decisions D-01 through D-04 from CONTEXT.md. The frontend (Plans B + C) and cross-context tests (Plan D) all depend on this schema landing first. Zero-migration backward compat per CONTEXT.md D-03 and ROADMAP.md Phase 1059 acceptance criterion 4.

Output: New `SublayerOverride` Pydantic model at `backend/app/modules/catalog/maps/schemas.py`, new `BasemapConfig.sublayer_overrides` field, new pytest file `backend/tests/test_basemap_sublayer_overrides.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1059-basemap-sublayer-editor-path-b-fix/1059-CONTEXT.md

<interfaces>
<!-- Existing BasemapConfig at backend/app/modules/catalog/maps/schemas.py:182 — extend with sublayer_overrides field -->

From backend/app/modules/catalog/maps/schemas.py (current shape, lines 182-214):

```python
class BasemapConfig(BaseModel):
    label_mode: BasemapLabelMode = Field(default=BasemapLabelMode.full, ...)
    road_visibility: BasemapSublayerVisibility = Field(default=BasemapSublayerVisibility.full, ...)
    boundary_visibility: BasemapSublayerVisibility = Field(default=BasemapSublayerVisibility.full, ...)
    building_visibility: bool = Field(default=True, ...)
    land_water_tone: BasemapLandWaterTone = Field(default=BasemapLandWaterTone.default, ...)
    relief_contrast: BasemapReliefContrast | None = Field(default=None, ...)
    opacity: float = Field(default=1.0, ge=0.0, le=1.0, ...)

    model_config = ConfigDict(extra="forbid")
```

Note `extra="forbid"` — DO NOT change. The new field must be declared on the model, not arrive through `extra`.

From backend/app/modules/catalog/maps/style_json.py:237 (round-trip cleaner — already calls `BasemapConfig.model_validate`):

```python
def _clean_basemap_config(value: Any) -> dict[str, Any] | None:
    if value is None: return None
    if not isinstance(value, dict): raise ValueError(...)
    return BasemapConfig.model_validate(value).model_dump(mode="json")
```

This path automatically inherits the new field once we declare it on the model — no change needed at style_json.py:237, :905, :1225 sites.

From existing backend test pattern at backend/tests/test_maps.py:44-65:

```python
def test_basemap_config_opacity_defaults_to_one():
    from app.modules.catalog.maps.schemas import BasemapConfig
    cfg = BasemapConfig()
    assert cfg.opacity == 1.0
```

New test file mirrors this shape — direct schema import, no DB fixture needed for the unit tests; round-trip-through-cleaner uses `_clean_basemap_config` import.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SublayerOverride Pydantic model and wire BasemapConfig.sublayer_overrides field</name>
  <files>backend/app/modules/catalog/maps/schemas.py</files>
  <read_first>
    - backend/app/modules/catalog/maps/schemas.py (lines 1-30 for imports, 160-220 for the surrounding enums + BasemapConfig)
  </read_first>
  <behavior>
    - SublayerOverride accepts {stroke_color, stroke_width, casing_color, casing_width, min_zoom, max_zoom, opacity}; every field optional and nullable
    - SublayerOverride() with all defaults yields stroke_color=None, stroke_width=None, etc.
    - stroke_width=2.5 round-trips; stroke_width=-1 raises ValidationError; stroke_width=25 raises ValidationError
    - min_zoom=0 and max_zoom=24 accepted; min_zoom=-0.5 rejected; max_zoom=24.5 rejected
    - opacity=0.5 accepted; opacity=1.5 rejected
    - stroke_color="#ff0000" accepted; stroke_color="red" rejected; stroke_color="javascript:alert(1)" rejected; stroke_color="#FFAABB" accepted (case-insensitive)
    - stroke_color=None accepted (= use basemap default)
    - BasemapConfig() with no sublayer_overrides arg yields sublayer_overrides=None (backward compat)
    - BasemapConfig(sublayer_overrides={"road": {"stroke_color": "#ff0000", "stroke_width": 2}}) parses and serializes back
    - BasemapConfig(sublayer_overrides={"road": {"stroke_color": "#ff0000", "extra_unknown_field": "x"}}) raises ValidationError due to extra="forbid" on SublayerOverride; tighten with `model_config = ConfigDict(extra="forbid")` on SublayerOverride too
    - BasemapConfig(sublayer_overrides={"some_brand_new_sublayer_id": {"opacity": 0.5}}) parses (key set is opaque — D-01)
  </behavior>
  <action>
    Per D-01 + D-02 (CONTEXT.md), declare a new Pydantic `SublayerOverride` model immediately before the existing `BasemapConfig` class at backend/app/modules/catalog/maps/schemas.py:182, then add the new field to BasemapConfig.

    Pattern for the SublayerOverride model (per D-02):
    - `stroke_color: str | None = Field(default=None, description="Stroke color hex #RRGGBB or null (use basemap default)")`
    - `stroke_width: float | None = Field(default=None, ge=0.0, le=20.0, description="Stroke width in pixels, 0-20")`
    - `casing_color: str | None = Field(default=None, ...)`
    - `casing_width: float | None = Field(default=None, ge=0.0, le=20.0, ...)`
    - `min_zoom: float | None = Field(default=None, ge=0.0, le=24.0, ...)`
    - `max_zoom: float | None = Field(default=None, ge=0.0, le=24.0, ...)`
    - `opacity: float | None = Field(default=None, ge=0.0, le=1.0, ...)`
    - `model_config = ConfigDict(extra="forbid")` (mirrors BasemapConfig)

    Add a field validator on each color field that enforces the hex regex `^#[0-9a-fA-F]{6}$` when the value is non-None. Use Pydantic v2 `field_validator`:

    ```
    @field_validator("stroke_color", "casing_color")
    @classmethod
    def _validate_hex(cls, v: str | None) -> str | None:
        if v is None: return None
        if not re.match(r"^#[0-9a-fA-F]{6}$", v):
            raise ValueError("Color must be in #RRGGBB hex format")
        return v
    ```

    Import `re` and `field_validator` at the top of schemas.py if not already imported. The threat-model gate is exactly this regex — it MUST reject `javascript:`, `data:`, raw color names, and 3/8-digit hex.

    After SublayerOverride is declared, add the new field to BasemapConfig (per D-01):

    `sublayer_overrides: dict[str, SublayerOverride] | None = Field(default=None, description="Per-sublayer style overrides keyed by semantic sublayer ID (road, boundary, building, ...). Opaque key set per CONTEXT.md D-01.")`

    Place the new field immediately AFTER the existing `opacity: float = ...` line but BEFORE `model_config = ConfigDict(extra="forbid")`. Order matters for readable schema output.

    DO NOT change `extra="forbid"` on BasemapConfig — the new field is declared, not opaque.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.modules.catalog.maps.schemas import BasemapConfig, SublayerOverride; cfg = BasemapConfig(sublayer_overrides={'road': SublayerOverride(stroke_color='#ff0000', stroke_width=2.5)}); assert cfg.sublayer_overrides['road'].stroke_color == '#ff0000'; assert cfg.sublayer_overrides['road'].stroke_width == 2.5; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `from app.modules.catalog.maps.schemas import SublayerOverride` succeeds
    - `BasemapConfig()` (no args) has `sublayer_overrides is None`
    - `SublayerOverride(stroke_width=-1)` raises `ValidationError`
    - `SublayerOverride(stroke_color="red")` raises `ValidationError`
    - `SublayerOverride(stroke_color="#FF0000")` succeeds
    - `BasemapConfig.model_validate({"sublayer_overrides": {"road": {"opacity": 0.5}}})` returns a model where `cfg.sublayer_overrides["road"].opacity == 0.5`
    - `BasemapConfig.model_validate({"sublayer_overrides": {"road": {"unknown_axis": 1}}})` raises `ValidationError` (SublayerOverride extra="forbid")
  </acceptance_criteria>
  <done>SublayerOverride class exists; BasemapConfig has sublayer_overrides field; schema-level validation matches the acceptance criteria above; ruff/mypy passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add backend pytest covering schema validation, round-trip cleaner, and legacy compat</name>
  <files>backend/tests/test_basemap_sublayer_overrides.py</files>
  <read_first>
    - backend/tests/test_maps.py (lines 1-80 for fixture/import patterns)
    - backend/app/modules/catalog/maps/style_json.py (lines 237-246 for _clean_basemap_config cleaner)
  </read_first>
  <behavior>
    Test functions (one assertion per test, pytest-style):
    1. `test_sublayer_override_defaults_all_none` — `SublayerOverride()` -> all 7 fields are None
    2. `test_sublayer_override_accepts_full_payload` — round-trips through model_validate + model_dump
    3. `test_sublayer_override_rejects_negative_stroke_width` — ValidationError on stroke_width=-0.1
    4. `test_sublayer_override_rejects_oversized_stroke_width` — ValidationError on stroke_width=20.5
    5. `test_sublayer_override_rejects_zoom_out_of_range` — ValidationError on min_zoom=-0.5 and max_zoom=24.5 (parameterized or two tests)
    6. `test_sublayer_override_rejects_opacity_out_of_range` — ValidationError on opacity=1.5
    7. `test_sublayer_override_rejects_malformed_hex` — ValidationError on each of: "red", "#abc", "#1234567", "javascript:alert(1)", "", "rgb(255,0,0)"
    8. `test_sublayer_override_accepts_uppercase_hex` — "#FFAABB" parses
    9. `test_sublayer_override_rejects_unknown_extra_field` — ValidationError on {"opacity": 0.5, "halo_blur": 1} (D-14 scope guardrail)
    10. `test_basemap_config_legacy_payload_deserializes_with_no_overrides` — `BasemapConfig.model_validate({"label_mode": "full", "road_visibility": "full", "boundary_visibility": "full", "building_visibility": True, "land_water_tone": "default", "opacity": 1.0})` yields sublayer_overrides=None (no required-field error)
    11. `test_basemap_config_with_overrides_round_trips` — payload with `sublayer_overrides={"road": {"stroke_color": "#ff0000", "stroke_width": 2}}` round-trips through `BasemapConfig.model_validate(...).model_dump(mode="json")` with the override preserved
    12. `test_clean_basemap_config_preserves_overrides` — `_clean_basemap_config({"label_mode": "full", "road_visibility": "full", "boundary_visibility": "full", "building_visibility": True, "land_water_tone": "default", "opacity": 1.0, "sublayer_overrides": {"road": {"stroke_color": "#00ff00"}}})` returns a dict where `result["sublayer_overrides"]["road"]["stroke_color"] == "#00ff00"`
    13. `test_clean_basemap_config_rejects_invalid_override` — `_clean_basemap_config({..., "sublayer_overrides": {"road": {"stroke_color": "not-hex"}}})` raises `ValueError` with message containing "Invalid basemap_config metadata" (per the existing wrap at style_json.py:245)
    14. `test_sublayer_overrides_opaque_keyset` — `BasemapConfig.model_validate({"sublayer_overrides": {"some_future_provider_specific_id": {"opacity": 0.5}}})` parses; opaque keys per D-01
  </behavior>
  <action>
    Create `backend/tests/test_basemap_sublayer_overrides.py` from scratch. Use the same import style as `test_maps.py` (direct `from app.modules.catalog.maps.schemas import ...`). No DB fixtures or app factory needed — these are pure schema/serializer unit tests.

    Use `pytest.raises(ValidationError)` for Pydantic validation failures (import from `pydantic`) and `pytest.raises(ValueError)` for `_clean_basemap_config` (it re-raises as ValueError per style_json.py:245).

    For test 7 use `pytest.mark.parametrize("bad_hex", ["red", "#abc", "#1234567", "javascript:alert(1)", "", "rgb(255,0,0)"])` so one mark covers the regression set. The empty string MUST be caught — Pydantic treats `None` and `""` differently; without the regex `""` would slip through.

    For test 12 import `from app.modules.catalog.maps.style_json import _clean_basemap_config`. The cleaner is a module-private function (leading underscore) but the test sits in the same module-level test namespace and is allowed to call it directly.

    Run the file with `docker compose exec api pytest tests/test_basemap_sublayer_overrides.py -x` and verify all 14 tests pass.
  </action>
  <verify>
    <automated>docker compose exec -T api pytest tests/test_basemap_sublayer_overrides.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `backend/tests/test_basemap_sublayer_overrides.py` with at least 14 test functions
    - All tests pass under `pytest -x`
    - At minimum tests 1, 7, 10, 11, 12, 13 (the security + legacy-compat + round-trip ones) pass — they're the threat-model + zero-migration acceptance evidence
    - `grep -c "def test_" backend/tests/test_basemap_sublayer_overrides.py` returns >= 14
  </acceptance_criteria>
  <done>14 tests pass; legacy basemap_config without sublayer_overrides round-trips cleanly; malformed hex/out-of-range/extra fields rejected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend autosave → backend POST/PATCH /maps/ | User-controlled JSON arrives via authenticated map save; Pydantic is the only validation layer between request payload and the opaque jsonb column |
| jsonb storage → render path | Stored dict re-emitted to viewer/shared/embed routes; if malformed value slips past Pydantic on write, it propagates to N read paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1059A-01 | Tampering | SublayerOverride.stroke_color / casing_color | mitigate | Field-validator enforces `^#[0-9a-fA-F]{6}$` regex; rejects `javascript:`, `data:`, raw names, 3/8-digit hex. Test 7 covers the regression set. |
| T-1059A-02 | Tampering | SublayerOverride numeric fields | mitigate | Pydantic `Field(ge=, le=)` clamps stroke_width 0-20, casing_width 0-20, min/max_zoom 0-24, opacity 0-1. Tests 3-6 lock the bounds. |
| T-1059A-03 | Tampering | SublayerOverride scope creep | mitigate | `model_config = ConfigDict(extra="forbid")` rejects unknown override axes — locks D-14 scope guardrail (no dash patterns, line caps, halo blur, text-font). Test 9 covers. |
| T-1059A-04 | Information disclosure | Legacy maps without sublayer_overrides | accept | Field default `None`; legacy payloads deserialize cleanly per D-03 (zero-migration). Test 10 + 12 cover. No PII exposure — basemap_config is non-secret. |
| T-1059A-05 | Denial of service | Large sublayer_overrides dict | accept | Existing FastAPI request-body size limit applies; per-key SublayerOverride model bounds individual values. No new ingress endpoint introduced. |
| T-1059A-SC | Tampering | Pydantic / re import paths | mitigate | No new packages installed — `pydantic` and `re` are existing stdlib + already-pinned deps. RESEARCH.md package legitimacy audit not applicable to this plan. |
</threat_model>

<verification>
- Schema unit tests pass: `docker compose exec -T api pytest tests/test_basemap_sublayer_overrides.py -x -v`
- Existing basemap tests still pass: `docker compose exec -T api pytest tests/test_maps.py -x` (regression guard — new field must not break existing tests)
- ruff lint passes: `docker compose exec -T api ruff check app/modules/catalog/maps/schemas.py`
- mypy passes if project enforces: `docker compose exec -T api mypy app/modules/catalog/maps/schemas.py 2>&1 | head -5`
- Manual round-trip smoke: import SublayerOverride + BasemapConfig in a python shell, build a config with overrides, dump, re-validate, assert equal.
</verification>

<success_criteria>
- New `SublayerOverride` Pydantic model exists at `backend/app/modules/catalog/maps/schemas.py` with 7 nullable fields, hex regex validator, numeric range constraints, and `extra="forbid"`.
- `BasemapConfig.sublayer_overrides: dict[str, SublayerOverride] | None = None` field added at the same file; BasemapConfig retains `extra="forbid"`.
- New pytest file `backend/tests/test_basemap_sublayer_overrides.py` exists with ≥14 tests; all pass.
- Existing `tests/test_maps.py` continues to pass (no regression on legacy BasemapConfig).
- `style_json.py:_clean_basemap_config` automatically picks up the new field (no changes required there; verified by test 12).
- Zero Alembic migration created (D-03 zero-migration backward compat).
</success_criteria>

<output>
Create `.planning/phases/1059-basemap-sublayer-editor-path-b-fix/1059-01-SUMMARY.md` when done.

Include in SUMMARY:
- Confirmation that SublayerOverride model + BasemapConfig.sublayer_overrides field are in place
- Test count (≥14) and all pass
- Confirmation that existing tests/test_maps.py still passes
- Note that downstream Plans B/C/D can now reference the SublayerOverride schema and depend on the backend persistence path being live
- Any deviations from D-02 field set (none expected)
</output>
</content>
</invoke>