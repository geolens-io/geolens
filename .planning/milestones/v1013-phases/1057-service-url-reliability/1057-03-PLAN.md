---
phase: 1057-service-url-reliability
plan: 03
type: execute
wave: 2
depends_on:
  - 1057-01
files_modified:
  - backend/app/processing/ingest/ogr.py
  - backend/app/modules/catalog/sources/crs_uri.py
  - backend/tests/test_crs_uri_parsing.py
  - frontend/src/components/import/ImportMetadataForm.tsx
autonomous: true
requirements:
  - CRS-06
must_haves:
  truths:
    - "User importing an OGC API Features source declaring URI-form CRS (e.g., http://www.opengis.net/def/crs/OGC/1.3/CRS84) sees the detected EPSG populated automatically; no manual CRS Override field interaction"
    - "Four URI/URN CRS forms are recognized: http(s)://www.opengis.net/def/crs/OGC/1.3/CRS84 → 4326; http(s)://www.opengis.net/def/crs/EPSG/0/{N} → {N}; urn:ogc:def:crs:EPSG::{N} → {N}; urn:ogc:def:crs:OGC:1.3:CRS84 → 4326"
    - "Unrecognized URIs fall through to today's behavior (CRS=null + frontend Override field visible) — no regression for users with bare EPSG:N or unrecognized URI forms"
    - "Repro demo.pygeoapi.io/master → Large Lakes import succeeds without CRS Override field interaction"
    - "Helper covers WFS URN-form CRS declarations too (e.g., urn:ogc:def:crs:EPSG::4326 from WFS 2.0 DefaultCRS) — single utility module serves all preview/probe paths through extract_srid_from_json"
  artifacts:
    - path: "backend/app/modules/catalog/sources/crs_uri.py"
      provides: "parse_crs_uri(uri: str) -> int | None — pure helper covering the 4 D-07 forms"
      contains: "def parse_crs_uri"
    - path: "backend/app/processing/ingest/ogr.py"
      provides: "extract_srid_from_json with parse_crs_uri fallback after projjson + WKT extraction"
      contains: "parse_crs_uri"
    - path: "frontend/src/components/import/ImportMetadataForm.tsx"
      provides: "CRS Override input gated on detectedCrs being null (auto-hide when probe carried non-null CRS); visible when null (today's behavior for escape hatch)"
    - path: "backend/tests/test_crs_uri_parsing.py"
      provides: "Regression test pinning the 4 URI/URN forms + the unrecognized-fallthrough behavior"
  key_links:
    - from: "backend/app/processing/ingest/ogr.py:extract_srid_from_json"
      to: "backend/app/modules/catalog/sources/crs_uri.py:parse_crs_uri"
      via: "fallback when projjson and WKT extraction return None"
      pattern: "parse_crs_uri\\("
    - from: "frontend/src/components/import/ImportMetadataForm.tsx:detectedCrs"
      to: "CRS Override input conditional render"
      via: "detectedCrs null → show input; non-null → hide input"
      pattern: "detectedCrs\\s*==?\\s*null|detectedCrs\\s*\\?"
---

<objective>
Fix CRS-06 (P2): User importing an OGC API Features source declaring URI- or URN-form CRS references (e.g., `http://www.opengis.net/def/crs/OGC/1.3/CRS84`) does not need to manually enter an EPSG override.

Per D-07, cover four URI/URN forms; anything unrecognized falls through to today's behavior (CRS=null + frontend Override field visible). Per planner-discretion note in D-14, place the helper in a **shared utility module** (`backend/app/modules/catalog/sources/crs_uri.py`) rather than inline in `ogcapi.py` because the same parsing benefits WFS URN-form CRS declarations (`urn:ogc:def:crs:EPSG::N`) that surface today through `parse_wfs_capabilities` at `backend/app/modules/catalog/sources/adapters/wfs.py`. Wire into `extract_srid_from_json` at `backend/app/processing/ingest/ogr.py:163` as the third fallback (after projjson and WKT) — this covers every preview-time srid extraction in one place, which catches OGC API + WFS + any other URN-emitting source.

Per D-08, frontend Override field auto-hides when the probe carries non-null CRS; stays visible (today's behavior) when CRS is null — escape hatch preserved for unrecognized URIs and probe-CRS-unknown cases.

Purpose: Eliminate the manual CRS Override step for the most common URI/URN forms emitted by GeoServer and pygeoapi. Helper is a pure 6-line regex match against a known-form list — unrecognized URIs return None, preserving today's null-CRS fallthrough.

Output: New `crs_uri.py` utility module, `extract_srid_from_json` extended with URI/URN fallback, frontend conditional render on the CRS Override input, regression test for the 4 forms + fallthrough.

**Wave 2 dependency note:** Depends on Plan 1057-01 (WFS-04) because both plans modify `backend/app/processing/ingest/ogr.py` — Plan A edits `run_ogr2ogr_service` (lines 567-583), Plan C edits `extract_srid_from_json` (lines 163-184). The two functions are completely independent in behavior, so Plan C can run immediately after Plan A merges; the wave-2 sequencing is a same-file-ownership guard, not a behavioral dependency. Plan C has zero file overlap with Plan B (1057-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1057-service-url-reliability/1057-CONTEXT.md
@.planning/quick/260519-smoke-v1012/SMOKE-v1012-REPORT.md

<interfaces>
<!-- Key signatures and call sites the executor needs. Extracted from codebase. -->

From backend/app/processing/ingest/ogr.py:163-184 (the wire-up point):
```python
def extract_srid_from_json(coord_system: dict) -> int | None:
    """Extract EPSG SRID from ogrinfo JSON coordinateSystem field."""
    if not coord_system:
        return None

    # Try projjson.id.code first
    projjson = coord_system.get("projjson")
    if projjson:
        id_obj = projjson.get("id")
        if id_obj and id_obj.get("authority") == "EPSG":
            code = id_obj.get("code")
            if code is not None:
                return int(code)

    # Fall back to parsing WKT for AUTHORITY["EPSG","XXXX"]
    wkt = coord_system.get("wkt")
    if wkt:
        match = re.search(r'AUTHORITY\["EPSG","(\d+)"\]', wkt)
        if match:
            return int(match.group(1))

    # NEW (Phase 1057 CRS-06 D-07): Fall back to parsing URI/URN forms
    # from the `name` field. ogrinfo populates coordinateSystem.name with
    # the source CRS reference (URI or URN) when projjson/WKT lack EPSG authority.

    return None
```

ogrinfo JSON shape (verified from preview.py code path and OGR API docs):
- `coord_system = {"projjson": {...}, "wkt": "...", "name": "http://www.opengis.net/def/crs/OGC/1.3/CRS84"}` — the `name` field is the typical carrier for URI-form CRS in OGC API responses.
- For WFS 2.0 DefaultCRS like `urn:ogc:def:crs:EPSG::4326`, the same `name` field is the carrier in current GDAL.

From backend/app/modules/catalog/sources/router.py:462 (the consumer chain):
```python
crs=preview_data["srid"],  # populated from extract_srid_from_json
```
This sets `ServicePreviewResponse.crs` which flows to frontend as `detectedCrs`.

From frontend/src/components/import/ImportMetadataForm.tsx:18 (the prop):
```typescript
interface ImportMetadataFormProps {
  defaultName: string;
  detectedCrs: number | null;
  ...
```
And the current render at lines 205-221:
```tsx
<div className="space-y-2">
  <Label htmlFor="import-crs">{t('metadata.crsLabel')}</Label>
  <Input
    id="import-crs"
    type="number"
    placeholder={detectedCrs ? t('...crsPlaceholderDetected', { crs: detectedCrs }) : t('...crsPlaceholderEmpty')}
    value={sridOverride}
    onChange={(e) => setSridOverride(e.target.value)}
  />
  <p className="text-xs text-muted-foreground">{t('metadata.crsHelpText')}</p>
</div>
```
The current UI ALWAYS renders the CRS input, displaying the detected CRS as placeholder. Per D-08, when `detectedCrs` is non-null, the entire `<div className="space-y-2"> ... CRS ... </div>` block hides (or renders a compact read-only "Detected CRS: EPSG:4326" line instead of the editable input). When `detectedCrs` is null (today's case for unrecognized URIs and unknown CRS), the input renders as-is for the manual escape hatch.

Note: D-08 says "auto-hide when probe carries non-null CRS". Two acceptable interpretations:
  - (A) **Hide the entire div** when `detectedCrs != null` — strict reading. The user has no way to override a detected CRS. **Picked per D-08 text.**
  - (B) **Hide the input, show a non-editable confirmation line** like "Detected CRS: EPSG:4326 (auto)" — softer UX, preserves visual confirmation that detection ran.
  Executor should pick (B) per "user sees detection succeeded" affordance; the override input remains buried behind a future "Change CRS" disclosure if needed later. But (A) is acceptable per D-08 strict reading.

From backend/app/modules/catalog/sources/adapters/wfs.py:35-70 (parse_wfs_capabilities — current URN emission point):
```python
# layers contain "crs": "urn:ogc:def:crs:EPSG::4326" (raw, unparsed)
```
This is informational — the WFS probe path emits URN strings into the `crs` field on the layer dict, but `LayerInfo.crs` is not currently a schema field (only ServicePreviewResponse.crs is). The fix at `extract_srid_from_json` covers this case at preview time when ogrinfo's `coordinateSystem.name` carries the URN.

URI/URN test fixtures (the four D-07 forms):
- `http://www.opengis.net/def/crs/OGC/1.3/CRS84` → 4326
- `https://www.opengis.net/def/crs/OGC/1.3/CRS84` → 4326
- `http://www.opengis.net/def/crs/EPSG/0/3857` → 3857
- `https://www.opengis.net/def/crs/EPSG/0/4326` → 4326
- `urn:ogc:def:crs:EPSG::4326` → 4326
- `urn:ogc:def:crs:EPSG::3857` → 3857
- `urn:ogc:def:crs:OGC:1.3:CRS84` → 4326
- Unrecognized: `http://example.com/crs/foo` → None; `urn:something:else` → None; empty string → None; None input → None
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create parse_crs_uri helper module + comprehensive unit tests for the 4 forms + fallthrough</name>
  <read_first>
    - backend/app/processing/ingest/ogr.py:163-184 (extract_srid_from_json — the future caller)
    - backend/app/modules/catalog/sources/adapters/wfs.py:35-70 (parse_wfs_capabilities — URN emission today)
    - .planning/phases/1057-service-url-reliability/1057-CONTEXT.md (D-07, D-08, D-13)
    - .planning/quick/260519-smoke-v1012/SMOKE-v1012-REPORT.md (Finding 6 — exact URI: http://www.opengis.net/def/crs/OGC/1.3/CRS84)
  </read_first>
  <files>backend/app/modules/catalog/sources/crs_uri.py, backend/tests/test_crs_uri_parsing.py</files>
  <behavior>
    Test class `TestParseCrsUri` with `pytest.parametrize` covering each form individually:
    - Test 1: `parse_crs_uri('http://www.opengis.net/def/crs/OGC/1.3/CRS84')` returns `4326`.
    - Test 2: `parse_crs_uri('https://www.opengis.net/def/crs/OGC/1.3/CRS84')` returns `4326` (HTTPS variant).
    - Test 3: `parse_crs_uri('http://www.opengis.net/def/crs/EPSG/0/3857')` returns `3857`.
    - Test 4: `parse_crs_uri('http://www.opengis.net/def/crs/EPSG/0/4326')` returns `4326`.
    - Test 5: `parse_crs_uri('https://www.opengis.net/def/crs/EPSG/0/32633')` returns `32633` (HTTPS + arbitrary code).
    - Test 6: `parse_crs_uri('urn:ogc:def:crs:EPSG::4326')` returns `4326`.
    - Test 7: `parse_crs_uri('urn:ogc:def:crs:EPSG::3857')` returns `3857`.
    - Test 8: `parse_crs_uri('urn:ogc:def:crs:EPSG::32633')` returns `32633` (arbitrary code via URN form).
    - Test 9: `parse_crs_uri('urn:ogc:def:crs:OGC:1.3:CRS84')` returns `4326`.
    - Test 10: `parse_crs_uri(None)` returns `None`.
    - Test 11: `parse_crs_uri('')` returns `None`.
    - Test 12: `parse_crs_uri('http://example.com/crs/foo')` returns `None` (unknown URI).
    - Test 13: `parse_crs_uri('urn:something:else')` returns `None` (unknown URN).
    - Test 14: `parse_crs_uri('EPSG:4326')` returns `None` — bare EPSG strings are NOT this helper's job (they're handled elsewhere; this helper covers ONLY the 4 forms in D-07). Document this in the docstring.
    - Test 15: `parse_crs_uri('http://www.opengis.net/def/crs/EPSG/0/abc')` returns `None` — non-numeric code defends against malformed URIs.
    - Test 16: `parse_crs_uri('http://www.opengis.net/def/crs/EPSG/0/99999999999999999999')` returns the parsed int OR None — executor should choose: either accept arbitrary-large EPSG codes or cap at a sane upper bound (e.g., `100_000`). Document the choice in the helper docstring. Recommendation: accept any non-negative int that parses successfully via `int()` (no upper bound) — EPSG authority controls the namespace.
  </behavior>
  <action>
    Create `backend/app/modules/catalog/sources/crs_uri.py` with:
    - Module docstring referencing Phase 1057 CRS-06, D-07, D-13 (no reprojection out of scope), and the four covered forms.
    - A single public function `def parse_crs_uri(value: str | None) -> int | None:` implementing the 4 forms via compiled regex patterns. Use `re.compile` at module top for the regex constants so they're not recompiled on every call. Pattern shapes:
      - `_RE_OGC_CRS84_HTTP = re.compile(r'^https?://www\.opengis\.net/def/crs/OGC/1\.3/CRS84/?$')`
      - `_RE_EPSG_HTTP = re.compile(r'^https?://www\.opengis\.net/def/crs/EPSG/0/(\d+)/?$')`
      - `_RE_EPSG_URN = re.compile(r'^urn:ogc:def:crs:EPSG::(\d+)$')`
      - `_RE_OGC_CRS84_URN = re.compile(r'^urn:ogc:def:crs:OGC:1\.3:CRS84$')`
    - Function body: handle `None`/empty → return `None`; try each pattern in turn; CRS84 patterns return `4326`; EPSG patterns return `int(match.group(1))` (no upper bound); no match → return `None`.
    - Type annotations + docstring on the function listing the 4 forms + the fallthrough behavior + the EPSG-bare-string exclusion (Test 14 rationale).

    Create `backend/tests/test_crs_uri_parsing.py` with `TestParseCrsUri` class implementing the 16 behaviors above. Use `pytest.parametrize` for the 4-form recognition tests and group fallthrough tests separately. Add file-level docstring referencing Phase 1057 CRS-06 + Finding 6 + the exact repro URI from the smoke report.

    All tests must pass via the standard pytest invocation.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend &amp;&amp; pytest tests/test_crs_uri_parsing.py -x -v 2>&amp;1 | tail -40</automated>
  </verify>
  <done>
    - `backend/app/modules/catalog/sources/crs_uri.py` exists with one public function `parse_crs_uri` and four compiled regex constants.
    - Module + function docstrings reference Phase 1057 CRS-06 + D-07 + the four covered forms.
    - `backend/tests/test_crs_uri_parsing.py` exists with at least 16 passing tests in `TestParseCrsUri`.
    - All tests green.
    - Helper accepts arbitrary EPSG integer codes without an artificial upper bound (or documents the chosen upper bound).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire parse_crs_uri into extract_srid_from_json as third fallback + add integration test pinning the OGC API CRS84 case</name>
  <read_first>
    - backend/app/processing/ingest/ogr.py:163-184 (extract_srid_from_json — the modification point)
    - backend/app/modules/catalog/sources/crs_uri.py (the new helper from Task 1)
    - backend/app/modules/catalog/sources/preview.py:147-156 (consumer in run_service_preview — note how coord_system is built from ogrinfo JSON)
    - backend/tests/test_crs_uri_parsing.py (the existing tests from Task 1 — Task 2 ADDS new tests, doesn't replace)
    - .planning/phases/1057-service-url-reliability/1057-CONTEXT.md (D-07 — adapter wire-up; D-13 — no reprojection)
  </read_first>
  <files>backend/app/processing/ingest/ogr.py, backend/tests/test_crs_uri_parsing.py</files>
  <behavior>
    - Test 1 (integration): `extract_srid_from_json({"name": "http://www.opengis.net/def/crs/OGC/1.3/CRS84"})` returns `4326` (URI fallback fires when projjson/WKT absent).
    - Test 2 (integration): `extract_srid_from_json({"name": "urn:ogc:def:crs:EPSG::3857"})` returns `3857` (URN fallback).
    - Test 3 (ordering): `extract_srid_from_json({"projjson": {"id": {"authority": "EPSG", "code": 4326}}, "name": "urn:ogc:def:crs:EPSG::3857"})` returns `4326` — projjson wins because it's the first fallback (existing behavior preserved; URI fallback is the THIRD-position guard, only fires when projjson + WKT both return None).
    - Test 4 (ordering): `extract_srid_from_json({"wkt": 'GEOGCS["...",AUTHORITY["EPSG","4326"]]', "name": "urn:ogc:def:crs:EPSG::3857"})` returns `4326` — WKT wins because it's the second fallback.
    - Test 5 (unrecognized fallthrough): `extract_srid_from_json({"name": "http://example.com/random"})` returns `None` (preserves null-CRS behavior).
    - Test 6 (empty): `extract_srid_from_json({})` returns `None` (preserves existing early-out).
    - Test 7 (defensive): `extract_srid_from_json({"name": None})` returns `None` (defends against missing/null `name` key — pure-helper tolerates).
  </behavior>
  <action>
    Edit `backend/app/processing/ingest/ogr.py:163-184`:
    - Add `from app.modules.catalog.sources.crs_uri import parse_crs_uri` to the imports section at the top of the file.
    - In `extract_srid_from_json`, append a third fallback block AFTER the WKT regex match (after line 182 `return int(match.group(1))`, before `return None` at line 184). The block must: (a) read `coord_system.get("name")` defensively (key may be absent or None); (b) if a non-empty name string is present, call `parse_crs_uri(name)`; (c) if the helper returns a non-None integer, return it directly; (d) otherwise fall through to the existing `return None`. Add a 3-line comment block immediately above referencing Phase 1057 CRS-06 + D-07 + the four covered forms + the unrecognized-fallthrough rationale.
    - Do NOT move the existing projjson and WKT blocks — D-07 explicitly positions URI parsing as the THIRD fallback, after projjson + WKT, so authoritative EPSG declarations always win.

    Append a new test class `TestExtractSridFromJsonUriFallback` to `backend/tests/test_crs_uri_parsing.py` implementing the 7 integration behaviors above. Use direct calls to `extract_srid_from_json` (imported from `app.processing.ingest.ogr`). No mocking needed — this is a pure helper.

    Add a docstring comment to the integration test class explicitly tracing to Phase 1057 CRS-06 + Finding 6 in the smoke report (the demo.pygeoapi.io/master Large Lakes case).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend &amp;&amp; pytest tests/test_crs_uri_parsing.py -x -v 2>&amp;1 | tail -50</automated>
  </verify>
  <done>
    - `extract_srid_from_json` in `ogr.py` imports `parse_crs_uri` from the new module.
    - Function body includes the third fallback block referencing Phase 1057 CRS-06 + D-07 + the unrecognized fallthrough.
    - Existing projjson and WKT logic is untouched (verified by Test 3 + Test 4 above — they pass only if the existing logic still wins over URI fallback).
    - `TestExtractSridFromJsonUriFallback` class added to test file with at least 7 passing tests.
    - All tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Frontend — auto-hide CRS Override input when detectedCrs is non-null + add component-level test for the conditional</name>
  <read_first>
    - frontend/src/components/import/ImportMetadataForm.tsx (full file — props at lines 16-30, sridOverride state at line 65, CRS input render at lines 205-221)
    - .planning/phases/1057-service-url-reliability/1057-CONTEXT.md (D-08)
    - frontend/src/components/import/ImportMetadataForm.tsx existing test file location: `grep -rn "ImportMetadataForm" frontend/src/**/*.test.tsx` (executor: probe first)
  </read_first>
  <files>frontend/src/components/import/ImportMetadataForm.tsx</files>
  <behavior>
    - Test 1 (component): When `detectedCrs={4326}` is passed, the CRS input (`#import-crs`) does NOT render — verified via `queryByLabelText(/CRS/)` returning null OR an attribute-based selector confirming the editable input is absent.
    - Test 2 (component): When `detectedCrs={null}` is passed, the CRS input renders normally (preserves today's escape hatch).
    - Test 3 (UX affordance — chosen interpretation): When `detectedCrs` is non-null, a read-only confirmation line is visible (e.g., "Detected CRS: EPSG:{detectedCrs}") so the user knows detection succeeded. Strict interpretation D-08-A (hide everything) is acceptable if executor decides; B (show non-editable badge) is preferred for affordance.
    - Test 4 (commit semantics): When the CRS input is hidden (`detectedCrs != null`), `srid_override` in the commit request is `null` (not populated). The backend already has the detected CRS; the override field exists only for the manual fallback.
  </behavior>
  <action>
    Edit `frontend/src/components/import/ImportMetadataForm.tsx` at lines 205-221:
    - Wrap the existing `<div className="space-y-2"> ... CRS Input ... </div>` in a conditional: `{!detectedCrs && ( <div className="space-y-2"> ... </div> )}`. This is option (A) per D-08 strict reading.
    - **Preferred option (B):** Render a compact non-editable confirmation line in the non-null branch using existing `Badge` or `<p>` markup, with a copy like `t('metadata.crsDetected', { crs: detectedCrs, defaultValue: 'Detected CRS: EPSG:{{crs}}' })`. The full input only renders when `!detectedCrs`. Add a new i18n key `metadata.crsDetected` with `defaultValue` inline (the codebase's i18n pattern, observed in `t('metadata.crsPlaceholderDetected', { crs: detectedCrs })` already covers the in-code default). Executor may also choose to ship option (A) and add the badge in a follow-up — but (B) is preferred per the planner-discretion note above.
    - Add an inline comment block above the conditional explaining D-08 + Phase 1057 + the fallthrough rationale: "When detectedCrs is null (unrecognized URI or true unknown), the manual override input remains visible as an escape hatch."
    - Do NOT modify the `srid_override` commit-request logic at lines 133-136. When the input is hidden, `sridOverride` state stays at its initial empty string, so `srid_override` resolves to `null` in the commit — exactly the desired semantics (Test 4).

    Check for any existing test file for `ImportMetadataForm.tsx`: `find frontend/src -name "ImportMetadataForm*.test.*"`. If a test file exists, add 4 new test cases per the behaviors above using the existing test patterns (React Testing Library + vitest). If no test file exists, do NOT create one (the typecheck plus the deferred Phase 1060 live MCP gate is sufficient for this small-surface UX change); instead, add a TODO comment in the component referencing the deferred manual verification at Phase 1060.

    Run frontend typecheck after the edit to confirm no breakage.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend &amp;&amp; npm run typecheck 2>&amp;1 | tail -20 ; echo "---" ; find frontend/src -name "ImportMetadataForm*.test.*" -exec echo "Test file exists: {}" \;</automated>
  </verify>
  <done>
    - CRS Override input (`#import-crs` + surrounding label/help text) renders only when `detectedCrs` is null/falsy.
    - When `detectedCrs` is non-null, a non-editable confirmation line is visible (option B preferred) OR the entire CRS section is hidden (option A acceptable).
    - Inline comment block above the conditional references D-08 + Phase 1057 + the escape-hatch rationale.
    - `npm run typecheck` reports 0 errors.
    - If an `ImportMetadataForm.test.tsx` file exists, the 4 behaviors are pinned with test cases; otherwise a TODO comment references the Phase 1060 live MCP gate.
    - `srid_override` commit semantics unchanged (verified via Test 4 above OR by reading the unchanged lines 133-136).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Remote service → ogrinfo subprocess → coordinateSystem.name | Untrusted URI/URN string from a remote OGC service feeds into parse_crs_uri |
| parse_crs_uri output (int) → PostGIS SRID consumer (ServicePreviewResponse.crs) | EPSG integer authoritative to the ingest pipeline; downstream SRID is used in spatial SQL via parameter binding (asyncpg), not string interpolation |
| Frontend conditional render on detectedCrs | No new trust boundary; `detectedCrs: number \| null` is already-validated TS type |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1057C-01 | Tampering | parse_crs_uri pattern matching against untrusted URI string | mitigate | Helper uses compiled regex with anchored `^...$` patterns; only known forms match. Unrecognized URIs return None (default-deny). No eval, no regex injection (the regexes are constants), no path or SQL manipulation. The function's signature is `str | None → int | None` — only pure-value transformations. |
| T-1057C-02 | Information Disclosure | URI string in logs | accept | The URI/URN is already exposed in probe response and preview response payloads as part of the OGC service contract. Helper does not log; structured logging upstream may log the parsed integer (4326, etc.) which is non-sensitive. |
| T-1057C-03 | Denial of Service | Pathological regex input from a malicious service | mitigate | Patterns are non-backtracking (no nested quantifiers, no alternation with overlapping prefixes). `\d+` against EPSG code is bounded by the URI length cap (httpx default + Pydantic max_length=2048 on ProbeRequest.url, which transitively bounds extracted strings). Worst-case regex match is O(n) on URI length. |
| T-1057C-04 | Spoofing | Service declaring a misleading URI (e.g., http://www.opengis.net/def/crs/EPSG/0/9999999) | accept | An attacker-controlled service can already inject arbitrary EPSG codes into the preview response. This helper only adds new URI forms to recognize; it does not weaken existing validation. Downstream PostGIS rejects unknown SRIDs at `Find_SRID`/`ST_Transform` time. No new attack surface. |
| T-1057C-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies; helper uses stdlib `re` only. Frontend changes use existing `Badge`/`Input`/`Label` components. RESEARCH.md `## Package Legitimacy Audit` not applicable. |
</threat_model>

<verification>
1. **Unit (helper):** `pytest backend/tests/test_crs_uri_parsing.py::TestParseCrsUri -x -v` — all 16 form/fallthrough tests green.
2. **Unit (integration):** `pytest backend/tests/test_crs_uri_parsing.py::TestExtractSridFromJsonUriFallback -x -v` — all 7 fallback-ordering tests green.
3. **Static (backend):** `grep -n "parse_crs_uri" backend/app/processing/ingest/ogr.py` — imported AND called from extract_srid_from_json (one match each).
4. **Static (backend):** `grep -n "Phase 1057\|CRS-06\|D-07" backend/app/modules/catalog/sources/crs_uri.py` — module-level reference present.
5. **Typecheck (frontend):** `npm run typecheck` — 0 errors.
6. **Static (frontend):** `grep -n "detectedCrs" frontend/src/components/import/ImportMetadataForm.tsx` — conditional render present.
7. **Live smoke deferred to Phase 1060:** demo.pygeoapi.io/master Large Lakes import without CRS Override interaction — verified in Phase 1060 MCP gate.
</verification>

<success_criteria>
- The 4 D-07 URI/URN forms parse to correct EPSG codes (verified by Task 1 unit tests).
- `extract_srid_from_json` uses URI parsing as the THIRD fallback, after projjson + WKT — authoritative declarations still win (verified by Task 2 ordering tests).
- Unrecognized URIs return None, preserving today's null-CRS fallthrough (verified by Task 1 + Task 2 fallthrough tests).
- Frontend CRS Override input hides when `detectedCrs` is non-null; stays visible when null (verified by Task 3 + typecheck).
- All test files green; `npm run typecheck` green.
- Helper module + the OGC API adapter wire-up are referenced in Phase 1057 SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/1057-service-url-reliability/1057-03-SUMMARY.md` when done. Include:
- Location of the helper (`crs_uri.py` shared utility — note that D-14's planner discretion led to placement under `backend/app/modules/catalog/sources/` rather than inline in `ogcapi.py`).
- Confirmation that `extract_srid_from_json` ordering is unchanged (projjson → WKT → URI).
- Frontend option chosen (A or B per the inline note).
- Test count + pass status backend + frontend typecheck status.
- Note that live MCP repro against demo.pygeoapi.io/master Large Lakes defers to Phase 1060.
</output>
</content>
</invoke>