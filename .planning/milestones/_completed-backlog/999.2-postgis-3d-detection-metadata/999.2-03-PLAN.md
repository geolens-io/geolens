---
phase: 999.2-postgis-3d-detection-metadata
plan: 03
type: execute
wave: 3
depends_on: [999.2-01, 999.2-02]
files_modified:
  - frontend/src/types/api.ts
  - frontend/src/pages/DatasetPage.tsx
autonomous: false
requirements: [D-05]

must_haves:
  truths:
    - "Frontend DatasetResponse type includes is_3d, n_dims, z_min, z_max"
    - "Dataset detail page shows Z range for 3D datasets"
    - "Z range is not shown for 2D datasets"
    - "Z range display is visually consistent with existing stats line"
  artifacts:
    - path: "frontend/src/types/api.ts"
      provides: "is_3d, n_dims, z_min, z_max on DatasetResponse"
    - path: "frontend/src/pages/DatasetPage.tsx"
      provides: "Z range display in stats line for vector datasets"
  key_links:
    - from: "frontend/src/pages/DatasetPage.tsx"
      to: "frontend/src/types/api.ts"
      via: "DatasetResponse.is_3d, z_min, z_max"
      pattern: "dataset\\.is_3d|dataset\\.z_min|dataset\\.z_max"
---

<objective>
Surface 3D geometry metadata in the dataset detail page UI.

Purpose: Users can see at a glance whether a dataset has 3D geometry and what the Z range is. Per D-05, this is an informational display on the dataset detail page.
Output: Updated TypeScript types, Z range indicator in the dataset stats line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/999.2-postgis-3d-detection-metadata/999.2-CONTEXT.md
@.planning/phases/999.2-postgis-3d-detection-metadata/999.2-01-SUMMARY.md
@.planning/phases/999.2-postgis-3d-detection-metadata/999.2-02-SUMMARY.md

@frontend/src/types/api.ts
@frontend/src/pages/DatasetPage.tsx
@frontend/src/components/dataset/DatasetDetailHeader.tsx

<interfaces>
<!-- From Plan 01 — Backend DatasetResponse now includes: -->
is_3d: bool | None       # True if geometry has Z dimension
n_dims: int | None       # 2, 3, or 4
z_min: float | None      # Minimum Z across all features
z_max: float | None      # Maximum Z across all features
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update frontend types and add Z range to stats line</name>
  <files>frontend/src/types/api.ts, frontend/src/pages/DatasetPage.tsx</files>
  <action>
**In `frontend/src/types/api.ts`**, add four fields to the `DatasetResponse` interface, after `geometry_type` (around line 82):

```typescript
is_3d: boolean | null;
n_dims: number | null;
z_min: number | null;
z_max: number | null;
```

**In `frontend/src/pages/DatasetPage.tsx`**, add a Z range indicator in the vector dataset stats line section. Find the `statsLine` JSX (around line 349). Inside the vector_dataset branch (the first `<>` block after `dataset.record_type === 'vector_dataset' || ...`), after the EPSG display and before the closing `</>`, add:

```tsx
{dataset.is_3d && (
  <>
    <Sep />
    <span className="inline-flex items-center gap-1">
      <span className="font-medium">3D</span>
      {dataset.z_min != null && dataset.z_max != null && (
        <span className="text-muted-foreground">
          Z: {dataset.z_min.toFixed(1)} to {dataset.z_max.toFixed(1)}
        </span>
      )}
    </span>
  </>
)}
```

This renders as: `3D Z: 12.0 to 847.0` inline with the existing stats (geometry type, feature count, EPSG). If z_min/z_max are null (shouldn't happen for 3D data but defensive), it just shows "3D". For 2D datasets, nothing is rendered.

The display follows the existing pattern: `<Sep />` separator, inline text, same `text-sm text-muted-foreground` styling inherited from the parent wrapper. No new components or imports needed.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>DatasetResponse type includes is_3d, n_dims, z_min, z_max. Dataset detail page conditionally renders 3D badge with Z range for vector datasets. TypeScript compiles without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify 3D metadata display</name>
  <action>Human verifies the 3D metadata display on the dataset detail page by uploading a 3D dataset and checking the stats line.</action>
  <what-built>3D geometry metadata display on the dataset detail page. For any vector dataset with 3D geometry (is_3d=true), the stats line shows "3D Z: {min} to {max}" alongside existing geometry type, feature count, and EPSG info.</what-built>
  <how-to-verify>
    1. Start the dev environment: `docker compose up -d --build backend` then open http://localhost:8080
    2. Upload a 3D dataset (e.g., a GeoJSON with Z coordinates in the positions array, or a shapefile with POINT Z geometry)
    3. Navigate to the dataset detail page
    4. Verify the stats line shows "3D" badge with Z range (e.g., "3D Z: 12.0 to 847.0")
    5. Navigate to a regular 2D dataset — verify no "3D" indicator appears
    6. Check that the Z range values look reasonable for the uploaded data
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint — requires human verification"</automated>
  </verify>
  <done>User confirms 3D metadata displays correctly for 3D datasets and is absent for 2D datasets.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → UI | z_min/z_max values displayed to user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.2-06 | I (Info Disclosure) | Z range in UI | accept | Z metadata has same visibility as existing dataset fields (extent_bbox, feature_count). No new access surface. |
</threat_model>

<verification>
- TypeScript compiles without errors
- DatasetResponse type has all four new fields
- DatasetPage conditionally renders 3D info only when dataset.is_3d is true
- Visual verification via checkpoint
</verification>

<success_criteria>
- Frontend TypeScript types match backend schema (is_3d, n_dims, z_min, z_max)
- 3D datasets show "3D Z: {min} to {max}" in the stats line
- 2D datasets show no 3D indicator
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/999.2-postgis-3d-detection-metadata/999.2-03-SUMMARY.md`
</output>
