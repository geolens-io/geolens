---
phase: 1061-security-audit-2026-05-19-remediation
plan: 06
type: execute
wave: 2
depends_on:
  - 1061-01
  - 1061-02
  - 1061-03
  - 1061-04
  - 1061-05
files_modified:
  - AGENTS.md
  - .pre-commit-config.yaml
  - docs-internal/audits/security-lessons.md
autonomous: true
requirements:
  - SEC-GUARD-01
must_haves:
  truths:
    - "AGENTS.md contains a Pre-Commit Checklist section pinning the visibility-filter coverage contract for new Record/Dataset/Map/RecordEmbedding-derived endpoints"
    - "AGENTS.md cross-links to docs-internal/audits/security-lessons.md as the authoritative recurring-patterns ledger"
    - "A pre-commit grep guardrail proposal is EVALUATED: either shipped in .pre-commit-config.yaml (if false-positive rate is low) OR documented as a manual review item with rationale for not auto-enforcing"
    - "The SSRF redirect-revalidation contract is ALSO pinned: any new `httpx.AsyncClient(follow_redirects=True)` outside `sources/security.py` must use `make_safe_client` instead — surfaced in the same Pre-Commit Checklist"
    - "docs-internal/audits/security-lessons.md updated with credential-rotation operator guidance (SEC-S06 follow-up note)"
  artifacts:
    - path: AGENTS.md
      provides: "Pre-Commit Checklist section with visibility-filter + SSRF coverage rules"
      contains: "Visibility-filter coverage"
    - path: .pre-commit-config.yaml
      provides: "Conditional — present only if grep heuristic is low-false-positive enough to ship"
      contains: "check_dataset_access"
    - path: docs-internal/audits/security-lessons.md
      provides: "Updated with credential-rotation guidance and SEC-S06 implementation reference"
      contains: "2026-05-20 — Phase 1061"
  key_links:
    - from: AGENTS.md
      to: docs-internal/audits/security-lessons.md
      via: "Cross-link in Pre-Commit Checklist"
      pattern: "security-lessons\\.md"
    - from: AGENTS.md
      to: backend/app/modules/catalog/authorization.py
      via: "Reference to check_dataset_access / apply_visibility_filter as the visibility-coverage contract"
      pattern: "check_dataset_access|apply_visibility_filter"
    - from: AGENTS.md
      to: backend/app/modules/catalog/sources/security.py
      via: "Reference to make_safe_client as the SSRF redirect-coverage contract"
      pattern: "make_safe_client"
---

<objective>
Close SEC-GUARD-01 — pin the headline architectural patterns from the audit into `AGENTS.md` so future contributors cannot reintroduce the same regressions. Two patterns to pin:

1. **Visibility-filter coverage** (audit's #1 regression surface — 5 of 7 HIGHs cluster here): any new handler that fetches a `Record` / `Dataset` / `Map` / `RecordEmbedding` by ID must call `check_dataset_access_or_anonymous` (read) or `check_dataset_access` (write/destructive), OR apply `apply_visibility_filter(stmt, user, user_roles, Record, DatasetGrant)` to the underlying query.

2. **SSRF redirect-revalidation** (SEC-S04 pattern — added in Plan 04): any new `httpx.AsyncClient(follow_redirects=True)` in a route handler must use `make_safe_client()` from `backend/app/modules/catalog/sources/security.py` instead of constructing a raw client.

Purpose: Locks the patterns into the developer-facing playbook. Removes any ambiguity for future contributors and AI agents working in this repo. Closes the architectural-guardrail HIGH from the audit.

Output:
- `AGENTS.md` gets a "Security pre-commit checklist" section under the existing "Security & Configuration Tips" heading.
- `.pre-commit-config.yaml` either:
  - **Ships** with grep hooks if the false-positive rate is acceptable (target: <5% on the current codebase as of Plans 01-05 stabilization), OR
  - **NOT shipped** — documented in AGENTS.md as a "manual review" checklist item with the evaluation rationale in SUMMARY.
- `docs-internal/audits/security-lessons.md` appended with the Phase 1061 implementation reference + the credential-rotation guidance flagged from Plan 05.

This plan runs in Wave 2 to ensure the patterns referenced in AGENTS.md match what actually shipped — no risk of documenting a contract that differs from the implementation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1061-security-audit-2026-05-19-remediation/1061-CONTEXT.md
@docs-internal/audits/sec-audit-20260519.md
@docs-internal/audits/security-lessons.md
@AGENTS.md

<interfaces>
<!-- Current AGENTS.md structure (55 lines) — the "Security & Configuration Tips" section is at line ~49-55 -->

```markdown
## Security & Configuration Tips

Use `.env.example` and `.env.test.example` as templates. Never commit secrets, coverage output, Playwright reports, virtual environments, or dependency directories.

Keep assistant and internal planning state out of git. `.gitignore` must continue to cover `.claude/`, `.codex/`, `.agents/`, `.planning/`, `.gsd/`, and `docs-internal/`; if any of those paths become tracked again, untrack them before public release work continues.

Keep root repository docs single-purpose: `README.md` is the public overview, ...
```

The new "Security pre-commit checklist" section will be appended within this existing section (or as its own H3 / H2).

<!-- Patterns to pin — from sec-audit-20260519.md §"Patterns found recurring" + security-lessons.md §"Rules to add" -->

Rule 1 (from security-lessons.md §1):
> Any new handler that fetches a `Record`, `Dataset`, `Map`, or `RecordEmbedding` by ID must:
>   - Call `check_dataset_access_or_anonymous` (read) or `check_dataset_access` + ownership check (write/destructive), OR
>   - Apply `apply_visibility_filter(stmt, user, user_roles, Record, DatasetGrant)` to the underlying query.
> Add a pre-commit grep that fails CI if a handler in `backend/app/modules/catalog/datasets/api/` or `backend/app/standards/` references `get_dataset(` without `check_dataset_access` or `apply_visibility_filter` in the same function body.

Rule 2 (from security-lessons.md §2):
> Any new `httpx.AsyncClient(follow_redirects=True, ...)` in a route handler must pass through a `make_safe_client()` factory that installs the per-hop `_revalidate_redirect` event hook.
> Add a pre-commit grep that fails CI if `httpx.AsyncClient(.*follow_redirects=True` appears outside `app.modules.catalog.sources.security`.

<!-- pre-commit grep heuristic complexity -->

The Rule 1 grep is HARD to implement cleanly because:
- "Same function body" is hard for grep to assert; requires per-file Python-AST awareness.
- A simple `grep -lE "get_dataset\\(" ... | xargs grep -L "check_dataset_access"` would flag ANY file where get_dataset appears anywhere but check_dataset_access does not — too noisy because many files have legitimate `get_dataset` usage in service-layer code where the gate runs at the router boundary.

The Rule 2 grep is EASY:
- A single regex `grep -rnE 'httpx\.AsyncClient\(.*follow_redirects=True' backend/app/ | grep -v 'sources/security.py'` produces zero results after Plan 04 ships. Any new addition trips the gate.

DECISION FRAMEWORK for shipping the grep:
- Rule 2 grep: SHIP (low FP rate verifiable, useful coverage).
- Rule 1 grep: EVALUATE FIRST — run the candidate regex against the current codebase post-Plans 01-04. If FP count ≤ ~3 files (i.e., legitimate cross-file patterns), ship with an `--exclude` list. If FP > 5 files, document as a manual review item in AGENTS.md instead.

<!-- security-lessons.md current state -->

The file is 37 lines, ledger-shaped (newest at top). Append a new entry at the top:

```markdown
## 2026-05-20 — Phase 1061: HIGH-severity remediation shipped

### Patterns now locked in AGENTS.md
- Visibility-filter coverage (Rule 1) — manual Pre-Commit Checklist item OR pre-commit grep (see decision in Plan 06 SUMMARY).
- SSRF redirect-revalidation (Rule 2) — pre-commit grep shipped; AGENTS.md cross-reference.

### Implementation references
- STAC visibility filter: Plan 1061-01 → `_base_published_raster_query(user, user_roles)` in `backend/app/standards/stac/router.py`
- IDOR fixes: Plan 1061-02 → `check_dataset_access` in 7 mutation/DDL handlers
- Related-datasets seed gate: Plan 1061-03 → `check_dataset_access_or_anonymous` BEFORE `get_related_datasets`
- SSRF redirect-revalidation: Plan 1061-04 → `make_safe_client` + `_revalidate_redirect` in `backend/app/modules/catalog/sources/security.py`
- Demo credential rotation: Plan 1061-05 → `scripts/init-demo-env.sh` + extended `validate_demo_credentials_guard`

### Operator guidance (SEC-S06 follow-up)
- Rotate demo credentials regularly (the script is idempotent — run with --force from a cron or restart hook).
- Capture the admin password from `scripts/init-demo-env.sh` stdout — it is the only output and is not recoverable from the file (admin password is hashed at-rest).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evaluate Rule 1 grep heuristic against the shipped codebase</name>
  <files>(no files modified — evaluation only)</files>
  <behavior>
    - Run the candidate grep heuristic against the current codebase (post-Plans 01-05).
    - Count false positives: files that legitimately use `get_dataset` without `check_dataset_access` in the same file (e.g., service-layer modules where the gate lives at the router boundary).
    - Decision: ship the grep if FP ≤ 3 with an explicit `--exclude`-list; document as manual review otherwise.
  </behavior>
  <action>
Run the candidate heuristic:

```bash
# Candidate 1 — file-level (any file with get_dataset must also have check_dataset_access)
echo "=== Candidate 1: file-level ==="
cd /Users/ishiland/Code/geolens
grep -l "get_dataset(" backend/app/modules/catalog/datasets/api/*.py backend/app/standards/*/*.py 2>/dev/null \
  | xargs -I{} sh -c 'if ! grep -q "check_dataset_access\|apply_visibility_filter" {}; then echo "FP candidate: {}"; fi'

# Candidate 2 — function-body-aware (more accurate, requires Python AST)
echo "=== Candidate 2: AST-aware (manual / future) ==="
echo "Requires per-function AST traversal — defer to Python tool, not grep."

# Candidate 3 — the route-decorator scope (only flag files with FastAPI route decorators)
echo "=== Candidate 3: route-decorator-scoped ==="
grep -lE "^@(stac_|ogc_|layers_|router)\.(get|post|patch|delete|put)" backend/app/modules/catalog/datasets/api/*.py backend/app/modules/catalog/layers/*.py backend/app/standards/*/*.py 2>/dev/null \
  | xargs -I{} sh -c 'if ! grep -q "check_dataset_access\|apply_visibility_filter" {}; then echo "Route-file without gate: {}"; fi'
```

Run each candidate. Count results. Record in SUMMARY:
- Candidate 1 FP count: ___
- Candidate 3 FP count: ___
- Decision: ship Candidate 3 (route-decorator-scoped) if FP ≤ 3, else document as manual review.

For each Candidate-3 false positive (i.e., a route file that legitimately does not need the gate — e.g., a public discovery endpoint or aggregate stats endpoint), record the file path + rationale. The `--exclude` list goes into `.pre-commit-config.yaml` if shipping.

Common legitimate-exempt files (predicted):
- `backend/app/standards/ogc/router.py:landing_page` — public landing page, no Record access.
- `backend/app/standards/stac/router.py:conformance` — pure static response, no DB access.
- Files that call `get_dataset` via SERVICE layer (`update_user_metadata`, `delete_dataset`) where the router-layer call site IS the gate site.

The Candidate-3 grep checks ROUTE FILES specifically (those with `@router.get`/etc. decorators), which is the audit's actual concern.
  </action>
  <verify>
    <automated>echo "Heuristic evaluation complete — see SUMMARY for FP counts and decision"</automated>
  </verify>
  <done>
- Both grep candidates run against the live codebase.
- FP count recorded.
- Ship/no-ship decision recorded in SUMMARY (with rationale).
- If shipping: exclude list captured for Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 2: AGENTS.md "Security pre-commit checklist" section</name>
  <files>AGENTS.md</files>
  <behavior>
    - Append a new H3 section under "Security & Configuration Tips" titled "Security pre-commit checklist".
    - Three rules pinned: (1) Visibility-filter coverage, (2) SSRF redirect-revalidation, (3) Demo credential refresh — with explicit file references that match what was shipped in Plans 01-05.
    - Cross-link to `docs-internal/audits/security-lessons.md` as the authoritative ledger.
    - Reference the actual function names and file paths (not generic descriptions).
  </behavior>
  <action>
Edit `AGENTS.md`. Append a new H3 section at the END of "Security & Configuration Tips" (just before the "Keep root repository docs single-purpose" paragraph).

Insert this section:

```markdown
### Security pre-commit checklist

The audit at `docs-internal/audits/sec-audit-20260519.md` and the recurring-patterns ledger at `docs-internal/audits/security-lessons.md` codify the patterns below. Any code change that touches catalog data access, external URL fetching, or demo-environment configuration must satisfy these rules.

**Rule 1 — Visibility-filter coverage** *(audit's #1 regression surface; 5 of 7 HIGHs in the 2026-05-19 audit clustered here)*

Any new FastAPI handler that fetches a `Record`, `Dataset`, `Map`, or `RecordEmbedding` by ID must do ONE of:

- Call `check_dataset_access_or_anonymous(db, dataset, dataset_id, user)` from `backend/app/modules/catalog/authorization.py` (read-side endpoints), OR
- Call `check_dataset_access(db, dataset, dataset_id, user)` from the same module (write/destructive endpoints; raises 404 on access denial), OR
- Apply `apply_visibility_filter(stmt, user, user_roles, Record, DatasetGrant)` to the underlying SQLAlchemy `Select` (list endpoints with their own query construction).

Reference implementations:
- `backend/app/standards/ogc/router.py` — OGC Features peer router (read path).
- `backend/app/standards/stac/router.py` — STAC router (read path; fixed in Phase 1061).
- `backend/app/modules/catalog/datasets/api/router_metadata.py` — 5 sibling mutation handlers (write path).

**Rule 2 — SSRF redirect-revalidation**

Any new `httpx.AsyncClient` configured with `follow_redirects=True` MUST be constructed via `make_safe_client()` from `backend/app/modules/catalog/sources/security.py` — never directly with `httpx.AsyncClient(follow_redirects=True, ...)`. The factory installs the per-hop `_revalidate_redirect` event hook that re-runs `validate_url_for_ssrf` against every 3xx `Location` header.

The same applies to ogr2ogr / GDAL subprocess calls that fetch user-supplied URLs: set `GDAL_HTTP_FOLLOWLOCATION=NO` in the subprocess env. Reference: `backend/app/processing/ingest/ogr.py` (service-ingest path).

**Rule 3 — Demo credentials are per-deploy**

`.env.demo` is gitignored and generated by `scripts/init-demo-env.sh`. Never commit `.env.demo` and never reintroduce the literal committed values (`demo-only-do-not-use-in-production-change-me`, `demodemo`, `geolens-demo-2026`, `minioadmin/minioadmin`). The boot-time `validate_demo_credentials_guard` in `backend/app/core/config.py` refuses these literals regardless of `GEOLENS_DEMO_MODE`. Rotate by re-running `scripts/init-demo-env.sh --force` and restarting the stack.

**Enforcement.** Rule 2 has a pre-commit grep hook in `.pre-commit-config.yaml`. Rule 1's grep heuristic is evaluated per Phase 1061 Plan 06 — see SUMMARY for the FP-count rationale; if not shipped, treat as a manual review line item. Rule 3 is enforced at backend boot — boot-failure is the signal.

See `docs-internal/audits/security-lessons.md` for the append-only ledger of recurring patterns + future rules.
```

If Task 1's decision was NOT to ship the Rule 1 grep (FP count too high), adjust the "Enforcement" paragraph to:
> Rule 1 is a manual review checklist item — its grep heuristic was evaluated against the current codebase in Phase 1061 and produced N false positives (see Plan 06 SUMMARY for the per-file rationale).

Atomic commit suggested:
- `docs(1061-06): SEC-GUARD-01 — Security pre-commit checklist in AGENTS.md`
  </action>
  <verify>
    <automated>grep -E "Visibility-filter coverage|make_safe_client|init-demo-env.sh" AGENTS.md | wc -l</automated>
  </verify>
  <done>
- AGENTS.md "Security pre-commit checklist" section added with 3 rules.
- Each rule references the actual function/file paths that shipped in Plans 01-05.
- Cross-link to `docs-internal/audits/security-lessons.md` present.
- Enforcement paragraph reflects the Task 1 ship/no-ship decision.
  </done>
</task>

<task type="auto">
  <name>Task 3: .pre-commit-config.yaml — Rule 2 grep guardrail (always ship) + Rule 1 grep (conditional)</name>
  <files>.pre-commit-config.yaml</files>
  <behavior>
    - Create `.pre-commit-config.yaml` if it does not exist (project currently has none).
    - Ship the Rule 2 grep unconditionally — easy regex with verified-zero FP count after Plan 04.
    - Ship the Rule 1 grep ONLY IF Task 1's evaluation found ≤ 3 FPs; otherwise omit and document the choice in AGENTS.md (already handled in Task 2).
    - Hook style: `repo: local` with `language: pygrep` or `system` (bash + grep). pygrep is preferred when expressible as a single regex.
  </behavior>
  <action>
**Step 1 — Check if `.pre-commit-config.yaml` exists**:

```bash
test -f .pre-commit-config.yaml && echo "EXISTS — extend it" || echo "MISSING — create from scratch"
```

The project currently does not have a `.pre-commit-config.yaml` (verified during plan-phase). So this task CREATES one.

**Step 2 — Create `.pre-commit-config.yaml`** with the Rule 2 grep:

```yaml
# Pre-commit hooks for GeoLens security guardrails.
# Phase 1061 SEC-GUARD-01: pins audit-derived patterns so regressions trip CI.
# See AGENTS.md "Security pre-commit checklist" and
# docs-internal/audits/security-lessons.md for the underlying rationale.
#
# Run locally:
#   pip install pre-commit && pre-commit install
#   pre-commit run --all-files

repos:
  - repo: local
    hooks:
      - id: ssrf-safe-client
        name: SSRF Rule 2 — httpx.AsyncClient(follow_redirects=True) outside sources/security.py
        description: >
          Any httpx.AsyncClient with follow_redirects=True must be constructed
          via make_safe_client() from backend/app/modules/catalog/sources/security.py.
          The factory installs the per-hop _revalidate_redirect event hook.
        language: pygrep
        entry: 'httpx\.AsyncClient\([^)]*follow_redirects=True'
        types: [python]
        # Allow the canonical factory home; deny everywhere else.
        exclude: '^backend/app/modules/catalog/sources/security\.py$'
        files: '^backend/app/.*\.py$'
```

**Step 3 — IF Task 1's evaluation found ≤ 3 FPs for the Rule 1 grep, ALSO ship it**:

Add this hook below the SSRF one (substitute the actual exclude list captured in Task 1):

```yaml
      - id: visibility-filter-coverage
        name: Visibility Rule 1 — route files with @router decorators must reference check_dataset_access or apply_visibility_filter
        description: >
          Any route handler that fetches a Record/Dataset/Map by ID must call
          check_dataset_access_or_anonymous (read) or check_dataset_access (write)
          or apply visibility_filter — see AGENTS.md Security pre-commit checklist.
        language: system
        entry: bash -c '
          for f in "$@"; do
            if grep -qE "^@(stac_|ogc_|layers_|router)\.(get|post|patch|delete|put)" "$f" \
              && grep -qE "get_dataset\(" "$f" \
              && ! grep -qE "check_dataset_access|apply_visibility_filter" "$f"; then
              echo "FAIL: $f has route handlers calling get_dataset without check_dataset_access or apply_visibility_filter"
              exit 1
            fi
          done
        ' --
        types: [python]
        files: '^backend/app/(modules/catalog|standards)/.*\.py$'
        exclude: |
          (?x)^(
            backend/app/standards/ogc/router\.py$|
            backend/app/standards/stac/router\.py$|
            <fill in any additional exempt files from Task 1>
          )$
```

If Task 1 found > 3 FPs, OMIT the visibility-filter-coverage hook from the YAML and note in SUMMARY that AGENTS.md's "Enforcement" paragraph already covers Rule 1 as manual review.

**Step 4 — Smoke test the config**:

```bash
# Verify the YAML is valid
python -c "import yaml; yaml.safe_load(open('.pre-commit-config.yaml'))" && echo "YAML OK"

# If pre-commit is installed, run a dry run
command -v pre-commit && pre-commit run --all-files --hook-stage manual 2>&1 | head -40
```

The hooks should produce ZERO failures after Plans 01-04 ship (i.e., they're regression detectors, not bug surfaces).

Atomic commit suggested:
- `feat(1061-06): SEC-GUARD-01 — pre-commit grep guardrails for SSRF + visibility`
  </action>
  <verify>
    <automated>test -f .pre-commit-config.yaml && python -c "import yaml; yaml.safe_load(open('.pre-commit-config.yaml'))" && echo "YAML OK"</automated>
  </verify>
  <done>
- `.pre-commit-config.yaml` exists and is valid YAML.
- Rule 2 (SSRF) hook present and matches zero current findings (i.e., does NOT trip on the shipped codebase).
- Rule 1 (visibility) hook present ONLY if Task 1's FP count was acceptable; otherwise omitted with rationale in SUMMARY.
- If `pre-commit` is installed locally, `pre-commit run --all-files` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 4: docs-internal/audits/security-lessons.md — Phase 1061 closing entry</name>
  <files>docs-internal/audits/security-lessons.md</files>
  <behavior>
    - Prepend a new ledger entry (newest-at-top format) recording Phase 1061's closure with implementation references and the credential-rotation operator guidance.
    - The 2026-05-19 audit entry (lines 1-37 of current file) stays unchanged as the prior audit.
  </behavior>
  <action>
Edit `docs-internal/audits/security-lessons.md`. Insert a NEW entry at the top (between the file header at line 1-3 and the existing `## 2026-05-19 — Security audit: full repo` entry).

Insert this:

```markdown
## 2026-05-20 — Phase 1061: HIGH-severity remediation shipped

### Patterns now locked in AGENTS.md (see `AGENTS.md` §"Security pre-commit checklist")

- **Rule 1 — Visibility-filter coverage** — see implementation references below.
- **Rule 2 — SSRF redirect-revalidation** — pre-commit grep shipped in `.pre-commit-config.yaml`.
- **Rule 3 — Demo credentials are per-deploy** — `scripts/init-demo-env.sh` + extended `validate_demo_credentials_guard`.

### Implementation references (Phase 1061)

| Audit Finding | Plan | Files | Pattern |
|---------------|------|-------|---------|
| SEC-S01 (STAC visibility) | 1061-01 | `backend/app/standards/stac/router.py` | `_base_published_raster_query(user, user_roles)` applies `apply_visibility_filter` |
| SEC-S02 (dataset metadata IDOR) | 1061-02 | `backend/app/modules/catalog/datasets/api/router.py` | `check_dataset_access` in 3 mutation handlers; owner-or-admin gate on delete |
| SEC-S03 (column DDL IDOR) | 1061-02 | `backend/app/modules/catalog/layers/router.py` | `check_dataset_access` in 4 DDL handlers |
| SEC-S04 (SSRF redirect) | 1061-04 | `backend/app/modules/catalog/sources/security.py` + 3 callsites | `make_safe_client()` + `_revalidate_redirect` event hook; `GDAL_HTTP_FOLLOWLOCATION=NO` |
| SEC-S05 (related-datasets IDOR) | 1061-03 | `backend/app/modules/catalog/datasets/api/router_data.py` | `check_dataset_access_or_anonymous` BEFORE `get_related_datasets` |
| SEC-S06 (demo credentials) | 1061-05 | `scripts/init-demo-env.sh`, `backend/app/core/config.py` | Per-deploy random credentials; guard refuses literal committed values |
| SEC-S07 (MinIO defaults) | 1061-05 | `docker-compose.yml` | `${VAR:?required}` fail-closed expansion |
| SEC-GUARD-01 (this) | 1061-06 | `AGENTS.md`, `.pre-commit-config.yaml`, this file | Pattern pinning + grep guardrails |

### Operator guidance — demo credential rotation (SEC-S06 follow-up)

`scripts/init-demo-env.sh` is idempotent — re-run with `--force` to rotate.

Rotation cadence recommendation:
- Public-internet demo deployments: rotate weekly or on every redeploy. Schedule a cron job that runs `--force` and restarts the stack.
- Local dev clones using the demo overlay: rotate when the workstation changes hands.

Capture the admin password from `scripts/init-demo-env.sh` stdout — it is the only output, and the password is hashed at-rest in the DB (not recoverable from the file).

### Deferred to follow-up phases

- **SEC-FU-01..FU-10** → Phase 1063 (LOW follow-up tickets).
- **SEC-S08..S16** → Phase 1062 (MEDIUM remediation).
- **SEC-CTRL-01** → Phase 1064 (close gate: regression suite + tag).
- **Pre-resolve final URL before ogr2ogr spawn** — additional SSRF defense-in-depth; deferred to SEC-FU follow-up sweep.
- **Narrow `_load_self_record_and_embedding` to consume seed via visibility-filtered query** — eliminates reliance on caller-side gating; deferred to SEC-FU.
- **Aggregate metadata leakage in `get_collections` / `get_collection`** — bounded carve-out documented in Plan 1061-01 SUMMARY; narrow to per-user CollectionDataset visibility in SEC-FU.

---

```

The horizontal rule (`---`) separates this new entry from the existing 2026-05-19 entry. DO NOT modify the 2026-05-19 entry's content.

Atomic commit suggested:
- `docs(1061-06): SEC-GUARD-01 — security-lessons ledger entry for Phase 1061`
  </action>
  <verify>
    <automated>grep -c "2026-05-20 — Phase 1061" docs-internal/audits/security-lessons.md</automated>
  </verify>
  <done>
- New entry prepended (newest-at-top convention preserved).
- 8 implementation references table is correct (matches what shipped in Plans 01-05).
- Operator-guidance + deferred-items sections present.
- The original 2026-05-19 entry is unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future contributor → catalog API | Future PRs adding Record-derived endpoints may reintroduce SEC-S01/S02/S03/S05-shape regressions |
| future contributor → external URL fetch | Future PRs adding httpx clients may reintroduce SEC-S04-shape regressions |
| future contributor → demo deployment | Future PRs reintroducing committed demo credentials may reintroduce SEC-S06/S07 regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1061-21 | Repudiation of audit findings | AGENTS.md silence on the patterns | mitigate | Pin all 3 rules in AGENTS.md (Task 2) |
| T-1061-22 | Tampering (future regression) | New httpx client added outside `security.py` | mitigate | `.pre-commit-config.yaml` grep hook (Task 3, Rule 2) — verified-zero FP rate |
| T-1061-23 | Tampering (future regression) | New route file added without visibility gate | mitigate or accept | Pre-commit grep IF Task 1 finds low FP; otherwise manual review documented in AGENTS.md |
| T-1061-24 | Information disclosure | Future contributor commits `.env.demo` | mitigate | `.gitignore` (Plan 05) + AGENTS.md Rule 3 (Task 2) + boot-time guard (Plan 05) |
</threat_model>

<verification>
1. **AGENTS.md content gate**:
   ```bash
   grep -c "Security pre-commit checklist\|Visibility-filter coverage\|make_safe_client\|init-demo-env.sh" AGENTS.md
   # Expected: ≥ 4 (one match each)
   ```
2. **YAML validity**:
   ```bash
   python -c "import yaml; yaml.safe_load(open('.pre-commit-config.yaml'))" && echo "YAML OK"
   ```
3. **Pre-commit hook regression gate** (if pre-commit installed):
   ```bash
   pre-commit run --all-files 2>&1 | tail -20
   # Expected: 0 failures — hooks are regression detectors, not bug surfaces
   ```
4. **Cross-reference gate**:
   ```bash
   grep -c "2026-05-20 — Phase 1061" docs-internal/audits/security-lessons.md
   # Expected: 1
   ```
</verification>

<success_criteria>
- AGENTS.md "Security pre-commit checklist" section pins all 3 rules with concrete file/function references that match what shipped in Plans 01-05.
- `.pre-commit-config.yaml` exists with the Rule 2 SSRF grep (always shipped) and optionally the Rule 1 visibility grep (shipped if Task 1 FP count was acceptable).
- Both grep hooks pass cleanly against the post-Plan 01-05 codebase (zero false positives that aren't in the documented exempt list).
- `docs-internal/audits/security-lessons.md` has a new prepended entry recording Phase 1061's closure with the 8-row implementation reference table.
- The operator-guidance section in security-lessons.md surfaces the demo credential rotation procedure.
- Deferred SEC-FU items are inventoried for Phase 1063 pickup.
</success_criteria>

<output>
Create `.planning/phases/1061-security-audit-2026-05-19-remediation/1061-06-SUMMARY.md` when done. Include:
- Task 1 FP evaluation result: Candidate-1 / Candidate-3 FP counts + ship/no-ship decision for Rule 1 grep.
- File diff summary for AGENTS.md, `.pre-commit-config.yaml`, security-lessons.md.
- Confirmation that pre-commit hooks pass against the shipped codebase.
- Forward-pointer: Phase 1063 (LOW follow-ups) should consume the "Deferred" inventory in security-lessons.md.
- Forward-pointer: Phase 1064 (close gate) verifies the full e2e/sec-audit.spec.ts suite and re-runs `/sec-audit` for merge-gate flip.
</output>
</content>
</invoke>