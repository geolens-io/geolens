---
gsd_state_version: 1.0
milestone: v1011
milestone_name: Map Builder Polish & Bug Sweep
status: executing
stopped_at: Phase 1051 Plan 07 (UX-04) complete — commit 57d88d01 (Settings → Widgets enable/disable labels + availability note + i18n × 4 locales)
last_updated: "2026-05-18T02:50:00Z"
last_activity: 2026-05-18 -- Phase 1051 Plan 07 (UX-04) shipped at commit 57d88d01 — SettingsEditorScene Switch aria-label switched from composite `settings.toggleWidget` to state-specific `settings.enableWidget` / `settings.disableWidget` keys with `{{name}}` interpolation; new availability-note paragraph ("Controls whether each widget appears on the map.") added inside CollapsibleContent above the rows; 3 new i18n keys × 4 locales = 12 entries (en/de/es/fr); duplicate-controls audit found ZERO duplicates (MapToolbar Measure/Legend buttons are live-interaction tools, not availability toggles — distinct semantic role); 5 new regression tests in `SettingsEditorScene.widgets.test.tsx`; 2 existing tests in `SettingsEditorScene.test.tsx` updated to track the new label pattern (Rule 1 inline fix); 15/15 pass on the two settings scene test files; 0 tsc errors; i18n parity script passes; 7 files changed (1 created, 6 modified); 0 backend files touched
progress:
  total_phases: 6
  completed_phases: 0
  total_plans: 13
  completed_plans: 7
  percent: 0
---

# State

## Current Position

Phase: 1051 — map-builder-polish-bug-sweep
Plan: 1051-08 (RESP-01 NavigationControl collision) — next to start
Status: 7/13 plans complete (Plan 01 BUG-01 at 8c6de63, Plan 02 BUG-02 at eeeb8be8, Plan 03 BUG-03 at 80bddc14, Plan 04 UX-01 at 278e8933, Plan 05 UX-02 at 79b0c0c6 + a69d00ac, Plan 06 UX-03 at 0957cf6d, Plan 07 UX-04 at 57d88d01)
Last activity: 2026-05-18 -- Phase 1051 Plan 07 (UX-04) shipped at commit 57d88d01 — Settings → Widgets Switch labels now "Enable {name}" / "Disable {name}" + availability-note paragraph + 3 i18n keys × 4 locales; duplicate-controls audit clean (MapToolbar is live-interaction not duplicate); 5 new regression tests; orchestrator owes Playwright MCP re-verify of: (a) Switch aria-labels render correctly, (b) availability note visible, (c) toggle OFF removes widget from on-map render, (d) toggle ON restores, (e) de/es/fr translations render

## Project Reference

See: .planning/PROJECT.md (updated 2026-05-17 — opened milestone v1011 Map Builder Polish & Bug Sweep)

**Core value:** Users can find any dataset in the catalog in seconds — search, see it on a map, understand what it is, and get it out in the format they need.
**Current focus:** Phase 1051 — map-builder-polish-bug-sweep (hygiene close of 11 user-reported items + INV-01 + EMRG-01 + CTRL-01)

## Last Shipped Milestone

**Version:** v1010.2 Builder Smoke Carryover
**Shipped:** 2026-05-17
**Phases:** 1050 (1 phase, 6 plans, 5/5 SMOKE-08..12 reqs + 7 inline code-review fixes)
**Tag:** v1010.2 (local)
**Archive:** `.planning/milestones/v1010.2-ROADMAP.md`

## Accumulated Context

### Decisions

- **v1011 is a hygiene-close milestone.** Single phase (1051), 13 sequential plans (11 user-reported items + 1 emergent-findings triage + 1 CTRL-01 close gate). Mirrors v1009.1 Phase 1045, v1010.1 Phase 1049, and v1010.2 Phase 1050 per `feedback_hygiene_milestone_pattern.md`.
- **Phase numbering continues at 1051.** v1010.2 ended at Phase 1050.
- **Execution path:** `/gsd-autonomous` with Playwright MCP orchestrator-scoped verification against live `localhost:8080` stack. MCP verification steps marked `checkpoint:orchestrator` so the autonomous executor hands back per `project_demo_uat_resume.md` and the v1010.1 lesson (MCP is orchestrator-scoped, NOT executor-spawnable).
- **Plan granularity:** atomic — one plan per requirement (favoring clean revert/audit over plan consolidation). RESP-01/02/03 kept atomic even though they share a responsive-layout theme, because their fixes likely touch different files (`NavigationControl` positioning vs `MapCoordReadout` positioning vs flyout chrome).
- **Plan 06 (UX-03 draggable basemap) is the largest scope** — touches DnD setup, basemap-as-group drag semantics, saved-map normalizer, MapLibre layer-order loop, and round-trip persistence. May need a new field on the saved-map schema (minimum surface per Out-of-Scope row 6). All other plans are tight-scope symptom fixes.
- **Plan 11 (INV-01) is a disposition plan** — investigation-then-decision. Orchestrator decides REMOVE vs FIX based on grep + Playwright MCP findings. Disposition recorded in commit message + CHANGELOG.
- **Plan 12 (EMRG-01) opens with placeholder content** and is updated as findings land during plans 01-11. If zero emergent findings, FINDINGS.md is still authored with explicit "0 emergent" note.
- **Per-plan verification:** Each user-reported plan includes (a) Playwright MCP pre-fix repro, (b) implementation, (c) vitest regression (where applicable — pure-CSS responsive fixes use manual MCP verify only), (d) Playwright MCP post-fix verify. Atomic commits.
- **Single CTRL-01 close gate at Plan 13** — batched typecheck + vitest + e2e:smoke:builder + Playwright MCP re-verify of all 11 items + CHANGELOG `[Unreleased]` population. Per `feedback_review_findings_inline.md`: any code-review findings surface during gate get fixed inline before close, not deferred to v1011.1.
- **Phase 1051 Plan 01 (BUG-01)**: Fix at adapter.addLayers level + defense-in-depth syncVisibility calls. Root cause was non-sync re-add paths (`swapLayerOnMap`, raster re-add in `handleStyleConfigChange`) skipping syncVisibility, exposing an adapter contract gap where fill/line/circle/heatmap addLayers ignored input.visible (unlike raster/hillshade/symbol/cluster which did). Fixed at BOTH levels: adapters honor input.visible directly, AND every non-sync caller explicitly invokes syncVisibility after addLayers. Shipped at commit `8c6de63` with 5 new vitest regression cases (13/13 in target file, 889/889 in builder suite, 0 tsc errors).
- **Phase 1051 Plan 02 (BUG-02)**: `handleRemove` (use-builder-layers.ts:316-356) gains the optimistic state update + rollback pattern from `handleBulkDelete` (lines 580-661). Pre-fix code called `removePerLayerCompanions` and `removeLayerMutation.mutate` but never updated React state — the user clicked delete and the sidebar row stayed visible until full page reload, because the React-Query invalidation refetch was gated by the `!hasUnsavedChanges` resync useEffect at line 181-186. Fix: capture `previousLayers = layersRef.current` before mutation, `setLocalLayers((prev) => prev.filter(...).map(reindex))` optimistically, `savedLayerBaselineRef.current` sync inside onSuccess (CR-01 pattern), `setLocalLayers(previousLayers)` rollback inside onError. `useRemoveLayer` in use-maps.ts already invalidates — no change there. Shipped at commit `eeeb8be8` with 5 new vitest regression cases (5/5 in new `use-builder-layers.delete.test.ts`, 162/162 in builder hook suite, 0 tsc errors). Tasks 1 + 3 (Playwright MCP gates) deferred to orchestrator per `<lesson_from_wave_1>` (MCP is orchestrator-scoped).
- **Phase 1051 Plan 03 (BUG-03)**: Fix at TWO levels — `FolderGroupRow.tsx` editing useEffect now wraps `inputRef.current.focus()` + `inputRef.current.select()` in `requestAnimationFrame` so it runs AFTER Radix DropdownMenu's `restoreFocus` fires synchronously on menu close, AND the kebab Rename `DropdownMenuItem` `onSelect` no longer calls `_e.preventDefault()` so the menu closes cleanly. Defense in depth — a regression on either lever alone won't re-introduce the bug. Live MCP-driven repro proved the focus-race nature (focus transiently landed on the input then bounced back to the kebab trigger when the menu closed). Shipped at commit `80bddc14` with 7 new vitest regression cases (Test 19-25: autofocus via double-click rename pipeline + source-level assertions that preventDefault is gone and requestAnimationFrame is wired + Escape/Enter/blur no-regression guards). 25/25 in FolderGroupRow.test.tsx, 939/939 in builder suite, 0 tsc errors. Notable workaround: Radix DropdownMenu's jsdom behavior changes when onSelect no longer calls preventDefault — `fireEvent.pointerDown → fireEvent.click(menuitem)` no longer renders the resulting state change. Switched BUG-03 behavior tests to the double-click path (same `handleStartRename` pipeline, more reliable jsdom surface) and added source-level assertions (`FolderGroupRow.type.toString()` with comment-stripping) for the two contract changes. Tasks 1 + 3 (Playwright MCP gates) deferred to orchestrator per phase 1051 pattern. Pattern reusable: any Radix DropdownMenu item that mounts a new focus target should use rAF-deferred focus to outrun `restoreFocus`.
- **Phase 1051 Plan 04 (UX-01)**: Group-row expand caret on BasemapGroupRow + FolderGroupRow now meets the 24×24 px touch-target contract. Caret className swapped from `text-xs text-muted-foreground ...` to `flex items-center justify-center h-6 w-6 -mx-1 rounded text-muted-foreground ...` and child changed from Unicode `▸` to `<ChevronRight className="h-4 w-4" aria-hidden="true" />` (Lucide). Per sketch 002 A "A-strict": StackRow.tsx:174 grid template `grid-cols-[16px_14px_22px_22px_1fr_22px]` is LOCKED at 16px — `-mx-1` (negative 4px each side) extends the visual hit-box 24px without altering the parent grid. Preserves rotate-90 + aria-expanded + transition animation. Cell 4 type-icon `▸` glyph in FolderGroupRow:238 explicitly preserved (different surface; asserted by FolderGroupRow Test 1). Shipped at commit `278e8933` with 4 new vitest regression cases (Test 13-16: hit-target className tokens, Lucide SVG presence + h-4 w-4 size, rotate-90 animation, aria-expanded reflection). 44/44 in both group-row test files, 943/943 in builder suite, 0 tsc errors. jsdom limitation: `getBoundingClientRect` measurement deferred to orchestrator Playwright MCP per phase 1051 pattern (MCP is orchestrator-scoped). Tasks 1 + 3 (pre/post-fix MCP measurement) deferred. Pattern reusable: hit-target expansion within a locked grid column via `-mx-1` negative margin overflow.
- **Phase 1051 Plan 05 (UX-02)**: New `SublayerConfigIndicators.tsx` component renders 0–4 derived Lucide-icon badges (Labels/Filter/DataDriven/OpacityModified) based on live `MapLayerResponse` config — pure derivation, no internal state, returns null when no condition met (per UI-SPEC §UX-02 footnote). `UnifiedStackPanel.tsx` SublayerRow no longer renders a per-row Slider; Cell 6 grid column widened 60px → 76px (4 × 16px badge + 3 × 4px gap = exact fit); `Slider` import removed (only-use removed). Opacity editing for basemap sublayers remains canonical via the LayerEditorPanel flyout (BasemapSublayerEditorScene line 213-220 unchanged) — no duplicate affordance introduced. `BasemapSublayerInfo` only carries id/name/visible/opacity/kind so the indicator receives `layer={null}` from SublayerRow (rendering an empty strip per UI-SPEC footnote); plumbing the full layer through is a deferred enhancement once basemap sublayers gain user-editable filter/label. `onSublayerOpacityChange` prop kept in UnifiedStackPanelProps (optional, orphan-by-design) but no longer destructured inside the component — mirrors the existing `onOpacityChange` convention so MapBuilderPage line 1047 stays out of scope. 16 new i18n entries under `indicators.{labels,filter,dataDriven,opacityModified}` × en/de/es/fr — parity preserved (`test:i18n` 2/2). Shipped via 2 atomic commits: `79b0c0c6` (feat: component + tests + i18n) and `a69d00ac` (refactor: slider swap). 8/8 in SublayerConfigIndicators.test.tsx, 32/32 in UnifiedStackPanel.test.tsx, 751/751 in builder suite, 0 tsc errors. Tasks 1 + 4 (Playwright MCP pre/post-fix screenshots) deferred to orchestrator per phase 1051 pattern. TDD RED + GREEN landed in a single commit because no prior production surface existed for the new component (test fails at module-resolution before component exists; test+component pair authored together is still test-first by sequence). Pattern reusable: pure-derivation status badge with `useTranslation` + `t(key, { defaultValue })` + Lucide h-3 w-3 + sr-only label, container `flex items-center gap-1`, badge `inline-flex items-center justify-center h-4 w-4 rounded-sm bg-[var(--primary-50)] text-[var(--primary-600)]`.
- **Phase 1051 Plan 07 (UX-04)**: Map Settings → Widgets section now uses state-specific aria-labels ("Enable {{name}}" when off / "Disable {{name}}" when on) instead of the composite `{action} {name} widget` template — better screen-reader semantics + per-locale word-order grammar. Added availability-clarifying descriptive note paragraph ("Controls whether each widget appears on the map.") inside `CollapsibleContent` above the rows at `text-[11px] text-muted-foreground` so it stays subordinate to the 12px row labels. **Duplicate-controls audit found ZERO duplicates** — grepped all `useWidgetStore.toggle` consumers (`SettingsEditorScene`, `MapToolbar`, `WidgetPanel.close`, `BuilderMap.subscribe`, `MeasurementWidget.close`). `SettingsEditorScene` is the single source of truth for availability (which widgets *can* appear); `MapToolbar`'s top-of-canvas Measure / Legend buttons are live-interaction tools (which widget is *currently* engaged) — distinct semantic role, not a duplicate. No removal performed. 3 new i18n keys × 4 locales = 12 entries (en/de/es/fr) under `settings.{enableWidget,disableWidget,widgetsAvailabilityNote}`; old `settings.toggleWidget` key kept in locale files (no longer code-referenced — flagged in SUMMARY deferred list for future i18n cleanup). 5 new regression tests in `SettingsEditorScene.widgets.test.tsx` (OFF label, ON label, callback id, note rendering, no-duplicate-Switch invariant); 2 existing tests in `SettingsEditorScene.test.tsx` updated to track the new label pattern (Rule 1 inline fix — same atomic commit). Shipped at commit `57d88d01` with 15/15 tests pass on the two settings scene test files, 0 tsc errors, i18n parity script clean. 7 files changed (1 created, 6 modified), 0 backend files touched. Tasks 1 + 3 (Playwright MCP pre/post-fix verify) deferred to orchestrator per phase 1051 pattern. **Pattern reusable:** state-specific i18n keys (enableX / disableX with {{name}} interpolation) over composite `{action} {name}` templates — each locale can word-order naturally (e.g., German "{{name}} aktivieren" vs English "Enable {{name}}"), and screen readers announce them as a single phrase without an interpolated 'verb noun widget' construction.
- **Phase 1051 Plan 06 (UX-03)**: Basemap row is now draggable in the unified layer stack with two-position toggle semantics ('top' renders basemap above data for 3D maps; 'bottom' is the legacy default below data). `BasemapGroupRowWrapper` (UnifiedStackPanel.tsx:244-287) lifted from `useDroppable` → `useSortable` mirroring `FolderGroupRowWrapper` (lines 312-389) verbatim — same destructure shape (`attributes`, `listeners`, `setActivatorNodeRef`, `transform`, `transition`, `isDragging`, `isOver`); `data: { source: 'stack', kind: 'basemap-group' }` preserved so existing catalog basemap-swap drops at `MapBuilderPage:623` continue to work via `over.id === basemapGroup.id`. `BasemapGroupRow.tsx:113` replaces the hidden `<span>` with a real `<button>` + `<GripVertical h-3.5 w-3.5 />` mirroring `FolderGroupRow.tsx:196-210`; when `isMultiSelectionActive=true`, listeners are conditionally NOT spread AND className flips to `cursor-not-allowed opacity-20` (defense-in-depth gate per POL-11). Persistence: new optional `MapBasemapConfig.basemap_position?: 'top' | 'bottom'` field — **jsonb-additive, zero backend migration** (per Out-of-Scope row 6 + PATTERNS.md Plan 06). Round-trip rides through the existing wholesale `basemap_config` serializer in `use-builder-save.ts:439` and the existing `_setBasemapConfigRaw(mapData.basemap_config)` load path in `use-builder-layers.ts:165`. Legacy maps load with `undefined` and default to 'bottom' everywhere via `basemapConfig?.basemap_position ?? 'bottom'`. New `reorderBasemapAboveData(map, position, sourcePrefix)` exported map-sync helper performs the inversion: when position='top', walks `map.getStyle().layers` and `moveLayer(id)` (no beforeId) for every non-data style layer (source NOT starting with the data prefix); strict no-op for 'bottom' or `undefined` (preserves the legacy data-above-basemap pipeline). Wired into THREE BuilderMap call sites — basemap-config effect (line 765), `onStyleLoad` handler, and `runSync` — plus inside `syncLayersToMap`'s reorder branch via the new `basemapPosition` option in `SyncOptions`; `orderKey` includes `bp:{position}` so dragging top↔bottom invalidates the no-change-skip. `MapBuilderPage.handleDragEnd` gained a basemap-drag branch BEFORE the standard `arrayMove`: detects `active.id === basemapGroup.id`, toggles position via `setBasemapConfig` (auto-dirty WR-02), announces via new `a11y.basemapPositionChanged` key. UnifiedStackPanel render-order branches on `basemapPosition` prop: dock row renders BEFORE the layers map when 'top', AFTER when 'bottom'; `sortableIds` insertion position mirrors the same so @dnd-kit's items-array ↔ DOM-order contract holds. Shipped at commit `0957cf6d` with 14 new vitest regression cases in `UnifiedStackPanel.basemap-drag.test.tsx` (drag-handle DOM + GripVertical SVG + multi-select gate; DOM order on top/bottom/default; reorderBasemapAboveData across position values, sourcePrefix variants, stale-layer race; MapBasemapConfig TypeScript shape + legacy-omission compat). 76/76 test files pass, 967/967 tests pass, 0 tsc errors, i18n parity 2/2 (8 new translation entries — 2 keys × 4 locales). 13 frontend files modified, 0 backend files touched (verified via `git status`). Tasks 1 + 4 (Playwright MCP pre/post-fix capture + reload round-trip + map.getStyle().layers inspection) deferred to orchestrator per phase 1051 pattern. **Patterns established (5 new):** (A) jsonb-additive persistence over schema migration when storage is opaque jsonb; (B) useDroppable→useSortable lift by copying the sibling sortable wrapper's destructure shape verbatim, preserving the `data` option for legacy drop-target semantics; (C) render-order branching in JSX on positional state when a participant has exactly N stations (here 2 — basemap top vs bottom); (D) last-pass override (reorderBasemapAboveData runs LAST after the standard reorder pipeline so the inversion is the final write); (E) orderKey must include any new positional axis (`bp:{position}`) or the no-change-skip silently no-ops subsequent renders.

### Out of Scope (per REQUIREMENTS.md)

- New Map Builder features beyond the 11 polish items (no renderer expansion, AI capability, time-series, collaboration)
- Backend/API changes outside what's strictly required to fix the 11 items (scope minimum API surface for any backend touch)
- Refactoring `MapBuilderPage`, builder hooks, or layer-adapters except where directly required (no v1010-style code-quality audit)
- Mobile-phone optimization (<800 px portrait) — v1011 small-screen targets tablet and narrow desktop
- New i18n keys beyond what fixes/conversions strictly require
- New saved-map schema fields beyond what UX-03 strictly requires

### Pending Todos

None at roadmap creation. EMRG-01 may produce new pending todos for defer-disposition findings.

### Blockers/Concerns

None at roadmap creation. All 11 user-reported items have either a clear repro URL (BUG-01) or a clear symptom to verify against Playwright MCP. INV-01 has an explicit disposition flow. EMRG-01 has a written-down triage protocol.

## Session Continuity

Last session: 2026-05-18T02:50:00Z
Stopped at: Phase 1051 Plan 07 (UX-04) complete — commit 57d88d01 (Settings → Widgets enable/disable labels + availability note + i18n × 4 locales)
Resume file: .planning/phases/1051-map-builder-polish-bug-sweep/1051-08-resp-sidebar-zoom-collision-PLAN.md (next-plan file under same phase)

Session resumed: 2026-05-18 — Wave 7 (UX-04) shipped; orchestrator owes Playwright MCP re-verify of: (a) Switch aria-labels render "Enable {name}" / "Disable {name}" correctly; (b) availability-note paragraph visible; (c) toggle widget OFF in Settings → confirm it disappears from on-map render; (d) toggle ON → confirm restoration; (e) MapToolbar Measure/Legend buttons still toggle live interaction (no regression); (f) switch language to de/es/fr → translations render (no untranslated `settings.enableWidget` raw key); plus backlog re-verify of Wave 6 items (basemap drag round-trip). Then advance to Wave 8 (RESP-01 NavigationControl collision).

## Operator Next Steps

- Begin Phase 1051 via `/gsd:plan-phase 1051` to author per-plan implementation specs, OR
- Begin Phase 1051 via `/gsd-autonomous --from 1051` for end-to-end execution with orchestrator-scoped Playwright MCP verification at each `checkpoint:orchestrator` step
- Ensure `docker compose up -d --build` stack is healthy before starting (5/5 services); Playwright MCP needs `http://localhost:8080` reachable
