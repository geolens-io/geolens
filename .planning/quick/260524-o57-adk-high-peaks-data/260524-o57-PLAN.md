---
quick: 260524-o57
type: operational
autonomous: false
files_modified:
  - scripts/marketing-data/adk-high-peaks/fetch_dem.py
  - scripts/marketing-data/adk-high-peaks/fetch_vectors.py
  - scripts/marketing-data/adk-high-peaks/build_dem_cog.sh
  - scripts/marketing-data/adk-high-peaks/clip_vectors.sh
  - scripts/marketing-data/adk-high-peaks/ingest_manifest.yaml
  - scripts/marketing-data/adk-high-peaks/README.md
  - scripts/marketing-data/adk-high-peaks/peaks_46.geojson
  - scripts/marketing-data/adk-high-peaks/compose_marketing_maps.py
  - .gitignore
  - .planning/quick/260524-o57-adk-high-peaks-data/260524-o57-SUMMARY.md
  - .planning/quick/260524-o57-adk-high-peaks-data/260524-o57-API-ISSUES.md
must_haves:
  truths:
    - "User can see the High Peaks AOI in the GeoLens catalog: at least 1 DEM raster + 1 aerial raster + 3 vector overlays ingested and visible at http://localhost:8080/datasets"
    - "User can open at least 1 saved map at http://localhost:8080/maps/{id} that renders the DEM hillshade + aerial + vector overlays composited and styled for marketing screenshots"
    - "All raw downloads (DEMs, orthos) live in `.scratch/adk-data/` and are NOT committed to git"
    - "All scripts, manifest YAML, the curated 46er GeoJSON, and the marketing-map composition script ARE committed to `scripts/marketing-data/adk-high-peaks/` so the work is reproducible"
    - "Final SUMMARY.md captures: AOI bbox used, every downloaded source + size + CRS, every COG produced, every dataset ingested with its dataset_id, every saved map with its map_id + URL, every error or human-interactive step required, and a clear list of follow-ups (e.g. 46er coords if upstream messy, aerial-portal scriptability verdict)"
    - "Final API-ISSUES.md is a dogfooding report on the GeoLens APIs themselves: every endpoint touched, request/response shape surprises, undocumented required fields, confusing error messages, retries + their triggers, slow endpoints (p95 timing), workarounds applied, and a prioritized list of API improvements for a future GeoLens phase to address. Empty sections are explicitly marked 'no issues observed' rather than omitted."
  artifacts:
    - path: ".planning/quick/260524-o57-adk-high-peaks-data/260524-o57-SUMMARY.md"
      provides: "Reproducible operational report with sections: AOI, Sources, COGs Built, Datasets Ingested, Saved Maps, Errors, Human Steps, Follow-ups"
      min_lines: 80
    - path: ".planning/quick/260524-o57-adk-high-peaks-data/260524-o57-API-ISSUES.md"
      provides: "Dogfooding report on GeoLens APIs. Sections: (1) Endpoints Touched (table of every endpoint + method + count + p50/p95 ms), (2) Auth Friction (login -> api-key bootstrap -> usage -> cleanup pain points), (3) Ingest Friction (upload/preview/commit/job-poll surprises: required-but-undocumented fields, confusing status semantics, raster vs vector branching gotchas, timeout behaviors), (4) Map Composition Friction (POST /api/maps + PATCH /api/maps/{id}/layers surprises: MapLayerInput required fields, paint/layout/style_config validation, terrain_config gotchas, popup_config quirks), (5) Catalog & Search Friction (listing/filtering/dataset lookup pain), (6) Error Message Quality (verbatim confusing errors + what they actually meant), (7) Documentation Gaps (what required reading source vs. being documented at /docs or in OpenAPI), (8) Performance Observations (slow endpoints, polling vs streaming opportunities), (9) Recommended API Improvements (prioritized list ready to become a future GeoLens phase)."
      min_lines: 60
    - path: "scripts/marketing-data/adk-high-peaks/README.md"
      provides: "Step-by-step reproduction guide so the user (or a future contributor) can rerun the whole pipeline end-to-end"
      min_lines: 40
    - path: "scripts/marketing-data/adk-high-peaks/ingest_manifest.yaml"
      provides: "geolens.yaml manifest applied via POST /api/ingest/manifest/apply for the local-file vector + COG ingest path"
    - path: "scripts/marketing-data/adk-high-peaks/peaks_46.geojson"
      provides: "Curated 46er peaks point dataset (or partial list with follow-up in SUMMARY if upstream not authoritative)"
  key_links:
    - from: "TNM 3DEP 1m DEM tiles"
      to: "Single mosaicked COG ingested as a raster dataset"
      via: "fetch_dem.py -> gdalbuildvrt -> gdal_translate / app.processing.raster.cog.convert_to_cog -> POST /api/ingest/upload + /api/ingest/commit"
      pattern: "raster_dataset"
    - from: "data.gis.ny.gov ArcGIS Hub (APA Blue Line, NYSDEC Hiking Trails)"
      to: "GeoJSON vectors clipped to AOI ingested as vector datasets"
      via: "fetch_vectors.py downloads -> ogr2ogr -clipsrc -> upload+commit (or manifest apply)"
      pattern: "vector"
    - from: "Ingested raster + vector datasets"
      to: "1-2 saved maps composed via POST /api/maps/ + PATCH /api/maps/{id}/layers"
      via: "compose_marketing_maps.py — builds the MapLayerInput JSON list, posts, prints the map URLs"
      pattern: "/api/maps/"
---

<objective>
Acquire DEM + aerial imagery + key vector overlays for the Adirondack High Peaks region, ingest end-to-end into the running local GeoLens stack, and compose at least one marketing-grade saved map. Every step that the user must do manually (interactive UI clicks, account sign-ups, etc.) is called out explicitly so the executor can pause + ask.

Purpose: Produce real, screenshot-worthy GeoLens content (DEM hillshade + aerials + trails + Blue Line + 46er peaks) so marketing screenshots, embeds, and demo videos have a strong, geographically resonant story to anchor on.

Output: Reproducible scripts under `scripts/marketing-data/adk-high-peaks/` + ingested data visible in the catalog + at least 1 saved map + a SUMMARY.md operational report. Raw downloads stay in `.scratch/adk-data/` (gitignored).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@docker-compose.yml
@scripts/README.md
@scripts/seed-natural-earth.py
@backend/app/processing/ingest/manifest_schemas.py
@backend/app/processing/ingest/manifest_sources.py
@backend/app/processing/ingest/manifest_router.py
@examples/manifests/first-catalog/geolens.yaml
@examples/manifests/public-cog/geolens.yaml
@backend/app/modules/catalog/maps/schemas.py

# AOI bbox (locked decision):
#   N=44.32, S=44.08, E=-73.85, W=-74.05  (WGS84 / EPSG:4326)
#   = roughly 12 mi E-W x 16 mi N-S, covering Lake Placid village, Mirror Lake,
#     Heart Lake, ADK Loj, Avalanche Lake, Mt. Marcy, Algonquin Peak.
#   GDAL/ogr2ogr/curl-bbox arg order: -te <minx> <miny> <maxx> <maxy>
#     ->  -te -74.05 44.08 -73.85 44.32
#   TNM API bbox order: <minx>,<miny>,<maxx>,<maxy>
#     ->  bbox=-74.05,44.08,-73.85,44.32
#
# Stack reachable at:
#   http://localhost:8080         (frontend + /api/* proxy — preferred)
#   http://localhost:8001         (direct api — rarely needed)
#   http://localhost:8001/docs    (OpenAPI)
#   psql -h localhost -p 5434 -U <POSTGRES_USER>   (if Postgres needed directly)
#
# Auth: bootstrap an API key the same way scripts/seed-natural-earth.py does:
#   1. POST /api/auth/login (form-encoded username/password, admin/admin in dev)
#   2. POST /api/auth/api-keys/ { name: "marketing-data-adk" }  -> returns plaintext key
#   3. Use header `X-Api-Key: <key>` on every subsequent request
#   4. On exit, DELETE /api/auth/api-keys/{key_id} (best-effort)
#
# Verified data endpoints (planner has already web-checked these):
#
#   * TNM 3DEP 1m DEM (fully programmatic, public domain):
#       https://tnmaccess.nationalmap.gov/api/v1/products
#         ?datasets=Digital Elevation Model (DEM) 1 meter
#         &bbox=-74.05,44.08,-73.85,44.32
#         &prodFormats=GeoTIFF
#         &max=50&outputFormat=JSON
#     Returns ~13 tiles, ~3.3 GB combined (each 10km x 10km, 20-350 MB).
#     Direct download URLs are on s3://prd-tnm/StagedProducts/Elevation/1m/Projects/...
#     Two NY projects overlap the AOI: `2014_New_York_Clinton_Essex_Lake_Champlain_QL2_LiDAR`
#     and `NY_NH_Gaps_D24`. Prefer Clinton_Essex_Lake_Champlain (denser native LiDAR);
#     fall back to NY_NH_Gaps_D24 only for tiles missing from the primary project.
#
#   * NYS Orthos (https://orthos.dhses.ny.gov/):
#       Interactive portal. Web-research said WMS/WMTS exist but the portal landing
#       page is 403 for unauthenticated bots — programmatic discovery is BLOCKED.
#       Plan must treat orthoimagery as a HUMAN-INTERACTIVE step: the executor
#       must produce explicit click-by-click instructions for the user and PAUSE.
#       Fallback if portal scripting is infeasible: USGS NAIP 0.6m via TNM
#         dataset="USDA National Agriculture Imagery Program (NAIP)" — same TNM
#         API call, same s3://prd-tnm download pattern, NAIP is public domain.
#       NAIP is 0.6m for 2023+ NY which is in the same quality ballpark as the
#       NYS 1ft Essex County orthos. Plan must surface this fallback choice as
#       a checkpoint decision before committing 6+ GB of downloads.
#
#   * data.gis.ny.gov ArcGIS Hub vectors:
#       APA Blue Line: search hub for "apa blue line" -> ArcGIS Hub item ID -> 
#         download as GeoJSON (whole-state — ~MB-scale, no need to clip the hub
#         download, just clip with ogr2ogr after).
#       NYSDEC Hiking Trails: https://data.gis.ny.gov/maps/ab5d56644a404b41bac8d72f32017e4e
#         exposes a `download` button + REST FeatureServer; standard ArcGIS Hub
#         download pattern is:
#           https://hub.arcgis.com/datasets/{itemid}_0.geojson
#         or, for the FeatureServer JSON dump:
#           https://services1.arcgis.com/.../FeatureServer/0/query?f=geojson&where=1%3D1&outFields=*
#         Verify the exact item IDs at fetch time — they can change.
#
#   * NHD hydrography (optional, deferrable):
#       USGS WBD/NHD HU8 04150301 covers the High Peaks. Same TNM API pattern
#       (dataset="National Hydrography Dataset (NHD) Best Resolution") -> a
#       File Geodatabase (.gdb) zip. Defer to the END of Task 2; ship without
#       it if it would push downloads over disk budget.
#
#   * 46er peaks: NO canonical machine-readable upstream confirmed. Plan must
#     curate `peaks_46.geojson` manually for the ~10-12 peaks INSIDE the bbox
#     (Marcy, Algonquin, Iroquois, Wright, Colden, Cliff, Redfield, Skylight,
#     Gray, Marshall, Phelps, Tabletop, Street, Nye) using authoritative
#     coordinates from the ADK 46ers list. If even that is messy, ship with
#     just the headline peaks (Marcy + Algonquin + Wright + Phelps) and log
#     a follow-up in SUMMARY.
#
# GeoLens ingestion paths (use these — don't reinvent):
#
#   Manifest path (preferred for batch):
#     POST /api/ingest/manifest/apply  body=ManifestApplyRequest
#     Schema at backend/app/processing/ingest/manifest_schemas.py
#     Source types supported: "vector", "raster_cog", "vrt"
#     URIs can be local relative paths (resolved under settings.upload_staging_dir),
#     http(s):// (SSRF-validated), or s3://.
#     LOCAL PATH GOTCHA: the upload_staging_dir is INSIDE the api container at
#     /app/storage/staging (per docker-compose volume mount). To ingest local
#     files you must either:
#       (a) docker cp them into the api container's staging dir, then call
#           manifest apply with a relative path, OR
#       (b) take the HTTP-upload path via POST /api/ingest/upload (multipart) +
#           POST /api/ingest/commit (sets file_type=raster/vector + metadata).
#     For COGs >100MB the upload path is simpler and avoids the docker-cp dance.
#     For the few small vector GeoJSONs the manifest apply with local paths via
#     docker cp is fine. Plan defaults to upload+commit for everything to keep
#     it uniform (one code path, easier to debug).
#
#   Direct upload path (used by scripts/seed-natural-earth.py):
#     POST /api/ingest/upload  (multipart File) -> returns job_id
#     POST /api/ingest/preview/{job_id}        -> raster vs vector branch
#     POST /api/ingest/commit/{job_id} body=VectorCommitRequest|RasterCommitRequest
#     GET  /api/ingest/jobs/{job_id}            -> poll until status="completed"
#     Allowed extensions: .geojson .json .csv .xlsx .gpkg .zip .tif .tiff .vrt
#     (canonical: backend/app/processing/ingest/router.py:_FALLBACK_ALLOWED_EXTENSIONS)
#     Authoritative source list in app/core/persistent_config.py UPLOAD_ALLOWED_EXTENSIONS.
#
#   Map composition:
#     POST /api/maps/  body=MapCreate {name, description, notes?, terrain_config?, basemap_config?}
#       -> returns {id, ...}
#     PATCH /api/maps/{id}/layers body=MapLayerDiffRequest
#       {added: [MapLayerInput, ...], updated: [], removed: [], order: null}
#     MapLayerInput fields (must-have):
#       dataset_id, sort_order, visible=true, opacity, layer_type
#         (one of "vector_geolens"|"raster_geolens"|"geojson"),
#         paint, layout, style_config (builder UI hints),
#         display_name, popup_config, show_in_legend.
#     For DEM-as-hillshade: layer_type="raster_geolens" + style_config.builder
#       with render_mode="hillshade" (per v1008 builder pattern); for the aerial
#       use render_mode="image". For 3D terrain on the map (separate from a
#       layer), set MapCreate.terrain_config.
#
# Existing helpers to reuse (do NOT rewrite):
#   - app/processing/raster/cog.py convert_to_cog() — gdal_translate-based COG
#     conversion. Server-side helper; not callable from outside the container.
#     For pre-ingest COG building locally use plain `gdal_translate -of COG`
#     (GDAL 3.1+; check `gdalinfo --version` in `.scratch/`). If the host
#     GDAL is older, build the COG by uploading the raw GeoTIFF + VRT to
#     /api/ingest/upload and letting the server-side ingest pipeline run
#     convert_to_cog. Either path is fine.
#   - scripts/seed-natural-earth.py — gold-standard reference for: bootstrap
#     API key + login + upload + preview + commit + poll loop with retries.
#     COPY THIS PATTERN, don't reinvent.

# Disk + time budget callouts:
#   * DEM raw downloads: ~3.3 GB across 13 tiles (varies — Clinton_Essex_Lake_Champlain
#     tiles are ~20-40 MB each, NY_NH_Gaps tiles are ~300 MB each).
#     If using the larger NY_NH_Gaps project for any tile, that one tile could
#     dominate the download budget — script must report per-tile size before
#     starting and let the user confirm if total > 5 GB.
#   * NAIP orthos: ~3-6 GB for the AOI (NAIP tiles are 6000x7400 county quarter
#     quads, ~150-400 MB each; expect 4-8 tiles in the bbox).
#     Total raw downloads ~6-10 GB. Mosaicked COGs after compression typically
#     0.5-1.5 GB each. Pre-flight: require `df -h .` show >= 25 GB free in
#     `.scratch/adk-data/` working dir before any download.
#   * Time: download ~10-30 min over a typical home connection;
#     gdalbuildvrt + gdal_translate -of COG ~5-15 min per raster mosaic on a
#     local laptop; ingest preview+commit ~1-5 min per raster (Procrastinate
#     queue + COG validation + thumbnail generation). Plan to spend ~1-2 hours
#     elapsed end-to-end.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0: Confirm aerial source (NYS orthos UI vs NAIP fallback) + disk budget</name>
  <decision>Which aerial source to use, and confirm disk budget</decision>
  <context>
The NYS Statewide Orthos portal (https://orthos.dhses.ny.gov/) is the higher-quality option (1-foot resolution, Essex County 2023+) but the planner could NOT confirm a documented programmatic download API — the portal landing page returns 403 to unauthenticated bots. Two paths exist:

1. **NYS Orthos via portal UI** — better resolution (1 ft / ~0.3 m), but the user must click through the portal to draw the rectangle + initiate the download (which delivers a zipped folder of tiles via email link). After download the executor takes over: unzip, gdalbuildvrt, COG conversion, ingest. Cost: ~20-40 min user time + a working email address.

2. **USGS NAIP 0.6m via TNM API fallback** — same TNM API + s3://prd-tnm pattern used for DEMs. Fully scriptable. Slightly lower resolution but still excellent at marketing zooms z13-z17. Public domain.

Path 1 is the "right" answer for marketing fidelity. Path 2 is the "ship-now" answer.

Also confirm pre-flight: `df -h /Users/ishiland/Code/geolens` should show >= 25 GB free. Currently 233 GiB available (per planner check) — plenty of headroom.
  </context>
  <options>
    <option id="orthos">
      <name>NYS Orthos (1ft, interactive)</name>
      <pros>Highest fidelity for marketing screenshots; matches the locked locked-scope-decision intent ("1-foot orthos starting point")</pros>
      <cons>User must click through orthos.dhses.ny.gov UI; ~20-40 min lost to UI + email download; executor blocks waiting for user file</cons>
    </option>
    <option id="naip">
      <name>NAIP 0.6m via TNM (scripted)</name>
      <pros>Zero human intervention; same TNM/s3://prd-tnm pattern already used for DEMs; consistent script style</pros>
      <cons>0.6m is "good enough" but not as crisp as 1ft at z17+; documented in SUMMARY as a quality trade-off</cons>
    </option>
    <option id="both">
      <name>Ship NAIP now, file Orthos as a follow-up</name>
      <pros>Marketing maps live today; Orthos can replace NAIP in a future polish task with no code changes (same MapLayer dataset swap)</pros>
      <cons>Two ingest passes if user ever does follow up; minor catalog churn</cons>
    </option>
  </options>
  <resume-signal>Select: orthos, naip, or both</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Build the download + COG pipeline scripts and produce the working data</name>
  <files>
scripts/marketing-data/adk-high-peaks/fetch_dem.py
scripts/marketing-data/adk-high-peaks/fetch_vectors.py
scripts/marketing-data/adk-high-peaks/build_dem_cog.sh
scripts/marketing-data/adk-high-peaks/clip_vectors.sh
scripts/marketing-data/adk-high-peaks/peaks_46.geojson
scripts/marketing-data/adk-high-peaks/README.md
.gitignore
.scratch/adk-data/  (working dir — gitignored)
  </files>
  <action>
Build the reproducible download + preprocess pipeline. Commit the scripts + curated 46er GeoJSON + README. Do NOT commit the raw downloads or mosaicked COGs.

Step 1 — Pre-flight + scratch dir + gitignore:
  - Run `df -h /Users/ishiland/Code/geolens` and abort with a clear error if free space < 25 GB. Surface in SUMMARY.
  - Run `gdalinfo --version` and `python3 -c "import osgeo.gdal; print(osgeo.gdal.__version__)"` (or `which gdal_translate gdalbuildvrt ogr2ogr`) and verify GDAL >= 3.1 (COG driver requirement). If missing, surface and ask user to brew install gdal (do not auto-install).
  - Create `.scratch/adk-data/{dem,aerial,vectors,cogs}/` directories.
  - Append to `.gitignore` (use Edit, don't rewrite the whole file): add `.scratch/` block under a new `# Marketing data working dir (raw downloads, mosaicked COGs)` header. Idempotent — grep first.
  - Verify the docker stack is healthy with `docker compose ps --format json | jq -r '.[] | select(.Health!="healthy") | .Service'` and abort if anything is unhealthy. SUMMARY: record the docker compose ps table.

Step 2 — fetch_dem.py (Python, idempotent):
  - Hit TNM API: GET https://tnmaccess.nationalmap.gov/api/v1/products?datasets=Digital%20Elevation%20Model%20(DEM)%201%20meter&bbox=-74.05,44.08,-73.85,44.32&prodFormats=GeoTIFF&max=50&outputFormat=JSON
  - Parse JSON; for each tile prefer the `2014_New_York_Clinton_Essex_Lake_Champlain_QL2_LiDAR` project, fall back to `NY_NH_Gaps_D24` only when no Clinton_Essex tile covers a given grid cell.
  - PRINT a summary table: filename, project, size_MB, downloadURL.
  - Sum total bytes; if > 5 GB, print and prompt user (interactive y/N) before downloading. Default to N on EOF.
  - Download all tiles into `.scratch/adk-data/dem/` with retry pattern (3 attempts, 2^attempt s backoff — mirror seed-natural-earth.py's download_dataset). Skip files that already exist with size > 0.
  - On exit print: `Downloaded N tiles, total X.X GB`.
  - Reuse the SSRF-safe download pattern? Not necessary here — these are read-only s3://prd-tnm.s3.amazonaws.com URLs, the public TNM stage bucket. Just use plain httpx with allow_redirects=True and a User-Agent header.

Step 3 — build_dem_cog.sh (bash, idempotent):
  - Skip if `.scratch/adk-data/cogs/adk_high_peaks_dem_1m.tif` already exists.
  - `cd .scratch/adk-data/dem/`
  - `gdalbuildvrt -te -74.05 44.08 -73.85 44.32 -te_srs EPSG:4326 ../cogs/adk_high_peaks_dem.vrt USGS_1M_*.tif`
  - Convert VRT to a single COG (re-project to EPSG:3857 web mercator for downstream tiling consistency; this matches how Titiler serves rasters):
    `gdalwarp -t_srs EPSG:3857 -of COG -co COMPRESS=DEFLATE -co BLOCKSIZE=512 -co OVERVIEWS=AUTO -co RESAMPLING=bilinear ../cogs/adk_high_peaks_dem.vrt ../cogs/adk_high_peaks_dem_1m.tif`
  - Validate with `gdalinfo` and grep for `LAYOUT=COG` (or use rio-cogeo if available).
  - Output file is `adk_high_peaks_dem_1m.tif` (~0.5-1.5 GB expected after compression).

Step 4 — aerial pipeline (branch on Task 0 decision):
  - If `naip`: extend fetch_dem.py with `--dataset naip` flag that hits the same TNM API with `datasets=USDA National Agriculture Imagery Program (NAIP)`. Download tiles to `.scratch/adk-data/aerial/`. Mosaic + reproject to EPSG:3857 + COG to `.scratch/adk-data/cogs/adk_high_peaks_naip_3857.tif`. Same script shape as build_dem_cog.sh — extend it with an `--aerial` mode rather than a second script.
  - If `orthos`: produce a clear README block titled "ORTHOS HUMAN-INTERACTIVE STEP" with click-by-click instructions: (1) visit https://orthos.dhses.ny.gov/, (2) zoom to Lake Placid, (3) draw rectangle covering the AOI bbox, (4) select latest year + 1-ft resolution, (5) submit + provide email, (6) when zip arrives, drop it at `.scratch/adk-data/aerial/orthos.zip`. After the file lands, the executor unzips, mosaics with gdalbuildvrt + builds the COG identically.
  - If `both`: do `naip` now; write the Orthos block in README under a "Follow-up: Replace NAIP with NYS 1ft Orthos" section.

Step 5 — fetch_vectors.py + clip_vectors.sh:
  - APA Blue Line: search the ArcGIS Hub for the canonical item. The hub URL pattern is `https://hub.arcgis.com/datasets/{item-id}.geojson` once you have the item ID. The plan-time research did not pin the exact item ID — fetch_vectors.py must (a) hit `https://apa.ny.gov/planning/gis/downloads.html`, scrape the Blue Line download link, OR (b) hit `https://data.gis.ny.gov/api/feed/dcat-us/1.1.json` and filter by `title contains "Blue Line"`. If neither works, fall back to using the FeatureServer query: `https://services1.arcgis.com/{org}/arcgis/rest/services/.../FeatureServer/0/query?where=1%3D1&outFields=*&f=geojson`. Save as `.scratch/adk-data/vectors/apa_blue_line.geojson`. Surface the URL chosen in SUMMARY.
  - NYSDEC Hiking Trails: item ID from URL https://data.gis.ny.gov/maps/ab5d56644a404b41bac8d72f32017e4e — try `https://hub.arcgis.com/datasets/ab5d56644a404b41bac8d72f32017e4e_0.geojson`. Save as `.scratch/adk-data/vectors/nysdec_hiking_trails.geojson`.
  - APA Land Classification: same hub-search pattern; save as `.scratch/adk-data/vectors/apa_land_classification.geojson`.
  - For each, after download:
    `ogr2ogr -t_srs EPSG:4326 -clipsrc -74.05 44.08 -73.85 44.32 .scratch/adk-data/vectors/{name}_aoi.geojson .scratch/adk-data/vectors/{name}.geojson`
  - The clipped *_aoi.geojson files are what we will ingest (much smaller, scoped to the AOI).

Step 6 — peaks_46.geojson (curated):
  - Hand-author a small FeatureCollection of points for the High Peaks inside the bbox. Authoritative coords from the ADK 46ers list (Marcy 44.1126/-73.9237, Algonquin 44.1432/-73.9866, Wright 44.1505/-73.9886, etc.). Properties per feature: `{"name": "Mt. Marcy", "elev_ft": 5344, "rank": 1, "is_official_46er": true}`.
  - If a peak's coordinates are uncertain, omit it and add to SUMMARY follow-ups. Target >= 8 peaks inside the bbox; ship with 4 (Marcy, Algonquin, Wright, Phelps) if upstream is messy.
  - CRS: EPSG:4326. File: `scripts/marketing-data/adk-high-peaks/peaks_46.geojson` (this one IS committed — curated content, ~few KB).

Step 7 — README.md:
  - Document: prerequisites (docker stack healthy, GDAL >= 3.1, ~25 GB free), each script, the AOI bbox, the data sources, the run order (fetch_dem -> build_dem_cog -> fetch_vectors -> clip_vectors -> ingest_manifest -> compose_marketing_maps), where outputs land, and how to clean up `.scratch/adk-data/` after.

Throughout: every error, retry, decision, and elapsed time should be appended to a session log that becomes part of SUMMARY.md in Task 3.
  </action>
  <verify>
    <automated>
ls -lh .scratch/adk-data/cogs/adk_high_peaks_dem_1m.tif \
  && ls -lh .scratch/adk-data/cogs/adk_high_peaks_*.tif \
  && ls -lh .scratch/adk-data/vectors/*_aoi.geojson \
  && python3 -c "import json; d=json.load(open('scripts/marketing-data/adk-high-peaks/peaks_46.geojson')); assert d['type']=='FeatureCollection'; assert len(d['features']) >= 4; print(f\"peaks_46.geojson: {len(d['features'])} features OK\")" \
  && grep -q "^\.scratch/" .gitignore \
  && test -f scripts/marketing-data/adk-high-peaks/README.md \
  && echo "Task 1 verify: PASS"
    </automated>
  </verify>
  <done>
.scratch/adk-data/cogs/ contains at least one DEM COG (~MB-GB sized .tif) AND one aerial COG (if `naip` or `both` chosen at Task 0; if `orthos`-only, the aerial COG step is intentionally pending the user-supplied zip).
.scratch/adk-data/vectors/ contains at least 3 clipped *_aoi.geojson files (APA Blue Line, NYSDEC Trails, plus one of: APA Land Classification or NHD).
scripts/marketing-data/adk-high-peaks/peaks_46.geojson is a valid GeoJSON FeatureCollection with >= 4 features.
.gitignore contains a `.scratch/` block.
scripts/marketing-data/adk-high-peaks/README.md documents the full pipeline.
None of `.scratch/` is staged for commit (verify with `git status --short`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Ingest into GeoLens + compose marketing map(s) + write the manifest</name>
  <files>
scripts/marketing-data/adk-high-peaks/ingest_manifest.yaml
scripts/marketing-data/adk-high-peaks/compose_marketing_maps.py
  </files>
  <action>
Now run the data through GeoLens and produce at least one saved map. Reuse the auth-bootstrap + upload + commit pattern from scripts/seed-natural-earth.py.

Step 1 — ingest_manifest.yaml:
  - Use the manifest_version: "1" schema from backend/app/processing/ingest/manifest_schemas.py.
  - Reference @examples/manifests/first-catalog/geolens.yaml + @examples/manifests/public-cog/geolens.yaml as templates.
  - Compose a manifest with all 5-7 datasets:
      * adk-high-peaks-dem-1m         type: raster_cog   (the mosaicked DEM COG)
      * adk-high-peaks-naip-2023      type: raster_cog   (NAIP aerial; omit if user chose orthos-only)
      * adk-blue-line-aoi             type: vector       (APA Blue Line clipped)
      * adk-hiking-trails-aoi         type: vector       (NYSDEC Trails clipped)
      * adk-land-classification-aoi   type: vector       (APA Land Classification clipped)
      * adk-46er-peaks                type: vector       (curated peaks_46.geojson)
  - metadata for each: tags (`adirondacks`, `high-peaks`, `marketing`, plus dataset-specific), organization, crs, license, attribution, bbox (the AOI).
  - publication: intent="published" for all (so they show in catalog without filter tricks).
  - CRITICAL: the `uri` field in this manifest can be local paths (under upload_staging_dir) OR https://. For COGs >100MB, prefer the direct HTTP-upload path (see Step 2) and DO NOT include them in the manifest apply — the manifest is the canonical record of what was ingested but is not the only ingest mechanism.
  - This file is committed for reproducibility; it is the "spec" of what got ingested. Actual ingestion in this task uses the upload+commit path below.

Step 2 — compose_marketing_maps.py:
  - Mirror the auth flow from scripts/seed-natural-earth.py (lines ~258-336): login -> mint API key -> use X-Api-Key header -> delete key on exit.
  - For each of the 6 datasets:
    a. POST /api/ingest/upload with the file (multipart). Get job_id.
       Use httpx.AsyncClient with timeout=180 (large COGs).
    b. POST /api/ingest/preview/{job_id} — returns RasterPreviewResponse or PreviewResponse.
    c. POST /api/ingest/commit/{job_id} with VectorCommitRequest or RasterCommitRequest:
         { title, summary, tags, visibility="public", record_status="published",
           organization, license, attribution, srid_override (if needed),
           strict_cog=False  (for rasters) }
    d. Poll GET /api/ingest/jobs/{job_id} every 5s until status="completed" or "failed" (max 600s for raster, 60s for vector). On failure surface the error and continue with remaining datasets.
    e. Capture the resulting dataset_id (from job response or by listing /api/datasets/?source_filename=...).
  - IDEMPOTENCY: before uploading each file, GET /api/datasets/?search=<key> and skip if a dataset with the manifest_key tag already exists (mirror seed-natural-earth.py's fetch_existing_datasets pattern).
  - After all 6 datasets are ingested, compose the saved map(s):
    
    Map 1 — "Adirondack High Peaks — Terrain & Trails" (the canonical marketing map):
      POST /api/maps/  body:
        {
          "name": "Adirondack High Peaks — Terrain & Trails",
          "description": "DEM hillshade + trails + 46er peaks over Lake Placid & Mt. Marcy. Marketing demo composition.",
          "terrain_config": { "enabled": false }   # leave 3D off for the headline shot
        }
      Then PATCH /api/maps/{id}/layers with these MapLayerInput entries (bottom -> top draw order):
        sort_order=0  dataset=NAIP/Orthos aerial   layer_type="raster_geolens" opacity=1.0
                       style_config={"builder": {"render_mode": "image"}}
        sort_order=1  dataset=DEM                  layer_type="raster_geolens" opacity=0.55
                       style_config={"builder": {"render_mode": "hillshade"}}
        sort_order=2  dataset=APA Blue Line        layer_type="vector_geolens" opacity=0.8
                       paint={ "line-color": "#1b6e3a", "line-width": 2, "line-dasharray": [3,2] }
                       display_name="Blue Line (APA boundary)"
        sort_order=3  dataset=NYSDEC Hiking Trails layer_type="vector_geolens" opacity=0.9
                       paint={ "line-color": "#c44d00", "line-width": 1.8 }
                       display_name="Hiking trails"
        sort_order=4  dataset=46er peaks           layer_type="vector_geolens" opacity=1.0
                       paint={ "circle-color": "#ffffff", "circle-radius": 5,
                               "circle-stroke-color": "#000000", "circle-stroke-width": 1.5 }
                       label_config={ "text-field": ["get","name"],
                                      "text-size": 12, "text-offset": [0,1.2],
                                      "text-halo-color": "#ffffff", "text-halo-width": 1.5 }
                       display_name="46er peaks"
                       popup_config={ "enabled": true, "expression": null,
                                      "visible_fields": ["name","elev_ft","rank"] }
    
    Map 2 (BONUS — only if Map 1 ingest & save returns 200 OK):
      "Adirondack High Peaks — 3D Relief" — same datasets but:
        terrain_config={ "enabled": true, "source": "<dem_dataset_id>", "exaggeration": 1.4 }
        DEM hillshade opacity higher (0.85), aerial removed for cleaner 3D, trails + peaks stay.
      Use this only if v1008 DEM-as-terrain path works without extra config. If it errors, log the error and ship just Map 1.

  - After each map create, print "Map saved: http://localhost:8080/maps/{id}".
  - On script exit (success or failure): delete the bootstrap API key (best-effort, just like seed-natural-earth.py).

Step 3 — record everything for SUMMARY:
  - Maintain a session JSON or list in the script: every upload, commit, job poll result, dataset_id, map_id, error.
  - Print this as a single block at the end so Task 3 (the SUMMARY writer, which runs interactively after) has structured input to paste in.

Step 4 — API issues capture (NEW — load-bearing for the dogfooding deliverable):
  - compose_marketing_maps.py MUST wrap every GeoLens HTTP call in an instrumented logger that records, per call:
      method, path, status_code, elapsed_ms, request_payload_summary (no secrets),
      response_summary (first 500 chars of body or structured field list), retry_count,
      and a free-form `friction` field that the script appends to whenever the developer
      had to: (a) read source to figure out a required field, (b) work around an
      unexpected error message, (c) handle a non-obvious status code, or
      (d) add a sleep/retry to make a request reliable.
  - At each major step (login, mint key, upload, preview, commit, job-poll, list datasets,
    create map, patch layers, delete map (if cleanup), delete key) — if anything required
    a workaround, an undocumented field, or a confusing error, append a structured note
    to a running `API_ISSUES_LOG` list in memory.
  - Write the raw log to `.scratch/adk-data/api_issues_log.jsonl` (one JSON line per call)
    so Task 3 can read it back when assembling API-ISSUES.md.
  - DO NOT redact useful debugging info — only redact the API key itself + any user JWT.
  - Specific friction points the script must explicitly probe and report on (not assume):
      * Does POST /api/auth/login require form-encoded or JSON? (existing seed script uses form-encoded — confirm and note if any path requires the other.)
      * Does POST /api/auth/api-keys/ require name only, or scopes? What is returned shape?
      * Does POST /api/ingest/upload accept multipart with what field name? Any size cap?
      * Does the preview response shape differ between raster and vector enough to need branching, and is that branching documented anywhere or only knowable by reading the source?
      * Does the commit endpoint validate srid/CRS and fail clearly, or fail in a confusing way?
      * What is the polling cadence GeoLens expects? Any rate-limit or retry-after?
      * How long does raster ingest take (timing) and is there a streaming/webhook alternative or is polling the only path?
      * For POST /api/maps/ — what is the minimum viable body? Does it require terrain_config, basemap_config, or are those optional? Any defaults applied server-side?
      * For PATCH /api/maps/{id}/layers — is the `order` field required when adding new layers, or only when reordering? What does the API do if the `order` array references unknown ids?
      * For MapLayerInput — which of `paint`, `layout`, `style_config`, `popup_config`, `label_config` are validated server-side and which are passed through as opaque JSON?
      * For label_config specifically: is it a top-level MapLayerInput field, or nested under style_config? Both spellings appear in casual descriptions — verify against schemas.py source.
      * Does the catalog list endpoint (/api/datasets/) support a `tags` filter directly, or must clients filter client-side after `search=`?
      * When ingest fails mid-job, what status is returned (failed vs partial), and is the job's error field machine-readable?
  - For EVERY question above, the script must produce a verdict (worked / surprising / undocumented / broken) and an evidence snippet.
  - If the script discovers a true bug (e.g. an endpoint that 500s on a valid payload, or returns a different shape than its Pydantic schema declares), elevate it to a top-of-document "Bugs Found" section in API-ISSUES.md and link the failing request/response.
  </action>
  <verify>
    <automated>
# 1. All committed scripts/manifest exist + manifest is valid YAML
test -f scripts/marketing-data/adk-high-peaks/ingest_manifest.yaml \
  && test -f scripts/marketing-data/adk-high-peaks/compose_marketing_maps.py \
  && python3 -c "import yaml; m=yaml.safe_load(open('scripts/marketing-data/adk-high-peaks/ingest_manifest.yaml')); assert m['manifest_version']=='1'; assert len(m['datasets']) >= 5; print(f\"manifest OK: {len(m['datasets'])} datasets\")" \
  && \
# 2. Stack still healthy after ingest
docker compose ps --format json | python3 -c "import sys,json; rows=[json.loads(l) for l in sys.stdin if l.strip()]; bad=[r for r in rows if r.get('Health') not in ('healthy','')]; assert not bad, f'unhealthy: {bad}'; print('stack: healthy')" \
  && \
# 3. At least 5 ADK-tagged datasets present in the catalog
curl -s -H "X-Api-Key: $(cat .scratch/adk-data/api_key 2>/dev/null || echo MISSING)" "http://localhost:8080/api/datasets/?limit=100" \
  | python3 -c "import sys,json; d=json.load(sys.stdin); adk=[x for x in d.get('datasets',[]) if 'adirondacks' in (x.get('tags') or []) or 'adk' in (x.get('title','').lower())]; assert len(adk) >= 5, f'only {len(adk)} ADK datasets'; print(f'datasets: {len(adk)} ADK')" \
  && \
# 4. At least 1 map saved
curl -s -H "X-Api-Key: $(cat .scratch/adk-data/api_key 2>/dev/null || echo MISSING)" "http://localhost:8080/api/maps/?search=Adirondack" \
  | python3 -c "import sys,json; d=json.load(sys.stdin); maps=[m for m in d.get('maps',[]) if 'adirondack' in m.get('name','').lower()]; assert len(maps) >= 1, f'no Adirondack maps found'; print(f'maps: {len(maps)} saved, first id={maps[0][\"id\"]}')" \
  && echo "Task 2 verify: PASS"
    </automated>
  </verify>
  <done>
GET /api/datasets/?limit=100 returns >= 5 datasets tagged `adirondacks`.
GET /api/maps/?search=Adirondack returns >= 1 saved map.
Opening http://localhost:8080/maps/{id} in a browser renders the DEM hillshade + aerial + vector overlays composited.
scripts/marketing-data/adk-high-peaks/ingest_manifest.yaml is committed and is a valid manifest_version=1 document with >= 5 dataset entries.
scripts/marketing-data/adk-high-peaks/compose_marketing_maps.py is committed and idempotent (rerunning it does NOT duplicate datasets or maps).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live MCP verify + write SUMMARY.md</name>
  <what-built>
The downloads + COGs in .scratch/adk-data/, the ingested datasets in the catalog, and the saved map(s) on the running stack. Now we verify visually + record everything.
  </what-built>
  <how-to-verify>
1. Open http://localhost:8080/maps/ in a browser. Confirm "Adirondack High Peaks — Terrain & Trails" appears in the list.

2. Click into the map. Confirm:
   - The aerial (NAIP or Orthos) is visible at high zoom (z14-z17).
   - The DEM hillshade reads as terrain texture, not blocky / nodata gaps.
   - Blue Line shows as a dashed green polygon outline.
   - Hiking trails show as orange/brown lines.
   - 46er peaks show as labeled white circles with peak name visible.
   - Clicking a peak shows a popup with name + elev_ft + rank.

3. (If Map 2 was created) Open it and confirm 3D terrain renders without errors in the browser console.

4. Open http://localhost:8080/datasets in a browser, search "Adirondack" or filter by tag `adirondacks`. Confirm all 5-7 datasets are listed, each shows a thumbnail/quicklook, and clicking each shows a non-empty Detail page.

5. After visual verification, the executor writes `.planning/quick/260524-o57-adk-high-peaks-data/260524-o57-SUMMARY.md` with sections:

   - AOI (bbox + any modest refinements)
   - Sources (each downloaded source with URL, size, CRS, license)
   - COGs Built (input tiles -> mosaicked output, size, CRS, compression)
   - Datasets Ingested (table: title, dataset_id, type, source_filename, size, manifest_key)
   - Saved Maps (name, map_id, URL, layer count, screenshot path if user grabbed one)
   - Errors (every retry, every job failure, every script abort with the full error message and what fixed it — or what is still broken)
   - Human Steps (every step that required user interaction — orthos portal click-through, disk-space confirmation prompt, browser-based map visual check)
   - Follow-ups (e.g. swap NAIP for NYS Orthos later; 46er peaks we couldn't pin; NHD hydrography not yet ingested; performance observations like "DEM rendering felt slow at z9-z11, consider building Titiler overviews")
   - Reproduction (one-line: "cd scripts/marketing-data/adk-high-peaks/ && ./fetch_dem.py && ./build_dem_cog.sh && ./fetch_vectors.py && ./clip_vectors.sh && python compose_marketing_maps.py")

6. Also write `.planning/quick/260524-o57-adk-high-peaks-data/260524-o57-API-ISSUES.md` by reading `.scratch/adk-data/api_issues_log.jsonl` and structuring the findings into the 9 sections defined in the must_haves artifacts list. Each section must be present even if empty — empty sections are explicitly marked "No issues observed during this run" rather than omitted. Cross-reference any bugs found with concrete file paths (e.g. `backend/app/processing/ingest/router.py:NNN`) when the executor can pinpoint where the surprising behavior lives. End the document with a prioritized "Recommended API Improvements" list ranked High / Medium / Low impact + a one-line rationale each, suitable for converting into a future GeoLens phase.

7. Final commit: stage scripts/marketing-data/adk-high-peaks/* + .gitignore + .planning/quick/260524-o57-adk-high-peaks-data/{260524-o57-SUMMARY.md, 260524-o57-API-ISSUES.md} and commit with message:
   `feat(marketing): ADK High Peaks dataset + saved map pipeline (quick 260524-o57)`

   Confirm `git status` is clean after the commit. Confirm `.scratch/` is NOT in the commit.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. "DEM looks blocky at z10 — needs overviews", "Blue Line clip cut off Wilmington wilderness")</resume-signal>
</task>

</tasks>

<verification>
Overall verification gates (all must pass before SUMMARY is written):

1. `df -h /Users/ishiland/Code/geolens` shows the scratch dir did not run out of space mid-pipeline (>= 5 GB free at end).
2. `docker compose ps --format json | jq -r '.[] | select(.Health!="healthy") | .Service'` returns empty (all services still healthy).
3. `curl -s http://localhost:8080/api/datasets/?limit=100` returns >= 5 ADK-tagged datasets.
4. `curl -s http://localhost:8080/api/maps/?search=Adirondack` returns >= 1 saved map.
5. Visual verification of the map(s) at http://localhost:8080/maps/{id} passes the must-haves in Task 3 step 2.
6. `git status --short` shows ONLY scripts/marketing-data/adk-high-peaks/* + .gitignore + the SUMMARY.md + the API-ISSUES.md as new/modified — nothing under `.scratch/`.
7. SUMMARY.md is >= 80 lines and contains all 8 required sections.
8. API-ISSUES.md is >= 60 lines, contains all 9 required sections (each marked "No issues observed" if empty rather than skipped), and includes a prioritized "Recommended API Improvements" list. The companion raw log at `.scratch/adk-data/api_issues_log.jsonl` is present (one JSON line per GeoLens HTTP call made by compose_marketing_maps.py).

Risks the executor should call out in SUMMARY:
- **Aerial portal scriptability**: did the user confirm the NYS Orthos portal supports scripted/REST download? If not, Task 0 picked `naip` or `both` — SUMMARY follow-up should be "test the WMS/WMTS endpoint mentioned in the data.gis.ny.gov search hub and confirm whether a future automation pass is feasible".
- **46er coordinates**: SUMMARY follow-up should list any peaks omitted for lack of authoritative coords. If the curated GeoJSON is < 8 features, follow-up should specifically link the ADK 46ers org site as the next source to mine.
- **CRS choices**: DEM was reprojected to EPSG:3857 (web mercator) for tiling consistency. Native 1m UTM 18N (EPSG:32618) would preserve true 1m resolution; if Titiler's bilinear resampling visibly degrades the hillshade at z16+, follow-up is "experiment with ingesting EPSG:32618 directly + letting Titiler reproject at request time".
- **Disk cleanup**: README should document `rm -rf .scratch/adk-data/dem .scratch/adk-data/aerial` (the raw GeoTIFFs) is safe to do after a successful ingest, since the COGs live in MinIO/local storage now. Keep `.scratch/adk-data/cogs/` for a few days in case re-ingest is needed.
</verification>

<success_criteria>
- At least 5 ADK High Peaks datasets visible at http://localhost:8080/datasets (1 DEM + 1 aerial + 3 vectors minimum).
- At least 1 saved map at http://localhost:8080/maps/{id} renders the DEM + aerial + vectors composited and looks marketing-grade in a screenshot.
- All scripts, the manifest YAML, the curated peaks_46.geojson, and the README live under `scripts/marketing-data/adk-high-peaks/` and are committed to git.
- `.scratch/adk-data/` is gitignored and contains the working raw downloads + COGs but is NOT tracked.
- The SUMMARY.md operational report at `.planning/quick/260524-o57-adk-high-peaks-data/260524-o57-SUMMARY.md` documents every step, every error, every human action, and every follow-up.
- The API-ISSUES.md dogfooding report at `.planning/quick/260524-o57-adk-high-peaks-data/260524-o57-API-ISSUES.md` documents every GeoLens API touched, every friction point, every undocumented behavior, every confusing error message, p50/p95 timing per endpoint, and a prioritized list of API improvements suitable for becoming a future GeoLens phase.
</success_criteria>

<output>
Create `.planning/quick/260524-o57-adk-high-peaks-data/260524-o57-SUMMARY.md` documenting the entire pipeline run, every error, and every follow-up.

Create `.planning/quick/260524-o57-adk-high-peaks-data/260524-o57-API-ISSUES.md` documenting every GeoLens API interaction, every friction point, every undocumented behavior, and a prioritized "Recommended API Improvements" list. Treat this as a dogfooding deliverable — its purpose is to give the next GeoLens phase a concrete, evidence-backed punch list for API hardening.
</output>
