---
mode: quick-full
description: "--validate lets remove the @docs folder since docs live in the getgeolens.com repo and we already have a docs-internal folder"
must_haves:
  truths:
    - The root `docs/` directory is removed from this repository; public product docs live in the `getgeolens.com` repo and private/internal material remains under ignored `docs-internal/`.
    - The scope is the root `docs/` folder only; do not delete `docs-internal/`, `backend/docs-internal/`, `frontend/docs/`, or unrelated planning/audit material.
    - No public repository-relative links to `docs/...` remain in README files, package metadata, CLI help text, contributor guidance, or active documentation tables after removal.
    - README visual assets currently under `docs/images/` must be moved to a non-docs public asset path, preferably `.github/assets/`, or replaced with verified stable external URLs before `docs/` is deleted.
    - Existing unrelated worktree changes must be preserved and excluded from any atomic commit; touched dirty files require careful before/after review.
    - `.gitignore` continues to cover internal planning/assistant paths and is updated so the removed root docs directory is not accidentally reintroduced.
  artifacts:
    - .gitignore
    - AGENTS.md
    - README.md
    - README.de.md
    - README.es.md
    - README.fr.md
    - CHANGELOG.md
    - .github/CONTRIBUTING.md
    - cli/README.md
    - cli/pyproject.toml
    - cli/geolens_cli/main.py
    - sdks/python/README.md
    - sdks/python/pyproject.toml
    - sdks/typescript/README.md
    - sdks/typescript/package.json
    - docs/
    - .github/assets/
    - .planning/quick/20260427-validate-lets-remove-the-docs-folder-sin/20260427-SUMMARY.md
    - .planning/quick/20260427-validate-lets-remove-the-docs-folder-sin/20260427-VERIFICATION.md
    - .planning/STATE.md
  key_links:
    - .planning/STATE.md
    - docs.getgeolens.com
    - docs/
    - docs-internal/
    - .gitignore
    - README.md
---

# Quick Task 20260427 Plan

## Context

- Project state says v13.7 is complete and the next milestone has not been opened; this quick task stands alone.
- `CLAUDE.md` is not present in this checkout.
- The worktree is already dirty across unrelated frontend/backend files and across several docs-related files. The executor must inspect target-file diffs before editing and stage only docs-removal-related changes.
- Current root `docs/` contents are `README.md`, `cli.md`, `sdks.md`, and README/repository image assets under `docs/images/`; several older `docs/*.md` files are already deleted in the worktree.
- Repository references to `docs/` currently include README image paths, package README links, package metadata documentation URLs, CLI help comments/text, AGENTS guidance, changelog links, and historical purge-script path filters. FastAPI `/docs`, `docs-internal/`, MDN/Swagger/PostgreSQL docs URLs, and storage test keys are not root docs-folder references.

## Task 1 - Inventory And Remove The Root Docs Folder

files:
- `docs/`
- `.github/assets/`
- `.gitignore`

action:
- Capture the pre-edit status with `git status --short` and inspect any existing diffs under `docs/`, `.gitignore`, README files, CLI files, SDK files, AGENTS, CHANGELOG, and `.github/CONTRIBUTING.md` before changing them.
- Enumerate tracked and untracked root docs content with `git ls-files docs`, `find docs -maxdepth 3 -type f`, and `git status --short docs`.
- Delete root `docs/` as a repository-owned folder. Remove `docs/README.md`, `docs/cli.md`, `docs/sdks.md`, and any remaining narrative/code-adjacent root docs files because public docs now live in the `getgeolens.com` repo.
- Preserve README/repository image assets by moving `docs/images/` to `.github/assets/` with `git mv` where possible, then remove the empty `docs/` directory. If an asset is already removed in the dirty worktree, do not recreate it unless a README reference still needs it.
- Update `.gitignore` to ignore root `docs/` and replace the old `!docs/images/*.png` exception with exceptions for the chosen asset path and asset types that are intentionally tracked.
- Do not touch `docs-internal/`, `backend/docs-internal/`, `frontend/docs/`, or internal planning directories.

verify:
- `test ! -d docs`
- `git status --short docs`
- `test -d .github/assets`
- `find .github/assets -maxdepth 1 -type f | sort`
- `rg -n "docs/images|!docs/images" .gitignore README*.md .github 2>/dev/null` returns no matches.

done:
- The root docs folder is gone, necessary README image assets live outside `docs/`, and ignore rules no longer depend on `docs/images/`.

## Task 2 - Clean Up Public Root Docs References

files:
- `README.md`
- `README.de.md`
- `README.es.md`
- `README.fr.md`
- `AGENTS.md`
- `.github/CONTRIBUTING.md`
- `CHANGELOG.md`
- `cli/README.md`
- `cli/pyproject.toml`
- `cli/geolens_cli/main.py`
- `sdks/python/README.md`
- `sdks/python/pyproject.toml`
- `sdks/typescript/README.md`
- `sdks/typescript/package.json`

action:
- Update README image paths from `docs/images/...` to the new non-docs asset path, or to verified stable hosted URLs if local assets are not retained.
- Replace public links to removed root docs with `https://docs.getgeolens.com/` links when an exact docs-site page is known and verified. If an exact page is not known, link to the docs-site root rather than guessing a broken deep URL.
- Update CLI and SDK package README references so they no longer direct users to `docs/cli.md` or `docs/sdks.md` in this repository. Keep package-level README content concise and point users at the docs site or package-local README sections.
- Update package metadata documentation URLs that currently point to `github.com/geolens-io/geolens/blob/main/docs/...`.
- Update CLI help strings and comments that say `see docs/cli.md`; keep any no-op flag behavior unchanged.
- Update AGENTS/repository guidance so it no longer describes `docs/` as an operations/documentation home. Preserve the rule that detailed product docs belong on docs.getgeolens.com and internal material belongs in ignored `docs-internal/`.
- Update active broken relative links in CHANGELOG and contributor guidance. Preserve purely historical references only when they are clearly audit/history text and not live user-facing links.
- Leave unrelated references alone, including FastAPI `/docs`, `docs-internal/` audit references, MDN/Swagger/PostgreSQL external docs URLs, and test fixture keys such as `docs/file.txt`.

verify:
- `rg -n "\\]\\(docs/|src=\"docs/|github.com/geolens-io/geolens/blob/main/docs|docs/cli\\.md|docs/sdks\\.md|docs/widget-development\\.md|docs/testing-and-ci\\.md" README*.md AGENTS.md CHANGELOG.md .github cli sdks`
- `rg -n "docs/" --glob '!docs-internal/**' --glob '!backend/docs-internal/**' --glob '!frontend/docs/**' --glob '!node_modules/**' --glob '!.git/**' .` is reviewed and every remaining match is either an external docs URL, FastAPI `/docs`, historical purge filter, external vendor docs URL, or an intentional non-root-docs string.
- `git diff --check`
- `make cli-test` if `cli/geolens_cli/main.py` or CLI packaging metadata changed.

done:
- Removing `docs/` does not leave broken public repository links, package metadata links, README images, or CLI guidance.

## Task 3 - Validate, Record, And Commit Atomically

files:
- `.planning/quick/20260427-validate-lets-remove-the-docs-folder-sin/20260427-SUMMARY.md`
- `.planning/quick/20260427-validate-lets-remove-the-docs-folder-sin/20260427-VERIFICATION.md`
- `.planning/STATE.md`

action:
- Write a concise summary describing the deleted root docs folder, where README assets now live, which public links were retargeted to docs.getgeolens.com, and which `docs/` references intentionally remain because they are not root-docs-folder links.
- Write verification notes with exact commands, exit statuses, and any accepted residual matches from the final `rg` audit.
- Update `.planning/STATE.md` Quick Tasks Completed for this quick task with status `Verified` only after validation succeeds.
- Review `git diff --name-status` and `git diff --stat` to confirm the commit contains only root-docs-removal work plus required reference cleanup and GSD artifacts.
- Stage and commit only relevant changes. Do not stage unrelated existing frontend/backend/map-builder/config changes already present in the worktree.

verify:
- `test ! -d docs`
- `test -s .planning/quick/20260427-validate-lets-remove-the-docs-folder-sin/20260427-SUMMARY.md`
- `test -s .planning/quick/20260427-validate-lets-remove-the-docs-folder-sin/20260427-VERIFICATION.md`
- `git diff --check`
- `git status --short`
- `git log -1 --oneline` after the atomic commit.

done:
- The quick task has durable summary and verification artifacts, STATE.md is updated, root `docs/` removal is validated, and only relevant docs-removal changes are committed.
