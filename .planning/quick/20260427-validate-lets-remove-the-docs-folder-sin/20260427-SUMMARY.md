---
status: completed
date: 2026-05-05
task: "--validate lets remove the @docs folder since docs live in the getgeolens.com repo and we already have a docs-internal folder"
---

# Quick Task 20260427 Summary

## Changes

- Removed the root `docs/` directory from this repository.
- Moved README and repository image assets from `docs/images/` to `.github/assets/`.
- Updated README image paths and the live root README reference table to stop linking into `docs/`.
- Retargeted CLI, SDK, package metadata, workflow comments, contributor guidance, AGENTS guidance, and active changelog links away from removed `docs/*.md` files.
- Updated `.gitignore` so root `docs/` is ignored going forward while `.github/assets/*.png` remains trackable for README assets.

## Intentional Remaining `docs/` Strings

- `docs.getgeolens.com` links point to the canonical docs site.
- External vendor docs URLs remain unchanged.
- `scripts/purge-history.sh` keeps historical root-docs path filters for release cleanup.
- `backend/tests/test_storage.py` keeps `docs/file.txt` as a storage object key fixture, not a repository docs path.

## Validation

- `test ! -d docs`: passed.
- Targeted stale-link scan for root docs links: no matches.
- Broad `docs/` scan reviewed: only accepted non-root-docs matches remain.
- `git diff --check`: passed.
- `git diff --cached --check`: passed.
- `make cli-test`: first run failed because `geolens_test` was missing on the default local Postgres target; rerun after `docker compose up -d --wait db` with explicit host-side DB vars passed (`170 passed`; backend round trip `8 passed, 2 skipped`).

## Implementation Commit

- `42ed0461` — `docs(quick-20260427): remove root docs folder`
