---
phase: 260518-qz1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_tile_cols_endpoint.py
  - backend/tests/test_tile_cache_cols_key.py
  - frontend/src/components/builder/__tests__/map-sync.heatmap-cols.test.ts
  - frontend/src/lib/__tests__/tile-utils.test.ts
autonomous: false
requirements:
  - F1-HEATMAP-LIVE
  - F2-COLS-ENDPOINT
  - F2-COLS-CACHE-KEY
  - F3-VIEWER-LIVE

must_haves:
  truths:
    - "Backend integration test confirms ?cols=<col> projects the column into MVT at z<10 (where the default budget would strip it)"
    - "Backend integration test confirms invalid cols= names (not in column_info) are silently dropped without erroring the tile request"
    - "Backend integration test confirms tile cache keys differentiate by cols= so two requests with different cols don't return each other's bytes"
    - "Frontend unit test confirms the heatmap-paint shape ({'_heatmap-weight-column': X, 'heatmap-weight': ['get', X]}) — exactly what HeatmapStyleControls writes — yields a single column extraction"
    - "Frontend unit test confirms buildSignedTileUrl correctly filters whitespace-only and falsy extraCols entries and URL-encodes the comma separator as %2C — covering edge cases beyond the existing tile-utils.test.ts:54-79 coverage."
    - "Live MCP verification of F1 (heatmap weight column flows through tiles at z=2) is captured in the SUMMARY as either CONFIRMED (with screenshot + tile URL inspection evidence) OR a BUG opened with the precise failure shape"
    - "Live MCP verification of F3 (embed-token + authenticated viewer at z=2 render data-driven colors correctly and tile URL includes &cols=pop_est) is captured in the SUMMARY as either CONFIRMED (with screenshot + tile URL inspection evidence) OR a BUG opened with the precise failure shape"
  artifacts:
    - path: "backend/tests/test_tile_cols_endpoint.py"
      provides: "Integration tests for ?cols= flow against a real PostGIS test DB"
      contains: "test_tile_endpoint_with_cols_param, test_tile_endpoint_cols_validates_against_column_info"
    - path: "backend/tests/test_tile_cache_cols_key.py"
      provides: "Cache-isolation test asserting cols= suffix participates in the cache key"
      contains: "test_tile_cache_key_includes_cols_suffix"
    - path: "frontend/src/components/builder/__tests__/map-sync.heatmap-cols.test.ts"
      provides: "Realistic heatmap-paint extraction test (full HeatmapStyleControls write shape)"
      contains: "describe('heatmap-weight-column extraction')"
    - path: ".planning/quick/260518-qz1-tile-cols-opt-in-followups-f1-f2-f3/260518-qz1-SUMMARY.md"
      provides: "Closeout summary with F1/F3 live-verify results captured by orchestrator post-executor"
      contains: "F1 live verify, F3 live verify, F4/F5 documentation-only acknowledgement"
  key_links:
    - from: "backend/tests/test_tile_cols_endpoint.py"
      to: "backend/app/processing/tiles/router.py:867 (cols param)"
      via: "GET /tiles/data.{table}/{z}/{x}/{y}.pbf?cols=<col>"
      pattern: "client\\.get.*cols="
    - from: "backend/tests/test_tile_cache_cols_key.py"
      to: "backend/app/platform/cache/tile_cache.py:74 (cols_key suffix)"
      via: "tile_cache.get(table, z, x, y, cols_key=...) with different cols_key values"
      pattern: "cols_key="
    - from: "frontend/src/components/builder/__tests__/map-sync.heatmap-cols.test.ts"
      to: "frontend/src/components/builder/map-sync.ts:428 (getDataDrivenColumnsForLayer)"
      via: "import + direct call with HeatmapStyleControls-shaped paint"
      pattern: "getDataDrivenColumnsForLayer"
    - from: "frontend/src/lib/__tests__/tile-utils.test.ts"
      to: "frontend/src/lib/tile-utils.ts:26 (buildSignedTileUrl extraCols param)"
      via: "buildSignedTileUrl(table, token, base, undefined, extraCols)"
      pattern: "extraCols|cols="
---

<objective>
Close out the three actionable follow-ups from `2026-05-18-tile-cols-followups.md` left over from the `c8c9d08f` tile `?cols=` opt-in fix:

- **F1** — Heatmap pipeline live-verify (orchestrator-owned MCP)
- **F2** — Backend integration tests for `?cols=` end-to-end (executor-owned)
- **F3** — Viewer (authenticated + embed-token) live-verify (orchestrator-owned MCP)

Purpose: The fix (`c8c9d08f`) is live and the builder categorical/graduated path is confirmed on Admin 0 Countries at z=2. Three same-code-path scenarios that the helper supports (`getDataDrivenColumnsForLayer` reads `_heatmap-weight-column`, `_height_column`, and `style_config.column`) were NOT exercised live, and the HTTP `?cols=` path has no integration coverage — only direct-call unit tests on `_select_tile_columns`. This plan closes that gap.

Output:
- 2 new backend integration test files (F2)
- 2 new frontend unit-test additions (F1 + F2 supporting code: extraction shape coverage + URL build coverage)
- SUMMARY.md capturing F1 + F3 live-verify outcomes (CONFIRMED or BUG) + F4/F5 acknowledgement as documentation-only

**Executor scope:** Tasks 1-3 below. Tasks F1-live and F3-live are orchestrator-owned post-executor steps (the executor cannot drive Playwright MCP — only the orchestrator/Claude Code can).

F4 and F5 from the todo are documentation-only and require NO action. The todo file already captures them. SUMMARY will explicitly acknowledge this.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/todos/pending/2026-05-18-tile-cols-followups.md

# The contract memory — `cols=` is intentionally unsigned (auth-by-sig, not auth-by-cols)
# Path: ~/.claude/projects/-Users-ishiland-Code-geolens/memory/project_tile_cols_optin.md

# Backend tile endpoint receiving ?cols=
@backend/app/processing/tiles/router.py

# Where `_select_tile_columns` lives (already unit-tested)
@backend/app/processing/tiles/service.py

# Cache key construction (cols_key suffix)
@backend/app/platform/cache/tile_cache.py

# Existing integration tests for tile endpoint — pattern to follow
@backend/tests/test_tiles.py

# Existing unit tests for the allowlist resolver — for reference only
@backend/tests/test_tile_column_allowlist.py

# Test DB lifecycle — _test_db_lifecycle (autouse session-scoped) handles DB creation
@backend/tests/conftest.py

# Frontend column-extractor + signed URL flow
@frontend/src/components/builder/map-sync.ts

# Heatmap paint write site — `HeatmapStyleControls` is the canonical writer
@frontend/src/components/builder/HeatmapStyleControls.tsx

# Existing column-extraction tests (already cover heatmap shape; we'll add the full HeatmapStyleControls shape)
@frontend/src/components/builder/__tests__/map-sync.data-driven-cols.test.ts

# Existing tile-utils tests (add extraCols branch)
@frontend/src/lib/__tests__/tile-utils.test.ts

<interfaces>
# Key types and contracts the executor needs. Extracted from codebase.
# Executor should use these directly — no codebase exploration needed.

From `backend/app/processing/tiles/router.py:856-867`:
```python
@router.get("/{table_path:path}/{z:int}/{x:int}/{y:int}.pbf", response_class=Response)
async def tile_endpoint(
    request: Request,
    table_path: str,
    z: int, x: int, y: int,
    sig: str | None = None,
    exp: int | None = None,
    scope: str | None = None,
    cols: str | None = None,        # ← The opt-in. Comma-separated. Unsigned.
    db: AsyncSession = Depends(get_db),
) -> Response: ...
```

From `backend/app/processing/tiles/router.py:903-909` (cols parsing):
```python
additional_columns: list[str] | None = None
cols_cache_key = ""
if cols:
    raw = [c.strip() for c in cols.split(",") if c.strip()]
    if raw:
        additional_columns = sorted(set(raw))
        cols_cache_key = ",".join(additional_columns)
```

From `backend/app/platform/cache/tile_cache.py:74-86`:
```python
async def get(
    self, table: str, z: int, x: int, y: int, cols_key: str = ""
) -> bytes | None:
    suffix = f":{cols_key}" if cols_key else ""
    key = f"tile:{table}:{z}:{x}:{y}{suffix}"
    # ...
```

From `backend/tests/test_tiles.py:32-69` (factory pattern to follow):
```python
async def _create_tile_test_dataset(
    session, *, created_by: uuid.UUID, table_name: str
) -> Dataset:
    # column_info includes: gid, name, value, geom, geom_4326
    # The "value" column (type integer) is the one we'll exercise via ?cols=value
```

From `backend/tests/conftest.py:94-275` — the `_test_db_lifecycle` autouse session fixture
creates a uuid-suffixed test database via `_session_test_database_name`, runs alembic
migrations, and tears it down at session end. The `client` fixture (line 279) is what
HTTP-integration tests use. The `test_db_session` fixture (line 472) yields an async
session against the SAME test DB. The fixture from `test_tiles.py` `_init_tile_pool_for_tests`
(line 141) is what wires up the asyncpg pool the tile endpoint uses for raw SQL — REUSE IT.

From `frontend/src/components/builder/HeatmapStyleControls.tsx:36-47` (canonical heatmap paint shape):
```typescript
const handleWeightColumnChange = useCallback((col: string) => {
  const newPaint = { ...paint };
  if (col === NONE_VALUE || col === '') {
    newPaint['heatmap-weight'] = 1;
    delete newPaint['_heatmap-weight-column'];
  } else {
    newPaint['heatmap-weight'] = ['get', col];           // ← runtime expression
    newPaint['_heatmap-weight-column'] = col;            // ← marker (what extractor reads)
  }
  onPaintChange(layer.id, newPaint);
}, ...);
```
So a heatmap layer with weight column `magnitude` ALWAYS has BOTH paint keys present
together. The extractor (`getDataDrivenColumnsForLayer`) walks both the marker AND
the `["get", X]` reference, so it's belt-and-suspenders robust — but the test should
mirror the real shape, not just the marker.

From `frontend/src/lib/tile-utils.ts:26-39` (signed URL builder):
```typescript
export function buildSignedTileUrl(
  tableName: string,
  tileToken: { sig: string; exp: number; scope: string } | null,
  tileBaseUrl?: string | null,
  tileVersion?: string | null,
  extraCols?: string[] | null,         // ← The opt-in caller passes the columns it needs
): string {
  // ...
  const cols = normalizeExtraCols(extraCols);  // sorts + dedupes + filters falsy
  return appendTileParams(url, tileToken, tileVersion, cols ? { cols } : {});
}
```
</interfaces>

# Stack-up requirements (executor MUST verify before running tests)
- Docker stack up (`docker compose ps` shows db + api healthy). F2 tests hit the test DB.
- The test DB is provisioned automatically by `_test_db_lifecycle` (autouse session fixture in conftest.py). The error message in the planning context (`InvalidCatalogNameError: database "geolens_test_..." does not exist`) typically surfaces when the **dev Postgres host** is unreachable. If `docker compose ps db` is healthy and `psql -h localhost -p 5432 -U geolens postgres -c "\l"` succeeds, the conftest fixture will create the test DB on session start.
- If the test DB error reproduces, the executor MUST diagnose at the conftest level — NOT bypass with custom DB setup. Likely causes: docker stack down, port conflict, or a stale settings.postgres_db_test default. Fix and re-run, do not work around.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (F2): Backend integration tests for ?cols= flow + cache key isolation</name>
  <files>
    backend/tests/test_tile_cols_endpoint.py,
    backend/tests/test_tile_cache_cols_key.py
  </files>
  <behavior>
    For `backend/tests/test_tile_cols_endpoint.py`:
    - Test `test_tile_endpoint_with_cols_param_projects_column_at_low_zoom`:
      - GIVEN a public vector dataset with column_info containing `value` (integer) and a feature with `value=42` inserted via `_create_data_table`
      - WHEN GET /tiles/data.{table}/2/2/2.pbf?cols=value (z=2 is below the _DEFAULT_NO_ATTR_BELOW_ZOOM=10 cutoff)
      - THEN response is 200 with content-type `application/vnd.mapbox-vector-tile`, non-empty body
      - Note: the test should compare against the SAME endpoint without `?cols=value` and confirm both succeed (we cannot easily decode MVT without adding a new dep — assert the response shape, status, and that the tile body length is non-zero, which proves the dataset is being queried at z=2 instead of the no-attrs branch returning b''). 
      - Document explicitly in a test docstring that decoding the MVT to assert the `value` attribute is feature-positive would require adding a new dev dep (`mapbox-vector-tile`), which is out of scope for this hygiene close. The integration test asserts the HTTP path runs without erroring; the unit tests in `test_tile_column_allowlist.py` already cover the data correctness of `_select_tile_columns(additional_columns=['value'])`.
    - Test `test_tile_endpoint_cols_silently_drops_invalid_names`:
      - WHEN GET /tiles/data.{table}/2/2/2.pbf?cols=does_not_exist
      - THEN response is 200 (NOT 400). The validation strips `does_not_exist` (not in column_info) but the tile still returns successfully. This proves the silent-drop contract.
    - Test `test_tile_endpoint_cols_silently_drops_sql_injection_attempt`:
      - WHEN GET /tiles/data.{table}/2/2/2.pbf?cols=drop+table+users;--
      - THEN response is 200 with valid MVT (the regex-validator drops the injection attempt before SQL substitution)
    - Test `test_tile_endpoint_cols_normalizes_permutations` (defense-in-depth around the cache key):
      - WHEN GET .../?cols=value,name AND .../?cols=name,value (same set, different order)
      - THEN both return 200 with non-empty MVT (the route sorts before cache lookup so they hit the same cache slot; this just confirms both produce valid responses).

    For `backend/tests/test_tile_cache_cols_key.py`:
    - Test `test_tile_cache_key_includes_cols_suffix_isolates_projections`:
      - GIVEN a mock `tile_cache` (AsyncMock) injected via patch on `app.processing.tiles.router.get_tile_cache` (follow the pattern from `test_empty_tile_sentinel_cache_hit_returns_204` at test_tiles.py:311)
      - WHEN GET .../?cols=value
      - THEN `mock_cache.get` was called with `cols_key="value"` (not `cols_key=""`)
      - WHEN GET .../ (no cols param)
      - THEN `mock_cache.get` was called with `cols_key=""` (default)
      - This proves the route threads the cols normalization into the cache key. Two different cols= values cannot collide because the suffix participates in the key construction.
    - Test `test_tile_cache_key_omits_suffix_when_cols_absent_or_empty`:
      - WHEN GET .../ (no `cols`)  AND GET .../?cols= (empty string)
      - THEN both calls to `mock_cache.get` use `cols_key=""` (backward-compatible with the pre-fix cache key shape)
  </behavior>
  <action>
    Create two new backend test files. DO NOT modify `test_tiles.py` (the planning context flagged its fixtures are touchy; new files isolate risk).

    **For `backend/tests/test_tile_cols_endpoint.py`:**
    1. Mirror the imports + helper-call pattern from `backend/tests/test_tiles.py:1-160`. Reuse the `_init_tile_pool_for_tests` fixture by IMPORTING it: `from tests.test_tiles import _init_tile_pool_for_tests` (pytest will pick it up via the `@pytest.mark.usefixtures` decorator). DO NOT duplicate the fixture body — that would create two pools competing on the same module-global `_tile_pool`.
    2. Also import the helpers: `from tests.test_tiles import _create_tile_test_dataset, _create_data_table, _cleanup_data_table`. These already insert a `value` integer column and a `(0,0)` point feature — exactly what we need.
    3. Decorate the test class with `@pytest.mark.usefixtures("_init_tile_pool_for_tests")` to wire the asyncpg pool.
    4. Each test follows the existing test_tiles.py pattern: create a unique-suffixed dataset + data table in `try`, exercise the endpoint, `_cleanup_data_table` in `finally`.
    5. For the cols+ permutation test, the response just needs to confirm both succeed — the actual cache-key correctness is covered by the dedicated cache test below.
    6. Do not import `mapbox-vector-tile` or attempt MVT decode. Use response status, content-type, and content-length checks.

    **For `backend/tests/test_tile_cache_cols_key.py`:**
    0. Import `_init_tile_pool_for_tests` from `tests.test_tiles` (same as step 1 of the endpoint test) and decorate the test class with `@pytest.mark.usefixtures("_init_tile_pool_for_tests")`. The mock-cache test falls through to the real PostGIS query path (step 2 sets `mock_cache.get.return_value = None`), which requires the asyncpg pool. Without this fixture the route's `get_tile_pool()` call raises before reaching the `mock_cache.set` assertion.
    1. Mirror the mock-injection pattern from `test_empty_tile_sentinel_cache_hit_returns_204` at `test_tiles.py:311-334`. Use `unittest.mock.AsyncMock` and `unittest.mock.patch` on `app.processing.tiles.router.get_tile_cache`.
    2. Configure `mock_cache.get.return_value = None` so the route falls through to the real PostGIS query (we want the test to exercise the full call shape, not short-circuit on a cache hit). The asserted call is on `mock_cache.get` — what `cols_key` value did the router pass?
    3. After the request, inspect `mock_cache.get.call_args` to assert the keyword argument `cols_key` is what we expect.
    4. Inspect `mock_cache.set.call_args` for the equivalent assertion on the write path (the route also calls `tile_cache.set` with the cols_key after fetching). If `mock_cache.get.return_value = None`, `tile_cache.set` SHOULD be called with the same `cols_key`.

    **Acceptance loop:**
    - Run `docker compose ps` to confirm db is up. If not, executor reports back to user — DO NOT proceed with mocked DB substitutes.
    - Run `cd backend && pytest tests/test_tile_cols_endpoint.py tests/test_tile_cache_cols_key.py -xvs` — must be green.
    - If `_test_db_lifecycle` errors with `InvalidCatalogNameError`, diagnose at the conftest level. Likely causes: dev Postgres unreachable (check `docker compose ps db`), port conflict, or `POSTGRES_DB_TEST` env not set. Fix the root cause; do NOT introduce a per-test DB substitute or skip the suite.

    Reference the relevant decision-trace per the source artifact (todo file lines 41-49 for F2 scope).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend &amp;&amp; uv run pytest tests/test_tile_cols_endpoint.py tests/test_tile_cache_cols_key.py -xvs 2>&amp;1 | tail -40</automated>
  </verify>
  <done>
    Both new test files exist, all tests pass, both files import (not duplicate) `_init_tile_pool_for_tests` from `test_tiles.py`. The cache-key test confirms `cols_key="value"` vs `cols_key=""` are distinct cache slots. The endpoint test confirms HTTP 200 for valid cols, valid cols→invalid-name, SQL-injection-attempt drops, and cols-permutation invariance — without requiring a new MVT-decode dev dep.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (F1 + F2 supporting): Frontend tests — heatmap shape + buildSignedTileUrl extraCols branch</name>
  <files>
    frontend/src/components/builder/__tests__/map-sync.heatmap-cols.test.ts,
    frontend/src/lib/__tests__/tile-utils.test.ts
  </files>
  <behavior>
    For `frontend/src/components/builder/__tests__/map-sync.heatmap-cols.test.ts` (NEW file):
    - Test `extracts a heatmap weight column from the FULL HeatmapStyleControls write shape`:
      - GIVEN paint = `{'_heatmap-weight-column': 'magnitude', 'heatmap-weight': ['get', 'magnitude'], 'heatmap-radius': 30, 'heatmap-intensity': 1}`
      - WHEN `getDataDrivenColumnsForLayer({style_config: null, paint})` is called
      - THEN result is `['magnitude']` (a single deduped entry — both the `_heatmap-weight-column` marker AND the `['get', 'magnitude']` paint expression reference the SAME column, and the Set dedupes them)
      - This is the realistic shape that `HeatmapStyleControls.handleWeightColumnChange` (line 43-44) actually writes. The existing test at `map-sync.data-driven-cols.test.ts:17-23` only tests the marker in isolation; this adds belt-and-suspenders coverage that the dedup works against the full write shape.
    - Test `extracts heatmap weight column even when only the `['get']` expression is present (marker missing)`:
      - GIVEN paint = `{'heatmap-weight': ['get', 'magnitude']}` (marker absent — defensive against future write paths that forget to set `_heatmap-weight-column`)
      - WHEN extractor runs
      - THEN result is `['magnitude']` (walks the expression tree and finds it)
    - Test `extracts heatmap weight column even when only the marker is present (expression missing)`:
      - GIVEN paint = `{'_heatmap-weight-column': 'magnitude', 'heatmap-weight': 1}` (someone manually edited the marker, expression still default-1)
      - WHEN extractor runs
      - THEN result is `['magnitude']` (reads marker directly)
    - Test `extracts both column-extractor inputs simultaneously when distinct (defensive)`:
      - GIVEN paint = `{'_heatmap-weight-column': 'col_a', 'heatmap-weight': ['get', 'col_b']}` (mismatch — shouldn't occur in practice but tests union semantics)
      - WHEN extractor runs
      - THEN result is `['col_a', 'col_b']` sorted (proves both inputs feed the same Set)

    For `frontend/src/lib/__tests__/tile-utils.test.ts` (ADD edge-case tests to existing file):
    The existing `describe('buildSignedTileUrl')` block at lines 54-79 already covers: single-col append, sort+dedupe, empty/null omit, and combined-with-signed-params. The NEW block adds ONLY the edge cases NOT covered by lines 54-79:
    - Test `omits &cols= when extraCols contains only whitespace`:
      - WHEN `buildSignedTileUrl('tbl', null, 'https://cdn.example.com', null, ['   '])`
      - THEN URL has NO `cols=` substring (whitespace-only filtered by `normalizeExtraCols`)
    - Test `omits &cols= when extraCols contains only falsy entries`:
      - WHEN `buildSignedTileUrl('tbl', null, 'https://cdn.example.com', null, [undefined as any, null as any])`
      - THEN URL has NO `cols=` substring (explicit falsy-entry filter, distinct from the existing empty-array case)
    - Test `URL-encodes the comma separator as %2C`:
      - WHEN `buildSignedTileUrl('tbl', null, 'https://cdn.example.com', null, ['col_a', 'col_b'])`
      - THEN URL contains exactly `cols=col_a%2Ccol_b` (not just `cols=` — proves the comma is URL-encoded per `appendTileParams:65`, distinct from the existing `.toContain('cols=a%2Cb')` assertion which is single-char-name shaped)
  </behavior>
  <action>
    For the new file `frontend/src/components/builder/__tests__/map-sync.heatmap-cols.test.ts`:
    1. Mirror the imports + describe pattern from `frontend/src/components/builder/__tests__/map-sync.data-driven-cols.test.ts:1-7`.
    2. Each test calls `getDataDrivenColumnsForLayer({style_config: ..., paint: ...})` directly and asserts on the returned array. Sort before assertion when order is undefined (Set→Array.from is insertion order, not sorted).
    3. Reference the test-file comment block to `HeatmapStyleControls.tsx:36-47` so a future reader sees WHY these specific shapes matter.

    For additions to `frontend/src/lib/__tests__/tile-utils.test.ts`:
    1. **First, READ `frontend/src/lib/__tests__/tile-utils.test.ts:54-79`** — this file already contains four `extraCols`-related tests inside the first `describe('buildSignedTileUrl')` block (single-col append, sort+dedupe, empty/null omit, combined-with-signed-params). DO NOT duplicate them.
    2. Add a new `describe('buildSignedTileUrl extraCols edge cases', () => { ... })` block AFTER the existing `describe('buildSignedTileUrl')` block. The new block contains ONLY the cases NOT covered by lines 54-79:
       - Whitespace-only entry filter: `['   ']` → URL has NO `cols=`
       - Falsy-entry filter (explicit): `[undefined as any, null as any]` → URL has NO `cols=`
       - Explicit URL-encoding assertion: when `extraCols=['col_a', 'col_b']`, URL contains exactly `cols=col_a%2Ccol_b` (not just `cols=` — proves the comma is URL-encoded per `appendTileParams:65`)
    3. Reuse the `mockToken` variable shape from existing tests in that file.
    4. The new block is intentionally small (~3 tests) — its purpose is edge-case coverage, not parity coverage. Parity is already in the existing block.

    **Acceptance loop:**
    - Run `cd frontend && npm test -- run src/components/builder/__tests__/map-sync.heatmap-cols.test.ts src/lib/__tests__/tile-utils.test.ts` — must be green.
    - If any test fails, FIX THE TEST (not the implementation) UNLESS the failure shape indicates a real bug in `getDataDrivenColumnsForLayer` or `buildSignedTileUrl`. In that case, STOP, report the failure shape, and let the orchestrator decide whether to open a bug per F1's "or bug opened" acceptance.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend &amp;&amp; npm test -- run src/components/builder/__tests__/map-sync.heatmap-cols.test.ts src/lib/__tests__/tile-utils.test.ts 2>&amp;1 | tail -40</automated>
  </verify>
  <done>
    Both files exist (one new, one extended), all new tests pass. The heatmap-shape test confirms the full `HeatmapStyleControls` paint write produces a deduped single-column extraction. The tile-utils additions cover edge cases NOT already in lines 54-79: whitespace-only filter, explicit falsy-entry filter, and exact `%2C` URL-encoding assertion. These tests are the deterministic, executor-runnable supporting code for F1's "if the extraction or URL build is broken, we'd catch it in vitest before MCP" assertion. The MCP verification itself remains an orchestrator-owned step (see Task 3).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (F1 + F3 orchestrator MCP handoff): Live verify heatmap + viewer at z&lt;10</name>
  <what-built>
    Tasks 1 and 2 produced deterministic test coverage for the `?cols=` flow:
    - Backend integration tests confirm the HTTP path: cols= passthrough, validation, cache-key isolation, SQL-injection drop (Task 1).
    - Frontend unit tests confirm: heatmap-paint shape extracts the weight column (F1 supporting code), and `buildSignedTileUrl(extraCols)` builds the correct URL shape (F2 supporting code) (Task 2).

    The remaining verification is **live UI behavior at z&lt;10**, which the executor cannot drive (Playwright MCP is orchestrator-scoped). This checkpoint hands F1 and F3 to the orchestrator (Claude Code) for live MCP verification on the running `localhost:8080` stack.
  </what-built>
  <how-to-verify>
    The orchestrator will drive Playwright MCP on `http://localhost:8080` for both surfaces. Pre-flight:

    1. Confirm Docker stack is up: `docker compose ps` shows db, api, worker, frontend, redis healthy.
    2. Confirm the user is logged in to the builder (cookies in MCP browser context).

    **F1 — Heatmap live verify (orchestrator):**
    1. Open or create a map. Add a POINT dataset (e.g. `ne_10m_populated_places` or `cities_5m` — anything with a numeric column).
    2. Switch the layer's Render As to `heatmap`.
    3. Configure a numeric weight column (e.g. `pop_max` or any int/float column).
    4. Zoom out to z=2.
    5. Capture evidence:
       - **(a) Tile URL inspection** — Use `mcp__chrome-devtools__list_network_requests` to find the tile request URL. Assert it contains `&cols=<weight_column>` (URL-encoded). PASS if present.
       - **(b) querySourceFeatures probe** — Run `map.querySourceFeatures(sourceId, {sourceLayer: 'data.<table>'})` in the page console (`mcp__chrome-devtools__evaluate_script`). Assert at least one feature has the weight column populated (`features[0].properties[weightCol] !== undefined`). PASS if true.
       - **(c) Visual** — Take a screenshot. Assert the heatmap shows intensity variation (NOT a uniform blob). PASS if visually distinct gradients are visible.

    Acceptance: All three (a)+(b)+(c) PASS → record CONFIRMED in SUMMARY.md.
    If ANY fail → record the failure shape (which assertion, what value seen vs expected, copy of tile URL, screenshot path) AND open a bug todo at `.planning/todos/pending/2026-05-18-heatmap-cols-bug.md` with the failure shape. Do NOT attempt to fix inline — the todo's F1 acceptance is "confirmed live OR bug opened".

    **F3 — Viewer live verify (orchestrator):**
    1. Identify the smoke map with Admin 0 Countries + graduated `pop_est` paint (per the todo, this is the v1011-shipped surface).
    2. Generate a public share link OR an embed token for the map. Use the API: `POST /api/maps/{id}/share-tokens/` for a share link OR `POST /api/maps/{id}/embed-tokens/` for an embed token.
    3. Open the resulting viewer URL (e.g. `/m/<slug>` for public share, `/maps/{id}?embed=1&token=<embed>` for embed).
    4. Zoom out to z=2.
    5. For BOTH the authenticated viewer (`/maps/{id}` with logged-in user) AND the embed-token viewer (`/maps/{id}?embed=1&token=...`):
       - **(a) Visual** — Screenshot at z=2. Assert the graduated colors render correctly (NOT uniform gray). PASS if distinct color bands visible.
       - **(b) Tile URL inspection** — Network log shows tile request URL contains `&cols=pop_est`. PASS if present.
       - **(c) (Optional, time-permitting) Token-refresh** — In dev tools, force-expire the auth token (or wait if you're patient). Assert the next tile request also contains `&cols=pop_est`. This proves the token-refresh path (`ViewerMap.tsx:632-643`) wires the cols param through. PASS if present.

    Acceptance: For each of the two viewer flavors (auth + embed), (a) and (b) must PASS. (c) is best-effort; if not exercised, note it in SUMMARY as "token-refresh path not live-verified — covered by code review of ViewerMap.tsx:632-643". CONFIRMED record if both flavors PASS; otherwise open a bug todo at `.planning/todos/pending/2026-05-18-viewer-cols-bug.md` with the failure shape (which viewer flavor, which assertion, screenshots, network log).

    **SUMMARY.md authoring** (orchestrator):
    After MCP verification, write `.planning/quick/260518-qz1-tile-cols-opt-in-followups-f1-f2-f3/260518-qz1-SUMMARY.md`. Sections:
    - **F1 result:** CONFIRMED (with evidence) or BUG-OPENED (with todo path)
    - **F2 result:** Task 1 + Task 2 outputs (test counts, files added), pytest + vitest green status
    - **F3 result:** CONFIRMED (auth + embed, with evidence) or BUG-OPENED (with todo path), token-refresh note if applicable
    - **F4 acknowledgement:** Documentation-only, no action required (callers in `use-map-layers.ts`, `use-feature-editing.ts`, `use-builder-layers.ts` left intentionally unmigrated per the todo's F4 table; revisit when adding data-driven styling outside builder/viewer)
    - **F5 acknowledgement:** Operational note only — self-heals on next token refresh per the todo
  </how-to-verify>
  <resume-signal>
    After orchestrator completes F1 + F3 MCP verification and writes SUMMARY.md, type "approved" to close this quick.

    If either F1 or F3 produces a BUG (todo opened), the SUMMARY records BUG-OPENED with todo path and this quick is still COMPLETE per the todo's "confirmed live OR bug opened" acceptance for F1 and F3. The bug todos themselves are out of scope for this quick.
  </resume-signal>

  <files>
    .planning/quick/260518-qz1-tile-cols-opt-in-followups-f1-f2-f3/260518-qz1-SUMMARY.md (orchestrator authors after MCP verify),
    .planning/todos/pending/2026-05-18-heatmap-cols-bug.md (conditional — only if F1 fails),
    .planning/todos/pending/2026-05-18-viewer-cols-bug.md (conditional — only if F3 fails)
  </files>
  <action>
    ORCHESTRATOR-OWNED — the executor returns at this checkpoint without running MCP. The orchestrator (Claude Code) drives Playwright MCP per <how-to-verify> above, then writes SUMMARY.md per the section structure listed there. Per planning constraints, the executor CANNOT run Playwright MCP browser tools — only the orchestrator can. This task is a structured handoff, not a unit of executor work.
  </action>
  <verify>
    <human-check>Orchestrator confirms both F1 and F3 are either CONFIRMED (evidence captured in SUMMARY) or BUG-OPENED (todo created with failure shape). The todo's acceptance criterion ("confirmed live OR bug opened") is satisfied either way.</human-check>
  </verify>
  <done>
    SUMMARY.md exists at .planning/quick/260518-qz1-tile-cols-opt-in-followups-f1-f2-f3/260518-qz1-SUMMARY.md with all five sections populated (F1, F2, F3, F4 ack, F5 ack). F1 and F3 each have either CONFIRMED evidence (tile URL snippets, screenshot paths, network log excerpts) or a BUG-OPENED reference to a todo file in .planning/todos/pending/.
  </done>
</task>

</tasks>

<verification>
- Task 1 acceptance: `cd backend && uv run pytest tests/test_tile_cols_endpoint.py tests/test_tile_cache_cols_key.py -xvs` returns 0 with all tests passing.
- Task 2 acceptance: `cd frontend && npm test -- run src/components/builder/__tests__/map-sync.heatmap-cols.test.ts src/lib/__tests__/tile-utils.test.ts` returns 0 with all tests passing.
- Task 3 acceptance: SUMMARY.md exists with F1/F2/F3 sections populated, F4/F5 acknowledged. F1 + F3 each are either CONFIRMED (with evidence references) or BUG-OPENED (with todo path).

Cross-checks (orchestrator runs before close):
- The full test suites still pass (`cd backend && uv run pytest -x` quick smoke; `cd frontend && npm run typecheck && npm test` quick smoke). The new tests should not regress anything; if they do, the regression is the bug and must be addressed inline before close.
- The `c8c9d08f` fix surface is still green — visit z=2 on the smoke map with Admin 0 Countries graduated pop_est in the builder, confirm uniform gray has NOT returned. (This is just sanity — the fix has been live since 2026-05-18; this checks we haven't broken it by touching adjacent test files.)
</verification>

<success_criteria>
- 2 new backend integration test files created and green: `test_tile_cols_endpoint.py`, `test_tile_cache_cols_key.py`
- 1 new frontend unit test file created and green: `map-sync.heatmap-cols.test.ts`
- 1 existing frontend test file extended: `tile-utils.test.ts` with a new `describe('buildSignedTileUrl extraCols edge cases', ...)` block (whitespace-only, falsy-entry, %2C URL-encoding — NOT duplicating the existing extraCols tests at lines 54-79)
- All 4 test files pass on first executor run (or fix-loop-until-green)
- SUMMARY.md exists with F1 + F3 live-verify outcomes captured by orchestrator (either CONFIRMED with evidence or BUG-OPENED with todo path)
- F4 + F5 acknowledged as documentation-only in SUMMARY (no code action)
- Todo file `2026-05-18-tile-cols-followups.md` ready to be moved from `pending/` to `done/` after SUMMARY ships (orchestrator handles this on `/clear`)
</success_criteria>

<output>
Create `.planning/quick/260518-qz1-tile-cols-opt-in-followups-f1-f2-f3/260518-qz1-SUMMARY.md` when done (orchestrator authors this post-MCP-verify).
</output>
</output>
