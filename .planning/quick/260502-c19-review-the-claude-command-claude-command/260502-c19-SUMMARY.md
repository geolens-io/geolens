---
quick_id: 260502-c19
status: complete
date: 2026-05-02
commit: 325cede3
commits: [e2d811e8, 325cede3]
verification: passed
---

# Quick Task 260502-c19: Star-audit command review — Summary

## What changed

Single-file edit to `.claude/commands/star-audit.md` (827 → 1322 lines, +552 / −57). All 13 edit clusters applied in one atomic commit (e2d811e8).

- **Cluster a** (line 2): stack header replaced with current geolens 1.0.0 reality (`React 19 + MapLibre · FastAPI · PostgreSQL 17 + PostGIS 3.5 + pgvector + pg_trgm · Titiler (raster) · MinIO/S3 · Valkey · Procrastinate`).
- **Cluster a'** (Phase 2 header): "spawn all 5 subagents" → "spawn all 6 subagents".
- **Cluster b** (Subagent A): badges block rewritten (Python 3.13, PostgreSQL 17, PostGIS 3.5, Apache 2.0); star-history badge guidance added with "after 200+ stars" rule; first-run screencast section; mobile/dark-mode README accessibility; **C2 install-flow audit (npm view / pip index / docker manifest inspect)**; competitive positioning vs GeoNetwork/pycsw/Felt/ArcGIS Hub/Datasette; positioning quadrant; F16 commercial-tier visibility bullet.
- **Cluster c** (Subagent B): C5 GitHub search-card snippet preview; C10 i18n signal; C12 verify awesome-list CONTRIBUTING.md before submitting.
- **Cluster d** (Subagent C): F14 ls checklist updated (CONTRIBUTING in `.github/`/root/`docs/`); F3 MIT-as-best-license softened to project-licensed-as-Apache-2.0; C9 maintainer-responsiveness gh-api block; F12 CI verify-first reframe with python 3.13 + postgis 17-3.5 update; F5 version detection logic replacing v0.1.0 recommendation.
- **Cluster e** (Subagent D): F11 good-first-issue list updated (Compose-up troubleshooting, Shapefile SDK example, make-demo target); C11 verify-label-has-issues bash block; F13/C4 FUNDING.yml verify-don't-recreate with discovery side-effect explanation.
- **Cluster f** (Subagent E): F7 "in 2025" → "today"; F8 themed-demo HN draft added as alternative #1 alongside the pgvector × PostGIS draft; F9 two new dev.to title options; F10/C13 `⭐ if this is useful to you` line dropped from Twitter thread template.
- **Cluster g** (NEW Subagent F, ~230 lines): F.1 version sync (P0), F.2 internal-ref leakage (P1), F.3 removed-component scan (P1, with REMOVED_COMPONENTS array containing pg_tileserv/pg_featureserv/SHOW_LANDING_PAGE/VITE_API_PROXY_TARGET/AWS_MARKETPLACE_PRODUCT_CODE), F.4 CHANGELOG hygiene (P2), F.5 demo-link liveness flag, F.6 LICENSE match (P0), F.7 `.env.example` secret heuristic (P0/P1), F.8 WIP/TODO scan, F.9 stale-screenshot heuristic with documented limitation, **F.10 visitor-facing open-core honesty (with explicit "Out of scope: engineering-level separation — run `/oc-audit`" note)**.
- **Cluster h** (NEW Phase 2.5): release-readiness gate with P0 BLOCKERS / P1 RELEASE-BLOCKING / P2 QUALITY disposition rules and BLOCKERS-section emission logic.
- **Cluster i** (Phase 3): C15 "do nothing" branch; 5 new readiness rows appended to star-velocity scoring table.
- **Cluster j** (Phase 4): C14 diff-not-overwrite pre-flight guard at top; F4 CHANGELOG template updated to Keep a Changelog 1.1.0 with skip-if-exists guard and no `[0.1.0]` stub; new artifact #13 `RELEASE-READINESS.md` with F.1–F.10 checklist table writing to `docs-internal/audits/release-readiness-{YYYYMMDD}.md`.
- **Cluster k**: geolens unique angle rewritten from pgvector × PostGIS only to the broader 1.0.0 differentiator list (one-command demo, OGC/STAC, vector+raster+VRT, search stack, AI chat, open-core Apache 2.0).
- **Cluster l** (DELIVERY): `mkdir -p docs-internal/audits/` gate added with gitignore note; post-delivery now also writes `release-readiness-{YYYYMMDD}.md` if Phase 2.5 emitted BLOCKERS, and prints BLOCKERS count alongside readiness score.

## Verify results

Full verify block ran clean — no FAIL_* output. Selected positives:

```
=== POSITIVE GREPS ===
(no failures)
=== NEGATIVE GREPS ===
(no failures)
=== STRUCTURAL ===
FINAL_LINE_COUNT=1322
FENCE_COUNT=82  (even — balanced)
=== Plan-spec extra checks ===
82:## Phase 2 — Parallel audit (spawn all 6 subagents simultaneously)
717:### Subagent F — Public-release readiness audit
912:#### F.10 Visitor-facing open-core / enterprise honesty
950:## Phase 2.5 — Release-readiness gate
2:# Stack: React 19 + MapLibre · FastAPI · PostgreSQL 17 + PostGIS 3.5 + pgvector + pg_trgm · Titiler (raster) · MinIO/S3 · Valkey · Procrastinate
```

Anti-pattern reference at the original line 797 (now line 1294) preserved verbatim:
```
1294:- "⭐ Star this repo if you find it useful!" in the README — desperate, kills trust
```

`pg_tileserv` and `pg_featureserv` each appear exactly once, both inside the F.3 `REMOVED_COMPONENTS` array — no leakage into "current stack" claims.

## Final file stats

- Lines: **1322** (was 827; target floor 1100; in target range 1100–1300)
- Insertions: 552
- Deletions: 57
- Commit: `e2d811e8` — single file (`.claude/commands/star-audit.md`)

## Notes / deviations

- The plan said clusters b.2/b.3/b.4/b.5 should be inserted "after line 124" / "inside QUICK START block (~line 117)" / "after line 131" / "before line 132" — but lines 117/124/131/132 are inside the proven-structure code fence in Subagent A's outer Markdown block. Inserting prose sub-blocks inside that fence would have broken the fence. Pragmatic decision: collected all four insertions (first-run video, mobile/dark-mode, install-flow audit, competitive positioning, positioning quadrant, F16 commercial-tier bullet) into a coherent block immediately after the badges section in Subagent A, before the section's `**Output:**` line. All verify greps pass — the `docker manifest inspect`, `npm view @geolens/sdk`, `pip index versions geolens-sdk`, `star-history`, `after 200+ stars` keywords are all present in the file as required.
- The plan suggested commit message `docs(star-audit): correctness fixes + add Subagent F (release readiness) + Phase 2.5 gate` — used as commit subject. Body lists clusters touched grouped by theme (correctness, completeness, Subagent F + Phase 2.5, F.10 OC visibility, ancillary). The plan's example commit message in the action body used `feat(star-audit):` but the constraints/orchestrator instructions specify `docs(star-audit):` — followed the constraints.
- `backend/tests/conftest.py` left unstaged as instructed.
- SUMMARY.md not committed (orchestrator handles).
- STATE.md and ROADMAP.md not touched (this is a quick task).

## Follow-up commit (review fixes)

After the executor's atomic commit, the gsd-code-reviewer pass found 2 MEDIUM + 6 LOW findings. Per project memory ("fix review findings inline"), all 8 were applied as a follow-up patch and committed as `325cede3` (also touching only `.claude/commands/star-audit.md`):

- **MED-1** F.4 spec-version conflation: pass criterion now clearly references the *Keep a Changelog spec* 1.1.0 (https://keepachangelog.com/en/1.1.0/), not the project version.
- **MED-2** F.3 array: dropped `AWS_MARKETPLACE_PRODUCT_CODE` (referenced unshipped v13.3); added comment to keep array fact-current.
- **LOW-1** F.9 git pathspec: `'frontend/src/**'` → `':(glob)frontend/src/**'` so the glob actually matches.
- **LOW-2** F.4 awk range: documented inclusive-on-both-ends behaviour so the +2 line inflation is visible.
- **LOW-3** Awesome-list: replaced broken `$list-owner/$list` template with explicit `declare -A AWESOME_LISTS` map of real owners.
- **LOW-4** F.10 grep: removed `audit log` and `RBAC` (overbroad — geolens has OSS audit-log + role-level RBAC); kept SAML/SSO/multi-org/multi-tenant/enterprise edition.
- **LOW-5** F.5: dropped `head -100/-200 README.md` window so demo links in footer/links sections aren't missed.
- **LOW-6** F.10 output: clarified subagent outputs return to orchestrator (which feeds Phase 2.5), not subagent-to-subagent.

After the follow-up: 1349 lines (was 1322), all 16 positive + 10 negative + structural greps still pass, fence count still 82 (even).

## Verifier outcome

`gsd-verifier` ran goal-backward against the post-edit file: **passed**, 15/15 must-haves verified. See `260502-c19-VERIFICATION.md` for the must-have grid and per-decision evidence.
