---
quick_id: 260502-c19
description: Review .claude/commands/star-audit.md for completeness, correctness, and public-release readiness
gathered: 2026-05-02
status: ready-for-planning
---

# Quick Task 260502-c19: Star-audit command review — Context

<domain>
## Task Boundary

The slash command `.claude/commands/star-audit.md` is a meta tool that audits the geolens repo for star-growth friction. The user wants a meta-review of THE COMMAND ITSELF to ensure:

1. **Complete** — covers everything a star-growth audit should cover
2. **Correct** — geolens-specific facts are accurate (the file was authored before 1.0.0 reset and several stack changes)
3. **Public-release readiness** — the command should also surface and validate launch-readiness signals: version sync, leftover internal artifacts, demo liveness, CHANGELOG hygiene, etc.

In-scope file: `.claude/commands/star-audit.md` (single edit target).
Out-of-scope: actually running the audit, changes to README/repo, changes to other slash commands.
</domain>

<decisions>
## Implementation Decisions (locked — do not revisit)

### 1. Output mode: Edit star-audit.md in place
- One atomic commit modifying `.claude/commands/star-audit.md`
- No separate findings document
- All review insights are encoded as edits to the command itself

### 2. Release scope: Repo-surface readiness
- Public-release readiness checks target what a visitor or launch reviewer notices, NOT release-machinery wiring
- Concrete checks to add:
  - Version sync (README badges, `frontend/package.json`, `backend/pyproject.toml`, `CHANGELOG.md` `[1.0.0]` entry, git tag) all agree
  - Leftover internal references (e.g. `docs-internal/`, `.planning/` paths in user-facing docs, `gsd-*` mentions, internal admin URLs)
  - Removed-component references (e.g. README/docs still mentioning `pg_tileserv` or `pg_featureserv` after their removal)
  - CHANGELOG.md hygiene: `[Unreleased]` doesn't have a stale dated section pretending to be unreleased; latest tagged version present; entries follow Keep a Changelog
  - Demo link liveness (if demo URL is referenced — note that liveness can't be checked from filesystem, must be flagged for human verification)
  - LICENSE present and matches what README claims
  - `.env.example` exists and doesn't contain real secrets
  - No "Coming soon", "WIP", "TODO" in user-facing files (README, top-level docs/)
  - Old screenshots/GIFs that show removed UI/components

### 3. Geolens facts: Update to current state
- Stack lines must reflect: PostGIS + pgvector + pg_trgm + Titiler (raster); pg_tileserv removed (v7.0); pg_featureserv removed (v2.2)
- Acknowledge 1.0.0 release on 2026-04-01 (versions reset from 13.x → 1.0.0)
- Latest milestone shipped: v13.2 Edition Lifecycle Hardening (2026-04-30)
- Open-core boundary should be acknowledged briefly (single-org free / single-tenant Enterprise paid) — but the command itself is community/star-focused so don't dwell on commercial positioning
- Keep the geo+vector unique-angle framing intact; that pitch still holds

### 4. Structure: New Subagent F + new Phase 2.5
- Add Subagent F: Public-Release Readiness as a 6th parallel auditor in Phase 2 (alongside A–E)
- Add Phase 2.5 — Release-readiness gate that synthesizes Subagent F output and flags BLOCKERS that must be fixed before any star-growth work (you don't promote a repo whose README still references removed services)
- Update Phase 4 (`--write` content) to include readiness artifacts (e.g. release-readiness checklist file)
- Update star-velocity scoring table to include readiness items

### 5. Open-core / enterprise split checks (added mid-flow per user question 2026-05-02)
- Star-audit should NOT duplicate `/oc-audit` (which handles engineering-level OC separation in depth)
- Star-audit should add a NARROW visitor-facing OC check inside Subagent F as F.10:
  - Honesty in feature lists — enterprise-only features marked `(Enterprise)` so community visitors don't bounce trying things that don't exist in OSS
  - Broken cross-repo refs — links/screenshots that point at `geolens-enterprise` paths the public repo doesn't have
  - Pricing/upsell tone — one understated link, never a banner
  - Explicit "out of scope: engineering-level separation — run `/oc-audit`" note in F.10 prompt

### Claude's Discretion
- Exact wording of new Subagent F prompts and check-bash blocks
- Whether to renumber phases or keep "2.5" — keep 2.5 for minimal diff
- Decision on whether Subagent F output BLOCKS the report or just adds a "Public-Release Readiness" section — prefer: it adds findings to the prioritised plan and surfaces a top-level BLOCKERS section if any P0 readiness issue is found
- Trim/sharpen any redundant sections discovered during the edit (e.g. duplicated badge advice) — minor cleanup ok, no major restructuring

</decisions>

<specifics>
## Specific References

- Current file: `.claude/commands/star-audit.md` (827 lines as of 2026-05-02)
- Geolens current state: see CLAUDE.md memory — stack, removed services, milestone status
- Recent reference for accurate facts:
  - `MEMORY.md` lines on shipped milestones (v6.0 → v13.2 chronology, 1.0.0 reset)
  - `MEMORY.md` "Project Stack": pg_tileserv removed (v7.0), pg_featureserv removed (v2.2), Titiler used for raster
- Open-core strategy: `feedback_no_security_by_obscurity.md` and project_oc_strategy memory
</specifics>

<canonical_refs>
## Canonical References

- Keep a Changelog 1.0.0 — https://keepachangelog.com/en/1.0.0/
- Semantic Versioning 2.0.0 — https://semver.org/
- GitHub Topics best practices — https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/classifying-your-repository-with-topics
- README anti-patterns reference (already cited in current command) — keep
</canonical_refs>
