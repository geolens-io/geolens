---
phase: quick-260324-qu5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/records/service.py
  - backend/app/datasets/service.py
  - backend/tests/test_ingest.py
  - frontend/src/components/dataset/DatasetMap.tsx
  - frontend/src/components/dataset/__tests__/DatasetMap.test.tsx
  - e2e/non-spatial.spec.ts
  - e2e/fixtures/sample-nonspatial.csv
autonomous: true
requirements: [NS-TEST-01, NS-BUG-01, NS-BUG-02]

must_haves:
  truths:
    - "Non-spatial datasets produce only csv download + ogc_features distributions (no vector_tiles, gpkg, geojson, shp)"
    - "DatasetMap returns early when geometryType is null, never adding vector tile sources"
    - "Backend tests verify distribution filtering for both CSV-derived and XLSX-derived non-spatial tables"
    - "Frontend Vitest test verifies DatasetMap renders safely with null geometryType"
    - "Playwright E2E tests verify upload of non-spatial CSV, dataset page rendering, and attribute table"
  artifacts:
    - path: "backend/app/records/service.py"
      provides: "generate_distributions with geometry_type filter"
      contains: "geometry_type"
    - path: "backend/app/datasets/service.py"
      provides: "Passes geometry_type to generate_distributions"
      contains: "geometry_type=geometry_type"
    - path: "backend/tests/test_ingest.py"
      provides: "Non-spatial distribution and query tests for CSV and XLSX"
      contains: "test_non_spatial_distributions"
    - path: "frontend/src/components/dataset/DatasetMap.tsx"
      provides: "Early return guard for null geometryType"
      contains: "geometryType"
    - path: "frontend/src/components/dataset/__tests__/DatasetMap.test.tsx"
      provides: "Non-spatial DatasetMap rendering test"
      contains: "non-spatial"
    - path: "e2e/non-spatial.spec.ts"
      provides: "Playwright E2E tests for non-spatial upload and dataset page"
      contains: "non-spatial"
  key_links:
    - from: "backend/app/datasets/service.py"
      to: "backend/app/records/service.py"
      via: "generate_distributions(session, dataset.id, record.id, table_name, geometry_type=geometry_type)"
      pattern: "generate_distributions.*geometry_type"
    - from: "backend/app/records/service.py"
      to: "_DISTRIBUTION_TEMPLATES"
      via: "conditional filtering based on geometry_type param"
      pattern: "geometry_type is None"
---

<objective>
Test non-spatial data support end-to-end and fix two confirmed bugs: (1) vector tile + spatial format distributions generated for non-spatial datasets, (2) DatasetMap missing guard for null geometryType.

Purpose: Non-spatial CSV/XLSX datasets should only produce appropriate distributions (csv + ogc_features) and the frontend should handle null geometry gracefully.
Output: Bug fixes in distribution generation and DatasetMap, plus comprehensive backend tests (CSV + XLSX), frontend Vitest tests, and Playwright E2E tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@backend/app/records/service.py (lines 270-408 — distribution generation)
@backend/app/datasets/service.py (lines 170-230 — register_dataset calls generate_distributions)
@backend/tests/test_ingest.py (lines 439-504 — existing non-spatial test)
@backend/tests/test_export.py (lines 286-323 — existing non-spatial export tests)
@frontend/src/components/dataset/DatasetMap.tsx (lines 42-50 props, 428-470 addVectorLayers)
@frontend/src/components/dataset/__tests__/DatasetMap.test.tsx
@e2e/upload.spec.ts (Playwright upload pattern)
@e2e/dataset-detail.spec.ts (Playwright dataset detail pattern)
@playwright.config.ts (E2E config — auth setup, storageState)

<interfaces>
From backend/app/records/service.py:
```python
async def generate_distributions(
    session: AsyncSession,
    dataset_id: uuid.UUID,
    record_id: uuid.UUID,
    table_name: str,
) -> list[RecordDistribution]:
```

From backend/app/datasets/service.py (line 211):
```python
await generate_distributions(session, dataset.id, record.id, table_name)
# Note: geometry_type is already in scope at this call site
```

From frontend DatasetMap props:
```typescript
interface DatasetMapProps {
  bbox: [number, number, number, number] | null;
  tableName: string | null;
  geometryType: string | null;
  datasetId?: string;
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix distribution generation for non-spatial datasets + backend tests (CSV and XLSX)</name>
  <files>backend/app/records/service.py, backend/app/datasets/service.py, backend/tests/test_ingest.py</files>
  <behavior>
    - Test: Registering a non-spatial CSV-derived table produces ONLY csv download + ogc_features distributions (not gpkg, geojson, shp, or vector_tiles)
    - Test: Registering a non-spatial XLSX-derived table produces ONLY csv download + ogc_features distributions (2-distribution constraint identical to CSV)
    - Test: Registering a spatial table still produces all 6 distributions (existing behavior preserved)
    - Test: OGC /collections/{id}/items returns features with geometry:null for non-spatial dataset
  </behavior>
  <action>
1. In `backend/app/records/service.py`, add `geometry_type: str | None = None` parameter to `generate_distributions()`.

2. Inside `generate_distributions`, when `geometry_type is None`:
   - When iterating `_DISTRIBUTION_TEMPLATES` (the loop that creates download distributions), only keep entries where `dist_type == "download" and fmt == "csv"` or `dist_type == "ogc_features"`. This means gpkg, geojson, and shp download entries are skipped entirely for non-spatial datasets.
   - Skip the **separate vector_tiles block** (the code at approximately lines 383-404 that creates the vector tile distribution entry with `distribution_type="vector_tiles"`). This block is NOT part of `_DISTRIBUTION_TEMPLATES` — it is a standalone code section below the template loop. When `geometry_type is None`, do not execute this block at all.
   - When `geometry_type` is NOT None (i.e., spatial dataset), keep ALL existing behavior unchanged — all template entries processed, vector_tiles block executed.

3. In `backend/app/datasets/service.py` line 211, pass the `geometry_type` kwarg:
   ```python
   await generate_distributions(session, dataset.id, record.id, table_name, geometry_type=geometry_type)
   ```

4. In `backend/tests/test_ingest.py`, add three new test methods to the existing `TestCsvNonSpatialPipeline` class after `test_csv_non_spatial_full_pipeline`:

   a. `test_non_spatial_csv_distributions`: Register a non-spatial table via `/ingest/register`, then GET `/datasets/{id}` and inspect the `distributions` array on the record. Assert only 2 distributions exist: one with `distribution_type="download", format="csv"` and one with `distribution_type="ogc_features"`. Assert NO vector_tiles, gpkg, geojson, or shp distributions. Follow existing pattern: create table in `data` schema with `gid serial PRIMARY KEY, name text, value integer`, register, assert, cleanup in `finally`.

   b. `test_non_spatial_xlsx_distributions`: Identical to (a) but with a differently-named table (e.g., `test_xlsx_nonspatial`) to represent an XLSX-derived table. The table structure is the same (non-spatial tables from XLSX have same schema after ogr2ogr). Register via `/ingest/register` with `title="Test XLSX Table"`. Assert the same 2-distribution constraint: only csv download + ogc_features. This confirms the geometry_type filter works regardless of original source format.

   c. `test_non_spatial_ogc_items`: Register a non-spatial table, GET `/collections/{dataset_id}/items`, assert 200, assert each feature has `geometry: null`, assert `numberMatched` matches row count (2).

   All tests follow the existing pattern: create table via SQL in `data` schema, register via `/ingest/register`, assert, cleanup in `finally` block.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && docker compose exec -T api python -m pytest tests/test_ingest.py::TestCsvNonSpatialPipeline -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>Non-spatial datasets get only csv+ogc_features distributions regardless of source format (CSV or XLSX). Spatial datasets unchanged. Four tests pass: CSV distribution filtering, XLSX distribution filtering, OGC items with null geometry, and the existing full pipeline test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix DatasetMap null geometryType guard + Vitest test</name>
  <files>frontend/src/components/dataset/DatasetMap.tsx, frontend/src/components/dataset/__tests__/DatasetMap.test.tsx</files>
  <behavior>
    - Test: DatasetMap renders shell with role="region" when geometryType is null (no crash)
    - Test: DatasetMap does NOT show edit trigger, zoom-to-extent, or nav-control when geometryType is null and tableName is null (non-spatial path)
  </behavior>
  <action>
1. In `frontend/src/components/dataset/DatasetMap.tsx`, add an early return guard in the `addVectorLayers` callback (line ~428-430). After the existing `if (!tableName || vectorLayersAdded.current) return;` check, add:
   ```typescript
   if (!geometryType) return;
   ```
   This prevents vector tile sources from being added when geometryType is null, even if the component somehow receives a tableName.

2. In `frontend/src/components/dataset/__tests__/DatasetMap.test.tsx`, add a new describe block `'DatasetMap non-spatial behavior'` with two tests:

   a. `'renders shell without crash when geometryType is null'`: Render `<DatasetMap bbox={null} tableName={null} geometryType={null} />`. Assert `screen.getByTestId('dataset-map-shell')` exists and has `role="region"`.

   b. `'does not show edit trigger or zoom for non-spatial dataset'`: Render `<DatasetMap bbox={null} tableName={null} geometryType={null} datasetId="ds-1" canEdit />`. Assert `queryByTestId('dataset-map-edit-trigger')` is null. Assert `queryByTitle('Zoom to dataset extent')` is null.

Follow existing mock patterns already in the test file (all mocks are file-level, no additional mocks needed).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npx vitest run src/components/dataset/__tests__/DatasetMap.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>DatasetMap safely handles null geometryType with early return guard. Two new Vitest tests pass confirming non-spatial rendering behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright E2E tests for non-spatial upload and dataset page</name>
  <files>e2e/non-spatial.spec.ts, e2e/fixtures/sample-nonspatial.csv</files>
  <action>
1. Create `e2e/fixtures/sample-nonspatial.csv` with simple tabular data (no geometry):
   ```csv
   name,value,category
   Alice,100,A
   Bob,200,B
   Charlie,300,A
   ```

2. Create `e2e/non-spatial.spec.ts` with three Playwright tests following existing patterns from `upload.spec.ts` and `dataset-detail.spec.ts`. The spec uses the authenticated `chromium` project (storageState from auth.setup.ts):

   a. `'upload non-spatial CSV and complete ingestion'`:
      - Navigate to `/import`
      - Upload `sample-nonspatial.csv` via `input[type="file"]` (same pattern as upload.spec.ts)
      - Wait for preview to show "sample-nonspatial" text (timeout 30s)
      - Click Import/Commit button
      - Wait for success/complete indication (timeout 30s)
      - Store the dataset URL from the success page for subsequent tests

   b. `'dataset page shows graceful non-spatial state'`:
      - After upload, navigate to the dataset detail page (use search: `/?q=sample-nonspatial`, click dataset link — same pattern as dataset-detail.spec.ts `openAdminCountriesDataset`)
      - Assert the page loads without errors (heading or title visible)
      - Assert no vector tile map errors visible (no error toast, no broken map indicators)
      - Assert the dataset metadata section shows `geometry_type` as null/none or the map area shows a non-spatial indicator

   c. `'attribute table shows rows for non-spatial dataset'`:
      - On the same dataset page, locate and open the attribute table (look for "Attributes" tab/button or table rows)
      - Assert at least 3 rows visible (Alice, Bob, Charlie)
      - Assert column headers include "name", "value", "category"

   Use `test.describe.serial` to ensure tests run in order (upload first, then verify). Mark the upload test as `test.slow()` since ingestion takes time.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && npx playwright test e2e/non-spatial.spec.ts --project=chromium 2>&1 | tail -20</automated>
  </verify>
  <done>Three Playwright E2E tests pass: non-spatial CSV upload completes, dataset page renders gracefully without map errors, attribute table displays all rows and columns.</done>
</task>

</tasks>

<verification>
1. Backend: `docker compose exec -T api python -m pytest tests/test_ingest.py -x -v -k "non_spatial"` -- all non-spatial tests pass (CSV + XLSX distributions, OGC items)
2. Frontend: `cd frontend && npx vitest run src/components/dataset/__tests__/DatasetMap.test.tsx` -- all tests pass including new non-spatial tests
3. E2E: `npx playwright test e2e/non-spatial.spec.ts --project=chromium` -- upload, dataset page, and attribute table tests pass
4. Existing tests: `docker compose exec -T api python -m pytest tests/test_export.py -x -v -k "non_spatial"` -- existing export tests still pass
</verification>

<success_criteria>
- generate_distributions accepts geometry_type and filters appropriately for non-spatial
- Non-spatial datasets produce exactly 2 distributions (csv download + ogc_features) for both CSV and XLSX sources
- Spatial datasets still produce all 6 distributions (no regression)
- DatasetMap has early return guard for null geometryType in addVectorLayers
- Playwright E2E tests verify full upload-to-viewing flow for non-spatial CSV
- All new and existing tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/260324-qu5-test-non-spatial-data-support-end-to-end/260324-qu5-SUMMARY.md`
</output>
