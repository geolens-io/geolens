---
phase: quick-260324-inspect-the-search-page-with-playwright-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/SearchPage.tsx
  - frontend/src/components/search/SearchBar.tsx
  - frontend/src/components/search/FilterPanel.tsx
  - frontend/src/components/search/SavedSearches.tsx
  - frontend/src/components/search/SearchResultCard.tsx
  - frontend/src/components/search/DatasetCardSkeleton.tsx
  - e2e/search.spec.ts
autonomous: true
requirements: [QUICK-260324-SEARCH-UX]

must_haves:
  truths:
    - "Implementation starts with a live Playwright inspection of the authenticated search page on the running GeoLens stack."
    - "The redesign stays inside the existing GeoLens design system and code patterns; no new dependencies are added."
    - "Search result cards are the primary UX target and must become easier to scan than the current dense title/metadata/preview stack."
    - "The search experience must remain responsive on mobile and desktop without horizontal overflow."
  artifacts:
    - path: "frontend/src/pages/SearchPage.tsx"
      provides: "search shell, hero/sticky transition, result list, loading/empty states"
    - path: "frontend/src/components/search/SearchResultCard.tsx"
      provides: "primary result-card layout, metadata hierarchy, preview presentation"
    - path: "frontend/src/components/search/DatasetCardSkeleton.tsx"
      provides: "loading-state parity for the card redesign"
    - path: "e2e/search.spec.ts"
      provides: "authenticated search-flow regression coverage"
  key_links:
    - from: "frontend/src/pages/SearchPage.tsx"
      to: "SearchBar / FilterPanel / SavedSearches / SearchResultCard"
      via: "landing hero and sticky browse header"
    - from: "frontend/src/components/search/SearchResultCard.tsx"
      to: "BBoxPreview / quicklook / provenance metadata"
      via: "card content and preview split"
    - from: "frontend/src/components/search/FilterPanel.tsx"
      to: "saved searches, facet chips, mobile/desktop filter toolbar"
      via: "browse-mode control surface"
---

<objective>
Modernize the GeoLens search UI/UX with Playwright-backed inspection first, then a focused card-first polish pass.

The end state should feel more intuitive and elegant while preserving the existing search architecture:
- clearer hierarchy between query controls, filters, saved searches, and results
- result cards that scan faster at a glance
- loading and empty states that match the new visual rhythm
- targeted e2e coverage for the revised search flow
</objective>

<tasks>
  <task type="auto">
    <name>Task 1: Playwright audit the current authenticated search experience</name>
    <files>frontend/src/pages/SearchPage.tsx, frontend/src/components/search/SearchResultCard.tsx, frontend/src/components/search/FilterPanel.tsx, frontend/src/components/search/SavedSearches.tsx, e2e/search.spec.ts</files>
    <action>
Use Playwright MCP against the running Docker stack on `http://localhost:8080` with the seeded admin login to inspect the current search landing and browse states before editing.

Capture the current layout behavior that matters for the redesign:
- hero versus sticky-search transition
- density of the filter bar and saved-search row
- result-card scanability, especially title, metadata, preview, and mobile collapse behavior
- any friction in the authenticated search flow that should be preserved or simplified

Write down the concrete UX target before touching code so the implementation stays grounded in the live page, not just the source.
    </action>
    <verify>
Record a short audit note in the task summary and confirm the page is reachable in Playwright as an authenticated user before any edits begin.
    </verify>
    <done>Authenticated Playwright inspection is complete and the redesign target is explicit enough to guide the implementation tasks.</done>
  </task>

  <task type="auto">
    <name>Task 2: Tighten the search shell and control hierarchy</name>
    <files>frontend/src/pages/SearchPage.tsx, frontend/src/components/search/SearchBar.tsx, frontend/src/components/search/FilterPanel.tsx, frontend/src/components/search/SavedSearches.tsx</files>
    <action>
Refine the top-level search experience so the page feels more deliberate and less crowded while keeping the existing search flow intact.

Focus on:
- making the landing hero, sticky search bar, and browse mode feel like one coherent experience
- reducing vertical clutter in the filter/search area
- improving how saved searches and active filters read as secondary affordances
- keeping the mobile filter surface compact, readable, and touch-friendly

Use existing GeoLens components and tokens only. Do not introduce new layout primitives or dependencies.
    </action>
    <verify>
Run targeted frontend linting on the touched search-shell files and confirm the page still renders and filters still open in the browser.
    </verify>
    <done>The search shell reads as a cleaner, more intentional control surface without regressing the current filtering or saved-search behavior.</done>
  </task>

  <task type="auto">
    <name>Task 3: Redesign result cards, skeletons, and search-flow regression coverage</name>
    <files>frontend/src/components/search/SearchResultCard.tsx, frontend/src/components/search/DatasetCardSkeleton.tsx, e2e/search.spec.ts</files>
    <action>
Rework the result cards so the list is easier to scan and the preview area supports the content instead of competing with it.

Emphasize:
- stronger title and metadata hierarchy
- clearer distinction between collections, vectors, rasters, and VRTs
- more intentional quicklook or preview treatment
- better spacing and truncation so cards feel lighter without losing useful metadata
- a skeleton that mirrors the new card proportions and hierarchy

Update the Playwright search spec to cover the intended search flow and guard the card/list experience after the redesign.
    </action>
    <verify>
Run `npm --prefix frontend run lint -- frontend/src/components/search/SearchResultCard.tsx frontend/src/components/search/DatasetCardSkeleton.tsx e2e/search.spec.ts` and `npx playwright test e2e/search.spec.ts --project=chromium` after the implementation lands.
    </verify>
    <done>Result cards and loading states present a more modern, intuitive hierarchy, and the search e2e test protects the new experience.</done>
  </task>
</tasks>

