---
phase: 260425-sl1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - backend/tests/test_stac_record_output.py
  - backend/app/modules/catalog/search/service.py
  - backend/tests/test_ai_chat.py
  - backend/tests/test_chat_streaming.py
  - backend/tests/test_ogc_collection_metadata.py
  - backend/tests/test_ogc_features.py
  - backend/tests/test_search.py
  - backend/tests/test_public_urls.py
autonomous: true
requirements:
  - AUDIT-20260425-CLUSTER-1
  - AUDIT-20260425-CLUSTER-2
  - AUDIT-20260425-CLUSTER-3
  - AUDIT-20260425-CLUSTER-4
  - AUDIT-20260425-CLUSTER-5
  - AUDIT-20260425-CLUSTER-6
tags:
  - test-debt
  - backend
  - pytest
  - audit-20260425

must_haves:
  truths:
    - "test_datetime_falls_back_to_created_at_when_no_temporal asserts properties.datetime equals record.created_at ISO 8601 with Z suffix (cluster 1)"
    - "test_raster_record_no_stac_extensions and test_no_bands_without_band_info both pass — dataset_to_ogc_record does NOT emit stac_extensions for raster records (cluster 2)"
    - "test_validate_overwrites_client_table_name and test_validate_filters_inaccessible_dataset unpack _validate_chat_layers as (validated, _basemap) tuple (cluster 3a)"
    - "All 5 mocked _validate_chat_layers patches in test_chat_streaming.py return tuple (layers, None) instead of bare list (cluster 3b)"
    - "Three OGC catalog tests pass ?limit=200 to /collections endpoint to defeat pagination pollution (cluster 4)"
    - "freezegun>=1.5.0 installed in backend/pyproject.toml dev group; api container rebuilt; freezegun importable inside container (cluster 5 prereq) — OR cluster-5-xfail-fallback equivalent if Task 4 install was blocked"
    - "test_search_filter_by_date_range wraps date arithmetic in freeze_time(datetime.now(timezone.utc)) context (cluster 5) — OR cluster-5-xfail-fallback equivalent if Task 4 install was blocked"
    - "test_load_public_url_overrides_unwraps_json_values resets public_urls._PUBLIC_URL_CACHE to None at top of test body (cluster 6)"
    - "Full backend pytest suite reports 0 unexpected failures after all per-cluster fixes (or any remaining failure is xfail-with-traceable-reason)"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "freezegun dev dependency declaration"
      contains: "freezegun"
    - path: "backend/uv.lock"
      provides: "Locked freezegun version graph"
      contains: "freezegun"
    - path: "backend/tests/test_stac_record_output.py"
      provides: "Renamed cluster-1 test + cluster-2 raster tests passing"
      contains: "test_datetime_falls_back_to_created_at_when_no_temporal"
    - path: "backend/app/modules/catalog/search/service.py"
      provides: "dataset_to_ogc_record no longer leaks stac_extensions on raster path"
    - path: "backend/tests/test_ai_chat.py"
      provides: "Tuple-unpack fixes for cluster 3a"
      contains: "_basemap"
    - path: "backend/tests/test_chat_streaming.py"
      provides: "AsyncMock return tuple fixes for cluster 3b"
      contains: "return_value=(CHAT_BODY"
    - path: "backend/tests/test_ogc_collection_metadata.py"
      provides: "?limit=200 query param on cluster-4 GETs"
      contains: 'params={"limit": 200}'
    - path: "backend/tests/test_ogc_features.py"
      provides: "?limit=200 query param on cluster-4 GET"
      contains: 'params={"limit": 200}'
    - path: "backend/tests/test_search.py"
      provides: "freeze_time-wrapped cluster-5 date-range test"
      contains: "from freezegun import freeze_time"
    - path: "backend/tests/test_public_urls.py"
      provides: "Cluster-6 cache-reset one-liner"
      contains: "_PUBLIC_URL_CACHE = None"
  key_links:
    - from: "backend/tests/test_stac_record_output.py::test_datetime_falls_back_to_created_at_when_no_temporal"
      to: "backend/app/modules/catalog/search/service.py:1051-1057"
      via: "intentional created_at fallback in serializer"
      pattern: "created_at.isoformat"
    - from: "backend/tests/test_chat_streaming.py mocks"
      to: "backend/app/processing/ai/router.py::_validate_chat_layers"
      via: "tuple[list[ChatMapLayer], str | None] return signature"
      pattern: "_validate_chat_layers"
    - from: "backend/tests/test_search.py::test_search_filter_by_date_range"
      to: "backend/pyproject.toml [dependency-groups].dev"
      via: "freezegun import requires installed package"
      pattern: "freezegun"
    - from: "backend/tests/test_public_urls.py::test_load_public_url_overrides_unwraps_json_values"
      to: "backend/app/core/public_urls.py:166"
      via: "module-global _PUBLIC_URL_CACHE short-circuits AsyncMock"
      pattern: "_PUBLIC_URL_CACHE"
---

<objective>
Restore the backend pytest green-baseline by addressing all 15 failures from `docs-internal/audits/test-debt-backend-20260425.md`. The researcher root-caused 14 of 15 failures with concrete, low-LOC fixes; only the final scope-safety valve (xfail) remains as a per-task fallback if a fix unexpectedly explodes.

Purpose: Future audits' green-baseline gates (used by `/post-impl`, `/quick --full` review, etc.) keep getting masked by these 15 known failures. Closing them lets future audits surface real new regressions instead of getting stuck on accumulated debt.

Output: 9 files modified across 6 clusters, `freezegun` added to dev deps, api container rebuilt, full backend pytest run with 0 unexpected failures recorded in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260425-sl1-address-the-debt-in-docs-internal-audits/260425-sl1-CONTEXT.md
@.planning/quick/260425-sl1-address-the-debt-in-docs-internal-audits/260425-sl1-RESEARCH.md
@docs-internal/audits/test-debt-backend-20260425.md

<interfaces>
<!-- Key signatures the executor needs without re-exploring the codebase. -->

From backend/app/processing/ai/router.py (referenced in cluster 3):
```python
async def _validate_chat_layers(
    session: AsyncSession,
    user: User,
    map_id: str,
    layers: list[ChatMapLayer],
) -> tuple[list[ChatMapLayer], str | None]:
    """Returns (validated_layers, basemap_style_or_None)."""
```

From backend/app/modules/catalog/search/service.py:1051-1057 (cluster 1, intentional behavior):
```python
else:
    # No temporal extent — use created_at as fallback so item passes STAC validation.
    stac_datetime = (
        record.created_at.isoformat().replace("+00:00", "Z")
        if record.created_at
        else None
    )
```

From backend/app/core/public_urls.py:166 (cluster 6 cache global):
```python
_PUBLIC_URL_CACHE: dict[str, str] | None = None  # 60s TTL, populated by first call
```

From backend/app/modules/catalog/search/router.py:980-981 (cluster 4 pagination ceiling):
```python
limit: int = Query(50, ge=1, le=200, description="Max items per page"),
```

From backend/pyproject.toml [dependency-groups].dev (cluster 5 install site — file is NOT alphabetized; freezegun goes at the END after `pystac>=1.14`):
```toml
dev = [
    "pytest>=9.0.3",
    "pytest-asyncio",
    "httpx",
    "ruff",
    "jsonschema>=4.19",
    "pytest-cov",
    "bandit>=1.8",
    "pip-audit>=2.7",
    "moto[s3]>=5.0.0",
    "fakeredis>=2.21.0",
    "pystac>=1.14",
    "freezegun>=1.5.0",   # APPEND HERE — final position after pystac
]
```
</interfaces>

<canonical_test_commands>
Per RESEARCH.md, all pytest runs go through the api container:

Single test:
```bash
docker compose exec -T api uv run pytest <test_path>::<test_name> -xvs
```

Multi-test (verifies a cluster's fixes don't regress siblings):
```bash
docker compose exec -T api uv run pytest <file1> <file2> -v
```

Full suite (final task only):
```bash
docker compose exec -T api uv run pytest -v --tb=short
# or via root Makefile:
make test
```
</canonical_test_commands>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Cluster 1 + Cluster 2 — fix STAC datetime test rename + remove raster stac_extensions leak</name>
  <files>backend/tests/test_stac_record_output.py, backend/app/modules/catalog/search/service.py</files>
  <action>
TWO related edits in `backend/tests/test_stac_record_output.py` plus ONE code edit in `backend/app/modules/catalog/search/service.py`.

**Cluster 1 (test rename, per CONTEXT D-Cluster1 LOCKED):**
Rename `test_datetime_null_when_no_temporal` (around line 116) to `test_datetime_falls_back_to_created_at_when_no_temporal`. Update its docstring to cite "audit 20260425 cluster 1" and reference `backend/app/modules/catalog/search/service.py:1051-1057` as the intentional fallback site. Replace the assertion so the test now expects `result["properties"]["datetime"]` to equal `dataset.record.created_at.isoformat().replace("+00:00", "Z")` (the RFC 3339 / ISO 8601 form with `Z` suffix that the serializer emits). Use the exact body in RESEARCH.md "Cluster 1" section as the canonical edit.

**Cluster 2 (code fix, override CONTEXT cluster-2 PARTIAL because RESEARCH proved it's a real regression):**

The leak site is pinpointed: `backend/app/modules/catalog/search/service.py:1219-1223` — the `if has_proj:` block:
```python
        # Declare STAC extensions used by this item
        if has_proj:
            ogc_record.setdefault("stac_extensions", []).append(
                "https://stac-extensions.github.io/projection/v2.0.0/schema.json"
            )
```
This is the ONLY `stac_extensions` write site in the entire file (verified via `grep -n stac_extensions backend/app/modules/catalog/search/service.py` → single hit at line 1221). Removing this 3-line block (the `if has_proj:` body + the comment line above it, lines 1219-1223) closes both failing cluster-2 tests with zero collateral risk.

1. **Verify line numbers before editing** — file may have shifted since this plan was written. Run:
   ```bash
   grep -n "stac_extensions" backend/app/modules/catalog/search/service.py
   ```
   Confirm exactly ONE hit. If multiple hits appear, STOP and re-evaluate scope before editing.
2. Delete lines 1219-1223 (the `# Declare STAC extensions used by this item` comment + the entire `if has_proj:` / `setdefault.append(...)` block). Use Edit tool with the exact 3-line block as `old_string`.
3. The two failing tests in `TestStacExtensionsRemoved` assert `"stac_extensions" not in result` for raster records — removing the only write site makes both pass. Sibling test `test_vector_record_no_stac_extensions` already PASSED (vector path never hit this block).
4. Run all of `tests/test_stac_record_output.py` (full file, ~13 tests) after the change to confirm sibling guards (`TestStacAssetsRemoved`, `TestNoStacBleedthrough`, `TestStacExtensionsRemoved::test_vector_record_no_stac_extensions`) still pass.

**Fallback (scope safety valve):** If removing the block somehow breaks unrelated sibling STAC tests (very unlikely — `stac_extensions` has only one write site), fall back to `@pytest.mark.xfail(reason="audit 20260425 cluster 2: stac_extensions leaks into raster OGC record output, see audit", strict=False)` on the two failing tests and document the rollback in SUMMARY.md. Cluster-1 rename is a strict edit and has no fallback.
  </action>
  <verify>
    <automated>docker compose exec -T api uv run pytest tests/test_stac_record_output.py -v</automated>
  </verify>
  <done>
- File contains `test_datetime_falls_back_to_created_at_when_no_temporal` (not the old `test_datetime_null_when_no_temporal` name).
- Test asserts `properties["datetime"]` equals `created_at.isoformat().replace("+00:00", "Z")`.
- `tests/test_stac_record_output.py::TestStacExtensionsRemoved::test_raster_record_no_stac_extensions` passes (or xfail-fallback applied with audit-traceable reason).
- `tests/test_stac_record_output.py::TestStacExtensionsRemoved::test_no_bands_without_band_info` passes (or xfail-fallback applied with audit-traceable reason).
- All other tests in `test_stac_record_output.py` (sibling STAC guards) still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cluster 3 — unpack _validate_chat_layers tuple in 7 chat tests</name>
  <files>backend/tests/test_ai_chat.py, backend/tests/test_chat_streaming.py</files>
  <action>
Apply the tuple-unpack fix from RESEARCH.md "Cluster 3". Single shared root cause: `_validate_chat_layers` returns `tuple[list[ChatMapLayer], str | None]` but tests treat it as a bare list. Override CONTEXT's "default to xfail for cluster 3" because RESEARCH proved the fix is trivial (5-7 line diff total).

**Cluster 3a — `backend/tests/test_ai_chat.py` (2 sites, direct calls):**
Around line 113 (in `test_validate_overwrites_client_table_name`) and around line 149 (in `test_validate_filters_inaccessible_dataset`), change:
```python
validated = await _validate_chat_layers(session, admin, str(map_obj.id), [layer])
```
to:
```python
validated, _basemap = await _validate_chat_layers(session, admin, str(map_obj.id), [layer])
```

**Cluster 3b — `backend/tests/test_chat_streaming.py` (5 sites, AsyncMock returns):**
Anchor edits by test name (more robust than line numbers — verified actual `return_value=` lines are 103, 134, 155, 213, 316; plan was off-by-1-to-2). In each of these 5 tests, locate the `patch("app.processing.ai.router._validate_chat_layers", ...)` block and change the `return_value=` line:

| Test name | Current `return_value=` | New `return_value=` |
|-----------|-------------------------|---------------------|
| `test_stream_returns_sse_events` | `CHAT_BODY["layers"]` | `(CHAT_BODY["layers"], None)` |
| `test_non_streaming_fallback` | `CHAT_BODY["layers"]` | `(CHAT_BODY["layers"], None)` |
| `test_tool_progress_events` | `CHAT_BODY["layers"]` | `(CHAT_BODY["layers"], None)` |
| `test_query_data_stage_events` | `body["layers"]` | `(body["layers"], None)` |
| `test_show_query_result_action_in_stream` | `body["layers"]` | `(body["layers"], None)` |

Preserve the variable spelling exactly (`CHAT_BODY` for the first 3, `body` for the last 2 — they use locally-scoped `body` dicts).

For reference (verified 2026-04-25), the actual `return_value=` lines are: 103, 134, 155, 213, 316. If using line-anchored edits, use those numbers. Test-name-anchored is preferred.

Do NOT refactor the mock layers to use the `ChatMapLayer` Pydantic class — RESEARCH explicitly notes the dict literals work because the codepath only attribute-accesses and unpacks. Tuple is the surgical fix.

**Fallback:** If any test fails for a different root cause after the tuple fix, mark only that one with `@pytest.mark.xfail(reason="audit 20260425 cluster 3: <one-line>", strict=False)` and continue. Don't xfail tests that pass.
  </action>
  <verify>
    <automated>docker compose exec -T api uv run pytest tests/test_ai_chat.py tests/test_chat_streaming.py -v</automated>
  </verify>
  <done>
- Both `test_ai_chat.py` cluster-3a tests pass.
- All 5 `test_chat_streaming.py` cluster-3b tests pass.
- No new failures in adjacent chat tests.
- (Or any individually-stubborn test is xfail'd with cluster-3 audit-traceable reason — but the goal is zero xfails.)
  </done>
</task>

<task type="auto">
  <name>Task 3: Cluster 4 — add ?limit=200 to OGC catalog list-page assertions</name>
  <files>backend/tests/test_ogc_collection_metadata.py, backend/tests/test_ogc_features.py</files>
  <action>
Per RESEARCH.md "Cluster 4" — root cause is suite-pollution + default `limit=50` on `/collections`. Fix is in the test query, not the endpoint.

Three GET requests need `params={"limit": 200}` added (200 is the route ceiling per `Query(50, ge=1, le=200)` at `backend/app/modules/catalog/search/router.py:981`):

1. `backend/tests/test_ogc_collection_metadata.py:383` (in `test_per_dataset_collection_has_extent_in_list`):
   - `await client.get("/collections")` → `await client.get("/collections", params={"limit": 200})`
2. `backend/tests/test_ogc_collection_metadata.py:418` (in `test_per_dataset_collection_has_root_link_in_list`): same edit.
3. `backend/tests/test_ogc_features.py:148` (in `test_collections_includes_dataset_collections`): same edit.

If the existing GET already passes other params (kwargs, headers, query strings), preserve them and merge `limit` in alongside.

**Fallback:** If the test still fails (e.g., session DB exceeds 200 datasets — unlikely per RESEARCH), fall back to `@pytest.mark.xfail(reason="audit 20260425 cluster 4: per-dataset entry not on first /collections page when DB has >200 datasets, see audit", strict=False)` on the failing test only.
  </action>
  <verify>
    <automated>docker compose exec -T api uv run pytest tests/test_ogc_collection_metadata.py::test_per_dataset_collection_has_extent_in_list tests/test_ogc_collection_metadata.py::test_per_dataset_collection_has_root_link_in_list tests/test_ogc_features.py::test_collections_includes_dataset_collections -v</automated>
  </verify>
  <done>
- All 3 cluster-4 tests pass when run together.
- Sibling OGC catalog tests still pass.
- (Or any individual stubborn test is xfail'd with cluster-4 audit-traceable reason.)
  </done>
</task>

<task type="auto">
  <name>Task 4: Cluster 5 prerequisite — install freezegun in api container</name>
  <files>backend/pyproject.toml, backend/uv.lock</files>
  <action>
Per RESEARCH.md "freezegun availability" + CONTEXT D-Cluster5 LOCKED.

1. Edit `backend/pyproject.toml` `[dependency-groups].dev` array. **The dev list is NOT alphabetized** (verified 2026-04-25: `pytest`, `pytest-asyncio`, `httpx`, `ruff`, `jsonschema`, `pytest-cov`, `bandit`, `pip-audit`, `moto[s3]`, `fakeredis`, `pystac` — chronological-ish, not sorted). **Append `"freezegun>=1.5.0",` directly after `"pystac>=1.14",`** as the new final entry. Do NOT use `pytest-freezer` — RESEARCH chose `freezegun` because the test code uses bare `pytest` decorators rather than plugin-marker style.

   Concrete diff target (verify line numbers before editing — file may have shifted):
   ```toml
   "fakeredis>=2.21.0",
   "pystac>=1.14",
   "freezegun>=1.5.0",   # ADD THIS LINE — final position
   ]
   ```

2. Regenerate the lockfile from inside the api container:
   ```bash
   docker compose exec -T api uv lock
   ```

3. Rebuild the api container so the new dep is materialized in the image:
   ```bash
   docker compose build api
   docker compose up -d api
   ```

4. Confirm the import works inside the live container:
   ```bash
   docker compose exec -T api python -c "from freezegun import freeze_time; print('ok')"
   ```

**Fallback:** If `uv lock` resolves to a conflicting dependency graph or container build fails for unrelated reasons, fall back to xfail'ing `test_search.py::test_search_filter_by_date_range` directly (skip Task 5 entirely) with `reason="audit 20260425 cluster 5: freezegun install blocked, deferred"` and document in SUMMARY.md. Do NOT regress existing dep pins to make freezegun fit.

This task ships only the install — the test edit is in Task 5 (which depends on this task succeeding).
  </action>
  <verify>
    <automated>docker compose exec -T api python -c "from freezegun import freeze_time; print('ok')" | grep -q '^ok$'</automated>
  </verify>
  <done>
- `backend/pyproject.toml` `[dependency-groups].dev` contains `freezegun>=1.5.0` as the last entry, directly after `pystac>=1.14`.
- `backend/uv.lock` updated.
- api container rebuilt and `from freezegun import freeze_time` succeeds inside it.
- (Or fallback applied: cluster-5 test xfail'd, Task 5 skipped, documented in SUMMARY.)
  </done>
</task>

<task type="auto">
  <name>Task 5: Cluster 5 — wrap date-range test in freeze_time</name>
  <files>backend/tests/test_search.py</files>
  <action>
Skip this task if Task 4 fell back to xfail.

Per RESEARCH.md "Cluster 5" + CONTEXT D-Cluster5 LOCKED.

1. Add imports at the top of `backend/tests/test_search.py` (alongside the existing `from datetime import date, timedelta` at line 14):
   ```python
   from datetime import datetime, timezone
   from freezegun import freeze_time
   ```
   Merge with existing `datetime` import: `from datetime import date, datetime, timedelta, timezone`. Add `from freezegun import freeze_time` as a separate import below the stdlib block.

2. Replace `test_search_filter_by_date_range` (around line 395) body with the canonical pattern from RESEARCH.md "Cluster 5" — wrap the date arithmetic AND the HTTP calls in `with freeze_time(datetime.now(timezone.utc)):`. Critical caveat from RESEARCH: the freeze instant MUST be `datetime.now(timezone.utc)` (real wall-clock, not a hardcoded historical date) because freezegun does NOT patch Postgres `NOW()` — Postgres `created_at` will use real wall-clock UTC, so the Python-side `today/yesterday/tomorrow` window must bracket the same moment.

3. Preserve the existing test's two-stage pattern: first GET checks `numberMatched >= 4` for the `yesterday..tomorrow` window; second GET checks `2020-01-01..2020-01-31` returns NONE of the freshly-created `search_datasets`. Both stages go inside the `freeze_time` context.

**Fallback:** If the test fails after the freeze fix (suggesting a different root cause beneath the boundary race), fall back to `@pytest.mark.xfail(reason="audit 20260425 cluster 5: date.today() vs Postgres NOW() boundary race, see audit", strict=False)`.
  </action>
  <verify>
    <automated>docker compose exec -T api uv run pytest tests/test_search.py::test_search_filter_by_date_range -xvs</automated>
  </verify>
  <done>
- `tests/test_search.py` imports `freeze_time` from `freezegun`.
- `test_search_filter_by_date_range` body wrapped in `with freeze_time(datetime.now(timezone.utc)):`.
- Test passes inside the api container.
- (Or fallback applied with audit-traceable xfail reason.)
  </done>
</task>

<task type="auto">
  <name>Task 6: Cluster 6 — reset _PUBLIC_URL_CACHE in test_load_public_url_overrides</name>
  <files>backend/tests/test_public_urls.py</files>
  <action>
Per RESEARCH.md "Cluster 6". Override CONTEXT cluster-6 LOCKED xfail because RESEARCH proved the root cause is obvious (a 1-line cache reset).

1. Open `backend/tests/test_public_urls.py`. The test in question is `test_load_public_url_overrides_unwraps_json_values` around line 211.

2. As the FIRST line of the test body (immediately after the `async def test_...():` signature line and before `db = AsyncMock()`), add:
   ```python
   public_urls._PUBLIC_URL_CACHE = None  # clear cross-test cache pollution (audit 20260425 cluster 6)
   ```

3. Verify the file already imports `public_urls` (look for `from app.core import public_urls` or `from app.core.public_urls import ...`). If only specific symbols are imported, add `from app.core import public_urls` at the top so the module-level reset works.

4. Do NOT add the optional session-autouse fixture from RESEARCH.md ("Recommend the planner consider promoting") — RESEARCH explicitly scoped that as a follow-up, not in scope for this task.

**Fallback:** If the test still fails after the cache reset (suggesting more than one polluter), fall back to `@pytest.mark.xfail(reason="audit 20260425 cluster 6: stale _PUBLIC_URL_CACHE global from prior test, see backend/app/core/public_urls.py:166", strict=False)`.
  </action>
  <verify>
    <automated>docker compose exec -T api uv run pytest tests/test_public_urls.py -v</automated>
  </verify>
  <done>
- `test_load_public_url_overrides_unwraps_json_values` first line sets `public_urls._PUBLIC_URL_CACHE = None`.
- Test passes both alone and as part of the full `test_public_urls.py` file run.
- (Or xfail fallback applied with audit-traceable reason.)
  </done>
</task>

<task type="auto">
  <name>Task 7: Final green-baseline verification — run full backend pytest suite</name>
  <files>(no edits; verification + SUMMARY recording only)</files>
  <action>
Run the full backend pytest suite per RESEARCH.md "Test runner command":

```bash
docker compose exec -T api uv run pytest -v --tb=short
```

(Equivalent to `make test` from the repo root.)

Tally the result:
- Pre-task baseline (per CONTEXT.md): 1950 pass / 15 fail / 1965 total.
- Expected post-task: 1965 pass / 0 unexpected fail. (Or, if any per-cluster fallback to xfail was triggered, those tests show as `xfail` — count and document them.)

If the suite reveals NEW failures unrelated to the audit's 15 (e.g., a recent PR introduced regression), document each one in SUMMARY.md with:
- Test name
- One-line failure shape
- Whether it predates this task or was introduced
- Recommendation (do not attempt to fix in this task — out of scope).

If any of the audit's 15 is still failing, decide between:
- Quick re-fix attempt (only if obvious from the new failure mode and <5 LOC)
- Apply per-cluster xfail fallback per the per-task instructions

Write the final state to `260425-sl1-SUMMARY.md` with:
- Counts: pass / fail / xfail / xpass / total
- Per-cluster outcome (fixed vs xfail-fallback, with audit-traceable reason if xfail)
- Any newly-discovered debt (separate section, not in scope but logged for follow-up)
- Reproducer command for any remaining failure

**Fallback:** None — this is a verification + reporting task, not an implementation task. If the suite cannot run (container down, infra issue), record the blocker in SUMMARY and stop.
  </action>
  <verify>
    <automated>docker compose exec -T api uv run pytest -v --tb=short</automated>
  </verify>
  <done>
- Full backend suite (`docker compose exec -T api uv run pytest -v --tb=short`) reports 0 unexpected failures across the full ~1965-test suite.
- All 7 cluster files (test_stac_record_output.py, test_ai_chat.py, test_chat_streaming.py, test_ogc_collection_metadata.py, test_ogc_features.py, test_search.py, test_public_urls.py) pass within that suite — every audited test is either PASS or XFAIL with audit-traceable reason.
- `260425-sl1-SUMMARY.md` records: per-cluster outcomes, final counts, any newly-discovered debt with reproducer commands.
- If Task 4 fell back, SUMMARY explicitly notes cluster 5 was xfail'd because freezegun install was blocked (matches the inverted must_haves truths above).
  </done>
</task>

</tasks>

<verification>

After all 7 tasks complete:

1. **Per-cluster pass:** Each `<verify>` command in tasks 1-6 returns exit code 0 (all targeted tests PASS or audit-traced XFAIL).
2. **Suite-level pass:** `docker compose exec -T api uv run pytest -v --tb=short` reports 0 unexpected failures.
3. **Audit closure:** Every test in `docs-internal/audits/test-debt-backend-20260425.md` "Failure list" is now in a known-good state (green or audit-traced xfail).
4. **No new debt:** No previously-passing test was broken by the changes (Task 1 cluster-2 code edit is the highest-risk; sibling STAC guards must still pass).
5. **freezegun installed:** `from freezegun import freeze_time` works inside the api container (or cluster-5 xfail-fallback documented in SUMMARY).
6. **Pyproject append-position preserved:** `freezegun` appears as the LAST entry in `[dependency-groups].dev`, directly after `pystac>=1.14` (the dev list is not alphabetized).

</verification>

<success_criteria>

- All 15 audit failures are addressed (fixed-test, fixed-code, or audit-traced xfail).
- Default outcome: 15 fixed, 0 xfail. Acceptable outcome: any combination where every audit-listed test is in a known state with audit-traceable reason.
- `freezegun>=1.5.0` is a permanent dev dep; `uv.lock` and api image both reflect it.
- No production-code change beyond the cluster-2 raster `stac_extensions` leak fix in `backend/app/modules/catalog/search/service.py` (lines 1219-1223 removed).
- Full backend pytest suite shows zero unexpected failures.
- `260425-sl1-SUMMARY.md` documents per-cluster outcomes, final counts, and any newly-discovered debt for future follow-up.

</success_criteria>

<output>
After completion, create `.planning/quick/260425-sl1-address-the-debt-in-docs-internal-audits/260425-sl1-SUMMARY.md` with:
- Final test counts (pass / fail / xfail / xpass / total)
- Per-cluster outcome table (cluster → fixed | xfail with reason)
- Any newly-discovered debt (separate "Out of scope" section)
- Reproducer commands for any remaining xfails
- Confirmation that `freezegun` is in `backend/pyproject.toml` and `backend/uv.lock`
</output>
