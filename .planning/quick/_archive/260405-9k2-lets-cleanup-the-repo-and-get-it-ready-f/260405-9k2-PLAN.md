---
phase: 260405-9k2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - CONTRIBUTING.md
  - scripts/purge-history.sh
autonomous: false
requirements: []
must_haves:
  truths:
    - "All 72 stray PNGs in repo root are deleted from working tree"
    - "Current uncommitted changes are preserved in a commit before cleanup"
    - ".gitignore blocks .agents/, .codex/, .claude/ and internal docs patterns"
    - ".gitignore allows public docs/ files to be tracked"
    - "Internal docs (audits/, GTM/, decisions/, api-contract, cloud-readiness, connection-budget) are removed from git tracking"
    - "CONTRIBUTING.md exists at repo root with contribution guidelines"
    - "CODE_OF_CONDUCT.md is tracked in git"
    - "A ready-to-run purge-history.sh script exists that the user can execute"
    - "All 27 tags are deleted by the purge script (fresh start for public release)"
  artifacts:
    - path: ".gitignore"
      provides: "Updated ignore rules for public release"
      contains: ".agents/"
    - path: "CONTRIBUTING.md"
      provides: "Contribution guidelines for open-source"
      min_lines: 30
    - path: "scripts/purge-history.sh"
      provides: "git-filter-repo history rewrite script"
      contains: "git filter-repo"
  key_links:
    - from: ".gitignore"
      to: "docs/"
      via: "selective ignore patterns for internal-only docs"
      pattern: "docs/audits/"
    - from: "scripts/purge-history.sh"
      to: "git history"
      via: "git-filter-repo --invert-paths"
      pattern: "filter-repo"
---

<objective>
Clean up the GeoLens repo for public open-source release. Commit current work, delete stray screenshots, update .gitignore for public-facing docs, remove internal docs from tracking, create CONTRIBUTING.md, and prepare a history-rewrite script for the user to run.

Purpose: The repo currently contains internal development artifacts (.planning/, internal docs, stray PNGs) that must not appear in a public release. This plan does all safe cleanup and prepares a script for the destructive history rewrite.
Output: Clean working tree with updated .gitignore, CONTRIBUTING.md, and a ready-to-run purge script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260405-9k2-lets-cleanup-the-repo-and-get-it-ready-f/260405-9k2-CONTEXT.md
@.planning/quick/260405-9k2-lets-cleanup-the-repo-and-get-it-ready-f/260405-9k2-RESEARCH.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit current changes, delete PNGs, update .gitignore, untrack internal docs</name>
  <files>.gitignore</files>
  <action>
Execute in this exact order:

**Step 1 — Commit current uncommitted changes.**
Stage and commit all 15 currently modified files (listed in git status). Use commit message: "Refine settings, search, and layout before public release cleanup". This preserves current work before any cleanup.

**Step 2 — Delete all 72 stray PNG screenshots in repo root.**
Run: `rm /Users/ishiland/Code/geolens/*.png`
These are dev artifacts already covered by the `*.png` gitignore rule. They are untracked but clutter the working tree.

**Step 3 — Update .gitignore for public release.**
Rewrite `.gitignore` with these changes:

a) ADD these entries (under a new "AI assistant working directories" section):
```
.agents/
.codex/
```
Note: `.claude/` is already present.

b) REPLACE the blanket `docs/` ignore line with selective internal-only doc patterns:
```
# Internal docs (not for public release)
docs/audits/
docs/GTM/
docs/decisions/
docs/handoff-*.md
docs/ux-plan-*.md
docs/ux-review-*.md
docs/dep-audit-*.md
docs/sec-audit-*.md
docs/api-contract-*.md
docs/cloud-readiness-*.md
docs/connection-budget.md
```

c) Keep ALL other existing gitignore rules unchanged (.planning/, .claude/, plans/, artifacts/, prd.md, todo.md, smoke-check.md, *.png rules, etc. — these still need to exist until the history rewrite happens).

**Step 4 — Remove internal tracked docs from git index.**
These files are currently tracked despite being in docs/. After .gitignore is updated to only ignore internal docs, remove the internal ones from tracking:
```bash
git rm --cached docs/audits/demo-ready-20260330.md
git rm --cached docs/audits/design-audit-20260331.md
git rm --cached docs/audits/doc-audit-20260401.md
git rm --cached docs/audits/post-impl-20260401.md
git rm --cached docs/audits/post-impl-20260403.md
git rm --cached docs/audits/test-audit-20260403.md
git rm --cached docs/GTM/GTM-EVALUATION.md
git rm --cached docs/decisions/adr-001-v12-record-first-architecture.md
git rm --cached docs/api-contract-full-2026-03-30.md
git rm --cached docs/cloud-readiness-assessment.md
git rm --cached docs/connection-budget.md
```
Do NOT remove the public docs from tracking. The public docs that must STAY tracked:
- docs/DESIGN-GUIDE.md
- docs/admin-guide.md
- docs/aws-security-groups.md
- docs/cloud-deployment.md
- docs/configuration-reference.md
- docs/database-design.md
- docs/images/ (all files)
- docs/install-guide.md
- docs/llm-data-features.md
- docs/llm-map-features.md
- docs/marketplace-description.md
- docs/metadata-standards.md
- docs/resource-sizing.md
- docs/testing-and-ci.md
- docs/upgrade-guide.md
- docs/widget-development.md

Note: widget-development.md is not in git ls-files. After the .gitignore change it may become visible — if so, `git add docs/widget-development.md` to start tracking it.

**Step 5 — Commit cleanup.**
Commit the .gitignore update + internal doc untracking with message: "Remove internal docs from tracking and update .gitignore for public release"
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && ls *.png 2>/dev/null | wc -l | grep -q "^0$" && echo "PNGs deleted OK" && git ls-files docs/ | grep -v -E "(audits|GTM|decisions|api-contract|cloud-readiness|connection-budget)" | wc -l | awk '{exit ($1 < 16)}' && echo "Public docs still tracked (16+)" && grep -q ".agents/" .gitignore && echo "gitignore updated" && git ls-files docs/widget-development.md | grep -q widget && echo "widget-development.md tracked" && echo "ALL CHECKS PASS"</automated>
  </verify>
  <done>All 72 PNGs deleted. Current work committed. .gitignore updated with .agents/.codex/ entries and selective docs/ patterns. Internal docs removed from tracking. Public docs remain tracked (16+ files). docs/widget-development.md is tracked.</done>
</task>

<task type="auto">
  <name>Task 2: Create CONTRIBUTING.md, verify CODE_OF_CONDUCT.md, and create history-rewrite script</name>
  <files>CONTRIBUTING.md, scripts/purge-history.sh</files>
  <action>
**Part A — Create CONTRIBUTING.md at repo root.**

Write a concise, practical contributing guide for GeoLens. Include:
- Welcome section (1-2 sentences about GeoLens being an open-source geospatial data catalog)
- How to report bugs (GitHub Issues)
- How to suggest features (GitHub Issues with "enhancement" label)
- Development setup (reference docs/install-guide.md for full setup; quick summary: requires Docker, docker compose up -d)
- Pull request process (fork, branch, PR against main, tests must pass)
- Code style (Python: ruff, TypeScript: ESLint + Prettier, follow existing conventions)
- Reference to CODE_OF_CONDUCT.md
- Reference to SECURITY.md for vulnerability reporting

Keep it under 100 lines. No boilerplate fluff. Direct and practical.

**Part B — Verify CODE_OF_CONDUCT.md is tracked.**

CODE_OF_CONDUCT.md already exists at repo root. Check if it is tracked: `git ls-files CODE_OF_CONDUCT.md`. If the output is empty (not tracked), run `git add CODE_OF_CONDUCT.md` to start tracking it.

**Part C — Create scripts/purge-history.sh.**

Create the `scripts/` directory if it does not exist. Write an executable bash script that:

1. Prints a clear warning that this is DESTRUCTIVE and rewrites all git history
2. Asks for confirmation (read -p "Type YES to continue: ")
3. Checks that git-filter-repo is installed, prints install instructions if not (`brew install git-filter-repo`)
4. Creates a mirror backup: `git clone --mirror . ../geolens-backup-$(date +%Y%m%d)`
5. Deletes all tags locally with a comment explaining why:
   ```bash
   # Delete all tags — starting fresh for public release (user decision).
   # Internal milestone tags (v1.0-v14.0) are not relevant to public consumers.
   git tag -l | xargs git tag -d
   ```
6. Runs git-filter-repo with --invert-paths for ALL these paths:
   - .planning/
   - plans/
   - prd.md
   - todo.md
   - smoke-check.md
   - artifacts/
   - .agents/
   - .codex/
   - .claude/
   - docs/audits/
   - docs/GTM/
   - docs/decisions/
   - docs/dep-audit-2026-03-31.md
   - docs/sec-audit-full-2026-03-30.md
   - docs/handoff-landing-page-oss-identity-20260403.md
   - docs/ux-plan-landing-page-20260403.md
   - docs/ux-review-map-builder-retroactive-2026-03-31.md
   - docs/api-contract-full-2026-03-30.md
   - docs/cloud-readiness-assessment.md
   - docs/connection-budget.md
   Include --force flag.
7. Re-adds origin remote: `git remote add origin https://github.com/geolens-io/geolens.git`
8. Prints instructions for the manual force-push step:
   ```
   echo ""
   echo "History rewrite complete. All tags deleted. Backup at ../geolens-backup-YYYYMMDD"
   echo ""
   echo "Next steps (run manually):"
   echo "  git push origin --force --all"
   echo ""
   echo "After force-push, all collaborators must re-clone."
   ```

The script should NOT automatically force-push — that step must be manual.
Make the script executable (chmod +x).

**Part D — Commit all files.**
Commit with message: "Add CONTRIBUTING.md and history-rewrite script for public release"
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && test -f CONTRIBUTING.md && echo "CONTRIBUTING exists" && test -x scripts/purge-history.sh && echo "Script is executable" && grep -q "filter-repo" scripts/purge-history.sh && echo "Script contains filter-repo" && grep -q ".planning/" scripts/purge-history.sh && echo "Script purges .planning/" && grep -q "CODE_OF_CONDUCT" CONTRIBUTING.md && echo "References CoC" && git ls-files CODE_OF_CONDUCT.md | grep -q CODE_OF_CONDUCT && echo "CODE_OF_CONDUCT.md is tracked" && grep -q "starting fresh for public release" scripts/purge-history.sh && echo "Tag deletion has rationale comment" && echo "ALL CHECKS PASS"</automated>
  </verify>
  <done>CONTRIBUTING.md exists with practical contribution guidelines. CODE_OF_CONDUCT.md is verified as tracked in git. scripts/purge-history.sh is executable and contains the complete git-filter-repo command to purge all internal paths, deletes all tags (with rationale comment), creates backup, and prints manual force-push instructions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify cleanup and run history rewrite</name>
  <files>scripts/purge-history.sh</files>
  <action>
User verifies all cleanup is correct, then runs the history-rewrite script manually.

What was built: Complete repo cleanup — stray PNGs deleted, .gitignore updated for public docs, internal docs untracked, CONTRIBUTING.md created, CODE_OF_CONDUCT.md verified as tracked, and history-rewrite script prepared.

How to verify:
1. Verify working tree is clean: `git status` — should show no unexpected changes
2. Verify no PNGs in root: `ls *.png` — should return nothing
3. Verify public docs are tracked: `git ls-files docs/` — should show ~16 public docs (install-guide, admin-guide, etc.) and NO internal docs (audits/, GTM/, decisions/)
4. Review CONTRIBUTING.md: `cat CONTRIBUTING.md` — should be practical and concise
5. Review the purge script: `cat scripts/purge-history.sh` — verify all paths look correct, note that all tags will be deleted (fresh start)
6. When ready to rewrite history, run:
   ```bash
   brew install git-filter-repo  # if not installed
   bash scripts/purge-history.sh
   ```
   Then manually: `git push origin --force --all`
  </action>
  <verify>User confirms cleanup is correct and history rewrite script is ready to run</verify>
  <done>User has approved the cleanup. History rewrite script is ready for manual execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history -> public GitHub | Internal artifacts must not leak to public repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9k2-01 | Information Disclosure | git history | mitigate | git-filter-repo purges .planning/, .claude/, internal docs from ALL commits |
| T-9k2-02 | Information Disclosure | GitHub cached objects | accept | GitHub eventually GCs unreferenced objects; user can contact GitHub support if needed |
| T-9k2-03 | Denial of Service | force push breaks clones | accept | Pre-public-release, limited collaborators; script prints re-clone instructions |
</threat_model>

<verification>
- `ls *.png 2>/dev/null | wc -l` returns 0
- `git ls-files docs/ | grep -c audits` returns 0
- `grep -c ".agents/" .gitignore` returns at least 1
- `test -f CONTRIBUTING.md` succeeds
- `git ls-files CODE_OF_CONDUCT.md | wc -l` returns 1
- `test -x scripts/purge-history.sh` succeeds
- `grep -c "filter-repo" scripts/purge-history.sh` returns at least 1
- `git ls-files docs/widget-development.md | wc -l` returns 1
</verification>

<success_criteria>
1. All stray PNGs removed from working tree
2. .gitignore correctly differentiates public vs internal docs
3. Internal docs removed from git tracking (but files still on disk)
4. CONTRIBUTING.md is practical and references CODE_OF_CONDUCT.md
5. CODE_OF_CONDUCT.md is tracked in git
6. purge-history.sh is a complete, executable script ready for user to run
7. Script deletes all 27 tags before history rewrite (fresh start)
8. Three clean commits: (a) current work, (b) cleanup, (c) new files
</success_criteria>

<output>
After completion, create `.planning/quick/260405-9k2-lets-cleanup-the-repo-and-get-it-ready-f/260405-9k2-SUMMARY.md`
</output>
