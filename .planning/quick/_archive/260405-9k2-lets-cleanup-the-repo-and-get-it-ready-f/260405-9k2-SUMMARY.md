---
phase: 260405-9k2
plan: 01
subsystem: repo
tags: [cleanup, open-source, gitignore, contributing, git-history]
dependency_graph:
  requires: []
  provides: [public-release-ready-repo]
  affects: [.gitignore, docs/, scripts/]
tech_stack:
  added: [git-filter-repo (script only)]
  patterns: [selective-gitignore, history-rewrite]
key_files:
  created:
    - CONTRIBUTING.md
    - scripts/purge-history.sh
  modified:
    - .gitignore
key_decisions:
  - Replaced blanket docs/ gitignore with selective internal-only patterns so public docs remain tracked
  - Chose git-filter-repo over BFG for history rewrite (better maintained, supports --invert-paths)
  - Script does NOT auto-push; force-push left as deliberate manual step
  - All 27 milestone tags deleted in purge script — fresh start for public consumers
metrics:
  duration: "~10 minutes"
  completed: "2026-04-05"
  tasks_completed: 2
  tasks_total: 3
  files_created: 2
  files_modified: 1
---

# Quick Task 260405-9k2: Repo Cleanup for Public Open-Source Release

One-liner: Committed in-flight work, deleted 72 stray PNGs, rewrote .gitignore to selectively ignore internal docs while tracking public docs, untracked 11 internal doc files, added CONTRIBUTING.md and an executable git-filter-repo history-rewrite script.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1a | Commit current work | `182f490b` | 17 files (settings, search, layout) |
| 1b | Delete PNGs + update .gitignore + untrack internal docs | `964711d4` | .gitignore, 11 docs/ deletions, docs/widget-development.md |
| 2 | CONTRIBUTING.md + purge-history.sh | `b63b47e9` | CONTRIBUTING.md, scripts/purge-history.sh |
| 3 | Human verify + run purge script | — | Checkpoint — awaiting user |

## What Was Built

**Commit `182f490b`:** Preserved in-flight changes to settings, search store, layout, and i18n files before destructive cleanup.

**Commit `964711d4`:**
- Deleted all 72 stray PNG screenshots from repo root (were gitignored but cluttered working tree)
- Updated `.gitignore`: added `.agents/` and `.codex/` under AI assistant working directories; replaced blanket `docs/` ignore with selective patterns covering only internal docs (audits/, GTM/, decisions/, handoffs, UX plans, dep-audits, sec-audits, api-contracts, cloud-readiness, connection-budget)
- Removed 11 internal docs from git tracking (files remain on disk): docs/audits/* (6 files), docs/GTM/GTM-EVALUATION.md, docs/decisions/adr-001-*.md, docs/api-contract-full-*.md, docs/cloud-readiness-assessment.md, docs/connection-budget.md
- Started tracking docs/widget-development.md (newly visible after removing blanket docs/ ignore)

**Commit `b63b47e9`:**
- Created `CONTRIBUTING.md` (51 lines): bug reporting, feature suggestions, dev setup (Docker), PR process, code style (ruff + ESLint/Prettier), references to CODE_OF_CONDUCT.md and SECURITY.md
- Verified `CODE_OF_CONDUCT.md` is already tracked in git
- Created `scripts/purge-history.sh` (executable): prints destructive warning, asks YES confirmation, checks git-filter-repo installed, creates mirror backup, deletes all tags (with rationale comment), runs git-filter-repo --invert-paths for 20 internal paths, re-adds origin remote, prints manual force-push instructions

## Checkpoint: Task 3 (Human Verify)

**Status:** Awaiting user verification and manual script execution.

**To verify and run:**
```bash
# Check working tree
git status

# Confirm no PNGs
ls *.png 2>/dev/null

# Verify tracked docs (should show ~19 public docs, no audits/GTM/decisions)
git ls-files docs/

# Review files
cat CONTRIBUTING.md
cat scripts/purge-history.sh

# When ready — install git-filter-repo if needed:
brew install git-filter-repo

# Run the history rewrite:
bash scripts/purge-history.sh

# Then manually force-push:
git push origin --force --all
```

## Deviations from Plan

None — plan executed exactly as written.

One minor note: The worktree's .planning/ files (PROJECT.md, ROADMAP.md, STATE.md) appeared in git status but are gitignored, so they were excluded from the Task 1 commit (only the 14 non-planning modified files were committed). This is correct behavior.

## Known Stubs

None — this task is pure repo hygiene with no UI/data stubs.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: information-disclosure | scripts/purge-history.sh | Script content lists all internal paths being purged — this is intentional and necessary for the script to function; no new surface created |

## Self-Check

- [x] CONTRIBUTING.md exists at `/Users/ishiland/Code/geolens/CONTRIBUTING.md`
- [x] scripts/purge-history.sh exists and is executable
- [x] Commit `182f490b` exists (current work)
- [x] Commit `964711d4` exists (cleanup)
- [x] Commit `b63b47e9` exists (contributing + script)
- [x] 0 PNGs in repo root
- [x] 0 audits/ entries in `git ls-files docs/`
- [x] .agents/ in .gitignore
- [x] docs/widget-development.md tracked

## Self-Check: PASSED
