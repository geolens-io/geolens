---
phase: 260329-p4p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/assets/basemaps/positron.png
  - frontend/src/assets/basemaps/dark.png
  - frontend/src/assets/basemaps/osm.png
  - frontend/src/assets/basemaps/bright.png
  - frontend/src/components/builder/BasemapPicker.tsx
autonomous: false
requirements: [BASEMAP-THUMBS]

must_haves:
  truths:
    - "Built-in basemap thumbnails show recognizable map imagery, not colored rectangles"
    - "Custom/admin-added basemaps display a generic map icon fallback"
    - "Thumbnails are slightly larger than before for better visibility"
    - "4-column grid layout is preserved"
  artifacts:
    - path: "frontend/src/assets/basemaps/positron.png"
      provides: "Positron basemap screenshot thumbnail"
    - path: "frontend/src/assets/basemaps/dark.png"
      provides: "Dark basemap screenshot thumbnail"
    - path: "frontend/src/assets/basemaps/osm.png"
      provides: "OpenStreetMap basemap screenshot thumbnail"
    - path: "frontend/src/assets/basemaps/bright.png"
      provides: "Bright basemap screenshot thumbnail"
    - path: "frontend/src/components/builder/BasemapPicker.tsx"
      provides: "Refactored basemap picker using static PNG imports and fallback icon"
  key_links:
    - from: "frontend/src/components/builder/BasemapPicker.tsx"
      to: "frontend/src/assets/basemaps/*.png"
      via: "Vite static imports"
      pattern: "import.*from.*@/assets/basemaps"
---

<objective>
Replace inline SVG basemap thumbnails with static PNG screenshots for built-in basemaps, add a globe/map icon SVG fallback for custom basemaps, and increase thumbnail size slightly.

Purpose: Basemap thumbnails currently show indistinguishable colored rectangles with grid lines. Real map screenshots make basemap selection intuitive.
Output: 4 PNG screenshot assets + refactored BasemapPicker component
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/components/builder/BasemapPicker.tsx
@frontend/src/lib/basemap-utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture basemap screenshots and create static assets</name>
  <files>
    frontend/src/assets/basemaps/positron.png
    frontend/src/assets/basemaps/dark.png
    frontend/src/assets/basemaps/osm.png
    frontend/src/assets/basemaps/bright.png
  </files>
  <action>
Create directory `frontend/src/assets/basemaps/`.

Capture 320x320 pixel PNG screenshots for each of the 4 built-in basemaps. Use a headless approach: launch a headless browser (Playwright or puppeteer if available, otherwise use curl to fetch raster tiles and ImageMagick to compose) showing each basemap style at approximately zoom 4-5 centered on an area with visible land, water, and roads (e.g., lat 40, lng -74 for eastern US/Atlantic).

Basemap sources:
- **positron**: `https://tiles.openfreemap.org/styles/positron` (vector style JSON -- render in MapLibre)
- **dark**: `https://tiles.openfreemap.org/styles/dark` (vector style JSON)
- **bright**: `https://tiles.openfreemap.org/styles/bright` (vector style JSON)
- **osm**: For OpenStreetMap raster tiles, compose a 320x320 crop from `https://tile.openstreetmap.org/{z}/{x}/{y}.png` tiles at z5 covering a recognizable area, or screenshot from openstreetmap.org.

If headless rendering is not feasible (no Playwright/puppeteer available), create placeholder PNGs using ImageMagick or Python PIL with distinct, recognizable color schemes matching each basemap's palette:
- positron: light gray (#e8e8e8) base with subtle light blue water shapes and thin gray road lines
- dark: dark navy (#1a1a2e) base with darker water and faint gray road lines
- bright: warm cream (#f5f0e8) base with green land masses and blue water
- osm: cream (#f0ede8) base with green parks, blue water, visible road network

Optimize each PNG to under 30KB using available compression tools (pngquant, optipng, or Python PIL with optimize=True).
  </action>
  <verify>
    <automated>ls -la frontend/src/assets/basemaps/*.png && test $(ls frontend/src/assets/basemaps/*.png | wc -l) -eq 4 && echo "All 4 PNGs exist"</automated>
  </verify>
  <done>4 PNG files exist in frontend/src/assets/basemaps/, each under 30KB, each 320x320 or similar resolution, visually distinct from one another</done>
</task>

<task type="auto">
  <name>Task 2: Refactor BasemapPicker to use static PNG imports and fallback icon</name>
  <files>frontend/src/components/builder/BasemapPicker.tsx</files>
  <action>
Refactor `frontend/src/components/builder/BasemapPicker.tsx`:

1. **Replace the `basemapThumbnail()` function.** Remove all inline SVG data URIs. Add Vite static imports at the top of the file:
   ```
   import positronThumb from '@/assets/basemaps/positron.png';
   import darkThumb from '@/assets/basemaps/dark.png';
   import osmThumb from '@/assets/basemaps/osm.png';
   import brightThumb from '@/assets/basemaps/bright.png';
   ```

2. **Create a lookup map and fallback:**
   ```
   const BUILTIN_THUMBNAILS: Record<string, string> = {
     'openfreemap-positron': positronThumb,
     'openfreemap-dark': darkThumb,
     'openstreetmap': osmThumb,
     'osm-standard': osmThumb,  // legacy alias
     'openfreemap-bright': brightThumb,
   };
   ```

3. **Create a globe/map SVG fallback** for custom basemaps (replacing the gray grid default). Use an inline SVG data URI showing a globe with latitude/longitude lines -- a recognizable "map" icon at small sizes:
   ```
   const FALLBACK_THUMBNAIL = `data:image/svg+xml,${encodeURIComponent(
     '<svg xmlns="http://www.w3.org/2000/svg" width="160" height="160" viewBox="0 0 160 160">' +
     '<rect fill="#e5e7eb" width="160" height="160" rx="8"/>' +
     '<circle cx="80" cy="72" r="36" fill="none" stroke="#9ca3af" stroke-width="2"/>' +
     '<ellipse cx="80" cy="72" rx="16" ry="36" fill="none" stroke="#9ca3af" stroke-width="1.5"/>' +
     '<line x1="44" y1="72" x2="116" y2="72" stroke="#9ca3af" stroke-width="1.5"/>' +
     '<line x1="80" y1="36" x2="80" y2="108" stroke="#9ca3af" stroke-width="1.5"/>' +
     '<text x="80" y="136" text-anchor="middle" font-size="12" fill="#9ca3af" font-family="system-ui,sans-serif">Map</text>' +
     '</svg>'
   )}`;
   ```

4. **Rewrite `basemapThumbnail()`:**
   ```
   function basemapThumbnail(id: string): string {
     return BUILTIN_THUMBNAILS[id] ?? FALLBACK_THUMBNAIL;
   }
   ```

5. **Increase thumbnail size slightly:**
   - Collapsed row thumbnail: change `w-6 h-6` (24px) to `w-8 h-8` (32px)
   - Expanded grid: change `gap-1.5` to `gap-2` for breathing room
   - Keep `grid-cols-4`, `w-full aspect-square`, and `text-[9px]` labels unchanged

Do NOT change any other component logic (open/close state, selection, labels toggle, etc.).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && npx tsc --noEmit 2>&1 | head -30 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>BasemapPicker imports 4 static PNGs for built-in basemaps, uses globe SVG fallback for unknown IDs, collapsed thumbnail is 32px (w-8 h-8), grid gap is gap-2, TypeScript compiles clean, existing tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify basemap thumbnails visually</name>
  <files>frontend/src/components/builder/BasemapPicker.tsx</files>
  <action>
Human verifies the basemap thumbnail replacement looks correct in the running application.
  </action>
  <verify>User confirms visual quality of thumbnails</verify>
  <done>User approves basemap thumbnail appearance</done>
  <what-built>Replaced basemap thumbnails with static PNG screenshots and globe fallback icon, increased thumbnail size</what-built>
  <how-to-verify>
    1. Open http://localhost:8080 and navigate to the Map Builder
    2. In the basemap picker sidebar, click to expand the basemap grid
    3. Verify: Each of the 4 built-in basemaps (Positron, Dark, OSM, Bright) shows a recognizable map image, not a colored rectangle
    4. Verify: Thumbnails are visually distinct and help identify each basemap style
    5. Verify: The collapsed row shows a slightly larger thumbnail than before (32px vs 24px)
    6. Verify: Grid layout is still 4 columns with adequate spacing
    7. If any custom basemaps are configured, verify they show a globe/map icon fallback instead of a gray grid
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles without errors: `npx tsc --noEmit`
- Existing test suite passes: `npx vitest run`
- 4 PNG assets exist in `frontend/src/assets/basemaps/`
- BasemapPicker imports PNGs via Vite static imports (not data URIs for built-ins)
- Fallback is a globe SVG, not a gray grid
</verification>

<success_criteria>
- Built-in basemap thumbnails are visually descriptive real map screenshots
- Custom basemaps get a recognizable map/globe fallback icon
- Thumbnail size increased (32px collapsed, gap-2 grid)
- 4-column grid layout preserved, labels unchanged
- No regressions in basemap selection functionality
</success_criteria>

<output>
After completion, create `.planning/quick/260329-p4p-in-map-creator-basemap-thumbnails-are-no/260329-p4p-SUMMARY.md`
</output>
