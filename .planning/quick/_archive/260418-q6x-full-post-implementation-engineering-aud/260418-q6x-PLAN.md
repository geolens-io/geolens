---
phase: 260418-q6x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/processing/ingest/metadata.py
  - backend/app/modules/catalog/features/service.py
  - backend/app/processing/ai/router.py
  - backend/app/processing/ingest/router.py
  - backend/app/processing/ingest/tasks_common.py
  - backend/app/processing/ingest/tasks_vrt.py
  - backend/app/processing/embeddings/service.py
  - backend/app/platform/cache/redis.py
  - frontend/src/hooks/use-settings.ts
  - frontend/src/components/maps/hooks/use-map-thumbnail.ts
  - frontend/src/components/dataset/DistributionsList.tsx
  - frontend/src/components/dataset/tabs/AccessTab.tsx
  - frontend/src/components/dataset/tabs/OverviewTab.tsx
  - frontend/src/components/admin/ApiKeyRevealDialog.tsx
  - frontend/src/components/builder/StyleSpecView.tsx
  - frontend/src/components/map/FeaturePopup.tsx
  - docs/audits/post-impl-20260418.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "All SQL table/column identifiers in metadata.py and features/service.py use _qtable() or quoted identifiers consistently"
    - "except Exception blocks in AI/ingest routers are narrowed to specific exception types where appropriate, or annotated with justification"
    - "Stale debug artifacts (app_structure.txt, builder-snapshot.md, layer-detail.md) are removed from git tracking"
    - "Frontend setTimeout calls in event handlers have proper cleanup or are documented as intentional fire-and-forget"
    - "staleTime: Infinity on settings and thumbnail hooks is removed or guarded by query invalidation"
    - "Audit report documents all findings with severity, file, and evidence"
  artifacts:
    - path: "docs/audits/post-impl-20260418.md"
      provides: "Full audit findings report"
    - path: "backend/app/modules/catalog/features/service.py"
      provides: "Quoted SQL identifiers via _qtable()"
      contains: "_qtable"
    - path: "backend/app/processing/ingest/metadata.py"
      provides: "Consistent _qtable() usage for all data.{table} references"
      contains: "_qtable"
  key_links:
    - from: "backend/app/modules/catalog/features/service.py"
      to: "backend/app/processing/ingest/metadata.py"
      via: "_qtable import"
      pattern: "from.*metadata.*import.*_qtable"
---

<objective>
Full post-implementation engineering audit covering SQL safety, exception handling, stale artifacts, React timer cleanup, and query cache hygiene.

Purpose: Harden the codebase after cumulative feature work. Prior audits fixed 300+ findings; this sweep catches remaining inconsistencies and new patterns from recent OGC API import work.
Output: Remediated code across backend SQL modules and frontend hooks, stale files removed, audit report at docs/audits/post-impl-20260418.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@docs/testing-and-ci.md
@docs/audits/post-impl-20260413.md
@.agents/skills/geolens-post-impl/SKILL.md

Key patterns:
- `_qtable()` in metadata.py (line 40): validates then returns `"data"."table_name"` — the canonical quoting function.
- `_validate_table_name()` in both metadata.py and features/service.py: regex `^[a-z0-9_]+$` — validation exists but quoting is inconsistent.
- `_sql_quote_ident()` in metadata.py (line 46): quotes individual column identifiers.

Guardrails from SKILL.md — do NOT flag these as problems:
- MapLibre imperative source/layer management
- transformRequest workarounds applied imperatively
- Zustand getState() outside React render
- GDAL/ogr2ogr subprocess usage in ingest
- FastAPI trailing-slash conventions (including OGC exception)
- console.error inside error boundaries or catch blocks
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQL identifier quoting + stale artifact removal + backend exception triage</name>
  <files>
    backend/app/modules/catalog/features/service.py,
    backend/app/processing/ingest/metadata.py,
    backend/app/processing/ai/router.py,
    backend/app/processing/ingest/router.py,
    backend/app/processing/ingest/tasks_common.py,
    backend/app/processing/ingest/tasks_vrt.py,
    backend/app/processing/embeddings/service.py,
    backend/app/platform/cache/redis.py,
    app_structure.txt,
    builder-snapshot.md,
    layer-detail.md
  </files>
  <action>
**P0 — SQL identifier quoting consistency:**

1. In `features/service.py`:
   - Import `_qtable` from `app.processing.ingest.metadata` (it already imports `extract_metadata` from there).
   - Replace every `data.{table_name}` string interpolation with `{_qtable(table_name)}`. There are 9 occurrences at lines: 129, 146, 177, 193, 221, 304, 346, 393, 412.
   - Remove the local `_validate_table_name()` function (lines 41-43) since `_qtable()` calls `_validate_table_name()` internally. Keep the `_COLUMN_NAME_RE` validation for column names — that is a different concern.
   - Each function currently calls `_validate_table_name(table_name)` before constructing SQL. After switching to `_qtable(table_name)`, these explicit calls become redundant for the table name (since `_qtable` validates internally). Remove only the redundant `_validate_table_name(table_name)` calls that precede SQL construction using `_qtable()`.

2. In `metadata.py`:
   - Lines 89, 93, 534, 660: replace `data.{table_name}` with `{_qtable(table_name)}`. These already call `_validate_table_name()` upstream, but `_qtable()` gives defense-in-depth quoting.
   - Line 660 also interpolates `{geom_col}` — wrap that with `_sql_quote_ident(geom_col)` (already validated on line 658, but quote for consistency).
   - Lines 93-94: the `{x_column}` and `{y_column}` interpolations — these are validated by `_TABLE_NAME_RE.match()` on line 85, wrap with `_sql_quote_ident()` for belt-and-suspenders.

**P1 — Stale git artifacts:**

3. Delete from git tracking:
   - `app_structure.txt`
   - `builder-snapshot.md`
   - `layer-detail.md`
   Run `git rm app_structure.txt builder-snapshot.md layer-detail.md`.

**P1 — Exception handling triage:**

4. Review each `except Exception` in these files and apply one of:
   - **Narrow** to specific exception type if the catch block handles a known failure mode (e.g., `subprocess.CalledProcessError`, `json.JSONDecodeError`, `sqlalchemy.exc.SQLAlchemyError`, `httpx.HTTPError`, `ValueError`, `OSError`).
   - **Annotate** with `# broad: <reason>` comment if the catch-all is intentional (e.g., wrapping an external process where any failure should yield the same user-facing error). This documents the audit decision.
   - Do NOT change exception handling in code paths where the catch block does logging + re-raise or logging + return-error-response for truly unpredictable external failures (GDAL subprocesses, AI model calls). Just add the annotation comment.

   Priority files (highest except-Exception density):
   - `processing/ai/router.py` (10 occurrences)
   - `processing/ingest/router.py` (9 occurrences)
   - `processing/ingest/tasks_common.py` (5)
   - `processing/ingest/metadata.py` (5)
   - `processing/ingest/tasks_vrt.py` (4)
   - `processing/embeddings/service.py` (4)
   - `platform/cache/redis.py` (4) — narrow to `redis.RedisError` where possible
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && uv run ruff check . && uv run ruff format --check . && uv run python -c "from app.modules.catalog.features.service import get_features; print('import ok')" && uv run python -c "from app.processing.ingest.metadata import _qtable; print(_qtable('test_table'))"</automated>
  </verify>
  <done>
    - All `data.{table_name}` interpolations in features/service.py use `_qtable()`
    - All `data.{table_name}` interpolations in metadata.py use `_qtable()`
    - Column name interpolations in metadata.py use `_sql_quote_ident()`
    - Three stale root-level files removed from git
    - Every `except Exception` in the 7 priority files is either narrowed or annotated
    - ruff check and format pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend timer cleanup + query cache fixes + audit report</name>
  <files>
    frontend/src/hooks/use-settings.ts,
    frontend/src/components/maps/hooks/use-map-thumbnail.ts,
    frontend/src/components/dataset/DistributionsList.tsx,
    frontend/src/components/dataset/tabs/AccessTab.tsx,
    frontend/src/components/dataset/tabs/OverviewTab.tsx,
    frontend/src/components/admin/ApiKeyRevealDialog.tsx,
    frontend/src/components/builder/StyleSpecView.tsx,
    frontend/src/components/map/FeaturePopup.tsx,
    docs/audits/post-impl-20260418.md
  </files>
  <action>
**P2 — staleTime: Infinity fixes:**

1. `use-settings.ts` (line 71): Change `staleTime: Infinity` to `staleTime: 5 * 60 * 1000` (5 minutes). Settings can change via admin panel, and Infinity means the hook never refetches. Check whether the settings mutation hooks already call `queryClient.invalidateQueries` for the settings query key — if they do, add a comment noting that; if they don't, the staleTime change is the fix.

2. `use-map-thumbnail.ts` (line 12): Change `staleTime: Infinity` to `staleTime: 60 * 1000` (1 minute). Thumbnails regenerate on re-capture. Same check: does the thumbnail mutation invalidate the query? If so, annotate. If not, the staleTime change prevents stale thumbnails.

3. Leave `use-edition.ts` (`staleTime: Infinity`) alone — edition is static per deployment, this is correct.
4. Leave `use-ingest.ts` (`staleTime: Infinity`) alone — job formats are static, correct.

**P2 — Fire-and-forget setTimeout cleanup:**

5. The following components use `setTimeout(() => setCopied(false), 2000)` without cleanup. This is a fire-and-forget pattern that can set state on an unmounted component. For each, use a ref-based pattern:

   ```typescript
   const timerRef = useRef<ReturnType<typeof setTimeout>>(undefined);
   // In the copy handler:
   clearTimeout(timerRef.current);
   timerRef.current = setTimeout(() => setCopied(false), 2000);
   // Add useEffect cleanup:
   useEffect(() => () => clearTimeout(timerRef.current), []);
   ```

   Apply to these files:
   - `DistributionsList.tsx` (line 87)
   - `AccessTab.tsx` (line 29)
   - `OverviewTab.tsx` (line 117)
   - `ApiKeyRevealDialog.tsx` (line 40)
   - `StyleSpecView.tsx` (lines 42, 45) — uses `setCopyState('idle')`, same pattern but for `copyState`
   - `FeaturePopup.tsx` (line 82) — uses `setCopiedKey(null)`, same pattern

**P3 — Audit report:**

6. Write `docs/audits/post-impl-20260418.md` summarizing ALL findings from this audit. Follow the format of the prior audit at `docs/audits/post-impl-20260413.md`. Include:
   - Date, scope (full), commit range
   - P0/P1/P2/P3 findings table with: ID, severity, category, file:line, description, status (fixed/annotated/deferred)
   - Baseline verification status (linting, tests)
   - Summary of what was changed vs. what was left as-is and why
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npx tsc --noEmit && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `staleTime: Infinity` removed from settings and thumbnail hooks, replaced with finite values
    - 6 components with fire-and-forget setTimeout now have ref-based cleanup
    - TypeScript compiles cleanly
    - Lint passes (warnings-only acceptable)
    - docs/audits/post-impl-20260418.md exists with categorized findings table
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL query construction | User-controlled dataset table names interpolated into SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q6x-01 | Tampering | features/service.py SQL interpolation | mitigate | Switch all `data.{table_name}` to `_qtable(table_name)` which validates + double-quotes identifiers |
| T-q6x-02 | Tampering | metadata.py SQL interpolation | mitigate | Switch remaining unquoted `data.{table_name}` to `_qtable()`, wrap column idents with `_sql_quote_ident()` |
| T-q6x-03 | Information Disclosure | Stale debug files in git | mitigate | Remove app_structure.txt, builder-snapshot.md, layer-detail.md from tracking |
| T-q6x-04 | Denial of Service | Broad `except Exception` swallowing OOM/resource errors | accept | Most are in ingest/AI routers where any subprocess failure should yield user-facing error; annotated for future narrowing |
</threat_model>

<verification>
1. Backend: `cd backend && uv run ruff check . && uv run ruff format --check .`
2. Frontend: `cd frontend && npx tsc --noEmit && npm run lint`
3. Grep: no remaining `data.{table_name}` without quoting in features/service.py or metadata.py
4. Grep: no `staleTime: Infinity` in use-settings.ts or use-map-thumbnail.ts
5. Confirm stale files removed: `git status` shows deleted files
</verification>

<success_criteria>
- Zero unquoted `data.{table_name}` SQL interpolations in features/service.py and metadata.py
- All `except Exception` blocks in priority files annotated or narrowed
- Three stale debug files removed from git
- staleTime: Infinity removed from settings and thumbnail hooks
- Six fire-and-forget setTimeout patterns converted to ref-based cleanup
- Audit report written at docs/audits/post-impl-20260418.md
- Backend lint clean, frontend tsc + lint clean
</success_criteria>

<output>
After completion, create `.planning/quick/260418-q6x-full-post-implementation-engineering-aud/260418-q6x-SUMMARY.md`
</output>
