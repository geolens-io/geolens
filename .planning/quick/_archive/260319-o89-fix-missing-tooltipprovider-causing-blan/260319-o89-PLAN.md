---
phase: quick
plan: 260319-o89
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/main.tsx
autonomous: true
requirements: [HOTFIX]

must_haves:
  truths:
    - "Root search page at / renders without crashing"
    - "Tooltips work throughout the app without requiring per-component TooltipProvider wrappers"
  artifacts:
    - path: "frontend/src/main.tsx"
      provides: "App-level TooltipProvider wrapping all routes"
      contains: "TooltipProvider"
  key_links:
    - from: "frontend/src/main.tsx"
      to: "frontend/src/components/ui/tooltip.tsx"
      via: "import { TooltipProvider }"
      pattern: "TooltipProvider"
---

<objective>
Fix blank white page crash on the root search page caused by missing TooltipProvider.

Purpose: Any component using `<Tooltip>` from radix-ui crashes with "Tooltip must be used within TooltipProvider" because main.tsx never wraps the app in `<TooltipProvider>`. This makes the entire app unusable.

Output: Working app with TooltipProvider at the root level.
</objective>

<execution_context>
@/Users/ishiland/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishiland/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/main.tsx
@frontend/src/components/ui/tooltip.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TooltipProvider to app root in main.tsx</name>
  <files>frontend/src/main.tsx</files>
  <action>
    Import `TooltipProvider` from `@/components/ui/tooltip`.

    In the render tree, wrap `<BrowserRouter>` and `<ThemedToaster />` with `<TooltipProvider>`, placing it inside `<ThemeProvider>`:

    ```tsx
    <ThemeProvider defaultTheme="system" storageKey="geolens-theme">
      <TooltipProvider>
        <BrowserRouter>
          <App />
        </BrowserRouter>
        <ThemedToaster />
      </TooltipProvider>
    </ThemeProvider>
    ```

    This ensures every component in the tree has access to the tooltip context. The default `delayDuration=0` from the TooltipProvider component is fine.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>main.tsx imports and renders TooltipProvider wrapping the app. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- App loads at http://localhost:8080/ without blank page or console errors
</verification>

<success_criteria>
- Root search page renders content instead of blank white page
- No "Tooltip must be used within TooltipProvider" console error
- All existing tooltip usage throughout the app continues to work
</success_criteria>

<output>
After completion, create `.planning/quick/260319-o89-fix-missing-tooltipprovider-causing-blan/260319-o89-SUMMARY.md`
</output>
