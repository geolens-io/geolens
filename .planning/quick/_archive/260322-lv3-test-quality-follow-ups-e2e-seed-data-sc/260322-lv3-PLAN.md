---
phase: quick-260322-lv3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seed-e2e.py
  - backend/tests/test_ingest.py
  - .planning/STATE.md
autonomous: true
requirements: [SEED-E2E, VERIFY-M42, VERIFY-F9L, CSV-PIPELINE-TEST]

must_haves:
  truths:
    - "E2e seed script creates the 2 critical datasets needed by Playwright tests"
    - "260320-m42 (multi-part geometry safety) is marked Verified in STATE.md"
    - "260321-f9l (error boundaries with i18n) is marked Verified in STATE.md"
    - "Non-spatial CSV pipeline test proves table registration, record_type=table, and feature query"
  artifacts:
    - path: "scripts/seed-e2e.py"
      provides: "Minimal e2e seed script for Playwright prerequisites"
      min_lines: 80
    - path: "backend/tests/test_ingest.py"
      provides: "TestCsvNonSpatialPipeline integration test class"
      contains: "class TestCsvNonSpatialPipeline"
  key_links:
    - from: "scripts/seed-e2e.py"
      to: "/api/ingest/upload"
      via: "httpx upload + preview + commit + poll"
      pattern: "ingest/upload"
    - from: "scripts/seed-e2e.py"
      to: "/api/catalog/collections/"
      via: "httpx POST create collection then POST add datasets"
      pattern: "catalog/collections"
    - from: "backend/tests/test_ingest.py"
      to: "backend/app/ingest/service.py"
      via: "register_existing_table path for non-spatial table"
      pattern: "ingest/register"
---

<objective>
Three test and quality follow-up items: (1) create a minimal e2e seed script that populates only the 2 datasets Playwright tests need, (2) retroactively verify two completed quick tasks and update STATE.md, (3) add a non-spatial CSV integration test that exercises the full register-and-query pipeline.

Purpose: Unblock e2e test runs, close verification gaps, and prove the non-spatial CSV pipeline end-to-end.
Output: `scripts/seed-e2e.py`, updated `backend/tests/test_ingest.py`, updated `.planning/STATE.md`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@scripts/seed-natural-earth.py (reference for upload+preview+commit+poll pattern)
@backend/tests/test_ingest.py (existing CSV upload test to extend)
@backend/app/ingest/service.py (register_existing_table path)
@backend/app/datasets/service.py (record_type = "table" when geometry_type is None)
@backend/app/collections/schemas.py (CollectionCreate accepts 'name', CollectionAddDatasetsRequest accepts 'dataset_ids')
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create minimal e2e seed script</name>
  <files>scripts/seed-e2e.py</files>
  <action>
Create `scripts/seed-e2e.py` that seeds only the 2 datasets needed by Playwright e2e tests. Follow the pattern in `seed-natural-earth.py` (httpx async, upload/preview/commit/poll via the HTTP API).

Hardcoded manifest of 2 datasets:
- `ne_10m_admin_0_countries` (cultural) -- needed by dataset-detail.spec.ts, collections.spec.ts
- `ne_10m_reefs` (physical) -- needed by search.spec.ts

Script should:
1. Accept `--base-url` (default `http://localhost:8080`), `--api-key` (required), `--cache-dir` (default `./cache`)
2. Download each zip from `https://naciscdn.org/naturalearth/10m/{theme}/{stem}.zip`
3. Cache downloads locally to avoid re-downloading
4. Upload via the 3-step ingest API: POST /api/ingest/upload, POST /api/ingest/preview/{job_id}, POST /api/ingest/commit/{job_id} (with title, visibility=public, srid_override=4326)
5. Poll GET /api/jobs/{job_id} until complete/failed (3s interval, 120s timeout)
6. Create a "World Countries" collection in two API calls:
   a. POST /api/catalog/collections/ with `{"name": "World Countries"}` -- CollectionCreate schema accepts `name` (not `title`) and optional `description`
   b. Extract the collection `id` from the response
   c. POST /api/catalog/collections/{collection_id}/datasets with `{"dataset_ids": [countries_dataset_id]}` -- CollectionAddDatasetsRequest schema accepts `dataset_ids` (list of UUIDs)
7. Print summary: datasets seeded, collection created, total time
8. Make it executable with `#!/usr/bin/env python3` and argparse CLI
9. Exit 0 on success, 1 on any failure

Derive display names from stems: `ne_10m_admin_0_countries` -> "Admin 0 Countries (10m)", `ne_10m_reefs` -> "Reefs (10m)".

Keep the script simple -- no retry logic, no concurrency (only 2 datasets). ~120 lines.
  </action>
  <verify>
    <automated>python scripts/seed-e2e.py --help</automated>
  </verify>
  <done>Script prints usage info with --base-url, --api-key, --cache-dir arguments. Script is executable and syntactically valid.</done>
</task>

<task type="auto">
  <name>Task 2: Retroactive verification of 260320-m42 and 260321-f9l</name>
  <files>.planning/STATE.md</files>
  <action>
Verify both tasks are intact by running grep checks, then update STATE.md.

For 260320-m42 (multi-part geometry safety), confirm:
1. `_geometry_sql()` in `backend/app/features/service.py` wraps with ST_Multi
2. `isMultiPartGeometry()` exported from `frontend/src/hooks/use-terra-draw.ts`
3. Multi-part editing guard in `frontend/src/hooks/use-feature-editing.ts` calls isMultiPartGeometry
4. Tests exist in `use-terra-draw.test.ts` and `test_features_crud.py`

For 260321-f9l (error boundaries with i18n), confirm:
1. All 4 boundary components exist in `frontend/src/components/error/`
2. AppErrorBoundary wraps in main.tsx
3. LazyLoadErrorBoundary wraps Suspense in App.tsx
4. RouteErrorBoundary on dataset/map routes in App.tsx
5. MapErrorBoundary wraps BuilderMap in MapBuilderPage.tsx
6. i18n keys in all 4 locales
7. Tests in ErrorBoundaries.test.tsx

After confirming both intact, update STATE.md Quick Tasks Completed table:
- Change 260320-m42 row (line ~139) status from "Complete" to "Verified"
- Change 260321-f9l row (line ~141) status from "Complete" to "Verified"
  </action>
  <verify>
    <automated>grep -c "260320-m42.*Verified\|260321-f9l.*Verified" .planning/STATE.md</automated>
  </verify>
  <done>Both grep checks pass (count = 2), confirming both tasks marked Verified in STATE.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Non-spatial CSV end-to-end integration test</name>
  <files>backend/tests/test_ingest.py</files>
  <behavior>
    - Test: Creating a non-spatial table in data schema, registering it via POST /ingest/register, results in a dataset with record_type='table'
    - Test: The registered non-spatial dataset is queryable via GET /datasets/{id}/features/ and returns the expected rows
    - Test: The dataset has no geometry columns (geometry_type is None)
  </behavior>
  <action>
Add `TestCsvNonSpatialPipeline` class to `backend/tests/test_ingest.py` with a single comprehensive test method `test_csv_non_spatial_full_pipeline`.

The class MUST include explicit no-op overrides for the two module-level autouse fixtures to prevent interference with the register path:

```python
class TestCsvNonSpatialPipeline:
    @pytest.fixture(autouse=True)
    def mock_ingest_task(self):
        """No-op override -- register path does not defer tasks."""
        yield

    @pytest.fixture(autouse=True)
    def mock_file_save(self):
        """No-op override -- register path does not save files."""
        yield
```

These class-level fixtures shadow the module-level `mock_ingest_task` and `mock_file_save` autouse fixtures, ensuring the register endpoint talks to real services without patched deferral or file-save mocks.

The test should:
1. Create a non-spatial table directly in the test DB data schema:
   ```sql
   CREATE TABLE data.test_csv_nonspatial (
       ogc_fid serial PRIMARY KEY,
       name text,
       value integer
   )
   ```
2. Insert 2 rows: ('Alice', 100), ('Bob', 200)
3. Register via POST /ingest/register with `{"table_name": "test_csv_nonspatial", "title": "Test CSV Table"}`
4. Assert 201 response with dataset_id
5. GET /datasets/{dataset_id}/ and verify record_type == 'table'
6. GET /datasets/{dataset_id}/features/ and verify 2 features returned with correct name/value data
7. Verify geometry_type is None in the dataset response

Clean up the created table in the data schema after test (use try/finally or fixture teardown).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_ingest.py::TestCsvNonSpatialPipeline -xvs 2>&1 | tail -20</automated>
  </verify>
  <done>Test passes: non-spatial table registered as record_type='table', features API returns 2 rows, no geometry columns present.</done>
</task>

</tasks>

<verification>
1. `python scripts/seed-e2e.py --help` prints usage without error
2. `grep -c "Verified" .planning/STATE.md` shows both 260320-m42 and 260321-f9l as Verified
3. `cd backend && python -m pytest tests/test_ingest.py::TestCsvNonSpatialPipeline -xvs` passes
</verification>

<success_criteria>
- E2e seed script exists, is syntactically valid, and documents its 2-dataset manifest
- Both retroactive tasks marked Verified in STATE.md after code-level confirmation
- Non-spatial CSV pipeline test proves the full register -> query path with record_type='table'
</success_criteria>

<output>
After completion, create `.planning/quick/260322-lv3-test-quality-follow-ups-e2e-seed-data-sc/260322-lv3-SUMMARY.md`
</output>
