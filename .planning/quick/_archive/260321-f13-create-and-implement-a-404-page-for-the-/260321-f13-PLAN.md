---
phase: quick-260321-f13
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/NotFoundPage.tsx
  - frontend/src/App.tsx
  - frontend/src/i18n/locales/en/common.json
  - frontend/src/i18n/locales/fr/common.json
  - frontend/src/i18n/locales/es/common.json
  - frontend/src/i18n/locales/de/common.json
autonomous: true
requirements: [QUICK-F13]

must_haves:
  truths:
    - "Navigating to a non-existent route shows a 404 page instead of silently redirecting home"
    - "The 404 page has a link back to the home page"
    - "The 404 page renders within the AppLayout (navbar visible)"
  artifacts:
    - path: "frontend/src/pages/NotFoundPage.tsx"
      provides: "404 page component"
      exports: ["NotFoundPage"]
    - path: "frontend/src/App.tsx"
      provides: "Catch-all route wired to NotFoundPage"
      contains: "NotFoundPage"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/NotFoundPage.tsx"
      via: "lazy import + Route path='*'"
      pattern: "path=\"\\*\".*NotFoundPage"
---

<objective>
Create a proper 404 Not Found page and wire it into the router.

Purpose: Currently the catch-all route (`path="*"`) silently redirects to `/`. Users navigating to invalid URLs get no feedback about what happened. A dedicated 404 page provides clear feedback and a path back to the app.

Output: NotFoundPage component, updated router catch-all, i18n keys in all 4 locales.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/App.tsx
@frontend/src/components/layout/AppLayout.tsx
@frontend/src/components/layout/ErrorState.tsx
@frontend/src/components/GeoLensLogo.tsx
@frontend/src/i18n/locales/en/common.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NotFoundPage component and i18n keys</name>
  <files>frontend/src/pages/NotFoundPage.tsx, frontend/src/i18n/locales/en/common.json, frontend/src/i18n/locales/fr/common.json, frontend/src/i18n/locales/es/common.json, frontend/src/i18n/locales/de/common.json</files>
  <action>
Create `frontend/src/pages/NotFoundPage.tsx` as a named export `NotFoundPage`.

Layout: centered content within the page (no extra navbar — AppLayout provides it). Include:
- A large "404" heading (text-6xl font-bold text-muted-foreground/50)
- A title line using `t('common:notFound.title')` (text-xl font-semibold)
- A description using `t('common:notFound.description')` (text-sm text-muted-foreground)
- A "Go home" button using react-router `Link` to "/" with `t('common:notFound.goHome')` text, styled as a primary Button from `@/components/ui/button`

Use `useTranslation('common')` for i18n.

Add i18n keys to all 4 locale `common.json` files under a new `"notFound"` object:

**en:** `{ "title": "Page not found", "description": "The page you are looking for does not exist or has been moved.", "goHome": "Go to home" }`

**fr:** `{ "title": "Page introuvable", "description": "La page que vous recherchez n'existe pas ou a été déplacée.", "goHome": "Aller à l'accueil" }`

**es:** `{ "title": "Página no encontrada", "description": "La página que busca no existe o ha sido movida.", "goHome": "Ir al inicio" }`

**de:** `{ "title": "Seite nicht gefunden", "description": "Die gesuchte Seite existiert nicht oder wurde verschoben.", "goHome": "Zur Startseite" }`
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>NotFoundPage.tsx exists with named export, all 4 locale files have notFound keys, TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire NotFoundPage into router catch-all</name>
  <files>frontend/src/App.tsx</files>
  <action>
In `frontend/src/App.tsx`:

1. Add lazy import at the top with the other page imports:
   `const NotFoundPage = lazy(() => import('./pages/NotFoundPage').then(m => ({ default: m.NotFoundPage })));`

2. Replace the existing catch-all route (line 81):
   `<Route path="*" element={<Navigate to="/" replace />} />`
   with:
   `<Route path="*" element={<NotFoundPage />} />`

Keep the catch-all inside the `<ProtectedRoute>` + `<AppLayout>` wrapper so the navbar is visible. This means unauthenticated users hitting a bad URL will first be sent to login (existing ProtectedRoute behavior), then see the 404 after login. This is acceptable since the app is behind auth.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Catch-all route renders NotFoundPage instead of redirecting to home. Navigate import can be removed if no longer used elsewhere (check first).</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- Navigate to `http://localhost:8080/some-nonexistent-path` while logged in — see 404 page with title, description, and "Go to home" link
- Click "Go to home" link — navigates to `/`
- Navbar is visible on the 404 page
</verification>

<success_criteria>
- Non-existent routes show a dedicated 404 page with clear messaging
- The page includes a navigation link back to the home page
- All 4 locales have translations
- The 404 page renders within the authenticated AppLayout
</success_criteria>

<output>
After completion, create `.planning/quick/260321-f13-create-and-implement-a-404-page-for-the-/260321-f13-SUMMARY.md`
</output>
