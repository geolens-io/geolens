---
phase: quick-260319-qu1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/dataset/DatasetMap.tsx
  - frontend/src/pages/DatasetPage.tsx
autonomous: false
requirements: [QU1-REVIEW]

must_haves:
  truths:
    - "All four record types (vector, raster, vrt, collection) render their map/preview correctly"
    - "Vector tile layers are added imperatively and display for Point, Line, and Polygon geometry types"
    - "Raster/VRT tile layers render with auth headers and error handling"
    - "Map controls (fullscreen, zoom-to-extent, edit trigger) appear in correct states"
    - "No accessibility gaps in map container or controls"
    - "No dead code paths or unreachable branches in DatasetMap"
  artifacts:
    - path: "frontend/src/components/dataset/DatasetMap.tsx"
      provides: "Map rendering for all dataset types"
    - path: "frontend/src/pages/DatasetPage.tsx"
      provides: "Hero map integration with state machine"
  key_links:
    - from: "DatasetPage.tsx"
      to: "DatasetMap.tsx"
      via: "props: bbox, tableName, geometryType, recordType, rasterTileUrl, onMapReady, onTileError"
    - from: "DatasetMap.tsx"
      to: "tile-utils.ts"
      via: "buildSignedTileUrl for vector tiles"
    - from: "DatasetMap.tsx"
      to: "maplibre-gl"
      via: "imperative addSource/addLayer for vector and raster"
---

<objective>
Review the DatasetMap component and its DatasetPage integration for completeness and correctness across all record types (vector, raster, VRT, collection).

Purpose: Identify bugs, missing edge cases, accessibility gaps, dead code, and potential improvements in the detail page map rendering pipeline.
Output: Documented findings with fixes applied for any issues discovered.
</objective>

<execution_context>
@/Users/ishiland/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishiland/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/components/dataset/DatasetMap.tsx
@frontend/src/pages/DatasetPage.tsx
@frontend/src/lib/tile-utils.ts
@frontend/src/lib/map-colors.ts
@frontend/src/components/layout/BBoxPreview.tsx
@frontend/src/components/dataset/__tests__/DatasetMap.test.tsx
@frontend/src/pages/__tests__/DatasetPage.hero.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit DatasetMap and DatasetPage for correctness issues</name>
  <files>frontend/src/components/dataset/DatasetMap.tsx, frontend/src/pages/DatasetPage.tsx, frontend/src/components/dataset/__tests__/DatasetMap.test.tsx, frontend/src/pages/__tests__/DatasetPage.hero.test.tsx</files>
  <action>
Systematically review DatasetMap.tsx and its DatasetPage.tsx integration. Check each area and document findings:

1. **Record type coverage**: Verify all 4 record types are handled correctly:
   - vector_dataset: vector tile layers added imperatively, bbox overlay, edit button
   - raster_dataset: raster tile layer with auth header, hero state machine, no-tile badge
   - vrt_dataset: same as raster path but with vrt_type awareness
   - collection: uses BBoxPreview on CollectionDetailPage (not DatasetMap) -- confirm this is intentional

2. **Geometry type handling**: Check addVectorLayers for all geometry types:
   - Point/MultiPoint -> circle layer
   - LineString/MultiLineString -> line layer
   - Polygon/MultiPolygon -> fill + outline layers
   - GeometryCollection or mixed -> verify fallback behavior

3. **Edge cases**:
   - null/undefined bbox with valid tableName (should still render map)
   - bbox with null tableName and no rasterTileUrl (should show "no spatial extent")
   - Large extent calculation (>90 lon or >60 lat span)
   - Dateline-crossing bboxes (minx > maxx)
   - Zero-area bboxes (point datasets with identical min/max)

4. **Accessibility**:
   - Map container has no aria-label or role
   - Zoom-to-extent button has title but missing aria-label
   - Edit geometry button has no aria-label
   - Fullscreen button correctly has aria-label -- confirm

5. **State management**:
   - vectorLayersAdded ref reset on tableName change
   - rasterLayersAdded ref reset on rasterTileUrl change
   - tileToken refresh updating source in-place
   - Hero state machine transitions (loading->loaded, loading->error, error->loading via retry)

6. **Potential bugs**:
   - transformRequest only checks for '/raster-tiles/' -- confirm vector tiles don't need auth headers (they use query-param auth via buildSignedTileUrl)
   - onMapReady fires immediately for vector datasets but relies on sourcedata event for raster -- check if this causes timing issues
   - raster error handler checks e.error.message.includes('raster-tile-source') -- fragile string matching
   - Theme basemap switching uses transformStyle but may drop custom sources/layers if style names match incorrectly

7. **Missing features or improvements**:
   - No loading indicator for vector tile loading
   - No error handling for vector tile failures
   - NavigationControl only shown during drawing mode for vectors (but always for raster/VRT) -- intentional?
   - scrollZoom only enabled in fullscreen -- intentional UX choice?

For each finding, categorize as: BUG (must fix), IMPROVEMENT (should fix), OBSERVATION (document only).

Apply fixes for any BUG-level findings. For IMPROVEMENT items, apply fixes if they are small and low-risk. Document all OBSERVATION items in the summary.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && npx vitest run frontend/src/components/dataset/__tests__/DatasetMap.test.tsx frontend/src/pages/__tests__/DatasetPage.hero.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All existing tests pass. Any bug fixes applied have corresponding test updates. Findings documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Code review of DatasetMap and DatasetPage hero map integration with any fixes applied</what-built>
  <how-to-verify>
    1. Review the findings summary for any BUG or IMPROVEMENT items that were fixed
    2. Open http://localhost:8080 and navigate to a vector dataset detail page -- confirm map renders with data
    3. Navigate to a raster dataset detail page -- confirm raster tiles render (or no-tile badge if no tiles)
    4. Navigate to a VRT dataset detail page -- confirm same raster rendering path works
    5. Check that edit geometry button appears for editors on vector datasets
    6. Check fullscreen toggle works on the map container
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to address</resume-signal>
</task>

</tasks>

<verification>
- All existing DatasetMap and DatasetPage tests pass
- No regressions introduced by any fixes
- Findings documented in summary
</verification>

<success_criteria>
- Complete audit of DatasetMap covering all record types, geometry types, edge cases, a11y, and state management
- Any BUG-level issues fixed with tests
- Findings categorized and documented in summary
</success_criteria>

<output>
After completion, create `.planning/quick/260319-qu1-review-the-detail-data-page-map-for-comp/260319-qu1-SUMMARY.md`
</output>
