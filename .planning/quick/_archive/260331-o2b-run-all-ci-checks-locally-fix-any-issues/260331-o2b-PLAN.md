---
phase: quick
plan: 260331-o2b
type: execute
wave: 1
depends_on: []
files_modified: []  # determined dynamically by fixes needed
autonomous: true
requirements: [local-ci-parity]

must_haves:
  truths:
    - "All backend lint checks (ruff check, ruff format) pass with zero errors"
    - "All frontend lint checks (eslint, tsc --noEmit, test:i18n, check:i18n:changed) pass with zero errors"
    - "Frontend test suite passes with coverage"
    - "Backend security scan (bandit, pip-audit) passes with zero findings"
    - "All changes are committed and pushed to the remote"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow definition — local commands must match these exactly"
  key_links:
    - from: "local check commands"
      to: ".github/workflows/ci.yml"
      via: "exact command parity"
      pattern: "commands run locally must be identical to CI steps"
---

<objective>
Run every CI check locally (matching the GitHub Actions workflow exactly), fix any failures, then commit and push all changes including the existing unstaged modifications.

Purpose: Ensure the working tree passes CI before pushing.
Output: Clean CI-passing codebase pushed to remote.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all CI lint and security checks, fix failures</name>
  <files>Files will be determined by what fails — likely within backend/ and frontend/</files>
  <action>
Run each CI check locally in the exact order and with the exact commands from .github/workflows/ci.yml. Fix any issues found before moving to the next check. Skip backend-test (needs PostGIS) and e2e-test (needs full stack).

**Backend lint (from backend/):**
1. `cd backend && uv sync --locked --dev`
2. `uv run ruff check .` — if failures, run `uv run ruff check --fix .` then manually fix remaining
3. `uv run ruff format --check .` — if failures, run `uv run ruff format .`

**Frontend lint (from frontend/):**
4. `cd frontend && npm ci`
5. `npm run test:i18n` — if failures, fix locale JSON files to ensure parity
6. `npm run check:i18n:changed` — if failures, add missing translation keys
7. `npm run lint` — if failures, run `npm run lint -- --fix` then manually fix remaining
8. `npx tsc --noEmit` — if failures, fix TypeScript errors in the reported files

**Security scan (from backend/):**
9. `cd backend && uv run bandit -r app/ -c pyproject.toml --severity-level high --confidence-level high`
10. `uv run pip-audit --strict --desc --ignore-vuln CVE-2026-4539`

For each check: if it passes, move on. If it fails, fix the issue, re-run the specific check to confirm the fix, then continue. Keep track of every file modified.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && uv run ruff check . && uv run ruff format --check . && uv run bandit -r app/ -c pyproject.toml --severity-level high --confidence-level high && uv run pip-audit --strict --desc --ignore-vuln CVE-2026-4539 && cd /Users/ishiland/Code/geolens/frontend && npm run test:i18n && npm run check:i18n:changed && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <done>All 8 CI checks (ruff check, ruff format, bandit, pip-audit, test:i18n, check:i18n:changed, eslint, tsc) pass locally with zero errors</done>
</task>

<task type="auto">
  <name>Task 2: Run frontend test suite with coverage</name>
  <files>frontend/ test files if any test fixes needed</files>
  <action>
Run the frontend test suite with coverage, matching the CI command exactly:

1. `cd frontend && npm run test:coverage`
2. If any tests fail, examine the failure output, fix the test or source code as appropriate
3. Re-run until all tests pass

This is separate from Task 1 because test failures may require different fixes than lint issues, and the test suite can take longer to run.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npm run test:coverage</automated>
  </verify>
  <done>Frontend test suite passes with coverage report generated, zero failures</done>
</task>

<task type="auto">
  <name>Task 3: Commit all changes and push</name>
  <files>All modified files from git status (pre-existing unstaged changes + any fixes from Tasks 1-2)</files>
  <action>
1. Run `git status` to see all changes (both pre-existing unstaged and new fixes)
2. Run `git diff --stat` to review the scope of changes
3. Stage all modified files with `git add` (use specific file paths, not -A)
4. Create a single commit with a message summarizing the changes — if fixes were needed mention what was fixed, otherwise note it as cleanup/updates
5. Push to the remote: `git push origin main`
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && git status --porcelain | grep -v '??' | wc -l | grep -q '^0$' && echo "Working tree clean" || echo "FAIL: uncommitted changes remain"</automated>
  </verify>
  <done>All changes committed to main branch and pushed to remote, working tree is clean, git status shows no modified files</done>
</task>

</tasks>

<verification>
Run the full CI check suite one final time before the commit to confirm everything passes:
- Backend: ruff check, ruff format --check, bandit, pip-audit
- Frontend: test:i18n, check:i18n:changed, lint, tsc --noEmit, test:coverage
- Git: working tree clean after push
</verification>

<success_criteria>
- All 5 CI jobs that can run locally (backend-lint, frontend-lint, frontend-test, security-scan) pass with zero errors
- backend-test is skipped (requires PostGIS container) — acceptable since CI handles this
- e2e-test is skipped (requires full Docker stack) — acceptable since CI handles this
- All changes (pre-existing + fixes) are committed and pushed to main
- Working tree is clean
</success_criteria>

<output>
After completion, create `.planning/quick/260331-o2b-run-all-ci-checks-locally-fix-any-issues/260331-o2b-SUMMARY.md`
</output>
