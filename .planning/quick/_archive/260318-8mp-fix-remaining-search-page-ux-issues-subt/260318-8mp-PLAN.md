---
phase: 260318-8mp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/i18n/locales/en/search.json
  - frontend/src/i18n/locales/de/search.json
  - frontend/src/i18n/locales/es/search.json
  - frontend/src/i18n/locales/fr/search.json
  - frontend/src/components/search/FilterPanel.tsx
  - frontend/src/components/search/__tests__/FilterPanel.test.tsx
autonomous: true
requirements: [SEARCH-UX-FIXES]

must_haves:
  truths:
    - "Search subtitle says 'geospatial data' not 'datasets'"
    - "Collection tab label is plural 'Collections' not singular"
    - "'All' tab count matches numberMatched (excludes collection facet count)"
    - "No empty secondary filter row renders for raster/VRT when no org/CRS filters exist"
    - "Date filter label says 'Date Added' not 'Upload Date'"
  artifacts:
    - path: "frontend/src/i18n/locales/en/search.json"
      provides: "Updated subtitle, dateRange, collections keys"
      contains: "geospatial data"
    - path: "frontend/src/components/search/FilterPanel.tsx"
      provides: "Fixed tab counts, plural label, conditional secondary row"
  key_links:
    - from: "FilterPanel.tsx"
      to: "search.json"
      via: "t('filters.collections') for plural tab label"
      pattern: "filters\\.collections"
---

<objective>
Fix five discrete search page UX issues: subtitle language, tab pluralization, count inconsistency between "All" tab and results, empty secondary filter rows, and "Upload Date" label.

Purpose: Eliminate user-facing inconsistencies on the search page identified during UX review.
Output: Corrected i18n strings across 4 locales and fixed FilterPanel logic.
</objective>

<execution_context>
@/Users/ishiland/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishiland/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260318-8mp-fix-remaining-search-page-ux-issues-subt/260318-8mp-RESEARCH.md
@frontend/src/i18n/locales/en/search.json
@frontend/src/components/search/FilterPanel.tsx
@frontend/src/components/search/__tests__/FilterPanel.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix i18n strings across all locales</name>
  <files>
    frontend/src/i18n/locales/en/search.json,
    frontend/src/i18n/locales/de/search.json,
    frontend/src/i18n/locales/es/search.json,
    frontend/src/i18n/locales/fr/search.json
  </files>
  <action>
In each locale's search.json, make these changes:

1. **subtitle** key: Replace dataset-specific wording with generic "geospatial data" phrasing.
   - en: `"Search, preview, and export geospatial data from the catalog"`
   - de: `"Geodaten aus dem Katalog suchen, ansehen und exportieren"`
   - es: `"Busque, previsualice y exporte datos geoespaciales del catalogo"`
   - fr: `"Recherchez, previsualisez et exportez des donnees geospatiales du catalogue"`

2. **filters.dateRange** key: Change from "Upload Date" equivalent to "Date Added" equivalent (matching existing `filters.dateAdded` wording in each locale).
   - en: `"Date Added"` (was "Upload Date")
   - de: `"Hinzugefugt am"` (was "Uploaddatum") -- match existing `dateAdded` key
   - es: `"Fecha de adicion"` (was "Fecha de carga") -- match existing `dateAdded` key
   - fr: `"Date d'ajout"` (was "Date d'import") -- match existing `dateAdded` key

3. **Add `filters.collections`** (plural) key for tab labels. Keep existing singular `filters.collection` for filter chips.
   - en: `"Collections"`
   - de: `"Sammlungen"`
   - es: `"Colecciones"`
   - fr: `"Collections"`

Use proper UTF-8 accented characters (e, u with accents etc.) matching the existing locale style.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && node -e "
      const locales = ['en','de','es','fr'];
      for (const l of locales) {
        const s = require('./frontend/src/i18n/locales/' + l + '/search.json');
        console.assert(!s.subtitle.toLowerCase().includes('dataset'), l + ' subtitle still says datasets');
        console.assert(s.filters.dateRange !== 'Upload Date' && s.filters.dateRange !== 'Uploaddatum' && s.filters.dateRange !== 'Fecha de carga' && s.filters.dateRange !== \"Date d'import\", l + ' dateRange not updated');
        console.assert(s.filters.collections, l + ' missing filters.collections key');
        console.log(l + ': OK');
      }
    "</automated>
  </verify>
  <done>All 4 locale files have updated subtitle (no "datasets"), dateRange matches dateAdded wording, and new plural collections key exists.</done>
</task>

<task type="auto">
  <name>Task 2: Fix FilterPanel count logic, tab label, and empty row</name>
  <files>
    frontend/src/components/search/FilterPanel.tsx,
    frontend/src/components/search/__tests__/FilterPanel.test.tsx
  </files>
  <action>
In FilterPanel.tsx, make these changes:

**A. "All" tab count -- exclude collections from sum (line ~342)**

Change the "All" count from summing all facet values to summing only dataset types:
```tsx
// Before:
Object.values(counts).reduce((a, b) => a + b, 0)

// After - sum only dataset record types, exclude collection:
(counts.vector_dataset ?? 0) + (counts.raster_dataset ?? 0) + (counts.vrt_dataset ?? 0)
```
Apply this same fix in the mobile toggle section as well (around line 618-620 area for the "All" mobile toggle).

**B. Collection tab label -- use plural key (lines ~357-359 desktop, ~637-639 mobile)**

Change both desktop and mobile Collection toggle items:
```tsx
// Before:
{t('filters.collection', { defaultValue: 'Collection' })}

// After:
{t('filters.collections', { defaultValue: 'Collections' })}
```

**C. Hide empty secondary filter row (line ~476)**

The current condition `recordType && recordType !== 'collection'` shows the row for raster/VRT even when no controls render. Change the condition to also check that at least one control would render:

```tsx
// Before:
{recordType && recordType !== 'collection' && (

// After - show row only when controls would render:
{recordType && recordType !== 'collection' && (
  recordType === 'vector_dataset' || organizations.length > 0 || srids.length > 0
) && (
```

This ensures the secondary row only appears when vector (always has geometry dropdown) OR when org/CRS dropdowns have data.

**D. Update FilterPanel tests**

Update the existing test at `__tests__/FilterPanel.test.tsx`:

1. Fix the "All" count test (line 34): The mock has vector=10, raster=5, vrt=0, collection=3. The "All" tab should now show 15 (10+5+0) not 18 (10+5+0+3). Update the test assertion and `totalResults` prop accordingly.

2. Fix the "collection toggle" test (line 53): Update regex from `/Collection.*\(3\)/` to `/Collections.*\(3\)/` (plural).

3. Add a test: "does not render secondary filter row for raster type when no org/crs available". Set record_type to raster_dataset with the existing mock (useCatalogSummary returns undefined), assert `queryByTestId('secondary-filter-row')` is null.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && npx vitest run frontend/src/components/search/__tests__/FilterPanel.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - "All" tab sums only dataset types (excludes collection count)
    - Collection tab says "Collections" (plural)
    - Secondary filter row hidden when no controls would render
    - All FilterPanel tests pass including new empty-row test
  </done>
</task>

</tasks>

<verification>
- `npx vitest run frontend/src/components/search/__tests__/FilterPanel.test.tsx` -- all tests pass
- Manual spot-check: open search page, verify subtitle, tab labels, count consistency, no empty filter rows, "Date Added" label
</verification>

<success_criteria>
1. Subtitle reads "geospatial data" not "datasets" in all 4 locales
2. Collection tab displays "Collections (N)" (plural) on desktop and mobile
3. "All" tab count equals sum of vector + raster + VRT counts only
4. No secondary filter row appears for raster/VRT types when catalog has no org/CRS data
5. Date filter label says "Date Added" in all locales
6. All existing and new FilterPanel tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/260318-8mp-fix-remaining-search-page-ux-issues-subt/260318-8mp-SUMMARY.md`
</output>
</task>
