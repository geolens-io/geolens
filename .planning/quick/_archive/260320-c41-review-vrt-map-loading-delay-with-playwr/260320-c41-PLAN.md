---
phase: quick-260320-c41
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/DatasetPage.tsx
  - frontend/src/pages/__tests__/DatasetPage.hero.test.tsx
autonomous: true
requirements: [QUICK-BUG-FIX]

must_haves:
  truths:
    - "VRT/raster DatasetMap mounts immediately (no 10s delay)"
    - "Skeleton overlay shows while tiles load, disappears on onMapReady"
    - "onMapReady callback fires from mounted DatasetMap, transitioning heroState to loaded"
    - "Error overlay still appears when tiles genuinely fail"
    - "Vector datasets render unchanged (no skeleton, no callbacks)"
  artifacts:
    - path: "frontend/src/pages/DatasetPage.tsx"
      provides: "Fixed hero state machine rendering logic"
      contains: "DatasetMap"
    - path: "frontend/src/pages/__tests__/DatasetPage.hero.test.tsx"
      provides: "Updated tests matching new overlay-based rendering"
      contains: "hero-skeleton"
  key_links:
    - from: "DatasetPage.tsx hero render block"
      to: "DatasetMap onMapReady"
      via: "DatasetMap always mounted for raster/VRT, skeleton is overlay"
      pattern: "onMapReady.*setHeroState.*loaded"
---

<objective>
Fix the circular dependency in DatasetPage's hero state machine that prevents VRT/raster maps from loading until a 10-second timeout.

Purpose: The current logic conditionally unmounts DatasetMap during loading state, but onMapReady (which exits loading state) can only fire from a mounted DatasetMap — creating a deadlock broken only by a timeout that shows an error overlay.

Output: DatasetMap always mounts for raster/VRT; skeleton shown as an absolute overlay that disappears when onMapReady fires.
</objective>

<execution_context>
@/Users/ishiland/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishiland/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/pages/DatasetPage.tsx
@frontend/src/pages/__tests__/DatasetPage.hero.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix hero state machine rendering and update tests</name>
  <files>frontend/src/pages/DatasetPage.tsx, frontend/src/pages/__tests__/DatasetPage.hero.test.tsx</files>
  <behavior>
    - Test: raster dataset shows BOTH skeleton overlay AND DatasetMap during loading state
    - Test: VRT dataset shows BOTH skeleton overlay AND DatasetMap during loading state
    - Test: skeleton disappears when onMapReady fires (autoFireMapReady=true)
    - Test: vector datasets still render DatasetMap without skeleton or callbacks (unchanged)
    - Test: error overlay still shows after timeout (unchanged behavior)
    - Test: retry behavior still works (unchanged)
  </behavior>
  <action>
    **Update tests first** in `DatasetPage.hero.test.tsx`:

    1. Change the "shows skeleton loading state for raster datasets initially" test (line 257-263):
       - Keep assertion that `hero-skeleton` is in the document
       - CHANGE: assert `dataset-map` IS in the document (not `not.toBeInTheDocument()`) — the map must mount during loading
       - The skeleton should be an overlay on top of the map

    2. Change the "shows skeleton loading state for VRT datasets initially" test (line 265-271):
       - Same change: assert `dataset-map` IS in the document during loading

    3. Add a new test: "transitions from loading to loaded when onMapReady fires":
       - Set `mockMapConfig.autoFireMapReady = true`
       - Setup raster dataset, render
       - Assert `hero-skeleton` is NOT in document (onMapReady fired immediately)
       - Assert `dataset-map` IS in document

    4. Keep all other tests unchanged — error/timeout/retry/vector tests should pass as-is.

    **Then fix the component** in `DatasetPage.tsx` around lines 609-612:

    1. Remove the either/or conditional. Replace with:
       - ALWAYS render `<DatasetMap>` (remove the `(!isRasterOrVrt || heroState !== 'loading')` guard on line 612)
       - Render skeleton as an ABSOLUTE overlay inside the map container when `isRasterOrVrt && heroState === 'loading'`:
         ```tsx
         {isRasterOrVrt && heroState === 'loading' && (
           <Skeleton data-testid="hero-skeleton" className="absolute inset-0 z-10 rounded-lg" />
         )}
         ```
       - The skeleton sits on top of the map container via absolute positioning, hiding the map visually while it loads
       - DatasetMap renders unconditionally below (it already has the onMapReady/onTileError callbacks for raster/VRT)

    2. Ensure the parent container already has `relative` in its className (it does: line 605 has `'relative'`).

    3. Do NOT change the timeout logic (lines 264-271), retry logic (lines 273-277), error overlay (lines 635+), or no-tile badge (line 630). These all work correctly once the map mounts immediately.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && npx vitest run frontend/src/pages/__tests__/DatasetPage.hero.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - Raster/VRT datasets: DatasetMap mounts immediately, skeleton shows as overlay, disappears on onMapReady
    - Vector datasets: unchanged behavior (no skeleton, no callbacks)
    - Error/retry/timeout: unchanged behavior
    - All hero state machine tests pass
  </done>
</task>

</tasks>

<verification>
- `npx vitest run frontend/src/pages/__tests__/DatasetPage.hero.test.tsx` — all tests pass
- `npx vitest run frontend/src/pages/__tests__/DatasetPage.edit-affordances.test.tsx` — no regressions
- Visual: raster/VRT dataset pages load map immediately with skeleton overlay, no 10s delay
</verification>

<success_criteria>
- DatasetMap is always mounted for raster/VRT datasets (no conditional unmount during loading)
- Skeleton is an absolute overlay that disappears when onMapReady fires
- No 10-second delay before map appears
- All existing hero state machine tests pass (updated assertions)
- No regressions in vector dataset rendering
</success_criteria>

<output>
After completion, create `.planning/quick/260320-c41-review-vrt-map-loading-delay-with-playwr/260320-c41-SUMMARY.md`
</output>
