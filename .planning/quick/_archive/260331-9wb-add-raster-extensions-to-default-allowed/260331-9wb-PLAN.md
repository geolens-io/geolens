---
phase: quick-260331-9wb
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/2026_03_31_0001-reset_upload_allowed_extensions.py
autonomous: true
requirements: [QUICK-9wb]

must_haves:
  truths:
    - "After migration runs, the upload_allowed_extensions setting returns the env default (.zip,.gpkg,.geojson,.json,.csv,.tif,.tiff,.xlsx,.xls)"
    - "The admin Storage settings page shows the full extension list including .tif and .tiff"
    - "No data is lost — only the stale DB override row is removed"
  artifacts:
    - path: "backend/alembic/versions/2026_03_31_0001-reset_upload_allowed_extensions.py"
      provides: "Alembic data migration that deletes the stale DB override"
      contains: "DELETE FROM catalog.app_settings WHERE key = 'upload_allowed_extensions'"
  key_links:
    - from: "alembic migration"
      to: "catalog.app_settings table"
      via: "DELETE WHERE key = 'upload_allowed_extensions'"
      pattern: "DELETE.*upload_allowed_extensions"
---

<objective>
Reset the stale DB override for `upload_allowed_extensions` so the env default (which includes .tif, .tiff, .xlsx, .xls) takes effect.

Purpose: The DB currently stores a value saved before raster support (v10.0) was added, missing raster and spreadsheet extensions. Deleting this row lets the PersistentConfig system fall through to the env default.
Output: One Alembic data migration file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/app/config.py (env default at line 30-32: `.zip,.gpkg,.geojson,.json,.csv,.tif,.tiff,.xlsx,.xls`)
@backend/app/persistent_config.py (UPLOAD_ALLOWED_EXTENSIONS config at line 424-429)
@backend/app/settings/models.py (AppSetting model — table `catalog.app_settings`, PK column `key`)
@backend/alembic/versions/2026_03_30_0001-add_missing_indexes.py (latest migration, revision `b3c4d5e6f7a8`)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Alembic data migration to delete stale upload_allowed_extensions override</name>
  <files>backend/alembic/versions/2026_03_31_0001-reset_upload_allowed_extensions.py</files>
  <action>
Create an Alembic migration file with:
- `revision`: generate a new unique hex string (e.g. `c5d6e7f8a9b0`)
- `down_revision`: `b3c4d5e6f7a8` (the current head)
- `upgrade()`: execute raw SQL `DELETE FROM catalog.app_settings WHERE key = 'upload_allowed_extensions'`
- `downgrade()`: no-op (we cannot reconstruct the old stale value, and it is not needed). Add a comment explaining this is intentionally empty — the old value was stale.

Follow the same file structure and import style as `2026_03_30_0001-add_missing_indexes.py`. Use `op.execute()` for the DELETE statement.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && python -c "import importlib.util; spec = importlib.util.spec_from_file_location('m', 'alembic/versions/2026_03_31_0001-reset_upload_allowed_extensions.py'); mod = importlib.util.module_from_spec(spec); spec.loader.exec_module(mod); assert mod.down_revision == 'b3c4d5e6f7a8'; assert 'upload_allowed_extensions' in str(mod.upgrade.__code__.co_consts); print('OK')"</automated>
  </verify>
  <done>Migration file exists, imports cleanly, targets the correct down_revision, and the upgrade function contains a DELETE for upload_allowed_extensions.</done>
</task>

</tasks>

<verification>
- Migration file parses without errors
- `down_revision` chains correctly from current head
- DELETE targets `catalog.app_settings` with key `upload_allowed_extensions`
</verification>

<success_criteria>
After running `alembic upgrade head`, the `upload_allowed_extensions` row is removed from `catalog.app_settings`, and the PersistentConfig system returns the env default value which includes .tif, .tiff, .xlsx, .xls.
</success_criteria>

<output>
After completion, create `.planning/quick/260331-9wb-add-raster-extensions-to-default-allowed/260331-9wb-SUMMARY.md`
</output>
