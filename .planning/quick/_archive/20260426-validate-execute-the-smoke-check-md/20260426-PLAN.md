---
mode: quick-full
description: "--validate execute the @smoke-check.md"
must_haves:
  truths:
    - This quick task is validation/execution only; do not plan feature work or broad refactors.
    - `smoke-check.md` is the checklist source of truth for the requested smoke pass.
    - `.agents/skills/geolens-smoke-check/SKILL.md` delegates directly to `.agents/skills/geolens-smoke/SKILL.md`.
    - The smoke workflow should reuse existing Playwright specs and app test surface before any ad hoc manual checks.
    - Only fix a small obvious issue when it directly blocks completing the requested smoke check; otherwise record failures with ownership clues.
  artifacts:
    - .planning/quick/20260426-validate-execute-the-smoke-check-md/20260426-SMOKE-REPORT.md
    - .planning/quick/20260426-validate-execute-the-smoke-check-md/20260426-VERIFICATION.md
    - test-results/
    - playwright-report/
  key_links:
    - .planning/STATE.md
    - smoke-check.md
    - .agents/skills/geolens-smoke-check/SKILL.md
    - .agents/skills/geolens-smoke/SKILL.md
    - playwright.config.ts
    - e2e/
    - README.md
---

# Quick Task 20260426 Plan

## Context

- Current project state says v13.7 is complete and the next milestone is not yet opened; this quick task stands alone.
- `smoke-check.md` targets `http://localhost:8080`, expects admin credentials `admin` / `admin`, and lists required fixture coverage for vector, raster, table, VRT, collection, saved map, shared map, and admin flows.
- The checklist names the main automated buckets: `npm run e2e:smoke:core`, `npm run e2e:smoke:builder`, `npm run e2e:smoke:fixtures`, `npm run e2e:smoke`, plus export, accessibility, and demo smoke specs.
- The `geolens-smoke-check` skill is only an alias; execution should follow `geolens-smoke`, including required reads of `playwright.config.ts`, relevant `e2e/` specs, and `README.md` quickstart if a local stack must be started.

## Task 1 - Confirm Scope, Stack, And Fixtures

files:
- `smoke-check.md`
- `.agents/skills/geolens-smoke-check/SKILL.md`
- `.agents/skills/geolens-smoke/SKILL.md`
- `playwright.config.ts`
- `README.md`
- `e2e/`

action:
- Resolve the smoke scope as full checklist validation because the request is `--validate execute the @smoke-check.md`.
- Read the Playwright config and relevant smoke specs to map checklist sections to existing commands before running anything.
- Determine whether the app stack is already usable at `http://localhost:8080`; if not, start the minimum local stack needed by the repo instructions.
- Verify fixture prerequisites are present or can be seeded by existing test setup; if fixture setup is missing, document the exact blocker instead of inventing new fixture data.

verify:
- `curl -fsS http://localhost:8080/health` returns backend health JSON through the frontend origin, or the verification artifact records why the stack could not be reached.
- `npm run e2e:smoke -- --list` or the closest supported Playwright listing command confirms the smoke specs are discoverable.
- Required fixture assumptions are recorded in `20260426-VERIFICATION.md`.

done:
- Smoke scope, stack status, fixture readiness, and the exact automated commands to run are known before executing the pass.

## Task 2 - Execute The Existing Smoke Surface

files:
- `smoke-check.md`
- `playwright.config.ts`
- `e2e/`
- `test-results/`
- `playwright-report/`

action:
- Run the checklist through the existing smoke buckets first: core, builder, fixtures, and then the aggregate smoke command if useful for ordering confirmation.
- Run export, accessibility, and demo smoke coverage when the stack and fixtures satisfy their prerequisites.
- Use Playwright MCP or targeted browser checks only for checklist items not covered clearly by existing specs or when a failure needs screenshot, DOM, console, or network evidence.
- If a small obvious blocker prevents smoke execution, make the narrowest fix needed to complete validation and rerun the affected check; do not add new product behavior in this quick task.

verify:
- `npm run e2e:smoke:core`
- `npm run e2e:smoke:builder`
- `npm run e2e:smoke:fixtures`
- `npm run e2e:smoke`
- `npm run e2e:export`
- Relevant accessibility and demo smoke Playwright specs run by their existing npm command or direct Playwright invocation documented in `20260426-VERIFICATION.md`.

done:
- The requested smoke checklist has been executed as far as the available stack and fixtures allow, with raw pass/fail evidence retained in the normal Playwright outputs.

## Task 3 - Record Results And Triage Only What Blocks Validation

files:
- `.planning/quick/20260426-validate-execute-the-smoke-check-md/20260426-SMOKE-REPORT.md`
- `.planning/quick/20260426-validate-execute-the-smoke-check-md/20260426-VERIFICATION.md`
- `smoke-check.md`
- `test-results/`
- `playwright-report/`

action:
- Write a concise smoke report covering flows exercised, pass/fail status, spec references, screenshots or trace paths when available, and the highest-priority regressions.
- Write verification notes with exact commands, exit status, stack/fixture assumptions, skipped items, and any local environmental blockers.
- For failures, include the exact URL or flow, failing spec, relevant console/network errors, and likely owning subsystem or file.
- If a narrow validation-blocking fix was made, document the changed files and rerun evidence; otherwise leave product files unchanged.

verify:
- `test -s .planning/quick/20260426-validate-execute-the-smoke-check-md/20260426-SMOKE-REPORT.md`
- `test -s .planning/quick/20260426-validate-execute-the-smoke-check-md/20260426-VERIFICATION.md`
- `git diff --check`

done:
- The quick task is ready for review with a durable smoke report, command evidence, clear skipped/failure rationale, and no unplanned feature implementation.
