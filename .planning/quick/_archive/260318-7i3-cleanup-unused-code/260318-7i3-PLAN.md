---
phase: 260318-7i3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/ingest/router.py
  - backend/app/raster/quicklook.py
  - backend/app/stac/router.py
  - backend/app/tiles/router.py
  - backend/app/datasets/router.py
  - frontend/src/components/import/VrtCreateDialog.tsx
autonomous: true
requirements: [CLEANUP-01]

must_haves:
  truths:
    - "Zero ruff F401/F841 violations in backend app/ directory"
    - "Zero eslint no-unused-vars violations in frontend src/"
    - "No unused functions or dead exports remain in backend services"
    - "No unused shadcn/ui components or dead frontend utility exports remain"
    - "All existing tests still pass after cleanup"
  artifacts:
    - path: "backend/app/ingest/router.py"
      provides: "Cleaned unused sqlalchemy.func import"
    - path: "backend/app/raster/quicklook.py"
      provides: "Cleaned unused Optional import and long_edge variable"
    - path: "backend/app/stac/router.py"
      provides: "Cleaned unused literal_column import"
    - path: "backend/app/tiles/router.py"
      provides: "Cleaned unused RasterAsset import"
  key_links:
    - from: "backend/app/"
      to: "ruff check"
      via: "F401/F841 rules"
      pattern: "Found 0 errors"
    - from: "frontend/src/"
      to: "eslint"
      via: "no-unused-vars rule"
      pattern: "no warnings or errors"
---

<objective>
Deep cleanup pass across the entire codebase to remove all dead imports, unused variables, unused functions, and dead files in both backend (Python/FastAPI) and frontend (React/TypeScript).

Purpose: Reduce code noise, improve maintainability, eliminate confusion from dead code paths.
Output: Clean codebase with zero lint violations for unused code rules, and no dead exports/files.
</objective>

<execution_context>
@/Users/ishiland/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishiland/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260318-7i3-cleanup-unused-code/260318-7i3-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend dead code removal</name>
  <files>backend/app/ingest/router.py, backend/app/raster/quicklook.py, backend/app/stac/router.py, backend/app/tiles/router.py, backend/app/datasets/router.py</files>
  <action>
Phase 1 - Fix known ruff violations (6 errors):
1. `backend/app/ingest/router.py:669` — remove unused `func` from `from sqlalchemy import func, text` (keep `text`)
2. `backend/app/raster/quicklook.py:4` — remove `from typing import Optional` (unused)
3. `backend/app/raster/quicklook.py:54` — remove `long_edge = max(src.width, src.height)` assignment (F841, value never read)
4. `backend/app/stac/router.py:446` — remove `from sqlalchemy import literal_column` (unused)
5. `backend/app/tiles/router.py:22` — remove `from app.raster.models import RasterAsset` (unused)
6. Check `backend/app/datasets/router.py` for the `sqlalchemy.orm.aliased` unused import and remove it

Phase 2 - Deep pass for dead functions/exports:
- Run `ruff check --select F401,F811,F841 app/` to confirm zero violations
- For each backend module (`app/*/service.py`, `app/*/schemas.py`), check for exported functions/classes that are never imported elsewhere. Use grep to verify each public function has at least one import site.
- SKIP alembic/ directory entirely (generated migration files — false positives per research)
- SKIP conftest.py and test fixtures (pytest uses discovery, not direct imports)
- SKIP `__init__.py` re-exports that use `__all__` or are imported via package path
- Be cautious with FastAPI `Depends()` — functions passed to Depends appear unused but are referenced as objects in router signatures

Phase 3 - Verify:
- Run `cd backend && uv run ruff check app/` — must show zero F401/F841 errors
- Run `cd backend && uv run pytest -x --timeout=120` — all tests must pass
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && uv run ruff check --select F401,F841 app/ 2>&1 && echo "RUFF CLEAN" || echo "RUFF FAILED"</automated>
  </verify>
  <done>Zero ruff F401/F841 violations. All dead functions/exports identified and removed. Backend tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend dead code removal</name>
  <files>frontend/src/components/import/VrtCreateDialog.tsx</files>
  <action>
Phase 1 - Fix known eslint violation:
1. Fix the `record_type` unused variable in the file flagged by eslint (grep for `no-unused-vars` in eslint output to locate exact file — likely in a component destructuring or assignment)

Phase 2 - Dead export detection in frontend:
- For each file in `src/lib/*.ts`, check every named export — grep the codebase for its usage. If an export has zero import sites, remove it.
- For each file in `src/api/*.ts`, check every named export — grep for usage. Remove dead exports.
- For each file in `src/hooks/*.ts`, check every named export — grep for usage. Remove dead hooks.
- Check `src/components/ui/*.tsx` — for each component file, verify it is imported somewhere outside itself. If zero imports, remove the entire file.
- Check for dead page components: cross-reference `src/pages/*.tsx` against route definitions in `src/App.tsx` (including lazy imports). Remove any page not in the router.

Phase 3 - Dynamic import awareness:
- Do NOT remove files referenced via `React.lazy(() => import('...'))` in App.tsx
- Do NOT remove i18n resource files (loaded dynamically)
- Do NOT remove test utility files (`__tests__/`, `*.test.ts`)

Phase 4 - Verify:
- Run `cd frontend && npx eslint .` — zero no-unused-vars errors
- Run `cd frontend && npx tsc --noEmit` — zero type errors
- Run `cd frontend && npx vitest run --passWithNoTests` — all tests pass
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npx eslint . 2>&1 | grep -c 'no-unused-vars' | grep -q '^0$' && echo "ESLINT CLEAN" || echo "ESLINT HAS UNUSED VARS"</automated>
  </verify>
  <done>Zero eslint no-unused-vars violations. No dead exports, hooks, or utility functions remain. Frontend builds and tests pass cleanly.</done>
</task>

</tasks>

<verification>
After both tasks complete, run full validation suite:

```bash
# Backend
cd /Users/ishiland/Code/geolens/backend && uv run ruff check app/
cd /Users/ishiland/Code/geolens/backend && uv run pytest -x --timeout=120

# Frontend
cd /Users/ishiland/Code/geolens/frontend && npx eslint .
cd /Users/ishiland/Code/geolens/frontend && npx tsc --noEmit
cd /Users/ishiland/Code/geolens/frontend && npx vitest run --passWithNoTests
```

All commands must exit cleanly with zero errors.
</verification>

<success_criteria>
- ruff check on backend app/ reports zero F401/F841 violations
- eslint on frontend reports zero no-unused-vars violations
- tsc --noEmit passes with zero errors
- All backend pytest tests pass
- All frontend vitest tests pass
- No dead exports, unused functions, or orphaned files remain
</success_criteria>

<output>
After completion, create `.planning/quick/260318-7i3-cleanup-unused-code/260318-7i3-SUMMARY.md`
</output>
