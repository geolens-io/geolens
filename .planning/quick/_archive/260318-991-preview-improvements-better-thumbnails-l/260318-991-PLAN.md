---
phase: 260318-991
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/raster/quicklook.py
  - frontend/src/components/search/DatasetCard.tsx
  - frontend/src/i18n/locales/en/search.json
  - frontend/src/i18n/locales/es/search.json
  - frontend/src/i18n/locales/fr/search.json
  - frontend/src/i18n/locales/de/search.json
autonomous: true
requirements: [PREVIEW-01, PREVIEW-02, PREVIEW-03]

must_haves:
  truths:
    - "Single-band rasters with flat/constant values produce a visible (non-black) quicklook thumbnail"
    - "Single-band rasters use a colormap instead of raw grayscale for visual clarity"
    - "Quicklook letterbox canvas uses neutral background instead of black"
    - "Raster/VRT cards show a loading indicator while quicklook fetches"
    - "Raster/VRT cards show a labeled fallback state when quicklook fails"
    - "Vector dataset cards continue to show BBoxPreview as before"
  artifacts:
    - path: "backend/app/raster/quicklook.py"
      provides: "Improved quicklook generation with colormap and neutral canvas"
      contains: "colorize"
    - path: "frontend/src/components/search/DatasetCard.tsx"
      provides: "Three-state preview: loading, error-fallback, quicklook image"
      contains: "ImageOff"
  key_links:
    - from: "frontend/src/components/search/DatasetCard.tsx"
      to: "/api/datasets/{id}/quicklook"
      via: "fetch in useEffect with loading/error states"
      pattern: "setLoading|setImgError"
    - from: "backend/app/raster/quicklook.py"
      to: "PIL ImageOps.colorize"
      via: "single-band colormap application"
      pattern: "colorize"
---

<objective>
Improve dataset card preview thumbnails: fix black-box quicklooks for single-band rasters, add loading/error states to the frontend preview panel, and use a colormap for single-band raster thumbnails.

Purpose: Single-band rasters with flat or low-contrast data currently produce black thumbnails. The frontend has no loading indicator during quicklook fetch and silently falls back to BBoxPreview on error with no label.
Output: Better quicklook generation + three-state frontend preview (loading, error, image)
</objective>

<execution_context>
@.planning/quick/260318-991-preview-improvements-better-thumbnails-l/260318-991-RESEARCH.md
</execution_context>

<context>
@frontend/src/components/search/DatasetCard.tsx
@backend/app/raster/quicklook.py
@frontend/src/components/layout/BBoxPreview.tsx
@frontend/src/i18n/locales/en/search.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix backend quicklook generation for single-band rasters</name>
  <files>backend/app/raster/quicklook.py</files>
  <action>
Modify `generate_quicklook()` in `backend/app/raster/quicklook.py` to fix black-box thumbnails:

1. **Single-band colormap**: After the percentile stretch produces a grayscale image (mode "L"), convert it to RGB using `PIL.ImageOps.colorize()`. Use a blue-to-white gradient: `colorize(img_gray, black="#1a1a2e", white="#e0e8f0", mid="#4477aa")`. This gives single-band rasters a visually distinct tinted appearance instead of raw grayscale.

2. **Flat-value handling**: When `p98 == p2` (constant value raster), instead of producing all-zeros black, fill with 128 (mid-gray) so the thumbnail is visually distinguishable. Then apply the colorize step. Change line ~139 from `np.zeros_like(band, dtype=np.uint8)` to `np.full_like(band, 128, dtype=np.uint8)`.

3. **All-nodata handling**: When `valid.size == 0 or np.all(mask)` (lines 128-132), change the fallback from black to mid-gray (128) as well, so even all-nodata rasters produce a visible (non-black) thumbnail.

4. **Canvas background**: Change the letterbox canvas color from black (0) to neutral gray. On line ~150, change `Image.new(img.mode, (size, size), 0)` to use a fill of `(40, 40, 50)` for RGB mode or `40` for L mode. Since single-band images are now RGB after colorize, use: `canvas = Image.new("RGB", (size, size), (40, 40, 50))`. For multi-band images that are already RGB, also use the same dark-neutral canvas.

5. **Ensure mode consistency**: After the colorize step for single-band, the image is RGB. The canvas creation near the bottom of the function should handle both RGB (all cases now) consistently. Before `img.thumbnail(...)`, if `img.mode == "L"` convert to RGB: `img = img.convert("RGB")`. Then always create an RGB canvas.

Import `ImageOps` from PIL at the point of use (inside the function, matching existing lazy-import pattern).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && python -c "
from backend.app.raster.quicklook import generate_quicklook
print('Module imports successfully')
# Verify colorize is used in source
import inspect
src = inspect.getsource(generate_quicklook)
assert 'colorize' in src, 'colorize not found in source'
assert '128' in src, 'mid-gray fallback not found'
print('All checks pass')
"</automated>
  </verify>
  <done>Single-band rasters use a blue colormap instead of grayscale. Flat-value and all-nodata rasters produce visible mid-gray thumbnails instead of black. Letterbox canvas is dark-neutral instead of pure black.</done>
</task>

<task type="auto">
  <name>Task 2: Add loading and labeled fallback states to DatasetCard preview</name>
  <files>frontend/src/components/search/DatasetCard.tsx, frontend/src/i18n/locales/en/search.json, frontend/src/i18n/locales/es/search.json, frontend/src/i18n/locales/fr/search.json, frontend/src/i18n/locales/de/search.json</files>
  <action>
Modify `frontend/src/components/search/DatasetCard.tsx` to implement a three-state preview panel:

1. **Add loading state**: Add `const [isLoading, setIsLoading] = useState(false);` alongside existing state. In the `useEffect` that fetches quicklook, set `setIsLoading(true)` before the fetch call and `setIsLoading(false)` in both `.then` (after setting src) and `.catch` (before setting imgError). Also set false in cleanup if revoked.

2. **Import icons**: Add `import { ImageOff, Loader2 } from 'lucide-react';` at the top.

3. **Replace preview panel rendering** (lines 185-195) with four-state conditional:

```tsx
{hasQuicklook && !imgError && quicklookSrc ? (
  <img
    src={quicklookSrc}
    alt={t('datasetCard.quicklookAlt', { name: feature.properties.title })}
    className="w-full h-full object-cover rounded-md"
  />
) : hasQuicklook && isLoading ? (
  <div className="flex items-center justify-center h-[120px] w-full bg-muted/30 rounded-md">
    <Loader2 className="h-4 w-4 animate-spin text-muted-foreground" />
  </div>
) : hasQuicklook && imgError ? (
  <div className="flex flex-col items-center justify-center h-[120px] w-full bg-muted/30 rounded-md text-muted-foreground gap-1">
    <ImageOff className="h-5 w-5 opacity-50" />
    <span className="text-xs">{t('datasetCard.previewUnavailable')}</span>
  </div>
) : (
  <BBoxPreview bbox={bbox} />
)}
```

4. **Add i18n key** to all four locale files. In the `"datasetCard"` section (which already has `"quicklookAlt"`), add:
   - en: `"previewUnavailable": "Preview unavailable"`
   - es: `"previewUnavailable": "Vista previa no disponible"`
   - fr: `"previewUnavailable": "Apercu non disponible"`
   - de: `"previewUnavailable": "Vorschau nicht verfugbar"`

This ensures: (a) while quicklook loads, users see a spinner instead of a flash of BBoxPreview; (b) when quicklook fails, users see a labeled "Preview unavailable" state with an icon instead of silently showing BBoxPreview (which would be misleading for a raster dataset).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>DatasetCard shows a Loader2 spinner during quicklook fetch, shows ImageOff + "Preview unavailable" label on error, shows quicklook image on success, and falls through to BBoxPreview for vector datasets. No flash of BBoxPreview during raster loading.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors: `cd frontend && npx tsc --noEmit`
- Python quicklook module imports cleanly: `python -c "from backend.app.raster.quicklook import generate_quicklook"`
- Search for "Preview unavailable" in en locale confirms i18n key exists
- DatasetCard.tsx contains ImageOff and Loader2 imports
</verification>

<success_criteria>
- Single-band raster quicklooks use a blue colormap and are visually non-black
- Flat-value and all-nodata rasters produce visible (mid-gray tinted) thumbnails
- Letterbox canvas is dark-neutral instead of pure black
- DatasetCard shows loading spinner during quicklook fetch
- DatasetCard shows labeled "Preview unavailable" fallback on quicklook error
- Vector datasets continue to show BBoxPreview unchanged
- All four locale files have the new i18n key
</success_criteria>

<output>
After completion, create `.planning/quick/260318-991-preview-improvements-better-thumbnails-l/260318-991-SUMMARY.md`
</output>
