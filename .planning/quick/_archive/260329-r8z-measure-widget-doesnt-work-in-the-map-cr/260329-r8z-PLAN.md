---
phase: 260329-r8z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/MapBuilderPage.tsx
autonomous: true
requirements: [MEASURE-FIX]

must_haves:
  truths:
    - "Measure widget receives a non-null map instance after the map loads"
    - "Clicking measure tool in map creator allows drawing measurement lines on the map"
    - "Existing imperative map operations (zoom, style sync, ephemeral layers) still work via ref"
  artifacts:
    - path: "frontend/src/pages/MapBuilderPage.tsx"
      provides: "mapInstance state + ref dual pattern"
      contains: "setMapInstance"
  key_links:
    - from: "frontend/src/pages/MapBuilderPage.tsx"
      to: "WidgetHost"
      via: "mapInstance state variable in ctx prop"
      pattern: "ctx=.*mapInstance"
---

<objective>
Fix the measure widget receiving `null` for `mapInstance` in the Map Creator.

Purpose: The WidgetHost reads `mapInstanceRef.current` during render, but ref mutations don't trigger re-renders. The measure widget (and all widgets) get `null` unless an unrelated state change causes a re-render after map load.

Output: Working measure widget in the map creator with a reliable map instance delivery pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/pages/MapBuilderPage.tsx
@.planning/quick/260329-r8z-measure-widget-doesnt-work-in-the-map-cr/260329-r8z-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mapInstance state to MapBuilderPage and pass to WidgetHost</name>
  <files>frontend/src/pages/MapBuilderPage.tsx</files>
  <action>
In `MapBuilderPage.tsx`:

1. Add a state variable alongside the existing ref (line ~68 area):
   ```tsx
   const [mapInstance, setMapInstance] = useState<MaplibreMap | null>(null);
   ```
   Import `useState` if not already imported (it likely is). Do NOT remove `mapInstanceRef` -- it is still used by `use-builder-layers.ts` for imperative operations.

2. Update the `handleMapRef` callback (line ~152) to also set state:
   ```tsx
   const handleMapRef = useCallback((map: MaplibreMap | null) => {
     layers.handleMapRef(map);
     setMapInstance(map);
     if (map) save.maybeAutoCaptureThumbnail(map);
   }, [layers, save]);
   ```

3. Update the WidgetHost JSX (line ~436) to use state instead of ref:
   ```tsx
   <WidgetHost ctx={{ mapInstance, layers: layers.localLayers, mapId: id! }} />
   ```

This is a 3-line change. The ref stays for imperative use; the state ensures a re-render delivers the map instance to widgets.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>WidgetHost receives mapInstance from state (not ref), ensuring widgets get the map instance after load without depending on race conditions. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Manual: Open map creator, click measure tool, click on map -- crosshair cursor appears and measurement points are placed
</verification>

<success_criteria>
- Measure widget works reliably in map creator on first load (no race condition)
- All other map builder functionality unchanged (layers, basemap, zoom, ephemeral results)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/quick/260329-r8z-measure-widget-doesnt-work-in-the-map-cr/260329-r8z-01-SUMMARY.md`
</output>
