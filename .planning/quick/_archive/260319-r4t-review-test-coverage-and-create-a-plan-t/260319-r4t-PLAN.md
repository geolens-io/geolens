---
phase: quick-260319-r4t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260319-r4t-review-test-coverage-and-create-a-plan-t/TEST-COVERAGE-REPORT.md
autonomous: true
requirements: [REVIEW-01]

must_haves:
  truths:
    - "A comprehensive report exists showing current test coverage across backend, frontend unit, and e2e"
    - "Report identifies specific high-value gaps with priority rankings"
    - "Report provides actionable improvement plan with effort estimates"
  artifacts:
    - path: ".planning/quick/260319-r4t-review-test-coverage-and-create-a-plan-t/TEST-COVERAGE-REPORT.md"
      provides: "Complete test coverage audit and improvement plan"
      min_lines: 100
  key_links: []
---

<objective>
Audit current test coverage across all layers (backend pytest, frontend vitest, Playwright e2e) and produce a prioritized improvement plan.

Purpose: Give the user a clear picture of where testing stands today and a concrete roadmap for improving coverage, reliability, and code quality.
Output: TEST-COVERAGE-REPORT.md with current state, gap analysis, and prioritized action items.
</objective>

<execution_context>
@/Users/ishiland/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishiland/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@frontend/vite.config.ts (vitest config with coverage settings)
@frontend/eslint.config.js (eslint with jsx-a11y, react-hooks, typescript-eslint)
@playwright.config.ts (e2e config: chromium, auth setup, 60s timeout)
@.github/workflows/ci.yml (CI pipeline: backend lint+test, frontend lint+typecheck+test, security scan, e2e)
@backend/pyproject.toml (pytest-asyncio, ruff, bandit, pip-audit, pytest-cov)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run coverage collectors and audit test inventory</name>
  <files>.planning/quick/260319-r4t-review-test-coverage-and-create-a-plan-t/TEST-COVERAGE-REPORT.md</files>
  <action>
Run frontend vitest coverage to get actual line/branch/function coverage numbers:
```bash
cd frontend && npm run test:coverage 2>&1 | tail -80
```

Collect backend test count and module coverage info (do NOT run backend tests since they require PostGIS — just count files and analyze structure):
```bash
find backend/tests -name 'test_*.py' | wc -l
find backend/app -name '*.py' | grep -v __pycache__ | wc -l
```

Then produce TEST-COVERAGE-REPORT.md with these sections:

## 1. Current State Summary

A table showing:
| Layer | Test Files | Source Files | Coverage Tool | CI Status |
With rows for: Backend (pytest), Frontend Unit (vitest), E2E (playwright), Linting (eslint+ruff), Security (bandit+pip-audit)

## 2. CI Pipeline Assessment

Document what the CI pipeline already runs (from ci.yml):
- Backend: ruff check, ruff format, pytest with coverage, bandit, pip-audit
- Frontend: eslint, tsc --noEmit, vitest with coverage, i18n parity checks
- E2E: Playwright chromium against docker compose
- Note: CI is comprehensive — covers lint, type-check, unit, integration, e2e, and security

## 3. Frontend Unit Test Gaps (Priority Order)

List untested areas found during audit:

**Components without any tests (7 directories):**
- collections/ (8 files) — CRUD operations, list views
- maps/ (5 files) — map builder components
- drawing/ (2 files) — terra-draw integration
- map/ (2 files) — base map components
- viewer/ (2 files) — public embed viewer
- create/ (1 file) — dataset creation
- settings/ (1 file) — user settings

**Pages without tests (10 of 19):**
- SearchPage, CollectionsPage, ImportPage, LoginPage, RegisterPage, MapBuilderPage, MapsPage, OAuthCallbackPage, PublicViewerPage, SettingsPage

**Hooks without tests (22 of 27):**
- use-search, use-dataset, use-records, use-settings, use-permissions, use-maps, use-vrt, use-ingest, use-features, use-embed-tokens, use-admin, use-api-keys, use-config-ops, use-ai-metadata, use-ai-availability, use-saved-searches, use-quicklook, use-mobile, use-terra-draw, use-url-search-sync, use-abort-signal, use-dataset-edit-capabilities

**API modules without tests (16 of 17):**
- Only client.ts has tests; all domain-specific API modules (auth, datasets, collections, search, records, etc.) lack tests

**Prioritize by:** (a) business criticality, (b) complexity/bug-prone, (c) effort-to-value ratio

## 4. E2E Test Gaps

Current e2e specs (10 files) cover: auth, search, upload, dataset-detail, collections, admin, permissions, export-runtime, accessibility, record-detail-ux-audit.

Missing e2e flows:
- Map builder (create/edit/share maps)
- Settings page (user preferences)
- VRT creation workflow
- Collection detail editing (partially covered)
- OAuth login flow (requires external IdP)
- Import page beyond basic upload

## 5. Backend Test Assessment

90 test files covering 189 source modules. Already has:
- Integration tests with PostGIS
- Auth, CRUD, OGC, STAC, search, raster, VRT, export
- Performance regression tests (marked, skipped by default)
- Coverage reporting in CI

Potential gaps to investigate (requires running tests):
- Run `pytest --cov=app --cov-report=term-missing` to identify uncovered lines
- Check if newer modules (v12.x features) have adequate test coverage

## 6. Code Quality Checks Assessment

Already in place:
- ESLint with typescript-eslint, react-hooks, jsx-a11y plugins
- Ruff (Python linter + formatter)
- Bandit (Python security static analysis)
- pip-audit (dependency vulnerability scan)
- TypeScript strict type checking (tsc --noEmit)

Potential additions:
- Frontend: Consider adding `eslint-plugin-testing-library` for test quality
- Frontend: Coverage thresholds in vitest config to prevent regression
- Backend: Coverage threshold enforcement in CI
- Consider: `knip` for dead code detection
- Consider: Bundle size tracking (already has manual chunks)

## 7. Recommended Improvement Plan

### Phase 1: Quick Wins (Low Effort, High Value)
1. Add coverage thresholds to vitest config (prevent regression)
2. Add coverage threshold to pytest CI step
3. Add `eslint-plugin-testing-library` for better test patterns

### Phase 2: Critical Path Unit Tests
1. Test API client modules (auth, datasets, search, records) — these are the data layer
2. Test core hooks (use-search, use-dataset, use-records, use-permissions) — business logic
3. Test SearchPage and CollectionsPage — most-used pages

### Phase 3: E2E Coverage Expansion
1. Map builder e2e flow
2. VRT creation e2e flow
3. Settings page e2e

### Phase 4: Deep Coverage
1. Remaining untested hooks
2. Remaining component directories (collections, maps, drawing)
3. Backend coverage gap analysis (requires PostGIS runner)

Each phase item should note estimated effort (S/M/L) and files involved.
  </action>
  <verify>
    <automated>test -f .planning/quick/260319-r4t-review-test-coverage-and-create-a-plan-t/TEST-COVERAGE-REPORT.md && wc -l .planning/quick/260319-r4t-review-test-coverage-and-create-a-plan-t/TEST-COVERAGE-REPORT.md | awk '{if ($1 >= 100) print "PASS"; else print "FAIL: only " $1 " lines"}'</automated>
  </verify>
  <done>TEST-COVERAGE-REPORT.md exists with all 7 sections, includes actual vitest coverage numbers, lists all identified gaps, and provides a prioritized 4-phase improvement plan with effort estimates.</done>
</task>

</tasks>

<verification>
- Report contains actual coverage percentages from vitest run
- All untested frontend areas are catalogued (components, pages, hooks, API modules)
- E2E coverage gaps identified against known routes
- Backend test inventory documented
- Improvement plan is prioritized and actionable with effort estimates
</verification>

<success_criteria>
- TEST-COVERAGE-REPORT.md is 100+ lines with all 7 sections
- Report includes real vitest coverage numbers (not estimates)
- Each gap is specific (file names, not vague descriptions)
- Improvement plan has 4 phases ordered by effort/value
</success_criteria>

<output>
After completion, create `.planning/quick/260319-r4t-review-test-coverage-and-create-a-plan-t/260319-r4t-SUMMARY.md`
</output>
