---
phase: quick-260325-lq4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/auth/AdminRoute.tsx
  - frontend/src/components/admin/StatsOverview.tsx
  - frontend/src/components/admin/UserList.tsx
  - frontend/src/pages/admin/AdminSharedMapsPage.tsx
  - frontend/src/pages/admin/AdminConfigOpsPage.tsx
  - frontend/src/i18n/locales/en/common.json
  - frontend/src/i18n/locales/es/common.json
  - frontend/src/i18n/locales/fr/common.json
  - frontend/src/i18n/locales/de/common.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Admin route crash in any admin page shows RouteErrorBoundary, not a blank screen"
    - "Non-admin user sees i18n-translated 403 page, not hardcoded English"
    - "Screen readers announce status dot health status and action menu purpose"
    - "SharedMaps page has consistent padding with other admin pages"
    - "Config import mutation state resets when user selects a new file"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "errorElement on admin route block"
      contains: "errorElement"
    - path: "frontend/src/components/auth/AdminRoute.tsx"
      provides: "i18n 403 page"
      contains: "useTranslation"
    - path: "frontend/src/components/admin/StatsOverview.tsx"
      provides: "aria-label on StatusDot"
      contains: "aria-label"
    - path: "frontend/src/components/admin/UserList.tsx"
      provides: "aria-label on action menu trigger"
      contains: "aria-label"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/error/RouteErrorBoundary.tsx"
      via: "errorElement prop on admin Route"
      pattern: "errorElement.*RouteErrorBoundary"
    - from: "frontend/src/components/auth/AdminRoute.tsx"
      to: "frontend/src/i18n/locales/en/common.json"
      via: "useTranslation hook for 403 text"
      pattern: "t\\('errors\\.forbidden"
---

<objective>
Fix low-effort admin section bugs and accessibility gaps identified during research review.

Purpose: Close critical (error boundary) and high-severity (i18n, a11y) gaps in the admin section. Produce a findings report categorizing all issues by severity for future work.
Output: 6 files fixed, findings report written to SUMMARY.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260325-lq4-review-the-admin-section-for-completenes/260325-lq4-CONTEXT.md
@.planning/quick/260325-lq4-review-the-admin-section-for-completenes/260325-lq4-RESEARCH.md

<interfaces>
From frontend/src/components/error/RouteErrorBoundary.tsx:
- Already imported in App.tsx for maps route
- Usage: `errorElement={<RouteErrorBoundary />}`

From frontend/src/i18n/locales/en/common.json:
- errors.* namespace exists with keys like unexpected, insufficientPermissions
- Need to add: errors.forbidden, errors.forbiddenAdmin

From frontend/src/pages/admin/AdminConfigOpsPage.tsx:
- importMutation from useImportConfig() hook
- handleFileChange at line 124 -- needs importMutation.reset() call
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix critical admin error boundary and i18n 403 page</name>
  <files>frontend/src/App.tsx, frontend/src/components/auth/AdminRoute.tsx, frontend/src/i18n/locales/en/common.json, frontend/src/i18n/locales/es/common.json, frontend/src/i18n/locales/fr/common.json, frontend/src/i18n/locales/de/common.json</files>
  <action>
1. **App.tsx** (H1): Add `errorElement={<RouteErrorBoundary />}` to the `<Route element={<AdminRoute />}>` at line 63. Import RouteErrorBoundary is already present in the file.

2. **AdminRoute.tsx** (H2): Replace hardcoded English "403 / Forbidden -- Admin access required" with i18n translations:
   - Add `import { useTranslation } from 'react-i18next';`
   - Use `const { t } = useTranslation();` (common namespace)
   - Replace h1 text with `{t('errors.forbidden')}`  -- "403"
   - Replace p text with `{t('errors.forbiddenAdmin')}` -- "Forbidden -- Admin access required"

3. **All 4 locale common.json files**: Add two new keys to the `errors` object:
   - `"forbidden": "403"` (en), appropriate translations for es/fr/de
   - `"forbiddenAdmin": "Forbidden — Admin access required"` with translations

Note: Research finding M7 (settings save toast) and L5 (export toast) are already implemented in `useUpdateSettings` and `useExportConfig` hooks respectively -- no changes needed.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Admin routes have error boundary. 403 page uses i18n in all 4 locales. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Fix accessibility gaps and UI inconsistencies</name>
  <files>frontend/src/components/admin/StatsOverview.tsx, frontend/src/components/admin/UserList.tsx, frontend/src/pages/admin/AdminSharedMapsPage.tsx, frontend/src/pages/admin/AdminConfigOpsPage.tsx</files>
  <action>
1. **StatsOverview.tsx** (L1): Add `role="img"` and `aria-label` to the StatusDot span:
   - `aria-label={status === 'ok' ? 'Healthy' : 'Degraded'}` and `role="img"`
   - These are infrastructure status indicators so plain English is acceptable (not user-facing content needing i18n)

2. **UserList.tsx** (L2): Add `aria-label` to the MoreHorizontal action button at ~line 232:
   - Add `aria-label={t('users.actionsFor', { name: user.username })}` to the `<Button>` wrapping `<MoreHorizontal>`
   - Add i18n key `users.actionsFor` to admin.json in all 4 locales: "Actions for {{name}}"

3. **AdminSharedMapsPage.tsx** (L3): Remove the inner `<div className="p-6">` wrapper at line 268. AdminLayout already provides p-6 padding. Just keep the `<Card>` directly inside the fragment.

4. **AdminConfigOpsPage.tsx** (L6): In the `handleFileChange` function (~line 124), add `importMutation.reset();` at the start of the function (after `pickingRef.current = false;`) to clear stale success state when selecting a new file. The importMutation is available in the ImportSection component scope -- verify it's accessible there and add it.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>StatusDot has aria-label. Action menu has aria-label. SharedMaps has correct padding. Import mutation resets on new file selection.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/ishiland/Code/geolens/frontend && npx tsc --noEmit
```
All modified files compile without errors.

```bash
cd /Users/ishiland/Code/geolens/frontend && grep -n "errorElement" src/App.tsx
```
Admin route block has errorElement.

```bash
cd /Users/ishiland/Code/geolens/frontend && grep -n "useTranslation" src/components/auth/AdminRoute.tsx
```
AdminRoute uses i18n.

```bash
cd /Users/ishiland/Code/geolens/frontend && grep -n "aria-label" src/components/admin/StatsOverview.tsx src/components/admin/UserList.tsx
```
Both files have aria-label attributes.
</verification>

<success_criteria>
- Admin routes have errorElement preventing blank-screen crashes
- 403 page displays translated text in all 4 locales
- StatusDot announces health status to screen readers
- UserList action menu is labeled for screen readers
- SharedMaps page padding matches other admin pages
- Config import stale success banner clears on new file selection
- TypeScript compiles clean with no new errors
</success_criteria>

<output>
After completion, create `.planning/quick/260325-lq4-review-the-admin-section-for-completenes/260325-lq4-SUMMARY.md`

Include a **Findings Report** section in the summary with all issues categorized by severity:

**Already Fixed (this task):** H1, H2, L1, L2, L3, L6
**Already Implemented (no action needed):** M7, L5
**Defer -- Medium Effort:** M1 (native selects -> shadcn), M2 (raw table -> shadcn Table), M3 (raw textarea -> shadcn Textarea), M4 (keyboard a11y on expandable rows), M6 (audit log action filter completeness)
**Defer -- Large Effort:** H3 (lazy-load user filter), M5 (SharedMaps semantic table), M8 (unsaved changes guard on tab nav)
</output>
