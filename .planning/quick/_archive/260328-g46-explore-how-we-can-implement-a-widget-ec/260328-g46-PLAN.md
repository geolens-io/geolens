---
phase: 260328-g46
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/widgets/types.ts
  - frontend/src/components/widgets/registry.ts
  - frontend/src/components/widgets/WidgetHost.tsx
  - frontend/src/components/widgets/WidgetPanel.tsx
  - frontend/src/components/widgets/index.ts
  - frontend/src/components/widgets/builtin/PlaceholderWidget.tsx
  - frontend/src/components/widgets/register-widgets.ts
  - frontend/src/stores/widget-store.ts
  - frontend/src/pages/MapBuilderPage.tsx
autonomous: false
requirements: [widget-framework, widget-slots, widget-registry, widget-host]

must_haves:
  truths:
    - "Widget framework types exist with WidgetSlot, WidgetContext, and WidgetDefinition interfaces"
    - "Widget registry can register, retrieve, and list widgets"
    - "Widget store tracks active widget visibility and supports toggle/open/close"
    - "WidgetHost renders active widgets into correct slot positions on the map"
    - "WidgetPanel provides shared panel chrome (header with label, icon, close button)"
    - "A placeholder widget proves the system works end-to-end in the map builder"
    - "MapBuilderPage renders WidgetHost without disrupting existing overlays"
  artifacts:
    - path: "frontend/src/components/widgets/types.ts"
      provides: "WidgetSlot, WidgetContext, WidgetDefinition type definitions"
      exports: ["WidgetSlot", "WidgetContext", "WidgetDefinition"]
    - path: "frontend/src/components/widgets/registry.ts"
      provides: "Module-level widget registry (Map<string, WidgetDefinition>)"
      exports: ["registerWidget", "getWidgets", "getWidget"]
    - path: "frontend/src/stores/widget-store.ts"
      provides: "Zustand store for widget visibility state"
      exports: ["useWidgetStore"]
    - path: "frontend/src/components/widgets/WidgetHost.tsx"
      provides: "Slot-based renderer grouping active widgets by position"
      exports: ["WidgetHost"]
    - path: "frontend/src/components/widgets/WidgetPanel.tsx"
      provides: "Shared panel chrome wrapper for all widgets"
      exports: ["WidgetPanel"]
    - path: "frontend/src/components/widgets/index.ts"
      provides: "Public barrel export"
    - path: "frontend/src/components/widgets/builtin/PlaceholderWidget.tsx"
      provides: "Proof-of-concept widget for end-to-end verification"
    - path: "frontend/src/components/widgets/register-widgets.ts"
      provides: "Static registration of all built-in widgets at import time"
  key_links:
    - from: "frontend/src/components/widgets/WidgetHost.tsx"
      to: "frontend/src/stores/widget-store.ts"
      via: "useWidgetStore selector for activeWidgets"
      pattern: "useWidgetStore.*activeWidgets"
    - from: "frontend/src/components/widgets/WidgetHost.tsx"
      to: "frontend/src/components/widgets/registry.ts"
      via: "getWidgets() to read registered definitions"
      pattern: "getWidgets\\(\\)"
    - from: "frontend/src/pages/MapBuilderPage.tsx"
      to: "frontend/src/components/widgets/WidgetHost.tsx"
      via: "WidgetHost rendered inside the map relative container"
      pattern: "<WidgetHost"
    - from: "frontend/src/components/widgets/WidgetHost.tsx"
      to: "frontend/src/components/widgets/WidgetPanel.tsx"
      via: "Each widget wrapped in WidgetPanel for chrome"
      pattern: "<WidgetPanel"
---

<objective>
Implement the core widget ecosystem infrastructure for map creation: type-safe registry, slot-based layout host, zustand visibility store, shared panel chrome, and a placeholder widget proving end-to-end integration.

Purpose: Establish a modular widget framework so new map tools (query results, measurement, attribute table, data viz) can be added as self-contained React components registered into fixed slot positions, without modifying MapBuilderPage each time.

Output: Working widget infrastructure with a visible placeholder widget in the map builder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/pages/MapBuilderPage.tsx
@frontend/src/stores/drawing-store.ts
@frontend/src/types/api.ts (lines 645-689 for MapLayerResponse, MapResponse)

<interfaces>
<!-- Existing types the widget system needs -->

From frontend/src/types/api.ts:
```typescript
export interface MapLayerResponse {
  id: string;
  dataset_id: string;
  dataset_name: string;
  dataset_geometry_type: string | null;
  dataset_table_name: string;
  dataset_extent_bbox: number[] | null;
  dataset_column_info: { name: string; type: string }[] | null;
  dataset_feature_count: number | null;
  dataset_sample_values: Record<string, unknown[]> | null;
  display_name: string | null;
  sort_order: number;
  visible: boolean;
  opacity: number;
  paint: Record<string, unknown>;
  layout: Record<string, unknown>;
  filter: FilterSpecification | null;
  label_config?: LabelConfig | null;
  style_config?: StyleConfig | null;
  layer_type?: string | null;
  dataset_record_type?: string | null;
  show_in_legend?: boolean;
}
```

From frontend/src/stores/drawing-store.ts (pattern reference for zustand store):
```typescript
export const useDrawingStore = create<DrawingState>()((set) => ({
  // ... flat state + action methods using set()
}));
```

From MapBuilderPage.tsx (integration point -- the map relative container at line 388):
```tsx
{/* Map */}
<div className="flex-1 relative">
  {/* ... existing sidebar toggle, BuilderMap, EphemeralBadge, MapLegend ... */}
  <MapLegend layers={legendLayers} />
</div>
```

MapBuilderPage has `mapInstanceRef` (useRef<MaplibreMap | null>), `layers.localLayers` (MapLayerResponse[]), and `id` (string from useParams) available in scope.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create widget type system, registry, and visibility store</name>
  <files>
    frontend/src/components/widgets/types.ts,
    frontend/src/components/widgets/registry.ts,
    frontend/src/stores/widget-store.ts
  </files>
  <action>
Create the widget framework contracts and state management:

**frontend/src/components/widgets/types.ts:**
- Define `WidgetSlot` union type with positions: `'top-left' | 'top-right' | 'bottom-left' | 'bottom-right' | 'sidebar-bottom' | 'map-overlay'`
- Define `WidgetContext` interface with: `mapInstance: MaplibreMap | null`, `layers: MapLayerResponse[]`, `mapId: string`. Import Map type from `maplibre-gl` and `MapLayerResponse` from `@/types/api`. Keep this interface narrow -- widgets that need more data import their own hooks.
- Define `WidgetDefinition` interface with: `id: string`, `label: string`, `icon: React.ComponentType<{ className?: string }>`, `slot: WidgetSlot`, `component: React.ComponentType<{ ctx: WidgetContext }>`, `defaultVisible?: boolean`

**frontend/src/components/widgets/registry.ts:**
- Module-level `Map<string, WidgetDefinition>` (NOT React Context -- per research anti-patterns)
- Export `registerWidget(def: WidgetDefinition): void` -- logs warning on duplicate id, overwrites
- Export `getWidgets(): WidgetDefinition[]` -- returns `Array.from(registry.values())`
- Export `getWidget(id: string): WidgetDefinition | undefined`

**frontend/src/stores/widget-store.ts:**
- Zustand store following the existing `drawing-store.ts` pattern (flat state + actions)
- State: `activeWidgets: Set<string>`
- Actions: `toggle(id: string)`, `open(id: string)`, `close(id: string)`
- `toggle` adds if missing, deletes if present; `open` adds; `close` deletes
- Each action creates a new Set (immutable update pattern for zustand)
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | grep -E "widgets/|widget-store" || echo "No type errors in widget files"</automated>
  </verify>
  <done>Three files exist with correct exports. Types compile without errors. Registry is a plain module-level Map. Store follows existing zustand pattern from drawing-store.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Build WidgetPanel, WidgetHost, placeholder widget, and wire into MapBuilderPage</name>
  <files>
    frontend/src/components/widgets/WidgetPanel.tsx,
    frontend/src/components/widgets/WidgetHost.tsx,
    frontend/src/components/widgets/builtin/PlaceholderWidget.tsx,
    frontend/src/components/widgets/register-widgets.ts,
    frontend/src/components/widgets/index.ts,
    frontend/src/pages/MapBuilderPage.tsx
  </files>
  <action>
**frontend/src/components/widgets/WidgetPanel.tsx:**
- Shared panel chrome wrapping every widget. Receives `def: WidgetDefinition` and `onClose: () => void` props, plus `children`.
- Renders a shadcn-style card container (use `rounded-lg border bg-background/95 backdrop-blur-sm shadow-lg` -- matches existing overlay styling in the app).
- Header row: `def.icon` (h-3.5 w-3.5), `def.label` as text-xs font-medium, close X button (lucide X icon, size icon-xs). Use flex items-center gap-1.5 for header.
- Content area below header with `overflow-auto max-h-64` to prevent unbounded growth.
- No collapse/resize for now -- keep it simple.

**frontend/src/components/widgets/WidgetHost.tsx:**
- Import `useWidgetStore` and `getWidgets` from registry
- Define `SLOT_POSITIONS` constant mapping each `WidgetSlot` to a Tailwind className:
  - `top-left`: `absolute top-3 left-3 z-10 flex flex-col gap-2`
  - `top-right`: `absolute top-14 right-3 z-10 flex flex-col gap-2` (below MapLibre nav control)
  - `bottom-left`: `absolute bottom-14 left-4 z-10 flex flex-col gap-2` (above MapLegend)
  - `bottom-right`: `absolute bottom-4 right-4 z-10 flex flex-col gap-2`
  - `sidebar-bottom`: leave empty string (handled differently, not absolute)
  - `map-overlay`: `absolute bottom-0 left-0 right-0 z-20`
- Receives `ctx: WidgetContext` prop
- Reads `activeWidgets` from `useWidgetStore`
- Filters `getWidgets()` to only active ones
- Groups by slot using a simple reduce
- For each slot with widgets, renders a positioned div containing `<WidgetPanel>` wrapping `<w.component ctx={ctx} />`
- `onClose` calls `useWidgetStore.getState().close(w.id)` (or use the hook)

**frontend/src/components/widgets/builtin/PlaceholderWidget.tsx:**
- Simple proof-of-concept widget component
- Receives `{ ctx: WidgetContext }` prop
- Renders: a small card body showing "Widget system active" text and `ctx.layers.length` layers count, and the current mapId
- Purely for verification -- will be removed or replaced later

**frontend/src/components/widgets/register-widgets.ts:**
- Imports `registerWidget` from registry
- Imports `PlaceholderWidget` from builtin/
- Imports an icon from lucide-react (use `Puzzle` or `LayoutGrid`)
- Calls `registerWidget({ id: 'placeholder', label: 'Widget Demo', icon: LayoutGrid, slot: 'top-left', component: PlaceholderWidget, defaultVisible: true })`
- This file is imported once at app startup

**frontend/src/components/widgets/index.ts:**
- Barrel export: re-export types from `./types`, `registerWidget`/`getWidgets`/`getWidget` from `./registry`, `WidgetHost` from `./WidgetHost`, `WidgetPanel` from `./WidgetPanel`
- Also `import './register-widgets'` as a side-effect import to ensure built-in widgets are registered when the module is loaded

**frontend/src/pages/MapBuilderPage.tsx:**
- Add import: `import { WidgetHost, getWidgets } from '@/components/widgets'` and `import { useWidgetStore } from '@/stores/widget-store'`
- Inside the `{/* Map */}` div (the `<div className="flex-1 relative">` at ~line 388), AFTER the `<MapLegend>` component, add:
  ```tsx
  <WidgetHost ctx={{ mapInstance: mapInstanceRef.current, layers: layers.localLayers, mapId: id! }} />
  ```
- Also add an effect: on mount, read all registered widgets with `defaultVisible: true` and call `useWidgetStore.getState().open(w.id)` for each. Use a `useEffect` with empty deps that runs once:
  ```tsx
  useEffect(() => {
    const store = useWidgetStore.getState();
    getWidgets().filter(w => w.defaultVisible).forEach(w => store.open(w.id));
  }, []);
  ```
- Do NOT touch existing `MapLegend`, `EphemeralBadge`, `DrawingToolbar`, or `FeaturePopup` -- they coexist as-is.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | tail -5 && cd frontend && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <done>WidgetHost renders inside the map builder. The placeholder widget appears in the top-left slot showing layer count and map ID. Existing map overlays (legend, ephemeral badge) are unaffected. The build compiles without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify widget ecosystem works in the map builder</name>
  <files>n/a</files>
  <action>
Human verifies the widget infrastructure is working end-to-end in the running application.
  </action>
  <how-to-verify>
    1. Start the dev server: `cd frontend && npm run dev`
    2. Navigate to any map in the builder (http://localhost:8080/maps/{id}/edit)
    3. Verify the placeholder widget panel appears in the top-left area of the map (below any toolbar, above the map content)
    4. The panel should show: an icon, "Widget Demo" label, a close X button, and body text with the layer count and map ID
    5. Click the X button -- the widget panel should disappear
    6. Verify existing overlays still work: MapLegend in bottom-left, DrawingToolbar (if in drawing mode), navigation controls top-right
    7. Confirm no console errors related to widgets
  </how-to-verify>
  <verify>Human confirms widget appears and is dismissible</verify>
  <done>Placeholder widget visible and closeable. Existing overlays unaffected. No console errors.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compilation passes with zero errors in widget files and widget-store
- Vite production build succeeds
- Widget infrastructure files exist at the expected paths under frontend/src/components/widgets/
- Widget store exists at frontend/src/stores/widget-store.ts
- MapBuilderPage imports and renders WidgetHost without disrupting existing layout
- Placeholder widget is visible and closeable in the map builder
</verification>

<success_criteria>
1. A developer can create a new widget by: (a) writing a React component accepting `{ ctx: WidgetContext }`, (b) calling `registerWidget()` with a definition, and (c) the widget appears in its designated slot
2. The widget visibility store correctly tracks open/close/toggle state
3. The WidgetHost groups and renders widgets by slot position
4. Existing map overlays (MapLegend, EphemeralBadge, navigation controls) are completely unaffected
5. The placeholder widget demonstrates the full pipeline: registration -> store -> host -> panel -> render
</success_criteria>

<output>
After completion, create `.planning/quick/260328-g46-explore-how-we-can-implement-a-widget-ec/260328-g46-01-SUMMARY.md`
</output>
