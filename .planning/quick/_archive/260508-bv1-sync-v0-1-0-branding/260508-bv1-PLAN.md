---
phase: 260508-bv1-sync-v0-1-0-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/public/favicon.svg
  - frontend/public/favicon-16.png
  - frontend/public/favicon-32.png
  - frontend/public/favicon-64.png
  - frontend/public/app-icon-light.png
  - frontend/public/app-icon-dark.png
  - frontend/public/app-icon-192.png
  - frontend/public/app-icon-512.png
  - frontend/public/manifest.webmanifest
  - frontend/index.html
  - BRANDING-VERSION
autonomous: true
requirements:
  - BRAND-FAVICON-01
  - BRAND-PWA-01
  - BRAND-VERSION-01
  - BRAND-PARITY-01

must_haves:
  truths:
    - "Favicon source is /Users/ishiland/Code/branding/logos/favicon/geolens-favicon.svg (simplified — no graticule), NOT logos/reticle/geolens-emblem-mono.svg"
    - "App-icon downscaled sources are 1024×1024 light/dark from logos/app-icon/, downscaled with sips (NOT regenerated from emblem files)"
    - "Brand version marker lives at repo root as BRANDING-VERSION (NOT inside .planning/, NOT inside CLAUDE.md)"
    - "frontend/src/index.css :29-38 (light --primary-50..900) is already byte-identical to branding tokens/brand-colors.css — verify, do not edit"
    - "frontend/src/index.css :258 (sans font-family) and :259 (mono font-family) are already byte-identical to branding tokens/typography.json — verify, do not edit"
    - "frontend/src/components/GeoLensLogo.tsx already inlines geolens-emblem-mono.svg geometry byte-identically — verify, do not edit"
    - "PWA theme_color and HTML <meta name=\"theme-color\"> are both #3b6fd4 (frozen sRGB approximation of --primary-500 per brand guide §6)"
    - "SVG favicon <link> carries sizes=\"any\" so Chromium picks the SVG over PNG fallbacks"
  artifacts:
    - path: frontend/public/favicon.svg
      provides: "Simplified favicon SVG (16-64 px optimized, no graticule, hardcoded #334155)"
      byte_identical_to: "/Users/ishiland/Code/branding/logos/favicon/geolens-favicon.svg"
    - path: frontend/public/favicon-16.png
      provides: "16×16 PNG fallback for browser tabs"
      byte_identical_to: "/Users/ishiland/Code/branding/logos/favicon/geolens-favicon-16.png"
    - path: frontend/public/favicon-32.png
      provides: "32×32 PNG fallback for browser tabs"
      byte_identical_to: "/Users/ishiland/Code/branding/logos/favicon/geolens-favicon-32.png"
    - path: frontend/public/favicon-64.png
      provides: "64×64 PNG fallback for browser tabs"
      byte_identical_to: "/Users/ishiland/Code/branding/logos/favicon/geolens-favicon-64.png"
    - path: frontend/public/app-icon-light.png
      provides: "1024×1024 light app icon (PWA/iOS/Android home-screen source)"
      byte_identical_to: "/Users/ishiland/Code/branding/logos/app-icon/geolens-app-icon-light.png"
    - path: frontend/public/app-icon-dark.png
      provides: "1024×1024 dark app icon (PWA/iOS/Android home-screen source)"
      byte_identical_to: "/Users/ishiland/Code/branding/logos/app-icon/geolens-app-icon-dark.png"
    - path: frontend/public/app-icon-192.png
      provides: "192×192 PWA install icon (downscaled from app-icon-light.png via sips -z 192 192)"
      dimensions: "192x192"
    - path: frontend/public/app-icon-512.png
      provides: "512×512 PWA install icon (downscaled from app-icon-light.png via sips -z 512 512)"
      dimensions: "512x512"
    - path: frontend/public/manifest.webmanifest
      provides: "Minimal PWA manifest with name/short_name/description/start_url/display/background_color/theme_color/icons array"
      contains: '"theme_color": "#3b6fd4"'
    - path: frontend/index.html
      provides: "Updated head with sizes=\"any\" on SVG favicon, 3 PNG favicon links, manifest link, theme-color meta"
      contains_lines:
        - 'rel="icon" type="image/svg+xml" sizes="any" href="/favicon.svg"'
        - 'rel="icon" type="image/png" sizes="16x16" href="/favicon-16.png"'
        - 'rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png"'
        - 'rel="icon" type="image/png" sizes="64x64" href="/favicon-64.png"'
        - 'rel="manifest" href="/manifest.webmanifest"'
        - 'name="theme-color" content="#3b6fd4"'
    - path: BRANDING-VERSION
      provides: "Repo-root grep-able version marker for the branding sync"
      contains: "geolens-io/branding v0.1.0"
  key_links:
    - from: ".planning/quick/260508-bv1-sync-v0-1-0-branding/260508-bv1-CONTEXT.md"
      to: "frontend/public/favicon.svg + manifest.webmanifest + BRANDING-VERSION"
      via: "Locked decisions: favicon variant switch, PWA manifest shape, version marker"
    - from: ".planning/quick/260508-bv1-sync-v0-1-0-branding/260508-bv1-RESEARCH.md"
      to: "frontend/index.html"
      via: "3 deltas vs CONTEXT — sizes=\"any\" on SVG link, <meta name=\"theme-color\">, sips downscale commands"
    - from: "/Users/ishiland/Code/branding @ tag v0.1.0"
      to: "frontend/public/{favicon*,app-icon-{light,dark}}"
      via: "Verbatim byte-identical copy (cmp must succeed)"
    - from: "frontend/index.html (manifest link)"
      to: "frontend/public/manifest.webmanifest"
      via: "Vite serves /public/* at root, browser fetches /manifest.webmanifest lazily"
---

<objective>
Sync `geolens-io/branding` v0.1.0 brand assets into the GeoLens app surface.

Purpose: GeoLens 1.0 needs a coherent browser-tab + PWA install identity that matches the canonical brand repo. Three concrete user-visible outcomes:
1. The browser tab favicon switches from the noisy mono-emblem variant to the simplified favicon variant designed for 16-64 px legibility.
2. The app gets a real PWA manifest, so "Add to Home Screen" / install prompts on supported browsers render the GeoLens app icon and brand color instead of a screenshot fallback.
3. A grep-able `BRANDING-VERSION` file pins the sync to v0.1.0 so future bumps are tractable.

Output: 8 new asset files in `frontend/public/`, 1 new `frontend/public/manifest.webmanifest`, 6 line additions to `frontend/index.html` (1 attribute on existing line + 5 new lines), and 1 root-level `BRANDING-VERSION` marker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260508-bv1-sync-v0-1-0-branding/260508-bv1-CONTEXT.md
@.planning/quick/260508-bv1-sync-v0-1-0-branding/260508-bv1-RESEARCH.md
@frontend/index.html
@/Users/ishiland/Code/branding/README.md
@/Users/ishiland/Code/branding/BRAND-GUIDE.md

<reference_paths>
Branding repo source paths (read-only — no edits to this repo):
- /Users/ishiland/Code/branding/logos/favicon/geolens-favicon.svg
- /Users/ishiland/Code/branding/logos/favicon/geolens-favicon-{16,32,64}.png
- /Users/ishiland/Code/branding/logos/app-icon/geolens-app-icon-{light,dark}.png
- /Users/ishiland/Code/branding/tokens/brand-colors.css (already mirrored in frontend/src/index.css :29-38, verify only)
- /Users/ishiland/Code/branding/tokens/typography.json (already mirrored in frontend/src/index.css :258-259, verify only)
- /Users/ishiland/Code/branding/logos/reticle/geolens-emblem-mono.svg (already mirrored in frontend/src/components/GeoLensLogo.tsx, verify only)
- Branding repo HEAD at sync time: tag v0.1.0 (commit 54d2dd6, with docs follow-up 48176c4)
</reference_paths>

<existing_html_anchor>
Current `frontend/index.html` line 23 reads exactly:
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

Task 2 must rewrite this single line to add `sizes="any"`, then insert 5 new lines (3 PNG favicons + manifest link + theme-color meta) into the head.
</existing_html_anchor>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy verbatim assets + downscale PWA icons</name>
  <files>
    frontend/public/favicon.svg,
    frontend/public/favicon-16.png,
    frontend/public/favicon-32.png,
    frontend/public/favicon-64.png,
    frontend/public/app-icon-light.png,
    frontend/public/app-icon-dark.png,
    frontend/public/app-icon-192.png,
    frontend/public/app-icon-512.png
  </files>
  <action>
File operations only — no HTML/code edits in this task. Run these commands in order from the repo root:

```bash
# 1. Copy 6 verbatim source files from branding repo to frontend/public/
#    (favicon.svg overwrites the existing mono-emblem favicon — this is intentional, per CONTEXT D-01)
cp /Users/ishiland/Code/branding/logos/favicon/geolens-favicon.svg frontend/public/favicon.svg
cp /Users/ishiland/Code/branding/logos/favicon/geolens-favicon-16.png frontend/public/favicon-16.png
cp /Users/ishiland/Code/branding/logos/favicon/geolens-favicon-32.png frontend/public/favicon-32.png
cp /Users/ishiland/Code/branding/logos/favicon/geolens-favicon-64.png frontend/public/favicon-64.png
cp /Users/ishiland/Code/branding/logos/app-icon/geolens-app-icon-light.png frontend/public/app-icon-light.png
cp /Users/ishiland/Code/branding/logos/app-icon/geolens-app-icon-dark.png frontend/public/app-icon-dark.png

# 2. Downscale 1024×1024 light source to PWA install slots using built-in macOS sips
#    (per RESEARCH §3 — sips is /usr/bin/sips, no Homebrew dep, lossless bicubic).
#    Source: app-icon-light.png (canonical light home-screen variant per brand guide §4).
#    Both ratios are clean integer-friendly downsamples (5.33× and 2× from 1024).
sips -z 192 192 frontend/public/app-icon-light.png --out frontend/public/app-icon-192.png
sips -z 512 512 frontend/public/app-icon-light.png --out frontend/public/app-icon-512.png
```

DO NOT edit `frontend/src/index.css` or `frontend/src/components/GeoLensLogo.tsx` — those are already byte-identical to the branding tokens/mono-emblem (verified in CONTEXT pre-existing alignment block, line 24-28). The `verify` step below re-confirms that alignment to catch drift; if cmp fails on either, surface the diff and stop — do not auto-edit.

Avoid `mv` (we want to keep the source files in `/Users/ishiland/Code/branding/`). Avoid ImageMagick `magick`/`convert` — `sips` is the locked tool per RESEARCH §3 (no fallback branching).

After this task: commit with `chore(brand): sync v0.1.0 favicon + app-icon assets to frontend/public/` (matches `chore(...)` style in recent commits like `d9d94c35 chore: fix demo overlay docker bugs`).
  </action>
  <verify>
    <automated>
# Byte-identity for 6 verbatim copies (cmp returns 0 on identical, exits non-zero on diff)
cmp frontend/public/favicon.svg     /Users/ishiland/Code/branding/logos/favicon/geolens-favicon.svg
cmp frontend/public/favicon-16.png  /Users/ishiland/Code/branding/logos/favicon/geolens-favicon-16.png
cmp frontend/public/favicon-32.png  /Users/ishiland/Code/branding/logos/favicon/geolens-favicon-32.png
cmp frontend/public/favicon-64.png  /Users/ishiland/Code/branding/logos/favicon/geolens-favicon-64.png
cmp frontend/public/app-icon-light.png /Users/ishiland/Code/branding/logos/app-icon/geolens-app-icon-light.png
cmp frontend/public/app-icon-dark.png  /Users/ishiland/Code/branding/logos/app-icon/geolens-app-icon-dark.png

# Dimensions for 2 downscaled PNGs (file(1) reports "PNG image data, W x H, ...")
file frontend/public/app-icon-192.png | grep -q '192 x 192'
file frontend/public/app-icon-512.png | grep -q '512 x 512'

# Pre-existing alignment re-confirmation (drift catch — must NOT have edited these)
cmp <(sed -n '29,38p' frontend/src/index.css) <(sed -n '/--primary-50:/,/--primary-900:/p' /Users/ishiland/Code/branding/tokens/brand-colors.css | head -10) || echo "DRIFT: index.css :29-38 no longer matches brand-colors.css — investigate before continuing"
grep -q "'IBM Plex Sans Variable', ui-sans-serif, system-ui, sans-serif"     frontend/src/index.css
grep -q "'IBM Plex Mono', ui-monospace, SFMono-Regular, Menlo, monospace"    frontend/src/index.css
grep -q 'cx="26" cy="26" r="18"'  frontend/src/components/GeoLensLogo.tsx
    </automated>
  </verify>
  <done>
- All 6 verbatim source files exist in `frontend/public/` and pass `cmp` byte-identity vs branding repo originals.
- `app-icon-192.png` is exactly 192×192; `app-icon-512.png` is exactly 512×512 (per `file(1)` output).
- Pre-existing alignment in `frontend/src/index.css` and `GeoLensLogo.tsx` re-confirmed; nothing in `frontend/src/` was modified.
- Atomic commit landed: `chore(brand): sync v0.1.0 favicon + app-icon assets to frontend/public/`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire favicons + manifest into index.html, create manifest.webmanifest</name>
  <files>
    frontend/index.html,
    frontend/public/manifest.webmanifest
  </files>
  <action>
Two file operations — both must land before the verify step runs.

**A. Create `frontend/public/manifest.webmanifest`** with this exact content (write fresh, not editing an existing file):

```json
{
  "name": "GeoLens",
  "short_name": "GeoLens",
  "description": "Open-source spatial data catalog with search, map builder, and OGC standards compliance. Upload, discover, visualize, and share geospatial data.",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#3b6fd4",
  "icons": [
    {
      "src": "/app-icon-192.png",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/app-icon-512.png",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/app-icon-light.png",
      "sizes": "1024x1024",
      "type": "image/png",
      "purpose": "any"
    }
  ]
}
```

Decisions baked into this JSON (per CONTEXT decisions block + RESEARCH §1, §4):
- `description` field exactly matches the existing `<meta name="description">` content from `frontend/index.html:25` (per CONTEXT D-02 manifest shape).
- `background_color: "#ffffff"` is the light atlas paper sRGB approximation of `--background` light (per CONTEXT decision).
- `theme_color: "#3b6fd4"` is the frozen sRGB approximation of `--primary-500` per brand guide §6 (per CONTEXT decision + RESEARCH §4 — must match HTML meta).
- `purpose: "any maskable"` on the 192/512 entries marks them as the canonical install icons (per planning constraints — PWA install minimum). The 1024×1024 light entry uses `purpose: "any"` for completeness; no maskable claim because the dark variant is not exposed via the manifest in this minimal v0.1 sync.

**B. Edit `frontend/index.html`** to make 6 changes in `<head>`:

Existing line 23 reads:
```
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
```

Replace that single line with the following 5-line block (note `sizes="any"` is now on the SVG line per RESEARCH §2):
```
    <link rel="icon" type="image/svg+xml" sizes="any" href="/favicon.svg" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png" />
    <link rel="icon" type="image/png" sizes="64x64" href="/favicon-64.png" />
    <link rel="manifest" href="/manifest.webmanifest" />
```

Then add a single `<meta name="theme-color">` line. Per RESEARCH §4, place it adjacent to the existing `<meta name="description">` block (currently line 25). Add it immediately AFTER the description meta, so the new line becomes line 26 (between description and og:title):
```
    <meta name="theme-color" content="#3b6fd4" />
```

Final ordering rationale (per RESEARCH §2):
- SVG favicon `<link>` stays FIRST so Chromium with `sizes="any"` picks it.
- PNG fallbacks ASCEND in size (16 → 32 → 64) so size-aware browsers pick the closest match ≤ target.
- `<link rel="manifest">` placed last in the favicon block — order doesn't matter for correctness (manifest fetch is non-blocking per RESEARCH §1) but grouping keeps the head readable.
- `<meta name="theme-color">` near the existing description meta keeps related meta tags clustered.

DO NOT touch any other line — keep the inline FOUC-prevention `<script>` (lines 5-22), `<meta charset>`, `<meta name="viewport">`, OG/Twitter meta block, `<title>`, `<body>`, and `<script type="module">` unchanged.

DO NOT add iOS `<meta name="apple-mobile-web-app-status-bar-style">` — RESEARCH §4 explicitly scopes it out for v0.1.0.

DO NOT install Vite plugins — RESEARCH §1 confirms `frontend/public/` files are served at root with no config changes.

After this task: commit with `feat(brand): wire favicon variants + PWA manifest into index.html` (matches `feat(...)` style in recent commits like `13fed4c4 feat(279-03): emit user.register audit event`).
  </action>
  <verify>
    <automated>
# A. Manifest JSON validity + key fields
python3 -c "import json; m = json.load(open('frontend/public/manifest.webmanifest')); assert m['name']=='GeoLens'; assert m['short_name']=='GeoLens'; assert m['start_url']=='/'; assert m['display']=='standalone'; assert m['theme_color']=='#3b6fd4'; assert m['background_color']=='#ffffff'; icons={i['sizes']:i for i in m['icons']}; assert '192x192' in icons; assert '512x512' in icons; assert '1024x1024' in icons; assert icons['192x192']['purpose']=='any maskable'; assert icons['512x512']['purpose']=='any maskable'; print('manifest OK')"

# B. index.html contains all 6 required lines (literal grep checks — no regex magic)
grep -F 'rel="icon" type="image/svg+xml" sizes="any" href="/favicon.svg"' frontend/index.html
grep -F 'rel="icon" type="image/png" sizes="16x16" href="/favicon-16.png"' frontend/index.html
grep -F 'rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png"' frontend/index.html
grep -F 'rel="icon" type="image/png" sizes="64x64" href="/favicon-64.png"' frontend/index.html
grep -F 'rel="manifest" href="/manifest.webmanifest"' frontend/index.html
grep -F 'name="theme-color" content="#3b6fd4"' frontend/index.html

# C. Negative: confirm the OLD line (without sizes="any") is gone
# Use grep -v on header comments per planner hygiene rule, then assert exact-old-line is absent
test "$(grep -v '^#' frontend/index.html | grep -c 'rel="icon" type="image/svg+xml" href="/favicon.svg" />')" = "0"

# D. Frontend build still passes (catches syntax errors in HTML)
cd frontend && npm run build 2>&1 | tail -20
    </automated>
  </verify>
  <done>
- `frontend/public/manifest.webmanifest` parses as valid JSON with name/short_name/start_url/display/theme_color/background_color all matching CONTEXT decisions and a 3-entry icons array (192/512 with `any maskable`, 1024 with `any`).
- `frontend/index.html` contains all 6 required lines verbatim (3 PNG favicons + SVG with `sizes="any"` + manifest link + theme-color meta).
- The OLD line `<link rel="icon" type="image/svg+xml" href="/favicon.svg" />` (no `sizes="any"`) is no longer present.
- `npm run build` from `frontend/` exits clean (HTML still parses, Vite copies `public/` contents to `dist/` including the new manifest + favicons).
- Atomic commit landed: `feat(brand): wire favicon variants + PWA manifest into index.html`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create BRANDING-VERSION marker at repo root</name>
  <files>
    BRANDING-VERSION
  </files>
  <action>
Write a single new file at the repo root (NOT inside `.planning/`, NOT inside `CLAUDE.md` — per CONTEXT decision rationale: "Single dedicated grep-able file as recommended by branding README §Version pinning. Avoids polluting CLAUDE.md (loaded into every Claude Code context window). Future bumps just rewrite this one file.").

Path: `BRANDING-VERSION` (sibling to `README.md`, `CHANGELOG.md`, `package.json`).

Exact contents (4 lines, no trailing blank line, no markdown formatting — this is a flat text marker):

```
Brand assets synced from geolens-io/branding v0.1.0
Sync date: 2026-05-08
Source path at sync: /Users/ishiland/Code/branding @ tag v0.1.0
Files synced: see .planning/quick/260508-bv1-sync-v0-1-0-branding/260508-bv1-SUMMARY.md
```

Rationale per CONTEXT (decisions block, "Brand-sync version marker"):
- Format follows the branding repo `README.md` §Version pinning recommendation: `Brand assets synced from geolens-io/branding v0.1.0`.
- The 4-line shape is dictated verbatim in CONTEXT — do not paraphrase or reorder.
- The SUMMARY.md path referenced on line 4 will be created by `/gsd-execute-phase` at completion (post-execute summary template); this marker forward-references it intentionally.

DO NOT add this file to `.gitignore`. DO NOT make it a markdown file (`.md` extension). DO NOT split into multiple files.

After this task: commit with `chore(brand): pin sync to geolens-io/branding v0.1.0 via BRANDING-VERSION marker` (matches `chore(...)` style).
  </action>
  <verify>
    <automated>
# File exists at repo root with exact 4-line content
test -f BRANDING-VERSION
test "$(wc -l < BRANDING-VERSION | tr -d ' ')" = "4"
grep -Fxq "Brand assets synced from geolens-io/branding v0.1.0"                                         BRANDING-VERSION
grep -Fxq "Sync date: 2026-05-08"                                                                       BRANDING-VERSION
grep -Fxq "Source path at sync: /Users/ishiland/Code/branding @ tag v0.1.0"                             BRANDING-VERSION
grep -Fxq "Files synced: see .planning/quick/260508-bv1-sync-v0-1-0-branding/260508-bv1-SUMMARY.md"     BRANDING-VERSION

# Confirm it's at repo root, not inside .planning/ or CLAUDE.md
test ! -f .planning/BRANDING-VERSION
test ! -f CLAUDE.md  # CLAUDE.md doesn't exist in this repo; ensure sync marker didn't accidentally create it
    </automated>
  </verify>
  <done>
- `BRANDING-VERSION` exists at repo root (sibling to `README.md`).
- File is exactly 4 lines, all 4 match the CONTEXT-locked content verbatim (grep -Fxq passes for each line).
- File is NOT inside `.planning/` and did NOT result in a `CLAUDE.md` being created.
- Atomic commit landed: `chore(brand): pin sync to geolens-io/branding v0.1.0 via BRANDING-VERSION marker`.
  </done>
</task>

</tasks>

<verification>
End-of-plan reachability check (validates all must-haves can be reached by a fresh-eyes reviewer):

```bash
# 1. All 8 new asset files exist in frontend/public/
ls frontend/public/favicon.svg frontend/public/favicon-{16,32,64}.png frontend/public/app-icon-{light,dark,192,512}.png

# 2. Manifest exists and is valid JSON with the right shape
python3 -m json.tool frontend/public/manifest.webmanifest > /dev/null

# 3. index.html has all 6 new/edited lines
test "$(grep -cE 'sizes="any" href="/favicon.svg"|favicon-(16|32|64)\.png|manifest\.webmanifest|theme-color' frontend/index.html)" = "6"

# 4. Version marker exists at repo root
test -f BRANDING-VERSION && grep -q 'v0.1.0' BRANDING-VERSION

# 5. Pre-existing alignment NOT regressed (drift catch)
grep -q "'IBM Plex Sans Variable', ui-sans-serif, system-ui, sans-serif"  frontend/src/index.css
grep -q 'cx="26" cy="26" r="18"'                                          frontend/src/components/GeoLensLogo.tsx

# 6. Frontend build clean (asset-pipeline integration check)
(cd frontend && npm run build) > /tmp/branding-build.log 2>&1 && grep -q 'manifest.webmanifest' /tmp/branding-build.log || echo "manifest may not be in dist — investigate"

# 7. Vite dev-server smoke (optional, manual): curl http://localhost:5173/manifest.webmanifest after `cd frontend && npm run dev`
```

If any check fails, do not write the SUMMARY.md — surface the failure and stop.
</verification>

<success_criteria>
Plan is complete when all of the following are true:

1. **Verbatim source-of-truth fidelity:** `cmp` succeeds for all 6 copied files vs the branding repo originals at `/Users/ishiland/Code/branding/`.
2. **Downscaled PWA icons exist at exact dimensions:** `file(1)` reports `192 x 192` and `512 x 512` for `app-icon-192.png` and `app-icon-512.png` respectively.
3. **PWA manifest is valid + correctly shaped:** `python3 -c "import json; ..."` validates name/short_name/start_url/display/theme_color/background_color match locked CONTEXT values; icons array has 192 + 512 + 1024 entries with correct purpose flags.
4. **HTML head correctly wired:** `frontend/index.html` contains 5 favicon-related `<link>` lines (1 SVG with `sizes="any"`, 3 PNG, 1 manifest), 1 theme-color meta. Old SVG-without-`sizes="any"` line is removed.
5. **Theme-color parity:** Both `<meta name="theme-color">` and manifest `theme_color` are exactly `#3b6fd4` (frozen sRGB approx of `--primary-500` per brand guide §6 + RESEARCH §4).
6. **Version marker pins the sync:** `BRANDING-VERSION` exists at repo root with all 4 CONTEXT-locked lines verbatim.
7. **No regression in pre-existing alignment:** `frontend/src/index.css` `--primary-*` tokens (light) + sans/mono font-families unchanged; `GeoLensLogo.tsx` mono geometry unchanged.
8. **Frontend build green:** `npm run build` from `frontend/` exits clean with `manifest.webmanifest` copied into `dist/`.
9. **Three atomic commits landed** in order on `main`:
   - `chore(brand): sync v0.1.0 favicon + app-icon assets to frontend/public/`
   - `feat(brand): wire favicon variants + PWA manifest into index.html`
   - `chore(brand): pin sync to geolens-io/branding v0.1.0 via BRANDING-VERSION marker`
</success_criteria>

<output>
After completion, create `.planning/quick/260508-bv1-sync-v0-1-0-branding/260508-bv1-SUMMARY.md` with:
- The 8 new files in `frontend/public/` (with byte sizes for the 6 verbatim copies and dimensions for the 2 downscaled PNGs).
- The 6 line changes in `frontend/index.html` (1 attribute add + 5 new lines).
- The 1 new manifest file with its locked theme/background colors.
- The new `BRANDING-VERSION` content.
- A note re-confirming pre-existing alignment was preserved (cmp on `index.css :29-38` and `GeoLensLogo.tsx` mono geometry).
- A pointer to RESEARCH §1-4 for the 4 mechanical resolutions encoded into this plan (manifest placement, favicon ordering with `sizes="any"`, sips downscaling, theme-color meta+manifest parity).
- Next-bump-checklist for v0.2.0+: rerun `cp` for changed files, rerun `sips` if app-icon source dimensions change, update `BRANDING-VERSION` line 1 + 2 + 3.
</output>
