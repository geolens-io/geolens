---
phase: 260328-t60
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [CI-PASS]

must_haves:
  truths:
    - "All backend linting passes (ruff check, ruff format)"
    - "All backend pytest tests pass"
    - "All backend security scans pass (bandit, pip-audit)"
    - "All frontend lint and typecheck passes (eslint, tsc)"
    - "All frontend i18n parity checks pass"
    - "All frontend vitest tests pass with coverage"
    - "All uncommitted changes are committed and pushed to origin/main"
  artifacts: []
  key_links: []
---

<objective>
Run the full CI check suite locally (matching .github/workflows/ci.yml), then commit all uncommitted changes and push to origin/main.

Purpose: Validate all pending changes pass CI before pushing to remote.
Output: Clean push to main with all checks green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.github/workflows/ci.yml

Docker services are already running (api, db, frontend, titiler, worker).
Backend tests run via: docker compose exec api uv run pytest
Backend lint runs via: cd backend && uv run ruff check . && uv run ruff format --check .
Backend security scan runs via: cd backend && uv run bandit -r app/ -c pyproject.toml --severity-level high --confidence-level high && uv run pip-audit --strict --desc
Frontend tests run via: cd frontend && npm run test:coverage
Frontend lint runs via: cd frontend && npm run lint && npx tsc --noEmit
Frontend i18n checks: cd frontend && npm run test:i18n && npm run check:i18n:changed
Git remote: origin https://github.com/geolens-io/geolens.git (main branch)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all backend CI checks</name>
  <files>backend/</files>
  <action>
Run backend linting, security scans, and tests locally, mirroring the CI pipeline:

1. Backend lint (from backend/ directory):
   - uv run ruff check .
   - uv run ruff format --check .

2. Backend security scan (from backend/ directory, mirrors CI security-scan job):
   - uv run bandit -r app/ -c pyproject.toml --severity-level high --confidence-level high
   - uv run pip-audit --strict --desc

3. Backend tests (via docker since they need PostGIS):
   - docker compose exec api uv run pytest -v --tb=short

If any check fails, diagnose and fix the issue before proceeding. Do NOT skip failing tests or security findings.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && uv run ruff check . && uv run ruff format --check . && uv run bandit -r app/ -c pyproject.toml --severity-level high --confidence-level high && uv run pip-audit --strict --desc && cd /Users/ishiland/Code/geolens && docker compose exec api uv run pytest -v --tb=short</automated>
  </verify>
  <done>All backend ruff checks pass with zero errors. Bandit and pip-audit report no issues. All pytest tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Run all frontend CI checks</name>
  <files>frontend/</files>
  <action>
Run frontend linting, typechecking, i18n checks, and tests locally, mirroring the CI pipeline:

1. i18n parity check:
   - cd frontend && npm run test:i18n
   - cd frontend && npm run check:i18n:changed

2. ESLint:
   - cd frontend && npm run lint

3. TypeScript typecheck:
   - cd frontend && npx tsc --noEmit

4. Frontend unit tests with coverage (matches CI's npm run test:coverage):
   - cd frontend && npm run test:coverage

If any check fails, diagnose and fix the issue before proceeding. Do NOT skip failing tests.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npm run test:i18n && npm run check:i18n:changed && npm run lint && npx tsc --noEmit && npm run test:coverage</automated>
  </verify>
  <done>All frontend eslint, typecheck, i18n, and vitest checks pass with zero errors. Coverage report generated.</done>
</task>

<task type="auto">
  <name>Task 3: Commit all changes and push to main</name>
  <files>all modified and untracked files from git status</files>
  <action>
Only proceed if Tasks 1 and 2 both passed completely.

1. Stage all modified and new files shown in git status (the uncommitted work).
   IMPORTANT: Do NOT stage the cache/ directory. Use git add with explicit file paths or patterns excluding cache/.

2. Create a single commit with a message summarizing the changes. Review the diffs to write an accurate commit message.

3. Push to origin main:
   - git push origin main
  </action>
  <verify>
    <automated>git status && git log --oneline -1 && git diff origin/main..HEAD --stat</automated>
  </verify>
  <done>All changes committed. Push to origin/main succeeds. git status shows clean working tree (except cache/ which is untracked and not committed).</done>
</task>

</tasks>

<verification>
- Backend ruff check and format pass
- Backend bandit and pip-audit pass
- All backend pytest tests pass
- Frontend eslint, tsc, i18n parity, and vitest all pass with coverage
- Git status shows clean working tree after push
- git log shows the new commit on main
</verification>

<success_criteria>
All CI-equivalent checks pass locally and all uncommitted changes are pushed to origin/main.
</success_criteria>

<output>
After completion, create `.planning/quick/260328-t60-run-all-tests-and-ci-checks-then-push-ch/260328-t60-SUMMARY.md`
</output>
