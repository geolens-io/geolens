---
phase: 260405-dn1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/LandingPage.tsx
  - frontend/src/pages/__tests__/LandingPage.test.tsx
  - frontend/src/App.tsx
  - frontend/src/api/settings.ts
  - backend/app/persistent_config.py
  - backend/app/settings/router.py
  - backend/app/settings/schemas.py
  - frontend/src/i18n/locales/en/search.json
  - frontend/src/i18n/locales/de/search.json
  - frontend/src/i18n/locales/fr/search.json
  - frontend/src/i18n/locales/es/search.json
autonomous: true
requirements: [QUICK-260405-dn1]

must_haves:
  truths:
    - "Non-authenticated users hitting / see the catalog search page directly"
    - "No marketing hero, product previews, or landing page copy exists in the app"
    - "The show_landing_page branding toggle is completely removed from backend and frontend"
    - "All 4 locale files have no dead landing/trust i18n keys"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Index route renders SearchPage"
      contains: "Route index element={<SearchPage"
    - path: "backend/app/settings/schemas.py"
      provides: "BrandingResponse without show_landing_page"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/SearchPage.tsx"
      via: "index route element"
      pattern: "Route index element=.*SearchPage"
    - from: "backend/app/settings/router.py"
      to: "backend/app/persistent_config.py"
      via: "no SHOW_LANDING_PAGE import"
      pattern: "SHOW_LANDING_PAGE"
      expect: "absent"
---

<objective>
Remove the landing page from GeoLens and route `/` directly to the search catalog.

Purpose: The landing page contains marketing content that belongs on getgeolens.com, not in the self-hosted app. All deployments (enterprise, community, cloud) should show the catalog search as the homepage.

Output: LandingPage deleted, index route swapped to SearchPage, SHOW_LANDING_PAGE backend config removed, i18n keys cleaned.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260405-dn1-review-landing-page-for-enterprise-commu/260405-dn1-CONTEXT.md
@.planning/quick/260405-dn1-review-landing-page-for-enterprise-commu/260405-dn1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete landing page and swap index route to SearchPage</name>
  <files>
    frontend/src/pages/LandingPage.tsx,
    frontend/src/pages/__tests__/LandingPage.test.tsx,
    frontend/src/App.tsx
  </files>
  <action>
1. Delete `frontend/src/pages/LandingPage.tsx` (222 lines, contains ProductPreview and TrustSignalStrip sub-components).
2. Delete `frontend/src/pages/__tests__/LandingPage.test.tsx` (111 lines).
3. Edit `frontend/src/App.tsx`:
   - Remove line 16: the `LandingPage` lazy import (`const LandingPage = lazy(() => import('./pages/LandingPage')...)`).
   - Change line 54 from `<Route index element={<LandingPage />} />` to `<Route index element={<SearchPage />} />`.
   - The `SearchPage` lazy import already exists at line 17 — no new import needed.
   - Do NOT add a redirect. The component swap is cleaner — `/` and `/search` both render SearchPage directly.

Shared dependencies (PageShell, useBranding, useAuthStore, GEOLENS_LICENSE_URL) are used elsewhere — do NOT delete them.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>LandingPage.tsx and its test deleted. Index route renders SearchPage. No LandingPage import remains in App.tsx. Frontend tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Remove SHOW_LANDING_PAGE from backend config and branding API</name>
  <files>
    backend/app/persistent_config.py,
    backend/app/settings/router.py,
    backend/app/settings/schemas.py,
    frontend/src/api/settings.ts
  </files>
  <action>
1. Edit `backend/app/persistent_config.py`: Delete the `SHOW_LANDING_PAGE` constant (around lines 530-535). Search for the exact variable name and remove it entirely.
2. Edit `backend/app/settings/router.py`:
   - Remove the `SHOW_LANDING_PAGE` import (line 26).
   - Remove `show_landing_page` from the `get_branding` response dict (lines 382-383). After cleanup, the response should only contain `show_badge`.
3. Edit `backend/app/settings/schemas.py`: Remove `show_landing_page: bool` field from `BrandingResponse` (line 130). The schema should only have `show_badge: bool`.
4. Edit `frontend/src/api/settings.ts`: Remove `show_landing_page: boolean` from the `BrandingConfig` TypeScript interface (line 113).

Note: The orphaned `branding.show_landing_page` row in the database is harmless — no migration needed. The admin settings UI (`SettingsAppearanceTab.tsx`) does NOT have a toggle for this field, so no admin UI change is required.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && python -m pytest tests/test_branding_settings.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>SHOW_LANDING_PAGE removed from persistent_config.py, settings router, schemas, and frontend BrandingConfig interface. Backend branding endpoint returns only `{"show_badge": true}`. Backend tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Clean dead i18n keys from all 4 locale files</name>
  <files>
    frontend/src/i18n/locales/en/search.json,
    frontend/src/i18n/locales/de/search.json,
    frontend/src/i18n/locales/fr/search.json,
    frontend/src/i18n/locales/es/search.json
  </files>
  <action>
For each of the 4 locale files (`en`, `de`, `fr`, `es`) at `frontend/src/i18n/locales/{locale}/search.json`:

1. Remove the `"landing"` top-level key and its entire nested object (contains: eyebrow, searchHelper, primaryCta, searchCta, openSourceTitle, openSourceBody, workspaceTitle, workspaceBody, features.*).
2. Remove the `"trust"` top-level key and its entire nested object (contains: license, ogc, github). This was only used by LandingPage's TrustSignalStrip.

KEEP all other keys — `"title"`, `"subtitle"`, `"filters"`, `"empty"`, `"card"`, `"datasetCard"`, etc. are used by SearchPage and other components.

After editing, validate each file is valid JSON (no trailing commas, no dangling keys).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && for f in frontend/src/i18n/locales/*/search.json; do echo "Checking $f..." && python3 -c "import json; d=json.load(open('$f')); assert 'landing' not in d, 'landing key still present'; assert 'trust' not in d, 'trust key still present'; print('  OK')" || exit 1; done && echo "All locale files clean"</automated>
  </verify>
  <done>All 4 locale search.json files have no `landing` or `trust` keys. Files are valid JSON. No dead i18n keys remain from the removed landing page.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx vitest run` -- all frontend tests pass (LandingPage test removed, no broken imports)
2. `cd backend && python -m pytest tests/test_branding_settings.py -x` -- branding tests pass without show_landing_page
3. Grep confirms no remaining references: `grep -r "LandingPage\|SHOW_LANDING_PAGE\|show_landing_page" frontend/src/ backend/app/ --include="*.tsx" --include="*.ts" --include="*.py" | grep -v node_modules` returns nothing
4. All 4 locale files parse as valid JSON with no `landing` or `trust` keys
</verification>

<success_criteria>
- Visiting `/` renders the SearchPage catalog (not a marketing landing page)
- No file named LandingPage exists in the codebase
- No `show_landing_page` field exists in backend schemas, persistent config, or frontend types
- All i18n locale files are clean of dead landing/trust keys
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/260405-dn1-review-landing-page-for-enterprise-commu/260405-dn1-SUMMARY.md`
</output>
