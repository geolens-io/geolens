---
phase: 260316-c8k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/search/FilterPanel.tsx
  - frontend/src/i18n/locales/en/search.json
  - frontend/src/i18n/locales/es/search.json
  - frontend/src/i18n/locales/fr/search.json
  - frontend/src/i18n/locales/de/search.json
  - .planning/quick/260316-c8k-address-stac-readiness-and-raster-vrt-di/STAC-GAP-ANALYSIS.md
autonomous: true
requirements:
  - STAC-AUDIT
  - VRT-CHIP
  - TYPE-FILTER

must_haves:
  truths:
    - "User sees four type filter chips: All, Vector, Raster, VRT"
    - "Clicking VRT chip filters catalog to show only VRT datasets"
    - "Geometry type filter is hidden when VRT is selected"
    - "STAC gap analysis document exists with prioritized next steps"
  artifacts:
    - path: "frontend/src/components/search/FilterPanel.tsx"
      provides: "VRT filter chip in both desktop and mobile ToggleGroups"
      contains: "vrt_dataset"
    - path: "frontend/src/i18n/locales/en/search.json"
      provides: "VRT i18n key"
      contains: "filters.vrt"
    - path: ".planning/quick/260316-c8k-address-stac-readiness-and-raster-vrt-di/STAC-GAP-ANALYSIS.md"
      provides: "STAC 1.1.0 compliance gap analysis"
      contains: "stac_version"
  key_links:
    - from: "FilterPanel.tsx"
      to: "search-store.ts"
      via: "setFilter('record_type', 'vrt_dataset')"
      pattern: "setFilter.*record_type.*vrt_dataset"
---

<objective>
Add VRT type filter chip to catalog search and produce a STAC 1.1.0 gap analysis document.

Purpose: Let users discover VRT datasets via explicit type filtering, and document what remains for STAC compliance.
Output: Updated FilterPanel with 4 chips, i18n keys for all locales, STAC gap analysis markdown.
</objective>

<execution_context>
@/Users/ishiland/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishiland/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260316-c8k-address-stac-readiness-and-raster-vrt-di/260316-c8k-CONTEXT.md
@.planning/quick/260316-c8k-address-stac-readiness-and-raster-vrt-di/260316-c8k-RESEARCH.md

@frontend/src/components/search/FilterPanel.tsx
@frontend/src/stores/search-store.ts
@frontend/src/i18n/locales/en/search.json

<interfaces>
<!-- Key contracts the executor needs -->

From frontend/src/stores/search-store.ts:
- `setFilter(key: string, value: string)` — sets filter and triggers search
- `record_type` field in store state — holds 'vector_dataset' | 'raster_dataset' | 'vrt_dataset' | ''
- `toParams()` serializes record_type to URL query params
- `restoreParams()` reads record_type from URL

From FilterPanel.tsx:
- `recordType` derived from `useSearchStore(s => s.record_type)`
- Desktop ToggleGroup at ~line 268 (3 items: all, vector_dataset, raster_dataset)
- Mobile ToggleGroup at ~line 401 (same 3 items)
- Geometry type filter hidden at lines 288 and 421 when `recordType !== 'raster_dataset'`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add VRT filter chip and fix geometry filter visibility</name>
  <files>frontend/src/components/search/FilterPanel.tsx, frontend/src/i18n/locales/en/search.json, frontend/src/i18n/locales/es/search.json, frontend/src/i18n/locales/fr/search.json, frontend/src/i18n/locales/de/search.json</files>
  <action>
In FilterPanel.tsx, add a fourth ToggleGroupItem for VRT in BOTH the desktop ToggleGroup (~line 284, after the raster_dataset item) and the mobile ToggleGroup (~line 417, after the raster_dataset item):

```tsx
<ToggleGroupItem value="vrt_dataset" className="text-xs px-2.5 h-7">
  {t('filters.vrt', { defaultValue: 'VRT' })}
</ToggleGroupItem>
```

For the mobile version, use `className="flex-1 text-xs"` to match the existing mobile chip styling.

Fix geometry type filter visibility at BOTH locations (lines 288 and 421). Change:
```tsx
{recordType !== 'raster_dataset' && (
```
to:
```tsx
{recordType !== 'raster_dataset' && recordType !== 'vrt_dataset' && (
```

Add i18n keys to all four locale search.json files. Add these keys inside the `"filters"` object:
- `"allTypes": "All"` (en), `"Todos"` (es), `"Tous"` (fr), `"Alle"` (de)
- `"vector": "Vector"` (en), `"Vector"` (es), `"Vecteur"` (fr), `"Vektor"` (de)
- `"raster": "Raster"` (en/es/fr/de — same in all languages)
- `"vrt": "VRT"` (en/es/fr/de — acronym, same in all languages)
- `"type": "Type"` (en), `"Tipo"` (es), `"Type"` (fr), `"Typ"` (de)

Note: Some of these keys (allTypes, vector, raster, type) may already exist via defaultValue fallbacks but are not in the JSON files. Add them all so translations are proper and not relying on defaultValue.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Four type filter chips (All/Vector/Raster/VRT) render in both desktop and mobile layouts. VRT chip sends record_type=vrt_dataset to the search store. Geometry type filter is hidden for both raster and VRT record types. All four locale files have the new i18n keys.</done>
</task>

<task type="auto">
  <name>Task 2: Create STAC 1.1.0 gap analysis document</name>
  <files>.planning/quick/260316-c8k-address-stac-readiness-and-raster-vrt-di/STAC-GAP-ANALYSIS.md</files>
  <action>
Create a structured STAC 1.1.0 compliance gap analysis document. Use the detailed gap table from the RESEARCH.md as the foundation, but expand into a proper deliverable with these sections:

1. **Executive Summary** — One paragraph: GeoLens has strong STAC foundations (DatasetAsset model, to_stac_properties(), OGC record output that is close to STAC Item format). Three critical gaps remain for Item compliance: stac_version, bbox, properties.datetime.

2. **What Already Exists** — Table mapping GeoLens artifacts to STAC concepts:
   - DatasetAsset model (key, href, media_type, roles, size_bytes) -> STAC Asset
   - RasterAsset.to_stac_properties() -> proj extension + bands
   - Record.record_type -> STAC Item type discrimination
   - OGC record output (GeoJSON Feature) -> STAC Item structure
   - dataset_to_ogc_record() -> STAC Item serialization base
   - Backfill migration -> Existing data is STAC-ready

3. **Gap Analysis Table** — Use the table from RESEARCH.md (STAC Requirement | Status | Gap | Priority). Include all rows covering Item, Collection, Catalog, and Conformance requirements.

4. **Prioritized Roadmap** — Group gaps into implementation phases:
   - **Quick Wins (1-2 tasks):** Add stac_version literal, compute bbox from geometry, add properties.datetime from temporal_start
   - **Medium Effort (1 phase):** Include DatasetAsset in record output as STAC assets dict, add Collection type/stac_version, add STAC conformance URIs
   - **Future (separate milestone):** Full STAC API endpoints (/stac/items, /stac/collections), STAC search endpoint with CQL2, catalog root endpoint

5. **Architecture Notes** — The OGC record output is already a GeoJSON Feature, making STAC Item conversion largely additive (add fields, don't restructure). The DatasetAsset model was designed STAC-aligned from day one. No breaking changes needed.

Per user decision: this is audit and documentation only. No code changes in this task.
  </action>
  <verify>
    <automated>test -f /Users/ishiland/Code/geolens/.planning/quick/260316-c8k-address-stac-readiness-and-raster-vrt-di/STAC-GAP-ANALYSIS.md && grep -c "stac_version" /Users/ishiland/Code/geolens/.planning/quick/260316-c8k-address-stac-readiness-and-raster-vrt-di/STAC-GAP-ANALYSIS.md</automated>
  </verify>
  <done>STAC gap analysis document exists with: executive summary, existing infrastructure mapping, full gap table with priorities, prioritized implementation roadmap, and architecture notes. Document is actionable for future STAC milestone planning.</done>
</task>

</tasks>

<verification>
- FilterPanel renders 4 type chips in both desktop and mobile views
- VRT chip correctly filters to vrt_dataset record_type
- Geometry type filter hidden for both raster_dataset and vrt_dataset
- All 4 locale files have filters.vrt key
- STAC gap analysis document is complete and actionable
- Existing frontend tests still pass
</verification>

<success_criteria>
- Four filter chips visible: All / Vector / Raster / VRT
- VRT filtering works end-to-end (chip -> store -> API query param)
- Geometry filter hidden for raster AND VRT types
- STAC gap analysis covers all STAC 1.1.0 Item/Collection/Catalog requirements
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/quick/260316-c8k-address-stac-readiness-and-raster-vrt-di/260316-c8k-SUMMARY.md`
</output>
