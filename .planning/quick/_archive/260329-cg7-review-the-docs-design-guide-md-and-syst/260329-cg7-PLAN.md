---
phase: 260329-cg7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/status-colors.ts
  - frontend/src/components/search/RecordTypeBadge.tsx
  - frontend/src/components/search/DatasetCard.tsx
  - frontend/src/components/search/SearchResultCard.tsx
  - frontend/src/components/dataset/ValidationStatus.tsx
  - frontend/src/components/dataset/tabs/OverviewTab.tsx
  - frontend/src/components/dataset/tabs/SourcesTab.tsx
  - frontend/src/components/dataset/QualityScoreCard.tsx
  - frontend/src/components/import/ImportPreview.tsx
  - frontend/src/components/import/FileDropzone.tsx
  - frontend/src/components/builder/SharePanel.tsx
  - frontend/src/components/builder/DatasetSearchPanel.tsx
  - frontend/src/components/builder/EphemeralBadge.tsx
  - frontend/src/components/builder/BasemapPicker.tsx
  - frontend/src/components/maps/MapCreateDialog.tsx
  - frontend/src/components/maps/VisibilityIcon.tsx
  - frontend/src/components/map/FeaturePopup.tsx
  - frontend/src/components/error/MapErrorBoundary.tsx
  - frontend/src/components/admin/StatsOverview.tsx
  - frontend/src/components/admin/AIStatusCard.tsx
  - frontend/src/components/admin/settings/SettingsAITab.tsx
  - frontend/src/components/admin/settings/EnvOnlyBanner.tsx
  - frontend/src/components/viewer/LayerLegend.tsx
  - frontend/src/pages/MapBuilderPage.tsx
  - frontend/src/pages/admin/AdminConfigOpsPage.tsx
  - frontend/src/pages/admin/AdminSharedMapsPage.tsx
  - frontend/src/components/search/__tests__/CollectionSearchCard.test.tsx
autonomous: false
requirements: []

must_haves:
  truths:
    - "No component uses hardcoded Tailwind palette colors (bg-red-*, text-green-*, etc.) for UI semantics -- only semantic tokens or status-colors.ts maps"
    - "No component uses transition-all -- only targeted property-list transitions"
    - "No component uses bg-gray-*, bg-slate-*, text-gray-*, text-slate-*, border-gray-*, border-slate-* -- only semantic tokens"
    - "Build passes with zero TypeScript errors"
  artifacts:
    - path: "frontend/src/lib/status-colors.ts"
      provides: "Extended status color maps for record types and ingestion status"
    - path: "frontend/src/components/search/RecordTypeBadge.tsx"
      provides: "Record type badges using status-colors.ts"
  key_links:
    - from: "frontend/src/components/search/RecordTypeBadge.tsx"
      to: "frontend/src/lib/status-colors.ts"
      via: "import recordTypeColors"
      pattern: "recordTypeColors"
    - from: "frontend/src/components/dataset/tabs/OverviewTab.tsx"
      to: "frontend/src/lib/status-colors.ts"
      via: "import qualityScoreClasses"
      pattern: "qualityScoreClasses"
---

<objective>
Systematically audit and update all frontend components to conform to docs/DESIGN-GUIDE.md. Three categories of violations were found across 25 files:

1. **Hardcoded Tailwind palette colors** (20 files) -- bg-red-*, text-green-*, border-amber-*, etc. used for UI status/semantic colors instead of semantic tokens or status-colors.ts
2. **transition-all usage** (7 files) -- causes layout thrash per GUIDE anti-pattern #5
3. **bg-gray-*/bg-slate-*/bg-white** (3 files) -- should use semantic tokens (bg-muted, text-muted-foreground, bg-background)

Purpose: Ensure all components respect the design system's token hierarchy, dark mode correctness, and transition rules.
Output: All 25+ files updated to use semantic tokens and design guide patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/DESIGN-GUIDE.md
@frontend/src/lib/status-colors.ts
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend status-colors.ts and fix search/dataset/import components (13 files)</name>
  <files>
    frontend/src/lib/status-colors.ts
    frontend/src/components/search/RecordTypeBadge.tsx
    frontend/src/components/search/DatasetCard.tsx
    frontend/src/components/search/SearchResultCard.tsx
    frontend/src/components/dataset/ValidationStatus.tsx
    frontend/src/components/dataset/tabs/OverviewTab.tsx
    frontend/src/components/dataset/tabs/SourcesTab.tsx
    frontend/src/components/dataset/QualityScoreCard.tsx
    frontend/src/components/import/ImportPreview.tsx
    frontend/src/components/import/FileDropzone.tsx
    frontend/src/components/map/FeaturePopup.tsx
    frontend/src/components/maps/MapCreateDialog.tsx
    frontend/src/components/maps/VisibilityIcon.tsx
    frontend/src/components/search/__tests__/CollectionSearchCard.test.tsx
  </files>
  <action>
**Step 1: Extend status-colors.ts** with new maps for patterns currently using hardcoded palette colors:

Add `recordTypeColors` map (used by RecordTypeBadge.tsx):
```ts
export const recordTypeColors: Record<string, string> = {
  collection: semanticBadgeColors.warning,    // was bg-amber-100 text-amber-700
  vector_dataset: semanticBadgeColors.info,   // was bg-blue-100 text-blue-700 -- note: info is teal-based now
  raster_dataset: semanticBadgeColors.success, // was bg-emerald-100 text-emerald-700
  vrt_dataset: 'border-violet-300 bg-violet-100 text-violet-950 dark:border-violet-900/60 dark:bg-violet-950/30 dark:text-violet-200', // was bg-violet-100 text-violet-700
  unknown: 'border-orange-300 bg-orange-100 text-orange-950 dark:border-orange-900/60 dark:bg-orange-950/30 dark:text-orange-200', // was bg-orange-100 text-orange-700
} as const;
```

Add `ingestionStatusColors` map for DatasetCard/SearchResultCard:
```ts
export const ingestionStatusColors: Record<string, string> = {
  draft: semanticBadgeColors.warning,
  ready: semanticBadgeColors.info,
  internal: 'border-border bg-muted text-muted-foreground',
} as const;
```

Add `validationLevelColors` for ValidationStatus.tsx:
```ts
export const validationLevelColors = {
  error: 'text-destructive',
  warning: 'text-warning',
  success: 'text-success',
} as const;
```

Add `healthDotColors` for SourcesTab health indicators:
```ts
export const healthDotColors = {
  healthy: 'bg-success',
  unhealthy: 'bg-destructive',
  unknown: 'bg-muted-foreground',
} as const;
```

**Step 2: Update RecordTypeBadge.tsx** -- Replace the inline `RECORD_TYPE_CONFIG` className entries with `recordTypeColors[type]` from status-colors.ts. Each badge should use `<Badge className={recordTypeColors[type] || recordTypeColors.unknown}>`.

**Step 3: Update DatasetCard.tsx** -- Replace lines ~108-110 `statusVariants` inline map with `ingestionStatusColors` from status-colors.ts. Replace `border-slate-500/50 text-slate-600 dark:text-slate-400` (internal) with `ingestionStatusColors.internal`. Also replace `border-purple-500/50 text-purple-600 dark:text-purple-400` (AI badge ~line 134) with `semanticBadgeColors` or `'border-violet-300 bg-violet-100 text-violet-950 dark:border-violet-900/60 dark:bg-violet-950/30 dark:text-violet-200'` from the vrt_dataset pattern (or add an `aiBadgeColor` to status-colors).

**Step 4: Update SearchResultCard.tsx** -- Same changes as DatasetCard.tsx (lines ~140-142 statusVariants and ~166 AI badge). It mirrors DatasetCard's pattern.

**Step 5: Update ValidationStatus.tsx** -- Replace hardcoded `text-rose-700 dark:text-rose-200`, `text-amber-700 dark:text-amber-200`, `text-emerald-700 dark:text-emerald-200` with `validationLevelColors.error`, `.warning`, `.success`. These are icon colors, not badge backgrounds, so the semantic token approach (`text-destructive`, `text-warning`, `text-success`) is correct here.

**Step 6: Update OverviewTab.tsx** -- Line ~165: replace `text-green-600 dark:text-green-400` with `text-success`. Lines ~226-230: replace the quality score border/text ternary chain with `qualityScoreClasses()` import from status-colors.ts (already exists, just not used here). If qualityScoreClasses doesn't return border classes matching the pattern, add a `qualityIndicatorClasses(score)` helper.

**Step 7: Update SourcesTab.tsx** -- Line ~166: replace `border-blue-300 bg-blue-50 ... text-blue-800 dark:border-blue-700 dark:bg-blue-950 dark:text-blue-200` info banner with semantic tokens: `border-info/30 bg-info/5 text-foreground` or use the `semanticBadgeColors.info` pattern wrapped in a div. Lines ~321-331: replace `bg-green-500`, `bg-red-500`, `bg-gray-300` health dots with `healthDotColors` from status-colors.ts.

**Step 8: Update QualityScoreCard.tsx** -- Line ~97: replace `transition-all` with `transition-[width]` (it's a progress bar width animation).

**Step 9: Update ImportPreview.tsx** -- Line ~37: replace `bg-emerald-100 text-emerald-700 dark:bg-emerald-900/30 dark:text-emerald-400` with appropriate `semanticBadgeColors.success`. Line ~111: replace `bg-blue-50 text-blue-700 border-blue-200 ...` info badge with `semanticBadgeColors.info`.

**Step 10: Update FileDropzone.tsx** -- Line ~45: replace `transition-all` with `transition-[color,background-color,border-color] duration-200 ease-out`.

**Step 11: Update FeaturePopup.tsx** -- Line ~151: replace `text-green-500` (boolean check icon) with `text-success`.

**Step 12: Update MapCreateDialog.tsx** -- Line ~133: replace `border-amber-500/50 ... text-amber-600 dark:text-amber-400` "AI" badge with a named export from status-colors or use `semanticBadgeColors.warning`.

**Step 13: Update VisibilityIcon.tsx** -- Line ~5: replace `text-amber-500` with `text-warning`.

**Step 14: Update CollectionSearchCard.test.tsx** -- Line ~29: update test assertion from `bg-amber-100` to match the new class from `recordTypeColors.collection` (which uses `semanticBadgeColors.warning`). The test should assert on `bg-amber-100` still since semanticBadgeColors.warning contains that class -- verify and adjust if needed.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npx tsc --noEmit 2>&1 | head -30 && grep -rn 'bg-gray-\|bg-slate-\|text-slate-\|border-slate-' src/components/search/ src/components/dataset/ src/components/import/ src/components/map/FeaturePopup.tsx src/components/maps/ 2>/dev/null | grep -v __tests__ | head -10; echo "---"; grep -c 'transition-all' src/components/dataset/QualityScoreCard.tsx src/components/import/FileDropzone.tsx 2>/dev/null</automated>
  </verify>
  <done>All 13 files use semantic tokens or status-colors.ts maps. No hardcoded palette colors for status semantics. No transition-all in QualityScoreCard or FileDropzone. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Fix builder, admin, viewer, error, and page-level components (12 files)</name>
  <files>
    frontend/src/components/builder/SharePanel.tsx
    frontend/src/components/builder/DatasetSearchPanel.tsx
    frontend/src/components/builder/EphemeralBadge.tsx
    frontend/src/components/builder/BasemapPicker.tsx
    frontend/src/components/error/MapErrorBoundary.tsx
    frontend/src/components/admin/StatsOverview.tsx
    frontend/src/components/admin/AIStatusCard.tsx
    frontend/src/components/admin/settings/SettingsAITab.tsx
    frontend/src/components/admin/settings/EnvOnlyBanner.tsx
    frontend/src/components/viewer/LayerLegend.tsx
    frontend/src/pages/MapBuilderPage.tsx
    frontend/src/pages/admin/AdminConfigOpsPage.tsx
    frontend/src/pages/admin/AdminSharedMapsPage.tsx
  </files>
  <action>
**SharePanel.tsx:**
- Line ~48: Replace `text-amber-500` icon class with `text-warning`.
- Lines ~506-508: Replace `bg-blue-50 dark:bg-blue-950/30 border border-blue-200 dark:border-blue-800` info box and `text-blue-600 dark:text-blue-400` / `text-blue-700 dark:text-blue-300` text with semantic tokens: use `border-info/30 bg-info/5` for the container and `text-info` for the icon, `text-sm text-foreground` for the paragraph text.

**DatasetSearchPanel.tsx:**
- Line ~94: Replace `text-emerald-600 border-emerald-400 dark:text-emerald-400` badge with `semanticBadgeColors.success` from status-colors.ts import.

**EphemeralBadge.tsx:**
- Line ~14: Replace `bg-orange-500` dot with `bg-warning` semantic token.

**BasemapPicker.tsx:**
- Line ~67: Replace `transition-all` with `transition-[color,background-color,box-shadow,border-color,opacity] duration-200 ease-out`.

**MapErrorBoundary.tsx:**
- Line ~33: Replace `text-amber-600 dark:text-amber-400` with `text-warning`.

**StatsOverview.tsx:**
- Line ~38: Replace `bg-emerald-500` with `bg-success`.
- Line ~119: Replace `text-emerald-500` with `text-success`.

**AIStatusCard.tsx:**
- Line ~74: Replace `transition-all` on the progress bar with `transition-[width]`.

**SettingsAITab.tsx:**
- Lines ~294-296, ~308-310: Replace `border-amber-500/30 bg-amber-500/5` warning boxes and `text-amber-500`/`text-amber-700 dark:text-amber-400` text with semantic tokens: `border-warning/30 bg-warning/5` for container, `text-warning` for icons, `text-foreground` for body text.
- Line ~327: Replace `transition-all` on progress bar with `transition-[width]`.
- Lines ~394, ~407: Replace `text-green-500` check icons with `text-success`.

**EnvOnlyBanner.tsx:**
- Lines ~12-18: Replace `border-blue-200 bg-blue-50 dark:border-blue-900 dark:bg-blue-950/50` with `border-info/30 bg-info/5`. Replace `text-blue-600 dark:text-blue-400` icon with `text-info`. Replace `text-blue-800 dark:text-blue-200` and `text-blue-700 dark:text-blue-300` text with `text-foreground` and `text-muted-foreground`. Replace `bg-blue-100 dark:bg-blue-900` code background with `bg-info/10`.

**LayerLegend.tsx:**
- Line ~56: Replace `transition-all duration-200` with `transition-[opacity,transform] duration-200 ease-out`.

**MapBuilderPage.tsx:**
- Lines ~193-194: Replace `transition-all duration-200` with `transition-[width,border-width] duration-200 ease-out` (sidebar collapse animation needs width transition).

**AdminConfigOpsPage.tsx:**
- Line ~113: Replace `text-green-600` with `text-success`.
- Lines ~463-464: Replace `border-green-500/50 bg-green-500/10` success box and `text-green-600` icon with `border-success/30 bg-success/5` and `text-success`.

**AdminSharedMapsPage.tsx:**
- Lines ~110, ~237: Replace `bg-green-600 text-white` active badge with proper semantic: use `semanticBadgeColors.success` from status-colors.ts.
- Line ~114: Replace `bg-amber-500 text-white` badge with `semanticBadgeColors.warning`.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npx tsc --noEmit 2>&1 | head -30 && echo "---palette-check---" && grep -rn 'bg-\(red\|green\|blue\|yellow\|orange\|purple\|amber\|emerald\|rose\|cyan\|teal\|indigo\|violet\|lime\|sky\|fuchsia\|pink\|slate\|gray\)-' src/components/builder/ src/components/admin/ src/components/viewer/ src/components/error/ src/pages/ --include="*.tsx" 2>/dev/null | grep -v __tests__ | head -10; echo "---transition-check---" && grep -rn 'transition-all' src/components/ src/pages/ --include="*.tsx" 2>/dev/null | head -10</automated>
  </verify>
  <done>All 12 files use semantic tokens or status-colors.ts. Zero hardcoded palette colors remain for UI semantics across the entire frontend. Zero transition-all usages remain. TypeScript compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 3: Visual spot-check in browser</name>
  <files>n/a</files>
  <action>
    Human visual verification of all design guide compliance changes.
    What was built: All 25 component files updated to use semantic design tokens instead of hardcoded Tailwind palette colors. transition-all replaced everywhere with targeted transitions. Status badges now use centralized status-colors.ts maps.
    Steps to verify:
    1. Visit http://localhost:8080 (ensure docker is running)
    2. Check search page -- record type badges (collection, vector, raster, VRT) should show colored badges
    3. Click into a dataset -- check OverviewTab quality score colors, SourcesTab health dots
    4. Toggle dark mode via user menu -- verify no white-on-dark or dark-on-white contrast issues on badges
    5. Visit Admin > Jobs -- check status badges render correctly
    6. Visit Admin > Share Tokens -- check active/expired badges
    7. Open Map Builder -- check sidebar collapse animation is smooth (no layout thrash)
    8. Check basemap picker hover transitions are smooth
  </action>
  <verify>
    <automated>echo "Human verification -- visual spot-check"</automated>
  </verify>
  <done>User confirms badges, status indicators, and transitions render correctly in both light and dark mode with no contrast issues.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` -- zero errors
- `grep -rn 'transition-all' src/components/ src/pages/ --include="*.tsx"` -- zero matches
- `grep -rn 'bg-gray-\|bg-slate-\|text-gray-\|text-slate-\|border-gray-\|border-slate-' src/components/ src/pages/ --include="*.tsx" | grep -v __tests__` -- zero matches
- Hardcoded palette color grep returns zero matches in component/page files (excluding test files, map-colors.ts, status-colors.ts): `grep -rn 'bg-\(red\|green\|blue\|yellow\|orange\|purple\|amber\|emerald\|rose\|cyan\|indigo\|violet\|lime\|sky\|fuchsia\|pink\)-[0-9]' src/components/ src/pages/ --include="*.tsx" | grep -v __tests__` -- NOTE: status-colors.ts itself legitimately uses palette classes for tinted badge backgrounds per DESIGN-GUIDE section on Status Badges, so exclude that file
</verification>

<success_criteria>
- All 25 files updated to use semantic tokens per DESIGN-GUIDE.md
- status-colors.ts extended with recordTypeColors, ingestionStatusColors, validationLevelColors, healthDotColors
- Zero transition-all in any component or page file
- Zero hardcoded gray/slate palette colors in components
- TypeScript compiles with zero errors
- Dark mode contrast maintained (semantic tokens handle this automatically)
</success_criteria>

<output>
After completion, create `.planning/quick/260329-cg7-review-the-docs-design-guide-md-and-syst/260329-cg7-SUMMARY.md`
</output>
