---
phase: quick-260320-e6i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/dataset/DatasetMap.tsx
  - frontend/src/components/dataset/__tests__/DatasetMap.test.tsx
autonomous: true
requirements: [E6I-MAP-CONTROLS]

must_haves:
  truths:
    - "Zoom-to-extent button is always visible when bbox exists, regardless of drawing state"
    - "NavigationControl (+/- zoom) shows for raster/VRT (always interactive) and vector when drawing"
    - "Mouse scroll zoom is disabled on all detail page maps unless in fullscreen mode"
    - "Existing tests updated to reflect new always-visible zoom-to-extent behavior"
  artifacts:
    - path: "frontend/src/components/dataset/DatasetMap.tsx"
      provides: "Fixed map controls visibility logic"
    - path: "frontend/src/components/dataset/__tests__/DatasetMap.test.tsx"
      provides: "Updated tests for map control visibility"
  key_links:
    - from: "DatasetMap.tsx"
      to: "MapGL"
      via: "scrollZoom={isFullscreen} prop disables scroll zoom"
    - from: "DatasetMap.tsx"
      to: "zoom-to-extent button"
      via: "hasBbox condition (no longer gated on isDrawing)"
---

<objective>
Fix vector detail page map controls so zoom-to-extent and fullscreen buttons are always accessible, and ensure scroll zoom stays disabled across all detail page maps.

Purpose: Users viewing vector datasets in read-only mode currently have no way to zoom to the dataset extent or use zoom controls. Raster/VRT pages show NavigationControl but not zoom-to-extent. This fix makes the zoom-to-extent button always visible when a bbox exists.
Output: Updated DatasetMap.tsx with corrected control visibility, updated tests.
</objective>

<execution_context>
@/Users/ishiland/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishiland/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/components/dataset/DatasetMap.tsx
@frontend/src/components/dataset/__tests__/DatasetMap.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix map control visibility in DatasetMap</name>
  <files>frontend/src/components/dataset/DatasetMap.tsx</files>
  <action>
In DatasetMap.tsx, make these targeted changes:

1. **Zoom-to-extent button (line ~1026):** Remove the `isDrawing` guard so the button shows whenever `hasBbox` is true:
   - Change `{isDrawing && hasBbox && (` to `{hasBbox && (`
   - This makes zoom-to-extent available on all detail pages (vector read-only, raster, VRT) whenever a bounding box exists.

2. **Control container positioning (line ~1025):** The container `top-[120px]` is positioned to sit below the NavigationControl. When NavigationControl is not shown (vector read-only), the controls should still be positioned at top-right. Change the container positioning:
   - Use `top-3 right-[10px]` as the base position when NavigationControl is NOT shown.
   - Keep `top-[120px] right-[10px]` when NavigationControl IS shown (i.e., when `isDrawing || recordType === 'raster_dataset' || recordType === 'vrt_dataset'`).
   - Compute a boolean like `const showNavControl = isDrawing || recordType === 'raster_dataset' || recordType === 'vrt_dataset';` (extract from the existing inline condition on line 981) and use it for both the NavigationControl rendering and the control container positioning:
     `className={cn("absolute z-10 flex flex-col gap-1 right-[10px]", showNavControl ? "top-[120px]" : "top-3")}`

3. **scrollZoom:** Already correct (`scrollZoom={isFullscreen}`). No change needed. Verify it stays as-is.

Do NOT change the NavigationControl visibility logic — it correctly shows only for interactive maps (drawing or raster/VRT).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && npx vitest run frontend/src/components/dataset/__tests__/DatasetMap.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Zoom-to-extent button renders for all record types when bbox exists. Control container position adjusts based on NavigationControl visibility. scrollZoom remains disabled unless fullscreen.</done>
</task>

<task type="auto">
  <name>Task 2: Update DatasetMap tests for new control visibility</name>
  <files>frontend/src/components/dataset/__tests__/DatasetMap.test.tsx</files>
  <action>
Update the existing tests and add new ones:

1. **Update test "keeps the hero map static until edit mode starts" (line ~85):** The assertion `expect(screen.queryByTitle('Zoom to dataset extent')).not.toBeInTheDocument()` is now WRONG — zoom-to-extent should always show when bbox is provided. Change to:
   `expect(screen.getByTitle('Zoom to dataset extent')).toBeInTheDocument();`

2. **Add new test "shows zoom-to-extent for vector dataset in read-only mode":**
   - Render DatasetMap with bbox, tableName, geometryType, datasetId, NO canEdit.
   - Assert zoom-to-extent button is in the document.
   - Assert NavigationControl is NOT in the document (no nav-control testid).

3. **Add new test "shows zoom-to-extent for raster dataset":**
   - Render DatasetMap with bbox, recordType="raster_dataset", rasterTileUrl="/raster-tiles/test/{z}/{x}/{y}.png".
   - Assert zoom-to-extent button is in the document.

4. **Add new test "does not show zoom-to-extent when no bbox":**
   - Render DatasetMap with bbox=null, tableName="example_table", geometryType="Polygon".
   - Assert zoom-to-extent button is NOT in the document.
   - Note: The component returns the "no spatial extent" message when both bbox and tableName are null, so use bbox=null with a valid tableName.

Keep all existing passing tests intact.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && npx vitest run frontend/src/components/dataset/__tests__/DatasetMap.test.tsx --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>All existing tests pass with updated assertions. New tests confirm zoom-to-extent visibility for vector read-only, raster, and absence when no bbox.</done>
</task>

</tasks>

<verification>
- `cd /Users/ishiland/Code/geolens && npx vitest run frontend/src/components/dataset/__tests__/DatasetMap.test.tsx` passes all tests
- `cd /Users/ishiland/Code/geolens && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -20` shows no type errors in modified files
</verification>

<success_criteria>
- Zoom-to-extent button visible on all detail pages when bbox exists (vector read-only, vector drawing, raster, VRT)
- NavigationControl only shows for interactive maps (raster/VRT always, vector during drawing)
- scrollZoom disabled unless fullscreen on all detail pages
- All DatasetMap tests pass
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/quick/260320-e6i-fix-vector-detail-page-map-controls-and-/260320-e6i-SUMMARY.md`
</output>
