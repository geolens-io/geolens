---
phase: 260419-pi8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/DatasetPage.tsx
  - frontend/src/components/dataset/DatasetStatsBar.tsx
  - frontend/src/components/dataset/tabs/OverviewTab.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "RecordTypeStats header shows only RecordTypeBadge, record status, and visibility badge"
    - "TableHero has no feature_count badge"
    - "Stats bar for raster/VRT does not show compression, dimensions, or file size"
    - "Overview sidebar metadata card does not show Updated or SRID"
  artifacts:
    - path: "frontend/src/pages/DatasetPage.tsx"
      provides: "Stripped RecordTypeStats and TableHero"
    - path: "frontend/src/components/dataset/DatasetStatsBar.tsx"
      provides: "Raster/VRT stats bar without compression, dimensions, file size"
    - path: "frontend/src/components/dataset/tabs/OverviewTab.tsx"
      provides: "Sidebar metadata card without Updated and SRID"
  key_links:
    - from: "DatasetStatsBar"
      to: "DatasetPage"
      via: "import and render"
      pattern: "DatasetStatsBar"
---

<objective>
Remove redundant data displays from the dataset details page where the same information appears in multiple locations (header stats, stats bar, overview sidebar).

Purpose: Reduce visual noise — stats bar is the primary quick-reference display; other locations should not duplicate it.
Output: Three modified files with cleaner, non-redundant data presentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/pages/DatasetPage.tsx
@frontend/src/components/dataset/DatasetStatsBar.tsx
@frontend/src/components/dataset/tabs/OverviewTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip RecordTypeStats and TableHero in DatasetPage.tsx</name>
  <files>frontend/src/pages/DatasetPage.tsx</files>
  <action>
In `RecordTypeStats` (lines 118-229):
- Remove the entire first `<div className="flex items-center gap-1.5 flex-wrap">` block (lines 124-216) which contains ALL the inline stats (geometry type, feature count, EPSG, 3D, elevation, raster band/resolution/epsg, VRT type/sources/bands/epsg).
- Replace it with a single line containing only `<RecordTypeBadge recordType={dataset.record_type} />`.
- In the second `<div>` (lines 217-226), remove the "Updated {formatRelativeDate(...)}" line (line 225) and the `<Sep />` before it (line 224). Keep only record status label and visibility badge.
- The resulting RecordTypeStats should render: RecordTypeBadge on one line, then record status + visibility badge on a second line.

In `TableHero` (lines 231-299):
- Remove the feature_count Badge block at lines 271-275 (the `{dataset.feature_count != null && ...}` conditional with the Badge).

Clean up imports that are no longer used after these removals:
- `Mountain` from lucide-react (was used for elevation icon)
- `computeRasterGsd` from `@/lib/geo-utils` (was used for raster GSD in header; still used in DatasetStatsBar so only remove from THIS file)
- `findElevationColumn` from `@/lib/geo-utils` (was used for elevation detection in header)
- `getGeometryTypeLabel` from `@/i18n/labels` (was used for geometry type in header; check if used elsewhere in file before removing)
- `formatNumber` from `@/lib/format` (was used for feature count in header; check if used elsewhere in file before removing)
- `Sep` component can stay — still used in the status/visibility line

Keep `formatRelativeDate` — verify it is still used elsewhere in the file before removing.
  </action>
  <verify>npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | grep -c "error" || echo "0 errors"</verify>
  <done>RecordTypeStats shows only RecordTypeBadge + record status + visibility badge. TableHero has no feature_count badge. No unused import warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Trim raster/VRT stats from DatasetStatsBar and sidebar metadata from OverviewTab</name>
  <files>frontend/src/components/dataset/DatasetStatsBar.tsx, frontend/src/components/dataset/tabs/OverviewTab.tsx</files>
  <action>
In DatasetStatsBar.tsx, within the `if (isRaster || isVrt)` block (lines 49-88):
- Remove the dimensions cell (lines 64-70): the `if (dataset.raster?.width != null && dataset.raster?.height != null)` block
- Remove the compression cell (lines 78-83): the `if (dataset.raster?.compression)` block
- Remove the file size cell (lines 90-96): the `if (dataset.raster?.size_bytes)` block
- Keep: bands, resolution, CRS, sources (VRT only), and the "Updated" cell at the end
- Clean up unused imports: remove `formatBytes` from the import line (no longer used after removing size_bytes cell)

In OverviewTab.tsx, within the sidebar metadata card:
- Remove the Updated row at line 346: `<SideKV label={t('metadata.updated', ...)} value={formatRelativeDate(dataset.updated_at)} mono />`
- Remove the SRID block at lines 358-360: the `{dataset.srid && (<SideKV label={t('overview.srid', ...)} .../>)}` conditional
- Check if `formatRelativeDate` is still used elsewhere in OverviewTab.tsx after removing the Updated row. If not, remove it from the imports.
  </action>
  <verify>npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | grep -c "error" || echo "0 errors"</verify>
  <done>Raster/VRT stats bar shows only bands, resolution, CRS, sources (VRT), updated. Overview sidebar metadata card no longer shows Updated or SRID rows.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --project frontend/tsconfig.json` passes with 0 errors
- Visual check: dataset detail page header shows only badge + status + visibility (no stats)
- Visual check: raster stats bar has no compression, dimensions, or file size cells
- Visual check: overview sidebar has no Updated or SRID rows
</verification>

<success_criteria>
- No data field appears in more than one location on the dataset details page
- Stats bar remains the single source of quick-reference stats
- Overview sidebar shows only fields unique to that context (license, source, format, maintainer, created, cadence, bbox)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260419-pi8-clean-up-redundant-data-on-dataset-detai/260419-pi8-SUMMARY.md`
</output>
