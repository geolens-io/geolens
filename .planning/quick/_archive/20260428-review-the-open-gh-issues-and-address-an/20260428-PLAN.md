---
description: review the open GH issues and address any and all that can be grouped into this session
mode: quick-full
status: planned
created: 2026-05-05
must_haves:
  truths:
    - Open issue review covers geolens-io/geolens issues #50-#59, #78, #81-#85, and #92.
    - Session scope is limited to issues that are cohesive, low-risk, and directly verifiable locally.
    - Root repository docs remain single-purpose; examples live under examples/ and README only gets compact operational snippets.
  artifacts:
    - examples/manifests/public-cog/geolens.yaml
    - examples/manifests/public-cog/README.md
    - examples/postman/geolens-ogc-api.postman_collection.json
    - examples/postman/README.md
    - examples/sdk/python/upload_shapefile_publish_map.py
    - README.md
    - backend/tests/conftest.py
    - .env.test.example
    - .planning/quick/20260428-review-the-open-gh-issues-and-address-an/20260428-SUMMARY.md
    - .planning/quick/20260428-review-the-open-gh-issues-and-address-an/20260428-VERIFICATION.md
  key_links:
    - https://github.com/geolens-io/geolens/issues/78
    - https://github.com/geolens-io/geolens/issues/81
    - https://github.com/geolens-io/geolens/issues/82
    - https://github.com/geolens-io/geolens/issues/83
    - https://github.com/geolens-io/geolens/issues/84
    - https://github.com/geolens-io/geolens/issues/85
---

# Quick Task 20260428 Plan

## Issue Triage

Open issues reviewed via `gh issue list --repo geolens-io/geolens --state open --limit 100 --json number,title,labels,body,url`.

Grouped into this session:

- #81, #82, #83, #84, #85: docs/example quick wins with shared first-run adoption intent.
- #78: isolated backend test-infra fix that removes a documented concurrent pytest hazard.

Deferred:

- #92: requires a product/release decision about `geolens-helm`, not a code-only fix.
- #50-#59: map-builder feature set spans UI, backend contracts, migrations, or larger UX design. #52 is smaller than the others, but mixing frontend map styling with docs/test-infra would broaden this quick task beyond a coherent batch.

## Tasks

### 1. Add adoption examples for #81, #84, and #85

files:
- `examples/manifests/public-cog/geolens.yaml`
- `examples/manifests/public-cog/README.md`
- `examples/postman/geolens-ogc-api.postman_collection.json`
- `examples/postman/README.md`
- `examples/sdk/python/upload_shapefile_publish_map.py`

action:
- Add a public COG manifest with source attribution and the real `geolens validate` / `geolens apply` commands.
- Add a Postman collection for OGC landing, collections, bbox item query, and OGC Records search.
- Add a runnable Python SDK workflow that uploads a zipped Shapefile, previews/commits ingest, waits for completion, creates a map, adds the dataset layer, and makes the map public.

verify:
- Validate JSON syntax for the Postman collection.
- Run Python compile check for the SDK example.
- Confirm manifest shape is compatible with existing schema examples.

done:
- Examples exist under `examples/`, include setup notes, and contain no secrets.

### 2. Update concise README snippets for #82 and #83

files:
- `README.md`

action:
- Add a compact semantic + spatial pgvector SQL example under "See It in Action".
- Add a compact first-run Docker Compose troubleshooting subsection under "Quick Start" covering ports 5434/8001/8080, `docker compose ps`, API/frontend/worker logs, and health delays.

verify:
- Check links and commands are aligned with current README Docker Compose ports.
- Keep content concise so root README stays overview-oriented.

done:
- README addresses both issues without creating a root `docs/` directory or duplicating the external docs site.

### 3. Isolate backend test database names for #78

files:
- `backend/tests/conftest.py`
- `.env.test.example`
- `backend/tests/test_ingest_column_preservation.py`

action:
- Generate a unique database name per pytest session from the configured `POSTGRES_DB_TEST` base.
- Patch `settings.postgres_db_test` for the session lifecycle so all test URLs and ogr2ogr safety redirects use the same isolated database.
- Restore the configured base name after setup/teardown paths and remove/update the inline concurrent-session warning.
- Update `.env.test.example` and ingest test comments to reflect automatic per-session suffixing.

verify:
- Run focused pytest for config or a lightweight backend test target.
- Run `cd backend && uv run ruff check tests/conftest.py tests/test_ingest_column_preservation.py`.

done:
- Two concurrent pytest sessions no longer target the same physical database name by default.
