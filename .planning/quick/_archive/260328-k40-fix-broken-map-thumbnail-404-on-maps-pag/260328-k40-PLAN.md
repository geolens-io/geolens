---
phase: 260328-k40
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/maps/MapCard.tsx
  - frontend/src/components/maps/MapCardGrid.tsx
  - frontend/src/components/maps/__tests__/MapCard.test.tsx
autonomous: true
requirements: [QUICK-K40]

must_haves:
  truths:
    - "Broken thumbnail (404) shows placeholder MapIcon instead of broken image"
    - "Valid thumbnail still renders correctly as an img"
    - "Both list view (MapCard) and grid view (MapCardGrid) handle the error identically"
  artifacts:
    - path: "frontend/src/components/maps/MapCard.tsx"
      provides: "List-view card with onError fallback"
      contains: "onError"
    - path: "frontend/src/components/maps/MapCardGrid.tsx"
      provides: "Grid-view card with onError fallback"
      contains: "onError"
    - path: "frontend/src/components/maps/__tests__/MapCard.test.tsx"
      provides: "Tests for thumbnail fallback behavior"
      contains: "onError"
  key_links:
    - from: "frontend/src/components/maps/MapCard.tsx"
      to: "img onError handler"
      via: "useState(imgError) toggles between img and MapIcon"
      pattern: "imgError.*setImgError"
    - from: "frontend/src/components/maps/MapCardGrid.tsx"
      to: "img onError handler"
      via: "useState(imgError) toggles between img and MapIcon"
      pattern: "imgError.*setImgError"
---

<objective>
Fix broken map thumbnails on the /maps page. When a map has a `thumbnail_url` but the actual file is missing from storage (404), the `<img>` tag shows a broken image icon. Add `onError` fallback handling to both `MapCard.tsx` and `MapCardGrid.tsx` so broken thumbnails gracefully fall back to the placeholder `MapIcon`.

Purpose: Eliminate broken image icons on the maps listing page when thumbnail files are missing from storage.
Output: Resilient thumbnail rendering in both list and grid map card views, with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/components/maps/MapCard.tsx
@frontend/src/components/maps/MapCardGrid.tsx
@frontend/src/components/search/__tests__/DatasetCard.test.tsx (test pattern reference)
@.planning/quick/260328-k40-fix-broken-map-thumbnail-404-on-maps-pag/260328-k40-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add onError thumbnail fallback to MapCard and MapCardGrid</name>
  <files>
    frontend/src/components/maps/MapCard.tsx
    frontend/src/components/maps/MapCardGrid.tsx
    frontend/src/components/maps/__tests__/MapCard.test.tsx
  </files>
  <behavior>
    - Test: MapCard renders img when thumbnail_url is present and no error
    - Test: MapCard renders MapIcon placeholder when thumbnail_url is null
    - Test: MapCard renders MapIcon placeholder when img fires onError (simulating 404)
    - Test: MapCardGrid renders img when thumbnail_url is present and no error
    - Test: MapCardGrid renders MapIcon placeholder when img fires onError
  </behavior>
  <action>
    1. In `MapCard.tsx`:
       - Add `import { useState } from 'react'` at top
       - Inside the `MapCard` component, add: `const [imgError, setImgError] = useState(false)`
       - Change the thumbnail conditional from `{map.thumbnail_url ? (` to `{map.thumbnail_url && !imgError ? (`
       - Add `onError={() => setImgError(true)}` prop to the existing `<img>` tag
       - No other changes needed — the existing `<MapIcon>` fallback in the else branch handles the rest

    2. In `MapCardGrid.tsx`:
       - Same changes as MapCard.tsx: add `useState` import, add `imgError` state, update conditional, add `onError` prop

    3. Create `frontend/src/components/maps/__tests__/MapCard.test.tsx`:
       - Use `@/test/test-utils` for render/screen (project convention)
       - Create a `makeMap()` helper returning a minimal `MapSummaryResponse` object with fields: `id`, `name`, `description`, `visibility`, `layer_count`, `created_at`, `updated_at`, `thumbnail_url`, `created_by_username`
       - Test MapCard: renders img when `thumbnail_url` is set
       - Test MapCard: renders placeholder when `thumbnail_url` is null
       - Test MapCard: renders placeholder after img onError fires — use `fireEvent.error()` on the img element, then assert MapIcon placeholder is shown and img is gone
       - Test MapCardGrid: renders placeholder after img onError fires (same approach)
       - Wrap renders in `MemoryRouter` since both components use `<Link>` from react-router
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npx vitest run src/components/maps/__tests__/MapCard.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - Both MapCard and MapCardGrid show MapIcon placeholder when thumbnail 404s
    - Both still show img when thumbnail loads successfully
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run src/components/maps/__tests__/MapCard.test.tsx` passes all tests
- Visual: On /maps page, maps with missing thumbnails show the MapIcon placeholder instead of broken image
</verification>

<success_criteria>
- No broken image icons on /maps page when thumbnails are missing
- Valid thumbnails still render correctly
- Tests cover the onError fallback path for both card variants
</success_criteria>

<output>
After completion, create `.planning/quick/260328-k40-fix-broken-map-thumbnail-404-on-maps-pag/260328-k40-SUMMARY.md`
</output>
