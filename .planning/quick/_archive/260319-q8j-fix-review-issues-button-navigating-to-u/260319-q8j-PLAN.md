---
phase: 260319-q8j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/dataset/tabs/OverviewTab.tsx
autonomous: true
requirements: [BUGFIX]

must_haves:
  truths:
    - "Clicking 'Review issues' button navigates to the Metadata tab validation card"
  artifacts:
    - path: "frontend/src/components/dataset/tabs/OverviewTab.tsx"
      provides: "Review issues button with correct validation field"
      contains: "onNavigateToValidationField?.('validation')"
  key_links:
    - from: "frontend/src/components/dataset/tabs/OverviewTab.tsx"
      to: "frontend/src/lib/dataset-validation-navigation.ts"
      via: "onNavigateToValidationField('validation') -> getValidationNavigationAction('validation') -> { tab: 'metadata', anchor: 'validation' }"
      pattern: "onNavigateToValidationField.*validation"
---

<objective>
Fix the "Review issues" button on the OverviewTab so it navigates to the Metadata tab's validation card instead of doing nothing.

Purpose: The button currently passes 'title' to onNavigateToValidationField, but 'title' has no mapping in getValidationNavigationAction, so the click is silently ignored.
Output: Working navigation from Review issues button to validation card.
</objective>

<execution_context>
@/Users/ishiland/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ishiland/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/components/dataset/tabs/OverviewTab.tsx
@frontend/src/lib/dataset-validation-navigation.ts
@frontend/src/components/dataset/tabs/MetadataTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Review issues button navigation field</name>
  <files>frontend/src/components/dataset/tabs/OverviewTab.tsx</files>
  <action>
    On line 163 of OverviewTab.tsx, change `onNavigateToValidationField?.('title')` to `onNavigateToValidationField?.('validation')`.

    The 'validation' field maps to `{ tab: 'metadata', anchor: 'validation' }` in dataset-validation-navigation.ts, which navigates to the Metadata tab and scrolls to the element with `data-field-anchor="validation"` (the validation Card in MetadataTab.tsx line 132).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && grep -n "onNavigateToValidationField.*'validation'" frontend/src/components/dataset/tabs/OverviewTab.tsx</automated>
  </verify>
  <done>Review issues button passes 'validation' field, which has a valid mapping in getValidationNavigationAction and navigates to the Metadata tab validation card.</done>
</task>

</tasks>

<verification>
grep confirms 'validation' is used instead of 'title' in the onClick handler.
</verification>

<success_criteria>
- OverviewTab.tsx line 163 uses 'validation' not 'title'
- No other references to onNavigateToValidationField('title') exist
</success_criteria>

<output>
After completion, create `.planning/quick/260319-q8j-fix-review-issues-button-navigating-to-u/260319-q8j-SUMMARY.md`
</output>
