---
phase: 260329-bn7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
  - docs/DESIGN-GUIDE.md
autonomous: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-03]

must_haves:
  truths:
    - "DESIGN-GUIDE.md has a Token Usage Hierarchy section telling developers which tokens to reach for first"
    - "The --info CSS token uses teal/cyan hue (~195) distinct from primary hue (250)"
    - "DESIGN-GUIDE.md has a Minimalism Rule one-liner in the Overview section"
    - "info-foreground tokens updated to match the new info hue in both light and dark modes"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "Updated --info and --info-foreground tokens with teal/cyan hue"
      contains: "--info: oklch"
    - path: "docs/DESIGN-GUIDE.md"
      provides: "Token Usage Hierarchy section, Minimalism Rule, updated info token values"
      min_lines: 700
  key_links:
    - from: "frontend/src/index.css"
      to: "docs/DESIGN-GUIDE.md"
      via: "Token values must match between CSS source and guide documentation"
      pattern: "--info.*oklch.*195"
---

<objective>
Update the GeoLens design guide and CSS tokens based on agreed audit findings.

Purpose: Three targeted improvements — (1) add token usage hierarchy so developers know which tokens to reach for first, (2) shift the info status color from primary-blue hue 250 to teal/cyan hue 195 so "informational" is visually distinct from "primary action", (3) add a minimalism rule to the design principles.

Output: Updated `frontend/src/index.css` (info token hue shift) and `docs/DESIGN-GUIDE.md` (two new sections + updated info token table rows).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/index.css
@docs/DESIGN-GUIDE.md
@frontend/src/lib/status-colors.ts

Note: status-colors.ts uses `sky-*` Tailwind palette classes for info badges, not the `--info` CSS custom property directly. The CSS token change does not break badge rendering — it aligns the token with the visual intent already present in badge classes.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shift --info token hue from primary-blue (250) to teal/cyan (195)</name>
  <files>frontend/src/index.css</files>
  <action>
In `frontend/src/index.css`, update 4 CSS custom property values:

In the `:root` block (light mode):
- `--info: oklch(0.55 0.18 250)` -> `--info: oklch(0.55 0.15 195)`
- `--info-foreground: oklch(0.985 0 0)` -> `--info-foreground: oklch(0.985 0 0)` (no change — white text on teal still works)

In the `.dark` block:
- `--info: oklch(0.72 0.17 250)` -> `--info: oklch(0.72 0.14 195)`
- `--info-foreground: oklch(0.20 0.05 250)` -> `--info-foreground: oklch(0.20 0.05 195)`

Rationale: hue 195 matches the viz-5 teal slot. Chroma reduced slightly (0.18->0.15 light, 0.17->0.14 dark) because OKLCH teal at high chroma clips in sRGB. These values stay in gamut.

Do NOT touch status-colors.ts — it uses Tailwind sky-* palette classes for badges, not the --info token.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && grep -c "195" frontend/src/index.css | grep -q "[2-9]" && echo "PASS: info tokens use hue 195" || echo "FAIL"</automated>
  </verify>
  <done>Both --info and --info-foreground in light and dark blocks use hue 195 instead of 250. The info status color is now visually distinct from the primary action color.</done>
</task>

<task type="auto">
  <name>Task 2: Add Token Usage Hierarchy, Minimalism Rule, and update info values in DESIGN-GUIDE.md</name>
  <files>docs/DESIGN-GUIDE.md</files>
  <action>
Three edits to docs/DESIGN-GUIDE.md:

**Edit A — Minimalism Rule (Section 1, after the "Key principles" list, before "Technical notes"):**
Add a new paragraph:

```
**Default rule:** When unsure which tokens or colors to use, stick with `background` + `card` + `primary`. Do not introduce new tokens or colors without a clear reason.
```

**Edit B — Token Usage Hierarchy (new subsection after Section 2 heading, before "### Primary Scale"):**
Insert a new subsection:

```
### Token Usage Hierarchy

When styling a component, reach for tokens in this order:

1. **Semantic tokens first** — `background`, `card`, `primary`, `foreground`, `muted-foreground`, `destructive`, `success`, `warning`, `info`. These cover 90% of use cases.
2. **Surface tokens for layering** — `surface-0` through `surface-3`. Only when you need explicit elevation stacking beyond the card/popover defaults.
3. **Primary scale tokens (rare)** — `primary-50` through `primary-900`. Only for custom tints that no semantic token provides (e.g., a subtle primary wash on a selected row).

**Hard rule:** Never use raw primary scale tokens for UI semantics. If you need "the action color", use `primary`. If you need "a subtle highlight", use `accent` or `muted`. The scale exists for edge cases, not daily use.
```

**Edit C — Update info token values in the Status Colors table (Section 2):**
Update the `--info` row:
- Light Value: `oklch(0.55 0.15 195)`
- Dark Value: `oklch(0.72 0.14 195)`
- Usage: `Running, in-progress` (unchanged)

Update the `--info-foreground` row:
- Light Value: `oklch(0.985 0 0)` (unchanged)
- Dark Value: `oklch(0.20 0.05 195)`

Keep all other rows in the Status Colors table unchanged.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && grep -q "Token Usage Hierarchy" docs/DESIGN-GUIDE.md && grep -q "Default rule" docs/DESIGN-GUIDE.md && grep -q "oklch(0.55 0.15 195)" docs/DESIGN-GUIDE.md && echo "PASS: all three edits present" || echo "FAIL"</automated>
  </verify>
  <done>DESIGN-GUIDE.md contains: (1) Minimalism Rule one-liner in Section 1, (2) Token Usage Hierarchy subsection in Section 2 before Primary Scale, (3) updated info token values matching the CSS changes from Task 1.</done>
</task>

</tasks>

<verification>
1. `grep "195" frontend/src/index.css` shows --info tokens on hue 195, not 250
2. `grep "Token Usage Hierarchy" docs/DESIGN-GUIDE.md` finds the new subsection
3. `grep "Default rule" docs/DESIGN-GUIDE.md` finds the minimalism one-liner
4. Info token values in DESIGN-GUIDE.md match values in index.css
5. No other tokens or files were modified
</verification>

<success_criteria>
- The --info CSS token is visually distinct from --primary (hue 195 vs 250)
- Developers reading the design guide see a clear token priority order
- The minimalism rule sets a conservative default for color decisions
- DESIGN-GUIDE.md and index.css are in sync for info token values
</success_criteria>

<output>
After completion, create `.planning/quick/260329-bn7-review-design-guide-md-audit-findings-an/260329-bn7-SUMMARY.md`
</output>
