---
phase: quick-260322-qg3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/REPO_STRUCTURE_RECOMMENDATION.md
autonomous: true
requirements: [QG3-01]

must_haves:
  truths:
    - "Document analyzes helm/, packer/, deploy/ directories for separation"
    - "Document includes pros/cons for each directory"
    - "Document provides a clear recommendation with rationale"
    - "Document covers monorepo vs polyrepo trade-offs at project scale"
    - "Document identifies when to revisit the decision"
  artifacts:
    - path: "docs/REPO_STRUCTURE_RECOMMENDATION.md"
      provides: "Written recommendation document"
      contains: "Recommendation"
  key_links: []
---

<objective>
Produce a recommendation document evaluating whether helm/, packer/, and deploy/ directories should move to separate repos under the geolens-io GitHub org.

Purpose: Inform future repo structure decisions with a written analysis grounded in actual coupling analysis.
Output: docs/REPO_STRUCTURE_RECOMMENDATION.md
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260322-qg3-evaluate-whether-helm-packer-ami-directo/260322-qg3-CONTEXT.md
@.planning/quick/260322-qg3-evaluate-whether-helm-packer-ami-directo/260322-qg3-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write repo structure recommendation document</name>
  <files>docs/REPO_STRUCTURE_RECOMMENDATION.md</files>
  <action>
Create docs/REPO_STRUCTURE_RECOMMENDATION.md using the research findings from 260322-qg3-RESEARCH.md. The document should be a polished, standalone recommendation that a future contributor or the project owner can reference.

Structure the document as follows:

1. **Executive Summary** -- One paragraph: keep everything in monorepo, here is why.

2. **Directory Inventory** -- Table of helm/, packer/aws/, packer/do/, packer/common/, deploy/, scripts/, docs/ with file counts, purpose, and CI integration status (all "None").

3. **CI/CD Coupling Analysis** -- Summarize that ci.yml and publish.yml have zero references to helm, packer, or deploy. Note the absence of packer validate or helm lint jobs.

4. **Per-Directory Evaluation** -- For each of helm/, packer/, deploy/:
   - Current state and dependencies
   - Pros of separation
   - Cons of separation
   - Verdict (one sentence)
   Include the packer cross-reference list (13+ files via ../../ paths). Note helm is self-contained (GHCR image refs only).

5. **Other Directories** -- Brief table covering docs/, plans/, scripts/, e2e/, .planning/ -- all "No" with one-line reasoning.

6. **Monorepo vs Polyrepo at This Scale** -- Project characteristics (single product, single developer, ~200 phases shipped), when monorepo is appropriate, when polyrepo benefits emerge.

7. **Recommendation** -- Keep everything in monorepo. List 4 specific actions to consider instead:
   - Add packer validate + helm lint CI jobs with path-based triggers
   - Create geolens-io/helm-charts when chart distribution is needed
   - Terraform is strongest future separation candidate
   - Revisit triggers: external chart consumers, separate infra CI, second contributor, Terraform addition

8. **Decision Date** and note to revisit if triggers are met.

Tone: Direct, technical, no fluff. Use tables where they improve scanability. This is a recommendation document, not a proposal -- state the recommendation clearly.
  </action>
  <verify>
    <automated>test -f docs/REPO_STRUCTURE_RECOMMENDATION.md && grep -q "Recommendation" docs/REPO_STRUCTURE_RECOMMENDATION.md && grep -q "helm" docs/REPO_STRUCTURE_RECOMMENDATION.md && grep -q "packer" docs/REPO_STRUCTURE_RECOMMENDATION.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>docs/REPO_STRUCTURE_RECOMMENDATION.md exists with complete analysis covering all directories, pros/cons, clear recommendation to keep monorepo, and revisit triggers</done>
</task>

</tasks>

<verification>
- Document exists at docs/REPO_STRUCTURE_RECOMMENDATION.md
- Contains sections for each evaluated directory (helm, packer, deploy)
- States clear recommendation (keep in monorepo)
- Includes revisit triggers
- No implementation changes made to repo structure
</verification>

<success_criteria>
A standalone recommendation document exists that any reader can use to understand why helm/, packer/, and deploy/ remain in the main repo, what would trigger reconsideration, and what incremental improvements to make instead.
</success_criteria>

<output>
After completion, create `.planning/quick/260322-qg3-evaluate-whether-helm-packer-ami-directo/260322-qg3-SUMMARY.md`
</output>
