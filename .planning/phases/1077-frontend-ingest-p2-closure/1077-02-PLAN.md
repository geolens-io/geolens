---
phase: 1077
plan: 02
type: execute
wave: 2
depends_on: [1077-01]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - ING-01
  - ING-05
tags: [frontend, verification, close-gate]
must_haves:
  - "Full frontend `npm run typecheck` exits 0"
  - "Full frontend `npm run test` exits 0 (vitest suite)"
  - "Phase 1077 VERIFICATION.md exists with `status: passed` frontmatter"
  - "STATE.md, ROADMAP.md, REQUIREMENTS.md all reflect Phase 1077 complete + ING-01, ING-05 closed"
---

# Plan 1077-02: Frontend Ingest P2 Close Gate

## Threat Model

| Threat | Mitigation |
|--------|------------|
| Helpers landed but full vitest suite regressed in unrelated areas | Full `npm run test` gate before close |
| Type drift across the codebase from new exports | Full `npm run typecheck` gate |
| Plan state inconsistent — STATE/ROADMAP/REQUIREMENTS not updated | Explicit task in this plan |

## Tasks

<task id="1">
<action>
Run full frontend regression gate:

```bash
cd frontend
npm run typecheck 2>&1 | tee /tmp/1077-02-typecheck.log
npm run test 2>&1 | tee /tmp/1077-02-vitest.log
```

Capture exit codes. If either fails, document the failure precisely (which file, which test) in VERIFICATION.md and mark `status: gaps_found`. Do NOT paper over.

If both pass, parse vitest output for total pass/fail/skip counts.
</action>
<read_first>
- frontend/package.json (typecheck + test script definitions)
- .planning/phases/1077-frontend-ingest-p2-closure/1077-01-PLAN.md (what landed)
</read_first>
<acceptance_criteria>
- `/tmp/1077-02-typecheck.log` exists with `npm run typecheck` exit code 0
- `/tmp/1077-02-vitest.log` exists; vitest reports 0 failures (skips are OK if documented)
</acceptance_criteria>
</task>

<task id="2">
<action>
Write `.planning/phases/1077-frontend-ingest-p2-closure/1077-VERIFICATION.md` with:

```yaml
---
status: passed
phase: 1077
verified: 2026-05-21
verifier: Plan 1077-02
---
```

(or `status: gaps_found` if Task 1 surfaced any.)

Body sections:
- ING-01 closure evidence (`getCogDownloadUrl` exists; `JobProgress.tsx` no longer string-concats)
- ING-05 closure evidence (`uploadChunks` helper exists; both call sites rewired; vitest covers behavior)
- Full-suite results table (typecheck pass/fail + vitest pass/fail/skip counts)
- Any deferrals (none expected for this small scope)
</action>
<read_first>
- /tmp/1077-02-typecheck.log
- /tmp/1077-02-vitest.log
- .planning/phases/1077-frontend-ingest-p2-closure/1077-01-PLAN.md
</read_first>
<acceptance_criteria>
- `.planning/phases/1077-frontend-ingest-p2-closure/1077-VERIFICATION.md` exists
- `grep -E "^status: (passed|gaps_found)" .planning/phases/1077-frontend-ingest-p2-closure/1077-VERIFICATION.md` returns one line
- `wc -l .planning/phases/1077-frontend-ingest-p2-closure/1077-VERIFICATION.md` returns ≥ 30
</acceptance_criteria>
</task>

<task id="3">
<action>
Write `.planning/phases/1077-frontend-ingest-p2-closure/1077-SUMMARY.md` with a phase-level summary mirroring `.planning/phases/1076-backend-ingest-p2-closure/1076-SUMMARY.md` shape: what shipped, files modified, test counts, references to per-plan SUMMARYs (1077-01-SUMMARY.md).

Update STATE.md:
- Phase 1077 → complete
- `progress.completed_phases` advanced to 3
- `progress.percent` recalculated
- Current Position → next is Phase 1078

Update ROADMAP.md: Phase 1077 → ✅ Complete.

Update REQUIREMENTS.md: ING-01 + ING-05 → Complete (checkboxes + Traceability table).

Commit atomically: `docs(1077): close Phase 1077 — Frontend Ingest P2 (ING-01, ING-05) verified`.
</action>
<read_first>
- .planning/phases/1076-backend-ingest-p2-closure/1076-SUMMARY.md (template shape)
- .planning/STATE.md
- .planning/ROADMAP.md
- .planning/REQUIREMENTS.md
</read_first>
<acceptance_criteria>
- `.planning/phases/1077-frontend-ingest-p2-closure/1077-SUMMARY.md` exists
- STATE.md `milestone: v1017` frontmatter; body Phase 1077 marked complete
- ROADMAP.md Phase 1077 → ✅ Complete (or equivalent checkbox state)
- REQUIREMENTS.md ING-01 + ING-05 checkboxes flipped to `[x]` and Traceability table Status → Complete
- Final commit message matches `docs(1077): close Phase 1077 — Frontend Ingest P2 (ING-01, ING-05) verified`
- Working tree clean after commit
</acceptance_criteria>
</task>

## Verification

After all 3 tasks: `git status` clean and `git log --oneline -1` shows the close commit.

## Commit Structure

- Task 1: no commit (gathering data into /tmp)
- Task 2: no commit (VERIFICATION.md is part of close commit)
- Task 3: `docs(1077): close Phase 1077 — Frontend Ingest P2 (ING-01, ING-05) verified` — single atomic commit covers SUMMARY + VERIFICATION + state files
