---
phase: 1077
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/datasets.ts
  - frontend/src/api/ingest.ts
  - frontend/src/api/_presignedUpload.ts
  - frontend/src/components/import/JobProgress.tsx
  - frontend/src/api/_presignedUpload.test.ts
autonomous: true
requirements:
  - ING-01
  - ING-05
tags: [frontend, refactor, ingest]
must_haves:
  - "`getCogDownloadUrl(id)` helper exists in `frontend/src/api/datasets.ts` and is called from `JobProgress.tsx`"
  - "`uploadChunks(urls, file, partSize)` helper exists in `frontend/src/api/_presignedUpload.ts`"
  - "Both `ingest.ts` and `datasets.ts` chunked-PUT loops are replaced with calls to `uploadChunks`"
  - "`frontend/src/api/_presignedUpload.test.ts` has at least one vitest test pinning the new helper's behavior"
  - "`npm run typecheck` exits 0"
  - "Existing vitest suites for `datasets`, `ingest`, and `JobProgress` continue to pass"
---

# Plan 1077-01: Frontend Ingest P2 Helper Extraction (ING-01 + ING-05)

## Threat Model

| Threat | Mitigation |
|--------|------------|
| Drift between two chunked-PUT loops causes silent inconsistency in future bug fixes | Extract to single helper; both sites import |
| Breaking change to existing presigned upload behavior under refactor | Test pins ETag handling; existing flow tests still pass |
| New helper has subtle API mismatch with consumers | TypeScript signature locks; typecheck gate |

## Tasks

<task id="1">
<action>
Add new helper `getCogDownloadUrl(id: string): string` in `frontend/src/api/datasets.ts` next to `getExportUrl()` (currently at ~lines 51-61). Use the same export pattern — match style exactly (named export, JSDoc one-liner). The URL pattern should match what `JobProgress.tsx:42` currently constructs.

Replace the string-concat site at `frontend/src/components/import/JobProgress.tsx:42` with `getCogDownloadUrl(datasetId)` (or whatever the local variable name is in that scope).
</action>
<read_first>
- frontend/src/api/datasets.ts (full file — locate `getExportUrl()` for pattern match)
- frontend/src/components/import/JobProgress.tsx (full file — see line 42 context)
</read_first>
<acceptance_criteria>
- `grep -nE "export (const|function) getCogDownloadUrl" frontend/src/api/datasets.ts` returns one line
- `grep -n "getCogDownloadUrl" frontend/src/components/import/JobProgress.tsx` returns at least one line
- `grep -nE "/download/cog['\"]" frontend/src/components/import/JobProgress.tsx` returns zero lines (the string-concat is gone)
- `npm run typecheck` (from `frontend/`) exits 0 (treat the diff as the entire change set)
</acceptance_criteria>
</task>

<task id="2">
<action>
Create new file `frontend/src/api/_presignedUpload.ts` exporting:

```typescript
/**
 * Upload a file to one or more presigned URLs as chunks.
 * Returns the array of ETag headers from the responses.
 */
export async function uploadChunks(
  urls: string[],
  file: File | Blob,
  partSize: number,
): Promise<string[]>
```

The implementation is the canonical chunked-PUT loop currently duplicated between `ingest.ts:147-159` and `datasets.ts:370-383`. If the two loops have diverged in a subtle way (e.g., one logs differently, one handles 4xx differently), prefer the more defensive shape and document the choice inline.

Rewire `frontend/src/api/ingest.ts:147-159` to import and call `uploadChunks(...)`.
Rewire `frontend/src/api/datasets.ts:370-383` to import and call `uploadChunks(...)`.
</action>
<read_first>
- frontend/src/api/ingest.ts (lines ~140-170)
- frontend/src/api/datasets.ts (lines ~365-390)
- frontend/src/api/client.ts (for context on shared HTTP wrappers; `uploadChunks` may use plain `fetch` since presigned URLs are pre-signed)
</read_first>
<acceptance_criteria>
- `frontend/src/api/_presignedUpload.ts` exists
- `grep -nE "export (async function|const) uploadChunks" frontend/src/api/_presignedUpload.ts` returns one line
- `grep -n 'from "./_presignedUpload"\|from "\./_presignedUpload"' frontend/src/api/ingest.ts` returns one line
- `grep -n 'from "./_presignedUpload"\|from "\./_presignedUpload"' frontend/src/api/datasets.ts` returns one line
- `grep -cE "for \(.* (of urls|i < urls.length|presignedUrl)" frontend/src/api/ingest.ts` returns 0 (no inline loop left)
- `grep -cE "for \(.* (of urls|i < urls.length|presignedUrl)" frontend/src/api/datasets.ts` returns 0
- `npm run typecheck` exits 0
</acceptance_criteria>
</task>

<task id="3">
<action>
Add `frontend/src/api/_presignedUpload.test.ts` with at least one vitest test that exercises `uploadChunks`. Use `vi.fn()` mocks of `fetch` for predictable ETags. Pin:
- A 3-chunk upload returns 3 ETags in order
- Each chunk's PUT body is the correct slice of the input File/Blob
- 4xx response from any chunk causes the helper to throw (test the error path)
</action>
<read_first>
- frontend/src/api/_presignedUpload.ts (the file you just created)
- Any existing vitest test in frontend/src/api/ (e.g., `client.test.ts` or `datasets.test.ts`) for fetch-mocking patterns
</read_first>
<acceptance_criteria>
- `frontend/src/api/_presignedUpload.test.ts` exists
- `grep -cE "describe|test\(|it\(" frontend/src/api/_presignedUpload.test.ts` returns ≥ 3 (one describe + at least 2 test cases — happy path + error path)
- `cd frontend && npm run test -- src/api/_presignedUpload.test.ts` (or vitest equivalent) exits 0
</acceptance_criteria>
</task>

## Verification

```bash
cd frontend && npm run typecheck && npm run test -- src/api/_presignedUpload.test.ts
```

Both must exit 0.

## Commit Structure

- Task 1: `refactor(1077-01): add getCogDownloadUrl helper, wire JobProgress (ING-01)`
- Task 2: `refactor(1077-01): extract uploadChunks helper, rewire ingest + datasets (ING-05)`
- Task 3: `test(1077-01): pin uploadChunks helper behavior`
- After SUMMARY: `docs(1077-01): complete frontend ingest P2 helper extraction (ING-01 + ING-05)`
