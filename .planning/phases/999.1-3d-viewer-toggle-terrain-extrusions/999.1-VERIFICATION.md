---
phase: 999.1-3d-viewer-toggle-terrain-extrusions
verified: 2026-04-12T21:10:00Z
status: human_needed
score: 19/19 must-haves verified (automated)
overrides_applied: 0
human_verification:
  - test: "Open a shared map viewer with a DEM raster layer. Confirm the MapLibre TerrainControl (mountain icon) appears in the top-right control stack, below the fullscreen control."
    expected: "TerrainControl button visible only when DEM layer present; absent when no DEM layer present (D-07)."
    why_human: "UI rendering and control stack position cannot be verified without a live browser."
  - test: "Click TerrainControl to enable 3D terrain. Verify map pitches to ~45 degrees with smooth animation and terrain elevation is visible."
    expected: "Pitch animates to 45 degrees, terrain elevation renders from DEM raster tiles via terrainrgb-encoded raster-proxy URL."
    why_human: "WebGL rendering, animation quality, and terrain visual output require live browser inspection."
  - test: "With polygon layers that have a height column configured (paint._height_column set), enable 3D mode and verify extruded buildings appear at zoom >= 14."
    expected: "Fill-extrusion companion layer visible as 3D buildings at zoom 14+."
    why_human: "Extrusion rendering is a visual check requiring a live map with configured polygon layers."
  - test: "While in 3D mode, switch the basemap. Terrain should persist after the basemap change."
    expected: "style.load re-seeds the terrain source and re-enables terrain; TerrainControl remains active."
    why_human: "Real-time state survival across basemap swaps requires live browser interaction."
  - test: "Reload the page and verify 3D resets to off (session-only per D-09)."
    expected: "Map starts in flat 2D mode; no terrain active on fresh load even if DEM layer present."
    why_human: "Session state reset requires browser page reload."
  - test: "Using an embed token for a map with a DEM layer, open in incognito window. Enable terrain. In DevTools Network tab, inspect a terrain tile request (/tiles/raster-proxy/) and confirm X-Embed-Token header is present (D-11)."
    expected: "X-Embed-Token header on terrain tile requests; terrain renders successfully (200 status)."
    why_human: "Network header inspection requires DevTools and a live embed context."
---

# Phase 999.1: 3D Viewer Toggle — Terrain & Extrusions Verification Report

**Phase Goal:** Add 3D terrain toggle and fill-extrusion building heights to the map viewer
**Verified:** 2026-04-12T21:10:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Single-band float rasters are auto-flagged as is_dem=True during ingest | VERIFIED | `cog.py:84` — `is_dem_candidate = src.count == 1 and _is_float_dtype(src.dtypes[0])`; `tasks.py:1612` — `is_dem=meta.get("is_dem_candidate", False)` |
| 2 | Dataset owner or admin can toggle is_dem via PATCH /datasets/{id} | VERIFIED | `datasets/schemas.py:356` — `is_dem: bool \| None`; `datasets/service.py:640-648` — updates `RasterAsset.is_dem`; router uses `require_permission("edit_metadata")` |
| 3 | Raster tile proxy appends algorithm=terrainrgb for is_dem datasets | VERIFIED | `tiles/router.py:273-278` — conditionally prepends `algorithm=terrainrgb` to `render_params` when `row.get("is_dem")` is true |
| 4 | SharedLayerResponse includes is_dem field for raster layers | VERIFIED | `maps/schemas.py:186` — `is_dem: bool = False`; `maps/service.py:806,828,859` — outerjoin, destructure, serialize `bool(ds_is_dem)` |
| 5 | fill-adapter adds a fill-extrusion companion layer when _height_column is present in paint | VERIFIED | `fill-adapter.ts:60-79` — conditional extrusion layer with `type: 'fill-extrusion'` |
| 6 | fill-extrusion companion layer uses coalesce/to-number expression for safe null handling | VERIFIED | `fill-adapter.ts:71` — `['coalesce', ['to-number', ['get', heightColumn], 0], 0]` |
| 7 | fill-extrusion layer has minzoom 14 for performance | VERIFIED | `fill-adapter.ts:69` — `minzoom: 14` |
| 8 | _height_column is stripped from MapLibre paint properties via CUSTOM_PAINT_PROPS | VERIFIED | `shared.ts:11` — `'_height_column'` in CUSTOM_PAINT_PROPS; `shared.ts:64` — `stripCustomProps` filters it |
| 9 | getLayerIds returns the extrusion layer ID alongside fill and outline IDs | VERIFIED | `fill-adapter.ts:166-168` — returns `[layerId, \`${layerId}-outline\`, \`${layerId}-extrusion\`]` |
| 10 | syncVisibility toggles extrusion layer visibility | VERIFIED | `fill-adapter.ts:153-162` — `map.setLayoutProperty(extrusionId, 'visibility', vis)` |
| 11 | MapLibre TerrainControl is visible in the top-right control stack when a DEM layer is present | VERIFIED (automated) | `ViewerMap.tsx:609-611` — `{terrainReady && <TerrainControl source="terrain-dem" exaggeration={1.5} position="top-right" />}` |
| 12 | DEM raster layers create a raster-dem terrain source pre-seeded on map load | VERIFIED | `ViewerMap.tsx:280-290` — `useEffect` seeds `raster-dem` source when `mapReady && demTileUrl` |
| 13 | Terrain source survives basemap changes via style.load re-sync | VERIFIED | `ViewerMap.tsx:552-569` — `onStyleLoad` re-seeds terrain source and restores terrain if `terrainActiveRef.current` |
| 14 | When terrain is enabled, map pitch animates to 45 degrees; when disabled, pitch resets to 0 and bearing to 0 | VERIFIED | `ViewerMap.tsx:293-308` — `terrain` event listener calls `easeTo({pitch: 45})` on enable and `easeTo({pitch: 0, bearing: 0})` on disable |
| 15 | 3D mode without DEM shows flat map with pitch/rotate (graceful no-op per D-07) | VERIFIED | `ViewerMap.tsx:158` — `demLayer` only found when `l.is_dem && l.dataset_record_type?.startsWith('raster')`; `terrainReady` stays false if no DEM → no TerrainControl rendered |
| 16 | Height column dropdown appears in polygon layer style editor | VERIFIED | `LayerStyleEditor.tsx:225-253` — `{isPolygon && numericColumns.length > 0 && (<div>Height column Select...</div>)}` |
| 17 | Dropdown lists only numeric columns from dataset_column_info | VERIFIED | `LayerStyleEditor.tsx:84-88` — `numericColumns = dataset_column_info.filter(col => NUMERIC_COLUMN_TYPES.has(col.type))` with 11 PostgreSQL numeric types |
| 18 | Selecting a column sets paint._height_column via onPaintChange; selecting None clears it | VERIFIED | `LayerStyleEditor.tsx:232-236` — `__none__` sentinel deletes key; otherwise sets `newPaint['_height_column'] = val`; `onPaintChange` called |
| 19 | Warning appears when bound height column is removed (absent from column_info) | VERIFIED | `LayerStyleEditor.tsx:254-259` — `AlertTriangle` warning when `currentHeightCol` set and column not found in `dataset_column_info` |

**Score:** 19/19 truths verified (automated checks)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/alembic/versions/2026_04_12_1400-add_is_dem_to_raster_assets.py` | Alembic migration adding is_dem column | VERIFIED | Contains `is_dem` in upgrade and downgrade; chains from revision `989ae68d7859` |
| `backend/app/raster/models.py` | RasterAsset.is_dem mapped column | VERIFIED | `is_dem: Mapped[bool] = mapped_column(nullable=False, server_default="false")` at line 71 |
| `backend/app/raster/cog.py` | is_dem_candidate heuristic | VERIFIED | `is_dem_candidate` at lines 84 and 124; `_is_float_dtype` at line 13 |
| `backend/app/tiles/router.py` | terrainrgb algorithm injection | VERIFIED | `algorithm=terrainrgb` at lines 274-277; `ra.is_dem` in SELECT at line 144 |
| `backend/app/maps/service.py` | is_dem on SharedLayerResponse | VERIFIED | `outerjoin(RasterAsset)` at line 810; serialized at line 859 |
| `frontend/src/components/builder/layer-adapters/fill-adapter.ts` | fill-extrusion companion layer logic | VERIFIED | `fill-extrusion` layer type, `extrusionId`, `minzoom: 14`, `coalesce`, `triggerRepaint` all present |
| `frontend/src/components/builder/layer-adapters/shared.ts` | _height_column in CUSTOM_PAINT_PROPS | VERIFIED | `'_height_column'` at line 11 of CUSTOM_PAINT_PROPS Set |
| `frontend/src/components/builder/__tests__/layer-adapters.test.ts` | fill-extrusion test cases | VERIFIED | 7 fill-extrusion test cases present; all 947 tests pass across 108 test files |
| `frontend/src/types/api.ts` | is_dem on SharedLayerResponse type | VERIFIED | `is_dem?: boolean` at line 837; `tile_url?: string` at line 838 |
| `frontend/src/components/viewer/ViewerMap.tsx` | TerrainControl, terrain source management, style.load re-sync | VERIFIED | `TerrainControl` imported and rendered, `terrain-dem` source, `raster-dem` type, pitch animation, style.load handler all present |
| `frontend/src/components/builder/LayerStyleEditor.tsx` | Height column dropdown and re-upload warning | VERIFIED | `_height_column`, `NUMERIC_COLUMN_TYPES`, `Height column` label, `__none__`, `AlertTriangle`, `was removed during re-upload` all present |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `backend/app/raster/cog.py` | `backend/app/ingest/tasks.py` | `is_dem_candidate` in extract_raster_metadata return dict | WIRED | `cog.py:124` returns `"is_dem_candidate": is_dem_candidate`; `tasks.py:1612` consumes `meta.get("is_dem_candidate", False)` |
| `backend/app/tiles/router.py` | titiler | `algorithm=terrainrgb` query param | WIRED | `tiles/router.py:274-277` conditionally builds param; `router.py:319-320` appends to titiler URL via `X-GeoLens-Render-Params` header |
| `frontend/src/components/builder/layer-adapters/fill-adapter.ts` | `shared.ts` | `stripCustomProps` removes `_height_column` before MapLibre paint | WIRED | `shared.ts:11` registers `_height_column` in CUSTOM_PAINT_PROPS; `fill-adapter.ts:12` reads `rawPaint['_height_column']` BEFORE `stripCustomProps` is called |
| `frontend/src/components/viewer/ViewerMap.tsx` | `SharedLayerResponse.is_dem` | finds DEM layer in layers prop | WIRED | `ViewerMap.tsx:158` — `layers.find((l) => l.is_dem && l.dataset_record_type?.startsWith('raster'))` |
| `frontend/src/components/viewer/ViewerMap.tsx` | style.load handler | re-applies terrain source after basemap change | WIRED | `ViewerMap.tsx:552-569` — `onStyleLoad` re-seeds and re-enables terrain |
| `frontend/src/components/builder/LayerStyleEditor.tsx` | `fill-adapter.ts` | `paint._height_column` stored in layer paint config | WIRED | `LayerStyleEditor.tsx:233-236` sets `newPaint['_height_column']`; `onPaintChange` persists; `fill-adapter.ts:12` reads it on addLayers |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `ViewerMap.tsx` | `demLayer` / `demTileUrl` | `SharedLayerResponse.is_dem` + `tile_url` from API | Yes — backend populates via RasterAsset.is_dem DB field and raster-proxy tile URL construction | FLOWING |
| `fill-adapter.ts` | `heightColumn` | `rawPaint['_height_column']` from layer paint config set via LayerStyleEditor | Yes — user selection persisted in map layer paint JSON | FLOWING |
| `maps/service.py` `get_shared_map` | `ds_is_dem` | SQLAlchemy `outerjoin(RasterAsset)` result | Yes — reads from `catalog.raster_assets.is_dem` via ORM | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| fill-extrusion test suite passes | `npx vitest run -- layer-adapters.test.ts` | 947 tests passed, 108 files | PASS |
| TypeScript compiles clean | `npx tsc --noEmit` | No output (no errors) | PASS |
| RasterAsset has is_dem field | `grep "is_dem" backend/app/raster/models.py` | `is_dem: Mapped[bool]` at line 71 | PASS |
| terrainrgb injection in tiles router | `grep "terrainrgb" backend/app/tiles/router.py` | conditional prepend at lines 274-277 | PASS |
| Commits exist | `git log --oneline` | `68e9695f`, `aed9c984`, `6aead7ea`, `64d251ab` all found | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| 3D-BACKEND-01 | Plan 01 | is_dem column + migration | SATISFIED | Migration + RasterAsset model field verified |
| 3D-BACKEND-02 | Plan 01 | DEM auto-detection heuristic | SATISFIED | `is_dem_candidate` in cog.py + ingest wiring |
| 3D-BACKEND-03 | Plan 01 | terrainrgb tile proxy injection | SATISFIED | tiles/router.py conditional terrainrgb param |
| 3D-BACKEND-04 | Plan 01 | PATCH is_dem + SharedLayerResponse | SATISFIED | datasets/service.py update + maps/schemas.py field |
| 3D-EXTRUSION-01 | Plan 02 | fill-extrusion companion layer | SATISFIED | fill-adapter.ts conditional extrusion layer |
| 3D-EXTRUSION-02 | Plan 02 | _height_column CUSTOM_PAINT_PROPS | SATISFIED | shared.ts CUSTOM_PAINT_PROPS entry |
| 3D-VIEWER-01 | Plan 03 | TerrainControl rendered conditionally | SATISFIED (automated) | ViewerMap.tsx `{terrainReady && <TerrainControl>}` |
| 3D-VIEWER-02 | Plan 03 | raster-dem terrain source pre-seeded | SATISFIED | ViewerMap.tsx useEffect seedTerrainSource |
| 3D-VIEWER-03 | Plan 03 | Pitch animation via terrain event | SATISFIED | ViewerMap.tsx terrain event listener + easeTo |
| 3D-VIEWER-04 | Plan 03 | style.load terrain re-sync | SATISFIED | ViewerMap.tsx onStyleLoad handler |
| 3D-BUILDER-01 | Plan 04 | Height column dropdown in LayerStyleEditor | SATISFIED | LayerStyleEditor.tsx height column Select |
| 3D-BUILDER-02 | Plan 04 | Re-upload warning | SATISFIED | LayerStyleEditor.tsx AlertTriangle warning |

### Anti-Patterns Found

No blockers or warnings found. No TODO/FIXME/placeholder comments in modified files. No empty implementations. TypeScript compiles clean with zero errors.

### Human Verification Required

The following items require a live browser session. They are blocking gates in Plan 03 (`task type="checkpoint:human-verify" gate="blocking"`).

---

**1. 3D Terrain Toggle Visual Verification (Plan 03 Task 2)**

**Test:** Start docker compose, open a shared map viewer with a DEM raster layer.
- Confirm TerrainControl (mountain icon) appears in top-right control stack when DEM present; absent when no DEM (D-07).
- Click TerrainControl — map should pitch to ~45 degrees with smooth animation and terrain elevation visible from DEM tiles.
- If polygon layers have height column configured, verify extruded buildings appear at zoom >= 14.
- Click TerrainControl again — map animates back to flat (pitch 0, bearing 0).
- Switch basemap in 3D mode — terrain should persist after basemap change.
- Reload page — 3D resets to off (session-only per D-09).
- Confirm TerrainControl uses MapLibre's built-in mountain icon styling, NOT a custom "3D" text button.

**Expected:** All behaviors match spec. TerrainControl is MapLibre native (D-01: zero custom UI).
**Why human:** WebGL rendering, animation quality, control stack visual position, and terrain elevation require live browser.

---

**2. Embed-Token Terrain Tile Authentication (Plan 03 Task 3 — D-11)**

**Test:** Generate an embed token for a map with a DEM raster layer. Open embed URL in incognito window. Enable terrain via TerrainControl. In DevTools Network tab, filter for `/tiles/raster-proxy/` requests and inspect request headers.

**Expected:** `X-Embed-Token` header present on terrain tile requests. Terrain renders with 200 status.
**Why human:** Network header inspection requires DevTools in a live embed context with a real embed token.

---

### Gaps Summary

No automated gaps found. All 19 must-haves are verified. The phase is functionally complete in code — the two items above are human visual/network confirmation of the primary user-facing behavior and an auth compliance check (D-11).

---

_Verified: 2026-04-12T21:10:00Z_
_Verifier: Claude (gsd-verifier)_
