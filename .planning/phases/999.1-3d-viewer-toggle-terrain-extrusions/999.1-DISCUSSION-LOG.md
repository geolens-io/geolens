# Phase 999.1: 3D Viewer Toggle (Terrain + Extrusions) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-12
**Phase:** 999.1-3d-viewer-toggle-terrain-extrusions
**Areas discussed:** 3D toggle UX, Height column binding, DEM identification flow, Empty/error states

**User context:** "KISS over complexity is important. We can always enhance later. For initial use case, I want to follow OGC specs."

---

## 3D Toggle UX

### Toggle Placement

| Option | Description | Selected |
|--------|-------------|----------|
| MapLibre TerrainControl | Built-in component — mountain icon, zero custom UI | ✓ |
| Custom toolbar button | Custom '3D' button in existing viewer toolbar | |

**User's choice:** MapLibre TerrainControl (Recommended)
**Notes:** Matches KISS principle — zero custom UI needed.

### No DEM Behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Still enable 3D pitch | Enable pitch/rotate + extrusions, terrain stays flat | ✓ |
| Show info toast | Enable 3D pitch + toast about missing terrain data | |

**User's choice:** Still enable 3D pitch (Recommended)
**Notes:** 3D mode = camera controls + extrusions, not terrain-required.

---

## Height Column Binding

### Configuration Method

| Option | Description | Selected |
|--------|-------------|----------|
| Style editor dropdown only | Lists numeric columns, explicit user choice | ✓ |
| Auto-detect + override | Auto-scan column names, pre-select if found | |

**User's choice:** Style editor dropdown only (Recommended)
**Notes:** No auto-detection magic. Explicit, no false positives.

### Viewer Flow

| Option | Description | Selected |
|--------|-------------|----------|
| Builder sets it, Viewer renders it | Configure in Builder, render in Viewer | ✓ |
| API-only for now | Height column via API only, no UI | |

**User's choice:** Builder sets it, Viewer renders it (Recommended)
**Notes:** Minimal scope — clean Builder/Viewer separation.

---

## DEM Identification Flow

### is_dem Flag Setting

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-flag on ingest + admin override | Soft heuristic flags single-band float, admin overrides | ✓ |
| Manual-only in dataset editor | No auto-detection, admin explicitly marks | |

**User's choice:** Auto-flag on ingest + admin override (Recommended)
**Notes:** Follows design doc recommendation.

### Permissions

| Option | Description | Selected |
|--------|-------------|----------|
| Dataset owner + admin | Matches existing dataset edit permissions | ✓ |
| Admin only | Tighter control but more friction | |

**User's choice:** Dataset owner + admin (Recommended)
**Notes:** Consistent with existing permission model.

---

## Empty/Error States

### No 3D Data Present

| Option | Description | Selected |
|--------|-------------|----------|
| Graceful no-op | Pitch/rotate on flat map, no toast or warning | ✓ |
| Brief info toast | One-time toast about DEM/height column | |

**User's choice:** Graceful no-op (Recommended)
**Notes:** Zero edge-case UI. KISS.

### WebGL Failures

| Option | Description | Selected |
|--------|-------------|----------|
| Existing MapErrorBoundary | Same retry fallback as any map failure | ✓ |
| 3D-specific fallback | Catch 3D errors, fall back to 2D with message | |

**User's choice:** Existing MapErrorBoundary (Recommended)
**Notes:** No new error handling needed — reuse Phase 224 work.

---

## Claude's Discretion

- Pitch transition animation (instant vs smooth)
- `minzoom` threshold for `fill-extrusion` layers
- Terrain source re-application in `style.load` hook

## Deferred Ideas

- BuilderMap.tsx full 3D mode
- User-adjustable terrain exaggeration slider
- Persisted 3D toggle state
- 3D Tiles / deck.gl / CesiumJS
- Point cloud rendering
- GeoJSON-Z streaming endpoint
- PostGIS 3D geometry detection & metadata
