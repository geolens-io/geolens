# Phase 999.4: Shared Vector Staging Pipeline - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-12
**Phase:** 999.4-shared-vector-staging-pipeline
**Areas discussed:** Extraction boundary, Feature parity, Test strategy, Error/cleanup contract

---

## Extraction Boundary

| Option | Description | Selected |
|--------|-------------|----------|
| Through post-process only | Helper ends after ensure_geom/clip/add_4326/grant, callers do metadata extraction | |
| Through metadata extraction | Helper includes metadata, sample_values, 3D detection, promote_z | ✓ |
| Through dataset creation/swap | Helper includes the divergent downstream steps with a strategy pattern | |

**User's choice:** Claude's discretion — selected "Through metadata extraction" as it maximizes deduplication while keeping the genuinely divergent steps (create dataset vs atomic swap) with callers.
**Notes:** Table name passed in by caller. CRS detection (ogrinfo) runs before the helper since ingest needs layer_name parameter.

---

## Feature Parity

| Option | Description | Selected |
|--------|-------------|----------|
| Keep asymmetry | User geometry override stays ingest-only, helper accepts optional result | ✓ |
| Add to reupload | Reupload gains geometry override support via shared helper | |

**User's choice:** Claude's discretion — selected "Keep asymmetry". Geometry override is driven by commit-step UI metadata which doesn't apply to reupload.

---

## Test Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Mock-only unit tests | Test helper orchestration logic with mocks | |
| Unit + integration | Mock-based unit tests plus 1-2 real ogr2ogr integration tests | ✓ |
| Full integration suite | Comprehensive integration tests for all code paths | |

**User's choice:** Claude's discretion — selected "Unit + integration" as the balanced approach. K2-PRE fixture already available for integration tests.

---

## Error/Cleanup Contract

| Option | Description | Selected |
|--------|-------------|----------|
| Helper handles cleanup | Helper accepts cleanup callback, manages error recovery | |
| Callers handle cleanup | Helper raises on failure, callers own cleanup and transactions | ✓ |

**User's choice:** Claude's discretion — selected "Callers handle cleanup". Keeps the helper pure and error semantics explicit. Each caller has different cleanup needs (file retention vs staging table drop).

---

## Claude's Discretion

- Helper function signature design (dataclass vs keyword args)
- Return value shape (dataclass vs tuple vs dict)
- Internal decomposition of the helper
- Import organization

## Deferred Ideas

None — discussion stayed within phase scope.
