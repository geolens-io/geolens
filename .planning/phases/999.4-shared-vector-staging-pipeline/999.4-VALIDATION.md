---
phase: 999.4
slug: shared-vector-staging-pipeline
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-27
---

# Phase 999.4 — Validation Strategy

> Per-phase validation contract reconstructed retroactively (State B). All requirements have automated verification.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 9.0.3 (with anyio + asyncio plugins) |
| **Config file** | `backend/pyproject.toml` |
| **Quick run command** | `docker compose exec -w /app api uv run pytest tests/test_staging_pipeline.py -v` |
| **Full suite command** | `docker compose exec -w /app api uv run pytest tests/test_staging_pipeline.py tests/test_staging_pipeline_integration.py tests/test_ingest.py tests/test_reupload.py` |
| **Estimated runtime** | ~20 seconds (1.5s unit + 2.3s integration + 15.6s regression) |

**Container note:** Backend service is named `api` (not `backend`); `uv run pytest` from `/app` working directory invokes the venv-installed pytest.

---

## Sampling Rate

- **After every task commit:** Run quick command (1.5s feedback)
- **After every plan wave:** Run full suite command (~20s)
- **Before `/gsd-verify-work`:** Full suite must be green ✓ (verified 2026-04-27)
- **Max feedback latency:** 20s

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 999.4-01-01 | 01 | 1 | `StagingResult` dataclass exists | — | N/A | static | `grep -n "class StagingResult" backend/app/processing/ingest/tasks_common.py` (line 57) | ✅ | ✅ green |
| 999.4-01-02 | 01 | 1 | `_ingest_vector_into_staging` helper exists (async) | — | N/A | static | `grep -n "async def _ingest_vector_into_staging" backend/app/processing/ingest/tasks_common.py` (line 465) | ✅ | ✅ green |
| 999.4-01-03 | 01 | 1 | Helper does NOT commit session in its body (D-10) | T-999.4-02 | Caller controls commit boundary | unit | `pytest tests/test_staging_pipeline.py::TestIngestVectorIntoStaging::test_session_commit_never_called` | ✅ | ✅ green |
| 999.4-01-04 | 01 | 1 | Helper raises on ogr2ogr failure (D-09) | T-999.4-03 | Failures surface, no silent swallow | unit | `pytest tests/test_staging_pipeline.py::TestIngestVectorIntoStagingErrors -v` (4 tests) | ✅ | ✅ green |
| 999.4-01-05 | 01 | 1 | Helper orchestration: ogr2ogr → reserved-col rename → DBF truncation → geom postprocess → metadata extract → 3D promote | T-999.4-01 | Caller-generated table names quoted via `_qtable()` | unit | `pytest tests/test_staging_pipeline.py::TestIngestVectorIntoStaging` (10 tests) | ✅ | ✅ green |
| 999.4-01-06 | 01 | 1 | No regressions in `ingest_file` / `reupload_file` callers (D-08) | — | N/A | regression | `pytest tests/test_ingest.py tests/test_reupload.py -x` (58 tests) | ✅ | ✅ green |
| 999.4-02-01 | 02 | 1 | Spatial GeoJSON ingest path through helper with real ogr2ogr | T-999.4-04 | Test DB ephemeral; cleanup in finally | integration | `pytest tests/test_staging_pipeline_integration.py::TestStagingPipelineIntegration::test_ingest_path_spatial_geojson` | ✅ | ✅ green |
| 999.4-02-02 | 02 | 1 | Reupload-style staging table naming pattern works | T-999.4-04 | Test DB ephemeral; cleanup in finally | integration | `pytest tests/test_staging_pipeline_integration.py::TestStagingPipelineIntegration::test_reupload_path_staging_table` | ✅ | ✅ green |
| 999.4-02-03 | 02 | 1 | Non-spatial CSV path produces correct StagingResult | T-999.4-04 | Test DB ephemeral; cleanup in finally | integration | `pytest tests/test_staging_pipeline_integration.py::TestStagingPipelineIntegration::test_nonspatial_csv_path` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Last full run (2026-04-27):** 14 + 3 + 58 = 75 tests passed, 0 failed, 32 pre-existing warnings (Pydantic V2 / asyncio.iscoroutinefunction deprecations, unrelated to this phase).

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. The phase added two new test files; no framework changes were required.

---

## Manual-Only Verifications

All phase behaviors have automated verification.

---

## Notes — Post-Phase Code Evolution

The phase shipped with `ingest_file` and `reupload_file` rewired through `_ingest_vector_into_staging`. Subsequent refactors (post-999.4) introduced an additional layer:

- `_run_staging_pipeline` (`tasks_common.py:365`) — now used directly by `tasks_reupload.py:163`
- `_ingest_vector_into_staging` (`tasks_common.py:465`) — still exists, calls `_run_staging_pipeline` internally; retained primarily as the test surface for orchestration logic
- `tasks_vector.py::ingest_file` — calls `run_ogr2ogr` + `_finalize_ingest` directly (no longer routes through the shared helper)

This evolution does **not** invalidate phase 999.4's Nyquist compliance: the helper still exists, all 75 tests still pass, and the `D-08` regression suite remains green. It is recorded here as informational context, not as a validation gap. Future phases that re-refactor the staging pipeline should carry forward this test coverage.

---

## Validation Sign-Off

- [x] All tasks have automated verify (no Wave 0 dependencies)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references — N/A (no missing)
- [x] No watch-mode flags
- [x] Feedback latency < 20s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-04-27 (retroactive State B reconstruction)

---

## Validation Audit 2026-04-27

| Metric | Count |
|--------|-------|
| Requirements analyzed | 9 |
| Covered (green) | 9 |
| Partial | 0 |
| Missing | 0 |
| Resolved this run | 0 (no gaps to fill) |
| Escalated to manual-only | 0 |
