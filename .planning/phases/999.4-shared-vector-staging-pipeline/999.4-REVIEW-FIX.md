---
phase: 999.4-shared-vector-staging-pipeline
fixed_at: 2026-04-13T00:00:00Z
review_path: .planning/phases/999.4-shared-vector-staging-pipeline/999.4-REVIEW.md
iteration: 1
findings_in_scope: 4
fixed: 4
skipped: 0
status: all_fixed
---

# Phase 999.4: Code Review Fix Report

**Fixed at:** 2026-04-13T00:00:00Z
**Source review:** .planning/phases/999.4-shared-vector-staging-pipeline/999.4-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 4
- Fixed: 4
- Skipped: 0

## Fixed Issues

### CR-01: SQL injection via unquoted table name interpolation in `_detect_and_override_geometry`

**Files modified:** `backend/app/ingest/tasks.py`
**Commit:** e69f3a6d
**Applied fix:** Replaced raw `data.{table_name}` f-string interpolation with `{_qtable(table_name)}` which properly quotes the identifier via the existing `_qtable` helper, matching the convention used throughout `metadata.py`.

### WR-01: Staging table name truncation produces wrong name when `dataset.table_name` is exactly 55 characters

**Files modified:** `backend/app/ingest/tasks.py`
**Commit:** d4e89f71
**Applied fix:** Replaced magic number `54` with named constants `_STAGING_SUFFIX = "_staging"` and `_STAGING_BASE_MAX = 63 - len(_STAGING_SUFFIX)` (evaluates to 55) at module level. Updated both call sites (ingest_file and reupload_service) to use the constants. This also addresses IN-02 (magic number).

### WR-02: Integration tests missing `@pytest.mark.asyncio` — tests silently pass without running

**Files modified:** `backend/tests/test_staging_pipeline_integration.py`
**Commit:** b8e0ee82
**Applied fix:** Added `@pytest.mark.asyncio` class-level decorator to `TestStagingPipelineIntegration` so pytest-asyncio properly drives all async test methods.

### WR-03: `user_wants_geom=True` with `user_metadata=None` raises `AttributeError` inside `_detect_and_override_geometry`

**Files modified:** `backend/app/ingest/tasks.py`
**Commit:** c11c5d01
**Applied fix:** Added early guard at top of `_ingest_vector_into_staging` that raises `ValueError` with a clear message when `user_wants_geom=True` but `user_metadata` is falsy, preventing silent fallback to empty dict.

## Skipped Issues

None.

---

_Fixed: 2026-04-13T00:00:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
