---
phase: 1015-duplicate-rendering-and-renderas-hardening
plan: 01
wave: 1
depends_on: []
autonomous: true
requirements: [DUP-01, DUP-02, DUP-03, DUP-04, DUP-05]
subsystem: frontend-builder
tags: [map-builder, duplicate-rendering, renderAs, playwright, tests]
files_modified:
  - e2e/builder.spec.ts
  - .planning/phases/1015-duplicate-rendering-and-renderas-hardening/1015-01-SUMMARY.md
  - .planning/phases/1015-duplicate-rendering-and-renderas-hardening/1015-VERIFICATION.md
---

<objective>
Harden duplicate-rendering browser coverage while preserving the existing v1 renderAs field discipline and unsupported-renderer constraints.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1015-duplicate-rendering-and-renderas-hardening/1015-CONTEXT.md
@frontend/src/components/builder/__tests__/renderAs.test.ts
@frontend/src/components/builder/__tests__/MapStackPanel.test.tsx
@frontend/src/components/builder/__tests__/DatasetSearchPanel.test.tsx
@frontend/src/components/builder/hooks/__tests__/use-builder-layers.test.ts
@e2e/builder.spec.ts
</context>

<tasks>
  <task type="auto">
    <name>Add browser coverage for duplicate rendering entry points</name>
    <files>e2e/builder.spec.ts</files>
    <action>Add a builder E2E test that duplicates a layer from row overflow, then duplicates again from the Add Dataset modal's `another rendering` action.</action>
    <verify>Assert backend layer counts and dataset IDs after each UI action, and assert the dataset-rendering header updates to 2 and 3 renderings.</verify>
    <done>DUP-01, DUP-02, and DUP-03 have browser-level proof.</done>
  </task>

  <task type="auto">
    <name>Run duplicate/renderAs focused gates</name>
    <files>test outputs only</files>
    <action>Run focused renderAs, stack, modal, hook, and builder smoke tests.</action>
    <verify>Record exact command results in summary and verification artifacts.</verify>
    <done>DUP-04 and DUP-05 retain current test evidence, and the new browser flow passes.</done>
  </task>

  <task type="auto">
    <name>Close phase state</name>
    <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, phase summary and verification artifacts</files>
    <action>Mark DUP-01..05 complete and Phase 1015 complete after verification.</action>
    <verify>Roadmap, requirements, and state agree on Phase 1015 completion and next phase 1016.</verify>
    <done>Phase 1015 is ready to continue into Phase 1016.</done>
  </task>
</tasks>

<verification>
- `cd frontend && npm run test -- DatasetSearchPanel MapStackPanel map-stack renderAs use-builder-layers --run`
- `npm run e2e:smoke:builder`
</verification>

<must_haves>
- No schema, migration, renderer, or persisted-shape changes.
- No new unsupported renderer options in the UI.
- No writes to `is_3d` from renderAs mutation paths.
</must_haves>

<success_criteria>
- [ ] Row overflow duplicate rendering creates a sibling layer with shared dataset identity and independent style fields.
- [ ] Add Dataset `another rendering` creates the same sibling-layer result.
- [ ] Dataset-rendering headers show accurate counts and support independent row actions.
- [ ] Tests prove renderAs writes only existing writable fields and never patches `is_3d`.
- [ ] Unsupported v1-punted renderers remain absent from the UI.
</success_criteria>
