---
phase: 1100
plan: 01
plan_id: 1100-01
name: CI Live-Verify + Close Gate
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md
  - CHANGELOG.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/MILESTONES.md
  - .planning/phases/1100-ci-live-verify-close-gate/1100-01-SUMMARY.md
autonomous: true
requirements: [CI-01, CLOSE-01]
requirements_addressed: [CI-01, CLOSE-01]
tags: [hygiene, close-gate, ci-live-verify, degraded-close, milestone-close]
must_haves:
  truths:
    - "Sequential pytest baseline shows `3062 passed / 0 failed / 38 skipped` LITERAL — preserves Phase 1098 SHA `b9be9027` zero (D-04a / D-05a HARD INVARIANT)"
    - "`-n 4` pytest baseline shows `3062 passed / 0 failed / 38 skipped` LITERAL — preserves Phase 1099 SHA `1314ba5f` zero (D-04b / D-05b HARD INVARIANT; single run sufficient per D-04b — Phase 1099 already proved 3-run determinism)"
    - "`-n auto` 3-run shows ≤30 distinct (failed+errors) per run with 0 ICN frames (D-04c PARA-01 envelope preserved)"
    - "`docker compose ps` shows 5/5 services healthy (api / db / frontend / titiler / worker) per D-04d"
    - "`curl http://localhost:8080/api/health` returns HTTP 200 — NO trailing slash per v1022 Phase 1097-01 [Rule 3] / MEMORY.md (D-04e)"
    - "CLOSE-GATE.md exists at `.planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md` embedding all T1 baselines verbatim + CI-01 degraded-close rationale citing v1022 precedent (D-01a/b/c/d)"
    - "CHANGELOG.md `[1.5.8]` block exists with per-requirement evidence (CI-01 degraded + OOS-01/02/03 + OAUTH-01/02/03 + CLOSE-01) with test pin names + line numbers + closure SHAs (D-02a)"
    - "Atomic close commit touches EXACTLY 5 paths: REQUIREMENTS.md + ROADMAP.md + 1100-01-SUMMARY.md + 1100-CLOSE-GATE.md + CHANGELOG.md (D-05c v1019 TD-13 atomic-flip rule)"
    - "REQUIREMENTS.md Traceability table shows CI-01 `Complete (degraded)` + CLOSE-01 `Complete` with `[x]` checkbox flips (D-05c)"
    - "ROADMAP.md Phase 1100 checkbox `[x]` flipped + plan 1100-01 checkbox `[x]` flipped + Progress table row `1/1 / Shipped / 2026-05-24`"
    - "Annotated tag `v1023` (local) exists at the T4 close-gate commit SHA with verify-gate baselines in annotation message (D-02b / D-05d)"
    - "Annotated tag `v1.5.8` (public) exists at the T4 close-gate commit SHA with CHANGELOG cross-reference (D-02b / D-02d / D-05d)"
    - "`.planning/MILESTONES.md` has new v1023 entry at top with shipped date + tags + verify-gate baselines (D-02c)"
    - "Phase 1098 + 1099 SUMMARY.md files unchanged — NO Phase 1098/1099 re-touch (CONTEXT.md out-of-scope guard-rails)"
    - "Production code unchanged — `git diff main...HEAD -- backend/app/` empty (D-06d carry-through from Phase 1098/1099 D-02)"
    - "Single-plan 5-task structure (T1 baselines → T2 CLOSE-GATE → T3 CHANGELOG → T4 atomic 5-file commit → T5 tags + MILESTONES) per D-03a (matches v1022 Phase 1097-01 + Phase 1098/1099 single-plan precedent)"
    - "T4 close-gate commit SHA captured explicitly at T5 step 5a (`/tmp/1100-close-sha.txt`) per D-03c (Tag commit SHA pinning rule — future milestones trace v1023 close without git log archaeology)"
  artifacts:
    - path: ".planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md"
      provides: "Verbatim verify-gate baselines + CI-01 degraded close rationale + per-requirement closure evidence table + v1024+ carry-forward block"
      min_lines: 80
    - path: ".planning/phases/1100-ci-live-verify-close-gate/1100-01-SUMMARY.md"
      provides: "Phase 1100 close summary with T1 evidence, hard-gates met, files touched, self-check"
      min_lines: 100
    - path: "CHANGELOG.md"
      provides: "[1.5.8] block with per-requirement evidence (CI-01 degraded + OOS-01/02/03 + OAUTH-01/02/03 + CLOSE-01)"
      contains: "## [1.5.8] - 2026-05-24"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CI-01 + CLOSE-01 traceability rows + checkboxes flipped to Complete"
      contains: "CI-01 | Phase 1100 | Complete"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 1100 checkbox flipped + plan checkbox flipped + Progress table row updated"
    - path: ".planning/MILESTONES.md"
      provides: "v1023 milestone entry at top with shipped date, tags, baselines, degraded-close annotation"
      contains: "## v1023 CI Live-Verify + OOS Hygiene Tail"
  key_links:
    - from: "1100-CLOSE-GATE.md"
      to: "Phase 1098 SHA b9be9027 + Phase 1099 SHA 1314ba5f"
      via: "verify-gate baseline citations"
      pattern: "b9be9027|1314ba5f"
    - from: "CHANGELOG.md [1.5.8] block"
      to: "closure SHAs for OOS-01/02/03 + OAUTH-01/02/03"
      via: "per-requirement evidence rows"
      pattern: "23336143|0068aa4f|431e2b54|9546a961|77affeac|f57f1a76|9922cce5"
    - from: "MILESTONES.md v1023 row"
      to: "tags v1023 (local) + v1.5.8 (public)"
      via: "tag SHA cross-reference"
      pattern: "v1023.*v1\\.5\\.8"
    - from: "Tag v1023 + v1.5.8"
      to: "T4 close-gate commit SHA"
      via: "annotated tag creation at HEAD post-T4"
      pattern: "git tag -a v1023.*-m"
---

<objective>
Close v1023 CI Live-Verify + OOS Hygiene Tail milestone with degraded CI-01 (user-authorized per CONTEXT.md D-01a) + full CLOSE-01: verify-gate baselines, CHANGELOG `[1.5.8]` entry, atomic close commit, and annotated tags `v1023` (local) + `v1.5.8` (public).

Purpose: Retire v1023 as a tagged release. The OOS triad (Phase 1098) and OAuth parallel-mode flakes (Phase 1099) are already closed at SHAs `b9be9027` and `1314ba5f` respectively. Phase 1100 captures the close-gate evidence document, writes the CHANGELOG release notes, and cuts the tags. CI-01 ships degraded mirroring v1022's precedent — GitHub Actions billing block at https://github.com/organizations/geolens-io/settings/billing has persisted since v1022 (verified at run `26359374410`); becomes v1024+ carry-forward.

Output:
- `.planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md` — verbatim verify-gate baselines + CI-01 degraded rationale + per-requirement evidence + v1024+ carry-forward block
- `.planning/phases/1100-ci-live-verify-close-gate/1100-01-SUMMARY.md` — phase close summary
- `CHANGELOG.md` — new `[1.5.8] - 2026-05-24` block above existing `[1.5.7]` block
- `.planning/REQUIREMENTS.md` — CI-01 (Complete degraded) + CLOSE-01 (Complete) traceability rows + checkbox flips
- `.planning/ROADMAP.md` — Phase 1100 checkbox + plan checkbox + Progress table row flipped
- `.planning/MILESTONES.md` — new v1023 entry at top
- Annotated tags `v1023` (local) + `v1.5.8` (public) at the T4 atomic close-gate commit SHA
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/1100-ci-live-verify-close-gate/1100-CONTEXT.md

# Reference precedents (degraded close + per-requirement evidence shape)
@.planning/milestones/v1022-phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md
@CHANGELOG.md

# v1023 baseline source-of-truth (the SHAs CHANGELOG [1.5.8] cites)
@.planning/phases/1098-oos-triad-closure/1098-01-SUMMARY.md
@.planning/phases/1099-oauth-parallel-mode-stabilization/1099-01-SUMMARY.md
@.planning/MILESTONES.md

<interfaces>
<!-- Key contracts the executor needs. NO codebase exploration needed. -->

## v1023 close-gate baselines (already proven at Phase 1098/1099)

Sequential pytest expected at T1: `3062 passed / 0 failed / 38 skipped` (literal-zero — Phase 1098 SHA `b9be9027`)
`-n 4` pytest expected at T1: `3062 passed / 0 failed / 38 skipped` (literal-zero — Phase 1099 SHA `1314ba5f`)
`-n auto` 3-run expected at T1: 0-2 distinct (F+E) per run within v1022 PARA-01 ≤30 envelope, zero OOS/OAUTH pin names

## CI-01 degraded close evidence (substitute for live `gh run watch`)

Per D-01b, substitute evidence is:
- `docker compose ps` → 5/5 services healthy
- `curl http://localhost:8080/api/health` (NO trailing slash per v1022 [Rule 3]) → 200 OK
- Sequential + `-n 4` + `-n auto` 3-run baselines captured at T1
- Billing-block reference: https://github.com/organizations/geolens-io/settings/billing (run `26359999664` annotation matches v1022 run `26359374410`)
- v1022 precedent: `.planning/milestones/v1022-phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md` § "CI-01 Live-Verify — DEFERRED to v1023"

## Closure SHAs for CHANGELOG [1.5.8]

| Req | Test pin | Pin location | SHA(s) |
|-----|----------|--------------|--------|
| OOS-01 | `test_router_orchestrator_modules_stay_within_loc_cap` | `backend/tests/test_layering.py:833` | `23336143` |
| OOS-02 | `test_readme_signature_maps_list_intact` (DELETED) | was `backend/tests/test_phase_275_readme_accuracy.py:116` | `0068aa4f` |
| OOS-03 | `test_make_safe_client_blocks_private_ip_redirect` (renamed from `test_make_safe_client_has_event_hook`) | `backend/tests/test_ssrf_redirect.py:~100` | `431e2b54` + `9546a961` (Rule-1 iter) + `77affeac` (WR-01/02 polish) |
| OAUTH-01 | `test_callback_missing_state_returns_error` | `backend/tests/test_oauth.py:975` (post-fix; was 869 pre-fix) | `f57f1a76` + `9922cce5` |
| OAUTH-02 | `test_callback_invalid_code_returns_error` | `backend/tests/test_oauth.py:1016` (post-fix; was 901 pre-fix) | `f57f1a76` + `9922cce5` |
| OAUTH-03 | `test_oauth_login_redirect` | `backend/tests/test_oauth.py:921` (post-fix; was 826 pre-fix) | `f57f1a76` + `9922cce5` |
| Phase 1098 atomic close | — | — | `b9be9027` |
| Phase 1099 atomic close | — | — | `1314ba5f` |

## Tag annotation message templates (D-02b)

```
v1023 CI Live-Verify + OOS Hygiene Tail (degraded close 2026-05-24)

Sequential: 3062 passed / 0 failed / 38 skipped (literal-zero — Phase 1098 SHA b9be9027)
-n 4: 3062 passed / 0 failed / 38 skipped (literal-zero — Phase 1099 SHA 1314ba5f)
-n auto 3-run: <T1 measurements — populate at T5 from /tmp/1100-verify-auto-*.log>

CI-01 deferred (GH Actions billing block at https://github.com/organizations/geolens-io/settings/billing); v1024+ carry-forward.
Close-gate SHA: <T4 SHA via git log -1 --format=%H>
```

```
v1.5.8 (test-infra hygiene)

Closes: CI-01 (degraded), OOS-01, OOS-02, OOS-03, OAUTH-01, OAUTH-02, OAUTH-03, CLOSE-01.
See CHANGELOG.md [1.5.8] for per-requirement evidence.
Close-gate SHA: <T4 SHA via git log -1 --format=%H>
```

## CHANGELOG [1.5.8] block target structure

Insert BETWEEN `## [Unreleased]` (line 12 — replace placeholder body) and `## [1.5.7] - 2026-05-24` (line 16 — leave untouched). Use the structural shape from CONTEXT.md `<specifics>` "CHANGELOG `[1.5.8]` block shape" but adapt prose per Claude's Discretion. Required sections:
- Header line referencing test-infra hygiene + retired-OOS theme
- `### Closed` — per-requirement bullet list with test pin + line + closure SHA
- `### Baselines (post-v1023)` — sequential + `-n 4` + `-n auto` numbers
- `### Notes` — v1024+ carry-forward (CI-01 billing) + cross-reference to Phase 1099 REVIEW IN-01..IN-04 (test-isolation observations)
- `### Migrations` (None — all changes test-infra)

The existing `## [Unreleased]` block's body (`_No entries yet — open the next milestone with /gsd:new-milestone to begin v1023._`) should be REPLACED with a new placeholder body for the next milestone (e.g., `_No entries yet — open the next milestone with /gsd:new-milestone._`), preserving the `## [Unreleased]` header above the new `## [1.5.8]` block.

## Stale-DB cleanup recipe (between -n auto runs)

```bash
docker compose exec -T db psql -U geolens -d geolens -At -c \
  "SELECT datname FROM pg_database WHERE datname LIKE 'geolens%test_gw%' OR datname LIKE 'geolens%test_master%';" \
  | xargs -I{} docker compose exec -T db psql -U geolens -d geolens -c "DROP DATABASE IF EXISTS \"{}\" WITH (FORCE);"
```

## `.env.test` recipe (MEMORY.md / Phase 1098/1099 carry-through)

```bash
cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/
```
The `set -a` / `set +a` envelope is LOAD-BEARING — without it, pytest fails with `InvalidCatalogNameError`. `POSTGRES_HOST=localhost`, `POSTGRES_PORT=5434`.

## REQUIREMENTS.md flip target shape (D-05c)

Current state (lines 80-87):
```
| CI-01 | Phase 1100 | Pending |
...
| CLOSE-01 | Phase 1100 | Pending |
```

Target state after T4:
```
| CI-01 | Phase 1100 | Complete (degraded) |
...
| CLOSE-01 | Phase 1100 | Complete |
```

Also flip lines 22 (`- [ ] **CI-01**:`) and 44 (`- [ ] **CLOSE-01**:`) checkboxes from `[ ]` to `[x]`.

## ROADMAP.md flip target shape

Line 95: `- [ ] **Phase 1100...` → `- [x] **Phase 1100...`
Line 140-141: plan checkboxes for 1100-01 + 1100-02 — flip 1100-01 to `[x]`; per CONTEXT.md D-03a only 1 plan ships, so the existing `- [ ] 1100-02: Close gate...` row should be REMOVED (planner consolidated into 1100-01).
Line 179: Progress row `1100. CI Live-Verify + Close Gate | v1023 | 0/2 | Not started | -` → `1100. CI Live-Verify + Close Gate | v1023 | 1/1 | Shipped | 2026-05-24` (plan count adjusted to 1/1 since 1100-02 was consolidated).
Line 85: v1023 milestone row → flip emoji `🚧` to `✅` and append shipped date.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture verify-gate baselines + docker health + /api/health</name>
  <files>/tmp/1100-verify-seq.log, /tmp/1100-verify-n4.log, /tmp/1100-verify-auto-A.log, /tmp/1100-verify-auto-B.log, /tmp/1100-verify-auto-C.log, /tmp/1100-docker-ps.txt, /tmp/1100-curl-health.txt</files>
  <read_first>
    - .planning/phases/1100-ci-live-verify-close-gate/1100-CONTEXT.md (D-04 verify gate + D-01b substitute evidence list)
    - .planning/phases/1099-oauth-parallel-mode-stabilization/1099-01-SUMMARY.md (expected baseline shape — table format reference)
    - .planning/milestones/v1022-phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md (stale-DB cleanup recipe + /api/health no-trailing-slash precedent)
  </read_first>
  <action>
    Capture v1023 close-gate evidence per D-04a..e:

    1. `docker compose ps` → save to `/tmp/1100-docker-ps.txt`. Verify 5/5 services (api / db / frontend / titiler / worker) all show `(healthy)`. If any service is not healthy, STOP and report — D-06b verify-gate regression handler.

    2. `curl -sf -o /dev/null -w "%{http_code}\n" http://localhost:8080/api/health` → save to `/tmp/1100-curl-health.txt`. MUST return `200`. Use NO trailing slash per v1022 [Rule 3] / MEMORY.md `redirect_slashes=False`. If returns non-200, STOP and report.

    3. Sequential baseline: `cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/ 2>&1 | tee /tmp/1100-verify-seq.log`. Final summary line MUST match `=== 3062 passed, 38 skipped, ... ===` with `0 failed`. If `failed > 0` literal, STOP per D-06b — Phase 1098 baseline regression.

    4. `-n 4` baseline (single run sufficient per D-04b — Phase 1099 already proved 3-run determinism): `cd backend && set -a && source ../.env.test && set +a && uv run pytest -n 4 tests/ 2>&1 | tee /tmp/1100-verify-n4.log`. Final summary line MUST match `========== 3062 passed, 38 skipped, ... ===========`. If `failed > 0` literal, STOP per D-06b — Phase 1099 baseline regression.

    5. `-n auto` 3-run with stale-DB cleanup between every run per `<interfaces>` "Stale-DB cleanup recipe":
       - Cleanup → `pytest -n auto` → save to `/tmp/1100-verify-auto-A.log`
       - Cleanup → `pytest -n auto` → save to `/tmp/1100-verify-auto-B.log`
       - Cleanup → `pytest -n auto` → save to `/tmp/1100-verify-auto-C.log`
       - Each run distinct (failed+errors) ≤30 per D-04c PARA-01 envelope. ZERO occurrences of any OOS pin name (`test_router_orchestrator_modules_stay_within_loc_cap` / `test_readme_signature_maps_list_intact` / `test_make_safe_client_has_event_hook` / `test_make_safe_client_blocks_private_ip_redirect`) AND zero occurrences of any OAUTH pin name (`test_callback_missing_state_returns_error` / `test_callback_invalid_code_returns_error` / `test_oauth_login_redirect`) in any failure list. Verify via `grep -E "(<pin names>)" /tmp/1100-verify-auto-*.log | grep -i fail | wc -l` → must return `0`.

    6. Tally results into a markdown table for T2 to embed verbatim (per Phase 1099 SUMMARY § "Verify Gate Evidence" shape).

    NO production code touched (D-06d). NO Phase 1098/1099 SUMMARY re-touch (CONTEXT.md guard-rails).
  </action>
  <verify>
    <automated>test -s /tmp/1100-verify-seq.log && test -s /tmp/1100-verify-n4.log && test -s /tmp/1100-verify-auto-A.log && test -s /tmp/1100-verify-auto-B.log && test -s /tmp/1100-verify-auto-C.log && test -s /tmp/1100-docker-ps.txt && test -s /tmp/1100-curl-health.txt && grep -q "3062 passed" /tmp/1100-verify-seq.log && grep -q "3062 passed" /tmp/1100-verify-n4.log && grep -q "200" /tmp/1100-curl-health.txt && [ "$(grep -cE 'test_router_orchestrator_modules_stay_within_loc_cap|test_readme_signature_maps_list_intact|test_make_safe_client_has_event_hook|test_make_safe_client_blocks_private_ip_redirect|test_callback_missing_state_returns_error|test_callback_invalid_code_returns_error|test_oauth_login_redirect' /tmp/1100-verify-auto-*.log 2>/dev/null | grep -i fail | wc -l)" = "0" ] && echo "T1 verify-gate evidence captured"</automated>
  </verify>
  <done>All 7 evidence files exist and non-empty. Sequential + `-n 4` show `3062 passed` literal-zero. All 3 `-n auto` runs ≤30 distinct (F+E) with zero OOS/OAUTH pin names in any failure list. `docker compose ps` shows 5/5 healthy. `curl /api/health` returned 200. No production code modified. Evidence ready for T2 verbatim embedding.</done>
</task>

<task type="auto">
  <name>Task 2: Write 1100-CLOSE-GATE.md with verbatim T1 baselines + degraded CI-01 rationale</name>
  <files>.planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md</files>
  <read_first>
    - .planning/milestones/v1022-phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md (template structure — header / per-criterion section / log file pointers / reproducer recipe)
    - .planning/phases/1100-ci-live-verify-close-gate/1100-CONTEXT.md (D-01..D-06 — embed citations)
    - /tmp/1100-verify-seq.log + /tmp/1100-verify-n4.log + /tmp/1100-verify-auto-{A,B,C}.log + /tmp/1100-docker-ps.txt + /tmp/1100-curl-health.txt (T1 evidence to embed verbatim)
  </read_first>
  <action>
    Write `.planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md` mirroring v1022 Phase 1097-01 CLOSE-GATE.md structure but adapted for v1023's full-zero state. Required sections:

    1. **Frontmatter** — `phase: 1100-ci-live-verify-close-gate` / `plan: 01` / `status: complete` / `captured: 2026-05-24` / `requirements_addressed: [CI-01, CLOSE-01]`.

    2. **Header** — Title + status + captured date (per v1022 precedent shape, but `status: COMPLETE` not `IN PROGRESS`).

    3. **CLOSE-01 (a) — Sequential baseline** — Embed `/tmp/1100-verify-seq.log` final summary line VERBATIM. Show delta table vs v1022 (3060/3 OOS/38 → 3062/0/38 — the OOS rows RETIRED, not bypassed, per D-04a HARD INVARIANT). State HARD INVARIANT preserved literal-zero. Cite Phase 1098 SHA `b9be9027`.

    4. **CLOSE-01 (b) — `-n 4` baseline** — Embed `/tmp/1100-verify-n4.log` summary line verbatim. Show delta vs v1022 (3059/4/38 → 3062/0/38). State HARD INVARIANT preserved literal-zero. Cite Phase 1099 SHA `1314ba5f` (single-run sufficient per D-04b — Phase 1099 proved 3-run determinism with 3062/0/38 ×3).

    5. **CLOSE-01 (c) — `-n auto` 3-run measurement** — Table with columns `Run | distinct (F+E) | ICN frames | PARA-01 gate | wallclock`. Embed Runs A/B/C distinct counts from `/tmp/1100-verify-auto-*.log`. Confirm PARA-01 envelope (≤30 per run, 0 ICN frames). State zero OAuth/OOS pin names in any failure list (cite the grep command + output from T1 step 5).

    6. **CLOSE-01 (d) — Live docker stack health spot-check** — Embed `/tmp/1100-docker-ps.txt` verbatim. Embed `/tmp/1100-curl-health.txt` showing `200`. Note no-trailing-slash per v1022 [Rule 3] / MEMORY.md `redirect_slashes=False`. Optional Playwright MCP visit SKIPPED per D-04f (no frontend deliverable in Phase 1100).

    7. **CLOSE-01 (e) — CHANGELOG `[1.5.8]` cross-reference** — Reference `CHANGELOG.md` `[1.5.8]` block (T3 will write). List the 7 per-requirement closures with test pin + line + closure SHA per D-02a (use the closure SHA table from `<interfaces>` "Closure SHAs for CHANGELOG [1.5.8]").

    8. **CI-01 — DEGRADED close (carry-forward to v1024+)** — Per D-01a/b/c/d:
       - State user-authorized degraded close 2026-05-24 (mirrors v1022 precedent — same billing block since v1022 run `26359374410`)
       - Substitute evidence captured at T1 (cite the 5 evidence types per D-01b)
       - NO fresh dispatch attempt per D-01d (rationale: re-running would just re-confirm billing block; skip the spam)
       - v1024+ carry-forward: capture billing block as CI-01-v1024 in `.planning/MILESTONES.md` (T5 enforces)
       - Cite billing-block URL: https://github.com/organizations/geolens-io/settings/billing
       - Cross-reference v1022 precedent: `.planning/milestones/v1022-phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md` § "CI-01 Live-Verify — DEFERRED to v1023"

    9. **Tags cut** — Placeholder section listing `v1023` (local) + `v1.5.8` (public) — T5 will populate the SHA after T4 commits.

    10. **Log file pointers** — Mirror v1022 § "Log file pointers (for re-verification)" + reproducer recipe (sequential + `-n 4` + `-n auto` cleanup-and-run loop). Cite `/tmp/` paths.

    11. **CLOSE-01 (f)** — Explicit note: live `gh run watch` evidence deferred per D-01a (degraded close); v1024+ carry-forward.

    12. **CLOSE-01 (g)** — Tags cut and recorded in `.planning/MILESTONES.md` (T5 enforces).

    Embed D-XX citations inline (D-01a/b/c/d / D-02a/b/c/d / D-04a/b/c/d/e/f / D-05a/b/c/d / D-06a/b/c/d) per CONTEXT.md fidelity rules.

    Target ~120-150 lines. Use markdown tables for baselines and verbatim code fences for log excerpts.
  </action>
  <verify>
    <automated>test -f .planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md && grep -q "3062 passed" .planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md && grep -q "degraded" .planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md && grep -q "b9be9027" .planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md && grep -q "1314ba5f" .planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md && grep -q "github.com/organizations/geolens-io/settings/billing" .planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md && grep -q "PARA-01" .planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md && [ $(wc -l < .planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md) -ge 80 ]</automated>
  </verify>
  <done>CLOSE-GATE.md exists with all 7 required sections (a-g) populated. Sequential + -n 4 baselines literal-zero embedded verbatim. -n auto 3-run table within PARA-01 envelope. Docker health + /api/health 200 embedded. CI-01 degraded rationale + billing-block URL + v1022 precedent cross-referenced. Phase 1098 (b9be9027) + Phase 1099 (1314ba5f) SHAs cited. File ≥80 lines.</done>
</task>

<task type="auto">
  <name>Task 3: Write CHANGELOG.md [1.5.8] block with per-requirement evidence</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md lines 1-45 (Unreleased + [1.5.7] block — structural precedent for [1.5.8])
    - .planning/phases/1100-ci-live-verify-close-gate/1100-CONTEXT.md (`<specifics>` "CHANGELOG `[1.5.8]` block shape" — planner-refined target)
    - .planning/phases/1098-oos-triad-closure/1098-01-SUMMARY.md (closure SHAs + test pin names for OOS-01/02/03)
    - .planning/phases/1099-oauth-parallel-mode-stabilization/1099-01-SUMMARY.md (closure SHAs + test pin names for OAUTH-01/02/03 + verify-gate baselines)
    - .planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md (T2 output — verbatim baselines to cite)
  </read_first>
  <action>
    Insert a new `## [1.5.8] - 2026-05-24` block in `CHANGELOG.md` between the existing `## [Unreleased]` block (currently at line 12) and `## [1.5.7] - 2026-05-24` (currently at line 16).

    Required structure (adapt prose per Claude's Discretion, but preserve required content):

    ```markdown
    ## [1.5.8] - 2026-05-24

    Test-infrastructure hygiene — retires the 3 pre-existing OOS sequential failures + 3 OAuth parallel-mode flakes so the post-milestone invariant becomes `sequential failed == 0` LITERAL (not "0 NEW failed"). CI-01 live-verify ships degraded (GitHub Actions billing block); v1024+ carry-forward.

    ### Closed

    - **CI-01** (degraded, Phase 1100): GitHub Actions live-verify deferred — persistent billing block at https://github.com/organizations/geolens-io/settings/billing since v1022 (run `26359374410`; same annotation persists at v1023 close). User-authorized degraded close 2026-05-24 mirroring v1022 precedent. Substitute evidence: 5/5 docker services healthy + `GET /api/health` returns 200 + sequential/`-n 4`/`-n auto` local baselines below. v1024+ carry-forward.

    - **OOS-01** (Phase 1098): `test_router_orchestrator_modules_stay_within_loc_cap` (`backend/tests/test_layering.py:833`) — TRIM path landed `backend/app/modules/catalog/maps/router.py` at 1793 LOC (-14 lines via private-helper docstring compression on `_build_frame_ancestors` + `_meta_to_kwargs`; under existing 1800 cap; zero behavior change). Allowlist unchanged. No Phase 999.x decomposition backlog promotion. SHA `23336143`.

    - **OOS-02** (Phase 1098): `test_readme_signature_maps_list_intact` (was `backend/tests/test_phase_275_readme_accuracy.py:116`) — test DELETED 2026-05-24. The README signature-stories section it pinned was retired in commit `4a7d1a29` (2026-05-22 "chore: remove demo overlay apparatus") along with the themed-demo docker overlay; restoring it would be a doc-lying regression. 8 sibling tests in the same file still pass. SHA `0068aa4f`.

    - **OOS-03** (Phase 1098): `test_make_safe_client_has_event_hook` → renamed to `test_make_safe_client_blocks_private_ip_redirect` (`backend/tests/test_ssrf_redirect.py:~100`) — behavioral SSRF-contract rewrite immune to module-level `mock.patch` contamination from sibling test. Two-iteration fix path: first iter still called `make_safe_client()` and tripped on global `httpx.AsyncClient` patching; Rule 1 inline iteration dropped the factory call entirely and tests `_revalidate_redirect(response)` directly, matching the 6 sibling tests at lines 22-97 that pass durably in full sequential. SHAs `431e2b54` + `9546a961` + WR-01/WR-02 polish at `77affeac`.

    - **OAUTH-01** (Phase 1099): `test_callback_missing_state_returns_error` (`backend/tests/test_oauth.py:975` post-fix; was `:869` pre-fix) — fixed via `client_session` fixture override (shares client's `dependency_overrides[get_db]` factory for single-connection writes-then-reads visibility) + `_ensure_public_app_url` monkeypatch fixture (pins `settings.public_app_url` + resets `_PUBLIC_URL_CACHE` to address Phase 268 H-27 / SEC-13 strict-config requirement). Two-iteration: iter-1 had `client.app` attribute bug → iter-2 fixed import to `from app.api.main import app`. SHAs `f57f1a76` + `9922cce5`.

    - **OAUTH-02** (Phase 1099): `test_callback_invalid_code_returns_error` (`backend/tests/test_oauth.py:1016` post-fix; was `:901` pre-fix) — same shared root-cause fix as OAUTH-01 (per REQUIREMENTS.md OAUTH-02 framing: "if one fix closes both, OAUTH-02 SUMMARY references the OAUTH-01 closure SHA").

    - **OAUTH-03** (Phase 1099): `test_oauth_login_redirect` (`backend/tests/test_oauth.py:921` post-fix; was `:826` pre-fix) — same shared root-cause fix as OAUTH-01/02. Surfaced 2026-05-24 during Phase 1098 verify-gate `-n auto` Run B; OAUTH-03 added to REQUIREMENTS.md mid-milestone.

    - **CLOSE-01** (Phase 1100): Verify-gate baselines captured + tags `v1023` (local) + `v1.5.8` (public) cut. See `.planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md` for evidence detail.

    ### Baselines (post-v1023)

    - Sequential pytest: 3062 passed / 0 failed / 38 skipped (LITERAL ZERO — `failed == 0` invariant strengthened from "0 NEW" to literal; OOS triad retired)
    - `-n 4` pytest: 3062 passed / 0 failed / 38 skipped (LITERAL ZERO — OAuth parallel-mode flakes retired)
    - `-n auto` 3-run: <quote distinct (F+E) counts from T1 verify-gate auto-A/B/C runs, e.g., "0/0/2 distinct deterministic — well under v1022 PARA-01 ≤30 envelope; 0 ICN frames; zero OOS/OAUTH pin names in any failure list">

    ### Notes

    - **v1024+ carry-forward (1 item):** CI-01 live-verify on real GitHub Actions infrastructure (depends on org billing resolution at https://github.com/organizations/geolens-io/settings/billing).
    - **Test-isolation observations (no defects):** Phase 1099 REVIEW IN-01..IN-04 — quality observations for a possible v1024+ test-isolation hygiene ledger; not regressions.
    - **Gate-shape:** verified locally to v1021 TEST-01 + v1022 PARA-01 depth via Phase 1100 T1 evidence.

    ### Migrations

    None. All v1.5.8 changes are test-infra hygiene (`backend/tests/test_layering.py` allowlist context unchanged + `backend/tests/test_phase_275_readme_accuracy.py` -21 LOC + `backend/tests/test_ssrf_redirect.py` behavioral rewrite + `backend/tests/test_oauth.py` fixture additions) + ONE production-code surface (`backend/app/modules/catalog/maps/router.py` -14 LOC docstring compression, zero behavior change). No API contract changes, no schema changes, no migrations.
    ```

    Also REPLACE the `## [Unreleased]` block's body (currently `_No entries yet — open the next milestone with /gsd:new-milestone to begin v1023._`) with `_No entries yet — open the next milestone with /gsd:new-milestone._` (drops the v1023 reference now that v1023 has shipped).

    DO NOT touch the existing `## [1.5.7]` block or any prior blocks.

    Use the Edit tool to insert the new block — DO NOT rewrite the whole file with Write.
  </action>
  <verify>
    <automated>grep -q "^## \[1.5.8\] - 2026-05-24" CHANGELOG.md && grep -q "OOS-01" CHANGELOG.md && grep -q "OAUTH-03" CHANGELOG.md && grep -q "23336143" CHANGELOG.md && grep -q "9922cce5" CHANGELOG.md && grep -q "github.com/organizations/geolens-io/settings/billing" CHANGELOG.md && grep -q "3062 passed / 0 failed / 38 skipped" CHANGELOG.md && [ "$(grep -c "^## \[1.5.7\] - 2026-05-24" CHANGELOG.md)" = "1" ]</automated>
  </verify>
  <done>CHANGELOG.md contains new `## [1.5.8] - 2026-05-24` block above `## [1.5.7]` block. All 7 closures (CI-01 + OOS-01/02/03 + OAUTH-01/02/03 + CLOSE-01) listed with test pin + line + closure SHA. Baselines section quotes literal-zero. Notes section documents v1024+ CI-01 carry-forward + billing-block URL. [1.5.7] block untouched (still exactly 1 occurrence). [Unreleased] body updated to drop v1023 reference.</done>
</task>

<task type="auto">
  <name>Task 4: Atomic close commit — flip REQUIREMENTS.md + ROADMAP.md + write SUMMARY.md + commit CLOSE-GATE.md + CHANGELOG.md in ONE commit</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/1100-ci-live-verify-close-gate/1100-01-SUMMARY.md, .planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md, CHANGELOG.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 22 + 44 + 80-87 (CI-01 + CLOSE-01 traceability rows to flip)
    - .planning/ROADMAP.md lines 85 + 95 + 140-141 + 179 (Phase 1100 + plan checkbox + Progress table)
    - .planning/phases/1098-oos-triad-closure/1098-01-SUMMARY.md (SUMMARY shape precedent — frontmatter / Goal Achieved / What Shipped / T1 evidence / Verify Gate Evidence / Files Touched / Hard Gates Met / Self-Check)
    - .planning/phases/1099-oauth-parallel-mode-stabilization/1099-01-SUMMARY.md (closer-shape precedent — final v1023 phase shape)
    - .planning/phases/1100-ci-live-verify-close-gate/1100-CONTEXT.md (D-05c atomic-flip rule — REQUIREMENTS.md + ROADMAP.md + SUMMARY.md + CLOSE-GATE.md + CHANGELOG.md SAME commit)
  </read_first>
  <action>
    Perform 4 in-memory edits THEN commit ATOMICALLY (per D-05c v1019 TD-13 atomic-flip rule — same commit must touch all 5 paths):

    **Edit 1 — `.planning/REQUIREMENTS.md`** (3 changes):
    - Line ~22: Flip `- [ ] **CI-01**:` checkbox to `- [x] **CI-01**:`
    - Line ~44: Flip `- [ ] **CLOSE-01**:` checkbox to `- [x] **CLOSE-01**:`
    - Lines 80 + 87 (Traceability table):
      ```
      | CI-01 | Phase 1100 | Pending |        →  | CI-01 | Phase 1100 | Complete (degraded) |
      | CLOSE-01 | Phase 1100 | Pending |     →  | CLOSE-01 | Phase 1100 | Complete |
      ```
    - Update line 96 "Last updated" timestamp to `2026-05-24 — traceability updated by executor (Phase 1100 CI-01 degraded close + CLOSE-01 close-gate)`

    **Edit 2 — `.planning/ROADMAP.md`** (4 changes):
    - Line 85: `🚧 **v1023 CI Live-Verify + OOS Hygiene Tail** — Phases 1098-1100 (in progress)` → `✅ **v1023 CI Live-Verify + OOS Hygiene Tail** — Phases 1098-1100 (shipped 2026-05-24, local tag `v1023`, public tag `v1.5.8`)` (move the row from in-progress to shipped position; also remove the "(In Progress)" line 89 header if grouping pattern demands — match v1022's archived shape)
    - Line 95: Flip `- [ ] **Phase 1100: CI Live-Verify + Close Gate**` → `- [x] **Phase 1100: CI Live-Verify + Close Gate**`
    - Lines 140-141 (Phase 1100 "Plans:" sub-list): The CONTEXT.md D-03a consolidates the original 2-plan split into a SINGLE plan `1100-01`. Replace:
      ```
      Plans:
      - [ ] 1100-01: CI live-verify (`gh run rerun 26359374410` or new dispatch + `gh run watch`) + embed run log
      - [ ] 1100-02: Close gate (final baseline measurements + CHANGELOG `[1.5.8]` + tags `v1023`/`v1.5.8`)
      ```
      WITH:
      ```
      Plans:
      - [x] 1100-01: CI Live-Verify (degraded — billing block) + Close Gate (baselines + CHANGELOG `[1.5.8]` + tags `v1023`/`v1.5.8`)
      ```
    - Line 179 (Progress table): `| 1100. CI Live-Verify + Close Gate | v1023 | 0/2 | Not started | - |` → `| 1100. CI Live-Verify + Close Gate | v1023 | 1/1 | Shipped | 2026-05-24 |`

    **Edit 3 — Write `.planning/phases/1100-ci-live-verify-close-gate/1100-01-SUMMARY.md`** (new file, ~150-180 lines, mirror Phase 1098/1099 SUMMARY shape):

    Required sections:
    - **Frontmatter** — `phase: 1100` / `plan: 01` / `subsystem: test-infra` / `tags: [hygiene, close-gate, ci-live-verify, degraded-close]` / `requires: []` / `provides: [v1023-milestone-close, ci-01-v1024-carry-forward]` / `affects` (list 5 modified files + 1 CLOSE-GATE + 1 SUMMARY new file) / `tech-stack.added: []` / `patterns: [degraded-close-with-carry-forward, atomic-5-file-flip]` / `key-files.created` (CLOSE-GATE + SUMMARY) / `key-files.modified` (REQUIREMENTS + ROADMAP + CHANGELOG + MILESTONES) / `decisions` (D-01..D-06 summary lines) / `metrics.duration` + `metrics.completed: 2026-05-24` / `requirements_addressed: [CI-01, CLOSE-01]`

    - **Goal Achieved** — v1023 milestone closes with literal-zero sequential + `-n 4` baselines (per D-04a/b HARD INVARIANT), CI-01 ships degraded mirroring v1022 precedent (per D-01a), tags `v1023` + `v1.5.8` cut.

    - **What Shipped** — Brief summary of each task:
      - T1: Verify-gate baselines (cite the actual numbers from T1 evidence)
      - T2: CLOSE-GATE.md written with 7 sections (a-g) populated
      - T3: CHANGELOG `[1.5.8]` block written with 7 closure entries
      - T4: Atomic 5-file close commit (this commit)
      - T5: Tags `v1023` (local) + `v1.5.8` (public) cut (T5 enforces post-commit)

    - **Pre-flight Evidence (T1)** — Embed `docker compose ps` (5 healthy) + `curl /api/health` 200 + `set -a` env probe + grep results showing pin paths.

    - **Verify Gate Evidence (T1)** — Table matching Phase 1099 SUMMARY § "Verify Gate Evidence" shape. Embed verbatim summary lines from `/tmp/1100-verify-*.log`.

    - **Files Touched** — Table with File / Change / LOC delta columns (5 modified + 2 new). Note `git log -1 --name-only` will show exactly 5 paths at the T4 atomic commit.

    - **Hard Gates Met** — `[x]` for D-04a/b/c/d/e + D-05a/b/c/d + D-06a/d (production code unchanged via `git diff main...HEAD -- backend/app/` empty check — note: `maps/router.py` was modified at Phase 1098 SHA `23336143`, so this check is "no changes since Phase 1099 SHA `1314ba5f`").

    - **Carry-forward to v1024+** — CI-01 live-verify (billing block) per D-01c. Reference `.planning/MILESTONES.md` v1023 entry (T5 enforces) for tracking.

    - **Patterns Reinforced** — Degraded-close-with-carry-forward (v1022 → v1023 → v1024 chain); atomic 5-file flip preserving v1019 TD-13; no fresh CI dispatch attempt per D-01d (skip-the-spam pattern).

    - **Self-Check: PASSED** — Bullet list of file state checks + commit-shape check (`git log -1 --name-only` shows exactly 5 paths).

    **Edit 4 — Atomic commit (T4)**:

    `git add .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/1100-ci-live-verify-close-gate/1100-01-SUMMARY.md .planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md CHANGELOG.md`

    `git commit -m "docs(1100-01): close v1023 — degraded CI-01 + CLOSE-01 + CHANGELOG [1.5.8]"` (use HEREDOC if multi-line body; D-05c demands ALL 5 paths in one commit; verify post-commit with `git log -1 --name-only` showing exactly 5 paths).

    NO Phase 1098/1099 SUMMARY re-touch. NO production code modification (only docs + CHANGELOG which is documentation per v1022 precedent). NO `.planning/MILESTONES.md` in this commit (T5 owns that file to keep T4 atomic at exactly 5 paths).
  </action>
  <verify>
    <automated>git show --name-only --format= HEAD | grep -v '^$' | sort > /tmp/1100-t4-files.txt && cat > /tmp/1100-t4-expected.txt <<'EOF'
CHANGELOG.md
.planning/REQUIREMENTS.md
.planning/ROADMAP.md
.planning/phases/1100-ci-live-verify-close-gate/1100-01-SUMMARY.md
.planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md
EOF
sort /tmp/1100-t4-expected.txt > /tmp/1100-t4-expected-sorted.txt && diff /tmp/1100-t4-files.txt /tmp/1100-t4-expected-sorted.txt && grep -q "Complete (degraded)" .planning/REQUIREMENTS.md && grep -q "^| CLOSE-01 | Phase 1100 | Complete |" .planning/REQUIREMENTS.md && grep -q "\[x\] \*\*Phase 1100" .planning/ROADMAP.md && grep -q "1/1 | Shipped | 2026-05-24" .planning/ROADMAP.md && test -f .planning/phases/1100-ci-live-verify-close-gate/1100-01-SUMMARY.md && grep -q "requirements_addressed: \[CI-01, CLOSE-01\]" .planning/phases/1100-ci-live-verify-close-gate/1100-01-SUMMARY.md && echo "T4 atomic commit valid"</automated>
  </verify>
  <done>Atomic commit touches EXACTLY 5 files (verified via `git log -1 --name-only` diff against expected list). REQUIREMENTS.md shows CI-01 `Complete (degraded)` + CLOSE-01 `Complete` with `[x]` flips. ROADMAP.md Phase 1100 + plan checkbox `[x]` flipped; v1023 milestone row moved to shipped + tags appended; Progress row shows `1/1 / Shipped / 2026-05-24`. SUMMARY.md exists with all required sections + frontmatter. CLOSE-GATE.md unchanged from T2. CHANGELOG.md unchanged from T3. NO Phase 1098/1099 SUMMARY re-touch.</done>
</task>

<task type="auto">
  <name>Task 5: Cut annotated tags v1023 (local) + v1.5.8 (public) + append MILESTONES.md v1023 row + push tags</name>
  <files>.planning/MILESTONES.md, git refs v1023 + v1.5.8</files>
  <read_first>
    - .planning/phases/1100-ci-live-verify-close-gate/1100-CONTEXT.md (`<code_context>` "Tag creation commands" — annotation message templates)
    - .planning/MILESTONES.md lines 1-50 (v1022 entry shape — append-to-top precedent)
    - .planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md (verify-gate baselines — embed in tag annotation message)
  </read_first>
  <action>
    Three sub-steps:

    **Step 5a — Capture T4 close-gate commit SHA:**
    ```bash
    CLOSE_SHA=$(git log -1 --format=%H)
    echo "$CLOSE_SHA" > /tmp/1100-close-sha.txt
    ```
    Verify `$CLOSE_SHA` matches the T4 atomic commit (sanity check: `git log -1 --oneline` should show "docs(1100-01): close v1023").

    **Step 5b — Create annotated tags (D-02b, D-05d, D-03c):**

    Adapt the templates from `<interfaces>` "Tag annotation message templates" with actual `-n auto` 3-run numbers from `/tmp/1100-verify-auto-*.log`:

    ```bash
    git tag -a v1023 -m "v1023 CI Live-Verify + OOS Hygiene Tail (degraded close 2026-05-24)

    Sequential: 3062 passed / 0 failed / 38 skipped (literal-zero — Phase 1098 SHA b9be9027)
    -n 4: 3062 passed / 0 failed / 38 skipped (literal-zero — Phase 1099 SHA 1314ba5f)
    -n auto 3-run: <actual T1 measurements, e.g., 0/0/2 distinct deterministic within v1022 PARA-01 envelope (≤30)>

    CI-01 deferred (GH Actions billing block at https://github.com/organizations/geolens-io/settings/billing); v1024+ carry-forward.
    Close-gate SHA: $CLOSE_SHA"

    git tag -a v1.5.8 -m "v1.5.8 (test-infra hygiene)

    Closes: CI-01 (degraded), OOS-01, OOS-02, OOS-03, OAUTH-01, OAUTH-02, OAUTH-03, CLOSE-01.
    See CHANGELOG.md [1.5.8] for per-requirement evidence.
    Close-gate SHA: $CLOSE_SHA"
    ```

    Both tags MUST point at `$CLOSE_SHA` (the T4 commit). Verify with `git rev-list -n 1 v1023` == `git rev-list -n 1 v1.5.8` == `$CLOSE_SHA`.

    **Step 5c — Append v1023 entry to MILESTONES.md (D-02c):**

    Insert a new section at the top of `.planning/MILESTONES.md` (above the current `## v1022 Parallel-Test Cascade Closure + Hygiene Tail` block at line 3). Use the v1022 entry as the structural precedent. Required content:

    ```markdown
    ## v1023 CI Live-Verify + OOS Hygiene Tail (Shipped: 2026-05-24)

    **Phases completed:** 3 phases (1098-1100), 3 plans, ~10 tasks

    **Local tag:** `v1023` (commit `$CLOSE_SHA`)
    **Public tag:** `v1.5.8` (at same commit `$CLOSE_SHA`)
    **Audit verdict:** PENDING — DEGRADED CLOSE (CI-01 deferred to v1024+; orchestrator runs /gsd:audit-milestone v1023 post-tag)

    **Degraded-close note:** v1023 ships with 7/8 requirements satisfied + CI-01 deferred to v1024+. CI-01 (live-verify of the `pytest-parallel-isolation` gate on real GitHub Actions infrastructure) was BLOCKED at close-gate time by the same GitHub Actions billing failure on the geolens-io account that blocked v1022 (run `26359374410`: 0/13 jobs executed; billing annotation persists since 2026-05-24). Per D-01d, no fresh dispatch was attempted — re-running would just re-confirm the billing block. Gate-shape verified locally via Phase 1100 Plan 1100-01's substitute evidence: 5/5 docker services healthy + `GET /api/health` returns 200 + sequential 3062/0/38 literal + `-n 4` 3062/0/38 literal + `-n auto` 3-run within v1022 PARA-01 ≤30 envelope with 0 ICN frames. v1024+ carry-forward chain: v1022 → v1023 → v1024.

    **Goal delivered:** Retired the 3 pre-existing OOS sequential failures + 3 OAuth parallel-mode flakes so the post-v1023 invariant becomes `sequential failed == 0` LITERAL (not "0 NEW failed"). Strengthened `-n 4` invariant from "0 NEW oauth flake" to literal-zero. CI-01 ships degraded mirroring v1022 precedent.

    **Key accomplishments:**

    1. **OOS-01 (Phase 1098)** — TRIM path: `backend/app/modules/catalog/maps/router.py` 1807 → 1793 LOC via private-helper docstring compression. Zero behavior change. Allowlist unchanged. SHA `23336143`.

    2. **OOS-02 (Phase 1098)** — `test_readme_signature_maps_list_intact` DELETED (the README signature-stories section it pinned was retired in commit `4a7d1a29` 2026-05-22 along with the themed-demo apparatus; restoring would be a doc-lying regression). 8 sibling tests preserved. SHA `0068aa4f`.

    3. **OOS-03 (Phase 1098)** — Behavioral SSRF-contract rewrite of `test_make_safe_client_has_event_hook` → `test_make_safe_client_blocks_private_ip_redirect`. Two-iteration fix path (Rule 1 inline): first iter still called `make_safe_client()` and tripped on global `httpx.AsyncClient` patching; iter-2 dropped the factory call entirely and tests `_revalidate_redirect(response)` directly. SHAs `431e2b54` + `9546a961` + WR-01/WR-02 polish at `77affeac`.

    4. **OAUTH-01/02/03 (Phase 1099)** — Shared root-cause fix: `client_session` fixture override (shares client's `dependency_overrides[get_db]` factory) + `_ensure_public_app_url` monkeypatch fixture (pins `settings.public_app_url` + resets `_PUBLIC_URL_CACHE`). Two-iteration: iter-1 had `client.app` attribute bug; iter-2 fixed import to `from app.api.main import app`. OAUTH-03 added mid-milestone after Phase 1098 verify-gate surfaced it. SHAs `f57f1a76` + `9922cce5`.

    5. **CI-01 (Phase 1100) — DEGRADED** — GitHub Actions billing block persistent since v1022; v1024+ carry-forward chain.

    6. **CLOSE-01 (Phase 1100)** — Atomic 5-file close commit per D-05c (v1019 TD-13 atomic-flip rule): REQUIREMENTS.md + ROADMAP.md + SUMMARY.md + CLOSE-GATE.md + CHANGELOG.md in single commit `$CLOSE_SHA`. Tags `v1023` + `v1.5.8` cut at the same SHA.

    **Test invariants at close:**

    - Sequential: 3062 passed / 0 failed / 38 skipped (LITERAL-ZERO — OOS triad retired per HARD INVARIANT D-05a)
    - `-n 4`: 3062 passed / 0 failed / 38 skipped (LITERAL-ZERO — OAuth flakes retired per HARD INVARIANT D-05b)
    - `-n auto` 3-run distinct: <T1 measurements> deterministic within v1022 PARA-01 ≤30 envelope, 0 ICN frames
    - Docker stack: 5 services healthy + `GET /api/health` returns 200 OK (no-trailing-slash per v1022 [Rule 3])

    **Migrations:** None. All v1023 changes are test-infra hygiene + minor production-code surface (`backend/app/modules/catalog/maps/router.py` -14 LOC docstring compression, zero behavior change).

    **Carry-forward to v1024+ (1 item):** CI-01-v1024 — `pytest-parallel-isolation` CI gate live-verify on real GitHub Actions infrastructure post-billing-resolution at https://github.com/organizations/geolens-io/settings/billing. Per D-01c rolling carry-forward chain: v1022 → v1023 → v1024+.

    **Patterns reinforced:**

    - **Degraded-close-with-carry-forward chain** — v1022 → v1023 → v1024+ shows external-dependency blockers (billing, third-party services) can roll forward across multiple milestones without holding the close indefinitely. Tag annotation + MILESTONES.md entry + CHANGELOG `### Notes` document the chain.
    - **Skip-the-spam dispatch policy (D-01d)** — when a CI gate is blocked by a known external constraint that's already documented, do NOT re-trigger the gate just to re-document the failure. Saves wallclock + noise.
    - **Atomic 5-file flip preserving v1019 TD-13** — close-gate commit touches exactly the 5 paths that constitute traceability (REQUIREMENTS + ROADMAP + SUMMARY + CLOSE-GATE + CHANGELOG); MILESTONES.md is a SEPARATE commit owned by T5 to keep the atomic commit shape constant.

    **Archive:** `.planning/phases/1098-oos-triad-closure/` + `.planning/phases/1099-oauth-parallel-mode-stabilization/` + `.planning/phases/1100-ci-live-verify-close-gate/` (orchestrator's /gsd:cleanup-milestone moves these to `.planning/milestones/v1023-phases/` post-audit).

    ---
    ```

    Commit MILESTONES.md as a SEPARATE atomic commit:
    `git add .planning/MILESTONES.md && git commit -m "docs(1100-01): append v1023 to MILESTONES.md (degraded-close ledger)"`

    **Step 5d — Push tags to origin (per D-02b / D-06c):**

    Attempt push:
    ```bash
    git push origin v1023 v1.5.8 2>&1 | tee /tmp/1100-tag-push.log
    ```

    Per D-06c, tag-push failure is recoverable — if push fails due to billing/auth/network, log the failure to `/tmp/1100-tag-push.log` and append a note to MILESTONES.md (in a follow-up edit) stating "Tag push deferred — see `/tmp/1100-tag-push.log` for failure mode; tags exist locally at `$CLOSE_SHA`." Do NOT block on push failure.

    Per CONTEXT.md `<specifics>`: "NO PR creation / no main-branch push beyond tag push (tag push is the only origin push in scope)." So if main branch is ahead of origin, do NOT push main — only push the two tag refs.

    NO Phase 1098/1099 SUMMARY re-touch. NO production code modification.
  </action>
  <verify>
    <automated>git rev-list -n 1 v1023 2>/dev/null | grep -q "$(cat /tmp/1100-close-sha.txt 2>/dev/null || echo 'NOSHA')" && git rev-list -n 1 v1.5.8 2>/dev/null | grep -q "$(cat /tmp/1100-close-sha.txt 2>/dev/null || echo 'NOSHA')" && git tag -l v1023 | grep -q "^v1023$" && git tag -l v1.5.8 | grep -q "^v1.5.8$" && grep -q "^## v1023 CI Live-Verify + OOS Hygiene Tail" .planning/MILESTONES.md && grep -q "Local tag:.*v1023" .planning/MILESTONES.md && grep -q "Public tag:.*v1\\.5\\.8" .planning/MILESTONES.md && git log -2 --oneline | head -2 | grep -q "MILESTONES" && echo "T5 tags + MILESTONES.md committed"</automated>
  </verify>
  <done>Tags `v1023` + `v1.5.8` exist locally and point at the T4 close-gate commit SHA. MILESTONES.md has new v1023 entry at top with shipped date + tags + baselines + degraded-close annotation + v1024+ carry-forward + patterns reinforced. MILESTONES.md committed in a SEPARATE follow-up commit (not in the T4 atomic-5-file commit). Tag-push attempted; if failed, deferred per D-06c with failure log preserved at `/tmp/1100-tag-push.log`. NO Phase 1098/1099 SUMMARY re-touch.</done>
</task>

</tasks>

<verification>
Phase-level checks after all 5 tasks complete:

1. **Sequential + `-n 4` literal-zero (D-04a/b / D-05a/b HARD INVARIANT):**
   ```bash
   grep -q "3062 passed.*0 failed.*38 skipped\|3062 passed, 38 skipped" /tmp/1100-verify-seq.log
   grep -q "3062 passed.*38 skipped" /tmp/1100-verify-n4.log
   # Both must return 0 (success)
   ```

2. **`-n auto` PARA-01 envelope preserved (D-04c):**
   ```bash
   # Check zero OOS/OAUTH pin names in any failure list across all 3 auto runs
   grep -E "test_router_orchestrator_modules_stay_within_loc_cap|test_readme_signature_maps_list_intact|test_make_safe_client_has_event_hook|test_make_safe_client_blocks_private_ip_redirect|test_callback_missing_state_returns_error|test_callback_invalid_code_returns_error|test_oauth_login_redirect" /tmp/1100-verify-auto-*.log | grep -i fail | wc -l
   # Must return 0
   ```

3. **Docker stack + /api/health (D-04d/e):**
   ```bash
   grep -c "(healthy)" /tmp/1100-docker-ps.txt  # Must be >= 5
   grep -q "^200$" /tmp/1100-curl-health.txt    # Must return 0
   ```

4. **Atomic 5-file flip (D-05c):**
   ```bash
   git log --format=%H | head -2 | tail -1 > /tmp/1100-t4-sha.txt  # T4 SHA = second-most-recent commit (T5 MILESTONES is most recent)
   git show --name-only --format= $(cat /tmp/1100-t4-sha.txt) | sort > /tmp/1100-t4-actual.txt
   echo -e ".planning/REQUIREMENTS.md\n.planning/ROADMAP.md\n.planning/phases/1100-ci-live-verify-close-gate/1100-01-SUMMARY.md\n.planning/phases/1100-ci-live-verify-close-gate/1100-CLOSE-GATE.md\nCHANGELOG.md" | sort > /tmp/1100-t4-expected.txt
   diff /tmp/1100-t4-actual.txt /tmp/1100-t4-expected.txt  # Must be empty (no diff = atomic flip correct)
   ```

5. **Tags at T4 commit (D-02b / D-05d):**
   ```bash
   git rev-list -n 1 v1023 = git rev-list -n 1 v1.5.8 = T4 close-gate SHA
   ```

6. **No production code modified beyond Phase 1098 baseline (D-06d):**
   ```bash
   # Compare against Phase 1099 close SHA 1314ba5f (the v1023 starting state for Phase 1100)
   git diff 1314ba5f...HEAD -- backend/app/  # Must be empty (Phase 1100 touched zero production code)
   ```

7. **No Phase 1098/1099 SUMMARY re-touch (CONTEXT.md guard-rails):**
   ```bash
   git diff 1314ba5f...HEAD -- .planning/phases/1098-oos-triad-closure/ .planning/phases/1099-oauth-parallel-mode-stabilization/
   # Must be empty
   ```

8. **CHANGELOG [1.5.8] block well-formed (D-02a):**
   ```bash
   grep -c "^## \[1.5.8\] - 2026-05-24" CHANGELOG.md  # Must be 1
   grep -c "^## \[1.5.7\] - 2026-05-24" CHANGELOG.md  # Must still be 1 (untouched)
   grep -q "OOS-01\|OOS-02\|OOS-03\|OAUTH-01\|OAUTH-02\|OAUTH-03\|CI-01.*degraded\|CLOSE-01" CHANGELOG.md
   ```

9. **REQUIREMENTS.md + ROADMAP.md flips correct (D-05c):**
   ```bash
   grep "CI-01 | Phase 1100 | Complete (degraded)" .planning/REQUIREMENTS.md
   grep "CLOSE-01 | Phase 1100 | Complete |" .planning/REQUIREMENTS.md
   grep "\[x\] \*\*Phase 1100" .planning/ROADMAP.md
   grep "1100. CI Live-Verify + Close Gate | v1023 | 1/1 | Shipped | 2026-05-24" .planning/ROADMAP.md
   ```

10. **MILESTONES.md v1023 entry at top (D-02c):**
    ```bash
    head -5 .planning/MILESTONES.md | grep -q "^## v1023 CI Live-Verify"
    ```
</verification>

<success_criteria>
- [ ] T1: All 7 verify-gate evidence files exist + sequential/`-n 4` literal-zero + `-n auto` PARA-01 envelope preserved + zero OOS/OAUTH pin names in any auto failure list + 5/5 docker healthy + `/api/health` 200
- [ ] T2: 1100-CLOSE-GATE.md ≥80 lines with sections (a)-(g) populated, baselines verbatim, CI-01 degraded rationale + billing-block URL + v1022 precedent cross-referenced
- [ ] T3: CHANGELOG.md `[1.5.8]` block above `[1.5.7]` (untouched) with 7 closure entries (CI-01 degraded + OOS-01/02/03 + OAUTH-01/02/03 + CLOSE-01) + baselines section + v1024+ carry-forward note
- [ ] T4: Atomic close commit touches EXACTLY 5 paths (REQUIREMENTS + ROADMAP + SUMMARY + CLOSE-GATE + CHANGELOG); REQUIREMENTS shows `Complete (degraded)` for CI-01 + `Complete` for CLOSE-01; ROADMAP shows Phase 1100 `[x]` + Progress row `1/1 / Shipped / 2026-05-24`
- [ ] T5: Tags `v1023` (local) + `v1.5.8` (public) cut at T4 SHA with verify-gate baselines in annotation; MILESTONES.md v1023 entry committed separately; tag push attempted (success OR deferred per D-06c)
- [ ] HARD INVARIANTS: Sequential `failed == 0` literal (D-05a) + `-n 4` `failed == 0` literal (D-05b) + atomic 5-file flip (D-05c) + tags at T4 SHA (D-05d)
- [ ] Out-of-scope guard-rails: No Phase 1098/1099 SUMMARY re-touch + no production-code change since Phase 1099 SHA `1314ba5f` + no PR creation + no main-branch push beyond tags
</success_criteria>

<output>
After T5 completes, write `.planning/phases/1100-ci-live-verify-close-gate/1100-01-SUMMARY.md` (this was done as part of T4's atomic 5-file commit per D-05c) — the file already exists at that path with the close summary.

The orchestrator (`/gsd-autonomous`) consumes this plan via `/gsd:execute-phase`. Post-execution:
- v1023 is shipped with degraded CI-01 + full CLOSE-01
- Tags `v1023` + `v1.5.8` annotate the close-gate SHA
- CHANGELOG `[1.5.8]` records per-requirement evidence
- MILESTONES.md tracks the v1024+ CI-01 carry-forward
- Orchestrator runs `/gsd:audit-milestone v1023` next to verify the degraded-close shape
</output>
