# Phase 1111 Plan 01: Builder Lint Closeout

## Goal

Make frontend lint clean after the mapbuilder QA/polish changes and prove the target map still loads cleanly.

## Tasks

- [x] Keep composite stack rows role-free while documenting the lint exception with Phase 1111 context.
- [x] Remove redundant native roles and stale lint-disable comments.
- [x] Fix the render-perf test side effect by moving captured handler assignment into an effect.
- [x] Fix hook dependency warnings by depending on specific stable handlers and static module constants.
- [x] Rerun focused tests, typecheck, lint, and a Playwright MCP smoke of the target map.

## Acceptance

- `npm run lint` exits with zero errors and zero warnings.
- `npm run typecheck` passes.
- Focused frontend tests pass.
- Playwright MCP confirms the target map loads with expected layer rows and no console warnings/errors after the lint patches.
