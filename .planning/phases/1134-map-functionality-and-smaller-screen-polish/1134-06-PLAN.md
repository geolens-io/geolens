---
phase: 1134-map-functionality-and-smaller-screen-polish
plan: 06
type: execute
wave: 2
depends_on:
  - 1134-01
  - 1134-02
  - 1134-03
  - 1134-04
  - 1134-05
files_modified:
  - .planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-06-MCP-VERIFY.md
autonomous: false
requirements:
  - MAP-07
  - MAP-08
  - MAP-09
  - MAP-10
  - MAP-16
  - MAP-17
  - MAP-18
  - MAP-19
  - MAP-20
  - MAP-22
tags:
  - playwright-mcp
  - live-verify
  - smaller-screen

must_haves:
  truths:
    - "At 1440×900, 800×600, and 414×896 viewports on canonical ADK map c39be324, every visible builder surface from Plans 01-05 behaves per UI-SPEC: visibility-toggle round-trip works for at least one layer per render mode; delete-layer leaves no orphan; rename-group focuses input; SheetContent shows single X; coord readout doesn't overlap chrome; filter pills don't collide; notes dot appears when notes added"
    - "Zero browser console errors across all three viewports during the smoke run"
    - "MCP report written to 1134-06-MCP-VERIFY.md with per-viewport screenshots + pass/fail per MAP requirement"
  artifacts:
    - path: ".planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-06-MCP-VERIFY.md"
      provides: "Live MCP smoke report — 10 MAP requirements × 3 viewports verified or flagged"
  key_links:
    - from: "Live ADK map"
      to: "Phase 1134 source changes"
      via: "MCP browser inspection at all 3 viewports"
      pattern: "1134-06-MCP-VERIFY"
---

<objective>
Live Playwright MCP smoke verification of all Phase 1134 work at the 3 ROADMAP-named viewports (1440×900 / 800×600 / 414×896 — same viewports as the Phase 1139 close-gate). Produces `1134-06-MCP-VERIFY.md` documenting per-viewport pass/fail per MAP requirement.

Purpose: Vitest unit/integration tests pin contracts but cannot catch real-browser layout collisions or wheel-event scroll containment. This plan exercises the canonical ADK map (`c39be324-6815-40e5-8143-00a2723827b2`) at the three viewports the Phase 1139 close-gate also uses. Pre-emptive verification at the phase-close boundary (not deferring to Phase 1139) caught WALK-SS-04 / WALK-SS-05 in Phase 1133 and matches v1011 MCP rigor.

Output:
- `1134-06-MCP-VERIFY.md` — a per-REQ × per-viewport pass/fail table with MCP-inspected evidence.
- Inline fixes for any P0/P1 findings discovered during the smoke (per v1011 pattern: fix-in-checkpoint rather than carry-forward to v1031).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-CONTEXT.md
@.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-UI-SPEC.md
@.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-01-SUMMARY.md
@.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-02-SUMMARY.md
@.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-03-SUMMARY.md
@.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-04-SUMMARY.md
@.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-05-SUMMARY.md
@.planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md

<interfaces>
<!-- MCP test surface map -->

Canonical ADK map: `c39be324-6815-40e5-8143-00a2723827b2`
Stack: `http://localhost:8080` (Vite dev proxy)
Login: admin / admin

Per-viewport workflow:
1. Resize browser to target dimensions via `mcp__playwright__browser_resize`.
2. Navigate to `/builder/c39be324-6815-40e5-8143-00a2723827b2`.
3. For each MAP requirement, perform the action listed in the contract and snapshot/inspect to verify expected DOM/visual state.

Viewports:
- 1440×900 — desktop baseline (full sidebar + editor)
- 800×600 — small-laptop (mobile-rail kicks in at <800px; this is at the boundary)
- 414×896 — phone portrait (editor-hidden, mobile-rail Sheet wrappers)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Playwright MCP live smoke at 1440×900, 800×600, 414×896</name>
  <files>.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-06-MCP-VERIFY.md</files>
  <what-built>
Plans 01-05 shipped: adapter regression pins (Plan 01), delete-layer adapter-driven sweep (Plan 02), rename-focus pins (Plan 03), ≤800px Sheet offset + filter chip overflow + coord readout pin + exhaustive SheetContent grep guard (Plan 04), Notes presence dot + MAP-19 scroll-containment pin (Plan 05).
  </what-built>
  <action>Run the live MCP smoke per the matrix in <how-to-verify>; record findings in `1134-06-MCP-VERIFY.md`. Apply inline fixes for any P0/P1 finding (v1011 pattern). File P2 findings as v1031 carry-forward in REQUIREMENTS.md Future Requirements.</action>
  <how-to-verify>
**Before starting:** Run `docker compose ps` to confirm `api`, `worker`, `frontend`, `db`, `titiler` are healthy. If not, `docker compose up -d` and wait 30s.

**For each of the 3 viewports (1440×900, 800×600, 414×896):**

1. `mcp__playwright__browser_resize` to the viewport.
2. `mcp__playwright__browser_navigate` to `http://localhost:8080/builder/c39be324-6815-40e5-8143-00a2723827b2` (sign in if prompted: admin/admin).
3. Wait for tiles loaded — `mcp__playwright__browser_snapshot` to confirm canvas rendered.
4. Run the MAP requirement matrix below; record PASS / FAIL / N/A per row.

**MAP requirement matrix (run at every viewport):**

| REQ | Action | Expected |
|-----|--------|----------|
| MAP-07 | Open the right-sidebar layer panel (or layer-editor flyout at ≤800px) | Sheet's top edge does NOT visually overlap the top-left NavigationControl. At 800/414, Sheet has the new `mt-12` offset; at 1440 Sheet is in column layout (not Sheet) so PASS by construction. |
| MAP-08 | Hover the map canvas, then check the top-right coord readout pill | Pill has `right-14` (56px right offset) — inspect via DOM snapshot. Pill text matches "{lat}° N · {lng}° E · z {zoom}". |
| MAP-09 | Open the basemap sheet (layer panel → Basemap section → expand) | Exactly 1 close button (Sheet auto-X suppressed; inner panel's X visible). |
| MAP-10 | For EVERY Sheet that opens in the builder, snapshot button accessible name | All Sheets render exactly 1 visible button with name matching `/close/i` (the inner panel's X). |
| MAP-16 | Click kebab on a folder group → "Rename group" | Input element receives focus on first paint (cursor blink visible in screenshot or `document.activeElement === input`). |
| MAP-17 | For each render mode visible on the ADK map (fill, line, circle, raster), click the kebab → Delete | Layer disappears from stack AND from canvas immediately. No orphan layer in `map.getStyle().layers` (use MCP console eval). |
| MAP-18 | For each render mode, toggle visibility off then on via eye icon | Canvas reflects change within ≤1 second; no console errors. Filter sync test: set a filter on a circle layer, then change opacity — filter still applies. |
| MAP-19 | Position mouse over canvas, wheel-scroll vigorously | Page body `scrollY` (read via MCP `mcp__playwright__browser_evaluate(() => window.scrollY)`) stays 0. Map zoom changes. |
| MAP-20 | Add filters to 3+ visible layers so chip column populates the top-left | Chip column does NOT visually overlap the bottom-left measurement widget at 800×600. Hover scroll on the chip container — chip column scrolls internally (max-h-[40vh] is respected). |
| MAP-22 | Open Notes panel; type "Test note"; close panel | Notes rail button shows the 6px primary-color dot at top-right. Clear notes → dot disappears. aria-label says "Map has notes". |

**Pass/fail per row + per viewport.** Capture screenshots for any FAIL.

**Console error check** (run once per viewport): `mcp__playwright__browser_console_messages` — assert 0 ERROR-level messages from `/ai/*` or `/api/maps/*` paths. WARN-level OK.

5. Write findings to `.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-06-MCP-VERIFY.md` using this skeleton:

```
# Phase 1134 — Live MCP Smoke Verification

**Run date:** {date}
**Stack:** localhost:8080 (admin/admin)
**Canonical map:** c39be324-6815-40e5-8143-00a2723827b2

## Per-viewport Results

### 1440×900 (desktop baseline)
| REQ | Status | Evidence | Notes |
|-----|--------|----------|-------|
| MAP-07 | PASS/FAIL | screenshot path | ... |

### 800×600 (small-laptop boundary)
...

### 414×896 (phone portrait)
...

## Console Error Audit
| Viewport | ERROR count | WARN count | Notes |
|----------|-------------|------------|-------|
| 1440×900 | 0 | ... | |

## Findings (P0/P1/P2 if any)
| Finding | REQ | Severity | Disposition |
|---------|-----|----------|-------------|
| ... | ... | P1 | fix inline / file as v1031 carry-forward |

## Sign-off
- [ ] 10/10 MAP requirements PASS at 1440×900
- [ ] 10/10 MAP requirements PASS at 800×600
- [ ] 10/10 MAP requirements PASS at 414×896
- [ ] 0 console errors across all 3 viewports
- [ ] Inline fixes applied for any P0/P1 (commit hash references)
```

**Fix-in-checkpoint policy:** Findings classified P0 or P1 MUST be fixed inline (v1011 pattern). P2/cosmetic findings may be filed as v1031 carry-forward in REQUIREMENTS.md Future Requirements. Do not advance to phase close with any P0/P1 unresolved.
  </how-to-verify>
  <verify>
    <human-check>Operator runs the MCP matrix at all 3 viewports and signs off the table in 1134-06-MCP-VERIFY.md. All 30 cells PASS or are documented with disposition.</human-check>
  </verify>
  <done>1134-06-MCP-VERIFY.md exists with all 30 cells filled in; sign-off checkboxes checked; any inline fixes committed.</done>
  <resume-signal>Type "approved" if all 30 cells (10 REQs × 3 viewports) PASS, all inline fixes commit-applied, and 0 console errors. Type "needs fix: {REQ-XX}" if a P0/P1 finding requires re-running a prior plan. Type "v1031: {reason}" if a P2 finding is being filed as carry-forward.</resume-signal>
</task>

</tasks>

<verification>
- `1134-06-MCP-VERIFY.md` exists with all 30 cells (10 MAP requirements × 3 viewports) filled in.
- Sign-off checkboxes all checked.
- Any P0/P1 findings have linked commit hashes for the inline fixes.
- Console-error audit shows 0 ERROR-level messages per viewport.
</verification>

<success_criteria>
- ROADMAP success criterion 5 satisfied: live verification across the 3 ROADMAP-named viewports.
- All 10 MAP requirements verified on real browser at small-screen viewports.
- Phase 1134 close-gate cleared before Phase 1135 begins (Phase 1135 hard dependency per HARD INVARIANT #2).
</success_criteria>

<output>
The output of this plan IS `1134-06-MCP-VERIFY.md`. After completion, also create `.planning/phases/1134-map-functionality-and-smaller-screen-polish/1134-06-SUMMARY.md` documenting:
- Run dates + viewports tested.
- PASS counts per viewport.
- Any inline fixes applied (commit hashes).
- Any v1031 carry-forwards filed (with REQUIREMENTS.md Future Requirements row references).
</output>
