---
phase: 1008-sidebar-view-model-and-renderas-foundation
plan: 01
wave: 1
depends_on: []
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03, ARCH-04, RENDER-01]
subsystem: frontend-builder
tags: [map-builder, sidebar, renderas, view-model, tests]
files_modified:
  - frontend/src/components/builder/renderAs.ts
  - frontend/src/components/builder/__tests__/renderAs.test.ts
  - .planning/phases/1008-sidebar-view-model-and-renderas-foundation/1008-01-SUMMARY.md
---

<objective>
Create the pure renderAs/view-model foundation for v1002 so later sidebar and Add Dataset work can derive supported rendering options without changing persisted schemas or exposing unsupported renderers.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1008-sidebar-view-model-and-renderas-foundation/1008-CONTEXT.md
@frontend/src/types/api.ts
@frontend/src/components/builder/map-stack.ts
@frontend/src/components/builder/layer-adapters/shared.ts
@frontend/src/components/builder/LayerStyleEditor.tsx
@frontend/src/components/builder/RasterLayerControls.tsx
</context>

<tasks>
  <task type="auto">
    <name>Add pure renderAs model utility</name>
    <files>frontend/src/components/builder/renderAs.ts</files>
    <action>Create a pure TypeScript module exporting stable renderAs ids/options, source classification, supported option derivation, and current-renderAs resolution for `MapLayerResponse`-like objects. Keep it free of React, network calls, persistence writes, and MapLibre map instance access.</action>
    <verify>TypeScript compiles and the module can be imported by tests without DOM setup.</verify>
    <done>RENDER-01 has a reusable utility surface for later UI phases.</done>
  </task>

  <task type="auto">
    <name>Encode v1002 renderer boundaries</name>
    <files>frontend/src/components/builder/renderAs.ts</files>
    <action>Limit options to Point/Symbol/Heatmap, Line, Fill/Stroke/Fill+Stroke/3D extrusion, Image, and Hillshade. Export an unsupported/punted id list or tests that prove Cluster, Hexbin, H3, Arrow, Animated path, Point 3D extrusion, timeline, recipes, cross-layer filters, and blend mode are not exposed.</action>
    <verify>No unsupported option appears in the returned option ids for any layer fixture.</verify>
    <done>ARCH-04 is reflected in code, not just prose.</done>
  </task>

  <task type="auto">
    <name>Add focused renderAs tests</name>
    <files>frontend/src/components/builder/__tests__/renderAs.test.ts</files>
    <action>Add Vitest coverage for point, line, polygon, raster, raster DEM, current renderAs detection from `style_config.render_mode`, polygon builder extrusion detection, and unsupported renderer omissions. Include a regression assertion that the utility does not produce writable `is_3d` data.</action>
    <verify>`cd frontend && npm run test -- renderAs --run` passes.</verify>
    <done>ARCH-01..04 and RENDER-01 have an executable foundation gate.</done>
  </task>

  <task type="auto">
    <name>Record summary and verification</name>
    <files>.planning/phases/1008-sidebar-view-model-and-renderas-foundation/1008-01-SUMMARY.md</files>
    <action>Document what landed, the requirement IDs covered, and the focused verification commands/results.</action>
    <verify>Summary exists and accurately reflects commands run.</verify>
    <done>Phase 1008 can hand off to Phase 1009 without rediscovering boundaries.</done>
  </task>
</tasks>

<verification>
- `cd frontend && npm run test -- renderAs --run`
- `cd frontend && npm run lint`
</verification>

<must_haves>
- No database migration, API schema edit, or persisted shape change.
- No UI dependency or component library change.
- No unsupported renderer is exposed by the utility.
- `is_3d` is treated only as input metadata and never as writable output.
- Tests are focused and do not require a running backend or browser.
</must_haves>

<success_criteria>
- [ ] `renderAs.ts` exports stable types/helpers for later phases.
- [ ] Supported renderAs options match the v1002 requirements exactly.
- [ ] Unsupported renderer omissions are covered by tests.
- [ ] Focused Vitest passes.
- [ ] Summary artifact records implementation and verification.
</success_criteria>
