---
phase: 224-post-impl-audit-remediation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/datasets/schemas.py
  - backend/app/records/schemas.py
  - backend/app/auth/schemas.py
  - backend/app/datasets/models.py
  - backend/app/maps/schemas.py
  - backend/app/maps/models.py
  - backend/app/maps/router.py
  - backend/app/persistent_config.py
  - backend/alembic/versions/
autonomous: true
requirements:
  - AUDIT-P1-6
  - AUDIT-P1-7
  - AUDIT-P1-8
  - AUDIT-P1-9
  - AUDIT-P1-10
  - AUDIT-P1-11
  - AUDIT-P1-12

must_haves:
  truths:
    - "Pydantic max_length on DatasetMeta, Record*, and UserCreate fields matches SQL column widths"
    - "MapVisibility enum includes 'unlisted' in Pydantic, matching the SQL CHECK constraint"
    - "MapLayerInput.layer_type is typed as Literal with only valid CHECK-constraint values"
    - "share_url in all 3 share token handlers is absolute (starts with http)"
    - "PersistentConfig uses parameterized generics for BASEMAPS, MAP_DEFAULTS, ENABLED_WIDGETS, ROLE_PERMISSIONS"
  artifacts:
    - path: "backend/alembic/versions/"
      provides: "Single migration tightening VARCHAR constraints for P1-6/7/8 fields"
      contains: "alter_column"
    - path: "backend/app/maps/schemas.py"
      provides: "MapVisibility with unlisted + MapLayerInput with Literal layer_type"
      contains: "unlisted"
    - path: "backend/app/maps/router.py"
      provides: "Absolute share_url via get_public_app_url"
      contains: "get_public_app_url"
    - path: "backend/app/persistent_config.py"
      provides: "Parameterized PersistentConfig generics for 4 config keys"
      contains: "BasemapEntry"
  key_links:
    - from: "backend/app/maps/router.py"
      to: "backend/app/public_urls.py"
      via: "get_public_app_url import for absolute share_url"
      pattern: "get_public_app_url"
    - from: "backend/app/persistent_config.py"
      to: "backend/app/settings/schemas.py"
      via: "BasemapEntry and MapDefaultsResponse imports for typed config"
      pattern: "BasemapEntry|MapDefaultsResponse"
---

<objective>
Resolve all backend type safety audit findings: P1-6 (DatasetMeta max_length drift), P1-7 (Record* column-width mismatches), P1-8 (UserCreate.email max_length), P1-9 (MapVisibility enum drift), P1-10 (MapLayerInput.layer_type bare str), P1-11 (share_url relative paths), and P1-12 (PersistentConfig bare generics).

Purpose: These 7 items prevent Pydantic from catching invalid input before it hits the database, causing 500/IntegrityError on user input and silent data corruption on config reads.
Output: 4 atomic commits (migration, MapVisibility+layer_type+share_url, PersistentConfig, schema tightening). All backend tests pass after each commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/224-post-impl-audit-remediation/224-CONTEXT.md
@.planning/phases/224-post-impl-audit-remediation/224-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/app/datasets/schemas.py (lines 250-355, DatasetMeta):
- update_frequency: str | None = Field(None, max_length=1000) # SQL column: VARCHAR(30)
- record_status: str | None = Field(None, max_length=1000) # SQL column: VARCHAR(20)
- sensitivity_classification: str | None = Field(None, max_length=1000) # SQL column: VARCHAR(20)
- language: str | None = Field(None) # SQL column: VARCHAR(10)

From backend/app/records/schemas.py:
- RecordContact.role: str | None = Field(None, max_length=100) # SQL: VARCHAR(30), has CHECK constraint with Literal values
- RecordKeyword.keyword_type: str | None = Field(None, max_length=100) # SQL: VARCHAR(20), has CHECK constraint
- RecordDistribution.distribution_type: str | None = Field(None, max_length=200) # SQL: VARCHAR(30)
- RecordDistribution.format: str | None = Field(None, max_length=200) # SQL: VARCHAR(50)
- RecordDistribution.media_type: str | None = Field(None, max_length=255) # SQL: VARCHAR(100)

From backend/app/auth/schemas.py:
- UserCreate.email: EmailStr = Field(..., max_length=320) # SQL: String(255)

From backend/app/maps/schemas.py:
- MapVisibility: str Enum with 3 values (private, internal, public) — SQL CHECK has 4 (adds unlisted)
- MapLayerInput.layer_type: str | None — SQL CHECK: ('vector_geolens', 'raster_geolens', 'geojson')

From backend/app/maps/router.py:
- Line 481: share_url=f"/m/{token}" (relative)
- Line 523: share_url=f"/m/{token}" (relative)
- Line 563: share_url=f"/m/{token}" (relative)

From backend/app/persistent_config.py:
- Line 603: PersistentConfig[list] — should be PersistentConfig[list[BasemapEntry]]
- Line 611: PersistentConfig[dict] — should be PersistentConfig[MapDefaultsResponse]
- Line 621: PersistentConfig[list] — should be PersistentConfig[list[str] | None]
- Line 638: PersistentConfig[dict] — should be PersistentConfig[dict[str, list[str]]]

From backend/app/public_urls.py:
- get_public_app_url(db, request=request) -> str — returns absolute URL like "https://myapp.example.com"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: P1-6/P1-7/P1-8 — Tighten Pydantic max_length + single Alembic migration</name>
  <files>backend/app/datasets/schemas.py, backend/app/records/schemas.py, backend/app/auth/schemas.py, backend/alembic/versions/</files>
  <read_first>
    - backend/app/datasets/schemas.py (lines 250-360 for DatasetMeta fields)
    - backend/app/records/schemas.py (lines 10-115 for RecordContact, RecordKeyword, RecordDistribution)
    - backend/app/auth/schemas.py (lines 15-25 for UserCreate/UserUpdate email)
    - backend/app/datasets/models.py (lines 265-350 for actual SQL column definitions and CHECK constraints)
  </read_first>
  <action>
**Schema changes (3 files):**

1. In `backend/app/datasets/schemas.py`, update `DatasetMeta` fields:
   - `update_frequency`: change `max_length=1000` to `max_length=30`
   - `record_status`: change `max_length=1000` to `max_length=20`
   - `sensitivity_classification`: change `max_length=1000` to `max_length=20`
   - `language`: add `max_length=10` (currently unbounded)

2. In `backend/app/records/schemas.py`, update these fields to match their SQL VARCHAR widths:
   - `RecordContact.role`: change `max_length=100` to `max_length=30`. If a CHECK constraint exists with specific allowed values, also change the type to `Literal["pointOfContact", "custodian", "owner", "distributor", "originator", "author", "publisher", "processor", "principalInvestigator"] | None` (check `datasets/models.py` for the exact CHECK values).
   - `RecordKeyword.keyword_type`: change `max_length=100` to `max_length=20`. If CHECK constraint exists, add `Literal[...]` matching CHECK values.
   - `RecordDistribution.distribution_type`: change `max_length=200` to `max_length=30`
   - `RecordDistribution.format`: change `max_length=200` to `max_length=50`
   - `RecordDistribution.media_type`: change `max_length=255` to `max_length=100`

3. In `backend/app/auth/schemas.py`, update:
   - `UserCreate.email`: change `max_length=320` to `max_length=255`
   - `UserUpdate.email` (if present): same change to `max_length=255`

**Single Alembic migration (per locked decision #3):**

4. Generate a new Alembic migration:
   ```bash
   cd backend && uv run alembic revision --autogenerate -m "tighten pydantic varchar constraints p1-6 p1-7 p1-8"
   ```
   The migration should contain `ALTER COLUMN ... TYPE VARCHAR(N)` for each tightened column. Review the auto-generated migration and ensure:
   - `update_frequency` VARCHAR(30)
   - `record_status` VARCHAR(20)
   - `sensitivity_classification` VARCHAR(20)
   - `language` VARCHAR(10)
   - `RecordContact.role` VARCHAR(30)
   - `RecordKeyword.keyword_type` VARCHAR(20)
   - `RecordDistribution.distribution_type` VARCHAR(30)
   - `RecordDistribution.format` VARCHAR(50)
   - `RecordDistribution.media_type` VARCHAR(100)
   - `User.email` VARCHAR(255)

   If autogenerate does not pick up changes (Pydantic max_length is not the same as SQLAlchemy column type), write the migration manually using `op.alter_column()`. Note: only write migration ALTER statements for columns whose SQL model `String(N)` is WIDER than the new Pydantic limit; if the SQL column already matches, skip the ALTER for that column.

5. Verify the migration applies cleanly:
   ```bash
   cd backend && uv run alembic upgrade head
   ```
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && uv run alembic upgrade head && uv run pytest tests/ -k "dataset or record or auth" -x --timeout=30 -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "max_length=30" backend/app/datasets/schemas.py` returns match for update_frequency
    - `grep -n "max_length=20" backend/app/datasets/schemas.py` returns matches for record_status and sensitivity_classification
    - `grep -n "max_length=10" backend/app/datasets/schemas.py` returns match for language
    - `grep -n "max_length=255" backend/app/auth/schemas.py` returns match for email
    - `grep -n "max_length=30" backend/app/records/schemas.py` returns match for role and distribution_type
    - New Alembic migration file exists in backend/alembic/versions/
  </acceptance_criteria>
  <done>All Pydantic max_length values match SQL column widths. Single Alembic migration applies cleanly. Invalid-length input now rejected at Pydantic validation, not at DB IntegrityError.</done>
</task>

<task type="auto">
  <name>Task 2: P1-9/P1-10 — Reconcile MapVisibility enum + type MapLayerInput.layer_type as Literal</name>
  <files>backend/app/maps/schemas.py, backend/app/maps/models.py</files>
  <read_first>
    - backend/app/maps/schemas.py (lines 1-50 for MapVisibility enum and MapLayerInput)
    - backend/app/maps/models.py (lines 20-30 for CHECK constraint on visibility, and layer_type CHECK)
  </read_first>
  <action>
**MapVisibility direction decision: ADD `unlisted` everywhere** (per Research recommendation and Claude's discretion — additive path, no DROP needed, aligns with CHECK constraint as source of truth).

1. In `backend/app/maps/schemas.py`, add `unlisted` to `MapVisibility` enum:
   ```python
   class MapVisibility(str, Enum):
       private = "private"
       internal = "internal"
       public = "public"
       unlisted = "unlisted"
   ```

2. In `backend/app/maps/schemas.py`, change `MapLayerInput.layer_type` from `str | None` to:
   ```python
   from typing import Literal

   layer_type: Literal["vector_geolens", "raster_geolens", "geojson"] | None = Field(
       default=None,
       description="Auto-detected from record_type if omitted",
   )
   ```

3. Verify `backend/app/maps/models.py` CHECK constraint already includes `unlisted` — no model change needed if it does. If the model uses a Python Enum class for the column type, ensure it also has `unlisted`.

Note: The frontend side of P1-9 (adding `unlisted` to `MapVisibility` in `types/api.ts`) is handled in Plan 224-04.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && uv run pytest tests/ -k "maps" -x --timeout=30 -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "unlisted" backend/app/maps/schemas.py` returns at least 1 match (in MapVisibility enum)
    - `grep -n 'Literal\[' backend/app/maps/schemas.py` returns at least 1 match (for layer_type)
    - `grep -n "vector_geolens.*raster_geolens.*geojson" backend/app/maps/schemas.py` returns 1 match (the Literal union)
  </acceptance_criteria>
  <done>MapVisibility enum has 4 values matching CHECK constraint. MapLayerInput.layer_type uses Literal validation. Invalid layer_type values now rejected by Pydantic. Maps tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: P1-11 — Fix share_url to be absolute using get_public_app_url</name>
  <files>backend/app/maps/router.py</files>
  <read_first>
    - backend/app/maps/router.py (lines 475-570 for the 3 share token handler sites)
    - backend/app/public_urls.py (lines 240-260 for get_public_app_url signature)
  </read_first>
  <action>
1. In `backend/app/maps/router.py`, add import at the top:
   ```python
   from app.public_urls import get_public_app_url
   ```
   (Check if this import already exists — it may not be present in maps/router.py yet.)

2. At each of the 3 sites that construct share_url:

   **Site 1 (~line 481):** Find `share_url=f"/m/{token}"` (or similar relative path). Replace with:
   ```python
   public_url = await get_public_app_url(db, request=request)
   share_url = f"{public_url}/m/{token_value}"
   ```
   Where `token_value` is the actual token string variable used at that site.

   **Site 2 (~line 523):** Same replacement pattern.

   **Site 3 (~line 563):** Same replacement pattern.

3. Ensure `request` is available in each handler's signature (it should be, as a FastAPI dependency). If `db` is named differently (e.g., `session`), use the correct variable name.

4. If `get_public_app_url` is async, use `await`. Check the function signature — it takes `(db: AsyncSession, request: Request | None = None)` and returns a string.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && uv run pytest tests/ -k "maps" -x --timeout=30 -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "get_public_app_url" backend/app/maps/router.py` returns at least 1 import + 3 usage sites
    - `grep -c 'f"/m/' backend/app/maps/router.py` returns 0 (no relative share_url remains)
    - `grep -n "share_url" backend/app/maps/router.py` shows all 3 sites constructing absolute URLs
  </acceptance_criteria>
  <done>All 3 share_url construction sites in maps/router.py use get_public_app_url for absolute URLs. Frontend share/copy actions now receive correct full URLs. Maps tests pass.</done>
</task>

<task type="auto">
  <name>Task 4: P1-12 — Parameterize PersistentConfig bare generics for 4 config keys</name>
  <files>backend/app/persistent_config.py</files>
  <read_first>
    - backend/app/persistent_config.py (lines 595-645 for the 4 bare generic PersistentConfig declarations)
    - backend/app/settings/schemas.py (for BasemapEntry and MapDefaultsResponse class definitions — needed for import)
  </read_first>
  <action>
1. In `backend/app/persistent_config.py`, add the necessary imports. To avoid circular imports, use a local import inside the module-level declaration block or use `TYPE_CHECKING`:

   Check the existing import structure first. If `persistent_config.py` is imported by `settings/schemas.py`, a top-level import will create a circular dependency. In that case, use `from __future__ import annotations` (already present if used elsewhere) and pass the type as a string to the generic subscript, while using a local import for the `type_=` runtime argument.

   Most likely safe approach — local import at declaration time:
   ```python
   # At the BASEMAPS declaration (~line 603):
   from app.settings.schemas import BasemapEntry
   BASEMAPS = PersistentConfig[list[BasemapEntry]](
       key="basemaps",
       type_=list[BasemapEntry],
       ...
   )
   ```

   If that causes circular import, wrap in a function or use lazy initialization.

2. Update the 4 declarations:

   **BASEMAPS (~line 603):**
   ```python
   # BEFORE: PersistentConfig[list](key="basemaps", type_=list, ...)
   # AFTER:
   PersistentConfig[list[BasemapEntry]](key="basemaps", type_=list[BasemapEntry], ...)
   ```

   **MAP_DEFAULTS (~line 611):**
   ```python
   # BEFORE: PersistentConfig[dict](key="map_defaults", type_=dict, ...)
   # AFTER:
   PersistentConfig[MapDefaultsResponse](key="map_defaults", type_=MapDefaultsResponse, ...)
   ```

   **ENABLED_WIDGETS (~line 621):**
   ```python
   # BEFORE: PersistentConfig[list](key="enabled_widgets", type_=list, ...)
   # AFTER:
   PersistentConfig[list[str] | None](key="enabled_widgets", type_=list[str] | None, ...)
   ```

   **ROLE_PERMISSIONS (~line 638):**
   ```python
   # BEFORE: PersistentConfig[dict](key="role_permissions", type_=dict, ...)
   # AFTER:
   PersistentConfig[dict[str, list[str]]](key="role_permissions", type_=dict[str, list[str]], ...)
   ```

3. Run the application startup check to verify no circular import:
   ```bash
   cd backend && uv run python -c "from app.persistent_config import BASEMAPS, MAP_DEFAULTS, ENABLED_WIDGETS, ROLE_PERMISSIONS; print('OK')"
   ```

4. If circular import occurs, restructure using one of:
   - Move the schema classes to a shared types module
   - Use `TYPE_CHECKING` guard for the annotation and a local import for the `type_=` arg
   - Define inline TypedDict classes in persistent_config.py matching the schema structures
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && uv run python -c "from app.persistent_config import BASEMAPS, MAP_DEFAULTS, ENABLED_WIDGETS, ROLE_PERMISSIONS; print('OK')" && uv run pytest tests/ -k "config" -x --timeout=30 -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "BasemapEntry" backend/app/persistent_config.py` returns at least 1 match
    - `grep -n "MapDefaultsResponse" backend/app/persistent_config.py` returns at least 1 match
    - `grep -c "PersistentConfig\[list\]" backend/app/persistent_config.py` returns 0 (no bare list generics)
    - `grep -c "PersistentConfig\[dict\]" backend/app/persistent_config.py` returns 0 (no bare dict generics)
    - `uv run python -c "from app.persistent_config import BASEMAPS"` exits 0 (no circular import)
  </acceptance_criteria>
  <done>All 4 PersistentConfig declarations use parameterized generics. TypeAdapter runtime validation now validates actual structure (list[BasemapEntry], MapDefaultsResponse, etc.) instead of passing any list/dict. Config tests pass. No circular import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | Untrusted user input crosses here (field lengths, enum values, layer types) |
| DB JSONB -> Python | Untrusted persisted config data crosses here (persistent_config reads) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-224-04 | Tampering | datasets/schemas.py, records/schemas.py | mitigate | Pydantic max_length aligned to SQL column widths prevents IntegrityError on over-length input (P1-6/7/8) |
| T-224-05 | Tampering | maps/schemas.py | mitigate | Literal type on layer_type prevents invalid enum values reaching CHECK constraint (P1-10) |
| T-224-06 | Information Disclosure | maps/router.py share_url | mitigate | Absolute URL via get_public_app_url prevents relative path confusion in shared links (P1-11) |
| T-224-07 | Tampering | persistent_config.py | mitigate | Parameterized TypeAdapter validates JSONB structure at read boundary, preventing type confusion (P1-12) |
</threat_model>

<verification>
After all 4 tasks:
```bash
cd /Users/ishiland/Code/geolens/backend && uv run alembic upgrade head
cd /Users/ishiland/Code/geolens/backend && uv run pytest --tb=short -q
cd /Users/ishiland/Code/geolens/backend && uv run ruff check .
```
</verification>

<success_criteria>
- All Pydantic max_length values match SQL column widths (9 fields across 3 schema files)
- Single Alembic migration for column-width changes applies cleanly
- MapVisibility enum has 4 values matching CHECK constraint
- MapLayerInput.layer_type uses Literal validation
- All 3 share_url sites use absolute URLs via get_public_app_url
- PersistentConfig uses parameterized generics for all 4 config keys
- Full backend test suite passes
- ruff check clean
</success_criteria>

<output>
After completion, create `.planning/phases/224-post-impl-audit-remediation/224-02-SUMMARY.md`
</output>
