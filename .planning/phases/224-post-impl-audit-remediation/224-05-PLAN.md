---
phase: 224-post-impl-audit-remediation
plan: 05
type: execute
wave: 3
depends_on:
  - 224-01
  - 224-02
  - 224-03
  - 224-04
files_modified:
  - .planning/phases/224-post-impl-audit-remediation/224-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - AUDIT-P0-1
  - AUDIT-P0-2
  - AUDIT-P0-3
  - AUDIT-P1-1
  - AUDIT-P1-2
  - AUDIT-P1-3
  - AUDIT-P1-4
  - AUDIT-P1-5
  - AUDIT-P1-6
  - AUDIT-P1-7
  - AUDIT-P1-8
  - AUDIT-P1-9
  - AUDIT-P1-10
  - AUDIT-P1-11
  - AUDIT-P1-12
  - AUDIT-P1-13
  - AUDIT-P1-14
  - AUDIT-P1-15
  - AUDIT-P1-16
  - AUDIT-P1-17
  - AUDIT-P1-18

must_haves:
  truths:
    - "Full backend test suite passes with zero failures"
    - "Full frontend test suite passes with zero failures"
    - "ruff check reports zero violations"
    - "tsc --noEmit reports zero type errors"
    - "All 24 audit items have been verified as resolved"
    - "REQUIREMENTS.md updated with all AUDIT-* requirement IDs"
  artifacts:
    - path: ".planning/phases/224-post-impl-audit-remediation/224-VERIFICATION.md"
      provides: "Verification report documenting status of all 24 P0/P1 items"
      contains: "AUDIT-P0-1"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table with AUDIT-* IDs mapped to Phase 224"
      contains: "AUDIT-P0-1"
  key_links:
    - from: "224-VERIFICATION.md"
      to: "All 4 prior plans"
      via: "Grep verification of each fix"
      pattern: "PASS|FAIL"
---

<objective>
Run full test suites, verify all 24 P0/P1 items are resolved, update REQUIREMENTS.md with audit requirement IDs, and produce the phase verification report.

Purpose: Confirm no regressions introduced by the 4 implementation plans and provide auditable closure of all 24 must-fix items.
Output: 224-VERIFICATION.md with per-item pass/fail status, updated REQUIREMENTS.md traceability table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/224-post-impl-audit-remediation/224-CONTEXT.md
@.planning/phases/224-post-impl-audit-remediation/224-01-SUMMARY.md
@.planning/phases/224-post-impl-audit-remediation/224-02-SUMMARY.md
@.planning/phases/224-post-impl-audit-remediation/224-03-SUMMARY.md
@.planning/phases/224-post-impl-audit-remediation/224-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suites and lint checks</name>
  <files></files>
  <read_first>
    - .planning/phases/224-post-impl-audit-remediation/224-CONTEXT.md (locked decision #7 for test baselines)
  </read_first>
  <action>
1. Run the full backend test suite:
   ```bash
   cd /Users/ishiland/Code/geolens/backend && uv run pytest --tb=short -q
   ```
   Record the pass/fail count.

2. Run the backend linter:
   ```bash
   cd /Users/ishiland/Code/geolens/backend && uv run ruff check .
   ```
   Must report 0 violations.

3. Run the frontend test suite:
   ```bash
   cd /Users/ishiland/Code/geolens/frontend && npm run test -- --run
   ```
   Record the pass/fail count.

4. Run the frontend type check:
   ```bash
   cd /Users/ishiland/Code/geolens/frontend && npx tsc --noEmit
   ```
   Must exit 0.

5. If any failures, document them clearly. Do NOT fix them in this plan — if regressions exist, they must be traced back to the plan that introduced them and fixed there.

6. Verify Alembic migrations apply cleanly (may already be applied):
   ```bash
   cd /Users/ishiland/Code/geolens/backend && uv run alembic upgrade head
   ```
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && uv run pytest --tb=short -q && uv run ruff check . && cd /Users/ishiland/Code/geolens/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Backend pytest reports 0 failures
    - ruff check reports 0 violations
    - Frontend tsc --noEmit exits 0
    - Frontend test suite reports 0 failures
  </acceptance_criteria>
  <done>Full test suite and lint checks pass across both backend and frontend with zero failures and zero violations.</done>
</task>

<task type="auto">
  <name>Task 2: Verify all 24 items resolved + write VERIFICATION.md + update REQUIREMENTS.md</name>
  <files>.planning/phases/224-post-impl-audit-remediation/224-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/224-post-impl-audit-remediation/224-CONTEXT.md (for the full 24-item list)
    - .planning/REQUIREMENTS.md (for the existing traceability table to extend)
    - .planning/phases/224-post-impl-audit-remediation/224-01-SUMMARY.md
    - .planning/phases/224-post-impl-audit-remediation/224-02-SUMMARY.md
    - .planning/phases/224-post-impl-audit-remediation/224-03-SUMMARY.md
    - .planning/phases/224-post-impl-audit-remediation/224-04-SUMMARY.md
  </read_first>
  <action>
1. **Verify each of the 24 items via grep/file inspection.** For each item, run the acceptance criteria grep commands from the corresponding plan task and record PASS/FAIL:

   **P0-1:** `grep -n "_eager_load_record_relations" backend/app/search/service.py backend/app/stac/router.py` — expect matches at all 3 call sites
   **P0-2:** `grep -n 'loading="lazy"' frontend/src/components/search/SearchResultCard.tsx` — expect match
   **P0-3:** `grep -n "503" backend/app/settings/router.py` — expect match in embedding rebuild block
   **P1-1:** `grep -c "asyncio.to_thread" backend/app/storage/local.py` — expect >= 6
   **P1-2:** `grep -n "timedelta" backend/app/auth/dependencies.py` — expect match near last_used_at
   **P1-3:** `grep -n "asyncio.gather" backend/app/ingest/router.py backend/app/datasets/router_reupload.py` — expect matches
   **P1-4:** `grep -n "skip.*limit" backend/app/maps/service.py` — expect match on get_maps_for_dataset
   **P1-5:** `grep -rn "from app.auth.visibility import.*get_user_roles" backend/app/jobs/router.py backend/app/ingest/service.py backend/app/auth/dependencies.py` — expect matches
   **P1-6:** `grep -n "max_length=30" backend/app/datasets/schemas.py` — expect match for update_frequency
   **P1-7:** `grep -n "max_length=30" backend/app/records/schemas.py` — expect match for role/distribution_type
   **P1-8:** `grep -n "max_length=255" backend/app/auth/schemas.py` — expect match for email
   **P1-9:** `grep -n "unlisted" backend/app/maps/schemas.py frontend/src/types/api.ts` — expect matches in both
   **P1-10:** `grep -n "Literal" backend/app/maps/schemas.py` — expect match for layer_type
   **P1-11:** `grep -n "get_public_app_url" backend/app/maps/router.py` — expect >= 3 usage sites
   **P1-12:** `grep -c "PersistentConfig\[list\]" backend/app/persistent_config.py` — expect 0 (no bare generics)
   **P1-13:** `grep -n "uuid4" backend/app/maps/router.py` — expect match in thumbnail handler
   **P1-14:** `grep -n "MapErrorBoundary" frontend/src/pages/DatasetPage.tsx` — expect >= 2 matches
   **P1-15:** `grep -n "oauth_failed" backend/app/auth/oauth/router.py` — expect match
   **P1-16:** `grep -n "db.rollback" backend/app/datasets/router.py` — expect match in delete handler
   **P1-17:** `grep -n "total_read\|limited_receive" backend/app/middleware/body_limit.py` — expect match
   **P1-18:** `grep -n "pg_try_advisory_xact_lock" backend/app/worker.py` — expect match

2. **Write `224-VERIFICATION.md`** with the following structure:
   ```markdown
   # Phase 224 — Verification Report

   **Date:** [current date]
   **Verified by:** Claude executor
   **Test results:** Backend [X/Y] pass, Frontend [X/Y] pass, ruff clean, tsc clean

   ## P0 Items

   | Item | Status | Evidence |
   |------|--------|----------|
   | P0-1: Cartesian joinedload | PASS/FAIL | [grep output] |
   | P0-2: base64 quicklook | PASS/FAIL | [grep output] |
   | P0-3: embedding rebuild | PASS/FAIL | [grep output] |

   ## P1 Items

   | Item | Status | Evidence |
   |------|--------|----------|
   | P1-1 through P1-18 | PASS/FAIL | [grep output per item] |

   ## Summary

   [X/24] items resolved. [Any failures documented with root cause.]
   ```

3. **Update REQUIREMENTS.md**: Add a new section under "### Backend Audit Remediation" (after the existing "Backend Config Hardening" section):
   ```markdown
   ### Backend Audit Remediation

   Requirements from post-impl audit (2026-04-11). Phase 224.

   - [x] **AUDIT-P0-1**: selectinload replaces joinedload at all 3 Cartesian sites (search, RRF, STAC)
   - [x] **AUDIT-P0-2**: SearchResultCard uses native <img loading="lazy"> instead of base64 fetch
   - [x] **AUDIT-P0-3**: Embedding rebuild failure returns 503 and rolls back embedding_dims config
   - [x] **AUDIT-P1-1**: LocalStorageProvider wraps all sync I/O in asyncio.to_thread
   - [x] **AUDIT-P1-2**: API key last_used_at deferred with 60s threshold
   - [x] **AUDIT-P1-3**: Presigned URL generation parallelized with asyncio.gather
   - [x] **AUDIT-P1-4**: get_maps_for_dataset paginated with skip/limit (default 50)
   - [x] **AUDIT-P1-5**: User roles deduplicated to canonical get_user_roles helper
   - [x] **AUDIT-P1-6**: DatasetMeta Pydantic max_length matches SQL column widths
   - [x] **AUDIT-P1-7**: Record* schema max_length matches SQL column widths (5 fields)
   - [x] **AUDIT-P1-8**: UserCreate.email max_length=255 matches User.email String(255)
   - [x] **AUDIT-P1-9**: MapVisibility enum includes 'unlisted' in Pydantic + TS (matches CHECK)
   - [x] **AUDIT-P1-10**: MapLayerInput.layer_type typed as Literal matching CHECK constraint
   - [x] **AUDIT-P1-11**: share_url is absolute via get_public_app_url (all 3 sites)
   - [x] **AUDIT-P1-12**: PersistentConfig uses parameterized generics for 4 config keys
   - [x] **AUDIT-P1-13**: Thumbnail upload uses temp-key pattern with rollback on failure
   - [x] **AUDIT-P1-14**: DatasetMap wrapped in MapErrorBoundary on DatasetPage
   - [x] **AUDIT-P1-15**: OAuth error uses stable error codes + correlation_id
   - [x] **AUDIT-P1-16**: Bulk delete commits per-item with rollback on failure
   - [x] **AUDIT-P1-17**: Chunked encoding body limit enforced via stream counting
   - [x] **AUDIT-P1-18**: Worker recovery uses advisory lock + heartbeat stale detection
   ```

   Also add the traceability rows to the existing table at the bottom of REQUIREMENTS.md:
   ```
   | AUDIT-P0-1 | Phase 224 | Complete |
   | AUDIT-P0-2 | Phase 224 | Complete |
   ... (all 24 IDs)
   ```

   Mark checkboxes as `[x]` only for items that PASSED verification. Use `[ ]` for any that FAILED.
  </action>
  <verify>
    <automated>test -f /Users/ishiland/Code/geolens/.planning/phases/224-post-impl-audit-remediation/224-VERIFICATION.md && echo "VERIFICATION.md exists"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/224-post-impl-audit-remediation/224-VERIFICATION.md` exists and contains all 24 item rows
    - `.planning/REQUIREMENTS.md` contains "AUDIT-P0-1" in the traceability table
    - Every PASS item has grep evidence; every FAIL item has documented root cause
  </acceptance_criteria>
  <done>All 24 P0/P1 items verified with grep evidence. VERIFICATION.md written with per-item PASS/FAIL. REQUIREMENTS.md updated with all AUDIT-* requirement IDs. Phase 224 audit remediation is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries in this verification plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-224-16 | Repudiation | VERIFICATION.md | mitigate | Every item verified with grep evidence, not subjective assessment |
</threat_model>

<verification>
All verification is performed within the tasks themselves. This is the verification plan.
</verification>

<success_criteria>
- Full backend test suite: 0 failures
- Full frontend test suite: 0 failures
- ruff check: 0 violations
- tsc --noEmit: 0 errors
- 224-VERIFICATION.md exists with 24/24 items documented
- REQUIREMENTS.md updated with AUDIT-* IDs
</success_criteria>

<output>
After completion, create `.planning/phases/224-post-impl-audit-remediation/224-05-SUMMARY.md`
</output>
