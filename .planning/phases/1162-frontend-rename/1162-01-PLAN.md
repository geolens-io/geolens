---
phase: 1162-frontend-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # --- Directory move (git mv): old paths removed, new paths created ---
  - frontend/src/components/map-plugins/PluginHost.tsx           # was WidgetHost.tsx
  - frontend/src/components/map-plugins/PluginPanel.tsx          # was WidgetPanel.tsx
  - frontend/src/components/map-plugins/PluginErrorBoundary.tsx  # was WidgetErrorBoundary.tsx
  - frontend/src/components/map-plugins/index.ts
  - frontend/src/components/map-plugins/registry.ts
  - frontend/src/components/map-plugins/register-plugins.ts      # was register-widgets.ts
  - frontend/src/components/map-plugins/types.ts
  - frontend/src/components/map-plugins/plugin-availability.ts   # was widget-availability.ts
  - frontend/src/components/map-plugins/builtin/MeasurementPlugin.tsx  # was MeasurementWidget.tsx
  - frontend/src/components/map-plugins/builtin/LegendPlugin.tsx       # was LegendWidget.tsx
  - frontend/src/components/map-plugins/__tests__/PluginHost.test.tsx          # was WidgetHost.test.tsx
  - frontend/src/components/map-plugins/__tests__/registry.test.ts
  - frontend/src/components/map-plugins/__tests__/plugin-availability.test.ts  # was widget-availability.test.ts
  # --- Store move ---
  - frontend/src/stores/map-plugin-store.ts                      # was map-widget-store.ts
  - frontend/src/stores/__tests__/map-plugin-store.test.ts       # was map-widget-store.test.ts
  - frontend/src/stores/__tests__/store-relocation-smoke.test.ts
  # --- Component consumers (import path + identifier rewrite) ---
  - frontend/src/components/admin/settings/SettingsMapTab.tsx
  - frontend/src/components/builder/SettingsEditorScene.tsx
  - frontend/src/components/builder/BuilderMap.tsx
  - frontend/src/components/builder/MapToolbar.tsx
  - frontend/src/components/builder/builder-action-contract.ts
  - frontend/src/components/map/MapCoordReadout.tsx
  - frontend/src/components/viewer/ViewerMap.tsx
  - frontend/src/components/builder/ActiveFilterChips.tsx
  - frontend/src/components/builder/DEMEditorScene.tsx
  - frontend/src/pages/MapBuilderPage.tsx
  # --- Consumer tests ---
  - frontend/src/components/builder/__tests__/MapBuilderPage.settings-wiring.test.tsx
  - frontend/src/components/builder/__tests__/MapBuilderPage.notes-ai-rail.test.tsx
  - frontend/src/components/builder/__tests__/MapBuilderPage.a11y.test.tsx
  - frontend/src/components/builder/__tests__/SettingsEditorScene.test.tsx
  - frontend/src/components/builder/__tests__/SettingsEditorScene.plugins.test.tsx  # renamed from SettingsEditorScene.widgets.test.tsx
  - frontend/src/components/builder/__tests__/MapToolbar.test.tsx
  - frontend/src/components/builder/__tests__/ActiveFilterChips.test.tsx
  - frontend/src/components/builder/__tests__/BuilderMap.terrain-visibility.test.tsx
  - frontend/src/components/builder/__tests__/BuilderMap.a11y.test.tsx
  - frontend/src/components/builder/__tests__/DEMEditorScene.test.tsx
  - frontend/src/pages/__tests__/MapBuilderPage.header-actions.test.tsx
  - frontend/src/pages/__tests__/PublicMapViewerPage.test.tsx
autonomous: true
requirements: [FE-RENAME-01, FE-RENAME-02, FE-RENAME-04]

must_haves:
  truths:
    - "frontend/src/components/map-widgets/ no longer exists; frontend/src/components/map-plugins/ holds all 10 entries (incl. __tests__/ and builtin/)"
    - "No import in frontend/src resolves to @/components/map-widgets or @/stores/map-widget-store"
    - "The map-plugins module, the map-plugin-store, and all component consumers use Plugin* identifiers (WidgetHost→PluginHost, useWidgetStore→usePluginStore, getEnabledWidgetDefinitions→getEnabledPluginDefinitions, toggle_widget→toggle_plugin, etc.)"
    - "The registry ID string literals 'measurement' and 'legend' are unchanged"
    - "After this plan `cd frontend && npm run typecheck` is 0 errors (mid-flight red is allowed; the COMMIT must be green)"
  artifacts:
    - path: "frontend/src/components/map-plugins/PluginHost.tsx"
      provides: "Renamed plugin host component (was WidgetHost) exporting PluginHost/PluginSidebar/usePartitionedPlugins"
      contains: "PluginHost"
    - path: "frontend/src/components/map-plugins/plugin-availability.ts"
      provides: "isPluginIdAvailable / getEnabledPluginDefinitions availability gating (was widget-availability)"
      contains: "isPluginIdAvailable"
    - path: "frontend/src/stores/map-plugin-store.ts"
      provides: "usePluginStore zustand store with activePlugins (was useWidgetStore/activeWidgets)"
      contains: "usePluginStore"
    - path: "frontend/src/components/builder/builder-action-contract.ts"
      provides: "BuilderSettingsAction toggle_plugin variant (was toggle_widget) with pluginId"
      contains: "toggle_plugin"
  key_links:
    - from: "frontend/src/pages/MapBuilderPage.tsx"
      to: "frontend/src/components/map-plugins"
      via: "import PluginHost + usePartitionedPlugins"
      pattern: "from ['\"]@/components/map-plugins"
    - from: "frontend/src/components/builder/MapToolbar.tsx"
      to: "frontend/src/components/map-plugins"
      via: "import getEnabledPluginDefinitions"
      pattern: "getEnabledPluginDefinitions"
---

<objective>
Rename the frontend map-plugin platform's directory, files, and ALL `Widget*` platform identifiers to `Plugin*` — a mechanical, atomic rename that lands in one wave so the TypeScript build stays self-consistent. Move `frontend/src/components/map-widgets/` → `map-plugins/` (all 10 entries) and `frontend/src/stores/map-widget-store.ts` → `map-plugin-store.ts`, rename the symbols they export, and rewrite every component/store consumer.

Covers FE-RENAME-01 (dir move + import paths), FE-RENAME-02 (identifier rename), FE-RENAME-04 (preserve `measurement`/`legend` ID literals). It does NOT touch the API-contract surface — that is plan 02. The split is by exclusive file ownership (zero file overlap between the two plans) and a single explicit type-seam at the `map.widgets`/`map.plugins` boundary (see <type_seam_protocol>).

Purpose: Give the frontend extensibility surface the honest "plugin" name. After this plan the COMPONENT vocabulary is fully `Plugin*`; plan 02 then flips the wire/contract field (`widgets`→`plugins`) so the frontend matches the Phase-1161 backend.
Output: `map-plugins/` + `map-plugin-store.ts` with `Plugin*` symbols; every component consumer updated; `measurement`/`legend` IDs untouched; `npm run typecheck` 0 at commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1161-backend-rename-and-contract/1161-02-SUMMARY.md

<critical_constraints>
1. **PRESERVE the ID string literals `'measurement'` and `'legend'`.** These are registry identifiers, NOT the word "widget". They appear as the `id:` field on the two built-in plugin definitions (`register-plugins.ts`), as `.has('measurement')`/`=== 'legend'` checks (MapToolbar, MeasurementPlugin `useWidgetStore.getState().close('measurement')`), and as values in tests. NEVER change these string values. You ARE renaming the COMPONENT names `MeasurementWidget`→`MeasurementPlugin`, `LegendWidget`→`LegendPlugin`, the FILE names, and the surrounding identifiers — but the `'measurement'` / `'legend'` literals stay byte-for-byte. Also DO NOT touch internal layer-id constants like `MEASURE_LINE_LAYER` / `MEASURE_POINTS_LAYER` in MeasurementPlugin (they are maplibre source/layer ids, not platform vocabulary).

2. **i18n locale JSON + i18n KEY strings are OUT OF SCOPE** (Phase 1163). Do NOT edit any file under `frontend/src/i18n/locales/`, and do NOT rename i18n KEY strings such as `'widgets.measurement.label'`, `t('widgets.closeWidget')`, `i18n.t('builder:widgets.widgetError')`, `t('widgets.legend.label')`. These are plain strings consumed by `t()` — they do NOT affect typecheck. Leave every `widgets.*` i18n key exactly as-is; 1163 renames them. (You MAY rename the surrounding TS identifiers, just not the quoted i18n key path.)

3. **HARD breaking cut.** No `widget` alias, no re-export shim. After this plan, `@/components/map-widgets` and `@/stores/map-widget-store` must not resolve.

4. **Use `git mv` for file/dir moves** (history-preserving, removes old path atomically). Moves FIRST (Task 1), content edits AFTER (Tasks 2-3). Do NOT hand-create + delete.

5. **Atomic rename — build is red mid-flight by design.** Commit only when `cd frontend && npm run typecheck` is 0 and the moved/consumer unit suites pass.
</critical_constraints>

<type_seam_protocol>
<!-- THE ONE TRICKY BOUNDARY. Read carefully. -->

Three files READ the Map response field `map.widgets` (which plan 02 renames to `map.plugins` in `types/api.ts`):
  - `pages/MapBuilderPage.tsx`        : `mapData.widgets` (lines ~231/244/251/253 — re-grep)
  - `components/builder/SettingsEditorScene.tsx` : passes map into `buildMapStack` (uses `MapStackMapInput`)
  - `components/builder/map-stack.ts`  : `MapStackMapInput.widgets` field + `map.widgets ?? []` read (~663) — **OWNED BY PLAN 02, NOT this plan** (see note below)

**Rule for THIS plan:** Do NOT rename the `.widgets` PROPERTY ACCESS on the Map RESPONSE object (`mapData.widgets`, `map.widgets`). That property is named by `types/api.ts`, which plan 02 owns and renames. If you rename `mapData.widgets`→`mapData.plugins` here while `MapResponse` still types it as `widgets`, typecheck FAILS at your commit.

So in `MapBuilderPage.tsx`: rename the platform identifiers (`WidgetHost`→`PluginHost`, `useWidgetStore`→`usePluginStore`, `getDefaultWidgetIds`→`getDefaultPluginIds`, `resolveAvailableWidgetIds`→`resolveAvailablePluginIds`, `usePartitionedWidgets`→`usePartitionedPlugins`, `toggleWidget`→`togglePlugin`, local vars `enabledWidgetIds`→`enabledPluginIds`, `activeWidgets`→`activePlugins`, `savedWidgetKey`→`savedPluginKey`, comments) — BUT leave `mapData.widgets` reads spelled `.widgets`. Plan 02 flips both the type field AND these reads in the same wave-2 commit, keeping each commit green.

**`map-stack.ts` is intentionally MOVED to plan 02's ownership** because its `MapStackMapInput.widgets` field, the `'interaction-widgets'` role, the `'interactions:widgets'` entry id, and the `map.widgets` read are all bound to the renamed Map field — renaming them belongs with the `types/api.ts` flip to stay green. `map-stack.ts` is NOT in this plan's files_modified. Do not edit it here.

`SettingsEditorScene.tsx` IS in this plan (it imports `getWidgets` from `@/components/map-widgets/registry` + has `onToggleWidget`/`widgetId` props). Rename those identifiers here, but if it reads `map.widgets`, leave that property access for plan 02 (re-grep; it mostly passes the whole map object through, so likely no direct `.widgets` access to worry about).
</type_seam_protocol>

<interfaces>
<!-- VERIFIED at HEAD 5779785b/da8fccb8. Re-grep each file before editing — trust live
     code over these cites (pattern from 1161-02 SUMMARY: re-grep before each edit). -->

map-widgets/ → map-plugins/ (file renames):
  WidgetErrorBoundary.tsx → PluginErrorBoundary.tsx   (class WidgetErrorBoundary → PluginErrorBoundary; prop widgetId → pluginId)
  WidgetHost.tsx          → PluginHost.tsx             (exports: WidgetHost→PluginHost, WidgetSidebar→PluginSidebar, usePartitionedWidgets→usePartitionedPlugins; local activeWidgets→activePlugins, enabledWidgetIds→enabledPluginIds)
  WidgetPanel.tsx         → PluginPanel.tsx            (export WidgetPanel→PluginPanel; uses useWidgetStore; JSDoc {@link WidgetHost}→{@link PluginHost})
  index.ts                → index.ts                   (barrel — rewrite ALL re-exports + the side-effect `import './register-widgets'` → `'./register-plugins'`)
  register-widgets.ts     → register-plugins.ts        (calls registerWidget→registerPlugin; imports ./builtin/MeasurementWidget→MeasurementPlugin, ./builtin/LegendWidget→LegendPlugin; KEEP id:'measurement'/'legend' + KEEP labelKey:'widgets.*' i18n strings)
  registry.ts             → registry.ts                (registerWidget→registerPlugin, getWidgets→getPlugins, getWidget→getPlugin, type WidgetDefinition→PluginDefinition; the `const registry`/local cache var may stay or rename — keep typecheck green; warn string "Widget \"...\"" is dev-only log, rename to Plugin)
  types.ts                → types.ts                   (WidgetAnchor→PluginAnchor, WidgetPlacement→PluginPlacement, WidgetContext→PluginContext, WidgetDefinition→PluginDefinition; KEEP the JSDoc i18n-key EXAMPLE string 'widgets.measurement.label')
  widget-availability.ts  → plugin-availability.ts     (getEnabledWidgetDefinitions→getEnabledPluginDefinitions, isWidgetIdAvailable→isPluginIdAvailable, resolveAvailableWidgetIds→resolveAvailablePluginIds, getDefaultWidgetIds→getDefaultPluginIds, sameWidgetIds→samePluginIds; params enabledWidgetIds→enabledPluginIds, widgetIds→pluginIds; getWidgets→getPlugins)
  builtin/MeasurementWidget.tsx → builtin/MeasurementPlugin.tsx  (export MeasurementWidget→MeasurementPlugin; import useWidgetStore from renamed store + type WidgetContext→PluginContext; KEEP useWidgetStore.getState().close('measurement') LITERAL 'measurement'; KEEP MEASURE_*_LAYER ids; KEEP t('widgets.*') keys)
  builtin/LegendWidget.tsx → builtin/LegendPlugin.tsx  (export LegendWidget→LegendPlugin; type WidgetContext→PluginContext; KEEP t('widgets.legend.*') keys + `legend-widget-${idx}` layerId string stays — it is a maplibre layer id, NOT vocabulary... actually rename to `legend-plugin-${idx}` only if no external selector depends on it; SAFER: leave it. Re-grep e2e specs? Those are Phase 1164 — leave the layerId string as-is to avoid breaking 1164's specs.)
  __tests__/WidgetHost.test.tsx → __tests__/PluginHost.test.tsx
  __tests__/registry.test.ts → __tests__/registry.test.ts (keep name)
  __tests__/widget-availability.test.ts → __tests__/plugin-availability.test.ts

store:
  stores/map-widget-store.ts → stores/map-plugin-store.ts   (interface WidgetState→PluginState, useWidgetStore→usePluginStore, field activeWidgets→activePlugins throughout)
  stores/__tests__/map-widget-store.test.ts → stores/__tests__/map-plugin-store.test.ts
  stores/__tests__/store-relocation-smoke.test.ts  (NOT renamed — update its '@/stores/map-widget-store' ref → '@/stores/map-plugin-store' + useWidgetStore/activeWidgets symbols)

cross-module consumers (re-grep "map-widgets|map-widget-store|Widget|widget" in each):
  components/admin/settings/SettingsMapTab.tsx : `import { getWidgets } from '@/components/map-widgets'` → getPlugins from '@/components/map-plugins'
  components/builder/SettingsEditorScene.tsx   : `import { getWidgets } from '@/components/map-widgets/registry'` → getPlugins from '@/components/map-plugins/registry'; props onToggleWidget→onTogglePlugin, widgetId→pluginId
  components/builder/BuilderMap.tsx            : `import { isWidgetIdAvailable } from '@/components/map-widgets'` → isPluginIdAvailable from '@/components/map-plugins'
  components/builder/MapToolbar.tsx            : imports useWidgetStore, useEnabledWidgets(KEEP — hook lives in use-settings, owned by plan 02; DO NOT rename the hook call here, only the store import + local vars), getEnabledWidgetDefinitions→getEnabledPluginDefinitions; locals measurementWidget→measurementPlugin, legendWidget→legendPlugin, availableWidgets→availablePlugins, activeWidgets→activePlugins, enabledWidgetIds→enabledPluginIds; KEEP .has('measurement')/'legend' literals + t('widgets.*') keys
  components/builder/builder-action-contract.ts: BuilderSettingsAction `type:'toggle_widget'; widgetId` → `type:'toggle_plugin'; pluginId`
  components/map/MapCoordReadout.tsx           : JSDoc-only refs `WidgetHost`/`WidgetHost.tsx:17` → PluginHost (comment text)
  components/viewer/ViewerMap.tsx              : JSDoc-only "embeddable iframe widget" — leave prose OR change to "plugin" (cosmetic; safe to change, not required for typecheck)
  components/builder/ActiveFilterChips.tsx     : comment-only "MeasurementWidget" → MeasurementPlugin (cosmetic)
  components/builder/DEMEditorScene.tsx        : comment-only "Compass widget" — this is NOT the plugin platform (it's a basemap compass). LEAVE unchanged.
  pages/MapBuilderPage.tsx                     : the big consumer — import PluginHost+getDefaultPluginIds+resolveAvailablePluginIds+usePartitionedPlugins from '@/components/map-plugins'; usePluginStore from '@/stores/map-plugin-store'; togglePlugin callback; SEE <type_seam_protocol> for the `mapData.widgets` reads (LEAVE those). KEEP useEnabledWidgets hook call (plan 02 renames the hook).

CONSUMER NOTE on `useEnabledWidgets`: that hook is defined in `hooks/use-settings.ts` (plan 02 territory) and re-exported as a name. To keep BOTH plans' commits green with ZERO shared files, this plan KEEPS calling `useEnabledWidgets()` by its current name in MapToolbar/MapBuilderPage; plan 02 renames the hook definition AND all call sites in the same wave-2 commit. (The call sites in MapToolbar/MapBuilderPage are thus touched by BOTH plans — but on DIFFERENT tokens: plan 01 touches the store/registry imports + local vars, plan 02 touches the `useEnabledWidgets`→`useEnabledPlugins` rename + `mapData.widgets`→`.plugins`. This is the intentional, surgical co-edit. Plan 02's interfaces block flags these exact files.)
</type_seam_protocol_addendum>
</interfaces>

<identifier_mapping>
| Old | New |
|-----|-----|
| dir `map-widgets/` | `map-plugins/` |
| `@/components/map-widgets` (+ `/registry`) | `@/components/map-plugins` (+ `/registry`) |
| `@/stores/map-widget-store` | `@/stores/map-plugin-store` |
| files `WidgetHost/WidgetPanel/WidgetErrorBoundary.tsx` | `PluginHost/PluginPanel/PluginErrorBoundary.tsx` |
| files `register-widgets.ts` / `widget-availability.ts` | `register-plugins.ts` / `plugin-availability.ts` |
| files `builtin/MeasurementWidget.tsx` / `LegendWidget.tsx` | `builtin/MeasurementPlugin.tsx` / `LegendPlugin.tsx` |
| `stores/map-widget-store.ts` | `stores/map-plugin-store.ts` |
| components `WidgetHost`/`WidgetSidebar`/`WidgetPanel`/`WidgetErrorBoundary` | `PluginHost`/`PluginSidebar`/`PluginPanel`/`PluginErrorBoundary` |
| components `MeasurementWidget`/`LegendWidget` | `MeasurementPlugin`/`LegendPlugin` |
| types `WidgetAnchor`/`WidgetPlacement`/`WidgetContext`/`WidgetDefinition`/`WidgetState` | `PluginAnchor`/`PluginPlacement`/`PluginContext`/`PluginDefinition`/`PluginState` |
| fns `registerWidget`/`getWidgets`/`getWidget` | `registerPlugin`/`getPlugins`/`getPlugin` |
| fns `getEnabledWidgetDefinitions`/`isWidgetIdAvailable`/`resolveAvailableWidgetIds`/`getDefaultWidgetIds`/`sameWidgetIds` | `getEnabledPluginDefinitions`/`isPluginIdAvailable`/`resolveAvailablePluginIds`/`getDefaultPluginIds`/`samePluginIds` |
| hook `useWidgetStore` + field `activeWidgets` | `usePluginStore` + `activePlugins` |
| hook `usePartitionedWidgets` | `usePartitionedPlugins` |
| action `type:'toggle_widget'; widgetId` | `type:'toggle_plugin'; pluginId` |
| prop `onToggleWidget(widgetId)` | `onTogglePlugin(pluginId)` |
| local vars `enabledWidgetIds`/`measurementWidget`/`legendWidget`/`availableWidgets`/`savedWidgetKey`/`enabledWidgetKey`/`toggleWidget`/`nextWidgets` | `enabledPluginIds`/`measurementPlugin`/`legendPlugin`/`availablePlugins`/`savedPluginKey`/`enabledPluginKey`/`togglePlugin`/`nextPlugins` |

**NEVER rename (this plan):** the string LITERALS `'measurement'`/`'legend'`; i18n key strings `'widgets.*'` / `'builder:widgets.*'`; maplibre layer-id consts (`MEASURE_LINE_LAYER`, `MEASURE_POINTS_LAYER`, `legend-widget-${idx}`); the hook NAME `useEnabledWidgets` (plan 02); the Map RESPONSE property `mapData.widgets`/`map.widgets` (plan 02); `map-stack.ts` (plan 02 file).
</identifier_mapping>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv the directory + store files to the plugin names</name>
  <files>frontend/src/components/map-widgets/* → frontend/src/components/map-plugins/* ; frontend/src/stores/map-widget-store.ts → frontend/src/stores/map-plugin-store.ts (+ its test)</files>
  <action>
    Move files ONLY (no content edits). Use `git mv` from repo root /Users/ishiland/Code/geolens:
    1. `git mv frontend/src/components/map-widgets frontend/src/components/map-plugins`
    2. Rename files inside (git mv each): WidgetHost.tsx→PluginHost.tsx, WidgetPanel.tsx→PluginPanel.tsx, WidgetErrorBoundary.tsx→PluginErrorBoundary.tsx, register-widgets.ts→register-plugins.ts, widget-availability.ts→plugin-availability.ts, builtin/MeasurementWidget.tsx→builtin/MeasurementPlugin.tsx, builtin/LegendWidget.tsx→builtin/LegendPlugin.tsx, __tests__/WidgetHost.test.tsx→__tests__/PluginHost.test.tsx, __tests__/widget-availability.test.ts→__tests__/plugin-availability.test.ts. (index.ts, registry.ts, types.ts, __tests__/registry.test.ts keep filenames.)
    3. `git mv frontend/src/stores/map-widget-store.ts frontend/src/stores/map-plugin-store.ts` and `git mv frontend/src/stores/__tests__/map-widget-store.test.ts frontend/src/stores/__tests__/map-plugin-store.test.ts`
    4. Also `git mv frontend/src/components/builder/__tests__/SettingsEditorScene.widgets.test.tsx frontend/src/components/builder/__tests__/SettingsEditorScene.plugins.test.tsx`.
    No content edited yet — build will be red. Resolved by Tasks 2-3.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && test ! -d frontend/src/components/map-widgets && test -f frontend/src/components/map-plugins/PluginHost.tsx && test -f frontend/src/components/map-plugins/builtin/MeasurementPlugin.tsx && test -f frontend/src/stores/map-plugin-store.ts && test -f frontend/src/components/builder/__tests__/SettingsEditorScene.plugins.test.tsx && echo MOVES_OK</automated>
  </verify>
  <done>map-widgets/ gone; map-plugins/ has all 10 entries with renamed filenames; map-plugin-store.ts + renamed test files exist. No content edited.</done>
</task>

<task type="auto">
  <name>Task 2: Rename Widget* symbols inside the moved map-plugins/ + store (+ their tests)</name>
  <files>frontend/src/components/map-plugins/{index.ts,types.ts,registry.ts,register-plugins.ts,plugin-availability.ts,PluginHost.tsx,PluginPanel.tsx,PluginErrorBoundary.tsx,builtin/MeasurementPlugin.tsx,builtin/LegendPlugin.tsx,__tests__/PluginHost.test.tsx,__tests__/registry.test.ts,__tests__/plugin-availability.test.ts} ; frontend/src/stores/map-plugin-store.ts ; frontend/src/stores/__tests__/{map-plugin-store.test.ts,store-relocation-smoke.test.ts}</files>
  <action>
    Rename every `Widget*` platform identifier → `Plugin*` INSIDE the moved files per the identifier_mapping table. Read-then-Edit each file; re-grep (`grep -nE 'Widget|widget|activeWidgets' <file>`) first.

    Per-file: rename component/type/fn/hook/field identifiers, internal cross-imports (`index.ts` re-exports from `./PluginHost` etc.; `register-plugins.ts` imports `./builtin/MeasurementPlugin`/`./builtin/LegendPlugin`; `PluginHost`/`PluginPanel`/`MeasurementPlugin`/`LegendPlugin` import `usePluginStore` from `@/stores/map-plugin-store`), dev-log strings ("Widget"→"Plugin"), JSDoc `{@link WidgetHost}`→`{@link PluginHost}`, and test `describe`/`it` titles + local fixture vars.

    CRITICAL PRESERVE (verify with grep after): `'measurement'` / `'legend'` LITERALS (registry id + `.close('measurement')`); i18n key strings `'widgets.*'` / `'builder:widgets.*'` (do NOT rename); `MEASURE_LINE_LAYER`/`MEASURE_POINTS_LAYER`/`legend-widget-${idx}` (maplibre ids — leave); do NOT rename the `useEnabledWidgets` import/call inside PluginHost (plan 02 renames the hook) — leave the import line `import { useEnabledWidgets } from '@/hooks/use-settings'` and its usage spelled with "Widgets" so this plan needs no edit to use-settings.ts.

    store-relocation-smoke.test.ts: update `@/stores/map-widget-store`→`@/stores/map-plugin-store` and useWidgetStore/activeWidgets symbols (keep filename).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && grep -rnE 'WidgetHost|WidgetPanel|WidgetErrorBoundary|WidgetDefinition|WidgetContext|WidgetState|registerWidget|getWidgets|useWidgetStore|activeWidgets|MeasurementWidget|LegendWidget' frontend/src/components/map-plugins frontend/src/stores/map-plugin-store.ts ; echo "--- expect 0 platform matches above (useEnabledWidgets hook ref in PluginHost is OK and not matched) ---" ; echo "--- measurement/legend literals present (expect both) ---" ; grep -rhoE "'(measurement|legend)'" frontend/src/components/map-plugins/register-plugins.ts | sort -u</automated>
  </verify>
  <done>The map-plugins module + map-plugin-store export only Plugin* symbols; internal imports resolve; `'measurement'`/`'legend'` literals + `widgets.*` i18n keys intact; only the deliberate `useEnabledWidgets` hook reference remains widget-spelled (plan 02).</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite component consumers + tests, then typecheck-0 and commit</name>
  <files>frontend/src/components/admin/settings/SettingsMapTab.tsx, frontend/src/components/builder/{SettingsEditorScene.tsx,BuilderMap.tsx,MapToolbar.tsx,builder-action-contract.ts,ActiveFilterChips.tsx}, frontend/src/components/map/MapCoordReadout.tsx, frontend/src/components/viewer/ViewerMap.tsx, frontend/src/pages/MapBuilderPage.tsx, and the consumer test files in files_modified</files>
  <action>
    Rewrite each consumer to the renamed module/store/identifiers. Re-grep each file first.

    Source consumers: SettingsMapTab (getWidgets→getPlugins from '@/components/map-plugins'); SettingsEditorScene (getPlugins from '@/components/map-plugins/registry'; onToggleWidget→onTogglePlugin, widgetId→pluginId; LEAVE any `map.widgets` read for plan 02); BuilderMap (isPluginIdAvailable from '@/components/map-plugins'); MapToolbar (usePluginStore from '@/stores/map-plugin-store', getEnabledPluginDefinitions from '@/components/map-plugins', locals→plugin per table, KEEP useEnabledWidgets hook + '.has('measurement')/'legend'' + t('widgets.*')); builder-action-contract (`toggle_widget`/`widgetId`→`toggle_plugin`/`pluginId`); MapCoordReadout + ActiveFilterChips + ViewerMap (comment/JSDoc cosmetic Widget→Plugin); MapBuilderPage (PluginHost+getDefaultPluginIds+resolveAvailablePluginIds+usePartitionedPlugins from '@/components/map-plugins'; usePluginStore; togglePlugin; locals per table; KEEP `useEnabledWidgets` hook call AND `mapData.widgets` reads per <type_seam_protocol>).

    Consumer tests: update vi.mock('@/components/map-widgets'...)→'@/components/map-plugins', vi.mock('@/components/map-widgets/registry'...)→'@/components/map-plugins/registry', useWidgetStore→usePluginStore, mocked fn names (getEnabledWidgetDefinitions→getEnabledPluginDefinitions etc.), onToggleWidget→onTogglePlugin, and `widgetId` test args. In SettingsEditorScene.plugins.test.tsx + SettingsEditorScene.test.tsx: rename the widget-toggle describe/it + the `onToggleWidget` assertions. KEEP `'measurement'`/`'legend'` literals + i18n key strings in tests.

    DO NOT touch i18n locales, `map-stack.ts`, or any plan-02 contract file. After all edits: run `cd frontend && npm run typecheck` (must be 0 — if the only remaining errors are `mapData.widgets`/`map.widgets` "property plugins does not exist", you renamed a seam read you shouldn't have; revert that token to `.widgets`). Then run the moved + consumer suites. Commit only when green.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npm run typecheck 2>&1 | tail -5 && npm run test -- src/components/map-plugins/ src/stores/ src/components/builder/__tests__/MapToolbar.test.tsx src/components/builder/__tests__/SettingsEditorScene.test.tsx src/components/builder/__tests__/SettingsEditorScene.plugins.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <done>`npm run typecheck` reports 0 errors; the moved map-plugins/ + stores/ suites + MapToolbar/SettingsEditorScene suites pass; no import resolves to map-widgets or map-widget-store; the only widget-spelled tokens left are the deliberate `useEnabledWidgets` hook + Map-response `.widgets` reads (handed to plan 02).</done>
</task>

</tasks>

<verification>
- `frontend/src/components/map-widgets/` does not exist; `map-plugins/` has 10 entries.
- `grep -rn "@/components/map-widgets\|@/stores/map-widget-store" frontend/src` → 0.
- `grep -rnE "WidgetHost|WidgetPanel|WidgetErrorBoundary|WidgetDefinition|WidgetContext|WidgetState|registerWidget|getWidgets|useWidgetStore|activeWidgets|getEnabledWidgetDefinitions|isWidgetIdAvailable|resolveAvailableWidgetIds|getDefaultWidgetIds|sameWidgetIds|usePartitionedWidgets|toggle_widget|MeasurementWidget|LegendWidget" frontend/src` → 0.
- `'measurement'`/`'legend'` literals preserved (registry + tests).
- `cd frontend && npm run typecheck` → 0.
- Remaining widget-spelled tokens are ONLY: `useEnabledWidgets` (hook, plan 02), `mapData.widgets`/`map.widgets` reads (plan 02), `map-stack.ts` contents (plan 02), and i18n key strings `widgets.*` (Phase 1163).
</verification>

<success_criteria>
- FE-RENAME-01: dir renamed, every component/store import resolves to new path.
- FE-RENAME-02: all Widget* platform identifiers in the module/store/component-consumer surface → Plugin* (contract-surface identifiers are plan 02).
- FE-RENAME-04: `measurement`/`legend` ID literals unchanged.
- `npm run typecheck` 0 at commit; moved + consumer suites green.
</success_criteria>

<output>
Create `.planning/phases/1162-frontend-rename/1162-01-SUMMARY.md` when done.
</output>
