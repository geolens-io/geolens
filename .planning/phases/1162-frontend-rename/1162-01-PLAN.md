---
phase: 1162-frontend-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # Directory move (git mv) — old paths removed, new paths created:
  - frontend/src/components/map-plugins/PluginHost.tsx           # was WidgetHost.tsx
  - frontend/src/components/map-plugins/PluginPanel.tsx          # was WidgetPanel.tsx
  - frontend/src/components/map-plugins/PluginErrorBoundary.tsx  # was WidgetErrorBoundary.tsx
  - frontend/src/components/map-plugins/index.ts
  - frontend/src/components/map-plugins/registry.ts
  - frontend/src/components/map-plugins/register-plugins.ts      # was register-widgets.ts
  - frontend/src/components/map-plugins/types.ts
  - frontend/src/components/map-plugins/plugin-availability.ts   # was widget-availability.ts
  - frontend/src/components/map-plugins/builtin/MeasurementPlugin.tsx  # was MeasurementWidget.tsx
  - frontend/src/components/map-plugins/builtin/LegendPlugin.tsx       # was LegendWidget.tsx
  - frontend/src/components/map-plugins/__tests__/PluginHost.test.tsx          # was WidgetHost.test.tsx
  - frontend/src/components/map-plugins/__tests__/registry.test.ts
  - frontend/src/components/map-plugins/__tests__/plugin-availability.test.ts  # was widget-availability.test.ts
  # Store move:
  - frontend/src/stores/map-plugin-store.ts                     # was map-widget-store.ts
  - frontend/src/stores/__tests__/map-plugin-store.test.ts      # was map-widget-store.test.ts
  - frontend/src/stores/__tests__/store-relocation-smoke.test.ts
  # Consumers (import path + identifier rewrite):
  - frontend/src/components/admin/settings/SettingsMapTab.tsx
  - frontend/src/components/builder/SettingsEditorScene.tsx
  - frontend/src/components/builder/BuilderMap.tsx
  - frontend/src/components/builder/MapToolbar.tsx
  - frontend/src/components/builder/ActiveFilterChips.tsx
  - frontend/src/components/builder/DEMEditorScene.tsx
  - frontend/src/components/builder/builder-action-contract.ts
  - frontend/src/components/builder/map-stack.ts
  - frontend/src/components/viewer/ViewerMap.tsx
  - frontend/src/components/map/MapCoordReadout.tsx
  - frontend/src/pages/MapBuilderPage.tsx
  # Consumer tests:
  - frontend/src/components/builder/__tests__/MapBuilderPage.settings-wiring.test.tsx
  - frontend/src/components/builder/__tests__/MapBuilderPage.notes-ai-rail.test.tsx
  - frontend/src/components/builder/__tests__/MapBuilderPage.a11y.test.tsx
  - frontend/src/components/builder/__tests__/SettingsEditorScene.test.tsx
  - frontend/src/components/builder/__tests__/SettingsEditorScene.widgets.test.tsx  # renamed to SettingsEditorScene.plugins.test.tsx
  - frontend/src/components/builder/__tests__/ActiveFilterChips.test.tsx
  - frontend/src/components/builder/__tests__/BuilderMap.terrain-visibility.test.tsx
  - frontend/src/components/builder/__tests__/BuilderMap.a11y.test.tsx
  - frontend/src/components/builder/__tests__/DEMEditorScene.test.tsx
  - frontend/src/components/builder/__tests__/MapToolbar.test.tsx
  - frontend/src/components/builder/__tests__/MapBuilderPage.notes-ai-rail.test.tsx
  - frontend/src/pages/__tests__/MapBuilderPage.header-actions.test.tsx
  - frontend/src/pages/__tests__/PublicMapViewerPage.test.tsx
autonomous: true
requirements: [FE-RENAME-01, FE-RENAME-02, FE-RENAME-04]

must_haves:
  truths:
    - "The directory frontend/src/components/map-widgets/ no longer exists; frontend/src/components/map-plugins/ holds all 10 entries (incl. __tests__/ and builtin/)"
    - "No import in frontend/src resolves to @/components/map-widgets or @/stores/map-widget-store"
    - "No Widget* platform identifier (WidgetHost/WidgetPanel/WidgetErrorBoundary/WidgetDefinition/WidgetContext/registerWidget/getWidgets/getEnabledWidgetDefinitions/isWidgetIdAvailable/useWidgetStore/WidgetState/activeWidgets) remains in frontend/src outside i18n locale JSON"
    - "The registry ID string literals 'measurement' and 'legend' are unchanged"
  artifacts:
    - path: "frontend/src/components/map-plugins/PluginHost.tsx"
      provides: "Renamed plugin host component (was WidgetHost)"
      contains: "PluginHost"
    - path: "frontend/src/components/map-plugins/registry.ts"
      provides: "Plugin registry with registerPlugin/getPlugins exports; preserves measurement+legend IDs"
      contains: "registerPlugin"
    - path: "frontend/src/components/map-plugins/plugin-availability.ts"
      provides: "isPluginIdAvailable / getEnabledPluginDefinitions availability gating (was widget-availability)"
      contains: "isPluginIdAvailable"
    - path: "frontend/src/stores/map-plugin-store.ts"
      provides: "usePluginStore zustand store (was useWidgetStore)"
      contains: "usePluginStore"
  key_links:
    - from: "frontend/src/components/builder/MapToolbar.tsx"
      to: "frontend/src/components/map-plugins"
      via: "import getEnabledPluginDefinitions"
      pattern: "from ['\"]@/components/map-plugins"
    - from: "frontend/src/components/builder/BuilderMap.tsx"
      to: "frontend/src/components/map-plugins"
      via: "import isPluginIdAvailable"
      pattern: "isPluginIdAvailable"
---

<objective>
Rename the frontend map-plugin platform's directory, files, and ALL `Widget*` platform identifiers to `Plugin*` — a mechanical, atomic rename that must land in one wave so the TypeScript build stays consistent. Move `frontend/src/components/map-widgets/` → `map-plugins/` (all 10 entries) and `frontend/src/stores/map-widget-store.ts` → `map-plugin-store.ts`, rename the symbols they export, and rewrite every import/usage across the app.

This plan covers FE-RENAME-01 (directory move + import paths), FE-RENAME-02 (identifier rename), and FE-RENAME-04 (preserve the `measurement`/`legend` ID literals). It deliberately does NOT touch the API contract surface (`types/api.ts`, `api/settings.ts`, `api/maps.ts`, `lib/query-keys.ts`, `lib/normalize-saved-map.ts`, `hooks/use-settings.ts`) — that is plan 02. Splitting this way keeps file ownership exclusive between the two plans except for a small overlap (see note in plan 02), so they run sequentially (02 depends on 01).

Purpose: Give the frontend extensibility surface the honest "plugin" name and unblock the queued plugin-platform roadmap. The frontend is currently consistent internally (it still says "widget" everywhere) but is BROKEN against the Phase-1161 backend at the API boundary — this plan does the vocabulary half, plan 02 does the contract half.
Output: A `map-plugins/` directory + `map-plugin-store.ts` with `Plugin*` symbols; every consumer updated; `measurement`/`legend` IDs untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1161-backend-rename-and-contract/1161-02-SUMMARY.md

<critical_constraints>
1. **PRESERVE the ID string literals `'measurement'` and `'legend'`** everywhere. These are registry identifiers, NOT the word "widget". They appear as the `id` field on the two built-in plugin definitions and as values in tests. NEVER change these string values. You ARE renaming the COMPONENT names `MeasurementWidget`→`MeasurementPlugin` and `LegendWidget`→`LegendPlugin` and the FILE names, but the `id: 'measurement'` / `id: 'legend'` literals stay byte-for-byte.

2. **i18n locale JSON is OUT OF SCOPE for this phase.** Do NOT edit any file under `frontend/src/i18n/locales/`. The ~64 `widget*` i18n KEYS are renamed in Phase 1163. In this plan you keep calling the existing i18n keys (e.g. `t('...widgets...')`) exactly as they are today. If — and only if — leaving an i18n key reference would break `npm run typecheck` (it will NOT, because i18n keys are plain strings, not typed), prefer leaving the key to 1163. Expect zero i18n edits.

3. **This is a HARD breaking cut.** No `widget` alias, no re-export shim, no back-compat. After this plan, `@/components/map-widgets` and `@/stores/map-widget-store` must not resolve.

4. **Use `git mv` for the file/dir moves** so history is preserved and the old paths are removed in the same operation. Do the moves FIRST, then edit contents. Do NOT hand-create new files and delete old ones.

5. **Atomic rename — the build is red mid-flight by design.** Do not commit until `npm run typecheck` is 0 and the moved unit suites pass. A partial rename (some files moved, some imports not updated) will not typecheck; that is expected during the work and must be fully resolved before the commit.
</critical_constraints>

<interfaces>
<!-- VERIFIED inventory from the live tree at 5779785b. The executor MUST re-grep
     each file before editing (line numbers drift) — pattern established in 1161-02
     SUMMARY: "trust the live code, re-grep before each edit". These are the
     identifiers to rename, not exact line cites. -->

map-widgets/ directory (10 entries, all move to map-plugins/):
  WidgetErrorBoundary.tsx   → PluginErrorBoundary.tsx
  WidgetHost.tsx            → PluginHost.tsx
  WidgetPanel.tsx           → PluginPanel.tsx
  index.ts                  → index.ts            (barrel; rewrite its re-exports)
  register-widgets.ts       → register-plugins.ts
  registry.ts               → registry.ts         (rename symbols inside)
  types.ts                  → types.ts            (rename WidgetDefinition/WidgetContext etc.)
  widget-availability.ts    → plugin-availability.ts
  __tests__/WidgetHost.test.tsx          → __tests__/PluginHost.test.tsx
  __tests__/registry.test.ts             → __tests__/registry.test.ts
  __tests__/widget-availability.test.ts  → __tests__/plugin-availability.test.ts
  builtin/MeasurementWidget.tsx → builtin/MeasurementPlugin.tsx  (id:'measurement' UNCHANGED)
  builtin/LegendWidget.tsx      → builtin/LegendPlugin.tsx        (id:'legend' UNCHANGED)

store (separate dir):
  stores/map-widget-store.ts                     → stores/map-plugin-store.ts
  stores/__tests__/map-widget-store.test.ts      → stores/__tests__/map-plugin-store.test.ts
  stores/__tests__/store-relocation-smoke.test.ts  (NOT renamed — edit its widget refs)

CONFIRMED cross-module imports that must be rewritten (grep -rn "map-widgets"):
  components/admin/settings/SettingsMapTab.tsx : import { getWidgets } from '@/components/map-widgets'
  components/builder/SettingsEditorScene.tsx   : import { getWidgets } from '@/components/map-widgets/registry'
  components/builder/BuilderMap.tsx            : import { isWidgetIdAvailable } from '@/components/map-widgets'
  components/builder/MapToolbar.tsx            : import { getEnabledWidgetDefinitions } from '@/components/map-widgets'
  + 6 vitest files with vi.mock('@/components/map-widgets'...) and vi.mock('@/components/map-widgets/registry'...)

CONFIRMED store symbols (stores/map-widget-store.ts):
  interface WidgetState            → PluginState
  export const useWidgetStore      → usePluginStore
  field activeWidgets: Set<string> → activePlugins: Set<string>
  (the open/close/toggle methods keep their names; only Widget→Plugin in symbol/field names)
</interfaces>

<identifier_mapping>
<!-- The complete Widget* → Plugin* rename table. Apply to ALL of frontend/src
     EXCEPT i18n locale JSON. The `measurement`/`legend` ID *string literals* are NOT here
     because they are NOT renamed. -->

| Old identifier / path                       | New identifier / path                       |
|---------------------------------------------|---------------------------------------------|
| dir `map-widgets/`                          | dir `map-plugins/`                          |
| `@/components/map-widgets`                   | `@/components/map-plugins`                   |
| `@/components/map-widgets/registry`         | `@/components/map-plugins/registry`         |
| file `WidgetHost.tsx`                        | `PluginHost.tsx`                            |
| file `WidgetPanel.tsx`                       | `PluginPanel.tsx`                          |
| file `WidgetErrorBoundary.tsx`              | `PluginErrorBoundary.tsx`                  |
| file `register-widgets.ts`                  | `register-plugins.ts`                      |
| file `widget-availability.ts`              | `plugin-availability.ts`                   |
| file `builtin/MeasurementWidget.tsx`        | `builtin/MeasurementPlugin.tsx`            |
| file `builtin/LegendWidget.tsx`             | `builtin/LegendPlugin.tsx`                 |
| `stores/map-widget-store.ts`                | `stores/map-plugin-store.ts`              |
| component `WidgetHost`                       | `PluginHost`                              |
| component `WidgetPanel`                      | `PluginPanel`                            |
| component `WidgetErrorBoundary`            | `PluginErrorBoundary`                    |
| component `MeasurementWidget`              | `MeasurementPlugin`                      |
| component `LegendWidget`                    | `LegendPlugin`                          |
| type `WidgetDefinition`                     | `PluginDefinition`                      |
| type `WidgetContext`                        | `PluginContext`                        |
| type/interface `WidgetState`               | `PluginState`                          |
| fn `registerWidget`                         | `registerPlugin`                       |
| fn `getWidgets`                             | `getPlugins`                          |
| fn `getEnabledWidgetDefinitions`           | `getEnabledPluginDefinitions`        |
| fn `isWidgetIdAvailable`                    | `isPluginIdAvailable`                |
| hook `useWidgetStore`                       | `usePluginStore`                     |
| field `activeWidgets`                       | `activePlugins`                      |
| any other local `widget`/`Widget` identifier (var, prop, comment, test name) referring to the platform | corresponding `plugin`/`Plugin` |

NOTE: `register-widgets.ts` may export a function whose name differs from the file name (e.g. `registerBuiltinWidgets` or it may run side-effecting `registerWidget(...)` calls). Re-grep the file and rename ALL Widget* symbols + the file name; preserve the `id:'measurement'`/`id:'legend'` arguments.
</identifier_mapping>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv the directory + store files to the plugin names</name>
  <files>
    frontend/src/components/map-widgets/* → frontend/src/components/map-plugins/* (all 10 entries),
    frontend/src/stores/map-widget-store.ts → frontend/src/stores/map-plugin-store.ts,
    frontend/src/stores/__tests__/map-widget-store.test.ts → frontend/src/stores/__tests__/map-plugin-store.test.ts
  </files>
  <action>
    Move files ONLY in this task (no content edits yet). Use `git mv` so history is preserved and old paths are deleted atomically.

    Steps (run from repo root /Users/ishiland/Code/geolens):
    1. `git mv frontend/src/components/map-widgets frontend/src/components/map-plugins` (moves the whole dir incl. __tests__/ and builtin/).
    2. Inside the moved dir, rename the individual files:
       - `git mv frontend/src/components/map-plugins/WidgetHost.tsx frontend/src/components/map-plugins/PluginHost.tsx`
       - `git mv frontend/src/components/map-plugins/WidgetPanel.tsx frontend/src/components/map-plugins/PluginPanel.tsx`
       - `git mv frontend/src/components/map-plugins/WidgetErrorBoundary.tsx frontend/src/components/map-plugins/PluginErrorBoundary.tsx`
       - `git mv frontend/src/components/map-plugins/register-widgets.ts frontend/src/components/map-plugins/register-plugins.ts`
       - `git mv frontend/src/components/map-plugins/widget-availability.ts frontend/src/components/map-plugins/plugin-availability.ts`
       - `git mv frontend/src/components/map-plugins/builtin/MeasurementWidget.tsx frontend/src/components/map-plugins/builtin/MeasurementPlugin.tsx`
       - `git mv frontend/src/components/map-plugins/builtin/LegendWidget.tsx frontend/src/components/map-plugins/builtin/LegendPlugin.tsx`
       - `git mv frontend/src/components/map-plugins/__tests__/WidgetHost.test.tsx frontend/src/components/map-plugins/__tests__/PluginHost.test.tsx`
       - `git mv frontend/src/components/map-plugins/__tests__/widget-availability.test.ts frontend/src/components/map-plugins/__tests__/plugin-availability.test.ts`
       - (`index.ts`, `registry.ts`, `types.ts`, `__tests__/registry.test.ts` keep their filenames — do NOT move them.)
    3. Move the store + its test:
       - `git mv frontend/src/stores/map-widget-store.ts frontend/src/stores/map-plugin-store.ts`
       - `git mv frontend/src/stores/__tests__/map-widget-store.test.ts frontend/src/stores/__tests__/map-plugin-store.test.ts`
    Do NOT edit file CONTENTS in this task — only move. The build will be red after this step; that is expected and resolved by Tasks 2-3.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && test ! -d frontend/src/components/map-widgets && test -f frontend/src/components/map-plugins/PluginHost.tsx && test -f frontend/src/components/map-plugins/builtin/MeasurementPlugin.tsx && test -f frontend/src/stores/map-plugin-store.ts && echo MOVES_OK</automated>
  </verify>
  <done>map-widgets/ dir is gone; map-plugins/ has all 10 entries with the renamed filenames; map-plugin-store.ts exists. No content edited yet.</done>
</task>

<task type="auto">
  <name>Task 2: Rename Widget* symbols inside the moved map-plugins/ + store files</name>
  <files>
    frontend/src/components/map-plugins/index.ts,
    frontend/src/components/map-plugins/types.ts,
    frontend/src/components/map-plugins/registry.ts,
    frontend/src/components/map-plugins/register-plugins.ts,
    frontend/src/components/map-plugins/plugin-availability.ts,
    frontend/src/components/map-plugins/PluginHost.tsx,
    frontend/src/components/map-plugins/PluginPanel.tsx,
    frontend/src/components/map-plugins/PluginErrorBoundary.tsx,
    frontend/src/components/map-plugins/builtin/MeasurementPlugin.tsx,
    frontend/src/components/map-plugins/builtin/LegendPlugin.tsx,
    frontend/src/stores/map-plugin-store.ts,
    frontend/src/components/map-plugins/__tests__/PluginHost.test.tsx,
    frontend/src/components/map-plugins/__tests__/registry.test.ts,
    frontend/src/components/map-plugins/__tests__/plugin-availability.test.ts,
    frontend/src/stores/__tests__/map-plugin-store.test.ts,
    frontend/src/stores/__tests__/store-relocation-smoke.test.ts
  </files>
  <action>
    Rename every `Widget*` platform identifier to `Plugin*` INSIDE the moved files, per the identifier_mapping table. Process each file with Read-then-Edit (re-grep first — line numbers from the inventory may have drifted).

    For EACH file above:
    1. Run `grep -nE 'Widget|widget|activeWidgets' <file>` to see live occurrences.
    2. Rewrite: component names, type/interface names (`WidgetDefinition`→`PluginDefinition`, `WidgetContext`→`PluginContext`, `WidgetState`→`PluginState`), function names (`registerWidget`→`registerPlugin`, `getWidgets`→`getPlugins`, `getEnabledWidgetDefinitions`→`getEnabledPluginDefinitions`, `isWidgetIdAvailable`→`isPluginIdAvailable`), the store hook (`useWidgetStore`→`usePluginStore`) and its field (`activeWidgets`→`activePlugins`), local vars, JSDoc/comments, and any internal cross-imports between these files (e.g. `index.ts` re-exports from `./WidgetHost` → `./PluginHost`; `registry.ts` or `register-plugins.ts` imports `./builtin/MeasurementWidget` → `./builtin/MeasurementPlugin`).
    3. CRITICAL — in `registry.ts`, `register-plugins.ts`, both `builtin/*Plugin.tsx`, and the test files: the `id:` field / argument string values `'measurement'` and `'legend'` are NOT touched. Verify with `grep -n "'measurement'\|'legend'"` after editing that they are still present and unchanged. You ARE renaming the surrounding component identifiers and the import paths to those component files.
    4. In `store-relocation-smoke.test.ts`: this file tests that the store import resolves from its new location — update its `@/stores/map-widget-store` reference to `@/stores/map-plugin-store` and any `useWidgetStore`/`activeWidgets` symbols. (It is NOT renamed — keep the filename.)
    5. In the moved test files, also rename `describe`/`it` titles and any local `widget` test-fixture variable names to plugin (these are not user-facing i18n).
    Do NOT edit any file under `frontend/src/i18n/locales/`. Do NOT edit `types/api.ts`, `api/*`, `lib/query-keys.ts`, `lib/normalize-saved-map.ts`, `hooks/use-settings.ts` — those belong to plan 02.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && grep -rl -E 'Widget|widget' frontend/src/components/map-plugins frontend/src/stores/map-plugin-store.ts frontend/src/stores/__tests__/map-plugin-store.test.ts frontend/src/stores/__tests__/store-relocation-smoke.test.ts | grep -v '^$' ; echo "---measurement/legend literals still present (expect both)---" ; grep -rhoE "'(measurement|legend)'" frontend/src/components/map-plugins/builtin | sort -u