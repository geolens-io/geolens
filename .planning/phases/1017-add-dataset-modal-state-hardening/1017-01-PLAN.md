---
phase: 1017-add-dataset-modal-state-hardening
plan: 01
wave: 1
depends_on: []
autonomous: true
requirements: [ADDH-01, ADDH-02, ADDH-03, ADDH-04, ADDH-05]
subsystem: frontend-builder
tags: [map-builder, add-dataset, modal, playwright, tests]
files_modified:
  - e2e/builder.spec.ts
  - .planning/phases/1017-add-dataset-modal-state-hardening/1017-01-SUMMARY.md
  - .planning/phases/1017-add-dataset-modal-state-hardening/1017-VERIFICATION.md
---

<objective>
Harden the Add Dataset modal by proving tab/filter contracts, row expansion, stateful actions, import routing, and keyboard-accessible controls in the real builder.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1017-add-dataset-modal-state-hardening/1017-CONTEXT.md
@frontend/src/components/builder/DatasetSearchPanel.tsx
@frontend/src/components/builder/__tests__/DatasetSearchPanel.test.tsx
@e2e/builder.spec.ts
</context>

<tasks>
  <task type="auto">
    <name>Add browser coverage for modal expansion and import routing</name>
    <files>e2e/builder.spec.ts</files>
    <action>Add a builder E2E test that opens Add Dataset, verifies tabs and unsupported scope-chip absence, expands a data row via keyboard, checks metadata/action visibility, and follows `Import data...` to `/import`.</action>
    <verify>Assert the flow uses accessible roles/names and does not require a page reload for modal state.</verify>
    <done>ADDH-01, ADDH-02, ADDH-04, and ADDH-05 have browser-level proof.</done>
  </task>

  <task type="auto">
    <name>Run Add Dataset focused gates</name>
    <files>test outputs only</files>
    <action>Run focused modal component tests and builder smoke.</action>
    <verify>Record exact command results in summary and verification artifacts.</verify>
    <done>ADDH-03 remains covered by component and browser tests.</done>
  </task>

  <task type="auto">
    <name>Close phase state</name>
    <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, phase summary and verification artifacts</files>
    <action>Mark ADDH-01..05 complete and Phase 1017 complete after verification.</action>
    <verify>Roadmap, requirements, and state agree on Phase 1017 completion and next phase 1018.</verify>
    <done>Phase 1017 is ready to continue into Phase 1018.</done>
  </task>
</tasks>

<verification>
- `npx playwright test e2e/builder.spec.ts --project=chromium -g "Add Dataset data rows expose supported filters"`
- `cd frontend && npm run test -- DatasetSearchPanel --run`
- `npm run e2e:smoke:builder`
</verification>

<must_haves>
- No schema, migration, renderer, catalog endpoint, or import workflow changes.
- No Curated/Your imports/Public scope chips until backend contract exists.
- Do not duplicate import logic inside the modal.
</must_haves>

<success_criteria>
- [ ] Modal tabs remain All/Vector/Raster/Basemap.
- [ ] Unsupported scope chips remain absent.
- [ ] Data rows transition among Add/added/another-rendering without a full page reload.
- [ ] Expanded rows expose metadata and reachable primary actions.
- [ ] Import footer routes to existing ImportPage.
</success_criteria>
