# Phase 1026 Plan: Cluster Compatibility and QA Closeout

**Status:** Complete
**Milestone:** v1005 Builder Point Cluster Foundation
**Requirements:** COMP-01, COMP-02, COMP-03, COMP-04, QA-01, QA-02, QA-03, QA-04, QA-05

## Goal

Prove the Cluster renderer does not regress saved maps, style JSON, public/shared/embed viewers, or prior render modes, then archive v1005 with clear future-scope boundaries.

## Scope

- Preserve existing Map, MapLayer, Dataset, and Record schemas.
- Preserve existing Point, Symbol, Heatmap, Arrow, polygon, raster, DEM, terrain, public viewer, shared viewer, and embed viewer behavior.
- Add compatibility coverage for cluster intent in style JSON export/import.
- Add viewer timing coverage so bounded GeoJSON cluster data arriving after initial render triggers map sync.
- Run focused frontend/backend/i18n/lint/build/ruff/builder-smoke gates and Playwright MCP live browser verification.

## Implementation

1. Extend backend style JSON builder alias canonicalization for cluster builder keys.
2. Lock style JSON policy: standalone style export keeps vector tile fallback layers but preserves `style_config.render_mode = "cluster"` metadata for import.
3. Update `ViewerMap` layer sync dependencies so shared/public/embed cluster layers resync after bounded GeoJSON data arrives.
4. Add viewer regression coverage for shared cluster layers fetched with embed-token context.
5. Run all closeout verification gates and record outcomes.
6. Update requirements, state, roadmap, milestone audit, and milestone archives.

## Acceptance

- Saved cluster intent round-trips through style JSON metadata without schema drift.
- Viewer map sync receives eligible bounded GeoJSON after async load and does not require a later unrelated state change.
- Existing render modes remain covered by focused renderAs, adapter, map-sync, style-config, public viewer, and builder smoke tests.
- Playwright MCP verifies Cluster can be selected, saved, reloaded, and inspected with a clean current-page console.
