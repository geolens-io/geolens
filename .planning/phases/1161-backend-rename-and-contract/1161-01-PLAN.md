---
phase: 1161-backend-rename-and-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/0025_widgets_to_plugins_rename.py
  - backend/app/modules/catalog/maps/models.py
  - backend/app/core/persistent_config.py
  - backend/tests/test_migration_0025_plugins_rename.py
autonomous: true
requirements: [BE-RENAME-01, BE-RENAME-02, BE-RENAME-03]
must_haves:
  truths:
    - "A new Alembic migration with revision 0025_widgets_to_plugins_rename chains off the current head 0024 and renames catalog.maps.widgets to catalog.maps.plugins"
    - "The same migration renames the enabled_widgets persistent_config key to enabled_plugins via UPDATE persistent_config SET key=... preserving the stored value"
    - "The migration downgrade() reverses both renames symmetrically (catalog.maps.plugins->widgets and the config key plugins->widgets)"
    - "An upgrade->downgrade->upgrade round-trip leaves the schema consistent and preserves existing JSONB data"
    - "The Map SQLAlchemy model exposes plugins (not widgets) with the same JSONB/nullable type"
    - "persistent_config.py exposes the renamed ENABLED_PLUGINS config object with key='enabled_plugins'"
    - "The deployed 0001_baseline.py and 0024 migrations are not edited"
  artifacts:
    - path: "backend/alembic/versions/0025_widgets_to_plugins_rename.py"
      provides: "Reversible column + config-key rename migration"
      contains: "down_revision"
    - path: "backend/app/modules/catalog/maps/models.py"
      provides: "Map.plugins ORM column"
      contains: "plugins: Mapped"
    - path: "backend/app/core/persistent_config.py"
      provides: "ENABLED_PLUGINS config object with key enabled_plugins"
      contains: "enabled_plugins"
    - path: "backend/tests/test_migration_0025_plugins_rename.py"
      provides: "Upgrade/downgrade round-trip test for the 0025 rename migration"
      contains: "0025"
  key_links:
    - from: "backend/alembic/versions/0025_widgets_to_plugins_rename.py"
      to: "head revision 0024"
      via: "down_revision string literal"
      pattern: "down_revision[^\\n]*\"0024\""
    - from: "backend/app/modules/catalog/maps/models.py"
      to: "catalog.maps.plugins column"
      via: "mapped_column JSONB"
      pattern: "plugins:\\s*Mapped"
---

<objective>
Rename the backend persistence layer's widget vocabulary to plugin at the schema + ORM layer.
Create a reversible Alembic migration that renames the `catalog.maps.widgets` JSONB column to
`catalog.maps.plugins` and the `enabled_widgets` persistent-config key to `enabled_plugins`, then
update the `Map` SQLAlchemy model and `persistent_config.py` to match the migrated schema. Hard
breaking cut — no back-compat alias.

Purpose: Establishes the persistence foundation for the whole milestone. Plan 02 (API contract +
tests) and every downstream phase depend on the column and config key already existing under the new
names. The migration is reversible (downgrade + round-trip test) per BE-RENAME-01/02.
Output: New migration `0025_widgets_to_plugins_rename.py`, updated model + persistent_config, and a
round-trip migration test proving reversibility and data preservation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The current Alembic head. The new migration MUST chain off THIS revision.
# CRITICAL: the revision id of this file is the STRING "0024" (see line 18 below) — NOT
# "0024_maps_og_image_uri" (that is only the filename) and NOT the brief's fictional
# "a3f8c21d9e04". Copy this file's module structure (typing.Union imports + the four
# module-level revision vars) exactly.
@backend/alembic/versions/0024_maps_og_image_uri.py

# Precedent for op.alter_column / add_column with schema="catalog" (the maps table is in the
# catalog schema, NOT public). Mirror its schema= usage.
@backend/alembic/versions/0011_map_basemap_config.py

<interfaces>
<!-- VALIDATED 2026-05-30 via git grep. Trust these line numbers over the brief/REQUIREMENTS,
     which drifted (REQUIREMENTS says model :87 [correct], persistent_config :128/:675, but there is
     NO separate `if key == "enabled_widgets"` default branch — the only refs are listed below). -->

Head migration module shape (backend/alembic/versions/0024_maps_og_image_uri.py:13-21):
```python
from typing import Union

import sqlalchemy as sa
from alembic import op

revision: str = "0024"
down_revision: Union[str, None] = "0023_geolens_readonly_role"
branch_labels: Union[str, tuple[str, ...], None] = None
depends_on: Union[str, tuple[str, ...], None] = None
```
=> the new file uses `revision = "0025_widgets_to_plugins_rename"` and `down_revision = "0024"`.

alter_column-with-schema precedent (backend/alembic/versions/0011_map_basemap_config.py:24,29):
```python
    op.add_column("maps", sa.Column("basemap_config", ...), schema="catalog")
    ...
    op.drop_column("maps", "basemap_config", schema="catalog")
```
=> column ops on the maps table MUST pass `schema="catalog"`.

Map model column (backend/app/modules/catalog/maps/models.py:86-87):
```python
    # Active widget IDs (null = use client defaults, [] = no widgets)
    widgets: Mapped[list | None] = mapped_column(JSONB, nullable=True, default=None)
```
Rename attr `widgets` -> `plugins` and update the comment to plugin vocabulary. Type/nullable/default
UNCHANGED.

persistent_config.py widget refs — the COMPLETE set (backend/app/core/persistent_config.py):
```python
# :128  comment inside env_default property
        # ENABLED_WIDGETS = None means "all widgets enabled"), so we
# :673  section header comment
# -- Widgets --
# :674  config object name
ENABLED_WIDGETS = PersistentConfig[list[str] | None](
# :675  the persisted key string (this is what the migration renames in the DB)
    key="enabled_widgets",
# :679  human label
    label="Enabled Widgets",
```
Rename: the object `ENABLED_WIDGETS` -> `ENABLED_PLUGINS`, `key="enabled_plugins"`, the comment at
:128 and header at :673 to plugin wording, and `label="Enabled Plugins"`. (The `ENABLED_WIDGETS`
object is also imported in settings/router.py — that consumer rename is plan 02, wave 2, so this
plan's verify uses an isolated import of persistent_config, NOT app.api.main.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the reversible 0025 widgets->plugins rename migration</name>
  <files>backend/alembic/versions/0025_widgets_to_plugins_rename.py</files>
  <action>
    Create a new Alembic migration that mirrors the module structure of
    0024_maps_og_image_uri.py exactly (same `from typing import Union`, `import sqlalchemy as sa`,
    `from alembic import op`, and the four module-level vars). Set
    `revision: str = "0025_widgets_to_plugins_rename"` and `down_revision: Union[str, None] = "0024"`
    — "0024" is the REAL head revision id (the file's revision string, confirmed via grep), NOT the
    filename and NOT the brief's `a3f8c21d9e04`. Leave `branch_labels`/`depends_on` as None.

    upgrade() does BOTH renames (BE-RENAME-01 + BE-RENAME-02):
      1. `op.alter_column("maps", "widgets", new_column_name="plugins", schema="catalog")` — the maps
         table is in the `catalog` schema (precedent: 0011_map_basemap_config.py uses
         `schema="catalog"`). Prefer `alter_column(new_column_name=...)` over raw SQL so it stays
         dialect-portable; on Postgres it emits `ALTER TABLE catalog.maps RENAME COLUMN widgets TO
         plugins`. Renaming preserves the existing JSONB array data — no drop/recreate, no transform.
      2. `op.execute("UPDATE persistent_config SET key='enabled_plugins' WHERE key='enabled_widgets'")`
         — renames the config-key row in place, preserving its stored value. (persistent_config is in
         the default/public schema — do NOT prefix it with catalog. If the existing migrations
         qualify it differently, match their convention; grep `persistent_config` in alembic/versions
         to confirm before writing.)

    downgrade() reverses BOTH symmetrically:
      1. `op.alter_column("maps", "plugins", new_column_name="widgets", schema="catalog")`.
      2. `op.execute("UPDATE persistent_config SET key='enabled_widgets' WHERE key='enabled_plugins'")`.

    Write a module docstring describing the rename (mirror 0024's docstring style). Do NOT edit
    0001_baseline.py or 0024 (both deployed). Do NOT rename the `measurement`/`legend` ID VALUES — the
    migration only touches the column NAME and the config KEY, never row values.
  </action>
  <verify>
    <automated>cd backend && set -a && source ../.env.test && set +a && uv run python -c "from alembic.config import Config; from alembic.script import ScriptDirectory; s=ScriptDirectory.from_config(Config('alembic.ini')); rev=s.get_revision('0025_widgets_to_plugins_rename'); assert rev is not None, 'revision not found'; assert rev.down_revision=='0024', f'down_revision is {rev.down_revision!r}'; assert s.get_current_head()=='0025_widgets_to_plugins_rename', f'head is {s.get_current_head()!r}'; print('migration graph OK: 0025 head, down_revision 0024')"</automated>
  </verify>
  <done>
    File `0025_widgets_to_plugins_rename.py` exists; its `down_revision == "0024"`; the script
    directory resolves `0025_widgets_to_plugins_rename` as the single head (no branch conflict);
    upgrade() renames both `catalog.maps.widgets`->`plugins` and the `enabled_widgets`->`enabled_plugins`
    config key; downgrade() reverses both. 0001_baseline.py and 0024 are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add upgrade/downgrade round-trip migration test</name>
  <files>backend/tests/test_migration_0025_plugins_rename.py</files>
  <action>
    Create a NEW pytest module testing the 0025 migration end-to-end (BE-RENAME-01 acceptance: an
    upgrade/downgrade round-trip test). There is no existing alembic round-trip test infra in
    backend/tests — before writing, grep backend/tests/conftest.py for the DB engine/connection
    fixture and any alembic helper, and follow the project's async test DB pattern (host-port recipe:
    the suite needs `.env.test` sourced; POSTGRES_HOST=localhost, port 5434).

    The test must, against the test database:
      1. Ensure schema is at head `0025_widgets_to_plugins_rename` (run `alembic upgrade head` via
         `alembic.command.upgrade(Config("alembic.ini"), "head")`, or reuse whatever migration the
         conftest already applies).
      2. Assert post-upgrade state: inspect `catalog.maps` columns — `plugins` present, `widgets`
         absent (use `sqlalchemy.inspect(engine).get_columns("maps", schema="catalog")`). Seed a
         `persistent_config` row with key `enabled_plugins` and a value like `["legend"]`.
      3. Downgrade exactly one step (`alembic.command.downgrade(cfg, "-1")`) and assert the reverse:
         `widgets` present, `plugins` absent; the config row is back under key `enabled_widgets` with
         its value preserved (`["legend"]`).
      4. Upgrade again (`alembic.command.upgrade(cfg, "+1")` or "head") and re-assert the post-upgrade
         state, proving the round-trip is idempotent and data survives.

    Keep the seed value using a real plugin ID (`legend`/`measurement`) to also document that ID
    values are preserved across the rename. If running real alembic up/down against the shared test DB
    is impractical under the parallel `-n 4` fixture model, instead assert the migration's
    `upgrade()`/`downgrade()` against an isolated SQLite-or-throwaway-Postgres connection created in
    the test (do NOT mutate the session-shared schema other tests depend on). Pick whichever matches
    the conftest's isolation model — document the choice in a module docstring.
  </action>
  <verify>
    <automated>cd backend && set -a && source ../.env.test && set +a && uv run pytest tests/test_migration_0025_plugins_rename.py -x -q</automated>
  </verify>
  <done>
    Round-trip test passes: after upgrade `catalog.maps.plugins` exists and `widgets` does not; after a
    one-step downgrade the column and the config key revert to `widgets`/`enabled_widgets` with values
    preserved; after re-upgrade the plugin names are restored. Test does not corrupt shared test-DB
    state for other tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rename Map.widgets->Map.plugins and persistent_config ENABLED_WIDGETS->ENABLED_PLUGINS</name>
  <files>backend/app/modules/catalog/maps/models.py, backend/app/core/persistent_config.py</files>
  <behavior>
    - `from app.modules.catalog.maps.models import Map; assert hasattr(Map, "plugins") and not hasattr(Map, "widgets")`.
    - `from app.core.persistent_config import ENABLED_PLUGINS; assert ENABLED_PLUGINS.key == "enabled_plugins"` and the old name `ENABLED_WIDGETS` no longer exists in the module (BE-RENAME-03).
    - Importing persistent_config in isolation succeeds (no NameError). NOTE: importing app.api.main will FAIL until plan 02 renames the settings/router.py `ENABLED_WIDGETS` consumer — that is expected and is verified in plan 02, not here.
  </behavior>
  <action>
    models.py:86-87 — rename the attribute `widgets` -> `plugins` (per BE-RENAME-03), keep
    `Mapped[list | None] = mapped_column(JSONB, nullable=True, default=None)` unchanged; update the
    `# Active widget IDs ...` comment to plugin wording. The migration (Task 1) renamed the underlying
    DB column to `plugins` so the ORM attribute must match.

    persistent_config.py — rename ALL refs from the validated set (BE-RENAME-03): object
    `ENABLED_WIDGETS` -> `ENABLED_PLUGINS` (:674), `key="enabled_widgets"` -> `key="enabled_plugins"`
    (:675), `label="Enabled Widgets"` -> `label="Enabled Plugins"` (:679), the `# -- Widgets --`
    header (:673) -> `# -- Plugins --`, and the comment at :128 (`ENABLED_WIDGETS = None means "all
    widgets enabled"`) to plugin wording. Do NOT rename `measurement`/`legend` (not present here, but
    invariant). After editing, grep both files case-insensitively for `widget`: ZERO matches allowed.
  </action>
  <verify>
    <automated>cd backend && set -a && source ../.env.test && set +a && uv run python -c "from app.modules.catalog.maps.models import Map; assert hasattr(Map,'plugins') and not hasattr(Map,'widgets'), 'model not renamed'; import app.core.persistent_config as pc; assert hasattr(pc,'ENABLED_PLUGINS') and pc.ENABLED_PLUGINS.key=='enabled_plugins' and not hasattr(pc,'ENABLED_WIDGETS'), 'config obj not renamed'; print('model+config OK')" && { if grep -riq "widget" app/modules/catalog/maps/models.py app/core/persistent_config.py; then echo "FAIL: widget refs remain"; grep -rni "widget" app/modules/catalog/maps/models.py app/core/persistent_config.py; exit 1; else echo "grep clean"; fi; }</automated>
  </verify>
  <done>
    `Map.plugins` exists (no `widgets`); `ENABLED_PLUGINS` exists with `key="enabled_plugins"` (no
    `ENABLED_WIDGETS`); both files have zero case-insensitive `widget` matches; persistent_config
    imports in isolation. (app.api.main import is intentionally still broken until plan 02.)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → DB (alembic) | `alembic upgrade head` mutates production schema + a live config row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1161-01 | Denial of Service | 0025 migration on a large `catalog.maps` | accept | `RENAME COLUMN` is an O(1) catalog-only metadata operation in Postgres (no table rewrite, no row scan); safe on any table size. |
| T-1161-02 | Tampering | downgrade path data integrity | mitigate | Round-trip test (Task 2) proves downgrade restores the original column/key names AND preserves row values; rename never touches data. |
| T-1161-03 | Repudiation | config-key UPDATE silently affecting 0 or >1 rows | accept | The `WHERE key='enabled_widgets'` UPDATE is exact-match on a unique key; 0 rows (feature never configured) is a valid no-op, and the key is unique so >1 is impossible. |
| T-1161-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies introduced in this plan — uses existing alembic/sqlalchemy. No package-install task; legitimacy gate N/A. |
</threat_model>

<verification>
- `0025_widgets_to_plugins_rename` resolves as the single alembic head with `down_revision == "0024"`.
- Round-trip test green: upgrade -> `catalog.maps.plugins` (no `widgets`); downgrade -> `widgets` (no `plugins`) with config value preserved; re-upgrade restores plugin names.
- `Map.plugins` + `ENABLED_PLUGINS` (key `enabled_plugins`) exist; no `widget` strings remain in models.py / persistent_config.py.
- 0001_baseline.py and 0024 are byte-for-byte unchanged (`git diff --stat` shows them untouched).
</verification>

<success_criteria>
- Reversible 0025 migration created, chained off head `0024`, renaming both the `catalog.maps.widgets` column and the `enabled_widgets` config key, with a symmetric downgrade (BE-RENAME-01, BE-RENAME-02).
- `Map` model exposes `plugins`; `persistent_config.py` exposes `ENABLED_PLUGINS`/`enabled_plugins` (BE-RENAME-03).
- Upgrade/downgrade round-trip test passes and preserves data.
</success_criteria>

<output>
Create `.planning/phases/1161-backend-rename-and-contract/1161-01-SUMMARY.md` when done.
</output>
