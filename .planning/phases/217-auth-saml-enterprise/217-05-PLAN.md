---
phase: 217-auth-saml-enterprise
plan: 05
type: execute
wave: 5
depends_on: [217-02, 217-03, 217-04]
files_modified:
  - backend/app/core/identity.py
  - backend/app/platform/extensions/defaults.py
  - backend/tests/test_extensions.py
  - docs/saml.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [SAML-08, SAML-11, SAML-12]
tags: [docstring-scrub, verification-gate, docs, phase-close]
must_haves:
  truths:
    - "Three SAML mentions in core docstrings replaced with neutral 'enterprise auth overlay' phrasing"
    - "git grep -i saml backend/ with carve-outs (alembic, fixtures, test_saml_overlay) returns zero hits"
    - "alembic check clean against enterprise overlay"
    - "Full backend pytest passes (2001 baseline + 10+ new SAML tests)"
    - "Enterprise overlay pytest passes"
    - "ruff check clean across modified backend files"
    - "All 5 ROADMAP §Phase 217 success criteria PASS"
    - "docs/saml.md exists with install + IdP config + multi-instance limitation + NameID format guidance"
  artifacts:
    - path: "backend/app/core/identity.py"
      provides: "Scrubbed docstring lines 15-17 (D-16)"
      contains: "enterprise auth overlay"
    - path: "backend/app/platform/extensions/defaults.py"
      provides: "Scrubbed docstring line 36 (D-16)"
      contains: "enterprise auth overlays"
    - path: "backend/tests/test_extensions.py"
      provides: "Scrubbed docstring line 230 (D-16)"
      contains: "Enterprise auth overlays"
    - path: "docs/saml.md"
      provides: "User-facing SAML documentation (install, config, troubleshooting, limitations)"
      contains: "geolens-enterprise"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 217 marked complete; STATE.md current position updated"
  key_links:
    - from: "Phase 217"
      to: "5/5 success criteria PASS"
      via: "ROADMAP §Phase 217 verification gate task"
      pattern: "All 5 success criteria"
---

<objective>
Close Phase 217 by scrubbing the three remaining SAML mentions in core docstrings (D-16), shipping `docs/saml.md` for user-facing SAML documentation, and running the phase verification gate confirming all 5 ROADMAP success criteria pass.

Purpose: SAML-08 ("`git grep -i saml` returns zero matches outside test fixtures and `docs-internal/`") + final SC#1..5 verification gate. Plan 02-04 ship the SAML feature; this plan certifies the boundary is clean and the phase is shippable.

Output: 3 docstring scrubs; `docs/saml.md` (~150 lines, user-facing); ROADMAP/STATE updated; full verification gate output committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/217-auth-saml-enterprise/217-CONTEXT.md
@.planning/phases/217-auth-saml-enterprise/217-RESEARCH.md
@.planning/phases/217-auth-saml-enterprise/217-PATTERNS.md
@.planning/phases/217-auth-saml-enterprise/217-VALIDATION.md
@.planning/phases/217-auth-saml-enterprise/217-01-SUMMARY.md
@.planning/phases/217-auth-saml-enterprise/217-02-SUMMARY.md
@.planning/phases/217-auth-saml-enterprise/217-03-SUMMARY.md
@.planning/phases/217-auth-saml-enterprise/217-04-SUMMARY.md
@backend/app/core/identity.py
@backend/app/platform/extensions/defaults.py
@backend/tests/test_extensions.py

<interfaces>
<!-- Verbatim docstring replacements per CONTEXT.md D-16 -->

backend/app/core/identity.py:15-17 — CURRENT:
```
Phase 217 (auth-saml-enterprise) is the first concrete consumer of
``IdentityExtension``: a SAML overlay registers an alternate backend
under the ``geolens.extensions`` entry-point group with key ``"identity"``
and ``get_identity_extension()`` returns it on subsequent requests.
```

REPLACE WITH (D-16 verbatim):
```
An enterprise auth overlay (e.g., the ``geolens-enterprise`` package) is
the first concrete consumer of ``IdentityExtension``: it registers an
alternate backend under the ``geolens.extensions`` entry-point group
with key ``"identity"`` and ``get_identity_extension()`` returns it on
subsequent requests.
```

backend/app/platform/extensions/defaults.py:36 — CURRENT:
```
The async signature is intentional (Pitfall 8). Phase 217's SAML
overlay will perform DB lookups; the dep wire-in does
``await ext.resolve_identity_from_token(token, request, db)``, so
all implementations — community and enterprise — MUST be async.
```

REPLACE WITH (D-16 — replace "Phase 217's SAML overlay will perform" with "enterprise auth overlays may perform"):
```
The async signature is intentional (Pitfall 8). Enterprise auth
overlays may perform DB lookups; the dep wire-in does
``await ext.resolve_identity_from_token(token, request, db)``, so
all implementations — community and enterprise — MUST be async.
```

backend/tests/test_extensions.py:230-231 — CURRENT:
```
must not raise TypeError. Phase 217's SAML overlay relies on this
contract for its DB-lookup wire-in.
```

REPLACE WITH (D-16 verbatim):
```
must not raise TypeError. Enterprise auth overlays rely on this
contract for their DB-lookup wire-in.
```

SC#1 verification (RESEARCH §15 Q1 carve-out):
```bash
git grep -i saml backend/ \
  ':!backend/alembic/' \
  ':!backend/tests/fixtures/saml/' \
  ':!backend/tests/test_saml_overlay.py'
# Expected: zero hits
```

The carve-outs are necessary because:
- `backend/alembic/versions/` contains immutable migration history (`0002_initial_tables.py` originally added the SAML CHECK literal; `2026_04_08_0001-strip_dead_saml_code.py`'s entire purpose IS the literal `'saml'` enum value).
- `backend/tests/fixtures/saml/` contains test asset files generated in Plan 01.
- `backend/tests/test_saml_overlay.py` is the integration test file created in Plans 02-04.

The 3 docstring lines + the integration tests + the test fixtures + the alembic immutable history are EXPECTED matches; the SC#1 carve-out lets us verify "no SAML implementation code in core" — the spirit per RESEARCH §1 Finding 3.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 01: Scrub three SAML mentions in core docstrings (D-16)</name>
  <files>backend/app/core/identity.py, backend/app/platform/extensions/defaults.py, backend/tests/test_extensions.py</files>
  <read_first>
    - .planning/phases/217-auth-saml-enterprise/217-CONTEXT.md (D-16 verbatim replacement text)
    - .planning/phases/217-auth-saml-enterprise/217-PATTERNS.md (Detail 15 — verbatim before/after for all 3 docstrings)
    - .planning/phases/217-auth-saml-enterprise/217-RESEARCH.md (§15 Q1 — SC#1 carve-out justification)
    - backend/app/core/identity.py (lines 1-30 — confirm exact line numbers and surrounding docstring)
    - backend/app/platform/extensions/defaults.py (lines 25-50 — confirm exact docstring location)
    - backend/tests/test_extensions.py (lines 220-245 — confirm exact docstring location)
  </read_first>
  <action>
    EDIT `backend/app/core/identity.py`:
    Locate the existing 3-line SAML mention (currently around lines 15-17, but verify by reading; the file's module docstring or class docstring may have shifted). Replace verbatim with the D-16 block from `<interfaces>`. Preserve indentation and surrounding sentences. Do NOT change any other content in the file.

    EDIT `backend/app/platform/extensions/defaults.py`:
    Locate the existing line about "Phase 217's SAML overlay will perform DB lookups" (currently around line 36). Replace just the phrase "Phase 217's SAML overlay will perform DB lookups" with "Enterprise auth overlays may perform DB lookups" — preserve everything before and after that phrase.

    EDIT `backend/tests/test_extensions.py`:
    Locate the existing 2-line "Phase 217's SAML overlay relies on this" mention (currently around lines 230-231). Replace per `<interfaces>` block.

    AFTER all 3 edits, run the SC#1 verification grep:
    ```bash
    git grep -i saml backend/ \
      ':!backend/alembic/' \
      ':!backend/tests/fixtures/saml/' \
      ':!backend/tests/test_saml_overlay.py'
    ```
    EXPECTED: zero hits. If any remain, investigate (could be schema field name like `sp_entity_id` triggering — but `git grep -i saml` matches the literal string "saml", not "sp_entity"; field names without "saml" in them are fine per RESEARCH §15 Q3).

    If the grep finds matches in `backend/app/modules/auth/oauth/models.py` or `schemas.py` (unlikely — column/field names like `idp_entity_id` don't contain "saml"), audit and decide: scrub if possible without breaking semantics; otherwise extend the carve-out list with explicit pathspec exclusion and document in this plan's SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && bash -c 'count=$(git grep -i saml backend/ ":!backend/alembic/" ":!backend/tests/fixtures/saml/" ":!backend/tests/test_saml_overlay.py" | wc -l); if [ "$count" -ne "0" ]; then echo "FAIL: $count SAML mentions remain in core (after carve-outs):"; git grep -in saml backend/ ":!backend/alembic/" ":!backend/tests/fixtures/saml/" ":!backend/tests/test_saml_overlay.py" | head -20; exit 1; fi; echo "OK: zero SAML mentions in core (after carve-outs)"'</automated>
    Plus: `cd /Users/ishiland/Code/geolens && python -c "
expectations = {
    'backend/app/core/identity.py': ('enterprise auth overlay', 'Phase 217'),
    'backend/app/platform/extensions/defaults.py': ('Enterprise auth overlays may perform', 'Phase 217'),
    'backend/tests/test_extensions.py': ('Enterprise auth overlays rely on this', 'Phase 217'),
}
for path, (must_have, must_not_have) in expectations.items():
    src = open(path).read()
    assert must_have in src, f'{path} missing replacement: {must_have!r}'
    assert must_not_have not in src, f'{path} still mentions {must_not_have!r}'
print('OK: all 3 docstrings scrubbed')
"`
    Plus: `cd backend && uv run pytest tests/test_extensions.py -x` (existing tests still pass after docstring change).
  </verify>
  <acceptance_criteria>
    - 3 files modified with the verbatim D-16 replacements.
    - "Phase 217" string is GONE from all 3 files (verified by grep).
    - "enterprise auth overlay" / "Enterprise auth overlays" phrasing is PRESENT in all 3 files.
    - `git grep -i saml backend/ ':!backend/alembic/' ':!backend/tests/fixtures/saml/' ':!backend/tests/test_saml_overlay.py'` returns zero hits (SAML-08 SC#1 satisfied with the documented carve-outs).
    - `pytest tests/test_extensions.py` still passes (docstring changes do not affect test logic).
    - Existing 2001-test backend baseline still green.
  </acceptance_criteria>
  <done>
    SC#1 satisfied: zero SAML mentions in core source (with the 3-pathspec carve-out for alembic immutable history + test fixtures + the integration test file). The 3 docstring breadcrumbs now describe the IdentityExtension surface in terms of "enterprise auth overlays" generically, not Phase 217 specifically.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 02: Write docs/saml.md user-facing documentation</name>
  <files>docs/saml.md</files>
  <read_first>
    - .planning/phases/217-auth-saml-enterprise/217-CONTEXT.md (D-15 hardening defaults, D-09 multi-instance limitation, D-11 ADFS attribute keys, D-17 deferred items)
    - .planning/phases/217-auth-saml-enterprise/217-RESEARCH.md (§14 Pitfalls 3 NameID format, 4 clock skew, 5 multi-instance, 14 sp_entity_id; §15 Q6 doc location recommendation)
    - docs/cli.md (Phase 216 user-facing doc — closest precedent for shape; mirror its sectioning)
  </read_first>
  <action>
    Create `docs/saml.md` (~150 lines) with the following sections (mirror `docs/cli.md` sectioning style — verify by reading it first):

    1. **Top-of-doc note (RESEARCH §15 Q6):**
       > "SAML SSO requires the geolens-enterprise overlay (commercial license). See [Enterprise Edition](#) for installation. Community edition does not include SAML."

    2. **Overview**
       - What it does: SP-initiated SSO from the user's IdP.
       - What it's not in V1 per D-15 / Deferred Ideas: SLO, IdP-initiated SSO, SCIM, SP-side AuthnRequest signing.

    3. **Installation**
       - `docker compose -f docker-compose.yml -f docker-compose.enterprise.yml up -d --build`
       - Brief note that this triggers `e002_add_saml_columns` migration which adds 4 nullable columns + relaxes the OAuth provider type CHECK.

    4. **IdP Configuration**
       - Per-IdP walkthroughs (Okta, Azure AD, ADFS) — concise (5-10 lines each):
         - Where to configure SP entityID (`{public_api_url}/auth/saml/{slug}` default)
         - Where to configure ACS URL (`{public_api_url}/auth/saml/{slug}/acs`)
         - How to download SP metadata (admin UI "Download SP Metadata" button OR `GET /auth/saml/{slug}/metadata`)
         - Required attributes: `email`, `displayName`, `groups` (or whatever `group_claim` is set to)
         - **Pitfall 3 callout**: Request `NAMEID_FORMAT_PERSISTENT`, NOT `emailAddress` — emails change, persistent IDs don't.

    5. **GeoLens Configuration**
       - Navigate to `/admin/saml` (only visible with enterprise overlay loaded).
       - Form fields explained: `slug`, `display_name`, `idp_entity_id`, `idp_sso_url`, `idp_certificate` (PEM paste), `sp_entity_id` (with the Pitfall 14 warning about exact-match), `default_role`, `group_claim`, `group_role_mapping` (JSON).
       - Click "Enable" + Save.
       - Test login: navigate to `{public_app_url}/login`, click the SAML provider's display name, redirected to IdP, sign in, redirected back into GeoLens with JWT issued.

    6. **Hardening Defaults** (D-15 transparency)
       - `want_assertions_signed = True` (IdP MUST sign assertions; rejects unsigned).
       - `authn_requests_signed = False` (no SP keypair in V1; future iteration).
       - `allow_unsolicited = False` (rejects IdP-initiated SSO).
       - `accepted_time_diff = 60` seconds (clock-skew tolerance; Pitfall 4).
       - In-memory replay cache (TTL 600s).

    7. **Limitations**
       - **Multi-instance replay-cache hole** (D-15, Pitfall 5): document explicitly. Recommend single-instance OR sticky-session load balancing for V1. Redis-backed cache is the upgrade path when multi-instance demand surfaces.
       - **No SP signing key**: AuthnRequests are unsigned. Most IdPs accept this; ADFS configurations requiring signed AuthnRequests will need a future iteration.
       - **No SLO**: users log out of GeoLens but stay logged in at the IdP (and vice versa).
       - **No IdP-initiated SSO**: SP-initiated only.

    8. **Troubleshooting**
       - "Assertion expired / NotBefore in future" → check clock sync; consider raising `accepted_time_diff` (Pitfall 4).
       - "Audience mismatch" → check `sp_entity_id` matches IdP-side registration exactly (Pitfall 14).
       - "Signature invalid" → check that `idp_certificate` PEM is current and matches the IdP's signing key.
       - "User created with weird role / always default" → check `group_claim` matches the attribute name your IdP sends (try `groups`, `Groups`, or the IdP's URN form).
       - "User can't log in after IdP changed their email" → IdP is using `NAMEID_FORMAT_EMAILADDRESS`; switch to `NAMEID_FORMAT_PERSISTENT` (Pitfall 3).

    9. **Audit Logging**
       - All SAML provider mutations (create / update / delete) are logged to the standard audit log with old/new values for non-secret fields.
       - Secret fields (`idp_certificate`) are redacted as `<redacted>` in audit log payloads (Pitfall 9 mitigation).
       - Query via existing `/admin/audit` UI or audit log API.

    Per CONTEXT.md D-15 and RESEARCH §15 Q6: this doc lives at `docs/saml.md` (NOT `docs/enterprise/saml.md`) — discoverable for community users browsing enterprise features.

    Per RESEARCH §15 Q6 + CONTEXT.md "Whether to ship a docs/runbooks/saml-troubleshooting.md" Discretion: troubleshooting section above covers the common failures; do NOT ship a separate runbooks file (defer per Discretion).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && python -c "
import os
assert os.path.exists('docs/saml.md'), 'docs/saml.md missing'
src = open('docs/saml.md').read()
assert len(src) > 2000, f'docs/saml.md too thin: {len(src)} chars'
required_sections = ['geolens-enterprise', 'commercial license', 'Installation', 'IdP Configuration', 'GeoLens Configuration', 'Hardening', 'Limitations', 'Troubleshooting', 'Audit', 'NAMEID_FORMAT_PERSISTENT', 'accepted_time_diff', 'sp_entity_id', 'multi-instance', '<redacted>']
for s in required_sections:
    assert s in src, f'docs/saml.md missing section/keyword: {s}'
print('OK: docs/saml.md complete')
"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/saml.md` exists (~150 lines, ~3-5KB).
    - Top-of-doc note about commercial license + enterprise overlay requirement (RESEARCH §15 Q6).
    - All 9 sections present (Overview / Installation / IdP Configuration / GeoLens Configuration / Hardening / Limitations / Troubleshooting / Audit Logging / + top-of-doc note).
    - Per-IdP walkthroughs for at least Okta, Azure AD, ADFS.
    - Pitfall callouts for 3 (NameID), 4 (clock skew), 5 (multi-instance), 14 (sp_entity_id).
    - Hardening defaults explicitly listed (D-15 transparency).
    - Audit log redaction documented (Pitfall 9 user-facing transparency).
  </acceptance_criteria>
  <done>
    User-facing SAML documentation shipped. Enterprise admins have a clear setup path; community users see the commercial-license note up front; all known V1 limitations are documented.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 03: Phase 217 verification gate — confirm all 5 ROADMAP success criteria PASS</name>
  <files>(no file modifications — verification-only checkpoint)</files>
  <action>Run every command listed in `<how-to-verify>` below; report the SC# of any failure to the user. The user reviews and types "approved" or describes the failing SC. This is a blocking checkpoint — execute-plan must not proceed past it without an explicit approval signal.</action>
  <verify><automated>(checkpoint — no automated verify; commands listed in how-to-verify section are run interactively)</automated></verify>
  <done>User has typed "approved" — all 5 ROADMAP §Phase 217 success criteria PASS, all health checks (alembic, pytest, ruff, frontend) green.</done>
  <what-built>
    All 5 plans for Phase 217 should now be merged:
    - Plan 01: alembic graph repair + e002 migration + Wave 0 fixtures
    - Plan 02: scaffold modernization + dual-Protocol registration + 8 SAML tests
    - Plan 03: schema + audit log extension + 3 SAML tests
    - Plan 04: frontend admin SAML page + edition gating + community-404 test
    - Plan 05 task 01: docstring scrub
    - Plan 05 task 02: docs/saml.md

    The verification gate runs SC#1..5 from ROADMAP §Phase 217 plus all the technical health checks
    (alembic, full pytest, ruff, enterprise tests).
  </what-built>
  <how-to-verify>
    Run each verification command from the repo root and confirm the expected outcome. Report any
    failures with the SC number that failed.

    **SC#1 — git grep zero matches in core (with carve-outs):**
    ```bash
    git grep -i saml backend/ \
      ':!backend/alembic/' \
      ':!backend/tests/fixtures/saml/' \
      ':!backend/tests/test_saml_overlay.py'
    ```
    Expected: empty output (zero lines).

    **SC#2 — Admin UI SAML tab visibility (community vs enterprise):**
    ```bash
    # Community mode: tab hidden
    docker compose down && docker compose up -d
    # Open http://localhost:8080 in browser, log in as admin, confirm no "SAML SSO" nav item
    # Direct URL navigation:
    curl -fsS -H "Authorization: Bearer $ADMIN_TOKEN" http://localhost:8000/api/auth/saml/anyslug/login
    # Expected: 404 Not Found
    ```
    ```bash
    # Enterprise mode: tab visible
    docker compose down && docker compose -f docker-compose.yml -f docker-compose.enterprise.yml up -d --build
    # Open http://localhost:8080, log in as admin, confirm "SAML SSO" nav item exists
    # Direct URL navigation works (200 OK redirect to IdP if a SAML provider is configured)
    ```

    **SC#3 — SP-initiated SSO end-to-end (automated test):**
    ```bash
    cd backend && uv run pytest tests/test_saml_overlay.py::test_saml_acs_signed_assertion_jit_provisions_user tests/test_saml_overlay.py::test_saml_metadata_xml_valid -x
    ```
    Expected: both tests pass.

    **SC#4 — attribute → role mapping audited:**
    ```bash
    cd backend && uv run pytest tests/test_saml_overlay.py::test_saml_provider_update_logs_old_new_role_mapping tests/test_saml_overlay.py::test_saml_attribute_to_role_mapping_via_provider_group_claim tests/test_saml_overlay.py::test_saml_provider_update_redacts_secret_fields -x
    ```
    Expected: all three tests pass.

    **SC#5 — Phase 214 seam is the only registration point:**
    ```bash
    cd backend && uv run pytest tests/test_saml_overlay.py::test_saml_overlay_registers_under_identity_and_routers -x
    cd ~/Code/geolens-enterprise && uv run pytest tests/test_registration.py::test_saml_extension_dual_registered_under_auth_and_identity -x
    ```
    Expected: both pass.

    **Technical health checks:**
    ```bash
    # 1. alembic clean against enterprise overlay
    docker compose -f docker-compose.yml -f docker-compose.enterprise.yml run --rm migrate sh -c \
      "uv add --editable /enterprise && uv run alembic upgrade heads && uv run alembic check"
    # Expected: zero diff

    # 2. Full backend pytest (2001 baseline + new SAML tests)
    cd backend && uv run pytest -x
    # Expected: ALL pass

    # 3. Enterprise overlay pytest
    cd ~/Code/geolens-enterprise && uv run pytest -x
    # Expected: ALL pass (registration tests + SAML config tests + replay cache tests)

    # 4. Ruff clean
    cd backend && uv run ruff check
    # Expected: zero violations

    # 5. Frontend builds + tests pass
    cd frontend && npm run typecheck && npm run lint && npm run test && npm run build
    # Expected: ALL clean
    ```

    Confirm by responding with one of:
    - "approved" — all 5 SC PASS, all health checks clean
    - "<SC# failed>: <description>" — one or more SC failed; describe so the executor can return to the failing plan and fix
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 SC PASS and all health checks clean. Otherwise describe which SC# or health check failed.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 04: Mark Phase 217 complete in ROADMAP and STATE; commit final phase artifacts</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 217 entry — currently "Plans: TBD")
    - .planning/STATE.md (Current Position section + Roadmap Snapshot table)
    - All 5 SUMMARY files: .planning/phases/217-auth-saml-enterprise/217-{01..05}-SUMMARY.md
  </read_first>
  <action>
    EDIT `.planning/ROADMAP.md`:

    1. In the Phase 217 entry, change `- [ ]` to `- [x]` and add `(completed YYYY-MM-DD)` to the title.
    2. Replace `**Plans**: TBD` with `**Plans:** 5/5 plans complete (verified <date> — all 5 ROADMAP SC verified PASS)`.
    3. Add the `Plans:` block listing all 5 plans with `[x]` status (mirror Phase 216's pattern).
    4. Update the Progress table: change `217. auth-saml-enterprise` row to `5/5 | Complete | <date>`.

    EDIT `.planning/STATE.md`:

    1. Update `status:` line to "Phase 217 shipped; ready to start Phase 218 (oc-audit-close-v13.1)".
    2. Update `stopped_at:` to "Phase 217 complete; Phase 218 next".
    3. Update `progress.completed_phases` from 5 to 6.
    4. Update `progress.completed_plans` to reflect 5 new plans (28 total).
    5. Update Current Position section: "Phase: 217 (auth-saml-enterprise) — COMPLETE <date>".
    6. Update Last activity line.
    7. Update the Roadmap Snapshot table's Phase 217 status if it has a status column.

    Then commit per project convention. Use git CLI; do NOT use heredoc (per CRITICAL_RULES). Stage modified files explicitly:
    ```bash
    git add backend/app/core/identity.py \
      backend/app/platform/extensions/defaults.py \
      backend/tests/test_extensions.py \
      docs/saml.md \
      .planning/ROADMAP.md \
      .planning/STATE.md
    git add -f .planning/phases/217-auth-saml-enterprise/217-*-PLAN.md \
      .planning/phases/217-auth-saml-enterprise/217-*-SUMMARY.md
    git commit -m "docs(217-05): scrub SAML mentions, ship docs/saml.md, mark Phase 217 complete"
    ```

    Note `.planning/` is gitignored per MEMORY.md — use `git add -f` for new planning files.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && python -c "
roadmap = open('.planning/ROADMAP.md').read()
state = open('.planning/STATE.md').read()
assert '[x] **Phase 217: auth-saml-enterprise**' in roadmap, 'Phase 217 not marked complete in ROADMAP'
assert '5/5' in roadmap and 'auth-saml-enterprise' in roadmap, 'Phase 217 plan count not updated'
assert 'Phase 217 shipped' in state or 'Phase 218' in state, 'STATE not updated to next phase'
print('OK')
"</automated>
    Plus: `cd /Users/ishiland/Code/geolens && git log -1 --oneline` shows the new commit.
  </verify>
  <acceptance_criteria>
    - ROADMAP Phase 217 entry marked `[x]` with completion date and 5/5 plans listed.
    - STATE.md current position advanced to Phase 218.
    - Git commit created including all docstring scrubs, docs/saml.md, ROADMAP/STATE updates, and 5 PLAN/SUMMARY files (force-added since `.planning/` is gitignored).
  </acceptance_criteria>
  <done>
    Phase 217 closed. Project state advanced to Phase 218. All artifacts committed. Phase 218 (oc-audit-close-v13.1) is the next phase to plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → operator | `docs/saml.md` informs operator decisions (NameID format, multi-instance posture, sp_entity_id matching). Misleading docs could lead to insecure deployments. |
| Verification gate → phase shippability | The gate task is the only thing standing between "code merged" and "phase declared complete." Skipping or weakening the gate ships partially-verified work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verification |
|-----------|----------|-----------|----------|-------------|-----------------|--------------|
| T-217-05-INCOMPLETE-SCRUB | Information disclosure | SAML mentions remain in core after Plan 05 task 01 | LOW | mitigate | SC#1 grep gate is the verification — task 01's `<verify>` automated command FAILS the task if any unaccounted-for SAML mention remains. Grep uses literal `-i saml` (per RESEARCH §15 Q3 not `-iE 'saml|idp_|sp_entity'`); column/field names like `idp_entity_id` are acceptable. | Task 01 acceptance criterion: zero hits from `git grep -i saml backend/` (with the 3 carve-outs). |
| T-217-05-DOC-MISLEAD | Spoofing (operator action) | `docs/saml.md` recommends weak posture | LOW | mitigate | Doc explicitly enumerates D-15 hardening defaults + Pitfall 3 (NameID), 5 (multi-instance), 14 (sp_entity_id) callouts. Acceptance criterion enumerates required keywords. | Task 02 acceptance: required-section/keyword list. |
| T-217-05-GATE-SKIP | Availability | Phase declared complete with failing SC | HIGH | mitigate | Verification gate is `checkpoint:human-verify` with `gate="blocking"`. Cannot proceed to Task 04 (mark complete) without explicit "approved" resume signal. Each SC has a specific automated command listed. | The checkpoint structure itself enforces this. |

**HIGH severity threats:** T-217-05-GATE-SKIP. Mitigated by the blocking checkpoint structure — execute-phase cannot continue past the checkpoint without user approval.
</threat_model>

<verification>
After all four tasks complete:
```bash
# 1. SC#1 — zero SAML mentions in core
git grep -i saml backend/ ':!backend/alembic/' ':!backend/tests/fixtures/saml/' ':!backend/tests/test_saml_overlay.py'

# 2. SC#3 — SP-initiated SSO end-to-end
cd backend && uv run pytest tests/test_saml_overlay.py -x

# 3. SC#5 — registration via Phase 214 seam
cd backend && uv run pytest tests/test_saml_overlay.py::test_saml_overlay_registers_under_identity_and_routers -x

# 4. alembic clean
docker compose -f docker-compose.yml -f docker-compose.enterprise.yml run --rm migrate sh -c \
  "uv add --editable /enterprise && uv run alembic check"

# 5. Full backend baseline + ruff
cd backend && uv run pytest -x && uv run ruff check

# 6. Enterprise overlay tests
cd ~/Code/geolens-enterprise && uv run pytest -x

# 7. Frontend gate
cd frontend && npm run typecheck && npm run lint && npm run test && npm run build

# 8. ROADMAP + STATE updated
grep -E "\[x\] \*\*Phase 217" .planning/ROADMAP.md
grep "Phase 217 shipped\|Phase 218" .planning/STATE.md
```
</verification>

<success_criteria>
- 3 docstring scrubs applied; `git grep -i saml` against core (with 3 carve-outs) returns zero hits.
- `docs/saml.md` exists with all 9 required sections and pitfall callouts.
- All 5 ROADMAP §Phase 217 success criteria PASS (verified by checkpoint task).
- Full backend pytest (2001 baseline + 10+ SAML tests) passes.
- Enterprise overlay pytest passes.
- Frontend builds, typechecks, lints, tests pass.
- alembic check clean.
- ruff clean.
- ROADMAP marks Phase 217 complete with 5/5 plans.
- STATE advanced to Phase 218.
- All artifacts committed.
</success_criteria>

<output>
After completion, create `.planning/phases/217-auth-saml-enterprise/217-05-SUMMARY.md`.
</output>
