---
phase: 224-catalog-god-module-split
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - backend/app/modules/catalog/datasets/domain/service_query.py
  - backend/app/modules/catalog/datasets/domain/service.py
autonomous: true
requirements: [DECOUPLE-01, DECOUPLE-03]
tags: [refactor, extract, service-query]
must_haves:
  truths:
    - "service_query.py exists with 5 public read-side coroutines — DECOUPLE-03"
    - "service.py shims the 5 moved symbols via re-export"
    - "Pytest GREEN, import surface preserved, ruff clean"
  artifacts:
    - path: "backend/app/modules/catalog/datasets/domain/service_query.py"
      provides: "get_dataset, list_datasets, get_datasets_list, get_dataset_detail, get_dataset_rows"
      contains: "async def get_dataset"
decisions:
  - "Extracted FOURTH per D-02. get_dataset is the most-imported symbol (used in router_metadata, router_vrt, layers/router, processing/export, etc.) — extracting it last among the read-side group minimizes mid-extraction churn."
  - "If query coroutines call helpers from service_metadata or service_lifecycle (already extracted), the cross-imports are allowed per D-05."
---

<objective>
Extract 5 read-side query coroutines into `service_query.py`. Replace in service.py with re-export shims.

Output: 1 new file, service.py shimmed, 1 atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/224-catalog-god-module-split/224-PLAN.md
@.planning/phases/224-catalog-god-module-split/224-01-baseline-imports.txt
@backend/app/modules/catalog/datasets/domain/service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create service_query.py with 5 read-side coroutines</name>
  <files>backend/app/modules/catalog/datasets/domain/service_query.py</files>
  <action>
Move (verbatim copies):
1. `async def get_dataset(...)`
2. `async def list_datasets(...)`
3. `async def get_datasets_list(...)`
4. `async def get_dataset_detail(...)`
5. `async def get_dataset_rows(...)`

Module docstring: "Dataset read-side queries: lookup, list, detail, rows (extracted from service.py — Phase 224)."

`__all__` = ["get_dataset", "list_datasets", "get_datasets_list", "get_dataset_detail", "get_dataset_rows"].

Imports: copy only what's needed; ruff F401.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && grep -c '^async def \(get_dataset\|list_datasets\|get_datasets_list\|get_dataset_detail\|get_dataset_rows\)' backend/app/modules/catalog/datasets/domain/service_query.py</automated>
  </verify>
  <acceptance_criteria>
    - Grep returns 5.
    - File <500 LOC.
    - Ruff clean.
  </acceptance_criteria>
  <done>service_query.py exists with 5 coroutines.</done>
</task>

<task type="auto">
  <name>Task 2: Update service.py — shims for 5 symbols</name>
  <files>backend/app/modules/catalog/datasets/domain/service.py</files>
  <action>
1. Delete the 5 moved bodies.
2. Add re-export:
```python
from app.modules.catalog.datasets.domain.service_query import (
    get_dataset,
    get_dataset_detail,
    get_dataset_rows,
    get_datasets_list,
    list_datasets,
)
```
3. Ruff F401.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && uv run python -c "from app.modules.catalog.datasets.domain.service import (get_dataset, list_datasets, get_datasets_list, get_dataset_detail, get_dataset_rows); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Smoke prints OK.
    - 5 bodies absent from service.py.
  </acceptance_criteria>
  <done>service.py shims query symbols.</done>
</task>

<task type="auto">
  <name>Task 3: Verify + atomic commit</name>
  <files>(no changes)</files>
  <action>
1. Pytest green.
2. Diff vs `224-01-baseline-imports.txt` → empty.
3. Boot smoke OK.

Commit:
```bash
git add backend/app/modules/catalog/datasets/domain/service_query.py \
        backend/app/modules/catalog/datasets/domain/service.py && \
git commit -m "$(cat <<'EOF'
refactor(224-05): extract service_query from datasets/domain/service.py

Move 5 read-side coroutines (get_dataset, list_datasets, get_datasets_list,
get_dataset_detail, get_dataset_rows) into service_query.py. service.py
retains re-export shims.

Pure mechanical move. Pytest GREEN; import surface preserved
(get_dataset is the highest-trafficked symbol in the public surface —
17+ consumer files depend on this re-export staying in place).
EOF
)"
```
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && git log -1 --oneline | grep -c '224-05'</automated>
  </verify>
  <acceptance_criteria>
    - Grep returns 1.
  </acceptance_criteria>
  <done>Plan 05 commit landed.</done>
</task>

</tasks>

<verification>
- service_query.py with 5 coroutines
- service.py shims them
- Pytest green, diff clean
</verification>

<success_criteria>
- [ ] service_query.py extracted
- [ ] service.py LOC further reduced
- [ ] DECOUPLE-01 preserved
- [ ] Atomic commit landed
</success_criteria>

<output>
Create `224-05-SUMMARY.md`.
</output>
