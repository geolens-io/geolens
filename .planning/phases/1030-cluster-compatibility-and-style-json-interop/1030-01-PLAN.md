# Phase 1030 Plan: Cluster Compatibility And Style JSON Interop

## Goal

Prove server-side Cluster preserves saved-map, viewer, and style JSON compatibility while keeping standalone style JSON fallback behavior explicit.

## Tasks

1. Inspect current style JSON export/import behavior for cluster intent from v1005.
2. Preserve existing drawable style output as normal point/vector fallback for standalone MapLibre styles.
3. Add source metadata that documents bounded GeoJSON, server-tile, or fallback cluster strategy without adding fields to persisted map/layer schemas.
4. Extend backend style JSON tests for large server-side cluster metadata and existing cluster intent round-trip.
5. Run the focused backend style JSON suite.

## Verification

- `cd backend && uv run pytest tests/test_maps_style_json.py -q`
