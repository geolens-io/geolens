---
phase: 1004-styling-and-cartography-control-polish
plan: 01
type: standard
wave: 1
depends_on: []
autonomous: true
requirements: [STYLE-01, STYLE-02, STYLE-03, STYLE-04, STYLE-05, STYLE-07, STYLE-08]
files_modified:
  - frontend/src/components/builder/LayerStyleEditor.tsx
  - frontend/src/components/builder/DataDrivenStyleEditor.tsx
  - frontend/src/components/builder/RasterLayerControls.tsx
  - frontend/src/components/builder/__tests__/LayerStyleEditor.test.tsx
  - frontend/src/components/builder/__tests__/DataDrivenStyleEditor.test.tsx
  - frontend/src/components/builder/__tests__/RasterLayerControls.test.tsx
  - frontend/src/i18n/locales/en/builder.json
  - frontend/src/i18n/locales/es/builder.json
  - frontend/src/i18n/locales/fr/builder.json
  - frontend/src/i18n/locales/de/builder.json
---

<objective>
Polish the selected-layer style controls by grouping them around visual intent, adding geometry-aware pending-style previews, recoverable validation, and scoped reset affordances while preserving existing `paint` and `style_config` contracts.
</objective>

<context>
@.planning/phases/1004-styling-and-cartography-control-polish/1004-CONTEXT.md
@.planning/REQUIREMENTS.md
@frontend/src/components/builder/LayerStyleEditor.tsx
@frontend/src/components/builder/DataDrivenStyleEditor.tsx
@frontend/src/components/builder/RasterLayerControls.tsx
@frontend/src/lib/normalize-style-config.ts
@frontend/src/components/builder/map-sync.ts
</context>

<tasks>
  <task type="auto">
    <name>Task 1: Group vector style controls by visual intent</name>
    <files>frontend/src/components/builder/LayerStyleEditor.tsx, frontend/src/components/builder/DataDrivenStyleEditor.tsx, frontend/src/i18n/locales/*/builder.json</files>
    <action>
      Reorganize vector style UI into clear visual sections for preview, render mode, data-driven styling, geometry appearance, effects, zoom range, and advanced JSON. Add compact geometry-aware swatches that reflect current pending layer paint/style_config before save. Preserve all callback payloads and style contracts.
    </action>
    <verify>cd frontend && npm run test -- LayerStyleEditor DataDrivenStyleEditor --run</verify>
    <done>Users can scan styling controls by intent and tests prove existing style callbacks still emit aligned `paint` and `style_config` updates.</done>
  </task>

  <task type="auto">
    <name>Task 2: Add recoverable style validation and reset affordances</name>
    <files>frontend/src/components/builder/DataDrivenStyleEditor.tsx, frontend/src/components/builder/RasterLayerControls.tsx, frontend/src/components/builder/__tests__/*.test.tsx, frontend/src/i18n/locales/*/builder.json</files>
    <action>
      Add action-oriented validation for categorical/graduated/unsupported style states and raster adjustment ranges. Ensure resets/cancels affect only the selected layer's style/raster settings and do not mutate labels, popups, filters, or metadata.
    </action>
    <verify>cd frontend && npm run test -- DataDrivenStyleEditor RasterLayerControls LayerStyleEditor --run</verify>
    <done>Invalid style states explain how to recover, unsupported imported states remain editable via Advanced JSON, and reset behavior is narrowly scoped.</done>
  </task>
</tasks>

<verification>
- `cd frontend && npm run test -- LayerStyleEditor DataDrivenStyleEditor RasterLayerControls --run`
- `cd frontend && npm run lint`
</verification>

<must_haves>
- STYLE-01: simple vector, raster, DEM, terrain-adjacent, symbol, and label-relevant styling is grouped by visual intent.
- STYLE-02: categorical, graduated, heatmap, zoom-expression, line-gradient, and raster adjustment validation is understandable.
- STYLE-03: compact swatches preview pending in-memory style changes before save.
- STYLE-04: style reset/cancel actions do not erase unrelated layer settings.
- STYLE-05: unsupported imported style states are surfaced without corrupting valid builder-managed config.
- STYLE-07: touched controls avoid clipping with stable compact layouts.
- STYLE-08: tests confirm builder callbacks still keep `paint` and `style_config` aligned.
</must_haves>

<success_criteria>
- [ ] Vector style controls are grouped by visual intent.
- [ ] Geometry-aware swatches reflect pending style state.
- [ ] Validation copy is recoverable and scoped.
- [ ] Existing style contract tests pass.
</success_criteria>
