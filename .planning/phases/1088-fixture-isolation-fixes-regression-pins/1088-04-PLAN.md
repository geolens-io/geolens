---
phase: 1088-fixture-isolation-fixes-regression-pins
plan: 04
type: execute
wave: 4
depends_on:
  - 1088-02
files_modified:
  - backend/tests/conftest.py
  - backend/tests/test_fixture_isolation_v1020.py
autonomous: true
conditional: true
condition: "Plan 1088-02 audit doc `DECISION:` line contains `SPAWN-1088-04` OR `SPAWN-1088-03-AND-1088-04`. If 1088-02 emits `PROCEED-TO-1088-05`, this plan is SKIPPED."
requirements:
  - FI-02
  - FI-03

must_haves:
  truths:
    - "Plan 1088-02's audit doc names this plan in its `DECISION:` branch"
    - "In-test connection contention (audit category 4.3) is mitigated by retry-with-backoff around `override_get_db` in `client` fixture (conftest.py:503-505)"
    - "Post-fix `pytest -n auto` returns < 30 failures in category 4.3"
    - "Sequential baseline `pytest tests/` stays green (`failed == 0`)"
    - "Regression pin `test_in_test_contention_retries_succeeds` exists at `backend/tests/test_fixture_isolation_v1020.py` and would FAIL against pre-1088-04 HEAD"
    - "v1019 patterns NOT regressed (NullPool branch, stagger, _make_test_async_engine signature)"
  artifacts:
    - path: "backend/tests/conftest.py"
      provides: "Retry-with-backoff wrapper around override_get_db at lines ~503-505"
      contains: "Plan 1088-04"
    - path: "backend/tests/test_fixture_isolation_v1020.py"
      provides: "Regression pin for category 4.3 in-test connection contention"
      contains: "def test_in_test_contention_retries_succeeds"
  key_links:
    - from: "backend/tests/conftest.py:503-505"
      to: "test_session_factory"
      via: "retry-with-backoff wrapper around session acquisition"
      pattern: "override_get_db"
---

<objective>
CONDITIONAL PLAN — only execute if Plan 1088-02's audit doc emits `DECISION: SPAWN-1088-04` or `DECISION: SPAWN-1088-03-AND-1088-04`. If 1088-02 emits `DECISION: PROCEED-TO-1088-05`, this plan is SKIPPED entirely.

Apply structural fix for audit category 4.3 (in-test connection contention, 87 pre-fix failures). Per audit Section 5 (`.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md:1289-1299`) the suggested approach is a retry-with-backoff wrapper around `override_get_db` in the `client` fixture (conftest.py:503-505), so transient `TooManyConnections` during request handling is retried at the session-factory level rather than failing the test.

Audit 4.3 mechanism: tests that internally open more than one DB connection within their body (multiple `TestClient.post(...)` calls) race the connection ceiling. Distinct from 4.2 — here the `client` fixture has completed setup and yielded; the test itself is opening connections mid-execution.

HARD requirement: post-fix category 4.3 < 30 failures.

Output: modified `backend/tests/conftest.py` + extended `backend/tests/test_fixture_isolation_v1020.py` + targeted re-measure proving 4.3 < 30.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-CONTEXT.md
@.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md
@.planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md
@.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-01-SUMMARY.md
@.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-02-SUMMARY.md

<interfaces>
Target surface — `override_get_db` at conftest.py:503-505 inside the `client` fixture:

Current shape:
- `async def override_get_db():` — inner async function definition inside `client` fixture body
- `    async with test_session_factory() as session:` — acquires async session
- `        yield session` — yields session to FastAPI dependency-injection

Post-fix shape: retry-with-backoff wrapper around `test_session_factory()` for `OperationalError("too many clients already")`. 2 retries with `[0.5s, 1.0s]` backoff (bounded in-test latency — must not stall tests indefinitely). Re-raise loudly after exhausting retries; re-raise non-contention OperationalErrors immediately (the fix is targeted at contention only).

v1019 patterns that MUST NOT be regressed:
- NullPool branch at conftest.py:64-66
- `_SETUP_STAGGER_SECONDS` at conftest.py:106
- `_make_test_async_engine` signature
- `app.dependency_overrides.clear()` teardown
- Singleton storage swap at conftest.py:516
- Plan 1088-01's structured OperationalError handler at conftest.py:~275 (this plan must NOT touch that region)

Audit Section 4.3 detail:
- 87 failures pre-fix
- Representative failures: `backend/tests/test_download_token.py::TestDownloadTokenEndpoint::test_download_token_private_non_owner`, `backend/tests/test_jobs_router.py::TestGetJobStatus::test_get_job_surfaces_temporal_parse_errors`, `backend/tests/test_column_ddl_idor.py::test_drop_column_owner_returns_200`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add retry-with-backoff around override_get_db</name>
  <files>backend/tests/conftest.py</files>
  <behavior>
    Pre-execution gate: read `.planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md` `DECISION:` line. If it does NOT contain `SPAWN-1088-04` (alone or in `SPAWN-1088-03-AND-1088-04`), HALT and emit `PLAN 1088-04 SKIP: 1088-02 audit does not require it`. Do not modify files.

    If gate passes: modify `override_get_db` inner function inside `client` fixture at conftest.py:~503-505. Shape:
    - Outer for-loop over backoff waits `[0.5, 1.0]` (2 retries total).
    - Inside loop: try `async with test_session_factory() as session: yield session` and break on success.
    - Except `OperationalError as e`: if `"too many clients already"` in `str(e).lower()`: `await asyncio.sleep(wait_s); continue`. Else: re-raise immediately (non-contention OperationalError preserved as test failure surface).
    - After loop exhausts without success: re-raise the last OperationalError (no swallow).
    - Add `import asyncio` to top of conftest.py if not already present.
    - Add a comment above the retry region citing Plan 1088-04 and audit Section 4.3.
    - DO NOT modify surrounding `client` fixture body (conftest.py:485-516 patches to app.core.db / health_service_module / storage provider).
    - DO NOT modify `app.dependency_overrides[get_db] = override_get_db` registration at conftest.py:507.
    - DO NOT touch Plan 1088-01's structured handler at conftest.py:~275.
  </behavior>
  <action>
    Read 1088-02 audit DECISION line; halt if it does not name 1088-04. Otherwise edit override_get_db at conftest.py:503-505 to wrap test_session_factory() acquisition with retry-with-backoff for OperationalError containing "too many clients already". Use 2 retries, waits [0.5, 1.0]. Preserve `async with test_session_factory() as session: yield session` inside retry loop. Re-raise non-contention OperationalError immediately. Re-raise loudly after exhausting retries. Add `import asyncio` only if not already present (verify via grep first). Surgical edit only — conftest.py:485-516 surrounding patches + conftest.py:507 dependency_overrides registration are out of scope.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && grep -q "override_get_db" tests/conftest.py && grep -qE "Plan 1088-04|1088-04" tests/conftest.py && grep -q "poolclass=NullPool" tests/conftest.py && grep -qE "_SETUP_STAGGER_SECONDS\s*=\s*[0-9.]+" tests/conftest.py</automated>
  </verify>
  <done>override_get_db modified with retry-with-backoff. Plan 1088-04 comment exists. v1019 NullPool branch preserved. _SETUP_STAGGER_SECONDS constant preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression pin for category 4.3 in-test contention</name>
  <files>backend/tests/test_fixture_isolation_v1020.py</files>
  <behavior>
    Pre-execution gate: same DECISION check as Task 1.

    Append `test_in_test_contention_retries_succeeds` to existing `backend/tests/test_fixture_isolation_v1020.py` (created by 1088-01). DO NOT touch existing pins (`test_lifecycle_retries_on_transient_too_many_clients` from 1088-01, `test_setup_phase_contention_retries_or_serializes` if 1088-03 ran).

    Test contract:
    - Mock `test_session_factory()` to raise `OperationalError("too many clients already")` on first acquisition, succeed on retry.
    - Assert retry path is exercised (sleep was called with 0.5s) and session is eventually yielded.
    - Pre-fix HEAD: no retry, OpErr propagates → test fails.
    - Post-fix HEAD: retry succeeds → test passes.

    Strategy: patch `tests.conftest.asyncio.sleep` to a no-op AsyncMock. Patch the session_factory acquisition path (likely requires extracting `override_get_db` body into a private helper during Task 1, OR using `pytest-asyncio` + a thin async wrapper that reproduces the retry logic inline). Executor picks whichever is cleaner.
  </behavior>
  <action>
    Read 1088-02 audit DECISION line; halt if it does not name 1088-04. Otherwise append `test_in_test_contention_retries_succeeds` to `backend/tests/test_fixture_isolation_v1020.py`. The test simulates the in-test contention path: first `test_session_factory()` acquisition raises `OperationalError("too many clients already")`, second acquisition succeeds, assert the retry yielded a session and asyncio.sleep was called. Use `pytest.mark.asyncio` if needed. The test MUST pass against post-Task-1 HEAD. If Task 1 extracted the retry logic into a private helper (e.g., `_retrying_get_db_session(factory)`), this test imports and exercises that helper directly — strongly recommended for clean test isolation.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && git grep -n "def test_in_test_contention_retries_succeeds" tests/test_fixture_isolation_v1020.py | grep -c 'def test_in_test_contention_retries_succeeds' | awk '$1 == 1 {exit 0} {exit 1}' && env $(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) uv run pytest tests/test_fixture_isolation_v1020.py::test_in_test_contention_retries_succeeds -x -v 2>&1 | tee /tmp/1088-04-task2-pin.log | grep -qE '1 passed'</automated>
  </verify>
  <done>`test_in_test_contention_retries_succeeds` exists at `backend/tests/test_fixture_isolation_v1020.py` (exactly 1 grep match). Test passes against post-Task-1 HEAD. Pre-existing pins (1088-01's and optionally 1088-03's) untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Targeted re-measure + sequential gate + SUMMARY + commit</name>
  <files>.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-04-SUMMARY.md</files>
  <action>
    Read 1088-02 audit DECISION line; halt if it does not name 1088-04. Otherwise:

    Step 1 — drop stale per-worker DBs per audit Section 1 Step 1b.
    Step 2 — sequential baseline: `cd /Users/ishiland/Code/geolens/backend && env $(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) uv run pytest tests/`. HARD GATE: `failed == 0`.
    Step 3 — targeted parallel re-measure: `env $(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) uv run pytest -n auto --junitxml=/tmp/v1020-remeasure-1088-04.xml tests/`.
    Step 4 — re-categorize via the audit's Section 1 Step-5 parser. Extract category 4.3 count. Assert `4.3 < 30`.

    Step 5 — write SUMMARY.md at `.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-04-SUMMARY.md` with: plan ID + commit SHA range; chosen retry parameters (waits, retry count); verbatim sequential baseline summary line; post-fix per-category counts with 4.3 highlighted; verbatim assertion `4.3 = <N> < 30`; cross-category drift if any; explicit note REQUIREMENTS.md flip is DEFERRED to Plan 1088-N.

    Step 6 — stage and commit Tasks 1+2+3: `git add backend/tests/conftest.py backend/tests/test_fixture_isolation_v1020.py .planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-04-SUMMARY.md`. Commit subject: `fix(1088-04): retry-with-backoff for in-test connection contention (audit 4.3)`. HEREDOC for body per Git Safety Protocol.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && test -f .planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-04-SUMMARY.md && grep -qE '4\.3.*<.*30|4\.3.*= [0-2][0-9]\b|in-test contention.*resolved' .planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-04-SUMMARY.md && git log -1 --format='%s' | grep -q '1088-04' && [ "$(git diff-tree --no-commit-id --name-only -r HEAD | grep -c '^\.planning/REQUIREMENTS\.md$')" = "0" ]</automated>
  </verify>
  <done>SUMMARY.md exists. Cites 4.3 post-fix count below threshold of 30. Commit subject contains `1088-04`. REQUIREMENTS.md NOT in commit's file list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test body → DB session factory | Tests internally trigger multiple `TestClient.post(...)` calls; each acquires a new connection. Mitigation: retry at session-factory level, bounded latency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1088-08 | Denial of Service | In-test DB connection acquisition under contention | mitigate | Retry-with-backoff `[0.5, 1.0]`s around `test_session_factory()` — cumulative latency bounded at 1.5s per session. |
| T-1088-09 | Tampering | Plan 1088-01's silent-swallow fix region could be inadvertently re-touched | mitigate | Task 1 explicitly scopes to conftest.py:503-505 region; conftest.py:~275 (1088-01 fix) is out of scope. Code review must verify no diff in 1088-01 region. |
| T-1088-SC | Tampering | Package legitimacy | N/A | No new packages introduced. `asyncio` is stdlib. |
</threat_model>

<verification>
1. Plan 1088-02 audit `DECISION:` line was read; if it did not name 1088-04, plan halted with no edits.
2. If executed: `grep -q "override_get_db" backend/tests/conftest.py` returns 0 (the function still exists).
3. If executed: `grep -q "poolclass=NullPool" backend/tests/conftest.py` returns 0 (v1019 pattern preserved).
4. If executed: `git grep -n "def test_in_test_contention_retries_succeeds" backend/tests/test_fixture_isolation_v1020.py` returns exactly 1.
5. If executed: targeted re-measure proves post-fix 4.3 < 30.
6. If executed: SUMMARY.md committed with subject containing `1088-04`.
7. If executed: REQUIREMENTS.md NOT in commit's file list.
8. Plan 1088-01's conftest.py:~275 fix region was not re-modified (verify by `git diff HEAD~1..HEAD backend/tests/conftest.py` showing only conftest.py:503-505 region changed).
</verification>

<success_criteria>
- Either: SKIPPED because 1088-02's `DECISION:` did not name this plan. OR:
- Retry-with-backoff added to override_get_db; v1019 patterns + 1088-01 region preserved; 4.3 post-fix < 30; regression pin added; sequential baseline `failed == 0`; SUMMARY.md committed; REQUIREMENTS.md flip deferred to 1088-N.
</success_criteria>

<output>
Create `.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-04-SUMMARY.md` when done (only if executed). Commit Tasks 1+2+3 together.
</output>
