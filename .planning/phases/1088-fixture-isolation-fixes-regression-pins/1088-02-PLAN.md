---
phase: 1088-fixture-isolation-fixes-regression-pins
plan: 02
type: execute
wave: 2
depends_on:
  - 1088-01
files_modified:
  - .planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md
autonomous: true
requirements:
  - FI-02

must_haves:
  truths:
    - "Stale per-worker test DBs are dropped before re-measurement (audit Section 1 Step 1b)"
    - "Sequential baseline `pytest tests/` returns `failed == 0` before parallel re-measure proceeds (HARD GATE)"
    - "Parallel `pytest -n auto --junitxml=/tmp/v1020-remeasure-1088-01.xml` is run after sequential baseline passes"
    - "All failures from the parallel run are re-categorized using the Section 1 Step-5 Python helper (the audit's reference parser)"
    - "Pre-fix vs post-fix counts are reported PER category (4.1, 4.2, 4.3, 4.4, 4.5) — cross-category drift is explicit"
    - "Decision-point output is unambiguous: either threshold-met (proceed to Plan 1088-05) or threshold-exceeded (spawn 1088-03 and/or 1088-04)"
    - "No code changes in this plan — audit document only"
  artifacts:
    - path: ".planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md"
      provides: "Post-1088-01 re-measurement: per-category counts + threshold-decision recommendation for Plans 1088-03/04/05"
      contains: "Category 4.1"
  key_links:
    - from: ".planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md"
      to: ".planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md"
      via: "cross-reference: Section 4 categories used as the orthogonal axis"
      pattern: "PYTEST-XDIST-FIXTURE-AUDIT-v1020"
---

<objective>
Re-measure the parallel test suite after Plan 1088-01's silent-swallow fix lands, then produce a decision document that drives Plans 1088-03/04/05. Per audit Section 5's re-measure protocol (`.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md:1312-1328`) and CONTEXT.md's specifics block:

Decision rules — branch on post-fix counts:
- Category 4.1 (per-worker DB lifecycle race) → expected drop from 407 → 0. If post-fix 4.1 > 0, Plan 1088-01 did not work; halt and revise.
- Category 4.2 (setup-phase contention) → if post-fix < 50, accept as transient flake (defer to HYG-02 in Phase 1090); if post-fix >= 50, structural fix needed → spawn Plan 1088-03.
- Category 4.3 (in-test contention) → if post-fix < 30, accept as flake; if post-fix >= 30, structural fix needed → spawn Plan 1088-04.
- Category 4.4 (teardown contention) + 4.5 (sandbox/assertion) → defer unless rising; document only.

This plan ships UNCONDITIONALLY (no skip path). Its output is the input gate for Plans 1088-03, 1088-04 (conditional), and 1088-05 (final close-out). No code changes — audit doc only.

Purpose: prevents over-engineering. Per CONTEXT.md `<specifics>`: "the audit explicitly says 'the v1020 fix strategy [may be] a single-category close rather than a four-category sweep' if 4.1 cascades resolve 4.2/4.3/4.4." This plan is the empirical check before any conditional structural fixes are spawned.

Output: `.planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md` + plan SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-CONTEXT.md
@.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md
@.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-01-SUMMARY.md

<interfaces>
Audit Section 1 reproducibility checklist (`.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md:220-235`) defines the 11-step measurement protocol. This plan reuses Steps 1, 1b, 2, 3 (sampler optional but recommended), 4, 5 verbatim.

Audit Section 1 Step 5 (`.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md:159-218`) contains the Python JUnit XML parser that maps each `<testcase>` failure to a category. The parser is the reference categorization tool — the re-measure MUST use this exact logic so post-fix counts are directly comparable to the audit's Section 4 counts.

Audit Section 4 categories (`.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md:989-1235`):
- 4.1: per-worker DB lifecycle race (gw15 setup failed) — 407 failures pre-fix
- 4.2: setup-phase connection contention — 150 failures pre-fix
- 4.3: in-test connection contention — 87 failures pre-fix
- 4.4: teardown-phase connection contention — 2 failures pre-fix
- 4.5: sandbox subsystem error / assertion failure — 2 failures pre-fix

The categorization heuristic (paraphrased from audit Section 1 Step 5 parser logic):
- 4.1 (lifecycle race): `error_class` contains `InvalidCatalogNameError` AND failure is during fixture setup phase (JUnit `<error>` not `<failure>`) AND the failing test's worker is the same as the missing per-worker DB
- 4.2 (setup contention): `error_class` contains `TooManyConnectionsError`/`CannotConnectNowError` AND failure timing is during `client` fixture setup (before test body)
- 4.3 (in-test contention): same exception classes, but during test body execution (not setup/teardown)
- 4.4 (teardown contention): same exception classes, during fixture teardown (after test yielded)
- 4.5: anything else (assertion failures, sandbox errors)

Re-measure decision thresholds (from CONTEXT.md LOCKED + audit Section 5):
- 4.1 expected: 0 (must drop from 407 → 0 if 1088-01 worked)
- 4.2 spawn-Plan-1088-03 threshold: >= 50 (else defer to HYG-02 as flake)
- 4.3 spawn-Plan-1088-04 threshold: >= 30 (else defer to HYG-02 as flake)
- 4.4: defer unless rising above pre-fix count of 2
- 4.5: defer unless rising above pre-fix count of 2
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop stale per-worker test DBs + sequential baseline gate</name>
  <files>/tmp/v1020-remeasure-1088-01-stale-dbs.txt, /tmp/v1020-remeasure-1088-01-sequential-baseline.log</files>
  <action>
    Mirror audit Section 1 Step 1b verbatim. Source `.env` for DB credentials, then list stale per-worker test DBs via `docker compose exec -T db psql -U geolens -d geolens -At -c "SELECT datname FROM pg_database WHERE datname LIKE 'geolens%test_gw%' OR datname LIKE 'geolens%test_master%';" > /tmp/v1020-remeasure-1088-01-stale-dbs.txt`. Generate a DROP script from the list and execute it idempotently via piped `psql`. Then mirror Step 2: sequential baseline re-verify via `cd /Users/ishiland/Code/geolens/backend && env $(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) uv run pytest tests/ 2>&1 | tee /tmp/v1020-remeasure-1088-01-sequential-baseline.log`. Assert `failed == 0` from the final summary line. HARD GATE: if `failed > 0`, halt — the spike halts immediately per audit Section 1 Step 2 protocol. A broken sequential baseline would contaminate the parallel-mode failure count with an unrelated regression. Per audit Section 1 Steps 1b + 2 (`.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md:72-108`).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && test -s /tmp/v1020-remeasure-1088-01-sequential-baseline.log && grep -qE '^[0-9]+ passed.*\b0 failed\b|0 failed.*[0-9]+ passed' /tmp/v1020-remeasure-1088-01-sequential-baseline.log</automated>
  </verify>
  <done>Stale per-worker DBs cleared (idempotent — count may be 0 if none existed). Sequential baseline log file exists and its final summary line shows `0 failed`.</done>
</task>

<task type="auto">
  <name>Task 2: Run parallel pytest -n auto with JUnit XML capture + categorize</name>
  <files>/tmp/v1020-remeasure-1088-01.xml, /tmp/v1020-remeasure-1088-01-categories.json</files>
  <action>
    Mirror audit Section 1 Steps 4 + 5. Step 4: run `cd /Users/ishiland/Code/geolens/backend && env $(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) uv run pytest -n auto --junitxml=/tmp/v1020-remeasure-1088-01.xml tests/ 2>&1 | tee /tmp/v1020-remeasure-1088-01-xdist.log`. The background `pg_stat_activity` sampler from audit Section 1 Step 3 is OPTIONAL for this re-measure (recommended but the categorization parser does not require sampler output — it consumes JUnit XML only). Capture the final summary line (e.g., `89 failed, 2401 passed, 27 skipped, 4 warnings, 560 errors in 269.12s` from the audit). Step 5: run the Section 1 Step-5 Python helper VERBATIM (the parser at `.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md:167-209`) against `/tmp/v1020-remeasure-1088-01.xml` to produce a failure inventory at `/tmp/v1020-remeasure-1088-01-inventory.json`. Then categorize each failure using the heuristic from `<interfaces>` above (or extend the Section 1 Step-5 helper inline with a categorization function — executor's call). Write per-category counts to `/tmp/v1020-remeasure-1088-01-categories.json` in the shape `{"4.1": <count>, "4.2": <count>, "4.3": <count>, "4.4": <count>, "4.5": <count>, "total": <count>}`. Sanity gate immediately after parsing: at least 1 node-ID uses the `backend/tests/` prefix (per audit Section 1 Step-5 sanity gate at line 215). Per audit Section 1 Steps 4-5 (`.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md:145-218`).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && test -s /tmp/v1020-remeasure-1088-01.xml && test -s /tmp/v1020-remeasure-1088-01-categories.json && python3 -c "import json; d=json.load(open('/tmp/v1020-remeasure-1088-01-categories.json')); assert set(d.keys()) >= {'4.1','4.2','4.3','4.4','4.5','total'}, f'missing keys: got {list(d.keys())}'; print('PASS:', d)"</automated>
  </verify>
  <done>JUnit XML file `/tmp/v1020-remeasure-1088-01.xml` exists. Categorized counts JSON `/tmp/v1020-remeasure-1088-01-categories.json` exists with keys `4.1`, `4.2`, `4.3`, `4.4`, `4.5`, `total`.</done>
</task>

<task type="auto">
  <name>Task 3: Write re-measure audit doc + decision-point recommendation</name>
  <files>.planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md</files>
  <action>
    Write a new audit document at `.planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md`. Required sections: (1) Header — title, date 2026-05-22, HEAD SHA (from `git rev-parse HEAD`), Phase 1088 / Plan 1088-02 attribution, cross-reference to the parent audit (`.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md`) and Plan 1088-01 SUMMARY. (2) Methodology recap — explicit statement that this re-measurement reuses audit Section 1 Steps 1b, 2, 4, 5 verbatim; cite measurement HEAD SHA. (3) Sequential baseline result — verbatim final summary line from `/tmp/v1020-remeasure-1088-01-sequential-baseline.log`; assert `failed == 0`. (4) Parallel run result — verbatim final summary line from `/tmp/v1020-remeasure-1088-01-xdist.log` (e.g., `X failed, Y passed, Z skipped, ...`); cite wall clock. (5) Per-category pre/post comparison TABLE with rows for 4.1, 4.2, 4.3, 4.4, 4.5 and columns `Pre-fix (audit Section 4)`, `Post-1088-01`, `Delta`, `Disposition`. The Disposition column entries: for 4.1 — `RESOLVED (407 → <N>)` if N is 0 or very small, `UNRESOLVED — halt + revise 1088-01` if N > 50; for 4.2 — `RESOLVED via cascade (defer to HYG-02)` if N < 50 else `STRUCTURAL FIX NEEDED — spawn Plan 1088-03`; for 4.3 — same shape but threshold 30 → `RESOLVED via cascade (defer to HYG-02)` or `STRUCTURAL FIX NEEDED — spawn Plan 1088-04`; for 4.4/4.5 — `DEFER (document only)` unless count > pre-fix. (6) Cross-category drift commentary — per audit Section 5 (`.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md:1321-1328`), the SUMMARY MUST show movement across ALL categories not just the targeted one. Note any unexpected drift (e.g., 4.3 went UP after 4.1 was fixed — this would indicate the cascade timing shifted). (7) Decision-point recommendation — explicit, machine-readable line for the orchestrator to consume: `DECISION: <PROCEED-TO-1088-05 | SPAWN-1088-03 | SPAWN-1088-04 | SPAWN-1088-03-AND-1088-04 | HALT-AND-REVISE-1088-01>`. Include 1-line rationale per branch. (8) Reproducibility checklist — list the input files (the two `/tmp/v1020-remeasure-1088-01-*.log`, `/tmp/v1020-remeasure-1088-01.xml`, `/tmp/v1020-remeasure-1088-01-categories.json`) and exact commands to regenerate. Per CONTEXT.md `<specifics>` "Re-measure protocol" + audit Section 5 protocol.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && test -f .planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md && grep -qE 'DECISION: (PROCEED-TO-1088-05|SPAWN-1088-03|SPAWN-1088-04|SPAWN-1088-03-AND-1088-04|HALT-AND-REVISE-1088-01)' .planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md && grep -qE '4\.1.*407' .planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md && grep -qE '4\.2.*150' .planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md && grep -qE '4\.3.*87' .planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md</automated>
  </verify>
  <done>Audit doc exists at the expected path. Contains pre/post table with all 5 categories. Contains explicit `DECISION: <branch>` line. Cross-references parent audit + Plan 1088-01 SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 4: SUMMARY + commit</name>
  <files>.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-02-SUMMARY.md</files>
  <action>
    Write SUMMARY.md at `.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-02-SUMMARY.md`. Required content: plan ID + phase ID + commit SHA range; tasks completed (Tasks 1-3 above); files modified (1 new audit doc — no code); the verbatim sequential baseline and parallel-run summary lines; the verbatim decision branch from the audit doc (`DECISION: ...`); explicit note that REQUIREMENTS.md FI-02/FI-03 traceability flip is DEFERRED to Plan 1088-N per TD-13. Stage and commit Tasks 3+4 together: `git add .planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md .planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-02-SUMMARY.md`. Commit subject: `docs(1088-02): re-measure after silent-swallow fix; <decision branch from audit>`. Use HEREDOC for body per Git Safety Protocol.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && test -f .planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-02-SUMMARY.md && grep -qE 'DECISION:' .planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-02-SUMMARY.md && git log -1 --format='%s' | grep -q '1088-02' && [ "$(git diff-tree --no-commit-id --name-only -r HEAD | grep -c '^\.planning/REQUIREMENTS\.md$')" = "0" ]</automated>
  </verify>
  <done>SUMMARY.md exists with verbatim decision branch. Most recent commit subject contains `1088-02`. REQUIREMENTS.md is NOT in the most recent commit (verified via `git diff-tree --no-commit-id --name-only -r HEAD`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit doc → downstream planner (1088-03/04/05) | The decision-point line `DECISION: <branch>` is read by the next orchestrator step. A mis-categorized failure (e.g., 4.2 mis-tagged as 4.3) could route to the wrong conditional plan. Mitigation: reuse the audit's Section 1 Step-5 parser verbatim. |
| Postgres → categorization parser (JUnit XML) | JUnit XML is the trusted artifact; sampler logs are advisory. No cross-trust-boundary inputs influence categorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1088-04 | Tampering | Category mis-tagging in the parser | mitigate | Reuse the audit's verbatim Section 1 Step-5 parser; sanity gate checks `backend/tests/` prefix on >= 1 node-ID. |
| T-1088-05 | Repudiation | Re-measurement results untraceable | mitigate | Audit doc cites HEAD SHA + commit range; reproducibility checklist names all input files. |
| T-1088-SC | Tampering | Package legitimacy | N/A | No new dependencies in this plan. |
</threat_model>

<verification>
1. `/tmp/v1020-remeasure-1088-01-sequential-baseline.log` shows `0 failed`.
2. `/tmp/v1020-remeasure-1088-01.xml` exists and is well-formed (`python3 -c "import xml.etree.ElementTree as ET; ET.parse('/tmp/v1020-remeasure-1088-01.xml').getroot()"` returns exit 0).
3. `/tmp/v1020-remeasure-1088-01-categories.json` contains keys `4.1, 4.2, 4.3, 4.4, 4.5, total`.
4. `.planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md` exists with pre/post table + decision branch.
5. SUMMARY.md exists at the plan path.
6. The most recent commit subject contains `1088-02`.
7. REQUIREMENTS.md is NOT in the most recent commit's file list (`git diff-tree --no-commit-id --name-only -r HEAD | grep -c '^\.planning/REQUIREMENTS\.md$'` returns 0).
</verification>

<success_criteria>
- Sequential baseline re-verified `failed == 0` before parallel re-measure.
- Parallel `pytest -n auto --junitxml=...` ran to completion.
- Per-category counts produced via the audit's Section 1 Step-5 parser.
- Audit doc `PYTEST-XDIST-REMEASURE-AFTER-1088-01.md` committed with pre/post table + explicit decision branch.
- No code changes in this plan.
- REQUIREMENTS.md traceability untouched (deferred to 1088-N).
</success_criteria>

<output>
Create `.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-02-SUMMARY.md` when done. Commit Tasks 3+4 together.
</output>
