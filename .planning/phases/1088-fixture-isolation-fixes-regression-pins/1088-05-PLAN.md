---
phase: 1088-fixture-isolation-fixes-regression-pins
plan: 05
type: execute
wave: 5
depends_on:
  - 1088-02
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - backend/tests/test_fixture_isolation_v1020.py
autonomous: true
requirements:
  - FI-02
  - FI-03

must_haves:
  truths:
    - "Final close gate run: sequential `pytest tests/` returns `failed == 0` (≥ 3036/0/38)"
    - "Final close gate run: parallel `pytest -n auto` returns 0 failures from cascade categories (4.1 + 4.2 + 4.3 + 4.4 = 0); 4.5 (≤ 2 sandbox/assertion) documented but not blocking"
    - "REQUIREMENTS.md FI-02 checkbox flipped `[ ]` → `[x]` AND traceability row `Pending` → `Complete`"
    - "REQUIREMENTS.md FI-03 checkbox flipped `[ ]` → `[x]` AND traceability row `Pending` → `Complete`"
    - "REQUIREMENTS.md FI-02 + FI-03 traceability rows cite every regression-pin test by exact `path::TestClass::test_name` node-ID (validated via `git grep -n` per TD-13 `req_citation_pinning` rule)"
    - "Phase 1088 ROADMAP.md entry checkbox flipped `[ ]` → `[x]`; Plans list filled with actual plan file names + statuses"
    - "Phase 1088 SUMMARY.md written and committed in the SAME commit as the REQUIREMENTS.md + ROADMAP.md flips (TD-13 `requirements_traceability_flip` rule)"
    - "Any regression-pin tests created by 1088-03/04 (if those plans executed) are consolidated under `backend/tests/test_fixture_isolation_v1020.py` — no orphaned pins elsewhere"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "FI-02 + FI-03 traceability rows updated to Complete with regression-pin node-ID citations"
      contains: "test_fixture_isolation_v1020"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 1088 entry marked [x] complete; Plans list reflects actual plan files"
      contains: "Phase 1088"
    - path: ".planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-05-SUMMARY.md"
      provides: "Phase close-out SUMMARY: pre/post counts, plans executed, plans skipped, final close-gate result"
      contains: "Phase 1088"
  key_links:
    - from: ".planning/REQUIREMENTS.md FI-02 row"
      to: "backend/tests/test_fixture_isolation_v1020.py"
      via: "node-ID citations per TD-13 req_citation_pinning"
      pattern: "test_fixture_isolation_v1020"
    - from: ".planning/REQUIREMENTS.md FI-03 row"
      to: "backend/tests/test_fixture_isolation_v1020.py"
      via: "node-ID citations per TD-13 req_citation_pinning"
      pattern: "test_fixture_isolation_v1020"
---

<objective>
Final close-out for Phase 1088. UNCONDITIONAL plan — always runs after Plan 1088-02 (and after 1088-03/04 if they spawned). Performs the TD-13 `requirements_traceability_flip` and Phase 1088 SUMMARY in a single commit, gated by a final close gate (sequential `failed == 0` + parallel cascade-categories = 0).

Per CONTEXT.md `<decisions>` "TD-13 rules in effect": "the FINAL plan in Phase 1088 (likely 1088-N for regression pins) must flip BOTH FI-02 AND FI-03 in REQUIREMENTS.md (`[ ]` → `[x]`, traceability row `Pending` → `Complete`) in the SAME commit as its SUMMARY.md write. The verify gate uses `git diff-tree --no-commit-id --name-only -r HEAD` (NOT `git log -1 --name-only`)."

Per v1019 retro (`.planning/retros/v1019-process.md` Incident 3): "the executor MUST flip REQUIREMENTS.md `[ ]` → `[x]` AND `Pending` → `Complete` for every requirement closed by the plan, in the SAME commit as the SUMMARY.md write. Plan 1081-02 missed this and required a fix-up commit at `5bf63166`."

Per v1019 retro Incident 1+2: "REQ-cited test names MUST use exact `path::TestClass::test_name` node-ID; planner MUST validate via `git grep -n "def <test_name>" <path>` before committing. Since FI-03 pins are NEW tests being created, the planner cites the *intended* node-ID, and the executor's verify gate confirms the test exists at that exact name after creation."

This plan owns:
1. Final close gate (re-run sequential + parallel, confirm thresholds).
2. Consolidate FI-03 regression pins (verify all pins are in `backend/tests/test_fixture_isolation_v1020.py`).
3. REQUIREMENTS.md flip FI-02 + FI-03 with verified node-ID citations.
4. ROADMAP.md flip Phase 1088 + plans list reconciliation.
5. Phase SUMMARY at `.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-05-SUMMARY.md` (this plan's own SUMMARY) AND Phase 1088 close SUMMARY at the same path OR a separate phase-level SUMMARY — executor's call but contents are merged.
6. All flips + SUMMARY in a SINGLE commit (per TD-13).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-CONTEXT.md
@.planning/retros/v1019-process.md
@.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md
@.planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md
@.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-01-SUMMARY.md
@.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-02-SUMMARY.md

<interfaces>
REQUIREMENTS.md row format for FI-02 (from `.planning/REQUIREMENTS.md`):
- Checkbox: `- [ ] **FI-02**:` → `- [x] **FI-02**:`
- Traceability table row: `| FI-02 | Phase 1088 | Pending |` → `| FI-02 | Phase 1088 | Complete |`
- The body text of FI-02 is preserved; only checkbox + traceability row change.
- IMPORTANT per CONTEXT.md `<decisions>`: the body of FI-02 SHOULD be extended (in the same commit) with a regression-pin node-ID citation if not already present, in the form: `Regression pins: backend/tests/test_fixture_isolation_v1020.py::test_lifecycle_retries_on_transient_too_many_clients[, ::test_setup_phase_contention_retries_or_serializes][, ::test_in_test_contention_retries_succeeds]`. Citations conditional on which plans 1088-01/03/04 actually ran.

REQUIREMENTS.md FI-03 row:
- Same flip shape: `[ ]` → `[x]` + `Pending` → `Complete`
- FI-03 body explicitly requires: "Each pin is cited in this file's traceability table by `path::TestClass::test_name` node-ID after the planner commits the plan." The executor MUST append the node-ID citations under the FI-03 entry.

ROADMAP.md Phase 1088 entry:
- Line 91: `- [ ] **Phase 1088: Fixture-Isolation Fixes + Regression Pins** —` → `- [x] **Phase 1088: ...** —`
- The "**Plans**: TBD" line at conftest position must be updated to list actual plan files with status (e.g., `**Plans**: 5/5 plans complete (1088-01 fix, 1088-02 re-measure, 1088-03 setup-fix [skipped|executed], 1088-04 in-test-fix [skipped|executed], 1088-05 final close)`).

Close-gate thresholds (HARD floors per CONTEXT.md `<specifics>`):
- Sequential `pytest tests/`: `failed == 0`, `passed >= 3036` (3037+ acceptable with new regression pins).
- Parallel `pytest -n auto`: failures from cascade categories 4.1 + 4.2 + 4.3 + 4.4 = 0.
- Category 4.5 (sandbox/assertion, ≤ 2 failures): documented but NOT blocking.
- Total `M failed + errors` may be > 0 ONLY if those are 4.5-class items.

TD-13 traceability flip verify gate:
- `git diff-tree --no-commit-id --name-only -r HEAD` (NOT `git log -1 --name-only`) — confirms REQUIREMENTS.md + SUMMARY.md + (optional ROADMAP.md) are ALL in the same commit.
- Per v1019 retro Incident 3, this was missed and required a separate fix-up commit `5bf63166`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate regression pins + final close gate</name>
  <files>/tmp/v1088-final-sequential.log, /tmp/v1088-final-parallel.xml, /tmp/v1088-final-categories.json</files>
  <action>
    Step 1 — Verify all v1020 regression pins live in `backend/tests/test_fixture_isolation_v1020.py`:
    - `git grep -nE "def test_(lifecycle_retries_on_transient_too_many_clients|setup_phase_contention_retries_or_serializes|in_test_contention_retries_succeeds)" backend/tests/` MUST return only matches inside `backend/tests/test_fixture_isolation_v1020.py`. If any pin lives in a different file, halt and consolidate first.
    - At minimum: 1088-01's `test_lifecycle_retries_on_transient_too_many_clients` MUST be present (UNCONDITIONAL pin).
    - Conditionally: `test_setup_phase_contention_retries_or_serializes` present iff 1088-03 ran; `test_in_test_contention_retries_succeeds` present iff 1088-04 ran. Read `.planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md` `DECISION:` line to determine expected pin set.

    Step 2 — Final sequential baseline gate. Drop stale per-worker DBs per audit Section 1 Step 1b. Then `cd /Users/ishiland/Code/geolens/backend && env $(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) uv run pytest tests/ 2>&1 | tee /tmp/v1088-final-sequential.log`. Assert `M failed == 0` and `N passed >= 3036`.

    Step 3 — Final parallel re-measure. `env $(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) uv run pytest -n auto --junitxml=/tmp/v1088-final-parallel.xml tests/ 2>&1 | tee /tmp/v1088-final-parallel.log`. Capture wall clock + summary line.

    Step 4 — Re-categorize via audit Section 1 Step-5 Python helper. Persist results to `/tmp/v1088-final-categories.json` in the shape `{"4.1": <count>, "4.2": <count>, "4.3": <count>, "4.4": <count>, "4.5": <count>, "total": <count>, "cascade_total": <4.1+4.2+4.3+4.4>}`. Assert `cascade_total == 0` (HARD GATE). If `cascade_total > 0`, halt — the plan must NOT proceed to TD-13 flip until all cascade categories are at 0. Category 4.5 (sandbox/assertion) MAY be > 0 (up to ~2) and is documented but not blocking.

    Per audit Section 5 + CONTEXT.md `<specifics>` "Hard floor recap".
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && test -f /tmp/v1088-final-categories.json && python3 -c "import json; d=json.load(open('/tmp/v1088-final-categories.json')); cascade = d.get('cascade_total', d['4.1']+d['4.2']+d['4.3']+d['4.4']); assert cascade == 0, f'cascade_total={cascade} (must be 0)'; print(f'PASS: cascade_total={cascade}, 4.5={d[\"4.5\"]}')"</automated>
  </verify>
  <done>Final sequential `failed == 0`. Final parallel cascade-categories (4.1+4.2+4.3+4.4) sum to 0. 4.5 documented (may be ≤ 2). All v1020 regression pins consolidated under `backend/tests/test_fixture_isolation_v1020.py`.</done>
</task>

<task type="auto">
  <name>Task 2: Validate node-ID citations + REQUIREMENTS.md + ROADMAP.md flips</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
    Per TD-13 `req_citation_pinning` rule (v1019 retro Incident 1): before flipping REQUIREMENTS.md, validate EVERY cited node-ID exists in the codebase.

    Step 1 — Build the citation set. Determine which pins exist by reading the audit DECISION line and the SUMMARYs of executed plans (1088-01, 1088-03 if ran, 1088-04 if ran). Expected citations:
    - Always: `backend/tests/test_fixture_isolation_v1020.py::test_lifecycle_retries_on_transient_too_many_clients`
    - If 1088-03 ran: `backend/tests/test_fixture_isolation_v1020.py::test_setup_phase_contention_retries_or_serializes`
    - If 1088-04 ran: `backend/tests/test_fixture_isolation_v1020.py::test_in_test_contention_retries_succeeds`

    Step 2 — Validate each citation by `git grep -n "def <test_name>" backend/tests/test_fixture_isolation_v1020.py` returning exactly 1 match per test. If any returns 0, halt — the pin is missing or named differently than the planner expected.

    Step 3 — Edit `.planning/REQUIREMENTS.md`:
    - FI-02 line ~20: checkbox `[ ]` → `[x]`.
    - FI-02 body: append a regression-pin reference line that lists each cited node-ID (one per line, bullet-formatted under the FI-02 entry, AFTER the existing acceptance criterion sentence). This makes FI-02 self-documenting for the audit.
    - FI-03 line ~22: checkbox `[ ]` → `[x]`.
    - FI-03 body: append the same node-ID citations under FI-03 (FI-03 explicitly requires per-pin citation; FI-02 cross-references for traceability).
    - Traceability table at line ~73-74: `| FI-02 | Phase 1088 | Pending |` → `| FI-02 | Phase 1088 | Complete |` AND `| FI-03 | Phase 1088 | Pending |` → `| FI-03 | Phase 1088 | Complete |`.
    - Update the "Coverage" block at line ~82-84 if needed (e.g., increment Phase 1088 closed count from 0 to 2).

    Step 4 — Edit `.planning/ROADMAP.md`:
    - Line 91: `- [ ] **Phase 1088: Fixture-Isolation Fixes + Regression Pins**` → `- [x] **Phase 1088: ...**`.
    - The Phase 1088 detail block (around lines 110-120) `**Plans**: TBD` → `**Plans**: <N>/<N> plans complete` listing actual files: `1088-01-PLAN.md (silent-swallow fix)`, `1088-02-PLAN.md (re-measure gate)`, `1088-03-PLAN.md (setup-contention [executed|skipped])`, `1088-04-PLAN.md (in-test-contention [executed|skipped])`, `1088-05-PLAN.md (final close-out)`.

    DO NOT commit yet — Task 3 stages all four files together (REQUIREMENTS.md + ROADMAP.md + SUMMARY.md + the test file if any final consolidation edits were made) in a single commit per TD-13 traceability_flip rule.

    Per v1019 retro Incident 3 (`.planning/retros/v1019-process.md`).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && grep -qE '\- \[x\] \*\*FI-02\*\*' .planning/REQUIREMENTS.md && grep -qE '\- \[x\] \*\*FI-03\*\*' .planning/REQUIREMENTS.md && grep -qE '\| FI-02 \| Phase 1088 \| Complete \|' .planning/REQUIREMENTS.md && grep -qE '\| FI-03 \| Phase 1088 \| Complete \|' .planning/REQUIREMENTS.md && grep -q 'test_lifecycle_retries_on_transient_too_many_clients' .planning/REQUIREMENTS.md && grep -qE '\- \[x\] \*\*Phase 1088' .planning/ROADMAP.md</automated>
  </verify>
  <done>REQUIREMENTS.md FI-02 + FI-03 both flipped to `[x]` AND `Complete`. All cited regression-pin node-IDs are validated (greppable in test file). ROADMAP.md Phase 1088 checkbox `[x]`. Plans list reflects actual plan files. NO commit made yet — staged for Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Phase SUMMARY + single-commit TD-13 traceability flip</name>
  <files>.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-05-SUMMARY.md</files>
  <action>
    Write Phase 1088 close SUMMARY at `.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-05-SUMMARY.md`. Required content:

    Header: plan ID + phase ID + Phase 1088 close attribution + date 2026-05-22.

    Summary of executed plans:
    - 1088-01: silent-swallow fix at conftest.py:275-278 + category-4.1 regression pin. Status: complete.
    - 1088-02: re-measure gate audit doc (`.planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md`). Decision branch: <verbatim from 1088-02 audit>.
    - 1088-03: <executed | SKIPPED per 1088-02 DECISION>. If executed: chosen fix shape + post-fix 4.2 count + regression pin.
    - 1088-04: <executed | SKIPPED per 1088-02 DECISION>. If executed: retry-with-backoff applied + post-fix 4.3 count + regression pin.
    - 1088-05: this close plan.

    Pre/post per-category table:
    - 4.1: 407 → <post-fix N> (Δ: <delta>) — RESOLVED via 1088-01
    - 4.2: 150 → <post-fix N> (Δ: <delta>) — <RESOLVED via cascade | RESOLVED via 1088-03 | DEFERRED to HYG-02 below threshold>
    - 4.3: 87 → <post-fix N> (Δ: <delta>) — <RESOLVED via cascade | RESOLVED via 1088-04 | DEFERRED to HYG-02 below threshold>
    - 4.4: 2 → <post-fix N> (Δ: <delta>) — <RESOLVED via cascade | DEFERRED>
    - 4.5: 2 → <post-fix N> (Δ: <delta>) — DEFERRED (sandbox/assertion, case-by-case to HYG-01)
    - Total: 648 → <post-fix N>; cascade_total: <pre-fix 646> → 0

    Final close gate: sequential `pytest tests/`: verbatim summary line from `/tmp/v1088-final-sequential.log`. Parallel `pytest -n auto`: verbatim summary line from `/tmp/v1088-final-parallel.log`. Cascade-categories assertion: `4.1 + 4.2 + 4.3 + 4.4 = 0` (cite the JSON output).

    FI-02 + FI-03 closure: explicit statement that both are Complete in REQUIREMENTS.md + traceability table; node-ID citations enumerated; TD-13 `requirements_traceability_flip` rule observed (flip + SUMMARY in SAME commit).

    v1019 patterns preserved (HARD floor): NullPool branch, _SETUP_STAGGER_SECONDS, _make_test_async_engine signature. Cite `grep -q "poolclass=NullPool" backend/tests/conftest.py` PASS.

    Out-of-scope items reaffirmed: no frontend, no schema, no max_connections bump, no -n cap, no CI workflow, no make test default switch, no perf benchmark, no skip audit, no flake hunt, no tag cuts. CI-01/CI-02/PERF-01 → Phase 1089. HYG-01/HYG-02/HYG-03 → Phase 1090.

    Carry-forward to Phase 1089: NONE expected. Phase 1088 hands off a green parallel baseline.

    Stage ALL files in a single git commit per TD-13 `requirements_traceability_flip`:
    - `git add .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-05-SUMMARY.md`
    - If any consolidation edits were made to `backend/tests/test_fixture_isolation_v1020.py` during Task 1, include that too.
    - Commit subject: `docs(1088-05): close Phase 1088 — flip FI-02 + FI-03; cascade categories 0`.
    - Commit body via HEREDOC, MUST cite: pre/post cascade_total (648 → 0), plans executed/skipped, node-ID citations enumerated, TD-13 rules observed.

    HARD GATE post-commit: verify ALL traceability files are in the SAME commit via `git diff-tree --no-commit-id --name-only -r HEAD`. Expected files: `.planning/REQUIREMENTS.md` + `.planning/ROADMAP.md` + `.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-05-SUMMARY.md`. If any are missing from the commit, FAIL — per v1019 retro Incident 3, this exact gap caused fix-up commit `5bf63166`.

    Per v1019 retro `.planning/retros/v1019-process.md` Incidents 1, 2, 3. Per CONTEXT.md `<decisions>` "TD-13 rules in effect".
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && test -f .planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-05-SUMMARY.md && git log -1 --format='%s' | grep -q '1088-05' && git diff-tree --no-commit-id --name-only -r HEAD | grep -q '^\.planning/REQUIREMENTS\.md$' && git diff-tree --no-commit-id --name-only -r HEAD | grep -q '^\.planning/ROADMAP\.md$' && git diff-tree --no-commit-id --name-only -r HEAD | grep -q '1088-05-SUMMARY\.md$' && grep -qE 'cascade_total.*0|cascade.*= 0\b' .planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-05-SUMMARY.md</automated>
  </verify>
  <done>Phase 1088 close SUMMARY exists. REQUIREMENTS.md + ROADMAP.md + SUMMARY.md ALL in the most recent commit (single-commit TD-13 traceability flip verified). cascade_total = 0 cited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner-cited test names → executor verify gate | TD-13 req_citation_pinning rule: every cited node-ID is validated via `git grep -n "def <test_name>" <path>` before commit. |
| Executor SUMMARY commit → audit (milestone audit consumer) | TD-13 requirements_traceability_flip rule: REQUIREMENTS.md + ROADMAP.md + SUMMARY.md MUST land in the SAME commit. Milestone audit (gsd-audit) consumes `git diff-tree --no-commit-id --name-only -r HEAD` to verify. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1088-10 | Tampering | Stale REQUIREMENTS.md traceability row (v1019 retro Incident 3) | mitigate | Task 3 stages REQUIREMENTS.md + ROADMAP.md + SUMMARY.md in a single commit; verify gate checks all three are in `git diff-tree --no-commit-id --name-only -r HEAD`. |
| T-1088-11 | Tampering | Paraphrased/incorrect test name in REQ citation (v1019 retro Incidents 1+2) | mitigate | Task 2 validates each cited node-ID via `git grep -n "def <test_name>" <path>` before edits. |
| T-1088-12 | Information Disclosure | Pre/post counts disclosed in SUMMARY | accept | Operational measurement state; not sensitive. |
| T-1088-SC | Tampering | Package legitimacy | N/A | No new packages. |
</threat_model>

<verification>
1. `/tmp/v1088-final-sequential.log` shows `failed == 0` and `passed >= 3036`.
2. `/tmp/v1088-final-categories.json` shows `cascade_total == 0` (sum of 4.1 + 4.2 + 4.3 + 4.4).
3. All v1020 regression pins live in `backend/tests/test_fixture_isolation_v1020.py` (greppable; no orphaned pins elsewhere).
4. `.planning/REQUIREMENTS.md` has `- [x] **FI-02**` and `- [x] **FI-03**` (checkbox flipped).
5. `.planning/REQUIREMENTS.md` traceability table has `| FI-02 | Phase 1088 | Complete |` and `| FI-03 | Phase 1088 | Complete |`.
6. `.planning/REQUIREMENTS.md` cites at least the 1088-01 regression-pin node-ID; cites 1088-03 + 1088-04 pins if those plans ran.
7. `.planning/ROADMAP.md` Phase 1088 entry is `- [x]`; **Plans**: line reflects actual plan files.
8. `.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-05-SUMMARY.md` exists.
9. Most recent commit is the TD-13 close commit (subject contains `1088-05`).
10. **TD-13 single-commit gate** (per v1019 retro Incident 3): `git diff-tree --no-commit-id --name-only -r HEAD` contains ALL of: `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-05-SUMMARY.md`.
</verification>

<success_criteria>
- Final close gate green: sequential `failed == 0` (3036+ passed); parallel cascade-categories (4.1+4.2+4.3+4.4) = 0; 4.5 documented (≤ 2 acceptable).
- All v1020 regression pins consolidated in `backend/tests/test_fixture_isolation_v1020.py`.
- REQUIREMENTS.md FI-02 + FI-03 flipped to `[x]` + `Complete` with node-ID citations validated via git grep.
- ROADMAP.md Phase 1088 flipped to `[x]` with reconciled plans list.
- Phase 1088 SUMMARY committed in the SAME commit as REQUIREMENTS.md + ROADMAP.md (TD-13 single-commit rule, observed via `git diff-tree --no-commit-id --name-only -r HEAD`).
- No orphaned regression pins, no fix-up commits required.
</success_criteria>

<output>
Create `.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-05-SUMMARY.md` when done. Single commit: REQUIREMENTS.md + ROADMAP.md + 1088-05-SUMMARY.md together.
</output>
