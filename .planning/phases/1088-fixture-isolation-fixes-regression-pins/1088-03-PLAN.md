---
phase: 1088-fixture-isolation-fixes-regression-pins
plan: 03
type: execute
wave: 3
depends_on:
  - 1088-02
files_modified:
  - backend/tests/conftest.py
  - backend/tests/test_fixture_isolation_v1020.py
autonomous: true
conditional: true
condition: "Plan 1088-02 audit doc `DECISION:` line contains `SPAWN-1088-03` OR `SPAWN-1088-03-AND-1088-04`. If 1088-02 emits `PROCEED-TO-1088-05`, this plan is SKIPPED (do not execute)."
requirements:
  - FI-02
  - FI-03

must_haves:
  truths:
    - "Plan 1088-02's audit doc names this plan in its `DECISION:` branch (`SPAWN-1088-03` or `SPAWN-1088-03-AND-1088-04`)"
    - "Setup-phase connection contention (audit category 4.2) is mitigated by a structural fix in the `client` fixture's setup path (conftest.py:451-507) OR in `_make_test_async_engine` (conftest.py:51-74) OR by widening `_SETUP_STAGGER_SECONDS` — executor picks during execution per audit Section 5 illustrative shapes"
    - "Post-fix `pytest -n auto` returns < 50 failures in category 4.2 (the threshold below which residual is acceptable flake — covered by HYG-02 in Phase 1090)"
    - "Sequential baseline `pytest tests/` stays green (`failed == 0`)"
    - "Regression pin `test_setup_phase_contention_retries_or_serializes` exists at `backend/tests/test_fixture_isolation_v1020.py` and would FAIL against the pre-1088-03 HEAD"
    - "v1019 patterns are NOT regressed: NullPool branch from commit ea24168c, 5s startup stagger from commit 1aaf81c5, _make_test_async_engine helper signature"
  artifacts:
    - path: "backend/tests/conftest.py"
      provides: "Structural fix for setup-phase connection contention — shape TBD (semaphore / retry-with-backoff / widen stagger)"
      contains: "Plan 1088-03"
    - path: "backend/tests/test_fixture_isolation_v1020.py"
      provides: "Regression pin for category 4.2 setup-phase connection contention"
      contains: "def test_setup_phase_contention_retries_or_serializes"
  key_links:
    - from: "backend/tests/conftest.py"
      to: "(fix surface — TBD: `_make_test_async_engine` OR `client` fixture body OR `_SETUP_STAGGER_SECONDS`)"
      via: "structural fix in setup path"
      pattern: "Plan 1088-03"
---

<objective>
**CONDITIONAL PLAN — only execute if Plan 1088-02's audit doc emits `DECISION: SPAWN-1088-03` or `DECISION: SPAWN-1088-03-AND-1088-04`. If 1088-02 emits `DECISION: PROCEED-TO-1088-05`, this plan is SKIPPED entirely (do not execute Tasks 1-3; do not commit).**

Apply a structural fix for audit category 4.2 (setup-phase connection contention — 150 pre-fix failures, may persist after 1088-01's silent-swallow fix). Per audit Section 5 (`.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md:1272-1287`) three illustrative shapes exist:
- (a) Widen the stagger window to 7-8s per worker (`_SETUP_STAGGER_SECONDS` at conftest.py:106) — slower wall-clock but more headroom (last worker at gw15 × 8 = 120s).
- (b) Per-worker setup-phase semaphore that serialises `client` fixture's `_make_test_async_engine` + `_ensure_roles_and_admin` calls across the test-DB-already-exists window.
- (c) Retry-with-backoff inside `_make_test_async_engine` for `TooManyConnections` (mirror 1088-01's 4.1 fix shape).

The executor picks the shape during execution, guided by 1088-02's audit doc (which may have additional commentary on which shape is most appropriate given the observed residual count). The HARD requirement: post-fix category 4.2 count must drop below 50.

Output: modified `backend/tests/conftest.py` + extended `backend/tests/test_fixture_isolation_v1020.py` with category-4.2 regression pin + targeted re-measure proving 4.2 < 50.

Purpose: this is the structural follow-up for the 23.1% slice of failures that may not resolve via 1088-01's lifecycle-race fix alone. Conditional on 1088-02's re-measure data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-CONTEXT.md
@.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md
@.planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md
@.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-01-SUMMARY.md
@.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-02-SUMMARY.md

<interfaces>
Existing surfaces (Plan 1088-01 left these untouched; this plan may touch them):

- `_make_test_async_engine(test_database_url)` at conftest.py:51-74 — per-worker async engine factory; NullPool branch for xdist mode. Signature MUST be preserved (called from `client` fixture at conftest.py:482).
- `_SETUP_STAGGER_SECONDS = 5.0` at conftest.py:106 — module-level constant; `_get_setup_stagger_delay()` reads it.
- `client` fixture at conftest.py:450-540 — async fixture that calls `_make_test_async_engine`, patches `app.core.db`, sets up `override_get_db`, etc.
- `_ensure_roles_and_admin` — referenced in audit Section 4.2 (`.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md:1094-1095`); executor MUST grep conftest.py for this symbol to locate the call site (audit cites it but does not pin the line number — `git grep -n "_ensure_roles_and_admin" backend/tests/conftest.py` resolves it).

v1019 patterns that MUST NOT be regressed:
- NullPool branch at conftest.py:64-66 (xdist mode returns `create_async_engine(test_database_url, poolclass=NullPool, echo=False)`).
- 5.0s stagger constant at conftest.py:106 (if widened, the comment block at conftest.py:77-105 explaining the rationale must be updated to reflect the new value).
- The `_derive_test_pool_sizing` helper at conftest.py:27-48 (returns `(5, 2)` for master, `(1, 0)` sentinel for xdist).
- All TD-09..TD-14 v1019 deliverables (frontend typecheck, /maps/new redirect, /api/api/ fix, process retro, ssl=False probe) — none of these are in scope but must not regress.

Audit Section 4.2 details:
- 150 failures pre-fix
- Mechanism: 16 workers' staggered setup windows overlap enough that the connection count spikes above max_connections=30 during the `client` fixture's `_make_test_async_engine` + `_ensure_roles_and_admin` window.
- Representative failures: `backend/tests/test_maps.py::TestCreateMap::test_create_map_as_viewer_forbidden`, `backend/tests/test_features_crud.py::TestInsertFeature::test_insert_feature_wrong_geometry_type`, `backend/tests/test_jobs_router.py::TestGetJobStatus::test_get_job_surfaces_legacy_collision_warning_message`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply structural fix for category 4.2 setup-phase contention</name>
  <files>backend/tests/conftest.py</files>
  <behavior>
    - First check: read `.planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md` `DECISION:` line. If it does NOT contain `SPAWN-1088-03` (e.g., it says `PROCEED-TO-1088-05` or `SPAWN-1088-04` alone), HALT — this plan should not be executing. Emit a clear stop signal to the orchestrator: `PLAN 1088-03 SKIP: 1088-02 audit does not require it`.
    - If the gate passes (audit says SPAWN-1088-03), apply ONE of three structural fixes (executor picks the most appropriate given 1088-02's residual count):
      - Shape (a) — Widen `_SETUP_STAGGER_SECONDS` from 5.0 to 7.0 or 8.0. Update the comment block at conftest.py:77-105 to reflect the new value (wall-clock impact: last worker delay rises from 75s to 105-120s; total parallel wall clock impact: +30-45s).
      - Shape (b) — Add a per-worker setup-phase serializer around the `client` fixture's `_make_test_async_engine` + `_ensure_roles_and_admin` calls. Use `filelock` (already-available stdlib alternative or pip) or a simpler `os.O_EXCL` lockfile approach. Lock-file path: `tempfile.gettempdir() + "/v1020-setup-phase.lock"`. The lock acquired during `client` fixture setup, released after engine creation completes.
      - Shape (c) — Retry-with-backoff inside `_make_test_async_engine` for `OperationalError("too many clients already")`. Mirror 1088-01's retry pattern (3 attempts, `[1.0, 2.0, 4.0]` backoff). The engine creation either succeeds eventually or raises loudly (no silent swallow).
    - Test contract for the chosen shape:
      - Sequential baseline `pytest tests/` stays green (`failed == 0`).
      - Post-fix `pytest -n auto` returns < 50 failures in category 4.2 (verified by Task 3's targeted re-measure).
      - v1019 patterns are preserved: NullPool branch still active in xdist mode; `_make_test_async_engine` signature unchanged (callers still invoke `_make_test_async_engine(settings.test_database_url)`).
  </behavior>
  <action>
    Read `.planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md`. If its `DECISION:` line does not name 1088-03, emit `PLAN 1088-03 SKIP: 1088-02 audit does not require it` and halt the executor — DO NOT modify any files. Otherwise, edit `backend/tests/conftest.py` per the chosen shape (a, b, or c above). Document the chosen shape + rationale in a code comment above the edited region citing this plan and audit Section 4.2. The executor MUST verify the chosen shape preserves the v1019 NullPool branch (grep `poolclass=NullPool` in conftest.py — must still match), the `_SETUP_STAGGER_SECONDS` rationale comment block (still present, optionally updated for new value), and the `_make_test_async_engine` signature (still `(test_database_url)` only — no breaking changes to callers).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && grep -q "poolclass=NullPool" tests/conftest.py && grep -qE "_SETUP_STAGGER_SECONDS\s*=\s*[0-9.]+" tests/conftest.py && grep -qE "Plan 1088-03|1088-03" tests/conftest.py</automated>
  </verify>
  <done>conftest.py modified. v1019 NullPool branch preserved (grep returns >= 1). `_SETUP_STAGGER_SECONDS` constant still exists. A comment block citing Plan 1088-03 exists somewhere in the file (proof of intentional edit).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression pin for category 4.2 setup-phase contention</name>
  <files>backend/tests/test_fixture_isolation_v1020.py</files>
  <behavior>
    - Test contract for the new regression pin `test_setup_phase_contention_retries_or_serializes`:
      - The test simulates N concurrent invocations of the chosen fix path (e.g., 3 mock workers calling `_make_test_async_engine` simultaneously) and asserts no `TooManyConnectionsError` propagates.
      - Pre-fix HEAD: under simulated concurrent demand, the chosen fix path would have raised `TooManyConnectionsError` (or whichever exception class the audit 4.2 cites — typically `OperationalError`/`asyncpg.exceptions.TooManyConnectionsError`).
      - Post-fix HEAD: the chosen shape (semaphore serializes / retry recovers / widened stagger prevents overlap) results in all N invocations completing successfully.
    - Pin shape depends on the chosen fix shape (a/b/c):
      - Shape (a) widened stagger → pin asserts `_get_setup_stagger_delay()` returns the new value for gw0..gw15 (e.g., 7s × 15 = 105s) AND the comment block at conftest.py:77-105 has been updated.
      - Shape (b) semaphore → pin spawns 3 `threading.Thread`s that each attempt to acquire the lock; assert only 1 holds at a time.
      - Shape (c) retry-with-backoff → pin patches `create_async_engine` to raise `OperationalError("too many clients already")` once then succeed; assert retry succeeds (similar shape to 1088-01's Task 2 pin but targeting `_make_test_async_engine`).
    - The test is added to the SAME file as 1088-01's pin (`backend/tests/test_fixture_isolation_v1020.py`) — not a new file. Co-located pins keep all v1020 fixture-isolation regression assertions in one greppable location.
  </behavior>
  <action>
    Read `.planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md`. If its `DECISION:` line does not name 1088-03, halt as in Task 1. Otherwise, append `test_setup_phase_contention_retries_or_serializes` to `backend/tests/test_fixture_isolation_v1020.py` (do NOT touch the existing `test_lifecycle_retries_on_transient_too_many_clients` from 1088-01). The pin shape matches the chosen fix shape from Task 1. The test MUST pass against post-Task-1 HEAD. Verify the function is greppable: `git grep -n "def test_setup_phase_contention_retries_or_serializes" backend/tests/test_fixture_isolation_v1020.py` returns exactly 1 match.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && git grep -n "def test_setup_phase_contention_retries_or_serializes" tests/test_fixture_isolation_v1020.py | grep -c 'def test_setup_phase_contention_retries_or_serializes' | awk '$1 == 1 {exit 0} {exit 1}' && env $(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) uv run pytest tests/test_fixture_isolation_v1020.py::test_setup_phase_contention_retries_or_serializes -x -v 2>&1 | tee /tmp/1088-03-task2-pin.log | grep -qE '1 passed'</automated>
  </verify>
  <done>`test_setup_phase_contention_retries_or_serializes` exists at `backend/tests/test_fixture_isolation_v1020.py` (greppable, exactly 1 match). Test passes against post-Task-1 HEAD. Pre-1088-01 pin (`test_lifecycle_retries_on_transient_too_many_clients`) is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Targeted re-measure + sequential gate + SUMMARY + commit</name>
  <files>.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-03-SUMMARY.md</files>
  <action>
    Read `.planning/audits/PYTEST-XDIST-REMEASURE-AFTER-1088-01.md`. If its `DECISION:` line does not name 1088-03, halt as above. Otherwise: (Step 1) drop stale per-worker DBs per audit Section 1 Step 1b. (Step 2) sequential baseline: `cd /Users/ishiland/Code/geolens/backend && env $(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) uv run pytest tests/` — HARD GATE `failed == 0`. (Step 3) targeted parallel re-measure: `env $(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) uv run pytest -n auto --junitxml=/tmp/v1020-remeasure-1088-03.xml tests/`. (Step 4) re-categorize via the audit's Section 1 Step-5 parser — extract category 4.2 count. Assert `4.2 < 50` (the threshold below which residual is acceptable flake per CONTEXT.md). Write SUMMARY.md at `.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-03-SUMMARY.md` with: plan ID + commit SHA range; chosen fix shape (a/b/c) + rationale; sequential baseline verbatim summary line; post-fix per-category counts (4.1, 4.2, 4.3, 4.4, 4.5) with 4.2 highlighted; verbatim assertion `4.2 = <N> < 50`. Stage and commit Tasks 1+2+3 together: `git add backend/tests/conftest.py backend/tests/test_fixture_isolation_v1020.py .planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-03-SUMMARY.md`. Commit subject: `fix(1088-03): structural fix for setup-phase connection contention (audit 4.2)`. Use HEREDOC for body per Git Safety Protocol. Body MUST cite chosen fix shape, post-fix 4.2 count, and explicit note that REQUIREMENTS.md flip is DEFERRED to Plan 1088-N per TD-13.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && test -f .planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-03-SUMMARY.md && grep -qE '4\.2.*<.*50|4\.2.*= [0-4][0-9]\b|setup-phase contention.*resolved' .planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-03-SUMMARY.md && git log -1 --format='%s' | grep -q '1088-03' && [ "$(git diff-tree --no-commit-id --name-only -r HEAD | grep -c '^\.planning/REQUIREMENTS\.md$')" = "0" ]</automated>
  </verify>
  <done>SUMMARY.md exists. Cites 4.2 post-fix count below threshold of 50. Commit subject contains `1088-03`. REQUIREMENTS.md NOT in commit's file list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest fixture → Postgres (concurrent setup) | Multiple xdist workers race for connection slots during `client` fixture setup. Mitigation domain: pool-sizing + stagger + retry, all client-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1088-06 | Denial of Service | `client` fixture setup connection contention | mitigate | Chosen structural fix (semaphore/retry/widen-stagger) — selected by executor based on 1088-02 audit data. |
| T-1088-07 | Tampering | v1019 NullPool branch could be inadvertently regressed | mitigate | Task 1 verify gate explicitly checks `grep -q "poolclass=NullPool" tests/conftest.py`. |
| T-1088-SC | Tampering | Package legitimacy | conditional | If executor picks shape (b) and chooses to add a new package (e.g., `filelock`), a `[ASSUMED]` `checkpoint:human-verify` is required before install per planner package-legitimacy gate. The executor should prefer stdlib-only approaches (e.g., `os.O_EXCL` + `tempfile.gettempdir()`) to avoid the gate entirely. |
</threat_model>

<verification>
1. Plan 1088-02's audit doc `DECISION:` line was read; if it did not name 1088-03, the plan halted with no file edits.
2. If executed: `grep -q "poolclass=NullPool" backend/tests/conftest.py` returns exit 0 (v1019 pattern preserved).
3. If executed: `git grep -n "def test_setup_phase_contention_retries_or_serializes" backend/tests/test_fixture_isolation_v1020.py` returns exactly 1 match.
4. If executed: targeted re-measure proves post-fix category 4.2 < 50 failures.
5. If executed: SUMMARY.md committed with subject containing `1088-03`.
6. If executed: REQUIREMENTS.md NOT in the commit's file list (`git diff-tree --no-commit-id --name-only -r HEAD | grep -c '^\.planning/REQUIREMENTS\.md$'` returns 0).
</verification>

<success_criteria>
- Either: SKIPPED entirely because 1088-02's `DECISION:` line did not name this plan. OR:
- Structural fix applied; v1019 patterns preserved; 4.2 post-fix < 50; regression pin added; sequential baseline `failed == 0`; SUMMARY.md committed; REQUIREMENTS.md flip deferred to 1088-N.
</success_criteria>

<output>
Create `.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-03-SUMMARY.md` when done (only if executed). Commit Tasks 1+2+3 together.
</output>
