---
phase: 1088-fixture-isolation-fixes-regression-pins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/conftest.py
  - backend/tests/test_fixture_isolation_v1020.py
autonomous: true
requirements:
  - FI-02
  - FI-03

must_haves:
  truths:
    - "Silent-swallow `except Exception: yield; return` at conftest.py:275-278 is removed"
    - "Transient `OperationalError(\"too many clients already\")` during _test_db_lifecycle setup triggers retry-with-backoff (3 attempts) rather than silent swallow"
    - "Truly-unreachable Postgres (DNS failure, connection refused, etc.) still results in pytest.skip with diagnostic message — existing semantics preserved for unit-only runs"
    - "After all retries exhaust, the fixture raises loudly (not swallows) so per-worker test DB creation failure surfaces as fixture error, NOT as 407 downstream InvalidCatalogNameError"
    - "Sequential baseline `cd backend && uv run pytest tests/` returns `failed == 0` (3036/0/38 or higher) — no regression in sequential mode"
    - "Regression pin `test_lifecycle_retries_on_transient_too_many_clients` exists at `backend/tests/test_fixture_isolation_v1020.py` and would FAIL against pre-fix HEAD silent-swallow shape"
  artifacts:
    - path: "backend/tests/conftest.py"
      provides: "Structured OperationalError handler replacing silent-swallow at lines 275-278"
      contains: "except OperationalError"
    - path: "backend/tests/test_fixture_isolation_v1020.py"
      provides: "Regression pin for category 4.1 per-worker DB lifecycle race"
      contains: "def test_lifecycle_retries_on_transient_too_many_clients"
  key_links:
    - from: "backend/tests/conftest.py:12"
      to: "sqlalchemy.exc.OperationalError"
      via: "import added alongside existing SQLAlchemyError import"
      pattern: "from sqlalchemy.exc import.*OperationalError"
    - from: "backend/tests/test_fixture_isolation_v1020.py"
      to: "sqlalchemy.exc.OperationalError"
      via: "import + unittest.mock.patch to simulate transient contention"
      pattern: "from sqlalchemy.exc import OperationalError"
---

<objective>
Replace the silent-swallow `except Exception: yield; return` at `backend/tests/conftest.py:275-278` with a structured handler that distinguishes "Postgres truly unreachable" (pytest.skip — preserves existing semantics for unit-only runs) from "transient connection contention under -n auto" (retry-with-backoff, fail loudly on exhaustion).

This is the FIX FIRST plan per audit Section 5 (`.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md` lines 1244-1271). Per CONTEXT.md LOCKED sequencing this plan ships UNCONDITIONALLY and atomically — re-measurement is deferred to Plan 1088-02. Closes category 4.1 (407/648 failures, 62.8% of total).

Purpose: the current silent-swallow is the dominant structural defect surfaced by Phase 1087's spike — when `dev_engine.connect()` raises `OperationalError("too many clients already")` during the staggered-startup window, the fixture yields with `should_drop_db=False` and returns, leaving the per-worker test DB uncreated. Downstream tests then fail with 407 `asyncpg.exceptions.InvalidCatalogNameError`. The structured handler retries on contention, fails loudly when contention exceeds retry budget, AND keeps the unreachable-host skip path intact.

Output: modified `backend/tests/conftest.py` (lines 12 import + 273-298 region) + new `backend/tests/test_fixture_isolation_v1020.py` with the category-4.1 regression pin. Plan 1088-01 SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-CONTEXT.md
@.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md
@.planning/retros/v1019-process.md

<interfaces>
Existing imports at conftest.py:1-19 (already present; only line 12 needs OperationalError added):

- `import os, time, uuid, tempfile, warnings`
- `import pytest, sqlalchemy, structlog`
- `from sqlalchemy import select, text`
- `from sqlalchemy.exc import SQLAlchemyError`    (current — line 12 — must become `from sqlalchemy.exc import SQLAlchemyError, OperationalError`)
- `from sqlalchemy.pool import NullPool`
- `from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine`
- `from app.core.config import settings`

The target fixture at conftest.py:241-447 has the structure: stagger delay → outer try → `dev_engine = sqlalchemy.create_engine(...)` → inner try `with dev_engine.connect() as conn: DROP + CREATE` → `should_drop_db = True` → `except Exception: yield; return` (TARGET lines 275-278) → `finally: dev_engine.dispose()` → continues to Init/Migrate/SAML-bridge → `yield` → outer finally: teardown.

Helper symbols already in scope inside the except (no new helpers required):

- `_quote_database_identifier(db_name)` is defined at conftest.py:193; `quoted_db_name` is assigned in the try-block at conftest.py:271 so it stays in scope inside the except clause.
- `_drop_test_database_if_exists(db_name)` at conftest.py:219 (NOT needed here — the retry recreates the engine, does not invoke this helper).
- `settings.database_url_sync` is imported via `from app.core.config import settings`.
- `time.sleep` is available via the top-of-file `import time`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add structured OperationalError handler in _test_db_lifecycle</name>
  <files>backend/tests/conftest.py</files>
  <behavior>
    - Test contract: the handler MUST replace the silent-swallow at lines 275-278.
    - The new except clause catches `sqlalchemy.exc.OperationalError` (NOT bare `Exception`).
    - On `"too many clients already"` substring in `str(e).lower()`: retry the DROP+CREATE block up to 3 times with backoff sleeps `[1.0, 2.0, 4.0]`. Between attempts, dispose the old `dev_engine` and recreate it (each attempt opens a fresh `sqlalchemy.create_engine` with `isolation_level="AUTOCOMMIT"`). On a successful retry: set `should_drop_db = True` and fall through to the existing Init phase at conftest.py:282. On exhausted retries: re-raise the final OperationalError (no swallow).
    - On any other OperationalError (DNS failure, refused connection, etc.): call `pytest.skip(f"Postgres unreachable: {e}")` — preserves the existing unit-only-run semantics for hosts where Postgres is genuinely unreachable.
    - DO NOT swallow non-OperationalError exceptions — let them propagate (the original `except Exception` was too broad).
    - Update line 12 to add `OperationalError` to the existing `from sqlalchemy.exc import SQLAlchemyError` line.
    - Add a brief code comment above the except block citing audit Section 4.1 for traceability.
  </behavior>
  <action>
    Edit `backend/tests/conftest.py`. Step (a): update line 12 import to `from sqlalchemy.exc import SQLAlchemyError, OperationalError`. Step (b): replace the silent-swallow block at lines 275-278 with a structured `except OperationalError as e` handler that branches on `"too many clients already" in str(e).lower()` — contention path retries with `[1.0, 2.0, 4.0]` backoff, disposes+recreates `dev_engine` per attempt, calls the same `with dev_engine.connect() as conn: DROP/CREATE` block as the original try, sets `should_drop_db = True` on success, falls through to the Init phase. After the final retry raises, re-raise (no swallow). Non-contention OperationalError path: `pytest.skip(f"Postgres unreachable: {e}")`. Step (c): add a comment above the except referencing audit Section 4.1 and this plan. Surgical edit only — DO NOT modify `_make_test_async_engine` (conftest.py:51-74), `_SETUP_STAGGER_SECONDS` (conftest.py:106), `_get_setup_stagger_delay` (conftest.py:109-129), the Init phase (conftest.py:282-321), the alembic block (conftest.py:323-362), the SAML bridge (conftest.py:364-401), the teardown block (conftest.py:405-446), the `client` fixture (conftest.py:450-540), or `override_get_db` (conftest.py:503-505). The `quoted_db_name` local is already in scope in the except (assigned at conftest.py:271); reuse it in the retry block. Per audit Section 4.1 (`.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md:996-1058`) and CONTEXT.md `<decisions>` "Fix shape for Plan 1088-01".
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && grep -q "from sqlalchemy.exc import.*OperationalError" tests/conftest.py && grep -q "except OperationalError" tests/conftest.py && grep -q "too many clients already" tests/conftest.py && ! python3 -c "import re,sys; src=open('tests/conftest.py').read(); m=re.search(r'except Exception:\n\s*#[^\n]*\n\s*yield\n\s*return', src); sys.exit(0 if m is None else 1)"</automated>
  </verify>
  <done>Line 12 imports `OperationalError`. The region from approximately conftest.py:275 onward contains `except OperationalError as e` with the contention-retry branch and the unreachable-host `pytest.skip` branch. The exact silent-swallow shape (`except Exception:\n    # DB host unreachable...\n    yield\n    return`) no longer exists in the `_test_db_lifecycle` setup region.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression pin for category 4.1 per-worker DB lifecycle race</name>
  <files>backend/tests/test_fixture_isolation_v1020.py</files>
  <behavior>
    - Test contract for the new file:
      - File-level docstring describes the file's purpose: regression pins for v1020 fixture-isolation fixes per FI-03, one test class or function per audit Section 4 category fixed in Phase 1088, cross-references audit at `.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md`.
      - Imports: `pytest`, `from unittest.mock import patch, MagicMock`, `from sqlalchemy.exc import OperationalError`, plus any conftest helpers needed.
    - Single regression-pin test function: `test_lifecycle_retries_on_transient_too_many_clients`.
    - Acceptance criteria for the test:
      - Asserts the post-fix retry path is taken when `dev_engine.connect()` raises a `OperationalError("too many clients already")` on the first attempt then succeeds.
      - Asserts the test DB IS created (NOT silently skipped) — i.e., the post-fix code path branched into the retry, NOT into the swallow.
      - Would FAIL against pre-fix HEAD where the silent-swallow caught the exception without retrying.
  </behavior>
  <action>
    Create `backend/tests/test_fixture_isolation_v1020.py`. Add file-level docstring citing audit Section 4.1 and FI-03. Add imports: `pytest`, `from unittest.mock import patch, MagicMock`, `from sqlalchemy.exc import OperationalError`. Implement `test_lifecycle_retries_on_transient_too_many_clients` that exercises the retry path. Two acceptable implementation strategies (executor picks): (Strategy A) — extract the retry logic into a private helper `_create_test_db_with_retry(make_engine_fn, db_name)` in conftest.py during Task 1, import it from this test, and assert that mocked `make_engine_fn` (first call raises `OperationalError("too many clients already")`, second call returns a working engine-context manager) triggers retry and returns success. (Strategy B) — call `_test_db_lifecycle` indirectly by patching `sqlalchemy.engine.Engine.connect` at the SQLAlchemy class level with a `side_effect` that raises once and succeeds on retry, then drive a small wrapper that reproduces the conftest retry block inline. Patch `tests.conftest.time.sleep` to a no-op so retries do not actually wait during the test. Final assertion: the mocked connect was called >= 2 times AND the function returned successfully (did not propagate the OperationalError). The executor's verify gate confirms the test PASSES against post-Task-1 HEAD; the executor MAY also run `git stash` to confirm the test FAILS against pre-Task-1 HEAD (recommended but not required — the assertion shape is sufficient to catch the silent-swallow case). Per CONTEXT.md `<decisions>` "Regression-pin shapes (LOCKED targets from audit Section 5)" — 4.1 entry. Per audit Section 5 (`.planning/audits/PYTEST-XDIST-FIXTURE-AUDIT-v1020.md:1336-1339`).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && git grep -n "def test_lifecycle_retries_on_transient_too_many_clients" tests/test_fixture_isolation_v1020.py | grep -c 'def test_lifecycle_retries_on_transient_too_many_clients' | awk '$1 == 1 {exit 0} {exit 1}' && env $(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) uv run pytest tests/test_fixture_isolation_v1020.py::test_lifecycle_retries_on_transient_too_many_clients -x -v 2>&1 | tee /tmp/1088-01-task2-pin.log | grep -qE '1 passed'</automated>
  </verify>
  <done>File `backend/tests/test_fixture_isolation_v1020.py` exists. `git grep -n "def test_lifecycle_retries_on_transient_too_many_clients" backend/tests/test_fixture_isolation_v1020.py` returns exactly 1 match. The test passes against post-Task-1 HEAD.</done>
</task>

<task type="auto">
  <name>Task 3: Sequential baseline preservation gate + SUMMARY + commit</name>
  <files>.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-01-SUMMARY.md</files>
  <action>
    HARD GATE: run sequential baseline preservation check, then write SUMMARY, then commit Tasks 1+2+3 deliverables in a single commit. Step 1 — run `cd /Users/ishiland/Code/geolens/backend && env $(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) uv run pytest tests/ 2>&1 | tee /tmp/1088-01-sequential-baseline.log` and capture the final summary line. Assert `M failed == 0` from that line. The 3036/0/38 number from v1019 close-gate is the floor for `N passed`; passes/skips MAY vary (e.g., 3037/0/38 after adding the new regression pin); the M=0 invariant is non-negotiable. If `failed > 0`, halt — the plan is rejected. Step 2 — write SUMMARY.md per the executor's SUMMARY template (`$HOME/.claude/get-shit-done/templates/summary.md`). The SUMMARY MUST include: plan ID, phase ID, commit SHA range (filled by executor); tasks completed (Tasks 1+2 above); files modified (backend/tests/conftest.py line 12 import + ~275-298 retry region + backend/tests/test_fixture_isolation_v1020.py new file); the verbatim sequential baseline summary line from `/tmp/1088-01-sequential-baseline.log` (cite exact `M failed, N passed, K skipped, ...` line, do not paraphrase); partial-close note for FI-02 (category 4.1, 407 failures addressed; full close depends on 1088-02 re-measure) and FI-03 (category-4.1 regression pin added at `backend/tests/test_fixture_isolation_v1020.py::test_lifecycle_retries_on_transient_too_many_clients`); explicit note that REQUIREMENTS.md FI-02/FI-03 traceability flip is DEFERRED to Plan 1088-N per CONTEXT.md LOCKED decision and TD-13 traceability rule; explicit note that re-measure under `pytest -n auto` is DEFERRED to Plan 1088-02 per CONTEXT.md atomicity rule. Step 3 — commit Tasks 1+2+3 deliverables together: stage `backend/tests/conftest.py backend/tests/test_fixture_isolation_v1020.py .planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-01-SUMMARY.md` and create a single commit with subject `fix(1088-01): replace silent-swallow with structured OperationalError handler` and body citing audit Section 4.1, the 407/648 (62.8%) impact, the deferral of re-measure to 1088-02, and the deferral of FI-02/FI-03 traceability flip to 1088-N. Use a HEREDOC for the commit body per Git Safety Protocol.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && test -f .planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-01-SUMMARY.md && grep -qE '^[0-9]+ passed.*0 failed|0 failed.*[0-9]+ passed|3036|3037' .planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-01-SUMMARY.md && git log -1 --format='%s' | grep -q '1088-01'</automated>
  </verify>
  <done>SUMMARY.md exists at the expected path. It cites the sequential baseline result with `failed == 0`. The commit landed (subject contains `1088-01`). REQUIREMENTS.md is NOT modified by this commit (verified separately by 1088-N's traceability flip gate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest fixture → Postgres (test DB lifecycle) | The `_test_db_lifecycle` fixture issues privileged DDL (CREATE/DROP DATABASE) on behalf of the test session. Under contention, an unhandled or silently-swallowed error mutates test outcomes downstream — a Tampering risk against the test-result audit trail. |
| Test runner → Postgres (per-worker) | Each xdist worker is a separate process with its own Postgres session; cross-worker isolation is process-level. A race in setup is an availability concern (denial of test execution) not a data-confidentiality concern. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1088-01 | Tampering | `backend/tests/conftest.py:_test_db_lifecycle` silent-swallow | mitigate | Task 1 replaces silent-swallow with retry-or-raise structured handler; CI failure mode becomes loud (fixture error in JUnit XML) instead of 407 misleading downstream `InvalidCatalogNameError` failures. |
| T-1088-02 | Denial of Service | Connection contention against `max_connections=30` during `pytest -n auto` | mitigate (partial) | Task 1's 3-attempt retry-with-backoff `[1s, 2s, 4s]` absorbs transient peaks; cumulative wait under contention is bounded at 7s (1+2+4) per worker which is below the staggered-startup window's 75s ceiling. If retries exhaust, the worker fails loudly (acceptable — surfaces real saturation rather than hiding it). |
| T-1088-03 | Information Disclosure | Error messages in CI logs | accept | `OperationalError` text from Postgres ("too many clients already") is generic operational state, not sensitive; logging it via `pytest.skip(f"Postgres unreachable: {e}")` is acceptable disclosure. |
| T-1088-SC | Tampering | Package legitimacy (no new dependencies introduced) | N/A | No `pip install` / `pyproject.toml` changes in this plan. `sqlalchemy.exc.OperationalError` is already imported transitively via `SQLAlchemyError` from the existing `sqlalchemy` dependency — no new package surface. Audit gate not required. |
</threat_model>

<verification>
1. `grep -q "from sqlalchemy.exc import.*OperationalError" backend/tests/conftest.py` returns exit 0.
2. `grep -q "except OperationalError" backend/tests/conftest.py` returns exit 0.
3. `grep -q "too many clients already" backend/tests/conftest.py` returns exit 0.
4. The exact triple-line pattern `except Exception:\n    # DB host unreachable[^\n]*\n    yield\n    return` does NOT exist in `_test_db_lifecycle` (`_drop_test_database_if_exists` at conftest.py:235 has its own bare `except` — that is unrelated and out of scope).
5. `git grep -n "def test_lifecycle_retries_on_transient_too_many_clients" backend/tests/test_fixture_isolation_v1020.py` returns exactly 1 match.
6. Sequential baseline (`cd backend && env $(grep -v '^#' .env.test | xargs) uv run pytest tests/`) ends with `0 failed` and `≥ 3036 passed`.
7. `.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-01-SUMMARY.md` exists and cites the sequential baseline line.
8. `git log -1 --format='%s' | grep -q '1088-01'` (the most recent commit is this plan's commit).
9. REQUIREMENTS.md is NOT changed by this commit: `git diff-tree --no-commit-id --name-only -r HEAD | grep -q '^\.planning/REQUIREMENTS\.md$'` returns exit 1.
</verification>

<success_criteria>
- Silent-swallow at conftest.py:275-278 replaced with structured `except OperationalError` handler that retries on `"too many clients already"` and pytest.skips on truly-unreachable hosts.
- New file `backend/tests/test_fixture_isolation_v1020.py` exists with `test_lifecycle_retries_on_transient_too_many_clients` (passes against post-fix HEAD).
- Sequential `pytest tests/` returns `failed == 0` (≥ 3036/0/38).
- Plan 1088-01 SUMMARY.md committed alongside the code change in a single commit.
- REQUIREMENTS.md unchanged in this commit (traceability flip deferred to 1088-N).
- No re-measurement under `pytest -n auto` performed in this plan (deferred to 1088-02 — atomicity rule per CONTEXT.md).
</success_criteria>

<output>
Create `.planning/phases/1088-fixture-isolation-fixes-regression-pins/1088-01-SUMMARY.md` when done.
</output>
