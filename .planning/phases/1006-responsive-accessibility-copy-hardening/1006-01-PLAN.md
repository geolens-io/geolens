---
phase: 1006-responsive-accessibility-copy-hardening
plan: 01
wave: 1
depends_on: []
autonomous: true
subsystem: frontend
requirements: [A11Y-01, A11Y-02, A11Y-03, A11Y-04, A11Y-05, A11Y-06]
files_modified:
  - frontend/src/hooks/use-auth.ts
  - frontend/src/pages/MapViewerGate.tsx
  - frontend/src/components/layout/AppLayout.tsx
  - frontend/src/pages/MapBuilderPage.tsx
  - frontend/src/components/builder/BuilderMap.tsx
  - frontend/src/i18n/locales/en/builder.json
  - frontend/src/i18n/locales/es/builder.json
  - frontend/src/i18n/locales/fr/builder.json
  - frontend/src/i18n/locales/de/builder.json
  - focused frontend tests
---

<objective>
Harden the builder's responsive shell, accessibility state, recovery copy, and locale completeness for the surfaces touched in Phases 1003-1005.
</objective>

<tasks>
  <task type="auto">
    <name>Harden auth shell route state</name>
    <action>Restore the current user into auth state when `getMe` succeeds for a persisted token; update `/maps/:id` gate and layout footer classification so token/user-null transitions do not show signed-out footer artifacts or load editor code prematurely.</action>
    <verify>Focused auth/layout/gate tests cover user restoration, authenticated user-null route loading, editor route footer suppression, and anonymous public map footer behavior.</verify>
    <done>F-1002-02 is closed for shell state and route classification.</done>
  </task>
  <task type="auto">
    <name>Tighten mobile builder shell controls</name>
    <action>Increase mobile sheet save and rail controls to 44px touch targets, constrain sheet width to leave map context visible, and preserve hidden/collapsed inert behavior for desktop sidebar content.</action>
    <verify>Focused MapBuilderPage tests assert mobile touch-target and sheet-width classes; existing builder tests still pass.</verify>
    <done>A11Y-01, A11Y-02, A11Y-03, and A11Y-06 are covered for touched builder shell controls.</done>
  </task>
  <task type="auto">
    <name>Add basemap recovery copy and locales</name>
    <action>Surface scoped, non-blocking basemap/network recovery copy in BuilderMap when basemap style or map tile failures occur; add en/es/fr/de builder strings.</action>
    <verify>Focused BuilderMap and i18n resource tests pass.</verify>
    <done>F-1002-08 and A11Y-04/A11Y-05 are covered for touched recovery copy.</done>
  </task>
</tasks>

<verification>
- `cd frontend && npm run test -- use-auth auth-store AppLayout MapViewerGate MapBuilderPage.header-actions BuilderMap.unit resources --run`
- `cd frontend && npm run lint`
</verification>

<must_haves>
- No footer artifact on authenticated builder route while `user` is temporarily null.
- Anonymous `/maps/:id` remains a public viewer route.
- Mobile builder shell controls touched in this phase meet 44px target sizing.
- Basemap failures use action-oriented copy and do not block layer editing.
- Touched builder copy has matching keys in en, es, fr, and de.
</must_haves>
