---
phase: 1012-add-dataset-modal-redesign
plan: 01
wave: 1
depends_on: []
autonomous: true
requirements: [ADD-01, ADD-02, ADD-03, ADD-04, ADD-05, ADD-06, ADD-07, ADD-08]
subsystem: frontend-builder
tags: [map-builder, add-dataset, search, basemap, tests]
files_modified:
  - frontend/src/components/builder/DatasetSearchPanel.tsx
  - frontend/src/components/builder/BuilderDialogs.tsx
  - frontend/src/pages/MapBuilderPage.tsx
  - frontend/src/components/builder/__tests__/DatasetSearchPanel.test.tsx
  - .planning/phases/1012-add-dataset-modal-redesign/1012-01-SUMMARY.md
---

<objective>
Rewrite the Add Dataset modal around the v1 row states, tabs, supported filters, expandable rows, basemap swap, and import routing while using only existing APIs and map fields.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1012-add-dataset-modal-redesign/1012-CONTEXT.md
@frontend/src/components/builder/DatasetSearchPanel.tsx
@frontend/src/components/builder/BuilderDialogs.tsx
@frontend/src/pages/MapBuilderPage.tsx
@frontend/src/components/builder/hooks/use-builder-layers.ts
@frontend/src/api/search.ts
@frontend/src/lib/basemap-utils.ts
</context>

<tasks>
  <task type="auto">
    <name>Expand modal props and action wiring</name>
    <files>frontend/src/components/builder/BuilderDialogs.tsx, frontend/src/pages/MapBuilderPage.tsx</files>
    <action>Pass current layers, basemap state/setters, and duplicate-rendering handler into `DatasetSearchPanel`.</action>
    <verify>Typecheck through build; panel tests exercise the handlers directly.</verify>
    <done>The modal can perform all v1 writes through existing handlers.</done>
  </task>

  <task type="auto">
    <name>Rewrite DatasetSearchPanel rows and tabs</name>
    <files>frontend/src/components/builder/DatasetSearchPanel.tsx</files>
    <action>Add All/Vector/Raster/Basemap tabs, search-first layout, supported filter chips, data/basemap row states, row expansion, preview metadata, and `/import` footer link.</action>
    <verify>Component tests cover tabs, filters, actions, expansion, and footer routing.</verify>
    <done>ADD-01..08 are implemented in the existing modal.</done>
  </task>

  <task type="auto">
    <name>Add modal behavior tests</name>
    <files>frontend/src/components/builder/__tests__/DatasetSearchPanel.test.tsx</files>
    <action>Mock search/basemap hooks and verify Add, added, another rendering, swap, in use, filters, expansion, and Import data routing.</action>
    <verify>`npm run test -- DatasetSearchPanel MapStackPanel map-stack renderAs use-builder-layers --run` passes.</verify>
    <done>Phase 1012 behavior has focused regression coverage.</done>
  </task>

  <task type="auto">
    <name>Record summary and verification</name>
    <files>.planning/phases/1012-add-dataset-modal-redesign/1012-01-SUMMARY.md</files>
    <action>Document implementation, requirement coverage, and verification commands/results.</action>
    <verify>Summary exists and matches commands run.</verify>
    <done>Phase 1012 can hand off to milestone QA closeout.</done>
  </task>
</tasks>

<verification>
- `cd frontend && npm run test -- DatasetSearchPanel MapStackPanel map-stack renderAs use-builder-layers --run`
- `cd frontend && npm run lint`
- `cd frontend && npm run build`
</verification>

<must_haves>
- Use existing dataset search API only.
- No Curated/Your imports/Public chips.
- No direct modal-to-stack drag.
- No import flow inside the modal.
- No new backend endpoint, migration, or persisted field.
</must_haves>

<success_criteria>
- [ ] Tabs are All, Vector, Raster, and Basemap.
- [ ] Data rows show Add to map, added, and another rendering states correctly.
- [ ] Basemap rows show swap and in-use states correctly.
- [ ] Expanded rows show preview/metadata and primary actions.
- [ ] Footer links to `/import`.
- [ ] Focused tests, lint, and build pass.
</success_criteria>
