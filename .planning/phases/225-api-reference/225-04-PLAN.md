---
phase: 225-api-reference
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - getgeolens.com/docs/src/content/docs/guides/api/auth.mdx
autonomous: true
requirements: [API-03]
must_haves:
  truths:
    - "A reader at `/guides/api/auth` sees three auth methods documented in this order: JWT Bearer → API key (header AND query) → OAuth/OIDC"
    - "The API-key header form uses `X-Api-Key: <key>` (NOT `Authorization: Bearer <api_key>`) — verified against `backend/app/modules/auth/dependencies.py:25`"
    - "Every curl example uses the placeholder host `https://geolens.example.com/api/...` — no `demo.getgeolens.com`, no `localhost`, no real credentials"
    - "Each section has at least one ` ```bash ` curl example"
  artifacts:
    - path: "getgeolens.com/docs/src/content/docs/guides/api/auth.mdx"
      provides: "Hand-authored API authentication page"
      min_lines: 80
      contains: "X-Api-Key"
  key_links:
    - from: "auth.mdx API key section"
      to: "backend behavior at backend/app/modules/auth/dependencies.py:25"
      via: "`X-Api-Key` header literal"
      pattern: "X-Api-Key"
    - from: "auth.mdx OAuth section"
      to: "/guides/admin/oauth (Phase 227 forward-ref)"
      via: "MDX link"
      pattern: "/guides/admin/oauth"
---

<objective>
Author `/guides/api/auth.mdx` — the single hand-authored authentication page for the GeoLens REST API. Three sections in order (D-10): JWT Bearer, API key (both forms), OAuth/OIDC. Each section has a working ` ```bash ` curl example. Placeholder host throughout (D-15).

**CRITICAL CORRECTION (planning_context #2 + RESEARCH.md §Pitfall 2):** CONTEXT.md D-12 contains an error. The original D-12 wording said `Authorization: Bearer <api_key>` for the API-key header. **This is wrong.** The actual backend reality, verified at `backend/app/modules/auth/dependencies.py:23-29` and confirmed in the FastAPI `_DESCRIPTION` at `backend/app/api/main.py:248-252`, is the `X-Api-Key` HTTP header. The auth.mdx content MUST use `X-Api-Key` and MUST NOT include the wrong `Authorization: Bearer <api_key>` form anywhere in the API-key section. (`Authorization: Bearer <jwt>` is correct for the JWT section — distinct flow.)

Purpose: Establishes the FIRST hand-authored MDX with non-trivial body content in the docs subtree. Implicitly anchors the convention for ogc.mdx (Plan 05), Plan 06's index.mdx, and Phase 226/227 prose.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/225-api-reference/225-CONTEXT.md
@.planning/phases/225-api-reference/225-RESEARCH.md
@.planning/phases/225-api-reference/225-PATTERNS.md
@/Users/ishiland/Code/geolens/backend/app/modules/auth/dependencies.py
@/Users/ishiland/Code/getgeolens.com/docs/src/content/docs/index.mdx
@/Users/ishiland/Code/getgeolens.com/docs/plugins/ec-pagefind-weight.mjs

<interfaces>
<!-- Source-of-truth API key header — DO NOT trust CONTEXT.md D-12; trust the backend file -->

From `backend/app/modules/auth/dependencies.py` (lines 23-29):
```python
async def _resolve_api_key(request: Request) -> ApiKey | None:
    raw = request.headers.get("X-Api-Key")
    if not raw:
        raw = request.query_params.get("api_key")
    if not raw:
        return None
    ...
```

The header name is **`X-Api-Key`** (case-insensitive but cite this casing). Resolution order: header > query > JWT > anonymous.

From `backend/app/api/main.py` (lines 248-252, the FastAPI `_DESCRIPTION` block):
> "Pass it as an `X-Api-Key` header or as `?api_key=...` in the query string. The header takes precedence."

JWT login endpoint (verified at `backend/app/modules/auth/router.py:54`):
- POST `/api/auth/login/` with form-urlencoded body `username=...&password=...`
- Returns `{access_token, refresh_token, token_type: "bearer"}`
- Use as `-H "Authorization: Bearer <access_token>"` (this IS Bearer — but for JWT, not API keys)

OAuth endpoints (verified at `backend/app/modules/auth/oauth/router.py:69`):
- GET `/api/auth/oauth/{provider_slug}/login` (browser redirect)
- Callback to `/api/auth/oauth/{provider_slug}/callback`
- Web UI extracts JWT and uses as Bearer token

Frontmatter convention (S-5, from `docs/src/content/docs/index.mdx`):
```mdx
---
title: ...
description: ...
---
```

Code-block convention (S-3, from `docs/plugins/ec-pagefind-weight.mjs`):
- Use ` ```bash ` for curl examples (NOT ` ```sh `)
- Expressive Code auto-applies `data-pagefind-weight="0.1"` — DO NOT manually add HTML attrs
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create auth.mdx with three corrected auth sections</name>
  <files>getgeolens.com/docs/src/content/docs/guides/api/auth.mdx</files>
  <read_first>
    - /Users/ishiland/Code/geolens/backend/app/modules/auth/dependencies.py (lines 23-29 — confirms `X-Api-Key`)
    - /Users/ishiland/Code/geolens/backend/app/api/main.py (lines 248-252, the `_DESCRIPTION` block — confirms backend's own self-documentation)
    - /Users/ishiland/Code/getgeolens.com/docs/src/content/docs/index.mdx (frontmatter pattern)
    - /Users/ishiland/Code/geolens/.planning/phases/225-api-reference/225-RESEARCH.md §Code Examples 3 (lines 574-609, verified curl shapes)
    - /Users/ishiland/Code/geolens/.planning/phases/225-api-reference/225-PATTERNS.md §`auth.mdx` (CREATE) (lines 402-449)
  </read_first>
  <action>
Create `/Users/ishiland/Code/getgeolens.com/docs/src/content/docs/guides/api/auth.mdx` with this exact content:

````mdx
---
title: API Authentication
description: JWT, API key (header and query forms), and OAuth/OIDC for the GeoLens REST API.
---

The GeoLens REST API supports three authentication methods. Pick the one that
matches your client:

- **JWT Bearer tokens** — short-lived; good for end-user apps and the web UI.
- **API keys** — long-lived; good for scripts, dashboards, and machine clients.
- **OAuth/OIDC** — admin-configured; good for SSO via Google, Microsoft, or generic OIDC providers.

Resolution order on every request: `X-Api-Key` header → `?api_key=` query
parameter → `Authorization: Bearer <jwt>` header → anonymous. The first match
wins.

Replace `https://geolens.example.com` with your GeoLens instance's URL in every
example below.

## JWT Bearer Tokens

For interactive end-user clients, exchange a username and password for a
short-lived access token plus a long-lived refresh token, then send the access
token as `Authorization: Bearer <jwt>` on each subsequent request.

**Step 1: log in.**

```bash
curl -X POST https://geolens.example.com/api/auth/login/ \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=alice&password=hunter2"
```

Response shape:

```json
{
  "access_token": "eyJhbGc...",
  "refresh_token": "...",
  "token_type": "bearer"
}
```

**Step 2: use the access token.**

```bash
curl https://geolens.example.com/api/collections/datasets/items \
  -H "Authorization: Bearer eyJhbGc..."
```

**Refresh tokens.** Access tokens are short-lived. When yours expires, exchange
the refresh token for a new pair via `POST /api/auth/refresh/`. Refresh tokens
rotate on every call — store the new one and discard the old.

**Where to store tokens.** In the browser, store the access token in memory
(not `localStorage`) and the refresh token in an httpOnly cookie. For
server-side scripts, treat both like any other secret: environment variables or
a secrets manager, never committed to source control.

## API Keys

API keys are long-lived credentials suitable for scripts, scheduled jobs, and
machine clients. They can be passed two ways. The header form is preferred;
the query-string form exists for tools that can't easily set headers (notably
`ogr2ogr`'s `OAPIF:` driver).

**Header form (preferred):**

```bash
curl https://geolens.example.com/api/collections/datasets/items \
  -H "X-Api-Key: glk_live_..."
```

**Query-string form:**

```bash
curl 'https://geolens.example.com/api/collections/datasets/items?api_key=glk_live_...'
```

The header takes precedence over the query string. Never combine the two on
the same request.

**Obtaining a key.** API keys are issued from the admin user-management page —
see [User management & RBAC](/guides/admin/users). Each key is scoped to a
single user account and inherits that user's permissions.

**Security.** API keys do not expire. Rotate them on a schedule and revoke any
key whose host environment changes. Treat a leaked API key the same as a
leaked password.

## OAuth / OIDC

GeoLens supports OAuth/OIDC sign-in via Google, Microsoft, and any generic
OIDC provider — configured by an administrator. End users do not call the
OAuth endpoints directly; they sign in through the GeoLens web UI, which
performs the standard authorization-code flow on their behalf.

The browser flow:

1. User clicks "Sign in with `<provider>`" in the GeoLens UI.
2. UI redirects to `https://geolens.example.com/api/auth/oauth/<provider>/login`.
3. Provider authenticates the user and calls back to
   `https://geolens.example.com/api/auth/oauth/<provider>/callback`.
4. GeoLens issues a JWT and the UI stores it.

Once the UI has a JWT, machine clients reuse it like any other Bearer token:

```bash
curl https://geolens.example.com/api/collections/datasets/items \
  -H "Authorization: Bearer eyJhbGc..."
```

**Provider setup.** OIDC client registration, group-to-role mapping, and the
list of supported providers are all admin-configured. See
[OAuth/OIDC setup](/guides/admin/oauth).

**Out of scope.** GeoLens does not currently support OIDC client-credentials
or PKCE machine flows. Machine-to-machine clients should use API keys.
````

Notes for the implementer:
- Use ` ```bash ` (not ` ```sh `) for every curl block — Phase 224 convention (S-3).
- Use ` ```json ` for the JSON response body block.
- DO NOT add a body `# Heading` matching the frontmatter `title` — Starlight emits the H1 from frontmatter.
- DO NOT introduce `<Aside>`, `<Card>`, or any custom Astro component imports — keep auth.mdx markdown-only (the index.mdx in Plan 06 may use Card/CardGrid).
- Forward links `/guides/admin/users` and `/guides/admin/oauth` will be flagged by `starlight-links-validator` (Plan 08) UNLESS that plan correctly registers `exclude: ['/guides/admin/**', ...]`. Plan 08's frontmatter `depends_on` will list this plan.

Implements API-03, D-10, D-11 (JWT), D-12 corrected (X-Api-Key header form + query form + resolution order note), D-13 (OAuth admin-configured + UI-flow + machine-client redirect), D-14 (no flow diagrams), D-15 (placeholder host).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && test -f src/content/docs/guides/api/auth.mdx</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF 'X-Api-Key' src/content/docs/guides/api/auth.mdx</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && (! grep -qF 'Authorization: Bearer <api_key>' src/content/docs/guides/api/auth.mdx) && echo "OK: no wrong header"</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && (! grep -qE '"Bearer +<?api[_-]?key' src/content/docs/guides/api/auth.mdx) && echo "OK: no Bearer api_key variants"</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF 'https://geolens.example.com' src/content/docs/guides/api/auth.mdx && (! grep -qF 'demo.getgeolens.com' src/content/docs/guides/api/auth.mdx)</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -cE '^## ' src/content/docs/guides/api/auth.mdx | grep -qE '^[3-9]$|^[1-9][0-9]+$' && echo "OK: at least 3 H2 sections"</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -cE '^```bash$' src/content/docs/guides/api/auth.mdx | grep -qE '^[5-9]$|^[1-9][0-9]+$' && echo "OK: at least 5 bash blocks"</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && npx astro check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/ishiland/Code/getgeolens.com/docs/src/content/docs/guides/api/auth.mdx` exists
    - Frontmatter contains `title: API Authentication` and `description:` (one sentence)
    - File contains literal string `X-Api-Key` (correct header per backend reality)
    - File does NOT contain the literal string `Authorization: Bearer <api_key>` (the wrong D-12 wording)
    - File does NOT contain any case-insensitive variant of `Bearer <api_key>` or `Bearer api_key` (the wrong header form)
    - File contains exactly three top-level sections in this order: `## JWT Bearer Tokens`, `## API Keys`, `## OAuth / OIDC` (per D-10)
    - File contains literal string `https://geolens.example.com/api/` (placeholder host per D-15)
    - File does NOT contain `demo.getgeolens.com` anywhere (D-19 link-rot prevention)
    - File does NOT contain `localhost:8000` anywhere (placeholder host policy)
    - File contains at least 5 ` ```bash ` fenced code blocks (each section has at least one curl)
    - File contains literal string `?api_key=` (query-string form per D-12)
    - File contains forward-link `/guides/admin/users` (D-12 admin link-out)
    - File contains forward-link `/guides/admin/oauth` (D-13 admin link-out)
    - File contains literal string `Resolution order` or `resolution order` (D-12 priority note)
    - File contains literal string `client-credentials` or `PKCE` in an out-of-scope statement (D-13)
    - `npx astro check` passes with 0 errors
    - File contains NO custom Astro `<Component>` JSX (markdown-only)
    - File body does NOT begin with a `# ` H1 (Starlight derives H1 from frontmatter — duplicate would render twice)
  </acceptance_criteria>
  <done>auth.mdx is committed with corrected `X-Api-Key` header throughout, three sections in the locked order, ≥5 curl examples, placeholder host only, forward refs to admin pages, no flow diagrams, and `astro check` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader → docs example → operator's GeoLens instance | Readers copy curl examples and run them against their own instances. Wrong header name leads to confused 401s and possibly weakened security posture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-225-01 | Information Disclosure | API-key examples leak real credentials | mitigate | Placeholder host `https://geolens.example.com/...` (D-15); placeholder credentials `glk_live_...`, `eyJhbGc...`; no real secrets. Acceptance criteria forbids `demo.getgeolens.com`, `localhost`, and `getgeolens.com` (real production) hosts. |
| T-225-AUTH | Spoofing / Information Disclosure | Wrong API-key header (`Authorization: Bearer <api_key>`) leads readers to attempt JWT-shaped auth that fails — driving them to weaker fallbacks like query-string keys in URLs (logged everywhere) | mitigate | Plan corrects D-12 inline. Acceptance criteria has explicit positive (`X-Api-Key` present) AND negative (`Authorization: Bearer <api_key>` absent) grep gates. Source of truth cited at `backend/app/modules/auth/dependencies.py:25` in plan body. (V2 ASVS) |
| T-225-08 | Information Disclosure | Forward-link to `/guides/admin/users` (Phase 227) confuses readers in v15.0 launch window | accept | Plan 08's links-validator `exclude` allowlist tolerates `/guides/admin/**` until Phase 227 lands. Reader sees a working link that 404s — preferred over inventing fake content. |
</threat_model>

<verification>
- `grep -qF 'X-Api-Key' src/content/docs/guides/api/auth.mdx`
- `(! grep -qF 'Authorization: Bearer <api_key>' src/content/docs/guides/api/auth.mdx)` (negative match)
- `grep -qF 'https://geolens.example.com' src/content/docs/guides/api/auth.mdx`
- `(! grep -qF 'demo.getgeolens.com' src/content/docs/guides/api/auth.mdx)` (negative match)
- `npx astro check` → 0 errors
- After Plan 03 lands: `npm run build` builds the page; `dist/guides/api/auth/index.html` contains `X-Api-Key`
</verification>

<success_criteria>
- `auth.mdx` documents three auth methods in the locked order
- Every API-key reference uses `X-Api-Key` header; NO instance of `Authorization: Bearer <api_key>` (wrong D-12 form) anywhere
- All curl examples use `https://geolens.example.com/api/...`
- `npx astro check` passes
- File compiles in `npm run build` once Plan 03 has wired the plugin
</success_criteria>

<output>
After completion, create `.planning/phases/225-api-reference/225-04-SUMMARY.md` documenting:
- Section count and order (must match D-10 exactly)
- Curl example count
- Confirmation `X-Api-Key` is present and `Authorization: Bearer <api_key>` is absent (paste both grep outputs)
- Placeholder host policy adherence
- Note for execute-phase: forward-links `/guides/admin/{users,oauth}` are intentional; Plan 08 must register them in the validator `exclude` list
</output>

## Truths

- D-10: `/guides/api/auth.mdx` covers exactly three auth methods, in this order: JWT Bearer, API key (header AND `?api_key=` query param), OAuth/OIDC. Each section has at least one working `curl` example.
- D-11: JWT section — POST `/api/auth/login` flow narrative, curl with `-H "Authorization: Bearer <jwt>"`, refresh-token rotation note, security note (token TTL + storage).
- D-12: API key section — both forms documented. **Header form CORRECTED** to `X-Api-Key: <api_key>` (NOT `Authorization: Bearer <api_key>`) per `backend/app/modules/auth/dependencies.py:25` source-of-truth. Query form: `?api_key=<key>`. Resolution order (header > query > JWT > anonymous) noted. Forward-link `/guides/admin/users` for key issuance.
- D-13: OAuth section — explains admin-configured OIDC providers, end-user authorization-code flow via web UI, curl with token-from-UI. Forward-references `/guides/admin/oauth`. NO documenting OIDC client-credentials/PKCE machine-flow (out of scope per CONTEXT).
- D-14: NO flow diagrams. Examples-driven prose only.
- D-15: All curl examples use placeholder host `https://geolens.example.com/api/...` with explicit "replace with your instance" note.
