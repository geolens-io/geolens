---
phase: 225-api-reference
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - getgeolens.com/docs/src/content/docs/guides/api/ogc.mdx
autonomous: true
requirements: [API-04]
must_haves:
  truths:
    - "A reader at `/guides/api/ogc` sees five sections in this order: OGC API Common, OGC API Records, OGC API Features, STAC 1.1, Tiles"
    - "Each section explains what the standard provides, lists the relevant geolens endpoint paths, includes at least one curl example, and includes at least one client-tool example (QGIS, ogr2ogr, or pystac-client)"
    - "Tile section explicitly distinguishes HMAC-signed access tokens (embedded maps, single-dataset-scoped) from generic API keys (D-18, Open Q #2)"
    - "All examples use `https://geolens.example.com/api/...` placeholder host (D-19) — no `demo.getgeolens.com`"
  artifacts:
    - path: "getgeolens.com/docs/src/content/docs/guides/api/ogc.mdx"
      provides: "Hand-authored OGC endpoints landing page"
      min_lines: 120
      contains: "## OGC API"
  key_links:
    - from: "ogc.mdx Records section"
      to: "GET /api/collections/datasets/items"
      via: "curl + ogr2ogr OAPIF examples"
      pattern: "/collections/datasets/items"
    - from: "ogc.mdx STAC section"
      to: "pystac-client.Client.open"
      via: "Python snippet"
      pattern: "pystac_client"
---

<objective>
Author `/guides/api/ogc.mdx` — a single-page landing covering five standards-based endpoint groups in the GeoLens REST API. Sections in this order (D-16): OGC API Common → OGC API Records → OGC API Features → STAC 1.1 → Tiles. Every section follows the same shape (what / paths / curl / client-tool) per D-17.

**Critical clarification on tile tokens (planning_context #4 / Open Q #2):** The Tiles section MUST describe the HMAC-signed token URL shape but MUST NOT paste a fake token in a copy-pasteable curl example. Tokens are session-bound, scoped to a single dataset, and obtained via `POST /api/tiles/tokens/`. Make this explicit in the page so readers don't treat tile tokens as generic API keys.

Purpose: Provides the integrator-facing "how do I use the standards endpoints" landing page. Auto-generated tag pages (Plan 03) cover the per-route schema details; this page is the curated guidance layer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/225-api-reference/225-CONTEXT.md
@.planning/phases/225-api-reference/225-RESEARCH.md
@.planning/phases/225-api-reference/225-PATTERNS.md
@/Users/ishiland/Code/geolens/backend/app/standards/ogc/router.py
@/Users/ishiland/Code/geolens/backend/app/standards/stac/router.py
@/Users/ishiland/Code/geolens/backend/app/processing/tiles/router.py

<interfaces>
<!-- Verified endpoint paths (researcher cited each line) -->

OGC API Common (`backend/app/standards/ogc/router.py`):
- `GET /api/` → landing (line 81)
- `GET /api/conformance` → 16 conformance class URIs (line 128)
- `GET /api/openapi.json` → spec (line 165 / FastAPI default)

OGC API Records (`backend/app/modules/catalog/search/router.py:1272`):
- `GET /api/collections/datasets/items` → list catalog records
- CQL2 query: `?filter=<expr>&filter-lang=cql2-text` (D-20: ONE example only)

OGC API Features (`backend/app/standards/ogc/router.py:244`):
- `GET /api/collections/{dataset_id}/items` → per-dataset feature items

STAC 1.1 (`backend/app/standards/stac/router.py`):
- prefix `/stac` (line 48)
- `GET /api/stac/` → STAC root catalog (line 241)
- `GET /api/stac/collections` → list (line 293)
- `POST /api/stac/search` → search (line 1072)

Tiles (`backend/app/processing/tiles/router.py`):
- `GET /api/tiles/{table_path}/{z}/{x}/{y}.pbf` → MVT vector tile (line 563)
- `POST /api/tiles/tokens/` → issue session token (line 510)
- `GET /api/tiles/token/{dataset_id}/` → fetch existing token (line 468)
- Raster: Titiler URL pattern (descriptive — verify shape against snapshot)

GDAL OAPIF driver (cited at https://gdal.org/en/latest/drivers/vector/oapif.html):
- `ogrinfo OAPIF:https://geolens.example.com/api/`
- `ogr2ogr -f GPKG out.gpkg "OAPIF:https://geolens.example.com/api/?api_key=..." {dataset_id}`

QGIS MetaSearch (cited at https://docs.qgis.org/3.44/.../plugins_metasearch.html):
- Web ▸ MetaSearch ▸ Services tab ▸ New ▸ Catalog Type: OGC API - Records

pystac-client (cited at https://pystac-client.readthedocs.io/en/stable/usage.html):
- `Client.open("https://geolens.example.com/api/stac/")`

Code-block convention (S-3): ` ```bash `, ` ```python `, ` ``` ` (no lang) for QGIS step lists. NEVER ` ```sh ` for content blocks.

Frontmatter (S-5): `title:` + `description:`; do NOT add body H1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create ogc.mdx with five standards-based sections</name>
  <files>getgeolens.com/docs/src/content/docs/guides/api/ogc.mdx</files>
  <read_first>
    - /Users/ishiland/Code/geolens/backend/app/standards/ogc/router.py (lines 81, 128, 244 — endpoint paths)
    - /Users/ishiland/Code/geolens/backend/app/standards/stac/router.py (lines 48, 241, 293, 1072 — STAC paths)
    - /Users/ishiland/Code/geolens/backend/app/processing/tiles/router.py (lines 468, 510, 563 — tile + token paths)
    - /Users/ishiland/Code/geolens/.planning/phases/225-api-reference/225-RESEARCH.md §Code Examples 4 (lines 611-697 — verbatim verified examples)
    - /Users/ishiland/Code/geolens/.planning/phases/225-api-reference/225-PATTERNS.md §`ogc.mdx` (CREATE) (lines 452-484)
  </read_first>
  <action>
Create `/Users/ishiland/Code/getgeolens.com/docs/src/content/docs/guides/api/ogc.mdx` with this exact content:

````mdx
---
title: OGC API & Standards Endpoints
description: OGC API Common, Records, Features, STAC, and tile endpoints with QGIS and ogr2ogr examples.
---

GeoLens implements several OGC and STAC standards alongside its REST API. This
page covers the standards-based endpoints. Reach for the
[auto-generated reference](/guides/api/) for per-route schemas, request/response
bodies, and the full conformance-class list.

Replace `https://geolens.example.com` with your GeoLens instance's URL in every
example below. Authentication follows the same rules as the rest of the API —
see [API Authentication](/guides/api/auth) for `X-Api-Key`, JWT, and OAuth
options.

## OGC API — Common

Provides the OGC API root, conformance declaration, and OpenAPI definition.
Every OGC API client begins here.

**Endpoints:**

- `GET /api/` — landing document with links to conformance, collections, and OpenAPI.
- `GET /api/conformance` — list of conformance class URIs (Records, Features, CQL2, etc.).
- `GET /api/openapi.json` — full OpenAPI definition.

**Curl:**

```bash
curl https://geolens.example.com/api/
curl https://geolens.example.com/api/conformance
```

For the full schema of every endpoint, see the
[Endpoints by Tag](/guides/api/) section in the auto-generated reference.

## OGC API — Records

The catalog itself is exposed as an OGC API Records collection at
`collections/datasets`. Use this to list, filter, and discover datasets
programmatically.

**Endpoints:**

- `GET /api/collections/datasets/items` — list catalog records (paginated, CQL2-filterable).

**Curl:**

```bash
curl https://geolens.example.com/api/collections/datasets/items
```

**CQL2 filter (one example):**

```bash
curl 'https://geolens.example.com/api/collections/datasets/items?filter=keywords%3D%27hydrology%27&filter-lang=cql2-text'
```

The full CQL2 grammar is advertised in `/api/conformance` (look for the
`cql2-text` and `features-filter` conformance classes).

**QGIS — MetaSearch (built-in plugin):**

```
1. Web ▸ MetaSearch ▸ MetaSearch (built-in, no install)
2. Services tab ▸ New
3. Name: GeoLens
   URL:  https://geolens.example.com/api/
   Catalog Type: OGC API - Records
4. Save → Search tab to query records.
```

**GDAL — ogr2ogr / ogrinfo (OAPIF driver):**

```bash
ogrinfo OAPIF:https://geolens.example.com/api/
```

## OGC API — Features

Per-dataset feature access for vector layers. Useful for exporting subsets to
GeoPackage, Shapefile, or any GDAL-supported format.

**Endpoints:**

- `GET /api/collections/{dataset_id}/items` — feature items for a single dataset (BBox, datetime, and CQL2 filterable).

**Curl:**

```bash
curl https://geolens.example.com/api/collections/{dataset_id}/items
```

**QGIS — Add Layer ▸ OGC API Features:**

```
1. Layer ▸ Add Layer ▸ Add WFS / OGC API Features Layer…
2. New connection: Name: GeoLens
   URL: https://geolens.example.com/api/?api_key=glk_live_...
3. Connect → pick collection → Add.
```

**GDAL — export to GeoPackage:**

```bash
ogr2ogr -f GPKG out.gpkg \
  "OAPIF:https://geolens.example.com/api/?api_key=glk_live_..." \
  {dataset_id}
```

The `?api_key=` query parameter is the recommended way to authenticate
ogr2ogr — the OAPIF driver does not currently let you set HTTP headers.

## STAC 1.1

GeoLens exposes raster collections (and their items) under a STAC 1.1 catalog
rooted at `/api/stac/`. Use any STAC client to search and ingest assets.

**Endpoints:**

- `GET /api/stac/` — STAC root catalog.
- `GET /api/stac/collections` — list of STAC collections.
- `POST /api/stac/search` — full STAC search (bbox, datetime, collections, intersects, query).

**Curl:**

```bash
curl https://geolens.example.com/api/stac/
```

**pystac-client:**

```python
from pystac_client import Client

client = Client.open("https://geolens.example.com/api/stac/")
search = client.search(
    collections=["my-raster-collection"],
    bbox=[-122.5, 37.5, -122.0, 38.0],
    datetime="2024-01-01/2024-12-31",
)
for item in search.items():
    print(item.id, item.assets["data"].href)
```

## Tile Endpoints

GeoLens serves vector and raster tiles via signed access tokens. Tile tokens
are **not** generic API keys — they are HMAC-signed, scoped to a single
dataset, and time-limited. They exist so that embedded maps can serve tiles
without exposing a permanent credential to the browser.

**Vector tile (MVT) URL shape:**

```
https://geolens.example.com/api/tiles/{dataset_id}/{z}/{x}/{y}.pbf?token={hmac_signed_token}
```

**Raster tile (Titiler-based) URL shape:**

Titiler-rendered raster tiles follow Titiler's standard `/tiles/...` URL
pattern under `/api/`. Confirm the exact path in your instance's
auto-generated reference under the Tiles tag.

**Obtaining a tile token.** Tile tokens are issued by an authenticated request
against:

- `POST /api/tiles/tokens/` — mint a new token for a dataset.
- `GET /api/tiles/token/{dataset_id}/` — fetch an existing token.

These endpoints accept the same authentication as the rest of the API
(`X-Api-Key`, JWT, OAuth-issued JWT). Tile tokens themselves are **not**
substitutes for those credentials — they are derived, scoped, and expire.

**QGIS — Add XYZ Tiles:**

```
1. Browser panel ▸ XYZ Tiles ▸ Right-click → New Connection…
2. Name: GeoLens — <dataset>
   URL:  https://geolens.example.com/api/tiles/{dataset_id}/{z}/{x}/{y}.pbf?token=<token>
3. OK → drag the connection onto the canvas.
```

Replace `<token>` with a value obtained from `POST /api/tiles/tokens/`.
Tokens issued in QGIS workflows should be treated as session credentials —
mint a fresh one when the previous one expires.
````

Notes for the implementer:
- ` ```bash ` for shell, ` ```python ` for pystac-client, ` ``` ` (no lang) for QGIS step lists — Phase 224 convention via Expressive Code.
- DO NOT add a body `# Heading` matching `title` — Starlight emits H1 from frontmatter.
- DO NOT introduce custom Astro components.
- The cross-link `/guides/api/` (the curated landing — Plan 06) and `/guides/api/auth` (Plan 04) are sibling pages within this phase — they will resolve clean once all Wave 2 plans land. They are NOT forward-references requiring `exclude`.
- The reference link to `Endpoints by Tag` resolves to whatever URL the auto-generated sidebar group lands at — for now, it points to `/guides/api/` (the curated landing). The reader can navigate from there.

Implements API-04, D-16 (single landing page, five sections in order), D-17 (uniform shape per section), D-18 (verified endpoint paths), D-19 (placeholder host, no demo.getgeolens.com), D-20 (one CQL2 example).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && test -f src/content/docs/guides/api/ogc.mdx</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -cE '^## ' src/content/docs/guides/api/ogc.mdx | grep -qE '^[5-9]$|^[1-9][0-9]+$' && echo "OK: ≥5 H2 sections"</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && for h in 'OGC API — Common' 'OGC API — Records' 'OGC API — Features' 'STAC 1.1' 'Tile Endpoints'; do grep -qF "## $h" src/content/docs/guides/api/ogc.mdx || { echo "MISSING: $h"; exit 1; }; done; echo "OK: all 5 sections present"</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF 'https://geolens.example.com' src/content/docs/guides/api/ogc.mdx && (! grep -qF 'demo.getgeolens.com' src/content/docs/guides/api/ogc.mdx)</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF 'OAPIF:' src/content/docs/guides/api/ogc.mdx && grep -qF 'pystac_client' src/content/docs/guides/api/ogc.mdx</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF 'cql2-text' src/content/docs/guides/api/ogc.mdx</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF 'HMAC' src/content/docs/guides/api/ogc.mdx && grep -qF 'not generic API keys' src/content/docs/guides/api/ogc.mdx || grep -qF 'not** generic API keys' src/content/docs/guides/api/ogc.mdx || grep -qF '**not** generic' src/content/docs/guides/api/ogc.mdx</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && (! grep -qE '\?token=[a-zA-Z0-9_-]{16,}' src/content/docs/guides/api/ogc.mdx) && echo "OK: no fake hard-coded tokens"</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && npx astro check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/ishiland/Code/getgeolens.com/docs/src/content/docs/guides/api/ogc.mdx` exists
    - Frontmatter has `title: OGC API & Standards Endpoints` and `description:`
    - Exactly five `## ` sections in this order (matched literally): `## OGC API — Common`, `## OGC API — Records`, `## OGC API — Features`, `## STAC 1.1`, `## Tile Endpoints`
    - File contains `https://geolens.example.com/api/` (placeholder host per D-19)
    - File does NOT contain `demo.getgeolens.com` anywhere
    - File does NOT contain `localhost:8000`
    - Records section contains `OAPIF:` and a CQL2 filter example using `?filter=` + `filter-lang=cql2-text` (D-20: only ONE such CQL2 example)
    - STAC section contains literal string `pystac_client` and shows `Client.open("https://geolens.example.com/api/stac/")`
    - Features section contains an `ogr2ogr -f GPKG` example
    - Tiles section contains literal string `HMAC` (or `HMAC-signed`) AND explicit "**not** generic API keys" or equivalent disambiguation phrasing
    - Tiles section describes the URL shape `{dataset_id}/{z}/{x}/{y}.pbf?token=...` but does NOT contain a hard-coded fake token long enough to look real (no `?token=` followed by ≥16 alphanumeric chars)
    - File contains forward-link `/guides/api/auth` (sibling cross-link to Plan 04)
    - File contains link `/guides/api/` (curated landing — Plan 06)
    - `npx astro check` passes with 0 errors
    - File body does NOT begin with `# ` H1 (Starlight derives H1 from frontmatter)
  </acceptance_criteria>
  <done>ogc.mdx documents all five standards groups in the locked order with verified endpoint paths, placeholder hosts, one CQL2 example, QGIS+GDAL+pystac client examples, and explicit tile-token disambiguation. astro check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader → docs example → operator's GeoLens instance | Same trust shape as Plan 04. Tile tokens are an extra concern — readers might mistake them for general API keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-225-04 | Information Disclosure | Tile-token examples leak token URL shapes that look "callable" but aren't | mitigate | Tiles section explicitly states tokens are HMAC-signed, session-bound, scoped to a single dataset, and NOT generic API keys. URL shape is described, not pasted with a real-looking token (acceptance criteria forbids tokens ≥16 alphanumeric chars). (V14 / V2 ASVS) |
| T-225-09 | Information Disclosure | OGC examples leak real ogr2ogr `?api_key=` invocations to logs | accept | Placeholder `glk_live_...` only. Reader is responsible for their own key hygiene. README and auth.mdx already cover rotation guidance. |
</threat_model>

<verification>
- All five sections present in correct order
- `https://geolens.example.com` everywhere; no `demo.getgeolens.com`, no `localhost:8000`
- `OAPIF:`, `pystac_client`, `cql2-text` strings all present
- HMAC disambiguation language present in Tiles section
- `npx astro check` clean
- After Plans 03 lands: `npm run build` builds the page; `dist/guides/api/ogc/index.html` exists
</verification>

<success_criteria>
- Five standards sections in locked order
- All endpoint paths match the backend source-of-truth files (researcher-verified line numbers)
- Tile token language unambiguous about scope and lifecycle
- `astro check` passes
</success_criteria>

<output>
After completion, create `.planning/phases/225-api-reference/225-05-SUMMARY.md` documenting:
- Section count and order (must match D-16 exactly)
- Curl example count
- Client tools demonstrated (QGIS, ogr2ogr, pystac-client)
- Confirmation tile-token disambiguation language is present
- Verified endpoint paths against backend source-of-truth files
</output>

## Truths

- D-16: Single `/guides/api/ogc.mdx` landing page with five sections: OGC API — Common, OGC API — Records, OGC API — Features, STAC 1.1, Tile endpoints. Matches REQUIREMENTS API-04 literal.
- D-17: Each section follows pattern: (1) what the standard provides, (2) relevant geolens endpoint paths (verified against backend source files in RESEARCH.md), (3) one curl example, (4) at least one client-tool example (QGIS metadata catalog setup, GDAL `ogr2ogr`, or `pystac-client`).
- D-20: CQL2 in Records section gets ONE example only (e.g., `?filter=keywords='hydrology'`). NO full CQL2 spec recreation; deeper CQL2 deferred to a future phase.
