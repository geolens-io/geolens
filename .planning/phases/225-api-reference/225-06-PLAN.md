---
phase: 225-api-reference
plan: 06
type: execute
wave: 4
depends_on: [02, 03]
files_modified:
  - getgeolens.com/docs/src/content/docs/guides/api/index.mdx
autonomous: true
requirements: [API-02]
must_haves:
  truths:
    - "Visiting `/guides/api/` shows a curated landing (NOT the auto-generated schema overview from starlight-openapi)"
    - "Page surfaces three top-level navigation cards/links: Authentication, OGC Endpoints, Endpoints by Tag"
    - "Page renders a 'Spec snapshot' callout showing `info.version` derived from the committed `geolens.json`"
    - "The Phase 224 placeholder ('Content for this section ships in Phase 225') is replaced — no longer present"
  artifacts:
    - path: "getgeolens.com/docs/src/content/docs/guides/api/index.mdx"
      provides: "Curated API Reference landing page"
      min_lines: 30
      contains: "Spec snapshot"
  key_links:
    - from: "index.mdx Spec snapshot callout"
      to: "src/content/openapi/geolens.json info.version"
      via: "JSON import"
      pattern: "import .* from .*openapi/geolens.json"
    - from: "index.mdx cards"
      to: "/guides/api/auth, /guides/api/ogc"
      via: "MDX links"
      pattern: "/guides/api/(auth|ogc)"
---

<objective>
Replace the Phase 224 placeholder at `getgeolens.com/docs/src/content/docs/guides/api/index.mdx` with a curated landing page (D-09): one-paragraph orientation, three navigation entries (Authentication / OGC Endpoints / Endpoints by Tag), and a "Spec snapshot" callout pulling `info.version` from the committed JSON snapshot.

**Astro content-collection precedence (RESEARCH.md §Summary finding #1):** Astro content-collection pages WIN over `injectRoute` from plugins. Even though starlight-openapi 0.25.0 emits a default schema-overview at `/guides/api/`, our hand-authored `index.mdx` takes the URL. This plan IS the mechanism that makes that override visible to readers.

Purpose: The curated landing is the front door for integrators. It needs to feel finished — not a placeholder, not the raw plugin output.

Output: A single MDX page with frontmatter, an Aside-rendered Spec snapshot callout (Starlight built-in), and a CardGrid with three Cards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/225-api-reference/225-CONTEXT.md
@.planning/phases/225-api-reference/225-RESEARCH.md
@.planning/phases/225-api-reference/225-PATTERNS.md
@/Users/ishiland/Code/getgeolens.com/docs/src/content/docs/index.mdx
@/Users/ishiland/Code/getgeolens.com/docs/src/content/docs/guides/api/index.mdx

<interfaces>
<!-- Starlight built-in components — available without imports beyond the standard module path -->

```typescript
// Starlight built-ins (provided by @astrojs/starlight)
// docs: https://starlight.astro.build/components/
import { Aside, Card, CardGrid } from '@astrojs/starlight/components';
```

`<Aside type="note|tip|caution|danger" title="...">` — boxed callout (we use `tip` for the spec-snapshot callout)
`<CardGrid>` — responsive grid layout for cards
`<Card title="..." icon="...">` — clickable card; child markdown + links allowed

JSON import in MDX (Astro 6.x supports this natively via Vite):
```mdx
---
import spec from '../../../openapi/geolens.json';
---
```

Path math: `src/content/docs/guides/api/index.mdx` → up to `src/content/` → into `openapi/`. Three `..` levels from the MDX file. Verify by reading the actual relative path from the file location.

Existing placeholder content (5 lines — to be REPLACED entirely):
```mdx
---
title: API Reference (coming soon)
---

Content for this section ships in Phase 225.
```

Convention from `docs/src/content/docs/index.mdx` lines 1-5:
```mdx
---
title: GeoLens Documentation
description: ...
template: doc
---
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace placeholder index.mdx with curated landing page (Spec snapshot + 3-card grid)</name>
  <files>getgeolens.com/docs/src/content/docs/guides/api/index.mdx</files>
  <read_first>
    - /Users/ishiland/Code/getgeolens.com/docs/src/content/docs/guides/api/index.mdx (current 5-line placeholder — must be fully replaced)
    - /Users/ishiland/Code/getgeolens.com/docs/src/content/docs/index.mdx (frontmatter + body convention reference)
    - /Users/ishiland/Code/getgeolens.com/docs/src/content/openapi/geolens.json (confirm Plan 02 committed it; verify path math)
    - /Users/ishiland/Code/geolens/.planning/phases/225-api-reference/225-PATTERNS.md §`index.mdx` (modify) (lines 348-399)
    - /Users/ishiland/Code/geolens/.planning/phases/225-api-reference/225-RESEARCH.md §Summary finding #1 (content collection precedes injectRoute)
  </read_first>
  <action>
Open `/Users/ishiland/Code/getgeolens.com/docs/src/content/docs/guides/api/index.mdx`. Delete the existing 5-line placeholder. Replace with this exact content:

````mdx
---
title: API Reference
description: REST and OGC API endpoints for the GeoLens self-hosted GIS catalog — auto-generated from the committed OpenAPI snapshot.
---

import { Aside, Card, CardGrid } from '@astrojs/starlight/components';
import spec from '../../../openapi/geolens.json';

The GeoLens REST API exposes the full catalog, search, map, and standards
endpoints. This reference is auto-generated from a committed
[OpenAPI snapshot](/guides/api/) of a running GeoLens instance — every
endpoint, parameter, and response schema is derived directly from the FastAPI
application.

<Aside type="tip" title="Spec snapshot">
  This reference is built from `geolens.json` v{spec.info.version} ({Object.keys(spec.paths).length} endpoints across {spec.tags?.length ?? 0} tags). Snapshot refresh procedure is documented in [`src/content/openapi/README.md`](https://github.com/geolens-io/getgeolens.com/blob/main/docs/src/content/openapi/README.md).
</Aside>

## Where to start

<CardGrid>
  <Card title="Authentication" icon="approve-check">
    JWT bearer tokens, API keys (header and query forms), and OAuth/OIDC.
    Start here if your client is making its first request.

    [Authentication →](/guides/api/auth)
  </Card>

  <Card title="OGC & Standards Endpoints" icon="open-book">
    OGC API Common, Records, Features, STAC 1.1, and tile endpoints with
    QGIS, GDAL, and pystac-client examples.

    [OGC Endpoints →](/guides/api/ogc)
  </Card>

  <Card title="Endpoints by Tag" icon="list-format">
    Browse the full reference grouped by FastAPI tag — Datasets, Search,
    Auth, Tiles, and more. Each operation includes parameters, request body,
    and response schemas.

    See the **Endpoints by Tag** section in the sidebar.
  </Card>
</CardGrid>
````

Notes for the implementer:
- Use the EXACT relative import path `../../../openapi/geolens.json`. From `src/content/docs/guides/api/index.mdx`: up to `src/content/docs/guides/`, up to `src/content/docs/`, up to `src/content/`, into `openapi/`. Three `..` segments.
- The `{spec.info.version}` and `{Object.keys(spec.paths).length}` JSX expressions are inline in the Aside body (Astro/MDX supports this).
- The third card "Endpoints by Tag" intentionally has NO direct link target — the auto-generated tag groups (from Plan 03's `openAPISidebarGroups` spread) only have permanent slugs once the plugin runs. Pointing readers at the sidebar is the safe move.
- `icon="approve-check"`, `icon="open-book"`, `icon="list-format"` — all valid Starlight built-in icon names per the official component reference.
- DO NOT add a body `# Heading` matching `title` (Starlight renders H1 from frontmatter).
- DO NOT use ` ``` ` fenced code in this file — Card content is markdown prose only; the spec callout uses inline JSX expressions, not code blocks.
- The link target `[OpenAPI snapshot](/guides/api/)` in the intro paragraph is the curated landing itself — change to `/guides/api/auth` if the executor finds that confusing during render. Acceptable variation (Claude's Discretion per CONTEXT.md).

Implements API-02 success criterion #1 (curated landing exists), D-09 (Spec snapshot callout reading `info.version` from JSON), D-08 (sidebar order — readers reach Endpoints by Tag via sidebar after Authentication and OGC).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && (! grep -qF 'Content for this section ships in Phase 225' src/content/docs/guides/api/index.mdx) && echo "OK: placeholder removed"</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF 'Spec snapshot' src/content/docs/guides/api/index.mdx</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF "import spec from '../../../openapi/geolens.json'" src/content/docs/guides/api/index.mdx</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF '{spec.info.version}' src/content/docs/guides/api/index.mdx</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF '<CardGrid>' src/content/docs/guides/api/index.mdx && grep -cE '<Card ' src/content/docs/guides/api/index.mdx | grep -qE '^[3-9]$' && echo "OK: 3 cards"</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF '/guides/api/auth' src/content/docs/guides/api/index.mdx && grep -qF '/guides/api/ogc' src/content/docs/guides/api/index.mdx</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && npx astro check 2>&1 | tail -3</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && npm run build 2>&1 | tail -10</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && test -f dist/guides/api/index.html && (! grep -qF 'Content for this section ships in Phase 225' dist/guides/api/index.html)</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/ishiland/Code/getgeolens.com/docs/src/content/docs/guides/api/index.mdx` exists
    - Frontmatter `title:` equals `API Reference` (NOT `API Reference (coming soon)`)
    - Frontmatter `description:` is present (1 sentence)
    - File does NOT contain literal string `Content for this section ships in Phase 225` (placeholder removed)
    - File does NOT contain literal string `coming soon` (case-insensitive)
    - File contains literal string `import spec from '../../../openapi/geolens.json'`
    - File contains literal `<Aside type="tip" title="Spec snapshot">`
    - File contains literal `{spec.info.version}` (inline JSX expression for snapshot version)
    - File contains literal `<CardGrid>` and at least 3 `<Card ` opening tags
    - File contains markdown link to `/guides/api/auth`
    - File contains markdown link to `/guides/api/ogc`
    - `npx astro check` passes with 0 errors
    - `npm run build` succeeds
    - `dist/guides/api/index.html` exists
    - `dist/guides/api/index.html` contains the snapshot version number (verifies the JSON import resolved at build time)
    - `dist/guides/api/index.html` does NOT contain placeholder text `Content for this section ships in Phase 225`
    - File body does NOT begin with `# ` H1
  </acceptance_criteria>
  <done>The Phase 224 placeholder is gone; curated landing renders with Aside callout (live snapshot version), CardGrid with 3 cards linking to Authentication / OGC Endpoints / sidebar tag tree; build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed snapshot → MDX import → rendered HTML | The snapshot's `info.version` and path/tag counts are surfaced verbatim in the page. If the snapshot is malformed, the build fails — not a runtime trust concern. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-225-02 | Information Disclosure | Stale snapshot version misleads integrators | mitigate | Spec snapshot callout makes `info.version` visible to readers (D-09). Plan 07's README documents the refresh procedure. Reader can compare against their own instance. |
| T-225-10 | Tampering | Malformed JSON crashes the build | mitigate | Plan 01's fetch script validates `openapi`, `info.version`, `paths` before writing. Plan 10's verify-build.sh re-validates structure on every build. |
</threat_model>

<verification>
- Placeholder text fully removed from source AND dist
- `Spec snapshot` callout present
- JSON import path correct: `../../../openapi/geolens.json`
- 3 cards with links to auth + ogc + sidebar reference
- `npx astro check` clean
- `npm run build` succeeds and `dist/guides/api/index.html` contains the live snapshot version
</verification>

<success_criteria>
- ROADMAP success criterion #1 satisfied: "Navigating to `/guides/api/` shows a structured API reference generated from the committed `openapi.json` snapshot"
- The page is curated (D-09 Spec snapshot callout) — not the bare auto-generated schema overview
- Plans 04 and 05 are reachable via the cards
</success_criteria>

<output>
After completion, create `.planning/phases/225-api-reference/225-06-SUMMARY.md` documenting:
- Snapshot version surfaced in the rendered Aside (paste the value from `dist/guides/api/index.html`)
- Card count and link targets
- Confirmation that the Astro content-collection page won the URL race against starlight-openapi's injectRoute (verifies RESEARCH.md §Summary finding #1)
- Confirmation no `coming soon` placeholder text remains anywhere in source or dist
</output>

## Truths

- D-09: `/guides/api/index.mdx` is a curated landing page with 1-paragraph orientation, three top-level cards/links (Authentication → `/guides/api/auth`, OGC Endpoints → `/guides/api/ogc`, Endpoints by Tag → `/guides/api/operations/tags/`), and a "Spec snapshot" Aside callout showing `info.version` from the committed JSON. Replaces the Phase 224 placeholder.
