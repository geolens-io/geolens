---
phase: 225-api-reference
plan: 08
type: execute
wave: 5
depends_on: [03, 04, 05, 06]
files_modified:
  - getgeolens.com/docs/package.json
  - getgeolens.com/docs/astro.config.mjs
autonomous: true
requirements: [CI-01]
must_haves:
  truths:
    - "`starlight-links-validator@0.23.0` is installed exactly (no caret/tilde) and registered as a Starlight plugin alongside starlight-openapi"
    - "The validator's `exclude` allowlist tolerates forward references to `/guides/admin/**`, `/guides/user/**`, and `/guides/quickstart/**` (Phases 226 and 227)"
    - "`npm run build` succeeds with the current MDX content (no broken internal links)"
    - "Introducing a deliberately broken internal link causes `npm run build` to fail with exit code != 0 (negative-test verifies the gate is live)"
  artifacts:
    - path: "getgeolens.com/docs/package.json"
      provides: "starlight-links-validator pinned to 0.23.0"
      contains: "starlight-links-validator"
    - path: "getgeolens.com/docs/astro.config.mjs"
      provides: "Validator plugin registration with exclude allowlist"
      contains: "starlightLinksValidator"
  key_links:
    - from: "astro.config.mjs starlight({plugins: [...]})"
      to: "starlight-links-validator"
      via: "plugin registration"
      pattern: "starlightLinksValidator\\("
    - from: "astro.config.mjs validator config"
      to: "Phase 226/227 forward references"
      via: "exclude glob array"
      pattern: "/guides/admin/\\*\\*"
---

<objective>
Install `starlight-links-validator@0.23.0`, register it as a Starlight plugin in `astro.config.mjs` (NOT a separate CI step per D-28), and configure its `exclude` allowlist to tolerate forward references to `/guides/admin/**`, `/guides/user/**`, and `/guides/quickstart/**` (Phases 226 and 227's destinations). Verify the gate is live by running an intentional negative test: introduce a broken link, confirm build fails, remove it.

Purpose: Forward references in `auth.mdx` (Plan 04) and (potentially) `ogc.mdx` (Plan 05) point at admin pages that won't exist until Phase 227. Without the `exclude` allowlist, every `npm run build` would fail. With the allowlist, the build passes; in Phase 227, those exclude entries are removed in the same PR that adds the destinations and the validator now CHECKS those links.

**Per planning_context correction #5 (D-29 path 1):** Use the `exclude` glob option — NO stub MDX pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/225-api-reference/225-CONTEXT.md
@.planning/phases/225-api-reference/225-RESEARCH.md
@.planning/phases/225-api-reference/225-PATTERNS.md
@/Users/ishiland/Code/getgeolens.com/docs/astro.config.mjs
@/Users/ishiland/Code/getgeolens.com/docs/package.json

<interfaces>
<!-- starlight-links-validator 0.23.0 API surface -->

```typescript
// default export
function starlightLinksValidator(options?: ValidatorOptions): StarlightPlugin

interface ValidatorOptions {
  exclude?: string[]                     // picomatch globs; e.g. '/guides/admin/**'
  errorOnFallbackPages?: boolean
  errorOnInconsistentLocale?: boolean
  errorOnRelativeLinks?: boolean
  errorOnInvalidHashes?: boolean
  errorOnLocalLinks?: boolean            // KEEP DEFAULT FALSE — don't validate http(s):// link reachability
  failOnError?: boolean
  // ...
}
```

Source: https://starlight-links-validator.vercel.app/configuration/ — `exclude: string[] | (infos) => boolean` (cited in RESEARCH.md §Sources).

Existing astro.config.mjs structure (after Plan 03 applies):
- `starlight({plugins: [starlightOpenAPI([...])]})` — Plan 03 added the `plugins:` array.
- Plan 08 EXTENDS that array — it does NOT create a new array.

Per RESEARCH.md §Pitfall 6: leave `errorOnLocalLinks: false` (default). Don't validate external URLs (the editLink.baseUrl GitHub URL is external; we don't want false positives).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install starlight-links-validator@0.23.0</name>
  <files>getgeolens.com/docs/package.json</files>
  <read_first>
    - /Users/ishiland/Code/getgeolens.com/docs/package.json (current state — Plan 03 added starlight-openapi; Plan 08 adds the validator alongside)
    - /Users/ishiland/Code/geolens/.planning/phases/225-api-reference/225-RESEARCH.md §Standard Stack (line 126 — version pinning rationale)
  </read_first>
  <action>
Run `npm install --save-exact starlight-links-validator@0.23.0` from `/Users/ishiland/Code/getgeolens.com/docs/`. The `--save-exact` flag pins to `0.23.0` (NOT `^0.23.0`).

Verify the resulting `package.json` `dependencies` block contains both:
```json
"starlight-links-validator": "0.23.0",
"starlight-openapi": "0.25.0",
```

If npm normalizes to `^0.23.0`, manually drop the caret in `package.json` so the committed version is exactly `"0.23.0"`.

Implements CI-01 (validator installation).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && node -e "const p=require('./package.json'); const v=p.dependencies['starlight-links-validator']; if (v !== '0.23.0') { console.error('expected 0.23.0, got', v); process.exit(1); } console.log('OK')"</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && test -d node_modules/starlight-links-validator && grep -qF '"version": "0.23.0"' node_modules/starlight-links-validator/package.json</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && node -e "const p=require('./package.json'); if (p.dependencies['starlight-openapi'] !== '0.25.0') { console.error('starlight-openapi version regressed:', p.dependencies['starlight-openapi']); process.exit(1); } console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies['starlight-links-validator']` equals exactly `"0.23.0"` (no caret/tilde)
    - `package.json` `dependencies['starlight-openapi']` still equals exactly `"0.25.0"` (no version drift)
    - `node_modules/starlight-links-validator/` exists with `"version": "0.23.0"`
    - `package-lock.json` updated
    - NO other deps added in this commit
  </acceptance_criteria>
  <done>starlight-links-validator 0.23.0 is installed exactly alongside starlight-openapi 0.25.0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Register starlight-links-validator in astro.config.mjs with exclude allowlist</name>
  <files>getgeolens.com/docs/astro.config.mjs</files>
  <read_first>
    - /Users/ishiland/Code/getgeolens.com/docs/astro.config.mjs (current state — Plan 03 already added `starlightOpenAPI` import and the `plugins:` array)
    - /Users/ishiland/Code/geolens/.planning/phases/225-api-reference/225-RESEARCH.md §Pattern 1 (lines 280-327 — combined plugins block reference)
    - /Users/ishiland/Code/geolens/.planning/phases/225-api-reference/225-RESEARCH.md §Pitfall 6 (lines 513-517 — leave errorOnLocalLinks: false)
  </read_first>
  <action>
Edit `/Users/ishiland/Code/getgeolens.com/docs/astro.config.mjs`. Make exactly these two changes — preserve every other line.

**Change 1 — Add ESM import** (alongside the Plan 03 import line):

After the line `import starlightOpenAPI, { openAPISidebarGroups } from 'starlight-openapi';`, insert:

```javascript
import starlightLinksValidator from 'starlight-links-validator';
```

**Change 2 — Append validator to the existing `plugins:` array** (the array Plan 03 created inside `starlight({...})`):

Locate the existing `plugins: [...]` block. It currently contains a single `starlightOpenAPI([...])` entry. After the closing `])` of that entry but BEFORE the closing `]` of the `plugins:` array, add:

```javascript
        // CI-01 / D-27..D-29: starlight-links-validator fails the build on broken
        // internal links. exclude: tolerates forward references to Phase 226/227
        // routes that don't exist yet — those exclude entries get removed in the
        // PRs that ship those phases.
        starlightLinksValidator({
          exclude: [
            '/guides/admin/**',     // Phase 227
            '/guides/user/**',      // Phase 227
            '/guides/quickstart/**', // Phase 226
          ],
        }),
```

The final `plugins:` array shape MUST be (with both plugins):
```javascript
      plugins: [
        starlightOpenAPI([
          {
            base: 'guides/api',
            schema: './src/content/openapi/geolens.json',
            sidebar: {
              collapsed: false,
              label: 'Endpoints by Tag',
              operations: { badges: true, sort: 'alphabetical' },
              tags: { sort: 'alphabetical' },
            },
          },
        ]),
        starlightLinksValidator({
          exclude: [
            '/guides/admin/**',
            '/guides/user/**',
            '/guides/quickstart/**',
          ],
        }),
      ],
```

**DO NOT** add any other `starlightLinksValidator` options (no `errorOnLocalLinks`, no `errorOnInvalidHashes`, no `failOnError`). Defaults apply (errorOnLocalLinks=false per Pitfall 6 — keeps GitHub editLink URLs valid).

**DO NOT** add the `routeMiddleware:` config in this plan — that's Plan 09's responsibility.

Implements CI-01, D-27 (plugin install), D-28 (Starlight plugin, NOT separate CI step), D-29 path 1 (exclude allowlist for forward refs).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF "import starlightLinksValidator from 'starlight-links-validator';" astro.config.mjs</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF "starlightLinksValidator({" astro.config.mjs</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF "'/guides/admin/**'" astro.config.mjs && grep -qF "'/guides/user/**'" astro.config.mjs && grep -qF "'/guides/quickstart/**'" astro.config.mjs</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF "starlightOpenAPI([" astro.config.mjs && grep -qF "...openAPISidebarGroups" astro.config.mjs</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && npx astro check 2>&1 | tail -3</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && npm run build 2>&1 | tail -10 && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `astro.config.mjs` contains literal `import starlightLinksValidator from 'starlight-links-validator';`
    - `astro.config.mjs` contains literal `starlightLinksValidator({` (registration call)
    - `astro.config.mjs` `exclude` array contains all three globs: `'/guides/admin/**'`, `'/guides/user/**'`, `'/guides/quickstart/**'`
    - `astro.config.mjs` does NOT contain `errorOnLocalLinks: true` (default false stays)
    - Plan 03's wiring intact: `starlightOpenAPI([` and `...openAPISidebarGroups` both still present
    - `npx astro check` passes with 0 errors
    - `npm run build` exits 0 (current MDX content has no broken internal links — auth.mdx forward-refs are excluded by the allowlist)
    - Plan 09's `routeMiddleware:` is NOT yet present (this plan stays surgical)
  </acceptance_criteria>
  <done>starlight-links-validator is wired alongside starlight-openapi; exclude allowlist tolerates Phase 226/227 forward refs; current build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Negative test — verify the validator gate is live</name>
  <files>getgeolens.com/docs/src/content/docs/guides/api/auth.mdx</files>
  <read_first>
    - /Users/ishiland/Code/getgeolens.com/docs/src/content/docs/guides/api/auth.mdx (auth.mdx written by Plan 04)
  </read_first>
  <action>
This task verifies that the validator actually FAILS the build when an internal link is broken. Procedure:

1. Make a temporary in-place edit to `auth.mdx`: append a single test line at the end of the file:

   ```mdx

   <!-- TEMP-LINKS-VALIDATOR-PROBE: -->[broken internal](/guides/this-route-does-not-exist)
   ```

2. Run `npm run build` from the docs directory:

   ```sh
   cd /Users/ishiland/Code/getgeolens.com/docs && npm run build
   ```

   **EXPECTED:** the build FAILS with exit code != 0 and the validator emits an error mentioning `/guides/this-route-does-not-exist`.

3. If the build SUCCEEDS, the gate is not live — fail the task and investigate. Likely causes:
   - `exclude` glob accidentally too permissive (e.g. `'/guides/**'` would mask all internal links)
   - Plugin registered at the wrong tier (top-level integrations vs starlight plugins)
   - Wrong package version

4. Remove the temporary test line — restore `auth.mdx` to its Plan 04 state.

5. Run `npm run build` again — must SUCCEED with exit 0 (gate is live but the corpus is clean).

This task touches `auth.mdx` only as a transient probe; the final `git diff src/content/docs/guides/api/auth.mdx` MUST show zero bytes changed against the Plan 04 commit.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && (! grep -qF 'TEMP-LINKS-VALIDATOR-PROBE' src/content/docs/guides/api/auth.mdx) && echo "OK: probe removed"</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && npm run build 2>&1 | tail -5 && echo "BUILD CLEAN"</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com && git diff --stat docs/src/content/docs/guides/api/auth.mdx 2>&1 | grep -v 'changed' | head -1 || echo "OK: auth.mdx clean against last commit"</automated>
  </verify>
  <acceptance_criteria>
    - During the probe step, `npm run build` failed with exit code != 0 and validator output mentioned the broken link path (executor reports the actual error message in their commit/SUMMARY)
    - After cleanup, `auth.mdx` does NOT contain literal string `TEMP-LINKS-VALIDATOR-PROBE`
    - After cleanup, `auth.mdx` does NOT contain literal string `/guides/this-route-does-not-exist`
    - After cleanup, `npm run build` succeeds with exit 0
    - The final committed version of `auth.mdx` is byte-identical to Plan 04's commit (`git diff` is empty)
  </acceptance_criteria>
  <done>Validator gate proven live (build fails on broken internal link, succeeds when corpus is clean); auth.mdx restored to its Plan 04 state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline → exclude allowlist → forward-references | The exclude allowlist intentionally bypasses validation for Phase 226/227 destinations. Maintainers must remove those exclude entries in the matching phase PRs or the gate degrades silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-225-12 | Repudiation | Forward-reference allowlist becomes permanent / forgotten | mitigate | Each exclude glob is commented inline with the phase that should land it (`// Phase 227`, `// Phase 226`). Phase 228's launch verification gate explicitly confirms the exclude list is empty before robots-flip (per RESEARCH.md §Threat Patterns). |
| T-225-06 | Tampering | Plugin supply chain (starlight-links-validator) | mitigate | Pinned to exact `0.23.0` (no caret/tilde). Re-pin requires changelog read. |
| T-225-13 | Spoofing | Validator silently disabled by misconfigured `exclude: ['/**']` | mitigate | Negative test in Task 3 PROVES the gate is live by introducing a real broken link and confirming build failure. Exclude globs are scoped to specific Phase 226/227 paths only — never to `/guides/api/**` or `/**`. |
</threat_model>

<verification>
- `package.json` shows both `starlight-links-validator: "0.23.0"` and `starlight-openapi: "0.25.0"`
- `astro.config.mjs` has both plugins registered inside `starlight({plugins: [...]})` (NOT in top-level `integrations:`)
- `exclude` allowlist has exactly 3 globs scoped to Phase 226/227 routes
- Negative test (Task 3) confirmed: build fails on a broken link, succeeds on a clean corpus
- Final `auth.mdx` is unchanged from Plan 04's commit
</verification>

<success_criteria>
- CI-01 satisfied: validator runs on every `npm run build` (CI inherits the gate via the existing build step)
- Forward references in auth.mdx don't fail the build
- A future broken link WILL fail the build (proven by negative test)
</success_criteria>

<output>
After completion, create `.planning/phases/225-api-reference/225-08-SUMMARY.md` documenting:
- Validator version pinned (0.23.0)
- exclude globs registered (3 entries with phase labels)
- Negative-test outcome — paste the actual error message from the broken-link probe
- Note for Phase 226 / 227 maintainers: remove the matching exclude glob in your PR
</output>

## Truths

- D-27: `starlight-links-validator@0.23.x` installed as a Starlight plugin. Build-time validation; failure mode = build exits non-zero on broken internal links. (Selected over `astro-broken-link-checker` per RESEARCH.md `peerDependencies` confirmation against Starlight 0.38.4 + Astro 6.x.)
- D-28: Wired as a Starlight plugin inside `astro.config.mjs` (NOT a separate CI step). Local `npm run build` catches breakage before CI; CI inherits the same gate via the existing `npm run build` step.
- D-29: Forward-reference tolerance via the validator's `exclude: string[]` option (path 1 — preferred). `exclude: ['/guides/admin/**', '/guides/user/**', '/guides/quickstart/**']` allows Phase 226/227 forward-references to resolve at build time. NO stub MDX pages (path 2) and NO external-URL hack (path 3).
