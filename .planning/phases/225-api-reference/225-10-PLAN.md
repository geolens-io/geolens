---
phase: 225-api-reference
plan: 10
type: execute
wave: 7
depends_on: [02, 03, 04, 05, 06, 09]
files_modified:
  - getgeolens.com/docs/scripts/verify-build.sh
  - getgeolens.com/docs/public/llms.txt
autonomous: true
requirements: [API-01, API-02, API-03, API-04, API-05, CI-01]
must_haves:
  truths:
    - "`bash scripts/verify-build.sh` asserts the snapshot file is present and structurally valid (API-01)"
    - "`bash scripts/verify-build.sh` asserts at least one tag overview directory exists in `dist/guides/api/operations/tags/` (API-02)"
    - "`bash scripts/verify-build.sh` asserts auto-generated pages LACK `data-pagefind-body` (D-21 / D-24)"
    - "`bash scripts/verify-build.sh` asserts hand-authored API pages HAVE `data-pagefind-body` (D-21 / D-24)"
    - "`bash scripts/verify-build.sh` asserts `dist/llms.txt` contains both new URLs (`/guides/api/auth` and `/guides/api/ogc`)"
    - "`docs/public/llms.txt` extends Phase 224 baseline with two new lines under `## Guides`"
  artifacts:
    - path: "getgeolens.com/docs/scripts/verify-build.sh"
      provides: "Phase 225 build-artifact assertions appended after Phase 224 fence"
      contains: "Phase 225 assertions"
    - path: "getgeolens.com/docs/public/llms.txt"
      provides: "Two additional lines for /guides/api/auth and /guides/api/ogc"
      contains: "/guides/api/auth"
  key_links:
    - from: "verify-build.sh Phase 225 fence"
      to: "dist/guides/api/operations/tags/*/index.html"
      via: "grep data-pagefind-body assertion"
      pattern: "data-pagefind-body"
    - from: "verify-build.sh Phase 225 fence"
      to: "dist/llms.txt"
      via: "grep https://docs.getgeolens.com/guides/api/{auth,ogc}"
      pattern: "https://docs.getgeolens.com/guides/api"
---

<objective>
Close the verification loop for Phase 225 by appending five new assertions to `docs/scripts/verify-build.sh` (per RESEARCH.md §Pattern 4) and extending `docs/public/llms.txt` with two new entries (per Phase 224 D-31). The verify-build.sh additions cover API-01 (snapshot presence + structural validity), API-02 (tag overview pages emitted), D-21/D-24 (Pagefind exclusion is correct on both ends), and SEO-04 extension (llms.txt URLs present).

**Per planning_context correction #6:** Pagefind index lives at `dist/pagefind/` (no underscore). Index files are gzipped binary — DO NOT grep them. Verify exclusion via `data-pagefind-body` presence/absence on rendered HTML in `dist/guides/api/...`.

Purpose: Wave 3 verification gate. Once this plan ships, every `npm run build && bash scripts/verify-build.sh` run gates the entire Phase 225 surface. CI inherits the gate via the existing `npm run verify` step (Phase 224's `docs-ci.yml`).

Output:
- `getgeolens.com/docs/scripts/verify-build.sh` — extended with a `# ─── Phase 225 assertions (API-01..05, CI-01) ───` fence and 4 assertion blocks, kept BEFORE the existing final `echo "All build-artifact assertions passed."` line
- `getgeolens.com/docs/public/llms.txt` — extended with two new bullet lines under `## Guides`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/225-api-reference/225-CONTEXT.md
@.planning/phases/225-api-reference/225-RESEARCH.md
@.planning/phases/225-api-reference/225-PATTERNS.md
@/Users/ishiland/Code/getgeolens.com/docs/scripts/verify-build.sh
@/Users/ishiland/Code/getgeolens.com/docs/public/llms.txt

<interfaces>
<!-- Existing verify-build.sh structure (143 lines) — Phase 224 fence at line 38, final echo at line 143 -->

Existing idiom (multiple instances):
```bash
echo "Asserting <NAME>: <human description>..."
test -f <path> || { echo "FAIL: <human msg>"; exit 1; }
grep -qF '<literal>' <path> || { echo "FAIL: <human msg>"; exit 1; }
```

Phase fence convention (line 38):
```bash
# ─── Phase 224 assertions (BRAND-01/02, SHELL-01..05, SEARCH-01/02, SEO-04) ───
```

Final echo (line 143):
```bash
echo "All build-artifact assertions passed."
```

NEW assertions append BEFORE the final echo, AFTER all existing Phase 224 assertions (which end at line ~141).

Existing llms.txt (11 lines):
```
# GeoLens Documentation

> Documentation for the GeoLens self-hosted GIS data catalog. ...

## Guides

- [Quickstart](https://docs.getgeolens.com/guides/quickstart): ...
- [User Guide](https://docs.getgeolens.com/guides/user): ...
- [Admin Guide](https://docs.getgeolens.com/guides/admin): ...
- [API Reference](https://docs.getgeolens.com/guides/api): ...
```

New entries to add (Phase 224 D-31 carryover): under `## Guides`, after the existing `[API Reference]` line.

Pagefind index location (RESEARCH.md §Pitfall 3 — verified via `ls /Users/ishiland/Code/getgeolens.com/docs/dist/pagefind/`):
- `dist/pagefind/` (NO leading underscore)
- Index files are gzipped binary — DO NOT grep them
- Use `data-pagefind-body` on rendered HTML as the upstream signal
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend docs/public/llms.txt with two new lines under ## Guides</name>
  <files>getgeolens.com/docs/public/llms.txt</files>
  <read_first>
    - /Users/ishiland/Code/getgeolens.com/docs/public/llms.txt (full file — preserve every existing line)
    - /Users/ishiland/Code/geolens/.planning/phases/225-api-reference/225-PATTERNS.md §`docs/public/llms.txt` (modify) (lines 237-271)
    - /Users/ishiland/Code/geolens/.planning/phases/224-brand-shell-search/224-CONTEXT.md §D-31 (the Phase 224 baseline + Phase 225 extension contract)
  </read_first>
  <action>
Edit `/Users/ishiland/Code/getgeolens.com/docs/public/llms.txt`. Append exactly two new lines AFTER the existing `[API Reference]` line, preserving every other line.

The final file must read exactly:

```
# GeoLens Documentation

> Documentation for the GeoLens self-hosted GIS data catalog. Covers install, user guide, admin guide, and OGC/REST API reference. Single canonical home for all GeoLens user-facing documentation; supersedes legacy backend/docs/{install,admin}.md files.

## Guides

- [Quickstart](https://docs.getgeolens.com/guides/quickstart): Stand up GeoLens with Docker Compose and complete first-login flow.
- [User Guide](https://docs.getgeolens.com/guides/user): Search, dataset detail, map builder, collections, imports, and exports.
- [Admin Guide](https://docs.getgeolens.com/guides/admin): User management, OAuth/OIDC, settings, backups, monitoring, and cloud deployment.
- [API Reference](https://docs.getgeolens.com/guides/api): OGC API (Common, Records, Features, STAC), REST endpoints with auth examples.
- [API Authentication](https://docs.getgeolens.com/guides/api/auth): JWT, API key (header and query forms), and OAuth/OIDC examples for the GeoLens REST API.
- [API OGC Endpoints](https://docs.getgeolens.com/guides/api/ogc): OGC API Common, Records, Features, STAC, and tile endpoints with QGIS and ogr2ogr examples.
```

Convention to honor (PATTERNS.md S-existing-convention):
- `- [Title](URL): Sentence-case description ending with a period.`
- Title Case for the bracketed label.
- Absolute `https://docs.getgeolens.com/...` URL — no trailing slash on the path component (note: `guides/api/auth` not `guides/api/auth/`).
- One sentence per entry.

DO NOT change any of the four existing entries. DO NOT add a sub-section heading.

Implements SEO-04 extension noted in Phase 224 D-31.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF 'https://docs.getgeolens.com/guides/api/auth' public/llms.txt</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF 'https://docs.getgeolens.com/guides/api/ogc' public/llms.txt</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF '[API Authentication]' public/llms.txt && grep -qF '[API OGC Endpoints]' public/llms.txt</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && for keep in '[Quickstart]' '[User Guide]' '[Admin Guide]' '[API Reference]'; do grep -qF "$keep" public/llms.txt || { echo "REGRESSION: missing $keep"; exit 1; }; done; echo "OK: existing entries preserved"</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -cE '^- \[' public/llms.txt | grep -qE '^[6-9]$|^1[0-9]$' && echo "OK: ≥6 bullet entries"</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal string `https://docs.getgeolens.com/guides/api/auth` (NO trailing slash)
    - File contains literal string `https://docs.getgeolens.com/guides/api/ogc` (NO trailing slash)
    - File contains literal `[API Authentication]` and `[API OGC Endpoints]` (Title Case bracket labels)
    - All four existing labels still present: `[Quickstart]`, `[User Guide]`, `[Admin Guide]`, `[API Reference]`
    - File has exactly 6 bullet lines under `## Guides` (4 original + 2 new)
    - Two new lines come AFTER the existing `[API Reference]` line (verify by ordering)
    - File ends with a single trailing newline
  </acceptance_criteria>
  <done>llms.txt extended with two new entries; existing four preserved; URL shapes follow Phase 224 D-31 convention.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append Phase 225 assertion fence to docs/scripts/verify-build.sh</name>
  <files>getgeolens.com/docs/scripts/verify-build.sh</files>
  <read_first>
    - /Users/ishiland/Code/getgeolens.com/docs/scripts/verify-build.sh (current state — 143 lines, Phase 224 fence at line 38, final echo at line 143)
    - /Users/ishiland/Code/geolens/.planning/phases/225-api-reference/225-RESEARCH.md §Pattern 4 (lines 417-457 — full assertion bodies)
    - /Users/ishiland/Code/geolens/.planning/phases/225-api-reference/225-PATTERNS.md §`verify-build.sh` (modify) (lines 125-180)
    - /Users/ishiland/Code/geolens/.planning/phases/225-api-reference/225-RESEARCH.md §Pitfall 3 (lines 495-499 — `dist/pagefind/` location, no underscore; do NOT grep gzipped binary)
  </read_first>
  <action>
Open `/Users/ishiland/Code/getgeolens.com/docs/scripts/verify-build.sh`. Locate the final `echo "All build-artifact assertions passed."` line (currently last line, ~line 143). Insert the following assertion fence + four assertion blocks immediately BEFORE that line. Preserve every existing line above and the final echo at the bottom.

```bash

# ─── Phase 225 assertions (API-01..05, CI-01) ───

echo "Asserting API-01: openapi snapshot present and non-empty in src/content/openapi/..."
test -f src/content/openapi/geolens.json \
  || { echo "FAIL: API-01 src/content/openapi/geolens.json missing — run 'npm run fetch-openapi' against a running geolens API"; exit 1; }
test -s src/content/openapi/geolens.json \
  || { echo "FAIL: API-01 src/content/openapi/geolens.json is empty"; exit 1; }
# Smoke check: snapshot is valid JSON with required OpenAPI 3.x fields.
node -e '
  const s = require("./src/content/openapi/geolens.json");
  if (typeof s.openapi !== "string") { console.error("snapshot missing openapi field"); process.exit(1); }
  if (!s.info || typeof s.info.version !== "string") { console.error("snapshot missing info.version"); process.exit(1); }
  if (!s.paths || Object.keys(s.paths).length === 0) { console.error("snapshot has zero paths"); process.exit(1); }
' || { echo "FAIL: API-01 src/content/openapi/geolens.json failed structural validation"; exit 1; }

echo "Asserting API-02: starlight-openapi rendered tag overview pages exist in dist/guides/api/operations/tags/..."
ls dist/guides/api/operations/tags/ 2>/dev/null | grep -q . \
  || { echo "FAIL: API-02 no tag overview pages emitted to dist/guides/api/operations/tags/ — starlight-openapi may not be wired"; exit 1; }

echo "Asserting D-21 / D-24: auto-generated reference pages are EXCLUDED from Pagefind (no data-pagefind-body)..."
# Starlight emits data-pagefind-body on <main> iff entry.data.pagefind !== false.
# pagefind-exclude.ts sets pagefind=false for /guides/api/operations/** — attribute MUST be absent.
shopt -s nullglob
for f in dist/guides/api/operations/tags/*/index.html; do
  if grep -q 'data-pagefind-body' "$f" 2>/dev/null; then
    echo "FAIL: D-21 auto-generated page $f has data-pagefind-body (should be excluded by pagefind-exclude.ts)"
    exit 1
  fi
done
shopt -u nullglob

echo "Asserting D-21 / D-24: hand-authored /guides/api/ pages REMAIN indexed (data-pagefind-body present)..."
for f in dist/guides/api/index.html dist/guides/api/auth/index.html dist/guides/api/ogc/index.html; do
  test -f "$f" || { echo "FAIL: hand-authored page missing: $f"; exit 1; }
  grep -q 'data-pagefind-body' "$f" \
    || { echo "FAIL: D-21 hand-authored page $f is missing data-pagefind-body — over-broad exclusion"; exit 1; }
done

echo "Asserting SEO-04 extension: llms.txt now includes /guides/api/auth and /guides/api/ogc..."
for path in /guides/api/auth /guides/api/ogc; do
  grep -qF "https://docs.getgeolens.com${path}" dist/llms.txt \
    || { echo "FAIL: llms.txt missing URL https://docs.getgeolens.com${path}"; exit 1; }
done

echo "Asserting API-03: auth.mdx uses X-Api-Key (NOT Authorization: Bearer <api_key>)..."
grep -qF 'X-Api-Key' dist/guides/api/auth/index.html \
  || { echo "FAIL: API-03 auth page missing X-Api-Key header reference"; exit 1; }
if grep -qF 'Authorization: Bearer &lt;api_key&gt;' dist/guides/api/auth/index.html \
   || grep -qF 'Authorization: Bearer <api_key>' dist/guides/api/auth/index.html; then
  echo "FAIL: API-03 auth page contains the wrong 'Authorization: Bearer <api_key>' form (D-12 correction not applied)"
  exit 1
fi

echo "Asserting API-04: ogc.mdx contains OAPIF, pystac-client, and CQL2 examples..."
grep -qF 'OAPIF:' dist/guides/api/ogc/index.html \
  || { echo "FAIL: API-04 ogc page missing OAPIF: example"; exit 1; }
grep -qE 'pystac[_-]client' dist/guides/api/ogc/index.html \
  || { echo "FAIL: API-04 ogc page missing pystac-client example"; exit 1; }
grep -qF 'cql2-text' dist/guides/api/ogc/index.html \
  || { echo "FAIL: API-04 ogc page missing CQL2 example"; exit 1; }

echo "Asserting API-05: src/content/openapi/README.md exists and references fetch-openapi..."
test -f src/content/openapi/README.md \
  || { echo "FAIL: API-05 src/content/openapi/README.md missing"; exit 1; }
grep -qF 'fetch-openapi' src/content/openapi/README.md \
  || { echo "FAIL: API-05 README missing fetch-openapi reference"; exit 1; }
grep -qF 'OASDIFF-01' src/content/openapi/README.md \
  || { echo "FAIL: API-05 README missing OASDIFF-01 deferral pointer"; exit 1; }

echo "Asserting CI-01: starlight-links-validator pinned in package.json..."
node -e '
  const p = require("./package.json");
  if (p.dependencies["starlight-links-validator"] !== "0.23.0") {
    console.error("FAIL: starlight-links-validator must be pinned to exactly 0.23.0");
    process.exit(1);
  }
  if (p.dependencies["starlight-openapi"] !== "0.25.0") {
    console.error("FAIL: starlight-openapi must be pinned to exactly 0.25.0");
    process.exit(1);
  }
' || exit 1

```

Notes for the implementer:
- Place the new fence + assertions BEFORE the final `echo "All build-artifact assertions passed."` line (which stays last).
- Use `shopt -s nullglob` / `shopt -u nullglob` around the for-loop so an empty `dist/guides/api/operations/tags/` (which would mean Plan 03 broke) does NOT silently pass — the prior assertion (`ls ... | grep -q .`) catches the empty-dir case explicitly.
- The negative-grep for `Authorization: Bearer <api_key>` checks BOTH the HTML-encoded form (`&lt;api_key&gt;`) AND the raw form, since Astro emits encoded angle brackets in rendered HTML.
- DO NOT grep `dist/pagefind/` — files are gzipped binary (RESEARCH.md §Pitfall 3). The `data-pagefind-body` presence/absence checks ARE the verification.
- DO NOT remove or modify ANY existing assertion in the file (the Phase 223 + Phase 224 fences are load-bearing).

Implements API-01 (snapshot present + valid), API-02 (tag pages emitted), API-03 (X-Api-Key correct + wrong form absent), API-04 (OGC examples present), API-05 (README references), CI-01 (validator pinned), D-21/D-24 (Pagefind exclusion shape), SEO-04 ext (llms.txt URLs).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF '# ─── Phase 225 assertions' scripts/verify-build.sh</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF 'API-01 src/content/openapi/geolens.json missing' scripts/verify-build.sh</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF 'API-02 no tag overview pages emitted' scripts/verify-build.sh</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF 'D-21 auto-generated page' scripts/verify-build.sh && grep -qF 'D-21 hand-authored page' scripts/verify-build.sh</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF 'X-Api-Key' scripts/verify-build.sh && grep -qF 'Authorization: Bearer' scripts/verify-build.sh</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF 'OAPIF:' scripts/verify-build.sh && grep -qF 'pystac' scripts/verify-build.sh</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -qF 'OASDIFF-01' scripts/verify-build.sh</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && tail -1 scripts/verify-build.sh | grep -qF 'All build-artifact assertions passed'</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && bash -n scripts/verify-build.sh && echo "BASH SYNTAX OK"</automated>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && rm -rf dist && npm run build 2>&1 | tail -5 && bash scripts/verify-build.sh 2>&1 | tail -10 && echo "VERIFY OK"</automated>
  </verify>
  <acceptance_criteria>
    - `verify-build.sh` contains literal `# ─── Phase 225 assertions (API-01..05, CI-01) ───`
    - Phase 225 fence appears AFTER the existing `# ─── Phase 224 assertions ...` fence
    - File contains all six new assertion echoes (`Asserting API-01:`, `Asserting API-02:`, two `Asserting D-21 / D-24:`, `Asserting SEO-04 extension:`, `Asserting API-03:`, `Asserting API-04:`, `Asserting API-05:`, `Asserting CI-01:`)
    - File contains negative match for the wrong API-key header form: grep for `Authorization: Bearer &lt;api_key&gt;` AND `Authorization: Bearer <api_key>`
    - File does NOT grep `dist/pagefind/` directory (those files are gzipped binary)
    - File does NOT grep any path with leading `_pagefind` (RESEARCH.md §Pitfall 3 — that's the wrong dir name)
    - The literal LAST line of the file is `echo "All build-artifact assertions passed."`
    - All Phase 223 and Phase 224 assertions preserved (verify by counting `Asserting ` echoes — must increase, not decrease)
    - `bash -n scripts/verify-build.sh` returns exit 0 (syntax check)
    - Running the script on a clean build produces no FAIL output and exits 0
    - Header `set -euo pipefail` and `cd "$(dirname "$0")/.."` preserved
  </acceptance_criteria>
  <done>verify-build.sh has the Phase 225 fence with 8 assertion blocks placed before the final echo; bash syntax clean; running it after a fresh build passes; existing Phase 223/224 assertions untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline → assertion script → CI gate | Assertions are the only automated gate that catches snapshot-rot, plugin misconfiguration, and Pagefind exclusion drift on every build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-225-02 | Information Disclosure | Snapshot drift unnoticed at build time | mitigate | API-01 assertion validates structural shape on every build. README (Plan 07) + landing-page Aside (Plan 06) make snapshot version visible to readers. OASDIFF-01 (deferred) closes the loop in a future phase. |
| T-225-AUTH | Spoofing | auth.mdx silently regresses to `Authorization: Bearer <api_key>` | mitigate | API-03 assertion has BOTH a positive grep (`X-Api-Key` present) AND a negative grep (wrong form absent in BOTH HTML-encoded and raw forms). Build fails on regression. |
| T-225-13 | Information Disclosure | Pagefind exclusion misconfigured (over- or under-broad) | mitigate | D-21/D-24 assertions check both ends: auto-pages MUST lack `data-pagefind-body`, hand-authored pages MUST have it. Either-direction breakage fails the build. |
| T-225-12 | Repudiation | Validator exclude allowlist forgotten / left after Phase 226/227 | accept | Phase 228 launch verification gate (out of scope for Phase 225) confirms the exclude list is empty before robots-flip. Plan 08's commit comments name each glob with its phase. |
</threat_model>

<verification>
- All six Phase 225 keywords present in verify-build.sh (`API-01`, `API-02`, `API-03`, `API-04`, `API-05`, `CI-01`, plus `D-21`)
- `bash -n scripts/verify-build.sh` clean
- After `rm -rf dist && npm run build && bash scripts/verify-build.sh`: script exits 0
- llms.txt has 6 bullet entries under `## Guides`
- The negative grep for `Authorization: Bearer <api_key>` exists in the assertion script (so future regressions fail loudly)
</verification>

<success_criteria>
- ROADMAP success criterion #1 (API-02): `/guides/api/` shows curated landing + tag pages emitted
- ROADMAP success criterion #2 (API-03): auth section has working curl examples for JWT, API key (header AND query), OAuth — all gated by negative-grep on the wrong header form
- ROADMAP success criterion #3 (API-04): OGC page covers Common, Records, Features, STAC, Tiles with QGIS/GDAL/pystac examples
- ROADMAP success criterion #4: API reference index does not appear in Pagefind (auto-pages excluded; hand-authored pages indexed) — gated by data-pagefind-body assertions
- ROADMAP success criterion #5 (API-05): README in `src/content/openapi/` documents refresh procedure — gated by content greps
- CI-01: `npm run build` is the gate (links-validator inline + verify-build.sh) — every CI run inherits this
</success_criteria>

<output>
After completion, create `.planning/phases/225-api-reference/225-10-SUMMARY.md` documenting:
- Final line count of verify-build.sh
- Each new `echo "Asserting ..."` heading present (paste grep output)
- llms.txt bullet count and the two new URLs
- Result of `npm run build && bash scripts/verify-build.sh` on the final state — must show "All build-artifact assertions passed."
</output>

## Truths

- D-04: `verify-build.sh` asserts `dist/.../openapi/geolens.json` (or rendered API pages) is present and non-empty. CI MUST NOT call `fetch-openapi.mjs` at build time — committed snapshot is the source of truth.
- D-24: `verify-build.sh` extends with two assertions: (a) zero `data-pagefind-body` entries in `dist/guides/api/operations/tags/*/index.html` (auto-tag pages excluded); (b) non-zero `data-pagefind-body` in `dist/guides/api/auth/index.html` (hand-authored stays indexed). Confirms exclusion scope is correct, not over-broad.
- D-30: Phase 225 ships ONLY: fetch script + snapshot + plugin wiring + landing/auth/OGC MDX + README + links-validator + verify-build.sh additions + new `llms.txt` lines. NO Quickstart, NO User/Admin guides, NO marketing-site changes, NO GA4, NO OG image generation, NO robots-flip.
- D-31: NO interactive "Try it out" console — TRY-IT-01 deferred (REQUIREMENTS Future Requirements). Out of scope for Phase 225.
- D-32: NO `oasdiff` drift CI job — OASDIFF-01 deferred. Out of scope for Phase 225.
