---
phase: 1043-error-empty-states-and-ia-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - 1043-02
files_modified:
  - frontend/src/components/builder/SidebarRail.tsx
  - frontend/src/pages/MapBuilderPage.tsx
  - frontend/src/components/builder/BasemapGroupEditorScene.tsx
  - frontend/src/components/builder/LayerEditorPanel.tsx
  - frontend/src/i18n/locales/en/builder.json
autonomous: true
requirements:
  - POL-17
  - POL-18
tags:
  - builder
  - information-architecture
  - sidebar-rail
  - scene-transitions

must_haves:
  truths:
    - "SidebarRail renders a final basemap-group button below the divider when a basemap is configured (LayoutGrid icon, h-[18px] w-[18px], aria-label, Tooltip)"
    - "The SidebarRail divider renders when layers.length > 0 OR basemapGroup is defined (no longer dangles below an empty layer list when basemap is present)"
    - "Selecting the basemap-group rail button calls onSelectLayer('basemap-group') and the rail button shows the selected-state visual (primary-50 bg + inset 2px primary shadow)"
    - "BasemapGroupEditorFooter Remove basemap button uses text-destructive with the destructive-hover oklch background, distinguishing it from the benign Reset appearance ghost button"
    - "LayerEditorPanel preserves scrollTop across editorScene transitions by reading bodyRef.scrollTop on unmount and restoring on remount"
    - "LayerEditorPanel restores keyboard focus to the panel header when transitioning from basemap-sublayer back to basemap-group"
    - "LayerEditorPanel default-scene section ordering remains: Render as → Appearance → Visibility → Filter → Labels → Source (already correct per UI-SPEC §POL-18 audit; verify-only)"
  artifacts:
    - path: "frontend/src/components/builder/SidebarRail.tsx"
      provides: "AUD-20 basemap rail button + divider logic + new basemapGroup prop"
      contains: "LayoutGrid"
    - path: "frontend/src/pages/MapBuilderPage.tsx"
      provides: "Wires basemapGroup prop into SidebarRail call site"
      contains: "basemapGroup"
    - path: "frontend/src/components/builder/BasemapGroupEditorScene.tsx"
      provides: "AUD-18 destructive styling on Remove basemap"
      contains: "text-destructive"
    - path: "frontend/src/components/builder/LayerEditorPanel.tsx"
      provides: "POL-18 scroll + focus preservation across scene transitions"
      contains: "savedScrollTopRef"
    - path: "frontend/src/i18n/locales/en/builder.json"
      provides: "basemapGroup.railLabel English copy"
      contains: "railLabel"
  key_links:
    - from: "frontend/src/pages/MapBuilderPage.tsx"
      to: "frontend/src/components/builder/SidebarRail.tsx"
      via: "basemapGroup prop derived from layers.localBasemap presence (id literal 'basemap-group')"
      pattern: "basemapGroup="
    - from: "frontend/src/components/builder/SidebarRail.tsx"
      to: "lucide-react LayoutGrid icon"
      via: "import + render at h-[18px] w-[18px] inside h-10 w-10 button"
      pattern: "LayoutGrid"
    - from: "frontend/src/components/builder/LayerEditorPanel.tsx"
      to: "<div data-testid=\"layer-editor-body\">"
      via: "bodyRef React.useRef attached to the scrollable body div"
      pattern: "ref=\\{bodyRef\\}|bodyRef.current"
---

<objective>
Close the v1009 information-architecture findings owned by Phase 1043: AUD-20 (SidebarRail missing the basemap-group button), AUD-18 (BasemapGroupEditorFooter destructive button asymmetry), and POL-18 (LayerEditorPanel scene-transition scroll + focus preservation, plus section-ordering verify). Also adds the canonical `basemap-group` rail entry expected by sketch responsive.md.

Purpose: AUD-20 P1 closes a visible regression — the SidebarRail divider currently dangles with nothing below it because the rail never renders the basemap. AUD-18 P1 makes the destructive Remove basemap action visually distinct so users don't conflate it with the benign Reset appearance. POL-18 closes the scroll/focus loss that breaks keyboard navigation when bouncing between basemap-group and basemap-sublayer scenes.
Output: SidebarRail accepts a new optional `basemapGroup` prop and renders an additional rail button + relocated divider; MapBuilderPage passes the basemap descriptor into the rail; BasemapGroupEditorFooter Remove button picks up text-destructive styling matching LayerEditorPanel's destructive ghost variant; LayerEditorPanel adds bodyRef + savedScrollTopRef + headerRef refs and the two useEffects that save/restore scroll and focus across editorScene transitions; one new English i18n key (`basemapGroup.railLabel`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1043-error-empty-states-and-ia-cleanup/1043-CONTEXT.md
@.planning/phases/1043-error-empty-states-and-ia-cleanup/1043-UI-SPEC.md
@frontend/src/components/builder/SidebarRail.tsx
@frontend/src/pages/MapBuilderPage.tsx
@frontend/src/components/builder/BasemapGroupEditorScene.tsx
@frontend/src/components/builder/LayerEditorPanel.tsx
@frontend/src/components/builder/UnifiedStackPanel.tsx

<interfaces>
<!-- SidebarRail current props (lines 10-17): { layers, selectedLayerId, onSelectLayer, onAddDataClick, onSettingsClick, isSettingsOpen? }. Add basemapGroup?: { id: string } | null per UI-SPEC §AUD-20 lines 357-396. -->
<!-- The basemap group's id is the literal string 'basemap-group' — confirmed in UnifiedStackPanel.tsx:258 and MapBuilderPage.tsx selectedLayerId branching at lines 459-464. The id is constant; do not derive a new id. -->
<!-- MapBuilderPage already has layers.localBasemap available (lines 177, 247-302). When localBasemap is non-null, a basemap group exists. Pass `{ id: 'basemap-group' }` (object literal — sufficient for the prop's minimal surface per UI-SPEC §"SidebarRail prop addition"). -->

From frontend/src/components/builder/SidebarRail.tsx (current shape):
- interface SidebarRailProps { layers: MapLayerResponse[]; selectedLayerId: string | null; onSelectLayer: (id: string | null) => void; onAddDataClick: (initialQuery?: string) => void; onSettingsClick: () => void; isSettingsOpen?: boolean; }
- Divider at line 102: `{layers.length > 0 && <div className="h-px w-8 bg-border my-1" />}` — must change to `{(layers.length > 0 || basemapGroup) && ...}` per UI-SPEC §AUD-20

From lucide-react (already used in SidebarRail for Settings + Plus):
- LayoutGrid (the basemap rail icon, mirrors the `⊞` glyph from sketch-findings-geolens)

From frontend/src/components/builder/BasemapGroupEditorScene.tsx:
- BasemapGroupEditorFooter at lines 247-264 currently renders both buttons as variant="ghost". Reset stays ghost; Remove gains className="flex-1 text-destructive hover:bg-[oklch(0.97_0.02_27)] hover:text-destructive" matching LayerEditorPanel.tsx:735.

From frontend/src/components/builder/LayerEditorPanel.tsx:
- editorScene prop type: `'default' | 'dem' | 'basemap-group' | 'basemap-sublayer' | 'settings'` at line 59
- Scrollable body: `<div data-testid="layer-editor-body" className="flex-1 overflow-y-auto">` at line 286-289
- Currently no bodyRef / scroll-preservation refs exist. Add useRef from React (already imported)

From frontend/src/pages/MapBuilderPage.tsx:
- SidebarRail call site at lines 940-952. Add `basemapGroup={layers.localBasemap ? { id: 'basemap-group' } : null}` to props.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SidebarRail basemap-group button + divider relocation (AUD-20 P1)</name>
  <files>frontend/src/components/builder/SidebarRail.tsx, frontend/src/pages/MapBuilderPage.tsx, frontend/src/i18n/locales/en/builder.json</files>
  <read_first>
    - .planning/phases/1043-error-empty-states-and-ia-cleanup/1043-UI-SPEC.md §"AUD-20" lines 357-396 and §"State Vocabulary" row "SidebarRail basemap button" (lines 568-573) for the rest/hover/selected style spec
    - frontend/src/components/builder/SidebarRail.tsx (entire file, 138 lines) for current shape
    - frontend/src/pages/MapBuilderPage.tsx lines 940-952 for the call site to update
    - frontend/src/i18n/locales/en/builder.json the existing `basemapGroup` object at line 802
  </read_first>
  <action>
    Apply AUD-20 P1 per UI-SPEC §AUD-20. In SidebarRail.tsx: (1) extend SidebarRailProps to add `basemapGroup?: { id: string } | null` (optional, defaults to null) — minimal surface as the UI-SPEC §"SidebarRail prop addition" specifies; (2) add `LayoutGrid` to the existing lucide-react imports (preserve alpha order); (3) add `basemapGroup = null` to the destructure parameter list; (4) replace the existing divider at line 102-104 with `{(layers.length > 0 || basemapGroup) && (<div className="h-px w-8 bg-border my-1" aria-hidden="true" />)}` so the divider is shown when EITHER overlay layers exist OR basemap is present (per UI-SPEC §AUD-20 lines 393-398); (5) AFTER the existing layer-buttons map (after line 135 closing `})`), append a new conditional Tooltip block that renders only when `basemapGroup` is defined. The button uses the exact JSX shape per UI-SPEC §AUD-20 lines 362-386: `data-selected={basemapGroup.id === selectedLayerId ? 'true' : undefined}`, the className uses cn() with the selected variant `bg-[var(--primary-50,oklch(0.97_0.02_250))] shadow-[inset_2px_0_0_var(--primary)]` and rest+hover variant `hover:bg-[var(--surface-2)] text-muted-foreground hover:text-foreground`, the icon is `<LayoutGrid className="h-[18px] w-[18px]" aria-hidden="true" />`, aria-label and TooltipContent both use `t('basemapGroup.railLabel', { defaultValue: 'Basemap group' })`, onClick fires `onSelectLayer(basemapGroup.id)`. Mirror the layer-button h-10 w-10 outer dimensions and focus-visible:ring-2 focus-visible:ring-ring pattern. In MapBuilderPage.tsx at the SidebarRail call site (lines 940-952), add the new prop: `basemapGroup={layers.localBasemap ? { id: 'basemap-group' } : null}` (the 'basemap-group' literal matches the constant at UnifiedStackPanel.tsx:258 — do not invent a new id). In en/builder.json, locate the existing `"basemapGroup"` object (line 802) and add `"railLabel": "Basemap group"` (alphabetical placement). Do not modify de/es/fr.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -c "basemapGroup" src/components/builder/SidebarRail.tsx</automated>
    Expected: ≥3 (prop type, destructure default, divider guard, button conditional). Also: `grep -c "LayoutGrid" src/components/builder/SidebarRail.tsx` returns ≥2 (import + usage); `grep -A1 "SidebarRail" src/pages/MapBuilderPage.tsx | grep -c basemapGroup` returns ≥1; `grep -c '"railLabel"' src/i18n/locales/en/builder.json` returns 1; `npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep SidebarRail | head` returns no errors.
  </verify>
  <done>
    Resize the browser to <1100px to trigger the rail. With a basemap configured, the rail shows: Settings, ＋ Add data, divider, layer buttons (if any), basemap-group button (LayoutGrid icon). Clicking the basemap button selects 'basemap-group' (the LayerEditorPanel opens to the basemap-group scene). Selected state shows the primary-50 background + inset shadow. Hover on unselected shows surface-2 background. Tooltip "Basemap group" renders on hover. With no overlay layers but basemap present, the divider still renders above the basemap button (does not dangle).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: BasemapGroupEditorFooter destructive Remove styling (AUD-18 P1)</name>
  <files>frontend/src/components/builder/BasemapGroupEditorScene.tsx</files>
  <read_first>
    - .planning/phases/1043-error-empty-states-and-ia-cleanup/1043-UI-SPEC.md §"AUD-18" lines 332-353 for the precedent and target className
    - frontend/src/components/builder/BasemapGroupEditorScene.tsx lines 247-264 (BasemapGroupEditorFooter)
    - frontend/src/components/builder/LayerEditorPanel.tsx line 735 (the established destructive ghost pattern: `text-destructive hover:bg-[oklch(0.97_0.02_27)] hover:text-destructive`)
  </read_first>
  <action>
    Apply AUD-18 P1 per UI-SPEC §AUD-18. In BasemapGroupEditorFooter (BasemapGroupEditorScene.tsx lines 247-264): leave the Reset appearance Button at line 255 unchanged (variant="ghost" className="flex-1"). Modify the Remove basemap Button at line 258: keep variant="ghost" but change className from `flex-1` to `flex-1 text-destructive hover:bg-[oklch(0.97_0.02_27)] hover:text-destructive` — exactly matching the established destructive-ghost pattern at LayerEditorPanel.tsx:735. The intent: footer asymmetry (sketch §"Footer button asymmetry") makes the destructive action visually distinguishable while preserving the same variant family. Do NOT introduce a confirmation dialog before remove (per CONTEXT.md §"Audit findings to address" line stating "Remove basemap destructive-color styling, no confirm dialog (per UI-SPEC discretion)"). Do NOT modify any other Button in this file or any sibling editor scene file.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -A2 "removeBasemap" src/components/builder/BasemapGroupEditorScene.tsx | grep -c "text-destructive"</automated>
    Expected: ≥1. Also: `grep -A2 "resetAppearance" src/components/builder/BasemapGroupEditorScene.tsx | grep -c "text-destructive"` returns 0 (Reset stays benign); `grep -c "oklch(0.97_0.02_27)" src/components/builder/BasemapGroupEditorScene.tsx` returns ≥1; `npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep BasemapGroupEditor | head` returns no errors.
  </verify>
  <done>
    Open the basemap-group scene (click basemap row, or rail basemap button after Task 1). Footer shows Reset appearance (benign ghost) and Remove basemap (destructive ghost: red text, light-red hover background). Pattern visually matches LayerEditorPanel's "Delete layer" destructive button.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: LayerEditorPanel scene-transition scroll + focus preservation (POL-18)</name>
  <files>frontend/src/components/builder/LayerEditorPanel.tsx</files>
  <read_first>
    - .planning/phases/1043-error-empty-states-and-ia-cleanup/1043-UI-SPEC.md §"Scene Transition State Preservation" lines 402-454 for the exact ref and useEffect patterns
    - .planning/phases/1043-error-empty-states-and-ia-cleanup/1043-UI-SPEC.md §"LayerEditorPanel Section Ordering" lines 287-309 to confirm section ordering is already correct (verify-only — no reordering work)
    - frontend/src/components/builder/LayerEditorPanel.tsx lines 150-170 (state hooks region) and lines 286-289 (the data-testid="layer-editor-body" scrollable div)
  </read_first>
  <action>
    Apply POL-18 per UI-SPEC §"Scene Transition State Preservation" (POL-18). In LayerEditorPanel.tsx: (1) confirm React useRef is already imported (file already uses useState/useEffect — useRef may need to be added to the import line); (2) inside the component near the existing state hooks (lines 150-170), declare three new refs: `const bodyRef = useRef<HTMLDivElement>(null);` `const savedScrollTopRef = useRef<number>(0);` `const prevSceneRef = useRef(editorScene);`; (3) attach `ref={bodyRef}` to the scrollable body div at line 286-289 (the one with data-testid="layer-editor-body"); (4) add a useEffect that fires on editorScene change and saves scrollTop on unmount/cleanup: `useEffect(() => { return () => { if (bodyRef.current) savedScrollTopRef.current = bodyRef.current.scrollTop; }; }, [editorScene]);` per UI-SPEC §"Scroll preservation contract"; (5) add a useEffect that fires after layer.id or editorScene changes and restores scrollTop: `useEffect(() => { if (bodyRef.current && savedScrollTopRef.current > 0) bodyRef.current.scrollTop = savedScrollTopRef.current; }, [layer.id, editorScene]);`; (6) add the prevSceneRef tracking + focus restoration per UI-SPEC §"Focus preservation contract" — the headerRef approach is described at UI-SPEC lines 442-454. For focus restore, identify the element to focus when transitioning back from basemap-sublayer to basemap-group: the panel's breadcrumb header element. Since the panel's header at lines 209-265 contains the close button (line 230) and the breadcrumb area (line 211-219), the simplest robust implementation is to set tabIndex={-1} on the existing `<header data-testid="layer-editor-header">` (verify the testid by reading lines 200-210) and call `headerRef.current?.focus()` inside a useEffect: `useEffect(() => { if (editorScene === 'basemap-group' && prevSceneRef.current === 'basemap-sublayer') { headerRef.current?.focus(); } prevSceneRef.current = editorScene; }, [editorScene]);` where `const headerRef = useRef<HTMLElement>(null)` and `ref={headerRef}` is attached to the header element. Important guard: ONLY restore scroll when scrollTop > 0 to avoid jumping to 0 on first render of a new layer; do NOT clear savedScrollTopRef.current to 0 during normal scroll, only on layer.id change (the existing reset block at lines 165-170 handles this). VERIFY-ONLY for section ordering: read lines 290-576 and confirm the order is Render as → Appearance → Visibility → Filter → Labels → Source as documented in UI-SPEC §"LayerEditorPanel Section Ordering" (no reordering needed — UI-SPEC explicitly states "The ordering is already correct"). If a deviation is found, log the deviation in the plan SUMMARY but do not silently re-order — that would be out of scope.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -c "savedScrollTopRef\|bodyRef\|headerRef\|prevSceneRef" src/components/builder/LayerEditorPanel.tsx</automated>
    Expected: ≥4 (one per ref). Also: `grep -c "ref={bodyRef}" src/components/builder/LayerEditorPanel.tsx` returns ≥1; `grep -c "useRef" src/components/builder/LayerEditorPanel.tsx` returns ≥3; `npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep LayerEditorPanel | head` returns no errors; `npx vitest run src/components/builder/__tests__/LayerEditorPanel.test.tsx 2>&1 | tail -5` — existing tests pass.
  </verify>
  <done>
    Open a layer with enough content to scroll the LayerEditorPanel body (e.g. expand Source section + open Filter + Labels). Scroll the body down. Click the basemap-group row to switch the editor to basemap-group scene. Click back to the same overlay layer in the stack. The panel returns with scrollTop preserved at the value it had before the scene swap. Open a basemap sublayer (drill in from the basemap-group). Click the breadcrumb back to basemap-group. Keyboard focus lands on the panel header (visible focus ring on the back/close button). Section ordering verify: when opening a vector layer, sections appear top-to-bottom as Render as → Appearance → Visibility → Filter → Labels → Source. Phase summary records: "POL-18 section ordering verified; no reordering work needed (already correct per UI-SPEC §POL-18)."
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click ↔ rail basemap selection | New rail button calls existing `onSelectLayer('basemap-group')` — same selection surface as the in-stack basemap row click. No new state path. |
| MapBuilderPage local state ↔ SidebarRail prop | Pass-through of `layers.localBasemap` truthiness as a `{ id: 'basemap-group' }` literal — no untrusted data flows through this prop. |
| LayerEditorPanel DOM ref access ↔ scroll/focus restoration | useRef pattern reads/writes scrollTop and calls focus() — both are same-origin DOM operations on this app's own elements. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1043-09 | Spoofing | basemap-group rail click | accept | Reuses existing onSelectLayer pathway; no new auth or selection-id surface. The 'basemap-group' literal is a constant matched at UnifiedStackPanel.tsx:258 — not user-supplied. |
| T-1043-10 | Tampering | basemapGroup prop | accept | Optional prop with strict TypeScript shape `{ id: string } | null`; only MapBuilderPage constructs the value, and only with the constant 'basemap-group' id. |
| T-1043-11 | Information Disclosure | scroll position persistence | accept | scrollTop value held only in component-local React ref; never serialized, never sent over the network, never logged. |
| T-1043-12 | Denial of Service | Scene-transition useEffect loop | mitigate | useEffect dependencies are scalar primitives (editorScene, layer.id) — no object identity churn. The savedScrollTopRef.current > 0 guard prevents restore-on-fresh-render jumps. |
| T-1043-13 | Repudiation | Remove basemap destructive action | accept | AUD-18 changes only the visual styling of the existing Remove button — same handler, same audit-log path (or lack thereof — basemap "remove" is local state reset, not a destructive backend mutation). |
</threat_model>

<verification>
1. `cd frontend && npx vitest run src/components/builder/__tests__/SidebarRail*.test.tsx 2>&1 | tail -5` — existing tests pass; if no test file exists, this is acceptable (Phase 1044 owns UAT spec coverage).
2. `cd frontend && npx vitest run src/components/builder/__tests__/LayerEditorPanel*.test.tsx 2>&1 | tail -5` — pass; existing tests should not regress on the scroll/focus refs (refs default to null, useEffects no-op when refs are unattached).
3. `cd frontend && npx vitest run src/components/builder/__tests__/BasemapGroupEditor*.test.tsx 2>&1 | tail -5` — pass.
4. `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -E "Rail|LayerEditor|MapBuilderPage|BasemapGroupEditor" | head` — no new TS errors.
5. `cd frontend && npx eslint src/components/builder/SidebarRail.tsx src/components/builder/LayerEditorPanel.tsx src/components/builder/BasemapGroupEditorScene.tsx src/pages/MapBuilderPage.tsx --max-warnings 0 2>&1 | tail` — clean.
6. `node -e "JSON.parse(require('fs').readFileSync('frontend/src/i18n/locales/en/builder.json'))"` — JSON valid; new key `basemapGroup.railLabel` present.
</verification>

<success_criteria>
- AUD-20 closed: SidebarRail renders a basemap-group rail button (LayoutGrid, h-[18px] w-[18px]) below the divider when `basemapGroup` prop is present; the divider no longer dangles when overlay layers are absent but basemap is configured.
- AUD-18 closed: BasemapGroupEditorFooter Remove basemap button uses text-destructive + destructive-hover background; Reset appearance stays benign; no confirmation dialog added (per UI-SPEC discretion).
- POL-18 advanced: LayerEditorPanel scrollTop persists across editorScene transitions; keyboard focus restores to panel header on basemap-sublayer → basemap-group return; default-scene section ordering verified-correct (no reordering work).
- POL-17 advanced: With Plan 01+02+03 shipped, all four POL-17 empty-state surfaces are addressed (Plan 02 owns Filter/Labels/Source/EmptyStackState; Plan 03 verifies section ordering invariant).
- New English-only key `basemapGroup.railLabel: "Basemap group"` present in en/builder.json; de/es/fr untouched.
- Builder vitest suite passes; TS clean; ESLint clean.
</success_criteria>

<output>
Create `.planning/phases/1043-error-empty-states-and-ia-cleanup/1043-03-SUMMARY.md` when done.
</output>
