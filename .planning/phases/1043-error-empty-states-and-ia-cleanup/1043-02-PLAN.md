---
phase: 1043-error-empty-states-and-ia-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 1043-01
files_modified:
  - frontend/src/components/builder/EmptyStackState.tsx
  - frontend/src/components/builder/DatasetSearchPanel.tsx
  - frontend/src/components/builder/LayerEditorPanel.tsx
  - frontend/src/i18n/locales/en/builder.json
autonomous: true
requirements:
  - POL-16
  - POL-17
tags:
  - builder
  - empty-states
  - copywriting
  - a11y

must_haves:
  truths:
    - "EmptyStackState renders the SUGGESTED eyebrow + list ONLY when SUGGESTED_DATASETS.length > 0; when empty, renders a starter-help block (MapPin icon + body copy + Browse catalog → button)"
    - "DatasetSearchPanel's empty-catalog state renders both the Upload a file → primary CTA AND a secondary Browse public catalog → link to /collections"
    - "LayerEditorPanel Source section renders 'No queryable columns indexed for this layer.' when columns.length === 0, in addition to the existing dataset/features/type/geometry metadata rows"
    - "Three new English-only i18n keys exist: unifiedStack.emptyHelpBody, search.browseCatalogCta, layerEditor.source.noColumns"
    - "LayerFilterEditor and LabelEditor empty states are NOT duplicated — those components already own their own no-columns copy via filters.noColumns and labels.noColumns"
  artifacts:
    - path: "frontend/src/components/builder/EmptyStackState.tsx"
      provides: "AUD-22 conditional SUGGESTED + starter-help fallback"
      contains: "MapPin"
    - path: "frontend/src/components/builder/DatasetSearchPanel.tsx"
      provides: "AUD-14 secondary Browse public catalog CTA"
      contains: "browseCatalogCta"
    - path: "frontend/src/components/builder/LayerEditorPanel.tsx"
      provides: "POL-17 Source-section no-columns empty state"
      contains: "source.noColumns"
    - path: "frontend/src/i18n/locales/en/builder.json"
      provides: "Three new English copy keys"
      contains: "browseCatalogCta"
  key_links:
    - from: "frontend/src/components/builder/EmptyStackState.tsx"
      to: "SUGGESTED_DATASETS array"
      via: "conditional render guard SUGGESTED_DATASETS.length > 0"
      pattern: "SUGGESTED_DATASETS.length"
    - from: "frontend/src/components/builder/DatasetSearchPanel.tsx"
      to: "/collections route"
      via: "react-router Link asChild on a Button variant=link"
      pattern: "/collections"
    - from: "frontend/src/components/builder/LayerEditorPanel.tsx"
      to: "Source section body"
      via: "columns.length === 0 conditional inside CollapsibleContent at line 538-576"
      pattern: "columns\\.length === 0"
---

<objective>
Close the empty-state findings owned by Phase 1043 (POL-17, AUD-14 P1, AUD-22 P0) by replacing the orphan SUGGESTED eyebrow with a starter-help block, restoring catalog-first positioning to DatasetSearchPanel's empty-catalog state, and adding the LayerEditorPanel Source section's "no queryable columns" copy. All three are intentional empty-state surfaces with explicit copy + iconography per the UI-SPEC empty-state matrix.

Purpose: AUD-22 is a fresh-install P0 — the orphan eyebrow renders on every default deployment because SUGGESTED_DATASETS ships empty. AUD-14 closes the v1008 catalog-first positioning regression. POL-17 Source completes the empty-state matrix; the Filter and Labels surfaces already own their own no-columns copy and must NOT be duplicated.
Output: EmptyStackState branch on SUGGESTED_DATASETS length; DatasetSearchPanel adds a secondary Link CTA in the empty-catalog branch; LayerEditorPanel Source section adds an explicit no-columns body sentence; three new English i18n keys (unifiedStack.emptyHelpBody, search.browseCatalogCta, layerEditor.source.noColumns).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1043-error-empty-states-and-ia-cleanup/1043-CONTEXT.md
@.planning/phases/1043-error-empty-states-and-ia-cleanup/1043-UI-SPEC.md
@.planning/phases/1043-error-empty-states-and-ia-cleanup/1043-01-SUMMARY.md
@frontend/src/components/builder/EmptyStackState.tsx
@frontend/src/components/builder/DatasetSearchPanel.tsx
@frontend/src/components/builder/LayerEditorPanel.tsx
@frontend/src/components/builder/suggested-datasets.ts

<interfaces>
<!-- EmptyStackState exports the canonical eyebrowClassName constant at line 17. Reuse it. -->
<!-- SUGGESTED_DATASETS is a const array at suggested-datasets.ts:32 (currently `[]`). Length-check is the AUD-22 gate. -->
<!-- LayerEditorPanel Source section conditional renders ColumnsReference only when columns.length > 0 at line 564. The new no-columns sentence belongs in the same conditional region but in the inverse branch. -->

From frontend/src/components/builder/EmptyStackState.tsx:
- `export const eyebrowClassName: string` at line 17
- `export interface EmptyStackStateProps { onOpenAddData: () => void; onAddDataset?: ... }` at line 10
- `export function EmptyStackState({ onOpenAddData, onAddDataset })` at line 153

From frontend/src/components/builder/suggested-datasets.ts:
- `export const SUGGESTED_DATASETS: SuggestedDataset[] = []` (currently empty by design)

From lucide-react (already used in adjacent files):
- MapPin (used per UI-SPEC §Empty State Matrix entry 1 as the AUD-22 starter-help icon)

From react-router-dom (already imported in DatasetSearchPanel for the Upload CTA Link):
- Link to="/collections" — the existing /collections catalog route per project routes

From frontend/src/components/ui/button:
- Button variant="link" size="sm" asChild (already used elsewhere in this file for the Upload CTA)

LayerEditorPanel.tsx Source section anatomy (lines 518-576):
- Collapsible open={sourceOpen} onOpenChange={setSourceOpen}
- CollapsibleContent body wraps a `<div className="px-4 py-2">` with metadata fields
- Inside the body, `{columns.length > 0 && <ColumnsReference columns={columns} />}` renders at line 564
- The new sentence sits beside this conditional, not inside it: render `<p>` when `columns.length === 0` per UI-SPEC §Empty State 5
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: EmptyStackState SUGGESTED conditional + starter-help fallback (AUD-22 P0)</name>
  <files>frontend/src/components/builder/EmptyStackState.tsx, frontend/src/i18n/locales/en/builder.json</files>
  <read_first>
    - .planning/phases/1043-error-empty-states-and-ia-cleanup/1043-UI-SPEC.md §"Empty State Matrix" entry 1 (lines 184-205) for the exact target shape
    - frontend/src/components/builder/EmptyStackState.tsx lines 1-50 (imports + eyebrowClassName + types) and lines 227-260 (current SUGGESTED section + Browse all link)
    - frontend/src/components/builder/suggested-datasets.ts (entire file, 32 lines — confirm SUGGESTED_DATASETS = [] and the SuggestedDataset type shape)
  </read_first>
  <action>
    Apply AUD-22 P0 per UI-SPEC §"Empty State Matrix" entry 1. In EmptyStackState.tsx: (1) add `MapPin` to the existing lucide-react imports (preserve existing import group ordering); (2) at lines 227-247, replace the unconditional Suggested section JSX with a conditional branch on `SUGGESTED_DATASETS.length`. When `SUGGESTED_DATASETS.length > 0`, render the existing eyebrow + ul exactly as today (no changes inside that branch). When `SUGGESTED_DATASETS.length === 0`, render the starter-help fallback per UI-SPEC: a `<div className="flex flex-col items-center gap-3 px-4 py-4 text-center">` containing `<MapPin className="h-8 w-8 text-muted-foreground" aria-hidden="true" />`, then `<p className="text-sm text-muted-foreground">{t('unifiedStack.emptyHelpBody', { defaultValue: 'Search the catalog to find datasets, or use the Upload button to add your own.' })}</p>`, then a `<button>` with `type="button"`, `aria-label="Browse all datasets in the Add Data modal"`, `onClick={() => onOpenAddData()}`, and `className="text-xs text-primary self-center hover:underline focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring rounded"` containing the text `{t('unifiedStack.browseAllShort', { defaultValue: 'Browse catalog →' })}`. Important: the EXISTING "Browse all" button at lines 250-257 (which calls onOpenAddData with the longer "Browse all datasets →" copy) stays unchanged and continues to render below the suggested-or-help section regardless of branch — do not remove it. The starter-help block's CTA uses a shorter "Browse catalog →" label per UI-SPEC §Copywriting Contract; it lives inside the help block as a self-centered link, while the original Browse all link remains as the page-level secondary affordance below. In en/builder.json under the existing `unifiedStack` object (line 770), add `"emptyHelpBody": "Search the catalog to find datasets, or use the Upload button to add your own."` and `"browseAllShort": "Browse catalog →"`. Do not modify de/es/fr.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -c "SUGGESTED_DATASETS.length" src/components/builder/EmptyStackState.tsx</automated>
    Expected: ≥1 (one length check). Also: `grep -c "emptyHelpBody" src/i18n/locales/en/builder.json` returns 1; `grep -c "browseAllShort" src/i18n/locales/en/builder.json` returns 1; `grep -c "MapPin" src/components/builder/EmptyStackState.tsx` returns ≥1; `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en/builder.json'))"` (run from frontend/) — exit 0.
  </verify>
  <done>
    On a fresh install (SUGGESTED_DATASETS = []), EmptyStackState renders MapPin icon + emptyHelpBody copy + Browse catalog → button instead of the orphan SUGGESTED eyebrow + empty ul. The eyebrow + ul still renders correctly when SUGGESTED_DATASETS is later populated. The original page-level Browse all link below remains. Vitest EmptyStackState tests pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DatasetSearchPanel empty-catalog secondary CTA (AUD-14 P1)</name>
  <files>frontend/src/components/builder/DatasetSearchPanel.tsx, frontend/src/i18n/locales/en/builder.json</files>
  <read_first>
    - .planning/phases/1043-error-empty-states-and-ia-cleanup/1043-UI-SPEC.md §"Empty State Matrix" entry 2 (lines 207-230) for the target structure
    - frontend/src/components/builder/DatasetSearchPanel.tsx lines 651-665 (current empty-catalog state) and the Link import line near the top
  </read_first>
  <action>
    Apply AUD-14 P1 per UI-SPEC §"Empty State Matrix" entry 2 (POL-17 catalog-first positioning). In DatasetSearchPanel.tsx, locate the empty-catalog branch at lines 651-665 (the State A block guarded by `debouncedQuery.trim().length === 0 && results.length === 0`). The block currently contains the Inbox icon, the catalogEmpty heading, and a single Upload CTA (`<Button variant="ghost" size="sm" asChild>` wrapping `<Link to="/import">`). Append a SECOND Button below the existing one: `<Button variant="link" size="sm" className="text-muted-foreground text-xs" asChild>` wrapping `<Link to="/collections">{t('search.browseCatalogCta', { defaultValue: 'Browse public catalog →' })}</Link>`. Per UI-SPEC §"Empty State Matrix" entry 2, the secondary CTA uses variant=link with text-muted-foreground text-xs to be visually subordinate to the Upload CTA. The /collections route already exists in this app's router. In en/builder.json under the existing "search" object (line 389), add `"browseCatalogCta": "Browse public catalog →"` (alphabetical placement). Do not modify any other empty/zero-result branch — only the unfiltered State A block at 651-665.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -c "browseCatalogCta" src/components/builder/DatasetSearchPanel.tsx</automated>
    Expected: ≥1. Also: `grep -c "browseCatalogCta" src/i18n/locales/en/builder.json` returns 1; `grep -A2 'to="/collections"' src/components/builder/DatasetSearchPanel.tsx | grep -c browseCatalogCta` returns ≥1; `npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep DatasetSearchPanel | head` returns no errors.
  </verify>
  <done>
    Open the Add Data modal with a fresh empty catalog (no datasets at all). The empty state shows the existing Inbox icon + "Your catalog is empty…" heading + "Upload a file →" primary CTA, plus a new secondary "Browse public catalog →" link routing to /collections. The secondary CTA is visually subordinate (variant=link, text-muted-foreground text-xs). Other empty / zero-result branches are unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: LayerEditorPanel Source section no-columns empty state (POL-17)</name>
  <files>frontend/src/components/builder/LayerEditorPanel.tsx, frontend/src/i18n/locales/en/builder.json</files>
  <read_first>
    - .planning/phases/1043-error-empty-states-and-ia-cleanup/1043-UI-SPEC.md §"Empty State Matrix" entries 3, 4, 5 (lines 232-277) — entries 3 (Filter) and 4 (Labels) explicitly state "no change needed; do NOT duplicate". Only entry 5 (Source) requires net-new work.
    - frontend/src/components/builder/LayerEditorPanel.tsx lines 518-576 (Source section Collapsible body)
    - frontend/src/i18n/locales/en/builder.json line 854 (existing layerEditor object — confirm the source.* sub-object already exists or add `source` as new sub-object)
  </read_first>
  <action>
    Apply POL-17 Source-section empty state per UI-SPEC §"Empty State Matrix" entry 5. In LayerEditorPanel.tsx Source section body (between lines 538 and 575, inside the `<div className="px-4 py-2">` that holds Dataset / Features / Type / Geometry rows and the conditional `{columns.length > 0 && <ColumnsReference ... />}` at line 564): add a new conditional sibling to the ColumnsReference render — `{columns.length === 0 && (<p className="text-xs text-muted-foreground">{t('layerEditor.source.noColumns', { defaultValue: 'No queryable columns indexed for this layer.' })}</p>)}`. Place it directly above the existing `{columns.length > 0 && <ColumnsReference ... />}` block — the two conditions are mutually exclusive so there is no double-render risk. Do NOT touch the Filter section (lines 449-481) or the Labels section (lines 484-516) — UI-SPEC explicitly defers those to LayerFilterEditor / LabelEditor which already own their own no-columns copy via existing keys filters.noColumns (en/builder.json:292) and labels.noColumns (en/builder.json:343). In en/builder.json, locate the `"layerEditor"` object (line 854); if a nested `"source"` object exists there, add `"noColumns": "No queryable columns indexed for this layer."` inside it; if no `"source"` sub-object exists, create one with that single key. Use Read or jq to confirm the current shape before editing.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -c "source.noColumns" src/components/builder/LayerEditorPanel.tsx</automated>
    Expected: ≥1. Also: `grep -c "noColumns" src/i18n/locales/en/builder.json` returns ≥3 (preserves existing filters.noColumns + labels.noColumns + new source.noColumns); `node -e "const j = JSON.parse(require('fs').readFileSync('src/i18n/locales/en/builder.json')); console.log(j.layerEditor.source.noColumns)"` (from frontend/) prints 'No queryable columns indexed for this layer.'; `npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep LayerEditorPanel | head` returns no errors.
  </verify>
  <done>
    Open a layer with `dataset_column_info` empty / undefined (zero indexed columns). Open the Source section. The body shows Dataset / Features / Type / Geometry metadata rows plus the new "No queryable columns indexed for this layer." sentence in muted text-xs. When columns are present, only ColumnsReference renders (no duplicate sentence). LayerEditorPanel tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click ↔ react-router navigation | New /collections Link is a same-origin internal route; no external navigation. |
| empty SUGGESTED_DATASETS branch ↔ component render | Length check on a static array — no untrusted-input branch. |
| layer column metadata ↔ empty-state copy | columns.length === 0 reads layer.dataset_column_info length, which originates from authenticated catalog API and is plain count, not user-rendered HTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1043-05 | Information Disclosure | Source no-columns sentence | accept | Sentence reveals only that the layer has zero indexed columns — same information already implied by absent ColumnsReference render. No new data exposure. |
| T-1043-06 | Tampering | i18n key resolution | accept | Three new static-string defaultValue fallbacks; no template interpolation of user-supplied data. |
| T-1043-07 | Spoofing | /collections internal Link | accept | Same-origin Link via react-router; no external redirect surface. |
| T-1043-08 | Denial of Service | Empty SUGGESTED branch | accept | Single length check on a static const; constant-time. |
</threat_model>

<verification>
1. `cd frontend && npx vitest run src/components/builder/__tests__/EmptyStackState*.test.tsx 2>&1 | tail -10` — existing tests pass; integration tests should not regress on the SUGGESTED_DATASETS = [] path.
2. `cd frontend && npx vitest run src/components/builder/__tests__/DatasetSearchPanel*.test.tsx 2>&1 | tail` — pass.
3. `cd frontend && npx vitest run src/components/builder/__tests__/LayerEditorPanel*.test.tsx 2>&1 | tail` — pass.
4. `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | head` — no new TS errors.
5. `cd frontend && npx eslint src/components/builder/EmptyStackState.tsx src/components/builder/DatasetSearchPanel.tsx src/components/builder/LayerEditorPanel.tsx --max-warnings 0 2>&1 | tail` — clean.
6. `node -e "JSON.parse(require('fs').readFileSync('frontend/src/i18n/locales/en/builder.json'))"` — JSON valid; the three new keys present (unifiedStack.emptyHelpBody, search.browseCatalogCta, layerEditor.source.noColumns) plus unifiedStack.browseAllShort.
</verification>

<success_criteria>
- AUD-22 closed: EmptyStackState renders intentional starter-help block when SUGGESTED_DATASETS is empty (default state); the orphan eyebrow no longer renders on fresh installs.
- AUD-14 closed: DatasetSearchPanel empty-catalog state offers both Upload primary CTA and Browse public catalog secondary link.
- POL-17 advanced: LayerEditorPanel Source section explicitly states "No queryable columns indexed for this layer." when columns are absent.
- Filter and Labels sections explicitly NOT duplicated — they already own filters.noColumns and labels.noColumns (per UI-SPEC §"Empty State Matrix" entries 3+4 and §"What to Avoid").
- Four new English-only keys (unifiedStack.emptyHelpBody, unifiedStack.browseAllShort, search.browseCatalogCta, layerEditor.source.noColumns) present in en/builder.json; de/es/fr untouched.
- All builder vitest passes; TS clean; ESLint clean.
</success_criteria>

<output>
Create `.planning/phases/1043-error-empty-states-and-ia-cleanup/1043-02-SUMMARY.md` when done.
</output>
