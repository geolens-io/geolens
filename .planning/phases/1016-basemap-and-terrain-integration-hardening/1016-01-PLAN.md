---
phase: 1016-basemap-and-terrain-integration-hardening
plan: 01
wave: 1
depends_on: []
autonomous: true
requirements: [MAPCTL-01, MAPCTL-02, MAPCTL-03, MAPCTL-04, MAPCTL-05]
subsystem: frontend-builder
tags: [map-builder, basemap, terrain, playwright, tests]
files_modified:
  - e2e/builder.spec.ts
  - .planning/phases/1016-basemap-and-terrain-integration-hardening/1016-01-SUMMARY.md
  - .planning/phases/1016-basemap-and-terrain-integration-hardening/1016-VERIFICATION.md
---

<objective>
Harden basemap and terrain integration by proving map-level writes, Add Dataset/sidebar state sync, overlay preservation, and save/reload behavior.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1016-basemap-and-terrain-integration-hardening/1016-CONTEXT.md
@frontend/src/components/builder/__tests__/MapStackPanel.test.tsx
@frontend/src/components/builder/__tests__/DatasetSearchPanel.test.tsx
@frontend/src/components/builder/__tests__/TerrainControls.test.tsx
@frontend/src/components/builder/__tests__/BuilderMap.unit.test.ts
@e2e/builder.spec.ts
</context>

<tasks>
  <task type="auto">
    <name>Add browser coverage for modal basemap swap persistence</name>
    <files>e2e/builder.spec.ts</files>
    <action>Add a builder E2E test that swaps to an alternate basemap from the Add Dataset modal, verifies modal `in use` state, checks sidebar label synchronization, saves, reloads, and verifies API persistence.</action>
    <verify>Assert overlay layer count is unchanged and no error toasts appear.</verify>
    <done>MAPCTL-01, MAPCTL-02, and MAPCTL-03 have browser-level proof.</done>
  </task>

  <task type="auto">
    <name>Run basemap and terrain focused gates</name>
    <files>test outputs only</files>
    <action>Run focused component/unit tests for basemap reset/swap, terrain controls, DEM `Use as terrain`, and builder map helper behavior.</action>
    <verify>Record exact command results in summary and verification artifacts.</verify>
    <done>MAPCTL-04 and MAPCTL-05 retain direct test evidence.</done>
  </task>

  <task type="auto">
    <name>Close phase state</name>
    <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, phase summary and verification artifacts</files>
    <action>Mark MAPCTL-01..05 complete and Phase 1016 complete after verification.</action>
    <verify>Roadmap, requirements, and state agree on Phase 1016 completion and next phase 1017.</verify>
    <done>Phase 1016 is ready to continue into Phase 1017.</done>
  </task>
</tasks>

<verification>
- `npx playwright test e2e/builder.spec.ts --project=chromium -g "swaps basemap from Add Dataset modal"`
- `cd frontend && npm run test -- MapStackPanel DatasetSearchPanel TerrainControls BuilderMap.unit --run`
- `npm run e2e:smoke:builder`
</verification>

<must_haves>
- No schema, migration, renderer, catalog, or import workflow changes.
- Basemap controls write only map-level basemap fields.
- Terrain controls write only `terrain_config`.
- No persisted basemap sublayer `MapLayer` rows.
</must_haves>

<success_criteria>
- [ ] Add Dataset basemap swap updates modal state and sidebar label immediately.
- [ ] Basemap save/reload persists the selected basemap style.
- [ ] Basemap swap/reset keeps overlay/data layers intact.
- [ ] Terrain enabled/source/exaggeration writes only `terrain_config`.
- [ ] Raster DEM `Use as terrain` sets terrain source without mutating the layer row.
</success_criteria>
