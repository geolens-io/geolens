---
phase: 1086-process-tightening-close-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/retros/v1019-process.md
  - /Users/ishiland/.claude/agents/gsd-planner.md
  - /Users/ishiland/.claude/agents/gsd-executor.md
  - /Users/ishiland/.claude/get-shit-done/templates/requirements.md
autonomous: true
requirements:
  - TD-13

must_haves:
  truths:
    - "The repo retro at .planning/retros/v1019-process.md narrates the three v1018 drift incidents (TD-02/03 paraphrased test names, tasks_common.py path+line drift, Plan 1081-02 SUMMARY checkbox-flip miss) and emits a named rule for each."
    - "The global gsd-planner skill carries an additive rule requiring REQ-cited test names to be path::TestClass::test_name form, validated against git grep before commit; production-code citations must include path + line, also validated against git grep."
    - "The global gsd-executor skill carries an additive rule that before SUMMARY commit, the executor MUST flip the REQUIREMENTS.md checkbox [ ] -> [x] and the traceability row Pending -> Complete for every requirement closed in this plan."
    - "The global templates/requirements.md template carries a 'good requirement' example that uses path::TestClass::test_name node-ID form."
    - "All four files exist (the retro is newly created; the three skill files were modified additively without rewriting existing content)."
  artifacts:
    - path: ".planning/retros/v1019-process.md"
      provides: "Repo-scoped retro narrative covering the three v1018 process-drift incidents + the rules added in response"
      contains: "test_register_emits_user_register_audit"
      min_lines: 60
    - path: "/Users/ishiland/.claude/agents/gsd-planner.md"
      provides: "Additive REQ-authoring node-ID rule"
      contains: "path::TestClass::test_name"
    - path: "/Users/ishiland/.claude/agents/gsd-executor.md"
      provides: "Additive SUMMARY-commit REQUIREMENTS.md checkbox-flip rule"
      contains: "REQUIREMENTS.md checkbox"
    - path: "/Users/ishiland/.claude/get-shit-done/templates/requirements.md"
      provides: "Good-requirement example using path::TestClass::test_name node-ID schema"
      contains: "path::TestClass::test_name"
  key_links:
    - from: ".planning/retros/v1019-process.md"
      to: "/Users/ishiland/.claude/agents/gsd-planner.md"
      via: "narrative explicitly references the new node-ID pinning rule and links the v1018 TD-02/03 paraphrased-test-name incident as motivation"
      pattern: "node-ID|node_id|path::TestClass"
    - from: ".planning/retros/v1019-process.md"
      to: "/Users/ishiland/.claude/agents/gsd-executor.md"
      via: "narrative explicitly references the new SUMMARY-commit checkbox-flip rule and links the v1018 Plan 1081-02 / commit 5bf63166 incident as motivation"
      pattern: "checkbox|Plan 1081-02|5bf63166"
---

<objective>
Close TD-13 by committing two artifacts: (1) a project-scoped retro at `.planning/retros/v1019-process.md` that narrates the three documentation-drift incidents from v1018 and the rules they motivate; (2) additive edits to three global GSD skill files at `~/.claude/` so the rules apply across all GSD projects, not just GeoLens.

Purpose: v1018 surfaced three documentation/process drift bugs that all slipped past code review and were only caught by sub-agents mid-audit (planner caught the paraphrased test names + the `tasks_common.py` path drift; integration checker caught the stale REQUIREMENTS.md checkbox). Tightening the authoring + executor workflow at the global skill level prevents recurrence across every future GSD-driven milestone.

Output: Four files — one new repo retro + three additively edited global skill files. No tag-cutting and no close-gate work in this plan (those are handled by Plan 1086-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/1086-process-tightening-close-gate/1086-CONTEXT.md
@.planning/milestones/v1018-MILESTONE-AUDIT.md
@CHANGELOG.md

<interfaces>
<!-- Exact node-IDs / paths / line numbers / commits the executor needs to cite verbatim in the retro and in the skill edits. -->
<!-- Cite ALL of these in the retro narrative; node-IDs and paths must match git grep before commit. -->

v1018 Incident 1 — TD-02/TD-03 paraphrased test names (REQ authoring drift):
- REQUIREMENTS.md (v1018) cited: `test_register_password_too_short` / `test_register_password_diversity`
- Actual tests in code: `test_register_emits_user_register_audit` / `test_register_disabled_does_not_emit_audit`
- Actual location: `backend/tests/test_phase_279_user_lifecycle.py` (line 131 and 187 per v1018 audit)
- Same fix shape (SEC-S16 12-char + 3/4 class password) applied successfully — drift was naming-only
- Reconciled in `.planning/audits/PYTEST-BASELINE-v1018.md` NEW-DISCOVERY table + CHANGELOG `[1.5.3]` TD-02/TD-03 entry
- Verbatim audit quote: "REQUIREMENTS.md TD-02/TD-03 test names ... do not exist in code — actual targets are test_register_emits_user_register_audit / test_register_disabled_does_not_emit_audit"

v1018 Incident 2 — tasks_common.py path + line drift (REQ authoring drift):
- v1017 audit cited: `backend/app/platform/jobs/tasks_common.py` lines 231 + 237
- Actual location: `backend/app/processing/ingest/tasks_common.py` lines 232 + 238
- Caught by the v1018 planner during Phase 1080 plan creation, before any executor touched the file
- Verbatim audit quote: "Planner caught (Phase 1080): tasks_common.py path drift in CONTEXT.md (platform/jobs -> processing/ingest); broad-except line numbers off-by-one (231/237 -> 232/238)"

v1018 Incident 3 — Plan 1081-02 SUMMARY checkbox-flip miss (executor workflow drift):
- Plan 1081-02 closed TD-05 in code (test mocks, test commit `9eccc80b`) but the SUMMARY commit did not flip REQUIREMENTS.md `[ ]` -> `[x]` for TD-05, nor did it flip the traceability-table row from Pending -> Complete
- Integration checker caught the stale row mid-audit
- Fixed inline at commit `5bf63166` (`docs(1081): mark TD-05 complete in REQUIREMENTS.md traceability`) — documentation-only, no functional impact
- Verbatim audit quote: "TD-05 REQUIREMENTS.md traceability row was stale at audit time (Plan 1081-02 SUMMARY commit did not flip [ ] -> [x]); fixed inline mid-audit (commit 5bf63166). Documentation-only — code/test/CHANGELOG all already showed TD-05 complete."

The three new rules these incidents motivate (use verbatim wording in the skill files):
1. gsd-planner rule:
   "REQ-cited test names MUST use exact path::TestClass::test_name node-ID form (e.g., `backend/tests/test_phase_279_user_lifecycle.py::TestRegisterAudit::test_register_emits_user_register_audit`). Before committing CONTEXT.md / REQUIREMENTS.md citations, the planner MUST validate each cited test exists by running `git grep -n 'def test_register_emits_user_register_audit' backend/tests/` (or equivalent) and confirming a single match. Production-code citations MUST include path + line (e.g., `backend/app/processing/ingest/tasks_common.py:232`); the planner MUST validate the cited line still contains the cited symbol by running `git grep -n '<symbol>' <path>` before commit."
2. gsd-executor rule:
   "Before the SUMMARY commit at the end of a plan, the executor MUST: (a) flip the REQUIREMENTS.md checkbox `[ ]` -> `[x]` for every requirement ID closed by this plan; (b) flip the matching traceability-table row from `Pending` -> `Complete`. This MUST happen in the SAME commit as the SUMMARY.md write — not a follow-up commit. If REQUIREMENTS.md doesn't exist or the requirement IDs aren't found, the executor MUST stop and surface the mismatch to the orchestrator rather than silently skipping the flip."
3. requirements.md template example:
   "Good requirements pin specific code-level targets in node-ID form when the requirement is test-shaped or production-code-shaped (e.g., AUTH-05 with `backend/tests/test_auth.py::TestAuthAudit::test_failed_login_emits_audit_event` or AUTH-06 with `backend/app/auth/validators.py:42`)."
</interfaces>
</context>

<read_first>
- /Users/ishiland/.claude/agents/gsd-planner.md (1278 lines; READ ENTIRE FILE so the additive edit can target the right section without rewriting existing content. The planner section to extend is `<context_fidelity>` because that block already discusses what the planner cites verbatim from CONTEXT.md / REQUIREMENTS.md. Use Edit, NOT Write.)
- /Users/ishiland/.claude/agents/gsd-executor.md (752 lines; READ ENTIRE FILE so the additive edit can target the right section. The executor section to extend is the SUMMARY-commit / plan-close workflow — the new section must sit immediately before `## PLAN COMPLETE` near line 724. Use Edit, NOT Write.)
- /Users/ishiland/.claude/get-shit-done/templates/requirements.md (231 lines; READ ENTIRE FILE so the additive example can be appended INSIDE the existing `<template>` block, between the existing `### Authentication` AUTH-01..04 examples and the `### [Category 2]` placeholder. Use Edit, NOT Write.)
- .planning/milestones/v1018-MILESTONE-AUDIT.md (the three incident quotes above are sourced from this file — re-read the `documentation_reconciliation` + `followup_observations` sections to capture verbatim phrasing for the retro narrative)
- .planning/REQUIREMENTS.md (TD-13 disposition + Traceability section)
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Write the v1019 process retro at .planning/retros/v1019-process.md</name>
  <files>.planning/retros/v1019-process.md</files>
  <action>Create the new file at `.planning/retros/v1019-process.md`. The retros/ directory does not exist yet — create it with `mkdir -p .planning/retros/` before writing. Use the Write tool for the file itself (this is a NEW file). The retro is project-scoped (GeoLens-specific) and is the canonical narrative for why the v1019 skill changes happened. Structure (use these exact section headings so the integration checker can grep them):

Required sections (in this order):
1. Frontmatter-free title line `# v1019 Process Retro: REQ Authoring Node-IDs + Executor SUMMARY Checkbox Flip`
2. Metadata block (Milestone, Phase, Requirement, Date — same shape as the v1018 milestone-audit)
3. `## Why This Retro Exists` — two sentences that name v1018 audit PASSED but surfaced three sub-agent-caught drift incidents, and say this retro encodes the rules they motivate
4. `## Incident 1: TD-02/TD-03 Paraphrased Test Names (REQ Authoring)` — narrative including: what REQUIREMENTS.md cited verbatim (`test_register_password_too_short` / `test_register_password_diversity`), what code actually contained verbatim (`test_register_emits_user_register_audit` / `test_register_disabled_does_not_emit_audit` at `backend/tests/test_phase_279_user_lifecycle.py:131,187`), who caught it (v1018 planner during Phase 1081), how it was reconciled (PYTEST-BASELINE-v1018.md NEW-DISCOVERY table + CHANGELOG [1.5.3]), and the cost (audit cycles spent untangling the naming drift). Then `### Rule Added` subsection naming the gsd-planner node-ID rule + the template example.
5. `## Incident 2: tasks_common.py Path + Line Drift (REQ Authoring)` — narrative including: what v1017 audit cited verbatim (`backend/app/platform/jobs/tasks_common.py` lines 231 + 237), what code actually contained verbatim (`backend/app/processing/ingest/tasks_common.py` lines 232 + 238), who caught it (v1018 planner during Phase 1080), and the cost. Then `### Rule Added` subsection naming the gsd-planner production-code path+line rule.
6. `## Incident 3: Plan 1081-02 SUMMARY Checkbox-Flip Miss (Executor Workflow)` — narrative including: Plan 1081-02 closed TD-05 in code at commit `9eccc80b` but did NOT flip REQUIREMENTS.md `[ ]` -> `[x]` for TD-05 nor the traceability row from Pending -> Complete; integration checker caught the stale row mid-audit; fixed inline at commit `5bf63166`; cost = documentation-only but would have cascaded into a misleading audit `requirements: 7/8`. Then `### Rule Added` subsection naming the gsd-executor SUMMARY-commit flip rule.
7. `## Cross-Reference: Where the Rules Live` — a markdown table with four rows (one row per rule), each row mapping Rule -> Global Skill File path -> Section name (e.g., `req_citation_pinning`, `requirements_traceability_flip`).
8. `## Verification` — two-line section noting that skill edits are gated by grep in Task 5 and the retro is the canonical project-scoped narrative.

Cite ALL three commits/paths/test-names VERBATIM as they appear in v1018-MILESTONE-AUDIT.md. Do not paraphrase — paraphrasing IS the bug this retro exists to fix.</action>
  <verify>
    <automated>test -f .planning/retros/v1019-process.md &amp;&amp; awk 'END { exit (NR &gt;= 60) ? 0 : 1 }' .planning/retros/v1019-process.md &amp;&amp; grep -q 'test_register_emits_user_register_audit' .planning/retros/v1019-process.md &amp;&amp; grep -q 'test_register_disabled_does_not_emit_audit' .planning/retros/v1019-process.md &amp;&amp; grep -q '5bf63166' .planning/retros/v1019-process.md &amp;&amp; grep -q 'processing/ingest/tasks_common.py' .planning/retros/v1019-process.md &amp;&amp; grep -q '232' .planning/retros/v1019-process.md &amp;&amp; grep -q '238' .planning/retros/v1019-process.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/retros/v1019-process.md` exists.
    - File is >= 60 lines (a paragraph-per-incident narrative is the floor; a one-paragraph retro is not the deliverable).
    - File names all three actual test names verbatim: `test_register_emits_user_register_audit` and `test_register_disabled_does_not_emit_audit`.
    - File names the actual `tasks_common.py` path (`processing/ingest`) and both actual line numbers (232 + 238) — not the v1017-audit-cited drift values (`platform/jobs`, 231/237).
    - File names the inline-fix commit hash `5bf63166`.
    - File has three named incidents matching the three above and one rule per incident with a cross-reference table to the skill file that encodes it.
  </acceptance_criteria>
  <done>The repo retro at `.planning/retros/v1019-process.md` exists, is >= 60 lines, names all three v1018 incidents with verbatim test names + paths + line numbers + commit hashes, and cross-references the three skill files modified in Tasks 2-4.</done>
</task>

<task type="auto">
  <name>Task 2: Append REQ-authoring node-ID rule to global gsd-planner skill</name>
  <files>/Users/ishiland/.claude/agents/gsd-planner.md</files>
  <action>EDIT (not Write) the existing `/Users/ishiland/.claude/agents/gsd-planner.md` — it's 1278 lines pre-edit. Add an additive rule. Locate the appropriate insertion point by reading the file first; the new rule belongs adjacent to the existing `<context_fidelity>` block (around line 52) because that's where the planner already discusses what it cites verbatim from CONTEXT.md / REQUIREMENTS.md. Do NOT rewrite the file — use Edit's old_string/new_string to surgically insert a new sibling block immediately after the closing `</context_fidelity>` tag.

The new section to insert (place this as a new top-level XML-tagged section, sibling to `<context_fidelity>`):

```
<req_citation_pinning>
## CRITICAL: REQ-Cited Code Targets Must Use Validated Node-IDs

When CONTEXT.md or REQUIREMENTS.md cites a specific test name or a specific production-code line, the planner MUST encode the citation as a validated node-ID, NOT as a paraphrase.

**Test name citations:** Use `path::TestClass::test_name` form, e.g.,
`backend/tests/test_phase_279_user_lifecycle.py::TestRegisterAudit::test_register_emits_user_register_audit`.

**Production-code citations:** Use `path:line` form with the symbol context noted, e.g.,
`backend/app/processing/ingest/tasks_common.py:232` (the first `except Exception:` clause inside `_job_phase_session`).

**Validation before commit:** Before committing CONTEXT.md or REQUIREMENTS.md, the planner MUST validate every cited target:
- For each test name: run `git grep -n "def <test_name>" <path>` and confirm a single match. Zero matches means the cited test does not exist (the v1018 TD-02/TD-03 drift pattern — the citation was a paraphrase, not a real symbol). Multiple matches means the citation is ambiguous and needs a fully-qualified class scope.
- For each production-code path:line: run `git grep -n "<expected symbol>" <path>` and confirm the cited line still contains the cited symbol. The v1018 `tasks_common.py` incident shows how 1-line drift between audit time and plan time can cascade into an executor citing a non-existent line.

**Origin:** v1018 surfaced three drift incidents that this rule prevents (see `.planning/retros/v1019-process.md` for the project-scoped narrative): TD-02/03 paraphrased test names, tasks_common.py path+line drift. The rule was added at v1019 close.
</req_citation_pinning>
```

Insert this block as a new top-level section IMMEDIATELY AFTER `</context_fidelity>`. The block must be VERBATIM as above — the acceptance gate greps for the exact phrase `path::TestClass::test_name`.</action>
  <verify>
    <automated>grep -q 'path::TestClass::test_name' /Users/ishiland/.claude/agents/gsd-planner.md &amp;&amp; grep -q 'req_citation_pinning' /Users/ishiland/.claude/agents/gsd-planner.md &amp;&amp; grep -q 'validated against' /Users/ishiland/.claude/agents/gsd-planner.md &amp;&amp; grep -q 'v1019-process.md' /Users/ishiland/.claude/agents/gsd-planner.md &amp;&amp; test $(wc -l &lt; /Users/ishiland/.claude/agents/gsd-planner.md) -gt 1278</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/ishiland/.claude/agents/gsd-planner.md` is now LONGER than its pre-edit baseline of 1278 lines (additive only, did not rewrite or shrink).
    - File contains the exact phrase `path::TestClass::test_name` (the node-ID schema).
    - File contains the new section marker `req_citation_pinning`.
    - File contains the phrase "validated against" (the enforcement language).
    - File cross-references `.planning/retros/v1019-process.md` so the project-scoped narrative is reachable from the global skill.
    - The rest of the file is byte-identical to its pre-edit state (additive constraint — no rewrites). Spot-check by greping for one stable pre-existing string like `<documentation_lookup>` or `<role>` and confirming the line still exists.
  </acceptance_criteria>
  <done>`/Users/ishiland/.claude/agents/gsd-planner.md` carries an additive `<req_citation_pinning>` section that names the node-ID form, the git-grep validation step, and cross-references the repo retro. File grew, did not shrink.</done>
</task>

<task type="auto">
  <name>Task 3: Append SUMMARY-commit checkbox-flip rule to global gsd-executor skill</name>
  <files>/Users/ishiland/.claude/agents/gsd-executor.md</files>
  <action>EDIT (not Write) the existing `/Users/ishiland/.claude/agents/gsd-executor.md` — it's 752 lines pre-edit. Add an additive rule. Read the file first to locate the SUMMARY-commit / plan-close workflow section. The file has sections at lines 321 (`## CHECKPOINT REACHED`), 374 (`## Plan-Level TDD Gate Enforcement`), 607 (`## Deviations from Plan`), 724 (`## PLAN COMPLETE`) — the new rule belongs IMMEDIATELY BEFORE `## PLAN COMPLETE` so it fires at exactly the moment the executor is preparing the SUMMARY commit.

Use Edit's old_string/new_string to surgically insert. Anchor on the `## PLAN COMPLETE` heading line as old_string; prepend the new block in new_string. Do NOT rewrite the file.

The new section to insert:

```
<requirements_traceability_flip>
## CRITICAL: Flip REQUIREMENTS.md Before SUMMARY Commit

Before staging the SUMMARY.md commit at the end of a plan, the executor MUST update `.planning/REQUIREMENTS.md` for every requirement ID closed by this plan:

1. **Checkbox flip:** Change `- [ ] **REQ-ID**: ...` to `- [x] **REQ-ID**: ...` in the requirements body.
2. **Traceability row flip:** Change the matching row in the `## Traceability` table from `Pending` to `Complete`.

Both edits MUST land in the SAME commit as the SUMMARY.md write — NOT a follow-up commit. If you discover after a SUMMARY commit that you missed the flip, write a new commit immediately (do not amend).

**Failure mode:** If `.planning/REQUIREMENTS.md` doesn't exist, or if the requirement IDs the plan claims to close aren't found in the file, STOP and surface the mismatch to the orchestrator. Do not silently skip the flip — the stale row will mislead the integration checker and the milestone audit.

**Origin:** v1018 Plan 1081-02 closed TD-05 in code but skipped this flip; the integration checker caught the stale row mid-audit and the flip landed as a documentation-only follow-up commit (`5bf63166`). The rule was added at v1019 close to make the flip part of the executor's standard plan-close workflow. See `.planning/retros/v1019-process.md` in the closing project for the project-scoped narrative.

**What to check:**
- Open `.planning/REQUIREMENTS.md` before staging the SUMMARY commit.
- For each requirement ID in the plan's frontmatter `requirements:` list, confirm the checkbox is `[x]` and the traceability row says `Complete`.
- Stage both `.planning/REQUIREMENTS.md` and the SUMMARY together with `git add`.
</requirements_traceability_flip>
```

Place this block as a new top-level XML-tagged section that sits IMMEDIATELY ABOVE the existing `## PLAN COMPLETE` heading.</action>
  <verify>
    <automated>grep -q 'requirements_traceability_flip' /Users/ishiland/.claude/agents/gsd-executor.md &amp;&amp; grep -q 'Flip REQUIREMENTS.md Before SUMMARY Commit' /Users/ishiland/.claude/agents/gsd-executor.md &amp;&amp; grep -q '5bf63166' /Users/ishiland/.claude/agents/gsd-executor.md &amp;&amp; grep -q 'Traceability row flip' /Users/ishiland/.claude/agents/gsd-executor.md &amp;&amp; test $(wc -l &lt; /Users/ishiland/.claude/agents/gsd-executor.md) -gt 752</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/ishiland/.claude/agents/gsd-executor.md` is now LONGER than its pre-edit baseline of 752 lines (additive only).
    - File contains the new section marker `requirements_traceability_flip`.
    - File contains the exact phrase `Flip REQUIREMENTS.md Before SUMMARY Commit`.
    - File contains the v1018 commit-hash reference `5bf63166` (so the cross-link to the repo retro is intact at the skill level).
    - File contains the phrase `Traceability row flip` (the second of the two required actions).
    - The rest of the file is byte-identical (additive constraint — spot-check by greping for one stable pre-existing string like `## PLAN COMPLETE`).
  </acceptance_criteria>
  <done>`/Users/ishiland/.claude/agents/gsd-executor.md` carries an additive `<requirements_traceability_flip>` section that names both required actions, the same-commit requirement, the failure mode, and cross-references commit `5bf63166`. File grew, did not shrink.</done>
</task>

<task type="auto">
  <name>Task 4: Append node-ID schema example to global requirements template</name>
  <files>/Users/ishiland/.claude/get-shit-done/templates/requirements.md</files>
  <action>EDIT (not Write) the existing `/Users/ishiland/.claude/get-shit-done/templates/requirements.md` — it's 231 lines pre-edit. Add an additive "Code-Pinned Examples" subsection that demonstrates node-ID-form citations. Read the file first to identify the insertion point: the file has a `<template>` block starting around line 5; inside, a `## v1 Requirements` section starts around line 13 with a `### Authentication` subsection containing AUTH-01..AUTH-04 examples (lines 17-22). The natural insertion point is INSIDE the `<template>` block, IMMEDIATELY AFTER the AUTH-01..AUTH-04 list but BEFORE the `### [Category 2]` heading, so anyone copy-pasting the template sees the node-ID pattern alongside the basic-shape examples.

Use Edit's old_string/new_string to surgically insert. Anchor on the AUTH-04 line as the end of the existing list; prepend the new subsection in new_string immediately after. Keep all surrounding template markup (the outer `<template>` tag, the backtick-fenced markdown block) intact.

The block to insert (verbatim — note the backtick-quoted node-IDs):

```
### Authentication — Code-Pinned Examples

When a requirement targets a specific test or specific production-code line, pin it in
node-ID form so the planner can validate the citation against `git grep` before commit
and the executor can run it directly. This prevents the "paraphrased test name" drift
pattern documented in v1019 (see `.planning/retros/v1019-process.md` in any project that
has run /gsd-complete-milestone v1019).

- [ ] **AUTH-05**: Failed login attempts emit a security audit event, pinned by
  `backend/tests/test_auth.py::TestAuthAudit::test_failed_login_emits_audit_event`
- [ ] **AUTH-06**: Password validation enforces a 12-char minimum + 3-of-4 character-class
  diversity, pinned by `backend/app/auth/validators.py:42` (the `MIN_PASSWORD_LENGTH`
  constant) and `backend/tests/test_password_policy.py::TestSEC_S16::test_min_length_enforced`
```</action>
  <verify>
    <automated>grep -q 'Code-Pinned Examples' /Users/ishiland/.claude/get-shit-done/templates/requirements.md &amp;&amp; grep -q 'test_failed_login_emits_audit_event' /Users/ishiland/.claude/get-shit-done/templates/requirements.md &amp;&amp; grep -q 'AUTH-05' /Users/ishiland/.claude/get-shit-done/templates/requirements.md &amp;&amp; grep -q 'AUTH-06' /Users/ishiland/.claude/get-shit-done/templates/requirements.md &amp;&amp; grep -q 'v1019-process.md' /Users/ishiland/.claude/get-shit-done/templates/requirements.md &amp;&amp; test $(wc -l &lt; /Users/ishiland/.claude/get-shit-done/templates/requirements.md) -gt 231</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/ishiland/.claude/get-shit-done/templates/requirements.md` is now LONGER than its pre-edit baseline of 231 lines (additive only).
    - File contains the new subsection heading `### Authentication — Code-Pinned Examples`.
    - File contains the test node-ID example string `test_failed_login_emits_audit_event`.
    - File contains both `AUTH-05` and `AUTH-06` example IDs.
    - File cross-references `.planning/retros/v1019-process.md`.
    - The rest of the file is byte-identical — spot-check by greping for the pre-existing AUTH-01 line.
  </acceptance_criteria>
  <done>`/Users/ishiland/.claude/get-shit-done/templates/requirements.md` carries an additive `### Authentication — Code-Pinned Examples` subsection inside the existing `<template>` block, with two example requirements pinning a test and a production-code line via node-ID form. File grew, did not shrink.</done>
</task>

<task type="auto">
  <name>Task 5: Final validation — retro + three skill files all cross-reference correctly</name>
  <files>.planning/retros/v1019-process.md, /Users/ishiland/.claude/agents/gsd-planner.md, /Users/ishiland/.claude/agents/gsd-executor.md, /Users/ishiland/.claude/get-shit-done/templates/requirements.md</files>
  <action>This is a verification-only task — no file writes. Run the integrity check that all four files are mutually consistent:

1. The retro names the three skill files in its cross-reference table — confirm each named skill-file path exists at the documented location with the new section marker present.
2. Each skill file's new section cross-references the retro at `.planning/retros/v1019-process.md` — confirm grep finds the reference.
3. The retro's three rule names match the skill-file section markers exactly:
   - Retro mentions `req_citation_pinning` -> gsd-planner.md must contain the marker
   - Retro mentions `requirements_traceability_flip` -> gsd-executor.md must contain the marker
   - Retro mentions `Code-Pinned Examples` -> templates/requirements.md must contain the heading

If any check fails, fix the inconsistent file (update the retro's cross-reference table to match actual section markers, or update the skill file's section marker to match the retro). Re-run the full automated grep gate below.</action>
  <verify>
    <automated>grep -q 'req_citation_pinning' .planning/retros/v1019-process.md &amp;&amp; grep -q 'req_citation_pinning' /Users/ishiland/.claude/agents/gsd-planner.md &amp;&amp; grep -q 'requirements_traceability_flip' .planning/retros/v1019-process.md &amp;&amp; grep -q 'requirements_traceability_flip' /Users/ishiland/.claude/agents/gsd-executor.md &amp;&amp; grep -q 'Code-Pinned Examples' .planning/retros/v1019-process.md &amp;&amp; grep -q 'Code-Pinned Examples' /Users/ishiland/.claude/get-shit-done/templates/requirements.md &amp;&amp; grep -q 'v1019-process.md' /Users/ishiland/.claude/agents/gsd-planner.md &amp;&amp; grep -q 'v1019-process.md' /Users/ishiland/.claude/agents/gsd-executor.md &amp;&amp; grep -q 'v1019-process.md' /Users/ishiland/.claude/get-shit-done/templates/requirements.md</automated>
  </verify>
  <acceptance_criteria>
    - All four files mutually reference each other: retro names all three skill section markers; each skill file references the retro by relative path.
    - The grep gate exits 0 — there is a transitive closure of cross-references binding the retro and the three skill files.
  </acceptance_criteria>
  <done>The four-file system is internally consistent: every cross-reference resolves, every section marker matches between the retro's table and the skill files. No file rewrites were needed beyond surgical Edit operations.</done>
</task>

</tasks>

<verification>
After all five tasks complete:

```bash
# All four files exist and contain the required markers
test -f .planning/retros/v1019-process.md
test -f /Users/ishiland/.claude/agents/gsd-planner.md
test -f /Users/ishiland/.claude/agents/gsd-executor.md
test -f /Users/ishiland/.claude/get-shit-done/templates/requirements.md

# Section markers present
grep -q 'req_citation_pinning' /Users/ishiland/.claude/agents/gsd-planner.md
grep -q 'requirements_traceability_flip' /Users/ishiland/.claude/agents/gsd-executor.md
grep -q 'Code-Pinned Examples' /Users/ishiland/.claude/get-shit-done/templates/requirements.md

# Retro narrative present
grep -q 'test_register_emits_user_register_audit' .planning/retros/v1019-process.md
grep -q '5bf63166' .planning/retros/v1019-process.md
grep -q 'processing/ingest/tasks_common.py' .planning/retros/v1019-process.md

# REQUIREMENTS.md flip happens at SUMMARY-commit time per the executor's new rule
grep -q '\- \[x\] \*\*TD-13\*\*' .planning/REQUIREMENTS.md
grep -E 'TD-13.*\|.*Phase 1086.*\|.*Complete' .planning/REQUIREMENTS.md
```
</verification>

<success_criteria>
- TD-13 is closed: the four-file deliverable exists, internally consistent, with every cross-reference resolving.
- The retro at `.planning/retros/v1019-process.md` is the canonical project-scoped narrative.
- The three global skill files at `~/.claude/` are extended (not rewritten) with the named rules and will apply to every future GSD-driven project.
- REQUIREMENTS.md TD-13 row is flipped to `[x]` + `Complete` in the same commit as this plan's SUMMARY (per the new executor rule this plan establishes — self-referentially: this is the first plan whose executor must obey the new rule).
</success_criteria>

<output>
Create `.planning/phases/1086-process-tightening-close-gate/1086-01-SUMMARY.md` when done. Cite all four file paths, the section markers added to each, and confirm the cross-reference grep gate passed. Include the TD-13 disposition row showing `Complete`.
</output>
