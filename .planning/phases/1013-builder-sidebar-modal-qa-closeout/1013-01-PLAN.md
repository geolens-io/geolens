---
phase: 1013-builder-sidebar-modal-qa-closeout
plan: 01
wave: 1
depends_on: []
autonomous: true
requirements: [QA-01, QA-02, QA-03, QA-04, QA-05, QA-06]
subsystem: frontend-builder
tags: [map-builder, qa, playwright, accessibility, tests]
files_modified:
  - e2e/builder.spec.ts
  - e2e/accessibility.spec.ts
  - .planning/phases/1013-builder-sidebar-modal-qa-closeout/1013-01-SUMMARY.md
---

<objective>
Close the v1002 milestone by aligning browser QA with the redesigned sidebar/Add Dataset UI, running focused unit/build gates, and documenting any environment-specific E2E limitations.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1013-builder-sidebar-modal-qa-closeout/1013-CONTEXT.md
@frontend/src/components/builder/__tests__/renderAs.test.ts
@frontend/src/components/builder/__tests__/MapStackPanel.test.tsx
@frontend/src/components/builder/__tests__/DatasetSearchPanel.test.tsx
@frontend/src/components/builder/hooks/__tests__/use-builder-layers.test.ts
@e2e/builder.spec.ts
@e2e/accessibility.spec.ts
</context>

<tasks>
  <task type="auto">
    <name>Align builder Playwright checks with the redesigned sidebar/modal</name>
    <files>e2e/builder.spec.ts</files>
    <action>Update stale basemap assertions to the inline swap popover and expand Add Dataset modal coverage across desktop/tablet tabs and footer actions.</action>
    <verify>Builder Playwright spec can locate the new accessible controls.</verify>
    <done>QA-05 browser coverage matches the current UI contract.</done>
  </task>

  <task type="auto">
    <name>Add modal accessibility coverage</name>
    <files>e2e/accessibility.spec.ts</files>
    <action>Add an axe scan for the Add Dataset dialog opened from the builder page.</action>
    <verify>Focused accessibility run has a dedicated modal check.</verify>
    <done>Add Dataset modal is not only covered by component tests.</done>
  </task>

  <task type="auto">
    <name>Run focused frontend gates</name>
    <files>frontend test/build outputs only</files>
    <action>Run focused Vitest suites, lint, build, and browser checks where the local stack supports them.</action>
    <verify>Record exact commands and pass/fail status in the phase summary.</verify>
    <done>QA-01..06 are backed by concrete verification evidence.</done>
  </task>

  <task type="auto">
    <name>Close planning state</name>
    <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/1013-builder-sidebar-modal-qa-closeout/1013-01-SUMMARY.md</files>
    <action>Mark QA requirements and Phase 1013 complete after verification; prepare the milestone for audit/archive.</action>
    <verify>Planning state accurately reflects shipped code and any documented E2E caveats.</verify>
    <done>v1002 is ready for milestone audit.</done>
  </task>
</tasks>

<verification>
- `cd frontend && npm run test -- DatasetSearchPanel MapStackPanel map-stack renderAs use-builder-layers --run`
- `cd frontend && npm run lint`
- `cd frontend && npm run build`
- `npm run e2e:smoke:builder` or focused `npx playwright test` subset if the stack is available
- Playwright MCP desktop/tablet snapshot of builder sidebar/Add Dataset modal if the stack is reachable
</verification>

<must_haves>
- No schema, migration, renderer, or persisted-shape changes.
- No broad E2E repair beyond selectors or checks directly invalidated by the redesign.
- No Carbon or new UI primitive dependency.
</must_haves>

<success_criteria>
- [ ] Existing focused unit/component suites pass and cover QA-01..04.
- [ ] Builder Playwright checks target the new row/modal controls.
- [ ] Add Dataset modal has browser-level responsive and a11y coverage.
- [ ] Focused lint/build pass.
- [ ] Any broader smoke limitation is documented with a clear owner/scope.
</success_criteria>
