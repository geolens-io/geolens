---
phase: 1054-seeder-console-route-import-polish
plan: "11"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - "UX-01 ('API Keys workflow is reachable from login within 1-2 clicks via primary admin nav, OR is signposted from the seeder docs so new users running `seed-ago-data.py` know exactly where to mint a key') is satisfied by Phase 1053 DOC-02 — no in-app navigation change is needed for this requirement."
    - "Phase 1053 commit `30e9361` in `~/Code/getgeolens.com` adds a `### Create your first API key` subsection under `## 4. Seed sample data` in the quickstart with a 5-step recipe: sign in → gear icon → API Keys tab → Create Key → copy immediately. The recipe is positioned adjacent to the seed-ago-data.py usage block so users reading the seeder docs see exactly where to mint a key."
    - "The audit's UX-01 finding states the 3-click discovery path (login → avatar → Settings → API Keys tab → Create Key) is acceptable UX but 'not signposted from the seeder docs or the quickstart.' Phase 1053 closes the signposting gap."
    - "No code, test, or i18n change is required for this requirement in Phase 1054."
  artifacts: []
  key_links:
    - from: "this plan's SUMMARY"
      to: ".planning/phases/1053-quickstart-docs-environment-hardening/1053-03-SUMMARY.md"
      via: "cross-reference to Phase 1053 DOC-02 closure"
      pattern: "1053-03"
---

<objective>
UX-01 closes as a zero-code requirement. The audit's recommendation has two paths — (a) add an in-app sidebar nav item for API Keys to shorten the 3-click discovery path, or (b) signpost the API-key-creation workflow from the seeder docs that a new user is most likely consulting. Per CONTEXT.md's locked decision: "**(b) is already satisfied by Phase 1053 DOC-02.** UX-01 may be auto-resolved. Planner should verify by reading the Phase 1053 DOC-02 SUMMARY and decide whether UX-01 needs additional work or is closeable as 'satisfied by Phase 1053'."

Verification against Phase 1053 DOC-02 SUMMARY (`.planning/phases/1053-quickstart-docs-environment-hardening/1053-03-SUMMARY.md`):

> Task 2 — DOC-02: Create your first API key subsection
>
> Added `### Create your first API key` subsection after the "Seed live ArcGIS Online data" block. Contains:
> - 5-step numbered recipe: sign in → gear icon → API Keys tab → Create Key → copy immediately
> - One-shot display caveat ("shown once in a confirmation modal")
> - Both `GEOLENS_API_KEY` env var (Option A) and `--api-key` flag (Option B) usage patterns
> - Tip Aside explaining admin-user key inherits admin permissions (relevant for ingestion)

The DOC-02 recipe is positioned **adjacent to the seed-ago-data.py usage block** — exactly where a user running the seeder will look. The `#create-your-first-api-key` anchor resolves correctly (verified in Phase 1053 SUMMARY's "Anchor Resolution" section). A new user following the quickstart finds the API-key workflow inline with the seeder docs they were already reading.

Per UX-01's requirement language: "**Discovery requirement, not a UI relocation requirement.**" The signposting path closes it.

Purpose: Formally close UX-01 with a zero-work SUMMARY that cross-references Phase 1053's commit. This plan ships nothing — its only artifact is the SUMMARY document confirming the requirement is satisfied.
Output: A SUMMARY document only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/1054-seeder-console-route-import-polish/1054-CONTEXT.md

# Source-of-truth: audit's UX-01 section + CONTEXT.md UX-01 decision space
@.planning/M001-7n8vpc-dry-run-audit.md

# Phase 1053 DOC-02 closure evidence — the cross-repo commit reference
@.planning/phases/1053-quickstart-docs-environment-hardening/1053-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Phase 1053 DOC-02 satisfies UX-01 and write the closure SUMMARY</name>
  <files>.planning/phases/1054-seeder-console-route-import-polish/1054-11-SUMMARY.md</files>
  <action>
    Part A — Verification. Read `.planning/phases/1053-quickstart-docs-environment-hardening/1053-03-SUMMARY.md` and confirm the following are present:

    1. The cross-repo commit SHA `30e9361` exists and is on `main` of `~/Code/getgeolens.com`.
    2. The commit adds a `### Create your first API key` subsection.
    3. The subsection contains a 5-step recipe matching the audit's UX-01 click-path observation (sign in → gear icon / Settings → API Keys tab → Create Key → copy).
    4. The anchor `#create-your-first-api-key` resolves under `## 4. Seed sample data` (so seeder-docs readers see it inline).

    If any of these are missing or have drifted, STOP and surface the gap — do NOT write the SUMMARY claiming closure. In that case, this plan would need to be replaced with one that adds an in-app nav surface (option a).

    Part B — Write the SUMMARY at `.planning/phases/1054-seeder-console-route-import-polish/1054-11-SUMMARY.md`.

    Required content shape (use prose, not just a checklist):

    ```markdown
    ---
    phase: 1054-seeder-console-route-import-polish
    plan: "11"
    subsystem: closure-cross-reference
    tags: [docs, api-key, ux-discovery, cross-repo]
    dependency_graph:
      requires: [1053-03]
      provides: [UX-01]
      affects: []
    tech_stack:
      added: []
      patterns: []
    key_files:
      created: []
      modified: []
    decisions:
      - "UX-01 closed as zero-code via cross-reference to Phase 1053 DOC-02 (commit `30e9361` in `~/Code/getgeolens.com`)"
      - "Discovery-by-docs path (option b from audit) chosen over in-app nav addition (option a) per CONTEXT.md locked decision"
    metrics:
      duration: "~5 minutes (verification + SUMMARY write)"
      completed: "(set at write time)"
      tasks_completed: 1
      files_changed: 0
    ---

    # Phase 1054 Plan 11: UX-01 Closure — Satisfied by Phase 1053 DOC-02

    ## Closure Reasoning

    UX-01's requirement language: ...

    The audit's observation: ...

    Phase 1053 DOC-02 closed the signposting gap via cross-repo commit `30e9361` ...

    ## Verification Evidence

    From `.planning/phases/1053-quickstart-docs-environment-hardening/1053-03-SUMMARY.md`:
    - Quote the Task 2 — DOC-02 section verbatim
    - Quote the Anchor Resolution section verbatim

    ## What Was NOT Done

    - No in-app navigation change (CONTEXT.md decision)
    - No new sidebar item (option a deferred — not needed because option b satisfies)
    - No backend or frontend code change
    - No i18n change

    ## Requirements Closed

    - **UX-01**: Closed via cross-reference. No additional work required in Phase 1054.
    ```

    The SUMMARY is the ONLY artifact this plan produces. No other files are touched.
  </action>
  <verify>
    <automated>test -f .planning/phases/1054-seeder-console-route-import-polish/1054-11-SUMMARY.md && grep -q "UX-01" .planning/phases/1054-seeder-console-route-import-polish/1054-11-SUMMARY.md && grep -q "30e9361" .planning/phases/1054-seeder-console-route-import-polish/1054-11-SUMMARY.md && echo "SUMMARY exists with UX-01 closure + cross-repo commit reference"</automated>
  </verify>
  <done>The SUMMARY file exists at the standard path. It cross-references Phase 1053 DOC-02 commit `30e9361`. It records UX-01 as closed with zero code change. No other files in the repo were modified by this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No code change → no new trust boundaries.

## STRIDE Threat Register

No threats in scope: this plan is a documentation-only closure. No new attack surface.
</threat_model>

<verification>
1. `test -f .planning/phases/1054-seeder-console-route-import-polish/1054-11-SUMMARY.md` — file exists.
2. `git diff --stat` after this plan should show only the new SUMMARY file (and possibly the ROADMAP update done by the planner — which is a different operation).
3. (No live verification needed — this is a doc cross-reference, not a behavior change.)
</verification>

<success_criteria>
- SUMMARY file at `.planning/phases/1054-seeder-console-route-import-polish/1054-11-SUMMARY.md` exists.
- SUMMARY cross-references Phase 1053 DOC-02 commit SHA `30e9361`.
- SUMMARY records UX-01 as closed via the "satisfied by Phase 1053" cross-reference path.
- Zero source code change in this plan.
</success_criteria>

<output>
This plan IS its SUMMARY. The Task 1 action above writes the SUMMARY directly as the deliverable.
</output>
