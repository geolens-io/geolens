---
phase: 1011-basemap-and-terrain-inline-rows
plan: 01
wave: 1
depends_on: []
autonomous: true
requirements: [BASE-01, BASE-02, BASE-03, BASE-04, TERRAIN-01, TERRAIN-02]
subsystem: frontend-builder
tags: [map-builder, basemap, terrain, sidebar, tests]
files_modified:
  - frontend/src/components/builder/map-stack.ts
  - frontend/src/components/builder/MapStackPanel.tsx
  - frontend/src/components/builder/MapStackItem.tsx
  - frontend/src/components/builder/__tests__/map-stack.test.ts
  - frontend/src/components/builder/__tests__/MapStackPanel.test.tsx
  - .planning/phases/1011-basemap-and-terrain-inline-rows/1011-01-SUMMARY.md
---

<objective>
Inline basemap and terrain controls in the stack sidebar while keeping all writes constrained to existing map-level fields.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1011-basemap-and-terrain-inline-rows/1011-CONTEXT.md
@frontend/src/components/builder/map-stack.ts
@frontend/src/components/builder/MapStackPanel.tsx
@frontend/src/components/builder/MapStackItem.tsx
@frontend/src/components/builder/BasemapAppearanceControls.tsx
@frontend/src/components/builder/TerrainControls.tsx
@frontend/src/lib/basemap-utils.ts
</context>

<tasks>
  <task type="auto">
    <name>Promote the current basemap label into the stack row</name>
    <files>frontend/src/components/builder/map-stack.ts, frontend/src/components/builder/MapStackPanel.tsx</files>
    <action>Resolve the current `BasemapEntry.label`, pass it into `buildMapStack`, and render the basemap row with that title.</action>
    <verify>Tests assert the basemap row is named from the registry and no basemap layer rows are created.</verify>
    <done>BASE-01 has a stable view-model contract.</done>
  </task>

  <task type="auto">
    <name>Inline basemap controls</name>
    <files>frontend/src/components/builder/MapStackPanel.tsx, frontend/src/components/builder/__tests__/MapStackPanel.test.tsx</files>
    <action>Replace the separate basemap picker/panel with row-adjacent sublayer controls plus swap/reset actions backed by existing `BasemapEntry` and `basemap_config` helpers.</action>
    <verify>Tests prove swap and reset call only existing basemap handlers with normalized config.</verify>
    <done>BASE-02..04 are satisfied without persisted presets.</done>
  </task>

  <task type="auto">
    <name>Move terrain into Relief</name>
    <files>frontend/src/components/builder/map-stack.ts, frontend/src/components/builder/MapStackPanel.tsx, frontend/src/components/builder/__tests__/map-stack.test.ts</files>
    <action>Derive the map-level terrain row inside the `relief` group and render existing `TerrainControls` there.</action>
    <verify>Tests assert terrain metadata remains map-level and the surface group contains only the foundation row.</verify>
    <done>TERRAIN-01 matches the sidebar IA.</done>
  </task>

  <task type="auto">
    <name>Add DEM Use as terrain action</name>
    <files>frontend/src/components/builder/MapStackItem.tsx, frontend/src/components/builder/MapStackPanel.tsx, frontend/src/components/builder/__tests__/MapStackPanel.test.tsx</files>
    <action>Add a raster DEM-only row overflow action that calls `onTerrainChange` with the layer's `dataset_id` and existing/default terrain values.</action>
    <verify>Tests assert the action exists for DEM rows, updates `terrain_config`, and does not route through layer mutation handlers.</verify>
    <done>TERRAIN-02 is satisfied.</done>
  </task>

  <task type="auto">
    <name>Record summary and verification</name>
    <files>.planning/phases/1011-basemap-and-terrain-inline-rows/1011-01-SUMMARY.md</files>
    <action>Document implementation, requirement coverage, and verification commands/results.</action>
    <verify>Summary exists and matches commands run.</verify>
    <done>Phase 1011 can hand off to the Add Dataset modal redesign.</done>
  </task>
</tasks>

<verification>
- `cd frontend && npm run test -- MapStackPanel map-stack renderAs use-builder-layers --run`
- `cd frontend && npm run lint`
- `cd frontend && npm run build`
</verification>

<must_haves>
- No new persisted fields or tables.
- No basemap `MapLayer` rows for sublayers.
- Basemap writes only use `basemap_style`, `show_basemap_labels`, and `basemap_config`.
- Terrain writes only use `terrain_config`.
- No saved basemap presets or Add Dataset modal work in this phase.
</must_haves>

<success_criteria>
- [x] Basemap group renders one current-basemap row named from `BasemapEntry.label`.
- [x] Basemap swap uses the enabled registry and reset uses config normalization.
- [x] Terrain row and controls appear in `relief` and preserve map-level writes.
- [x] Raster DEM rows expose `Use as terrain`.
- [x] Focused tests, lint, and build pass.
</success_criteria>
