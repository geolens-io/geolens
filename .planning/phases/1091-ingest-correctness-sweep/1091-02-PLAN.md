---
phase: 1091-ingest-correctness-sweep
plan: 02
type: execute
wave: 2
depends_on: ["1091-01"]
files_modified:
  - backend/app/processing/ingest/tasks_common.py
  - backend/tests/test_quicklook_async_context.py
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [INGEST-01]
must_haves:
  truths:
    - "The fix proposed in `.planning/audits/INGEST-QUICKLOOK-ASYNC-CONTEXT-v1021.md` Section 3 is applied to `backend/app/processing/ingest/tasks_common.py` at the line range named there."
    - "A regression test at `backend/tests/test_quicklook_async_context.py` reproduces the original `MissingGreenlet` shape under a pre-fix simulation AND passes under the fix (positive-form assertion that the post-commit quicklook block completes without raising)."
    - "After a clean `docker compose down -v && up -d --build` rebuild + `scripts/seed-natural-earth.py` run, `/api/admin/jobs/?status=failed` returns zero rows."
    - "After the same clean rebuild + seed, the `urban_areas_landscan_10m` dataset has a non-null `quicklook_256_uri`."
    - "Sequential pytest baseline `3047/0/38` is preserved (or improved if new passing tests added)."
    - "REQUIREMENTS.md INGEST-01 row flips `Pending` → `Complete` and `[ ]` → `[x]` in the SAME commit as the SUMMARY.md write per v1019 TD-13 atomic-flip rule. Traceability cell cites the new test node-ID."
  artifacts:
    - path: "backend/app/processing/ingest/tasks_common.py"
      provides: "The quicklook async-context boundary fix"
      contains: "_generate_quicklook"
    - path: "backend/tests/test_quicklook_async_context.py"
      provides: "Regression test pinning the MissingGreenlet shape (XFAIL or monkeypatched negative-form) AND the post-fix positive-form behavior"
      exports: ["test_quicklook_commit_completes_without_missing_greenlet"]
    - path: ".planning/REQUIREMENTS.md"
      provides: "INGEST-01 traceability row updated"
      contains: "INGEST-01"
  key_links:
    - from: "backend/tests/test_quicklook_async_context.py"
      to: "backend/app/processing/ingest/tasks_common.py"
      via: "import + direct call to `_generate_quicklook`"
      pattern: "from app.processing.ingest.tasks_common import"
    - from: ".planning/REQUIREMENTS.md"
      to: "backend/tests/test_quicklook_async_context.py"
      via: "INGEST-01 traceability cell cites the test node-ID"
      pattern: "test_quicklook_async_context\\.py::"
---

<objective>
Apply the fix proposed in `.planning/audits/INGEST-QUICKLOOK-ASYNC-CONTEXT-v1021.md` (Section 3) to `backend/app/processing/ingest/tasks_common.py` and add a regression test at `backend/tests/test_quicklook_async_context.py` that pins both the bug shape (negative-form, against simulated pre-fix code) and the fixed behavior (positive-form, against the live post-fix code).

**Purpose:** Close INGEST-01 by eliminating the `MissingGreenlet: greenlet_spawn has not been called` failure on the post-commit quicklook block for `urban_areas_landscan_10m` and the 0..N other datasets that may share the same bug shape under different feature counts / geometry types. The fix is targeted at the async-context-boundary line only — broader `tasks_common.py` refactor is out of scope per REQUIREMENTS.md INGEST-01.

**Output:** Code edit + new test file + REQUIREMENTS.md traceability flip + SUMMARY.md in one atomic close commit (TD-13 contract). Live docker-rebuild verification on a clean stack confirms zero `status=failed` rows and a non-null quicklook URI on the previously-failing dataset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/1091-ingest-correctness-sweep/1091-CONTEXT.md
@.planning/audits/INGEST-QUICKLOOK-ASYNC-CONTEXT-v1021.md
@.planning/phases/1091-ingest-correctness-sweep/1091-01-SUMMARY.md

<interfaces>
Source surfaces this plan touches. Executor uses the audit doc's Section 3 line range as the authoritative location to edit; the citations below are anchors for what should be near the change.

From `backend/app/processing/ingest/tasks_common.py`:
- `_generate_quicklook(session, dataset, table_name, geometry_type) -> None` at lines 627-672. Current shape: two nested try/except blocks (generate phase + commit phase), broad Exception catch on each, structlog warning on failure, no re-raise (non-fatal). The audit doc Section 3 names the exact line(s) to change.
- `_finalize_ingest(ctx) -> Dataset` at lines 675-837. Calls `_generate_quicklook` at line 826 (or thereabouts; line may shift slightly if the fix changes preceding logic). The post-`session.commit()` at line 822 is the architectural boundary the fix is aware of.
- The module-level `async_session` import at lines 215-217 (inside `_job_phase_session` for now). If the fix shape opens a fresh session for the quicklook block, the import may need to be hoisted to module top or kept lazy — follow the existing pattern in the file.

From `backend/app/core/db/session.py`:
- `async_session = async_sessionmaker(engine, expire_on_commit=False)` at line 26. The fix may consume `async_session()` as an async-context-manager.

From `backend/tests/conftest.py` (referenced — do not modify):
- `test_db_session` fixture (per-test isolated session, line ~844). The new regression test will use this fixture, mirroring the pattern in `backend/tests/test_tasks_common_phase_brackets.py:31-69`.
- `client` fixture (HTTP client for integration assertions, if the regression test goes that deep).

From `backend/tests/test_tasks_common_phase_brackets.py` (referenced — pattern source):
- Shape of `_get_admin_id`, `_create_pending_job`, `_select_status` helpers. The new test file may copy or import these helpers (preferred: import to keep DRY, but file-local copy is acceptable if it avoids fixture coupling).

From `backend/tests/test_quicklook_predicate.py` (referenced — pattern source):
- Shape of `@pytest.mark.anyio`-decorated `async def test_...(client, test_db_session)` test functions. Tests in this file create Dataset rows directly via the session and assert on the predicate. The new test file uses the same fixture + test-shape vocabulary.

From `.planning/audits/INGEST-QUICKLOOK-ASYNC-CONTEXT-v1021.md` (REQUIRED input):
- Section 3 "Root Cause + Proposed Fix" — the executor MUST read this section verbatim and translate it into the code edit. If the audit's proposed fix shape is unclear or appears wrong after read, halt and surface a `<gap>` to the operator BEFORE editing code. Spike-driven planning means we honor the spike's verdict; we do not silently second-guess it during the fix.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply the audit-proposed fix to tasks_common.py and add the regression test</name>
  <files>backend/app/processing/ingest/tasks_common.py, backend/tests/test_quicklook_async_context.py</files>
  <behavior>
    - **Negative-form pin (bug shape):** With the original buggy code restored or simulated (monkeypatch the offending line back, OR use a fresh helper that mirrors the pre-fix call path), calling the quicklook path against a multipolygon dataset with >1000 features raises `MissingGreenlet` (or the audit-named exception class). Use `pytest.raises(sqlalchemy.exc.MissingGreenlet)` or `pytest.raises(Exception, match="greenlet_spawn")`. Mark as `@pytest.mark.xfail(strict=True, raises=MissingGreenlet, reason="pre-fix shape — pins the bug we just closed")` if monkeypatching is infeasible; XFAIL with strict=True still serves as a regression pin because pytest fails the test if the bug stops reproducing.
    - **Positive-form pin (fix works):** Calling `_generate_quicklook(session, dataset, table_name, geometry_type)` against a freshly-committed Dataset on a real test table with multipolygon geometry (loaded from the existing test factories OR a minimal CREATE TABLE in the test setup) completes without raising and sets `dataset.quicklook_256_uri` to a non-empty string. If the fix uses a fresh session, assert the parent session passed in is left untouched (e.g., the parent session's commit-state is not perturbed by the quicklook block).
    - **Edge case (relationship safety):** After the fixed `_generate_quicklook` returns, `dataset.record` is still accessible without triggering a lazy-load failure. (Guards H3 from the spike — if the fix involves a fresh session, the parent session's ORM identity map must still hold the relationship.)
    - **Concurrency (deferred — clear via audit, do NOT add up-front test):** This regression test exercises a SINGLE quicklook call. It does NOT exercise 3× concurrent quicklook against the asyncpg pool `max_size=3` (the production seed runs at `asyncio.Semaphore(3)`). The audit doc Section 3 RISKS clause (1091-01 Task 3 step 4) MUST clear pool-sizing for the chosen fix shape — if the fix adds an extra checkout per ingest, the audit either names a pool bump OR proves headroom. If the audit accepts the risk explicitly, this test stays single-call. If the audit identifies a real contention shape, ADD a 3× concurrent test then (not now — premature otherwise).
  </behavior>
  <action>
1. **Read** `.planning/audits/INGEST-QUICKLOOK-ASYNC-CONTEXT-v1021.md` Section 3 in full. Extract: (a) the exact file:line range to change in `tasks_common.py`, (b) the literal fix shape (what changes from / to), (c) the named regression test functions.
2. **Apply** the fix to `backend/app/processing/ingest/tasks_common.py` at the line range named in the audit. Common shapes the audit may name (executor implements whichever the audit specifies):
   - **Shape A — fresh-session-for-quicklook:** Wrap the call site at tasks_common.py:826 (the `await _generate_quicklook(...)` invocation inside `_finalize_ingest`) in `async with async_session() as ql_session:` and pass `ql_session` instead of the parent `session`. Re-fetch the Dataset row (`await ql_session.get(Dataset, dataset.id)`) inside the fresh session before passing it to `_generate_quicklook`. This isolates a potentially-poisoned cursor on the parent session from the quicklook work.
   - **Shape B — rollback-before-quicklook:** Insert `await session.rollback()` after the commit at tasks_common.py:822 and before the `_generate_quicklook` call at line 826 — explicit rollback flushes any stale transaction state. (Less likely shape; rollback after a successful commit is usually a no-op but may clear cursor state.)
   - **Shape C — defensive try/except around the broad commit-phase block in `_generate_quicklook`:** Catch `MissingGreenlet` specifically (or a broader `Exception` if not narrowable) inside the existing line-661-672 try block, log it, and return rather than letting it propagate. (This is the LEAST satisfying fix — it papers over the bug rather than closing it. The audit likely rules this out.)
   - Shape names are illustrative — implement whatever the audit Section 3 names. If the audit names a SHAPE not listed here, follow the audit literally.
3. **Create** `backend/tests/test_quicklook_async_context.py` with:
   - Module docstring citing INGEST-01 and the audit doc path.
   - Imports: `pytest`, `from app.processing.ingest.tasks_common import _generate_quicklook` (or whichever function the audit pins), `from app.platform.jobs.models import IngestJob` if needed for context, plus model imports for the Dataset ORM.
   - The 2-3 test functions named in the audit. At minimum: `test_quicklook_commit_completes_without_missing_greenlet(test_db_session)` (positive-form). Optional: `test_quicklook_xfail_under_simulated_prefix_path(test_db_session)` marked `@pytest.mark.xfail(strict=True)` (negative-form / bug-shape pin).
   - Each test follows the @pytest.mark.anyio + async def shape used by `backend/tests/test_quicklook_predicate.py` and `backend/tests/test_tasks_common_phase_brackets.py`.
   - The positive-form test sets up a real Dataset + a real PostGIS table with >100 multipolygon rows (using SQL `INSERT INTO data.<table> SELECT ... FROM generate_series` or similar — the existing test fixtures may already provide a helper). Asserts: `_generate_quicklook(...)` returns None (no exception); the dataset.quicklook_256_uri attribute is set OR is left None+logged (depending on whether the test storage backend supports the put() call — match the existing pattern in `test_quicklook_predicate.py`).
4. **Run the new test in isolation** to confirm green: `cd backend && uv run pytest tests/test_quicklook_async_context.py -xvs`. If the test fails, do not commit — fix until green.
5. **Run the full pytest suite sequentially** to confirm no regression: `cd backend && uv run pytest -n 4 tests/`. Target: 3047 + N new tests passed, 0 failed, 38 skipped (or higher pass count if new tests added; the 0 failed is non-negotiable per HARD INVARIANT in CONTEXT.md).
6. If pool size in tasks_common.py:70-76 (`min_size=1, max_size=3`) is a concern flagged by the audit (Shape A uses an additional checkout per ingest), document the headroom calculation: seed runs at `asyncio.Semaphore(3)` (scripts/seed-natural-earth.py:977), 3 concurrent quicklook blocks × 1 extra session each = 3 extra checkouts. The pool max is 3, so the fix likely needs either pool sizing bump OR the fresh session uses a different pool (the test-engine NullPool doesn't apply in production). The audit Section 3 must commit to a position on this — re-read if unclear.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/test_quicklook_async_context.py -xvs &amp;&amp; uv run pytest -n 4 tests/ --tb=no -q</automated>
  </verify>
  <done>
    - `backend/app/processing/ingest/tasks_common.py` modified per audit Section 3.
    - `backend/tests/test_quicklook_async_context.py` exists, runs green in isolation, and contains at least one positive-form `pytest.mark.anyio async def` test named per audit Section 3.
    - Full `pytest -n 4 tests/` shows `failed=0` (3047/0/38 baseline preserved or improved with new passing tests).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The async-context-boundary fix is applied to `_generate_quicklook` / `_finalize_ingest` in `tasks_common.py`, and the regression test in `backend/tests/test_quicklook_async_context.py` passes. Now we need to confirm the fix closes the bug on the live failing dataset by rebuilding the docker stack from scratch and re-running the canonical seed.
  </what-built>
  <how-to-verify>
1. **Tear down and rebuild**:
   ```bash
   cd /Users/ishiland/Code/geolens
   docker compose down -v
   docker compose up -d --build
   ```
   Wait ~3 minutes for all services healthy. Confirm with `docker compose ps -a` — db/api/worker/titiler/frontend healthy, migrate exited 0.
2. **Run the canonical seed**:
   ```bash
   python3 scripts/seed-natural-earth.py --base-url http://localhost:8080 --username admin --password admin
   ```
   Expected: "Succeeded: 109, Failed: 0" at the end.
3. **Mint admin JWT** (no trailing slash on `/api/auth/login`):
   ```bash
   TOKEN=$(curl -s -X POST http://localhost:8080/api/auth/login -d 'username=admin&password=admin' | python3 -c "import json,sys; print(json.load(sys.stdin)['access_token'])")
   ```
4. **Acceptance criterion (a)** — zero failed jobs:
   ```bash
   curl -s "http://localhost:8080/api/admin/jobs/?status=failed&limit=200" -H "Authorization: Bearer $TOKEN" | python3 -c "import json,sys; data=json.load(sys.stdin); print('failed:', len(data.get('jobs', data if isinstance(data, list) else [])))"
   ```
   Expected: `failed: 0`. (Adjust the JSON path if the response wraps jobs in a different key — pretty-print first to confirm shape: `curl -s ... | python3 -m json.tool | head -30`.)
5. **Acceptance criterion (b)** — `urban_areas_landscan_10m` has a quicklook:
   ```bash
   DSID=$(curl -s "http://localhost:8080/api/datasets/?limit=200" -H "Authorization: Bearer $TOKEN" | python3 -c "
   import json, sys
   items=json.load(sys.stdin)['datasets']
   match=next((i for i in items if i.get('source_filename')=='ne_10m_urban_areas_landscan.zip'), None)
   print(match['id'] if match else 'MISSING')")
   echo "Dataset ID: $DSID"
   curl -s "http://localhost:8080/api/datasets/$DSID" -H "Authorization: Bearer $TOKEN" | python3 -c "
   import json, sys
   d=json.load(sys.stdin)
   print('quicklook_256_uri:', d.get('quicklook_256_uri'))
   print('feature_count:', d.get('feature_count'))
   print('record_status:', d.get('record_status'))"
   ```
   Expected: `quicklook_256_uri` is a non-null string (e.g. `vectors/<uuid>/quicklook_256.png`); `feature_count: 6018`; `record_status: published`.
6. **Worker log sanity check** — confirm no `quicklook_failed` warnings for the urban_areas_landscan job:
   ```bash
   docker compose logs worker --no-color --since=10m | grep -i "quicklook_failed\|MissingGreenlet" || echo "GREEN: no quicklook errors"
   ```
   Expected: `GREEN: no quicklook errors`.

If all 4 acceptance probes pass, type `approved` to continue. If any fail, describe the failure shape (paste the failing curl output) so the executor can iterate on the fix.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (paste failing curl output)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Atomic close — REQUIREMENTS.md flip + SUMMARY.md + ROADMAP.md row in one commit</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/1091-ingest-correctness-sweep/1091-02-SUMMARY.md, .planning/ROADMAP.md</files>
  <action>
Apply the v1019 TD-13 atomic-4-file close protocol in a single commit:

1. **REQUIREMENTS.md update** — edit `.planning/REQUIREMENTS.md`:
   - In the `### Ingestion` section, flip the leading checkbox: `- [ ] **INGEST-01**` → `- [x] **INGEST-01**`.
   - Append a "Closed:" suffix to the requirement bullet citing the test node-ID per TD-13 `req_citation_pinning`. Validate the node-ID via `git grep -n "def <test_name>" backend/tests/test_quicklook_async_context.py` — must return at least one hit. Format: `Closed: see backend/tests/test_quicklook_async_context.py::test_<name> (and second test if added).`
   - In the `## Traceability` table at the bottom, flip the `INGEST-01` row's status: `| INGEST-01 | Phase 1091 | Pending |` → `| INGEST-01 | Phase 1091 | Complete |`.
2. **SUMMARY.md create** — write `.planning/phases/1091-ingest-correctness-sweep/1091-02-SUMMARY.md` using the standard summary template. Required sections:
   - Frontmatter: `phase: 1091-ingest-correctness-sweep`, `plan: 02`, `requirements: [INGEST-01]`, `status: complete`, `tdd: true`, plus `verification: live_docker_rebuild` to flag the Task 2 human-verify gate.
   - **What shipped:** Cite the audit's root-cause statement (one sentence) + the code line(s) changed in `tasks_common.py` (file:line range) + the new test file path.
   - **How verified:** Cite the Task 2 acceptance-criteria results (failed-job count: 0, quicklook_256_uri non-null on urban_areas_landscan_10m, worker logs clean, pytest -n 4 = 3047+/0/38).
   - **Patterns confirmed:** v1019/v1020 spike-first pattern continues to deliver (audit → fix → test → live verification, no shotgun-debug commits).
   - **Followups (if any):** If Task 1's pool-size analysis surfaced a future concern, document here as a deferred item; do NOT bloom into v1021 scope.
3. **ROADMAP.md update** — edit `.planning/ROADMAP.md` Progress table: `| 1091. Ingest Correctness Sweep | 0/TBD | Not started | - |` → `| 1091. Ingest Correctness Sweep | 2/3 | In progress | - |` (will become `3/3 | Complete | <date>` after Plan 1091-03 closes). Update the phase row checkbox if you want, but do NOT mark the phase complete yet — that's 1091-03's job.
4. **Single commit** — stage and commit all 4 files in ONE git commit. `.planning/` files are gitignored; force-add them. Commit message format (no AI/bot mention per project conventions):
   ```
   fix(ingest): close INGEST-01 — quicklook async-context boundary

   - tasks_common.py: <one-line fix description from audit Section 3>
   - test_quicklook_async_context.py: regression pins (positive-form + xfail bug-shape)
   - Live verified: docker rebuild + seed-natural-earth.py → 0 failed jobs, urban_areas_landscan_10m has quicklook
   ```
   Stage command (single command, single commit):
   ```bash
   git add backend/app/processing/ingest/tasks_common.py backend/tests/test_quicklook_async_context.py
   git add -f .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/1091-ingest-correctness-sweep/1091-02-SUMMARY.md .planning/audits/INGEST-QUICKLOOK-ASYNC-CONTEXT-v1021.md .planning/phases/1091-ingest-correctness-sweep/1091-01-SUMMARY.md
   ```
   (1091-01-SUMMARY.md may already be in git from Plan 1091-01's close — `git add -f` is idempotent. The audit doc is included to ensure it's pushed alongside the close commit even if 1091-01 already committed it.)

   Then commit with hooks (NO `--no-verify`):
   ```bash
   git commit -m "$(cat <<'EOF'
   fix(ingest): close INGEST-01 — quicklook async-context boundary

   - tasks_common.py: [one-line from audit Section 3]
   - test_quicklook_async_context.py: regression pins
   - Live verified: docker rebuild + seed → 0 failed jobs, urban_areas_landscan_10m has quicklook
   EOF
   )"
   ```
5. If pre-commit hooks fail, fix the issue and create a NEW commit (do NOT amend per project conventions).
  </action>
  <verify>
    <automated>git log -1 --format="%s" | grep -q "close INGEST-01" &amp;&amp; grep -q "^- \[x\] \*\*INGEST-01\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "^\| INGEST-01 \| Phase 1091 \| Complete \|" .planning/REQUIREMENTS.md &amp;&amp; test -f .planning/phases/1091-ingest-correctness-sweep/1091-02-SUMMARY.md &amp;&amp; git grep -nq "def test_quicklook_commit_completes_without_missing_greenlet\|def test_quicklook_xfail_under_simulated_prefix_path\|def test_quicklook" backend/tests/test_quicklook_async_context.py</automated>
  </verify>
  <done>
    - `git log -1` shows a single commit titled "fix(ingest): close INGEST-01 — quicklook async-context boundary" (or close equivalent).
    - REQUIREMENTS.md INGEST-01 checkbox is `[x]` and traceability row is `Complete`.
    - 1091-02-SUMMARY.md exists with required sections.
    - ROADMAP.md Progress table shows 2/3 plans complete.
    - All 4 files committed atomically (no orphaned WIP).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → DB (asyncpg) | Long-running ingest task holds DB connection across pure-Python work; the bug is at this boundary. |
| test fixture → engine | The regression test uses the test-engine session factory (NullPool); the production fix uses the app engine (sized pool). |
| operator → docker stack | Operator runs rebuild + seed during checkpoint:human-verify; no untrusted data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1091-03 | Tampering (regression introduction) | `_generate_quicklook` rewrite | mitigate | New test `test_quicklook_async_context.py` pins both bug-shape (XFAIL strict) and fixed-behavior (positive-form). Full pytest -n 4 baseline must stay green (3047/0/38 invariant). |
| T-1091-04 | Denial of service (pool exhaustion) | Fresh session per ingest if Shape A applied | mitigate | Task 1 step 6 explicitly checks pool sizing against concurrent seed semaphore. If headroom is insufficient, the audit/fix names a pool-size bump or a different fix shape. |
| T-1091-05 | Information disclosure | Worker log output | accept | Quicklook errors are warnings logged with table name + error string truncated to ~500 chars in existing patterns; no PII or secret data crosses into logs. No new log lines introduced by this fix. |

Security-enforcement gate applies (default-on); no package installs (zero `npm install` / `uv add`), no new external dependencies.
</threat_model>

<verification>
- Code fix applied to `backend/app/processing/ingest/tasks_common.py` at the line range named in audit Section 3.
- `backend/tests/test_quicklook_async_context.py` exists and runs green: `cd backend && uv run pytest tests/test_quicklook_async_context.py -xvs` exits 0.
- Sequential pytest baseline preserved: `cd backend && uv run pytest -n 4 tests/` shows `failed=0` (3047 or higher passing tests, 38 skipped).
- Live docker rebuild + canonical seed produces zero `status=failed` rows (Task 2 acceptance criterion (a)).
- `urban_areas_landscan_10m` dataset has non-null `quicklook_256_uri` post-seed (Task 2 acceptance criterion (b)).
- Worker logs during the seed show NO `quicklook_failed` warnings AND no `MissingGreenlet` stack traces (Task 2 acceptance criterion (c)).
- REQUIREMENTS.md INGEST-01 checkbox flipped to `[x]` AND traceability row flipped to `Complete` in same commit as SUMMARY.md (TD-13 atomic-4-file rule).
- Single commit with no `--no-verify` (no hook skip).
</verification>

<success_criteria>
- INGEST-01 acceptance criteria (a), (b), (c) all green per REQUIREMENTS.md.
- Regression test pins the bug shape (so a future revert is loud).
- Sequential pytest 3047/0/38 preserved (HARD INVARIANT from CONTEXT.md).
- TD-13 atomic-4-file close commit lands cleanly.
- Plan 1091-03 can begin work (OPS-01 reconciliation) without re-touching this fix.
</success_criteria>

<output>
Create `.planning/phases/1091-ingest-correctness-sweep/1091-02-SUMMARY.md` as part of Task 3's atomic close commit (see Task 3 action).
</output>
