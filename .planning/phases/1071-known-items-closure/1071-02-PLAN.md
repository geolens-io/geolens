---
phase: 1071-known-items-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - backend/app/processing/ingest/ogr.py
  - .planning/todos/resolved/2026-05-20-v1062-in01-password-env-doc.md
  - .planning/todos/resolved/2026-05-20-v1063-in01-sanitize-authorization-token-8char-doc.md
autonomous: true
requirements:
  - KNOWN-08
  - KNOWN-11

must_haves:
  truths:
    - "Operators reading .env.example see PASSWORD_MIN_LENGTH and PASSWORD_REQUIRE_CLASSES documented near the auth settings"
    - "Callers of _sanitize_authorization_token can read why an 8-char token gets rejected from the function's docstring without consulting git blame"
    - "The two pending todos for these INFO findings are closed and archived in resolved/"
  artifacts:
    - path: ".env.example"
      provides: "PASSWORD_MIN_LENGTH and PASSWORD_REQUIRE_CLASSES env-var documentation"
      contains: "PASSWORD_MIN_LENGTH"
    - path: "backend/app/processing/ingest/ogr.py"
      provides: "Docstring on _sanitize_authorization_token explaining the 8-char minimum"
      contains: "8-character"
    - path: ".planning/todos/resolved/2026-05-20-v1062-in01-password-env-doc.md"
      provides: "Archived v1062 IN-01 pending todo with resolution preamble"
    - path: ".planning/todos/resolved/2026-05-20-v1063-in01-sanitize-authorization-token-8char-doc.md"
      provides: "Archived v1063 IN-01 pending todo with resolution preamble"
  key_links:
    - from: ".env.example"
      to: "backend/app/core/config.py"
      via: "Settings.password_min_length / Settings.password_require_classes"
      pattern: "PASSWORD_(MIN_LENGTH|REQUIRE_CLASSES)"
    - from: "backend/app/processing/ingest/ogr.py"
      to: "_sanitize_authorization_token docstring"
      via: "raises ValueError on len < 8"
      pattern: "8-character"
---

<objective>
Close the two documentation-only INFO findings from v1062 IN-01 and v1063 IN-01: document `PASSWORD_MIN_LENGTH` / `PASSWORD_REQUIRE_CLASSES` in `.env.example`, and document the 8-character minimum on `_sanitize_authorization_token` inline at the function.

Purpose: New operators reading `.env.example` currently have no signal that the v1014 SEC-S16 password complexity knobs exist (they default to 12-char minimum + 3-of-4 classes, configurable). Callers of `_sanitize_authorization_token` get a SEC-FU-04-prefixed `ValueError` for a 7-char token with no inline-documented rationale; the floor is intentional but undiscoverable.

Output: One commit per finding. Two pending-todo files moved to `.planning/todos/resolved/` with a 2-line resolution preamble appended at the top.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/1071-known-items-closure/1071-CONTEXT.md
@.planning/todos/pending/2026-05-20-v1062-in01-password-env-doc.md
@.planning/todos/pending/2026-05-20-v1063-in01-sanitize-authorization-token-8char-doc.md

# Surfaces being edited:
@.env.example
@backend/app/processing/ingest/ogr.py
@backend/app/modules/auth/password_policy.py

## Pre-existing documentation patterns in .env.example
The file's convention is:
- Section header in `# --- Section ---` shape
- Per-var block: 1 explanatory comment + 1 Type/Default line + 1 var (commented if optional)

The "Authentication" section (around the `JWT_SECRET_KEY` block) is the right
neighborhood for the new PASSWORD_* entries — they belong with the JWT and
admin credential knobs.

## _sanitize_authorization_token current docstring
The function already has a docstring (ogr.py:30-40); the existing text explains
the base64url-charset rationale but does NOT mention the 8-character floor
that follows on line 43-47. The fix is to add a paragraph naming the floor
and the rationale (defense against truncated tokens being silently accepted).

## Pending-todo archive pattern (from v1014/v1015 close)
The "move to resolved" pattern: prepend a `---` frontmatter-shaped resolution
block at the top of the file:

```
---
resolved: 2026-05-21
resolved_in_phase: 1071
resolved_by_plan: 1071-02
resolution: "<one-line description of the closure>"
commit: <sha-of-the-closing-commit>
---
```

Then `git mv` from `pending/` to `resolved/`.

</context>

<tasks>

<task type="auto">
  <name>Task 1: Document PASSWORD_MIN_LENGTH and PASSWORD_REQUIRE_CLASSES in .env.example</name>
  <files>.env.example</files>
  <action>
  Read `.env.example` and locate the Authentication section (around the
  `JWT_SECRET_KEY`/`JWT_ALGORITHM`/`ACCESS_TOKEN_EXPIRE_MINUTES`/`REFRESH_TOKEN_EXPIRE_DAYS`
  block). After the existing JWT block but before the `GEOLENS_ADMIN_USERNAME`
  block, insert a Password Complexity sub-block.

  Use the project's existing comment format (header line, explanatory line,
  Type/Default line, commented var line). Verbatim text to insert (preserve
  the 2-blank-line separator style):

  ```
  # Password complexity (Phase 1062 SEC-S16). Enforced at every password entry
  # point (register, change-password, admin create, SAML→local conversion).
  # Defaults shown — uncomment to override.

  # Minimum password length in characters.
  # Type: integer | Default: 12
  # PASSWORD_MIN_LENGTH=12

  # Number of character classes required (out of 4: lowercase, uppercase, digit, symbol).
  # Type: integer (1-4) | Default: 3
  # PASSWORD_REQUIRE_CLASSES=3
  ```

  Do NOT change the defaults. Do NOT add a fourth knob (e.g., breached-password
  denylist) — that was deferred to a future milestone per the SEC-S16
  module docstring (`password_policy.py:14-16`).
  </action>
  <verify>
    <automated>grep -c 'PASSWORD_MIN_LENGTH\|PASSWORD_REQUIRE_CLASSES' .env.example | awk '$1 >= 2 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>Two new commented env-var entries documented near the existing auth settings; .env.example otherwise unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Document the 8-char minimum inline on _sanitize_authorization_token</name>
  <files>backend/app/processing/ingest/ogr.py</files>
  <action>
  In `backend/app/processing/ingest/ogr.py`, locate `_sanitize_authorization_token`
  (function starts at line 29, current docstring ends at line 40, the 8-char
  check is at lines 43-47).

  Extend the docstring with a new paragraph explaining the 8-character floor.
  Keep the existing docstring content intact; add the new paragraph BEFORE
  the final "Returns the token unchanged…" sentence.

  Verbatim paragraph to insert:

  ```
  Tokens shorter than 8 characters raise ValueError with a SEC-FU-04-prefixed
  message. The 8-character floor is intentional: legitimate JWT-shaped tokens
  always exceed this (a minimal three-segment header.payload.signature exceeds
  20 characters), and accepting 1-7 character tokens lets a single accidental
  truncation upstream — for example, a quoted JSON field cut at the wrong
  index, or an ArcGIS deployment that emits a short tracking token mistaken
  for a bearer — silently slip into the GDAL_HTTP_HEADERS pipeline. Callers
  who legitimately need to pass a sub-8-character token must use a different
  authentication path (e.g., GDAL_HTTP_HEADER_FILE with a custom header).
  ```

  The docstring should now have three paragraphs:
  1. SEC-FU-04 rationale (existing — base64url charset and CRLF defense)
  2. The 8-character minimum (new)
  3. Returns/Raises summary (existing — moved to the end if not already there)

  Do NOT change the implementation. The `len(token) < 8` check at line 43
  stays exactly as it is.
  </action>
  <verify>
    <automated>cd backend && grep -A 30 'def _sanitize_authorization_token' app/processing/ingest/ogr.py | grep -E '8-character|eight-character' | head -3</automated>
  </verify>
  <done>_sanitize_authorization_token docstring carries an inline-documented rationale for the 8-character floor; no behavior change.</done>
</task>

<task type="auto">
  <name>Task 3: Archive the two pending todos to resolved/</name>
  <files>
    .planning/todos/pending/2026-05-20-v1062-in01-password-env-doc.md,
    .planning/todos/pending/2026-05-20-v1063-in01-sanitize-authorization-token-8char-doc.md,
    .planning/todos/resolved/2026-05-20-v1062-in01-password-env-doc.md,
    .planning/todos/resolved/2026-05-20-v1063-in01-sanitize-authorization-token-8char-doc.md
  </files>
  <action>
  For each of the two pending-todo files, do the following sequence:

  1. Prepend a resolution preamble at the top of the file (BEFORE the existing
     frontmatter `---` block). The preamble shape is:

     ```
     ---
     resolved: 2026-05-21
     resolved_in_phase: 1071
     resolved_by_plan: 1071-02
     resolution: "<finding-specific 1-line description>"
     commit: <sha-of-the-task-1-or-task-2 commit>
     ---
     ```

  2. Move the file from `pending/` to `resolved/` via `git mv` (preserves
     blame history).

  Resolution text per file:

  - `2026-05-20-v1062-in01-password-env-doc.md`:
    `Documented PASSWORD_MIN_LENGTH and PASSWORD_REQUIRE_CLASSES in .env.example near the existing auth settings.`

  - `2026-05-20-v1063-in01-sanitize-authorization-token-8char-doc.md`:
    `Added inline docstring paragraph on _sanitize_authorization_token explaining the 8-character floor as defense against silent token truncation.`

  Use the commit SHAs from Tasks 1 and 2 (they will be created sequentially;
  the executor can run `git log -1 --format=%H` after each commit to capture).

  Do NOT edit the body of either todo file beyond prepending the preamble;
  the original Finding/Solution/Deferred rationale stays for posterity.
  </action>
  <verify>
    <automated>ls .planning/todos/resolved/2026-05-20-v1062-in01-password-env-doc.md .planning/todos/resolved/2026-05-20-v1063-in01-sanitize-authorization-token-8char-doc.md 2>&1 | grep -v 'No such' | wc -l | awk '$1 == 2 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>Both pending-todo files moved to .planning/todos/resolved/ with resolution preamble; STATE.md "Pending Todos" section will be updated by the orchestrator at SUMMARY-write time.</done>
</task>

</tasks>

<verification>
- `.env.example` shows `PASSWORD_MIN_LENGTH` and `PASSWORD_REQUIRE_CLASSES` documented near `JWT_SECRET_KEY` / `GEOLENS_ADMIN_USERNAME`
- `grep -A 30 '_sanitize_authorization_token' backend/app/processing/ingest/ogr.py` shows the new 8-character-floor paragraph in the docstring
- Both pending todos exist in `.planning/todos/resolved/` (not in `pending/`) with the resolution preamble at the top
- No code behavior changed (docs-only); existing tests pass unchanged
</verification>

<success_criteria>
- Two atomic commits (one per finding):
  - `docs(env): document PASSWORD_MIN_LENGTH and PASSWORD_REQUIRE_CLASSES (KNOWN-08)`
  - `docs(ingest): document 8-char minimum on _sanitize_authorization_token (KNOWN-11)`
- Optional third commit for the todo archival:
  - `docs(planning): archive v1062 IN-01 + v1063 IN-01 to resolved/ (KNOWN-08, KNOWN-11)`
- KNOWN-08 and KNOWN-11 ticked in REQUIREMENTS.md
</success_criteria>

<commit_message>
docs(env): document PASSWORD_MIN_LENGTH and PASSWORD_REQUIRE_CLASSES (KNOWN-08)

Phase 1062 SEC-S16 shipped configurable password complexity knobs but
.env.example was never updated. New operators reading the file have no
signal these exist. Adds two commented env-var entries in the
Authentication section with default values shown.

Closes KNOWN-08 (v1062 IN-01 pending todo).
</commit_message>

<output>
Create `.planning/phases/1071-known-items-closure/1071-02-SUMMARY.md` when done.
</output>