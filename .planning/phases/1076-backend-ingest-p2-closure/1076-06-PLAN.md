---
phase: 1076-backend-ingest-p2-closure
plan: 06
type: execute
wave: 2
depends_on:
  - 1076-01
  - 1076-02
  - 1076-03
  - 1076-04
  - 1076-05
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/1076-backend-ingest-p2-closure/1076-VERIFICATION.md
  - .planning/phases/1076-backend-ingest-p2-closure/1076-SUMMARY.md
autonomous: true
requirements:
  - ING-02
  - ING-03
  - ING-04
  - ING-06
  - ING-07
tags:
  - close-gate
  - verification
  - phase-completion
  - traceability

must_haves:
  truths:
    - "Targeted regression tests for all 5 ING requirements pass: `test_phase_2_commit_boundary.py`, `test_storage_get_stream.py`, `test_worker_exports_sweep.py`, `test_reupload_swap_lock_retry.py`, `test_strict_cog_enforcement.py`"
    - "Full ingest test suite passes against the targeted set: `pytest tests/test_ingest.py tests/test_ingest_metadata.py tests/test_ingest_progress.py tests/test_ingest_service_geometry_type.py tests/test_ingest_column_preservation.py tests/test_ingest_fan_out.py tests/test_raster_ingest.py tests/test_raster_validation.py tests/test_raster_schema.py tests/test_reupload.py tests/test_reupload_idor.py tests/test_reupload_service.py tests/test_reupload_record_type_guard.py tests/test_ensure_geom_column.py tests/test_attribute_metadata.py tests/test_staging_pipeline.py tests/test_staging_pipeline_integration.py`"
    - "VERIFICATION.md exists in the phase directory documenting which tests passed/failed, command outputs, and any anomalies encountered"
    - "SUMMARY.md exists in the phase directory summarizing the 5 ING closures + per-plan SUMMARY references"
    - "STATE.md updated: current position advanced past Phase 1076; decisions/blockers reflect ING-02..07 closure"
    - "ROADMAP.md `Phase 1076` checkbox flipped to `- [x]`; all 6 plan rows marked complete; traceability table in REQUIREMENTS.md updated (ING-02..07 status `Pending` → `Complete`)"
  artifacts:
    - path: ".planning/phases/1076-backend-ingest-p2-closure/1076-VERIFICATION.md"
      provides: "Test execution evidence for the phase"
      min_lines: 50
    - path: ".planning/phases/1076-backend-ingest-p2-closure/1076-SUMMARY.md"
      provides: "Phase closure summary with per-plan references"
      min_lines: 30
    - path: ".planning/STATE.md"
      provides: "Updated position, decisions, last activity"
      contains: "Phase 1076"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 1076 marked complete with all 6 plans checked"
      contains: "[x] **Phase 1076"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table updated"
      contains: "Complete"
  key_links:
    - from: ".planning/phases/1076-backend-ingest-p2-closure/1076-SUMMARY.md"
      to: "5 per-plan SUMMARYs"
      via: "links / references to 1076-01..05-SUMMARY.md"
      pattern: "1076-0[1-5]-SUMMARY"
    - from: ".planning/STATE.md"
      to: ".planning/ROADMAP.md"
      via: "Current Position advanced to next phase (1077 or 1078)"
      pattern: "1076"
---

<objective>
Verify all 5 ING requirements closed by Plans 01-05; capture evidence in VERIFICATION.md; write phase-level SUMMARY.md; update STATE.md / ROADMAP.md / REQUIREMENTS.md to reflect Phase 1076 completion.

Purpose: Phase 1076 is the backend-half of v1017's ingest P2 closure. Plans 01-05 are independent Wave 1 work; this plan is the Wave 2 close-gate that proves no plan regressed another, captures the test evidence trail, and hands clean state to downstream phases (1077, 1078, 1079).

Output:
- All targeted regression tests run; results captured in VERIFICATION.md with command outputs (pass counts, timing, any skips/xfails)
- Phase-level SUMMARY.md cross-references 5 per-plan SUMMARYs and documents the cumulative impact
- STATE.md / ROADMAP.md / REQUIREMENTS.md reflect Phase 1076 closure
- Phase 1077 and 1078 can be started immediately (no inter-phase test contamination)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/1076-backend-ingest-p2-closure/1076-CONTEXT.md
@.planning/phases/1076-backend-ingest-p2-closure/1076-01-SUMMARY.md
@.planning/phases/1076-backend-ingest-p2-closure/1076-02-SUMMARY.md
@.planning/phases/1076-backend-ingest-p2-closure/1076-03-SUMMARY.md
@.planning/phases/1076-backend-ingest-p2-closure/1076-04-SUMMARY.md
@.planning/phases/1076-backend-ingest-p2-closure/1076-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run targeted regression tests and capture VERIFICATION.md</name>
  <read_first>
    - All 5 per-plan SUMMARY files (1076-01-SUMMARY.md through 1076-05-SUMMARY.md) — confirm each plan reported completion
    - .planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-05-VERIFICATION.md (format precedent for VERIFICATION.md)
  </read_first>
  <acceptance_criteria>
    - VERIFICATION.md exists at `.planning/phases/1076-backend-ingest-p2-closure/1076-VERIFICATION.md`
    - File contains, at minimum: per-requirement section (ING-02, ING-03, ING-04, ING-06, ING-07) with the corresponding test command and pass count
    - A combined run summary section showing the cumulative ingest test count + pass count
    - Any unexpected test failures or skips are documented with disposition (fix-now, defer-to-1079, or known-environmental)
    - File length &gt;50 lines
  </acceptance_criteria>
  <action>
    Run the following test commands and capture outputs. Use `cd backend &amp;&amp; uv run pytest ...` for all backend tests.

    1. **Per-plan regression tests (one command per ING)**:
       - ING-02: `cd backend &amp;&amp; uv run pytest tests/test_phase_2_commit_boundary.py tests/test_ensure_geom_column.py -x -v`
       - ING-03: `cd backend &amp;&amp; uv run pytest tests/test_storage_get_stream.py tests/test_phase_273_download_token.py tests/test_download_token.py -x -v`
       - ING-04: `cd backend &amp;&amp; uv run pytest tests/test_worker_exports_sweep.py tests/test_worker.py -x -v`
       - ING-06: `cd backend &amp;&amp; uv run pytest tests/test_reupload_swap_lock_retry.py tests/test_reupload.py tests/test_reupload_idor.py tests/test_reupload_service.py tests/test_reupload_record_type_guard.py -x -v`
       - ING-07: `cd backend &amp;&amp; uv run pytest tests/test_strict_cog_enforcement.py tests/test_commit_request_schemas.py tests/test_raster_ingest.py tests/test_raster_validation.py tests/test_raster_schema.py -x -v`

    2. **Full ingest sweep** (everything touched in the phase, to catch cross-plan regressions):
       ```
       cd backend &amp;&amp; uv run pytest \
         tests/test_phase_2_commit_boundary.py \
         tests/test_storage_get_stream.py \
         tests/test_worker_exports_sweep.py \
         tests/test_reupload_swap_lock_retry.py \
         tests/test_strict_cog_enforcement.py \
         tests/test_ingest.py \
         tests/test_ingest_metadata.py \
         tests/test_ingest_progress.py \
         tests/test_ingest_service_geometry_type.py \
         tests/test_ingest_column_preservation.py \
         tests/test_ingest_fan_out.py \
         tests/test_raster_ingest.py \
         tests/test_raster_validation.py \
         tests/test_raster_schema.py \
         tests/test_reupload.py \
         tests/test_reupload_idor.py \
         tests/test_reupload_service.py \
         tests/test_reupload_record_type_guard.py \
         tests/test_ensure_geom_column.py \
         tests/test_attribute_metadata.py \
         tests/test_staging_pipeline.py \
         tests/test_staging_pipeline_integration.py \
         tests/test_commit_request_schemas.py \
         -x
       ```

    3. **Lint / type-check sanity**:
       - `cd backend &amp;&amp; uv run mypy app/processing/ingest/ app/platform/storage/ app/platform/jobs/` (capture any new errors — pre-existing ones are not blockers but should be noted)
       - `cd backend &amp;&amp; uv run ruff check app/processing/ingest/ app/platform/storage/ app/platform/jobs/`

    Then create `.planning/phases/1076-backend-ingest-p2-closure/1076-VERIFICATION.md` with the following structure:

    ```markdown
    ---
    phase: 1076-backend-ingest-p2-closure
    verified_at: 2026-05-21
    verdict: PASS
    requirements_verified: 5
    requirements_pending: 0
    ---

    # Phase 1076 Verification

    ## Requirement Closure Evidence

    ### ING-02 — metadata.py phase-2 commit boundary
    - Test command: `cd backend &amp;&amp; uv run pytest tests/test_phase_2_commit_boundary.py tests/test_ensure_geom_column.py -x -v`
    - Result: N passed in Xs
    - Static grep verification: `grep -c "await session.commit" backend/app/processing/ingest/metadata.py` → 1 (only `rename_reserved_columns`)

    ### ING-03 — Local-storage COG streaming
    - Test command: `cd backend &amp;&amp; uv run pytest tests/test_storage_get_stream.py tests/test_phase_273_download_token.py tests/test_download_token.py -x -v`
    - Result: N passed in Xs
    - Static grep verification: `grep -c "storage.get_stream" backend/app/modules/catalog/datasets/api/router_export.py` → 1; `grep -c "io.BytesIO" ...` → 0

    ### ING-04 — Worker exports temp-dir mtime guard
    - Test command: ...
    - Result: ...
    - Static grep verification: `grep -c "EXPORTS_SWEEP_AGE_SECONDS" ...` → ≥2; `grep -c "sweep_skipped_recent_export" ...` → 1

    ### ING-06 — Reupload swap lock_timeout retry
    - Test command: ...
    - Result: ...
    - Static grep verification: `grep -c "reupload_swap_lock_contention" ...` → 1; `grep -c "_is_lock_timeout_error" ...` → ≥2

    ### ING-07 — strict_cog opt-in flag
    - Test command: ...
    - Result: ...
    - Static grep verification: `grep -c "strict_cog" backend/app/processing/ingest/schemas.py` → ≥1; `grep -c "strict_cog" backend/app/processing/ingest/tasks_raster.py` → ≥1

    ## Full Ingest Sweep
    - Command: `cd backend &amp;&amp; uv run pytest [list of 22 files] -x`
    - Result: M passed, K skipped (with rationale), 0 failed in Ys

    ## Type-check + lint
    - `mypy` exit code: 0 (or list of pre-existing issues with confirmation no new ones introduced)
    - `ruff check` exit code: 0

    ## Anomalies
    - List anything weird: tests that needed mock adjustments, environmental quirks, Phase-1075-style sequential-only failures, etc.
    - If zero anomalies: write "None encountered."

    ## Disposition
    - All 5 ING requirements (ING-02, ING-03, ING-04, ING-06, ING-07) verified CLOSED.
    - Phase 1076 ready to mark complete in STATE.md / ROADMAP.md / REQUIREMENTS.md.
    ```

    Fill the test result counts with the ACTUAL output from the pytest runs above. Do not guess; if a test failed, surface the failure with the disposition (fix-now via a Plan 1076-07 or defer-to-1079).
  </action>
  <verify>
    <automated>test -f .planning/phases/1076-backend-ingest-p2-closure/1076-VERIFICATION.md &amp;&amp; wc -l .planning/phases/1076-backend-ingest-p2-closure/1076-VERIFICATION.md | awk '{exit ($1 &gt;= 50) ? 0 : 1}'</automated>
  </verify>
  <done>
    - File `.planning/phases/1076-backend-ingest-p2-closure/1076-VERIFICATION.md` exists with all 5 ING sections + full-sweep + anomalies sections
    - Word count: at least 50 lines
    - All test commands listed produced exit code 0 (or anomalies documented with disposition)
    - YAML frontmatter `verdict: PASS` (or `PARTIAL` with explicit list of unmet requirements)
  </done>
</task>

<task type="auto">
  <name>Task 2: Write phase-level SUMMARY.md</name>
  <read_first>
    - All 5 per-plan SUMMARY files
    - .planning/phases/1076-backend-ingest-p2-closure/1076-VERIFICATION.md (Task 1's output)
    - .planning/phases/1075-conftest-test-db-lifecycle-refactor-baseline-fixes/1075-05-SUMMARY.md (precedent format)
  </read_first>
  <acceptance_criteria>
    - File `.planning/phases/1076-backend-ingest-p2-closure/1076-SUMMARY.md` exists
    - Frontmatter includes: `phase`, `requirements` (list of ING-02..07), `plans_completed: 6`, `verdict`
    - Body cross-references the 5 per-plan SUMMARYs
    - Body lists production-code files touched + test files added
    - Body documents cumulative impact and any cross-plan interactions
    - File length &gt;30 lines
  </acceptance_criteria>
  <action>
    Create `.planning/phases/1076-backend-ingest-p2-closure/1076-SUMMARY.md`:

    ```markdown
    ---
    phase: 1076-backend-ingest-p2-closure
    completed_at: 2026-05-21
    requirements: [ING-02, ING-03, ING-04, ING-06, ING-07]
    plans_completed: 6
    verdict: PASS
    ---

    # Phase 1076 — Backend Ingest P2 Closure

    ## Summary

    Closed 5 backend P2 findings from `INGEST-AUDIT-2026-05-21.md` — all
    deferred from v1015 / v1016 per the Phase 1073 remediation plan:

    - **ING-02 (P2-02)** — Dropped 4 internal `await session.commit()` calls
      from `metadata.py` phase-2 helpers; added regression test asserting
      phase-2 failure rolls back column-add via `_finalize_ingest`'s outer
      commit boundary.
    - **ING-03 (P2-03)** — Added `StorageProvider.get_stream()` + local
      implementation; rewired local-storage COG download to use chunked
      streaming. Eliminates 5 GB resident memory spike on self-hosters.
    - **ING-04 (P2-04)** — Worker exports temp-dir sweep now guards on
      `stat.st_mtime > 1 hour`; in-flight large exports survive worker
      restarts; per-skipped-entry log events enable ops correlation.
    - **ING-06 (P2-08)** — `_apply_reupload_swap` retries once on
      `LockNotAvailableError` with `SET LOCAL lock_timeout = '15s'` plus
      200ms sleep; WARNING + INFO log events bracket the retry.
    - **ING-07 (P2-09)** — Added optional `strict_cog: bool = False` field
      to `RasterCommitRequest`. When True, non-COG TIFFs are rejected at
      commit time instead of routing through `check_and_prepare_cog`
      conversion. Backward-compatible default preserves existing flows.

    ## Plan References

    - [Plan 01 — ING-02 metadata.py commit boundary](1076-01-SUMMARY.md)
    - [Plan 02 — ING-03 local-storage COG streaming](1076-02-SUMMARY.md)
    - [Plan 03 — ING-04 exports sweep mtime guard](1076-03-SUMMARY.md)
    - [Plan 04 — ING-06 swap lock_timeout retry](1076-04-SUMMARY.md)
    - [Plan 05 — ING-07 strict_cog opt-in flag](1076-05-SUMMARY.md)
    - [Plan 06 — Phase verification + close-gate](this file)

    ## Production-Code Files Touched

    - `backend/app/processing/ingest/metadata.py` — 4 internal commits removed (ING-02)
    - `backend/app/processing/ingest/tasks_common.py` — `_apply_reupload_swap` retry, `_is_lock_timeout_error` helper (ING-06)
    - `backend/app/processing/ingest/schemas.py` — `RasterCommitRequest.strict_cog` field (ING-07)
    - `backend/app/processing/ingest/tasks_raster.py` — strict-mode gate (ING-07)
    - `backend/app/platform/storage/provider.py` — `get_stream` Protocol method (ING-03)
    - `backend/app/platform/storage/local.py` — chunked streaming impl (ING-03)
    - `backend/app/platform/storage/s3.py` — get_stream NotImplementedError stub (ING-03)
    - `backend/app/modules/catalog/datasets/api/router_export.py` — COG download rewired (ING-03)
    - `backend/app/platform/jobs/worker.py` — mtime-guarded sweep (ING-04)

    ## Tests Added

    - `backend/tests/test_phase_2_commit_boundary.py` — N lines, 3 methods (ING-02)
    - `backend/tests/test_storage_get_stream.py` — N lines, 4 methods (ING-03)
    - `backend/tests/test_worker_exports_sweep.py` — N lines, 4 methods (ING-04)
    - `backend/tests/test_reupload_swap_lock_retry.py` — N lines, 5 methods (ING-06)
    - `backend/tests/test_strict_cog_enforcement.py` — N lines, 4 methods (ING-07)

    (Replace `N` and the per-test method counts with the actual values
    from each plan's SUMMARY.)

    ## Cross-Plan Interactions

    - Plan 01 (ING-02) and Plan 04 (ING-06) both touch the ingest worker
      pipeline but in disjoint regions (`metadata.py` vs `tasks_common.py`'s
      reupload helper); no file overlap, no test interference.
    - Plan 02 (ING-03) adds a new method to the storage Protocol; no
      other plan touches storage providers.
    - Plan 03 (ING-04) is isolated in the worker boot path; no test or
      production-code overlap with the other plans.
    - Plan 05 (ING-07) extends `RasterCommitRequest` and adds a guard
      in `tasks_raster.ingest_raster`; orthogonal to the vector path
      touched by Plan 01.

    All 5 plans landed in Wave 1 with zero `files_modified` overlap; no
    inter-plan dependency was needed.

    ## Verification

    See [1076-VERIFICATION.md](1076-VERIFICATION.md) for the full test
    evidence. Headline: all 5 ING requirements verified CLOSED; full ingest
    sweep (22 test files) passes.

    ## Next Phases

    - **Phase 1077** (Frontend Ingest P2 Closure — ING-01, ING-05): unblocked, depends only on Phase 1075 (already complete).
    - **Phase 1078** (CI Alembic Workflow — CI-01): independent; can start in parallel.
    - **Phase 1079** (Close Gate + Hygiene — TI-03, VG-01, HYG-01): depends on Phase 1076 + 1077 + 1078.
    ```

    Replace placeholder `N` values with the actual line counts and test-method counts pulled from each per-plan SUMMARY.
  </action>
  <verify>
    <automated>test -f .planning/phases/1076-backend-ingest-p2-closure/1076-SUMMARY.md &amp;&amp; wc -l .planning/phases/1076-backend-ingest-p2-closure/1076-SUMMARY.md | awk '{exit ($1 &gt;= 30) ? 0 : 1}'</automated>
  </verify>
  <done>
    - File `.planning/phases/1076-backend-ingest-p2-closure/1076-SUMMARY.md` exists
    - Frontmatter includes phase, requirements, plans_completed, verdict
    - Body cross-references all 5 per-plan SUMMARYs
    - Production-code files + test files lists are populated with actual paths
    - Word count: at least 30 lines
  </done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md, ROADMAP.md, REQUIREMENTS.md</name>
  <read_first>
    - .planning/STATE.md (current — capture format precedent)
    - .planning/ROADMAP.md (current — find Phase 1076 row + progress table)
    - .planning/REQUIREMENTS.md (current — traceability table)
  </read_first>
  <acceptance_criteria>
    - STATE.md: `Current Position` advanced past Phase 1076; `last_updated` and `last_activity` set to today (2026-05-21); `Phase 1076 complete` added to Decisions/log; `progress.completed_phases` incremented
    - ROADMAP.md: `Phase 1076` checkbox flipped to `- [x]`; the progress table row for Phase 1076 shows `6/6` plans and status `Complete`; each of the 6 plan lines uses `- [x] 1076-XX-PLAN.md` format if those lines are populated (some plan-list formats may differ — match existing convention)
    - REQUIREMENTS.md: traceability table entries for ING-02 / ING-03 / ING-04 / ING-06 / ING-07 change `Pending` → `Complete`
  </acceptance_criteria>
  <action>
    Three file edits using the Read + Edit pattern (NOT Write — preserve everything outside the specific updates):

    A. `.planning/STATE.md`
    - Update frontmatter:
      - `stopped_at`: change to `"Phase 1076 complete (6/6 plans); ING-02..07 closed; ready for Phase 1077/1078/1079"`
      - `last_updated`: `"2026-05-21T..."` (use a sensible current timestamp; mirror format from existing file)
      - `last_activity`: `"2026-05-21"`
      - `progress.completed_phases`: increment from `1` to `2`
      - `progress.completed_plans`: increment by the 6 plans landed in 1076 (5 + verification plan)
      - `progress.percent`: recalculate (`completed_phases / total_phases * 100`)
    - Body updates:
      - `## Current Position` block: change `Phase: 1075` → `Phase: 1076 — backend-ingest-p2-closure — COMPLETE`; advance `Plan: 5 of 5 (all done)` → `Plan: 6 of 6 (all done)`; update `Next phase: 1076 / 1077 / 1078` → `Next phase: 1077 / 1078 (1079 gated on all three)`
      - Add a new entry at the top of `### Decisions`: `- 2026-05-21 (v1017 Phase 1076): all 5 ING requirements (ING-02..07) closed; backend ingest P2 tail fully resolved. Cumulative impact: 4 internal commits dropped from metadata.py, 1 new streaming method on StorageProvider, 1 mtime-guarded worker sweep, 1 single-retry on lock_timeout, 1 opt-in strict_cog flag. See .planning/phases/1076-backend-ingest-p2-closure/1076-VERIFICATION.md for the test evidence trail.`
      - In `## Operator Next Steps`: remove the `/gsd:plan-phase 1076` bullet (now done); leave the `1077` and `1078` bullets as actionable next steps; remove the gating reference to 1075-06 if it's already resolved

    B. `.planning/ROADMAP.md`
    - In `## Phases` → `### 🚧 v1017 Test Infra & Audit Tail (In Progress)` block at lines ~107-111: flip `- [ ] **Phase 1076: Backend Ingest P2 Closure**` to `- [x] **Phase 1076: Backend Ingest P2 Closure** ... (Complete 2026-05-21)`
    - In `## Progress` table at lines ~178-185: update the Phase 1076 row from `0/0 | Not started | -` to `6/6 | Complete | 2026-05-21`
    - If the Phase 1076 Plans subsection at lines ~141 has `**Plans:** TBD`, replace it with:
      ```
      **Plans:** 6/6 plans complete
      - [x] 1076-01-PLAN.md — ING-02 metadata.py phase-2 commit boundary
      - [x] 1076-02-PLAN.md — ING-03 local-storage COG streaming
      - [x] 1076-03-PLAN.md — ING-04 worker exports sweep mtime guard
      - [x] 1076-04-PLAN.md — ING-06 reupload swap lock_timeout retry
      - [x] 1076-05-PLAN.md — ING-07 strict_cog opt-in flag
      - [x] 1076-06-PLAN.md — Phase verification + close-gate
      ```

    C. `.planning/REQUIREMENTS.md`
    - In the `## Traceability` table at lines ~62-79, change the Status column for these rows from `Pending` to `Complete`:
      - `| ING-02 | Phase 1076 | Pending |` → `| ING-02 | Phase 1076 | Complete |`
      - `| ING-03 | Phase 1076 | Pending |` → `| ING-03 | Phase 1076 | Complete |`
      - `| ING-04 | Phase 1076 | Pending |` → `| ING-04 | Phase 1076 | Complete |`
      - `| ING-06 | Phase 1076 | Pending |` → `| ING-06 | Phase 1076 | Complete |`
      - `| ING-07 | Phase 1076 | Pending |` → `| ING-07 | Phase 1076 | Complete |`
    - Optionally flip the requirements checkboxes at lines 27, 28, 29, 31, 32 (the `- [ ]` lines for ING-02..07) to `- [x]`. This is consistent with how Phase 1075 closed (TI-01 and TI-02 lines 14, 15 are already `- [x]`).
  </action>
  <verify>
    <automated>grep -c "Phase 1076" .planning/STATE.md .planning/ROADMAP.md &amp;&amp; grep "ING-02.*Complete" .planning/REQUIREMENTS.md &amp;&amp; grep "ING-07.*Complete" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - STATE.md `Current Position` reflects Phase 1076 complete
    - ROADMAP.md Phase 1076 row in progress table reads `6/6 | Complete`
    - All 5 ING-XX rows in REQUIREMENTS.md traceability table read `Complete`
    - `grep -c "1076-06-PLAN.md" .planning/ROADMAP.md` returns `&gt;= 1`
  </done>
</task>

<task type="auto">
  <name>Task 4: Commit phase closure</name>
  <read_first>
    - Latest git log to confirm what's already committed: `git log --oneline -20`
    - Working tree status: `git status` to see all modified files
  </read_first>
  <acceptance_criteria>
    - All Phase 1076 production-code files + test files committed (if not already committed by the per-plan executors)
    - Phase-level VERIFICATION.md + SUMMARY.md + STATE.md/ROADMAP.md/REQUIREMENTS.md updates committed
    - Final commit message follows project convention (no AI/bot footer per CLAUDE.md): `docs(1076): close Phase 1076 — Backend Ingest P2 (ING-02..07) verified`
    - `git status` reports clean working tree after the commit
  </acceptance_criteria>
  <action>
    Sequence:

    1. Run `git status` to see what's modified. Most production-code files SHOULD already be committed by the per-plan executors (each plan's task includes the production-code change). The remaining uncommitted files for THIS plan are typically the phase-level `.planning/` files: VERIFICATION.md, SUMMARY.md, STATE.md, ROADMAP.md, REQUIREMENTS.md.

    2. Stage ONLY the .planning/ files (be explicit; do not use `git add -A` — protects against accidentally committing untracked debug files):
       ```
       git add \
         .planning/phases/1076-backend-ingest-p2-closure/1076-VERIFICATION.md \
         .planning/phases/1076-backend-ingest-p2-closure/1076-SUMMARY.md \
         .planning/STATE.md \
         .planning/ROADMAP.md \
         .planning/REQUIREMENTS.md
       ```

    3. If any production-code or test files appear in `git status` (meaning a per-plan executor didn't commit), stage them with explicit `git add &lt;path&gt;` per file and include them in this commit. The commit message should reflect that.

    4. Commit with HEREDOC:
       ```
       git commit -m "$(cat &lt;&lt;'EOF'
       docs(1076): close Phase 1076 — Backend Ingest P2 (ING-02..07) verified

       All 5 ING requirements closed:
       - ING-02: metadata.py phase-2 commit boundary + regression test
       - ING-03: local-storage COG streaming via storage.get_stream()
       - ING-04: worker exports temp-dir mtime guard
       - ING-06: _apply_reupload_swap lock_timeout single retry
       - ING-07: strict_cog opt-in flag on RasterCommitRequest

       See .planning/phases/1076-backend-ingest-p2-closure/1076-SUMMARY.md
       and 1076-VERIFICATION.md for evidence.
       EOF
       )"
       ```

    5. Verify `git status` is clean after the commit; run `git log --oneline -5` to confirm the commit landed.

    DO NOT push. The user (or the next phase orchestrator) will push when v1017 ships under the public `v1.5.2` tag at the v1079 close-gate.

    DO NOT add any AI/bot/Claude footer per CLAUDE.md.
  </action>
  <verify>
    <automated>git status --porcelain | wc -l | awk '{exit ($1 == 0) ? 0 : 1}'</automated>
    <!-- 0 = clean working tree after commit -->
  </verify>
  <done>
    - `git status` reports clean
    - `git log --oneline -3` shows the close commit on top with the message starting `docs(1076):`
    - No `--no-verify` was used (pre-commit hooks ran clean OR were not present)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan is documentation + verification. It does not modify any production code. The only "boundary" worth modeling is the integrity of the test evidence chain.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1076-22 | Repudiation | VERIFICATION.md test counts could be fabricated | mitigate | Test commands captured verbatim with their actual exit codes; re-runnable by any operator; static grep verifications cross-check the production-code state independent of test results |
| T-1076-23 | Tampering | STATE.md / ROADMAP.md / REQUIREMENTS.md updates accidentally drop unrelated entries | mitigate | Use Edit (not Write) on these files so non-target lines stay byte-identical; `git diff` review before commit |
| T-1076-SC | Tampering | npm/pip/cargo installs | accept | Pure planning-doc updates + git commit; no installs. |
</threat_model>

<verification>
- `.planning/phases/1076-backend-ingest-p2-closure/1076-VERIFICATION.md` exists (&gt;50 lines, frontmatter `verdict: PASS`)
- `.planning/phases/1076-backend-ingest-p2-closure/1076-SUMMARY.md` exists (&gt;30 lines, frontmatter `requirements: [ING-02, ING-03, ING-04, ING-06, ING-07]`)
- `grep "1076-06-PLAN.md" .planning/ROADMAP.md` returns at least 1 hit (plan-list line present)
- `grep -E "ING-(02\|03\|04\|06\|07).*Complete" .planning/REQUIREMENTS.md` returns 5 hits
- `git log --oneline -3 | grep "docs(1076):"` returns 1 hit
- `git status --porcelain` produces no output (clean tree)
</verification>

<success_criteria>
1. All targeted regression tests run and pass; results captured in VERIFICATION.md.
2. Full ingest sweep passes — no plan regressed another plan's surface.
3. Phase-level SUMMARY.md cross-references all 5 per-plan SUMMARYs.
4. STATE.md, ROADMAP.md, REQUIREMENTS.md reflect Phase 1076 closure.
5. Single commit `docs(1076): close Phase 1076 ...` carries the close-gate state.
6. `git status` clean after commit; ready for Phase 1077/1078/1079 to start.
</success_criteria>

<output>
This plan IS the phase close-gate. The `1076-SUMMARY.md` created in Task 2 is the phase-level summary. No further per-plan SUMMARY is required for this plan, but the executor should note in the commit message that this plan acted as the verification phase.
</output>
