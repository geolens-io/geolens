---
phase: 1085-pytest-n-auto-stabilization
plan: 02
type: execute
wave: 2
depends_on: [1085-01]
files_modified:
  - backend/tests/conftest.py
  - db/postgresql.conf
  - docker-compose.yml
  - Makefile
  - backend/tests/test_conftest_pool_sizing.py
autonomous: true
requirements: [TD-10]
must_haves:
  truths:
    - "`pytest -n auto backend/tests/` completes with exit code 0 against the rebuilt stack — no `asyncpg.exceptions.CannotConnectNowError`, no `asyncpg.exceptions.ConnectionFailureError`, no `FATAL: sorry, too many clients already`, no `InvalidCatalogNameError` for any worker test DB"
    - "Sequential `cd backend && env $(...env.test...) uv run pytest tests/` still passes at 3025+ / 0 failures (the v1018 baseline must not regress in sequential mode)"
    - "The fix lives in exactly the file path(s) named by Section 5 of `.planning/audits/PYTEST-XDIST-SPIKE-v1019.md` — no scope creep, no unrelated edits"
    - "A regression-pin test (`backend/tests/test_conftest_pool_sizing.py` or shape-appropriate equivalent) locks in the chosen knob so a future maintainer reverting the fix breaks the test, not the suite"
    - "The chosen-shape rationale from the spike doc is restated in a same-file comment at the modified site so future grep'ers find the WHY at the WHERE"
    - "No `pytest.mark.skip` decorators added to make any gate pass"
  artifacts:
    - path: "backend/tests/conftest.py"
      provides: "Per-worker asyncpg pool sized to live within Postgres max_connections (shape (a) — default if spike picks it)"
      contains: "PYTEST_XDIST_WORKER"
    - path: "db/postgresql.conf"
      provides: "Bumped max_connections ceiling (shape (b) — only if spike picks it)"
      contains: "max_connections"
    - path: "docker-compose.yml"
      provides: "May require shared_buffers / memory bump alongside max_connections (shape (b) only)"
      contains: "memory"
    - path: "Makefile"
      provides: "Capped `-n` test target (shape (c) — only if spike picks it)"
      contains: "test-parallel"
    - path: "backend/tests/test_conftest_pool_sizing.py"
      provides: "Regression pin for the chosen knob — asserts the fix is in place"
      contains: "PYTEST_XDIST_WORKER"
  key_links:
    - from: ".planning/audits/PYTEST-XDIST-SPIKE-v1019.md"
      to: "backend/tests/conftest.py (or db/postgresql.conf or Makefile)"
      via: "Section 5 chosen fix shape names the file path(s) to modify"
      pattern: "chosen.*shape|Plan 02 will touch"
    - from: "backend/tests/conftest.py (shape (a) site)"
      to: "backend/tests/test_conftest_pool_sizing.py"
      via: "regression test imports the pool-sizing helper and asserts behavior across worker_id values"
      pattern: "PYTEST_XDIST_WORKER|pool_size"
    - from: "db/postgresql.conf (shape (b) site)"
      to: "docker-compose.yml `db` service `shared_buffers` reservation"
      via: "max_connections × work_mem must fit within shared_buffers + memory cap"
      pattern: "shared_buffers|memory: 768M"
---

<objective>
Apply the fix shape chosen in `.planning/audits/PYTEST-XDIST-SPIKE-v1019.md` Section 5 — one of (a) per-worker pool sizing in `backend/tests/conftest.py`, (b) `max_connections` bump in `db/postgresql.conf` (+ docker-compose memory headroom if needed), or (c) cap `-n` at 4 or 8 in `Makefile`. Pin the chosen knob with a regression test, restate the rationale in a same-file comment, and verify both `pytest -n auto backend/tests/` (rebuilt stack, zero cascade errors) and sequential `uv run pytest backend/tests/` (3025+/0 baseline preserved) pass.

Purpose: Close TD-10. The spike doc is the single source of truth for which file(s) to edit. The executor MUST read the spike doc's Section 5 BEFORE picking a task path; the three shape-tasks below are MUTUALLY EXCLUSIVE — exactly ONE is executed based on the spike's choice.

Output: The minimum diff that lands the chosen fix + a regression-pin test that locks the knob in place + a green run on both `-n auto` and sequential modes. No scope creep — do NOT mix shapes (e.g., don't bump `max_connections` AND change pool sizing); the spike picked ONE shape for a reason.

Critical context — Phase 1085-01 dependency:
- This plan CANNOT execute without `.planning/audits/PYTEST-XDIST-SPIKE-v1019.md` committed.
- Pre-flight in Task 1: read the spike doc's Section 5 to determine which downstream task to execute.
- If the spike doc is missing or Section 5 doesn't pick exactly one shape, STOP and return to Plan 01 for repair.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/1085-pytest-n-auto-stabilization/1085-CONTEXT.md
@.planning/phases/1085-pytest-n-auto-stabilization/1085-01-SUMMARY.md
@.planning/audits/PYTEST-XDIST-SPIKE-v1019.md
@.planning/audits/PYTEST-BASELINE-v1018.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

**Spike doc contract** (`.planning/audits/PYTEST-XDIST-SPIKE-v1019.md` — read Section 5 FIRST):
  Section 5 names exactly ONE chosen fix shape — (a), (b), or (c) — and the file path(s) Plan 02 modifies.
  If Section 5 is missing or ambiguous, this plan is BLOCKED on Plan 01 repair.

**Shape (a) site** — `backend/tests/conftest.py:354-360`:
  test_engine = create_async_engine(
      settings.test_database_url,
      pool_size=5,
      max_overflow=2,
      pool_timeout=30,
      echo=False,
  )

  # The xdist worker id is captured in pytest_configure() and surfaced at runtime
  # via os.environ["PYTEST_XDIST_WORKER"] (e.g. "gw0", "gw1", ..., or "master"
  # when xdist is inactive). To scale pool sizing per worker, read the env var
  # at fixture time and derive pool_size / max_overflow from the worker count.
  #
  # Worker count detection — three approaches in order of robustness:
  #   1. config.workerinput["workerinput"] inside pytest_configure (not visible
  #      to the client fixture body)
  #   2. Parse PYTEST_XDIST_WORKER_COUNT env var — set by some xdist versions
  #      but NOT guaranteed by 3.6+; check via `pytest --help | grep workercount`
  #      against the installed version
  #   3. Simplest: assume `-n auto` → workers == os.cpu_count() and use a
  #      conservative per-worker budget that lives within `max_connections=30`
  #      regardless of host. e.g. `pool_size=1, max_overflow=1` per worker →
  #      2 conn × 16 workers = 32, ONE over ceiling (acceptable if pool_timeout
  #      absorbs the queue), OR `pool_size=1, max_overflow=0` → 16 conn ≤ 30.
  #
  # The fixture body runs PER TEST (function-scope), so the engine is created
  # 3000+ times per worker — but only ONE is live at a time per worker. The
  # pool ceiling is per-engine-instance, not cumulative.

**Shape (b) site** — `db/postgresql.conf:11`:
  max_connections = 30                   # PERF-05 (Phase 274): matches v13.13
                                         # worst-case API(16) + Worker(13) + Admin(1).

  # Bumping max_connections has memory consequences. Each connection ~10MB of
  # shared_buffers overhead + work_mem * sort operations. Current sizing:
  #   shared_buffers = 512MB (db/postgresql.conf:60)
  #   work_mem = 8MB (db/postgresql.conf:61)
  #   db container memory cap = 768M (docker-compose.yml:113)
  #
  # Math: 30 × 10MB + work_mem peaks ≈ 300-350MB → fits 768M cap. Bumping to
  # 150 → 1500MB of overhead alone, FAR past the 768M cap → OOM. A shape (b)
  # fix MUST also bump docker-compose.yml's db service memory limit (likely
  # to 1G or 1.5G), which perturbs the 2GB VPS budget calculations at
  # docker-compose.yml:60-77. If the spike picks (b), the doc-comment in
  # docker-compose.yml around the 2GB budget needs an update too.

**Shape (c) site** — `Makefile:27-31` (existing `test` target) + a new `test-parallel` target:
  test:
  	docker compose exec api uv run pytest -v --tb=short

  # Note: the existing `test` target runs INSIDE the api container via
  # `docker compose exec api` — not the host. For the host xdist scenario
  # (the one this plan fixes), the invocation is:
  #   cd backend && env $(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) \
  #     uv run pytest -n auto tests/
  # which lives outside the Makefile today. Shape (c) needs to add a new
  # `test-parallel` target that captures `-n 4` or `-n 8` for host invocations,
  # AND optionally inject `addopts = "-m 'not perf' -n 4"` in
  # backend/pyproject.toml:82 to make `pytest tests/` default to capped
  # parallelism on the host.
  #
  # Trade-off: changing pyproject.toml's addopts is a global change. A safer
  # shape (c) keeps addopts unchanged and only adds the new Makefile target.

**Regression test scaffold** (`backend/tests/test_conftest_pool_sizing.py` — new file, shape-dependent):

  # Shape (a) scaffold — asserts pool_size derivation responds to PYTEST_XDIST_WORKER:
  import os
  import pytest
  from sqlalchemy.ext.asyncio import create_async_engine
  from app.core.config import settings
  # Import the helper that conftest uses to derive pool sizing
  # (the executor introduces this helper in the conftest edit).
  from tests.conftest import _derive_test_pool_sizing  # or whatever name is chosen

  def test_pool_sizing_for_master_session_is_unchanged(monkeypatch):
      """Sequential pytest (worker_id=master) keeps the historical (5, 2) pool."""
      monkeypatch.setenv("PYTEST_XDIST_WORKER", "master")
      size, overflow = _derive_test_pool_sizing()
      assert size == 5 and overflow == 2

  def test_pool_sizing_for_xdist_worker_lives_within_max_connections(monkeypatch):
      """Under xdist (gw0/gw1/...), pool sizing scales DOWN per worker."""
      monkeypatch.setenv("PYTEST_XDIST_WORKER", "gw0")
      size, overflow = _derive_test_pool_sizing()
      # Per-worker ceiling × worker count must live within max_connections=30
      # with admin headroom (~4 conn for psql + alembic + autovac).
      assert (size + overflow) * 16 + 4 <= 30, (
          f"Pool {size}+{overflow}=7 per worker × 16 workers = {(size+overflow)*16} "
          f"exceeds the max_connections=30 ceiling (PERF-05). Re-run the spike."
      )

  # Shape (b) scaffold — asserts max_connections is set to the spike's chosen number:
  # def test_postgresql_conf_max_connections_bumped():
  #     conf = pathlib.Path(__file__).parent.parent.parent / "db" / "postgresql.conf"
  #     content = conf.read_text()
  #     # Spike chose XXX → assert that line is present
  #     assert "max_connections = <SPIKE_NUMBER>" in content

  # Shape (c) scaffold — asserts the Makefile has a `test-parallel` target with capped `-n`:
  # def test_makefile_test_parallel_target_caps_n():
  #     mk = pathlib.Path(__file__).parent.parent.parent / "Makefile"
  #     content = mk.read_text()
  #     assert "test-parallel:" in content
  #     # Spike chose -n 4 or -n 8 → assert presence
  #     assert re.search(r"pytest.*-n\s+[48]\s", content)

**Sequential-mode invariant** (`.planning/audits/PYTEST-BASELINE-v1018.md`):
  Sequential `cd backend && env $(.env.test) uv run pytest tests/` produces:
  3025 passed, 0 failed, 38 skipped, 14 deselected in ~539s on the spike host.
  The fix MUST NOT regress this — if shape (a) reduces pool_size below 5 in
  sequential mode, request handlers that need >1 concurrent DB conn within a
  single test will block on `pool_timeout=30`. Hence the regression test above
  pins the sequential case (worker_id=master) at the historical (5, 2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read the spike doc, branch to the shape-specific edit, apply the fix + regression pin</name>
  <files>backend/tests/conftest.py, db/postgresql.conf, docker-compose.yml, Makefile, backend/tests/test_conftest_pool_sizing.py</files>
  <read_first>
    - .planning/audits/PYTEST-XDIST-SPIKE-v1019.md (MANDATORY — Section 5 names which file to edit; if Section 5 is missing or ambiguous, STOP and surface the gap)
    - .planning/phases/1085-pytest-n-auto-stabilization/1085-01-SUMMARY.md (confirms which sampling strategy was used and what numbers were observed)
    - backend/tests/conftest.py (focus on lines 25-40 for xdist worker capture, lines 90-109 for per-worker DB naming, lines 354-360 for the current `(5, 2)` pool sizing — modify ONLY if spike picked shape (a))
    - db/postgresql.conf (focus on line 11 — modify ONLY if spike picked shape (b))
    - docker-compose.yml (focus on lines 79-116 — modify ONLY if spike picked shape (b) AND memory cap needs a bump; see Shape (b) site notes in interfaces)
    - Makefile (focus on lines 27-31 — add new `test-parallel` target ONLY if spike picked shape (c); do NOT modify existing `test` target which runs INSIDE the api container)
    - backend/pyproject.toml (focus on lines 80-88 — pytest addopts; modify ONLY if spike's shape (c) rationale specifically calls for an `addopts` injection)
    - .planning/audits/PYTEST-BASELINE-v1018.md (sequential baseline: 3025/0/38 in 539s — the regression floor)
  </read_first>
  <action>
    Step 0 — Pre-flight. Read `.planning/audits/PYTEST-XDIST-SPIKE-v1019.md` Section 5. Extract:
    - The chosen fix shape: (a), (b), or (c) — exactly one
    - The file(s) Plan 02 modifies: one or two file paths

    If Section 5 picks more than one shape, or is missing, or doesn't name a file path: STOP. Surface a gap report ("Plan 02 blocked — spike doc Section 5 ambiguous: <quote the offending text>") and return to the orchestrator. Do NOT guess.

    Step 1 — Branch to the shape-specific edit. Execute exactly ONE of the three sub-tasks below based on the spike's choice. Do NOT execute more than one. Do NOT add unrelated edits to files outside the spike's named scope.

    ─── Shape (a): Per-worker pool sizing in `backend/tests/conftest.py` ───

    1a-1. Introduce a small helper near the top of `backend/tests/conftest.py` (alongside the existing `_WORKER_ID` capture at line 22), named `_derive_test_pool_sizing()` (or a name the executor prefers). It returns a `(pool_size, max_overflow)` tuple based on `os.environ.get("PYTEST_XDIST_WORKER", "master")`:

      def _derive_test_pool_sizing() -> tuple[int, int]:
          """Return (pool_size, max_overflow) sized to live within Postgres max_connections.

          Sequential mode (worker_id == "master") keeps the historical (5, 2) pool that
          the suite was built against. Under pytest-xdist, the per-worker pool is scaled
          DOWN so the total fan-out (workers × per-worker conn) lives within the
          max_connections=30 ceiling set in db/postgresql.conf:11 (PERF-05 / Phase 274).

          Math (rationale from `.planning/audits/PYTEST-XDIST-SPIKE-v1019.md`):
              - max_connections = 30
              - admin headroom (psql + alembic + autovac) ≈ 4 conn
              - usable budget = 30 − 4 = 26
              - assume `-n auto` worst case = 16 workers (M-series macOS host)
              - per-worker budget = floor(26 / 16) = 1 conn → use pool_size=1, max_overflow=1
                (overflow gives 1 slot of burst headroom; pool_timeout=30 absorbs queueing)

          The (5, 2) sequential default is preserved because request handlers in some
          tests (e.g., reupload, IDOR) need >1 concurrent conn within a single test.
          The v1018 sequential baseline (3025/0/38 in 539s) is the regression floor.
          """
          worker_id = os.environ.get("PYTEST_XDIST_WORKER", "master")
          if worker_id == "master":
              return (5, 2)
          # Under xdist: per-worker = 1 + 1 = 2 conn (worst case 16 × 2 = 32, ONE
          # over ceiling, absorbed by pool_timeout=30; see SPIKE doc for measured
          # numbers).
          return (1, 1)

    1a-2. Modify the `create_async_engine` call at lines 354-360 to use the derived sizing:

      pool_size, max_overflow = _derive_test_pool_sizing()
      test_engine = create_async_engine(
          settings.test_database_url,
          pool_size=pool_size,
          max_overflow=max_overflow,
          pool_timeout=30,
          echo=False,
      )

    1a-3. Restate the rationale in a same-file comment ABOVE the `create_async_engine` block (4-6 lines). Reference the SPIKE doc by path so future grep'ers find the WHY:

      # Pool sizing is derived per pytest-xdist worker via _derive_test_pool_sizing()
      # to live within Postgres max_connections=30 (db/postgresql.conf:11). Under
      # `-n auto` (16 workers on M-series macOS), per-worker = 1+1=2 conn so total
      # fan-out (32) is one over ceiling, absorbed by pool_timeout=30. Sequential
      # mode (worker_id=master) keeps the historical (5, 2) for the request handlers
      # that need concurrent DB conns within a single test. See
      # .planning/audits/PYTEST-XDIST-SPIKE-v1019.md for measured numbers + rationale.

    1a-4. Create `backend/tests/test_conftest_pool_sizing.py` with the regression-pin tests from the interfaces scaffold (shape (a) version — both `test_pool_sizing_for_master_session_is_unchanged` and `test_pool_sizing_for_xdist_worker_lives_within_max_connections`).

    Files modified (shape (a)): `backend/tests/conftest.py` + `backend/tests/test_conftest_pool_sizing.py`. Do NOT touch `db/postgresql.conf`, `docker-compose.yml`, or `Makefile`.

    ─── Shape (b): max_connections bump in `db/postgresql.conf` (+ memory headroom) ───

    1b-1. Edit `db/postgresql.conf:11`:

      max_connections = <SPIKE_CHOSEN_NUMBER>   # PERF-05 (Phase 274) updated for v1019 TD-10:
                                                # bumped from 30 to <N> to accommodate
                                                # `pytest -n auto` xdist fan-out (16 workers
                                                # × per-worker pool = 7 → up to 112). See
                                                # .planning/audits/PYTEST-XDIST-SPIKE-v1019.md
                                                # for the measured numbers + rationale.

    1b-2. Check shared_buffers + memory math. If `<SPIKE_CHOSEN_NUMBER>` is > ~80 (the shared_buffers=512M ceiling for connection overhead at ~10MB each + work_mem peaks), bump:
       - `db/postgresql.conf:60` shared_buffers (e.g., 512MB → 768MB)
       - `docker-compose.yml:113` db service memory limit (e.g., 768M → 1G or 1.5G)
       - Update the 2GB VPS budget comment block at `docker-compose.yml:60-77` to reflect the new sum
       - If `<SPIKE_CHOSEN_NUMBER>` is ≤ 60, the existing shared_buffers=512M + memory=768M envelope is fine — only the conf line changes.

    1b-3. Restate the rationale in a same-file comment in `db/postgresql.conf` (already inline above), AND if the docker-compose memory cap changed, add a same-line comment at `docker-compose.yml:113`:

      memory: 1G   # TD-10 v1019: bumped from 768M to accommodate max_connections=<N>

    1b-4. Create `backend/tests/test_conftest_pool_sizing.py` with the shape (b) regression scaffold (asserts `db/postgresql.conf` contains the new `max_connections = <N>` line).

    1b-5. Rebuild the db container: `docker compose up -d --build db && docker compose exec db psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -c "SHOW max_connections;"` and confirm the new value is live.

    Files modified (shape (b)): `db/postgresql.conf` + (conditionally) `docker-compose.yml` + `backend/tests/test_conftest_pool_sizing.py`. Do NOT touch `backend/tests/conftest.py` or `Makefile`.

    ─── Shape (c): Cap `-n` in Makefile (+ optionally pyproject.toml addopts) ───

    1c-1. Add a new target to `Makefile` (BELOW the existing `test:` target at lines 27-31). Use the cap chosen in the spike (4 or 8):

      # TD-10 v1019: capped-parallelism host test target. Existing `test:` target runs
      # INSIDE the api container via `docker compose exec api` — that path doesn't hit
      # the host xdist fan-out. `test-parallel` is the host invocation, capped at -n <N>
      # to live within Postgres max_connections=30 (db/postgresql.conf:11). See
      # .planning/audits/PYTEST-XDIST-SPIKE-v1019.md for measured numbers + rationale.
      test-parallel:
      	cd backend && env $$(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) \
      	  uv run pytest -n <N> tests/

      Where `<N>` is the spike-chosen cap (4 or 8). Update the `.PHONY:` line at Makefile:9 to include `test-parallel`.

    1c-2. (Conditional, ONLY if spike doc Section 5 explicitly calls for it.) Inject `-n <N>` into `backend/pyproject.toml:82` addopts. Default: do NOT modify pyproject.toml; the capped behavior is opt-in via `make test-parallel`. The default `pytest tests/` invocation keeps current sequential shape so the v1018 baseline (3025/0/38) is unchanged.

    1c-3. Create `backend/tests/test_conftest_pool_sizing.py` with the shape (c) regression scaffold (asserts `Makefile` contains the new `test-parallel:` target with a capped `-n` flag).

    Files modified (shape (c)): `Makefile` + (conditionally) `backend/pyproject.toml` + `backend/tests/test_conftest_pool_sizing.py`. Do NOT touch `backend/tests/conftest.py`, `db/postgresql.conf`, or `docker-compose.yml`.

    ─── End of shape branches ───

    Step 2 — Common post-edit assertions (apply regardless of chosen shape):
    - The regression-pin test file (`backend/tests/test_conftest_pool_sizing.py`) exists with at least one test
    - NO `pytest.mark.skip` decorators were added
    - The same-file comment near the modified site names the rationale and references the spike doc by path
  </action>
  <verify>
    <automated>test -f backend/tests/test_conftest_pool_sizing.py && cd /Users/ishiland/Code/geolens/backend && env $(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) uv run pytest tests/test_conftest_pool_sizing.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_conftest_pool_sizing.py` exists and `uv run pytest tests/test_conftest_pool_sizing.py -x` exits 0
    - Exactly ONE of (a)/(b)/(c) was applied — the diff matches the file scope named by spike doc Section 5
    - If shape (a): `git diff backend/tests/conftest.py` shows a `_derive_test_pool_sizing()` helper + modified `create_async_engine` call + the same-file rationale comment; `db/postgresql.conf`, `docker-compose.yml`, `Makefile` are unchanged
    - If shape (b): `git diff db/postgresql.conf` shows the bumped `max_connections` line; `docker-compose.yml` may show a memory cap bump (only if spike's chosen number requires it); `backend/tests/conftest.py`, `Makefile` are unchanged; the live `docker compose exec db psql -c "SHOW max_connections;"` returns the new number
    - If shape (c): `git diff Makefile` shows a new `test-parallel:` target with `-n <4|8>`; `backend/tests/conftest.py`, `db/postgresql.conf`, `docker-compose.yml` are unchanged
    - NO `pytest.mark.skip` decorators added to any file (`git diff | grep -c "pytest.mark.skip"` ≤ count in baseline)
    - A same-file comment at the modified site references `.planning/audits/PYTEST-XDIST-SPIKE-v1019.md` by path
  </acceptance_criteria>
  <done>
    The chosen fix shape is applied at the spike-named file(s) only, the regression-pin test passes in isolation, and the rationale is restated near the edit. Plan 02's verification (Task 2 below) confirms the end-to-end cascade is gone and the sequential baseline holds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify pytest -n auto green + sequential baseline preserved against rebuilt stack</name>
  <files>backend/tests/conftest.py (read-only verification), .planning/phases/1085-pytest-n-auto-stabilization/1085-02-SUMMARY.md</files>
  <read_first>
    - .planning/audits/PYTEST-XDIST-SPIKE-v1019.md (the chosen shape determines which command to invoke for the xdist run — shape (c) uses `make test-parallel`; shapes (a)/(b) use `pytest -n auto` directly)
    - .planning/audits/PYTEST-BASELINE-v1018.md (sequential baseline: 3025/0/38/14 in ~539s — the regression floor)
    - backend/tests/conftest.py (only to confirm the edit from Task 1 is still in place; do NOT modify)
  </read_first>
  <action>
    Run two pytest invocations against the live (rebuilt-where-needed) stack and confirm both pass.

    Step 1 — Stack-up. If shape (b) was chosen, ensure the rebuild from Task 1 step 1b-5 stuck: `docker compose ps db` shows healthy, `docker compose exec db psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -c "SHOW max_connections;"` returns the bumped number. For shapes (a) and (c), no rebuild needed — the existing db container is fine.

    Step 2 — Sequential mode verification (the regression floor). Run:

      cd backend && env $(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) \
        uv run pytest tests/ 2>&1 | tee /tmp/v1019-seq.log

    Expected: `=== 3025+ passed, 0 failed, 38 skipped, 14 deselected in ~5XXs ===` (matches v1018 baseline shape; passed count may exceed 3025 if the new regression-pin tests in `test_conftest_pool_sizing.py` add 2-3 tests, which is fine).

    Acceptance: exit 0, failed = 0, passed ≥ 3025, `InvalidCatalogNameError` count in log = 0.

    Step 3 — Parallel mode verification (the TD-10 close). Pick the right invocation based on the chosen shape:

      # Shape (a) or (b): host invocation directly
      cd backend && env $(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) \
        uv run pytest -n auto tests/ 2>&1 | tee /tmp/v1019-parallel.log

      # Shape (c): use the new Makefile target (or direct -n 4/-n 8 invocation per spike)
      make test-parallel 2>&1 | tee /tmp/v1019-parallel.log

    Expected: exit 0, failed = 0, no `asyncpg.exceptions.CannotConnectNowError`, no `asyncpg.exceptions.ConnectionFailureError`, no `FATAL: sorry, too many clients already`, no `InvalidCatalogNameError`. Wall-clock should be ≤ sequential (parallelism is the whole point — if it's not faster, the cap was too aggressive).

    Acceptance grep:

      grep -ciE "CannotConnectNowError|ConnectionFailureError|too many clients already|InvalidCatalogNameError" /tmp/v1019-parallel.log

    MUST return 0. Any non-zero count is a fix-failure — return to Task 1 to revisit the spike's chosen shape or its parameter (e.g., shape (a) per-worker pool too generous; shape (b) max_connections still too low; shape (c) cap too high).

    Step 4 — Write the plan SUMMARY at `.planning/phases/1085-pytest-n-auto-stabilization/1085-02-SUMMARY.md` capturing:
    - Which shape was chosen (a/b/c) — one line, matches Plan 01 SUMMARY
    - Diff scope (files modified) — list
    - Sequential run result — `passed/failed/skipped/wall-clock` from `/tmp/v1019-seq.log`
    - Parallel run result — `passed/failed/skipped/wall-clock` from `/tmp/v1019-parallel.log` + the four-error grep count (which MUST be 0)
    - Delta vs v1018 baseline — passed delta, wall-clock delta sequential, wall-clock parallel-vs-sequential ratio
    - Regression pin — name of the test file + its passing assertion
    - REQUIREMENTS.md TD-10 traceability — confirm the checkbox is flipped to `[x]` in the same commit (per TD-13 process tightening lesson from v1018 — executor flips the checkbox before commit)

    Step 5 — Flip the TD-10 checkbox in `.planning/REQUIREMENTS.md`. Find the line:

      - [ ] **TD-10**: Stabilize `pytest -n auto` on 16 xdist workers ...

    and change `[ ]` to `[x]`. ALSO update the traceability table at the bottom of REQUIREMENTS.md:

      | TD-10 | Phase 1085 | Pending |  →  | TD-10 | Phase 1085 | Complete |

    This is the v1018 TD-13 lesson applied prophylactically — the executor MUST flip the checkbox before committing, not leave it for the auditor to catch mid-audit.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && env $(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) uv run pytest -n auto tests/ 2>&1 | tee /tmp/v1019-parallel-verify.log && grep -ciE "CannotConnectNowError|ConnectionFailureError|too many clients already|InvalidCatalogNameError" /tmp/v1019-parallel-verify.log | grep -q "^0$" && cd /Users/ishiland/Code/geolens/backend && env $(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) uv run pytest tests/ -x --tb=no -q 2>&1 | tail -3 | grep -E "^[0-9]+ passed"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -n auto backend/tests/` (or `make test-parallel` for shape (c)) exits 0
    - `grep -ciE "CannotConnectNowError|ConnectionFailureError|too many clients already|InvalidCatalogNameError" /tmp/v1019-parallel.log` returns exactly `0`
    - Sequential `uv run pytest backend/tests/` exits 0 with `passed ≥ 3025` and `failed = 0` (the regression floor from PYTEST-BASELINE-v1018.md holds)
    - Sequential `skipped` count is within ±2 of the v1018 baseline of 38 (new regression-pin tests in `test_conftest_pool_sizing.py` may add 2-3 tests but none should be skipped)
    - Parallel run completes faster than sequential (parallel < sequential wall-clock) — sanity check that the chosen cap actually exercises parallelism
    - `.planning/REQUIREMENTS.md` TD-10 line shows `[x]` (checkbox flipped) AND the traceability table shows `TD-10 | Phase 1085 | Complete`
    - `.planning/phases/1085-pytest-n-auto-stabilization/1085-02-SUMMARY.md` exists with all six items from Step 4 captured
    - No new `pytest.mark.skip` decorators introduced anywhere
  </acceptance_criteria>
  <done>
    `pytest -n auto` runs green against the rebuilt stack with zero cascade-error signatures in the log, the sequential baseline of 3025+/0/38 is preserved, the regression-pin test in `test_conftest_pool_sizing.py` passes in isolation, REQUIREMENTS.md TD-10 is checked off + traceability updated, and the 1085-02-SUMMARY.md captures the full close-loop measurement vs the v1018 baseline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike doc Section 5 → Plan 02 executor | The spike doc names the shape and file. If the executor picks a different shape (e.g., "shape (a) is easier, let me do that even though spike picked (b)"), the fix lands in the wrong scope and either misses the cascade root cause or introduces unrelated config drift. Pre-flight check at Task 1 Step 0 is the safeguard. |
| test conftest pool sizing → asyncpg pool runtime | Shape (a) edits a hot-path fixture that runs 3000+ times per worker. An off-by-one in the math (e.g., returning (0, 0) for an unknown worker_id, or returning (5, 2) under xdist accidentally) breaks the suite outright. Regression test pins both branches (master + xdist) explicitly. |
| docker-compose memory cap → 2GB VPS budget | Shape (b)'s memory headroom bump (if needed) changes the 2GB VPS sum documented at docker-compose.yml:60-77. If the cap goes up but the doc-comment doesn't, future ops reading the budget gets stale numbers. Acceptance criterion includes updating the comment block. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1085-02-01 | Tampering | Plan 02 executor picks the wrong shape (mismatch with spike) | mitigate | Task 1 Step 0 mandates reading Section 5 of the spike doc and matching the shape. The "STOP and surface a gap report" instruction is explicit when Section 5 is ambiguous. |
| T-1085-02-02 | Tampering | Shape (a) helper returns wrong sizing for unknown worker_id | mitigate | `_derive_test_pool_sizing()` has explicit `worker_id == "master"` branch; any non-master string falls through to the xdist clause. Regression test asserts both branches (master keeps (5,2); gw0 returns the scaled-down pair). |
| T-1085-02-03 | Repudiation | Shape (b) max_connections bump without shared_buffers/memory bump | mitigate | Task 1 Step 1b-2 has explicit math: if new max_connections > ~80, shared_buffers AND docker-compose memory cap MUST bump too. If ≤ 60, current envelope is fine. The 60-80 grey zone is the executor's judgment call; spike doc rationale should cover. |
| T-1085-02-04 | DoS | Sequential mode regresses under shape (a) (pool too small) | mitigate | Sequential mode is the `worker_id == "master"` branch which returns the historical `(5, 2)` — explicitly unchanged. Regression test `test_pool_sizing_for_master_session_is_unchanged` pins this. Task 2 Step 2 re-runs the full sequential baseline as a backstop. |
| T-1085-02-05 | DoS | Parallel mode still triggers cascade despite the fix | mitigate | Task 2 Step 3 greps the log for all four cascade-error signatures. Non-zero count is a fix-failure and Task 1 must be revisited (perhaps with a tighter pool, lower max_connections target, or smaller `-n` cap). The verify gate's exit-on-nonzero-grep makes the failure mode loud, not silent. |
| T-1085-02-06 | Information Disclosure | `.env.test` env-var dump in run logs | accept | `/tmp/v1019-*.log` files are local artifacts; `.env.test` itself is gitignored and contains only test credentials (admin/admin per the geolens convention). Logs are not committed. |
| T-1085-02-07 | Tampering | Executor forgets to flip TD-10 checkbox in REQUIREMENTS.md | mitigate | Task 2 Step 5 is an explicit pre-commit step. The v1018 audit caught this exact omission as TD-13 — Plan 02's acceptance criterion makes the flip part of the close-loop, applying the v1018 lesson prophylactically. |

**Block-on:** None — all threats are mitigated by the structured shape-branching + regression pin + cascade-error grep gate.
</threat_model>

<verification>
- Spike doc was read; the chosen shape from Section 5 matches the file(s) modified
- `pytest -n auto backend/tests/` (or `make test-parallel` for shape (c)) exits 0 against the rebuilt stack
- `grep -ciE "CannotConnectNowError|ConnectionFailureError|too many clients already|InvalidCatalogNameError" /tmp/v1019-parallel.log` returns `0`
- Sequential `uv run pytest backend/tests/` exits 0 with `passed ≥ 3025` and `failed = 0`
- `backend/tests/test_conftest_pool_sizing.py` exists and `uv run pytest tests/test_conftest_pool_sizing.py -x` exits 0
- `.planning/REQUIREMENTS.md` TD-10 line shows `[x]` and traceability row shows `Complete`
- `git diff` scope matches spike doc Section 5 — no out-of-scope file edits
- `git diff | grep -c "pytest.mark.skip"` shows no new skip decorators
</verification>

<success_criteria>
- TD-10 closed: `pytest -n auto backend/tests/` runs green against the rebuilt stack with zero cascade-error signatures
- Sequential `uv run pytest backend/tests/` still passes at 3025+/0/38 — the v1018 baseline is preserved
- The fix is in exactly the file(s) the spike doc named — no scope creep
- A regression-pin test locks the chosen knob so a future maintainer's accidental revert is caught at suite-time, not at production-time
- REQUIREMENTS.md TD-10 is checked off (the v1018 TD-13 process lesson is applied)
- 1085-02-SUMMARY.md captures the full close-loop measurement vs the v1018 baseline
</success_criteria>

<output>
Create `.planning/phases/1085-pytest-n-auto-stabilization/1085-02-SUMMARY.md` when done — capture:
- Chosen shape (a/b/c) — must match Plan 01 SUMMARY
- Files modified — list (with line counts)
- Sequential run result — passed/failed/skipped/wall-clock from `/tmp/v1019-seq.log`
- Parallel run result — passed/failed/skipped/wall-clock from `/tmp/v1019-parallel.log` + the four-error grep count (which MUST be 0)
- Delta vs v1018 baseline — passed delta, wall-clock delta sequential, parallel-vs-sequential wall-clock ratio
- Regression pin — name of the new test file + its passing assertion names
- REQUIREMENTS.md TD-10 checkbox flip — confirm `[x]` is in the same commit (TD-13 process lesson applied)
</output>
