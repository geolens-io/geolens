---
phase: 223-bootstrap-infrastructure-lock
plan: 01
subsystem: infra

tags:
  - astro
  - starlight
  - cloudflare-pages
  - sitemap
  - oklch
  - docs-site
  - canonical-url
  - noindex

# Dependency graph
requires:
  - phase: external-getgeolens.com
    provides: marketing-site repo (parity reference for Node engine, wrangler.toml shape, OKLCH primary palette, ci.yml template)

provides:
  - "docs/ subtree inside getgeolens.com repo with independent dependency tree (own package.json, node_modules, dist)"
  - "Astro 6 + Starlight 0.38.4 skeleton that builds cleanly to docs/dist/"
  - "Locked URL structure: sidebar groups under /guides/ (quickstart, user, admin, api) declared upfront in astro.config.mjs"
  - "Canonical URL anchor: site = https://docs.getgeolens.com — pinned in astro.config.mjs"
  - "Belt-and-suspenders noindex: site-wide <meta name='robots' content='noindex, nofollow'> + robots.txt Disallow: /"
  - "Legacy URL redirects: 9 rules (3 paths × 3 variants) for /install, /admin, /api → /guides/* (MIG-02)"
  - "Sitemap generation: @astrojs/sitemap integrated, sitemap-index.xml + sitemap-0.xml emitted at build"
  - "Load-bearing verify-build.sh script asserting all 'forever-wrong if skipped' invariants on every build"
  - "Minimal Starlight accent placeholder (custom.css) using GeoLens primary blue (~hue 250) — full token bridge deferred to Phase 224"

affects:
  - 223-02 (Plan 02 Wave 2: GitHub Actions docs-ci.yml + marketing ci.yml paths-ignore patch + CF Pages dashboard config + custom domain attachment)
  - 224 (BRAND-01 full OKLCH 50-950 token mapping; BRAND-02 Inter font; SHELL-01..05 sidebar labels/nav/edit-this-page; SEARCH-01..03 Pagefind config)
  - 225 (API-01..05 openapi.json + starlight-openapi plugin)
  - 226 (Quickstart content under /guides/quickstart and /guides/install)
  - 227 (User/admin guide content under /guides/user, /guides/admin; CONTRIBUTING.md _redirects convention)
  - 228 (SEO go-live: robots.txt flip, noindex removal, sitemap submission to GSC, GA4 install on both sites)

# Tech tracking
tech-stack:
  added:
    - "astro@^6.1.3 (matches marketing pin — D-20)"
    - "@astrojs/starlight@^0.38.4 (peerDeps satisfied: astro ^6.0.0)"
    - "@astrojs/sitemap@^3.7.2"
    - "@astrojs/check@^0.9.8"
    - "typescript@^5.9.3"
  patterns:
    - "Independent docs/ subtree inside marketing repo — single git repo, two CF Pages projects, two CI workflows (Plan 02 owns workflow files)"
    - "Belt-and-suspenders noindex (robots.txt + meta) flips together in Phase 228, never piecemeal (D-07/D-08)"
    - "URL structure lock via empty sidebar groups: autogenerate directories declared upfront so future phases cannot regress to flat URLs (D-11)"
    - "Three-rule redirect pattern per legacy path: /foo, /foo/, /foo/* with :splat (D-15)"
    - "Load-bearing build-artifact assertion script (verify-build.sh) called in CI as the gate"

key-files:
  created:
    - "/Users/ishiland/Code/getgeolens.com/docs/package.json (pinned deps, Node ≥22.12.0)"
    - "/Users/ishiland/Code/getgeolens.com/docs/package-lock.json (generated by npm install)"
    - "/Users/ishiland/Code/getgeolens.com/docs/tsconfig.json (extends astro/tsconfigs/strict)"
    - "/Users/ishiland/Code/getgeolens.com/docs/astro.config.mjs (site, customCss, noindex head, /guides/ sidebar groups, sitemap)"
    - "/Users/ishiland/Code/getgeolens.com/docs/wrangler.toml (getgeolens-docs, compatibility_date 2025-01-01)"
    - "/Users/ishiland/Code/getgeolens.com/docs/.nvmrc (20)"
    - "/Users/ishiland/Code/getgeolens.com/docs/.gitignore (node_modules, dist, .astro)"
    - "/Users/ishiland/Code/getgeolens.com/docs/README.md (local dev pointer + phase boundaries)"
    - "/Users/ishiland/Code/getgeolens.com/docs/src/content.config.ts (docsLoader + docsSchema — required by Starlight 0.38)"
    - "/Users/ishiland/Code/getgeolens.com/docs/src/content/docs/index.mdx (stub homepage, planned-URL TOC anchoring /guides/)"
    - "/Users/ishiland/Code/getgeolens.com/docs/src/styles/custom.css (3 Starlight accent slots, light + dark)"
    - "/Users/ishiland/Code/getgeolens.com/docs/public/favicon.svg (byte-identical copy from marketing)"
    - "/Users/ishiland/Code/getgeolens.com/docs/public/robots.txt (Disallow: / + sitemap declaration)"
    - "/Users/ishiland/Code/getgeolens.com/docs/public/_redirects (9 redirect rules, /quickstart explicitly excluded)"
    - "/Users/ishiland/Code/getgeolens.com/docs/scripts/verify-build.sh (load-bearing assertion gate)"
  modified: []

key-decisions:
  - "Mirror marketing's Astro ^6.1.3 pin exactly (D-20); Starlight 0.38.4 peerDeps require Astro 6"
  - "wrangler.toml compatibility_date = 2025-01-01 (new project starts fresh; marketing uses 2024-01-01)"
  - "Empty /guides/ sidebar groups declared via autogenerate so Phase 224 sidebar fill-in needs zero astro.config.mjs edits (D-11)"
  - "Belt-and-suspenders noindex (robots.txt + meta tag) — both injected at build, both flip together in Phase 228 (D-07/D-08)"
  - "/quickstart explicitly excluded from docs _redirects (owned by marketing site per HeroSection.astro/Nav.astro, D-14)"
  - "verify-build.sh has NO GA4 grep — SEO-06 deferred to Phase 228 (D-19)"
  - "verify-build.sh has NO Inter font / Pagefind / OG image checks — those mature in phase 224/228"

patterns-established:
  - "Pattern 1: Independent docs/ subtree as Astro project — own package.json, isolated node_modules, gitignored dist; cross-contamination prevented at npm install layer"
  - "Pattern 2: Build-artifact assertions live in scripts/verify-build.sh — fail the build, not just the diff. Keeps invariants enforceable in CI without bespoke YAML logic"
  - "Pattern 3: URL structure pinned upfront via empty Starlight sidebar groups — content phases populate, never restructure"
  - "Pattern 4: Visible URL anchor on stub homepage TOC — anyone landing on the deploy sees /guides/install, /guides/admin, /guides/api before any content ships"

requirements-completed:
  - BOOT-01
  - BOOT-02
  - BOOT-03
  - BOOT-04
  - MIG-02
  - SEO-05
  - CI-02

# Metrics
duration: 5min
completed: 2026-04-25
---

# Phase 223 Plan 01: Bootstrap & Infrastructure Lock Summary

**Astro 6 + Starlight 0.38.4 docs/ skeleton with locked /guides/ URL structure, canonical anchor at docs.getgeolens.com, belt-and-suspenders noindex, 9 legacy redirects, and a load-bearing verify-build.sh that fails the build if any "forever-wrong if skipped" property breaks.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-25T18:02:34Z
- **Completed:** 2026-04-25T18:07:20Z
- **Tasks:** 3
- **Files created:** 15 (in `getgeolens.com/docs/` subtree)
- **Files modified (planning repo):** 1 (STATE.md, three commits)

## Accomplishments

- **Independent docs/ build pipeline shipped:** docs/ subtree with own package.json, package-lock.json (427 packages), node_modules, and dist/. `npm install` completes in ~35s with no contamination of marketing-site deps. T-223-CROSS-CONTAM half mitigated (Plan 02 owns the CI/workflow side).
- **URL structure locked at the config layer:** astro.config.mjs declares four empty sidebar groups (Quickstart, User Guide, Admin Guide, API Reference) with `autogenerate: { directory: 'guides/<group>' }`. Built HTML renders all four group labels visibly. T-223-FLAT-URL mitigated structurally (verify-build.sh asserts no flat URLs in any built HTML).
- **Canonical URL anchor pinned:** `site: 'https://docs.getgeolens.com'` in astro.config.mjs drives Starlight's auto-emitted `<link rel="canonical" href="https://docs.getgeolens.com/">` and the @astrojs/sitemap plugin's `https://docs.getgeolens.com/`-prefixed entries. T-223-WRONG-CANONICAL mitigated.
- **Belt-and-suspenders noindex live:** `<meta name="robots" content="noindex, nofollow">` injected via Starlight head config on every page; `Disallow: /` in robots.txt. Both flip together in Phase 228. T-223-EARLY-INDEX mitigated.
- **9 legacy redirects in place:** /install, /admin, /api each mapped through 3 rules (exact, trailing-slash, splat) → /guides/*. /quickstart explicitly excluded (marketing owns it). All 301. MIG-02 satisfied.
- **Load-bearing verify-build.sh exits 0:** Asserts canonical, noindex meta, sitemap-index.xml exists, _redirects copied with ≥9 `301` rules, no flat URLs in HTML, robots.txt has `Disallow: /`. Final line: `All build-artifact assertions passed.` Plan 02's CI workflow will invoke this script as the gate.

## Task Commits

Each task was committed atomically. Code commits are in `/Users/ishiland/Code/getgeolens.com` (separate repo); STATE.md commits are in `/Users/ishiland/Code/geolens` (this repo).

1. **Task 1: Scaffold docs/ project structure with pinned dependencies, config files, and verify-build script** — getgeolens.com@`35f8150` (feat) + planning-repo@`b15dda4d` (state)
2. **Task 2: Author astro.config.mjs, stub homepage, custom.css, robots.txt, and _redirects** — getgeolens.com@`77b7c63` (feat) + planning-repo@`07730afc` (state)
3. **Task 3: Run astro check, build, and verify-build.sh; assert clean build artifacts and no flat URLs** — planning-repo@`bc0d5b2c` (state) [no source changes; verification only]

**Plan metadata commit (this SUMMARY.md + ROADMAP.md + STATE.md final):** to follow this commit.

## Files Created/Modified

### `getgeolens.com/docs/` subtree (created — 15 files)

- `package.json` — pinned Astro ^6.1.3, Starlight 0.38.4, sitemap 3.7.2, check 0.9.8, typescript ^5.9.3; Node engine ≥22.12.0
- `package-lock.json` — 427-package dependency tree, regenerated by `npm install` from inside docs/
- `tsconfig.json` — extends `astro/tsconfigs/strict`
- `wrangler.toml` — `name = "getgeolens-docs"`, `compatibility_date = "2025-01-01"`, `pages_build_output_dir = "dist"`
- `.nvmrc` — single line `20` (mirrors marketing exactly)
- `.gitignore` — `node_modules/`, `dist/`, `.astro/`, `.DS_Store`
- `README.md` — local dev pointer + phase boundary doc (224/225/226-227/228)
- `astro.config.mjs` — site config, output static, customCss, noindex head, 4 sidebar groups under /guides/, sitemap integration
- `src/content.config.ts` — docsLoader + docsSchema (required by Starlight 0.38)
- `src/content/docs/index.mdx` — stub homepage with title frontmatter + 3 planned-URL TOC links (/guides/install, /guides/admin, /guides/api)
- `src/styles/custom.css` — minimal placeholder accent: 3 Starlight slots in :root and :root[data-theme='dark']; OKLCH values cherry-picked from marketing primary scale
- `public/favicon.svg` — byte-identical copy of marketing favicon
- `public/robots.txt` — `User-agent: *` / `Disallow: /` + Sitemap directive
- `public/_redirects` — 9 redirect rules (3 paths × 3 variants); `/quickstart` excluded by design
- `scripts/verify-build.sh` — load-bearing assertion gate, executable

### Planning repo (modified)

- `.planning/STATE.md` — current position updated three times (one per task)

## Decisions Made

- **Use astro@^6.1.3 verbatim from marketing** (D-20). Starlight 0.38.4 peerDeps require `astro ^6.0.0`; the existing marketing pin is the load-bearing version reference for the milestone.
- **wrangler.toml compatibility_date = "2025-01-01"** — clean fresh-project date per RESEARCH §3.4. Marketing uses 2024-01-01; not relevant for static-site builds (`compatibility_date` only affects Workers Functions runtime).
- **`autogenerate: { directory: 'guides/<X>' }` over `items: []`** — both render empty groups silently, but autogenerate ergonomically populates as soon as the first MDX file lands in Phase 224. No astro.config.mjs edit needed.
- **OKLCH values cherry-picked, not full palette** — only `--sl-color-accent`, `--sl-color-accent-low`, `--sl-color-accent-high` mapped, in light + dark. Full 50-950 ladder + token-drift detection ships in Phase 224 (BRAND-01/BRAND-04).
- **Stub homepage TOC links to /guides/install (not /guides/quickstart)** — Phase 226's content lands at /guides/install per ROADMAP. /guides/quickstart sidebar group is reserved for the Phase 226 quickstart-specific landing.
- **No Inter font, no GA4, no Pagefind config, no OG image, no llms.txt** — all explicitly deferred per D-12, D-19, plus Phase 224/228 boundaries.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 — Plan inconsistency] `_redirects` D-14 comment vs over-strict literal grep**
- **Found during:** Task 2 (author _redirects)
- **Issue:** The plan's `<acceptance_criteria>` says `_redirects` "does NOT contain `/quickstart`" and the `<verify><automated>` block uses `! grep -q "/quickstart" docs/public/_redirects`. But the same plan's Step 5 file template explicitly includes the comment line `# /quickstart is OWNED BY MARKETING (getgeolens.com) — DO NOT add it here.` The literal grep fails on the file the plan itself prescribes.
- **Fix:** Honored intent (D-14: no `/quickstart` redirect rule in docs `_redirects`) over literal verification syntax. Verified instead with `! grep -E '^/quickstart\s+.*301$'` — matches only actual redirect rules ending in `301`. Result: no `/quickstart` redirect rule present; only the documentation comment per D-14/D-16 maintenance convention. The comment line is load-bearing for D-16 ("`_redirects` opens with a comment block stating 'every page rename adds a 301 here'") and explicitly documents D-14 ownership.
- **Files modified:** None (file matches plan template; only the verification approach was adjusted).
- **Verification:** `grep -E '^/quickstart\s+.*301$' docs/public/_redirects` returns no matches; `verify-build.sh` exits 0; functional intent preserved.
- **Committed in:** Captured in Task 2 commit getgeolens.com@`77b7c63`.

---

**Total deviations:** 1 auto-fixed (1 plan inconsistency / Rule 1)
**Impact on plan:** Zero functional impact. The /quickstart-claim ban (D-14) and the maintenance-convention comment (D-16) coexist by design — one is the rule, the other is the documentation of it. No scope creep, no rework needed.

## Issues Encountered

- **Bash cwd persistence between tool calls.** Per the executor sandbox notes ("agent threads always have their cwd reset between bash calls"), I had to explicitly use `git -C /Users/ishiland/Code/geolens` and `git -C /Users/ishiland/Code/getgeolens.com` for all commits to ensure each commit landed in the correct repo. Two early STATE.md commits failed-then-succeeded because the previous bash session's cwd had drifted into getgeolens.com (where `.planning/` is not a tracked path). All commits ultimately landed in the right repo; no work lost.

- **No GA4 / Inter font / Pagefind code accidentally introduced.** Verified by `grep -qiE 'gtag|googletagmanager|G-[A-Z0-9]{8,}|@fontsource' docs/astro.config.mjs` returning no matches and `grep -qiE 'gtag|GA4|googletagmanager' docs/scripts/verify-build.sh` returning no matches. Phase boundaries honored.

## Threat Mitigations Validated

| Threat | Mitigation | Validated by |
|--------|-----------|--------------|
| T-223-EARLY-INDEX | robots.txt `Disallow: /` + site-wide noindex meta | `verify-build.sh` asserts both; `grep -F 'name="robots"' dist/index.html | grep noindex` returns the head entry; `grep -F 'Disallow: /' dist/robots.txt` matches |
| T-223-FLAT-URL | /guides/ sidebar groups + /guides/ TOC homepage + verify-build.sh assertion | `! grep -rE 'href="/(install\|admin\|api)(/\|\"\|#)' dist/ --include='*.html'` returns no matches; verify-build.sh exits 0 |
| T-223-WRONG-CANONICAL | `site: 'https://docs.getgeolens.com'` in astro.config.mjs | `dist/index.html` contains `<link rel="canonical" href="https://docs.getgeolens.com/">` (verified by spot grep + verify-build.sh assertion) |
| T-223-CROSS-CONTAM (Plan 01 portion) | Independent docs/package.json, gitignored docs/node_modules + docs/dist | `test -d docs/node_modules` (yes); `! test -L docs/node_modules` (yes — real dir); `! test -d /Users/ishiland/Code/getgeolens.com/node_modules/@astrojs/starlight` (yes — starlight not hoisted to repo root) |

Plan 02 owns the remaining T-223-CROSS-CONTAM mitigation (CI path filters in docs-ci.yml + paths-ignore patch on marketing ci.yml).

## User Setup Required

None for this plan. Plan 02 (Wave 2) generates the CF Pages dashboard config + custom domain attachment + GitHub secrets verification — that plan emits the user-setup instructions.

## Next Phase Readiness

Plan 02 inputs ready:
- `docs/dist/` is a deployable artifact (`npm run build` exits 0, verify-build.sh exits 0)
- `docs/package-lock.json` exists (Plan 02 CI uses `npm ci` with `cache-dependency-path: docs/package-lock.json`)
- `docs/wrangler.toml` `name = "getgeolens-docs"` matches the planned CF Pages project name (D-03)
- `docs/.nvmrc` exists (Plan 02 CI uses `node-version-file: docs/.nvmrc`)

Plan 02 to-do:
- Add `.github/workflows/docs-ci.yml` with check → build → cloudflare/pages-action@v1
- Patch existing `.github/workflows/ci.yml` (marketing) with `paths-ignore: ['docs/**']` on push and pull_request triggers
- Create `getgeolens-docs` project in Cloudflare Pages dashboard with `rootDirectory: docs`
- Attach custom domain `docs.getgeolens.com`; verify TLS via `curl -I`
- Verify deploy at `*.pages.dev` and at `docs.getgeolens.com`

## Self-Check: PASSED

Verified post-summary:

```
$ test -f /Users/ishiland/Code/getgeolens.com/docs/package.json && echo FOUND
FOUND
$ test -f /Users/ishiland/Code/getgeolens.com/docs/astro.config.mjs && echo FOUND
FOUND
$ test -f /Users/ishiland/Code/getgeolens.com/docs/wrangler.toml && echo FOUND
FOUND
$ test -f /Users/ishiland/Code/getgeolens.com/docs/scripts/verify-build.sh && echo FOUND
FOUND
$ test -x /Users/ishiland/Code/getgeolens.com/docs/scripts/verify-build.sh && echo EXEC
EXEC
$ test -f /Users/ishiland/Code/getgeolens.com/docs/public/_redirects && echo FOUND
FOUND
$ test -f /Users/ishiland/Code/getgeolens.com/docs/src/content/docs/index.mdx && echo FOUND
FOUND
$ git -C /Users/ishiland/Code/getgeolens.com log --oneline | grep -E '(35f8150|77b7c63)'
77b7c63 feat(docs): add astro config, stub homepage, custom.css, robots.txt, _redirects (refs geolens phase 223-01 task 2)
35f8150 feat(docs): scaffold docs/ subtree with pinned deps and verify-build.sh (refs geolens phase 223-01 task 1)
$ git -C /Users/ishiland/Code/geolens log --oneline | grep -E '(b15dda4d|07730afc|bc0d5b2c)'
bc0d5b2c state(223-01): task 3 complete — build green, verify-build.sh PASSED
07730afc state(223-01): task 2 complete — astro config + redirects (getgeolens.com@77b7c63)
b15dda4d state(223-01): task 1 complete — docs/ subtree scaffolded (getgeolens.com@35f8150)
$ cd /Users/ishiland/Code/getgeolens.com/docs && bash scripts/verify-build.sh
Asserting canonical URL points to docs.getgeolens.com...
[canonical line]
Asserting noindex meta present...
[noindex line]
Asserting sitemap-index.xml generated...
Asserting _redirects copied to dist/...
Asserting _redirects contains expected three-rule pattern...
Asserting NO flat URLs (/install, /admin, /api) in built HTML internal links...
Asserting Disallow: / present in robots.txt...
Disallow: /
All build-artifact assertions passed.
Exit: 0
```

All claims validated. Plan 01 properties are demonstrably true on disk; Plan 02 may proceed.

---
*Phase: 223-bootstrap-infrastructure-lock*
*Completed: 2026-04-25*
