# Phase 999.17: Builder terrain/legend consistency & DEM dedup - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-31
**Phase:** 999.17-builder-terrain-legend-consistency-and-dem-dedup
**Areas discussed:** Legend representation, Delete-source behavior, Fix 3 scope (mesh+hillshade)

---

## Area selection

The phase brief (`docs-internal/audits/BUCKET-B-builder-terrain-legend-phase-brief.md`) is SPEC-grade — it locks file targets and accept criteria and leaves exactly three open decisions. User selected all three to discuss, plus directed execution via `/gsd-autonomous` with the Playwright MCP server as the live close-gate.

---

## Legend representation

| Option | Description | Selected |
|--------|-------------|----------|
| Single '3D terrain' entry | One synthetic legend entry driven by `terrain_config`, mirroring the stack's synthetic `relief:terrain` row. Best cross-surface consistency; viewers see terrain is active. Costs a swatch/icon + i18n key in both surfaces. | ✓ |
| Drop entirely | Pure predicate filter — legend shows only painting layers, terrain has no legend presence. Simplest/lowest-risk. | |

**User's choice:** Single '3D terrain' entry
**Notes:** Reinforces the phase's "consistency" goal — legend ↔ stack visually agree. Hillshade-mode relief that paints still appears as a normal entry.

---

## Delete-source behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Clear + non-blocking toast | Auto-clear `terrain_config`, show a brief notice. Fixes dangling-config bug, no blocking modal, autonomous-friendly. | ✓ |
| Warn / confirm first | Block deletion with a confirm dialog. Safest against accidental terrain loss but adds a modal flow that interrupts the live-MCP gate. | |
| Clear silently | Clear with no notice. Minimal code, no user feedback. | |

**User's choice:** Clear + non-blocking toast
**Notes:** Balances bug fix (no dangling config) with autonomous-run friendliness (no blocking dialog for the MCP gate to handle).

---

## Fix 3 scope (mesh + hillshade on one DEM)

| Option | Description | Selected |
|--------|-------------|----------|
| Defer to a future backlog item | Keep phase tight (legend + dedup only); Fix 3 is optional and touches the POLISH-02 anti-crash guard. | |
| Include Fix 3 now | Relax `BuilderMap` demLayer lookup to resolve terrain by `terrain_config.source_dataset_id`, closing the mesh+hillshade gap in one pass. Touches POLISH-02 guard; needs extra live-MCP verification. | ✓ |

**User's choice:** Include Fix 3 now
**Notes:** All three brief fixes are now in-scope. Flagged risk: the dual-consumer path interacts with the POLISH-02 anti-crash guard (`map-sync.ts:~621-649`) and the HYG-01 (v1033) hillshade dual-consumer fix — needs extra live-MCP verification.

---

## Claude's Discretion

- Exact label/icon/swatch for the synthetic "3D terrain" legend entry (subject to design tokens + i18n parity).
- Toast copy wording for the delete-source case.
- Whether to factor a shared derivation helper across `LegendPlugin` and viewer `LayerLegend`, or keep per-surface predicates.

## Deferred Ideas

None — Fix 3 was elevated into scope, so all three brief fixes are in-phase. Discussion stayed within the phase domain.
