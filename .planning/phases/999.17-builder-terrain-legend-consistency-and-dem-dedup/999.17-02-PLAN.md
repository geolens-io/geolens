---
phase: 999.17-builder-terrain-legend-consistency-and-dem-dedup
plan: 02
type: execute
wave: 2
depends_on: ["999.17-01"]
files_modified:
  - frontend/src/components/builder/DEMEditorScene.tsx
  - frontend/src/components/builder/hooks/use-builder-layers.ts
  - frontend/src/components/builder/hooks/__tests__/use-builder-layers.test.ts
  - frontend/src/i18n/locales/en/builder.json
  - frontend/src/i18n/locales/es/builder.json
  - frontend/src/i18n/locales/fr/builder.json
  - frontend/src/i18n/locales/de/builder.json
autonomous: true
requirements: [FIX-2-DEDUP]
must_haves:
  truths:
    - "Toggling terrain / switching render mode on a DEM dataset never creates a 2nd render_mode:'terrain' layer on the same dataset — the existing DEM layer is reused."
    - "Deleting the terrain source layer (or the last DEM layer) auto-clears terrain_config."
    - "On that auto-clear, a non-blocking toast is shown (no blocking confirm dialog)."
    - "Unbinding/rebinding terrain repeatedly does not accumulate duplicate DEM layers."
  artifacts:
    - path: "frontend/src/components/builder/hooks/use-builder-layers.ts"
      provides: "Terrain bind/unbind that reuses the existing DEM layer + delete paths that clear terrain_config"
      contains: "terrain_config"
    - path: "frontend/src/components/builder/hooks/__tests__/use-builder-layers.test.ts"
      provides: "Reducer tests: bind reuses layer (no duplicate); delete clears terrain_config"
      contains: "terrain"
  key_links:
    - from: "frontend/src/components/builder/DEMEditorScene.tsx"
      to: "frontend/src/components/builder/hooks/use-builder-layers.ts"
      via: "handleSwitchMode / onTerrainBind calls terrain bind that reuses layer"
      pattern: "onTerrainBind|handleSwitchMode"
    - from: "frontend/src/components/builder/hooks/use-builder-layers.ts"
      to: "terrain_config"
      via: "delete path clears terrain_config + emits toast"
      pattern: "terrain_config"
---

<objective>
Fix 2 (DEM dedup + delete teardown, per D-04/D-05). Two systemic fixes to the builder
terrain flow so the duplicate-DEM-layer bug (map `8dd6a129` drifted to 3 terrain layers)
can't recur:

1. DEDUP (D-04): terrain-bind / render-mode switch must REUSE the existing DEM-terrain layer
   for the dataset — never create a 2nd `render_mode:"terrain"` layer on the same DEM dataset.
   Fix the bind/switch flow (`DEMEditorScene.tsx` handleSwitchMode/onTerrainBind →
   `use-builder-layers.ts` terrain bind/unbind).
2. DELETE TEARDOWN (D-05): when the terrain source layer (or last DEM layer) is deleted,
   auto-CLEAR `terrain_config` and surface a NON-BLOCKING toast (e.g. "3D terrain disabled —
   source layer removed"). No blocking confirm dialog. Fix the delete paths in
   `use-builder-layers.ts` that currently leave dangling `terrain_config`.

Purpose: kill the duplicate-accumulation bug at the source and prevent dangling terrain_config
when its backing layer is gone.
Output: bind/switch reuse logic, delete-path teardown + toast, reducer unit tests, toast i18n
key with en/es/fr/de parity.

DEPENDENCY NOTE: This plan runs in Wave 2 (depends_on 999.17-01) ONLY because it edits the
same `*/builder.json` i18n locale files Plan 01 touches (terrain3d keys); to keep file
ownership clean for parallel safety, this plan runs after Plan 01 merges its locale edits.
There is no logical code dependency on Plan 01's legend changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.17-builder-terrain-legend-consistency-and-dem-dedup/999.17-CONTEXT.md
@docs-internal/audits/BUCKET-B-builder-terrain-legend-phase-brief.md

# Edit-site sources (READ to confirm exact current line numbers before editing — see note below)
@frontend/src/components/builder/DEMEditorScene.tsx
@frontend/src/components/builder/hooks/use-builder-layers.ts
</context>

<line_number_warning>
IMPORTANT: All line numbers in CONTEXT.md and the phase brief are APPROXIMATE and have
drifted (~400 lines off in places). The phase RESEARCH.md line-number correction table is not
on disk. DO NOT trust any hard-coded line number from the brief. Locate every edit site by
SYMBOL NAME using grep first, then edit:

  grep -n "handleSwitchMode\|onTerrainBind" frontend/src/components/builder/DEMEditorScene.tsx
  grep -n "terrain_config\|terrainBind\|terrainUnbind\|TERRAIN" frontend/src/components/builder/hooks/use-builder-layers.ts
  grep -n "render_mode" frontend/src/components/builder/hooks/use-builder-layers.ts

Use the live grep result as ground truth, not the brief's numbers (brief cites bind/unbind
~:1067-1077 and delete ~:287-329, ~:611-696 — treat as hints only).
</line_number_warning>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Terrain bind/switch reuses existing DEM layer (no duplicate)</name>
  <files>frontend/src/components/builder/hooks/use-builder-layers.ts, frontend/src/components/builder/DEMEditorScene.tsx, frontend/src/components/builder/hooks/__tests__/use-builder-layers.test.ts</files>
  <behavior>
    - Binding terrain to a DEM dataset that ALREADY has a render_mode:'terrain' layer reuses that layer; the layer count for the dataset does not increase.
    - Switching render mode (handleSwitchMode) on an existing DEM layer mutates that layer's render_mode in place rather than adding a new layer.
    - Bind → unbind → bind on the same dataset leaves exactly one DEM layer (no accumulation across cycles).
    - Binding terrain to a dataset with NO existing terrain layer still produces exactly one (regression — don't break the legit create path).
  </behavior>
  <action>
    Implement D-04. grep the terrain bind/unbind handling in `use-builder-layers.ts` (search
    `terrain_config`, `render_mode`, and any terrain bind/unbind reducer/case names) and the
    `handleSwitchMode`/`onTerrainBind` callers in `DEMEditorScene.tsx`.

    Before creating a `render_mode:"terrain"` layer for a dataset, look up whether an existing layer for
    that dataset already has `render_mode:"terrain"` (match by the dataset id — read the existing layer
    shape to find the dataset-id field; likely `source_dataset_id` or `dataset_id`). If one exists, REUSE
    it (update terrain_config / its render_mode in place) instead of inserting a new layer. The
    render-mode switch path must mutate the existing layer's render_mode rather than append a layer.

    Keep the surgical PATCH /maps/{id}/layers convention intact: when patching a layer that has a
    `style_config`, re-include `style_config` in the patch (project memory: omitting it nulls it). Confirm
    the existing bind path already does this; preserve it.

    Do NOT touch BuilderMap.tsx terrain resolution (that is Plan 03), map-sync, or the POLISH-02 guard.

    Add/extend `hooks/__tests__/use-builder-layers.test.ts` (create if absent — grep for an existing test
    file for this hook first) exercising the reducer/bind logic for the four behaviors above. The test must
    assert layer COUNT for the dataset stays at 1 across bind/switch/rebind.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/components/builder/hooks/__tests__/use-builder-layers.test.ts</automated>
  </verify>
  <done>Terrain bind and render-mode switch reuse the existing DEM layer; no 2nd terrain layer on the same dataset across bind/unbind/rebind; reducer test green asserting count stays 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete terrain source layer clears terrain_config + non-blocking toast</name>
  <files>frontend/src/components/builder/hooks/use-builder-layers.ts, frontend/src/components/builder/hooks/__tests__/use-builder-layers.test.ts, frontend/src/i18n/locales/en/builder.json, frontend/src/i18n/locales/es/builder.json, frontend/src/i18n/locales/fr/builder.json, frontend/src/i18n/locales/de/builder.json</files>
  <behavior>
    - Deleting the layer that backs active terrain (the terrain source / last DEM layer for the dataset) clears terrain_config on the map state.
    - On that clear, a non-blocking toast is emitted (no blocking confirm dialog gates the delete).
    - Deleting a non-terrain layer does NOT clear terrain_config and does NOT emit the toast.
    - If a DEM dataset still has another DEM layer after the delete (edge case), terrain_config is preserved (only clear when the backing source is actually gone).
  </behavior>
  <action>
    Implement D-05. grep the delete paths in `use-builder-layers.ts` (brief hints ~:287-329 and
    ~:611-696 — confirm by grepping for the delete/remove reducer cases and the bulk-delete path; note
    project has POST /maps/{id}/layers/bulk-delete). In each delete path, after removing layer(s), check
    whether the deleted set included the layer backing active terrain (i.e. the `terrain_config`'s
    source/dataset no longer has a backing DEM layer). If so, clear `terrain_config` and emit a
    non-blocking toast.

    Reuse the builder's existing recoverable non-blocking toast/banner mechanism (introduced v1009 per
    CONTEXT.md) — grep how other builder flows surface toasts and follow that pattern; do NOT introduce a
    blocking confirm dialog. Toast copy via a new i18n key (wording is Claude's Discretion per CONTEXT.md),
    e.g. EN "3D terrain disabled — source layer removed".

    Add the toast i18n key under the builder namespace with en/es/fr/de parity (e.g.
    `builder.terrain.disabledOnDelete` — match the real namespace nesting; grep an existing builder toast
    key first to mirror structure). All four locales must have real translations (i18n gate is
    deterministic).

    Do NOT touch BuilderMap.tsx, map-sync, or the POLISH-02 guard.

    Extend `hooks/__tests__/use-builder-layers.test.ts` covering the four delete behaviors above (clear on
    backing-layer delete, toast emitted, no clear on unrelated delete, preserve when another DEM layer
    remains).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/components/builder/hooks/__tests__/use-builder-layers.test.ts && npm run test:i18n</automated>
  </verify>
  <done>Deleting the terrain-backing layer clears terrain_config + shows a non-blocking toast; unrelated deletes don't; toast i18n key present in all four locales; reducer + i18n tests green.</done>
</task>

</tasks>

<verification>
Frontend-only deterministic gates (executor runs; no backend, no `make openapi`):
- `cd frontend && npm run typecheck` → 0 errors
- `cd frontend && npm run test -- --run` → green incl. use-builder-layers.test
- `cd frontend && npm run test:i18n` → parity 2/2
- `cd frontend && npm run e2e:smoke:builder` → green (run after both tasks)
</verification>

<success_criteria>
- Terrain bind / render-mode switch never creates a duplicate `render_mode:"terrain"` layer on the same dataset (D-04).
- Deleting the terrain source / last DEM layer auto-clears `terrain_config` with a non-blocking toast, no blocking dialog (D-05).
- Reducer unit tests assert layer count stays 1 across bind cycles and terrain_config clears on backing-layer delete.
- typecheck 0; vitest green; test:i18n 2/2; e2e:smoke:builder green.
- BuilderMap terrain resolution, map-sync, and POLISH-02 guard untouched by this plan.
</success_criteria>

<output>
Create `.planning/phases/999.17-builder-terrain-legend-consistency-and-dem-dedup/999.17-02-SUMMARY.md` when done.
</output>

---

## Plan-Check Advisory (fold in during execution)
**A2 — Delete-teardown must key on dataset identity, not "a DEM layer was deleted."** Per CONTEXT D-05, only clear `terrain_config` (and fire the toast) when the DELETED layer's dataset matches `terrain_config.source_dataset_id` (i.e. it was the terrain source / last DEM backing terrain). In a multi-DEM map, deleting a different DEM layer must NOT clear `terrain_config`. Add a unit assertion for the multi-DEM negative case (delete a non-terrain DEM → `terrain_config` preserved, no toast).
