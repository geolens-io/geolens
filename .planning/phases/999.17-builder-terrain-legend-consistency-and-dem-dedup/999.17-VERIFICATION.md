---
phase: "999.17"
slug: builder-terrain-legend-consistency-and-dem-dedup
status: passed
verified: 2026-06-01
verifier: orchestrator (live MCP gate is orchestrator-only per D-09)
---

# Phase 999.17 — Verification

**Goal:** Fix the cross-surface legend↔stack inconsistency for terrain-suppressed DEM layers, stop terrain-bind from accumulating duplicate DEM layers, and let one DEM drive both the 3D mesh and a visible hillshade overlay.

## Success criteria (goal-backward)

| # | Criterion | Result | Evidence |
|---|-----------|--------|----------|
| 1 | Legend excludes terrain-suppressed DEM layers in BOTH builder `LegendPlugin` and viewer `LayerLegend`; active terrain shows as ONE synthetic "3D terrain" entry; painting hillshade relief still shows | ✅ PASS | Live MCP on map `8dd6a129`: legend = `3D terrain` (1×, first position) + ADK peaks + Hiking trails + Land classification + TNM/NY Orthos aerial; **0 phantom DEM rows** (only DEM/hillshade/elevation text anywhere on page was a CSS comment). Stable across 2 reloads. Unit: `terrain-legend.test.ts`, `LegendPlugin.terrain.test.tsx`, `LayerLegend.test.tsx`. |
| 2 | Terrain-bind/dedup never accumulates a 2nd terrain layer on one dataset; deleting the terrain source clears `terrain_config` + non-blocking toast (multi-DEM safe) | ✅ PASS | Live: exactly **1** `raster-dem` source (`terrain-dem`) — no duplicate. Unit: dedup guard in `handleDuplicateRendering` + `shouldClearTerrainOnDelete` dataset-identity predicate + multi-DEM negative case + (HI-01) failure-rollback restores `terrain_config`. |
| 3 | One DEM drives both the 3D mesh and a visible hillshade overlay; POLISH-02 guard not regressed | ✅ PASS (with note) | Live: `map.getTerrain()` → `{source:"terrain-dem"}` (mesh attached) even though resolver no longer requires `render_mode==='terrain'`. BL-01 guard narrowed to skip hillshade only on tileSize **mismatch**; both branches unit-tested in `map-sync.raster.test.ts`. See live-gate note below. |

## Deterministic gate (all green)

- `npm run typecheck` → **0 errors**
- `npx vitest run` (full) → **2707 passed / 8 failed**; the 8 are all in `SharePanel.test.tsx` (share-link "restrict to domains" toggle) — **pre-existing and unrelated** (999.17 touches no share code; `git diff --name-only 2d0f207c..HEAD` contains zero SharePanel files).
- `npm run test:i18n` → **2/2** parity (en/es/fr/de) for `plugins.legend.terrain3d`, `viewer.legend.terrain3d`, `toasts.terrainDuplicateBlocked`, `toasts.terrainDisabledSourceRemoved`.
- `npm run e2e:smoke:builder` (repo root, fresh bundle) → **26 passed**.
- POLISH-02 guard (`map-sync.ts`) — only change is the narrowed-guard helper `shouldSkipHillshadeForTerrain` + tileSize param; no unrelated edits.

## Live MCP close-gate (orchestrator-driven, D-08/D-09)

Map `8dd6a129` (Adirondack High Peaks — 3D Relief), `localhost:8080`, admin, fresh bundle:
- Terrain mesh attached (`getTerrain()` non-null) ✓
- 1 `raster-dem` source, 0 phantom DEM legend rows ✓
- 1 synthetic "3D terrain" legend entry (first position, per advisory A1) ✓
- Console: **0 errors / 0 warnings** throughout, incl. with a 2nd DEM consumer active on the terrain dataset (no `dem dimension mismatch`) ✓
- Stable across reloads; sample map left unmutated (test layer discarded, not saved) ✓

**Live-gate note (honest scope):** the *exact* hillshade-mode-on-the-terrain-DEM visible paint could not be driven through the live UI because any rendering of the active-terrain DEM dataset is suppressed from the editable layer stack (the v1035 BLDR-03 behavior). The hillshade-specific guard branch (matched-tileSize → hillshade runs; mismatch → skip) is covered deterministically by `map-sync.raster.test.ts`, and the dual-DEM-consumer coexistence was confirmed console-clean live. Recommended manual spot-check: on a map with terrain + a hillshade-mode layer on a *second* DEM dataset, confirm the hillshade paints.

## Code review findings — all resolved

REVIEW.md returned 1 blocker + 1 high + 2 medium; all fixed in the gap-closure wave:
- **BL-01** (blocker) — POLISH-02 guard narrowed so hillshade paints on a terrain DEM when tileSize matches (`7ec15858`).
- **HI-01** (high) — `terrain_config` restored on delete-mutation failure (`09936673`).
- **MD-01** (medium) — synthetic legend entry requires a backing DEM layer (no phantom on dangling config) (`6484e716`).
- **MD-02** (medium) — `shouldClearTerrainOnDelete` uses canonical `isTerrainCapableDemLayer` (`f9599972`).

## Verdict

**PASSED.** All 3 fixes delivered; deterministic gate green; live MCP gate confirms Fix 1/2/3 with a clean console; all code-review findings closed. One sub-assertion (hillshade visible-paint on the exact terrain DEM) is unit-covered rather than live-UI-driven, documented above.
