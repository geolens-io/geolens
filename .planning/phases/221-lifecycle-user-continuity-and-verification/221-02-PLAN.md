---
phase: 221
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/edition-deactivation.md
  - docs/edition-reactivation.md
autonomous: true
requirements:
  - LIFECYCLE-06
gap_closure: false
tags:
  - documentation
  - runbook
  - saml-conversion
must_haves:
  truths:
    - "docs/edition-deactivation.md no longer contains the Phase 220 line-81 TODO blockquote ('Phase 221 ships the user re-onboarding procedure')"
    - "docs/edition-deactivation.md has a top-level '## Handling existing SAML users' section with 6 substeps (inventory, decide, convert, communicate, verify, audit-confirm) plus an OIDC manual-procedure appendix"
    - "The new section's curl example uses POST /admin/users/<user-id>/convert-saml-to-local/ WITH trailing slash, references /auth/login/ (NOT /auth/token) for token acquisition, and surfaces the audit-log action name 'auth.convert_saml_to_local'"
    - "The new section explicitly orders 'convert users AFTER overlay removal' to close the SAML-login race window (Pitfall 8)"
    - "docs/edition-reactivation.md gains a one-paragraph forward-pointer noting that local-password conversions persist after reactivation and reverse conversion is on the deferred roadmap"
  artifacts:
    - path: "docs/edition-deactivation.md"
      provides: "Operator-facing 'Handling existing SAML users' section replacing line-81 TODO blockquote"
      contains: "## Handling existing SAML users"
    - path: "docs/edition-reactivation.md"
      provides: "Forward-pointer paragraph for previously-converted SAML users"
      contains: "converted"
  key_links:
    - from: "docs/edition-deactivation.md (pre-flight step 3)"
      to: "docs/edition-deactivation.md (Handling existing SAML users section)"
      via: "in-document anchor link `(#handling-existing-saml-users)` replacing the TODO blockquote"
      pattern: "handling-existing-saml-users"
    - from: "docs/edition-reactivation.md (caveats paragraph)"
      to: "docs/edition-deactivation.md (Handling existing SAML users)"
      via: "markdown link to edition-deactivation.md"
      pattern: "edition-deactivation"
---

<objective>
Replace the Phase 220 line-81 TODO blockquote in `docs/edition-deactivation.md` with a real, copy-paste-able "Handling existing SAML users" runbook section, and add a one-paragraph forward-pointer to `docs/edition-reactivation.md` warning that local-password conversions persist after reactivation (per LIFECYCLE-06 SC#2 + D-12 + D-13).

Purpose: Phase 220 deliberately left a TODO marker at `docs/edition-deactivation.md:81` that promised Phase 221 would ship the re-onboarding procedure. This plan closes that promise. Operators who run a deactivation get a concrete, tested procedure (curl against the new endpoint from Plan 01) instead of a "communicate to users out-of-band and convert manually via the admin UI" placeholder that was never true.

Output:
- Lines 79-81 of `docs/edition-deactivation.md` (the existing TODO blockquote) replaced with a forward-link to a new top-level section, plus the explicit "convert AFTER deactivation" ordering note.
- New top-level `## Handling existing SAML users` section in `docs/edition-deactivation.md` placed AFTER `## Database state after the safe path` and BEFORE `## Destructive path: permanent decommissioning`.
- One short new section in `docs/edition-reactivation.md` (`## Note on previously converted SAML users`) inserted between `## End-to-end smoke test` and `## Why this works`.

This plan is independent of Plan 01 — the documentation references the endpoint contract from Plan 01 but does not require Plan 01 to be merged first. Plan 02 and Plan 01 ship together in Wave 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/221-lifecycle-user-continuity-and-verification/221-CONTEXT.md
@.planning/phases/221-lifecycle-user-continuity-and-verification/221-RESEARCH.md
@.planning/phases/221-lifecycle-user-continuity-and-verification/221-PATTERNS.md
@.planning/phases/220-lifecycle-runbooks-and-preservation/220-CONTEXT.md
@docs/edition-deactivation.md
@docs/edition-reactivation.md

<interfaces>
Existing structure of `docs/edition-deactivation.md` (verified by direct file read):
- Lines 60-77: pre-flight inventory SQL — REUSED (extended) in the new section's Step 1.
- Lines 79-81: TARGET — TODO blockquote to be replaced (the substep heading + blank line + blockquote).
- Line 83: `4. **Plan a maintenance window.**` — PRESERVE verbatim.
- Line 85: `5. **Confirm the snapshot is restorable...**` — PRESERVE verbatim.
- Lines 87-138: §Deactivation sequence (canonical path) — style reference for substep formatting (`### Step N:` headings, fenced bash/sql blocks, `> **Bold** ...` blockquote callouts).
- Line 140 (approx): `## Database state after the safe path` — the new section is inserted AFTER this.
- Line 144 (approx): `## Destructive path: permanent decommissioning` — the new section is inserted BEFORE this.

Inventory SQL pattern (lines 70-77 of the existing doc — extended for per-user iteration in the new section):
```sql
SELECT u.id, u.username, u.email, op.slug AS provider_slug
FROM catalog.users u
JOIN catalog.oauth_accounts oa ON oa.user_id = u.id
JOIN catalog.oauth_providers op ON op.id = oa.provider_id
WHERE op.provider_type = 'saml'
  AND u.auth_provider = 'oauth';
```

Auth-token acquisition (verified `backend/tests/conftest.py:328-334`; Pitfall 5 — endpoint is `/auth/login/`, NOT `/auth/token`):
```bash
TOKEN=$(curl -fsS -X POST http://localhost:8000/auth/login/ \
  -d "username=$ADMIN_USER&password=$ADMIN_PASSWORD" \
  | jq -r .access_token)
```

Conversion endpoint (defined by Plan 01 Task 3 — trailing slash MANDATORY per Pitfall 4):
```bash
curl -fsS -X POST -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"password": "<temp-strong-password>"}' \
  http://localhost:8000/admin/users/<user-id>/convert-saml-to-local/
```

Existing structure of `docs/edition-reactivation.md` (verified):
- Lines 1-7: opening narrative.
- Line 8: `## Re-mount the overlay`.
- Line 22: `## Post-reactivation verification checklist`.
- Line 64: `## End-to-end smoke test`.
- Line 68: `## Why this works` — the new section is inserted BEFORE this.
- Line 72: `## References`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the line-81 TODO blockquote and add the Handling existing SAML users section in docs/edition-deactivation.md</name>
  <files>docs/edition-deactivation.md</files>
  <read_first>
    - docs/edition-deactivation.md (full file — confirm current line 81 wording, the inventory SQL at lines 60-77, the maintenance-window sentence at line 83, and the existing section structure for tone/format)
    - .planning/phases/221-lifecycle-user-continuity-and-verification/221-CONTEXT.md (D-12 — 6-step structure locked)
    - .planning/phases/221-lifecycle-user-continuity-and-verification/221-PATTERNS.md (the §docs/edition-deactivation.md block — exact REPLACE target lines + section structure)
    - .planning/phases/221-lifecycle-user-continuity-and-verification/221-RESEARCH.md (Pattern 4 — full sketch; Pitfalls 4, 5, 8)
    - .planning/phases/220-lifecycle-runbooks-and-preservation/220-CONTEXT.md (Pitfall context — runbook tone reference)
  </read_first>
  <action>
**Step A.** Read the file with the Read tool to confirm exact line numbers (they should still be ~79-81 for the TODO blockquote, ~83 for the maintenance-window sentence, ~140 for "Database state after the safe path", ~144 for "Destructive path"). If any line drifted, adjust the Edit tool match strings accordingly.

**Step B.** Replace lines 79-81 (the TODO blockquote substep). The current text is:

```markdown
3. **Communicate to SAML-authenticated users.**

   > **Existing SAML users will lose their login path.** Phase 221 ships the user re-onboarding procedure ("Handling existing SAML users", planned). Until that lands, communicate the downgrade to SAML users out-of-band and convert their accounts manually via the admin UI (set local password or convert to OIDC).
```

Replace with:

```markdown
3. **Communicate to SAML-authenticated users.** See [Handling existing SAML users](#handling-existing-saml-users) below for the conversion procedure. **Order matters:** convert each user *after* the overlay is removed (so `/auth/saml/*` returns 404 and no in-flight SAML login can race the conversion).
```

Do NOT touch lines 83+ (the maintenance-window sentence) or the surrounding pre-flight content.

**Step C.** Insert a new top-level section AFTER `## Database state after the safe path` (which ends with the paragraph mentioning `docs/edition-reactivation.md`) and BEFORE the `## Destructive path: permanent decommissioning` heading.

Exact section content to insert (preserve all formatting; the literal heading text is critical for the auto-generated GitHub anchor `#handling-existing-saml-users`):

```markdown
## Handling existing SAML users

Existing SAML-authenticated users lose their login path the moment the enterprise overlay is removed (`/auth/saml/*` returns 404). Each affected user must be re-onboarded before they can sign in again. The canonical procedure for v13.2 is **conversion to local-password** via a dedicated admin endpoint that runs every step in a single database transaction — `users.id` is unchanged across the conversion, so every foreign-key reference (`audit_logs.user_id`, `user_roles.user_id`, `datasets.created_by`, `datasets.updated_by`, `api_keys.user_id`, `share_tokens.user_id`, etc.) remains valid by virtue of the user row not moving.

> **Run conversions AFTER the overlay is removed** (steps 1–6 of the canonical path above). Once `/auth/saml/*` returns 404, no in-flight SAML login can race the conversion. Running conversions before deactivation creates a window where a SAML ACS POST may hit a partially-converted user state.

### Step 1: Inventory SAML users

Reuse the inventory query from §pre-flight (extended to project per-user fields for iteration):

\```sql
SELECT u.id, u.username, u.email, op.slug AS provider_slug
FROM catalog.users u
JOIN catalog.oauth_accounts oa ON oa.user_id = u.id
JOIN catalog.oauth_providers op ON op.id = oa.provider_id
WHERE op.provider_type = 'saml'
  AND u.auth_provider = 'oauth';
\```

Save the result — you will iterate over `(id, username, email, provider_slug)` in step 3.

### Step 2: Decide a conversion target per user

| Target | When to use | Procedure |
|---|---|---|
| **Local-password** | Default. Universal — works regardless of OIDC config state. | Endpoint below (Step 3). |
| **OIDC re-link** | You have an OIDC `oauth_providers` row already configured and the user prefers federated SSO. | Manual procedure — see [Appendix: OIDC conversion (manual)](#appendix-oidc-conversion-manual). |

### Step 3: Run the conversion endpoint per user

First, obtain an admin JWT against the local-password admin login flow (replace `$ADMIN_USER` / `$ADMIN_PASSWORD` with your admin credentials — these are set via `GEOLENS_ADMIN_USERNAME` / `GEOLENS_ADMIN_PASSWORD` env vars):

\```bash
TOKEN=$(curl -fsS -X POST http://localhost:8000/auth/login/ \
  -d "username=$ADMIN_USER&password=$ADMIN_PASSWORD" \
  | jq -r .access_token)
\```

Then convert each SAML user (the trailing slash on the URL is mandatory — without it FastAPI returns a 307 redirect that may drop the JSON body):

\```bash
curl -fsS -X POST -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"password": "<temp-strong-password>"}' \
  http://localhost:8000/admin/users/<user-id>/convert-saml-to-local/
\```

A successful response (HTTP 200) is the user's `UserResponse` payload with `auth_provider` reflecting the new `"local"` state. The endpoint, in a single transaction:

- Validates the user is currently SAML-authenticated (`auth_provider='oauth'` AND has an `oauth_accounts` row pointing at a `provider_type='saml'` provider). Otherwise 422.
- Sets `users.password_hash` to a bcrypt hash of the supplied password.
- Flips `users.auth_provider` from `'oauth'` to `'local'`.
- Deletes the user's `oauth_accounts` row pointing at the SAML provider. The SAML `oauth_providers` row itself is preserved — other users may still link to it post-reactivation.
- Writes one `audit_log` entry with `action='auth.convert_saml_to_local'` and `details={"from": "saml", "to": "local", "provider_slug": "<slug>"}`. Password material is never logged.

All steps run in one transaction; any failure rolls back without writing the audit-log row.

> **Self-conversion is blocked.** If `<user-id>` matches the admin invoking the call, the endpoint returns 422 (`Cannot convert your own account; use a different admin account`) — this prevents an admin from fat-fingering their own new password and locking themselves out of the deployment. Use a second admin account when you need to convert your own.

### Step 4: Communicate the new credentials out-of-band

Choose a secure delivery channel for the temporary password (encrypted email, password manager share, in-person credential drop). Each user logs in to `/login` using their existing username and the new local-password credential. Encourage users to change the password immediately after first login via the standard self-service flow.

### Step 5: Verify the user can log in

Smoke-test one converted user before notifying the rest:

\```bash
curl -fsS -X POST http://localhost:8000/auth/login/ \
  -d "username=$USERNAME&password=<temp-strong-password>"
\```

Expected: HTTP 200 with `access_token`. If you receive 401, confirm the conversion endpoint returned 200 in step 3 and the user supplied the temporary password verbatim (no surrounding whitespace).

### Step 6: Confirm the audit trail recorded the conversion

\```sql
SELECT user_id, action, resource_id, details
FROM catalog.audit_logs
WHERE action = 'auth.convert_saml_to_local'
ORDER BY created_at DESC
LIMIT 10;
\```

Each conversion produces exactly one row. The `user_id` field is the admin who performed the conversion; `resource_id` is the user being converted; `details` is the allow-listed dictionary with `from`, `to`, and `provider_slug`. If a row is missing for a conversion you ran, the underlying transaction rolled back — re-run the conversion after investigating the response payload from step 3.

### Appendix: OIDC conversion (manual)

If your deployment has an OIDC `oauth_providers` row configured and a user prefers federated SSO over local-password, OIDC conversion has no automated endpoint in v13.2 (deferred to a future polish phase). The manual procedure:

1. Have the user run the OIDC enrollment flow from their perspective — sign in via the OIDC provider button on `/login`. GeoLens JIT-creates a new `oauth_accounts` linkage row pointing at the OIDC provider for that user.
2. Manually delete the user's SAML `oauth_accounts` row via SQL — match on `user_id` AND the SAML `provider_id`.
3. The user's `auth_provider` stays `'oauth'`; their `users.id` is unchanged; their audit history and role memberships remain intact.

Automating this two-step procedure is on the deferred roadmap — this manual procedure exists for operators who want OIDC-federated continuity instead of local-password during the v13.2 lifecycle.
```

**Step D.** Verify the new section is between `## Database state after the safe path` and `## Destructive path: permanent decommissioning`. The "Destructive path" section MUST remain intact below the new section — do not modify or remove any of its content.

**Step E.** Sanity-check the file: open with the Read tool and confirm (a) the TODO blockquote is gone, (b) the new section heading is exact-match `## Handling existing SAML users`, (c) the destructive-path content still follows below.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && grep -q "## Handling existing SAML users" docs/edition-deactivation.md && grep -q "convert-saml-to-local/" docs/edition-deactivation.md && grep -q "auth.convert_saml_to_local" docs/edition-deactivation.md && grep -q "/auth/login/" docs/edition-deactivation.md && (! grep -q "Phase 221 ships the user re-onboarding" docs/edition-deactivation.md) && (! grep -q "manually via the admin UI" docs/edition-deactivation.md) && (! grep -q "/auth/token" docs/edition-deactivation.md) && grep -q "Plan a maintenance window" docs/edition-deactivation.md && grep -q "## Destructive path: permanent decommissioning" docs/edition-deactivation.md</automated>
  </verify>
  <acceptance_criteria>
    - **Negative greps (must NOT find — TODO marker and false claim removed):**
      - `grep -q "Phase 221 ships the user re-onboarding" docs/edition-deactivation.md` exits NON-ZERO.
      - `grep -q "manually via the admin UI" docs/edition-deactivation.md` exits NON-ZERO.
      - `grep -q "/auth/token" docs/edition-deactivation.md` exits NON-ZERO (Pitfall 5 — wrong endpoint name MUST not appear).
    - **Positive greps (must find — new section content present):**
      - `grep -q "## Handling existing SAML users" docs/edition-deactivation.md` exits 0 (top-level section, exact heading).
      - `grep -q "convert-saml-to-local/" docs/edition-deactivation.md` exits 0 (curl example with trailing slash — Pitfall 4).
      - `grep -q "auth.convert_saml_to_local" docs/edition-deactivation.md` exits 0 (audit-log action name documented — D-14).
      - `grep -q "/auth/login/" docs/edition-deactivation.md` exits 0 (correct token endpoint per Pitfall 5).
      - `grep -q -i "after the overlay is removed" docs/edition-deactivation.md` exits 0 (Pitfall 8 ordering note present).
      - `grep -q -i "appendix.*oidc\|oidc conversion" docs/edition-deactivation.md` exits 0 (OIDC appendix present).
      - `grep -q "handling-existing-saml-users" docs/edition-deactivation.md` exits 0 (in-document anchor link from §pre-flight step 3 present).
      - `grep -q -i "self-conversion is blocked\|cannot convert your own account" docs/edition-deactivation.md` exits 0 (self-lockout warning surfaced — T-221-01).
      - `grep -q "provider_type = 'saml'" docs/edition-deactivation.md` exits 0 (inventory SQL present in new section, in addition to its existing copy in §pre-flight).
    - **Negative grep for password-in-audit-details:** `grep -E -i 'details.*"password"' docs/edition-deactivation.md` returns nothing (the documented audit `details` allow-list does NOT mention password — security invariant T-221-03).
    - **Preservation greps (existing content not destroyed):**
      - `grep -q "Plan a maintenance window" docs/edition-deactivation.md` exits 0 (line 83 preserved).
      - `grep -q "pg_dump" docs/edition-deactivation.md` exits 0 (existing destructive-path content preserved).
      - `grep -q "## Destructive path: permanent decommissioning" docs/edition-deactivation.md` exits 0.
      - `grep -q "edition-reactivation" docs/edition-deactivation.md` exits 0 (existing cross-link preserved).
    - **Markdown sanity:** `awk 'BEGIN{c=0} /^\\\`\\\`\\\`/{c++} END{exit c%2}' docs/edition-deactivation.md` exits 0 (every fenced code block is closed).
  </acceptance_criteria>
  <done>
    Lines 79-81 of docs/edition-deactivation.md no longer contain the Phase 221 TODO blockquote — they contain a forward-link with the explicit "after the overlay is removed" ordering note. The new top-level `## Handling existing SAML users` section exists between `## Database state after the safe path` and `## Destructive path: permanent decommissioning` with all 6 substeps + OIDC appendix. All positive greps pass; all negative greps exit non-zero. The maintenance-window sentence at line 83 and the destructive-path content are preserved verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Note on previously converted SAML users to docs/edition-reactivation.md</name>
  <files>docs/edition-reactivation.md</files>
  <read_first>
    - docs/edition-reactivation.md (full file — confirm current section order: Re-mount → Post-reactivation verification checklist → End-to-end smoke test → Why this works → References)
    - .planning/phases/221-lifecycle-user-continuity-and-verification/221-CONTEXT.md (D-13 — one-paragraph forward-pointer locked)
    - .planning/phases/221-lifecycle-user-continuity-and-verification/221-PATTERNS.md (the §docs/edition-reactivation.md block — placement recommendation: between End-to-end smoke test and Why this works)
  </read_first>
  <action>
**Step A.** Read the file with the Read tool to confirm exact line numbers of `## End-to-end smoke test` (currently ~line 64) and `## Why this works` (currently ~line 68). The new section is inserted between them.

**Step B.** Insert this exact section content (with one blank line before the heading and one blank line after the closing paragraph, following the existing section spacing):

```markdown
## Note on previously converted SAML users

If SAML users were converted to local-password during deactivation (per [`docs/edition-deactivation.md`](edition-deactivation.md) §Handling existing SAML users), those conversions persist after reactivation. Each converted user continues to log in with the local-password credential issued during deactivation — re-mounting the SAML overlay does not automatically re-link them to the SAML provider. Their `users.id`, role memberships, audit history, and dataset ownership are intact (the conversion was non-destructive across `users.id`-keyed records); only the auth path is now `local` instead of `oauth`.

If you want a previously-converted user back on SAML federated SSO, that re-linking is currently a manual procedure — automating reverse conversion (local → SAML on reactivation) is on the deferred roadmap. Operators who need it today can either (a) delete the local user and let the user re-authenticate via the IdP, which JIT-creates a fresh user row (loses any local-only metadata), or (b) manually `INSERT` an `oauth_accounts` row pointing at the SAML provider for the user and flip `users.auth_provider` back to `'oauth'` (preserves all FK history but requires manual SQL).
```

**Step C.** Confirm `## Why this works`, `## References`, and the existing cross-link bullets at the bottom of the file remain intact and unchanged.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && grep -q "## Note on previously converted SAML users" docs/edition-reactivation.md && grep -q -i "converted" docs/edition-reactivation.md && grep -q "edition-deactivation" docs/edition-reactivation.md && grep -q -i "deferred" docs/edition-reactivation.md && grep -q "## Why this works" docs/edition-reactivation.md && grep -q "## References" docs/edition-reactivation.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "## Note on previously converted SAML users" docs/edition-reactivation.md` exits 0 (new section present, exact heading).
    - `grep -q -i "converted" docs/edition-reactivation.md` exits 0 (D-13 / RESEARCH §LIFECYCLE-06 grep target).
    - `grep -q "edition-deactivation" docs/edition-reactivation.md` exits 0 (cross-link present — was already present at line 74; preserved AND new link added in the new section).
    - `grep -q -i "deferred" docs/edition-reactivation.md` exits 0 (forward-pointer to deferred roadmap explicit).
    - `grep -q -i "manual" docs/edition-reactivation.md` exits 0 (manual procedure mentioned).
    - **Preservation greps:**
      - `grep -q "## Why this works" docs/edition-reactivation.md` exits 0 (existing section preserved below the new one).
      - `grep -q "## References" docs/edition-reactivation.md` exits 0.
      - `grep -q "deferred=True" docs/edition-reactivation.md` exits 0 (existing technical detail at line 70 preserved).
    - **Section ordering:** `awk '/^## /{print NR, $0}' docs/edition-reactivation.md` outputs section headings in this order: Re-mount the overlay → Post-reactivation verification checklist → End-to-end smoke test → Note on previously converted SAML users → Why this works → References.
  </acceptance_criteria>
  <done>
    `docs/edition-reactivation.md` has a new top-level section `## Note on previously converted SAML users` between `## End-to-end smoke test` and `## Why this works`. The paragraph documents that local-password conversions persist after reactivation, names the manual reverse-conversion options, and points at the deferred roadmap. All positive greps pass; existing sections (`## Why this works`, `## References`) remain intact in the correct order.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator (human reading) → Documentation | One-way, read-only. The runbook prescribes commands that the operator runs against the live deployment; trust boundary is the operator's terminal. |
| Documentation → External tools (curl, jq, psql) | The operator copies commands from the runbook and executes them. Documentation correctness is the trust contract — incorrect commands cause operational failures or security regressions. |

## STRIDE Threat Register (Plan 02 scope — documentation-only)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-221-01 | Elevation of Privilege | Self-conversion lockout (operator runs the curl with their own user_id and locks themselves out) | mitigate | Step 3 of the new section explicitly surfaces the self-conversion guard as a `> **Self-conversion is blocked.**` blockquote, naming the 422 response and recommending a second admin account. Backed in code by Plan 01 Task 3 (T-221-01 in Plan 01's threat model). |
| T-221-03 | Information Disclosure | Audit-log poisoning — operator believes the password ends up in `audit_logs.details` | mitigate | Step 3 explicitly states "Password material is never logged" and Step 6's audit-confirm SQL/output documents the allow-list `{from, to, provider_slug}` only. No example SQL or curl in the runbook ever shows password material flowing into the audit table. |
| T-221-04 | Tampering | Race with SAML login (operator runs conversion BEFORE deactivation; a SAML ACS POST hits a partially-converted user) | mitigate | The new section's preamble blockquote `> **Run conversions AFTER the overlay is removed**` explicitly orders the procedure. The forward-link replacing line 81 reinforces "convert each user *after* the overlay is removed." Both surfaces of the document agree. |
| T-221-PITFALL-4 | Tampering | Trailing-slash 307 redirect drops JSON body | mitigate | Every URL in the runbook (curl example) is documented WITH the trailing slash. Acceptance grep `grep -q "convert-saml-to-local/" docs/edition-deactivation.md` enforces. |
| T-221-PITFALL-5 | Tampering | Operator hits `/auth/token` (does not exist) and gets 404 | mitigate | Token-acquisition curl uses `/auth/login/` (form-data POST). Negative acceptance grep `grep -q "/auth/token" docs/edition-deactivation.md` exits NON-ZERO enforces. |

## Out-of-scope threats (handled by Plan 01 in code)

| Threat ID | Why out of scope |
|-----------|------------------|
| T-221-02 (Privilege escalation: non-admin invokes endpoint) | Mitigated by `require_permission("manage_users")` dependency in Plan 01 Task 3, not by documentation. |
| T-221-05 (Partial-failure leaves stale rows) | Mitigated by single-transaction conversion in Plan 01 Task 2/3, not by documentation. |
| T-221-06 (Multi-IdP DELETE over-reach) | Mitigated by id-scoped DELETE in Plan 01 Task 2, not by documentation. |

</threat_model>

<verification>
- Negative greps (TODO markers and wrong endpoints removed): `! grep -q "Phase 221 ships the user re-onboarding" docs/edition-deactivation.md`; `! grep -q "manually via the admin UI" docs/edition-deactivation.md`; `! grep -q "/auth/token" docs/edition-deactivation.md`.
- Positive greps (new section content): all entries listed in Task 1 and Task 2 acceptance criteria.
- Markdown sanity: balanced fenced code blocks (`awk` parity check).
- Preservation: existing maintenance-window sentence (line 83), destructive-path section, cross-links, and the entire `docs/edition-reactivation.md` body except the new insertion remain intact.
- Cross-document symmetry: both runbooks reference each other, and both reference the new endpoint and audit-log action name consistently.
</verification>

<success_criteria>
- LIFECYCLE-06 documentation slice complete: deactivation runbook has a tooled re-onboarding procedure, and the reactivation runbook warns about persisted conversions.
- Phase 220's line-81 TODO marker is gone from the repo (verified by negative grep).
- The new section uses the exact endpoint contract Plan 01 ships (trailing slash, audit action name, allow-listed details, self-conversion guard wording).
- All five Plan 02 STRIDE threats (T-221-01, T-221-03, T-221-04, T-221-PITFALL-4, T-221-PITFALL-5) have a concrete, grep-checkable mitigation in the runbook.
- No regressions in any other doc surface; preservation greps pass.
</success_criteria>

<output>
After completion, create `.planning/phases/221-lifecycle-user-continuity-and-verification/221-02-SUMMARY.md`.
</output>
