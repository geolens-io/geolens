---
phase: 1158-builder-layer-visibility-dem-consolidation
plan: 02
type: execute
wave: 2
depends_on: ["1158-01"]
files_modified:
  - frontend/src/components/builder/__tests__/UnifiedStackPanel.basemap-drag.test.tsx
  - frontend/src/components/builder/__tests__/color-relief-sync.test.ts
  - frontend/src/components/builder/__tests__/BuilderMap.terrain-visibility.test.tsx
  - frontend/src/components/builder/__tests__/UnifiedStackPanel.dem-rows.test.tsx
autonomous: true
requirements: [BLDR-01, BLDR-02, BLDR-03, BLDR-04]

must_haves:
  truths:
    - "A unit test proves a raster basemap layer is NOT lifted above data by reorderBasemapAboveData at position='top' (BLDR-01)"
    - "A unit test proves the color-relief companion addLayer carries layout.visibility==='none' when the parent is hidden and 'visible' when shown (BLDR-04)"
    - "A test proves applyTerrainConfig sets terrain to null when the terrain DEM layer is hidden and re-attaches when shown (BLDR-02)"
    - "A test proves the terrain-mode DEM layer is suppressed from the UnifiedStackPanel render while hillshade DEM rows still render (BLDR-03)"
  artifacts:
    - path: "frontend/src/components/builder/__tests__/UnifiedStackPanel.basemap-drag.test.tsx"
      provides: "BLDR-01 raster-basemap skip test (added to the existing UX-03 reorderBasemapAboveData describe block)"
      contains: "raster"
    - path: "frontend/src/components/builder/__tests__/color-relief-sync.test.ts"
      provides: "BLDR-04 layout.visibility assertions on the companion addLayer"
      contains: "visibility"
    - path: "frontend/src/components/builder/__tests__/BuilderMap.terrain-visibility.test.tsx"
      provides: "BLDR-02 terrain attach/detach-on-visibility integration test"
      contains: "setTerrain"
    - path: "frontend/src/components/builder/__tests__/UnifiedStackPanel.dem-rows.test.tsx"
      provides: "BLDR-03 terrain-row suppression test"
      contains: "render_mode"
  key_links:
    - from: "color-relief-sync.test.ts createMockMap addLayer mock"
      to: "syncColorReliefLayer layout assertion"
      via: "capturing the full addLayer object argument (mock.calls) to read layout.visibility"
      pattern: "addLayer.*mock"
---

<objective>
Add the regression test pins required by REQUIREMENTS.md for the four Phase 1158 fixes, plus a BLDR-03 row-suppression test. These tests assert the behavior implemented in Plan 01 and must be green at phase close.

- BLDR-01 pin: raster basemap not lifted above data at position='top' (extend the existing UX-03 describe block in UnifiedStackPanel.basemap-drag.test.tsx).
- BLDR-02 pin: getTerrain()/setTerrain — terrain becomes null when the terrain DEM is hidden, re-attaches when shown.
- BLDR-03 pin: terrain-mode DEM layer is suppressed from the UnifiedStackPanel stack; hillshade DEM rows still render.
- BLDR-04 pin: color-relief companion layout.visibility follows the parent's visible flag.

Purpose: Lock the four fixes against regression with deterministic vitest specs (the existing mock recipes make these self-contained — no live map needed).
Output: One extended test file (basemap-drag), one extended test file (color-relief-sync), two new test files (BuilderMap.terrain-visibility, UnifiedStackPanel.dem-rows). All green; `e2e:smoke:builder` stays green for BLDR-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1158-builder-layer-visibility-dem-consolidation/1158-PATTERNS.md
@.planning/phases/1158-builder-layer-visibility-dem-consolidation/1158-01-SUMMARY.md

<interfaces>
<!-- Test infrastructure already present at HEAD 130b83fc. Reuse it — do not invent new mock shapes. -->

UnifiedStackPanel.basemap-drag.test.tsx already has `describe('UX-03: reorderBasemapAboveData (map-sync helper)', ...)`
with a `makeMockMap(layers: Array<{ id; source?; type?; layout? }>)` helper (returns { moveLayer: vi.fn(), getLayer,
getStyle }) and Tests 7–11. Test 7 already asserts moveLayer call counts and not-called for base fills/data.
The makeMockMap layer objects support a `type` field — a raster basemap fixture is `{ id, type: 'raster', source: <non-data source> }`.

color-relief-sync.test.ts already has `createMockMap()` (getLayer/addLayer/removeLayer/getSource vi.fns; addLayer
stores only { id }) and `makeInput(overrides)` (full AdapterLayerInput with `visible: true` togglable via overrides).
To assert layout.visibility, read the captured argument from the addLayer mock's call list (the mock receives the
full layer object even though it only stores the id). Enable the companion with paint `{ '_hypso-enabled': true }`
and style_config `{ render_mode: 'hillshade' }` and a present source so the add path runs.

BuilderMap.a11y.test.tsx has the canonical `vi.hoisted` fakeMap + `vi.mock('@vis.gl/react-maplibre')` recipe:
fakeMap exposes on/off/once/emit, isStyleLoaded(()=>true), setTerrain: vi.fn(), getSource, getLayer, getStyle,
triggerRepaint. The Map mock fires onLoad({ target: fakeMap }) in a useEffect. This is the analog to drive
applyTerrainConfig through a real BuilderMap render.

isDemTerrainVisualSuppressed(layer) — true when is_dem===true && style_config.render_mode==='terrain'.
StackRow renders a glyph per render_mode (⛰ hillshade, ◬ terrain) and each row carries data-row-id / role="listitem".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BLDR-01 + BLDR-04 unit pins (basemap-drag raster skip; color-relief companion visibility)</name>
  <files>frontend/src/components/builder/__tests__/UnifiedStackPanel.basemap-drag.test.tsx, frontend/src/components/builder/__tests__/color-relief-sync.test.ts</files>
  <read_first>
    - frontend/src/components/builder/__tests__/UnifiedStackPanel.basemap-drag.test.tsx (UX-03 describe block, makeMockMap :268-277, Tests 7–11 :279-344 — copy Test 7's assertion style)
    - frontend/src/components/builder/__tests__/color-relief-sync.test.ts (createMockMap :8-27, makeInput :29-52, an existing add/remove test for the enable gating)
    - frontend/src/components/builder/color-relief-sync.ts (syncColorReliefLayer :66-113 — confirms the enable gate `_hypso-enabled === true && render_mode==='hillshade'` and that addLayer now carries layout from input.visible)
    - .planning/phases/1158-builder-layer-visibility-dem-consolidation/1158-PATTERNS.md (Test Pin Locations table; MapLibre mock recipe)
  </read_first>
  <action>
    BLDR-01: in UnifiedStackPanel.basemap-drag.test.tsx, inside the existing `describe('UX-03: reorderBasemapAboveData ...')` block, add a new test (next sequential number after Test 11) named to the effect of "raster basemap layer is NOT lifted above data at position='top'". Build a styleLayers fixture via the existing makeMockMap that includes: a raster basemap layer `{ id: 'imagery-basemap', type: 'raster', source: 'esri-imagery' }` (non-data source), a non-raster reference-detail layer `{ id: 'road-primary', type: 'line', source: 'openmaptiles' }`, and a data layer `{ id: 'layer-data-1', source: 'source-data-population' }`. Call `reorderBasemapAboveData(map, 'top')` and assert `expect(map.moveLayer).not.toHaveBeenCalledWith('imagery-basemap')`, `expect(map.moveLayer).toHaveBeenCalledWith('road-primary')`, and `expect(map.moveLayer).not.toHaveBeenCalledWith('layer-data-1')`. Mirror Test 7's assertion idiom.

    BLDR-04: in color-relief-sync.test.ts, add two tests asserting the companion's layout.visibility tracks input.visible. To read the layout, capture the addLayer argument — e.g. spy on the existing createMockMap addLayer mock and read `(map.addLayer as ReturnType<typeof vi.fn>).mock.calls[0][0].layout.visibility`. Test A: `makeInput({ visible: false, paint: { '_hypso-enabled': true }, style_config: { render_mode: 'hillshade' } })` with the source pre-added so the add path runs → assert the captured addLayer object has `layout.visibility === 'none'`. Test B: same with `visible: true` → assert `layout.visibility === 'visible'`. Ensure the source exists in the mock before calling syncColorReliefLayer (push the sourceId into the mock's `_sources` set, matching how existing enabled-path tests set it up). Do not modify createMockMap or makeInput signatures beyond what overrides already allow.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run color-relief-sync UnifiedStackPanel.basemap-drag</automated>
  </verify>
  <acceptance_criteria>
    - A new test in the UX-03 describe block asserts `moveLayer` is NOT called with the raster basemap id and IS called with the non-raster detail layer id at position='top'.
    - Two new color-relief-sync tests assert the captured addLayer object's `layout.visibility` is 'none' when input.visible===false and 'visible' when input.visible===true.
    - The targeted vitest run passes (all tests in both files green, including pre-existing tests).
    - No production source files edited (test files only).
  </acceptance_criteria>
  <done>BLDR-01 raster-skip and BLDR-04 companion-visibility behaviors are pinned and green.</done>
</task>

<task type="auto">
  <name>Task 2: BLDR-02 + BLDR-03 pins (terrain attach/detach-on-visibility; terrain-row suppression)</name>
  <files>frontend/src/components/builder/__tests__/BuilderMap.terrain-visibility.test.tsx, frontend/src/components/builder/__tests__/UnifiedStackPanel.dem-rows.test.tsx</files>
  <read_first>
    - frontend/src/components/builder/__tests__/BuilderMap.a11y.test.tsx (vi.hoisted fakeMap :89-147 and the @vis.gl/react-maplibre mock :149-161; the props/render harness used to mount BuilderMap)
    - frontend/src/components/builder/BuilderMap.tsx (applyTerrainConfig :379-411 — note it needs a raster token via tokenMap and a terrain-mode demLayer; the effect at :898-913)
    - frontend/src/components/builder/UnifiedStackPanel.tsx (render loop :977 and the visibleStackLayers filter added in Plan 01; how StackRow/data-row-id is emitted)
    - frontend/src/components/builder/__tests__/UnifiedStackPanel.basemap-drag.test.tsx (top-of-file: the full UnifiedStackPanel render harness — required props, provider wrappers, render() usage — reuse it verbatim for the dem-rows test)
    - .planning/phases/1158-builder-layer-visibility-dem-consolidation/1158-PATTERNS.md (React component map mock note for BLDR-02; Test Pin Locations table)
  </read_first>
  <action>
    BLDR-02 (BuilderMap.terrain-visibility.test.tsx, NEW file): copy the `vi.hoisted` fakeMap + `vi.mock('@vis.gl/react-maplibre')` recipe from BuilderMap.a11y.test.tsx. Extend the fakeMap so `getSource` returns a truthy raster-dem source for TERRAIN_SOURCE_ID (so ensureRasterDemTerrainSource does not block the attach path) and keep `setTerrain: vi.fn()`, `isStyleLoaded: ()=>true`. Render BuilderMap with: a terrain-mode DEM layer (`is_dem: true`, `style_config: { render_mode: 'terrain' }`, `dataset_id` matching), a `terrainConfig` with `enabled: true` and `source_dataset_id` equal to that dataset_id, and a `tokenMap` entry for that dataset_id with `kind: 'raster'` and a tile_url/tile_size/zoom range (mirror the token shape applyTerrainConfig reads at :394-411). Reuse the minimal required-props set from the a11y harness; only the terrain-relevant props need realistic values, others can stay at the a11y defaults. Test A: render with the DEM layer `visible: true` → assert `fakeMap.setTerrain` was last called with an object containing `source: TERRAIN_SOURCE_ID` (terrain attached). Test B: re-render (rerender) with the same layer `visible: false` → assert `fakeMap.setTerrain` was called with `null` (terrain detached). Drive the idle/load path via the a11y harness's onLoad/emit mechanism so applyTerrainConfig runs. Extracting the effective-enabled computation into a new exported helper is NOT permitted — the audited fix keeps it inline in applyTerrainConfig, so this test stays at the component-integration level. If the integration render proves un-mountable in this harness despite reusing the a11y recipe, document the specific blocker in the SUMMARY (for the Phase 1160 live MCP gate to cover BLDR-02) rather than changing the Plan 01 fix shape to make it testable.

    BLDR-03 (UnifiedStackPanel.dem-rows.test.tsx, NEW file): reuse the UnifiedStackPanel render harness from UnifiedStackPanel.basemap-drag.test.tsx (same required props, providers, render()). Provide a `layers` prop containing: one hillshade DEM layer (`is_dem: true`, `style_config: { render_mode: 'hillshade' }`, e.g. id 'dem-hillshade') and one terrain-mode DEM layer (`is_dem: true`, `style_config: { render_mode: 'terrain' }`, e.g. id 'dem-terrain'). Render and assert: the hillshade row IS present (`document.querySelector('[data-row-id="dem-hillshade"]')` truthy) and the terrain row is NOT rendered (`[data-row-id="dem-terrain"]` is null). Use the data-row-id attribute the existing basemap-drag tests query (they already assert `[data-row-id="data-1"]`), so confirm the exact attribute/id mapping while reading the harness. This proves the visibleStackLayers filter suppresses the terrain-mode DEM row.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run BuilderMap.terrain-visibility UnifiedStackPanel.dem-rows</automated>
  </verify>
  <acceptance_criteria>
    - BuilderMap.terrain-visibility.test.tsx asserts setTerrain is called with an object containing `source: TERRAIN_SOURCE_ID` when the terrain DEM layer is visible, and with `null` when it is hidden.
    - UnifiedStackPanel.dem-rows.test.tsx asserts the hillshade DEM row renders (data-row-id present) and the terrain-mode DEM row does not (data-row-id null).
    - Both new test files pass; no production source edited; the BLDR-02 fix shape from Plan 01 is unchanged (no helper extraction).
    - The targeted vitest run passes.
  </acceptance_criteria>
  <done>BLDR-02 terrain attach/detach-on-visibility and BLDR-03 terrain-row suppression are pinned and green.</done>
</task>

<task type="auto">
  <name>Task 3: Full frontend gate — typecheck + vitest + builder e2e smoke</name>
  <files>frontend/src/components/builder/__tests__/UnifiedStackPanel.basemap-drag.test.tsx, frontend/src/components/builder/__tests__/color-relief-sync.test.ts, frontend/src/components/builder/__tests__/BuilderMap.terrain-visibility.test.tsx, frontend/src/components/builder/__tests__/UnifiedStackPanel.dem-rows.test.tsx</files>
  <read_first>
    - .planning/phases/1158-builder-layer-visibility-dem-consolidation/1158-PATTERNS.md (Build & Test Recipe — typecheck → vitest → e2e:smoke:builder)
    - frontend/package.json (confirm the `typecheck`, `test`, and the builder e2e smoke script names — e2e:smoke:builder)
  </read_first>
  <action>
    Run the full frontend regression gate and confirm green: (1) `npm run typecheck` must be 0 errors; (2) the full vitest suite `npm test -- --run` must be green (catches any cross-file fallout from the Plan 01 UnifiedStackPanel/map-sync edits, e.g. existing UnifiedStackPanel tests that count rows); (3) the builder e2e smoke subset (`e2e:smoke:builder`, or `npm run test:e2e -- e2e/builder*.spec.ts` per PATTERNS.md) must be green — BLDR-03 changes the rendered stack, so confirm no smoke spec regressed on DEM-layer row counts. If any pre-existing vitest spec asserted the old three-row / terrain-row-present behavior, update that spec to the new single-DEM-row expectation (this is the only sanctioned production-adjacent spec edit; do not weaken assertions to pass). Record the exact pass counts in the SUMMARY. If the e2e smoke cannot run in this environment (no browser/stack), document that explicitly in the SUMMARY and flag it for the Phase 1160 close-gate rather than marking it green.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0.
    - `npm test -- --run` (full vitest) is green, including all four BLDR test files and any pre-existing UnifiedStackPanel/map-sync/BuilderMap specs.
    - `e2e:smoke:builder` is green (or its inability to run in this environment is explicitly documented in the SUMMARY for the Phase 1160 gate).
    - Any spec updated to reflect the new single-DEM-row stack tightens (not weakens) its expectation; the SUMMARY lists every spec touched and why.
  </acceptance_criteria>
  <done>typecheck 0, full vitest green, builder e2e smoke green (or documented-deferred); pass counts recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Test-only plan. No production code, no new trust boundary, no new auth/data-egress/input surface. Same disposition as Plan 01.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1158-02 | (none) | vitest test files | accept | No attack surface — adds/extends client-side unit/integration tests only. |
</threat_model>

<verification>
- `cd frontend && npm run typecheck` → 0 errors.
- `cd frontend && npm test -- --run` → full vitest green (the four BLDR test files plus all pre-existing specs).
- `e2e:smoke:builder` green (BLDR-03 changes the rendered stack; confirm no smoke regression) or explicitly documented as un-runnable here for the Phase 1160 gate.
</verification>

<success_criteria>
- BLDR-01 pin green: raster basemap not lifted above data at position='top'.
- BLDR-02 pin green: terrain detaches when the DEM layer is hidden, re-attaches when shown.
- BLDR-03 pin green: terrain-mode DEM row suppressed from the stack; hillshade DEM row still renders.
- BLDR-04 pin green: color-relief companion layout.visibility follows the parent visible flag.
- typecheck 0, full vitest green, builder e2e smoke green (or documented-deferred).
</success_criteria>

<output>
Create `.planning/phases/1158-builder-layer-visibility-dem-consolidation/1158-02-SUMMARY.md` when done.
</output>
