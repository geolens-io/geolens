---
phase: 1163-i18n-key-rename
plan: 02
type: execute
wave: 2
depends_on: ["1163-01"]
files_modified:
  - frontend/src/components/map-plugins/register-plugins.ts
  - frontend/src/components/map-plugins/PluginPanel.tsx
  - frontend/src/components/map-plugins/PluginErrorBoundary.tsx
  - frontend/src/components/map-plugins/builtin/MeasurementPlugin.tsx
  - frontend/src/components/map-plugins/builtin/LegendPlugin.tsx
  - frontend/src/components/builder/MapToolbar.tsx
  - frontend/src/components/builder/SettingsEditorScene.tsx
  - frontend/src/components/admin/settings/SettingsMapTab.tsx
  - frontend/src/components/map-plugins/__tests__/PluginHost.test.tsx
  - frontend/src/components/map-plugins/__tests__/registry.test.ts
  - frontend/src/components/builder/__tests__/SettingsEditorScene.test.tsx
  - frontend/src/components/builder/__tests__/SettingsEditorScene.plugins.test.tsx
  - frontend/src/components/builder/__tests__/MapBuilderPage.settings-wiring.test.tsx
autonomous: true
requirements: [I18N-01]

must_haves:
  truths:
    - "Every `t('widgets.*')` / `t('settings.widgets*')` / `labelKey: 'widgets.*'` / `builder:widgets.*` call site is repointed to its renamed plugin key"
    - "Tests asserting the renamed VALUES are updated ('WIDGETS'→'PLUGINS', 'This widget encountered an error'→'This plugin encountered an error')"
    - "`npm run typecheck` reports 0 errors"
    - "`npm run test:i18n` passes (parity 2/2) and the renamed map-plugins + settings-editor vitest suites are green"
    - "Zero old-key i18n call sites remain (no `t('widgets`, `labelKey: 'widgets`, `builder:widgets`, `settings.widgets` in frontend/src non-locale code)"
    - "Plugin ID literals `measurement`/`legend` unchanged; the `legend` namespace segment in `t('plugins.legend.*')` kept"
  artifacts:
    - path: "frontend/src/components/map-plugins/register-plugins.ts"
      provides: "labelKey repointed to plugins.measurement.label / plugins.legend.label"
      contains: "plugins.measurement.label"
    - path: "frontend/src/components/builder/SettingsEditorScene.tsx"
      provides: "settings.plugins* keys + PLUGINS defaultValues"
      contains: "settings.pluginsLabel"
  key_links:
    - from: "frontend/src code call sites"
      to: "renamed plugin i18n keys defined in Plan 01"
      via: "t('plugins.*') / t('settings.plugins*') / labelKey 'plugins.*' / i18n.t('builder:plugins.pluginError')"
      pattern: "(builder:)?plugins\\.|settings\\.plugins"
---

<objective>
Update every `t('...')` call site (and string-literal i18n key, e.g. `labelKey: 'widgets.measurement.label'`) in `frontend/src` that Phase 1162 deliberately left pointing at the old `widgets.*` paths, repointing each to the renamed plugin key that Plan 01 created. Also update the small number of tests that assert the renamed user-visible VALUES. Then run the full Phase 1163 verification gate.

This is the second half of **I18N-01** and the phase-terminal gate. It depends on Plan 01 (the renamed keys must already exist, or these call sites resolve to nothing). Together Plan 01 + Plan 02 deliver I18N-01: zero widget-namespaced keys in locale files, zero old-key call sites, full 4-locale parity, typecheck 0.

Purpose: Make the running UI render the renamed plugin strings (not raw keys or stale "Widget" text) and lock the phase with green typecheck + parity + suites.
Output: ~13 modified files; typecheck 0; i18n parity green; renamed suites green; zero old-key call sites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Plan 01 SUMMARY — confirms the renamed keys this plan repoints to actually exist.
@.planning/phases/1163-i18n-key-rename/1163-01-SUMMARY.md

# The i18n parity gate this plan must pass.
@frontend/src/i18n/resources.test.ts

# The two builtin plugins + host/panel/error-boundary that hold most call sites.
@frontend/src/components/map-plugins/register-plugins.ts
@frontend/src/components/map-plugins/PluginErrorBoundary.tsx
@frontend/src/components/builder/SettingsEditorScene.tsx
</context>

<notes>
## Verified call-site inventory (HEAD e3c4c67c, branch main) — the authoritative work list

Phase 1162's terminal gate already removed all NON-i18n widget→plugin identifiers, so the remaining `widget` strings in non-locale frontend code are i18n key references + a handful of cosmetic prose strings. Count at plan time: **27 `t()`/string-literal i18n-key references** (this includes the **11 `labelKey: 'widgets.*'`** literals) across production + test files. Re-run STEP A's grep on the live tree before editing — treat its output as the work list, not this snapshot.

### PRODUCTION call sites (MUST fix — wrong key = missing translation at runtime)

- `register-plugins.ts:8` `labelKey: 'widgets.measurement.label'` → `'plugins.measurement.label'`; `:17` `'widgets.legend.label'` → `'plugins.legend.label'`
- `PluginPanel.tsx:34` `aria-label={t('widgets.closeWidget')}` → `t('plugins.closePlugin')`
- `PluginErrorBoundary.tsx:24` `i18n.t('builder:widgets.widgetError')` → `i18n.t('builder:plugins.pluginError')`
- `MeasurementPlugin.tsx` :264,:268 distance · :278,:282 area · :295 clickToMeasure · :305 metric/imperial · :311,:314 clear — all `t('widgets.measurement.X')` → `t('plugins.measurement.X')`
- `LegendPlugin.tsx` :87 noLayers · :132 low · :133 high · :134 weightedBy · :197 unavailable — all `t('widgets.legend.X')` → `t('plugins.legend.X')`
- `MapToolbar.tsx` :63 `t('widgets.measurement.label', …)` → `t('plugins.measurement.label', …)`; :117,:124 `t('widgets.legend.label', …)` → `t('plugins.legend.label', …)`
- `SettingsEditorScene.tsx` :163 `settings.widgetsLabel` (defaultValue 'WIDGETS' → 'PLUGINS') · :167 `settings.widgetsEnabledCount`→`settings.pluginsEnabledCount` · :175 `settings.noWidgets`→`settings.noPlugins` · :181 `settings.widgetsAvailabilityNote`→`settings.pluginsAvailabilityNote` · :185 `settings.widgetsGroupAria` (defaultValue 'Widgets' → 'Plugins') · :192 `settings.disableWidget`→`settings.disablePlugin` · :193 `settings.enableWidget`→`settings.enablePlugin`. Also update the prose comments `// Widgets` (L20) and `{/* Section 3: WIDGETS */}` (L151) → plugins/PLUGINS for vocabulary cleanliness (same file, free).
- `SettingsMapTab.tsx` :63 `t('settings.widgets.title')` → `t('settings.plugins.title')`; :66 `t('settings.widgets.description')` → `t('settings.plugins.description')` (admin namespace)

Keep the namespace prefix and interpolation args intact (e.g. `useTranslation('builder')` vs `'admin'`; `{ count }`, `{ name }`, `{ column }`, `defaultValue`). Update each `defaultValue` string that contains the widget word to its plugin equivalent so the fallback also reads correctly (e.g. 'WIDGETS'→'PLUGINS', 'Widgets'→'Plugins'). The `measurement`/`legend` ID segment in keys is KEPT (only the leading `widgets.`→`plugins.` namespace changes).

### TEST call sites — two categories

(a) String-literal i18n KEYS used as `labelKey` in mocked registries (repoint so the mock resolves consistently):
- `PluginHost.test.tsx` :46–49 `labelKey: 'widgets.a'/'widgets.b'/'widgets.crash'/'widgets.sidebar'` → `'plugins.a'/'plugins.b'/'plugins.crash'/'plugins.sidebar'` (arbitrary test keys; rename for consistency with the new namespace)
- `SettingsEditorScene.test.tsx` :82,:83 · `SettingsEditorScene.plugins.test.tsx` :93,:94 · `MapBuilderPage.settings-wiring.test.tsx` :74 — all `labelKey: 'widgets.measurement.label'`/`'widgets.legend.label'` → `'plugins.measurement.label'`/`'plugins.legend.label'`

(b) Assertions on renamed VALUES (break when Plan 01 changes the value — MUST update):
- `SettingsEditorScene.test.tsx:129` `expect(screen.getByText('WIDGETS'))` → `'PLUGINS'`
- `PluginHost.test.tsx:137` `expect(screen.getByText('This widget encountered an error'))` → `'This plugin encountered an error'` (the inline comment at :136 already notes "i18n value is renamed in Phase 1163")
- Cosmetic test prose/fixtures in these same files (cheap, serve the "no widgets vocabulary" intent): `registry.test.ts:5` comment `register-widgets.ts`→`register-plugins.ts`; `:11,:26,:41,:45` `'Test Widget'`/`'Updated Widget'` arbitrary fixture labels → `'Test Plugin'`/`'Updated Plugin'`; `PluginHost.test.tsx:37` `'Widget crashed!'`→`'Plugin crashed!'`; the `describe('SettingsEditorScene · Widgets section …')` + `// Widgets` header comments → plugins.

### OUT OF SCOPE for this phase (do NOT gate on these; leave or defer to Phase 1164)

These contain the substring "widget" but are NOT i18n keys and are NOT covered by I18N-01 (whose zero-widget assertion is scoped to **locale files**):
- `registry.ts:8` console.warn `Widget "..."`, `PluginErrorBoundary.tsx:17` logger.error `Widget "..."`, `PluginPanel.tsx:19` + `types.ts:23` JSDoc — internal dev/log strings. Renaming to "Plugin" is reasonable cleanup but is genuinely Phase 1164's broader vocabulary sweep; if you rename them while in-file, fine, but they are NOT a gate.
- `LegendPlugin.tsx:169` `layerId={`legend-widget-${idx}`}` — a passthrough prop to ColorizedGeometryIcon; NOT a data-testid, NOT asserted in any test or e2e (verified). Low-risk to rename to `legend-plugin-${idx}` for consistency, but it is an internal DOM id, not an i18n key — NOT a gate.
- `ActiveFilterChips.tsx:128` + `DEMEditorScene.tsx:285` + `DEMEditorScene.test.tsx:292,293` "compass widget" comments — unrelated UI-spec prose, not the plugin platform; LEAVE unchanged.
- `normalize-saved-map.test.ts:109,119,151` — `plugins: ['widget-a']` test fixture VALUE + a "undefined widgets" test name; exercise the already-renamed `plugins` field with arbitrary id strings + a descriptive title. Not an i18n key. Optional; LEAVE unless trivially clarifying.

The phase gate (Task 2 STEP C) asserts ONLY what I18N-01 mandates: zero widget in locale files + zero old-key i18n call sites + parity + typecheck. It does NOT assert zero "widget" substring across all of src (that broader sweep is Phase 1164).
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Repoint all production i18n call sites + string-literal keys to plugin namespace</name>
  <files>frontend/src/components/map-plugins/register-plugins.ts, frontend/src/components/map-plugins/PluginPanel.tsx, frontend/src/components/map-plugins/PluginErrorBoundary.tsx, frontend/src/components/map-plugins/builtin/MeasurementPlugin.tsx, frontend/src/components/map-plugins/builtin/LegendPlugin.tsx, frontend/src/components/builder/MapToolbar.tsx, frontend/src/components/builder/SettingsEditorScene.tsx, frontend/src/components/admin/settings/SettingsMapTab.tsx</files>
  <action>
Implements I18N-01 (production call-site half).

STEP A — re-run the discovery grep against the live tree and use it as the work list:
`cd frontend && grep -rn "widgets\.\|builder:widgets\|settings\.widgets\|widgetsLabel\|widgetsEnabledCount\|noWidgets\|widgetsGroupAria\|enableWidget\|disableWidget\|widgetsAvailabilityNote\|widgetError\|closeWidget" src --include=*.ts --include=*.tsx | grep -v "src/i18n/locales/" | grep -v "__tests__"`

STEP B — repoint every PRODUCTION match per the inventory in `<notes>`: register-plugins labelKeys → `plugins.measurement.label`/`plugins.legend.label`; PluginPanel `closeWidget`→`closePlugin`; PluginErrorBoundary `builder:widgets.widgetError`→`builder:plugins.pluginError`; MeasurementPlugin + LegendPlugin + MapToolbar `widgets.measurement.*`/`widgets.legend.*`→`plugins.*`; SettingsEditorScene `settings.widgets*`→`settings.plugins*` with defaultValues 'WIDGETS'→'PLUGINS' and 'Widgets'→'Plugins'; SettingsMapTab `settings.widgets.title`/`description`→`settings.plugins.*`. Preserve namespace prefixes, interpolation args, and the `measurement`/`legend` key segments. Update widget-word defaultValues to their plugin equivalents. Optionally clean the two SettingsEditorScene prose comments (`// Widgets`, `Section 3: WIDGETS`).
  </action>
  <verify>
  <automated>cd frontend && test "$(grep -rn 'widgets\.\|builder:widgets\|settings\.widgets\|widgetsLabel\|widgetsEnabledCount\|noWidgets\|widgetsGroupAria\|enableWidget\|disableWidget\|widgetsAvailabilityNote\|widgetError\|closeWidget' src --include=*.ts --include=*.tsx | grep -v 'src/i18n/locales/' | grep -v '__tests__' | wc -l | tr -d ' ')" = "0" && echo PROD_CALLSITES_CLEAN</automated>
  </verify>
  <done>Zero old-key i18n references in production (non-test, non-locale) frontend code; `register-plugins.ts` uses `plugins.measurement.label`/`plugins.legend.label`; `SettingsEditorScene.tsx` uses `settings.pluginsLabel`/`noPlugins`/`pluginsGroupAria`/`enablePlugin`/`disablePlugin`/`pluginsAvailabilityNote`/`pluginsEnabledCount`; `SettingsMapTab.tsx` uses `settings.plugins.title`/`description`; `PluginErrorBoundary.tsx` uses `builder:plugins.pluginError`.</done>
</task>

<task type="auto">
  <name>Task 2: Update test i18n keys + value assertions, then run the full phase gate</name>
  <files>frontend/src/components/map-plugins/__tests__/PluginHost.test.tsx, frontend/src/components/map-plugins/__tests__/registry.test.ts, frontend/src/components/builder/__tests__/SettingsEditorScene.test.tsx, frontend/src/components/builder/__tests__/SettingsEditorScene.plugins.test.tsx, frontend/src/components/builder/__tests__/MapBuilderPage.settings-wiring.test.tsx</files>
  <action>
Implements I18N-01 (test half) + runs the phase-terminal gate.

STEP A — update test string-literal i18n KEYS (category (a) in `<notes>`): PluginHost.test.tsx repoint `labelKey: 'widgets.a/b/crash/sidebar'`→`'plugins.a/b/crash/sidebar'`; SettingsEditorScene.test.tsx (:82,:83), SettingsEditorScene.plugins.test.tsx (:93,:94), MapBuilderPage.settings-wiring.test.tsx (:74) repoint `labelKey: 'widgets.measurement.label'`/`'widgets.legend.label'`→`'plugins.measurement.label'`/`'plugins.legend.label'`.

STEP B — update value assertions (category (b)): SettingsEditorScene.test.tsx:129 `'WIDGETS'`→`'PLUGINS'`; PluginHost.test.tsx:137 `'This widget encountered an error'`→`'This plugin encountered an error'`. Clean cosmetic test prose/fixtures in these files (`'Test Widget'`/`'Updated Widget'`→Plugin; `register-widgets.ts`→`register-plugins.ts` in the registry.test.ts comment; `'Widget crashed!'`→`'Plugin crashed!'`; the `· Widgets section` describe + `// Widgets` comments → plugins).

STEP C — run the full Phase 1163 gate (all green):
1. `cd frontend && npm run typecheck` → 0 errors
2. `cd frontend && npm run test:i18n` → parity 2/2 (both `it()` blocks pass)
3. `cd frontend && npm run test -- src/components/map-plugins src/components/builder/__tests__/SettingsEditorScene.test.tsx src/components/builder/__tests__/SettingsEditorScene.plugins.test.tsx src/components/builder/__tests__/MapBuilderPage.settings-wiring.test.tsx` → renamed suites green (PluginHost error-boundary asserting the renamed string; SettingsEditorScene WIDGETS→PLUGINS; registry/plugins suites)
4. Grep gates (both must be 0): `grep -rin "widget" src/i18n/locales/` and `grep -rn "widgets\.\|builder:widgets\|settings\.widgets\|widgetsLabel\|widgetsEnabledCount\|noWidgets\|widgetsGroupAria\|enableWidget\|disableWidget\|widgetsAvailabilityNote\|widgetError\|closeWidget\|labelKey: 'widgets" src --include=*.ts --include=*.tsx | grep -v "src/i18n/locales/"`
  </action>
  <verify>
  <automated>cd frontend && npm run typecheck && npm run test:i18n && test "$(grep -rin widget src/i18n/locales/ | wc -l | tr -d ' ')" = "0" && test "$(grep -rn "widgets\.\|builder:widgets\|settings\.widgets\|widgetsLabel\|widgetsEnabledCount\|noWidgets\|widgetsGroupAria\|enableWidget\|disableWidget\|widgetsAvailabilityNote\|widgetError\|closeWidget\|labelKey: 'widgets" src --include=*.ts --include=*.tsx | grep -v 'src/i18n/locales/' | wc -l | tr -d ' ')" = "0" && echo GATE_PASS</automated>
  </verify>
  <done>typecheck 0; `test:i18n` parity green; zero `widget` in locale json; zero old-key i18n call sites (prod + test) in frontend code; renamed map-plugins + settings-editor suites green. Output `GATE_PASS`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only repoints i18n key references and updates string assertions in tests. No network/auth/data-handling surface changes; no new untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1163-02 | Denial of Service (UX) | builder/admin UI rendering raw `t()` keys or stale "Widget" text | mitigate | Task 1 repoints every production call site (grep-verified to 0 old keys); Task 2 STEP C runs typecheck + `test:i18n` parity + the renamed suites, so a missed key or a stale value assertion fails the phase. |
| T-1163-03 | Tampering | locale↔call-site key drift across the two-plan split | mitigate | Plan 02 depends_on 1163-01; its gate greps BOTH locale files (=0 widget) AND call sites (=0 old key) in one pass, catching any key that exists on only one side. |
| T-1163-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure key-string + test-assertion edits to existing committed files. Legitimacy gate N/A. |
</threat_model>

<verification>
Phase gate (Task 2 STEP C, all green on `cd frontend`):
1. `npm run typecheck` = 0 errors
2. `npm run test:i18n` = parity 2/2 (both `it()` blocks pass)
3. `npm run test -- src/components/map-plugins …SettingsEditorScene… …MapBuilderPage.settings-wiring…` = renamed suites green
4. `grep -rin "widget" src/i18n/locales/` = 0
5. `grep -rn "widgets\.\|builder:widgets\|settings\.widgets\|…\|labelKey: 'widgets" src --include=*.ts --include=*.tsx | grep -v "src/i18n/locales/"` = 0

Maps to ROADMAP Phase 1163 success criteria:
- SC#1 (no widget-namespaced key remains in any locale file) → gate #4 (combined with Plan 01)
- SC#2 (4 locales identical key sets, parity 2/2) → gate #2
- SC#3 (UI renders "Plugin" not "Widget") → gate #1 + #3 + #5 (call sites resolve to renamed keys whose values read "Plugin"; value-asserting tests confirm)
</verification>

<success_criteria>
I18N-01 fully satisfied (Plan 01 + Plan 02):
- All ~64 widget-namespaced i18n keys renamed to the plugin namespace (en/es/fr/de × builder/admin).
- Zero old-key i18n call sites in `frontend/src` (production + tests).
- Full 4-locale key parity (`npm run test:i18n` 2/2).
- Zero `widget` references in `frontend/src/i18n/locales/`.
- `npm run typecheck` 0; renamed plugin + settings-editor suites green.
- Plugin ID literals `measurement` / `legend` preserved (milestone invariant).
</success_criteria>

<output>
Create `.planning/phases/1163-i18n-key-rename/1163-02-SUMMARY.md` when done.
</output>
