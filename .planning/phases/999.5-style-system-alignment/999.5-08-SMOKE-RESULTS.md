---
phase: 999.5-style-system-alignment
plan: 08
artifact: smoke-results
status: automated-gates-passed-awaiting-manual-smoke
generated: 2026-04-26
operator: pending
---

# Phase 999.5 — Smoke Results

**Verification record for `/gsd-verify-work`.** Cross-references every observation back to a locked decision (D-01..D-11) per D-10's "single bundled phase" delivery model.

> **State at file generation:** All seven automated gates green (Task 1 complete). Manual visual smoke checklist (Task 2, M1–M11) is awaiting the operator.

---

## Automated Gates

| # | Gate | Command | Exit | Pass/Fail | Decisions Covered |
|---|------|---------|------|-----------|-------------------|
| G1 | Drift CI (token-sync) | `bash docs/scripts/check-token-sync.sh` (in `getgeolens.com`) | 0 | PASS | D-03 (IBM Plex font-family drift); Phase 224 BRAND-04 (10-stop primary palette) |
| G2 | Marketing root build | `npm run build` (in `getgeolens.com`) | 0 | PASS | D-01 (typography), D-06 (rationale comment compiles cleanly) |
| G3 | Docs astro check | `npx astro check` (in `getgeolens.com/docs`) | 0 | PASS | D-05 (Starlight `logo` schema-valid) |
| G4 | Docs build | `npm run build` (in `getgeolens.com/docs`) | 0 | PASS | D-01, D-02, D-04, D-05 (full docs surface) |
| G5 | App DESIGN-GUIDE.md unchanged | `git status --porcelain docs/DESIGN-GUIDE.md` (in `geolens`) | 0 lines | PASS | D-09 (north-star untouched) |
| G6 | New cross-surface DESIGN-GUIDE.md present | `test -f getgeolens.com/DESIGN-GUIDE.md && wc -l` | present, 310 lines (≥80) | PASS | D-07, D-08 (new guide authored) |
| G7 | Logo SVGs byte-identical | `cmp public/logo.svg docs/src/assets/logo.svg` (in `getgeolens.com`) | 0 | PASS | D-05 (single canonical SVG) |

### Gate Output Snippets (last lines of each command)

**G1 — Drift CI:**
```
All 10 --primary-* stops in sync between marketing and docs.
IBM Plex Sans Variable font-family declaration found in all available surfaces.
EXIT=0
```

**G2 — Marketing build:**
```
[build] ✓ Completed in 835ms.
[@astrojs/sitemap] sitemap-index.xml created at dist
[build] 4 page(s) built in 867ms
[build] Complete!
```

**G3 — Docs astro check:**
```
[check] Getting diagnostics for Astro files in /Users/ishiland/Code/getgeolens.com/docs...
Result (11 files):
- 0 errors
- 0 warnings
- 0 hints
```

**G4 — Docs build:**
```
[starlight:pagefind] Found 238 HTML files.
[starlight:pagefind] Finished building search index in 114ms.
 validating links
· All internal links are valid. ·
[build] 238 page(s) built in 3.85s
[build] Complete!
```

**G5 — App DESIGN-GUIDE.md unchanged:**
```
$ git status --porcelain docs/DESIGN-GUIDE.md
(empty output)
$ git log --oneline -1 -- docs/DESIGN-GUIDE.md
26e7c930 fix: dataset detail UX review + post-impl audit remediation   ← pre-999.5, untouched
```

**G6 — New DESIGN-GUIDE.md:** 310 lines at `getgeolens.com/DESIGN-GUIDE.md` (commit `86a4dab`).

**G7 — Logo SVG byte-identity:** `public/logo.svg` and `docs/src/assets/logo.svg` byte-identical (commits `0e5ea6b` and `cb5ac49`).

---

## Manual Smoke Checks

> **PRECONDITION:** Two dev servers running (operator must start them):
>
> Window 1 — Marketing: `cd /Users/ishiland/Code/getgeolens.com && npm run dev` (default port 4321)
> Window 2 — Docs: `cd /Users/ishiland/Code/getgeolens.com/docs && npm run dev` (Astro picks free port, usually 4322)
>
> Open browser to each printed URL. For Aside checks (M4–M7), navigate to a docs page that contains `:::note`, `:::tip`, `:::caution`, `:::danger` callouts. If no current page has all four, add a sandbox MDX page or use existing API/auth pages.

| # | Decision | Behavior | How To Verify | Observed | Pass/Fail | Notes |
|---|----------|----------|---------------|----------|-----------|-------|
| M1 | D-01 | Marketing renders IBM Plex Sans Variable | DevTools → Inspect `<body>` → Computed → `font-family` starts with `"IBM Plex Sans Variable"`; Network tab shows `wght.css` request | _pending operator_ | _pending_ | _record observed font-family value_ |
| M2 | D-01 | Docs renders IBM Plex Sans Variable | Same as M1 on docs site | _pending_ | _pending_ | |
| M3 | D-01 | Docs renders IBM Plex Mono in code blocks | Inspect any `<code>` element on docs page; `font-family` starts with `"IBM Plex Mono"` | _pending_ | _pending_ | |
| M4-light | D-04 | Docs `:::tip` brand blue (light mode) | Open page with tip callout in light mode; left border, icon, title text are brand blue (hue ~250) | _pending_ | _pending_ | |
| M4-dark | D-04 | Docs `:::tip` brand blue (dark mode) | Toggle dark mode; same brand-blue treatment persists | _pending_ | _pending_ | |
| M5-light | D-04 | Docs `:::note` brand blue (light mode) | Inspect note callout; brand blue (NOT Starlight default hsl 234) | _pending_ | _pending_ | |
| M5-dark | D-04 | Docs `:::note` brand blue (dark mode) | Toggle dark; brand blue persists | _pending_ | _pending_ | |
| M6 | D-04 (semantic preserve) | Docs `:::caution` STILL Starlight orange | Inspect caution callout; border = Starlight orange (hsl 41); MUST NOT have shifted | _pending_ | _pending_ | _critical: must remain orange_ |
| M7 | D-04 (semantic preserve) | Docs `:::danger` STILL Starlight red | Inspect danger callout; border = Starlight red (hsl 339); MUST NOT have shifted | _pending_ | _pending_ | _critical: must remain red_ |
| M8 | D-02 | Docs sidebar warm tint in LIGHT mode | Light mode + sidebar visible; background is `oklch(0.99 0.003 85)` — visibly warmer than pure white | _pending_ | _pending_ | |
| M9 | D-02 / RESEARCH Pitfall 5 | Docs sidebar STAYS DARK in dark mode | Toggle dark; sidebar remains Starlight `--sl-color-gray-6` dark; warm tint MUST NOT leak | _pending_ | _pending_ | _critical: regression check_ |
| M10 | D-05 | Docs nav shows GeoLens logo | Inspect docs nav header; SVG logomark renders alongside "GeoLens Docs" title (replacesTitle: false); alt text "GeoLens"; SVG inherits text color via `currentColor` | _pending_ | _pending_ | |
| M11 | D-06 | global.css comment uses rationale format | Eyeball `getgeolens.com/src/styles/global.css` lines 1–11 — pre-verified by file inspection (see below) | PASS | PASS | _content review confirmed during Task 1; reproduced below_ |

### M11 Pre-Verified Content (from `getgeolens.com/src/styles/global.css` lines 1–11)

```css
/*
 * GeoLens Marketing Site — Global Styles
 * Design tokens kept in sync with: geolens/frontend/src/index.css
 * Font: IBM Plex Sans Variable (self-hosted via @fontsource-variable/ibm-plex-sans)
 * Color space: OKLCH throughout
 * Dark mode: Intentional brand decision — marketing is light-only.
 *   Rationale: marketing surfaces prioritize first-impression clarity; dual-mode adds
 *   visual QA surface area (hero contrast, marketing imagery) disproportionate to benefit.
 *   Revisit if: a design owner is onboarded, or the product ships dark mode to >50% of
 *   signed-in users. Tracked as a separate backlog item (deferred from Phase 999.5).
 */
```

The "Intentional / Rationale / Revisit if" structure (D-06) is present and the prose reads as intended — not placeholder copy. **M11 PASS.**

---

## Decision Coverage Audit

For each locked decision D-01..D-11, the gate or check that provides observable evidence:

| Decision | Title | Evidence Source | Status |
|----------|-------|-----------------|--------|
| D-01 | Option B partial alignment (typography, logo, callout accents bridged) | G2 (build), G4 (build), G1 (drift CI font-family); M1, M2, M3 (visual font rendering) | Automated: PASS · Manual: pending |
| D-02 | Override `--sl-color-bg-sidebar` to warmer tint | G4 (build); M8 (light), M9 (dark — leak check) | Automated: PASS · Manual: pending |
| D-03 | Extend `check-token-sync.sh` with IBM Plex assertion | G1 (drift CI exit 0 with both assertions printed) | PASS |
| D-04 | Override Aside accent slots `note` + `tip` to brand blue, preserve `caution`/`danger` | G4 (build); M4, M5 (note/tip blue light+dark), M6, M7 (caution/danger preserved) | Automated: PASS · Manual: pending |
| D-05 | Logo extraction + Starlight `logo` config | G3 (astro check schema-valid), G7 (logo SVG byte-identity); M10 (visual logo render) | Automated: PASS · Manual: pending |
| D-06 | Formal rationale comment for marketing dark-mode deferral | G2 (build compiles); M11 (content review) | PASS (M11 pre-verified) |
| D-07 | New `getgeolens.com/DESIGN-GUIDE.md` at marketing repo root | G6 (file present, 310 lines) | PASS |
| D-08 | DESIGN-GUIDE.md covers four sections (Shared / Marketing / Docs / Drift CI) | G6 + 999.5-07-SUMMARY.md commit `86a4dab` confirms 4-section structure | PASS |
| D-09 | `geolens/docs/DESIGN-GUIDE.md` NOT modified | G5 (`git status --porcelain` empty; last commit on file is pre-999.5 `26e7c930`) | PASS |
| D-10 | Single bundled 999.5 phase delivery | G1–G7 + this artifact (one phase, eight plans, all gates in one record) | PASS |
| D-11 | Plan-here-execute-there cross-repo split | This SMOKE-RESULTS.md lives in `geolens/.planning/`; implementation commits live in `getgeolens.com/gsd/phase-225-api-reference` (commits `d00aeaf`, `cb5ac49`, `0e5ea6b`, `ae61247`, `6fc8ab1`, `fca068e`, `86a4dab`) | PASS |

**Coverage summary:** All eleven decisions D-01..D-11 have at least one supporting gate or check. Five decisions (D-03, D-07, D-08, D-09, D-10, D-11) and M11 are fully closed by automated/file-based evidence. Five decisions (D-01, D-02, D-04, D-05, D-06) require manual visual confirmation in addition to the automated builds — operator must complete M1–M10 before sign-off.

---

## Out-of-Scope Confirmation

Per CONTEXT.md, Option B intentionally diverges from Option A on five items. Each was confirmed not to have leaked into Phase 999.5's diffs:

| # | Out-of-Scope Item | Verification | Status |
|---|-------------------|--------------|--------|
| OOS-1 | Atlas-paper warm neutrals on marketing+docs | Marketing global.css `:root` uses cool-white `--background` token from Phase 212 (untouched by 999.5 commit `d00aeaf`); docs custom.css overrides only `--sl-color-bg-sidebar`, not body backgrounds | PASS — no warm neutrals leaked |
| OOS-2 | 4-tier `--surface-0..3` hierarchy | `--surface-0..3` exist in marketing global.css from Phase 212-01 (commit `2bb493f`); 999.5 commit `d00aeaf` did NOT add or modify them. Docs custom.css has zero `--surface-*` tokens (grep returned no matches). | PASS — no NEW surface tokens added by this phase |
| OOS-3 | Elevation shadow tokens | Grep `--elevation-` / `--shadow-elevation` against `getgeolens.com/`: no matches in custom.css; the only hit is in marketing global.css from Phase 212 (pre-existing context, untouched by 999.5) | PASS — no elevation tokens added by this phase |
| OOS-4 | Marketing dark mode | Grep `^\.dark\|prefers-color-scheme.*dark` in marketing global.css: no matches. Marketing remains light-only with the D-06 rationale comment. | PASS — light-only preserved |
| OOS-5 | `--code-*` token bridge to Expressive Code | Grep `--code-(bg\|fg\|comment\|keyword)` in docs custom.css: no matches. Expressive Code's theme remains untouched. | PASS — no code-token bridge |
| OOS-6 | App `geolens/docs/DESIGN-GUIDE.md` modified | G5 confirms zero git status changes; last touch is pre-999.5 commit `26e7c930` | PASS — north star intact |

---

## Sign-Off

**Operator initials/date:** _____________________
**Sign-off type:** ☐ approved (all 11 manual checks pass) ☐ partial (some failures, see notes) ☐ revise (specific change requested)

**Failure log (if any):**

_If any M-check fails, list (a) the M-id, (b) decision impacted, (c) screenshot path or evidence, (d) gap-closure plan recommendation per `/gsd-plan-phase 999.5 --gaps`._

_If all manual checks pass: phase 999.5 is ready for `/gsd-verify-work`._

---

## Continuation Notes

This artifact was generated by the executor for plan 999.5-08. The automated portion (Task 1, gates G1–G7) is complete and recorded above. The manual portion (Task 2, checks M1–M10; M11 is pre-verified) requires the human operator to:

1. Start `npm run dev` in marketing and docs.
2. Walk the M1–M10 checklist using the "How To Verify" column.
3. Replace the `_pending_` cells with observed values + PASS/FAIL.
4. Sign off above.

Once sign-off is recorded, the executor will create `999.5-08-SUMMARY.md`, update STATE.md / ROADMAP.md, and the phase is ready for `/gsd-verify-work`.
