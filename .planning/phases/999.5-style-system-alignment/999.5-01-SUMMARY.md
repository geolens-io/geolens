---
phase: 999.5-style-system-alignment
plan: 01
subsystem: infra
tags: [design-system, fonts, dependencies, cross-repo, ibm-plex, npm]

# Dependency graph
requires:
  - phase: 224-brand-shell-search
    provides: Inter font baseline that this phase replaces with IBM Plex
provides:
  - "@fontsource-variable/ibm-plex-sans installed in marketing root"
  - "@fontsource-variable/ibm-plex-sans installed in docs subtree"
  - "@fontsource/ibm-plex-mono installed in both"
  - "Inter font packages removed from both manifests"
affects: [999.5-02, 999.5-03, 999.5-04, 999.5-05, 999.5-06, 999.5-07, 999.5-08]

# Tech tracking
tech-stack:
  added: ["@fontsource-variable/ibm-plex-sans@^5.2.8", "@fontsource/ibm-plex-mono@^5.2.7"]
  patterns: ["Cross-repo dependency swap with audit-before-remove"]

key-files:
  created: []
  modified:
    - /Users/ishiland/Code/getgeolens.com/package.json
    - /Users/ishiland/Code/getgeolens.com/package-lock.json
    - /Users/ishiland/Code/getgeolens.com/docs/package.json
    - /Users/ishiland/Code/getgeolens.com/docs/package-lock.json

key-decisions:
  - "Case A — @fontsource/inter devDep is unused; src/lib/og.ts loads inter-700.ttf locally from src/assets/fonts/, not from node_modules"
  - "OG image script font swap (Pitfall 6) deferred — out of scope for Plan 01 (package-only); local TTF asset is unaffected"

patterns-established:
  - "Audit-before-remove: grep scripts/ for fontsource imports before pruning devDeps"
  - "Cross-repo execution boundary (D-11): planning artifacts in geolens, implementation commits in getgeolens.com"

requirements-completed: []

# Metrics
duration: 6min
completed: 2026-04-26
---

# Phase 999.5 Plan 01: IBM Plex Font Package Installation Summary

**Swapped Inter for IBM Plex Sans Variable + IBM Plex Mono in both `getgeolens.com` and `getgeolens.com/docs` package manifests, establishing the dependency foundation for the Phase 999.5 typography alignment.**

## Performance

- **Duration:** ~6 min
- **Started:** 2026-04-26T14:02:00Z
- **Completed:** 2026-04-26T14:08:42Z
- **Tasks:** 3
- **Files modified:** 4 (package.json + package-lock.json in 2 directories)

## Accomplishments

- Marketing root `package.json` now declares `@fontsource-variable/ibm-plex-sans@^5.2.8` and `@fontsource/ibm-plex-mono@^5.2.7`
- Docs subtree `package.json` declares the same two packages
- Both `@fontsource-variable/inter` (dependency) and `@fontsource/inter` (devDependency, marketing only) removed
- `npm install` ran clean in both directories; CSS files Plan 03 and 04 will import (`wght.css`, `400.css`) are present in both `node_modules` trees
- Audit confirmed `@fontsource/inter` was safe to remove (the local `src/assets/fonts/inter-700.ttf` used by `src/lib/og.ts` is a separate static asset, not a node module import)

## Task Commits

Tasks were committed atomically in the **`getgeolens.com`** repo (per D-11). The geolens repo receives only the SUMMARY.md.

1. **Task 1: Audit getgeolens.com scripts for @fontsource/inter usage** — read-only, no commit (audit findings inform Task 2)
2. **Task 2: Update marketing root package.json + npm install** — `1dadc66` (chore in getgeolens.com repo)
3. **Task 3: Update docs subtree package.json + npm install** — `9a02a76` (chore in getgeolens.com repo)

## Files Created/Modified

**In `getgeolens.com` repo (commits `1dadc66`, `9a02a76`):**
- `package.json` — swapped `@fontsource-variable/inter` -> `@fontsource-variable/ibm-plex-sans`; added `@fontsource/ibm-plex-mono`; removed `@fontsource/inter` devDep
- `package-lock.json` — regenerated by `npm install`
- `docs/package.json` — swapped `@fontsource-variable/inter` -> IBM Plex Sans + Mono
- `docs/package-lock.json` — regenerated by `npm install`

**In `geolens` repo (this commit):**
- `.planning/phases/999.5-style-system-alignment/999.5-01-SUMMARY.md` — this file

## Audit Findings (Task 1)

**Conclusion: Case A — `@fontsource/inter` package is unused in scripts/ and elsewhere; safe to remove.**

Detailed findings:
- `grep -rn "fontsource" /Users/ishiland/Code/getgeolens.com/scripts/` → no matches
- `grep -rn "@fontsource/inter" /Users/ishiland/Code/getgeolens.com/` → only matches in `package.json`, `package-lock.json`, and `docs/` derivatives (no source code imports)
- `src/lib/og.ts` (satori OG image generator) loads font data via `fs.readFileSync('src/assets/fonts/inter-700.ttf')` — a local TTF file in the repo, NOT from `@fontsource/inter` node module
- The local `inter-700.ttf` (420 KB) remains in place; replacing it is out of scope for Plan 01 (package manifest only) and is a candidate for a follow-up task

This means RESEARCH.md Pitfall 2 was confirmed to be a non-issue for the package removal — but RESEARCH.md Pitfall 6 (satori variable-font limitation) is still latently true: if a future plan replaces the local `inter-700.ttf` asset with an IBM Plex equivalent, it must use a non-variable static-weight TTF.

## Decisions Made

- **Audit interpretation:** Followed Case A path (no script edits) because the `@fontsource/inter` node module is not imported. The local TTF asset (`src/assets/fonts/inter-700.ttf`) is a separate concern outside this plan's scope (`files_modified` lists only the four manifest/lockfile paths).
- **Commit split:** Used two commits (one per repo subdirectory) instead of bundling. Made the changeset trail clearer for the marketing root vs docs subtree change history.

## Deviations from Plan

None — plan executed exactly as written. Audit found Case A; package edits applied per the Case A branch in Task 2; no script edits required; Task 3 followed unchanged.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required. Both `npm install` runs completed cleanly.

## Next Phase Readiness

- **Plan 02 (global.css update) ready:** marketing `node_modules/@fontsource-variable/ibm-plex-sans/wght.css` exists for the new `@import` line.
- **Plan 03/04 (custom.css update) ready:** docs `node_modules/@fontsource-variable/ibm-plex-sans/wght.css` and `node_modules/@fontsource/ibm-plex-mono/400.css` both exist for the new `@import` lines.
- **No blockers** for downstream plans.
- **Latent concern (deferred):** `src/lib/og.ts` still loads the local `inter-700.ttf` — OG image generation will continue to render in Inter until a follow-up task replaces the local TTF with an IBM Plex equivalent (non-variable, per Pitfall 6). Not in this phase's scope, not on critical path for the typography alignment in CSS surfaces.

## Self-Check: PASSED

Verification commands run after SUMMARY.md drafted:

- `[ -f /Users/ishiland/Code/getgeolens.com/package.json ]` → FOUND
- `[ -f /Users/ishiland/Code/getgeolens.com/docs/package.json ]` → FOUND
- `[ -d /Users/ishiland/Code/getgeolens.com/node_modules/@fontsource-variable/ibm-plex-sans ]` → FOUND
- `[ -d /Users/ishiland/Code/getgeolens.com/node_modules/@fontsource/ibm-plex-mono ]` → FOUND
- `[ -d /Users/ishiland/Code/getgeolens.com/docs/node_modules/@fontsource-variable/ibm-plex-sans ]` → FOUND
- `[ -d /Users/ishiland/Code/getgeolens.com/docs/node_modules/@fontsource/ibm-plex-mono ]` → FOUND
- Commit `1dadc66` in getgeolens.com → FOUND
- Commit `9a02a76` in getgeolens.com → FOUND

---
*Phase: 999.5-style-system-alignment*
*Completed: 2026-04-26*
