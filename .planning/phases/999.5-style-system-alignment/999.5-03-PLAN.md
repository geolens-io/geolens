---
phase: 999.5-style-system-alignment
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - /Users/ishiland/Code/getgeolens.com/src/styles/global.css
autonomous: true
requirements: []
tags: [design-system, fonts, marketing, css, dark-mode-rationale]

must_haves:
  truths:
    - "Marketing global.css imports IBM Plex Sans Variable instead of Inter (D-01)"
    - "Marketing --font-sans CSS variable resolves to 'IBM Plex Sans Variable' (D-01)"
    - "Marketing html fallback font-family references IBM Plex Sans Variable (D-01, RESEARCH.md Pitfall 1)"
    - "global.css header comment rewritten to 'Intentional / Rationale / Revisit if' format (D-06)"
    - "No 'Inter Variable' or '@fontsource-variable/inter' references remain in global.css (D-01)"
    - "Implementation lands in getgeolens.com repo per D-11"
  artifacts:
    - path: "/Users/ishiland/Code/getgeolens.com/src/styles/global.css"
      provides: "Marketing site IBM Plex font + rewritten dark-mode rationale comment"
      contains: "IBM Plex Sans Variable"
  key_links:
    - from: "global.css @import"
      to: "node_modules/@fontsource-variable/ibm-plex-sans/wght.css"
      via: "Astro CSS bundling"
      pattern: "@import '@fontsource-variable/ibm-plex-sans/wght.css'"
---

<objective>
Update `/Users/ishiland/Code/getgeolens.com/src/styles/global.css` to swap Inter Variable for IBM Plex Sans Variable in three locations (lines 10, 39, 117 per RESEARCH.md File-by-File Change Map) AND rewrite the header comment block (lines 1–7) from the v14.0-era "Dark mode: NOT supported" phrasing to the explicit "Intentional / Rationale / Revisit if" rationale format per D-06.

Purpose: D-01 (Option B partial typography alignment) for the marketing surface; D-06 (formal dark-mode deferral with explicit rationale).

Output: Updated `global.css` with IBM Plex Sans Variable threaded through the import, the `@theme --font-sans` token, and the html fallback font-family — plus a header comment that documents marketing's light-only intent in the format the rest of the design system uses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ishiland/Code/geolens/.planning/PROJECT.md
@/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-CONTEXT.md
@/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-RESEARCH.md
@/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-01-PLAN.md

<interfaces>
<!-- Verbatim before/after blocks from RESEARCH.md "File 1: getgeolens.com/src/styles/global.css" -->

CHANGE 1 — Header comment block (lines 1–7), replace from:
```css
/*
 * GeoLens Marketing Site — Global Styles
 * Design tokens kept in sync with: geolens/frontend/src/index.css
 * Font: Inter Variable (self-hosted via @fontsource-variable/inter)
 * Color space: OKLCH throughout
 * Dark mode: NOT supported (light only for v14.0 launch)
 */
```
To:
```css
/*
 * GeoLens Marketing Site — Global Styles
 * Design tokens kept in sync with: geolens/frontend/src/index.css
 * Font: IBM Plex Sans Variable (self-hosted via @fontsource-variable/ibm-plex-sans)
 * Color space: OKLCH throughout
 * Dark mode: Intentional brand decision — marketing is light-only.
 *   Rationale: marketing surfaces prioritize first-impression clarity; dual-mode adds
 *   visual QA surface area (hero contrast, marketing imagery) disproportionate to benefit.
 *   Revisit if: a design owner is onboarded, or the product ships dark mode to >50% of
 *   signed-in users. Tracked as a separate backlog item (deferred from Phase 999.5).
 */
```

CHANGE 2 — Font import (line 10), replace:
```css
@import '@fontsource-variable/inter/wght.css';
```
With:
```css
@import '@fontsource-variable/ibm-plex-sans/wght.css';
```

CHANGE 3 — `@theme` font-sans token (line 39), replace:
```css
  --font-sans: 'Inter Variable', ui-sans-serif, system-ui, sans-serif;
```
With:
```css
  --font-sans: 'IBM Plex Sans Variable', ui-sans-serif, system-ui, sans-serif;
```

CHANGE 4 — `html` fallback font (line 117), replace:
```css
  font-family: var(--font-sans, 'Inter Variable', ui-sans-serif, system-ui, sans-serif);
```
With:
```css
  font-family: var(--font-sans, 'IBM Plex Sans Variable', ui-sans-serif, system-ui, sans-serif);
```

NO OTHER CHANGES. Marketing has no `--font-mono` token (no monospace usage) — do NOT add one. Per RESEARCH.md.

Line numbers above are from RESEARCH.md as of 2026-04-26; if the file has drifted, locate by string match (the exact strings above) rather than absolute line numbers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap Inter -> IBM Plex Sans in global.css and rewrite dark-mode rationale comment</name>
  <files>/Users/ishiland/Code/getgeolens.com/src/styles/global.css</files>
  <read_first>
    - /Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-RESEARCH.md (File 1: getgeolens.com/src/styles/global.css — all four change blocks)
    - /Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-CONTEXT.md (D-01, D-06)
    - /Users/ishiland/Code/getgeolens.com/src/styles/global.css (READ FULL CURRENT CONTENT before editing — confirm line numbers from RESEARCH.md still match, or locate by exact string)
  </read_first>
  <action>
    Working directory: `/Users/ishiland/Code/getgeolens.com` (the marketing repo). Per D-11, this task targets the `getgeolens.com` repo.

    Apply ALL FOUR changes to `/Users/ishiland/Code/getgeolens.com/src/styles/global.css` (use the Edit tool with the exact before/after strings from the `<interfaces>` block above):

    1. **Header comment rewrite (D-06):** Replace the 7-line header comment block. The new comment must follow the "Intentional / Rationale / Revisit if" format. Use the verbatim replacement text from the `<interfaces>` block.

    2. **Font import swap (D-01):** Replace the single `@import '@fontsource-variable/inter/wght.css';` line with `@import '@fontsource-variable/ibm-plex-sans/wght.css';`.

    3. **`@theme` --font-sans swap (D-01):** Replace the `'Inter Variable'` font-family in the `--font-sans` declaration with `'IBM Plex Sans Variable'`. The fallback chain (`ui-sans-serif, system-ui, sans-serif`) stays the same.

    4. **`html` fallback font swap (D-01, addresses RESEARCH.md Pitfall 1):** Replace the `'Inter Variable'` in the `var(--font-sans, ...)` fallback chain with `'IBM Plex Sans Variable'`. This is the fallback that fires if `--font-sans` is unset; leaving it as Inter would request a font that no longer exists in node_modules.

    DO NOT add a `--font-mono` token (marketing has no monospace usage; RESEARCH.md File 1 explicitly says do not add one).

    DO NOT change any other lines (OKLCH primary palette, Tailwind imports, surface/elevation tokens — all stay unchanged per Option B Out-of-Scope items).

    Commit in the `getgeolens.com` repo with a conventional-commit message such as `style(brand): swap Inter -> IBM Plex Sans in marketing global.css; rewrite dark-mode rationale comment (Phase 999.5 D-01, D-06)`. Per D-11, no commit in the geolens repo from this task.

    Optional sanity check after edit (per RESEARCH.md Validation Architecture sampling — does not need to be in CI):
    ```bash
    cd /Users/ishiland/Code/getgeolens.com && npm run build 2>&1 | tail -20
    ```
    A green build proves the @import resolves against the new IBM Plex package installed by Plan 01.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com && grep -q "@fontsource-variable/ibm-plex-sans/wght.css" src/styles/global.css && grep -q "'IBM Plex Sans Variable'" src/styles/global.css && ! grep -q "Inter Variable\|@fontsource-variable/inter\|@fontsource/inter" src/styles/global.css && grep -q "Intentional brand decision" src/styles/global.css && grep -q "Rationale:" src/styles/global.css && grep -q "Revisit if:" src/styles/global.css && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "@fontsource-variable/ibm-plex-sans/wght.css" /Users/ishiland/Code/getgeolens.com/src/styles/global.css` exits 0 (the IBM Plex import is present)
    - `grep -c "'IBM Plex Sans Variable'" /Users/ishiland/Code/getgeolens.com/src/styles/global.css | grep -v '^#'` returns 2 or more (one in `--font-sans` token, one in `html` fallback; comment block also references it)
    - `! grep -q "Inter Variable" /Users/ishiland/Code/getgeolens.com/src/styles/global.css` (no Inter font-family remnants)
    - `! grep -q "@fontsource-variable/inter\|@fontsource/inter" /Users/ishiland/Code/getgeolens.com/src/styles/global.css` (no Inter package imports)
    - `! grep -q "Dark mode: NOT supported" /Users/ishiland/Code/getgeolens.com/src/styles/global.css` (the v14.0-era phrasing is gone)
    - `grep -q "Intentional brand decision" /Users/ishiland/Code/getgeolens.com/src/styles/global.css` exits 0 (D-06 rationale format)
    - `grep -q "Rationale:" /Users/ishiland/Code/getgeolens.com/src/styles/global.css` exits 0
    - `grep -q "Revisit if:" /Users/ishiland/Code/getgeolens.com/src/styles/global.css` exits 0
    - `cd /Users/ishiland/Code/getgeolens.com && npm run build` exits 0 (Astro build green; proves IBM Plex import resolves)
    - No changes to OKLCH primary palette tokens (`--primary-50` through `--primary-900` lines unchanged); verify with `git diff src/styles/global.css` — only header, line 10, line 39, line 117 should be modified
  </acceptance_criteria>
  <done>
    `global.css` now imports IBM Plex Sans Variable, declares it as `--font-sans`, and uses it in the `html` fallback. The header comment uses the D-06 rationale format. Marketing builds green against the new font package. No other tokens were touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. CSS-only change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.5-03 | (n/a) | global.css | accept | No new threat surface — CSS tokens, font-family declarations, and a markdown-style comment block. No authentication, input validation, network-exposed endpoints, or data flow changes. ASVS L1 not applicable. |
</threat_model>

<verification>
```bash
cd /Users/ishiland/Code/getgeolens.com

# Token + import sync
grep -n "ibm-plex-sans\|IBM Plex" src/styles/global.css
grep -n "Inter" src/styles/global.css || echo "OK: no Inter references"

# Header rationale present
head -15 src/styles/global.css

# Build green
npm run build 2>&1 | tail -5
```

If the build fails with `Cannot find module '@fontsource-variable/ibm-plex-sans'`, Plan 01 (npm install) did not complete — block on Plan 01 before retrying.
</verification>

<success_criteria>
- All four edits applied (header comment + 3 font references).
- No Inter references remain in `global.css`.
- Header comment uses the "Intentional / Rationale / Revisit if" format per D-06.
- `npm run build` succeeds in `/Users/ishiland/Code/getgeolens.com`.
- A commit in `getgeolens.com` records the change. No commit in geolens repo from this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/999.5-style-system-alignment/999.5-03-SUMMARY.md` documenting:
- The four edit locations (line numbers in the post-edit file)
- The before/after of the header comment block
- `npm run build` exit status
- Commit hash in `getgeolens.com` repo
</output>
