---
phase: 999.5-style-system-alignment
plan: 08
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified: []
autonomous: false
requirements: []
tags: [verification, visual-smoke, build-gates, design-system, cross-repo]

must_haves:
  truths:
    - "Marketing build (npm run build) exits 0 with IBM Plex Sans Variable applied (D-01)"
    - "Docs build (npm run build) exits 0 with IBM Plex Sans + Mono, brand-blue Asides, sidebar tint, and Starlight logo wired (D-01, D-02, D-04, D-05)"
    - "check-token-sync.sh exits 0 with both primary palette and IBM Plex font assertions green (D-03, Phase 224 BRAND-04 invariant)"
    - "Browser smoke check confirms marketing renders IBM Plex Sans Variable (D-01)"
    - "Browser smoke check confirms docs renders IBM Plex (sans + mono) and Aside note/tip render brand blue (D-01, D-04)"
    - "Browser smoke check confirms docs Aside caution still renders Starlight orange and danger still renders Starlight red (D-04 semantic preservation)"
    - "Browser smoke check confirms docs sidebar shows warmer tint in light mode and stays at Starlight default in dark mode (D-02, RESEARCH.md Pitfall 5)"
    - "Browser smoke check confirms docs nav shows the canonical GeoLens logo (D-05)"
    - "global.css header comment uses the 'Intentional / Rationale / Revisit if' rationale format (D-06)"
    - "All eleven decisions D-01 through D-11 have observable evidence (D-10: single bundled phase verification)"
  artifacts:
    - path: "/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-08-SMOKE-RESULTS.md"
      provides: "Phase-level visual smoke checklist results — supports /gsd-verify-work decision"
      min_lines: 30
  key_links:
    - from: "smoke checklist (this plan)"
      to: "decisions D-01..D-11"
      via: "explicit decision-id references in each verification line"
      pattern: "D-0[1-9]|D-1[01]"
---

<objective>
Phase-level verification gate: run all automated build + drift-CI commands across both repos, then drive a manual visual smoke checklist against `npm run dev` for both surfaces. Cross-reference every observation back to a locked decision (D-01..D-11) so `/gsd-verify-work` has a single artifact to consult. This plan IS the closing checkpoint for D-10's "single bundled phase" delivery model.

Purpose: D-10 — confirm the bundled phase shipped end-to-end across both repos. D-04 / D-02 / D-06 / D-05 all require visual confirmation that automated tests cannot fully cover; this is the only place those manual gates run.

Output: A SMOKE-RESULTS markdown artifact in the planning directory recording each automated and manual check, the decision it covers, and pass/fail status. The plan is non-autonomous because the manual checklist requires a human (the operator).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ishiland/Code/geolens/.planning/PROJECT.md
@/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-CONTEXT.md
@/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-VALIDATION.md
@/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-RESEARCH.md
@/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-01-PLAN.md
@/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-02-PLAN.md
@/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-03-PLAN.md
@/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-04-PLAN.md
@/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-05-PLAN.md
@/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-06-PLAN.md
@/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-07-PLAN.md

<interfaces>
<!-- Manual visual checklist derived from RESEARCH.md "Validation Architecture" + VALIDATION.md "Manual-Only Verifications" -->
<!-- Each item maps to one or more decision IDs (D-01..D-11) -->

The manual checklist is structured as: PRECONDITION (server up), then OBSERVE (specific browser actions), then RECORD (pass/fail with screenshot path optional).

Per VALIDATION.md, these are the manual-only verifications:

| # | Decision | Behavior | Test instructions |
|---|----------|----------|-------------------|
| M1 | D-01 | Marketing renders IBM Plex Sans Variable | `cd /Users/ishiland/Code/getgeolens.com && npm run dev`, open root URL, DevTools → Computed → font-family on `<body>` shows `"IBM Plex Sans Variable"`; Network tab shows `wght.css` loaded |
| M2 | D-01 | Docs renders IBM Plex Sans Variable | `cd /Users/ishiland/Code/getgeolens.com/docs && npm run dev`, open any docs page, DevTools as above |
| M3 | D-01 | Docs renders IBM Plex Mono in code blocks | DevTools → inspect any `<code>` element → font-family shows `"IBM Plex Mono"` |
| M4 | D-04 | Docs Aside `:::tip` renders brand blue (NOT purple) | Open any docs page with a `:::tip` callout; border + icon + title text are brand blue (hue 250); confirm both light and dark mode |
| M5 | D-04 | Docs Aside `:::note` renders brand blue | Inspect page with `:::note`; same brand blue color treatment; confirm light + dark |
| M6 | D-04 (semantic preservation) | Docs Aside `:::caution` STILL renders Starlight orange | Inspect page with `:::caution`; border remains Starlight orange (hsl 41) — must NOT have shifted |
| M7 | D-04 (semantic preservation) | Docs Aside `:::danger` STILL renders Starlight red | Inspect page with `:::danger`; border remains Starlight red (hsl 339) — must NOT have shifted |
| M8 | D-02 | Docs sidebar shows warm tint in LIGHT mode | Light mode + scroll position visible — sidebar background is subtly warmer than pure white |
| M9 | D-02 (RESEARCH.md Pitfall 5) | Docs sidebar STAYS DARK in dark mode | Toggle dark mode — sidebar must NOT show the warm tint; must remain Starlight `--sl-color-gray-6` dark |
| M10 | D-05 | Docs nav shows the GeoLens logo (alongside "GeoLens Docs" title) | Inspect docs site nav header — canonical SVG renders with `currentColor` taking the nav text color; alt text "GeoLens" |
| M11 | D-06 | global.css comment uses rationale format | `head -15 /Users/ishiland/Code/getgeolens.com/src/styles/global.css` shows "Intentional brand decision... Rationale: ... Revisit if: ..." (this is automatable but listed here for completeness — the rationale is a content review) |

Required content of the SMOKE-RESULTS artifact (Plan 08 deliverable):
- Section "Automated Gates" with rows for each automated command (build, build, drift CI, astro check) and its exit code
- Section "Manual Smoke Checks" with rows M1–M11 (decision ID, behavior, observed result, pass/fail)
- Section "Decision Coverage Audit" — for each of D-01 through D-11, list which automated gate or manual check provided the evidence
- Section "Out-of-Scope Confirmation" — confirm Option B intentional divergences are still in place: marketing has no dark mode token block, neither surface has `--surface-0..3`, no elevation tokens, no `--code-*` bridge, geolens/docs/DESIGN-GUIDE.md is unchanged
- Section "Sign-Off" with operator initials/date or notes if any check failed (and the corresponding gap-closure plan should be created)

Per D-11, the SMOKE-RESULTS file lives in the geolens planning directory (planning artifact), but all observations are about the getgeolens.com surfaces.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all automated gates (builds + drift CI + astro check) and record results</name>
  <files>/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-08-SMOKE-RESULTS.md</files>
  <read_first>
    - /Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-VALIDATION.md (full Phase Requirements → Test Map)
    - /Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-CONTEXT.md (D-01..D-11)
  </read_first>
  <action>
    Working directory: This task READS from `/Users/ishiland/Code/getgeolens.com` (running automated commands there) and WRITES to the geolens planning directory.

    Run each automated gate in order and capture the exit code + last 5 lines of stdout/stderr. If any gate fails, STOP and surface the failure to the operator before continuing — these are blocking gates for D-10.

    1. **Marketing root drift CI sanity:**
       ```bash
       cd /Users/ishiland/Code/getgeolens.com
       bash docs/scripts/check-token-sync.sh; echo "exit=$?"
       ```
       Expected: exit 0, output includes both "All 10 --primary-* stops in sync..." (Phase 224 invariant) and "IBM Plex Sans Variable font-family declaration found in all available surfaces." (Phase 999.5 D-03).

    2. **Marketing root build:**
       ```bash
       cd /Users/ishiland/Code/getgeolens.com
       npm run build 2>&1 | tail -10; echo "exit=${PIPESTATUS[0]}"
       ```
       Expected: exit 0.

    3. **Docs subtree astro check:**
       ```bash
       cd /Users/ishiland/Code/getgeolens.com/docs
       npx astro check 2>&1 | tail -10; echo "exit=${PIPESTATUS[0]}"
       ```
       Expected: exit 0.

    4. **Docs subtree build:**
       ```bash
       cd /Users/ishiland/Code/getgeolens.com/docs
       npm run build 2>&1 | tail -10; echo "exit=${PIPESTATUS[0]}"
       ```
       Expected: exit 0.

    5. **D-09 invariant — geolens/docs/DESIGN-GUIDE.md unchanged:**
       ```bash
       cd /Users/ishiland/Code/geolens
       git status --porcelain docs/DESIGN-GUIDE.md
       echo "lines_changed=$(git status --porcelain docs/DESIGN-GUIDE.md | wc -l)"
       ```
       Expected: empty output (no modifications).

    6. **D-07 invariant — new DESIGN-GUIDE.md present:**
       ```bash
       test -f /Users/ishiland/Code/getgeolens.com/DESIGN-GUIDE.md && echo "present=yes" || echo "present=no"
       wc -l /Users/ishiland/Code/getgeolens.com/DESIGN-GUIDE.md
       ```
       Expected: present=yes, line count ≥ 80.

    7. **D-05 invariant — both logo SVGs present and identical:**
       ```bash
       test -f /Users/ishiland/Code/getgeolens.com/public/logo.svg
       test -f /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg
       cmp /Users/ishiland/Code/getgeolens.com/public/logo.svg /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg && echo "identical=yes"
       ```
       Expected: both files present, byte-identical.

    Initialize `/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-08-SMOKE-RESULTS.md` with the "Automated Gates" section filled in (each gate name, command, exit code, pass/fail, and decision IDs covered).

    DO NOT proceed to Task 2 if any automated gate fails. Surface the failure to the operator and recommend opening a gap-closure plan.
  </action>
  <verify>
    <automated>test -f /Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-08-SMOKE-RESULTS.md && grep -q "Automated Gates" /Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-08-SMOKE-RESULTS.md && cd /Users/ishiland/Code/getgeolens.com && bash docs/scripts/check-token-sync.sh > /tmp/drift-08.log 2>&1 && cd docs && npm run build > /tmp/docs-build-08.log 2>&1 && cd /Users/ishiland/Code/getgeolens.com && npm run build > /tmp/mkt-build-08.log 2>&1 && cd /Users/ishiland/Code/geolens && test -z "$(git status --porcelain docs/DESIGN-GUIDE.md)" && test -f /Users/ishiland/Code/getgeolens.com/DESIGN-GUIDE.md && test -f /Users/ishiland/Code/getgeolens.com/public/logo.svg && test -f /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg && cmp /Users/ishiland/Code/getgeolens.com/public/logo.svg /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - SMOKE-RESULTS.md file exists at `/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-08-SMOKE-RESULTS.md`
    - File contains "## Automated Gates" section
    - `bash /Users/ishiland/Code/getgeolens.com/docs/scripts/check-token-sync.sh` exits 0 (Phase 224 BRAND-04 + Phase 999.5 D-03 both green)
    - `cd /Users/ishiland/Code/getgeolens.com && npm run build` exits 0 (D-01, D-06 marketing surface)
    - `cd /Users/ishiland/Code/getgeolens.com/docs && npx astro check` exits 0 (D-05 schema-valid logo wiring)
    - `cd /Users/ishiland/Code/getgeolens.com/docs && npm run build` exits 0 (D-01, D-02, D-04, D-05 docs surface)
    - `git status --porcelain /Users/ishiland/Code/geolens/docs/DESIGN-GUIDE.md` returns empty (D-09 invariant)
    - `test -f /Users/ishiland/Code/getgeolens.com/DESIGN-GUIDE.md` exits 0 (D-07)
    - `cmp /Users/ishiland/Code/getgeolens.com/public/logo.svg /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg` exits 0 (D-05 byte-identity)
    - SMOKE-RESULTS.md "Automated Gates" section records all 7 gate results with their exit codes
  </acceptance_criteria>
  <done>
    All automated gates pass; SMOKE-RESULTS.md has the Automated Gates section populated. The phase is ready for the manual smoke checklist (Task 2).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual visual smoke checklist (M1–M11) against npm run dev</name>
  <what-built>
    Phase 999.5 has shipped: IBM Plex Sans Variable + IBM Plex Mono on marketing and docs surfaces, brand-blue Aside `note` and `tip` overrides (with caution/danger preserved as Starlight semantic colors), warmer light-mode docs sidebar tint, canonical GeoLens logo wired into the Starlight nav, rewritten dark-mode rationale comment in marketing global.css, drift CI extended with IBM Plex font-family assertion, and a new cross-surface DESIGN-GUIDE.md at the marketing repo root. All automated gates passed in Task 1.

    This checkpoint runs the manual visual smoke checklist (M1–M11) that automated checks cannot cover — confirming the visible result on screen matches the locked decisions.
  </what-built>
  <how-to-verify>
    Open two terminal windows, one for marketing and one for docs:

    **Window 1 — Marketing (http://localhost:4321 by default):**
    ```bash
    cd /Users/ishiland/Code/getgeolens.com
    npm run dev
    ```
    Open browser to the printed URL.

    **Window 2 — Docs (http://localhost:4322 or similar — Astro will pick a free port):**
    ```bash
    cd /Users/ishiland/Code/getgeolens.com/docs
    npm run dev
    ```
    Open a second browser tab to the printed URL. Navigate to any guide page that contains `:::note`, `:::tip`, `:::caution`, `:::danger` callouts. If no such page exists, temporarily add a sandbox MDX page with one of each callout, or use existing API/auth pages that already use them.

    **Run the checklist:**

    1. **M1 (D-01) — Marketing IBM Plex Sans:** DevTools → Inspect `<body>` → Computed pane → font-family. Expected: starts with `"IBM Plex Sans Variable"`. Network tab shows `wght.css` request. **PASS / FAIL** (record observed font-family value)

    2. **M2 (D-01) — Docs IBM Plex Sans:** Same procedure on docs site. Expected: `"IBM Plex Sans Variable"`. **PASS / FAIL**

    3. **M3 (D-01) — Docs IBM Plex Mono:** Inspect any `<code>` element on a docs page. Expected: font-family starts with `"IBM Plex Mono"`. **PASS / FAIL**

    4. **M4 (D-04) — Docs Aside `:::tip` is brand blue:** Open page with a tip callout. Expected: left border, icon, and title text are brand blue (hue ~250). Repeat in dark mode (toggle theme). **PASS / FAIL** for each mode

    5. **M5 (D-04) — Docs Aside `:::note` is brand blue:** Same procedure with note. Expected: brand blue (NOT Starlight default hsl 234 blue — the bridged blue is OKLCH hue 250). Repeat in dark mode. **PASS / FAIL** for each mode

    6. **M6 (D-04 semantic preservation) — Docs Aside `:::caution` STILL Starlight orange:** Open page with caution callout. Expected: orange border (hsl 41). MUST NOT have shifted to brand blue. **PASS / FAIL**

    7. **M7 (D-04 semantic preservation) — Docs Aside `:::danger` STILL Starlight red:** Open page with danger callout. Expected: red border (hsl 339). MUST NOT have shifted to brand blue. **PASS / FAIL**

    8. **M8 (D-02) — Docs sidebar warm tint in LIGHT mode:** Light mode + scroll position visible. Expected: sidebar background is `oklch(0.99 0.003 85)` — visibly warmer than pure white but only slightly. **PASS / FAIL**

    9. **M9 (D-02 / RESEARCH.md Pitfall 5) — Docs sidebar STAYS DARK in dark mode:** Toggle theme to dark. Expected: sidebar background is dark (Starlight `--sl-color-gray-6`) — must NOT show the warm tint. If dark sidebar appears washed-out warm white, the override leaked into the dark block. **PASS / FAIL**

    10. **M10 (D-05) — Docs nav shows GeoLens logo:** Inspect the docs site nav header. Expected: SVG logomark renders alongside the "GeoLens Docs" title text (since `replacesTitle: false`); alt text "GeoLens"; the SVG inherits the nav text color via `currentColor`. **PASS / FAIL**

    11. **M11 (D-06) — global.css comment uses rationale format:** Already verified by grep in Task 1 acceptance criteria, but eyeball the actual prose in `/Users/ishiland/Code/getgeolens.com/src/styles/global.css` lines 1–12 to confirm the "Intentional / Rationale / Revisit if" wording reads as intended (not a placeholder). **PASS / FAIL**

    **After running the checklist:** append a "## Manual Smoke Checks" section to `999.5-08-SMOKE-RESULTS.md` with one row per item (M1..M11), the decision ID, observed result, pass/fail, and any notes/screenshots.

    Then add the "## Decision Coverage Audit" and "## Out-of-Scope Confirmation" sections — for each D-01..D-11, name which gate or check covered it; for the out-of-scope items, run quick greps to confirm marketing has no `--surface-0`, neither surface has elevation tokens added, and no `--code-*` bridge leaked into custom.css.

    Finally add "## Sign-Off" with the operator's initials + date.

    **If any check FAILS:**
    - Record the failure with screenshot path
    - Note the decision ID(s) it impacts
    - Recommend opening a gap-closure plan via `/gsd-plan-phase 999.5 --gaps`
    - Do NOT mark the phase verified

    **If all checks PASS:**
    - Sign off in SMOKE-RESULTS.md
    - Phase 999.5 is ready for `/gsd-verify-work` and milestone completion
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved — all 11 manual checks pass, sign-off recorded` (proceed to /gsd-verify-work)
    - `partial — checks {M#}, {M#} failed, see SMOKE-RESULTS for detail` (planner should open gap-closure)
    - `revise — {specific deviation, e.g., "M10: replacesTitle: false makes logo look cramped, switch to true"}` (open follow-up edit)
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Verification-only plan; reads files and observes a local dev server.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.5-08 | (n/a) | Phase verification gate | accept | No new threat surface — running build/test commands and observing browser output. The dev server is local-only (default Astro bind 127.0.0.1) and does not expose new endpoints to the network. ASVS L1 not applicable. |
</threat_model>

<verification>
The SMOKE-RESULTS.md artifact IS the verification record. Run:

```bash
test -f /Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-08-SMOKE-RESULTS.md
grep -c "^## " /Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-08-SMOKE-RESULTS.md  # expect ≥ 4 sections
grep -c "PASS\|FAIL" /Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-08-SMOKE-RESULTS.md  # expect ≥ 11
grep -q "Sign-Off" /Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-08-SMOKE-RESULTS.md
```
</verification>

<success_criteria>
- All seven automated gates from Task 1 pass.
- All eleven manual smoke checks (M1–M11) recorded with pass/fail in SMOKE-RESULTS.md.
- Decision Coverage Audit shows every locked decision D-01..D-11 has at least one supporting gate or check.
- Out-of-Scope Confirmation verifies the Option B divergences are still in place (no marketing dark mode tokens, no surface hierarchy, no elevation tokens, no `--code-*` bridge, app guide unchanged).
- Operator signs off in SMOKE-RESULTS.md.
- This is a planning-artifact commit IN THE GEOLENS REPO (per D-11: SMOKE-RESULTS.md lives in `.planning/`); the plan touches no files in `getgeolens.com`.
</success_criteria>

<output>
After completion, create `.planning/phases/999.5-style-system-alignment/999.5-08-SUMMARY.md` summarizing:
- Total gates run + pass count
- Any failures and the gap-closure recommendation (or "no gaps; phase verified")
- Pointer to SMOKE-RESULTS.md as the detailed evidence
- Commit hash for the SMOKE-RESULTS.md commit in the geolens repo
- Phase 999.5 closing status (ready for `/gsd-verify-work` or paused awaiting gap closure)
</output>
