# Phase 999.5: Style System Alignment - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-26
**Phase:** 999.5-style-system-alignment
**Areas discussed:** Alignment scope (A/B/C), Marketing dark mode policy, DESIGN-GUIDE structure, Phase decomposition

---

## Alignment scope (A/B/C)

| Option | Description | Selected |
|--------|-------------|----------|
| B — Partial alignment (Recommended) | Bridge typography (IBM Plex), logo continuity, Aside → primary-700. Keep Starlight neutrals, skip elevation/surface hierarchy on marketing+docs, keep marketing light-only. ~1 phase. Documented divergence. | ✓ |
| A — Full alignment | Marketing+docs adopt every token: IBM Plex, warm atlas-paper neutrals, 4-tier surfaces, dual-mode (forces marketing dark mode), elevation tokens, full Aside override. ~2-3 phases. Largest refactor, biggest brand consistency win. | |
| C — Fork | Accept three independent design systems. Rip out the partial bridge. Document DESIGN-GUIDE.md as app-only. ~half day. Walks back Phase 224 work. | |
| B+ — Partial now, Full later | Ship Option B in 999.5. Add a documented future phase that revisits Full alignment once design owner exists or marketing dark mode is product-validated. | |

**User's choice:** B — Partial alignment
**Notes:** Locked the headline strategic decision. Drives all downstream questions in this phase.

---

## Polish items within Option B

| Option | Description | Selected |
|--------|-------------|----------|
| Warmer sidebar tint | Override --sl-color-bg-sidebar to oklch(0.99 0.003 85) so docs sidebar feels less Starlight-default-cool. ~1 line. | ✓ |
| Extend check-token-sync.sh | Drift CI greps IBM Plex font-family declarations across all three surfaces. ~3 lines. | ✓ |
| Aside info + caution colors too | Beyond `tip` (purple → primary-700), also override `note`/`caution`/`danger` Starlight slot colors so all callouts read in brand palette. | ✓ |
| Logo re-extraction audit | Verify <Logo> is genuinely shared between app and marketing today; if not, extract a shared SVG into a single source of truth. | ✓ |

**User's choice:** All four (multiSelect).
**Notes:** Maximalist B — every cheap polish item included.

---

## Logo extraction status

| Option | Description | Selected |
|--------|-------------|----------|
| Don't know — verify in research phase | Punt to gsd-phase-researcher to grep both repos and confirm. | ✓ |
| Already shared — just wire docs | Logo is identical or already imported from a shared package. | |
| Needs extraction | App and marketing each have their own logo file. First task is extraction. | |

**User's choice:** Defer to research phase.
**Notes:** Researcher will grep both repos and confirm whether the SVG is genuinely shared.

---

## Marketing dark mode policy

| Option | Description | Selected |
|--------|-------------|----------|
| Formally defer with rationale | Update the comment in global.css:6 with explicit rationale and revisit triggers. Park as a backlog idea. | ✓ |
| Defer silently | Leave the existing global.css:6 comment untouched. | |
| Add dark mode now anyway | Even though Option B doesn't require it, ship dark mode for marketing in this phase. (Pulls one Option A item into Option B.) | |
| Park as separate backlog phase | Create backlog 999.6 'Marketing dark mode' as its own future phase. | |

**User's choice:** Formally defer with rationale.
**Notes:** Marketing stays light-only. Comment in `global.css:6` is rewritten with explicit rationale and revisit triggers so future audits stop flagging it as a gap.

---

## DESIGN-GUIDE structure

| Option | Description | Selected |
|--------|-------------|----------|
| Add 'Scope and Cross-Surface Application' section to existing app guide | Single guide stays the north star. New section enumerates what marketing+docs adopt and what they don't. | |
| Split into APP + MARKETING guides | Rename DESIGN-GUIDE.md → DESIGN-GUIDE-APP.md and add sibling DESIGN-GUIDE-MARKETING.md. | |
| Three guides: APP / MARKETING / DOCS | Maximally explicit — each surface gets its own guide. | |
| **Other (user freeform):** Put a new design guide in the getgeolens repo that references the design guide here | New `getgeolens.com/DESIGN-GUIDE.md` documents marketing+docs and references upstream to the app's DESIGN-GUIDE.md. App guide stays unmodified. | ✓ |

**User's choice:** Freeform — new design guide in `getgeolens.com` repo that references the app's guide.
**Notes:** Cleaner than the handoff's proposed scope-section approach. Keeps the app guide focused on the app; the new getgeolens guide owns the cross-surface story without token duplication.

---

## What the new getgeolens.com/DESIGN-GUIDE.md should document

| Option | Description | Selected |
|--------|-------------|----------|
| Shared tokens (link upstream) | Lists what's adopted from the app guide and links to it for canonical token values — not duplicated. | (Claude discretion: ✓) |
| Marketing-specific tokens | Documents intentional divergence: cool-white background, light-only mode, no surface hierarchy, no elevation. | (Claude discretion: ✓) |
| Docs-specific tokens | Documents Starlight overrides: which slots are bridged, which keep Starlight defaults, Expressive Code untouched. | (Claude discretion: ✓) |
| Drift CI contract | Documents what check-token-sync.sh greps for and why. | (Claude discretion: ✓) |

**User's choice:** "You decide."
**Notes:** Claude defaulted to all four. Each section is small, and a single load-bearing guide is more useful than a stub.

---

## Phase decomposition

| Option | Description | Selected |
|--------|-------------|----------|
| Single bundled 999.5 phase | One phase covers all marketing + docs + DESIGN-GUIDE.md + drift CI work. Recommended. | ✓ |
| Split into 999.5 (marketing) + 999.6 (docs) | Two phases run in parallel against the getgeolens.com repo. | |
| Split: 999.5 (tokens) + 999.6 (guide doc) | Implementation phase ships token changes; documentation phase writes the guide afterwards. | |

**User's choice:** Single bundled 999.5 phase.
**Notes:** Option B is small enough that splitting adds coordination overhead without parallelism gains.

---

## Cross-repo execution boundary

| Option | Description | Selected |
|--------|-------------|----------|
| Plan here, execute in getgeolens.com checkout | PLAN.md lives in geolens/.planning/. Executor cd's into ../getgeolens.com to apply changes and commits there. | (Claude discretion: ✓) |
| Plan + execute entirely in getgeolens.com | Open getgeolens.com as its own GSD workspace. | |
| Hybrid — update north-star app guide here, everything else in getgeolens.com | If any tweaks to geolens/docs/DESIGN-GUIDE.md are needed (none expected under Option B), do them here. | |

**User's choice:** "You decide."
**Notes:** Claude defaulted to Plan-here-execute-there. Keeps all GSD planning artifacts unified in geolens/.planning/. Two distinct git histories will be touched (planning docs in geolens, code in getgeolens.com); planner and executor must be explicit about which repo each task targets.

---

## Claude's Discretion

- Sections of the new `getgeolens.com/DESIGN-GUIDE.md` (D-08): all four sections included.
- Cross-repo execution boundary (D-11): plan-here, execute in `../getgeolens.com` checkout.

## Deferred Ideas

- Marketing dark mode — track as backlog (suggested 999.6) with explicit revisit triggers.
- Full Option A alignment — reconsider only if a design owner is hired or marketing dark mode work begins.
- `--code-*` token bridge to Expressive Code — explicitly rejected; Expressive Code's syntax theme stays.
- Three-way design guide split (APP/MARKETING/DOCS) — rejected; one guide per repo is sufficient.
- A11Y re-audit pass — only required if Option A is chosen later; not needed for Option B's typography-only swap.
