---
phase: 999.5-style-system-alignment
plan: 05
type: execute
wave: 2
depends_on: [02]
files_modified:
  - /Users/ishiland/Code/getgeolens.com/docs/astro.config.mjs
autonomous: true
requirements: []
tags: [design-system, logo, starlight, astro-config, cross-repo]

must_haves:
  truths:
    - "Starlight `logo` config block added to docs/astro.config.mjs (D-05)"
    - "logo.src points to './src/assets/logo.svg' (relative to docs project root, avoiding cross-project Vite issues per RESEARCH.md Pitfall 3)"
    - "logo.alt is 'GeoLens' (accessible alt text for screen readers)"
    - "logo.replacesTitle is false (logo + 'GeoLens Docs' title text both shown — see RESEARCH.md Open Question 3 for fallback)"
    - "Existing Starlight config keys (title, customCss, sidebar, integrations, etc.) are preserved unchanged"
    - "Implementation lands in getgeolens.com repo per D-11"
  artifacts:
    - path: "/Users/ishiland/Code/getgeolens.com/docs/astro.config.mjs"
      provides: "Starlight logo wiring referencing the canonical logo.svg from Plan 02"
      contains: "logo:"
  key_links:
    - from: "astro.config.mjs starlight({ logo: { src: ... } })"
      to: "/Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg"
      via: "Vite asset import resolved from docs project root"
      pattern: "src: './src/assets/logo.svg'"
---

<objective>
Add a Starlight `logo` config block to `/Users/ishiland/Code/getgeolens.com/docs/astro.config.mjs` per RESEARCH.md "File 4" — pointing `logo.src` at `./src/assets/logo.svg` (the docs-internal copy created by Plan 02), setting `alt: 'GeoLens'`, and `replacesTitle: false` so the docs nav shows the canonical logomark alongside the existing "GeoLens Docs" title text.

Purpose: D-05 wiring half — make the logo asset created in Plan 02 actually render in the Starlight nav header.

Output: A minimal additive change to `astro.config.mjs` (a 5-line `logo: { ... }` block inserted after the `title` key inside `starlight({})`), with the existing Starlight integration config otherwise unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ishiland/Code/geolens/.planning/PROJECT.md
@/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-CONTEXT.md
@/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-RESEARCH.md
@/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-02-PLAN.md

<interfaces>
<!-- Verbatim diff target from RESEARCH.md "File 4: getgeolens.com/docs/astro.config.mjs" -->

CURRENT (anchor near line 14 of astro.config.mjs):
```js
starlight({
  title: 'GeoLens Docs',
  // Phase 223 placeholder — full token bridge ships in Phase 224 (BRAND-01)
  customCss: ['./src/styles/custom.css'],
```

AFTER:
```js
starlight({
  title: 'GeoLens Docs',
  logo: {
    src: './src/assets/logo.svg',
    alt: 'GeoLens',
    replacesTitle: false,
  },
  // Phase 223 placeholder — full token bridge ships in Phase 224 (BRAND-01)
  customCss: ['./src/styles/custom.css'],
```

Path resolution rationale (RESEARCH.md):
- `astro.config.mjs` lives at `/Users/ishiland/Code/getgeolens.com/docs/astro.config.mjs`
- The Astro project root is `/Users/ishiland/Code/getgeolens.com/docs/`
- `./src/assets/logo.svg` resolves to `/Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg` — the file Plan 02 Task 2 created
- This path is INSIDE the docs project root, so Vite's "cannot import outside project root" landmine (RESEARCH.md Pitfall 3) does NOT fire
- DO NOT use `'../../public/logo.svg'` — that traverses outside the docs project root and Vite may reject it

Field semantics:
- `src`: file path Starlight feeds to Vite asset pipeline (resolved from project root)
- `alt`: accessible alt text rendered as `<img alt="GeoLens">` (screen readers; the visible "GeoLens Docs" title text is separate)
- `replacesTitle: false`: keeps the existing site `title: 'GeoLens Docs'` text rendered alongside the logo. RESEARCH.md Open Question 3 notes that if the icon-only logo at small size reads poorly next to the title text, `replacesTitle: true` is the fallback — defer that judgment to the visual smoke check in Plan 08; Plan 05 lands the documented default first.

Dependency: Plan 02 must be complete (the `logo.svg` file at `docs/src/assets/logo.svg` must exist), otherwise Astro will fail to resolve the asset import at build time.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Starlight logo config block to docs/astro.config.mjs</name>
  <files>/Users/ishiland/Code/getgeolens.com/docs/astro.config.mjs</files>
  <read_first>
    - /Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-RESEARCH.md ("File 4: getgeolens.com/docs/astro.config.mjs" + "Starlight Logo Wiring" section + Pitfall 3 + Open Question 3 + Assumption A1)
    - /Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-CONTEXT.md (D-05)
    - /Users/ishiland/Code/getgeolens.com/docs/astro.config.mjs (READ FULL CURRENT CONTENT — confirm anchor lines and existing structure before editing)
    - /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg (must exist from Plan 02 Task 2)
  </read_first>
  <action>
    Working directory: `/Users/ishiland/Code/getgeolens.com/docs` (the docs subtree). Per D-11, this task targets the `getgeolens.com` repo.

    Pre-flight check: confirm `getgeolens.com/docs/src/assets/logo.svg` exists from Plan 02 Task 2:
    ```bash
    test -f /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg || { echo "BLOCKED: Plan 02 not complete — logo.svg missing"; exit 1; }
    ```
    If the file is missing, halt and report the blocker (Plan 02 dependency unsatisfied).

    Use the Edit tool to add a `logo: { ... }` block to the Starlight integration config inside `astro.config.mjs`. Locate the anchor by string match (NOT line number, since other phases may have shifted line numbers):

    Find the exact two-line anchor:
    ```
      title: 'GeoLens Docs',
      // Phase 223 placeholder — full token bridge ships in Phase 224 (BRAND-01)
    ```

    Replace with:
    ```
      title: 'GeoLens Docs',
      logo: {
        src: './src/assets/logo.svg',
        alt: 'GeoLens',
        replacesTitle: false,
      },
      // Phase 223 placeholder — full token bridge ships in Phase 224 (BRAND-01)
    ```

    DO NOT modify any other config keys (`customCss`, `sidebar`, `integrations`, `lastUpdated`, `editLink`, etc.). DO NOT change the `title` text — it stays `'GeoLens Docs'` and renders alongside the logo.

    Verify the docs build green AND `astro check` passes (the Starlight schema validates `logo` config fields):
    ```bash
    cd /Users/ishiland/Code/getgeolens.com/docs
    npx astro check 2>&1 | tail -10
    npm run build 2>&1 | tail -20
    ```

    Both must exit 0. If `astro check` fails with a schema error on `logo`, this is the Assumption A1 risk (`replacesTitle` field name) — read `node_modules/@astrojs/starlight/schemas/config.ts` to confirm the exact field names accepted in installed version 0.38.4 and adjust accordingly. If the schema accepts `replacesTitle` (expected per Starlight docs), proceed.

    Commit in `getgeolens.com` repo with a conventional-commit message such as `feat(docs): wire canonical GeoLens logo into Starlight nav (Phase 999.5 D-05)`. Per D-11, no commit in geolens repo from this task.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -q "logo: {" astro.config.mjs && grep -q "src: './src/assets/logo.svg'" astro.config.mjs && grep -q "alt: 'GeoLens'" astro.config.mjs && grep -q "replacesTitle: false" astro.config.mjs && grep -q "title: 'GeoLens Docs'" astro.config.mjs && test -f src/assets/logo.svg && npx astro check > /tmp/docs-astro-check-05.log 2>&1 && npm run build > /tmp/docs-build-05.log 2>&1 && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "logo: {" /Users/ishiland/Code/getgeolens.com/docs/astro.config.mjs` exits 0
    - `grep -q "src: './src/assets/logo.svg'" /Users/ishiland/Code/getgeolens.com/docs/astro.config.mjs` exits 0
    - `grep -q "alt: 'GeoLens'" /Users/ishiland/Code/getgeolens.com/docs/astro.config.mjs` exits 0
    - `grep -q "replacesTitle: false" /Users/ishiland/Code/getgeolens.com/docs/astro.config.mjs` exits 0
    - `grep -q "title: 'GeoLens Docs'" /Users/ishiland/Code/getgeolens.com/docs/astro.config.mjs` exits 0 (existing title preserved)
    - `grep -q "customCss: \['\.\/src\/styles\/custom\.css'\]" /Users/ishiland/Code/getgeolens.com/docs/astro.config.mjs` exits 0 (existing customCss key preserved)
    - `! grep -q "src: '\.\.\/" /Users/ishiland/Code/getgeolens.com/docs/astro.config.mjs` (no parent-traversal path that would trigger RESEARCH.md Pitfall 3 — `../public/logo.svg` is forbidden)
    - `test -f /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg` exits 0 (Plan 02 dependency satisfied)
    - `cd /Users/ishiland/Code/getgeolens.com/docs && npx astro check` exits 0 (Starlight schema accepts the logo config block)
    - `cd /Users/ishiland/Code/getgeolens.com/docs && npm run build` exits 0 (full docs build green; logo asset resolved by Vite)
  </acceptance_criteria>
  <done>
    `docs/astro.config.mjs` declares the Starlight logo config pointing at the canonical SVG. `astro check` and `npm run build` both pass. The visual confirmation that the logo actually renders in the nav happens in Plan 08's manual smoke checklist.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — config-only change referencing a same-repo static asset.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.5-05 | (n/a) | astro.config.mjs (build-time config) | accept | No new threat surface — declarative Astro/Starlight config keys with file path string, alt text, and boolean. No authentication, input validation, network-exposed endpoints, or data flow changes. Asset is repo-controlled per Plan 02. ASVS L1 not applicable. |
</threat_model>

<verification>
```bash
cd /Users/ishiland/Code/getgeolens.com/docs

# Config keys present
grep -A 4 "logo: {" astro.config.mjs

# Astro schema check
npx astro check

# Full build
npm run build 2>&1 | tail -5
```

Manual visual confirmation deferred to Plan 08 (`npm run dev` + nav inspection).
</verification>

<success_criteria>
- `logo: { src: './src/assets/logo.svg', alt: 'GeoLens', replacesTitle: false }` block present inside the Starlight integration config in `astro.config.mjs`.
- All other Starlight config keys preserved unchanged.
- `npx astro check` exits 0 (Starlight schema accepts the new block in v0.38.4).
- `npm run build` exits 0 (Vite resolves the SVG asset).
- A commit in `getgeolens.com` records the change. No commit in geolens repo from this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/999.5-style-system-alignment/999.5-05-SUMMARY.md` documenting:
- The exact 5-line `logo` block as committed
- Confirmation that `astro check` and `npm run build` both passed
- Any deviation if Starlight schema rejected `replacesTitle` (Assumption A1) — note the corrective field name from `node_modules/@astrojs/starlight/schemas/config.ts`
- Commit hash in `getgeolens.com` repo
</output>
