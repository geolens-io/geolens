---
phase: 999.5-style-system-alignment
plan: 02
subsystem: design-system
tags: [design-system, logo, svg, brand, cross-repo, currentcolor, starlight, astro]

# Dependency graph
requires:
  - phase: 999.5-01
    provides: IBM Plex font packages installed in marketing + docs (manifest swap)
provides:
  - Canonical currentColor logo SVG at getgeolens.com/public/logo.svg (marketing-served)
  - Byte-identical mirror at getgeolens.com/docs/src/assets/logo.svg (Astro asset pipeline)
  - currentColor inheritance enables brand-aware tinting wherever the logo is consumed
affects: [999.5-05, 999.5-07]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Static SVG asset duplicated across project roots to satisfy Astro/Vite asset import constraints (Pitfall 3 mitigation)
    - currentColor stroke for brand-color inheritance from CSS context

key-files:
  created:
    - /Users/ishiland/Code/getgeolens.com/public/logo.svg
    - /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg
  modified: []

key-decisions:
  - "Canonicalized on existing favicon geometry (viewBox 0 0 64 64, circle r=22 cx/cy 27) — NOT a JSX-to-SVG extraction of GeoLensLogo.tsx (which uses different proportions r=18 cx/cy 26)"
  - "Two byte-identical SVG files: one in public/ for marketing, one in docs/src/assets/ to avoid Astro/Vite cross-project-root asset import errors (RESEARCH Pitfall 3)"
  - "stroke=currentColor instead of hardcoded #334155 — only deviation from existing favicon.svg, lets Starlight nav inherit text color"
  - "favicon.svg files left untouched — they keep hardcoded #334155 for browser favicon use where currentColor cannot resolve"
  - "Nav.astro inline SVG left untouched — already uses currentColor and renders correctly (RESEARCH Open Question 2)"

patterns-established:
  - "Cross-repo SVG duplication: maintenance convention is byte-identical sync between public/ and docs/src/assets/ (will be documented in Plan 07 DESIGN-GUIDE.md)"
  - "currentColor stroke pattern for brand assets that need to inherit color from CSS context"

requirements-completed: []

# Metrics
duration: 1min
completed: 2026-04-26
---

# Phase 999.5 Plan 02: Canonical Logo SVG Summary

**Two byte-identical 584-byte currentColor logo SVGs created at `getgeolens.com/public/logo.svg` and `getgeolens.com/docs/src/assets/logo.svg`, unblocking Plan 05's Starlight `logo.src` wiring with brand-aware color inheritance.**

## Performance

- **Duration:** ~1 min
- **Started:** 2026-04-26T14:15:09Z
- **Completed:** 2026-04-26T14:15:59Z
- **Tasks:** 2
- **Files created:** 2 (both in getgeolens.com repo)

## Accomplishments

- Canonical logo SVG authored at `getgeolens.com/public/logo.svg` using `stroke="currentColor"` for brand-color inheritance.
- Byte-identical mirror placed at `getgeolens.com/docs/src/assets/logo.svg` so Starlight's `logo.src` can resolve through Astro's asset pipeline without cross-project-root errors.
- Existing `getgeolens.com/public/favicon.svg` preserved unmodified (keeps hardcoded `#334155` for browser favicon use).
- App's `GeoLensLogo.tsx` React component left untouched per D-09 spirit (app is the unmodified north star).
- Marketing `Nav.astro` inline SVG left untouched (RESEARCH Open Question 2 — already renders correctly with currentColor).

## Task Commits

Each task was committed atomically in the `getgeolens.com` repo on branch `gsd/phase-225-api-reference`:

1. **Task 1: Create canonical logo SVG at `getgeolens.com/public/logo.svg`** — `0e5ea6b` (feat)
2. **Task 2: Mirror canonical logo SVG to `getgeolens.com/docs/src/assets/logo.svg`** — `cb5ac49` (feat)

**Plan metadata commit:** _(forthcoming in geolens repo — SUMMARY.md, STATE.md, ROADMAP.md)_

Plan 01's two earlier commits (`9a02a76`, `1dadc66`) were pre-existing and not part of this plan's scope.

## Files Created/Modified

- `getgeolens.com/public/logo.svg` (NEW, 584 bytes) — Marketing-root canonical logo SVG. Served at `https://getgeolens.com/logo.svg` for any future `<img src="/logo.svg">` use.
- `getgeolens.com/docs/src/assets/logo.svg` (NEW, 584 bytes) — Astro-pipeline copy for Starlight's `logo.src` config (Plan 05 will wire `logo: { src: './src/assets/logo.svg', alt: 'GeoLens', replacesTitle: false }`).

## Verification Evidence

```text
test -f public/logo.svg                                              → exits 0
test -f docs/src/assets/logo.svg                                     → exits 0
cmp public/logo.svg docs/src/assets/logo.svg                         → exits 0 (byte-identical)
grep -c 'stroke="currentColor"' public/logo.svg                      → 3
grep -c 'cx="27" cy="27" r="22"' public/logo.svg                     → 2
grep 'aria-hidden="true"' public/logo.svg                            → match
! grep '#334155' public/logo.svg                                     → no match (no hardcoded color)
! grep 'fill="#' public/logo.svg                                     → no match (no hardcoded fill)
git diff --name-only public/favicon.svg                              → empty (favicon untouched)
wc -c public/logo.svg docs/src/assets/logo.svg                       → 584 each, 1168 total
```

## Decisions Made

None beyond the plan's locked decisions. Both tasks executed exactly per the plan's verbatim SVG content from RESEARCH.md "Logo Extraction Findings — Recommended Approach for D-05".

## Deviations from Plan

None — plan executed exactly as written. SVG content is byte-identical to the canonical block specified in the plan's `<interfaces>` section.

## Issues Encountered

None. The `docs/src/assets/` directory did not exist; `mkdir -p` created it as the plan instructed. No build steps run in this plan (asset wiring is Plan 05's scope), so no compilation or runtime verification was needed beyond filesystem and content checks.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **Plan 05 (Starlight logo wiring) is unblocked.** It can add `logo: { src: './src/assets/logo.svg', alt: 'GeoLens', replacesTitle: false }` to `getgeolens.com/docs/astro.config.mjs` referencing the file created here in Task 2.
- **Plan 07 (DESIGN-GUIDE.md authoring)** should document the dual-file maintenance convention: `public/logo.svg` and `docs/src/assets/logo.svg` must stay byte-identical (any update to one requires the same update to the other).
- **Cross-repo coordination:** Implementation lives in `getgeolens.com` (commits `0e5ea6b`, `cb5ac49` on branch `gsd/phase-225-api-reference`). Planning artifacts (this SUMMARY) live in `geolens` on branch `main` per D-11.

## Self-Check: PASSED

- FOUND: /Users/ishiland/Code/getgeolens.com/public/logo.svg
- FOUND: /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg
- FOUND: commit 0e5ea6b in /Users/ishiland/Code/getgeolens.com (Task 1)
- FOUND: commit cb5ac49 in /Users/ishiland/Code/getgeolens.com (Task 2)

---
*Phase: 999.5-style-system-alignment*
*Plan: 02*
*Completed: 2026-04-26*
