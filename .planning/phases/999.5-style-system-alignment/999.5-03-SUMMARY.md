---
phase: 999.5-style-system-alignment
plan: 03
subsystem: ui
tags: [design-system, fonts, marketing, css, dark-mode-rationale, ibm-plex, oklch, cross-repo]

# Dependency graph
requires:
  - phase: 999.5-01
    provides: "@fontsource-variable/ibm-plex-sans installed in marketing root node_modules (the @import target for this plan)"
  - phase: 224-brand-shell-search
    provides: "Inter Variable baseline in global.css (BRAND-01) that this plan supersedes"
provides:
  - "Marketing global.css imports IBM Plex Sans Variable (D-01)"
  - "Marketing --font-sans @theme token resolves to 'IBM Plex Sans Variable' (D-01)"
  - "Marketing html font-family fallback chain references 'IBM Plex Sans Variable' (D-01, RESEARCH Pitfall 1)"
  - "global.css header comment uses 'Intentional / Rationale / Revisit if' format documenting marketing's light-only intent (D-06)"
affects: [999.5-04, 999.5-05, 999.5-06, 999.5-07, 999.5-08]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Cross-repo CSS edit (D-11): planning artifacts in geolens, implementation commit in getgeolens.com"
    - "Dark-mode deferral comment format: 'Intentional: [decision]. Rationale: [why]. Revisit if: [trigger].'"

key-files:
  created: []
  modified:
    - /Users/ishiland/Code/getgeolens.com/src/styles/global.css

key-decisions:
  - "Applied all four edits in a single Edit-tool sequence per RESEARCH.md File 1 change map; no scope creep"
  - "Did NOT add --font-mono (RESEARCH.md File 1 explicit guidance — marketing has no monospace usage)"
  - "Did NOT modify Nav.astro inline SVG (out of scope per plan; D-05 deliverable is logo file + Starlight config, not nav cleanup)"

patterns-established:
  - "Dark-mode deferral comment template: header block uses 'Intentional / Rationale / Revisit if' triplet so future audits read the decision instead of inferring it"
  - "Three-location font-family swap: @import + @theme token + html fallback chain — leaving any one stale produces silent font requests for the deleted package (Pitfall 1)"

requirements-completed: []

# Metrics
duration: 1min
completed: 2026-04-26
---

# Phase 999.5 Plan 03: Marketing global.css IBM Plex + Dark-Mode Rationale Summary

**Swapped Inter Variable for IBM Plex Sans Variable across the marketing global.css `@import`, `@theme --font-sans`, and `html` font-family fallback; rewrote the header comment block to the D-06 'Intentional / Rationale / Revisit if' dark-mode-deferral format.**

## Performance

- **Duration:** ~1 min
- **Started:** 2026-04-26T14:19:40Z
- **Completed:** 2026-04-26T14:21:05Z
- **Tasks:** 1
- **Files modified:** 1 (`getgeolens.com/src/styles/global.css`)

## Accomplishments

- Marketing `global.css` imports `@fontsource-variable/ibm-plex-sans/wght.css` (was `@fontsource-variable/inter/wght.css`)
- `--font-sans` token under `@theme` block resolves to `'IBM Plex Sans Variable', ui-sans-serif, system-ui, sans-serif`
- `html` rule's `var(--font-sans, ...)` fallback chain references `'IBM Plex Sans Variable'` (closes RESEARCH Pitfall 1 — no orphaned Inter request)
- Header comment block (lines 1–11 of post-edit file) now documents marketing's light-only intent in the D-06 'Intentional / Rationale / Revisit if' format
- `npm run build` in `/Users/ishiland/Code/getgeolens.com` exits 0; 4 pages built; IBM Plex woff2 subsets (latin, latin-ext, cyrillic, cyrillic-ext, greek, vietnamese) emitted to `dist/_astro/`; no Inter assets remain

## Task Commits

Single task — atomic commit in **getgeolens.com** repo only (per D-11). The geolens repo receives only the SUMMARY.md and STATE.md/ROADMAP.md updates.

1. **Task 1: Swap Inter -> IBM Plex Sans in global.css and rewrite dark-mode rationale comment** — `d00aeaf` (style in getgeolens.com on branch `gsd/phase-225-api-reference`)

## Files Created/Modified

**In `getgeolens.com` repo (commit `d00aeaf`, branch `gsd/phase-225-api-reference`):**
- `src/styles/global.css` — four edits across the file:
  - Lines 1–11: header comment block (was 7 lines; now 11 lines with multi-line rationale)
  - Line 14 (was 10): `@import` swapped to IBM Plex Sans Variable
  - Line 43 (was 39): `--font-sans` @theme token swapped to IBM Plex Sans Variable
  - Line 121 (was 117): `html` font-family `var()` fallback string swapped to IBM Plex Sans Variable

**In `geolens` repo (final metadata commit):**
- `.planning/phases/999.5-style-system-alignment/999.5-03-SUMMARY.md` — this file
- `.planning/STATE.md` — position advanced (Plan 3 → 4) and decisions appended
- `.planning/ROADMAP.md` — phase 999.5 progress row updated

## Before / After of Header Comment Block

**Before (7 lines, v14.0-era):**
```css
/*
 * GeoLens Marketing Site — Global Styles
 * Design tokens kept in sync with: geolens/frontend/src/index.css
 * Font: Inter Variable (self-hosted via @fontsource-variable/inter)
 * Color space: OKLCH throughout
 * Dark mode: NOT supported (light only for v14.0 launch)
 */
```

**After (11 lines, D-06 rationale format):**
```css
/*
 * GeoLens Marketing Site — Global Styles
 * Design tokens kept in sync with: geolens/frontend/src/index.css
 * Font: IBM Plex Sans Variable (self-hosted via @fontsource-variable/ibm-plex-sans)
 * Color space: OKLCH throughout
 * Dark mode: Intentional brand decision — marketing is light-only.
 *   Rationale: marketing surfaces prioritize first-impression clarity; dual-mode adds
 *   visual QA surface area (hero contrast, marketing imagery) disproportionate to benefit.
 *   Revisit if: a design owner is onboarded, or the product ships dark mode to >50% of
 *   signed-in users. Tracked as a separate backlog item (deferred from Phase 999.5).
 */
```

## Build Verification

- `cd /Users/ishiland/Code/getgeolens.com && npm run build` → exit code **0**
- Output: `[build] 4 page(s) built in 1.92s` / `[build] Complete!`
- IBM Plex assets emitted in `dist/_astro/`:
  - `ibm-plex-sans-latin-wght-normal.IvpUvPa2.woff2`
  - `ibm-plex-sans-latin-ext-wght-normal.CIII54If.woff2`
  - `ibm-plex-sans-cyrillic-wght-normal.BAAhND-U.woff2`
  - `ibm-plex-sans-cyrillic-ext-wght-normal.d45eAU9y.woff2`
  - `ibm-plex-sans-greek-wght-normal.CmyJS8uq.woff2`
  - `ibm-plex-sans-vietnamese-wght-normal.Dg1JeJN0.woff2`
- `find dist -name "*inter*"` → empty (no Inter font assets remain)

## Acceptance Criteria — All Pass

| Check | Result |
|-------|--------|
| `grep -q "@fontsource-variable/ibm-plex-sans/wght.css" src/styles/global.css` | OK |
| `grep -c "'IBM Plex Sans Variable'" src/styles/global.css` returns 2 (token + html fallback) | OK (2 matches) |
| `! grep -q "Inter Variable" src/styles/global.css` | OK |
| `! grep -q "@fontsource-variable/inter\|@fontsource/inter" src/styles/global.css` | OK |
| `! grep -q "Dark mode: NOT supported" src/styles/global.css` | OK |
| `grep -q "Intentional brand decision" src/styles/global.css` | OK |
| `grep -q "Rationale:" src/styles/global.css` | OK |
| `grep -q "Revisit if:" src/styles/global.css` | OK |
| `npm run build` exits 0 | OK |
| `git diff src/styles/global.css` shows only header + 3 single-line swaps; OKLCH palette + Tailwind imports + surface/elevation tokens unchanged | OK (+9/-5, exclusively the four targeted regions) |

## Decisions Made

- Followed the four-change blueprint from RESEARCH.md File 1 verbatim. No interpretation needed because RESEARCH.md captured exact before/after strings for every replacement.
- Chose to confirm the build emits IBM Plex woff2 subsets in `dist/_astro/` rather than only checking grep — proves the import resolves at build time and the fontsource CSS actually pulls in the font files (a stronger guarantee than string presence alone).

## Deviations from Plan

None — plan executed exactly as written. All four edits applied with the exact RESEARCH.md before/after strings; no scope changes; no other lines touched (verified by `git diff --stat` showing +9/-5 across only the four targeted regions of global.css).

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **Plan 04 (docs custom.css update) ready:** the IBM Plex `@import` pattern proven on marketing applies identically to docs; no remaining unknowns about the IBM Plex package shape.
- **Plan 05 (Starlight logo wiring) ready:** independent of this plan; can run in any order with Plan 04.
- **Plan 06 (drift CI extension) ready:** `check-token-sync.sh` will now find the `IBM Plex Sans Variable` string in marketing `global.css` (one of the three surfaces it must verify); the assertion can be added confidently.
- **No blockers** for downstream plans.
- **Open latent concern (deferred from Plan 01):** `getgeolens.com/src/lib/og.ts` still loads `src/assets/fonts/inter-700.ttf` for OG image generation. This is unchanged by this plan and remains a candidate for a follow-up task (RESEARCH.md Pitfall 6 / open question 1).

## Self-Check: PASSED

Verification commands run after SUMMARY.md drafted:

- `[ -f /Users/ishiland/Code/getgeolens.com/src/styles/global.css ]` → FOUND
- `git -C /Users/ishiland/Code/getgeolens.com log --oneline | grep -q "d00aeaf"` → FOUND (commit `d00aeaf` exists on branch `gsd/phase-225-api-reference`)
- `grep -q "@fontsource-variable/ibm-plex-sans/wght.css" /Users/ishiland/Code/getgeolens.com/src/styles/global.css` → FOUND
- `grep -q "Intentional brand decision" /Users/ishiland/Code/getgeolens.com/src/styles/global.css` → FOUND
- `! grep -q "Inter Variable" /Users/ishiland/Code/getgeolens.com/src/styles/global.css` → confirmed absent

---
*Phase: 999.5-style-system-alignment*
*Completed: 2026-04-26*
