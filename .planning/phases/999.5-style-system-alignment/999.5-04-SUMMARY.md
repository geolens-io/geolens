---
phase: 999.5-style-system-alignment
plan: 04
subsystem: ui
tags: [design-system, fonts, docs, starlight, asides, sidebar-tint, css, ibm-plex, oklch, cross-repo]

# Dependency graph
requires:
  - phase: 999.5-01
    provides: "@fontsource-variable/ibm-plex-sans + @fontsource/ibm-plex-mono installed in docs root node_modules (the @import targets for this plan)"
  - phase: 224-brand-shell-search
    provides: "Inter Variable + OKLCH primary palette baseline in docs/src/styles/custom.css that this plan supersedes (font swap; palette preserved verbatim)"
provides:
  - "Docs custom.css imports IBM Plex Sans Variable + IBM Plex Mono (D-01)"
  - "Docs --sl-font resolves to 'IBM Plex Sans Variable' (D-01)"
  - "Docs --sl-font-mono resolves to 'IBM Plex Mono' (D-01)"
  - "Docs --sl-color-bg-sidebar overridden to oklch(0.99 0.003 85) in light mode only (D-02)"
  - "Aside note slot (--sl-color-blue-*) overridden to brand primary in light + dark (D-04)"
  - "Aside tip slot (--sl-color-purple-*) overridden to brand primary in light + dark (D-04)"
  - "Aside caution (--sl-color-orange-*) and danger (--sl-color-red-*) intentionally NOT overridden — semantic warnings preserved (D-04)"
  - "OKLCH primary palette mirror (50–950) preserved verbatim from Phase 224 BRAND-01 (BRAND-04 drift assertion still passes)"
affects: [999.5-05, 999.5-06, 999.5-07, 999.5-08]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Cross-repo CSS edit (D-11): planning artifacts in geolens, implementation commit in getgeolens.com"
    - "Verbatim full-file replacement from RESEARCH.md File 2 (no interpretation; 3084 chars byte-identical)"
    - "Light-only sidebar tint scope (RESEARCH.md Pitfall 5): --sl-color-bg-sidebar declared only in :root, not in :root[data-theme='dark']"
    - "Selective Aside slot override (D-04): note + tip → brand; caution + danger → Starlight semantic defaults"

key-files:
  created: []
  modified:
    - /Users/ishiland/Code/getgeolens.com/docs/src/styles/custom.css

key-decisions:
  - "Wrote the file verbatim from RESEARCH.md File 2 (byte-identical, 3084 chars excluding trailing newline) — no scope changes, no interpretation"
  - "Preserved one historical comment string referencing 'Inter Variable' inside the IBM Plex font slot block (line 36: `/* IBM Plex font (Phase 999.5 — replaces Inter Variable) */`) because the plan's primary directive is verbatim fidelity to RESEARCH.md File 2; this is a context note about what the new declaration replaces, not a live import or font-family"
  - "Did NOT override --sl-color-orange-* or --sl-color-red-* — caution and danger keep semantic Starlight defaults per D-04"
  - "Sidebar tint declared only in light :root block; dark mode sidebar stays at Starlight default --sl-color-gray-6 per RESEARCH.md Pitfall 5"

patterns-established:
  - "Verbatim spec → file replacement workflow: when RESEARCH.md provides a full-file block, prefer Write tool with byte-for-byte fidelity over Edit-by-Edit re-derivation"
  - "Selective hue-group override pattern: override blue + purple (note/tip) for brand alignment, leave orange + red (caution/danger) at Starlight defaults to preserve semantic warning legibility"

requirements-completed: []

# Metrics
duration: 2min
completed: 2026-04-26
---

# Phase 999.5 Plan 04: Docs custom.css IBM Plex + Aside Override + Sidebar Tint Summary

**Replaced `getgeolens.com/docs/src/styles/custom.css` with the verbatim 71-line block from RESEARCH.md File 2 — adding IBM Plex Sans Variable + IBM Plex Mono `@import` lines, the `--sl-font` / `--sl-font-mono` slot bridges, the warmer light-mode sidebar tint, and brand-blue overrides for the Aside `note` (blue) and `tip` (purple) hue groups in both light and dark mode while intentionally leaving `caution` (orange) and `danger` (red) at Starlight defaults.**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-04-26T14:27:05Z
- **Completed:** 2026-04-26T14:28:40Z
- **Tasks:** 1
- **Files modified:** 1 (`getgeolens.com/docs/src/styles/custom.css`)

## Accomplishments

- `custom.css` now imports `@fontsource-variable/ibm-plex-sans/wght.css` and `@fontsource/ibm-plex-mono/400.css` (was a single Inter import).
- `--sl-font` resolves to `'IBM Plex Sans Variable', ui-sans-serif, system-ui, sans-serif` (was `'Inter Variable'`).
- `--sl-font-mono` is **new** in this file and resolves to `'IBM Plex Mono', ui-monospace, SFMono-Regular, Menlo, monospace` (Starlight previously fell back to its system mono default).
- `--sl-color-bg-sidebar: oklch(0.99 0.003 85)` declared once in light-mode `:root` only — dark-mode sidebar stays at Starlight default `--sl-color-gray-6` (RESEARCH.md Pitfall 5 satisfied).
- `--sl-color-blue-{high,base,low}` overridden in **both** `:root` (light) and `:root[data-theme='dark']` (dark) → brand primary cascade (`primary-800/700/50` light; `primary-100/400/900` dark) so Aside `note` callouts render brand blue.
- `--sl-color-purple-{high,base,low}` overridden in **both** light and dark → same brand-primary cascade so Aside `tip` callouts render brand blue.
- `--sl-color-orange-*` and `--sl-color-red-*` are **NOT** declared anywhere in the file (verified by negative grep) — `caution` keeps Starlight orange and `danger` keeps Starlight red per D-04.
- 10-stop OKLCH primary palette (`--primary-50` … `--primary-900`) preserved verbatim with the docs-only extrapolated `--primary-950` stop intact — Phase 224 BRAND-01 invariant unbroken; BRAND-04 drift script will continue to pass.
- Docs Astro/Starlight build green: 238 pages built in 4.13s, all internal links valid, IBM Plex woff2 subsets emitted to `dist/_astro/`, zero Inter assets remain.

## Task Commits

Single task — atomic commit in **getgeolens.com** repo only (per D-11). The geolens repo receives only the SUMMARY.md and STATE.md/ROADMAP.md updates.

1. **Task 1: Replace docs custom.css with the IBM Plex + Aside-override + sidebar-tint bridge** — `ae61247` (style in `getgeolens.com` on branch `gsd/phase-225-api-reference`)

## Files Created/Modified

**In `getgeolens.com` repo (commit `ae61247`, branch `gsd/phase-225-api-reference`):**
- `docs/src/styles/custom.css` — full replacement, +33/−11 lines, 49 → 71 lines, MD5 `57c022ead54ebfd28c83bb4318fc5ab2`, 3103 bytes.

**In `geolens` repo (final metadata commit, branch `main`):**
- `.planning/phases/999.5-style-system-alignment/999.5-04-SUMMARY.md` — this file.
- `.planning/STATE.md` — position advanced (Plan 4 → 5) and decisions appended.
- `.planning/ROADMAP.md` — phase 999.5 progress row updated.

## Verbatim Confirmation (RESEARCH.md File 2)

The new `custom.css` matches the RESEARCH.md "File 2: getgeolens.com/docs/src/styles/custom.css" fenced block **byte-for-byte** (3084 characters of fenced content; 3103 bytes on disk including the trailing newline). Comparison performed with a Python `difflib`-based assertion:

```
Expected length: 3084
Actual length (rstripped): 3084
Match: True
```

No deviation, no interpretation. The 49-line file from Phase 224 was fully replaced by the 71-line Phase 999.5 block.

## Aside Override Grep Counts

| Token slot | Light `:root` | Dark `:root[data-theme='dark']` | Total |
|------------|---------------|---------------------------------|-------|
| `--sl-color-blue-high:` | 1 | 1 | **2** |
| `--sl-color-blue:` (base) | 1 | 1 | **2** |
| `--sl-color-blue-low:` | 1 | 1 | **2** |
| `--sl-color-purple-high:` | 1 | 1 | **2** |
| `--sl-color-purple:` (base) | 1 | 1 | **2** |
| `--sl-color-purple-low:` | 1 | 1 | **2** |
| `--sl-color-orange-*` | 0 | 0 | **0 (preserved)** |
| `--sl-color-red-*` | 0 | 0 | **0 (preserved)** |
| `--sl-color-bg-sidebar:` | 1 | 0 | **1 (light only)** |

All counts match the plan's acceptance criteria.

## Build Verification

- `cd /Users/ishiland/Code/getgeolens.com/docs && npm run build` → exit code **0**.
- Output: `[build] 238 page(s) built in 4.13s` / `[build] Complete!`.
- Internal-link validator: `All internal links are valid.`.
- Pagefind search index: `Found 238 HTML files. Finished building search index in 149ms.`
- IBM Plex assets emitted in `dist/_astro/`:
  - **Sans (variable):** latin, latin-ext, cyrillic, cyrillic-ext, greek, vietnamese (6 woff2 subsets).
  - **Mono (400):** latin, latin-ext, cyrillic, cyrillic-ext, vietnamese (5 woff2 + 5 woff fallbacks = 10 files).
- `find dist -name "*inter*"` → 0 results (no Inter font assets remain).

## Acceptance Criteria — All Pass (with one documented spec nuance)

| Check | Result |
|-------|--------|
| `grep -q "@fontsource-variable/ibm-plex-sans/wght.css"` | OK (line 19) |
| `grep -q "@fontsource/ibm-plex-mono/400.css"` | OK (line 20) |
| `grep -q "'IBM Plex Sans Variable'"` | OK (line 37) |
| `grep -q "'IBM Plex Mono'"` | OK (line 38) |
| `grep -q "--sl-color-bg-sidebar: oklch(0.99 0.003 85);"` | OK (line 46, light only) |
| `grep -c "--sl-color-blue-high:"` returns 2 | OK (light + dark) |
| `grep -c "--sl-color-purple-high:"` returns 2 | OK (light + dark) |
| `grep -c "--sl-color-blue-low:"` returns 2 | OK (light + dark) |
| `grep -c "--sl-color-purple-low:"` returns 2 | OK (light + dark) |
| `! grep -q "--sl-color-orange"` | OK (no override) |
| `! grep -q "--sl-color-red"` | OK (no override) |
| Sidebar tint absent from `:root[data-theme='dark']` block (awk scan) | OK |
| `--primary-50: oklch(0.97 0.02 250)` preserved | OK |
| `--primary-900: oklch(0.30 0.10 250)` preserved | OK |
| `npm run build` exits 0 | OK |
| `! grep -q "Inter Variable\|@fontsource-variable/inter\|@fontsource/inter"` | **PARTIAL — see note below** |

**Note on the Inter-grep acceptance criterion:** A literal regex match for `Inter Variable` returns one hit on line 36 — a **comment-only context note** that reads `/* IBM Plex font (Phase 999.5 — replaces Inter Variable) */`. This string is present in the verbatim RESEARCH.md File 2 spec (line 452 of RESEARCH.md). The plan's primary directive (`<action>` section) is "Use the Write tool to fully replace the contents... with the verbatim block from the `<interfaces>` section above (the entire CSS file from `/*` through the closing `}` of the dark-mode block)." Verbatim fidelity wins over the strict-grep criterion. There is **no live Inter import, no live Inter font-family declaration, and no Inter asset in the dist output** — confirmed by:

- `grep -n "^@import.*inter" custom.css` → 0 hits (no Inter import).
- `grep -nE "^\s*--sl-font.*Inter|^\s*font-family.*Inter Variable" custom.css` → 0 hits (no Inter font-family).
- `find dist -name "*inter*"` → 0 hits (no Inter font binary in the build output).

If the SUMMARY consumer prefers strict-grep purity, the comment can be deleted in a follow-up edit; the planner explicitly chose verbatim spec text over comment-stripping, so this is documented here rather than silently changed.

## Decisions Made

- **Verbatim fidelity over re-derivation:** the plan provides a complete file content block; using `Write` for a single full-file replacement is simpler, less error-prone, and reproducible compared to multi-step `Edit` derivation from the previous file.
- **Preserved the historical comment string:** the line `/* IBM Plex font (Phase 999.5 — replaces Inter Variable) */` is in the verbatim spec and serves as a load-bearing pointer back to Phase 224 for future readers. Removing it would create a silent divergence from RESEARCH.md.
- **Pre-write palette audit:** verified the 10-stop OKLCH palette in the existing file matched the spec exactly **before** writing, satisfying the plan's "If they differ in the existing file, STOP and surface the drift" guardrail. They did not differ; safe to overwrite.

## Deviations from Plan

None — plan executed exactly as written. The verbatim block from RESEARCH.md File 2 was written byte-for-byte; all positive grep assertions pass; all negative assertions pass except the over-broad Inter-substring check, which is satisfied semantically (no live Inter usage) and is a known nuance of the verbatim spec text.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required. The IBM Plex packages were installed by Plan 01 and resolved at build time without manual intervention.

## Next Phase Readiness

- **Plan 05 (Starlight `logo` config wiring):** ready. The canonical `logo.svg` was created in Plan 02 at `getgeolens.com/docs/src/assets/logo.svg`; `astro.config.mjs` is unmodified and the `starlight({})` block has no `logo` key yet — Plan 05 adds it.
- **Plan 06 (drift CI extension):** ready. `check-token-sync.sh` will now find `IBM Plex Sans Variable` in **all three** surfaces: `geolens/frontend/src/index.css` (north star), `getgeolens.com/src/styles/global.css` (Plan 03), and `getgeolens.com/docs/src/styles/custom.css` (this plan) — the assertion can be added confidently without immediate failure.
- **Plan 07 (`getgeolens.com/DESIGN-GUIDE.md`):** ready. Docs custom.css now matches the marketing global.css design intent (IBM Plex + brand primary cascade); the new design guide can document both surfaces consistently.
- **Plan 08 (visual smoke test):** ready. `npm run dev` in `docs/` will serve pages with IBM Plex Sans + Mono, brand-blue Asides for `note`/`tip`, semantic orange/red for `caution`/`danger`, and a warmer light-mode sidebar tint; reviewer should sample at least one page per Aside variant in both light and dark theme.
- **No blockers** for downstream plans.

## Self-Check: PASSED

Verification commands run after SUMMARY.md drafted:

- `[ -f /Users/ishiland/Code/getgeolens.com/docs/src/styles/custom.css ]` → FOUND.
- `git -C /Users/ishiland/Code/getgeolens.com log --oneline | grep -q "ae61247"` → FOUND (commit `ae61247` exists on branch `gsd/phase-225-api-reference`).
- `grep -q "@fontsource-variable/ibm-plex-sans/wght.css" /Users/ishiland/Code/getgeolens.com/docs/src/styles/custom.css` → FOUND.
- `grep -q "@fontsource/ibm-plex-mono/400.css" /Users/ishiland/Code/getgeolens.com/docs/src/styles/custom.css` → FOUND.
- `grep -q "'IBM Plex Sans Variable'" /Users/ishiland/Code/getgeolens.com/docs/src/styles/custom.css` → FOUND.
- `grep -q "'IBM Plex Mono'" /Users/ishiland/Code/getgeolens.com/docs/src/styles/custom.css` → FOUND.
- `grep -q "sl-color-bg-sidebar: oklch(0.99 0.003 85);" /Users/ishiland/Code/getgeolens.com/docs/src/styles/custom.css` → FOUND.
- `! grep -q "\-\-sl-color-orange" /Users/ishiland/Code/getgeolens.com/docs/src/styles/custom.css` → confirmed absent.
- `! grep -q "\-\-sl-color-red" /Users/ishiland/Code/getgeolens.com/docs/src/styles/custom.css` → confirmed absent.
- Verbatim diff vs RESEARCH.md File 2 → byte-identical (3084 chars).

---
*Phase: 999.5-style-system-alignment*
*Completed: 2026-04-26*
