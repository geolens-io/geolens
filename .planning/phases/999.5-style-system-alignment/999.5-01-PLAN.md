---
phase: 999.5-style-system-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/ishiland/Code/getgeolens.com/package.json
  - /Users/ishiland/Code/getgeolens.com/package-lock.json
  - /Users/ishiland/Code/getgeolens.com/docs/package.json
  - /Users/ishiland/Code/getgeolens.com/docs/package-lock.json
autonomous: true
requirements: []
tags: [design-system, fonts, dependencies, cross-repo]

must_haves:
  truths:
    - "Marketing package.json declares @fontsource-variable/ibm-plex-sans@^5.2.8 (D-01)"
    - "Marketing package.json declares @fontsource/ibm-plex-mono@^5.2.7 (D-01)"
    - "Marketing package.json no longer declares @fontsource-variable/inter (D-01)"
    - "Docs package.json declares @fontsource-variable/ibm-plex-sans@^5.2.8 (D-01)"
    - "Docs package.json declares @fontsource/ibm-plex-mono@^5.2.7 (D-01)"
    - "Docs package.json no longer declares @fontsource-variable/inter (D-01)"
    - "node_modules in both repos contains the IBM Plex packages after npm install"
    - "Planning artifacts stay in geolens repo; implementation lands in getgeolens.com (D-11)"
  artifacts:
    - path: "/Users/ishiland/Code/getgeolens.com/package.json"
      provides: "Marketing IBM Plex deps + Inter removal"
      contains: "@fontsource-variable/ibm-plex-sans"
    - path: "/Users/ishiland/Code/getgeolens.com/docs/package.json"
      provides: "Docs IBM Plex deps + Inter removal"
      contains: "@fontsource-variable/ibm-plex-sans"
    - path: "/Users/ishiland/Code/getgeolens.com/node_modules/@fontsource-variable/ibm-plex-sans"
      provides: "Marketing IBM Plex Sans installed"
    - path: "/Users/ishiland/Code/getgeolens.com/docs/node_modules/@fontsource-variable/ibm-plex-sans"
      provides: "Docs IBM Plex Sans installed"
  key_links:
    - from: "package.json dependencies"
      to: "node_modules/@fontsource-variable/ibm-plex-sans/wght.css"
      via: "npm install"
      pattern: "wght.css"
---

<objective>
Add IBM Plex Sans Variable + IBM Plex Mono npm packages to both `getgeolens.com/package.json` and `getgeolens.com/docs/package.json`. Remove `@fontsource-variable/inter` from both. Audit `getgeolens.com/scripts/` for any `@fontsource/inter` usage before deciding whether to keep or replace the marketing-root `@fontsource/inter` devDependency. Run `npm install` in both directories so subsequent CSS swap plans can `@import` the new font CSS.

Purpose: Establish the dependency foundation for D-01 (Option B partial alignment — typography). All downstream CSS work depends on these packages being installed.

Output: Updated `package.json` + `package-lock.json` files in both `getgeolens.com` (root) and `getgeolens.com/docs/`, with IBM Plex packages installed in `node_modules`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ishiland/Code/geolens/.planning/PROJECT.md
@/Users/ishiland/Code/geolens/.planning/ROADMAP.md
@/Users/ishiland/Code/geolens/.planning/STATE.md
@/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-CONTEXT.md
@/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-RESEARCH.md

<interfaces>
<!-- Verbatim package version pins from RESEARCH.md (verified against npm registry 2026-04-26) -->
<!-- These are the EXACT version specifiers to add to package.json dependencies -->

```json
{
  "@fontsource-variable/ibm-plex-sans": "^5.2.8",
  "@fontsource/ibm-plex-mono": "^5.2.7"
}
```

CSS import paths used by the geolens app (matching syntax used in subsequent CSS plans):
```css
@import '@fontsource-variable/ibm-plex-sans/wght.css';
@import '@fontsource/ibm-plex-mono/400.css';
```

Packages to remove from both manifests:
- `@fontsource-variable/inter` (currently `^5.2.8` in both `getgeolens.com/package.json` dependencies and `getgeolens.com/docs/package.json` dependencies)
- `@fontsource/inter` (currently `^5.2.8` in `getgeolens.com/package.json` devDependencies — REMOVE ONLY IF audit shows no script uses it)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit getgeolens.com scripts for @fontsource/inter usage</name>
  <files>(read-only — produces audit notes for Task 2 decision)</files>
  <read_first>
    - /Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-RESEARCH.md (Pitfall 2 + Open Question 1)
    - /Users/ishiland/Code/getgeolens.com/package.json (current devDependencies)
    - /Users/ishiland/Code/getgeolens.com/scripts/ (full directory listing)
  </read_first>
  <action>
    Working directory: `/Users/ishiland/Code/getgeolens.com` (the marketing repo).

    Per RESEARCH.md Pitfall 2 and Open Question 1, before removing `@fontsource/inter` from the marketing devDependencies, audit the marketing scripts directory for any actual usage. Run:

    ```bash
    cd /Users/ishiland/Code/getgeolens.com
    grep -rn "fontsource/inter\|@fontsource-variable/inter\|fontsource-variable/inter" scripts/ 2>/dev/null || echo "NO_USAGE_FOUND"
    grep -rn "fontsource" scripts/ 2>/dev/null || echo "NO_FONTSOURCE_USAGE"
    ```

    Document the result in the task summary as one of two cases:

    **Case A — No Inter usage found:** `@fontsource/inter` devDependency can be safely removed in Task 2.

    **Case B — Inter usage found (e.g., satori OG generation):** Record the file(s) and line(s) using Inter. Per RESEARCH.md Pitfall 6, satori does not support variable fonts, so the script needs a non-variable replacement. The recommended replacement is `@fontsource/ibm-plex-sans@^5.2.8` (non-variable, static-weight). In Case B:
      - In Task 2, ALSO add `"@fontsource/ibm-plex-sans": "^5.2.8"` to marketing devDependencies (alongside the runtime `@fontsource-variable/ibm-plex-sans`)
      - Update the script(s) to import `@fontsource/ibm-plex-sans` (specific weight file, e.g. `400.css` or the package's font binary path) instead of Inter
      - The script-edit step is part of Task 2's action — keep this audit task read-only.

    Do NOT modify any files in this task. The next task acts on the findings.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com && grep -rn "fontsource" scripts/ 2>/dev/null; echo "audit_exit_code=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Grep command across `/Users/ishiland/Code/getgeolens.com/scripts/` ran successfully and produced output (either matches or "NO_FONTSOURCE_USAGE")
    - Audit findings recorded in commit message / task summary as either "Case A — no Inter usage" or "Case B — Inter used in {files}; will replace with @fontsource/ibm-plex-sans"
    - No source files modified by this task
  </acceptance_criteria>
  <done>
    Audit conclusion (Case A or Case B) is recorded and ready to inform Task 2's package.json edits and any necessary script update.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update getgeolens.com (marketing root) package.json + npm install</name>
  <files>
    /Users/ishiland/Code/getgeolens.com/package.json,
    /Users/ishiland/Code/getgeolens.com/package-lock.json
  </files>
  <read_first>
    - /Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-RESEARCH.md (Standard Stack section + File 8: getgeolens.com/package.json)
    - /Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-CONTEXT.md (D-01)
    - /Users/ishiland/Code/getgeolens.com/package.json (read CURRENT contents before editing)
    - Audit findings from Task 1 (Case A vs Case B)
  </read_first>
  <action>
    Working directory: `/Users/ishiland/Code/getgeolens.com` (the marketing repo). Per D-11, this task targets the `getgeolens.com` repo, NOT the geolens repo.

    Edit `/Users/ishiland/Code/getgeolens.com/package.json` per RESEARCH.md File 8:

    1. **Remove from `dependencies`:** `"@fontsource-variable/inter": "^5.2.8"` (exact match for the existing line)
    2. **Add to `dependencies`:** `"@fontsource-variable/ibm-plex-sans": "^5.2.8"`
    3. **Add to `dependencies`:** `"@fontsource/ibm-plex-mono": "^5.2.7"`
    4. **devDependencies (`@fontsource/inter`) handling:**
       - **Case A (Task 1 found no usage):** Remove `"@fontsource/inter": "^5.2.8"` from `devDependencies`.
       - **Case B (Task 1 found usage):** Keep removing `"@fontsource/inter"`, but ADD `"@fontsource/ibm-plex-sans": "^5.2.8"` to `devDependencies` for satori/OG-image script use, AND update the offending script file(s) to import from `@fontsource/ibm-plex-sans` instead of `@fontsource/inter`. Per RESEARCH.md Pitfall 6, satori requires non-variable fonts — `@fontsource/ibm-plex-sans` is the non-variable static-weight package (different from the runtime variable `@fontsource-variable/ibm-plex-sans`).

    Then run npm install to update `package-lock.json` and populate `node_modules`:

    ```bash
    cd /Users/ishiland/Code/getgeolens.com
    npm install
    ```

    Note (per D-11): The marketing repo's commit history is independent. Make a commit in `getgeolens.com` with a conventional-commit message such as `chore(deps): swap Inter -> IBM Plex Sans/Mono (Phase 999.5 D-01)`. Do NOT commit any files in the geolens repo from this task; planning-artifact commits in geolens are separate.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com && grep -q '"@fontsource-variable/ibm-plex-sans": "\^5\.2\.8"' package.json && grep -q '"@fontsource/ibm-plex-mono": "\^5\.2\.7"' package.json && ! grep -q '"@fontsource-variable/inter"' package.json && test -d node_modules/@fontsource-variable/ibm-plex-sans && test -d node_modules/@fontsource/ibm-plex-mono && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"@fontsource-variable/ibm-plex-sans": "\^5\.2\.8"' /Users/ishiland/Code/getgeolens.com/package.json` exits 0
    - `grep -q '"@fontsource/ibm-plex-mono": "\^5\.2\.7"' /Users/ishiland/Code/getgeolens.com/package.json` exits 0
    - `! grep -q '"@fontsource-variable/inter"' /Users/ishiland/Code/getgeolens.com/package.json` (Inter Variable removed)
    - `test -d /Users/ishiland/Code/getgeolens.com/node_modules/@fontsource-variable/ibm-plex-sans` exits 0
    - `test -d /Users/ishiland/Code/getgeolens.com/node_modules/@fontsource/ibm-plex-mono` exits 0
    - `test -f /Users/ishiland/Code/getgeolens.com/node_modules/@fontsource-variable/ibm-plex-sans/wght.css` exits 0 (CSS file Plan 03 will import)
    - If Task 1 returned Case B: any updated script file imports from `@fontsource/ibm-plex-sans` not `@fontsource/inter` — verify with `! grep -rn "fontsource/inter\|fontsource-variable/inter" /Users/ishiland/Code/getgeolens.com/scripts/`
  </acceptance_criteria>
  <done>
    Marketing root package.json reflects IBM Plex deps, Inter is removed, npm install completed, and the CSS file Plan 03 will import (`wght.css`) is present in node_modules.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update getgeolens.com/docs/package.json + npm install</name>
  <files>
    /Users/ishiland/Code/getgeolens.com/docs/package.json,
    /Users/ishiland/Code/getgeolens.com/docs/package-lock.json
  </files>
  <read_first>
    - /Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-RESEARCH.md (Standard Stack section + File 9: getgeolens.com/docs/package.json)
    - /Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-CONTEXT.md (D-01)
    - /Users/ishiland/Code/getgeolens.com/docs/package.json (read CURRENT contents before editing)
  </read_first>
  <action>
    Working directory: `/Users/ishiland/Code/getgeolens.com/docs` (the docs subtree). Per D-11, this task targets the `getgeolens.com` repo, NOT the geolens repo.

    Edit `/Users/ishiland/Code/getgeolens.com/docs/package.json` per RESEARCH.md File 9:

    1. **Remove from `dependencies`:** `"@fontsource-variable/inter": "^5.2.8"` (exact match for existing line)
    2. **Add to `dependencies`:** `"@fontsource-variable/ibm-plex-sans": "^5.2.8"`
    3. **Add to `dependencies`:** `"@fontsource/ibm-plex-mono": "^5.2.7"`

    The docs subtree does not have an OG image script that uses Inter, so no script audit is needed here.

    Then run npm install:

    ```bash
    cd /Users/ishiland/Code/getgeolens.com/docs
    npm install
    ```

    The commit for this task can be combined with Task 2's commit in the marketing repo (single conventional-commit covering both manifests + lockfiles), or split — operator's discretion. Suggested commit message: `chore(deps): swap Inter -> IBM Plex Sans/Mono in docs subtree (Phase 999.5 D-01)`.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com/docs && grep -q '"@fontsource-variable/ibm-plex-sans": "\^5\.2\.8"' package.json && grep -q '"@fontsource/ibm-plex-mono": "\^5\.2\.7"' package.json && ! grep -q '"@fontsource-variable/inter"' package.json && test -d node_modules/@fontsource-variable/ibm-plex-sans && test -d node_modules/@fontsource/ibm-plex-mono && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"@fontsource-variable/ibm-plex-sans": "\^5\.2\.8"' /Users/ishiland/Code/getgeolens.com/docs/package.json` exits 0
    - `grep -q '"@fontsource/ibm-plex-mono": "\^5\.2\.7"' /Users/ishiland/Code/getgeolens.com/docs/package.json` exits 0
    - `! grep -q '"@fontsource-variable/inter"' /Users/ishiland/Code/getgeolens.com/docs/package.json` exits 0
    - `test -d /Users/ishiland/Code/getgeolens.com/docs/node_modules/@fontsource-variable/ibm-plex-sans` exits 0
    - `test -d /Users/ishiland/Code/getgeolens.com/docs/node_modules/@fontsource/ibm-plex-mono` exits 0
    - `test -f /Users/ishiland/Code/getgeolens.com/docs/node_modules/@fontsource-variable/ibm-plex-sans/wght.css` exits 0 (file Plan 04 imports)
    - `test -f /Users/ishiland/Code/getgeolens.com/docs/node_modules/@fontsource/ibm-plex-mono/400.css` exits 0 (file Plan 04 imports)
  </acceptance_criteria>
  <done>
    Docs subtree package.json reflects IBM Plex deps, Inter is removed, npm install completed, and both font CSS files Plan 04 will import are present in `docs/node_modules`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. Font packages are downloaded from the npm registry over HTTPS (existing supply-chain trust boundary, unchanged from baseline).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.5-01 | Tampering | npm registry supply chain (font packages) | accept | `@fontsource-*` packages are widely used and not security-sensitive (no executable code path beyond CSS + font binaries). Existing `package-lock.json` integrity hashes pin the resolved tarballs; `npm install` verifies SRI hashes by default. ASVS L1 not applicable to font assets. |

No new threat surface — package dependency swap, no authentication, input validation, network-exposed endpoints, or data flow changes.
</threat_model>

<verification>
Run from the appropriate repo:

```bash
# Marketing root
cd /Users/ishiland/Code/getgeolens.com
grep '"@fontsource' package.json
test -d node_modules/@fontsource-variable/ibm-plex-sans
test -d node_modules/@fontsource/ibm-plex-mono
! test -d node_modules/@fontsource-variable/inter || echo "WARN: Inter dir still present (npm install may not have pruned)"

# Docs subtree
cd /Users/ishiland/Code/getgeolens.com/docs
grep '"@fontsource' package.json
test -d node_modules/@fontsource-variable/ibm-plex-sans
test -d node_modules/@fontsource/ibm-plex-mono
```

If any check fails, re-run `npm install` and re-verify.
</verification>

<success_criteria>
- Both `package.json` manifests declare IBM Plex Sans Variable + IBM Plex Mono with the exact version pins from RESEARCH.md (`^5.2.8` and `^5.2.7`).
- Neither `package.json` declares `@fontsource-variable/inter`.
- Both `node_modules` trees contain the IBM Plex packages and the specific CSS files (`wght.css`, `400.css`) that Plans 03 and 04 will `@import`.
- If marketing OG scripts used Inter (Case B), they have been migrated to `@fontsource/ibm-plex-sans` (non-variable static weight), satisfying RESEARCH.md Pitfall 6.
- A commit in the `getgeolens.com` repo records the dependency swap. No commits in the geolens repo from this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/999.5-style-system-alignment/999.5-01-SUMMARY.md` documenting:
- Task 1 audit result (Case A or Case B + file list if Case B)
- Marketing package.json before/after (which deps changed)
- Docs package.json before/after
- Commit hash(es) in `getgeolens.com` repo
- Any deviations from RESEARCH.md (e.g., script-edit decisions for OG generation)
</output>
