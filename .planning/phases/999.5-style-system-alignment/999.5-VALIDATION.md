---
phase: 999.5
slug: style-system-alignment
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-26
---

# Phase 999.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Astro check + shell drift CI + manual visual smoke |
| **Config file** | `docs/astro.config.mjs` (Astro), `docs/scripts/check-token-sync.sh` (drift CI) |
| **Quick run command** | `cd /Users/ishiland/Code/getgeolens.com/docs && bash scripts/check-token-sync.sh` |
| **Full suite command** | `cd /Users/ishiland/Code/getgeolens.com && npm run build && cd docs && npm run build && bash scripts/verify-build.sh` |
| **Estimated runtime** | ~30 seconds (drift CI ~2s; full build ~25s) |

---

## Sampling Rate

- **After every task commit:** Run `bash docs/scripts/check-token-sync.sh` (drift CI assertion)
- **After every plan wave:** Run `npm run build` in both marketing root and `docs/` subtree
- **Before `/gsd-verify-work`:** Full suite must be green + manual visual smoke checklist executed
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Decision Ref | Test Type | Automated Command | File Exists | Status |
|---------|------|------|--------------|-----------|-------------------|-------------|--------|
| 999.5-01-* | 01 | 1 | D-03, D-04 (font deps) | unit (npm install) | `cd /Users/ishiland/Code/getgeolens.com && npm install` | ✅ | ⬜ pending |
| 999.5-02-* | 02 | 2 | D-03, D-06 (marketing CSS) | drift CI + build | `bash docs/scripts/check-token-sync.sh && npm run build` | ✅ (after extension) | ⬜ pending |
| 999.5-03-* | 03 | 2 | D-02, D-04 (docs Starlight) | drift CI + build | `bash check-token-sync.sh && npm run build` (in docs/) | ✅ | ⬜ pending |
| 999.5-04-* | 04 | 1 | D-05 (logo extraction) | file exists | `test -f getgeolens.com/public/logo.svg && test -f getgeolens.com/docs/src/assets/logo.svg` | ❌ W0 | ⬜ pending |
| 999.5-05-* | 05 | 2 | D-05 (Starlight logo wiring) | astro check | `npx astro check` (in docs/) | ✅ | ⬜ pending |
| 999.5-06-* | 06 | 3 | D-03 (drift CI extension) | self-test | `bash check-token-sync.sh` exit 0 with all three surfaces aligned | ✅ | ⬜ pending |
| 999.5-07-* | 07 | 3 | D-07, D-08 (DESIGN-GUIDE) | grep | `test -f getgeolens.com/DESIGN-GUIDE.md && grep -q "## " getgeolens.com/DESIGN-GUIDE.md` | ❌ W0 | ⬜ pending |
| 999.5-08-* | 08 | 4 | all (visual smoke) | manual | dev server visual checklist | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*Plan IDs above are illustrative — actual plan numbering decided by gsd-planner.*

---

## Wave 0 Requirements

This phase has minimal Wave 0 — most validation reuses existing infrastructure (drift CI script, Astro build).

- [ ] Logo asset files created: `getgeolens.com/public/logo.svg` + `getgeolens.com/docs/src/assets/logo.svg`
- [ ] New marketing-root file scaffolded: `getgeolens.com/DESIGN-GUIDE.md` (empty until plan 07 fills it)
- [ ] Drift CI script extension verified to exit 0 on the pre-change baseline (so failures during the phase signal real drift, not script bugs)

*No test framework install required — validation runs through existing shell script + Astro build.*

---

## Manual-Only Verifications

| Behavior | Decision Ref | Why Manual | Test Instructions |
|----------|--------------|------------|-------------------|
| Marketing site renders IBM Plex Sans Variable | D-01 (Option B typography) | Font rendering can't be unit-tested | `cd getgeolens.com && npm run dev`, open browser, DevTools → Computed → font-family on `<body>` shows `"IBM Plex Sans Variable"`; Network tab shows `wght.css` loaded |
| Marketing site renders IBM Plex Mono in code blocks | D-01 | Font rendering | DevTools → inspect any `<code>` element → font-family shows `"IBM Plex Mono"` |
| Docs site renders IBM Plex Sans Variable | D-01 | Font rendering | `cd getgeolens.com/docs && npm run dev`, DevTools as above |
| Docs Aside `:::tip` renders brand blue (not purple) | D-04 | Visual confirmation | Open any docs page with a `:::tip` callout; border + icon should be brand blue |
| Docs Aside `:::note` renders brand blue | D-04 | Visual confirmation | Inspect page with `:::note` |
| Docs Aside `:::caution` still renders orange | D-04 (semantic preservation) | Confirm overrides did NOT bleed into orange/red slots | Inspect page with `:::caution`; border should remain Starlight orange |
| Docs Aside `:::danger` still renders red | D-04 | Same | Inspect page with `:::danger`; border should remain Starlight red |
| Docs sidebar shows warm tint (light mode) | D-02 | Visual | Light mode + scroll position visible — sidebar background is subtly warmer than pure white |
| Docs sidebar stays dark (dark mode) | D-02 | Visual confirmation that `:root` override doesn't leak into `.dark` | Toggle dark mode — sidebar must NOT show the warm tint |
| Docs nav shows GeoLens logo (not text "GeoLens Docs") | D-05 | Visual confirmation of Starlight logo wiring | Inspect docs site nav header — logo SVG renders, alt text "GeoLens" |
| Marketing dark-mode comment rewritten to rationale format | D-06 | Comment content review | `head -20 getgeolens.com/src/styles/global.css` — comment matches "Intentional: ... Rationale: ... Revisit if: ..." pattern |

---

## Validation Sign-Off

- [ ] All tasks have automated verify (drift CI / build / file-exists) OR Wave 0 dependencies
- [ ] Sampling continuity: drift CI runs after every CSS commit (no 3 consecutive tasks without automated verify)
- [ ] Wave 0 covers all MISSING references (logo SVGs, DESIGN-GUIDE.md scaffold)
- [ ] No watch-mode flags (drift CI is single-shot; build is single-shot)
- [ ] Feedback latency < 30s
- [ ] Manual visual smoke checklist executed before `/gsd-verify-work`
- [ ] `nyquist_compliant: true` set in frontmatter once executor verifies all per-task entries are green

**Approval:** pending
