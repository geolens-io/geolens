---
phase: 999.5-style-system-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/ishiland/Code/getgeolens.com/public/logo.svg
  - /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg
autonomous: true
requirements: []
tags: [design-system, logo, assets, cross-repo]

must_haves:
  truths:
    - "Canonical currentColor logo SVG exists at getgeolens.com/public/logo.svg (D-05)"
    - "Same canonical SVG copied to getgeolens.com/docs/src/assets/logo.svg for Astro asset pipeline (D-05, RESEARCH.md Pitfall 3)"
    - "Both files use stroke=\"currentColor\" not hardcoded #334155 (RESEARCH.md Logo Extraction Findings)"
    - "Both files use the canonical viewBox 0 0 64 64 with circle cx=27 cy=27 r=22 (RESEARCH.md)"
    - "geolens/docs/DESIGN-GUIDE.md is NOT modified (D-09)"
    - "Implementation lands in getgeolens.com repo per D-11"
  artifacts:
    - path: "/Users/ishiland/Code/getgeolens.com/public/logo.svg"
      provides: "Marketing-served canonical logo SVG"
      contains: "currentColor"
    - path: "/Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg"
      provides: "Astro-pipeline copy of canonical logo for Starlight logo.src wiring (Plan 05)"
      contains: "currentColor"
  key_links:
    - from: "/Users/ishiland/Code/getgeolens.com/public/logo.svg"
      to: "/Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg"
      via: "byte-for-byte copy"
      pattern: "<svg.*viewBox=\"0 0 64 64\""
---

<objective>
Create the canonical `currentColor` GeoLens logo SVG at two file locations: `getgeolens.com/public/logo.svg` (served at marketing root for any future `<img src="/logo.svg">` use) and `getgeolens.com/docs/src/assets/logo.svg` (Astro asset pipeline location for Starlight's `logo.src` config in Plan 05). Per RESEARCH.md, this is a NEW currentColor file authored from the existing favicon geometry (`r=22, cx/cy 27`), not an extraction of the React `GeoLensLogo.tsx` component (which has different proportions).

Purpose: D-05 — provide a single shared logo SVG that the docs Starlight nav can consume. Splitting from extraction wiring in Plan 05 lets logo asset creation run in parallel with Wave 1 dependency installs.

Output: Two byte-identical SVG files, each ~10 lines, containing the canonical GeoLens logomark using `stroke="currentColor"` so the SVG inherits its color from CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ishiland/Code/geolens/.planning/PROJECT.md
@/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-CONTEXT.md
@/Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-RESEARCH.md

<interfaces>
<!-- Verbatim canonical SVG content from RESEARCH.md "Logo Extraction Findings" -->
<!-- This SVG MUST be written byte-for-byte identical to BOTH file paths -->

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" fill="none" aria-hidden="true">
  <defs>
    <clipPath id="geolens-logo-clip">
      <circle cx="27" cy="27" r="22"/>
    </clipPath>
  </defs>
  <circle cx="27" cy="27" r="22" stroke="currentColor" stroke-width="3"/>
  <g clip-path="url(#geolens-logo-clip)" stroke="currentColor" stroke-width="2" opacity="0.55">
    <line x1="7" y1="27" x2="47" y2="27"/>
    <ellipse cx="27" cy="27" rx="11" ry="22"/>
  </g>
  <line x1="41" y1="41" x2="55" y2="55" stroke="currentColor" stroke-width="4.5" stroke-linecap="round"/>
</svg>
```

Geometry rationale (from RESEARCH.md):
- ViewBox 0 0 64 64 + circle r=22 at cx/cy 27 matches the existing favicon proportions (already used by marketing nav inline SVG and both repo favicon.svg files)
- `stroke="currentColor"` is the only deviation from the existing favicon files (which hardcode `#334155`); this lets Starlight inherit the nav text color
- Does NOT match the app `GeoLensLogo.tsx` React component (r=18, cx/cy 26, more graticule detail) — the React component is intentionally different geometry tuned for in-app sizing; canonicalizing on the simpler favicon variant per RESEARCH.md recommendation

DO NOT touch the existing `getgeolens.com/public/favicon.svg` or `getgeolens.com/docs/public/favicon.svg` — they keep their hardcoded `#334155` for browser favicon use.

DO NOT touch `geolens/frontend/src/components/GeoLensLogo.tsx` — the app's React component remains independent (per D-09 spirit; the app is the unmodified north star).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canonical logo SVG at getgeolens.com/public/logo.svg</name>
  <files>/Users/ishiland/Code/getgeolens.com/public/logo.svg</files>
  <read_first>
    - /Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-RESEARCH.md (Logo Extraction Findings + Recommended Approach for D-05)
    - /Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-CONTEXT.md (D-05)
    - /Users/ishiland/Code/getgeolens.com/public/favicon.svg (the geometry reference — but DO NOT modify it)
  </read_first>
  <action>
    Working directory: `/Users/ishiland/Code/getgeolens.com` (the marketing repo). Per D-11, this task targets the `getgeolens.com` repo.

    Create new file `/Users/ishiland/Code/getgeolens.com/public/logo.svg` with EXACTLY the following content (verbatim from RESEARCH.md Logo Extraction Findings — Recommended Approach for D-05):

    ```svg
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" fill="none" aria-hidden="true">
      <defs>
        <clipPath id="geolens-logo-clip">
          <circle cx="27" cy="27" r="22"/>
        </clipPath>
      </defs>
      <circle cx="27" cy="27" r="22" stroke="currentColor" stroke-width="3"/>
      <g clip-path="url(#geolens-logo-clip)" stroke="currentColor" stroke-width="2" opacity="0.55">
        <line x1="7" y1="27" x2="47" y2="27"/>
        <ellipse cx="27" cy="27" rx="11" ry="22"/>
      </g>
      <line x1="41" y1="41" x2="55" y2="55" stroke="currentColor" stroke-width="4.5" stroke-linecap="round"/>
    </svg>
    ```

    DO NOT modify `getgeolens.com/public/favicon.svg` (existing file with hardcoded `#334155` — favicon.svg keeps its hardcoded color so favicons render predictably in browser chrome).

    DO NOT modify `getgeolens.com/src/components/layout/Nav.astro` (its inline SVG already uses `currentColor` and renders correctly — RESEARCH.md Open Question 2 confirms leaving it alone is the right call).

    Commit in the `getgeolens.com` repo with a conventional-commit message such as `feat(brand): add canonical currentColor logo.svg (Phase 999.5 D-05)`. Per D-11, no commit in the geolens repo from this task.
  </action>
  <verify>
    <automated>test -f /Users/ishiland/Code/getgeolens.com/public/logo.svg && grep -q 'stroke="currentColor"' /Users/ishiland/Code/getgeolens.com/public/logo.svg && grep -q 'viewBox="0 0 64 64"' /Users/ishiland/Code/getgeolens.com/public/logo.svg && grep -q 'cx="27" cy="27" r="22"' /Users/ishiland/Code/getgeolens.com/public/logo.svg && ! grep -q '#334155' /Users/ishiland/Code/getgeolens.com/public/logo.svg && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /Users/ishiland/Code/getgeolens.com/public/logo.svg` exits 0
    - `grep -q 'stroke="currentColor"' /Users/ishiland/Code/getgeolens.com/public/logo.svg` exits 0 (3 occurrences expected: outer circle, graticule group, handle line)
    - `grep -q 'viewBox="0 0 64 64"' /Users/ishiland/Code/getgeolens.com/public/logo.svg` exits 0
    - `grep -q 'cx="27" cy="27" r="22"' /Users/ishiland/Code/getgeolens.com/public/logo.svg` exits 0 (2 occurrences expected: clipPath circle + visible circle)
    - `! grep -q '#334155' /Users/ishiland/Code/getgeolens.com/public/logo.svg` (no hardcoded slate-700 stroke)
    - `! grep -q 'fill="#' /Users/ishiland/Code/getgeolens.com/public/logo.svg` (no hardcoded fill colors)
    - `grep -q 'aria-hidden="true"' /Users/ishiland/Code/getgeolens.com/public/logo.svg` exits 0 (decorative SVG; alt text comes from Starlight `logo.alt` config)
    - `getgeolens.com/public/favicon.svg` is unchanged (verify with `git status` showing no modification — favicon stays at hardcoded color)
  </acceptance_criteria>
  <done>
    Canonical marketing-root logo SVG exists at `/Users/ishiland/Code/getgeolens.com/public/logo.svg` with currentColor strokes, the favicon is untouched, and the file is committed in the marketing repo.
  </done>
</task>

<task type="auto">
  <name>Task 2: Copy canonical logo SVG to getgeolens.com/docs/src/assets/logo.svg</name>
  <files>/Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg</files>
  <read_first>
    - /Users/ishiland/Code/geolens/.planning/phases/999.5-style-system-alignment/999.5-RESEARCH.md (Starlight Logo Wiring section + Pitfall 3: Starlight Logo Path Resolution)
    - /Users/ishiland/Code/getgeolens.com/public/logo.svg (Task 1 output — source of truth)
  </read_first>
  <action>
    Working directory: `/Users/ishiland/Code/getgeolens.com/docs` (the docs subtree). Per D-11, this task targets the `getgeolens.com` repo.

    Per RESEARCH.md "Starlight Logo Wiring" + Pitfall 3: Astro/Vite cannot import asset files from outside the docs/ project root, so the canonical SVG must also live at a docs-internal path. The docs subtree's asset pipeline expects `src/assets/`.

    1. Confirm `getgeolens.com/docs/src/assets/` exists (create with `mkdir -p` if not):
       ```bash
       mkdir -p /Users/ishiland/Code/getgeolens.com/docs/src/assets
       ```

    2. Copy `getgeolens.com/public/logo.svg` byte-for-byte to `getgeolens.com/docs/src/assets/logo.svg`:
       ```bash
       cp /Users/ishiland/Code/getgeolens.com/public/logo.svg /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg
       ```

    3. Verify byte equality with `cmp` (must exit 0):
       ```bash
       cmp /Users/ishiland/Code/getgeolens.com/public/logo.svg /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg
       ```

    Note: This creates a duplicated asset (one file in `public/`, one in `docs/src/assets/`). Plan 07's DESIGN-GUIDE.md will document the duplication and the maintenance convention (any update must apply to both files). Per RESEARCH.md, this is the safe path that avoids cross-project-root Vite resolution errors.

    Commit in `getgeolens.com` repo (can be combined with Task 1 commit). Suggested message: `feat(docs): mirror canonical logo.svg into docs asset pipeline (Phase 999.5 D-05)`.
  </action>
  <verify>
    <automated>test -f /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg && cmp /Users/ishiland/Code/getgeolens.com/public/logo.svg /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg && grep -q 'stroke="currentColor"' /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg` exits 0
    - `cmp /Users/ishiland/Code/getgeolens.com/public/logo.svg /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg` exits 0 (byte-identical)
    - `grep -q 'stroke="currentColor"' /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg` exits 0
    - `grep -q 'viewBox="0 0 64 64"' /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg` exits 0
    - `! grep -q '#334155' /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg`
  </acceptance_criteria>
  <done>
    Docs asset copy of the canonical logo exists at `/Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg` and is byte-identical to the marketing-root copy. Plan 05 can now wire it into Starlight's `logo.src` config without cross-project-root Vite issues.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. SVG is a static asset served from CDN with no JavaScript execution context.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.5-02 | Tampering | Static SVG asset (logo.svg) | accept | SVG contains only declarative geometry (circles, lines, paths) and `currentColor` — no embedded `<script>`, `<foreignObject>`, or external references. Served from same-origin static path; not user-uploaded. ASVS L1 V14 (data integrity) satisfied by repo-controlled checked-in asset. |

No new threat surface — static SVG file authored from declarative geometry, no authentication, input validation, network-exposed endpoints, or data flow changes.
</threat_model>

<verification>
```bash
# Both files exist with currentColor (not hardcoded color)
test -f /Users/ishiland/Code/getgeolens.com/public/logo.svg
test -f /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg

# Byte-identical
cmp /Users/ishiland/Code/getgeolens.com/public/logo.svg \
    /Users/ishiland/Code/getgeolens.com/docs/src/assets/logo.svg

# Geometry + color sanity
grep -c 'stroke="currentColor"' /Users/ishiland/Code/getgeolens.com/public/logo.svg | grep -v '^#' # expect 3

# Favicon untouched
git -C /Users/ishiland/Code/getgeolens.com diff --name-only public/favicon.svg
# Expect empty output
```
</verification>

<success_criteria>
- Both file paths exist and are byte-identical.
- Both use `stroke="currentColor"` consistently (no hardcoded color values).
- ViewBox and circle geometry match the canonical (`viewBox="0 0 64 64"`, `cx="27" cy="27" r="22"`).
- `getgeolens.com/public/favicon.svg` and `getgeolens.com/docs/public/favicon.svg` remain unchanged.
- A commit in `getgeolens.com` records the new SVG file(s). No commits in geolens repo from this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/999.5-style-system-alignment/999.5-02-SUMMARY.md` documenting:
- The two file paths created and their byte size
- Confirmation that the SVGs are byte-identical
- Confirmation that the favicon files were not touched
- Commit hash(es) in `getgeolens.com` repo
</output>
