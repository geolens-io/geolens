# Phase 1103 Plan: Builder Mixed Layer Reorder

**Requirements:** BUILDER-01, BUILDER-02

## Tasks

- Fix `handleReorder` to use one reindexed layer array for React state and immediate MapLibre reorder.
- Add a focused `syncLayersToMap` regression for vector-over-raster ordering.
- Correct the ADK compose script so saved maps canonicalize vectors above raster layers.
- Verify through tests and Playwright drag/save/reload.
