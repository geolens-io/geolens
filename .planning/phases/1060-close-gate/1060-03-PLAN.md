---
phase: 1060-close-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/1060-close-gate/1060-CLEAN-LOG.md
autonomous: true
requirements:
  - CLEAN-01

must_haves:
  truths:
    - "Dataset `ec18b546-d86d-4375-8e1f-8564b6a75687` (reupload sandbox) is no longer present in the localhost:8080 catalog after DELETE returns 2xx"
    - "Dataset `54763119-0cf4-448e-a950-81551d090267` (AGO Wildfire) is no longer present in the localhost:8080 catalog after DELETE returns 2xx"
    - "Dataset `667a6c65-cdbc-4158-87f2-21a7e791ba7c` (OGC API Large Lakes) is no longer present in the localhost:8080 catalog after DELETE returns 2xx"
    - "Each DELETE call's HTTP status + final GET 404 confirmation are captured in the clean log file"
  artifacts:
    - path: ".planning/phases/1060-close-gate/1060-CLEAN-LOG.md"
      provides: "Authoritative log of 3 DELETE calls + post-delete GET 404 verification per CLEAN-01"
      min_lines: 25
      contains: "ec18b546-d86d-4375-8e1f-8564b6a75687"
  key_links:
    - from: "DELETE /api/datasets/{id}"
      to: "GET /api/datasets/{id} → 404"
      via: "post-delete verification via HTTP curl"
      pattern: "HTTP/.*404"
    - from: "Admin auth"
      to: "DELETE call Authorization header"
      via: "POST /api/auth/login → Bearer token"
      pattern: "Authorization: Bearer"
---

<objective>
Execute the 3 DELETE calls against the live `localhost:8080` catalog to remove the v1012 smoke repro datasets (CLEAN-01). Per A-03 user decision: Claude executes the deletes against the live API (admin auth), then verifies each by GET → 404. The clean log file records per-dataset HTTP status for Plan 04 ingestion.

Purpose: ROADMAP success criterion #1 — "The 3 v1012 smoke repro datasets are deleted from the `localhost:8080` catalog".

Output: `.planning/phases/1060-close-gate/1060-CLEAN-LOG.md` — per-dataset DELETE result + GET-404 verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1060-close-gate/1060-CONTEXT.md
@.planning/phases/1060-close-gate/1060-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Authenticate against admin and confirm token works</name>
  <files>.planning/phases/1060-close-gate/1060-CLEAN-LOG.md</files>
  <read_first>
    .planning/phases/1060-close-gate/1060-SUMMARY.md (lines 117-132 — the existing curl recipe for DELETE + the admin-credentials recipe; note the auth endpoint is `/api/auth/login` and may or may not require a trailing slash)
    Project memory MEMORY.md "Credentials" section — `GEOLENS_ADMIN_USERNAME` and `GEOLENS_ADMIN_PASSWORD` env vars; default `admin` / `admin` for local dev
    Project memory MEMORY.md "Known Issues" section — FastAPI trailing slash redirects: `/auth/login` may behave differently than `/auth/login/`. Test both.
  </read_first>
  <action>
    Authenticate, verify the token works, then create the clean log scaffold.

    Step 1 — Stack health gate (blocking): run `docker compose ps`. All services must show `(healthy)`. If unhealthy, document and stop. Do NOT autonomously bring the stack up.

    Step 2 — Obtain admin token via `curl -s -X POST http://localhost:8080/api/auth/login -H 'Content-Type: application/x-www-form-urlencoded' -d 'username=admin&password=admin' | jq -r .access_token` and capture into shell variable `TOKEN`. If TOKEN is null or empty, the login failed — log the response body verbatim and stop. Notes: the frontend proxy is at port 8080; if local config maps the API at port 8001 instead, use 8001 (confirm via `docker compose ps` port mapping). If `/api/auth/login` returns 307, retry with `/api/auth/login/` (with trailing slash) — pick whichever returns 200 with a JSON body containing `access_token`.

    Step 3 — Verify token works via `curl -s -H "Authorization: Bearer $TOKEN" http://localhost:8080/api/datasets/ec18b546-d86d-4375-8e1f-8564b6a75687 -o /dev/null -w "%{http_code}\n"`. Expected: 200 (dataset still present) or 404 (already deleted from prior session). If 401, the token didn't bind correctly — log and stop. If 404, note that Task 2 will still attempt all 3 deletes idempotently (a DELETE on an already-deleted dataset typically returns 404 which is acceptable).

    Step 4 — Write the clean log scaffold at `.planning/phases/1060-close-gate/1060-CLEAN-LOG.md` using the Write tool. Required frontmatter fields: `phase: 1060-close-gate`, `artifact: clean-log`, `created: <ISO-8601 timestamp>`, `status: pending`, `deletes_total: 3`, `deletes_succeeded: 0`, `deletes_failed: 0`. Body sections in order: `# Phase 1060: Dataset Cleanup Log (CLEAN-01)`; `**Purpose:** ROADMAP success criterion #1 — delete the 3 v1012 smoke repro datasets from localhost:8080.`; `**Stack health (at cleanup start):** <fill>`; `**Auth status:** token obtained at <timestamp>`; `## Targets` markdown table with columns `Dataset ID | Description | Pre-delete GET | DELETE status | Post-delete GET | Verdict` and 3 PENDING rows (in this exact order):
    - `ec18b546-d86d-4375-8e1f-8564b6a75687` | reupload sandbox (v3 = 49 wildfire points) | PENDING | PENDING | PENDING | PENDING
    - `54763119-0cf4-448e-a950-81551d090267` | AGO Wildfire Response Points (49 features) | PENDING | PENDING | PENDING | PENDING
    - `667a6c65-cdbc-4158-87f2-21a7e791ba7c` | OGC API Large Lakes (25 features) | PENDING | PENDING | PENDING | PENDING

    After the Targets table add a `## Per-Dataset Detail` section with placeholder text "(Populated by Task 2)".

    Constraints: use `localhost:8080` per A-03 (the frontend proxy port). Do NOT use the internal `api:8000` Docker hostname. The token is short-lived (typically 15 min); if Task 2 takes too long and a DELETE returns 401, refresh the token mid-flow. Do NOT prompt for confirmation before deletes — A-03 explicitly authorized Claude to execute.
  </action>
  <acceptance_criteria>
    - Stack health captured from `docker compose ps`.
    - Admin token obtained and pre-flight GET on the first dataset confirms auth works (200 or 404, not 401/500).
    - `1060-CLEAN-LOG.md` scaffold exists with the documented frontmatter + 3 PENDING rows in the Targets table.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/1060-close-gate/1060-CLEAN-LOG.md && grep -c 'PENDING' .planning/phases/1060-close-gate/1060-CLEAN-LOG.md | awk '{exit ($1>=3)?0:1}'</automated>
  </verify>
  <done>Token works, log scaffold exists with 3 PENDING rows.</done>
</task>

<task type="auto">
  <name>Task 2: Execute 3 DELETEs + verify each by post-delete GET 404</name>
  <files>.planning/phases/1060-close-gate/1060-CLEAN-LOG.md</files>
  <read_first>
    backend/app/modules/catalog/datasets/api (grep for the DELETE handler signature in router files to know expected success status codes — 200, 202, and 204 are all acceptable; 404 is acceptable on second-run idempotency)
  </read_first>
  <action>
    For each of the 3 dataset IDs in the order listed in the Targets table, run the same 3-step pattern: pre-delete GET → DELETE → post-delete GET. Update the Targets row + add a Per-Dataset Detail subsection.

    For each dataset id `$ID`:

    Step 1 — Pre-delete GET (status only). Run `curl -s -H "Authorization: Bearer $TOKEN" http://localhost:8080/api/datasets/$ID -o /dev/null -w "%{http_code}"` and capture as `$PRE`. Expected: 200 (dataset still present) OR 404 (already deleted in prior session). Both are acceptable starting states.

    Step 2 — DELETE. Run `curl -s -X DELETE -H "Authorization: Bearer $TOKEN" http://localhost:8080/api/datasets/$ID -o /dev/null -w "%{http_code}"` and capture as `$DEL`. Expected: 200, 202, or 204 (any 2xx). 404 is acceptable if `$PRE` was 404. 401 means the token expired — re-auth from Task 1 Step 2 and retry once. Anything else is a failure — record `$DEL` verbatim and continue to Step 3 (still attempt the post-delete GET to capture the state).

    Step 3 — Post-delete GET (confirm gone). Run the same GET command as Step 1 and capture as `$POST`. Expected: 404. If 200, the delete didn't take effect — record as a failure.

    Step 4 — Compute Verdict:
    - PASS: `$DEL` is in the set `{200, 202, 204, 404}` AND `$POST == 404`.
    - FAIL: any other combination.

    Step 5 — Update log:
    - Update the Targets table row: fill in PRE / DEL / POST columns; set Verdict to PASS or FAIL.
    - Add a Per-Dataset Detail subsection with `### $ID — <description>` heading; then bullets for `Pre-delete GET: $PRE`, `DELETE: $DEL`, `Post-delete GET: $POST`, `Verdict: PASS|FAIL`, and optional `Notes:` (only if FAIL or if PRE was 404).
    - Update frontmatter counters (`deletes_succeeded`, `deletes_failed`) atomically as you go.

    After all 3 datasets processed, set the frontmatter `status` field:
    - `passed` if all 3 PASS
    - `partial` if 1-2 PASS (others FAIL); ROADMAP criterion #1 may still hold if the FAIL was due to `$PRE == 404` (data already gone)
    - `failed` if 0 PASS

    Constraints: process the 3 datasets in the order listed in `1060-CLEAN-LOG.md` (deterministic order makes the log easier to read). Do NOT batch DELETE in a single call — separate per-dataset HTTP calls make the per-row evidence specific. If a 5xx error returns on DELETE, do NOT retry more than once. Record the failure verbatim and continue to the next dataset. Do NOT touch any non-target datasets — sanity check by explicitly grepping your own log file for any UUID OTHER than the 3 named ones and ensuring none appear in DELETE-call lines.
  </action>
  <acceptance_criteria>
    - All 3 dataset rows in the Targets table have PRE / DEL / POST values populated (no PENDING).
    - All 3 rows show Verdict PASS or FAIL.
    - For PASS rows: post-delete GET is 404.
    - For FAIL rows: failure mode described with the raw HTTP status.
    - Frontmatter `status` set to `passed` / `partial` / `failed`.
    - Per-Dataset Detail subsections populated for all 3 IDs.
  </acceptance_criteria>
  <verify>
    <automated>grep -c -E '^\| `[a-f0-9-]{36}`.*\| (PASS|FAIL) \|$' .planning/phases/1060-close-gate/1060-CLEAN-LOG.md | awk '{exit ($1==3)?0:1}'</automated>
  </verify>
  <done>3 DELETEs executed, 3 post-delete GETs confirm 404 (or document fail mode); log status set.</done>
</task>

</tasks>

<verification>
- 3 DELETEs executed against `localhost:8080/api/datasets/<id>` with admin Bearer auth
- Each DELETE followed by a GET that returns 404 (or failure documented)
- `1060-CLEAN-LOG.md` records HTTP status per dataset + per-dataset detail block
- Frontmatter `deletes_total=3, deletes_pending=0` (success+fail sum to 3)
</verification>

<success_criteria>
ROADMAP success criterion #1 (3 datasets deleted) is satisfied; Plan 04 SUMMARY can cite the log file as the CLEAN-01 evidence.
</success_criteria>

<output>
The clean log AT `.planning/phases/1060-close-gate/1060-CLEAN-LOG.md` IS the deliverable; no separate per-plan SUMMARY file needed.
</output>
