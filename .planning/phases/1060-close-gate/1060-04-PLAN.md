---
phase: 1060-close-gate
plan: 04
type: execute
wave: 2
depends_on:
  - 1060-01
  - 1060-02
  - 1060-03
files_modified:
  - CHANGELOG.md
  - .planning/phases/1060-close-gate/1060-SUMMARY.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - CTRL-01

must_haves:
  truths:
    - "CHANGELOG.md `## [Unreleased]` heading is promoted to `## [1.3.0] - 2026-05-20` (the v1.3.0 public tag per A-01, NOT v1.4.0 from ROADMAP)"
    - "All 4 smoke gates pass at re-verify time: `typecheck` 0 errors, `vitest` green, `e2e:smoke:builder` green (post Plan 01), `test:i18n` 2/2"
    - "Local git tag `v1013` is created at HEAD (NOT pushed per A-04)"
    - "Public git tag `v1.3.0` is created at HEAD (NOT pushed per A-04)"
    - "`.planning/phases/1060-close-gate/1060-SUMMARY.md` frontmatter `status` field is `complete` (was `partial` from prior session)"
    - "`.planning/STATE.md` is updated: position reflects v1013 SHIPPED; milestone status complete; last shipped milestone updated"
    - "`.planning/REQUIREMENTS.md` traceability table marks CLEAN-01 and CTRL-01 as Complete"
    - "`.planning/ROADMAP.md` v1013 section marked shipped with the date; Phase 1060 checkbox `[x]`"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Promoted v1013 release block at [1.3.0] header"
      contains: "## [1.3.0] - 2026-05-20"
    - path: ".planning/phases/1060-close-gate/1060-SUMMARY.md"
      provides: "Final close summary with status: complete"
      contains: "status: complete"
    - path: ".planning/STATE.md"
      provides: "Updated milestone position post-ship"
      contains: "Last Shipped Milestone"
  key_links:
    - from: "git tag v1013"
      to: "current HEAD commit"
      via: "git tag (lightweight, follows v1010/v1011/v1012 local pattern)"
      pattern: "v1013"
    - from: "git tag v1.3.0"
      to: "current HEAD commit"
      via: "git tag -a (annotated for release-notes tooling)"
      pattern: "v1\\.3\\.0"
    - from: "CHANGELOG [1.3.0] block"
      to: "v1013 ROADMAP requirements"
      via: "10 v1013 REQ-ID mentions"
      pattern: "WFS-04|PROBE-05|CRS-06|CLASS-07|GPKG-0[1-3]|BSE-01|CLEAN-01|CTRL-01"
---

<objective>
Promote the CHANGELOG `[Unreleased]` block to `[1.3.0] - 2026-05-20`, re-run all smoke gates to confirm they remain green after Plan 01's e2e fixes, cut local tags `v1013` + `v1.3.0` (NOT pushed per A-04), and finalize the v1013 SUMMARY + STATE.md + REQUIREMENTS.md + ROADMAP.md tracker artifacts.

Purpose: ROADMAP success criterion #5 — CHANGELOG promoted, local v1013 tag created, public v1.3.0 tag created (per A-01 user decision the public tag is v1.3.0 — v1012 actually shipped as v1.2.1 so v1013's next minor is v1.3.0, not v1.4.0).

This is the actual "ship" step. It runs after Plans 01+02+03 succeed in Wave 1, ingests their log files for evidence, then writes the closing tracker state.

Output: Promoted CHANGELOG, finalized 1060-SUMMARY.md with status=complete, updated STATE.md/REQUIREMENTS.md/ROADMAP.md, 2 local git tags.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/1060-close-gate/1060-CONTEXT.md
@.planning/phases/1060-close-gate/1060-SUMMARY.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 1 dependency gate + re-run all smoke gates</name>
  <files></files>
  <read_first>
    .planning/phases/1060-close-gate/1060-SUMMARY.md (lines 36-44 — the prior session's smoke gate table: backend pytest 2713/2713, frontend vitest 2091/2091, typecheck 0, i18n 2/2 already passed; only e2e:smoke:builder was red)
    .planning/phases/1060-close-gate/1060-MCP-REVERIFY.md (Plan 02 output — confirm Wave 1 finished)
    .planning/phases/1060-close-gate/1060-CLEAN-LOG.md (Plan 03 output — confirm Wave 1 finished)
    .planning/phases/1060-close-gate/1060-01-SUMMARY.md (Plan 01 output — confirm e2e fixes landed)
    package.json (the `scripts` block — confirm exact smoke commands `test:i18n` and `e2e:smoke:builder`)
  </read_first>
  <action>
    Confirm Wave 1 deliverables exist with passing statuses, then re-run all smoke gates and capture per-gate results for ingestion in Task 4's SUMMARY rewrite.

    Wave 1 dependency check (blocking; HALT if any check fails):
    Read `.planning/phases/1060-close-gate/1060-MCP-REVERIFY.md` and confirm the YAML frontmatter `status` field is `passed` or `partial` AND that the P0 gates G-01 (WFS-04) and G-05 (GPKG-01) both have verdict PASS in the Gate Results table. Read `.planning/phases/1060-close-gate/1060-CLEAN-LOG.md` and confirm frontmatter `status: passed`. Read `.planning/phases/1060-close-gate/1060-01-SUMMARY.md` and confirm Plan 01 finished with `e2e:smoke:builder` exit 0. If ANY of these dependencies are missing OR have status `failed`, do NOT proceed — surface the blocking dependency to the user. Tagging against incomplete close-work is the failure mode this gate prevents.

    Re-run smoke gates in this exact order (each command's exit code must be 0):
    1. Frontend typecheck: run `npx tsc --noEmit` from `/Users/ishiland/Code/geolens/frontend`. Capture exit code, error count, wall-clock seconds.
    2. i18n parity: run `npm run test:i18n` from `/Users/ishiland/Code/geolens/frontend`. Capture exit code, pass count (expect 2/2), wall-clock seconds.
    3. Frontend vitest: run `npx vitest run --pool=forks` from `/Users/ishiland/Code/geolens/frontend`. Capture exit code, pass/fail/skip counts, total files, wall-clock seconds.
    4. e2e:smoke:builder: run `npm run e2e:smoke:builder` from `/Users/ishiland/Code/geolens`. Capture exit code, pass/fail/skip counts, wall-clock seconds.

    Backend pytest is OPTIONAL at this re-verify step. The prior session ran 2713/2713 PASS against an unchanged source tree, and Plan 01 only modifies e2e/*.spec.ts files (which aren't in the backend pytest suite). Add a one-line note that backend pytest re-run is SKIPPED for this reason — UNLESS Plan 01's 1060-01-SUMMARY.md records that it modified any backend Python file, in which case run `cd /Users/ishiland/Code/geolens/backend && uv run pytest -x` and capture the result.

    If ANY gate is red:
    - typecheck red: a genuine TypeScript error was introduced by Plan 01's source-touching changes. Surface the error verbatim, then either fix at the call site with the smallest scope possible OR revert the Plan 01 change that broke it. Re-run the gate.
    - vitest red: same protocol — fix the failing test or revert the offending change.
    - i18n red: locale parity broke (Plan 01 added a key in en but not de/es/fr, or removed a key from one locale only). Sync the missing keys across all 4 locales.
    - e2e:smoke:builder red: Plan 01's fix didn't actually take. Re-open Plan 01's task at the failing test and address.

    Do NOT proceed to Task 2 (CHANGELOG promote) until all 4 gates exit 0.

    Write a memo block at the bottom of `1060-SUMMARY.md` (under a `<!-- Plan 04 Task 1 memo -->` HTML comment marker) with the per-gate exit codes, counts, and wall-clock times. Task 4 will ingest this memo when rewriting the SUMMARY.

    Constraints: the smoke re-run is non-mutating — these commands only execute existing tests, they do not write source files. Source-file mutation begins at Task 2. Do NOT proceed to tag creation if any gate is red. A-02 explicitly requires "BOTH must be resolved before tag" — smoke green is the gate.
  </action>
  <acceptance_criteria>
    - Wave 1 deliverables (`1060-MCP-REVERIFY.md`, `1060-CLEAN-LOG.md`, `1060-01-SUMMARY.md`) exist with passing statuses; P0 gates G-01 + G-05 PASS.
    - `npx tsc --noEmit` exits 0 (frontend, 0 type errors).
    - `npm run test:i18n` exits 0 (2/2 PASS).
    - `npx vitest run --pool=forks` exits 0 (target ≥2091 PASS based on prior baseline).
    - `npm run e2e:smoke:builder` exits 0.
    - Per-gate exit codes + pass counts + wall-clock seconds captured in a memo block at the bottom of `1060-SUMMARY.md` under `<!-- Plan 04 Task 1 memo -->`.
    - If smoke is red, work halts (no tagging until green).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npx tsc --noEmit && npm run test:i18n && npx vitest run --pool=forks && cd /Users/ishiland/Code/geolens && npm run e2e:smoke:builder</automated>
  </verify>
  <done>All 4 smoke gates exit 0; Wave 1 outputs confirmed; per-gate memo block written into 1060-SUMMARY.md for Task 4 ingestion.</done>
</task>

<task type="auto">
  <name>Task 2: Promote CHANGELOG `[Unreleased]` → `[1.3.0] - 2026-05-20`</name>
  <files>CHANGELOG.md</files>
  <read_first>
    CHANGELOG.md (lines 1-110 — Unreleased block already populated by prior session with full v1013 content; the `## [1.2.1] - 2026-05-19` block at line 111 is the prior release header — pattern: `## [<version>] - YYYY-MM-DD`)
    .planning/phases/1060-close-gate/1060-CONTEXT.md (Amendment 2026-05-20 — A-01: tag scheme `v1013` local + `v1.3.0` public, NOT v1.4.0)
  </read_first>
  <action>
    Promote the existing `## [Unreleased]` heading to `## [1.3.0] - 2026-05-20`. Do NOT rewrite the body content — the prior session already wrote a complete release block (lines 14-110).

    Step 1 — Read CHANGELOG.md line 12 to confirm it currently reads `## [Unreleased]`. If it does not (e.g. the heading was already promoted by another run), stop and surface to the user — do NOT silently re-promote or write a new heading on top of an existing one.

    Step 2 — Use Edit (NOT bash sed) to replace the single line `## [Unreleased]` at line 12 with two blocks separated by a blank line: a new empty `## [Unreleased]` skeleton (Keep-a-Changelog hygiene — keeps future commits from landing under [1.3.0]) followed by `## [1.3.0] - 2026-05-20`. The skeleton should have a single body line reading `No unreleased changes.` directly under it. Concretely the replacement turns `## [Unreleased]` (1 line) into 5 lines: `## [Unreleased]`, blank, `No unreleased changes.`, blank, `## [1.3.0] - 2026-05-20`.

    Step 3 — Verify the body block (now under `## [1.3.0]`) is intact: run a grep that the existing block heading `### Ingest hardening + multi-layer GPKG + basemap sublayer styling (v1013 milestone)` is still present. Run a second grep for the 10 v1013 REQ-IDs (`WFS-04|PROBE-05|CRS-06|CLASS-07|GPKG-01|GPKG-02|GPKG-03|BSE-01|CLEAN-01|CTRL-01`) — expect ≥10 matches in total within the [1.3.0] block.

    Step 4 — Audit for any stale `v1.4.0` references in the file. Run `grep -n 'v1\.4\.0' CHANGELOG.md`. The prior session's body might cite `v1.4.0` (since the original plan was to ship v1.4.0). For each hit:
    - If the reference is in the [1.3.0] body and refers to "next public tag", change it to `v1.3.0` and add an HTML provenance comment immediately above the edited line: `<!-- 1060/A-01: v1.4.0→v1.3.0 because v1012 shipped as v1.2.1, not v1.3.0 -->`.
    - If the reference is in older release blocks ([1.0.0], etc.), leave it untouched (those are historical).

    Step 5 — Spot-check structural integrity: confirm the file still parses as Keep-a-Changelog (`## [<version>] - YYYY-MM-DD` pattern); confirm the `> Note on version history` block at line 10 is preserved (it's the public-vs-private version explainer — load-bearing context for users); confirm `## [1.3.0]` sorts immediately above `## [1.2.1]` with no orphaned blocks between.

    Constraints: do NOT rewrite the [1.3.0] body content — the prior session's CHANGELOG block (lines 14-110) is shipping copy and was reviewed in the partial close. Do NOT remove the `> Note on version history` block. Date is `2026-05-20` (today, per the planning context).
  </action>
  <acceptance_criteria>
    - `grep -c '^## \[1.3.0\] - 2026-05-20$' CHANGELOG.md` returns 1.
    - `grep -c '^## \[Unreleased\]$' CHANGELOG.md` returns 1 (the new top skeleton).
    - All 10 v1013 REQ-IDs (WFS-04, PROBE-05, CRS-06, CLASS-07, GPKG-01, GPKG-02, GPKG-03, BSE-01, CLEAN-01, CTRL-01) present in the [1.3.0] block.
    - No bare `v1.4.0` references remain (or all such refs have HTML provenance comments).
    - `> Note on version history` block at the top of the file intact.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && grep -c '^## \[1.3.0\] - 2026-05-20$' CHANGELOG.md | awk '{exit ($1==1)?0:1}' && grep -c '^## \[Unreleased\]$' CHANGELOG.md | awk '{exit ($1==1)?0:1}'</automated>
  </verify>
  <done>CHANGELOG block promoted to [1.3.0]; new [Unreleased] skeleton on top; 10 REQ-IDs intact; no stale v1.4.0 references.</done>
</task>

<task type="auto">
  <name>Task 3: Commit CHANGELOG + cut local tags v1013 and v1.3.0 (NOT pushed)</name>
  <files></files>
  <read_first>
    .planning/phases/1060-close-gate/1060-CONTEXT.md (Amendment 2026-05-20 — A-04: cut tags locally only, do NOT push)
    Project memory MEMORY.md (Milestone Status notes — recent tag patterns: v1010/v1010.1/v1010.2/v1011/v1011.1/v1012 are local; v1.3.0 was the public tag used by v1012)
  </read_first>
  <action>
    Commit the CHANGELOG change from Task 2 first (otherwise the tags would point at a commit that doesn't include the release header), THEN cut the 2 tags at HEAD.

    Step 1 — Sanity-check the working tree. Run `git status` and confirm the only NEW dirty file is `CHANGELOG.md`. Also expect the Wave 1 planning files (`.planning/phases/1060-close-gate/1060-MCP-REVERIFY.md`, `1060-CLEAN-LOG.md`, `1060-01-SUMMARY.md`) to be untracked or modified — they will be committed at Task 4. If unexpected source files are dirty (anything outside `.planning/` and `CHANGELOG.md`), STOP and surface — Task 1 should have caught Plan 01 source-level mutations, but defense in depth.

    Step 2 — Stage CHANGELOG.md alone. Use `git add CHANGELOG.md` (NOT `git add -A`, NOT `git add .`). Confirm via `git diff --cached --name-only` returns exactly `CHANGELOG.md`.

    Step 3 — Commit using a HEREDOC. The commit subject is `chore(1060): promote CHANGELOG to v1.3.0 (v1013 milestone close)`. The commit body explains the [Unreleased] → [1.3.0] promotion, lists the 10 v1013 REQ-IDs being closed, notes the A-01 reason for v1.3.0 instead of v1.4.0 (v1012 shipped as v1.2.1, not v1.3.0), and notes A-04's tags-local-only constraint. Use the HEREDOC pattern from the system prompt: `git commit -m "$(cat <<'EOF' ... EOF )"` to preserve formatting.

    Step 4 — Check if either tag already exists (idempotency). Run `git tag --list 'v1013'` and `git tag --list 'v1.3.0'`. If either returns the tag name, log "Tag already exists from prior session — skipping creation; verifying it points at HEAD." and verify the tag's commit equals the just-committed CHANGELOG commit (via `git rev-parse v1013 == git rev-parse HEAD`). If it points at a different commit, STOP and surface — do NOT autonomously delete and recreate (A-04 implies local-only mutations only; tag deletion is a user decision).

    Step 5 — Cut local tag `v1013` (lightweight, follows v1010/v1011/v1012 pattern from MEMORY.md). Run `git tag v1013` from `/Users/ishiland/Code/geolens`. Lightweight (no `-a`, no `-m`) — milestone tags are conventionally lightweight in this repo. Skip this step if Step 4 found the tag already at HEAD.

    Step 6 — Cut public tag `v1.3.0` (annotated, for release-notes tooling). Run `git tag -a v1.3.0 -m "<annotation message>"`. The annotation message subject is `v1.3.0 — Ingest Hardening (v1013 milestone)`. The body lists all 10 closed v1013 REQ-IDs (WFS-04, PROBE-05, CRS-06, CLASS-07, GPKG-01, GPKG-02, GPKG-03, BSE-01, CLEAN-01, CTRL-01) one per line with one-sentence descriptions. Use a multi-line `-m` quoted string. Skip this step if Step 4 found the tag already at HEAD.

    Step 7 — Confirm both tags exist and point at the same commit. Run `git tag --list 'v1013'` (returns `v1013`) and `git tag --list 'v1.3.0'` (returns `v1.3.0`). Run `git rev-parse v1013` and `git rev-parse v1.3.0` — outputs must be identical. If they differ, a hook auto-committed something between the two tag calls — surface to user.

    Step 8 — Explicit non-push confirmation. Do NOT run `git push`, `git push --tags`, `git push origin v1013`, `git push origin v1.3.0`, or set up tag upstream tracking. A-04 explicitly says local only. Log "Tags created locally; not pushed. User will `git push origin v1013 v1.3.0` when ready."

    Constraints: do NOT push or set up upstream tracking. Do NOT amend the commit after tagging (orphans the tags). Do NOT use `git tag -d` autonomously even if a typo is discovered — surface to user. Use HEREDOC for multi-line commit messages. Idempotent: re-running Step 4-6 against an already-tagged state should be a no-op (skip and verify).
  </action>
  <acceptance_criteria>
    - `git diff --cached --name-only` returned exactly `CHANGELOG.md` before commit.
    - The CHANGELOG-promote commit landed with the documented subject `chore(1060): promote CHANGELOG to v1.3.0 (v1013 milestone close)`.
    - `git tag --list 'v1013'` returns `v1013`.
    - `git tag --list 'v1.3.0'` returns `v1.3.0`.
    - `git rev-parse v1013 == git rev-parse v1.3.0` (both at same commit).
    - That commit is the HEAD of the current branch.
    - No `git push` was executed in this task (verify by checking `git log @{u}..HEAD --oneline` shows unpushed commits ahead of upstream OR by stating "did not invoke git push").
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && [ "$(git rev-parse v1013 2>/dev/null)" = "$(git rev-parse v1.3.0 2>/dev/null)" ] && [ -n "$(git tag --list 'v1013')" ] && [ -n "$(git tag --list 'v1.3.0')" ] && [ "$(git rev-parse v1013)" = "$(git rev-parse HEAD)" ]</automated>
  </verify>
  <done>CHANGELOG committed; v1013 + v1.3.0 tags exist locally at HEAD; not pushed.</done>
</task>

<task type="auto">
  <name>Task 4: Finalize 1060-SUMMARY.md (status: complete) + update STATE.md + REQUIREMENTS.md + ROADMAP.md tracker files</name>
  <files>
    .planning/phases/1060-close-gate/1060-SUMMARY.md,
    .planning/STATE.md,
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    .planning/phases/1060-close-gate/1060-SUMMARY.md (the existing partial summary from prior session — frontmatter `status: partial`, body documents what landed and what was deferred)
    .planning/phases/1060-close-gate/1060-MCP-REVERIFY.md (Plan 02 output — pull G-01..G-12 verdicts for the closed evidence section)
    .planning/phases/1060-close-gate/1060-CLEAN-LOG.md (Plan 03 output — pull 3 dataset delete verdicts)
    .planning/phases/1060-close-gate/1060-01-SUMMARY.md (Plan 01 output — pull e2e fix dispositions)
    .planning/STATE.md (frontmatter `status: completed`, position block at lines 18-23 — needs full rewrite to reflect ship)
    .planning/REQUIREMENTS.md (traceability table at lines 87-97 — flip CLEAN-01 and CTRL-01 from Pending to Complete)
    .planning/ROADMAP.md (lines 75-88 — flip Phase 1060 checkbox from `[ ]` to `[x]`; flip milestone bullet from 🔄 to ✅ with date)
    Project memory MEMORY.md (Milestone Status section — existing entries for prior milestones are the template for the new v1013 entry; note shape: 1-2 sentences with tag, date, phase range, REQ count, tagged version)
  </read_first>
  <action>
    Rewrite `1060-SUMMARY.md` with `status: complete` and aggregate Wave 1 evidence, then update the 3 tracker files (STATE.md, REQUIREMENTS.md, ROADMAP.md) so the milestone is recorded as shipped.

    Step 1 — Rewrite `1060-SUMMARY.md`. Use the Write tool (full rewrite — prior content is preserved in git history via the prior session's commit). New frontmatter (replace existing): `phase: 1060-close-gate`, `status: complete`, `shipped: 2026-05-20`, `plans_completed: 4`, `plans_total: 4`, `tag_local: v1013`, `tag_public: v1.3.0`, `key_files.modified` listing CHANGELOG.md + the 4 tracker files + all source files modified across Plans 01-04 (read from each plan's files_modified). Remove the `deferred_to_user` block (everything was completed). New body sections in this order:

    1. `# Phase 1060: v1013 Close Gate` (top-level)
    2. `## Status — COMPLETE`. One-paragraph close stating 4 plans landed, all 4 ROADMAP success criteria met, tags created locally.
    3. `## Smoke Gates (final pre-tag run)`. Ingest the memo block from Task 1 — render as a table: gate | result | pass count | wall-clock | command.
    4. `## CTRL-01 acceptance criterion #3 — Live Playwright MCP Re-Verify`. Ingest the Gate Results table from `1060-MCP-REVERIFY.md` — render the 12 gates with PASS/FAIL verdicts, with a one-line summary of failures (if any).
    5. `## CLEAN-01 acceptance criterion #1 — Dataset Cleanup`. Ingest the Targets table from `1060-CLEAN-LOG.md` — 3 dataset IDs with PASS verdicts.
    6. `## E2E Smoke Triage — Plan 01 Dispositions`. Ingest from `1060-01-SUMMARY.md`: 2 dispositions (real-fix / test-pin / flaky-pin) with file + line + rationale comment.
    7. `## Inline Code-Review Fixes`. Carry forward the table from the prior partial summary (10 issues across 4 phases). Add a final-tally line.
    8. `## Tags Created`. Two lines: `v1013` (lightweight, local) and `v1.3.0` (annotated, local). Reference commit SHA from `git rev-parse v1013`.
    9. `## Net Deliverables`. v1013 totals: phase count (4: 1057-1060), plan count (sum across phases), commit count since milestone start (run `git rev-list --count <prior milestone tag>..HEAD`; if v1012 archive commit `7262bdea` is reachable, use it; else cite the 78-commits-since-start number from prior STATE.md), test counts (backend pytest, frontend vitest, e2e:smoke:builder), requirements satisfied (10/10), v1013.1 deferrals (0).
    10. `## Next Steps`. Two lines: "Push tags when ready: `git push origin v1013 v1.3.0`" and "Plan 1014 milestone work or close milestone via `/gsd:milestone-complete`."

    Step 2 — Update `.planning/STATE.md`:
    - Frontmatter: `milestone: v1014` (or whatever the next milestone is — if undetermined, leave as `v1013` and add a comment that milestone-complete needs running). `milestone_name`: TBD or v1013 retained. `status: completed`. `last_updated` to ISO-8601 now. `last_activity`: "2026-05-20 — Phase 1060 SHIPPED; v1013 tagged locally as v1013 + v1.3.0; CHANGELOG promoted; 4 plans / N tasks closed". `progress.completed_phases`: 9 (i.e. 1057, 1058, 1059, 1060 all shipped). `progress.percent`: 100.
    - Body section `## Current Position`: replace with one-line status that v1013 is COMPLETE; next milestone is TBD or v1014.
    - Body section `## Last Shipped Milestone`: replace existing v1012 content with a v1013 entry mirroring v1012's shape — version, shipped date 2026-05-20, phases 1057-1060, plans count, requirements 10/10, tag `v1013` local + `v1.3.0` public, archive path (note: archive itself is not part of this plan — milestone-complete handles that).
    - Body section `## v1013 Phase Map`: change Phase 1060 row's `Status` column from `0/?` to `4/4 Complete`.
    - Body section `## Operator Next Steps`: replace with "Run `/gsd:milestone-complete` to archive v1013 ROADMAP, or `/gsd:plan-milestone` for the next milestone."

    Step 3 — Update `.planning/REQUIREMENTS.md` traceability table at lines 87-97: change `CLEAN-01` row's Status column from `Pending` to `Complete`. Change `CTRL-01` row's Status column from `Pending` to `Complete`. Update the closing line `*Last updated:*` at the bottom of the file to `2026-05-20 — v1013 milestone shipped (10/10 reqs complete)`.

    Step 4 — Update `.planning/ROADMAP.md`:
    - Line 75 (or wherever the v1013 active milestone bullet is): change `🔄 **v1013 Ingest Hardening**` to `✅ **v1013 Ingest Hardening**` and append `(shipped 2026-05-20, local tag v1013, public tag v1.3.0)` plus a future archive reference placeholder `— see [archive](milestones/v1013-ROADMAP.md)` (the archive file may not exist yet — that's fine, `/gsd:milestone-complete` creates it).
    - Line 88 (the Phase 1060 checkbox): change `- [ ] **Phase 1060: Close Gate**` to `- [x] **Phase 1060: Close Gate**` and append `(shipped 2026-05-20)`.
    - At the bottom of the v1013 active section, change any mention of `v1.4.0` to `v1.3.0` with an inline comment `<!-- 1060/A-01: actual public tag is v1.3.0 -->`.
    - Progress table at line 163-168: change Phase 1060 row's `Plans Complete` column to `4/4` and `Status` column to `Complete`, fill `Completed` column with `2026-05-20`.

    Step 5 — Commit the 5-file update in a single commit. Stage exactly: `.planning/phases/1060-close-gate/1060-SUMMARY.md`, `.planning/phases/1060-close-gate/1060-MCP-REVERIFY.md`, `.planning/phases/1060-close-gate/1060-CLEAN-LOG.md`, `.planning/phases/1060-close-gate/1060-01-SUMMARY.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`. Commit subject: `docs(1060): finalize v1013 close — SUMMARY complete + tracker state shipped`. Commit body: short paragraph summarizing tags created, 4 ROADMAP success criteria met, and the user-next-step (push tags + run milestone-complete).

    Step 6 — Update the v1013 tags to point at THIS new commit (the docs commit is the actual final state of the milestone, not the CHANGELOG-only commit from Task 3). Delete both tags (`git tag -d v1013` and `git tag -d v1.3.0`) and re-create them at the new HEAD with the same annotation messages from Task 3. Verify with `git rev-parse v1013 == git rev-parse HEAD` and same for v1.3.0. This re-tag step is intentional: the canonical "v1013 ship" commit is the one that finalizes tracker state, not the one that only promotes CHANGELOG.

    Constraints: do NOT push tags. Do NOT touch `.planning/milestones/v1013-ROADMAP.md` (that file is created by `/gsd:milestone-complete`, not by this plan — A-04's tag-local-only spirit applies to milestone archive too: it's a user-initiated step). Use Edit for surgical changes; use Write only for the full 1060-SUMMARY.md rewrite. Keep STATE.md within the existing schema (`gsd_state_version: 1.0` in frontmatter). REQUIREMENTS.md is two-cell edits, not a full rewrite.
  </action>
  <acceptance_criteria>
    - `.planning/phases/1060-close-gate/1060-SUMMARY.md` frontmatter `status: complete`, `shipped: 2026-05-20`, `tag_local: v1013`, `tag_public: v1.3.0`.
    - The new SUMMARY body has all 10 sections listed in Step 1, populated from Wave 1 evidence.
    - `.planning/STATE.md` `status: completed`, last_activity reflects v1013 ship, `## Last Shipped Milestone` block describes v1013.
    - `.planning/REQUIREMENTS.md` traceability table shows `CLEAN-01 | Complete` and `CTRL-01 | Complete`.
    - `.planning/ROADMAP.md` v1013 milestone bullet is `✅` with shipped date; Phase 1060 checkbox is `[x]`; v1.4.0 references are either gone or annotated; Progress table shows Phase 1060 at 4/4 Complete.
    - Single commit lands all 5 tracker updates with the documented subject.
    - Tags `v1013` and `v1.3.0` are re-pointed at the finalize-tracker commit (the new HEAD), still pointing at the same commit as each other.
    - No `git push` invocations.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens && grep -q '^status: complete$' .planning/phases/1060-close-gate/1060-SUMMARY.md && grep -q '| CLEAN-01 .* Complete ' .planning/REQUIREMENTS.md && grep -q '| CTRL-01 .* Complete ' .planning/REQUIREMENTS.md && grep -q '\[x\] \*\*Phase 1060' .planning/ROADMAP.md && [ "$(git rev-parse v1013)" = "$(git rev-parse HEAD)" ]</automated>
  </verify>
  <done>1060-SUMMARY.md status=complete; STATE.md, REQUIREMENTS.md, ROADMAP.md updated to reflect ship; tags re-pointed at finalize-tracker HEAD commit.</done>
</task>

</tasks>

<verification>
- `CHANGELOG.md` has `## [1.3.0] - 2026-05-20` heading and new `## [Unreleased]` skeleton above it
- 4 smoke gates green (typecheck 0, i18n 2/2, vitest green, e2e:smoke:builder green)
- Local tags `v1013` + `v1.3.0` exist and point at the same commit (Task 4's finalize-tracker commit)
- `1060-SUMMARY.md` frontmatter `status: complete`
- `STATE.md` documents v1013 as shipped
- `REQUIREMENTS.md` marks CLEAN-01 + CTRL-01 Complete
- `ROADMAP.md` Phase 1060 checkbox `[x]`, v1013 milestone bullet `✅`
- No tags pushed
</verification>

<success_criteria>
All 5 ROADMAP success criteria are satisfied with the v1.3.0 tag substitution per A-01:
1. (Plan 03) 3 datasets deleted from localhost:8080 catalog
2. (Plan 01 + Task 1 re-verify) Smoke gates green: typecheck 0, vitest green, e2e:smoke:builder green, i18n parity 2/2
3. (Plan 02) Live Playwright MCP re-verify confirms WFS-04, PROBE-05, GPKG-01, GPKG-02, BSE-01
4. Code-review pass complete with inline fixes (carried forward from prior session — 23 inline review fixes already documented)
5. (Task 2 + Task 3) CHANGELOG [Unreleased] promoted to [1.3.0]; local v1013 tag created; public v1.3.0 tag created (per A-01 NOT v1.4.0)
</success_criteria>

<output>
Final summary file is `.planning/phases/1060-close-gate/1060-SUMMARY.md` (status: complete). No separate per-plan-04 SUMMARY needed — Task 4's SUMMARY rewrite IS the phase-level deliverable.
</output>
