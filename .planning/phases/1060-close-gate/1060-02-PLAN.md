---
phase: 1060-close-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/1060-close-gate/1060-MCP-REVERIFY.md
autonomous: true
requirements:
  - CTRL-01

must_haves:
  truths:
    - "All 5 ROADMAP-named live MCP gates (WFS-04, PROBE-05, GPKG-01, GPKG-02, BSE-01) are exercised against running localhost:8080 and recorded PASS/FAIL with per-gate evidence (DOM observation OR network log OR live MapLibre paint-property output)"
    - "All 5 BSE-01 sub-gates from Phase 1059's human_verification list (live preview, persist+reload, viewer/shared/embed parity, reset, legacy compat) are exercised and recorded"
    - "Total 12 live re-verify items resolved (no items left as 'untested' or 'skipped' without explicit user-visible rationale)"
    - "If any gate fails, the gap is documented with enough detail that Plan 04 can decide whether to ship or block tag creation"
  artifacts:
    - path: ".planning/phases/1060-close-gate/1060-MCP-REVERIFY.md"
      provides: "Authoritative live MCP re-verify log for CTRL-01 acceptance criterion #3"
      min_lines: 80
      contains: "## Gate Results"
  key_links:
    - from: "Live MCP verification"
      to: "ROADMAP success criterion #3"
      via: ".planning/phases/1060-close-gate/1060-MCP-REVERIFY.md frontmatter status field"
      pattern: "status:\\s*(passed|failed|partial)"
    - from: "Per-gate PASS verdict"
      to: "Plan 04 SUMMARY ingestion"
      via: "MCP-REVERIFY.md '## Gate Results' table consumed by Plan 04 Task 4"
      pattern: "WFS-04.*PASS|FAIL"
---

<objective>
Drive the 12 live Playwright MCP re-verify gates against running `localhost:8080`, recording per-gate PASS/FAIL with concrete evidence. CTRL-01 acceptance criterion #3 is satisfied only when all 12 gates resolve.

Purpose: the prior session left these as "deferred to user" because Playwright MCP was disconnected. The Amendment 2026-05-20 in CONTEXT.md confirms MCP is restored. Per `feedback_playwright_mcp_self_verify.md`, when MCP is available and the stack is running, Claude drives the verification rather than handing back.

Output: `.planning/phases/1060-close-gate/1060-MCP-REVERIFY.md` — a single authoritative log file consumed by Plan 04 to populate the final 1060-SUMMARY.md and decide tag readiness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1060-close-gate/1060-CONTEXT.md
@.planning/phases/1060-close-gate/1060-SUMMARY.md
@.planning/phases/1057-service-url-reliability/1057-VERIFICATION.md
@.planning/phases/1058-multi-layer-gpkg-handling/1058-VERIFICATION.md
@.planning/phases/1059-basemap-sublayer-editor-path-b-fix/1059-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stack health gate + bootstrap MCP log file</name>
  <files>.planning/phases/1060-close-gate/1060-MCP-REVERIFY.md</files>
  <read_first>
    .planning/phases/1060-close-gate/1060-SUMMARY.md (lines 75-94 — the full 12-gate list with surface references)
    .planning/phases/1057-service-url-reliability/1057-VERIFICATION.md (frontmatter `deferred_to_1060` block — 4 gates with exact endpoint URLs)
    .planning/phases/1058-multi-layer-gpkg-handling/1058-VERIFICATION.md (frontmatter `human_verification` block — 3 gates)
    .planning/phases/1059-basemap-sublayer-editor-path-b-fix/1059-VERIFICATION.md (frontmatter `human_verification` block — 5 BSE-01 gates)
  </read_first>
  <action>
    Confirm stack health, then create the MCP re-verify log file as the authoritative record.

    Step 1 — Stack health gate (blocking): run `docker compose ps`. All services must show `(healthy)`. If any service is not healthy, document the unhealthy service in `1060-MCP-REVERIFY.md` (still create the file scaffold) and stop — return control to user with the specific docker-compose state. Do NOT run `docker compose up -d` or `docker compose down -v` autonomously (these are user-state-mutating per `feedback_playwright_mcp_self_verify.md`).

    Step 2 — Verify Playwright MCP availability: confirm `mcp__playwright__browser_navigate`, `mcp__playwright__browser_click`, `mcp__playwright__browser_evaluate`, `mcp__playwright__browser_snapshot`, `mcp__playwright__browser_console_messages`, `mcp__playwright__browser_network_requests` tools are accessible. If not, stop and document in the log.

    Step 3 — Write the log file scaffold at `.planning/phases/1060-close-gate/1060-MCP-REVERIFY.md` using the Write tool. Required frontmatter fields: `phase: 1060-close-gate`, `artifact: live-mcp-reverify`, `created: <ISO-8601 timestamp>`, `status: pending`, `gates_total: 12`, `gates_passed: 0`, `gates_failed: 0`, `gates_pending: 12`. Body sections in order: `# Phase 1060: Live Playwright MCP Re-Verify Log` (top-level); `**Purpose:** CTRL-01 acceptance criterion #3 — confirm all v1013 user-visible truths render correctly on live localhost:8080.`; `**Stack health (at re-verify start):** <fill from docker compose ps>`; `**MCP tools available:** <yes/no with list of tools confirmed>`; `## Gate Results` with a markdown table with columns `Gate | Surface | REQ | Result | Evidence | Notes` and 12 PENDING rows for G-01 through G-12. The rows are (in order):
    - G-01 | WFS abstract-geom import (ahocevar.com/geoserver/wfs Countries of the World) | WFS-04 | PENDING | — | —
    - G-02 | OGC API probe ≤5s (demo.pygeoapi.io/master 17 collections) | PROBE-05 | PENDING | — | —
    - G-03 | OGC API CRS auto-detect (demo.pygeoapi.io/master Large Lakes) | CRS-06 | PENDING | — | —
    - G-04 | Service URL VEC label (Natural Earth Points) | CLASS-07 | PENDING | — | —
    - G-05 | Reupload File path layer-select (multi-layer GPKG) | GPKG-01 | PENDING | — | —
    - G-06 | Preview pane Layer + schema diff | GPKG-02 | PENDING | — | —
    - G-07 | Bulk Review Ingest all layers fan-out | GPKG-03 | PENDING | — | —
    - G-08 | BSE-01: stroke color live preview | BSE-01 | PENDING | — | —
    - G-09 | BSE-01: persist + reload | BSE-01 | PENDING | — | —
    - G-10 | BSE-01: viewer / shared / embed parity (3 routes) | BSE-01 | PENDING | — | —
    - G-11 | BSE-01: Reset clears override | BSE-01 | PENDING | — | —
    - G-12 | BSE-01: legacy map (no overrides) renders cleanly | BSE-01 | PENDING | — | —

    After the Gate Results table, add a `## Per-Gate Detail` section with placeholder text "(Populated by Tasks 2+3 — Phase 1057+1058 gates in Task 2, Phase 1059 BSE-01 gates in Task 3)".

    Constraints: use admin credentials per CLAUDE.md (`admin` / `admin` for local dev) when MCP browser session needs login. If the stack is unhealthy and recovery requires `docker compose down -v && up -d --build` (e.g. migration 0017 not applied), document the discovery and surface to the user — do NOT execute the down -v. Do not silently merge gates (e.g. don't claim G-08+G-09+G-10 as "all PASS" without exercising each).
  </action>
  <acceptance_criteria>
    - `1060-MCP-REVERIFY.md` exists with the documented frontmatter + Gate Results table structure containing 12 PENDING rows.
    - Stack health captured from `docker compose ps`.
    - MCP tool availability documented (yes/no).
    - If stack is unhealthy, the file documents the unhealthy state and the task halts (downstream tasks will be blocked).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/1060-close-gate/1060-MCP-REVERIFY.md && grep -q '^| G-12 ' .planning/phases/1060-close-gate/1060-MCP-REVERIFY.md</automated>
  </verify>
  <done>Log file scaffold exists; stack health captured; MCP tool availability confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Drive Phase 1057 + Phase 1058 live MCP gates (G-01 through G-07)</name>
  <files>.planning/phases/1060-close-gate/1060-MCP-REVERIFY.md</files>
  <read_first>
    .planning/phases/1057-service-url-reliability/1057-VERIFICATION.md (`deferred_to_1060` frontmatter — 4 gates with exact repro endpoints)
    .planning/phases/1058-multi-layer-gpkg-handling/1058-VERIFICATION.md (`human_verification` frontmatter — 3 gates)
    e2e/fixtures/multi-layer-gpkg.gpkg (binary fixture used by GPKG gates — confirm exists via `ls -la e2e/fixtures/multi-layer-gpkg.gpkg`)
    frontend/src/components/import/ReuploadDialog.tsx (lines 581-826 — `selecting-file-layer` step + preview pane Layer line + schema-change advisory banner at `data-testid="schema-change-advisory"`)
    frontend/src/components/import/BulkReviewList.tsx (lines 343-367 — "Ingest all layers" button)
  </read_first>
  <action>
    Drive 7 live MCP gates G-01 through G-07. For each gate, populate the Gate Results row in the log file AND add a Per-Gate Detail subsection.

    For each gate, the loop is identical: (1) navigate via MCP, (2) execute repro steps, (3) capture observable behavior, (4) compare to expected, (5) record verdict + evidence. NEVER fabricate evidence. If MCP returns inconclusive output, the gate is FAIL not "good enough".

    **G-01 (WFS-04):** Navigate via `mcp__playwright__browser_navigate` to `localhost:8080/import`, choose the Service URL tab, enter `https://ahocevar.com/geoserver/wfs` (or use the URL pre-saved in the catalog), select the "Countries of the World" layer (verify exact layer name in the live probe response — examples include `topp:states` or `topp:tasmania_state_boundaries`), proceed through preview, click Import. Wait for completion. Expected: dataset appears in My Datasets within 60s with feature count > 0; no `asyncpg.exceptions.InvalidParameterValueError` in worker logs (check via `docker compose logs worker --tail=50` after the import attempt). Evidence: dataset_id from the catalog list view + final ingest status from the import-progress UI.

    **G-02 (PROBE-05):** Navigate to `localhost:8080/import`, choose the Service URL tab, enter `https://demo.pygeoapi.io/master`. Time the probe via `mcp__playwright__browser_evaluate` running a JS snippet that captures `performance.now()` before clicking the probe button and again after the layer-list renders. Expected: end-to-end probe completes ≤5000ms. Evidence: timing number recorded.

    **G-03 (CRS-06):** Continue from G-02's pygeoapi probe — select the Large Lakes layer, proceed to preview. Expected: CRS Override field is NOT visible (hidden because URI-form CRS was auto-detected to EPSG:4326). The detected CRS read-only confirmation reads "Detected CRS: EPSG:4326 (auto-detected — no override needed)". Click Import; verify the import succeeds without the user touching the override field. Evidence: DOM snapshot via `mcp__playwright__browser_snapshot` of preview pane (no `#import-crs` input element present) + final dataset_id.

    **G-04 (CLASS-07):** Navigate to Service URL with `https://ahocevar.com/geoserver/wfs`, find a Natural Earth point layer (e.g. `ne:ne_10m_populated_places` if present, else any point layer with `geometry_type=null` in probe response — confirm by inspecting the `/api/probe` response in `mcp__playwright__browser_network_requests`). Verify the layer's TypeTag in the layer-select list reads "VEC", not "RAS". Evidence: DOM snapshot of layer-select list with the relevant row visible + the TypeTag content extracted via `mcp__playwright__browser_evaluate`.

    **G-05 (GPKG-01):** Navigate to `localhost:8080`, pick any dataset that was originally ingested from a single-layer file (e.g. one of the seed datasets), open the More overflow → Reupload File → upload `e2e/fixtures/multi-layer-gpkg.gpkg` from the repo (use `mcp__playwright__browser_file_upload` if available, else use the file picker). Expected: dialog transitions to a step containing `data-testid="reupload-file-layer-select"` showing both `buildings` and `addresses` rows; if the original dataset's `source_layer` matches one of these, that row is pre-selected. Click Preview Layer → preview pane displays "File: multi-layer-gpkg.gpkg" + "Layer: <chosen>". Evidence: DOM snapshot at the layer-select step.

    **G-06 (GPKG-02):** Continue from G-05 — at the preview step, verify the "Layer: <chosen>" line is visible AND that a `data-testid="schema-change-advisory"` banner appears (because the chosen layer's columns differ from the original dataset's schema). Evidence: DOM snapshot with both lines + the advisory banner.

    **G-07 (GPKG-03):** Navigate to `localhost:8080/import`, drag `e2e/fixtures/multi-layer-gpkg.gpkg` into the upload area. Wait for preview. Expected: "Ingest all 2 layers as separate datasets" button is visible. Click it. Expected: a results modal opens listing "buildings" and "addresses" with CheckCircle2 icons; within 30 seconds the catalog has 2 new datasets named approximately "multi-layer-gpkg: buildings" and "multi-layer-gpkg: addresses". Evidence: 2 new dataset_ids from the catalog after the wait, retrieved via `GET /api/datasets?limit=50` against the authenticated API.

    For each gate, write a Per-Gate Detail block in the log file with this content shape: a `### G-XX: <gate name> (<REQ-ID>)` heading; then bulleted lines for **Expected:** (one-line ROADMAP/VERIFICATION ask), **Observed:** (one-line of what MCP saw), **Evidence:** (DOM snapshot ref / timing number / dataset_id / log line), **Verdict:** PASS or FAIL, and optional **Notes:** (only if FAIL or if observation deviated meaningfully).

    Update the Gate Results table row for each gate (PENDING → PASS or FAIL) and the frontmatter counters (`gates_passed`, `gates_failed`, `gates_pending`) as you go.

    Constraints: do NOT clean up test datasets created during these gates — Plan 03 handles dataset cleanup. The 3 named CLEAN-01 dataset IDs are separate from the gate-created datasets. If a gate genuinely fails (production bug), do NOT attempt to fix it inline — surface to Plan 04 via the log. The choice "ship anyway, document deferral" vs "block tag, plan v1013.1" is for the user; the executor's job is honest reporting. Use admin credentials from CLAUDE.md (`admin` / `admin` default for local dev). If credentials don't work, document and stop.
  </action>
  <acceptance_criteria>
    - All 7 gates (G-01 through G-07) have a row in the Gate Results table with verdict PASS or FAIL (not PENDING).
    - Each gate has a Per-Gate Detail subsection with Expected / Observed / Evidence / Verdict.
    - Frontmatter counters updated to reflect 7 gates resolved.
    - Evidence references are concrete (timing numbers, dataset_ids, network responses, DOM testids) — not generic "PASS".
    - For any FAIL, the failure mode is described in enough detail that Plan 04 can choose ship-with-deferral vs block-tag.
  </acceptance_criteria>
  <verify>
    <automated>grep -E '^\| G-0[1-7] .* (PASS|FAIL) ' .planning/phases/1060-close-gate/1060-MCP-REVERIFY.md | wc -l | awk '{exit ($1==7)?0:1}'</automated>
  </verify>
  <done>7 Phase 1057+1058 gates have PASS/FAIL verdicts with concrete evidence; per-gate detail blocks populated.</done>
</task>

<task type="auto">
  <name>Task 3: Drive Phase 1059 BSE-01 live MCP gates (G-08 through G-12)</name>
  <files>.planning/phases/1060-close-gate/1060-MCP-REVERIFY.md</files>
  <read_first>
    .planning/phases/1059-basemap-sublayer-editor-path-b-fix/1059-VERIFICATION.md (lines 9-25 — full 5-item human_verification block with surfaces + expected outcomes)
    frontend/src/components/builder/BasemapSublayerEditorScene.tsx (5 sections at lines 94-238: STROKE / CASING / ZOOM / OPACITY / RESET)
    frontend/src/lib/builder/basemap-style-mutation.ts (`applySublayerOverrides` helper + idle-retry pattern at lines 51-65)
    frontend/src/components/viewer/ViewerMap.tsx (line 568 — viewer/shared/embed call site with VIEWER_SOURCE_PREFIX)
    frontend/src/pages/MapBuilderPage.tsx (lines 487-518 — `SUBLAYER_ID_OVERRIDE_KEY` mapping + `updateSublayerOverride` callback)
  </read_first>
  <action>
    Drive 5 live MCP gates G-08 through G-12 covering BSE-01 — the 5 human_verification items from 1059-VERIFICATION.md.

    **G-08 (BSE-01: live preview):** Navigate via `mcp__playwright__browser_navigate` to `localhost:8080/builder/<map-id>` (use an existing test map or create one via `POST /api/maps/`), expand the basemap group in the layer stack, click the Roads sublayer to open the BasemapSublayerEditorScene. Use the stroke color picker to set the color to red (`#ff0000`). Expected: road lines on the map canvas immediately render in red without page reload; STROKE section heading is visible. Evidence: DOM snapshot showing the color picker at #ff0000 + a `mcp__playwright__browser_evaluate` call returning `map.getPaintProperty('<a road layer id>', 'line-color')` to confirm the live MapLibre state (read the actual road layer id from the loaded style via `map.getStyle().layers`).

    **G-09 (BSE-01: persist + reload):** Continue from G-08 — wait for autosave (or click explicit save if available), then `mcp__playwright__browser_navigate` to a different page (e.g. `/datasets`), then back to `/builder/<map-id>`. Expected: on reload, the color picker still shows red (#ff0000) AND the road lines remain red. Evidence: post-reload DOM snapshot of the color picker + post-reload `map.getPaintProperty` output.

    **G-10 (BSE-01: viewer/shared/embed parity — 3 sub-routes):** With the saved map containing the red road override:
    - (a) Navigate to `/m/<map-id>` (viewer route) — expect red road lines.
    - (b) Navigate to the shared link URL (find/create via the share-link feature — typically a `POST /api/maps/<id>/share` returning a token; URL pattern `/m/<token>`) — expect red road lines.
    - (c) Navigate to the embed URL (`/embed/<token>`) — expect red road lines.
    All 3 sub-routes must show the same red road lines. Evidence: 3 DOM snapshots + 3 `map.getPaintProperty` outputs (one per route). Per-route sub-verdicts can be sub-bullets within the G-10 detail block; G-10 overall verdict is PASS only when all 3 sub-routes pass.

    **G-11 (BSE-01: Reset clears override):** Return to the builder, open the Roads sublayer editor, expand the RESET section, click Reset, confirm. Expected: stroke color reverts to basemap default on the map immediately; on save + reload, the override is gone (no red lines); other sublayers (boundary, building) are unaffected. Evidence: DOM snapshot of color picker showing default + post-reload `map.getPaintProperty` showing default + a check that the saved `basemap_config.sublayer_overrides['road']` is removed (via `GET /api/maps/<id>` JSON inspection on the network panel).

    **G-12 (BSE-01: legacy map backward compat):** Identify a saved map created BEFORE Phase 1059 (i.e. one whose `basemap_config` JSON does NOT contain a `sublayer_overrides` key). If no such map exists locally, create a fresh one via `POST /api/maps/` with a `basemap_config` payload that omits `sublayer_overrides` entirely. Navigate to both `/builder/<map-id>` and `/m/<map-id>`. Expected: map renders with default basemap styling; no console errors; `applySublayerOverrides` short-circuits immediately. Evidence: console message log (no errors related to `sublayer_overrides`) captured via `mcp__playwright__browser_console_messages` + DOM snapshot of default-styled map.

    For each gate, populate the Gate Results table row AND a Per-Gate Detail subsection using the same template shape from Task 2 (Expected / Observed / Evidence / Verdict / optional Notes).

    Update frontmatter counters to reflect all 12 gates resolved. Set the top-level `status` field:
    - `passed` if all 12 PASS
    - `failed` if any P0 gate fails (G-01 WFS-04 and G-05 GPKG-01 are P0; failures here SHOULD block tag)
    - `partial` if some P1/P2 gates fail but P0 + headline features pass; Plan 04 decides ship-or-block

    Constraints: G-10 has 3 sub-routes — all 3 must be checked; do not skip a route because the first 2 passed. The order matters: G-08 → G-09 → G-10 → G-11 → G-12; each gate's setup builds on the prior one's state, do not parallelize. When the saved override is cleared by Reset (G-11), persist the empty state before running G-12 — but G-12 needs a legacy-style payload (no `sublayer_overrides` key at all), not a recently-reset payload (`sublayer_overrides: {}` or `sublayer_overrides.road: null`). These payload shapes differ; use a fresh `POST /api/maps/` with explicit `basemap_config` body that omits the key for G-12.
  </action>
  <acceptance_criteria>
    - All 5 gates (G-08 through G-12) have verdict PASS or FAIL (not PENDING).
    - G-10 has 3 sub-route results documented (viewer + shared + embed).
    - All 12 gates total have verdicts; `gates_pending` frontmatter is 0.
    - Top-level `status` set to `passed` / `failed` / `partial`.
    - Each gate's evidence cites concrete MapLibre paint-property output OR network response OR DOM testid — not "looked fine".
  </acceptance_criteria>
  <verify>
    <automated>grep -E '^\| G-(0[8-9]|1[0-2]) .* (PASS|FAIL) ' .planning/phases/1060-close-gate/1060-MCP-REVERIFY.md | wc -l | awk '{exit ($1==5)?0:1}'</automated>
  </verify>
  <done>All 5 BSE-01 gates resolved; total 12 gates resolved; frontmatter status set to passed/failed/partial; counters consistent.</done>
</task>

</tasks>

<verification>
- `.planning/phases/1060-close-gate/1060-MCP-REVERIFY.md` exists
- Frontmatter `gates_total=12, gates_pending=0, gates_passed+gates_failed=12`
- All 12 Gate Results rows show PASS or FAIL
- Per-Gate Detail subsections present for each of G-01..G-12
- For G-10, all 3 sub-routes (viewer, shared, embed) have verdicts
- Top-level `status` is `passed` | `failed` | `partial`
</verification>

<success_criteria>
ROADMAP success criterion #3 (live MCP re-verify of WFS-04, PROBE-05, GPKG-01, GPKG-02, BSE-01) is satisfied with concrete per-gate evidence — Plan 04 can consume the log to populate the final SUMMARY tag-readiness verdict.
</success_criteria>

<output>
The MCP re-verify log AT `.planning/phases/1060-close-gate/1060-MCP-REVERIFY.md` IS the deliverable; no separate per-plan SUMMARY file needed. Plan 04 reads this log directly.
</output>
