# Plan 1131-01: Validation API, Docs, OpenAPI, and SDKs

**Status:** Complete
**Requirements:** VAL-02, VAL-03, VAL-04, API-05, DOC-01, DOC-02, DOC-03

## Tasks

- [x] Add JSON Schema 2020-12 validation backed by the vendored DCAT-US 3.0 schema graph.
- [x] Expose catalog and per-dataset validation report routes.
- [x] Include JSON path, schema path, validator, and message for each validation error.
- [x] Preserve catalog visibility filtering and per-dataset access checks for validation endpoints.
- [x] Document routes, validation behavior, schema source, migration notes, and accepted mapping gaps.
- [x] Move `jsonschema` into backend runtime dependencies.
- [x] Refresh OpenAPI and generated SDK artifacts in a clean tree containing only committed DCAT-US source changes.

## Verification

- `cd backend && env $(grep -v '^#' ../.env.test.example | grep -v '^$' | xargs) uv run pytest tests/test_dcat.py tests/test_datasets.py::TestDatasetSubRouterRouting::test_dcat_catalog_not_captured_by_dataset_id -q` -> 22 passed.
- `cd backend && uv run ruff format app/standards/dcat_us app/modules/catalog/datasets/api/router_export.py tests/test_dcat.py`
- `cd backend && uv run ruff check app/standards/dcat_us app/modules/catalog/datasets/api/router_export.py tests/test_dcat.py`
- `make openapi`
- `make sdks`
