---
phase: 1133-audit-first-builder-walkthrough
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md
autonomous: true
requirements:
  - WALK-01
user_setup: []

must_haves:
  truths:
    - "Audit doc exists at `.planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md` with the agreed section skeleton present."
    - "Every render mode (fill / line / circle / symbol / heatmap / cluster / raster / basemap / DEM/terrain) has a dedicated table populated with at least 1 finding row OR an explicit `## No findings — verified clean (evidence: ...)` line."
    - "Every finding row records the canonical ADK map id `c39be324-6815-40e5-8143-00a2723827b2` OR a different specific representative map id used for the repro."
    - "Smaller-screen (≤800px) findings table covers right-sidebar Sheet, NavigationControl overlap, MapCoordReadout overlap, and SheetContent close-button duplication surfaces explicitly — even when the live result is PASS (recorded as `## Verified clean — v1011 RESP-01/02/03 still live`)."
    - "Phase 1134-1138 routing table contains one row per P0/P1/P2 finding mapping the finding to its owning downstream phase (1134 / 1135 / 1136 / 1137 / 1138 / 1139)."
  artifacts:
    - path: ".planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md"
      provides: "Single ground-truth audit doc for v1030 polish sweep."
      contains: "## Methodology, ## Render Mode: fill, ## Render Mode: line, ## Render Mode: circle, ## Render Mode: symbol, ## Render Mode: heatmap, ## Render Mode: cluster, ## Render Mode: raster, ## Render Mode: basemap, ## Render Mode: DEM/terrain, ## Smaller-Screen (≤800px) Findings, ## Phase 1134-1138 Routing Table"
  key_links:
    - from: ".planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "every routing-table row cites a v1030 REQ ID (MAP-* / AI-* / EDITOR-* / SHARE-* / EASY-* / QA-*)"
      pattern: "MAP-|AI-|EDITOR-|SHARE-|EASY-|QA-"
    - from: ".planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md"
      to: "live `localhost:8080` Playwright MCP session"
      via: "every finding row records the canonical or representative map id used for the repro"
      pattern: "c39be324-6815-40e5-8143-00a2723827b2|[0-9a-f]{8}-[0-9a-f]{4}"
---

<objective>
Establish `1133-BUILDER-WALKTHROUGH-AUDIT.md` as the single ground-truth backlog for v1030, populated with a live Playwright MCP walkthrough of every render mode plus smaller-screen findings. This plan ALSO owns creating the section skeleton (all H2s) that Plans 02-05 append into, so every subsequent plan can edit cleanly without merge-style conflicts.

Purpose: Phases 1134-1138 verify against this doc. A missing render mode or missing skeleton section blocks downstream planning. WALK-01 is the load-bearing v1030 requirement (per ROADMAP success criterion 1).

Output: `.planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md` with full skeleton + populated render-mode tables + smaller-screen findings + Phase 1134-1138 routing table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/1133-audit-first-builder-walkthrough/1133-CONTEXT.md
@.planning/milestones/v1011-MILESTONE-AUDIT.md
@.planning/milestones/v1028-MILESTONE-AUDIT.md
@frontend/src/components/builder/builder-action-contract.ts
@frontend/src/components/builder/map-sync.ts
@frontend/src/components/builder/BuilderMap.tsx
@frontend/src/components/builder/layer-adapters/fill-adapter.ts
@frontend/src/components/builder/layer-adapters/line-adapter.ts
@frontend/src/components/builder/layer-adapters/circle-adapter.ts
@frontend/src/components/builder/layer-adapters/symbol-adapter.ts
@frontend/src/components/builder/layer-adapters/heatmap-adapter.ts
@frontend/src/components/builder/layer-adapters/cluster-adapter.ts
@frontend/src/components/builder/layer-adapters/raster-adapter.ts
@frontend/src/components/builder/layer-adapters/hillshade-adapter.ts

<interfaces>
<!-- Anchor surfaces the audit doc references. Recorded here so the executor does not have to grep them again. -->

Render-mode layer-adapter files (live on `main`):
- fill-adapter.ts
- line-adapter.ts
- circle-adapter.ts
- symbol-adapter.ts
- heatmap-adapter.ts
- cluster-adapter.ts
- raster-adapter.ts
- hillshade-adapter.ts  (DEM/terrain)

Basemap is handled via `MapBasemapConfig` + `map-sync.ts` (not a per-render-mode adapter); v1011 UX-03 added `basemap_position` jsonb-additive ordering.

Canonical ADK map: `c39be324-6815-40e5-8143-00a2723827b2` (per project memory `260524-o57`, validated in v1011 11/11 live MCP smoke).

v1011 closures already shipped — DO NOT re-file as new gaps (cite v1011 if surface still matches the v1011 fix):
- BUG-01 visibility-toggle adapter contract → confirm `addLayers(visible)` honored across ALL adapters during MCP walk
- BUG-02 delete-layer optimistic + rollback
- BUG-03 rename-group rAF-deferred focus
- RESP-01 NavigationControl `top-left` + `data-builder-canvas` margin-top
- RESP-02 MapCoordReadout `right-14` offset + `showScale` prop
- RESP-03 `<SheetContent showCloseButton={false}>` opt-out
- INV-01 DETAIL LEVEL toggle REMOVED from `BasemapSublayerEditorScene.tsx:16-18` (positive-form pin: surface stays gone)

Routing-table targets (one row per finding maps to one owning phase):
- Phase 1134 — MAP-07/08/09/10/16/17/18/19/20/22 (tier-1 bugs + smaller-screen)
- Phase 1135 — AI-01/02/03/04/05/08/09 (chat polish; depends on 1134 stable boundary)
- Phase 1136 — EDITOR-RASTER-01..04, EDITOR-LINE-01/02, EDITOR-FILL-04, EDITOR-BASEMAP-02/03
- Phase 1137 — SHARE-02/03/04/06/07/09
- Phase 1138 — EASY-02/11/18
- Phase 1139 — QA-01/02/03/04 (close-gate; no new findings)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create audit doc skeleton with all H2 sections</name>
  <files>.planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md</files>
  <read_first>
    - .planning/phases/1133-audit-first-builder-walkthrough/1133-CONTEXT.md (`<specifics>` block confirms the audit doc shape — table columns `Surface | Bug Shape | Severity | Reproducer | Owning Phase`)
    - .planning/ROADMAP.md (Phase 1133 success criteria 1-5 — the 5 sections downstream plans append to)
    - .planning/REQUIREMENTS.md (WALK-01..05 are the 5 axes; routing-table targets MAP/AI/EDITOR/SHARE/EASY/QA)
  </read_first>
  <action>
    Write the initial audit doc with the agreed top-of-file structure. The exact section list (in this order, all H2 unless noted):

    1. `# v1030 Builder Walkthrough Audit` (H1, single)
    2. `## Methodology` — record: canonical map id `c39be324-6815-40e5-8143-00a2723827b2`, viewport defaults (1440×900 primary, 800×600 smaller-screen pass), env (`localhost:8080` Vite dev proxy → `api:8000`), MCP driver invocation context (`/gsd-autonomous --use-playwright-mcp`), date-of-walk placeholder for the executor to fill, browser console capture method (record all console.error / console.warn).
    3. `## Render Mode: fill` (table header only — populated in Task 2)
    4. `## Render Mode: line` (table header only)
    5. `## Render Mode: circle` (table header only)
    6. `## Render Mode: symbol` (table header only)
    7. `## Render Mode: heatmap` (table header only)
    8. `## Render Mode: cluster` (table header only)
    9. `## Render Mode: raster` (table header only)
    10. `## Render Mode: basemap` (table header only)
    11. `## Render Mode: DEM/terrain` (table header only)
    12. `## Smaller-Screen (≤800px) Findings` (table header only)
    13. `## AI Consumer-Gating Matrix` (header + STUB note "Populated by Plan 02 — see 1133-02-PLAN.md")
    14. `## todo.md Staleness Pass` (header + STUB note "Populated by Plan 03 — see 1133-03-PLAN.md")
    15. `## Invariant Grep Checks` (header + STUB note "Populated by Plan 04 — see 1133-04-PLAN.md")
    16. `## SHARE-08 Disposition` (header + STUB note "Populated by Plan 05 — see 1133-05-PLAN.md")
    17. `## Phase 1134-1138 Routing Table` (table header only — populated in Task 3 of this plan)

    Render-mode table header (use IDENTICAL columns across every render mode for the routing-table consolidation):
    `| Finding ID | Surface | Bug Shape | Severity (P0/P1/P2) | Reproducer (map id + steps) | Owning Phase | REQ ID |`

    Smaller-Screen header:
    `| Finding ID | Surface | Viewport | Bug Shape | Severity | Reproducer | Owning Phase | REQ ID |`

    Phase 1134-1138 Routing Table header:
    `| Finding ID | Phase | REQ ID | Surface | Severity | Notes |`

    Finding-ID convention: `WALK-{render-mode-letter}-{nn}` where letter ∈ {F,L,C,S,H,X,R,B,D} (fill/line/circle/symbol/heatmap/cluster/raster/basemap/DEM). Smaller-screen finds use prefix `WALK-SS-{nn}`. Each ID stable across the doc — referenced from the routing table.

    Do NOT populate finding rows yet; only the skeleton. Plans 02-05 append into their stub sections; Tasks 2-3 of this plan populate render-mode tables, smaller-screen findings, and routing table.
  </action>
  <verify>
    <automated>grep -c "^## " .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md | grep -qE "^1[5-9]$|^[2-9][0-9]+$" &amp;&amp; grep -q "^## Methodology" .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md &amp;&amp; grep -q "^## Render Mode: fill" .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md &amp;&amp; grep -q "^## Render Mode: DEM/terrain" .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md &amp;&amp; grep -q "^## Phase 1134-1138 Routing Table" .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - The file contains ≥15 H2 sections (one each for the 9 render modes + smaller-screen + AI matrix stub + todo.md stub + invariant grep stub + SHARE-08 stub + routing table + methodology = 16).
    - Render-mode H2 names match EXACTLY: `## Render Mode: fill`, `## Render Mode: line`, `## Render Mode: circle`, `## Render Mode: symbol`, `## Render Mode: heatmap`, `## Render Mode: cluster`, `## Render Mode: raster`, `## Render Mode: basemap`, `## Render Mode: DEM/terrain`.
    - Methodology section names the canonical map id `c39be324-6815-40e5-8143-00a2723827b2` and records `localhost:8080` as the env.
    - Stub sections (`## AI Consumer-Gating Matrix`, `## todo.md Staleness Pass`, `## Invariant Grep Checks`, `## SHARE-08 Disposition`) contain a one-line "Populated by Plan NN" note pointing to the corresponding plan file (02, 03, 04, 05).
  </acceptance_criteria>
  <done>Audit doc exists with the full H2 skeleton; downstream Plans 02-05 can append into their stub sections without conflict.</done>
</task>

<task type="auto">
  <name>Task 2: Run live Playwright MCP walkthrough and populate per-mode tables</name>
  <files>.planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md</files>
  <read_first>
    - .planning/milestones/v1011-MILESTONE-AUDIT.md (BUG-01..03, UX-01..04, RESP-01..03, INV-01 closures — confirm regression coverage during the walk)
    - frontend/src/components/builder/BasemapSublayerEditorScene.tsx lines 16-18 (DETAIL LEVEL removed comment — confirm surface stays gone in DOM)
    - frontend/src/components/builder/layer-adapters/fill-adapter.ts, line-adapter.ts, circle-adapter.ts, symbol-adapter.ts, heatmap-adapter.ts, cluster-adapter.ts, raster-adapter.ts, hillshade-adapter.ts (each is the surface area to walk per render mode)
    - frontend/src/components/builder/MapBuilderPage.tsx / BuilderMap.tsx (entry points for the walk)
  </read_first>
  <action>
    Drive a live Playwright MCP walkthrough at `http://localhost:8080` and populate the 9 render-mode H2 sections + the Smaller-Screen findings table created in Task 1. Operate against the canonical ADK map `c39be324-6815-40e5-8143-00a2723827b2` first; for render modes the canonical doesn't exercise, navigate to at least one representative map per mode (use the maps list endpoint or pick from project memory — record the chosen map id in the Reproducer column).

    Stack health precondition: before opening MCP, confirm `docker compose ps` shows api / db / titiler / worker / frontend healthy. If not, run `docker compose up -d` and wait for health.

    Per render mode, exercise these surfaces (apply to each adapter type that supports the gesture):
    - Visibility toggle off → confirm map reflects; toggle on → confirm map reflects (BUG-01 regression watch).
    - Opacity slider min/mid/max → confirm visible change + no console errors.
    - Color picker change → confirm visible change + no console errors.
    - Delete from row (trash icon) → confirm layer removed from stack AND from MapLibre canvas AND from style.json (no orphan source) (BUG-02 regression watch).
    - Rename group → confirm rAF-deferred focus lands in the rename input on first paint (BUG-03 regression watch).
    - Drag-reorder within stack (where applicable) → confirm v1011 dnd-kit `disabled.droppable` per-drag-source contract still holds (no basemap-group becoming collision target during catalog drags).
    - Save → reload → confirm layer paint/layout state survives the round-trip.

    Smaller-screen (≤800px viewport) pass: resize MCP browser to 800×600, then exercise the same surfaces with focus on layout collision detection:
    - Right-sidebar Sheet vs NavigationControl overlap (v1011 RESP-01 regression watch).
    - Lat/long pill vs map widgets (v1011 RESP-02 regression watch).
    - Basemap selector close-button doubling (v1011 RESP-03 regression watch — `<SheetContent showCloseButton={false}>`).
    - Sidebar collapse trigger position (per Pitfall #10: NavigationControl stays `top-left`; the FIX is the sidebar collapse trigger position, NOT moving NavigationControl).

    For EVERY finding row record: stable Finding ID per the convention from Task 1, surface name (file/component or feature), bug shape (one-line description of what's wrong), severity P0/P1/P2, reproducer (map id + reproducible steps for a future verifier), owning phase, REQ ID this maps to.

    For render modes with zero findings: record an explicit `_(No findings — verified clean for {visibility toggle, opacity, color, delete, rename, drag, save→reload} on YYYY-MM-DD via MCP)_` line under the table — DO NOT leave the table empty.

    For surfaces that match a v1011 fix and still work: record a row tagged `VERIFIED — v1011 {BUG-01|BUG-02|BUG-03|RESP-01|RESP-02|RESP-03|INV-01} regression PASS` with Severity = `—` (the regression test itself, NOT a new finding).

    Console capture: any `console.error` / `console.warn` encountered during the walk gets a row even if the user-visible behavior is correct (P2 at minimum). Use MCP's console-capture tooling — do not rely on memory.

    Record the walk date and elapsed time at the top of the Methodology section so future verifiers can correlate with git log.
  </action>
  <verify>
    <automated>for mode in fill line circle symbol heatmap cluster raster basemap "DEM/terrain"; do grep -A 30 "^## Render Mode: $mode" .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md | grep -qE "^\| WALK-[FLCSHXRBD]-|No findings — verified clean|VERIFIED — v1011" || { echo "FAIL: Render Mode '$mode' has no findings row, no clean line, and no v1011 regression row" &gt;&amp;2; exit 1; }; done &amp;&amp; grep -q "c39be324-6815-40e5-8143-00a2723827b2" .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 9 render-mode sections has at least one row OR an explicit "No findings — verified clean" line OR a v1011-regression PASS row.
    - The `## Smaller-Screen (≤800px) Findings` table covers all 4 known v1011-shape surfaces (sidebar overlap, NavigationControl, MapCoordReadout, SheetContent close button) — PASS rows count.
    - The canonical ADK map id `c39be324-6815-40e5-8143-00a2723827b2` appears in at least one Reproducer column.
    - Walk date and elapsed time recorded in Methodology section.
    - At least one row per render mode references an adapter file (fill-adapter.ts / line-adapter.ts / etc.) OR a builder component (UnifiedStackPanel / LayerEditorPanel / etc.) in the Surface column — naming the artifact, not paraphrasing.
  </acceptance_criteria>
  <done>Render-mode tables and smaller-screen findings table are populated from live MCP evidence; v1011 regressions either PASS or are filed as new findings.</done>
</task>

<task type="auto">
  <name>Task 3: Populate Phase 1134-1138 routing table from render-mode + smaller-screen findings</name>
  <files>.planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md</files>
  <read_first>
    - The audit doc itself (now populated by Task 2) — Finding IDs and their Owning Phase columns drive the routing table.
    - .planning/REQUIREMENTS.md traceability table (lines 128-173) — the REQ ID column on each row must match a v1030 v1 requirement; orphaned findings → flag for v1031 in the same row's Notes column.
  </read_first>
  <action>
    For every non-PASS finding row across all 9 render-mode tables and the Smaller-Screen table, append a corresponding row to `## Phase 1134-1138 Routing Table` mapping `Finding ID` → `Phase` → `REQ ID` → `Surface` → `Severity` → `Notes`.

    Routing rules (use these unless a finding demands otherwise — call out the override in Notes):
    - Delete-layer / visibility-toggle / rename-group / smaller-screen layout findings → Phase 1134 (MAP-* requirements).
    - AI chat / suggestion chip / disabled-state findings → Phase 1135 (AI-* requirements). NOTE: Plan 02 owns the AI matrix; this task does NOT need to pre-route AI findings — leave them for Plan 02 to route into the matrix section + routing table.
    - Per-editor table-stakes findings (raster sliders, line-cap/line-join, fill extrusion hint, "No basemap" preset) → Phase 1136 (EDITOR-* requirements).
    - Share/embed findings → Phase 1137 (SHARE-* requirements).
    - Easy-win cross-cutting findings (Cmd/Ctrl+S, popup URL/media, empty-layer state) → Phase 1138 (EASY-* requirements).
    - Findings that have no v1030 REQ ID — record REQ ID as `(unmapped)` and Notes as "Flag to v1031 carry-forward in REQUIREMENTS.md Future Requirements". Do NOT silently drop.

    PASS rows (verified-clean / v1011-regression-still-live) do NOT need routing-table rows — the audit doc retains them as regression evidence in the render-mode section.

    For each routing row, confirm the REQ ID exists in `REQUIREMENTS.md` traceability table (lines 128-173); if it doesn't, treat as "(unmapped)".
  </action>
  <verify>
    <automated>grep -A 200 "^## Phase 1134-1138 Routing Table" .planning/phases/1133-audit-first-builder-walkthrough/1133-BUILDER-WALKTHROUGH-AUDIT.md | grep -cE "^\| WALK-(F|L|C|S|H|X|R|B|D|SS)-" | awk '{ if ($1 &gt;= 1) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `## Phase 1134-1138 Routing Table` contains at least 1 routed row per non-PASS finding across the 9 render-mode tables + smaller-screen table.
    - Every routing-table row has a non-empty Phase column matching `113[4-9]` AND a REQ ID column matching either a v1030 REQ pattern (MAP/AI/EDITOR/SHARE/EASY/QA followed by digits) OR the literal `(unmapped)`.
    - Zero rows route to Phase 1133 itself (audit is read-only this phase).
    - Zero rows route to phases outside 1133-1139.
  </acceptance_criteria>
  <done>Routing table is the canonical handoff sheet: downstream-phase planners read it to know which finding closes which REQ.</done>
</task>

</tasks>

<verification>
- The audit doc exists at the path declared in `files_modified`.
- All 9 render-mode H2 sections are present and named exactly as specified.
- Every render-mode table is populated OR has an explicit "No findings — verified clean" line OR a v1011-regression PASS row.
- Smaller-Screen findings table has rows covering all 4 known v1011-shape surfaces.
- Phase 1134-1138 Routing Table has at least 1 routed row per non-PASS finding.
- Canonical ADK map id `c39be324-6815-40e5-8143-00a2723827b2` appears at least once.
</verification>

<success_criteria>
- WALK-01 satisfied: every render mode walked live + tabulated.
- Routing table is the canonical handoff sheet for Phases 1134-1138 planners.
- Stub sections for Plans 02-05 are in place; downstream plans can append cleanly.
</success_criteria>

<output>
Create `.planning/phases/1133-audit-first-builder-walkthrough/1133-01-SUMMARY.md` when done.
</output>
