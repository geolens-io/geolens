---
phase: 1095-cascade-fix-wr-02-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_tiles.py
  - backend/tests/test_embed_tokens.py
  - backend/tests/test_tile_signing.py
  - backend/tests/test_fixture_isolation_v1020.py
  - .planning/REQUIREMENTS.md
  - .planning/phases/1095-cascade-fix-wr-02-closure/1095-01-SUMMARY.md
autonomous: true
requirements: [PARA-01]
must_haves:
  truths:
    - "Each of the 3 `_init_tile_pool_for_tests` fixtures wraps its `asyncpg.create_pool(...)` call in `_run_with_too_many_clients_retry` so transient `TooManyConnectionsError` under `-n auto` is retried instead of failing the fixture."
    - "A new regression pin `test_init_tile_pool_retries_on_transient_too_many_clients` in `backend/tests/test_fixture_isolation_v1020.py` proves the wrap pattern retries on injected `TooManyConnectionsError` and succeeds on a later attempt."
    - "`pytest -n auto` 3-run baseline with stale-DB cleanup between runs produces ≤30 distinct (failed+errors) per run deterministically (PARA-01 acceptance criterion (a))."
    - "Sequential 3055/0/38 and `-n 4` 3054/0/38 baselines preserved — HARD INVARIANT per v1019 TD-13 (PARA-01 acceptance criteria (b)+(c))."
    - "REQUIREMENTS.md PARA-01 traceability row flips `[ ]` → `[x]` and `Pending` → `Complete` in the SAME commit as `1095-01-SUMMARY.md` per v1019 TD-13 `requirements_traceability_flip`."
  artifacts:
    - path: "backend/tests/test_tiles.py"
      provides: "test_tiles fixture wraps `asyncpg.create_pool` in `_run_with_too_many_clients_retry`"
      contains: "_run_with_too_many_clients_retry"
    - path: "backend/tests/test_embed_tokens.py"
      provides: "test_embed_tokens fixture wraps `asyncpg.create_pool` in `_run_with_too_many_clients_retry`"
      contains: "_run_with_too_many_clients_retry"
    - path: "backend/tests/test_tile_signing.py"
      provides: "test_tile_signing fixture wraps `asyncpg.create_pool` in `_run_with_too_many_clients_retry`"
      contains: "_run_with_too_many_clients_retry"
    - path: "backend/tests/test_fixture_isolation_v1020.py"
      provides: "new pin `test_init_tile_pool_retries_on_transient_too_many_clients`"
      contains: "def test_init_tile_pool_retries_on_transient_too_many_clients"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PARA-01 row flipped to `[x]` + `Complete`"
      contains: "PARA-01"
    - path: ".planning/phases/1095-cascade-fix-wr-02-closure/1095-01-SUMMARY.md"
      provides: "Plan 01 SUMMARY committed in same atomic 6-file commit"
      min_lines: 30
  key_links:
    - from: "backend/tests/test_tiles.py:_init_tile_pool_for_tests"
      to: "backend/tests/conftest.py:_run_with_too_many_clients_retry"
      via: "from tests.conftest import _run_with_too_many_clients_retry"
      pattern: "_run_with_too_many_clients_retry"
    - from: "backend/tests/test_embed_tokens.py:_init_tile_pool_for_tests"
      to: "backend/tests/conftest.py:_run_with_too_many_clients_retry"
      via: "from tests.conftest import _run_with_too_many_clients_retry"
      pattern: "_run_with_too_many_clients_retry"
    - from: "backend/tests/test_tile_signing.py:_init_tile_pool_for_tests"
      to: "backend/tests/conftest.py:_run_with_too_many_clients_retry"
      via: "from tests.conftest import _run_with_too_many_clients_retry"
      pattern: "_run_with_too_many_clients_retry"
    - from: "backend/tests/test_fixture_isolation_v1020.py:test_init_tile_pool_retries_on_transient_too_many_clients"
      to: "backend/tests/conftest.py:_run_with_too_many_clients_retry"
      via: "from tests.conftest import _run_with_too_many_clients_retry (already imported, see line 39)"
      pattern: "_run_with_too_many_clients_retry"
---

<objective>
Apply Shape A* (per audit `.planning/audits/PYTEST-NAUTO-CATEGORY-4-1-v1022.md` Section 3.2): wrap the 3 sibling `_init_tile_pool_for_tests` fixtures' direct `asyncpg.create_pool(...)` calls in the existing `_run_with_too_many_clients_retry` envelope at `backend/tests/conftest.py:359`. Add one regression pin proving the wrap retries on injected `TooManyConnectionsError`. Re-run `pytest -n auto` 3-run baseline with stale-DB cleanup and confirm ≤30 distinct deterministic per run. Atomic-6-file commit flips PARA-01 in REQUIREMENTS.md.

Purpose: Close PARA-01 acceptance criteria (a), (b), (c), (d) — (e) already shipped at Phase 1094. The 3 sibling fixture copies bypass ALL conftest.py retry envelopes (audit Section 1.4 dominant root cause: 16 workers × 3 conns = 48-conn demand vs `max_connections=30` ceiling); wrapping them in the existing 7s-budget retry envelope absorbs the bursty contention window without changing `max_connections`, the engine wrapper, or the `-n auto` worker count.

Output: 3 edited fixture files + 1 new pin + 1 REQUIREMENTS flip + 1 SUMMARY = 6 files in a single atomic commit per v1019 TD-13 rule #3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/1095-cascade-fix-wr-02-closure/1095-CONTEXT.md
@.planning/audits/PYTEST-NAUTO-CATEGORY-4-1-v1022.md
@.planning/phases/1094-cascade-spike/1094-01-SUMMARY.md

<interfaces>
Key signatures and patterns the executor needs. Extracted from codebase at plan-time.
Executor should use these directly — no codebase exploration needed.

From `backend/tests/conftest.py:359` (the envelope being reused) — async-native helper.
Signature: `async def _run_with_too_many_clients_retry(coro_fn, sleep_fn=asyncio.sleep, backoffs=_SETUP_PHASE_RETRY_BACKOFFS)`.
Catches `_TRANSIENT_CONTENTION_EXCEPTIONS = (OperationalError, asyncpg.exceptions.TooManyConnectionsError, asyncpg.exceptions.CannotConnectNowError)` (conftest.py:352).
Backoff schedule `_SETUP_PHASE_RETRY_BACKOFFS = (1.0, 2.0, 4.0)` (conftest.py:333) = 7s total budget.
Non-contention `OperationalError` propagates immediately. Budget exhaustion re-raises loudly (NOT silent-swallow).
The `coro_fn` argument is a zero-arg async callable that returns the coroutine to be awaited on each attempt — each retry calls `coro_fn()` FRESH so the asyncpg connection is re-acquired (not reused). Match the existing canonical caller `_acquire_test_session_with_retry` for shape conventions.

Current shape of the 3 sibling fixtures (all 3 are near-identical at the wrap site):

- `backend/tests/test_tiles.py:141-157` — plain `@pytest.fixture` decorator, no autouse, no gate. The 3-line `await asyncpg.create_pool(dsn=dsn, min_size=1, max_size=3, command_timeout=10)` call lives at line 151. Surrounding lines establish the `dsn` (line 150), assign result to `pool_module._tile_pool` (line 154), `yield` (line 155), and clean up via `await pool.close()` + `pool_module._tile_pool = None` (lines 156-157). Preserve every surrounding line; only the 3-line `await asyncpg.create_pool(...)` call changes.

- `backend/tests/test_embed_tokens.py:37-62` — `@pytest.fixture(autouse=True)` + `request` parameter + intersection gate at lines 40-51 (`db_fixtures = {...}; if not db_fixtures.intersection(request.fixturenames): yield; return`). The wrap only applies INSIDE the post-gate branch at line 56. PRESERVE the intersection gate verbatim — its purpose is to short-circuit non-DB-fixture tests so they pay zero asyncpg-pool-init cost; widening it would cost runtime on tile-unrelated tests.

- `backend/tests/test_tile_signing.py:101-113` — plain `@pytest.fixture` decorator, no autouse, no gate (identical shape to test_tiles.py). The 3-line `await asyncpg.create_pool(...)` call lives at line 107.

Wrap pattern to apply at each call site (replace the `pool = await asyncpg.create_pool(...)` block):

```
pool = await _run_with_too_many_clients_retry(
    lambda: asyncpg.create_pool(
        dsn=dsn, min_size=1, max_size=3, command_timeout=10
    )
)
```

The `lambda: asyncpg.create_pool(...)` is the zero-arg async callable contract; it returns the coroutine that the envelope awaits on each attempt. The envelope catches `TooManyConnectionsError` (already in `_TRANSIENT_CONTENTION_EXCEPTIONS` at conftest.py:354), sleeps via `_SETUP_PHASE_RETRY_BACKOFFS`, retries up to 4 attempts total.

Import to add at each fixture file (all 3): `from tests.conftest import _run_with_too_many_clients_retry`. `asyncpg` is already imported at the top of each file (validated at plan-time: `test_tiles.py:16`, `test_embed_tokens.py:21`, `test_tile_signing.py:17`).

Existing pin convention in `backend/tests/test_fixture_isolation_v1020.py:33-40` — the import block already pulls in `_run_with_too_many_clients_retry` + `_SETUP_PHASE_RETRY_BACKOFFS` + `_RetryingAsyncEngine` + `_make_op_error` helper at line 43 (reuse for SQLAlchemy-wrapped contention). For RAW asyncpg contention injection, see existing pattern at `test_engine_retry_catches_raw_asyncpg_too_many_connections:977` which uses `asyncpg.exceptions.TooManyConnectionsError("sorry, too many clients already")` directly — mirror that exact construction.
</interfaces>

<verified_line_numbers>
All line numbers re-validated via `git grep -n` at plan-time per v1019 TD-13:

- `backend/tests/conftest.py:118` — `_SETUP_STAGGER_SECONDS = 5.0`
- `backend/tests/conftest.py:256` — `_CREATE_DB_RETRY_BACKOFFS = (1.0, 2.0, 4.0)`
- `backend/tests/conftest.py:333` — `_SETUP_PHASE_RETRY_BACKOFFS = (1.0, 2.0, 4.0)`
- `backend/tests/conftest.py:352` — `_TRANSIENT_CONTENTION_EXCEPTIONS` tuple
- `backend/tests/conftest.py:354` — `asyncpg.exceptions.TooManyConnectionsError` member of tuple
- `backend/tests/conftest.py:359` — `async def _run_with_too_many_clients_retry(`
- `backend/tests/test_tiles.py:142` — `async def _init_tile_pool_for_tests():` (fixture)
- `backend/tests/test_tiles.py:151` — `pool = await asyncpg.create_pool(` (FIX SITE)
- `backend/tests/test_embed_tokens.py:38` — `async def _init_tile_pool_for_tests(request):` (autouse fixture)
- `backend/tests/test_embed_tokens.py:56` — `pool = await asyncpg.create_pool(` (FIX SITE)
- `backend/tests/test_tile_signing.py:102` — `async def _init_tile_pool_for_tests():` (fixture)
- `backend/tests/test_tile_signing.py:107` — `pool = await asyncpg.create_pool(` (FIX SITE)
- `backend/tests/test_fixture_isolation_v1020.py:33-40` — existing import block (already pulls `_run_with_too_many_clients_retry`)
- `backend/tests/test_fixture_isolation_v1020.py:904` — existing `test_engine_retry_succeeds_on_transient_too_many_clients` (naming reference)
- `backend/tests/test_fixture_isolation_v1020.py:977` — existing `test_engine_retry_catches_raw_asyncpg_too_many_connections` (raw asyncpg injection reference)
- `backend/tests/test_fixture_isolation_v1020.py:1029` — existing `test_engine_retry_propagates_non_transient_operational_error` (naming reference)
- `backend/tests/test_fixture_isolation_v1020.py:1070` — existing `test_engine_retry_exhausts_budget_then_fails_loudly` (naming reference)
- `backend/tests/test_fixture_isolation_v1020.py:1114` — current EOF (append new pin AFTER this line)
- 32-test pin subset = `test_fixture_isolation_v1020` (15 tests) + `test_conftest_pool_sizing` (10) + `test_conftest_lifecycle` (6) + 1 skipped — validated at plan-time via `pytest --collect-only -q`
</verified_line_numbers>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight gate (validate line numbers + 32-test pin baseline + docker stack health)</name>
  <files>(read-only; no edits)</files>
  <action>
Validate plan-time citations are still accurate at executor-time HEAD and the baseline subset is green BEFORE any refactor lands. Three checks, all required to PASS before Task 2.

Check 1 — re-validate the 19 line-numbered references in `<verified_line_numbers>` above. Run `git grep -nE "def _run_with_too_many_clients_retry|_SETUP_PHASE_RETRY_BACKOFFS = |_TRANSIENT_CONTENTION_EXCEPTIONS = \(|_CREATE_DB_RETRY_BACKOFFS = |_SETUP_STAGGER_SECONDS = " backend/tests/conftest.py` plus `git grep -nE "async def _init_tile_pool_for_tests|asyncpg.create_pool" backend/tests/test_tiles.py backend/tests/test_embed_tokens.py backend/tests/test_tile_signing.py` plus `git grep -nE "^def test_engine_retry_" backend/tests/test_fixture_isolation_v1020.py`. If ANY line drifts by more than ±5 lines, STOP and re-anchor the fix-site call before proceeding (v1019 TD-13 `req_citation_pinning` rule).

Check 2 — 32-test pin subset spot-check (sequential baseline preservation proxy per audit Section 2.4). Run `cd backend && set -a && source ../.env.test && set +a && uv run pytest -k "test_fixture_isolation_v1020 or test_conftest_pool_sizing or test_conftest_lifecycle" -v`. Expected `32 passed, 1 skipped, 3077 deselected in ~4s`. This is the cheap proxy for full 9min sequential re-run; appropriate because no code changes have shipped yet. MUST PASS before any refactor.

Check 3 — docker stack health gate (required for the `pytest -n auto` measurement gate at Task 7 per MEMORY.md `.env.test` load-bearing note). Run `docker compose ps -a`. Expected 5 services (api, worker, db, frontend, titiler) healthy + `migrate` exited 0. If degraded, run `docker compose up -d --build api worker db frontend titiler` and re-check.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && set -a && source ../.env.test && set +a && uv run pytest -k "test_fixture_isolation_v1020 or test_conftest_pool_sizing or test_conftest_lifecycle" --no-header -q 2>&amp;1 | tail -3 | grep -E "32 passed"</automated>
  </verify>
  <done>All 19 line-number citations verified; 32-test pin subset is `32 passed, 1 skipped`; docker stack is 5/5 healthy. Stop and report drift if any check fails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrap fix site 1 — `backend/tests/test_tiles.py:151`</name>
  <files>backend/tests/test_tiles.py</files>
  <behavior>
    - The wrapped call returns the same `asyncpg.Pool` instance shape as the unwrapped call (same kwargs: `dsn`, `min_size=1`, `max_size=3`, `command_timeout=10`).
    - On a successful first attempt, the envelope adds zero observable overhead (no sleep, returns immediately on the first attempt's coroutine result).
    - On a transient `TooManyConnectionsError`, the envelope retries up to 3 times with backoffs (1.0, 2.0, 4.0) before re-raising.
    - The fixture's downstream `pool_module._tile_pool = pool` + `yield` + `await pool.close()` cleanup contract is preserved unchanged.
  </behavior>
  <action>
Add import at the top of `backend/tests/test_tiles.py` (group with the existing `from tests.factories import get_user_id` block at line 24): `from tests.conftest import _run_with_too_many_clients_retry`.

At fix site (current line 151 — the `pool = await asyncpg.create_pool(...)` 3-line call inside `_init_tile_pool_for_tests`), replace the existing `pool = await asyncpg.create_pool(dsn=dsn, min_size=1, max_size=3, command_timeout=10)` 3-line call with the wrap pattern shown in `<interfaces>` above (lambda-wrapped invocation passed to `_run_with_too_many_clients_retry`).

Keep all other lines in the fixture unchanged — the `dsn = ...` line ABOVE the call (line 150), the `pool_module._tile_pool = pool` + `yield` + `await pool.close()` + `pool_module._tile_pool = None` lines BELOW the call (lines 154-157).

Per audit Section 3.2 + CONTEXT.md decision: REUSE the existing envelope and existing `_SETUP_PHASE_RETRY_BACKOFFS = (1.0, 2.0, 4.0)` (7s budget). Do NOT add new helpers, new constants, or change `max_size=3` — the optional `max_size=2` knob is operator-discretion deferred per CONTEXT.md `<specifics>`; leave at 3 for this pass and let the post-fix `-n auto` measurement at Task 7 tell us if the retry envelope alone is sufficient.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && set -a && source ../.env.test && set +a && uv run pytest backend/tests/test_tiles.py --no-header -q 2>&amp;1 | tail -3</automated>
  </verify>
  <done>Import added; line 151 fix site wraps `asyncpg.create_pool` in `_run_with_too_many_clients_retry` via lambda; `pytest backend/tests/test_tiles.py` passes (same pass count as pre-refactor). `git grep -n "_run_with_too_many_clients_retry" backend/tests/test_tiles.py` returns at least 2 lines (the import + the call).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wrap fix site 2 — `backend/tests/test_embed_tokens.py:56`</name>
  <files>backend/tests/test_embed_tokens.py</files>
  <behavior>
    - Same as Task 2 but for the autouse fixture. The `db_fixtures.intersection(request.fixturenames)` gate at lines 40-51 stays unchanged — the wrap only applies INSIDE the `if`-branch, after the gate.
    - For tests with no DB-fixture intersection, the early-return path (lines 50-51 `yield; return`) is preserved verbatim — no retry overhead for non-DB tests.
  </behavior>
  <action>
Add import near the top of `backend/tests/test_embed_tokens.py` (group with existing `tests.factories` import at line 34): `from tests.conftest import _run_with_too_many_clients_retry`.

At fix site (current line 56 — the `pool = await asyncpg.create_pool(...)` 3-line call), replace with the same lambda-wrapped pattern as Task 2 (see `<interfaces>`).

DO NOT modify the `db_fixtures` intersection gate (lines 40-51) — its purpose is to short-circuit non-DB-fixture tests entirely so they pay zero asyncpg-pool-init cost. The wrap only applies inside the "did intersect" branch.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && set -a && source ../.env.test && set +a && uv run pytest backend/tests/test_embed_tokens.py --no-header -q 2>&amp;1 | tail -3</automated>
  </verify>
  <done>Import added; line 56 fix site wrapped; `db_fixtures` intersection gate unchanged; `pytest backend/tests/test_embed_tokens.py` passes (same pass count as pre-refactor). `git grep -n "_run_with_too_many_clients_retry" backend/tests/test_embed_tokens.py` returns at least 2 lines.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wrap fix site 3 — `backend/tests/test_tile_signing.py:107`</name>
  <files>backend/tests/test_tile_signing.py</files>
  <behavior>
    - Same as Task 2.
    - This file's fixture has neither autouse nor an intersection gate; the wrap is the same single-block shape as Task 2.
  </behavior>
  <action>
Add import near the top of `backend/tests/test_tile_signing.py` (group with existing `tests.` imports if present; otherwise add after the `app.` imports): `from tests.conftest import _run_with_too_many_clients_retry`.

At fix site (current line 107 — the `pool = await asyncpg.create_pool(...)` 3-line call), replace with the same lambda-wrapped pattern as Task 2 (see `<interfaces>`).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && set -a && source ../.env.test && set +a && uv run pytest backend/tests/test_tile_signing.py --no-header -q 2>&amp;1 | tail -3</automated>
  </verify>
  <done>Import added; line 107 fix site wrapped; `pytest backend/tests/test_tile_signing.py` passes (same pass count as pre-refactor). `git grep -n "_run_with_too_many_clients_retry" backend/tests/test_tile_signing.py` returns at least 2 lines.</done>
</task>

<task type="auto">
  <name>Task 5: Sanity check — combined 3-file post-refactor passes + zero unwrapped pool-creation calls remain</name>
  <files>(read-only verification; no edits)</files>
  <action>
Run the 3 test modules together to catch any cross-fixture regression (e.g. an import collision) before the regression pin lands. Run `cd backend && set -a && source ../.env.test && set +a && uv run pytest backend/tests/test_tiles.py backend/tests/test_embed_tokens.py backend/tests/test_tile_signing.py -v --no-header`.

Then `git grep -nE "asyncpg.create_pool" backend/tests/test_tiles.py backend/tests/test_embed_tokens.py backend/tests/test_tile_signing.py` — each of the 3 results MUST appear inside a `lambda:` argument to `_run_with_too_many_clients_retry`. Verify by `grep -B 2` on each file: every `asyncpg.create_pool` line must be preceded within 2 lines by `_run_with_too_many_clients_retry`. If any file shows an unwrapped call, return to Task 2/3/4 to fix.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && set -a && source ../.env.test && set +a && uv run pytest backend/tests/test_tiles.py backend/tests/test_embed_tokens.py backend/tests/test_tile_signing.py --no-header -q 2>&amp;1 | tail -3 && for f in backend/tests/test_tiles.py backend/tests/test_embed_tokens.py backend/tests/test_tile_signing.py; do grep -B 2 "asyncpg.create_pool" "/Users/ishiland/Code/geolens/$f" | grep -q "_run_with_too_many_clients_retry" && echo "OK: $f wrapped" || echo "FAIL: $f unwrapped"; done</automated>
  </verify>
  <done>All 3 modules pass; combined `pytest` exit 0 with same pass-count delta = 0 vs pre-refactor; all 3 files show `OK: ... wrapped` (no unwrapped `asyncpg.create_pool` remains in the 3 fixture files).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Add regression pin `test_init_tile_pool_retries_on_transient_too_many_clients`</name>
  <files>backend/tests/test_fixture_isolation_v1020.py</files>
  <behavior>
    - The pin proves the wrap pattern at the 3 fixture sites would retry on transient `TooManyConnectionsError` rather than failing on first attempt.
    - The pin does NOT require a live Postgres host — it monkey-patches `asyncpg.create_pool` to raise `TooManyConnectionsError` on the first 2 attempts then return a sentinel pool on the 3rd.
    - The pin invokes `_run_with_too_many_clients_retry` with the same `lambda: asyncpg.create_pool(...)` shape used at the 3 fixture sites — so a future regression that drops the wrap at any fixture site is caught by drift between the pin's invocation shape and the fixture's invocation shape (the pin documents the expected calling convention).
    - Assertions: (a) the returned pool is the sentinel from the 3rd attempt, (b) `asyncpg.create_pool` was called exactly 3 times, (c) the injected `sleep_fn` was called exactly 2 times with backoffs `(1.0, 2.0)`, (d) `_SETUP_PHASE_RETRY_BACKOFFS == (1.0, 2.0, 4.0)` drift-guard.
  </behavior>
  <action>
Append a new test function `test_init_tile_pool_retries_on_transient_too_many_clients` at the end of `backend/tests/test_fixture_isolation_v1020.py` (after current line 1114). The import block at lines 33-40 already pulls in `_run_with_too_many_clients_retry` + `_SETUP_PHASE_RETRY_BACKOFFS` — REUSE both directly, no new imports needed except `asyncpg.exceptions` (mirror line 996 of existing `test_engine_retry_catches_raw_asyncpg_too_many_connections`).

Test body shape (mirror the existing `test_engine_retry_catches_raw_asyncpg_too_many_connections:977` pattern for raw asyncpg injection — `asyncpg.exceptions.TooManyConnectionsError("sorry, too many clients already")` literal). Construct a fake `asyncpg.create_pool` callable that tracks call count: on calls 1+2 it raises `TooManyConnectionsError("sorry, too many clients already")`; on call 3 it returns a sentinel `MagicMock(name="pool")`. Inject a recording `async def fake_sleep(seconds): sleep_calls.append(seconds)`.

Invoke via the same wrap-shape used at the 3 fixture sites: `result = asyncio.run(_run_with_too_many_clients_retry(lambda: fake_create_pool(dsn="...", min_size=1, max_size=3, command_timeout=10), sleep_fn=fake_sleep))`. The lambda invocation must mirror the 3 fixture sites EXACTLY (same kwargs in same order) so any future refactor that breaks the fixture invocation shape also breaks the pin.

Assertions (4 total): (a) `result` is the sentinel MagicMock (proves retry returned, not raise), (b) `call_count == 3` (proves attempts 1+2 failed and attempt 3 succeeded), (c) `sleep_calls == [1.0, 2.0]` (proves 2 sleeps between 3 attempts at the canonical first-two backoffs), (d) `assert _SETUP_PHASE_RETRY_BACKOFFS == (1.0, 2.0, 4.0)` drift-guard (mirror line 968 of existing engine-retry pin).

Optional but recommended per CONTEXT.md `<specifics>` Plan 01 Step 6 + audit Section 5.1: ALSO add `test_init_tile_pool_no_retry_pre_fix_raises_too_many_clients` as an `@pytest.mark.xfail(raises=asyncpg.exceptions.TooManyConnectionsError, reason="pre-fix regression pin: see .planning/audits/PYTEST-NAUTO-CATEGORY-4-1-v1022.md Section 3.2")` that calls a fake_create_pool directly (no envelope) and expects the TMC to propagate. This is documentation evidence of the bug shape. Skip if context budget tight; add in Plan 02 if deferred.

Name the NEW pin exactly `test_init_tile_pool_retries_on_transient_too_many_clients` — node-ID `backend/tests/test_fixture_isolation_v1020.py::test_init_tile_pool_retries_on_transient_too_many_clients` per v1019 TD-13 `req_citation_pinning` rule. Cite this node-ID in the Plan 01 SUMMARY's `key-files` block.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && set -a && source ../.env.test && set +a && uv run pytest backend/tests/test_fixture_isolation_v1020.py::test_init_tile_pool_retries_on_transient_too_many_clients -v --no-header 2>&amp;1 | tail -5 | grep -E "1 passed|2 passed"</automated>
  </verify>
  <done>New pin `test_init_tile_pool_retries_on_transient_too_many_clients` PASSES. Combined `pytest backend/tests/test_fixture_isolation_v1020.py` shows pre-refactor pass count + 1 (or +2 if xfail pin included). 32-test pin subset re-runs as `33 passed, 1 skipped, 3076 deselected` (was `32 passed`).</done>
</task>

<task type="auto">
  <name>Task 7: `pytest -n auto` 3-run post-fix baseline measurement (PARA-01 acceptance criterion (a))</name>
  <files>(measurement; logs written to /tmp/)</files>
  <action>
Re-run the audit Section 2 reproduction recipe POST-FIX to confirm Shape A* dropped the cascade. Three runs of `pytest -n auto tests/` with stale-DB cleanup between runs.

For each run RUN in {1, 2, 3}:

Step (a) — stale-DB cleanup BEFORE the run (mirroring audit Section 2.1 / PYTEST-XDIST-PERF-v1020.md Section 1 Step 1b):
- Enumerate via `docker compose exec -T db psql -U geolens -d geolens -At -c "SELECT datname FROM pg_database WHERE datname LIKE 'geolens%test_gw%' OR datname LIKE 'geolens%test_master%';" > /tmp/v1022-1095-stale-dbs-run${RUN}.txt`
- Generate DROP SQL by iterating the enumerated list into `DROP DATABASE IF EXISTS "$db";` and write to `/tmp/v1022-1095-drop-stale-run${RUN}.sql`
- Apply: `docker compose exec -T db psql -U geolens -d geolens < /tmp/v1022-1095-drop-stale-run${RUN}.sql`

Step (b) — invoke pytest with both .log + .xml capture per audit Section 2.2:
- `cd backend && set -a && source ../.env.test && set +a && uv run pytest -n auto tests/ --junitxml=/tmp/v1022-1095-post-fix-nauto-run${RUN}.xml > /tmp/v1022-1095-post-fix-nauto-run${RUN}.log 2>&1`

Step (c) — extract the 7 measurement numbers per run per audit Section 2.3:
- `tail -1 /tmp/v1022-1095-post-fix-nauto-run${RUN}.log` (passed/failed/errors/wallclock)
- `grep -cE '^E.*TooManyConnectionsError|^\|.*TooManyConnectionsError' /tmp/v1022-1095-post-fix-nauto-run${RUN}.log` (TMC exception frames)
- `grep -cE '^E.*InvalidCatalogNameError|^\|.*InvalidCatalogNameError' /tmp/v1022-1095-post-fix-nauto-run${RUN}.log` (ICN exception frames — should remain 0 per audit Section 1.1)
- distinct = failed + errors (the gate metric)

Acceptance gate: **distinct ≤ 30 deterministic across all 3 runs**. Pre-fix baseline was 14/14/21 (already ≤30 — the dominant cascade source at the 3 fixture sites; absorbing this surface should push the new baseline meaningfully lower, with residual failures concentrated in the pre-existing OOS rows + oauth flake-class per audit Section 1.2 delta analysis).

If gate is BREACHED on any run (distinct > 30 for that run): per CONTEXT.md `<deferred>`, options are (1) iter-2 in-checkpoint — tweak `_SETUP_PHASE_RETRY_BACKOFFS` upward OR identify a 4th fixture site the spike missed via `git grep -nE "asyncpg.create_pool" backend/tests/`; (2) escalate to v1023+ with documented residual surface. Land inline per v1021 Phase 1091 iter-2 precedent — do NOT silently accept a > 30 measurement.

Capture the 3-run measurement table to `/tmp/v1022-1095-post-fix-baseline.md` for embedding in 1095-01-SUMMARY.md at Task 8. Include the delta vs pre-fix baseline (14/14/21) and the qualitative breakdown of remaining failures by category (TMC / ICN / pre-existing OOS / oauth flake / other).
  </action>
  <verify>
    <automated>for RUN in 1 2 3; do DISTINCT=$(awk '/=========/{last=$0} END{print last}' /tmp/v1022-1095-post-fix-nauto-run${RUN}.log | grep -oE "[0-9]+ failed|[0-9]+ error" | awk '{sum+=$1} END {print sum+0}'); echo "Run ${RUN} distinct = ${DISTINCT}"; test "${DISTINCT}" -le 30 || { echo "FAIL: Run ${RUN} BREACHED ≤30 gate"; exit 1; }; done && echo "All 3 runs ≤30 distinct — PARA-01 (a) GREEN"</automated>
  </verify>
  <done>3 runs captured to `/tmp/v1022-1095-post-fix-nauto-run{1,2,3}.{log,xml}`. distinct ≤30 deterministic across all 3 runs. Measurement table written to `/tmp/v1022-1095-post-fix-baseline.md` for SUMMARY embedding. Delta vs pre-fix 14/14/21 baseline quantified. If BREACHED: iter-2 in-checkpoint per CONTEXT.md `<deferred>` (do NOT proceed to Task 8 with a failing gate).</done>
</task>

<task type="auto">
  <name>Task 8: Atomic-6-file commit + PARA-01 REQUIREMENTS traceability flip + SUMMARY.md write</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/1095-cascade-fix-wr-02-closure/1095-01-SUMMARY.md</files>
  <action>
Three-step close per CONTEXT.md rules #2, #3 (atomic-N-file commit + traceability flip).

Step (a) — flip PARA-01 in `.planning/REQUIREMENTS.md`. Edit the file in TWO places:

1. Line 22 `- [ ] **PARA-01**: ...` → `- [x] **PARA-01**: ...` (checkbox flip)
2. Line 75 (Traceability table) `| PARA-01 | Phase 1094 (spike) + Phase 1095 (fix) | Pending |` → `| PARA-01 | Phase 1094 (spike) + Phase 1095 (fix) | Complete |`

Per v1019 TD-13 `requirements_traceability_flip`: the flip MUST land in the SAME commit as `1095-01-SUMMARY.md` — NOT a separate commit, NOT a follow-up.

Step (b) — write `.planning/phases/1095-cascade-fix-wr-02-closure/1095-01-SUMMARY.md` per the GSD `summary.md` template. Required frontmatter sections: `requires` (Phase 1094 audit), `provides` (PARA-01 closure + Shape A* wrap at 3 fixture sites + new pin), `affects` (1095-02, 1096, 1097), `tech-stack.patterns` (Shape A* lambda-wrap of zero-arg async callable; envelope reuse over new-helper creation), `key-files.modified` (3 fixture files + REQUIREMENTS.md), `key-files.created` (new pin + this SUMMARY), `key-decisions` (Shape A* chosen over A/B/C/D/E/F per audit Section 3.3; `max_size=3` left unchanged for this pass), `requirements-completed` (`PARA-01`).

The SUMMARY body MUST include the Task 7 measurement table verbatim (3-run distinct counts + delta vs pre-fix 14/14/21 baseline + remaining-failure-by-category breakdown). Cite the new pin's full node-ID `backend/tests/test_fixture_isolation_v1020.py::test_init_tile_pool_retries_on_transient_too_many_clients` per v1019 TD-13 `req_citation_pinning` so Phase 1096/1097 planners can reference it without re-discovery.

Step (c) — atomic commit. Stage EXACTLY 6 files (verify via `git status` shows zero other staged files):
1. `backend/tests/test_tiles.py`
2. `backend/tests/test_embed_tokens.py`
3. `backend/tests/test_tile_signing.py`
4. `backend/tests/test_fixture_isolation_v1020.py`
5. `.planning/REQUIREMENTS.md`
6. `.planning/phases/1095-cascade-fix-wr-02-closure/1095-01-SUMMARY.md`

Commit message shape (mirror v1021 Phase 1093-02 style): `test(1095-01): Shape A* wrap _init_tile_pool_for_tests pool init in retry envelope; close PARA-01`. Body: 1-2 sentence summary of the cascade source + fix + measurement gate result + PARA-01 flip + reference to audit doc. Include `Closes PARA-01.` line per v1019 TD-13 trace convention.

If `git status` shows ANY 7th file staged (e.g. a stray .log artifact), unstage with `git restore --staged <file>` and recount. The atomic-6-file rule is per CONTEXT.md `<decisions>` `Atomic-N-file commit per plan` — drift to a 5- or 7-file commit means a hygiene step is missing.
  </action>
  <verify>
    <automated>STAGED=$(git diff --cached --name-only | wc -l | tr -d ' '); test "$STAGED" = "6" || { echo "FAIL: staged file count = $STAGED, expected 6"; git diff --cached --name-only; exit 1; }; grep -E "^\- \[x\] \*\*PARA-01\*\*" .planning/REQUIREMENTS.md && grep -E "^\| PARA-01 \| .* \| Complete \|" .planning/REQUIREMENTS.md && test -f .planning/phases/1095-cascade-fix-wr-02-closure/1095-01-SUMMARY.md && echo "Atomic-6-file commit ready"</automated>
  </verify>
  <done>Exactly 6 files staged: 3 fixture files + new pin + REQUIREMENTS.md (with both PARA-01 flips) + 1095-01-SUMMARY.md. Single atomic commit lands. `git log -1 --stat` shows 6 files changed. REQUIREMENTS.md PARA-01 row reads `[x]` + `Complete`. PARA-01 acceptance criteria (a)/(b)/(c)/(d) GREEN (plus (e) already shipped at Phase 1094) — full PARA-01 closure achieved.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-infra / production-code | Phase 1095 modifies test-fixture code ONLY; no production-code path changes. The 3 `_init_tile_pool_for_tests` fixtures live entirely in `backend/tests/*.py` and the `_run_with_too_many_clients_retry` envelope is in `backend/tests/conftest.py`. No production-code engine wrapping, no FastAPI request-handler change. |
| conftest helper API surface | `_run_with_too_many_clients_retry` is the public-ish helper consumed by 3 fixture files + the regression pin. Drift in its signature would break all 4 callers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1095-01-01 | Tampering | Test-fixture `asyncpg.create_pool` wrap | mitigate | Regression pin `test_init_tile_pool_retries_on_transient_too_many_clients` documents the expected wrap shape; CI gate on the pin prevents silent removal of the wrap. |
| T-1095-01-02 | Denial-of-Service | `_run_with_too_many_clients_retry` budget exhaustion | accept | Worst case = 7s blocking wait per fixture instance under sustained contention; bounded by `_SETUP_PHASE_RETRY_BACKOFFS = (1.0, 2.0, 4.0)`. Acceptable for a test-only retry envelope; production envelope (`backend/app/core/db/session.py`) is out-of-scope per REQUIREMENTS.md. |
| T-1095-01-03 | Repudiation | Silent-swallow of contention errors | mitigate | Existing envelope at conftest.py:454-456 re-raises loudly after budget exhaustion (audit-validated; matches v1020 audit Section 4.1 anti-pattern condemnation). Pin Task 6 assertion (a) proves retry returns the sentinel pool — non-success would fail the pin loudly. |
| T-1095-01-04 | Information-Disclosure | DSN exposure in retry-budget exception trace | accept | DSN is the local test DB DSN (postgresql://geolens@localhost:5434/...); no production secrets. Acceptable. |
| T-1095-01-05 | Elevation-of-Privilege | n/a | n/a | No privilege boundary crossed by a test-fixture retry wrap. |
| T-1095-01-SC | Tampering | npm/pip/cargo installs | n/a | Phase 1095 Plan 01 installs no new packages. The `asyncpg` import is pre-existing at `test_tiles.py:16` / `test_embed_tokens.py:21` / `test_tile_signing.py:17`. No legitimacy gate required. |

</threat_model>

<verification>

Plan 01 verification (rolls into phase-level verification at Plan 02 close):

- [ ] Task 1 — 32-test pin subset shows `32 passed, 1 skipped` pre-refactor
- [ ] Tasks 2-4 — each fixture file's `pytest <file>` passes post-refactor with pass-count delta = 0
- [ ] Task 5 — combined `pytest test_tiles.py test_embed_tokens.py test_tile_signing.py` passes; `grep -B 2` confirms 0 unwrapped `asyncpg.create_pool` calls remain in the 3 fixture files
- [ ] Task 6 — new pin `test_init_tile_pool_retries_on_transient_too_many_clients` PASSES; 32-test pin subset becomes `33 passed, 1 skipped`
- [ ] Task 7 — `pytest -n auto` 3-run baseline shows distinct ≤30 deterministic per run (gate captured to `/tmp/v1022-1095-post-fix-nauto-run{1,2,3}.{log,xml}`)
- [ ] Task 8 — `git diff --cached --name-only | wc -l` reads exactly `6`; REQUIREMENTS.md PARA-01 reads `[x]` + `Complete`; single commit ships

Sequential 3055/0/38 baseline preservation is verified via the 32-test pin subset proxy (audit Section 2.4) — full 9min sequential re-run is OPTIONAL for this plan and MANDATORY at Plan 02 close (where it serves as the rollup pre-tag gate).

</verification>

<success_criteria>

Plan 01 SUCCESS = all of:

1. 3 fixture files wrap their `asyncpg.create_pool` call in `_run_with_too_many_clients_retry` (Tasks 2-4)
2. Combined `pytest` of the 3 modules passes with pass-count delta = 0 vs pre-refactor (Task 5)
3. New regression pin `test_init_tile_pool_retries_on_transient_too_many_clients` PASSES (Task 6)
4. `pytest -n auto` 3-run baseline shows distinct ≤30 deterministic per run (Task 7)
5. Atomic-6-file commit lands with REQUIREMENTS.md PARA-01 flipped to `[x]` + `Complete` (Task 8)

</success_criteria>

<output>
Create `.planning/phases/1095-cascade-fix-wr-02-closure/1095-01-SUMMARY.md` per Task 8 (includes 3-run measurement table verbatim, new pin node-ID citation, PARA-01 closure rationale, link to audit Section 3.2 for the wrap pattern). Single atomic 6-file commit closes Plan 01.
</output>
