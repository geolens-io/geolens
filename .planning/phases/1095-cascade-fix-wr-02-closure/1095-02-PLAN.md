---
phase: 1095-cascade-fix-wr-02-closure
plan: 02
type: execute
wave: 2
depends_on: ["1095-01"]
files_modified:
  - backend/tests/conftest.py
  - backend/tests/test_fixture_isolation_v1020.py
  - .planning/REQUIREMENTS.md
  - .planning/phases/1095-cascade-fix-wr-02-closure/1095-02-SUMMARY.md
autonomous: true
requirements: [PARA-02]
must_haves:
  truths:
    - "`_invoke_sleep_in_sync_context` at `backend/tests/conftest.py:624` either yields the asyncio event loop non-blockingly (Shape Y1) OR carries a load-bearing inline-commented rationale + mitigation (Shape Y2). Silent deferral is NOT acceptable per PARA-02 acceptance criterion (a)."
    - "A new regression pin (suggested `test_engine_retry_yields_event_loop_during_backoff` co-located with the existing 4 `test_engine_retry_*` pins) asserts that concurrent asyncio task scheduling proceeds during the retry-backoff window — proves the loop is NOT starved for up to 7s budget (PARA-02 acceptance criterion (b))."
    - "All 4 existing `test_engine_retry_*` pins at `backend/tests/test_fixture_isolation_v1020.py:904, 977, 1029, 1070` continue passing (PARA-02 acceptance criterion (c))."
    - "Plan 01's post-fix `pytest -n auto` baseline (≤30 distinct deterministic per run) is preserved — no regression introduced by the WR-02 fix."
    - "Sequential 3055/0/38 baseline preserved as the rollup pre-tag gate at Plan 02 close (HARD INVARIANT per v1019 TD-13)."
    - "REQUIREMENTS.md PARA-02 traceability row flips `[ ]` → `[x]` and `Pending` → `Complete` in the SAME commit as `1095-02-SUMMARY.md` per v1019 TD-13 `requirements_traceability_flip`."
  artifacts:
    - path: "backend/tests/conftest.py"
      provides: "WR-02 fix at line 624 — `_invoke_sleep_in_sync_context` non-blocking yield OR load-bearing rationale"
      contains: "_invoke_sleep_in_sync_context"
    - path: "backend/tests/test_fixture_isolation_v1020.py"
      provides: "new pin asserting asyncio loop continues scheduling during retry-backoff"
      contains: "def test_engine_retry_yields_event_loop_during_backoff"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PARA-02 row flipped to `[x]` + `Complete`"
      contains: "PARA-02"
    - path: ".planning/phases/1095-cascade-fix-wr-02-closure/1095-02-SUMMARY.md"
      provides: "Plan 02 SUMMARY committed in same atomic 4-file commit"
      min_lines: 30
  key_links:
    - from: "backend/tests/conftest.py:_install_dbapi_connect_retry._retry_do_connect:706"
      to: "backend/tests/conftest.py:_invoke_sleep_in_sync_context:624"
      via: "_invoke_sleep_in_sync_context(sleep_fn, backoffs[attempt])"
      pattern: "_invoke_sleep_in_sync_context"
    - from: "backend/tests/conftest.py:_RetryingAsyncEngine.connect:801"
      to: "backend/tests/conftest.py:_invoke_sleep_in_sync_context:624"
      via: "_invoke_sleep_in_sync_context(self._sleep_fn, self._backoffs[attempt])"
      pattern: "_invoke_sleep_in_sync_context"
    - from: "backend/tests/test_fixture_isolation_v1020.py:test_engine_retry_yields_event_loop_during_backoff"
      to: "backend/tests/conftest.py:_invoke_sleep_in_sync_context"
      via: "indirect — invokes via `_RetryingAsyncEngine.connect()` with concurrent asyncio.create_task assertion"
      pattern: "_invoke_sleep_in_sync_context"
---

<objective>
Close PARA-02 — the WR-02 footgun at `backend/tests/conftest.py:624` where `_invoke_sleep_in_sync_context` calls blocking `time.sleep(seconds)` (line 652) when invoked from the production path (`sleep_fn is asyncio.sleep`), freezing the asyncio event loop for up to the full `_SETUP_PHASE_RETRY_BACKOFFS = (1.0, 2.0, 4.0)` 7s budget. Per audit Section 4.3, WR-02 is INDEPENDENT of PARA-01's cascade — but is a real footgun that needs closure for forward-safety.

Apply Shape Y1 (non-blocking yield — preferred per CONTEXT.md) OR Shape Y2 (load-bearing inline rationale + mitigation) based on whether the sync-context invocation constraint allows asyncio yield. Add a regression pin asserting concurrent task scheduling proceeds during the retry-backoff window. Verify the 4 existing `test_engine_retry_*` pins continue passing. Re-run `pytest -n auto` 3-run baseline to confirm zero regression vs Plan 01 post-fix state. Atomic-4-file commit flips PARA-02 in REQUIREMENTS.md.

Purpose: PARA-02 acceptance criteria (a), (b), (c) — (d) already preliminary at Phase 1094 (audit Section 4.3 disposition INDEPENDENT). Without WR-02 closure, any future scenario where the cascade source shifts back to Category 4.3 in-test engine-wrapper contention would re-introduce the 7s loop-starvation window. The fix is hygiene-shape forward-safety, not a Phase 1095 prerequisite for the `-n auto` measurement gate.

Output: 1 edited conftest.py (WR-02 fix at line 624) + 1 new pin + 1 REQUIREMENTS flip + 1 SUMMARY = 4 files in a single atomic commit per v1019 TD-13 rule #3.

Plan 02 also owns the **phase-level rollup verification gate** at close: sequential 3055/0/38 + `-n 4` 3054/0/38 + `-n auto` 3-run ≤30 distinct deterministic across the post-PARA-01 + post-PARA-02 state. See `<verification>` block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/1095-cascade-fix-wr-02-closure/1095-CONTEXT.md
@.planning/audits/PYTEST-NAUTO-CATEGORY-4-1-v1022.md
@.planning/phases/1094-cascade-spike/1094-01-SUMMARY.md
@.planning/phases/1095-cascade-fix-wr-02-closure/1095-01-SUMMARY.md

<interfaces>
Key signatures and patterns the executor needs. Extracted from codebase at plan-time.
Executor should use these directly — no codebase exploration needed.

The function being modified — `_invoke_sleep_in_sync_context` at `backend/tests/conftest.py:624-661`:

```
def _invoke_sleep_in_sync_context(sleep_fn, seconds):
    """Invoke a ``sleep_fn`` from a synchronous context.

    The async retry helpers (`_run_with_too_many_clients_retry`,
    `_acquire_test_session_with_retry`) accept an async ``sleep_fn``
    (default `asyncio.sleep`) and `await` it. But `engine.connect()` is
    synchronous in SQLAlchemy 2.x — it returns the AsyncConnection
    proxy without performing any I/O — so the retry loop around it is
    a sync loop. This helper bridges the two:

    - If ``sleep_fn`` is the production ``asyncio.sleep`` reference
      specifically: use ``time.sleep`` for the actual blocking delay
      (do NOT try to drive asyncio.sleep from sync context — that
      requires an event loop and adds 100ms+ of overhead per call).
    - If ``sleep_fn`` is some OTHER async coroutine function (test
      injection like ``async def fake_sleep(s): sleep_calls.append(s)``):
      use ``asyncio.run`` to actually execute the body so the closure
      observes the call. Tests calling `wrapper.connect()` are
      synchronous (no surrounding event loop), so `asyncio.run` is
      safe.
    - If ``sleep_fn`` is a regular synchronous function: call directly.
    """
    if sleep_fn is asyncio.sleep:
        # Production path: skip event-loop overhead, just block.
        time.sleep(seconds)                              # ← WR-02 footgun at line 652
    elif asyncio.iscoroutinefunction(sleep_fn):
        # Test-injected async sleep: drive it via asyncio.run.
        asyncio.run(sleep_fn(seconds))
    else:
        # Plain synchronous sleep_fn.
        sleep_fn(seconds)
```

Caller paths (validated via `git grep -n "_invoke_sleep_in_sync_context" backend/tests/conftest.py`):

1. `backend/tests/conftest.py:706` inside `_install_dbapi_connect_retry._retry_do_connect` (event handler on SQLAlchemy `do_connect`).
2. `backend/tests/conftest.py:843` inside `_RetryingAsyncEngine.connect()` method (engine wrapper sync method).

BOTH callers invoke `_invoke_sleep_in_sync_context` from a synchronous Python function body. The existing inline rationale at line 635-637 explicitly chose `time.sleep` over `asyncio.run(asyncio.sleep(seconds))` to "skip event-loop overhead" (the `100ms+ of overhead per call` claim).

Audit Section 4.3 disposition: WR-02 is INDEPENDENT of the observed Category 4.X cascade source. Both callers (`_install_dbapi_connect_retry._retry_do_connect` and `_RetryingAsyncEngine.connect()`) wrap the SQLAlchemy engine-layer surface — the observed cascade source `_init_tile_pool_for_tests` bypasses both by calling `asyncpg.create_pool()` directly. So the WR-02 fix is forward-safety hygiene, not a prerequisite for the `-n auto` ≤30 gate (Plan 01 already satisfied that gate per CONTEXT.md `<decisions>`).

---

Shape Y1 (PREFERRED per CONTEXT.md `<decisions>` — non-blocking yield):

The production-path branch `if sleep_fn is asyncio.sleep` is the load-bearing site. The current `time.sleep(seconds)` blocks the calling thread, which in the asyncio runtime is the event-loop thread when invoked from a greenlet-bridge sync context (asyncpg's `_connect_addr` path).

Shape Y1 replaces the blocking sleep with an asyncio.run-driven asyncio.sleep, ACCEPTING the ~100ms overhead the original rationale wanted to avoid, because: (a) the overhead is amortized over 1-3 retry attempts per fixture instance (not a hot loop), (b) freeing the event loop for connection-release events during the 7s budget is more valuable than 100ms × 3 attempts = 300ms overhead per pool init, (c) audit Section 4.4 caveat: WR-02 IS a real footgun if Category 4.3 cascade re-emerges (forward-safety).

Shape Y1 candidate body (production-path branch only — preserve the other 2 branches verbatim):

```
if sleep_fn is asyncio.sleep:
    # WR-02 fix (PARA-02 / Plan 1095-02): yield the event loop
    # non-blockingly so connection-release events can fire during
    # the retry-backoff window. The audit (Section 4.4 caveat) names
    # this as a real footgun if Category 4.3 cascade re-emerges:
    # blocking time.sleep here would freeze the asyncio loop up to
    # the full 7s budget. asyncio.run() adds ~100ms event-loop
    # bootstrap per call, amortized over 1-3 retry attempts per
    # fixture instance = ~300ms per pool init — acceptable cost for
    # forward-safety against connection-release starvation.
    asyncio.run(asyncio.sleep(seconds))
```

Shape Y2 (FALLBACK — load-bearing rationale): if Shape Y1 measurably regresses Plan 01's post-fix `-n auto` baseline (e.g. distinct climbs above 30 due to asyncio.run bootstrap cost compounding under contention), keep `time.sleep` AND add an inline comment block at line 651-652 explaining: (1) why blocking is load-bearing here (sync-context constraint, asyncio.run overhead), (2) the documented mitigation in the engine wrapper itself (e.g. limit retry budget, or short-circuit to first-attempt-only for production path). Pick Y1 by default; fall back to Y2 only if Task 5's re-run measurement reveals regression.

---

Existing 4 `test_engine_retry_*` pins at `backend/tests/test_fixture_isolation_v1020.py:904, 977, 1029, 1070` — all 4 inject `async def fake_sleep(seconds): sleep_calls.append(seconds)` and call `wrapper.connect()` from sync test-function context. They go through `_RetryingAsyncEngine.connect()` → `_invoke_sleep_in_sync_context(self._sleep_fn, ...)` → `asyncio.iscoroutinefunction(sleep_fn)` branch → `asyncio.run(sleep_fn(seconds))`. The Shape Y1 fix changes ONLY the `if sleep_fn is asyncio.sleep` branch (the production path) — the test-injection branch (`elif asyncio.iscoroutinefunction(sleep_fn)`) is UNCHANGED, so all 4 existing pins continue exercising the same code path.

PARA-02 acceptance criterion (c) requires zero regression on the 4 existing pins. Task 4 verifies via the 32-test pin subset.

---

New regression pin shape (Task 4) — `test_engine_retry_yields_event_loop_during_backoff`:

The pin proves the Shape Y1 fix actually yields the loop during retry-backoff. Strategy: invoke the engine wrapper's retry path from an asyncio task that ALSO has a concurrent task running; assert the concurrent task makes progress during the backoff window.

Naming convention: this pin extends the existing `test_engine_retry_*` family (covers the engine-wrapper retry surface) rather than `test_init_tile_pool_*` (which is the new family Plan 01 introduced for the pool-init surface). The WR-02 footgun lives on the engine-wrapper path (audit Section 4.1 call-site map), so `test_engine_retry_yields_event_loop_during_backoff` is the semantically correct name. Co-locate with the existing 4 pins (append after current EOF line 1114 + Plan 01's new `test_init_tile_pool_*` pin).

Pin invocation shape (mirror the existing `test_engine_retry_succeeds_on_transient_too_many_clients:904` setup of `_FakeAsyncEngine` + `_RetryingAsyncEngine` wrapper) — see existing test at line 904 for the `_FakeAsyncEngine` + `_make_op_error` + `_RetryingAsyncEngine` construction pattern.

Assertion shape: schedule a concurrent `asyncio.create_task(some_progress_counter())` BEFORE invoking the wrapper's `connect()`; the wrapper's retry budget should `asyncio.run(asyncio.sleep(1.0))` per backoff attempt (Shape Y1 production path); during each sleep, the concurrent task's progress counter should increment. Assert the counter advanced by at least N steps during the retry window (where N is calibrated so a blocking time.sleep WOULD have left N=0). Tolerance window: 2.1s for first backoff (1.0s) + asyncio.run bootstrap.

Note: this pin must NOT use `sleep_fn=fake_sleep` injection because that takes the `elif asyncio.iscoroutinefunction` branch (which already non-blocking by virtue of `asyncio.run(sleep_fn(seconds))`). It must use `sleep_fn=asyncio.sleep` (the production reference) to exercise the Shape Y1 fix branch. Use the real `asyncio.sleep` and shorten `_backoffs=(0.1, 0.2, 0.4)` so the test completes in ~0.7s wall clock (mirror the existing exhaustion pin at line 1094 which overrides budget to `(0.0, 0.0, 0.0)`).

</interfaces>

<verified_line_numbers>
All line numbers re-validated via `git grep -n` at plan-time per v1019 TD-13:

- `backend/tests/conftest.py:624` — `def _invoke_sleep_in_sync_context(sleep_fn, seconds):` (WR-02 fix site)
- `backend/tests/conftest.py:650` — `if sleep_fn is asyncio.sleep:` (production-path branch — Shape Y1 target)
- `backend/tests/conftest.py:652` — `time.sleep(seconds)` (the WR-02 blocking call to replace)
- `backend/tests/conftest.py:653` — `elif asyncio.iscoroutinefunction(sleep_fn):` (test-injection branch — DO NOT MODIFY)
- `backend/tests/conftest.py:658` — `asyncio.run(sleep_fn(seconds))` (test-injection sleep driver — DO NOT MODIFY)
- `backend/tests/conftest.py:664` — `def _install_dbapi_connect_retry(sync_engine, sleep_fn, backoffs):` (caller path 1)
- `backend/tests/conftest.py:690` — `def _retry_do_connect(dialect, conn_rec, cargs, cparams):` (inner event handler)
- `backend/tests/conftest.py:706` — `_invoke_sleep_in_sync_context(sleep_fn, backoffs[attempt])` (caller invocation 1)
- `backend/tests/conftest.py:711` — `class _RetryingAsyncEngine:` (caller path 2 — class definition)
- `backend/tests/conftest.py:801` — `def connect(self):` (caller path 2 — sync method)
- `backend/tests/conftest.py:843` — `_invoke_sleep_in_sync_context(self._sleep_fn, self._backoffs[attempt])` (caller invocation 2)
- `backend/tests/test_fixture_isolation_v1020.py:904` — `test_engine_retry_succeeds_on_transient_too_many_clients` (existing pin 1 — must continue passing)
- `backend/tests/test_fixture_isolation_v1020.py:977` — `test_engine_retry_catches_raw_asyncpg_too_many_connections` (existing pin 2)
- `backend/tests/test_fixture_isolation_v1020.py:1029` — `test_engine_retry_propagates_non_transient_operational_error` (existing pin 3)
- `backend/tests/test_fixture_isolation_v1020.py:1070` — `test_engine_retry_exhausts_budget_then_fails_loudly` (existing pin 4)
- `backend/tests/test_fixture_isolation_v1020.py:1114` — EOF after Plan 01's new `test_init_tile_pool_*` pin lands at Task 6 of Plan 01 (append the new `test_engine_retry_yields_event_loop_during_backoff` AFTER Plan 01's pin)
</verified_line_numbers>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight gate (Plan 01 closed cleanly + verify line numbers + 33-test pin subset baseline)</name>
  <files>(read-only; no edits)</files>
  <action>
Three checks, all required to PASS before Task 2.

Check 1 — Plan 01 close state. Confirm Plan 01 shipped: `git log --oneline -3` should show the Plan 01 close commit. Read `.planning/phases/1095-cascade-fix-wr-02-closure/1095-01-SUMMARY.md` to confirm:
- (a) the 3 fixture wraps landed (Tasks 2-4)
- (b) the new `test_init_tile_pool_retries_on_transient_too_many_clients` pin PASSES
- (c) `pytest -n auto` 3-run baseline shows distinct ≤30 deterministic per run
- (d) REQUIREMENTS.md PARA-01 reads `[x]` + `Complete`

If any (a)/(b)/(c)/(d) is missing or red, STOP — Plan 02 cannot proceed until Plan 01 closes cleanly.

Check 2 — re-validate the 17 line-numbered references in `<verified_line_numbers>` above. Run `git grep -nE "def _invoke_sleep_in_sync_context|def _install_dbapi_connect_retry|class _RetryingAsyncEngine|def _retry_do_connect|def connect|asyncio.run\\(sleep_fn|time.sleep|asyncio.iscoroutinefunction" backend/tests/conftest.py | head -30` and `git grep -nE "^def test_engine_retry_|^def test_init_tile_pool_" backend/tests/test_fixture_isolation_v1020.py`. If ANY line drifts by more than ±5 lines, STOP and re-anchor (v1019 TD-13 `req_citation_pinning` rule).

Check 3 — 33-test pin subset spot-check (sequential baseline preservation proxy). Run `cd backend && set -a && source ../.env.test && set +a && uv run pytest -k "test_fixture_isolation_v1020 or test_conftest_pool_sizing or test_conftest_lifecycle" -v`. Expected `33 passed, 1 skipped, 3076 deselected in ~4s` (Plan 01 added the new pin, taking 32 → 33). MUST PASS before any WR-02 fix lands.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && set -a && source ../.env.test && set +a && uv run pytest -k "test_fixture_isolation_v1020 or test_conftest_pool_sizing or test_conftest_lifecycle" --no-header -q 2>&amp;1 | tail -3 | grep -E "33 passed"</automated>
  </verify>
  <done>Plan 01 close commit verified (`git log -1` shows the Plan 01 commit with REQUIREMENTS.md PARA-01 `[x]` + `Complete`); all 17 line numbers verified; 33-test pin subset is `33 passed, 1 skipped`.</done>
</task>

<task type="auto">
  <name>Task 2: Choose Shape Y1 vs Y2 — read invocation context + apply chosen shape</name>
  <files>backend/tests/conftest.py</files>
  <action>
Read `backend/tests/conftest.py` lines 620-670 (the `_invoke_sleep_in_sync_context` definition + the two callers' context). Verify the two caller paths per `<interfaces>` above:

1. `_install_dbapi_connect_retry._retry_do_connect` (line 690-708) — invoked as a SQLAlchemy `do_connect` event handler. Reaches `_invoke_sleep_in_sync_context` at line 706 from inside a `for attempt in range(attempt_budget)` loop. The handler returns a DBAPI connection; SQLAlchemy invokes it from inside `dialect.connect(...)` which under asyncpg is driven through the `greenlet_spawn` boundary. Calling thread = the event-loop thread (when invoked from async code) or a worker thread (when invoked from sync code via `asyncio.to_thread`). The CURRENT `time.sleep` blocks whichever thread is the caller.

2. `_RetryingAsyncEngine.connect()` (line 801-847) — explicitly synchronous per the docstring (line 803-811): `AsyncEngine.connect()` returns an `AsyncConnection` proxy WITHOUT performing any database I/O. The actual connection acquisition happens later inside `async with ... as conn:`. So the test-pin regression path catches contention on the synchronous wrapper-method call itself. In production, this path is rarely the failing surface (the audit Section 4.1 caller map confirms it does not fire on the observed cascade).

Decision per CONTEXT.md `<decisions>` `WR-02 fix shape`: **default to Shape Y1** (non-blocking yield). Both callers are inside synchronous Python function bodies invoked from contexts where asyncio.run() is safe (no surrounding event loop in test-pin regression; greenlet-bridge context in production allows asyncio.run with the ~100ms overhead acceptable per `<interfaces>` rationale).

Apply Shape Y1 at line 650-652. Replace the existing branch body verbatim with the Shape Y1 candidate body shown in `<interfaces>` above (REPLACE `time.sleep(seconds)` with `asyncio.run(asyncio.sleep(seconds))`, AND insert the inline-comment block immediately ABOVE the new call documenting the WR-02 fix + Plan 1095-02 reference + 100ms overhead acknowledgment + 7s budget hazard rationale + audit Section 4.4 cross-reference).

DO NOT MODIFY the other 2 branches (`elif asyncio.iscoroutinefunction(sleep_fn)` at line 653 + `else` plain-sync branch at line 659-661). Those preserve the existing test-injection contract that the 4 existing `test_engine_retry_*` pins rely on (PARA-02 acceptance criterion (c)).

Update the function docstring (lines 625-648) — change the bullet point at lines 634-637 to reflect Shape Y1 behavior. New bullet shape (replace verbatim the existing `- If sleep_fn is the production asyncio.sleep reference specifically: use time.sleep ...` bullet):

```
- If ``sleep_fn`` is the production ``asyncio.sleep`` reference
  specifically: drive it via ``asyncio.run(asyncio.sleep(seconds))``
  so the event loop yields to other tasks during the backoff window
  (WR-02 / PARA-02 fix per Plan 1095-02). The ~100ms event-loop
  bootstrap per call is acceptable cost amortized over 1-3 retry
  attempts; freeing the loop for connection-release events during
  the 7s budget is the load-bearing benefit. See
  ``.planning/audits/PYTEST-NAUTO-CATEGORY-4-1-v1022.md`` Section 4.4.
```

If at Task 5 the post-fix `-n auto` baseline regresses vs Plan 01 (distinct climbs above 30 on any run), fall back to Shape Y2: revert this task's change to keep `time.sleep`, AND add the load-bearing rationale comment block at line 651-652 (without removing it) explaining sync-context constraint + asyncio.run overhead measurement + documented mitigation. Re-emit this task's verification with the Y2 rationale and re-run Task 5's measurement gate; PARA-02 acceptance criterion (a) accepts EITHER Y1 yield OR Y2 documented rationale.
  </action>
  <verify>
    <automated>grep -A 3 "if sleep_fn is asyncio.sleep:" backend/tests/conftest.py | head -10 && (grep -q "asyncio.run(asyncio.sleep(seconds))" backend/tests/conftest.py && echo "Shape Y1 applied") || (grep -A 5 "if sleep_fn is asyncio.sleep:" backend/tests/conftest.py | grep -qE "WR-02|load-bearing" && echo "Shape Y2 (load-bearing rationale) applied") || echo "FAIL: neither Y1 nor Y2 evident"</automated>
  </verify>
  <done>EITHER (a) line 652 reads `asyncio.run(asyncio.sleep(seconds))` with the inline WR-02 comment block + updated docstring bullet (Shape Y1), OR (b) line 652 retains `time.sleep(seconds)` AND has an inline load-bearing rationale comment block referencing WR-02 + PARA-02 + audit Section 4.4 (Shape Y2). The other 2 branches (`elif`/`else`) are unchanged byte-for-byte.</done>
</task>

<task type="auto">
  <name>Task 3: Verify 4 existing `test_engine_retry_*` pins still pass (PARA-02 acceptance criterion (c))</name>
  <files>(read-only verification; no edits)</files>
  <action>
The 4 existing pins at `backend/tests/test_fixture_isolation_v1020.py:904, 977, 1029, 1070` all inject `async def fake_sleep(seconds): sleep_calls.append(seconds)` — which goes through the `elif asyncio.iscoroutinefunction(sleep_fn)` branch (line 653 — UNCHANGED by Task 2). They should continue passing byte-for-byte.

Run the 4 pins individually + the 33-test pin subset:

```
cd backend && set -a && source ../.env.test && set +a && uv run pytest \
  backend/tests/test_fixture_isolation_v1020.py::test_engine_retry_succeeds_on_transient_too_many_clients \
  backend/tests/test_fixture_isolation_v1020.py::test_engine_retry_catches_raw_asyncpg_too_many_connections \
  backend/tests/test_fixture_isolation_v1020.py::test_engine_retry_propagates_non_transient_operational_error \
  backend/tests/test_fixture_isolation_v1020.py::test_engine_retry_exhausts_budget_then_fails_loudly \
  -v --no-header
```

Then re-run the 33-test pin subset to confirm zero regression:

```
cd backend && set -a && source ../.env.test && set +a && uv run pytest \
  -k "test_fixture_isolation_v1020 or test_conftest_pool_sizing or test_conftest_lifecycle" \
  --no-header -q
```

PARA-02 acceptance criterion (c) closes if all 4 + the 33-test subset shows `33 passed, 1 skipped`. If any pin regresses, the fix at Task 2 inadvertently touched the test-injection branch — STOP and fix Task 2 (the `elif` and `else` branches must be byte-for-byte unchanged).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && set -a && source ../.env.test && set +a && uv run pytest backend/tests/test_fixture_isolation_v1020.py::test_engine_retry_succeeds_on_transient_too_many_clients backend/tests/test_fixture_isolation_v1020.py::test_engine_retry_catches_raw_asyncpg_too_many_connections backend/tests/test_fixture_isolation_v1020.py::test_engine_retry_propagates_non_transient_operational_error backend/tests/test_fixture_isolation_v1020.py::test_engine_retry_exhausts_budget_then_fails_loudly --no-header -q 2>&amp;1 | tail -3 | grep -E "4 passed"</automated>
  </verify>
  <done>All 4 existing `test_engine_retry_*` pins PASS post-WR-02-fix. 33-test pin subset still shows `33 passed, 1 skipped`. PARA-02 acceptance criterion (c) GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add regression pin `test_engine_retry_yields_event_loop_during_backoff` (PARA-02 acceptance criterion (b))</name>
  <files>backend/tests/test_fixture_isolation_v1020.py</files>
  <behavior>
    - The pin proves the Shape Y1 production-path fix actually yields the asyncio event loop during the retry-backoff window.
    - If Shape Y2 was chosen at Task 2 (load-bearing time.sleep retained), the pin is RECONFIGURED to assert the documented mitigation instead — see "Shape Y2 pin alternative" below.
    - The pin invokes the engine-wrapper retry path with `sleep_fn=asyncio.sleep` (the production reference) — NOT a fake_sleep injection — to exercise the Shape Y1 branch.
    - The pin schedules a concurrent task BEFORE invoking the retry-driving path; asserts the concurrent task makes progress during the backoff window (counter advances by ≥ N steps where N would be 0 under blocking time.sleep).
    - The pin uses a shortened `_backoffs=(0.1, 0.2, 0.4)` so wall-clock test runtime is ~0.7s rather than 7s.
  </behavior>
  <action>
Append a new test function `test_engine_retry_yields_event_loop_during_backoff` to `backend/tests/test_fixture_isolation_v1020.py` (after the current EOF — which is Plan 01's `test_init_tile_pool_retries_on_transient_too_many_clients` pin). Co-locate with the existing 4 `test_engine_retry_*` pins via naming convention.

**Shape Y1 pin body (DEFAULT — applies if Task 2 chose Y1):**

Construct a `_FakeAsyncEngine` (mirror line 904 pattern) with `connect_outcomes=[<TooManyConnectionsError>, None]` so the wrapper retries once then succeeds.

Build the wrapper with `sleep_fn=asyncio.sleep` (PRODUCTION reference — NOT a fake_sleep injection) and `backoffs=(0.1,)` (single 100ms backoff to keep wall-clock test runtime ~0.1s + asyncio.run bootstrap overhead).

Test body shape (full async test function — use `async def test_...` + `@pytest.mark.asyncio` if the file convention supports it; OTHERWISE drive an inner async helper via `asyncio.run(...)` from a sync test function, mirroring the existing test conventions in the file):

The inner async function: (a) schedules a concurrent `asyncio.create_task(...)` that increments a counter every 10ms via `await asyncio.sleep(0.01)` loop; (b) yields immediately via `await asyncio.sleep(0)` to let the counter task start; (c) invokes `wrapper.connect()` (synchronous call from this async context — the test exercises the same call-shape the test-engine wrapper exposes); (d) immediately after `connect()` returns, cancels the counter task and reads its final count.

Assertions: (1) `wrapper.connect()` returned a non-None pool sentinel (proves retry succeeded), (2) `fake_engine.connect_calls == 2` (1 fail + 1 retry), (3) `counter_value >= 5` (proves the concurrent task ran ≥ 5 increments during the 100ms backoff — under blocking time.sleep, counter would have stalled at 0 or 1 since the loop is frozen).

Tolerance: the assertion `counter_value >= 5` is calibrated so a Shape Y1 fix (asyncio.run releases the loop briefly per call) produces ≥ 5 increments and a regression to Shape blocking-time.sleep produces ≤ 1 increment. Adjust the lower bound after a first dry-run if necessary, but err on the side of catching the regression (false-pass risk > false-fail risk; if false-fail occurs, lower the bound by 1 each iteration).

Cite the new pin's full node-ID `backend/tests/test_fixture_isolation_v1020.py::test_engine_retry_yields_event_loop_during_backoff` per v1019 TD-13 `req_citation_pinning` so the Plan 02 SUMMARY can reference it.

**Shape Y2 pin alternative (applies ONLY if Task 2 chose Y2):**

If load-bearing rationale was chosen, the pin shape changes — instead of asserting loop-yield (which a blocking time.sleep would fail), assert the DOCUMENTED MITIGATION lives at the source-of-record line. Pin body becomes a static-text grep test: read `backend/tests/conftest.py:650-655`, assert the inline comment contains both `WR-02` AND `PARA-02` AND the audit cross-reference (`Section 4.4`). This is a regression pin against silent removal of the load-bearing rationale, not a behavioral assertion. Name remains `test_engine_retry_yields_event_loop_during_backoff` for traceability symmetry — the docstring distinguishes Y1 vs Y2 fixture-state.

In EITHER shape, the pin name + node-ID are identical so PARA-02 acceptance criterion (b) cites the same node-ID regardless of Y1/Y2 fork.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/backend && set -a && source ../.env.test && set +a && uv run pytest backend/tests/test_fixture_isolation_v1020.py::test_engine_retry_yields_event_loop_during_backoff -v --no-header 2>&amp;1 | tail -5 | grep -E "1 passed"</automated>
  </verify>
  <done>New pin `test_engine_retry_yields_event_loop_during_backoff` PASSES. Combined `pytest backend/tests/test_fixture_isolation_v1020.py` shows pass count +1 vs Plan 01 close. 33-test pin subset re-runs as `34 passed, 1 skipped, 3075 deselected` (was `33 passed`). PARA-02 acceptance criterion (b) GREEN.</done>
</task>

<task type="auto">
  <name>Task 5: `pytest -n auto` 3-run post-WR-02 baseline + sequential 3055/0/38 rollup gate</name>
  <files>(measurement; logs written to /tmp/)</files>
  <action>
Re-run the `pytest -n auto` 3-run baseline POST-WR-02-FIX to confirm Shape Y1 did NOT regress Plan 01's ≤30 distinct deterministic gate. AND run the full sequential baseline as the rollup pre-tag gate (HARD INVARIANT per v1019 TD-13).

Step (a) — `pytest -n auto` 3-run baseline with stale-DB cleanup (same recipe as Plan 01 Task 7):

For each RUN in {1, 2, 3}:
- Stale-DB cleanup BEFORE the run via `docker compose exec -T db psql ... > /tmp/v1022-1095-02-stale-dbs-run${RUN}.txt` + DROP SQL generation + apply (mirror audit Section 2.1).
- Invoke pytest: `cd backend && set -a && source ../.env.test && set +a && uv run pytest -n auto tests/ --junitxml=/tmp/v1022-1095-02-post-wr02-nauto-run${RUN}.xml > /tmp/v1022-1095-02-post-wr02-nauto-run${RUN}.log 2>&1`
- Extract: `tail -1`, TMC count, ICN count, distinct = failed + errors.

Acceptance gate: **distinct ≤ 30 deterministic across all 3 runs** (same threshold as Plan 01 Task 7 — Shape Y1 must NOT regress the gate). Compare delta vs Plan 01 post-fix baseline at `/tmp/v1022-1095-post-fix-nauto-run{1,2,3}.log`; expected delta ≈ 0 (audit Section 4.3 disposition INDEPENDENT — WR-02 fix should not affect the cascade source which bypasses both `_invoke_sleep_in_sync_context` callers entirely).

If gate BREACHED on any run: this is the Y1-regression scenario from Task 2 fork. Revert Task 2's Y1 fix; apply Y2 (load-bearing rationale + retained `time.sleep`); re-run Task 4 (pin reconfigured to Y2 shape); re-run this task's measurement. Land Y2 path per CONTEXT.md `<decisions>` `WR-02 fix shape` Y1/Y2 fallback option.

Step (b) — sequential 3055/0/38 rollup gate (HARD INVARIANT — v1019 TD-13). Run:

```
cd backend && set -a && source ../.env.test && set +a && \
  uv run pytest tests/ --no-header -q 2>&1 | tee /tmp/v1022-1095-02-sequential-rollup.log
```

Expected `3055 passed, 0 NEW failed, 38 skipped, 3 pre-existing OOS` (test_layering + test_phase_275 + test_ssrf_redirect per CONTEXT.md `<domain>`). Wall clock ~9 minutes.

The "0 NEW failed" gate is the HARD INVARIANT — any new failure attributable to v1022 changes blocks Plan 02 close. The 3 pre-existing OOS rows are explicitly allowed.

Step (c) — `-n 4` rollup gate (PARA-01 acceptance criterion (c), re-verified here as the phase rollup):

```
cd backend && set -a && source ../.env.test && set +a && \
  uv run pytest -n 4 tests/ --no-header -q 2>&1 | tee /tmp/v1022-1095-02-n4-rollup.log
```

Expected `3054 passed, 0 NEW failed, 38 skipped, 4 OOS` (3 pre-existing + 1 oauth flake-class per PYTEST-XDIST-PERF-v1020.md Section 2 n=8 row).

Capture the 3-run `-n auto` measurement table + sequential rollup + `-n 4` rollup numbers to `/tmp/v1022-1095-02-rollup-gates.md` for embedding in 1095-02-SUMMARY.md at Task 6. Include delta vs Plan 01 post-fix baseline.
  </action>
  <verify>
    <automated>for RUN in 1 2 3; do DISTINCT=$(awk '/=========/{last=$0} END{print last}' /tmp/v1022-1095-02-post-wr02-nauto-run${RUN}.log | grep -oE "[0-9]+ failed|[0-9]+ error" | awk '{sum+=$1} END {print sum+0}'); echo "-n auto Run ${RUN} distinct = ${DISTINCT}"; test "${DISTINCT}" -le 30 || { echo "FAIL: Run ${RUN} BREACHED ≤30 gate"; exit 1; }; done && SEQ_FAILED=$(awk '/=========/{last=$0} END{print last}' /tmp/v1022-1095-02-sequential-rollup.log | grep -oE "[0-9]+ failed" | awk '{print $1}'); echo "Sequential failed = ${SEQ_FAILED:-0}"; test "${SEQ_FAILED:-0}" -le 3 || { echo "FAIL: sequential ${SEQ_FAILED} > 3 OOS rows — HARD INVARIANT breach"; exit 1; }; echo "Rollup gate GREEN"</automated>
  </verify>
  <done>3-run `-n auto` distinct ≤30 deterministic per run preserved vs Plan 01 baseline; sequential rollup shows `3055 passed, 0 NEW failed, 38 skipped` (3 pre-existing OOS allowed); `-n 4` rollup shows `3054 passed, 0 NEW failed, 38 skipped` (4 OOS allowed); all measurement logs captured to /tmp/. If Shape Y1 regressed the `-n auto` gate, switched to Shape Y2 and re-ran successfully.</done>
</task>

<task type="auto">
  <name>Task 6: Atomic-4-file commit + PARA-02 REQUIREMENTS traceability flip + SUMMARY.md write</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/1095-cascade-fix-wr-02-closure/1095-02-SUMMARY.md</files>
  <action>
Three-step close per CONTEXT.md rules #2, #3 (atomic-4-file commit + traceability flip).

Step (a) — flip PARA-02 in `.planning/REQUIREMENTS.md`. Edit the file in TWO places:

1. Line 24 `- [ ] **PARA-02**: ...` → `- [x] **PARA-02**: ...` (checkbox flip)
2. Line 76 (Traceability table) `| PARA-02 | Phase 1095 | Pending |` → `| PARA-02 | Phase 1095 | Complete |`

Per v1019 TD-13 `requirements_traceability_flip`: the flip MUST land in the SAME commit as `1095-02-SUMMARY.md`.

Step (b) — write `.planning/phases/1095-cascade-fix-wr-02-closure/1095-02-SUMMARY.md` per the GSD `summary.md` template. Required frontmatter sections:
- `requires` (Plan 1095-01 close + Phase 1094 audit Section 4.3 INDEPENDENT disposition)
- `provides` (PARA-02 closure + WR-02 fix at conftest.py:624 + new `test_engine_retry_yields_event_loop_during_backoff` pin)
- `affects` (1096-hygiene-tail, 1097-live-verify-close-gate)
- `tech-stack.patterns` (Shape Y1 asyncio.run yield over blocking time.sleep at sync-context boundaries; ~100ms event-loop bootstrap acceptable for non-hot retry budgets; OR Shape Y2 load-bearing rationale if Y1 regressed)
- `key-files.modified` (conftest.py + REQUIREMENTS.md)
- `key-files.created` (new pin + this SUMMARY)
- `key-decisions` (Shape Y1 chosen by default per CONTEXT.md; Y2 fallback documented; pin co-located in `test_engine_retry_*` family because WR-02 footgun lives on engine-wrapper path per audit Section 4.1)
- `requirements-completed` (`PARA-02`)

The SUMMARY body MUST include:
- Task 5 measurement tables verbatim (3-run `-n auto` + sequential + `-n 4` + delta vs Plan 01 baseline)
- The chosen Shape (Y1 or Y2) with rationale
- New pin node-ID citation `backend/tests/test_fixture_isolation_v1020.py::test_engine_retry_yields_event_loop_during_backoff` per v1019 TD-13 `req_citation_pinning`
- Phase 1095 rollup gate summary: PARA-01 (Plan 01 close) + PARA-02 (this Plan close) = both `[x]` + `Complete` in REQUIREMENTS.md; phase 1095 ROADMAP success criteria 1-5 all GREEN
- Carry-forward to Phase 1096 (HYG-01 — WR-01/03/04) noted

Step (c) — atomic commit. Stage EXACTLY 4 files (verify via `git status` shows zero other staged files):
1. `backend/tests/conftest.py`
2. `backend/tests/test_fixture_isolation_v1020.py`
3. `.planning/REQUIREMENTS.md`
4. `.planning/phases/1095-cascade-fix-wr-02-closure/1095-02-SUMMARY.md`

Commit message shape: `test(1095-02): WR-02 Shape Y1 yield fix in _invoke_sleep_in_sync_context; close PARA-02; close Phase 1095` (substitute Y2 if applicable). Body: 1-2 sentence summary of the WR-02 footgun + chosen shape + measurement gate result + PARA-02 flip + Phase 1095 rollup status + reference to audit Section 4. Include `Closes PARA-02.` line per v1019 TD-13 trace convention.

If `git status` shows ANY 5th file staged (e.g. a stray .log artifact), unstage with `git restore --staged <file>` and recount. The atomic-4-file rule is per CONTEXT.md `<decisions>` `Atomic-N-file commit per plan`.
  </action>
  <verify>
    <automated>STAGED=$(git diff --cached --name-only | wc -l | tr -d ' '); test "$STAGED" = "4" || { echo "FAIL: staged file count = $STAGED, expected 4"; git diff --cached --name-only; exit 1; }; grep -E "^\- \[x\] \*\*PARA-02\*\*" .planning/REQUIREMENTS.md && grep -E "^\| PARA-02 \| .* \| Complete \|" .planning/REQUIREMENTS.md && test -f .planning/phases/1095-cascade-fix-wr-02-closure/1095-02-SUMMARY.md && echo "Atomic-4-file commit ready; PARA-02 traceability flipped"</automated>
  </verify>
  <done>Exactly 4 files staged: conftest.py (WR-02 fix) + new pin + REQUIREMENTS.md (with both PARA-02 flips) + 1095-02-SUMMARY.md. Single atomic commit lands. `git log -1 --stat` shows 4 files changed. REQUIREMENTS.md PARA-02 row reads `[x]` + `Complete`. PARA-02 acceptance criteria (a)/(b)/(c) GREEN; (d) was preliminary at Phase 1094 (audit Section 4.3 disposition INDEPENDENT, finalized here). Phase 1095 closes with BOTH PARA-01 + PARA-02 marked Complete.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-infra / production-code | Phase 1095 Plan 02 modifies test-fixture machinery (`backend/tests/conftest.py`); no production-code path changes. The `_invoke_sleep_in_sync_context` helper is consumed ONLY by `_install_dbapi_connect_retry` and `_RetryingAsyncEngine` — both test-fixture-only paths. |
| asyncio loop vs sync calling thread | The Shape Y1 fix introduces `asyncio.run(asyncio.sleep(...))` from a sync-context calling thread. This bootstraps a NEW event loop per call (cost: ~100ms). The fix accepts this cost as the load-bearing tradeoff for yielding the original event loop's queue to other tasks during the retry-backoff window. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1095-02-01 | Tampering | `_invoke_sleep_in_sync_context` Shape Y1 fix | mitigate | Regression pin `test_engine_retry_yields_event_loop_during_backoff` asserts concurrent task scheduling proceeds; CI failure on the pin catches silent reversion to blocking sleep. |
| T-1095-02-02 | Denial-of-Service | Shape Y1 100ms × 3 attempts = 300ms bootstrap overhead per pool init | accept | 300ms/pool-init is amortized across the 7s retry budget; impact negligible vs 7s loop-starvation hazard. Task 5 measurement gate proves no `-n auto` baseline regression vs Plan 01 post-fix. |
| T-1095-02-03 | Repudiation | Silent removal of WR-02 inline rationale (Shape Y2 path) | mitigate | If Y2 chosen, the pin asserts the rationale text exists at the source-of-record line (grep-based pin per Task 4 Shape Y2 alternative). |
| T-1095-02-04 | Information-Disclosure | n/a | n/a | No data crosses a trust boundary; sleep durations are integer seconds with no sensitive content. |
| T-1095-02-05 | Elevation-of-Privilege | n/a | n/a | No privilege boundary crossed by a sleep-helper refactor. |
| T-1095-02-06 | Spoofing | n/a | n/a | n/a |
| T-1095-02-SC | Tampering | npm/pip/cargo installs | n/a | Phase 1095 Plan 02 installs no new packages. `asyncio` is stdlib; no legitimacy gate required. |

</threat_model>

<verification>

## Plan 02 verification

- [ ] Task 1 — Plan 01 close commit verified (REQUIREMENTS.md PARA-01 reads `[x]` + `Complete`); 33-test pin subset is `33 passed, 1 skipped`; all 17 line citations confirmed
- [ ] Task 2 — `_invoke_sleep_in_sync_context` line 652 either reads `asyncio.run(asyncio.sleep(seconds))` (Y1) OR retains `time.sleep(seconds)` + load-bearing rationale (Y2); `elif`/`else` branches unchanged byte-for-byte
- [ ] Task 3 — All 4 existing `test_engine_retry_*` pins PASS post-WR-02-fix (PARA-02 acceptance criterion (c) GREEN)
- [ ] Task 4 — New pin `test_engine_retry_yields_event_loop_during_backoff` PASSES; 33-test subset becomes `34 passed, 1 skipped` (PARA-02 acceptance criterion (b) GREEN)
- [ ] Task 5 — 3-run `-n auto` distinct ≤30 deterministic per run preserved; sequential 3055/0/38 + `-n 4` 3054/0/38 baselines preserved (HARD INVARIANT GREEN)
- [ ] Task 6 — `git diff --cached --name-only | wc -l` reads exactly `4`; REQUIREMENTS.md PARA-02 reads `[x]` + `Complete`; single commit ships

## Phase 1095 rollup verification (THIS PLAN OWNS THE PRE-TAG GATE)

The 5 ROADMAP success criteria for Phase 1095 (lines 119-123 of ROADMAP.md) all satisfied:

1. **`pytest -n auto` ≤30 distinct deterministic across 3 runs with stale-DB cleanup** — Plan 01 Task 7 first proved; Plan 02 Task 5 re-confirmed post-WR-02. Captured to `/tmp/v1022-1095-post-fix-nauto-run{1,2,3}.{log,xml}` (Plan 01) AND `/tmp/v1022-1095-02-post-wr02-nauto-run{1,2,3}.{log,xml}` (Plan 02). Delta between the two = ≈ 0 per audit Section 4.3 INDEPENDENT disposition.

2. **Sequential 3055/0/38 preserved + `-n 4` 3054/0/38 preserved (HARD INVARIANT)** — Plan 02 Task 5 runs both as the rollup pre-tag gate. Captured to `/tmp/v1022-1095-02-sequential-rollup.log` + `/tmp/v1022-1095-02-n4-rollup.log`. 3 pre-existing OOS allowed (test_layering + test_phase_275 + test_ssrf_redirect); 4 OOS in `-n 4` (3 pre-existing + 1 oauth flake-class).

3. **Regression pin in `test_fixture_isolation_v1020.py` covers retry shape** — Plan 01 added `test_init_tile_pool_retries_on_transient_too_many_clients` (covers `_init_tile_pool_for_tests` retry shape per audit Section 5.1 reclassification). Plan 02 added `test_engine_retry_yields_event_loop_during_backoff` (covers WR-02 loop-yield shape). ROADMAP success criterion 4 (which originally cited the per-worker DB lifecycle retry shape) is reclassified per audit Section 5.1: the `test_init_tile_pool_*` pin family is the correct surface coverage on the current HEAD.

4. **`_invoke_sleep_in_sync_context` non-blocking yield OR load-bearing rationale** — Plan 02 Task 2 closes via Shape Y1 (asyncio.run yield) default or Shape Y2 (retained time.sleep + rationale) fallback.

5. **REQUIREMENTS.md PARA-01 + PARA-02 = `[x]` + `Complete`** — Plan 01 Task 8 flips PARA-01; Plan 02 Task 6 flips PARA-02. Both atomic with their SUMMARY.md per v1019 TD-13.

Phase 1095 ships closed when Plan 02 Task 6 commit lands. Next phase: 1096 (HYG-01 — WR-01/03/04). Carry-forward documented in 1095-02-SUMMARY.md.

</verification>

<success_criteria>

Plan 02 SUCCESS = all of:

1. WR-02 fix lands at `backend/tests/conftest.py:624-661` (Shape Y1 or Y2) — Task 2
2. All 4 existing `test_engine_retry_*` pins continue passing (PARA-02 (c)) — Task 3
3. New pin `test_engine_retry_yields_event_loop_during_backoff` PASSES (PARA-02 (b)) — Task 4
4. `pytest -n auto` 3-run baseline distinct ≤30 deterministic per run preserved vs Plan 01 post-fix; sequential 3055/0/38 + `-n 4` 3054/0/38 baselines preserved (Phase 1095 HARD INVARIANT + ROADMAP success criteria 1-2) — Task 5
5. Atomic-4-file commit lands with REQUIREMENTS.md PARA-02 flipped to `[x]` + `Complete` (PARA-02 (a/b/c) + traceability flip) — Task 6
6. Phase 1095 closes with BOTH PARA-01 + PARA-02 = `Complete` in REQUIREMENTS.md (rollup gate satisfied)

</success_criteria>

<output>
Create `.planning/phases/1095-cascade-fix-wr-02-closure/1095-02-SUMMARY.md` per Task 6 (includes 3-run `-n auto` + sequential + `-n 4` measurement tables verbatim, chosen Shape Y1/Y2 rationale, new pin node-ID citation, PARA-02 closure rationale, Phase 1095 rollup gate summary, carry-forward to Phase 1096 noted). Single atomic 4-file commit closes Plan 02 and Phase 1095.
</output>
