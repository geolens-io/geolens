---
phase: 1041
plan: "03"
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - frontend/src/components/builder/hooks/use-builder-layers.ts
  - frontend/src/pages/MapBuilderPage.tsx
autonomous: true
requirements:
  - POL-09
tags:
  - mapbuilder
  - bulk-actions
  - state-mutation
  - rollback

must_haves:
  truths:
    - "User who clicks bulk Visibility with N rows selected sees a single optimistic state update flip ALL N rows to the opposite-of-majority visibility in one React render."
    - "User who drags the bulk Opacity slider sees ALL N selected rows take the new opacity in one React render; no per-row API call fires (persisted via existing Save gate)."
    - "User who clicks bulk Group with N loose vector layers selected sees ONE new folder-group row appear with all N as children, all in one local-state mutation (no API call until Save)."
    - "User who clicks bulk Ungroup with N folder-group rows selected sees all N groups removed and their children promoted to top level, in one local-state mutation."
    - "User who clicks bulk Delete (after confirmation) sees N parallel DELETE requests fire via Promise.allSettled; on ANY failure, ALL rows roll back to their pre-delete state and a single error toast fires."
    - "User who succeeds at any bulk op sees no toast (the optimistic state IS the confirmation per UI-SPEC §4); selection clears."
    - "User who fails bulk delete sees one error toast with the localized 'errorDeleteRolledBack' copy and the selection is preserved (so the user can retry)."
    - "User who runs any bulk op never triggers a per-row toast — only the single bulk-op-level toast on failure."
  artifacts:
    - path: "frontend/src/components/builder/hooks/use-builder-layers.ts"
      provides: "handleBulkVisibility(ids), handleBulkOpacity(ids, opacity), handleBulkGroup(ids), handleBulkUngroup(ids), handleBulkDelete(ids) — exported from useBuilderLayers return"
      contains: "handleBulkDelete"
    - path: "frontend/src/pages/MapBuilderPage.tsx"
      provides: "Plan 02 stub handlers replaced with the real handlers from useBuilderLayers; the [Phase 1041 Plan 03] console.warn lines are removed"
      contains: "handleBulkDelete"
  key_links:
    - from: "frontend/src/components/builder/hooks/use-builder-layers.ts handleBulkDelete"
      to: "frontend/src/api/maps.ts removeLayerFromMapApi"
      via: "Promise.allSettled over N parallel calls; rollback on any reject"
      pattern: "Promise\\.allSettled.*removeLayerFromMapApi"
    - from: "frontend/src/pages/MapBuilderPage.tsx"
      to: "frontend/src/components/builder/BulkActionBar.tsx"
      via: "real handlers threaded through UnifiedStackPanel as onBulk* props"
      pattern: "layers\\.handleBulk"
    - from: "frontend/src/components/builder/hooks/use-builder-layers.ts handleBulkVisibility/Opacity/Group/Ungroup"
      to: "setLocalLayers + setHasUnsavedChanges"
      via: "single setState updater that maps over prev and selectively transforms ids in selection set"
      pattern: "setLocalLayers\\(.+selectedIds\\.has"
---

<objective>
Implement the five bulk operation handlers in `use-builder-layers.ts` per PATTERNS.md "Key Architecture Notes for Planner" #1: visibility / opacity / group / ungroup are PURE LOCAL STATE MUTATIONS (single `setLocalLayers` call each, persisted via the existing Save gate); only `handleBulkDelete` calls the per-layer DELETE endpoint and uses `Promise.allSettled` + rollback. Replace the Plan 02 stubs in `MapBuilderPage` with the real handlers. Closes POL-09 (atomic bulk ops with single rollback for delete).

Purpose: This is the workhorse plan — Plan 01 wired selection state, Plan 02 wired the visible action surface, Plan 03 wires the actual operations. The split between local-state ops and the only-delete-rolls-back contract is critical and was clarified in PATTERNS.md (line 415) and the additional_constraints block — the planner must not invent N-parallel-PATCH calls for visibility/opacity because no per-layer PATCH endpoint exists for those properties; they are deferred to the map save.

Output: Five new exported handlers from `useBuilderLayers`. MapBuilderPage's stub handlers are replaced with the real ones. The `[Phase 1041 Plan 03]` console.warn lines from Plan 02 are gone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-CONTEXT.md
@.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-PATTERNS.md
@.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-UI-SPEC.md
@.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-02-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase 2026-05-14. -->

From frontend/src/components/builder/hooks/use-builder-layers.ts (existing):
- `localLayers: MapLayerResponse[]` state with `setLocalLayers` setter
- `layersRef: useRef(localLayers)` — fresh-state ref synced via useLayoutEffect at lines 84-87
- `setHasUnsavedChanges(true)` — call after any local mutation
- `setExpandedLayerId(prev => ...)` — clear if the expanded id is being deleted
- `removeLayerMutation: ReturnType<typeof useRemoveLayer>` — TanStack mutation; serial. PATTERNS.md note: prefer raw `removeLayerFromMapApi` for parallel batching.
- `handleRemove(layerId)` at lines 261-275 — analog for single delete (mutation-based, with toast)
- `handleCreateGroupWithLayer(layerId)` at lines 282-317 — analog for single-row to group transformation
- `handleUngroup(groupId)` at lines 330-345 — analog for single ungroup
- `handleDeleteGroup(groupId)` at lines 347-358 — analog for group cascade delete (filter children too)
- Return shape includes `handleRemove`, `handleCreateGroupWithLayer`, `handleUngroup`, `handleDeleteGroup`, `handleToggleVisibility`, `handleOpacityChange` etc.

From frontend/src/components/builder/hooks/use-layer-map-sync.ts:
- `handleToggleVisibility(layerId, visible?)` at line 63 — calls `applyLayerUpdate(layerId, (l) => ({...l, visible: nextVisible}), (map) => map.setLayoutProperty(...))`. The map-side update sets MapLibre layer visibility for vector + outline + label + extrusion + cluster sub-layers (six sub-ids: layer-{id}, layer-{id}-outline, layer-{id}-label, layer-{id}-extrusion, layer-{id}-cluster, layer-{id}-cluster-count).
- `handleOpacityChange(layerId, opacity)` at line 185 — analog for opacity. Also calls `applyLayerUpdate` and `map.setPaintProperty(...)` for the relevant paint properties.
- `applyLayerUpdate(layerId, updateFn, mapSideEffectFn)` — the shared primitive used by all per-layer ops.

From frontend/src/api/maps.ts (line 143):
```typescript
export async function removeLayerFromMapApi(mapId: string, layerId: string): Promise<void>;
// DELETE /maps/{mapId}/layers/{layerId} — fires real DELETE. Rejects on non-2xx.
```

From frontend/src/components/builder/types or use-builder-layers.ts (GroupedLayer):
```typescript
type GroupedLayer = Omit<MapLayerResponse, 'layer_type'> & {
  layer_type?: string | null;          // 'group:folder' | 'group:basemap' | dataset record type
  parent_group_id?: string | null;     // in-memory only — not persisted to API
};
```

From frontend/src/pages/MapBuilderPage.tsx (Plan 02 stubs to replace):
```typescript
const handleClearSelection = useCallback(() => { setSelectedIds(new Set()); }, []);
const handleBulkVisibility = useCallback((ids: Set<string>) => { console.warn('[Phase 1041 Plan 03] handleBulkVisibility not wired yet:', ids); }, []);
// ... and 4 more stubs ...
```
Plan 03 replaces these with calls into `layers.handleBulkVisibility`, etc., where `layers = useBuilderLayers(...)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement handleBulkVisibility / Opacity / Group / Ungroup as single-setState local mutations</name>
  <files>frontend/src/components/builder/hooks/use-builder-layers.ts</files>
  <read_first>
    - frontend/src/components/builder/hooks/use-builder-layers.ts entire file (782 lines — focus on lines 60-100 for hook signature, 261-358 for handleRemove + group analogs, 700-770 for return statement)
    - frontend/src/components/builder/hooks/use-layer-map-sync.ts lines 1-200 (applyLayerUpdate primitive — used by handleToggleVisibility / handleOpacityChange to also touch the live MapLibre instance, NOT just localLayers)
    - .planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-PATTERNS.md "use-builder-layers.ts" section + "Key Architecture Notes for Planner" #1
    - .planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-UI-SPEC.md "4. Atomic bulk operations"
  </read_first>
  <behavior>
    - Test 1: Calling `handleBulkVisibility(new Set(['a','b','c']))` when [a:visible, b:visible, c:hidden] (majority visible) flips all three to hidden in ONE setLocalLayers call (assert via mock spy on setLocalLayers — exactly 1 call within the handler).
    - Test 2: Calling `handleBulkVisibility(new Set(['a','b']))` when [a:hidden, b:hidden] (majority NOT visible) flips both to visible in ONE setLocalLayers call.
    - Test 3: Calling `handleBulkVisibility` ALSO calls the live-map setLayoutProperty path for each layer (the same path handleToggleVisibility uses) — assert via spy on map.setLayoutProperty: called for each of the {layer-{id}, layer-{id}-outline, ...} sub-layer ids of each selected id.
    - Test 4: Calling `handleBulkVisibility` calls `setHasUnsavedChanges(true)` exactly once.
    - Test 5: Calling `handleBulkOpacity(new Set(['a','b']), 0.4)` updates BOTH a.opacity and b.opacity to 0.4 in one setLocalLayers call AND fires the live-map setPaintProperty path for each.
    - Test 6: Calling `handleBulkGroup(new Set(['a','b','c']))` when all three are loose vector layers creates ONE new group row (id like `group-{timestamp}`) and sets parent_group_id=newGroupId for all three; the new group inherits the sort_order of the FIRST selected layer; sort_order is renumbered globally.
    - Test 7: Calling `handleBulkGroup` when ANY id is already in a group is a no-op (the BulkActionBar disable rule prevents this UI-side; the handler is the defense-in-depth — it returns early without mutation).
    - Test 8: Calling `handleBulkUngroup(new Set(['g1','g2']))` removes both group rows AND clears parent_group_id on all children of g1 and g2.
    - Test 9: All 4 handlers DO NOT call any API (no `removeLayerFromMapApi`, no `fetch`, no `mutation.mutate`). Persistence is deferred to Save.
    - Test 10: All 4 handlers do NOT show a success toast (per UI-SPEC §4: "no toast — state is already correct from optimistic update").
  </behavior>
  <action>
Add four handlers to `useBuilderLayers` per PATTERNS.md "use-builder-layers.ts" section + UI-SPEC §4 + Key Architecture Note #1. Implementation steps:

1. **Insertion point.** Place the new handlers after `handleDeleteGroup` (line ~358) and before any subsequent handler, grouped together. Order: handleBulkVisibility, handleBulkOpacity, handleBulkGroup, handleBulkUngroup.

2. **handleBulkVisibility(selectedIds: Set string).** This is a `useCallback` that:
   - Snapshots `const current = layersRef.current;`
   - Computes `const selectedLayers = current.filter((l) => selectedIds.has(l.id));`
   - If `selectedLayers.length === 0` return early.
   - Determines `const visibleCount = selectedLayers.filter((l) => l.visible !== false).length;` and `const majorityVisible = visibleCount > selectedLayers.length / 2;` and `const nextVisible = !majorityVisible;`.
   - Single setLocalLayers call: `setLocalLayers((prev) => prev.map((l) => selectedIds.has(l.id) ? { ...l, visible: nextVisible } : l));`
   - Single setHasUnsavedChanges(true).
   - Live-map sync: iterate `selectedLayers.forEach((l) => { /* mirror the map.setLayoutProperty calls from use-layer-map-sync.ts handleToggleVisibility lines 70-83 — for each of the 6 sub-layer ids (layer-{id}, layer-{id}-outline, label, extrusion, cluster, cluster-count), call map.setLayoutProperty(subId, 'visibility', nextVisible ? 'visible' : 'none') if map.getLayer(subId) */ });`. Read `mapInstanceRef.current` directly. If map is null, skip the live-map step (the next render will reconcile).
   - **Important:** do NOT call the existing `handleToggleVisibility(id)` per-id, because that would fire N React re-renders (each call triggers its own setState via applyLayerUpdate). The bulk version is one setState + one map mutation pass.
   - Deps: `[mapInstanceRef]`. layersRef.current is read fresh — no dep needed (matches existing pattern at lines 282-317).

3. **handleBulkOpacity(selectedIds: Set string, opacity: number).** Mirror handleBulkVisibility:
   - Snapshot, filter selectedLayers, early-return if empty.
   - Single setLocalLayers: `setLocalLayers((prev) => prev.map((l) => selectedIds.has(l.id) ? { ...l, opacity } : l));`
   - Single setHasUnsavedChanges(true).
   - Live-map sync: for each selected layer, mirror handleOpacityChange's setPaintProperty calls. Re-read `use-layer-map-sync.ts` line 185 `handleOpacityChange` to identify the exact paint properties (likely `fill-opacity`, `line-opacity`, `circle-opacity`, `raster-opacity`, `icon-opacity`, `text-opacity` on the corresponding sub-layer ids — use the adapter resolution there for accuracy). If the per-layer logic is intricate, an acceptable fallback is to invoke the existing `handleOpacityChange(id, opacity)` for each id IF the executor verifies that doing so still results in a single React commit (React batches setState within event handlers in React 18+ — but to be safe, prefer the single-setState path with manual map mutations).
   - Deps: `[mapInstanceRef]`.

4. **handleBulkGroup(selectedIds: Set string).** Per UI-SPEC §3 enable rule: ALL selected must be loose vector layers (no parent_group_id, layer_type !== 'group:folder', dataset_record_type === 'vector_dataset'). Implementation:
   - Snapshot current = layersRef.current.
   - Defense-in-depth guard: `const selectedLayers = current.filter((l) => selectedIds.has(l.id) && l.dataset_record_type === 'vector_dataset' && !(l as GroupedLayer).parent_group_id && (l as GroupedLayer).layer_type !== 'group:folder');`. If `selectedLayers.length !== selectedIds.size` (some failed the guard) OR `selectedLayers.length < 2`, return early.
   - Generate `const groupId = ` `group-${Date.now()}` (matches handleCreateGroupWithLayer pattern at line 284).
   - Determine groupName: count existing folder groups + 1, e.g. `Group ${existingGroupCount + 1}`.
   - Determine insertion sort_order: use the smallest sort_order among selectedLayers — the new group row sits at the position of the FIRST selected layer.
   - Construct the new group row: `{ id: groupId, display_name: groupName, layer_type: 'group:folder', sort_order: minSortOrder, parent_group_id: null, ...minimal MapLayerResponse defaults }`. Mirror handleCreateGroupWithLayer's groupRow construction at lines 296-303.
   - Single setLocalLayers: produce `next = [...prev]`. For each selectedId, set `parent_group_id = groupId` on the matching layer. Splice the new group row in at the position of the first selected layer (or just push and sort by sort_order). Renumber sort_order globally via `next.map((l, i) => ({ ...l, sort_order: i }))`. Return `next`.
   - setGroupMeta((prev) => ({ ...prev, [groupId]: { expanded: true } }));  // auto-expand so children are visible
   - setHasUnsavedChanges(true).
   - No live-map sync needed — group rows are UI-only (no MapLibre layer).
   - Deps: `[]`.

5. **handleBulkUngroup(selectedIds: Set string).** Per UI-SPEC §3 enable rule: ALL selected must be folder-group rows.
   - Snapshot current = layersRef.current.
   - Defense-in-depth guard: `const selectedGroups = current.filter((l) => selectedIds.has(l.id) && (l as GroupedLayer).layer_type === 'group:folder');`. If `selectedGroups.length !== selectedIds.size` OR `selectedGroups.length === 0`, return early.
   - Single setLocalLayers: filter out the group rows themselves AND clear parent_group_id on any layer whose parent_group_id is in `selectedIds`. Renumber sort_order. Mirror handleUngroup at lines 330-345 but iterate over the set.
   - setHasUnsavedChanges(true).
   - Deps: `[]`.

6. **Export.** Add all four to the return statement of `useBuilderLayers` (around line 740-770). Match the existing alphabetical-ish ordering — group them with the bulk-related additions to make the diff obvious.

7. **No toast on success.** Per UI-SPEC §4: success state is the optimistic update itself. Do NOT call toast.success in any of these four handlers. (Error toast for delete only — Task 2.)

8. **Type the params.** All four use `selectedIds: Set string` as the first param. Opacity uses `opacity: number` (0..1) as the second.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npx tsc -b --noEmit 2>&amp;1 | tail -10 && grep -c "handleBulkVisibility\|handleBulkOpacity\|handleBulkGroup\|handleBulkUngroup" /Users/ishiland/Code/geolens/frontend/src/components/builder/hooks/use-builder-layers.ts</automated>
  </verify>
  <acceptance_criteria>
    - tsc 0 errors.
    - All four handler names appear in `use-builder-layers.ts` (each `grep -c handleBulkXxx` >= 2: declaration + return-statement export).
    - Each handler uses `setLocalLayers((prev) => ...)` exactly ONCE (not N times in a loop).
    - Each handler calls `setHasUnsavedChanges(true)` exactly ONCE.
    - No `removeLayerFromMapApi`, no `fetch`, no `.mutate(` in any of the 4 handlers.
    - No `toast.success` in any of the 4 handlers.
    - Defense-in-depth guards present: `handleBulkGroup` checks `dataset_record_type === 'vector_dataset' && !parent_group_id && layer_type !== 'group:folder'`; `handleBulkUngroup` checks `layer_type === 'group:folder'`.
    - Existing builder vitest suite still green: `cd frontend && npx vitest run src/components/builder/hooks/__tests__/ 2>&1 | tail -10` reports 0 NEW failures.
  </acceptance_criteria>
  <done>
    - All acceptance criteria pass.
    - The four handlers are exported and ready for Task 2 to wire into MapBuilderPage.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement handleBulkDelete with Promise.allSettled + rollback + wire all 5 into MapBuilderPage</name>
  <files>frontend/src/components/builder/hooks/use-builder-layers.ts, frontend/src/pages/MapBuilderPage.tsx</files>
  <read_first>
    - frontend/src/components/builder/hooks/use-builder-layers.ts lines 261-275 (handleRemove — single-delete analog with mutation + toasts)
    - frontend/src/api/maps.ts lines 143-150 (removeLayerFromMapApi signature)
    - frontend/src/components/import/UploadForm.tsx lines 109-131 (Promise.allSettled analog)
    - frontend/src/pages/MapBuilderPage.tsx (Plan 02 stub handlers area; the JSX `<UnifiedStackPanel ...>` props pass-through)
    - .planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-PATTERNS.md "Bulk delete" paragraph + Key Architecture Notes #1
    - .planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-UI-SPEC.md "4. Atomic bulk operations" + "5. Bulk delete confirmation"
  </read_first>
  <behavior>
    - Test 1: Calling `handleBulkDelete(new Set(['a','b','c']))` snapshots layersRef.current as previousLayers, then calls setLocalLayers((prev) => prev.filter(l => !selectedIds.has(l.id))) ONCE, then fires THREE parallel removeLayerFromMapApi calls.
    - Test 2: When all three DELETE calls resolve, the optimistic state is kept; the queryClient.invalidateQueries for the map's layers fires once at the end; the handler returns Promise resolving to `true`.
    - Test 3: When ONE DELETE call rejects, setLocalLayers is called a SECOND time with previousLayers (rollback), and toast.error fires once with `t('bulkActions.errorDeleteRolledBack', { count: 3 })`; the handler returns Promise resolving to `false`.
    - Test 4: handleBulkDelete returns Promise<boolean> (true on success, false on any failure).
    - Test 5: handleBulkDelete clears expandedLayerId if it is in selectedIds (mirrors handleRemove pattern at line 263).
    - Test 6: handleBulkDelete does NOT use removeLayerMutation.mutate — it uses removeLayerFromMapApi DIRECTLY for parallelism (per PATTERNS.md note: TanStack mutations serialize; raw API for parallel batching).
    - Test 7: After Plan 03 ships, MapBuilderPage.tsx contains zero `[Phase 1041 Plan 03]` console.warn lines.
    - Test 8: MapBuilderPage passes wrappers around `layers.handleBulkVisibility`, `.handleBulkOpacity`, `.handleBulkGroup`, `.handleBulkUngroup`, `.handleBulkDelete` as props to `<UnifiedStackPanel>` — the wrappers clear selection on success.
  </behavior>
  <action>
Add `handleBulkDelete` to `use-builder-layers.ts` and replace the Plan 02 stubs in `MapBuilderPage`. Implementation steps:

1. **Imports.** In `use-builder-layers.ts`, add `import { removeLayerFromMapApi } from '@/api/maps';` to the existing imports (alphabetical position). Verify no circular import — `api/maps.ts` is leaf-level. Also add `import { useQueryClient } from '@tanstack/react-query';` if not already imported (check existing imports first).

2. **queryClient handle.** Inside `useBuilderLayers`, add `const queryClient = useQueryClient();` near the top of the hook body (after the existing useState declarations, before the useEffects).

3. **handleBulkDelete(selectedIds: Set string).** Place after handleBulkUngroup. Implementation:
   - Signature: `const handleBulkDelete = useCallback(async (selectedIds: Set<string>): Promise<boolean> => { ... }, [mapId, t, queryClient]);`
   - Early return false if `!mapId || selectedIds.size === 0`.
   - Snapshot: `const previousLayers = layersRef.current;`
   - Snapshot the selected layer ids as an array: `const idsToDelete = Array.from(selectedIds);`
   - Clear expandedLayerId if it is in the selection: `setExpandedLayerId((prev) => prev && selectedIds.has(prev) ? null : prev);`
   - Optimistic update — single setLocalLayers: `setLocalLayers((prev) => prev.filter((l) => !selectedIds.has(l.id)).map((l, i) => ({ ...l, sort_order: i })));`. Renumber sort_order so the order stays compact.
   - Fire N parallel DELETEs:
     ```
     const results = await Promise.allSettled(
       idsToDelete.map((id) => removeLayerFromMapApi(mapId, id))
     );
     ```
   - On any rejection: `const anyFailed = results.some((r) => r.status === 'rejected');`
     - If anyFailed: `setLocalLayers(previousLayers);` (rollback), `toast.error(t('bulkActions.errorDeleteRolledBack', { count: idsToDelete.length }));`. Return `false`. Do NOT clear selection — caller decides (we return false so MapBuilderPage knows not to clear).
   - On all success: `await queryClient.invalidateQueries({ queryKey: ['maps', mapId] });` and return `true`. No success toast (per UI-SPEC §4).
   - Deps: `[mapId, t, queryClient]`.

4. **Export.** Add `handleBulkDelete` to the return statement of `useBuilderLayers`. Group with the other 4 bulk handlers from Task 1.

5. **Replace Plan 02 stubs in MapBuilderPage.** Find the 5 `console.warn('[Phase 1041 Plan 03] handleBulkXxx not wired yet:', ...)` blocks added in Plan 02. Replace each useCallback body with a wrapper that calls into `layers.handleBulkXxx` and clears selection on success. Per UI-SPEC §4: visibility, opacity, group, ungroup all clear selection synchronously on success; delete clears only on the success path of the returned Promise.

   Example wrappers:
   ```
   const onBulkVisibility = useCallback((ids: Set<string>) => {
     layers.handleBulkVisibility(ids);
     setSelectedIds(new Set());
   }, [layers, setSelectedIds]);

   const onBulkOpacity = useCallback((ids: Set<string>, opacity: number) => {
     layers.handleBulkOpacity(ids, opacity);
     // NOTE: do NOT clear selection on opacity drag — the slider fires multiple times during drag.
     // Selection clears only on commit. For v1.5, accept that opacity drag preserves selection;
     // user can Escape to clear. Document in SUMMARY.
   }, [layers]);

   const onBulkGroup = useCallback((ids: Set<string>) => {
     layers.handleBulkGroup(ids);
     setSelectedIds(new Set());
   }, [layers, setSelectedIds]);

   const onBulkUngroup = useCallback((ids: Set<string>) => {
     layers.handleBulkUngroup(ids);
     setSelectedIds(new Set());
   }, [layers, setSelectedIds]);

   const onBulkDelete = useCallback((ids: Set<string>) => {
     layers.handleBulkDelete(ids).then((ok) => {
       if (ok) setSelectedIds(new Set());
       // on failure: selection preserved so user can retry
     });
   }, [layers, setSelectedIds]);
   ```

   The `[Phase 1041 Plan 03]` console.warn lines must be GONE (grep returns 0).

6. **Selection-clear nuance for opacity.** The opacity slider fires `onValueChange` continuously during drag. If we cleared selection on the first onValueChange, the subsequent ones would have an empty selection set. Solution: opacity-bulk-op preserves selection; user dismisses with Escape OR by clicking another row. Document this exception in the SUMMARY (it is Claude's discretion territory; UI-SPEC §4 says "selection clears on successful bulk op completion" but for a continuously-firing slider this would be hostile UX).

7. **Cancel-mid-flight guard (defensive note).** If the user clicks Save (which fires its own DELETE-everything-then-POST flow) while a bulk delete is in flight, the rollback path could conflict. Acceptable in this phase: the bulk delete completes within ~hundreds of milliseconds and Save also requires the user to click a button — they cannot do both simultaneously without intent. Document this as a known race in the SUMMARY that would need real handling if we add background autosave (out of scope for v1.5).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npx tsc -b --noEmit 2>&amp;1 | tail -10 && grep -c "handleBulkDelete" /Users/ishiland/Code/geolens/frontend/src/components/builder/hooks/use-builder-layers.ts && grep -c "Promise\.allSettled" /Users/ishiland/Code/geolens/frontend/src/components/builder/hooks/use-builder-layers.ts && grep -c "removeLayerFromMapApi" /Users/ishiland/Code/geolens/frontend/src/components/builder/hooks/use-builder-layers.ts && grep -c "errorDeleteRolledBack" /Users/ishiland/Code/geolens/frontend/src/components/builder/hooks/use-builder-layers.ts && grep -c "Phase 1041 Plan 03" /Users/ishiland/Code/geolens/frontend/src/pages/MapBuilderPage.tsx && grep -c "layers.handleBulk" /Users/ishiland/Code/geolens/frontend/src/pages/MapBuilderPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tsc 0 errors.
    - `handleBulkDelete` declared in `use-builder-layers.ts` (>=2 grep occurrences for declaration + return).
    - `Promise.allSettled` used inside handleBulkDelete (>=1 occurrence).
    - `removeLayerFromMapApi` imported and called inside handleBulkDelete (>=2 occurrences: import + call).
    - `errorDeleteRolledBack` referenced via `t('bulkActions.errorDeleteRolledBack', ...)` inside handleBulkDelete.
    - `Phase 1041 Plan 03` string returns 0 grep matches in MapBuilderPage.tsx (all stubs replaced).
    - `layers.handleBulk` returns >=5 grep matches in MapBuilderPage.tsx (5 wrappers calling into the hook handlers).
    - `useQueryClient` imported and called once.
    - `queryClient.invalidateQueries` called inside handleBulkDelete success path.
    - No new design tokens introduced; `git diff frontend/src/index.css` returns empty.
    - Build green: `cd frontend && npm run build 2>&1 | tail -5` exit 0.
  </acceptance_criteria>
  <done>
    - All acceptance criteria pass.
    - Bulk Delete with mocked failing API call rolls back the optimistic state and surfaces the error toast (verified by Plan 04 vitest).
    - Bulk Delete with passing API call removes the layers, invalidates the query cache, and clears selection (verified by Plan 04 vitest).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component → API | handleBulkDelete fires N parallel DELETEs; failures must trigger atomic rollback to preserve user's mental model |
| Optimistic state → Persistent state | Visibility / opacity / group / ungroup mutate `localLayers`; persistence happens only on Save — race with explicit Save click is the design boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1041-09 | Tampering | handleBulkGroup defense-in-depth | mitigate | Even if BulkActionBar's `canGroup` gate is bypassed via direct hook invocation, handleBulkGroup re-checks every selected layer's `dataset_record_type === 'vector_dataset' && !parent_group_id && layer_type !== 'group:folder'` and returns early on any mismatch. No partial state. |
| T-1041-10 | Denial of Service | handleBulkDelete with N=1000 selections | accept | Selection model has no hard cap; a malicious or fat-fingered Cmd-A-then-Delete would fire 1000 parallel DELETEs. The browser will queue these per-host (typically 6 concurrent). Acceptable for v1.5 — the typical map has <50 layers. Add a hard cap (e.g., 50) only if Phase 1044 UAT surfaces real abuse. |
| T-1041-11 | Information Disclosure | handleBulkDelete error toast | mitigate | Error toast uses i18n key `bulkActions.errorDeleteRolledBack` with only the `{count}` interpolated — never the layer names or IDs. No PII leakage. |
| T-1041-12 | Repudiation | Per-layer DELETE audit trail | accept | Each `removeLayerFromMapApi` call hits the existing per-layer DELETE endpoint, which already writes the standard audit log entry. Bulk delete audit appears as N rapid single-delete entries — acceptable for v1.5; a "bulk_delete" audit action would be a future enhancement (out of scope). |
| T-1041-13 | Elevation of Privilege | None new | accept | No new authorization surface. The existing per-layer DELETE endpoint enforces map ownership. handleBulkDelete cannot escalate beyond what handleRemove already does, just N times. |
</threat_model>

<verification>
End-of-plan checks:

```bash
cd /Users/ishiland/Code/geolens/frontend
npx tsc -b --noEmit                         # 0 errors
npx vitest run src/components/builder/ 2>&1 | tail -20    # 0 NEW failures
npm run build 2>&1 | tail -5                # 0 errors
grep -c "Phase 1041 Plan 03" src/pages/MapBuilderPage.tsx    # must be 0 (stubs replaced)
```

Manual smoke (optional):
1. Run dev server, open Map Builder with 4 overlay layers.
2. Cmd-click 3 rows → BulkActionBar appears.
3. Click Visibility → all 3 hide in one render; selection clears.
4. Cmd-click 2 rows → click Group → new "Group 1" row appears with both as children; selection clears.
5. Cmd-click the new group row → click Ungroup → group disappears, children promote to top level.
6. Cmd-click 2 rows → click Delete → confirmation appears → click "Delete 2 layers" → both DELETE calls fire in parallel (verify in Network tab); rows disappear; selection clears.
7. (To test rollback) temporarily make `removeLayerFromMapApi` throw; repeat step 6 → rows reappear; toast shows error; selection preserved.
</verification>

<success_criteria>
- POL-09 fully shipped: 4 local-state bulk handlers (visibility/opacity/group/ungroup) + 1 API-driven bulk handler (delete) with Promise.allSettled + rollback + single error toast.
- All 5 Plan 02 stubs replaced in MapBuilderPage with real handlers.
- Selection clears on successful bulk op (with the documented opacity exception).
- No new design tokens introduced.
- No new backend endpoints.
- All four hard constraints from CONTEXT.md respected.
</success_criteria>

<output>
After completion, create `.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-03-SUMMARY.md`. Document:
- Decision: visibility/opacity live-map sync chose single-setState path with manual map mutations vs N-call applyLayerUpdate. State which path was chosen and why.
- Decision: opacity bulk op does NOT clear selection on each onValueChange (slider fires continuously during drag). User dismisses via Escape. UI-SPEC §4 said "clear on success" but for a continuous control this would be hostile UX. Acceptable per Claude's Discretion in CONTEXT.md.
- Known race: bulk delete in flight + user clicks Save → behavior is "last writer wins" because Save also calls DELETE on absent layers and POST on present ones. Acceptable for v1.5 (no background autosave).
- Audit trail note: bulk delete writes N audit entries via the per-layer DELETE endpoint. No "bulk_delete" composite audit action — that would be a future enhancement.
</output>
