---
phase: 1041
plan: "04"
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - frontend/src/components/builder/__tests__/UnifiedStackPanel.multi-select.test.tsx
  - frontend/src/components/builder/__tests__/BulkActionBar.test.tsx
  - frontend/src/components/builder/hooks/__tests__/use-builder-layers.bulk-ops.test.ts
autonomous: true
requirements:
  - POL-06
  - POL-07
  - POL-08
  - POL-09
  - POL-10
  - POL-11
tags:
  - mapbuilder
  - vitest
  - test-coverage
  - selection
  - bulk-actions

must_haves:
  truths:
    - "User runs `npx vitest run src/components/builder/__tests__/UnifiedStackPanel.multi-select.test.tsx` and sees coverage of: Cmd-click toggle, Shift-click range, plain-click clears, Space toggle, Escape clears, outside-click clears, basemap boundary refusal, drag-start clears multi-selection."
    - "User runs `npx vitest run src/components/builder/__tests__/BulkActionBar.test.tsx` and sees coverage of: bar render condition (>=2 selected), 5 action buttons, disable rules (canGroup/canUngroup), confirmation state machine (Delete → confirm → fire), Cancel autoFocus, Escape exits confirmation, ARIA toolbar/aria-live."
    - "User runs `npx vitest run src/components/builder/hooks/__tests__/use-builder-layers.bulk-ops.test.ts` and sees coverage of: handleBulkVisibility single setState, handleBulkOpacity single setState, handleBulkGroup creates one group with children, handleBulkUngroup, handleBulkDelete success path (parallel DELETEs + invalidate), handleBulkDelete failure path (rollback + error toast)."
    - "User runs `npx vitest run src/components/builder/` at the end of Plan 04 and sees a NEW POL-06..POL-11 test surface that is green; the existing builder vitest suite is also still green."
  artifacts:
    - path: "frontend/src/components/builder/__tests__/UnifiedStackPanel.multi-select.test.tsx"
      provides: "Selection-model tests covering POL-06 (mouse + keyboard), POL-07 (visual state via aria-selected + checkbox presence), POL-10 (clearing rules), POL-11 (basemap boundary)"
      min_lines: 200
    - path: "frontend/src/components/builder/__tests__/BulkActionBar.test.tsx"
      provides: "BulkActionBar tests covering POL-08 (render + 5 actions), confirmation state machine, ARIA"
      min_lines: 150
    - path: "frontend/src/components/builder/hooks/__tests__/use-builder-layers.bulk-ops.test.ts"
      provides: "Hook tests covering POL-09 (atomic local-state ops + delete-rollback)"
      min_lines: 200
  key_links:
    - from: "UnifiedStackPanel.multi-select.test.tsx"
      to: "frontend/src/pages/MapBuilderPage.tsx + frontend/src/components/builder/UnifiedStackPanel.tsx"
      via: "Render the integrated MapBuilderPage with mocked APIs OR render UnifiedStackPanel with controlled selectedIds prop"
      pattern: "render\\(.+UnifiedStackPanel|render\\(.+MapBuilderPage"
    - from: "BulkActionBar.test.tsx"
      to: "frontend/src/components/builder/BulkActionBar.tsx"
      via: "Direct render with mock layers + selectedIds"
      pattern: "import.+BulkActionBar"
    - from: "use-builder-layers.bulk-ops.test.ts"
      to: "frontend/src/components/builder/hooks/use-builder-layers.ts"
      via: "renderHook with mocked map + mocked removeLayerFromMapApi"
      pattern: "renderHook.+useBuilderLayers"
---

<objective>
Land vitest coverage for the entire Phase 1041 surface — the selection model (Plan 01), the BulkActionBar component (Plan 02), and the bulk operation handlers (Plan 03). Three new test files; ~25 tests total. Closes the test-debt side of POL-06..POL-11 by giving each requirement assertions a future regression can fail on.

Purpose: Phase 1044 owns the Playwright UAT spec (POL-24) and the full a11y + i18n verification (POL-22, POL-23, POL-25). Plan 04 here is the unit/integration vitest tier that catches per-component regressions before they reach Playwright. Without Plan 04, a careless future refactor of `setSelectedIds` or `Promise.allSettled` could silently break atomic rollback and only Playwright would catch it days later.

Output: Three new test files, all green, asserting the contracts defined in Plans 01-03. The full builder vitest sweep stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-CONTEXT.md
@.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-PATTERNS.md
@.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-UI-SPEC.md
@.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-01-SUMMARY.md
@.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-02-SUMMARY.md
@.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-03-SUMMARY.md

<interfaces>
<!-- Test patterns to mirror — already proven in the Phase 1040 suite -->

From frontend/src/components/builder/__tests__/UnifiedStackPanel.test.tsx (existing — Phase 1040 suite):
- Uses `@testing-library/react` `render`, `screen`, `fireEvent`, `within`.
- Wraps render in `<I18nextProvider i18n={i18n}>` + `<TooltipProvider>` + `<DndContext>` + `<MemoryRouter>` (or whatever the existing test setup uses — copy the wrapper helper).
- Mocks i18n via the existing test i18n instance.
- Phase 1040 added `Phase 1040 catalog drop` describe blocks — mirror that structure with a `Phase 1041 multi-selection` describe block.

From frontend/src/components/builder/__tests__/DatasetSearchPanel.dragdrop.test.tsx (existing — Phase 1040 plan 04):
- Pattern for spying on `useDraggable` via `vi.spyOn(dndCore, 'useDraggable')`.
- Pattern for mocking the `react-dnd-kit` context.

From frontend/src/components/builder/hooks/__tests__/use-builder-layers.add-dataset.test.ts (existing — currently failing per POL-20):
- `renderHook` pattern for testing the useBuilderLayers hook directly.
- Mocks for `useAddLayer`, `useRemoveLayer` (TanStack mutations).
- **WARNING:** This file has a known worker-timeout bug (POL-20 — Phase 1039 owns the fix). Plan 04 here MUST NOT make the worker problem worse. Avoid heavy `vi.mock` at the file scope (PATTERNS.md note: prefer `vi.spyOn` per-test). If `bulk-ops.test.ts` triggers the same worker exit, document the symptom and defer the test to be re-enabled after Phase 1039 ships its fix — but DO NOT skip the assertions; convert to a synchronous test using a different test boundary (e.g., test the handler in isolation with a pure-function adapter).

From frontend/src/components/builder/hooks/use-builder-layers.ts (Plan 03 output):
```typescript
function useBuilderLayers(mapData, mapInstanceRef, mapId, addLayerMutation, removeLayerMutation): {
  // ... existing returns ...
  handleBulkVisibility: (ids: Set<string>) => void;
  handleBulkOpacity: (ids: Set<string>, opacity: number) => void;
  handleBulkGroup: (ids: Set<string>) => void;
  handleBulkUngroup: (ids: Set<string>) => void;
  handleBulkDelete: (ids: Set<string>) => Promise<boolean>;
  // ... existing returns ...
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Selection model + clearing tests (UnifiedStackPanel.multi-select.test.tsx)</name>
  <files>frontend/src/components/builder/__tests__/UnifiedStackPanel.multi-select.test.tsx</files>
  <read_first>
    - frontend/src/components/builder/__tests__/UnifiedStackPanel.test.tsx (existing — copy the test wrapper helper, render setup, mock layer fixtures)
    - frontend/src/components/builder/__tests__/DatasetSearchPanel.dragdrop.test.tsx (existing — Phase 1040 pattern for cross-context tests)
    - frontend/src/components/builder/UnifiedStackPanel.tsx (Plan 01 + Plan 02 output — verify final selection-state shape and prop names)
    - frontend/src/pages/MapBuilderPage.tsx (Plan 01 + Plan 03 output — verify the lifted selection state and bulk handler wrappers)
    - .planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-UI-SPEC.md sections 1, 2, 6, 7, 8 (canonical contract — tests must mirror these exactly)
  </read_first>
  <action>
Create `frontend/src/components/builder/__tests__/UnifiedStackPanel.multi-select.test.tsx`. Follow the existing `UnifiedStackPanel.test.tsx` wrapper pattern. Implementation steps:

1. **Test wrapper.** Copy the `renderUnifiedStackPanel` helper (or whatever the existing file calls it) — same I18nextProvider + TooltipProvider + DndContext + MemoryRouter wrappers. The helper must accept overrides for: `layers` (mock GroupedLayer[]), `selectedIds` (Set<string>), `selectedLayerId` (string | null), and the 5 onBulk* + onCmdClick + onShiftClick + onCheckboxClick callbacks.

   Decision: because Plan 01 lifts selectedIds to MapBuilderPage, the cleanest test boundary is to render `MapBuilderPage` with mocked api/data hooks. Alternatively, render `UnifiedStackPanel` directly with the new selection props (after Plan 01 reshapes its props interface). Choose whichever requires LESS mocking — likely render UnifiedStackPanel directly with controlled props (matches existing test file's pattern) and verify that the right callbacks fire on the right events.

2. **Test fixtures.** Build a `mockLayers` array with at least:
   - 3 vector overlay layers (`a`, `b`, `c` — `dataset_record_type: 'vector_dataset'`, `layer_type: null`)
   - 1 raster overlay layer (`r1` — `dataset_record_type: 'raster_dataset'`)
   - 1 folder-group row (`g1` — `layer_type: 'group:folder'`)
   - Basemap group row (`bm` — `layer_type: 'group:basemap'`) plus 1 basemap sublayer (`bm-1` — child of `bm`)
   This shape covers all the boundary cases below.

3. **Describe block: `Phase 1041 — selection model (POL-06)`.**
   - Test 1: "Cmd-click toggles a row into selectedIds" — fire a click with `metaKey: true` on row `a`, assert that `onCmdClick` mock callback was called with `'a'` AND `onSelectLayer` was NOT called.
   - Test 2: "Shift-click after Cmd-click extends the range" — Cmd-click `a`, then Shift-click `c`; assert `onShiftClick` called with `'c'`. (The actual range computation lives in MapBuilderPage; this test asserts the wiring fires.)
   - Test 3: "Plain click on a row calls onSelectLayer" — click row `b` with no modifiers; assert `onSelectLayer('b')` called and `onCmdClick` NOT called.
   - Test 4: "Space key on a focused row fires onCmdClick" — focus row `a`, `fireEvent.keyDown(row, { key: ' ' })`; assert `onCmdClick('a')` called and `event.defaultPrevented`.

4. **Describe block: `Phase 1041 — visual state (POL-07)`.**
   - Test 5: "isMultiSelected=true renders a Checkbox in the caret column" — render with `isMultiSelectionActive=true, isMultiSelected=true` for row `a`; query for `<input type='checkbox'>` (Radix Checkbox renders an internal checkbox) within row `a`; assert it is checked.
   - Test 6: "isMultiSelectionActive=false hides the Checkbox" — render with `isMultiSelectionActive=false`; assert no Checkbox is rendered for row `a`.
   - Test 7: "aria-selected on the row reflects (selected || isMultiSelected)" — render with `selected=false, isMultiSelected=true`; query for the row element; assert `aria-selected="true"`.
   - Test 8: "Listbox has aria-multiselectable=true" — render the panel; assert the role="listbox" element has `aria-multiselectable="true"`.

5. **Describe block: `Phase 1041 — clearing rules (POL-10)`.**
   - Test 9: "Escape clears selection" — render with `selectedIds=new Set(['a'])`; fire `keydown` Escape on the document; assert `onClearSelection` (or however Plan 01 surfaces clearing) was called. If selectedIds is internal to UnifiedStackPanel, assert the next render shows aria-selected='false' on row `a`.
   - Test 10: "outside-click clears selection" — render with `selectedIds=new Set(['a'])`; create a sibling `<div data-testid='outside'>outside</div>`; `fireEvent.mouseDown` on the outside element; assert clearing.
   - Test 11: "click inside the stack panel does NOT clear selection via outside-click handler" — fire mousedown on row `b` inside the panel; assert outside-click handler did NOT fire (selection preserved). The row's own onClick handles the per-row behavior.

6. **Describe block: `Phase 1041 — basemap boundary (POL-11)`.**
   - Test 12: "Cmd-click on basemap group row does NOT add to selectedIds" — fire metaKey click on `bm`; assert `onCmdClick` either not called OR called but the parent handler refuses (depends on where the boundary guard lives — per Plan 01 it lives in the parent's `handleCmdClick`). Best assertion: render the integrated MapBuilderPage and assert that after the gesture, no checkbox appears on `bm` AND the BulkActionBar is not rendered.
   - Test 13: "Cmd-click on basemap sublayer row does NOT add to selectedIds" — same as Test 12 but on `bm-1`.
   - Test 14: "BasemapGroupRow with isMultiSelectionActive=true renders cursor-not-allowed in className" — render with the prop true; assert the row's outer element has class `cursor-not-allowed`.

7. **Describe block: `Phase 1041 — drag-start mutual exclusion (POL-10)`.**
   - Test 15: "Calling MapBuilderPage handleDragStart while selectedIds.size > 0 clears selection" — render the integrated MapBuilderPage with mocked layers; programmatically dispatch a DnD drag-start event (via DndContext's `useSensors`-driven test path OR by directly calling the hoisted handleDragStart in a test-only export). Assert selectedIds clears.
   - If MapBuilderPage doesn't expose the handler directly, use the indirect path: render with `selectedIds=new Set(['a'])`; render a `<DragOverlay>` test harness; fire a draggable's pointer-down + move + drop sequence; assert the multi-selection visual is gone after drag-start.

7b. **Describe block: `Phase 1041 — Shift+Arrow keyboard extension (POL-06)`.**
   - Test 16: "Shift+ArrowDown extends selection to the next selectable row AND moves DOM focus" — render with rows `[bm, a, b, c]` (basemap + 3 overlays) and `selectedIds=new Set(['a'])`. Programmatically focus the row element with `data-row-id='a'`. Fire `keyDown` with `{ key: 'ArrowDown', shiftKey: true }` on the stack panel. Assert: `selectedIds` is now `new Set(['a','b'])`, the row with `data-row-id='b'` is `document.activeElement`, AND `event.defaultPrevented === true`.
   - Test 17: "Shift+ArrowUp at top of selectable rows is a no-op" — render with `selectedIds=new Set(['a'])` (`a` is the topmost overlay above the basemap group). Focus row `a`. Fire keyDown Shift+ArrowUp. Assert: `selectedIds` unchanged (still `{'a'}`), DOM focus still on row `a`. (Confirms basemap group/sublayer ids are excluded from the `selectableRowIds` traversal — the keyboard cannot escape into the basemap boundary.)
   - Test 18: "Plain ArrowDown without Shift does NOT extend selection" — render same scenario; focus `a`; fire keyDown ArrowDown without Shift; assert `selectedIds` unchanged. (Plain arrow navigation is owned by `@dnd-kit` KeyboardSensor; this assertion just confirms our Shift handler didn't accidentally trigger on the no-shift path.)

8. **Test-only mocks.** Per PATTERNS.md anti-pattern note: avoid file-level `vi.mock` for `@dnd-kit/core` (causes worker exit per POL-20 root cause). Use `vi.spyOn` per-test where needed. Mock i18next via the existing test i18n setup. Mock the api hooks (`useAddLayer`, `useRemoveLayer`) with `vi.fn` that returns `{ mutate: vi.fn(), mutateAsync: vi.fn() }` shape.

9. **Run target.** All tests should pass within 5 seconds. If any test is flaky (selection-state timing), add `await waitFor(() => expect(...))` from `@testing-library/react`.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npx vitest run src/components/builder/__tests__/UnifiedStackPanel.multi-select.test.tsx 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `frontend/src/components/builder/__tests__/UnifiedStackPanel.multi-select.test.tsx`.
    - File contains at least 4 describe blocks: "selection model (POL-06)", "visual state (POL-07)", "clearing rules (POL-10)", "basemap boundary (POL-11)" — and ideally a 5th for "drag-start mutual exclusion".
    - File contains at least 12 distinct `it(...)` test cases (some describe blocks may have more than one test).
    - `npx vitest run src/components/builder/__tests__/UnifiedStackPanel.multi-select.test.tsx` reports all tests pass and 0 unhandled worker errors.
    - The file does NOT use `vi.mock('@dnd-kit/core', ...)` at file scope (PATTERNS.md anti-pattern; use spyOn per-test).
    - Existing `UnifiedStackPanel.test.tsx` still green (Plan 04 must not regress Phase 1040 coverage): `npx vitest run src/components/builder/__tests__/UnifiedStackPanel.test.tsx 2>&1 | tail -5` reports 0 failures.
  </acceptance_criteria>
  <done>
    - All acceptance criteria pass.
    - Each POL-06/07/10/11 requirement has at least one test that would fail if a future refactor broke that requirement.
  </done>
</task>

<task type="auto">
  <name>Task 2: BulkActionBar component tests (BulkActionBar.test.tsx)</name>
  <files>frontend/src/components/builder/__tests__/BulkActionBar.test.tsx</files>
  <read_first>
    - frontend/src/components/builder/BulkActionBar.tsx (Plan 02 output — verify final prop shape, render conditions, confirmation state machine)
    - frontend/src/components/builder/__tests__/UnifiedStackPanel.test.tsx (test wrapper helper to copy)
    - .planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-UI-SPEC.md sections "Decision A", 3, 5, 7
  </read_first>
  <action>
Create `frontend/src/components/builder/__tests__/BulkActionBar.test.tsx`. Render BulkActionBar directly with controlled props (no need to render the full UnifiedStackPanel). Implementation steps:

1. **Test wrapper.** A minimal wrapper: `<I18nextProvider i18n={i18n}><TooltipProvider>{children}</TooltipProvider></I18nextProvider>`. No DndContext or Router needed — BulkActionBar is self-contained.

2. **Test fixtures.** Build a `mockLayers` array (5 entries: 3 vector loose, 1 raster, 1 folder-group). Build helper functions: `makeProps({ selectedIds, layers })` that returns a full props object with `vi.fn()` for all 6 callbacks.

3. **Describe block: `BulkActionBar — render condition (POL-08)`.**
   - Test 1: "Renders role='toolbar' with aria-label including count" — render with selectedIds.size=2; query `[role='toolbar']`; assert `aria-label` contains '2'.
   - Test 2: "Renders aria-live='polite'" — assert the toolbar has aria-live attribute.
   - Test 3: "Shows '2 selected' count label" — assert text '2 selected' is visible.
   - Test 4: "Shows all 5 action buttons in normal state" — query for buttons by aria-label patterns: 'visibility', 'opacity', 'group', 'ungroup', 'delete'; assert each is present.

4. **Describe block: `BulkActionBar — disable rules (POL-08)`.**
   - Test 5: "Group button disabled when any selected layer has parent_group_id" — selectedIds includes a layer with `parent_group_id` set; assert Group button has `disabled` attr OR `aria-disabled="true"` OR class `pointer-events-none`.
   - Test 6: "Group button disabled when raster layer in selection" — selectedIds includes a raster layer; assert Group disabled.
   - Test 7: "Ungroup button disabled when not all are folder-group rows" — selectedIds includes mix of group + loose; assert Ungroup disabled.
   - Test 8: "Ungroup button enabled when all selected are folder-group rows" — selectedIds includes only folder-group ids; assert Ungroup enabled.
   - Test 9: "Visibility button reflects majority-visible state with EyeOff icon" — selectedIds includes layers where majority are visible; assert the Eye-style icon component rendered indicates the toggle-to-hide direction (test by testid or by inner SVG class).

5. **Describe block: `BulkActionBar — confirmation state machine (POL-08, UI-SPEC §5)`.**
   - Test 10: "Clicking Delete enters confirmation state" — render bar; click Delete; assert: 5 action buttons gone OR hidden, confirmation label visible with text matching `/Delete 2 layers\? This cannot be undone\./`, two new buttons (Cancel + 'Delete 2 layers').
   - Test 11: "Cancel button has autoFocus" — enter confirmation; assert `document.activeElement` is the Cancel button (or Cancel button has `autoFocus` attr).
   - Test 12: "Clicking Cancel returns to normal state" — enter confirmation; click Cancel; assert 5 action buttons visible again, confirmation label gone.
   - Test 13: "Pressing Escape returns to normal state" — enter confirmation; fireEvent.keyDown on the toolbar with key='Escape'; assert returned to normal state.
   - Test 14: "Clicking confirm 'Delete 2 layers' fires onBulkDelete" — enter confirmation; click the destructive confirm button; assert `onBulkDelete` mock called once with selectedIds.
   - Test 15: "Confirmation Escape does NOT bubble to clear selection" — same as Test 13; assert that the parent's selection-clear handler (passed as `onClearSelection`) was NOT called (Escape inside confirmation only exits confirmation).

6. **Describe block: `BulkActionBar — bulk handler invocations (POL-09 wiring)`.**
   - Test 16: "Clicking Visibility fires onBulkVisibility(selectedIds)" — assert mock called once with the Set.
   - Test 17: "Changing the Opacity slider fires onBulkOpacity(selectedIds, value/100)" — fire `onValueChange` on the Slider; assert mock called with the selectedIds and the new opacity (0..1).
   - Test 18: "Clicking Group fires onBulkGroup(selectedIds)" — when canGroup is true.
   - Test 19: "Clicking Ungroup fires onBulkUngroup(selectedIds)" — when canUngroup is true.

7. **No DnD dependency.** BulkActionBar must not depend on DndContext for testing — confirms PATTERNS.md "BulkActionBar must NOT interact with useDndContext" rule.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npx vitest run src/components/builder/__tests__/BulkActionBar.test.tsx 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `frontend/src/components/builder/__tests__/BulkActionBar.test.tsx`.
    - File contains at least 4 describe blocks covering: render condition, disable rules, confirmation state machine, handler invocations.
    - File contains at least 15 distinct `it(...)` test cases.
    - All tests pass.
    - 0 unhandled worker errors.
    - File does not import or use DndContext.
  </acceptance_criteria>
  <done>
    - All acceptance criteria pass.
    - POL-08 has comprehensive vitest coverage.
    - The confirmation state machine + autoFocus on Cancel are pinned by tests so future refactor cannot regress AUD-09.
  </done>
</task>

<task type="auto">
  <name>Task 3: Bulk op handler tests with delete-rollback (use-builder-layers.bulk-ops.test.ts)</name>
  <files>frontend/src/components/builder/hooks/__tests__/use-builder-layers.bulk-ops.test.ts</files>
  <read_first>
    - frontend/src/components/builder/hooks/__tests__/use-builder-layers.add-dataset.test.ts (existing — KNOWN to have worker-timeout bug per POL-20; copy the renderHook + mock-mutation pattern but be careful not to inherit the bug — see PATTERNS.md anti-pattern note about file-level vi.mock)
    - frontend/src/components/builder/hooks/use-builder-layers.ts (Plan 03 output — verify the 5 handler signatures and their internal calls)
    - frontend/src/api/maps.ts (removeLayerFromMapApi signature for mocking)
    - .planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-PATTERNS.md "Key Architecture Notes for Planner" #1 (the local-state vs API-state distinction)
    - .planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-UI-SPEC.md "4. Atomic bulk operations"
  </read_first>
  <action>
Create `frontend/src/components/builder/hooks/__tests__/use-builder-layers.bulk-ops.test.ts`. Use `renderHook` from `@testing-library/react` to test the hook directly. Implementation steps:

1. **Worker-safety guard.** Per PATTERNS.md anti-pattern: do NOT `vi.mock('@dnd-kit/core', ...)` at file scope. The known POL-20 bug in `use-builder-layers.add-dataset.test.ts` may be related to fixture cleanup leak. Mitigations:
   - Use `vi.spyOn` per-test rather than file-level vi.mock.
   - Add `afterEach(() => { vi.restoreAllMocks(); cleanup(); })` from `@testing-library/react`.
   - For the API mock: `vi.mock('@/api/maps', async () => ({ ...await vi.importActual('@/api/maps'), removeLayerFromMapApi: vi.fn() }))` — this is the SAFE way to mock a single export; document this pattern in the file header.

2. **Test fixtures.** Build a `mapData` mock that satisfies `MapResponse` shape with at least 4 layers: 3 vector loose (`a`, `b`, `c`), 1 folder-group (`g1`). Build the `mapInstanceRef` mock as `{ current: { setLayoutProperty: vi.fn(), setPaintProperty: vi.fn(), getLayer: vi.fn().mockReturnValue({ id: 'layer-x' }) } }`. Build the addLayerMutation + removeLayerMutation mocks per existing test pattern.

3. **renderHook setup.** Wrap with the QueryClientProvider (TanStack) — required because Plan 03 added `useQueryClient` to the hook. Pattern:
   ```typescript
   const queryClient = new QueryClient({ defaultOptions: { queries: { retry: false } } });
   const wrapper = ({ children }) => <QueryClientProvider client={queryClient}>{children}</QueryClientProvider>;
   const { result } = renderHook(() => useBuilderLayers(mapData, mapInstanceRef, mapId, addLayerMutation, removeLayerMutation), { wrapper });
   ```

4. **Describe block: `useBuilderLayers — handleBulkVisibility (POL-09)`.**
   - Test 1: "Toggles all selected layers to opposite of majority in one setLocalLayers call" — set up state with [a:visible, b:visible, c:hidden]; call `result.current.handleBulkVisibility(new Set(['a','b','c']))`; await next render; assert `result.current.localLayers.find(l => l.id === 'a').visible === false`, same for b and c.
   - Test 2: "Calls setLocalLayers exactly once" — spy on the underlying setState (or assert via the React DevTools render counter — alternative: assert that `result.current.localLayers` reference changed exactly once between hook invocations).
   - Test 3: "Calls map.setLayoutProperty for each selected layer's sub-layer ids" — assert the mock `setLayoutProperty` was called with appropriate sub-layer ids (e.g., 'layer-a', 'layer-a-outline', etc.).
   - Test 4: "Sets hasUnsavedChanges to true" — assert `result.current.hasUnsavedChanges === true` after the call.
   - Test 5: "Does NOT call removeLayerFromMapApi" — assert mock not called.

5. **Describe block: `useBuilderLayers — handleBulkOpacity (POL-09)`.**
   - Test 6: "Updates opacity on all selected layers in one setState" — call with opacity 0.4; assert all selected layers' opacity is 0.4.
   - Test 7: "Calls map.setPaintProperty for each selected layer" — assert mock called.

6. **Describe block: `useBuilderLayers — handleBulkGroup (POL-09)`.**
   - Test 8: "Creates one folder-group row with all selected layers as children" — call with `new Set(['a','b','c'])` (all loose vector); assert localLayers gains a new entry with `layer_type='group:folder'` and that a, b, c now have `parent_group_id` set to the new group's id.
   - Test 9: "Defense-in-depth: returns early if any selected has parent_group_id" — pre-set b.parent_group_id='g1'; call handleBulkGroup with `['a','b','c']`; assert localLayers unchanged.
   - Test 10: "Defense-in-depth: returns early if any selected is raster" — call with `['a','r1']` where r1 is raster; assert unchanged.

7. **Describe block: `useBuilderLayers — handleBulkUngroup (POL-09)`.**
   - Test 11: "Removes folder-group rows and clears parent_group_id on children" — set up state with g1 + child layers; call handleBulkUngroup(new Set(['g1'])); assert g1 is gone and children have parent_group_id=null.
   - Test 12: "Defense-in-depth: returns early if any selected is not group:folder" — call with mix; assert unchanged.

8. **Describe block: `useBuilderLayers — handleBulkDelete (POL-09)`.**
   - Test 13: "Success path: fires N parallel removeLayerFromMapApi calls" — mock `removeLayerFromMapApi` to resolve; call `handleBulkDelete(new Set(['a','b','c']))`; assert mock called 3 times with mapId + each id; assert returned promise resolves to `true`.
   - Test 14: "Success path: optimistically removes from localLayers" — assert localLayers no longer contains a, b, c after the optimistic step (use a synchronous assertion before the awaits resolve, OR assert the final state).
   - Test 15: "Success path: invalidates ['maps', mapId] query" — spy on `queryClient.invalidateQueries`; assert called with `{ queryKey: ['maps', mapId] }`.
   - Test 16: "Success path: clears expandedLayerId if it was in selection" — pre-set expandedLayerId='a'; call delete with set including 'a'; assert expandedLayerId becomes null.
   - Test 17: "Failure path: rollback on any rejection" — mock `removeLayerFromMapApi` to reject for one id, resolve for others; await `handleBulkDelete(...)`; assert localLayers ROLLED BACK to the pre-delete state (a, b, c are present again); assert returned promise resolves to `false`.
   - Test 18: "Failure path: shows single error toast" — spy on `toast.error`; assert called exactly once (not 3 times) with a string matching the i18n key result for 'errorDeleteRolledBack'.
   - Test 19: "Failure path: does NOT clear selection (caller's responsibility)" — handler returns false; selection clearing is in MapBuilderPage wrapper, tested elsewhere.

9. **Cleanup.** Add `afterEach(() => { vi.restoreAllMocks(); cleanup(); })`. If a test causes worker exit, narrow the test scope (test the handler in isolation) and document.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend && npx vitest run src/components/builder/hooks/__tests__/use-builder-layers.bulk-ops.test.ts 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `frontend/src/components/builder/hooks/__tests__/use-builder-layers.bulk-ops.test.ts`.
    - File contains at least 5 describe blocks (one per bulk handler).
    - File contains at least 17 distinct `it(...)` test cases (Tests 1-19, with possibly 1-2 collapsed).
    - All tests pass.
    - 0 unhandled worker errors / 0 'Worker exited unexpectedly' / 0 'Timeout terminating forks worker'.
    - File does NOT use file-level `vi.mock('@dnd-kit/core', ...)`; uses `vi.spyOn` or selective `vi.mock` with `vi.importActual` for `@/api/maps` only.
    - Existing failing test `use-builder-layers.add-dataset.test.ts` is NOT modified by Plan 04 (its fix is Phase 1039 / POL-20).
    - Full builder test sweep does not regress: `cd frontend && npx vitest run src/components/builder/ 2>&1 | tail -10` reports the same baseline failure count as before Plan 04 (i.e., the 5 known POL-19 failures + the POL-20 worker-timeout — Plan 04 does not fix or worsen these).
  </acceptance_criteria>
  <done>
    - All acceptance criteria pass.
    - POL-09 atomic-with-rollback contract is pinned by Test 17/18 — any future refactor that breaks rollback or introduces N error toasts will fail these tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test environment → vitest worker | Worker-timeout bug (POL-20) shows that careless `vi.mock` at file scope can cause worker exits; Plan 04 must use `vi.spyOn` or selective `vi.mock` to avoid inheriting the bug |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1041-14 | Denial of Service | Vitest worker timeout | mitigate | Use `vi.spyOn` per-test, never `vi.mock('@dnd-kit/core', ...)` at file scope. Add `afterEach cleanup`. If `bulk-ops.test.ts` triggers the symptom, narrow the test boundary (renderHook the bulk handlers in isolation rather than through full useBuilderLayers). Document any test that had to be narrowed. |
| T-1041-15 | Spoofing | Mocked removeLayerFromMapApi | accept | Test mocks are intentional; the mock returning rejected/resolved is the test surface. No production trust boundary crossed. |
</threat_model>

<verification>
End-of-plan checks:

```bash
cd /Users/ishiland/Code/geolens/frontend
npx tsc -b --noEmit                                 # 0 errors
npx vitest run src/components/builder/__tests__/UnifiedStackPanel.multi-select.test.tsx 2>&1 | tail -10
npx vitest run src/components/builder/__tests__/BulkActionBar.test.tsx 2>&1 | tail -10
npx vitest run src/components/builder/hooks/__tests__/use-builder-layers.bulk-ops.test.ts 2>&1 | tail -10
npx vitest run src/components/builder/ 2>&1 | tail -10  # baseline failures (POL-19/20) preserved; new tests green
```

Phase 1041 success criteria for the test surface:
- 3 new test files created.
- ~50 new tests total (15 + 19 + 17 = 51).
- All new tests green.
- Baseline failures (POL-19/20 — Phase 1039 territory) NOT made worse by Plan 04.
</verification>

<success_criteria>
- POL-06, POL-07, POL-08, POL-09, POL-10, POL-11 each have at least one vitest assertion that would fail if a future refactor broke that requirement.
- 3 new test files committed.
- Builder vitest sweep delta: +51 passing tests, 0 new failures, 0 new worker errors.
- Baseline POL-19/POL-20 known failures are unchanged (Phase 1039's domain).
</success_criteria>

<output>
After completion, create `.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-04-SUMMARY.md`. Document:
- Total new tests count.
- Any test that had to be narrowed due to worker-timeout symptoms (with the symptom described).
- Whether Test 15 (queryClient.invalidateQueries spy) needed any special wrapper around the QueryClientProvider — Plan 03 SUMMARY may have surfaced a wrinkle.
- The baseline failure count of `npx vitest run src/components/builder/` BEFORE Plan 04 vs AFTER — must be identical except for the +51 passing tests.
- Note for Phase 1044: this plan covers the unit/integration tier; Playwright UAT spec (`e2e/builder-v1-5.spec.ts`) for POL-24 is Phase 1044's responsibility.
</output>
