---
phase: 1041
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/builder/UnifiedStackPanel.tsx
  - frontend/src/components/builder/StackRow.tsx
  - frontend/src/components/builder/FolderGroupRow.tsx
  - frontend/src/components/builder/BasemapGroupRow.tsx
  - frontend/src/pages/MapBuilderPage.tsx
autonomous: true
requirements:
  - POL-06
  - POL-07
  - POL-10
  - POL-11
tags:
  - mapbuilder
  - selection
  - a11y
  - listbox

must_haves:
  truths:
    - "User can Cmd/Ctrl-click a stack row to toggle it into a multi-selection set without opening the LayerEditorPanel."
    - "User can Shift-click a row to range-select from the last toggle anchor to the clicked row, inclusive."
    - "User can press Space on a focused row to toggle it into the selection set."
    - "User can press Shift+ArrowUp / Shift+ArrowDown to extend selection by one row."
    - "User sees multi-selected rows render with the same primary-50 fill + 2px primary left rail as single-selection, plus a visible checkbox in the caret column."
    - "User cannot include the basemap group row or any basemap sublayer row in the multi-selection set; the basemap row shows cursor-not-allowed when Cmd-hovered during multi-selection mode."
    - "User clears the multi-selection by pressing Escape, by clicking outside the stack panel, or by route change (component unmount)."
    - "User who starts a drag while a multi-selection is active sees the multi-selection clear before drag begins."
  artifacts:
    - path: "frontend/src/components/builder/UnifiedStackPanel.tsx"
      provides: "selectedIds Set<string> state + lastToggleAnchor ref + outside-click/Escape clearing + role='listbox' aria-multiselectable='true' + isMultiSelectionActive prop threading"
      contains: "selectedIds"
    - path: "frontend/src/components/builder/StackRow.tsx"
      provides: "isMultiSelected/isMultiSelectionActive/onCmdClick/onShiftClick/onCheckboxClick props + Checkbox swap in caret column + Cmd/Shift handler in handleRowClick + Space toggle in onKeyDown"
      contains: "isMultiSelected"
    - path: "frontend/src/components/builder/FolderGroupRow.tsx"
      provides: "isMultiSelected/isMultiSelectionActive/onCmdClick/onCheckboxClick props + Checkbox swap in caret column when multi-selection active"
      contains: "isMultiSelected"
    - path: "frontend/src/components/builder/BasemapGroupRow.tsx"
      provides: "isMultiSelectionActive prop + cursor-not-allowed visual when multi-selection active (no-op handlers — boundary refusal)"
      contains: "cursor-not-allowed"
    - path: "frontend/src/pages/MapBuilderPage.tsx"
      provides: "handleDragStart clears UnifiedStackPanel selection via callback prop OR sets a flag the panel observes; integration shipped via existing onDragStart wiring"
      contains: "handleDragStart"
  key_links:
    - from: "frontend/src/components/builder/UnifiedStackPanel.tsx"
      to: "frontend/src/components/builder/StackRow.tsx"
      via: "isMultiSelected, isMultiSelectionActive, onCmdClick, onShiftClick, onCheckboxClick props threaded through SortableStackRow"
      pattern: "isMultiSelected"
    - from: "frontend/src/components/builder/UnifiedStackPanel.tsx"
      to: "document mousedown event"
      via: "useEffect outside-click listener guarded by stackPanelRef.contains"
      pattern: "addEventListener.*mousedown"
    - from: "frontend/src/pages/MapBuilderPage.tsx"
      to: "frontend/src/components/builder/UnifiedStackPanel.tsx"
      via: "onDragStartClearSelection callback prop forwarded into handleDragStart"
      pattern: "onDragStartClearSelection|setSelectedIds"
---

<objective>
Land the selection model and visual state for multi-row selection in the unified layer stack: `selectedIds: Set<string>` in `UnifiedStackPanel`, modifier-aware click + keyboard handlers in `StackRow` / `FolderGroupRow`, basemap boundary refusal in `BasemapGroupRow`, and drag-start mutual exclusion in `MapBuilderPage`. Closes POL-06 (modifier + keyboard selection), POL-07 (visual state with checkbox + tint + aria), POL-10 (Escape / outside-click / route-change clearing), POL-11 (basemap-group boundary).

Purpose: This is the foundation for Phase 1041. Plan 02 (BulkActionBar) and Plan 03 (bulk handlers) consume the `selectedIds` state and the `isMultiSelectionActive` prop landed here. No bulk action surface, no handlers, no tests in this plan — strictly the selection model + visual + ARIA + clearing rules.

Output: A `UnifiedStackPanel` that tracks `selectedIds` with mouse + keyboard, renders multi-selected rows with the locked v1008 `.row.selected` visual + a Checkbox in the caret column, refuses basemap-boundary inclusion silently, clears on Escape / outside-click / unmount / drag-start, and exposes `aria-multiselectable="true"` on its listbox role.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-CONTEXT.md
@.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-PATTERNS.md
@.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-UI-SPEC.md
@.planning/phases/1040-drag-from-catalog-into-stack/1040-04-SUMMARY.md
@.claude/skills/sketch-findings-geolens/SKILL.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase 2026-05-14. -->

From frontend/src/components/builder/StackRow.tsx (existing — to extend):
```typescript
interface StackRowProps {
  layer: GroupedLayer;
  selected: boolean;          // single-selection flyout focus — keep semantics
  onSelectLayer: (id: string) => void;
  onToggleVisibility: (id: string) => void;
  onOpacityChange: (id: string, opacity: number) => void;
  onRename: (id: string, name: string) => void;
  onDelete: (id: string) => void;
  // ... existing drag/sortable props ...
}
```

Existing single-selection visual at StackRow.tsx:152-154 (DO NOT MODIFY — multi-selection reuses these classes):
```typescript
selected && 'bg-[var(--primary-50,theme(colors.accent.DEFAULT))] shadow-[inset_2px_0_0_var(--primary)]',
```

Existing aria-selected at StackRow.tsx:146 (DO MODIFY — combine with multi-selection):
```typescript
aria-selected={selected}   // ← becomes: selected || isMultiSelected
```

From frontend/src/components/builder/UnifiedStackPanel.tsx (existing — to extend):
- Existing `aria-multiselectable="false"` at ~line 725 → flip to `"true"` and add `aria-label`
- Existing layer rendering loop at ~lines 800-819 maps layers to `<SortableStackRow>` — thread new selection props here
- `useDndContext()` already called at top of component for DragOverlay branching (Phase 1040)
- No selection state today — `selectedLayerId` (single-selection) lives in `use-builder-layers.ts` and is passed in as prop

From frontend/src/components/builder/BasemapGroupRow.tsx (existing — to extend):
- Existing `visibilityDisabled` cursor pattern at lines 31-35, 132-146 — reuse `cursor-not-allowed` form for multi-selection refusal

From frontend/src/components/builder/FolderGroupRow.tsx (existing — to extend):
- Caret button at lines 132-148 — wrap in `isMultiSelectionActive ? <Checkbox /> : <button>▸</button>` swap

From frontend/src/components/ui/checkbox.tsx (already installed — Radix-based shadcn/new-york):
```typescript
function Checkbox(props: React.ComponentProps<typeof CheckboxPrimitive.Root>): JSX.Element;
// Use: <Checkbox className="h-3.5 w-3.5" checked={...} aria-checked={...} aria-label={...}
//        onClick={(e) => e.stopPropagation()} onPointerDown={(e) => e.stopPropagation()} />
```

From frontend/src/components/map/BasemapToggle.tsx (analog for outside-click — lines 24-31):
```typescript
useEffect(() => {
  if (!open) return;
  function handleClick(e: MouseEvent) {
    if (ref.current && !ref.current.contains(e.target as Node)) setOpen(false);
  }
  document.addEventListener('mousedown', handleClick);
  return () => document.removeEventListener('mousedown', handleClick);
}, [open]);
```

From frontend/src/pages/MapBuilderPage.tsx (existing handleDragStart at line 427):
```typescript
const handleDragStart = useCallback((event: DragStartEvent) => {
  setDragActiveId(String(event.active.id));
  handleSelectLayer(null);   // ← already clears single-selection; add: clear selectedIds
  document.documentElement.classList.add('dragging-active');
  lastOverIdRef.current = null;
  // ...announce(...)...
}, [handleSelectLayer, announce, t]);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Selection state, outside-click, Escape, drag-start clearing in UnifiedStackPanel</name>
  <files>frontend/src/components/builder/UnifiedStackPanel.tsx, frontend/src/pages/MapBuilderPage.tsx</files>
  <read_first>
    - frontend/src/components/builder/UnifiedStackPanel.tsx (entire file — 867 lines; identify existing aria-multiselectable site, layer-mapping loop, useDndContext call, the closing div of the scrollable list)
    - frontend/src/components/map/BasemapToggle.tsx lines 1-50 (outside-click pattern analog)
    - frontend/src/pages/MapBuilderPage.tsx lines 420-510 (handleDragStart, handleDragEnd, handleDragCancel — wiring sites for selection clearing)
    - .planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-PATTERNS.md (selection state + outside-click block; "DnD mutual exclusion" architecture note #3)
    - .planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-UI-SPEC.md sections 1, 6, 7, 8 (selection model, clearing rules, ARIA, keyboard path)
  </read_first>
  <behavior>
    - Test 1: Cmd-click on a row id 'a' adds 'a' to selectedIds; Cmd-click again removes it.
    - Test 2: Click on row id 'b' after Cmd-selecting 'a' clears selectedIds AND sets selectedLayerId='b' (single-selection focus path unchanged).
    - Test 3: Shift-click on row 'c' after Cmd-selecting 'a' adds the contiguous range from 'a' to 'c' to selectedIds (uses lastToggleAnchor='a').
    - Test 4: Pressing Escape on a focused row with selectedIds.size >= 1 clears selectedIds; selectedLayerId is unchanged.
    - Test 4b: Pressing Shift+ArrowDown with selectedIds={'a'} and DOM focus on row 'a' extends selectedIds to {'a','b'} (the next selectable row id) AND moves DOM focus to row 'b'. Shift+ArrowUp at the top of the list is a no-op (clamped). The basemap group + sublayer ids are excluded from the `selectableRowIds` traversal.
    - Test 5: A document mousedown outside the stack panel ref clears selectedIds; a mousedown inside (on a row) does not (the row's own onClick handles selection).
    - Test 6: Calling handleDragStart in MapBuilderPage clears UnifiedStackPanel selectedIds (mutual exclusion — drag and multi-select do not compose).
    - Test 7: Component unmount cleanup runs — selectedIds is reset to empty Set on next mount (handles route change naturally).
    - Test 8: aria-multiselectable on the listbox is the literal string 'true' after the change.
    - (Tests written in Plan 04. This task ships the implementation; the behavior list above defines the contract Plan 04 will assert.)
  </behavior>
  <action>
Add multi-selection state and clearing rules to `UnifiedStackPanel` (per UI-SPEC §1, §6, §7, §8 and PATTERNS.md "Selection state + outside-click pattern"). Implementation steps:

1. Inside `UnifiedStackPanel` component body, after the existing `useDndContext()` call, add: `const [selectedIds, setSelectedIds] = useState<Set<string>>(new Set());`, `const lastToggleAnchor = useRef<string | null>(null);`, and `const stackPanelRef = useRef<HTMLDivElement>(null);`. Attach `stackPanelRef` to the existing root scrollable container that holds the layer list (the same element that owns `role="listbox"`).

2. Derive `const isMultiSelectionActive = selectedIds.size > 0;` once per render. This boolean is threaded down to `SortableStackRow`, `FolderGroupRowWrapper`, and `BasemapGroupRow` so they conditionally show the checkbox column and the cursor-not-allowed cue.

3. Add three handler callbacks (memoized with `useCallback`):
   - `handleCmdClick(id)` — toggle id in selectedIds (immutable set update via `new Set(prev); next.has(id) ? next.delete(id) : next.add(id)`); set `lastToggleAnchor.current = id`.
   - `handleShiftClick(id)` — compute the index of `lastToggleAnchor.current` and `id` within the SAME ordered list the renderer uses (use `sortableIds`-equivalent: the flattened ordered ID array of currently rendered, selectable rows, EXCLUDING basemap group + sublayer IDs); add the inclusive range `[min, max]` to selectedIds. If lastToggleAnchor is null, fall back to single-toggle behavior on `id` only.
   - `handleCheckboxClick(id)` — alias to `handleCmdClick(id)` (semantically identical).

4. Add boundary guard inside the handlers: before any add, check `isBasemapBoundaryId(id)` — a helper that returns `true` if `id === basemapGroup?.id` OR `id` is in `basemapGroup?.sublayers?.map(s => s.id)`. If true, return early without modifying state. This is per POL-11 / UI-SPEC §1 boundary enforcement (silent — no toast).

5. Outside-click clearing (POL-10): after the state declarations, add a `useEffect` keyed on `[selectedIds.size]`. When `selectedIds.size === 0` return early. Otherwise attach `document.addEventListener('mousedown', ...)` that checks `if (stackPanelRef.current?.contains(e.target as Node)) return;` then calls `setSelectedIds(new Set()); lastToggleAnchor.current = null;`. Cleanup: removeEventListener. Pattern source: `BasemapToggle.tsx` lines 24-31.

6. Escape clearing (POL-10): in the same `useEffect` (or a sibling), attach `document.addEventListener('keydown', ...)` that fires `setSelectedIds(new Set())` when `e.key === 'Escape'` AND `selectedIds.size > 0`. Do not touch `selectedLayerId` — Escape is selection-only. Cleanup paired.

6b. **Shift+ArrowUp/Down keyboard extension (POL-06).** In the SAME keydown listener as Escape (or a sibling listener attached to `stackPanelRef.current` rather than `document`, to scope it to the stack panel), detect `e.shiftKey && (e.key === 'ArrowUp' || e.key === 'ArrowDown')`. When matched: read `document.activeElement` to find the currently focused row's id (rows expose their layer/group id via a `data-row-id={id}` attribute on the role="option" element — add this attribute to `SortableStackRow`, `FolderGroupRowWrapper`, and the folder-group child rows in step 11/12). Build the ordered `selectableRowIds` array (the same flattened list used by `handleShiftClick` in step 3 — exclude basemap group + sublayers). Find the focused id's index; compute the adjacent index (-1 for ArrowUp, +1 for ArrowDown); clamp to `[0, length-1]`. If the adjacent id exists, call `handleShiftClick(adjacentId)`, then move DOM focus to that row's element via `panelRef.current?.querySelector('[data-row-id="${adjacentId}"]')?.focus()`. Call `e.preventDefault()` to suppress page scroll. If there is no focused row in the panel (activeElement is null or outside), no-op.

7. Listbox ARIA (POL-23 foundation, UI-SPEC §7): change the existing `aria-multiselectable="false"` on the listbox role element to `aria-multiselectable="true"` and add `aria-label={t('unifiedStack.listboxLabel', { defaultValue: 'Map layers' })}`. Use the i18n `t` already imported in this component.

8. Drag-start mutual exclusion (POL-10, UI-SPEC §6): expose a clearing function via a new prop `onSelectionShouldClear?: () => void` on UnifiedStackPanel — but simpler: use a callback ref pattern. Add a NEW prop `onDragStartClearSelectionRef?: React.MutableRefObject<(() => void) | null>` that the parent sets so it can call `clearSelection()` from `MapBuilderPage.handleDragStart`. Simpler alternative: lift `selectedIds` to `MapBuilderPage` (matches the Phase 1040 lift-DndContext pattern). Choose the simpler alternative — lift `selectedIds`, `setSelectedIds`, `lastToggleAnchor` ref, and the three handlers into `MapBuilderPage`, then thread them down as props to `UnifiedStackPanel`. This matches the Phase 1040 architecture (DndContext lifted) and lets `handleDragStart` directly call `setSelectedIds(new Set())`. Document this choice in the task summary.

9. In `MapBuilderPage.handleDragStart` (line 427), add `setSelectedIds(new Set()); lastToggleAnchor.current = null;` immediately after `handleSelectLayer(null);`. Add `setSelectedIds` and `lastToggleAnchor` to the `useCallback` deps. This is the single line per `additional_constraints` "Drag + selection mutual exclusion" rule.

10. Thread `selectedIds`, `isMultiSelectionActive`, `handleCmdClick`, `handleShiftClick`, `handleCheckboxClick` into the `<UnifiedStackPanel>` JSX in `MapBuilderPage`.

11. Inside `UnifiedStackPanel`, in the layer-mapping loop (around lines 800-819), pass new props to `SortableStackRow`: `isMultiSelected={selectedIds.has(layer.id)}`, `isMultiSelectionActive={isMultiSelectionActive}`, `onCmdClick={handleCmdClick}`, `onShiftClick={handleShiftClick}`, `onCheckboxClick={handleCheckboxClick}`. Also pass them through `SortableStackRow` to the inner `StackRow`.

12. For the existing `FolderGroupRowWrapper` and child layers inside folder groups: thread the same five props.

13. For `BasemapGroupRow`: pass only `isMultiSelectionActive` (boundary refusal — no add handlers; the row's own onClick falls through to single-selection path which does NOT add to selectedIds because the boundary guard in step 4 rejects it).

14. Route-change clearing (POL-10): rely on natural unmount — `UnifiedStackPanel` is keyed by `mapId` in `MapBuilderPage`, so route change unmounts the component and `selectedIds` initializes empty on remount. No explicit `useLocation` watcher needed. Document this design choice in the summary so reviewers know it is intentional.

Per D-01 (Hard Constraints): use ONLY existing `sketch-findings-geolens` tokens — no new design tokens. The selection visual is shipped in Task 2 below; this task is state + handlers + ARIA only.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend &amp;&amp; npx tsc -b --noEmit 2>&amp;1 | tail -10 &amp;&amp; grep -c "selectedIds" /Users/ishiland/Code/geolens/frontend/src/pages/MapBuilderPage.tsx &amp;&amp; grep -c "isMultiSelectionActive" /Users/ishiland/Code/geolens/frontend/src/components/builder/UnifiedStackPanel.tsx &amp;&amp; grep -c 'aria-multiselectable="true"' /Users/ishiland/Code/geolens/frontend/src/components/builder/UnifiedStackPanel.tsx &amp;&amp; grep -c "isBasemapBoundaryId\|basemapGroup?.sublayers\|basemapBoundary" /Users/ishiland/Code/geolens/frontend/src/components/builder/UnifiedStackPanel.tsx /Users/ishiland/Code/geolens/frontend/src/pages/MapBuilderPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tsc compiles 0 errors.
    - `setSelectedIds` called inside `handleDragStart` in `MapBuilderPage.tsx` (grep returns ≥1 match for `setSelectedIds(new Set())` within ±20 lines of `handleDragStart`).
    - `aria-multiselectable="true"` literal string present on the listbox element in `UnifiedStackPanel.tsx` (exactly 1 match — the old "false" must be gone).
    - `aria-label` on the listbox element references the i18n key `unifiedStack.listboxLabel` (or equivalent).
    - Outside-click `useEffect` present: `addEventListener('mousedown'` AND a paired `removeEventListener('mousedown'` in the cleanup return.
    - Escape `keydown` listener present: an `if (e.key === 'Escape' && selectedIds.size > 0)` branch that calls `setSelectedIds(new Set())`.
    - Shift+ArrowUp/Down keydown listener present in `UnifiedStackPanel.tsx`: a branch matching `e.shiftKey && (e.key === 'ArrowUp' || e.key === 'ArrowDown')` that calls `handleShiftClick` with the adjacent id and calls `e.preventDefault()` (grep returns ≥1 match for `'ArrowDown'` AND ≥1 for `'ArrowUp'` AND ≥1 for `data-row-id` in `UnifiedStackPanel.tsx`).
    - Each row component (`StackRow`, `FolderGroupRow`, folder-group child renderer) has a `data-row-id={layer.id}` (or equivalent) on its outer role="option" element so the keyboard handler can derive the focused row's id.
    - `selectedIds`, `isMultiSelectionActive`, `handleCmdClick`, `handleShiftClick`, `handleCheckboxClick` exist in either `MapBuilderPage.tsx` or are passed as props on `<UnifiedStackPanel ...>` (decision per step 8 — lift to MapBuilderPage).
    - `BasemapGroupRow` receives `isMultiSelectionActive` prop in JSX (`<BasemapGroupRow ... isMultiSelectionActive={...}>`).
    - `FolderGroupRowWrapper` (or equivalent) receives all 5 selection props.
    - Existing `aria-multiselectable="false"` literal is GONE (`grep -c '"false"' src/components/builder/UnifiedStackPanel.tsx | grep -c aria-multiselectable` returns 0 for that combination).
  </acceptance_criteria>
  <done>
    - All acceptance criteria pass.
    - Existing builder vitest suite still green: `cd frontend && npx vitest run src/components/builder/UnifiedStackPanel.test.tsx src/components/builder/__tests__/ src/pages/__tests__/MapBuilderPage.header-actions.test.tsx 2>&1 | tail -20` reports 0 failures (Plan 04 adds the new selection-specific tests).
    - No new design tokens introduced; `git diff frontend/src/index.css` returns empty for this task.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Checkbox + Cmd/Shift/Space handlers + multi-selection visual in row components</name>
  <files>frontend/src/components/builder/StackRow.tsx, frontend/src/components/builder/FolderGroupRow.tsx, frontend/src/components/builder/BasemapGroupRow.tsx</files>
  <read_first>
    - frontend/src/components/builder/StackRow.tsx (entire file — 392 lines; identify StackRowProps interface line 26, handleRowClick line 137, onKeyDown lines 157-162, the existing caret span at lines 164-171, aria-selected at line 146, the selected visual at lines 152-154)
    - frontend/src/components/builder/FolderGroupRow.tsx (entire file — caret button at lines 132-148; FolderGroupRowProps interface)
    - frontend/src/components/builder/BasemapGroupRow.tsx (entire file — visibilityDisabled pattern at lines 31-35, 132-146; the row's outer className at lines 75-80)
    - frontend/src/components/ui/checkbox.tsx (Radix wrapper — confirm prop shape: `checked`, `onCheckedChange` vs `onClick`, `aria-checked`)
    - .planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-PATTERNS.md (StackRow modify section + FolderGroupRow modify section + BasemapGroupRow modify section)
    - .planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-UI-SPEC.md sections 1, 2 (selection model + selection state visual)
  </read_first>
  <behavior>
    - Test 1: A row with `selected=true` AND `isMultiSelected=false` renders `bg-[var(--primary-50,...)]` + `shadow-[inset_2px_0_0_var(--primary)]` and NO checkbox.
    - Test 2: A row with `selected=false` AND `isMultiSelected=true` AND `isMultiSelectionActive=true` renders the SAME visual classes (primary-50 + inset rail) PLUS a Checkbox in the caret column with `checked=true` and `aria-checked='true'`.
    - Test 3: A row with `isMultiSelectionActive=true` AND `isMultiSelected=false` renders an unchecked Checkbox in the caret column.
    - Test 4: A row with `isMultiSelectionActive=false` renders the existing hidden caret span (no checkbox), regardless of `isMultiSelected`.
    - Test 5: Cmd-click on the row body fires `onCmdClick(layer.id)` and does NOT fire `onSelectLayer(layer.id)`.
    - Test 6: Shift-click on the row body fires `onShiftClick(layer.id)` and does NOT fire `onSelectLayer(layer.id)`.
    - Test 7: Plain click on the row body fires `onSelectLayer(layer.id)` (existing behavior).
    - Test 8: Space keydown on the focused row fires `onCmdClick(layer.id)` and calls `e.preventDefault()` (so it does not scroll).
    - Test 9: aria-selected on the row element is `true` when `selected || isMultiSelected` is true.
    - Test 10: BasemapGroupRow with `isMultiSelectionActive=true` renders `cursor-not-allowed` in its className.
    - Test 11: BasemapGroupRow has NO onCmdClick / onShiftClick / onCheckboxClick handlers (they are absent from the prop interface — boundary refusal lives in UnifiedStackPanel via the basemap boundary guard).
    - Test 12: FolderGroupRow with `isMultiSelectionActive=true` shows a Checkbox in place of the caret button; with `false` shows the caret button.
    - (Tests written in Plan 04. This task ships implementation; behavior list defines Plan 04's assertions.)
  </behavior>
  <action>
Modify `StackRow`, `FolderGroupRow`, `BasemapGroupRow` per UI-SPEC §2 and PATTERNS.md "row" sections. Implementation steps:

1. **StackRow.tsx — Props.** Extend `StackRowProps` interface (line 26 area) with the 5 new optional props from PATTERNS.md "New props to add":
   - `isMultiSelected?: boolean`
   - `isMultiSelectionActive?: boolean`
   - `onCmdClick?: (id: string) => void`
   - `onShiftClick?: (id: string) => void`
   - `onCheckboxClick?: (id: string) => void`

2. **StackRow.tsx — Imports.** Add `import { Checkbox } from '@/components/ui/checkbox';` to the top of the file (the file path is correct — confirmed via `frontend/src/components/ui/checkbox.tsx` exists).

3. **StackRow.tsx — handleRowClick.** Replace the existing handler at line 137 with the modifier-aware version from PATTERNS.md: detect `e.metaKey || e.ctrlKey` first (preventDefault, call `onCmdClick?.(layer.id)`, return), then `e.shiftKey` (preventDefault, call `onShiftClick?.(layer.id)`, return), then fall through to the existing `onSelectLayer(layer.id)`. Do NOT modify any other behavior of the handler.

4. **StackRow.tsx — onKeyDown.** Replace the existing handler at lines 157-162 with the version from PATTERNS.md: keep existing `Enter` → `onSelectLayer`; add `Space` → `preventDefault + onCmdClick?.(layer.id)`. Do NOT add Shift+ArrowUp/Down handlers in this row component — those are stack-panel-level concerns (the parent's keydown listener at the listbox level handles row-to-row arrow navigation; deferred to Plan 04 if needed for the keyboard-only path test).

5. **StackRow.tsx — Caret column swap.** Replace the hidden caret span at lines 164-171 with the conditional from PATTERNS.md: `{isMultiSelectionActive ? <Checkbox ... /> : <span aria-hidden="true" style={{ visibility: 'hidden' }} className="text-xs text-muted-foreground">▸</span>}`. Checkbox attributes per PATTERNS.md: `className="h-3.5 w-3.5"`, `checked={isMultiSelected ?? false}`, `aria-checked={isMultiSelected ?? false}`, `aria-label={t('bulkActions.selectRow', { name: displayName, defaultValue: 'Select {{name}}' })}`. Use the existing `t` from `useTranslation('builder')` already in the file. The `onCheckedChange` callback (Radix Checkbox uses this, not `onClick`) calls `onCheckboxClick?.(layer.id)`. Add `onPointerDown={(e) => e.stopPropagation()}` and `onClick={(e) => e.stopPropagation()}` to prevent the row drag handler and the row click handler from firing when the checkbox is interacted with.

6. **StackRow.tsx — aria-selected.** Update line 146: change `aria-selected={selected}` to `aria-selected={selected || (isMultiSelected ?? false)}`.

7. **StackRow.tsx — Visual classes.** No changes — the existing `selected && 'bg-[var(--primary-50,...)] shadow-[inset_2px_0_0_var(--primary)]'` at lines 152-154 must ALSO trigger when `isMultiSelected` is true. Either: (a) compute `const visuallySelected = selected || (isMultiSelected ?? false);` once and use `visuallySelected &&` in the className; or (b) add a sibling `(isMultiSelected ?? false) && 'bg-[var(--primary-50,...)] shadow-[inset_2px_0_0_var(--primary)]'` line. Choose (a) — single computed boolean, no duplicated string.

8. **FolderGroupRow.tsx — Props.** Extend `FolderGroupRowProps` with: `isMultiSelected?: boolean`, `isMultiSelectionActive?: boolean`, `onCmdClick?: (id: string) => void`, `onCheckboxClick?: (id: string) => void`. (No `onShiftClick` — folder groups have no caret-column shift semantics in this phase; the row body click is shift-handled at the parent's `handleShiftClick` via the same pathway as StackRow.)

9. **FolderGroupRow.tsx — Imports.** Add `import { Checkbox } from '@/components/ui/checkbox';`.

10. **FolderGroupRow.tsx — Caret/Checkbox swap.** Wrap the existing caret button at lines 132-148 in `{isMultiSelectionActive ? <Checkbox ... /> : <button>...existing caret button...</button>}`. Checkbox attributes mirror StackRow Task step 5: `h-3.5 w-3.5`, `checked={isMultiSelected ?? false}`, `aria-checked`, `aria-label={t('bulkActions.selectGroup', { name: groupName, defaultValue: 'Select {{name}}' })}`, `onCheckedChange` → `onCheckboxClick?.(groupId)`, `onPointerDown/onClick stopPropagation`.

11. **FolderGroupRow.tsx — aria-selected + visual.** If FolderGroupRow has an `aria-selected` attr today, update it to also include `isMultiSelected`. If no aria-selected exists today, add `aria-selected={(isMultiSelected ?? false) || undefined}` on the row element. Apply the same primary-50 + inset rail visual when `isMultiSelected` is true (mirror StackRow Task step 7).

12. **FolderGroupRow.tsx — handleRowClick / onKeyDown.** Mirror StackRow Task steps 3-4: add Cmd-click → `onCmdClick`, Shift-click → `onShiftClick` (NOTE: re-add `onShiftClick` to the interface from step 8 if needed — the parent threads it through), Space → `onCmdClick`. The folder-group "click row body to select group" semantics today (if any) become the fall-through plain-click behavior.

13. **BasemapGroupRow.tsx — Props.** Add ONE new optional prop: `isMultiSelectionActive?: boolean`. Do NOT add `onCmdClick`, `onShiftClick`, or `onCheckboxClick` — the boundary refusal lives in `UnifiedStackPanel.handleCmdClick` (Task 1 step 4). The basemap row is silent at the row level.

14. **BasemapGroupRow.tsx — cursor-not-allowed.** In the row's outer `className` (lines 75-80), append: `(isMultiSelectionActive ?? false) && 'cursor-not-allowed'`. Wrap with the existing `cn()` helper.

15. **BasemapGroupRow.tsx — Caret swap.** Do NOT swap the basemap caret for a checkbox. The basemap row keeps its `⊞` glyph at all times — basemap is non-selectable and the visual must NOT suggest it can be selected. PATTERNS.md confirms: BasemapGroupRow does NOT receive checkbox swap.

16. **i18n key dependency.** This task references `t('bulkActions.selectRow', ...)` and `t('bulkActions.selectGroup', ...)`. Plan 02 lands the `bulkActions` namespace in `en/builder.json`. Until Plan 02 ships, the `defaultValue` fallback ('Select {{name}}') keeps the UI functional and the translation key noted for Plan 02 to add.

Per D-01 hard constraint: ONLY existing tokens. No new color, spacing, motion tokens.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/geolens/frontend &amp;&amp; npx tsc -b --noEmit 2>&amp;1 | tail -10 &amp;&amp; grep -c "isMultiSelected" /Users/ishiland/Code/geolens/frontend/src/components/builder/StackRow.tsx &amp;&amp; grep -c "isMultiSelected" /Users/ishiland/Code/geolens/frontend/src/components/builder/FolderGroupRow.tsx &amp;&amp; grep -c "isMultiSelectionActive" /Users/ishiland/Code/geolens/frontend/src/components/builder/BasemapGroupRow.tsx &amp;&amp; grep -c "cursor-not-allowed" /Users/ishiland/Code/geolens/frontend/src/components/builder/BasemapGroupRow.tsx &amp;&amp; grep -c "@/components/ui/checkbox" /Users/ishiland/Code/geolens/frontend/src/components/builder/StackRow.tsx /Users/ishiland/Code/geolens/frontend/src/components/builder/FolderGroupRow.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tsc 0 errors.
    - `StackRowProps` interface contains all 5 new optional props (grep `"isMultiSelected\?:"`, `"onCmdClick\?:"`, `"onShiftClick\?:"`, `"onCheckboxClick\?:"`, `"isMultiSelectionActive\?:"` each ≥1 in `StackRow.tsx`).
    - `FolderGroupRowProps` contains the 4 new props (omits `onShiftClick` only if step 12 doesn't need it — re-include if step 12 added the body-shift-click; preferred to include for symmetry).
    - `BasemapGroupRowProps` contains `isMultiSelectionActive?: boolean` AND does NOT contain `onCmdClick` / `onCheckboxClick` (grep returns 0 for those in BasemapGroupRow.tsx).
    - `import { Checkbox } from '@/components/ui/checkbox'` present in StackRow.tsx and FolderGroupRow.tsx (grep returns ≥1 each).
    - `Checkbox` JSX element present in StackRow.tsx with `checked={isMultiSelected` substring AND `aria-checked` attribute AND `onCheckedChange` callback.
    - `cursor-not-allowed` appears in BasemapGroupRow.tsx outer className (≥1 match).
    - `aria-selected={selected || (isMultiSelected` (or equivalent boolean OR) present in StackRow.tsx (line 146 area).
    - `metaKey || ctrlKey` branch present in StackRow.tsx handleRowClick.
    - `e.key === ' '` (Space) branch present in StackRow.tsx onKeyDown.
    - Existing `selected && 'bg-[var(--primary-50` visual class still in StackRow.tsx (single-selection visual unchanged).
    - The phase 1040 vitest suite still green: `cd frontend && npx vitest run src/components/builder/__tests__/UnifiedStackPanel.test.tsx src/components/builder/__tests__/DatasetSearchPanel.dragdrop.test.tsx 2>&1 | tail -10` reports 0 failures.
  </acceptance_criteria>
  <done>
    - All acceptance criteria pass.
    - All three row components compile and render without runtime errors when `isMultiSelectionActive=false` (existing single-selection behavior preserved exactly).
    - When the executor manually verifies in Storybook OR by spinning up the app: a row with `isMultiSelectionActive=true` and `isMultiSelected=true` renders the primary-50 fill + inset rail + a checked Radix checkbox in the caret column. (Manual visual verification optional — Playwright verification is Phase 1044's responsibility per UI-SPEC.)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Document → Component | Document-level mousedown / keydown listeners reach into React component state via setSelectedIds — must not leak across UnifiedStackPanel unmount |
| User input → Selection state | Modifier-key clicks and keyboard events drive selection state mutations; basemap boundary must hold even under crafted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1041-01 | Tampering | UnifiedStackPanel.handleCmdClick / handleShiftClick | mitigate | Boundary guard `isBasemapBoundaryId(id)` runs BEFORE any setSelectedIds call. Even if the row prop wiring incorrectly passes a basemap ID, the handler refuses it. Test in Plan 04 must cover: simulating a Cmd-click on a basemap row id results in selectedIds remaining empty. |
| T-1041-02 | Denial of Service | document-level event listeners | mitigate | Both `mousedown` and `keydown` listeners are mounted only when `selectedIds.size > 0` (the useEffect early-returns when size===0) and tear down via cleanup. Component unmount triggers cleanup automatically. Verified by acceptance criterion: cleanup `removeEventListener` paired with mount. |
| T-1041-03 | Information Disclosure | aria-selected + aria-multiselectable | accept | ARIA state is the intended disclosure surface for screen readers — disclosing selection state is the feature, not a leak. No PII in row IDs (UUIDs / kebab slugs only). |
| T-1041-04 | Repudiation | selection state changes | accept | Selection is ephemeral UI state; no audit trail required. The persistent action (bulk delete) is logged by the existing per-layer DELETE endpoint's audit trail (no new audit surface in this plan). |
</threat_model>

<verification>
End-of-plan checks:

```bash
cd /Users/ishiland/Code/geolens/frontend
npx tsc -b --noEmit                # 0 errors
npx vitest run src/components/builder/ 2>&1 | tail -20    # 0 NEW failures (existing failures from POL-19/20/21 are Phase 1039 scope and remain status quo until Plan 04 + Phase 1044 close)
npm run build 2>&1 | tail -10      # 0 errors
```

Manual smoke (optional, not required for this plan):
1. Open Map Builder with a saved map containing 3+ overlay layers + basemap group.
2. Cmd-click two overlay rows — both gain primary-50 tint + visible checkbox.
3. Cmd-click basemap row — no visual change (silent refusal).
4. Press Escape — selection clears.
5. Cmd-click an overlay row, then start dragging another row — selection clears at drag-start.
</verification>

<success_criteria>
- All POL-06 mouse interactions wired (Cmd-click toggle, Shift-click range, plain click clears).
- All POL-06 keyboard interactions partially wired (Space toggles; Shift+Arrow extension may land in Plan 04 keyboard-nav layer).
- POL-07 visual state shipped (primary-50 + inset rail + checkbox in caret column; aria-selected reflects multi-state).
- POL-10 clearing rules wired (Escape, outside-click, drag-start, unmount).
- POL-11 basemap boundary enforced (silent refusal in handlers; cursor-not-allowed visual on basemap row).
- ARIA: `aria-multiselectable="true"` + `aria-label` on listbox; `aria-checked` on checkbox; `aria-selected` includes multi-state.
- No new design tokens introduced.
- No new backend endpoints called.
- All four hard constraints from CONTEXT.md respected.
</success_criteria>

<output>
After completion, create `.planning/phases/1041-multi-layer-selection-and-bulk-ops/1041-01-SUMMARY.md` per the SUMMARY template. Document:
- Decision: lifted `selectedIds` state to `MapBuilderPage` (vs keeping in UnifiedStackPanel) — rationale per Task 1 step 8.
- Decision: relied on natural unmount for route-change clearing instead of `useLocation` watcher — rationale per Task 1 step 14.
- Decision: BasemapGroupRow does NOT receive Cmd/Shift/Checkbox handlers — boundary refusal lives at the parent handler level.
- Note for Plan 02: `t('bulkActions.selectRow', ...)` and `t('bulkActions.selectGroup', ...)` keys referenced but not yet added to `en/builder.json` — Plan 02 must add them.
- Note for Plan 04: Shift+ArrowUp/Down extension may need to be wired at the listbox level (parent keydown handler that derives sibling IDs from `sortableIds`).
</output>
