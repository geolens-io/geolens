# Phase 1104 Plan: Terrain Rendering and Config

**Requirements:** TERRAIN-01, TERRAIN-02, TERRAIN-03, TERRAIN-04

## Tasks

- Derive raster token `maxzoom` from stored COG resolution metadata with a legacy fallback.
- Load raster asset metadata for single and batch tile-token endpoints.
- Update the terrain exaggeration control to use `terrain-dem` only when that source exists.
- Add unit coverage for DEM and aerial resolution-derived zooms.
- Verify the live ADK relief map opens cleanly and the exaggeration slider updates without console errors.
