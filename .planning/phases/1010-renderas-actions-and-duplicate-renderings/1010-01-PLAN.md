---
phase: 1010-renderas-actions-and-duplicate-renderings
plan: 01
wave: 1
depends_on: []
autonomous: true
requirements: [RENDER-02, RENDER-03, RENDER-04, RENDER-05, RENDER-06, RENDER-07, RENDER-08]
subsystem: frontend-builder
tags: [map-builder, renderas, duplicate-rendering, tests]
files_modified:
  - frontend/src/components/builder/renderAs.ts
  - frontend/src/components/builder/MapStackItem.tsx
  - frontend/src/components/builder/MapStackPanel.tsx
  - frontend/src/components/builder/hooks/use-builder-layers.ts
  - frontend/src/pages/MapBuilderPage.tsx
  - frontend/src/components/builder/__tests__/renderAs.test.ts
  - frontend/src/components/builder/__tests__/MapStackPanel.test.tsx
  - frontend/src/components/builder/hooks/__tests__/use-builder-layers.test.ts
  - .planning/phases/1010-renderas-actions-and-duplicate-renderings/1010-01-SUMMARY.md
---

<objective>
Wire the v1 renderAs selector and duplicate-rendering actions over existing map-layer fields and existing add-layer/save paths.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1010-renderas-actions-and-duplicate-renderings/1010-CONTEXT.md
@frontend/src/components/builder/renderAs.ts
@frontend/src/components/builder/MapStackItem.tsx
@frontend/src/components/builder/MapStackPanel.tsx
@frontend/src/components/builder/hooks/use-builder-layers.ts
@frontend/src/components/builder/layer-adapters/registry.ts
@frontend/src/components/builder/layer-adapters/fill-adapter.ts
@frontend/src/types/api.ts
</context>

<tasks>
  <task type="auto">
    <name>Add renderAs patch builder</name>
    <files>frontend/src/components/builder/renderAs.ts, frontend/src/components/builder/__tests__/renderAs.test.ts</files>
    <action>Add a pure `buildRenderAsPatch` helper that validates options, returns only existing writable fields, maps 3D extrusion to builder metadata, and never returns `is_3d`.</action>
    <verify>Tests cover every supported source family and unsupported omissions.</verify>
    <done>RENDER-02..07 have executable mutation boundaries.</done>
  </task>

  <task type="auto">
    <name>Wire row renderAs changes</name>
    <files>frontend/src/components/builder/MapStackItem.tsx, frontend/src/components/builder/MapStackPanel.tsx, frontend/src/components/builder/hooks/use-builder-layers.ts, frontend/src/pages/MapBuilderPage.tsx</files>
    <action>Add `onRenderAsChange`, call the pure patch builder, update local layer state, mark dirty, and swap the live MapLibre adapter where possible.</action>
    <verify>Component tests click a row renderAs option and observe the handler call; hook/utility tests prove patch shape.</verify>
    <done>Row renderAs control becomes functional without schema drift.</done>
  </task>

  <task type="auto">
    <name>Add duplicate rendering action</name>
    <files>frontend/src/components/builder/MapStackItem.tsx, frontend/src/components/builder/MapStackPanel.tsx, frontend/src/components/builder/hooks/use-builder-layers.ts, frontend/src/components/builder/hooks/__tests__/use-builder-layers.test.ts</files>
    <action>Add a row overflow action that creates a sibling layer with the same dataset and copied style fields through the existing add-layer mutation.</action>
    <verify>Tests assert `dataset_id`, copied style fields, and next sort order.</verify>
    <done>RENDER-08 is satisfied for the row entry point.</done>
  </task>

  <task type="auto">
    <name>Record summary and verification</name>
    <files>.planning/phases/1010-renderas-actions-and-duplicate-renderings/1010-01-SUMMARY.md</files>
    <action>Document implementation, requirement coverage, and verification commands/results.</action>
    <verify>Summary exists and matches commands run.</verify>
    <done>Phase 1010 can hand off to basemap/terrain inline work.</done>
  </task>
</tasks>

<verification>
- `cd frontend && npm run test -- renderAs MapStackPanel use-builder-layers --run`
- `cd frontend && npm run lint`
- `cd frontend && npm run build`
</verification>

<must_haves>
- RenderAs patch output never includes `is_3d`.
- No new backend endpoint, migration, or persisted field.
- No punted renderers become selectable.
- Duplicate rendering uses existing `MapLayerInput` / add-layer API.
- Add Dataset modal duplicate entry point remains deferred to Phase 1012.
</must_haves>

<success_criteria>
- [ ] Supported renderAs options mutate only existing fields.
- [ ] Polygon 3D extrusion writes existing builder height/scale/minzoom/opacity fields.
- [ ] Row renderAs control invokes real mutation handling.
- [ ] Row overflow exposes Duplicate rendering and creates a sibling layer input with copied style fields.
- [ ] Focused tests, lint, and build pass.
</success_criteria>
