# Phase 1029 Plan: Cluster Exploration Interactions

## Goal

Add expected cluster exploration affordances across builder and viewer maps without changing saved map state or persisted schemas.

## Tasks

1. Add shared cluster interaction helpers for companion layer IDs, aggregate popup metadata, feature detection, coordinates, and expansion zoom.
2. Wire builder map hit-testing to include cluster circle/count companion layers and activate clusters on pointer click or canvas keyboard action.
3. Wire public/shared/embed viewer hit-testing to the same cluster activation path.
4. Surface bounded/server/fallback cluster state in the map stack and viewer legend.
5. Add focused unit/component coverage and run frontend lint.

## Verification

- Focused Vitest for cluster interaction helpers, map stack state, and viewer click activation.
- Frontend ESLint.
