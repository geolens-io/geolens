---
phase: 1045-builder-smoke-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/builder/BulkActionBar.tsx
  - frontend/src/components/builder/UnifiedStackPanel.tsx
  - frontend/src/components/builder/StackRow.tsx
  - frontend/src/components/builder/LayerStyleEditor.tsx
  - frontend/src/components/builder/BuilderMap.tsx
  - frontend/src/pages/MapBuilderPage.tsx
autonomous: false
requirements:
  - SP-01
  - SP-02
  - SP-03
  - SP-04
  - SP-05

must_haves:
  truths:
    - "Multi-select 2+ layers shows Group / Ungroup / Delete buttons reachable inside the 340px sidebar at 1440x900"
    - "Panning the builder map updates the lat/lng numbers in the top-right readout in real time"
    - "Shift-click on a layer row extends selection to a range from the last-clicked anchor"
    - "Opening a freshly-added layer's editor does not show the 'Pending style preview' banner"
    - "SP-03 verification recorded in VERIFICATION.md (PASS via B-01 fix or ESCALATE)"
  artifacts:
    - path: ".planning/phases/1045-builder-smoke-polish/VERIFICATION.md"
      provides: "Per-SP pass/skip record (5 lines after Plan A)"
      contains: "SP-01"
  key_links:
    - from: "frontend/src/components/builder/BulkActionBar.tsx"
      to: "frontend/src/pages/MapBuilderPage.tsx"
      via: "mount point inside sidebar aside; overflow menu collapse"
      pattern: "BulkActionBar"
    - from: "frontend/src/components/builder/BuilderMap.tsx"
      to: "frontend/src/pages/MapBuilderPage.tsx"
      via: "move/moveend event -> coord readout state"
      pattern: "on\\(.move"
---

<objective>
Plan A of Phase 1045 — close the BLOCKER + 4 MAJOR findings from the 2026-05-15 Map Builder smoke check. B-01 (live-add maplibre sync) already shipped at commit 85738f1c; SP-03 is verification-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/quick/260515-cej-docker-rebuild-builder-smoke/FINDINGS.md
</context>

## Goal

Restore the v1009 builder's most visible promises: bulk-delete actually reachable, coordinate readout truthful, shift-click range-select working, "Pending preview" banner gated on real dirty state. Verify B-01 fix closes the DEM auto-add interaction (M-02).

## Per-plan done definition

1. All 5 tasks committed (conventional commits, no AI/bot attribution, ≤72 char subjects)
2. `cd frontend && npx tsc --noEmit` — clean
3. `cd frontend && npx eslint src/` — clean
4. `cd frontend && npm test -- --run BulkActionBar LayerStyleEditor UnifiedStackPanel StackRow BuilderMap MapBuilderPage` — passes
5. `/gsd-code-review --fix` run; every finding addressed inline (no deferrals per feedback_review_findings_inline.md)
6. Commit chain pushed (do NOT merge — phase-level CTRL-01 gates the whole phase)

## Sequencing rationale

SP-01 first (BLOCKER, headline v1009 promise unmet). SP-05 + SP-04 grouped because both touch LayerStyleEditor / UnifiedStackPanel selection logic. SP-02 self-contained map header work. SP-03 verification-only, runs last in the plan against the now-stable builder.

<tasks>

<task type="auto" tdd="false">
  <name>Task A.1 — SP-01: Unclip BulkActionBar so Group/Ungroup/Delete are reachable</name>
  <files>frontend/src/components/builder/BulkActionBar.tsx, frontend/src/components/builder/UnifiedStackPanel.tsx, frontend/src/pages/MapBuilderPage.tsx</files>
  <action>BulkActionBar today renders inside the sidebar `<aside class="overflow-hidden">` and its inner flex row (Visibility + Opacity + Group + Ungroup + Delete) exceeds 340px so Group/Ungroup/Delete are clipped off-screen at the default 1440×900 desktop viewport. Per REQUIREMENTS.md SP-01, prefer the overflow-menu compaction when width &lt; ~400px (lower regression risk than relocating the mount point). Replace the trailing button cluster with a `…` overflow trigger (Lucide `MoreHorizontal`) that opens a popover containing Group / Ungroup / Delete. Visibility and Opacity stay inline. Keep keyboard semantics — Delete must remain reachable via the popover trigger then arrow-navigation. Do NOT relocate the mount point unless overflow-menu approach turns out impossible; if it does, escape the `<aside>` and span across the builder bottom (full width). No new design tokens; reuse existing menu primitives.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npx eslint src/components/builder/BulkActionBar.tsx src/components/builder/UnifiedStackPanel.tsx &amp;&amp; npm test -- --run BulkActionBar</automated>
    <human-check>Playwright: multi-select 2 vector layers, click overflow `…`, click Delete — confirmation dialog appears. Repeat with Group on 2 vector rows and Ungroup on a folder-group row.</human-check>
  </verify>
  <done>All three (Group/Ungroup/Delete) are reachable from the visible UI inside the 340px sidebar at 1440×900. Existing Visibility + Opacity behavior unchanged.</done>
  <commit>fix(builder): unclip BulkActionBar Group/Ungroup/Delete via overflow menu</commit>
</task>

<task type="auto" tdd="true">
  <name>Task A.2 — SP-05: Gate "Pending style preview" banner on real dirty-tracking</name>
  <files>frontend/src/components/builder/LayerStyleEditor.tsx</files>
  <behavior>
    - Opening a freshly-added layer's editor with no edits: banner NOT visible
    - Changing one paint value: banner visible with Reset action
    - Clicking Reset: banner disappears, editor draft equals layer server state
    - Editor draft deep-equals layer.paint/layout/style_config -&gt; hasUnsavedChanges() returns false
  </behavior>
  <action>The banner today is gated on the existence of an editor draft, which is created eagerly on open. Replace that gate with a memoized deep-equal comparison between the editor draft (paint/layout/style_config) and the layer's server state. Export a small `hasUnsavedStyleChanges(draft, layer)` helper for testability; use it both for the banner conditional and for the Reset button enabled state. Clean up the banner copy to be unambiguous: keep "Pending style preview — Reflects this layer before save" only when dirty.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npx eslint src/components/builder/LayerStyleEditor.tsx &amp;&amp; npm test -- --run LayerStyleEditor</automated>
    <human-check>Playwright: add a vector layer, open its editor, banner is NOT visible. Change one paint value, banner appears. Click Reset, banner disappears.</human-check>
  </verify>
  <done>Banner never appears on untouched-layer first-open. Appears on first real mutation. Disappears on Reset.</done>
  <commit>fix(builder): gate Pending style preview banner on real dirty state</commit>
</task>

<task type="auto" tdd="true">
  <name>Task A.3 — SP-04: Shift-click extends layer selection as a range</name>
  <files>frontend/src/components/builder/UnifiedStackPanel.tsx, frontend/src/components/builder/StackRow.tsx</files>
  <behavior>
    - Click row A: selection = {A}, anchor = A
    - Shift-click row B (B below A): selection = {A, ..., B}, anchor unchanged
    - Shift-click row C (C above A): selection = {C, ..., A}, anchor unchanged
    - Cmd/Ctrl-click row D: selection toggles D, anchor = D
    - Plain click row E: selection = {E}, anchor = E
  </behavior>
  <action>Current shift-click replaces selection because the handler treats Shift identically to a plain click. Extract a `computeNextSelection(rows, event, currentSelection, anchor)` pure helper that returns `{selection, anchor}`. On `e.shiftKey`, compute the index range between anchor and clicked row in the rendered list order (skip basemap-row / non-selectable rows) and replace selection with that range. On `e.metaKey || e.ctrlKey`, toggle the clicked row and update anchor. Plain click clears and sets anchor. Wire the helper into the row click handler in UnifiedStackPanel; pass `anchor` as a `useRef` to avoid stale closure issues. Standard list-box behavior — match macOS Finder semantics for both directions.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npx eslint src/components/builder/UnifiedStackPanel.tsx src/components/builder/StackRow.tsx &amp;&amp; npm test -- --run UnifiedStackPanel</automated>
    <human-check>Playwright: click row A then shift-click row B (two rows below) → both A and B in selection plus the row between. Then ⌘-click row D → toggles D individually.</human-check>
  </verify>
  <done>Shift-click range-extends selection in both directions. ⌘/Ctrl-click still toggles individually. Plain click clears.</done>
  <commit>feat(builder): shift-click extends layer selection as range</commit>
</task>

<task type="auto" tdd="false">
  <name>Task A.4 — SP-02: Subscribe coord readout to MapLibre move events</name>
  <files>frontend/src/pages/MapBuilderPage.tsx, frontend/src/components/builder/BuilderMap.tsx</files>
  <action>Locate the "° N · ° W · z" readout JSX (search `°N` / `° N` near the builder map header). Today the component reads a default `viewState` source that never re-syncs after auto-fit, so zoom updates because BuilderMap pushes it but lat/lng stay at 20.00°N · 0.00°E. Fix: subscribe to MapLibre `move` (or `moveend`) events from the map instance and lift center+zoom into a small `useMapViewState(map)` hook (or local state inside the readout component). Use the react-maplibre `useMap()` hook if already in use; otherwise reach the map via the existing ref. Format lat/lng using the existing formatter; do not change the visible string format. Throttle to animation frame if needed, but a plain `on('move', …)` setState is fine — the readout is a single element.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npx eslint src/pages/MapBuilderPage.tsx src/components/builder/BuilderMap.tsx</automated>
    <human-check>Playwright: pan the map to ~Grand Canyon (36.2°N / 112.3°W); the readout should show numbers close to that within one move event. Pan to a different region; numbers should update again.</human-check>
  </verify>
  <done>Lat/lng track `map.getCenter()` on every pan/zoom. Zoom continues to track as it does today.</done>
  <commit>fix(builder): subscribe coord readout to maplibre move events</commit>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task A.5 — SP-03: Verify B-01 fix closes M-02 (DEM auto-add)</name>
  <files>.planning/phases/1045-builder-smoke-polish/VERIFICATION.md</files>
  <what-built>No code change. B-01 already shipped at commit 85738f1c. SP-03 is a verification-only task to confirm M-02 is closed by the B-01 fix.</what-built>
  <how-to-verify>
    1. In a browser at http://localhost:8080, log in as admin.
    2. Create a fresh map.
    3. Add one vector layer (any QA canyon vector dataset).
    4. Add the QA Grand Canyon DEM as Image mode.
    5. Without reloading, observe whether both layers render at the auto-fit zoom.
    6. Expected: both render. If yes → write to VERIFICATION.md: `SP-03: PASS — B-01 fix closes M-02; vector + DEM render on first add without reload.`
    7. If still broken → write: `SP-03: ESCALATE — B-01 fix did NOT close M-02. See follow-up ticket.` and surface to user before proceeding.
  </how-to-verify>
  <resume-signal>Type "approved" after writing the SP-03 line in VERIFICATION.md, OR describe the failure if M-02 is still broken.</resume-signal>
</task>

</tasks>

<verification>
- Frontend typecheck + lint clean
- Vitest passes for affected files (BulkActionBar, LayerStyleEditor, UnifiedStackPanel, StackRow, BuilderMap, MapBuilderPage)
- Playwright human-check passes for SP-01/02/04/05
- VERIFICATION.md updated with SP-03 result
</verification>

<success_criteria>
- 5/5 tasks complete (4 code commits + 1 verification line)
- All A-plan must_haves true
- /gsd-code-review --fix run with findings addressed inline
</success_criteria>

<output>
Create `.planning/phases/1045-builder-smoke-polish/1045-01-SUMMARY.md` when done.
</output>
