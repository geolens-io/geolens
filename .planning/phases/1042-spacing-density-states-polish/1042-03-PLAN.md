---
phase: 1042-spacing-density-states-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 1042-01
files_modified:
  - frontend/src/components/builder/LayerEditorPanel.tsx
  - frontend/src/components/builder/BasemapGroupEditorScene.tsx
  - frontend/src/components/builder/BasemapSublayerEditorScene.tsx
  - frontend/src/components/builder/SettingsEditorScene.tsx
  - frontend/src/components/builder/DEMEditorScene.tsx
  - frontend/src/components/builder/BasemapGroupRow.tsx
  - frontend/src/components/builder/FolderGroupRow.tsx
  - frontend/src/components/builder/SidebarRail.tsx
  - frontend/src/components/builder/__tests__/LayerEditorPanel.test.tsx
  - frontend/src/components/builder/__tests__/BasemapGroupEditorScene.test.tsx
autonomous: true
requirements:
  - POL-13
  - POL-14
tags:
  - builder
  - layer-editor
  - settings
  - density
  - state-vocabulary

must_haves:
  truths:
    - "LayerEditorPanel header uses px-4 py-3 (not px-2 py-2)"
    - "LayerEditorTypePill colorizes per record kind (vector / raster|vrt / basemap / fallback)"
    - "All collapsible carets in LayerEditorPanel + BasemapGroupRow + FolderGroupRow use transition-transform duration-[--motion-fast]"
    - "BasemapGroupEditorScene + BasemapSublayerEditorScene + SettingsEditorScene + DEMEditorScene section wrappers use px-4 py-2 (not py-3)"
    - "BasemapGroupEditorScene Scene-B sublayer rows use the canonical 7-cell grid (not inline-style flex)"
    - "BasemapGroupRow grip is hidden (aria-hidden) — basemap is non-draggable; AUD-04 silent-no-op resolved"
    - "SidebarRail row hover token aligned to hover:bg-[var(--surface-2)] (matches StackRow)"
  artifacts:
    - path: "frontend/src/components/builder/LayerEditorPanel.tsx"
      provides: "Header padding fix, type-pill color-by-kind, caret duration consistency"
      contains: "duration-[--motion-fast]"
    - path: "frontend/src/components/builder/BasemapGroupEditorScene.tsx"
      provides: "py-2 section padding + canonical 7-cell sublayer grid"
      contains: "grid-cols-[16px_14px_22px_22px_1fr_60px_22px]"
    - path: "frontend/src/components/builder/BasemapGroupRow.tsx"
      provides: "Hidden grip + caret duration"
      contains: "aria-hidden"
    - path: "frontend/src/components/builder/SidebarRail.tsx"
      provides: "Hover token unification to --surface-2"
      contains: "var(--surface-2)"
  key_links:
    - from: "LayerEditorPanel.tsx LayerEditorTypePill"
      to: "OKLCH type-color tokens (--type-vector-bg, --type-raster-bg, --primary-50)"
      via: "cn() conditional driven by caps.kind"
      pattern: "type-vector-bg"
    - from: "BasemapGroupEditorScene.tsx Scene B sublayer <li>"
      to: "Canonical 7-cell grid rules"
      via: "Tailwind grid-cols-[...] className replacing inline style"
      pattern: "grid-cols"
    - from: "BasemapGroupRow.tsx grip slot"
      to: "AUD-04 silent-no-op resolution"
      via: "aria-hidden span replacing the grip button"
      pattern: "aria-hidden"
---

<objective>
Apply Cluster D (LayerEditorPanel header + type pill + caret consistency, AUD-05/06/07), the four-scene `py-2` normalization + Scene-B canonical grid (AUD-16/17), the BasemapGroupRow grip-hide that resolves AUD-04, the row-caret duration fix in BasemapGroupRow + FolderGroupRow (AUD-07 row-side), and the SidebarRail hover-token unification (AUD-21).

Purpose: This plan owns the bulk of the spacing/density/state vocabulary unification (POL-13/14). The files cluster naturally because: (a) all four scene components reuse the LayerEditorPanel `px-4 py-2` section-padding pattern; (b) the row caret duration fix in BasemapGroupRow + FolderGroupRow shares the same target value as the LayerEditorPanel section caret fix; (c) the AUD-04 grip-hide for BasemapGroupRow lives in the same row component; (d) SidebarRail's hover-token fix is a single-line change owned by no other plan. Bundling them into one plan keeps the file-ownership map tight and parallel-safe with Plans 02 and 04.

Output: Polished LayerEditorPanel with sketch-spec header padding, type-colored pill, and unified caret motion. Four scene components with consistent `px-4 py-2` section padding and Scene B's sublayer rows on the canonical 7-cell grid. BasemapGroupRow with a hidden grip + duration-applied caret. FolderGroupRow with duration-applied caret. SidebarRail with the StackRow-aligned hover token. Vitest coverage extended in two existing test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/1042-spacing-density-states-polish/1042-CONTEXT.md
@.planning/phases/1042-spacing-density-states-polish/1042-PATTERNS.md
@.planning/phases/1042-spacing-density-states-polish/1042-UI-SPEC.md
@.planning/phases/1039-ux-audit-and-test-debt-closeout/BUILDER-UX-AUDIT.md

# Source files in scope:
@frontend/src/components/builder/LayerEditorPanel.tsx
@frontend/src/components/builder/BasemapGroupEditorScene.tsx
@frontend/src/components/builder/BasemapSublayerEditorScene.tsx
@frontend/src/components/builder/SettingsEditorScene.tsx
@frontend/src/components/builder/DEMEditorScene.tsx
@frontend/src/components/builder/BasemapGroupRow.tsx
@frontend/src/components/builder/FolderGroupRow.tsx
@frontend/src/components/builder/SidebarRail.tsx

# Pattern source for the canonical 7-cell grid (REFERENCE ONLY — not modified by this plan):
@frontend/src/components/builder/UnifiedStackPanel.tsx

<interfaces>
<!-- Key landmarks per PATTERNS.md §Cluster D and §Cluster F. -->

LayerEditorPanel.tsx (existing, 766 LOC):
- LayerEditorTypePill function at lines 73-93. `caps.kind` already computed at line 74.
- Header className at line 203 — currently `px-2 py-2`.
- Section caret elements at lines 449, 484, 518 — currently `transition-transform duration-150`.

BasemapGroupRow.tsx (existing, 232 LOC):
- Grip button at lines 112-127 — currently renders cursor-grab; useSortable is registered upstream but basemap id is excluded from sortableIds in UnifiedStackPanel.tsx (silent no-op confirmed in audit AUD-04).
- Row caret around line 99/104 — currently `transition-transform` with no duration.

FolderGroupRow.tsx (existing, 349 LOC):
- Row caret around line 142/180 — currently `transition-transform` with no duration.

BasemapGroupEditorScene.tsx (existing, 248 LOC):
- Section wrapper divs at lines 78, 128, 207 — currently `px-4 py-3`.
- Sublayer <li> around line 137-203 (per PATTERNS.md) uses inline `style={{ height: '32px', display: 'flex', alignItems: 'center', gap: '8px', padding: '0 4px' }}`.

UnifiedStackPanel.tsx (REFERENCE ONLY — owned by Plan 04):
- Canonical 7-cell sublayer grid at line 427: `grid grid-cols-[16px_14px_22px_22px_1fr_60px_22px] gap-2 items-center py-2 px-2`.

SettingsEditorScene.tsx — section wrappers at lines 115, 167, 226 use `py-3`.
DEMEditorScene.tsx — section wrappers at lines 163, 203, 359 use `py-3`.
BasemapSublayerEditorScene.tsx — `py-3` occurrences need to change to `py-2`.

SidebarRail.tsx (existing, 138 LOC):
- Hover token at line 121 — currently `hover:bg-accent`. Target: `hover:bg-[var(--surface-2)]`.

Required type tokens (UI-SPEC §Color #8) — confirm present in index.css before Task 1:
`--type-vector`, `--type-vector-bg`, `--type-raster`, `--type-raster-bg`, `--primary-50`, `--primary-700`. PATTERNS.md notes these are used elsewhere already (e.g. UnifiedStackPanel.tsx:473).

Existing tests:
- frontend/src/components/builder/__tests__/LayerEditorPanel.test.tsx — extend with type-pill color-by-kind + header padding assertion + caret duration assertion.
- frontend/src/components/builder/__tests__/BasemapGroupEditorScene.test.tsx — extend with sublayer-grid + section-padding assertion.
- frontend/src/components/builder/__tests__/BasemapGroupRow.test.tsx, FolderGroupRow.test.tsx, SidebarRail.test.tsx — already exist; no new tests required (single-line className changes are covered by the file-level grep gates in Task 3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LayerEditorPanel — header padding, type-pill colorization, caret duration (AUD-05/06/07)</name>
  <files>frontend/src/components/builder/LayerEditorPanel.tsx, frontend/src/components/builder/__tests__/LayerEditorPanel.test.tsx</files>
  <behavior>
    - Test 1 (AUD-05): The header element of LayerEditorPanel renders with className containing both `px-4` and `py-3` (not `px-2 py-2`).
    - Test 2 (AUD-06): When the layer's `caps.kind === 'vector'`, the LayerEditorTypePill renders with className containing `bg-[var(--type-vector-bg)]` AND `text-[var(--type-vector)]`. Repeat per kind: raster/vrt -> `--type-raster-bg`/`--type-raster`; basemap -> `--primary-50`/`--primary-700`. Fallback (unknown kind) keeps `--surface-2`/`text-muted-foreground`.
    - Test 3 (AUD-07): Each of the three section caret elements (Filter / Labels / Source) renders with className containing `transition-transform duration-[--motion-fast]` (and gains `rotate-90` when the section is open).
  </behavior>
  <action>Apply Cluster D fixes from PATTERNS.md and UI-SPEC §Findings AUD-05/06/07. Step 1 — AUD-05: Change the LayerEditorPanel header className at line 203 from `flex flex-col px-2 py-2 border-b shrink-0` to `flex flex-col px-4 py-3 border-b shrink-0` per UI-SPEC table row AUD-05. Step 2 — AUD-06: Inside `LayerEditorTypePill` at lines 73-93, replace the constant className at line 90 with a `cn()` call driven by `caps.kind` (already computed at line 74) per UI-SPEC table row AUD-06: vector -> `bg-[var(--type-vector-bg)] text-[var(--type-vector)]`; raster|vrt -> `bg-[var(--type-raster-bg)] text-[var(--type-raster)]`; basemap -> `bg-[var(--primary-50)] text-[var(--primary-700)]`; fallback -> `bg-[var(--surface-2)] text-muted-foreground`. BEFORE writing the cn() block, run a defensive grep `cd frontend && grep -nE 'type-vector|type-raster|primary-700' src/index.css` to confirm all referenced tokens exist (PATTERNS.md notes UnifiedStackPanel.tsx:473 already references these). Step 3 — AUD-07: Change `duration-150` to `duration-[--motion-fast]` on the three section carets at lines 449, 484, 518. Plan 01 ships the `--motion-fast` token in `:root`. Add the three test cases described in <behavior> to LayerEditorPanel.test.tsx; reuse the existing fixture factory in the file (do not invent new dataset shapes). Do NOT touch the section content, footer buttons, delete-confirm autoFocus (AUD-09 is Phase 1043), or any other LayerEditorPanel surface.</action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/builder/__tests__/LayerEditorPanel.test.tsx --reporter=basic</automated>
  </verify>
  <done>LayerEditorPanel header reads at sketch density; type pill colors by record kind across all four cases; three section carets reference --motion-fast; existing LayerEditorPanel tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Four scene components — section padding (py-2) + Scene-B canonical 7-cell sublayer grid (AUD-16/17)</name>
  <files>frontend/src/components/builder/BasemapGroupEditorScene.tsx, frontend/src/components/builder/BasemapSublayerEditorScene.tsx, frontend/src/components/builder/SettingsEditorScene.tsx, frontend/src/components/builder/DEMEditorScene.tsx, frontend/src/components/builder/__tests__/BasemapGroupEditorScene.test.tsx</files>
  <behavior>
    - Test 1 (AUD-16): BasemapGroupEditorScene section wrappers all render with className containing `px-4 py-2` (not `py-3`). Assert by querying for at least three section wrapper divs and checking each one's className.
    - Test 2 (AUD-17): BasemapGroupEditorScene Scene B's sublayer list renders each <li> with className containing `grid grid-cols-[16px_14px_22px_22px_1fr_60px_22px]`. Inline `style={{ height: '32px' }}` is gone.
    - Test 3 (AUD-17): Each Scene B sublayer row's caret column (col 1) is present but visibility-hidden (no visible glyph since sublayers are not collapsible from this scene); each row's grip column (col 2) is present but `opacity-0 pointer-events-none` (sublayers are not draggable from Scene B).
  </behavior>
  <action>Apply Cluster D scene-padding + Scene-B grid fixes from PATTERNS.md and UI-SPEC §Findings AUD-16/17. Step 1 — AUD-16 across all four scenes: Change `px-4 py-3` to `px-4 py-2` at every section wrapper div in BasemapGroupEditorScene.tsx (lines 78, 128, 207), BasemapSublayerEditorScene.tsx (grep for `py-3` and change to `py-2` at section-level wrappers — leave non-section heights like `h-9` row controls alone per PATTERNS.md), SettingsEditorScene.tsx (lines 115, 167, 226 + collapsible button trigger; per PATTERNS.md leave the line-181 `h-9` row alone), and DEMEditorScene.tsx (lines 163, 203, 359). Run `cd frontend && grep -n 'px-4 py-3' src/components/builder/BasemapGroupEditorScene.tsx src/components/builder/BasemapSublayerEditorScene.tsx src/components/builder/SettingsEditorScene.tsx src/components/builder/DEMEditorScene.tsx` BEFORE editing to enumerate every occurrence; after editing the same grep should return zero matches in those four files. Step 2 — AUD-17: In BasemapGroupEditorScene.tsx around line 137-203, replace the inline-style flex `<li>` with the canonical 7-cell grid pattern from UnifiedStackPanel.tsx:427 per PATTERNS.md §Cluster D and UI-SPEC §Findings AUD-17. Target className: `group/row grid grid-cols-[16px_14px_22px_22px_1fr_60px_22px] gap-2 items-center py-2 px-2`. Cell mapping: Col 1 (16px caret) is a hidden span with `style={{ visibility: 'hidden' }} className="h-[14px] w-[14px]" aria-hidden="true"`; Col 2 (14px grip) is a hidden span with `className="opacity-0 pointer-events-none" aria-hidden="true"`; Col 3 (22px eye toggle) is the existing visibility button — keep as-is; Col 4 (22px type icon) is the existing SublayerTypeIcon — keep as-is, ensure 22px width; Col 5 (1fr name) is the existing label span — keep as-is; Col 6 (60px opacity) is the existing Slider — keep as-is; Col 7 (22px spacer for kebab alignment) is an empty `aria-hidden="true"` span. Do NOT add or remove sublayer-row interactions — just the grid restructure. Add to BasemapGroupEditorScene.test.tsx the three test cases from <behavior>. Other three scene tests (BasemapSublayerEditorScene, SettingsEditorScene, DEMEditorScene) are NOT extended — the spacing change is uniform and the file-level grep gate plus the BasemapGroupEditorScene test plus existing scene tests provide sufficient regression coverage. Do NOT touch AUD-18 (BasemapGroupEditorScene.tsx footer Reset/Remove asymmetry — Phase 1043 owns that per BUILDER-UX-AUDIT.md §Notes).</action>
  <verify>
    <automated>cd frontend && (grep -hv '^[[:space:]]*//' src/components/builder/BasemapGroupEditorScene.tsx src/components/builder/BasemapSublayerEditorScene.tsx src/components/builder/SettingsEditorScene.tsx src/components/builder/DEMEditorScene.tsx | grep -c 'px-4 py-3' | grep -q '^0$') && npx vitest run src/components/builder/__tests__/BasemapGroupEditorScene.test.tsx --reporter=basic</automated>
  </verify>
  <done>All four scene components show zero `px-4 py-3` matches in non-comment lines; Scene B sublayer rows render on the canonical 7-cell grid; BasemapGroupEditorScene tests pass (existing + 3 new).</done>
</task>

<task type="auto">
  <name>Task 3: Row carets (BasemapGroupRow + FolderGroupRow) + BasemapGroupRow grip hide (AUD-04) + SidebarRail hover token (AUD-21)</name>
  <files>frontend/src/components/builder/BasemapGroupRow.tsx, frontend/src/components/builder/FolderGroupRow.tsx, frontend/src/components/builder/SidebarRail.tsx</files>
  <action>Apply three small, targeted className/structure changes that close the remaining state-vocabulary findings owned by this plan. Step 1 — AUD-07 row-side: In BasemapGroupRow.tsx around line 99/104 (the row caret), append `duration-[--motion-fast]` to the existing `transition-transform` className. In FolderGroupRow.tsx around line 142/180 (the row caret), do the same — append `duration-[--motion-fast]`. UI-SPEC §Motion Timing Reference lists these as the "Caret rotate (section expand/collapse)" entries. Step 2 — AUD-04 (silent-no-op resolution): In BasemapGroupRow.tsx at lines 112-127 (the grip button), replace the entire grip button with `<span aria-hidden="true" className="h-[14px] w-[14px]" />` per PATTERNS.md §Cluster F preferred fix. Basemap is excluded from sortableIds upstream; rendering a draggable-looking grip is misleading and a11y-noisy. Do NOT remove `useSortable` registration — that is upstream wiring owned by Plan 04 (UnifiedStackPanel sortableIds). The grip-hide alone resolves the user-facing finding. Step 3 — AUD-21: In SidebarRail.tsx at line 121, change `hover:bg-accent` to `hover:bg-[var(--surface-2)]` per UI-SPEC §Findings AUD-21. This aligns the rail row hover with StackRow.tsx:429 (`hover:bg-[var(--surface-2,theme(colors.accent.DEFAULT))]`); UI-SPEC notes the two tokens resolve to nearly the same OKLCH value, so this is a token-consistency fix not a visual rebrand. Run `cd frontend && grep -n 'transition-transform' src/components/builder/BasemapGroupRow.tsx src/components/builder/FolderGroupRow.tsx` after the change to confirm both row carets carry `duration-[--motion-fast]`. Do NOT touch the row's drag state, the row body className, the kebab, or AUD-19 rail icon sizing (AUD-19 lives in Plan 04 because the corresponding header-side fix is in UnifiedStackPanel.tsx).</action>
  <verify>
    <automated>cd frontend && grep -A1 'transition-transform' src/components/builder/BasemapGroupRow.tsx src/components/builder/FolderGroupRow.tsx | grep -q 'duration-\[--motion-fast\]' && grep -q 'aria-hidden="true"' src/components/builder/BasemapGroupRow.tsx && grep -q 'hover:bg-\[var(--surface-2)\]' src/components/builder/SidebarRail.tsx && npx vitest run src/components/builder/__tests__/BasemapGroupRow.test.tsx src/components/builder/__tests__/FolderGroupRow.test.tsx src/components/builder/__tests__/BuilderRail.test.tsx --reporter=basic</automated>
  </verify>
  <done>Both row carets reference `duration-[--motion-fast]`; BasemapGroupRow grip is replaced with an aria-hidden span; SidebarRail hover uses `--surface-2`; existing BasemapGroupRow / FolderGroupRow / BuilderRail (which covers SidebarRail) tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client browser <- React component tree | All edits are presentation-layer className/structure changes. No new request boundary, no untrusted-input sink. |
| pointer/keyboard input -> @dnd-kit registration | BasemapGroupRow grip-hide narrows the visible drag affordance surface; useSortable hook itself is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1042-03-01 | T (Tampering) | LayerEditorTypePill cn() driven by caps.kind | accept | `caps.kind` is derived from canonical layer-capability typing internal to the builder; values are enumerated and a fallback class is provided for unknown kinds — no string injection possible. |
| T-1042-03-02 | I (Information Disclosure) | type-pill color choice surfaces dataset record kind to viewer | accept | Record kind is already visible elsewhere in the same panel (icon, scene routing); colorizing the pill does not leak new metadata. |
| T-1042-03-03 | E (Elevation of Privilege) | BasemapGroupRow grip removal | mitigate | Replacing the grip with `aria-hidden` span removes a misleading interactive surface that could fool users into believing they can reorder the basemap; fix lowers user confusion not privilege. Verify by running existing BasemapGroupRow.test.tsx — drag attempts on the row should already be no-ops via the upstream sortableIds exclusion; this change just stops advertising the false affordance. |
| T-1042-03-04 | D (Denial of Service) | SidebarRail hover token swap | accept | Single-token rename; no new layout/paint cost. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/builder/__tests__/LayerEditorPanel.test.tsx src/components/builder/__tests__/BasemapGroupEditorScene.test.tsx src/components/builder/__tests__/BasemapGroupRow.test.tsx src/components/builder/__tests__/FolderGroupRow.test.tsx src/components/builder/__tests__/BuilderRail.test.tsx --reporter=basic` — all pass.
- `cd frontend && grep -hv '^[[:space:]]*//' src/components/builder/BasemapGroupEditorScene.tsx src/components/builder/BasemapSublayerEditorScene.tsx src/components/builder/SettingsEditorScene.tsx src/components/builder/DEMEditorScene.tsx | grep -c 'px-4 py-3'` — equals zero.
- `cd frontend && npm run lint -- src/components/builder/LayerEditorPanel.tsx src/components/builder/BasemapGroupEditorScene.tsx src/components/builder/BasemapSublayerEditorScene.tsx src/components/builder/SettingsEditorScene.tsx src/components/builder/DEMEditorScene.tsx src/components/builder/BasemapGroupRow.tsx src/components/builder/FolderGroupRow.tsx src/components/builder/SidebarRail.tsx` — clean.
- Manual smoke (recommended): open a vector layer in the LayerEditorPanel — header reads at sketch density, type pill is colored; open a raster layer — pill switches to raster palette; open a basemap-group scene — sections reflow at py-2 density; expand/collapse a folder group row — caret rotates with --motion-fast easing; hover SidebarRail buttons — hover surface matches StackRow hover.
</verification>

<success_criteria>
- [x] POL-13 (spacing/density token normalization): LayerEditorPanel header reaches sketch `14/14/12px` rhythm; four scene components normalized to `px-4 py-2`; Scene B sublayer rows on the canonical 7-cell grid.
- [x] POL-14 (state vocabulary unification): all collapsible carets across LayerEditorPanel + BasemapGroupRow + FolderGroupRow share `transition-transform duration-[--motion-fast]`; type pill colorizes by record kind; SidebarRail hover token aligns with StackRow.
- [x] AUD-04 silent-no-op resolved by hiding the BasemapGroupRow grip; AUD-21 hover-token consistency restored.
- [x] No new tokens introduced; no shadcn components added; no backend touches.
- [x] No file overlap with Plan 02 (BulkActionBar/DatasetSearchPanel) or Plan 04 (UnifiedStackPanel/StackRow/EmptyStackState/builder.json/use-builder-layers) — Plans 02/03/04 are truly parallel under Wave 2.
- [x] Existing tests in LayerEditorPanel, BasemapGroupEditorScene, BasemapGroupRow, FolderGroupRow, BuilderRail (covers SidebarRail) all stay green.
</success_criteria>

<output>
After completion, create `.planning/phases/1042-spacing-density-states-polish/1042-03-SUMMARY.md` per the template, naming the exact line numbers edited per file, the verified type-token names found in `index.css`, the post-edit grep result for `px-4 py-3` across the four scenes (must be zero), and the post-edit confirmation that BasemapGroupRow grip is no longer interactive.
</output>
