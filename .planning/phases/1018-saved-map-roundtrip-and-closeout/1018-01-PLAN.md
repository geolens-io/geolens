---
phase: 1018-saved-map-roundtrip-and-closeout
plan: 01
wave: 1
depends_on: []
autonomous: true
requirements: [ROUND-01, ROUND-02, ROUND-03, ROUND-04]
subsystem: frontend-builder
tags: [map-builder, roundtrip, public-viewer, playwright, tests, closeout]
files_modified:
  - e2e/builder.spec.ts
  - frontend/src/components/builder/hooks/__tests__/use-builder-save.test.ts
  - frontend/src/pages/__tests__/PublicMapViewerPage.test.tsx
  - frontend/src/pages/__tests__/PublicViewerPage.test.tsx
  - .planning/phases/1018-saved-map-roundtrip-and-closeout/1018-01-SUMMARY.md
  - .planning/phases/1018-saved-map-roundtrip-and-closeout/1018-VERIFICATION.md
---

<objective>
Close v1003 by proving saved-map round trips, public/shared viewer compatibility, and final verification evidence for the builder sidebar/Add Dataset redesign.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1018-saved-map-roundtrip-and-closeout/1018-CONTEXT.md
@e2e/builder.spec.ts
@frontend/src/components/builder/hooks/__tests__/use-builder-save.test.ts
@frontend/src/pages/__tests__/PublicMapViewerPage.test.tsx
@frontend/src/pages/__tests__/PublicViewerPage.test.tsx
</context>

<tasks>
  <task type="auto">
    <name>Add browser zoom-range round-trip coverage</name>
    <files>e2e/builder.spec.ts</files>
    <action>Add a builder E2E test that edits a layer zoom range, saves, checks API response key stability and persisted layout values, reloads, and confirms the sidebar row still shows the saved range.</action>
    <verify>Assert both map-level and layer-level key sets remain unchanged across save.</verify>
    <done>ROUND-01 and ROUND-02 have browser-level proof.</done>
  </task>

  <task type="auto">
    <name>Harden builder save and viewer contracts</name>
    <files>use-builder-save.test.ts, PublicMapViewerPage.test.tsx, PublicViewerPage.test.tsx</files>
    <action>Add focused tests for duplicate-rendering save diff plus basemap/terrain config metadata, and assert public/shared viewers forward terrain config.</action>
    <verify>Run focused save/viewer tests.</verify>
    <done>ROUND-02 and ROUND-03 are pinned at component/unit level.</done>
  </task>

  <task type="auto">
    <name>Run closeout gates and close milestone state</name>
    <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, phase summary and verification artifacts</files>
    <action>Run focused tests, builder smoke, lint, build, and Playwright MCP console/UI inspection; mark ROUND-01..04 and v1003 complete.</action>
    <verify>Roadmap, requirements, and state agree on v1003 completion.</verify>
    <done>v1003 is ready for user review or archive.</done>
  </task>
</tasks>

<verification>
- `npx playwright test e2e/builder.spec.ts --project=chromium -g "round-trips layer zoom range"`
- `cd frontend && npm run test -- use-builder-save PublicMapViewerPage PublicViewerPage --run`
- `npm run e2e:smoke:builder`
- `cd frontend && npm run lint`
- `cd frontend && npm run build`
</verification>

<must_haves>
- No schema, migration, renderer, catalog endpoint, or import workflow changes.
- Existing saved-map field names remain unchanged.
- Public/shared viewer tests cover builder-authored basemap and terrain settings.
</must_haves>

<success_criteria>
- [ ] Existing saved maps load/save without persisted schema drift.
- [ ] Duplicate renderings, basemap config, terrain config, and zoom-range layout settings round-trip or save through existing fields.
- [ ] Public/shared viewer behavior remains compatible with builder-authored basemap and terrain settings.
- [ ] Closeout records verification commands, Playwright MCP observations, and residual gaps.
</success_criteria>
