---
phase: 1097-live-verify-close-gate
plan: 02
type: execute
wave: 2
depends_on:
  - 1097-01
files_modified:
  - .planning/phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md
  - .planning/MILESTONES.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/1097-live-verify-close-gate/1097-02-SUMMARY.md
autonomous: false
requirements:
  - CI-01
  - CLOSE-01
user_setup: []

must_haves:
  truths:
    - "Operator explicitly confirmed `git push origin main` before push fires (CONTEXT.md push-gate decision)"
    - "Plan 01's close-gate commit pushed to origin/main successfully (push exit 0; remote ref updated)"
    - "`gh run watch $RUN_ID` shows the `pytest-parallel-isolation` job completing GREEN (CI-01 (a))"
    - "The `pytest-parallel-isolation` job log block is embedded verbatim in 1097-01-CLOSE-GATE.md CI-01 (f) section (CI-01 (b))"
    - "Tags `v1022` (local) + `v1.5.7` (public) cut at Plan 01's close-gate SHA and pushed to origin"
    - "`.planning/MILESTONES.md` has a new v1022 entry (mirror v1021 entry format) referencing the close-gate SHA + tag refs"
    - "`.planning/REQUIREMENTS.md` flipped: CI-01 `[ ]` → `[x]` + Pending → Complete; CLOSE-01 `[ ]` → `[x]` + Pending → Complete (BOTH in the same atomic-4-file commit)"
    - "Phase 1097 ready for milestone audit/complete/cleanup lifecycle (orchestrator handles post-Phase 1097)"
  artifacts:
    - path: ".planning/phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md"
      provides: "CI-01 (f) live-verify log section + CLOSE-01 (g) tag-cut section appended"
      contains: "## CLOSE-01 (f) — CI-01 live-verify"
    - path: ".planning/MILESTONES.md"
      provides: "v1022 milestone entry recording tags + close-gate SHA + 4-phase rollup"
      contains: "## v1022 Parallel-Test Cascade Closure"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CI-01 + CLOSE-01 flipped to [x] + Complete in single atomic commit (v1019 TD-13 traceability_flip)"
      contains: "- [x] **CI-01**"
    - path: ".planning/phases/1097-live-verify-close-gate/1097-02-SUMMARY.md"
      provides: "Plan 02 + Phase 1097 close rollup"
      min_lines: 40
  key_links:
    - from: ".planning/phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md (CI-01 (f) section)"
      to: "/tmp/v1022-1097-ci-live-verify.log + gh-run-url"
      via: "embedded log block + GitHub Actions run URL citation"
      pattern: "github.com/.+/actions/runs/[0-9]+"
    - from: ".planning/MILESTONES.md"
      to: "git tag refs (v1022 + v1.5.7)"
      via: "tag SHA + symbolic-ref citation"
      pattern: "v1022.*v1\\.5\\.7"
    - from: ".planning/REQUIREMENTS.md Traceability table"
      to: "Phase 1097 row"
      via: "`| CI-01 | Phase 1097 | Complete |` + `| CLOSE-01 | Phase 1097 | Complete |`"
      pattern: "\\| (CI-01|CLOSE-01) \\| Phase 1097 \\| Complete \\|"
---

<objective>
Live-verify the `pytest-parallel-isolation` CI gate against real GitHub Actions infrastructure (CI-01), then cut the milestone tags + flip both CI-01 and CLOSE-01 to Complete in a single atomic-4-file commit (CLOSE-01).

Purpose: Closes Phase 1097, closes v1022 milestone. CI-01 has been deferred since v1020 Phase 1089 (the `pytest-parallel-isolation` gate was added but never live-verified on real CI infrastructure post-merge) — this plan closes that v1020 carry-forward. CLOSE-01 ships the tag-cut + traceability flip that ends the milestone.

Output: 4-file atomic commit modifying 1097-01-CLOSE-GATE.md (append CI-01 + tag-cut sections) + MILESTONES.md (v1022 entry) + REQUIREMENTS.md (CI-01 + CLOSE-01 flip) + 1097-02-SUMMARY.md. Plus 2 tags pushed to origin (`v1022` + `v1.5.7`). Plus 1 push to origin/main (the Plan 01 close-gate commit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/1097-live-verify-close-gate/1097-CONTEXT.md
@.planning/phases/1097-live-verify-close-gate/1097-01-PLAN.md
@.planning/phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md
@.planning/MILESTONES.md

<interfaces>
<!-- CI gate target (read-only — no edits) -->
<!-- Verified at plan-time (2026-05-24) via `git grep -n`. -->

CI workflow:
- .github/workflows/ci.yml:499 — `pytest-parallel-isolation:` job definition
- .github/workflows/ci.yml:500 — `name: Pytest Parallel Isolation` (used to find job ID via gh CLI)
- .github/workflows/ci.yml:584 — `name: Run tests with -n 4` (the key step whose log is the CI-01 (b) evidence)
- .github/workflows/ci.yml:590 — final `run:` line (`uv run pytest -n 4 -v --tb=short -m 'not perf'`)
- .github/workflows/ci.yml:593 — next job (`frontend-lint:`) — defines the upper bound of the job range

Workflow file: .github/workflows/ci.yml (workflow ID derivable via `gh workflow list --workflow=ci.yml`).

GitHub CLI commands (verified shape per CONTEXT.md Operator commands block):
- `git push origin main`
- `RUN_ID=$(gh run list --workflow=ci.yml --limit=1 --json databaseId --jq '.[0].databaseId')`
- `gh run watch $RUN_ID` — blocks until run completes
- `JOB_ID=$(gh run view $RUN_ID --json jobs --jq '.jobs[] | select(.name == "Pytest Parallel Isolation") | .databaseId')`
- `gh run view $RUN_ID --log --job=$JOB_ID > /tmp/v1022-1097-ci-live-verify.log`
- `git tag v1022 <CLOSE_SHA>`
- `git tag v1.5.7 <CLOSE_SHA>`
- `git push origin v1022 v1.5.7`

NOTE on job name: the `pytest-parallel-isolation:` key in ci.yml is the workflow-level job key; the `name: Pytest Parallel Isolation` (note the spaces and capitalization at line 500) is what `gh run view --json jobs` returns in the `.name` field. Filter by display name, not the workflow-key slug.

NOTE on auth: `gh` CLI must already be authenticated (`gh auth status` returns OK). If auth fails, the executor surfaces a checkpoint asking the operator to run `gh auth login` — this is the dynamic auth-gate pattern per planner skill checkpoints section, NOT a pre-planned task.

NOTE on close-gate SHA: Plan 01's commit SHA is the close-gate SHA used for tagging. Capture it BEFORE pushing via `CLOSE_SHA=$(git log -1 --format=%H)`. Pushing does not change the SHA. Tags are cut at this SHA AFTER the push lands AND AFTER CI-01 verifies GREEN.

Failure handling:
- If `pytest-parallel-isolation` is RED on first live run: do NOT cut tags. Per CONTEXT.md decisions "Tag cut decision tree" — the failure shape gets documented in 1097-02-SUMMARY.md and fed back into a PARA-01 iteration plan (Plan 1097-03 if narrow / v1023 spike if broad). REQUIREMENTS.md CI-01 + CLOSE-01 NOT flipped. The atomic-4-file commit shape changes to atomic-2-file (1097-01-CLOSE-GATE.md with the failure log + 1097-02-SUMMARY.md with the failure analysis) — no MILESTONES.md update, no REQUIREMENTS.md flip.

MILESTONES.md format (mirror v1021's entry, currently at line 3-19 — read this block BEFORE editing to match the heading + bulleted-list shape):
- Heading: `## v1022 Parallel-Test Cascade Closure + Hygiene Tail (Shipped: 2026-05-24)`
- Phases-completed line: `**Phases completed:** 4 phases, 5 plans, ~22 tasks` (verify exact counts via `git log --oneline --since "2026-05-24 00:00" | grep -E "(109[4-7])"`)
- Local tag: `**Local tag:** v1022 (commit <CLOSE_SHA>)`
- Public tag: `**Public tag:** v1.5.7 (at same commit)`
- Key accomplishments: 4-5 bullets covering PARA-01 + PARA-02 + HYG-01 + CI-01 + CLOSE-01 with the test-pin + line-number citations

REQUIREMENTS.md flip targets (verified at plan-time via `git grep -n`):
- Line 37: `- [ ] **CI-01**: Live-verify the pytest-parallel-isolation ...` → `- [x] **CI-01**: ...` (post-close: append `**Closed (Plan 1097-02):** <CLOSE_SHA> — GitHub Actions run <RUN_URL> shows pytest-parallel-isolation GREEN; log embedded in 1097-01-CLOSE-GATE.md CI-01 (f) section.`)
- Line 41: `- [ ] **CLOSE-01**: Close gate ...` → `- [x] **CLOSE-01**: ...` (post-close: append `**Closed (Plan 1097-02):** Tags v1022 + v1.5.7 cut at <CLOSE_SHA>; recorded in .planning/MILESTONES.md. All 7 acceptance criteria (a)-(g) satisfied.`)
- Line 78: `| CI-01 | Phase 1097 | Pending |` → `| CI-01 | Phase 1097 | Complete |`
- Line 79: `| CLOSE-01 | Phase 1097 | Pending |` → `| CLOSE-01 | Phase 1097 | Complete |`
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Operator confirmation before push</name>
  <decision>Whether to push v1022 commits to origin/main NOW so CI-01 can fire on real GitHub Actions infrastructure</decision>
  <context>
Phase 1097-02 is the only v1022 phase that requires a destructive-to-shared-state operation: `git push origin main`. Per CONTEXT.md decisions Push gate section + global CLAUDE.md "Executing actions with care" rule, this checkpoint pauses BEFORE the push fires so the operator can:

- Review the Plan 01 close-gate commit one last time (`git log -1 --stat`)
- Confirm the working tree is clean (no accidental staged-but-uncommitted changes)
- Confirm the local `main` is what they want on remote (run `git ls-remote origin main` to inspect remote HEAD; Plan 01's commit will land on top of it)
- Decide whether autonomous-mode execution continues OR they handle the push manually

If the operator picks Option B (manual push), they will run `git push origin main` themselves and then resume the autonomous workflow via `/gsd-autonomous --from 1097` once CI is firing. The executor halts here and surfaces a CHECKPOINT_RESUME marker.

If the operator picks Option A (autonomous continues), execution proceeds to Task 2 which performs the push.

Per CONTEXT.md: "Autonomous mode is active but `git push` is a destructive-to-shared-state action that warrants confirmation per global CLAUDE.md rule." No other v1022 task triggers a push; Plan 02 Task 4 also pushes tags but those follow the GREEN-CI gate so are not separately confirmed.
  </context>
  <options>
    <option id="push-now">
      <name>Option A — Push now, autonomous continues</name>
      <pros>Phase 1097 closes in one autonomous session; CI fires within ~30 seconds of push; tag-cut lands within ~15 min of CI-GREEN</pros>
      <cons>Operator not directly observing the push event; if push triggers an unexpected branch-protection rule or pre-receive hook failure, executor halts mid-flight and has to be resumed manually anyway</cons>
    </option>
    <option id="manual-push">
      <name>Option B — Stop here, operator pushes manually + resumes</name>
      <pros>Operator has full control over the push event; can verify remote state pre/post; can defer push to a low-traffic window</pros>
      <cons>Two-session handoff; `/gsd-autonomous --from 1097` must be re-invoked after push; small risk of session-state drift if operator forgets the resume invocation</cons>
    </option>
  </options>
  <resume-signal>Type `push-now` to continue autonomously OR `manual-push` to stop and resume later. If `manual-push`, executor halts and surfaces the resume command.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Push close-gate commit + capture run ID</name>
  <files>(no files modified locally; remote ref origin/main updated; /tmp/v1022-1097-run-meta.json captured)</files>
  <action>
Push Plan 01's close-gate commit to origin and capture the CI run metadata for Task 3's `gh run watch`.

(1) Capture the close-gate SHA BEFORE push (push does not change SHA, but capture-before-push removes any ambiguity): `CLOSE_SHA=$(git log -1 --format=%H)`. This SHA is the tag target for Task 4.

(2) `git push origin main`. Expect exit 0. If push fails (e.g., remote ahead, branch protection violation, pre-receive hook), surface the failure and halt — do NOT retry without operator review. Common failure shapes:
   - `! [rejected] main -> main (non-fast-forward)`: remote moved since last fetch. Run `git fetch origin && git status` to confirm shape, then ask operator whether to rebase or abort.
   - `remote: error: GH013: Repository rule violations found`: branch protection failure. Surface the rule violation and halt.

(3) Wait ~10 seconds for CI to register the push event (GitHub Actions has a ~5-30s lag between push receipt and run creation).

(4) Capture the run metadata: `RUN_META=$(gh run list --workflow=ci.yml --limit=1 --json databaseId,headSha,status,url,createdAt --jq '.[0]')`. Echo it AND persist to `/tmp/v1022-1097-run-meta.json` for Task 3.

(5) Verify `headSha` field matches `CLOSE_SHA` — the captured run MUST be the run for our just-pushed commit, not a stale prior run. If they differ, the GH API hasn't picked up our push yet; sleep 10s and re-query up to 5 times. If after 50s of total waits headSha still doesn't match, halt and surface the mismatch.

(6) Extract `databaseId` as `RUN_ID` for Task 3.

Auth note: if `gh run list` fails with auth error, surface a dynamic auth-gate checkpoint asking the operator to run `gh auth login`. Do NOT proceed past Task 2 without authenticated gh.
  </action>
  <verify>
    <automated>git rev-parse @{u} 2>/dev/null | grep -q "$(git log -1 --format=%H)" && test -s /tmp/v1022-1097-run-meta.json && python3 -c "import json; d=json.load(open('/tmp/v1022-1097-run-meta.json')); import subprocess; sha=subprocess.check_output(['git','log','-1','--format=%H']).decode().strip(); assert d['headSha']==sha, f'headSha {d[\"headSha\"]} != close-gate SHA {sha}'; print(f'RUN_ID={d[\"databaseId\"]} status={d[\"status\"]} url={d[\"url\"]}')"</automated>
  </verify>
  <done>
`git push origin main` exit 0. `git rev-parse @{u}` matches HEAD (origin/main updated). `/tmp/v1022-1097-run-meta.json` exists with `databaseId` + `headSha` + `status` (likely `queued` or `in_progress`) + `url` fields populated. `headSha` matches the close-gate SHA. `RUN_ID` captured for Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: gh run watch for pytest-parallel-isolation + extract job log + GREEN/RED gate</name>
  <files>
/tmp/v1022-1097-ci-live-verify.log (capture file; embedded into CLOSE-GATE.md in Task 5),
/tmp/v1022-1097-ci-full-run-status.json (capture file for SUMMARY.md context)
  </files>
  <action>
Block until CI run completes, then extract the `pytest-parallel-isolation` job log for embedding in CLOSE-GATE.md per CI-01 (b).

(1) `RUN_ID=$(python3 -c "import json; print(json.load(open('/tmp/v1022-1097-run-meta.json'))['databaseId'])")` then `gh run watch $RUN_ID --interval 10 --exit-status`. This blocks until the run reaches a terminal state (success/failure/cancelled). The `--exit-status` flag makes gh exit non-zero if the run failed. Runtime budget: 5-20 min (typical ci.yml run is ~10-15 min; pytest-parallel-isolation alone is ~5-8 min but it runs concurrently with other jobs). Set Bash timeout to 1800000 ms (30 min ceiling).

(2) If `gh run watch` exits non-zero (run failed): the FULL ci.yml run failed. Capture `gh run view $RUN_ID --json conclusion,jobs --jq '.jobs[] | select(.conclusion != "success") | {name, conclusion, url}'` to identify WHICH job(s) failed. If `pytest-parallel-isolation` is in the failure list, this is the CI-01 RED case — branch to step (4b). If another job failed but `pytest-parallel-isolation` succeeded, the CI-01 gate is GREEN — still proceed to step (3) but note in CLOSE-GATE.md that other jobs failed (this is a soft-FAIL of the broader CI run, but CI-01's acceptance is narrowly about `pytest-parallel-isolation`).

(3) Extract `pytest-parallel-isolation` job log:
   - `JOB_ID=$(gh run view $RUN_ID --json jobs --jq '.jobs[] | select(.name == "Pytest Parallel Isolation") | .databaseId')` (note: filter by display name `Pytest Parallel Isolation`, NOT workflow-key `pytest-parallel-isolation` — the JSON `.name` field is the display name from ci.yml line 500).
   - If `$JOB_ID` is empty, the job didn't run (likely `needs: changes` job determined no backend changes — but the close-gate commit IS test-infra-only, however it modifies `.planning/` which the `changes` filter at ci.yml:15 does NOT classify as backend. Inspect the `changes` job output to confirm whether `backend` flag fired. If `backend == false`, the gate skip is correct per the `if:` condition at ci.yml:502 (`backend == 'true' || alembic == 'true' || event_name == 'push'`) — note that `event_name == 'push'` ALWAYS fires on main push, so the gate SHOULD run regardless of file filters. If gate didn't run despite push, that's an unexpected behavior to surface and halt).
   - `gh run view $RUN_ID --log --job=$JOB_ID > /tmp/v1022-1097-ci-live-verify.log`.
   - Capture overall run status: `gh run view $RUN_ID --json conclusion,headBranch,headSha,event,jobs > /tmp/v1022-1097-ci-full-run-status.json`.

(4a) GREEN CI-01 case — `gh run watch --exit-status` exit 0 AND pytest-parallel-isolation conclusion is `success`:
   - Verify the log contains the expected `Run tests with -n 4` step output ending in pytest summary line (e.g., `passed in Xs` or similar `-n 4` summary shape).
   - Verify exit code in log is 0.
   - Proceed to Task 4 (tag cut).

(4b) RED CI-01 case — pytest-parallel-isolation conclusion is `failure` (or `timed_out` or `cancelled`):
   - DO NOT cut tags.
   - DO NOT flip CI-01 or CLOSE-01.
   - Document failure shape: extract last 100 lines of job log + identify which test(s) failed.
   - Per CONTEXT.md decisions Tag cut decision tree: "If CI-01 RED: feed failure back into a PARA-01 iteration plan."
   - Branch the rest of the plan: Task 4 is SKIPPED, Task 5 produces a smaller atomic-2-file commit (CLOSE-GATE.md with RED log + 1097-02-SUMMARY.md with failure analysis + recommended Plan 1097-03 outline). MILESTONES.md and REQUIREMENTS.md are NOT touched.
   - Surface a clear "CI-01 RED — Phase 1097 cannot close without iteration" message to the operator.

(5) Print run summary: URL, conclusion, pytest-parallel-isolation conclusion, runtime, log path. The operator should see at a glance whether Task 4 will proceed (GREEN) or halt (RED).
  </action>
  <verify>
    <automated>test -s /tmp/v1022-1097-ci-live-verify.log && python3 -c "import json; d=json.load(open('/tmp/v1022-1097-ci-full-run-status.json')); jobs=d['jobs']; pp=[j for j in jobs if j['name']=='Pytest Parallel Isolation']; assert len(pp)==1, 'pytest-parallel-isolation job not found'; print(f'pytest-parallel-isolation conclusion: {pp[0][\"conclusion\"]}'); assert pp[0]['conclusion']=='success', f'CI-01 RED: conclusion={pp[0][\"conclusion\"]} — do NOT proceed to Task 4'"</automated>
  </verify>
  <done>
`/tmp/v1022-1097-ci-live-verify.log` is non-empty and contains the `pytest-parallel-isolation` job log. `/tmp/v1022-1097-ci-full-run-status.json` has `pytest-parallel-isolation` job with `conclusion: success`. CI-01 (a)+(b) acceptance evidence captured. Ready for Task 4 tag cut. (If CI-01 RED: this task halts the plan; Task 4 is skipped; Task 5 produces a failure-shape commit instead.)
  </done>
</task>

<task type="auto">
  <name>Task 4: Cut tags v1022 + v1.5.7 at close-gate SHA + push to origin</name>
  <files>(no files modified locally; remote refs origin/v1022 + origin/v1.5.7 created)</files>
  <action>
ONLY EXECUTE if Task 3 completed in the GREEN branch (CI-01 (a) satisfied). If Task 3 hit the RED branch, this task is SKIPPED and Task 5 produces the failure-shape commit.

(1) Re-capture the close-gate SHA from Plan 01's commit (it has NOT changed since Task 2's capture, but re-confirm to be defensive): `CLOSE_SHA=$(git log -1 --format=%H)`. Verify this SHA matches the `headSha` in `/tmp/v1022-1097-run-meta.json` — if they differ, somehow HEAD moved between Task 2 and Task 4 (operator concurrent commit?), which is a critical inconsistency. Halt and surface.

(2) Verify the SHA exists and is the close-gate commit:
   - `git log -1 --format="%H %s" $CLOSE_SHA` → must show subject `docs(1097-01): CLOSE-01 close-gate baselines + CHANGELOG [1.5.7]`.

(3) Verify tags don't already exist (defensive — if a prior aborted Plan 02 run left dangling tags, they need cleanup first):
   - `git tag -l v1022 v1.5.7` → must be empty. If either tag exists locally, ask the operator whether to delete (`git tag -d v1022 v1.5.7`) and re-cut, or abort.

(4) Cut tags at the close-gate SHA:
   - `git tag v1022 $CLOSE_SHA`
   - `git tag v1.5.7 $CLOSE_SHA`

(5) Push tags to origin:
   - `git push origin v1022 v1.5.7` → expect exit 0 + two "[new tag]" lines in output. If either push fails (e.g., remote tag already exists), halt and surface — the remote-tag-collision case suggests an operator concurrently pushed a v1022 tag, which needs investigation before proceeding.

(6) Verify remote tags landed:
   - `git ls-remote origin refs/tags/v1022 refs/tags/v1.5.7` → must show both tags pointing at `$CLOSE_SHA`.

(7) Save the verified close-gate SHA + tag refs to `/tmp/v1022-1097-tag-refs.json` for Task 5's MILESTONES.md write:
   ```
   echo "{\"close_sha\": \"$CLOSE_SHA\", \"v1022_sha\": \"$(git rev-list -n 1 v1022)\", \"v1_5_7_sha\": \"$(git rev-list -n 1 v1.5.7)\", \"date\": \"2026-05-24\"}" > /tmp/v1022-1097-tag-refs.json
   ```

Reversibility note: tag pushes ARE reversible (`git push origin :refs/tags/<tag>` deletes the remote tag) but the operation triggers downstream consumers (GitHub release UI, MILESTONES.md citation, public-tag SemVer consumer assumption). Once pushed, treat as durable. If Task 5 fails and forces a Phase 1097 retry, the same tags can be re-cut at the SAME SHA without contention (idempotent at the same target).
  </action>
  <verify>
    <automated>git rev-list -n 1 v1022 2>/dev/null | grep -q "$(git log -1 --format=%H)" && git rev-list -n 1 v1.5.7 2>/dev/null | grep -q "$(git log -1 --format=%H)" && git ls-remote origin refs/tags/v1022 refs/tags/v1.5.7 | wc -l | grep -qx 2 && test -s /tmp/v1022-1097-tag-refs.json</automated>
  </verify>
  <done>
Tags `v1022` + `v1.5.7` exist locally pointing at Plan 01's close-gate SHA. Both tags pushed to origin and `git ls-remote` confirms. `/tmp/v1022-1097-tag-refs.json` has the close_sha + tag SHA references for Task 5. CLOSE-01 (g) acceptance criterion satisfied. Ready for Task 5 atomic-4-file commit.
  </done>
</task>

<task type="auto">
  <name>Task 5: Append CI-01 section to CLOSE-GATE.md + write MILESTONES.md v1022 entry + flip CI-01/CLOSE-01 in REQUIREMENTS.md + write SUMMARY.md + atomic-4-file commit</name>
  <files>
.planning/phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md,
.planning/MILESTONES.md,
.planning/REQUIREMENTS.md,
.planning/phases/1097-live-verify-close-gate/1097-02-SUMMARY.md
  </files>
  <action>
Four documents, ONE atomic commit. Per CONTEXT.md decisions "Atomic-N-file commit per plan" rule — Plan 02's commit modifies EXACTLY 4 files; no other files in the same commit.

(1) Append to 1097-01-CLOSE-GATE.md — add two new sections at the bottom (CI-01 (f) + CLOSE-01 (g)) that replace the "PENDING Plan 1097-02" stubs:

   - Section CI-01 (f) — CI-01 live-verify:
     - GitHub Actions run URL (from `/tmp/v1022-1097-run-meta.json` `.url` field)
     - Conclusion + runtime (from `/tmp/v1022-1097-ci-full-run-status.json`)
     - Embedded log block: extract the last ~50 lines of the `Run tests with -n 4` step from `/tmp/v1022-1097-ci-live-verify.log` (specifically the pytest summary tail; not the full 5000+ line job log). Wrap in a fenced code block in CLOSE-GATE.md.
     - Cross-reference: "Closes the v1020 Phase 1089 deferred operator action documented in `.planning/milestones/v1020-MILESTONE-AUDIT.md` Deferred Items table."

   - Section CLOSE-01 (g) — Tags cut:
     - Verbatim git tag output: `git show v1022 --no-patch --format=fuller | head -10` and same for v1.5.7
     - Close-gate SHA quoted
     - Cross-reference: "Recorded in `.planning/MILESTONES.md` v1022 entry."

   Use the Edit tool (one Edit call per section append) or Read-modify-Write — do NOT recreate the file from scratch (Plan 01's content must be preserved verbatim).

(2) Write MILESTONES.md v1022 entry — insert at the TOP of the file (before the current v1021 entry at line 3). Mirror v1021's heading + bulleted-list shape. Required content (read v1021's entry at lines 3-19 FIRST as the template):

   ```markdown
   ## v1022 Parallel-Test Cascade Closure + Hygiene Tail (Shipped: 2026-05-24)

   **Phases completed:** 4 phases (1094-1097), <N> plans, <M> tasks

   **Local tag:** `v1022` (commit `<CLOSE_SHA>`)
   **Public tag:** `v1.5.7` (at same commit)
   **Audit verdict:** PENDING (orchestrator runs /gsd:audit-milestone post-Phase-1097)

   **Key accomplishments:**

   1. **PARA-01 (Phase 1095-01) — Per-worker DB lifecycle cascade closed:** Wrapped `_init_tile_pool_for_tests` `asyncpg.create_pool` in existing `_run_with_too_many_clients_retry` envelope at 3 sibling sites (`test_tiles.py:152`, `test_embed_tokens.py:57`, `test_tile_signing.py:108`). `pytest -n auto` distinct failures: pre-fix 126-383 per run → 20/8/16 (Plan 1095-01) → 3/2/3 (Plan 1095-02) → 5/2/2 (Phase 1096 close). Regression pin `test_init_tile_pool_retries_on_transient_too_many_clients` at line 1144.

   2. **PARA-02 (Phase 1095-02) — WR-02 loop-starvation footgun closed:** Shape Y2 load-bearing rationale at `_invoke_sleep_in_sync_context` after Shape Y1 (`asyncio.run(asyncio.sleep(seconds))`) produced 658 `RuntimeError: asyncio.run() cannot be called from a running event loop` cascade failures at Plan 1095-02 Task 5 Run 1 (production caller path via SQLAlchemy `greenlet_spawn` has running loop in calling thread). Structural mitigation lives at PARA-01. Regression pin `test_engine_retry_yields_event_loop_during_backoff` at line 1253 (Shape Y2 static-text token assertion).

   3. **HYG-01 (Phase 1096-01) — Engine-retry envelope hygiene closed:** WR-03 narrowed `except Exception:` at `conftest.py:842` to `except (TypeError, AttributeError, InvalidRequestError):` (3 documented SQLAlchemy event-API failure shapes; tuple expanded from plan-spec by Rule 1 when MagicMock surfaced `InvalidRequestError`). WR-04 added `event.remove(...)` teardown in `_RetryingAsyncEngine.dispose()` at `conftest.py:958-962`. WR-01 + WR-01-1095 carry: 3 new pins at lines 1391 + 1557 + 1666 covering the `do_connect` event-handler retry path + fixture-layer parity for raw-asyncpg catch + non-transient propagation.

   4. **CI-01 (Phase 1097-02) — pytest-parallel-isolation gate live-verified:** First post-merge run of the v1020 Phase 1089 `pytest-parallel-isolation` CI gate at `.github/workflows/ci.yml:499-590` completed GREEN on real GitHub Actions infrastructure. Run URL: `<RUN_URL>`. Closes the v1020 deferred operator action.

   5. **CLOSE-01 (Phase 1097-02) — Milestone close-gate:** Sequential baseline preserved at 3060/3 OOS/38 (HARD INVARIANT 0 NEW failures); `-n 4` preserved at 3057/4-7/38; `-n auto` 3-run distinct deterministic ≤30 with 0 ICN frames. Docker stack 5-services-healthy + `/api/health/` 200. CHANGELOG `[1.5.7]` block lists all closures with test-pin + line-number citations.

   **Test invariants at close:**
   - Sequential: 3060 passed / 3 OOS / 38 skipped (HARD INVARIANT preserved)
   - `-n 4`: 3057 passed / 4-7 OOS+flake / 38 skipped (HARD INVARIANT preserved)
   - `-n auto` 3-run distinct: <run1>/<run2>/<run3> deterministic ≤30 with 0 ICN frames (PARA-01 acceptance gate preserved)

   **Carry-forwards to v1023:** TBD (deferred to milestone audit phase — orchestrator's /gsd:audit-milestone identifies them).

   **Archive:** `.planning/phases/1094-cascade-spike/` + `.planning/phases/1095-cascade-fix-wr-02-closure/` + `.planning/phases/1096-hygiene-tail/` + `.planning/phases/1097-live-verify-close-gate/` (orchestrator's /gsd:cleanup-milestone moves these to `.planning/milestones/v1022-phases/` post-audit).

   ---
   ```

   Fill the `<CLOSE_SHA>`, `<N>`, `<M>`, `<RUN_URL>`, `<run1>/<run2>/<run3>` placeholders with actual captured values. Compute plan/task counts from `git log --since "2026-05-24 00:00" --oneline | grep -cE "(109[4-7])"`.

(3) Flip REQUIREMENTS.md — exactly 4 line-level edits via Edit tool:

   - Line 37 (`- [ ] **CI-01**:` → `- [x] **CI-01**:`) PLUS append closure note: `**Closed (Plan 1097-02):** <CLOSE_SHA> — GitHub Actions run <RUN_URL> shows pytest-parallel-isolation GREEN; log embedded in 1097-01-CLOSE-GATE.md CI-01 (f) section.`
   - Line 41 (`- [ ] **CLOSE-01**:` → `- [x] **CLOSE-01**:`) PLUS append closure note: `**Closed (Plan 1097-02):** Tags v1022 + v1.5.7 cut at <CLOSE_SHA>; recorded in .planning/MILESTONES.md. All 7 acceptance criteria (a)-(g) satisfied.`
   - Line 78 (`| CI-01 | Phase 1097 | Pending |` → `| CI-01 | Phase 1097 | Complete |`)
   - Line 79 (`| CLOSE-01 | Phase 1097 | Pending |` → `| CLOSE-01 | Phase 1097 | Complete |`)

   IMPORTANT: read REQUIREMENTS.md FIRST (Read tool) before Edit calls — line numbers may have drifted by ±1 if other edits landed in the interim. Use `git grep -n '\\[ \\] \\*\\*CI-01\\*\\*' .planning/REQUIREMENTS.md` to find current line.

(4) Write 1097-02-SUMMARY.md — standard template with Plan 02 + Phase 1097 close evidence:

   ```markdown
   ---
   phase: 1097-live-verify-close-gate
   plan: 02
   completed: 2026-05-24
   status: success
   requirements_completed: [CI-01, CLOSE-01]
   files_modified:
     - .planning/phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md
     - .planning/MILESTONES.md
     - .planning/REQUIREMENTS.md
     - .planning/phases/1097-live-verify-close-gate/1097-02-SUMMARY.md
   ---

   # Plan 1097-02: CI-01 Live-Verify + Tag Cut — SUMMARY

   ## Verification Gate Matrix

   | Gate | Expected | Actual | Status |
   |------|----------|--------|--------|
   | Task 1: operator confirmation | Option A (push-now) OR Option B (manual+resume) | <fill> | PASS |
   | Task 2: git push origin main | exit 0 + headSha match | <fill> | PASS |
   | Task 3: gh run watch | pytest-parallel-isolation conclusion=success | <fill> | PASS |
   | Task 3: log captured | /tmp/v1022-1097-ci-live-verify.log non-empty | <fill> | PASS |
   | Task 4: tags cut + pushed | v1022 + v1.5.7 at close-gate SHA on origin | <fill> | PASS |
   | Task 5: CLOSE-GATE.md (f)+(g) appended | both sections present | <fill> | PASS |
   | Task 5: MILESTONES.md v1022 entry | inserted at top of file | <fill> | PASS |
   | Task 5: REQUIREMENTS.md CI-01 flipped | `[x]` + Complete | <fill> | PASS |
   | Task 5: REQUIREMENTS.md CLOSE-01 flipped | `[x]` + Complete | <fill> | PASS |
   | Task 5: atomic-4-file commit | exactly 4 files in commit | <fill> | PASS |

   ## CI-01 Live-Verify Evidence

   - Run URL: <RUN_URL>
   - Run conclusion: success
   - pytest-parallel-isolation job conclusion: success
   - Runtime: <fill>
   - Log path: /tmp/v1022-1097-ci-live-verify.log (embedded in 1097-01-CLOSE-GATE.md (f) section)

   ## CLOSE-01 Tag-Cut Evidence

   - Close-gate SHA: <CLOSE_SHA>
   - Local tag v1022: <git show v1022 first line>
   - Public tag v1.5.7: <git show v1.5.7 first line>
   - Origin push: 2 [new tag] entries confirmed via git ls-remote

   ## Deviations from Plan

   <list any task-spec deviations; ideally none>

   ## Phase 1097 Close

   Phase 1097 = COMPLETE. v1022 milestone = SHIPPED at <CLOSE_SHA> with tags v1022 + v1.5.7.

   Next: orchestrator runs /gsd:audit-milestone v1022 → /gsd:complete-milestone v1022 → /gsd:cleanup-milestone v1022. Phase 1097 directory becomes `.planning/milestones/v1022-phases/1097-live-verify-close-gate/` post-cleanup.
   ```

(5) Atomic-4-file commit — stage ONLY these 4 files; no other files in the same commit:

   ```
   git add .planning/phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md \
     .planning/MILESTONES.md \
     .planning/REQUIREMENTS.md \
     .planning/phases/1097-live-verify-close-gate/1097-02-SUMMARY.md
   git commit -m "feat(1097-02): CI-01 live-verify green + tags v1022/v1.5.7 cut"
   ```

(6) Push the commit: `git push origin main`. This is the SECOND push of Plan 02 (Task 2's push was the Plan 01 close-gate commit; Task 5's push is the Plan 02 milestone-close commit). It will fire ANOTHER CI run, but that run is post-tag and is not part of CI-01's acceptance. The tags v1022 + v1.5.7 remain pinned at the Plan 01 SHA (NOT the Plan 02 SHA) per CLOSE-01 (g)'s literal acceptance criterion: "tags cut at the close-gate commit SHA" (the close-gate commit = Plan 01).

CRITICAL — atomicity rule: REQUIREMENTS.md CI-01 + CLOSE-01 flips MUST land in the SAME commit as SUMMARY.md per v1019 TD-13 `requirements_traceability_flip` rule. Splitting CI-01 flip into its own commit and CLOSE-01 flip into another is a process violation. ALL 4 files in 1 commit.
  </action>
  <verify>
    <automated>git log -1 --stat --format="%H %s" | head -1 | grep -E "feat\(1097-02\): CI-01 live-verify green \+ tags v1022/v1\.5\.7 cut" && git log -1 --name-only --format="" | grep -cE "(1097-01-CLOSE-GATE\.md|MILESTONES\.md|REQUIREMENTS\.md|1097-02-SUMMARY\.md)" | grep -qx 4 && grep -qE "^\- \[x\] \*\*CI-01\*\*" .planning/REQUIREMENTS.md && grep -qE "^\- \[x\] \*\*CLOSE-01\*\*" .planning/REQUIREMENTS.md && grep -qE "\| CI-01 \| Phase 1097 \| Complete \|" .planning/REQUIREMENTS.md && grep -qE "\| CLOSE-01 \| Phase 1097 \| Complete \|" .planning/REQUIREMENTS.md && grep -q "## v1022 Parallel-Test Cascade Closure" .planning/MILESTONES.md && grep -q "## CLOSE-01 (f) — CI-01 live-verify" .planning/phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md && grep -q "## CLOSE-01 (g) — Tags cut" .planning/phases/1097-live-verify-close-gate/1097-01-CLOSE-GATE.md && grep -qE "requirements_completed: \[CI-01, CLOSE-01\]" .planning/phases/1097-live-verify-close-gate/1097-02-SUMMARY.md</automated>
  </verify>
  <done>
Atomic-4-file commit landed with message `feat(1097-02): CI-01 live-verify green + tags v1022/v1.5.7 cut`. Exactly 4 files modified (1097-01-CLOSE-GATE.md + MILESTONES.md + REQUIREMENTS.md + 1097-02-SUMMARY.md); ZERO other files. REQUIREMENTS.md CI-01 + CLOSE-01 both flipped to `[x]` + Complete (4 line-edits total). MILESTONES.md has v1022 entry at top. CLOSE-GATE.md has both (f) and (g) sections appended. SUMMARY.md frontmatter has `requirements_completed: [CI-01, CLOSE-01]`. Commit pushed to origin/main. Phase 1097 = COMPLETE. v1022 = SHIPPED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local git → origin/main | `git push origin main` mutates shared remote ref (destructive-to-shared-state) |
| local git → origin tags | `git push origin v1022 v1.5.7` creates immutable public refs (SemVer consumer assumption) |
| gh CLI → GitHub API | gh CLI authenticates to GitHub API; runs `gh run watch` + `gh run view`; reads job logs |
| ci.yml → real GitHub Actions runner | First post-merge live execution of v1022 test-infra changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1097-05 | Spoofing | gh CLI auth token | accept | `gh auth status` pre-check; auth-gate dynamic checkpoint if token expires; user's existing gh auth surface (not Plan 02 concern) |
| T-1097-06 | Tampering | Pushed close-gate commit on origin/main | mitigate | Operator confirmation gate (Task 1) BEFORE push; SHA capture pre-push for tag-cut idempotency; immutable git history |
| T-1097-07 | Repudiation | Tags v1022 + v1.5.7 | mitigate | Tags cut at captured SHA, ls-remote verification, recorded in MILESTONES.md with SHA citation |
| T-1097-08 | Information Disclosure | CI job log embedded in CLOSE-GATE.md | accept | Log is GitHub-public for the public repo path; contains test output only (no secrets per ci.yml env block — secrets are `JWT_SECRET_KEY: test-secret-key-for-ci-padding-32chars` test-only); embedding the relevant ~50 lines is intentional CI-01 (b) evidence |
| T-1097-09 | Denial of Service | Pushing main triggers full ci.yml run (~10-15 min runner-time + cost) | accept | Phase 1097 ships v1022; one CI run is the intended cost; future close-gates can amortize |
| T-1097-10 | Elevation of Privilege | gh CLI permissions on repo | accept | Operator's existing gh permissions; if push fails on branch protection, Task 2 halts and surfaces — no privilege escalation attempted |
| T-1097-SC | Tampering | npm/pip/cargo installs | accept | NO package installs in Plan 02 — git/gh CLI + .md edits only |

No package installs in Plan 02 → no Package Legitimacy Audit required.
</threat_model>

<verification>
Plan 02 verification gates (all MUST PASS for plan to be COMPLETE):

1. Task 1 operator confirmation captured (push-now or manual-push selected)
2. `git push origin main` exit 0 (Task 2) AND `origin/main == HEAD` post-push
3. CI run captured with `headSha == CLOSE_SHA` (Task 2) — defends against picking up a stale run
4. `gh run watch` returns success AND pytest-parallel-isolation conclusion `success` (Task 3) — CI-01 (a)+(b)
5. Tags v1022 + v1.5.7 cut at close-gate SHA AND pushed to origin (Task 4) — CLOSE-01 (g)
6. CLOSE-GATE.md (f) + (g) sections appended (Task 5)
7. MILESTONES.md has v1022 entry at top with all 5 key-accomplishments bullets (Task 5)
8. REQUIREMENTS.md flipped: CI-01 line 37 `[x]`, CLOSE-01 line 41 `[x]`, Traceability lines 78+79 Complete (Task 5)
9. SUMMARY.md has `requirements_completed: [CI-01, CLOSE-01]` in frontmatter (Task 5)
10. Atomic-4-file commit landed (Task 5) — CONTEXT.md atomic-N rule + v1019 TD-13 traceability_flip rule
11. Plan 02 commit pushed to origin/main (Task 5)

Failure-branch (CI-01 RED) gates:
- Task 3 halts execution; Task 4 SKIPPED; Task 5 produces atomic-2-file commit (CLOSE-GATE.md failure log + SUMMARY.md failure analysis); REQUIREMENTS.md NOT flipped; MILESTONES.md NOT updated; tags NOT cut. Phase 1097 carries forward to a follow-up PARA-01 iteration plan.
</verification>

<success_criteria>
Plan 1097-02 succeeds when:
- Task 1 operator confirmation captured
- Plan 01 close-gate commit pushed to origin
- pytest-parallel-isolation CI gate completes GREEN on first post-merge run (CI-01 (a)+(b))
- Tags v1022 (local) + v1.5.7 (public) cut at close-gate SHA AND pushed (CLOSE-01 (g))
- 1097-01-CLOSE-GATE.md sections (f) + (g) appended with verbatim log + tag refs (CI-01 (b) + CLOSE-01 (f)/(g))
- MILESTONES.md v1022 entry written at top of file (CLOSE-01 (g))
- REQUIREMENTS.md CI-01 + CLOSE-01 flipped to [x] + Complete in SAME commit as SUMMARY.md (v1019 TD-13 traceability_flip)
- Atomic-4-file commit landed AND pushed to origin
- Phase 1097 = COMPLETE; v1022 milestone = SHIPPED
- Orchestrator can proceed to /gsd:audit-milestone v1022

Plan 1097-02 fails (CI-01 RED branch) when:
- pytest-parallel-isolation conclusion is failure/timed_out/cancelled
- Task 4 SKIPPED, Task 5 produces failure-shape atomic-2-file commit
- Phase 1097 NOT closed; carry-forward to Plan 1097-03 iteration
</success_criteria>

<output>
Create `.planning/phases/1097-live-verify-close-gate/1097-02-SUMMARY.md` when done. Commit message: `feat(1097-02): CI-01 live-verify green + tags v1022/v1.5.7 cut`. Push the commit to origin/main. Phase 1097 = COMPLETE. v1022 milestone = SHIPPED at the close-gate SHA with tags `v1022` (local) + `v1.5.7` (public).
</output>
