---
phase: 216-features-and-quickstart-pages
plan: 04
type: execute
wave: 3
depends_on: [216-02]
files_modified:
  - getgeolens.com/src/components/previews/RasterVrtPreview.astro
  - getgeolens.com/src/components/previews/AiChatPreview.astro
  - getgeolens.com/src/components/previews/RbacPreview.astro
  - getgeolens.com/src/pages/preview-test.astro
autonomous: true
requirements: [FEAT-02]
tags: [astro, picture, astro-assets, new-preview-components]

must_haves:
  truths:
    - "RasterVrtPreview.astro renders a `<Picture>` of raster-vrt.png inside BrowserFrame with url='app.geolens.io/datasets/...'"
    - "AiChatPreview.astro renders a `<Picture>` of ai-chat.png inside BrowserFrame with url='app.geolens.io/maps/...' (D-14 correct route)"
    - "RbacPreview.astro renders a `<Picture>` of rbac.png inside BrowserFrame with url='app.geolens.io/admin/users'"
    - "All 3 new components use the import form of astro:assets (never string paths)"
    - "All 3 new components include descriptive alt text"
    - "preview-test.astro imports and displays all 3 new components so a reviewer can visually QA them"
    - "`npm run build` succeeds and dist/_astro/ contains AVIF+WebP for raster-vrt, ai-chat, rbac screenshots"
    - "`npm run preview` + grep on /preview-test HTML output finds all 3 new component render results"
  artifacts:
    - path: "getgeolens.com/src/components/previews/RasterVrtPreview.astro"
      provides: "Raster/VRT capability preview — renders raster-vrt.png inside BrowserFrame"
      contains: "import { Picture } from 'astro:assets'"
    - path: "getgeolens.com/src/components/previews/AiChatPreview.astro"
      provides: "AI Chat capability preview — renders ai-chat.png, URL honest-matches /maps/:id per D-14"
      contains: "app.geolens.io/maps/"
    - path: "getgeolens.com/src/components/previews/RbacPreview.astro"
      provides: "RBAC capability preview — renders rbac.png from the /admin/users page capture"
      contains: "/admin/users"
    - path: "getgeolens.com/src/pages/preview-test.astro"
      provides: "Visual QA page listing all 6 preview components (3 retrofitted + 3 new)"
      contains: "RasterVrtPreview"
  key_links:
    - from: "RasterVrtPreview.astro"
      to: "src/assets/screenshots/raster-vrt.png"
      via: "astro:assets import"
      pattern: "import.*assets/screenshots/raster-vrt\\.png"
    - from: "AiChatPreview.astro"
      to: "src/assets/screenshots/ai-chat.png"
      via: "astro:assets import"
      pattern: "import.*assets/screenshots/ai-chat\\.png"
    - from: "RbacPreview.astro"
      to: "src/assets/screenshots/rbac.png"
      via: "astro:assets import"
      pattern: "import.*assets/screenshots/rbac\\.png"
    - from: "preview-test.astro"
      to: "3 new preview components"
      via: "Astro component imports"
      pattern: "import Raster.*import AiChat.*import Rbac"
---

<objective>
Create 3 net-new preview components for the Raster/VRT, AI Chat, and RBAC capabilities that Phase 214 did not include. Each follows the same `<BrowserFrame> + <Picture>` pattern as the retrofitted Phase 214 components in Plan 03. Update preview-test.astro so a human reviewer can visually QA all 6 preview components on a single page before Plan 05 wires them into /features.

Purpose: Complete the preview inventory required by the `/features` page (6 capabilities × 1 preview each). Plans 03 and 04 together produce the full set of 6 preview components.

Output:
- 3 new .astro files in src/components/previews/ (~25 lines each)
- preview-test.astro updated to import + display all 3 new components alongside the 3 existing (retrofitted) ones
- `npm run build` produces AVIF+WebP for the 3 new screenshots
- `/preview-test` route renders all 6 previews without layout shift or broken imports
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/216-features-and-quickstart-pages/216-CONTEXT.md
@.planning/phases/216-features-and-quickstart-pages/216-RESEARCH.md
@.planning/phases/216-features-and-quickstart-pages/216-02-SUMMARY.md

# BrowserFrame — used as-is
@/Users/ishiland/Code/getgeolens.com/src/components/previews/BrowserFrame.astro

# Existing preview-test page (will be expanded)
@/Users/ishiland/Code/getgeolens.com/src/pages/preview-test.astro

<interfaces>
<!-- Target shape for all 3 new preview components. Identical pattern to Plan 03. -->

```astro
--- <!-- astro frontmatter fence -->
import BrowserFrame from './BrowserFrame.astro';
import { Picture } from 'astro:assets';
import screenshot from '../../assets/screenshots/{capability}.png';
--- <!-- astro frontmatter fence -->

<BrowserFrame url="app.geolens.io/{real-route}" class="w-full">
  <Picture
    src={screenshot}
    formats={['avif', 'webp']}
    alt="{descriptive alt text}"
    widths={[448, 896]}
    sizes="(max-width: 640px) 100vw, 448px"
    loading="lazy"
    class="w-full block"
  />
</BrowserFrame>
```

CRITICAL details:
- Import form ONLY — never string paths (research Pitfall 1)
- `widths={[448, 896]}` — matches the 448px BrowserFrame + 2x retina
- `class="w-full block"` — prevents inline whitespace gap (research Pattern 1)
- BrowserFrame `class="w-full"` — carries forward Phase 215 Plan 04 responsive fix
- Alt text: DESCRIPTIVE, not generic — describe the captured content (A11Y carry-forward)

Per-component specs:

| Component | Filename | Screenshot import | BrowserFrame URL | Alt text template (executor tailors to actual capture) |
|-----------|----------|-------------------|------------------|--------------------------------------------------------|
| RasterVrtPreview.astro | raster-vrt.png | `../../assets/screenshots/raster-vrt.png` | `app.geolens.io/datasets/gebco-bathymetry` | "GeoLens dataset detail page for a GEBCO bathymetry raster showing raster metadata, type badge, and colormap preview" |
| AiChatPreview.astro | ai-chat.png | `../../assets/screenshots/ai-chat.png` | `app.geolens.io/maps/demo-map` | If full conversation: "GeoLens AI chat panel inside the Map Builder responding to a query about aquifer datasets with a dataset card and map context" — If empty-panel (per D-15): "GeoLens AI chat panel inside the Map Builder ready to accept natural language queries about map data" |
| RbacPreview.astro | rbac.png | `../../assets/screenshots/rbac.png` | `app.geolens.io/admin/users` | "GeoLens admin users page showing the user list with role assignments, approval status, and per-user actions" |

Why these URLs (per CONTEXT.md D-01 and research Q2):
- Raster/VRT: real route is `/datasets/:id` — use a seeded raster ID slug (e.g., `gebco-bathymetry`)
- AI Chat: CONTEXT.md D-01 listed `app.geolens.io/chat` but research Pitfall 5 + D-14 confirm NO standalone /chat route exists. The ChatPanel lives inside `/maps/:id`. The BrowserFrame URL pill MUST reflect this (honest marketing).
- RBAC: real route is `/admin/users` per research Q2 confirmation via AdminUsersPage.tsx

Alt text for AI Chat — D-14/D-15 conditional:
Read 216-02-SUMMARY.md to determine which variant was captured. The summary documents whether ai-chat.png is the full-conversation variant (D-14) or the empty-panel fallback (D-15). The alt text should match what the reader actually sees. Do not write alt text that claims "responding to a query" if the panel is empty.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 3 new preview components</name>
  <files>
    getgeolens.com/src/components/previews/RasterVrtPreview.astro,
    getgeolens.com/src/components/previews/AiChatPreview.astro,
    getgeolens.com/src/components/previews/RbacPreview.astro
  </files>
  <action>
    Create 3 new .astro files in getgeolens.com/src/components/previews/, each ~25 lines. All follow the retrofit pattern from Plan 03 exactly — the only differences are the import path, URL prop, and alt text.

    File 1: RasterVrtPreview.astro
    ```astro
    ---
    import BrowserFrame from './BrowserFrame.astro';
    import { Picture } from 'astro:assets';
    import rasterVrtScreenshot from '../../assets/screenshots/raster-vrt.png';
    ---

    <BrowserFrame url="app.geolens.io/datasets/gebco-bathymetry" class="w-full">
      <Picture
        src={rasterVrtScreenshot}
        formats={['avif', 'webp']}
        alt="GeoLens dataset detail page for a GEBCO bathymetry raster showing raster metadata, type badge, and colormap preview"
        widths={[448, 896]}
        sizes="(max-width: 640px) 100vw, 448px"
        loading="lazy"
        class="w-full block"
      />
    </BrowserFrame>
    ```

    File 2: AiChatPreview.astro — CRITICAL: URL must reflect the real route per D-14 + research Pitfall 5
    ```astro
    ---
    import BrowserFrame from './BrowserFrame.astro';
    import { Picture } from 'astro:assets';
    import aiChatScreenshot from '../../assets/screenshots/ai-chat.png';
    ---

    <BrowserFrame url="app.geolens.io/maps/demo-map" class="w-full">
      <Picture
        src={aiChatScreenshot}
        formats={['avif', 'webp']}
        alt="{READ 216-02-SUMMARY.md for variant — use one of:}
          full conversation: 'GeoLens AI chat panel inside the Map Builder responding to a natural language query about aquifer datasets'
          empty panel:       'GeoLens AI chat panel inside the Map Builder ready to accept natural language queries about map data'"
        widths={[448, 896]}
        sizes="(max-width: 640px) 100vw, 448px"
        loading="lazy"
        class="w-full block"
      />
    </BrowserFrame>
    ```
    Executor must read 216-02-SUMMARY.md before writing the alt text. The alt text is NOT a placeholder with a ternary — it's a single descriptive string, chosen based on the captured variant.

    Do NOT write the URL as `app.geolens.io/chat` — that route does not exist (CONTEXT.md D-01 had this wrong, research Pitfall 5 corrects it). The real ChatPanel lives inside the Map Builder.

    File 3: RbacPreview.astro
    ```astro
    ---
    import BrowserFrame from './BrowserFrame.astro';
    import { Picture } from 'astro:assets';
    import rbacScreenshot from '../../assets/screenshots/rbac.png';
    ---

    <BrowserFrame url="app.geolens.io/admin/users" class="w-full">
      <Picture
        src={rbacScreenshot}
        formats={['avif', 'webp']}
        alt="GeoLens admin users page showing the user list with role assignments, approval status, and per-user actions"
        widths={[448, 896]}
        sizes="(max-width: 640px) 100vw, 448px"
        loading="lazy"
        class="w-full block"
      />
    </BrowserFrame>
    ```

    For all 3 files:
    - NO `min-height` styles (Pitfall 4)
    - NO wrapper divs between BrowserFrame and Picture (Pitfall 3 — aria-hidden propagation)
    - Component is the outermost element (no extra div)
    - File is self-contained: no external props required

    Verify after writing:
    ```bash
    grep -c "import { Picture }" src/components/previews/RasterVrtPreview.astro src/components/previews/AiChatPreview.astro src/components/previews/RbacPreview.astro
    ```
    Should return 3 lines each showing `:1`.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com && for f in RasterVrtPreview AiChatPreview RbacPreview; do test -f "src/components/previews/$f.astro" || { echo "MISSING: $f"; exit 1; }; grep -q "import { Picture } from 'astro:assets'" "src/components/previews/$f.astro" || { echo "NO PICTURE IMPORT: $f"; exit 1; }; grep -q "formats={\['avif', 'webp'\]}" "src/components/previews/$f.astro" || { echo "NO FORMATS: $f"; exit 1; }; grep -q "alt=" "src/components/previews/$f.astro" || { echo "NO ALT: $f"; exit 1; }; ! grep -q "min-height" "src/components/previews/$f.astro" || { echo "HAS MIN-HEIGHT: $f"; exit 1; }; done && grep -q "from '../../assets/screenshots/raster-vrt.png'" src/components/previews/RasterVrtPreview.astro && grep -q "from '../../assets/screenshots/ai-chat.png'" src/components/previews/AiChatPreview.astro && grep -q "from '../../assets/screenshots/rbac.png'" src/components/previews/RbacPreview.astro && grep -q "/maps/" src/components/previews/AiChatPreview.astro && ! grep -q "/chat\"" src/components/previews/AiChatPreview.astro && grep -q "/admin/users" src/components/previews/RbacPreview.astro && npm run check 2>&1 | tail -5 && echo "TASK 1 CHECKS PASS"</automated>
  </verify>
  <done>
    - RasterVrtPreview.astro, AiChatPreview.astro, RbacPreview.astro all exist
    - Each imports `{ Picture }` from `astro:assets`
    - Each imports its respective PNG via the import form (not string path)
    - Each has `formats={['avif', 'webp']}` and descriptive alt text
    - AiChatPreview uses `/maps/` in the URL (NOT `/chat`) — honest matching per D-14 + research
    - RbacPreview uses `/admin/users` in the URL
    - No file contains `min-height` styles
    - `astro check` passes with zero errors
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Expand preview-test.astro to include all 6 previews + build verification</name>
  <files>
    getgeolens.com/src/pages/preview-test.astro
  </files>
  <action>
    Update getgeolens.com/src/pages/preview-test.astro to import and display all 6 preview components (3 retrofitted in Plan 03 + 3 new in this plan). This page is the reviewer's visual QA surface — it should render all 6 previews in order with clear labels.

    Current state (Phase 214): imports SearchPreview, MapBuilderPreview, DatasetDetailPreview — displays them in 3 sections with h2 labels.

    Target state (Phase 216): all 6 components displayed in FEAT-01 capability order per D-03:
    1. Search — SearchPreview (retrofitted in Plan 03)
    2. Map Builder — MapBuilderPreview (retrofitted in Plan 03)
    3. Data Ingestion — DatasetDetailPreview (retrofitted in Plan 03, repurposed for Data Ingestion)
    4. Raster/VRT — RasterVrtPreview (new, Plan 04)
    5. AI Chat — AiChatPreview (new, Plan 04)
    6. RBAC — RbacPreview (new, Plan 04)

    Keep the existing page structure (SiteLayout + max-w-3xl + space-y-16). Each section has an h2 label with the capability name and the component in a wrapper div that applies the same `w-full max-w-md mx-auto` cap used on the homepage sections (matches Phase 215 Plan 04 responsive container pattern).

    Example updated preview-test.astro frontmatter:
    ```astro
    ---
    import SiteLayout from '../components/layout/SiteLayout.astro';
    import SearchPreview from '../components/previews/SearchPreview.astro';
    import MapBuilderPreview from '../components/previews/MapBuilderPreview.astro';
    import DatasetDetailPreview from '../components/previews/DatasetDetailPreview.astro';
    import RasterVrtPreview from '../components/previews/RasterVrtPreview.astro';
    import AiChatPreview from '../components/previews/AiChatPreview.astro';
    import RbacPreview from '../components/previews/RbacPreview.astro';
    ---
    ```

    Each section in the body:
    ```astro
    <section class="space-y-4">
      <h2 class="text-lg font-semibold" style="color: var(--foreground);">1. Search — SearchPreview</h2>
      <div class="w-full max-w-md mx-auto">
        <SearchPreview />
      </div>
    </section>
    ```

    Repeat for all 6 capabilities. Keep the existing page-level intro h1 and description. Add a line to the description noting "6 previews — all real screenshots via `<Picture>` from astro:assets."

    Update the h1 wording if it currently says "Phase 214" — make it say "Phase 216 — 6 capability previews".

    Remove the ASSET-01/02/03 labels from the old Phase 214 version — they don't match the new FEAT capability framing.

    Build verification:
    ```
    cd /Users/ishiland/Code/getgeolens.com
    npm run build
    ```
    Expected: exit 0. Verify `dist/preview-test/index.html` exists and contains references to all 6 previews:
    ```bash
    for name in search map-builder data-ingestion raster-vrt ai-chat rbac; do
      grep -q "$name\." dist/preview-test/index.html || echo "MISSING: $name in preview-test HTML"
    done
    ```
    Each of the 6 capability screenshot names should appear in the built HTML (as part of the generated AVIF/WebP filename references from Astro's image pipeline).

    Preview smoke check:
    ```
    npm run preview &
    sleep 3
    curl -sSf http://localhost:4321/preview-test/ > /tmp/preview.html
    kill %1
    grep -c '<picture>' /tmp/preview.html
    ```
    Expected: 6 (one `<picture>` element per preview). If fewer, diagnose which components failed to render.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com && grep -q "import SearchPreview" src/pages/preview-test.astro && grep -q "import RasterVrtPreview" src/pages/preview-test.astro && grep -q "import AiChatPreview" src/pages/preview-test.astro && grep -q "import RbacPreview" src/pages/preview-test.astro && npm run build 2>&1 | tail -10 && test -f dist/preview-test/index.html && for name in search map-builder data-ingestion raster-vrt ai-chat rbac; do grep -q "$name" dist/preview-test/index.html || { echo "MISSING in preview-test HTML: $name"; exit 1; }; done && PC=$(grep -c "<picture>" dist/preview-test/index.html) && test "$PC" -ge 6 && echo "ALL 6 PREVIEWS RENDER IN preview-test BUILD OUTPUT"</automated>
  </verify>
  <done>
    - preview-test.astro imports all 6 preview components
    - preview-test.astro displays all 6 in FEAT-01 capability order
    - `npm run build` exits 0
    - dist/preview-test/index.html contains references to all 6 screenshot filenames (via Astro's generated AVIF/WebP srcset)
    - Built HTML contains at least 6 `<picture>` elements (one per preview)
    - Page can be smoke-tested via `npm run preview` + curl without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Astro build → image pipeline | Same as Plan 03 — imported PNGs processed at build, hashed output in dist/_astro/ |
| Marketing honesty | AI chat URL pill on the BrowserFrame must not mislead evaluators |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-216-04-01 | Tampering | String-path import regression | mitigate | Same as Plan 03 — verify grep checks for the import form. TypeScript `src: ImageMetadata` type also catches string paths at build time. |
| T-216-04-02 | Spoofing | BrowserFrame URL showing /chat (which doesn't exist) | mitigate | AiChatPreview.astro is explicitly verified to use `/maps/` in the URL prop and NOT `/chat`. Research Pitfall 5 + D-14 mandate this. Task verify greps for both strings. |
| T-216-04-03 | Information Disclosure | Alt text describing a conversation that doesn't exist | mitigate | Executor must read 216-02-SUMMARY.md to determine the AI chat variant (D-14 full conversation or D-15 empty panel) and write alt text that matches reality. If the committed screenshot is empty-panel, alt text must say "ready to accept queries" not "responding to a query". |
| T-216-04-04 | Elevation of Privilege | New preview component as XSS vector | accept | Astro .astro files are compiled at build time. No user input flows into preview components. Alt text is static author-controlled text. |
| T-216-04-05 | Denial of Service | Preview test page bloats with 6 full-size images | mitigate | All images use `loading="lazy"` and Astro's responsive srcset. Preview test page is internal (not linked from public nav) so traffic is near-zero. |

Primary risk mitigated: T-216-04-03 (marketing honesty about AI chat state). The verify step requires the executor to read the Plan 02 summary before writing alt text, which forces the "does the alt text match what the user sees?" check.
</threat_model>

<verification>
Phase-level verification for this plan:

1. **File existence check**
   - `test -f src/components/previews/RasterVrtPreview.astro` → exists
   - `test -f src/components/previews/AiChatPreview.astro` → exists
   - `test -f src/components/previews/RbacPreview.astro` → exists

2. **Import form check (Pitfall 1 guard)**
   - All 3 new files use `import ... from '../../assets/screenshots/*.png'` (import form, not string path)

3. **URL prop honesty check**
   - AiChatPreview uses `/maps/` in the URL (NOT `/chat`)
   - RbacPreview uses `/admin/users`
   - RasterVrtPreview uses `/datasets/`

4. **Syntax check**
   - `npm run check` → 0 errors

5. **Build + optimization check**
   - `npm run build` → exit 0
   - dist/_astro/ contains AVIF+WebP derivatives for raster-vrt.png, ai-chat.png, rbac.png (confirmed by listing dist/_astro/ or grep of the built HTML)

6. **preview-test smoke render**
   - dist/preview-test/index.html contains at least 6 `<picture>` tags
   - dist/preview-test/index.html contains references to all 6 capability screenshot names
</verification>

<success_criteria>
Plan 04 is complete when:

1. 3 new preview components (RasterVrt, AiChat, Rbac) exist in src/components/previews/
2. Each component follows the same pattern as Plan 03's retrofits (BrowserFrame + Picture from astro:assets + import form + `class="w-full block"`)
3. AiChatPreview URL prop uses `/maps/` (NOT `/chat`) — corrects CONTEXT.md D-01's stale route
4. Alt text for AiChatPreview matches the committed variant from 216-02-SUMMARY.md (D-14 full conversation OR D-15 empty panel — executor picks based on reality)
5. preview-test.astro imports and displays all 6 preview components in FEAT-01 order
6. `npm run build` exits 0 and dist/_astro/ contains AVIF+WebP for raster-vrt.png, ai-chat.png, rbac.png
7. dist/preview-test/index.html contains at least 6 `<picture>` elements
8. `npm run check` passes with zero errors

Parallelizability: This plan runs in parallel with Plan 03 (both depend only on Plan 02) since they touch disjoint file sets. Both must complete before Plan 05 can consume the 6 preview components.
</success_criteria>

<output>
After completion, create `.planning/phases/216-features-and-quickstart-pages/216-04-SUMMARY.md` documenting:
- 3 new component file paths + line counts (expected ~25 each)
- Alt text chosen for AiChatPreview (and which variant it matches — D-14 or D-15)
- URL props used for each (raster-vrt: /datasets/..., ai-chat: /maps/..., rbac: /admin/users)
- preview-test.astro now displays 6 previews in FEAT-01 order
- Build output AVIF+WebP file list for the 3 new screenshots
- preview-test HTML output showing 6 `<picture>` tags
- Next step: Plan 05 (features page) consumes all 6 previews
</output>
