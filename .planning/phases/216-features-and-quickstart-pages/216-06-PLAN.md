---
phase: 216-features-and-quickstart-pages
plan: 06
type: execute
wave: 4
depends_on: [216-02]
files_modified:
  - getgeolens.com/src/pages/quickstart/index.astro
  - getgeolens.com/src/components/quickstart/QuickstartOutcome.astro
autonomous: true
requirements: [QUICK-01, QUICK-02, QUICK-03]
tags: [astro, quickstart-page, code-blocks, marketing, docker-compose]

must_haves:
  truths:
    - "/quickstart page is reachable at http://localhost:4321/quickstart/ after `npm run preview`"
    - "Page has 8 logical sections in D-05 order: Prerequisites → Step 1 Get code → Step 2 Configure env → Step 3 Start services → Step 4 Log in → Step 5 Upload dataset → What you'll see (QUICK-03) → Troubleshooting → Next steps"
    - "All port references use the CORRECTED defaults: 5434 (Postgres), 8001 (API), 8080 (Frontend) — per research Q9"
    - "Page does NOT reference port 5432, 8000, or Redis as required (research Q9 corrected CONTEXT.md D-05 which listed '5432/6379/8000/8080')"
    - "Each step has at least one copyable code block with shell commands"
    - "QUICK-03 outcome section renders quickstart-outcome.png via `<Picture>` wrapped in BrowserFrame"
    - "Step 5 (Upload) links to Natural Earth 1:110m countries GeoJSON per research Q10 (public domain sample)"
    - "SiteLayout wraps the page with correct title/description/ogImage/jsonLd"
    - "`npm run build` exits 0 and produces dist/quickstart/index.html"
    - "Built HTML contains all step headings, code blocks, and the outcome `<picture>` element"
  artifacts:
    - path: "getgeolens.com/src/pages/quickstart/index.astro"
      provides: "Full /quickstart page — 8 sections in D-05 order, all port references corrected per research Q9"
      min_lines: 180
    - path: "getgeolens.com/src/components/quickstart/QuickstartOutcome.astro"
      provides: "QUICK-03 outcome section — prose + quickstart-outcome.png inside BrowserFrame"
      contains: "quickstart-outcome.png"
  key_links:
    - from: "quickstart/index.astro"
      to: "SiteLayout.astro"
      via: "layout wrapper"
      pattern: "SiteLayout"
    - from: "QuickstartOutcome.astro"
      to: "src/assets/screenshots/quickstart-outcome.png"
      via: "astro:assets import"
      pattern: "import.*assets/screenshots/quickstart-outcome\\.png"
    - from: "Step 5 prose"
      to: "Natural Earth 1:110m countries"
      via: "external link"
      pattern: "naturalearth.*countries"
---

<objective>
Build the `/quickstart` page at `getgeolens.com/src/pages/quickstart/index.astro` — the developer onboarding surface. A developer must be able to go from `git clone` to a running GeoLens instance in under 10 minutes following only this page.

Purpose:
- Fulfill QUICK-01 (step-by-step guide), QUICK-02 (copyable code blocks), QUICK-03 (outcome description + screenshot)
- Apply the research Q9 port corrections — the CONTEXT.md D-05 preliminary port list was WRONG (listed 5432/6379/8000/8080, actual defaults are 5434/8001/8080 with Redis not required)
- Use the real quickstart-outcome.png captured in Plan 02 for the "What you'll see" section
- Ship with honest troubleshooting (4 common issues per D-05)

Output:
- `/quickstart/index.astro` — the page (~200 lines)
- `QuickstartOutcome.astro` — extracted outcome component (QUICK-03 specific, uses quickstart-outcome.png via `<Picture>`)
- `npm run build` produces `dist/quickstart/index.html` with all 8 sections
- Code blocks use either plain `<pre><code>` (per D-09) OR Astro `<Code>` from astro:components (zero-JS, build-time Shiki per research Q11) — executor's call
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/216-features-and-quickstart-pages/216-CONTEXT.md
@.planning/phases/216-features-and-quickstart-pages/216-RESEARCH.md
@.planning/phases/216-features-and-quickstart-pages/216-02-SUMMARY.md

# SiteLayout contract
@/Users/ishiland/Code/getgeolens.com/src/components/layout/SiteLayout.astro

# BrowserFrame used-as-is for the outcome screenshot wrapper
@/Users/ishiland/Code/getgeolens.com/src/components/previews/BrowserFrame.astro

# Homepage index for jsonLd pattern reference
@/Users/ishiland/Code/getgeolens.com/src/pages/index.astro

<interfaces>
<!-- Page content contract. D-05 + research Q9 corrected values applied. -->

## Page structure (D-05)

1. **Page header** — h1 "Quickstart: Run GeoLens locally in 10 minutes", intro paragraph
2. **Prerequisites** — system requirements bullet list
3. **Step 1: Get the code** — git clone OR tarball download (two code blocks)
4. **Step 2: Configure environment** — copy .env.example and edit required vars (code block + inline note about 3 production-change vars)
5. **Step 3: Start services** — docker compose up (code block + expected output description)
6. **Step 4: Log in and verify** — open localhost:8080, use admin credentials
7. **Step 5: Upload your first dataset** — download Natural Earth countries, upload via UI
8. **What you'll see (QUICK-03)** — prose + real screenshot (QuickstartOutcome.astro)
9. **Troubleshooting** — 4 common issues
10. **Next steps** — links to /features and GitHub

## CORRECTED content per research (OVERRIDES CONTEXT.md D-05)

### Port defaults (research Q9 — verified from .env.example + docker-compose.yml)

CORRECT:
- Frontend: http://localhost:8080 (FRONTEND_PORT default 8080 → container 5173)
- API: http://localhost:8001 (API_PORT default 8001 → container 8000)
- Postgres: localhost:5434 (DB_PORT default 5434, NOT the usual 5432)
- Redis: NOT required for default dev setup (only in cloud-dev profile)

CONTEXT.md D-05 Step 1 (Prerequisites) listed "open ports 5432/6379/8000/8080". This is WRONG. Research Q9 line 371: "The actual ports used locally are 5434, 8001, and 8080. Redis/Valkey is not in the default docker-compose.yml."

The Prerequisites section MUST say:
- "Open local ports: 5434 (PostgreSQL), 8001 (API), 8080 (Frontend UI)"
- Do NOT mention Redis as a prereq

The Troubleshooting Port Conflict bullet MUST list 5434/8001/8080, NOT 5432/8000/8080.

### Minimum env vars (research Q9 — verified from .env.example)

Actual required vars:
```
POSTGRES_DB=geolens
POSTGRES_USER=geolens
POSTGRES_PASSWORD=geolens
JWT_SECRET_KEY=dev-only-change-me-in-production
GEOLENS_ADMIN_USERNAME=admin
GEOLENS_ADMIN_PASSWORD=admin
```

`.env.example` has all of these set out-of-the-box for dev — copying .env.example → .env is the ONLY Step 2 action needed. The CHANGE IN PRODUCTION callout should cover POSTGRES_PASSWORD, JWT_SECRET_KEY, GEOLENS_ADMIN_PASSWORD per research Q9 line 375.

### Step 5 sample dataset (research Q10)

Use Natural Earth 1:110m countries (~100KB, public domain):
- Direct download: https://naturalearth.s3.amazonaws.com/110m_cultural/ne_110m_admin_0_countries.zip
- Recognizable to GIS professionals, tiny download, requires no auth
- Do NOT reference e2e/fixtures/sample.geojson — that's an internal test fixture, not documented

### Code highlighting decision (research Q11 + D-09)

D-09 locked "plain `<pre><code>` elements, no JS copy button". Research Q11 confirmed that Astro's `<Code>` from `astro:components` is zero-JS (build-time Shiki) and satisfies D-09's intent.

Executor decision for this plan: USE raw `<pre><code>`. Rationale:
- Simpler styling control (single pre element with inline-style background + padding + font-family)
- D-09's language is literal and explicit
- Shiki theming is a polish concern that Phase 217 can address if needed
- Fewer moving parts — build never breaks due to Shiki grammar mismatches

If the executor strongly prefers Shiki highlighting, `<Code>` from astro:components is ALSO acceptable — research Q11 blessed it. The choice is discretion, not a blocker. Do NOT use both — pick one and stick with it for consistency.

### Code block styling (D-09)

Style each `<pre>` with:
- background: var(--surface-3) or var(--surface-2)
- border-radius: 0.5rem (rounded-lg)
- padding: 1rem (p-4)
- overflow-x: auto for long lines
- font-family: ui-monospace, monospace
- font-size: 0.875rem (text-sm)

Make code blocks visually distinct from prose. User selects text and presses Cmd/Ctrl+C to copy — no JS button.

### QUICK-03 outcome section

Create QuickstartOutcome.astro as a dedicated component. Structure:
- Short prose: "After Step 5, your browser shows the GeoLens catalog with your first dataset. The map preview, metadata panel, and OGC API endpoints at http://localhost:8001/collections are all live."
- `<BrowserFrame url="app.geolens.io" class="w-full">` wrapping `<Picture src={quickstartOutcomeScreenshot} ... />`
- Use the import form: `import quickstartOutcomeScreenshot from '../../assets/screenshots/quickstart-outcome.png'`
- Alt text: "GeoLens catalog landing page showing an uploaded Natural Earth countries dataset with metadata and map preview" (or tailor to the captured reality)

Per CONTEXT.md D-13 and Plan 02, quickstart-outcome.png may be an alias of search.png. That's fine — the file exists at the documented path regardless.

### Troubleshooting issues (D-05 step 8 — CORRECTED ports)

4 items per D-05:
1. Port conflict — "If port 5434, 8001, or 8080 is already in use, edit .env and set DB_PORT, API_PORT, or FRONTEND_PORT to free values." (NOT 5432/8000/8080)
2. Admin login fails — "Confirm GEOLENS_ADMIN_USERNAME and GEOLENS_ADMIN_PASSWORD in .env match the credentials you're typing. Restart services after .env changes."
3. Services slow to start — "The first docker compose up builds images and downloads the Postgres + PostGIS base layers (~1 GB). Subsequent starts are instant."
4. File upload fails — "Confirm the file is one of the supported formats: GeoJSON, Shapefile ZIP, GeoPackage, CSV, or a public OGC service URL."

## jsonLd for SiteLayout

```javascript
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'HowTo',
  name: 'Run GeoLens locally in 10 minutes',
  description: 'Step-by-step guide to installing and running a local GeoLens instance via docker compose.',
  totalTime: 'PT10M',
  supply: [{ '@type': 'HowToSupply', name: 'Docker Compose v2' }],
  tool: [{ '@type': 'HowToTool', name: 'git' }, { '@type': 'HowToTool', name: 'docker' }],
};
```

HowTo schema is the right schema.org type for a quickstart guide.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create QuickstartOutcome component</name>
  <files>
    getgeolens.com/src/components/quickstart/QuickstartOutcome.astro
  </files>
  <action>
    Create src/components/quickstart/ directory (does not exist yet) and write QuickstartOutcome.astro.

    File contents (the component is small — this IS the whole QUICK-03 outcome surface):

    ```astro
    ---
    import BrowserFrame from '../previews/BrowserFrame.astro';
    import { Picture } from 'astro:assets';
    import quickstartOutcomeScreenshot from '../../assets/screenshots/quickstart-outcome.png';
    ---

    <div class="space-y-6">
      <p class="text-base" style="color: var(--muted-foreground); line-height: 1.6;">
        After Step 5, your browser shows the GeoLens catalog with your first dataset. The map preview, metadata panel, and OGC API endpoints at <code style="background: var(--surface-2); padding: 0.1rem 0.35rem; border-radius: 0.25rem; font-size: 0.85em;">http://localhost:8001/collections</code> are all live. Use the search bar to filter datasets, click a card for metadata, and open the map builder to compose multi-layer maps.
      </p>

      <div class="w-full max-w-2xl mx-auto">
        <BrowserFrame url="app.geolens.io" class="w-full">
          <Picture
            src={quickstartOutcomeScreenshot}
            formats={['avif', 'webp']}
            alt="GeoLens catalog landing page showing an uploaded dataset with metadata and map preview"
            widths={[448, 896, 1344]}
            sizes="(max-width: 640px) 100vw, (max-width: 1024px) 640px, 720px"
            loading="lazy"
            class="w-full block"
          />
        </BrowserFrame>
      </div>
    </div>
    ```

    Notes:
    - CRITICAL: port 8001 (NOT 8000) — research Q9 correction (CONTEXT.md D-05 mentions port 8000 in the Step 4 and QUICK-03 sections but research corrected this to 8001).
    - Use `max-w-2xl mx-auto` for the outcome container (slightly wider than the FeatureStripe previews at max-w-md, since this is the page's main visual payoff and deserves more real estate)
    - Alt text describes the post-quickstart reality — tailor if the captured PNG differs from expectation
    - No `min-height` (Pitfall 4 guard)
    - The outer div has no `aria-hidden` — BrowserFrame already has it
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com && test -f src/components/quickstart/QuickstartOutcome.astro && grep -q "import { Picture } from 'astro:assets'" src/components/quickstart/QuickstartOutcome.astro && grep -q "from '../../assets/screenshots/quickstart-outcome.png'" src/components/quickstart/QuickstartOutcome.astro && grep -q "localhost:8001" src/components/quickstart/QuickstartOutcome.astro && ! grep -q "localhost:8000" src/components/quickstart/QuickstartOutcome.astro && ! grep -q "min-height" src/components/quickstart/QuickstartOutcome.astro && npm run check 2>&1 | tail -5 && echo "TASK 1 OK"</automated>
  </verify>
  <done>
    - src/components/quickstart/QuickstartOutcome.astro exists
    - Imports Picture from astro:assets and quickstart-outcome.png via the import form
    - References `http://localhost:8001/collections` (NOT 8000)
    - No `min-height` references
    - `astro check` passes
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build /quickstart/index.astro page composition + verify build</name>
  <files>
    getgeolens.com/src/pages/quickstart/index.astro
  </files>
  <action>
    Create getgeolens.com/src/pages/quickstart/index.astro (the directory src/pages/quickstart/ does not exist — create it as part of writing this file).

    Page structure (D-05 order, research Q9 corrected content):

    1. Frontmatter:
       ```astro
       ---
       import SiteLayout from '../../components/layout/SiteLayout.astro';
       import QuickstartOutcome from '../../components/quickstart/QuickstartOutcome.astro';

       const ogImage = new URL('/og/quickstart.png', Astro.site).href;

       const jsonLd = {
         '@context': 'https://schema.org',
         '@type': 'HowTo',
         name: 'Run GeoLens locally in 10 minutes',
         description: 'Step-by-step guide to installing and running a local GeoLens instance via docker compose.',
         totalTime: 'PT10M',
         supply: [{ '@type': 'HowToSupply', name: 'Docker Compose v2' }],
         tool: [{ '@type': 'HowToTool', name: 'git' }, { '@type': 'HowToTool', name: 'docker' }],
       };
       ---
       ```

    2. SiteLayout with props:
       - title: "Quickstart — GeoLens"
       - description: "Run your own GeoLens data catalog in under 10 minutes. Clone the repo, run docker compose up, log in, and upload your first dataset."
       - ogImage, jsonLd as above

    3. Inside SiteLayout, a main content wrapper `<div class="max-w-3xl mx-auto px-6 py-20 space-y-16">` (narrower than /features since it's a reading surface).

    4. Page header:
       - h1: "Quickstart: Run GeoLens locally in 10 minutes" (text-4xl font-semibold)
       - Intro: 2-3 sentences explaining what this quickstart delivers (a working local instance with admin access + one uploaded dataset, ready for evaluation)

    5. Prerequisites section (D-05 step 1):
       - h2 "Prerequisites"
       - Bullet list:
         - Docker Compose v2
         - ~4 GB RAM free
         - ~10 GB disk (for Postgres + PostGIS base images)
         - Open local ports: **5434** (PostgreSQL), **8001** (API), **8080** (Frontend UI) ← CORRECTED per research Q9
       - Do NOT mention Redis. Do NOT mention ports 5432 or 8000.

    6. Step 1: Get the code
       - h2 "Step 1: Get the code"
       - Short paragraph: "Clone the GeoLens repository or download the latest release tarball."
       - Code block 1 (git clone):
         ```
         git clone https://github.com/geolens-io/geolens.git
         cd geolens
         ```
       - Inline note: "Or download the release tarball:"
       - Code block 2 (tarball):
         ```
         curl -L https://github.com/geolens-io/geolens/archive/refs/heads/main.tar.gz -o geolens.tar.gz
         tar -xzf geolens.tar.gz
         cd geolens-main
         ```

    7. Step 2: Configure environment
       - h2 "Step 2: Configure environment"
       - Short paragraph: "Copy the example .env file. The defaults work out of the box for local development — no editing required unless you want to change the admin username or password."
       - Code block:
         ```
         cp .env.example .env
         ```
       - Callout: 3 vars marked `[CHANGE IN PRODUCTION]` in .env.example — POSTGRES_PASSWORD, JWT_SECRET_KEY, GEOLENS_ADMIN_PASSWORD. "You don't need to change them for local development, but the .env.example comments flag them for production deployment."

    8. Step 3: Start services
       - h2 "Step 3: Start services"
       - Code block:
         ```
         docker compose up -d --build
         ```
       - Paragraph: "The first build downloads Postgres + PostGIS base images (~1 GB). Subsequent starts take under 10 seconds."
       - Expected output: a sentence noting you can verify with `docker compose ps` that the `api`, `db`, and `frontend` services are running.

    9. Step 4: Log in and verify
       - h2 "Step 4: Log in and verify"
       - Paragraph: "Open the GeoLens UI:"
       - Code block (or inline code — planner's call):
         ```
         http://localhost:8080
         ```
       - Paragraph: "Log in with the default credentials from .env.example:"
       - Bullet list:
         - Username: `admin`
         - Password: `admin`
       - Paragraph: "The catalog will be empty — that's fine. Upload your first dataset in the next step."

    10. Step 5: Upload your first dataset
        - h2 "Step 5: Upload your first dataset"
        - Paragraph: "Download a tiny public-domain sample — Natural Earth's 1:110m countries shapefile (~100KB) — and upload it through the GeoLens UI."
        - Code block (or link):
          ```
          curl -LO https://naturalearth.s3.amazonaws.com/110m_cultural/ne_110m_admin_0_countries.zip
          ```
        - Paragraph (inline): "In the GeoLens UI, click the upload button in the catalog header, select the ZIP file, and confirm the detected CRS (EPSG:4326). Click **Commit** to publish."

    11. What you'll see (QUICK-03) — use the QuickstartOutcome component:
        - h2 "What you'll see"
        - `<QuickstartOutcome />` — the component handles the prose + screenshot

    12. Troubleshooting
        - h2 "Troubleshooting"
        - 4 bulleted items per D-05 + research Q9 corrections:
          - **Port conflict** — "If port 5434, 8001, or 8080 is already in use by another service, edit `.env` and change `DB_PORT`, `API_PORT`, or `FRONTEND_PORT` to free values. Then run `docker compose down && docker compose up -d --build` to apply."
          - **Admin login fails** — "Confirm `GEOLENS_ADMIN_USERNAME` and `GEOLENS_ADMIN_PASSWORD` in `.env` match what you're typing. After editing `.env`, restart services with `docker compose restart api`."
          - **Services slow to start** — "The first `docker compose up` builds images and downloads ~1 GB of base layers. Watch progress with `docker compose logs -f`. Subsequent starts are near-instant."
          - **File upload fails** — "Confirm the file is one of the supported formats: GeoJSON, Shapefile ZIP, GeoPackage, CSV, or a public OGC service URL. Check the upload dialog for validation errors."

    13. Next steps
        - h2 "Next steps"
        - Bullet list:
          - Explore capabilities on the [Features page](/features)
          - View the source on [GitHub](https://github.com/geolens-io/geolens)
          - Read the [CONTRIBUTING.md](https://github.com/geolens-io/geolens/blob/main/CONTRIBUTING.md) for development setup

    ## Code block styling

    Define a `<style>` block in the page (or inline the styles on each <pre>) so all `<pre>` elements share the same treatment:
    ```html
    <style>
      .quickstart-code {
        background: var(--surface-2);
        border: 1px solid var(--border);
        border-radius: 0.5rem;
        padding: 1rem;
        overflow-x: auto;
        font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
        font-size: 0.875rem;
        line-height: 1.6;
        color: var(--foreground);
      }
    </style>
    ```
    Then `<pre class="quickstart-code"><code>...</code></pre>` for each block.

    Alternative: apply the same styles inline with a style attribute if the scoped CSS approach produces visual inconsistencies. Keep ALL code blocks consistent.

    ## Verification commands after writing

    ```
    cd /Users/ishiland/Code/getgeolens.com
    npm run build
    test -f dist/quickstart/index.html

    # CORRECTED port references present
    grep -q "5434" dist/quickstart/index.html
    grep -q "8001" dist/quickstart/index.html
    grep -q "8080" dist/quickstart/index.html

    # WRONG port references absent (research Q9)
    ! grep -q "localhost:8000" dist/quickstart/index.html
    ! grep -q "localhost:5432" dist/quickstart/index.html
    ! grep -q "5432/6379" dist/quickstart/index.html

    # Redis not mentioned as required
    ! grep -iq "redis" dist/quickstart/index.html || echo "WARN: redis mentioned"

    # All step headings present
    for h in "Prerequisites" "Step 1: Get" "Step 2: Configure" "Step 3: Start" "Step 4: Log in" "Step 5: Upload" "What you'll see" "Troubleshooting" "Next steps"; do
      grep -q "$h" dist/quickstart/index.html || { echo "MISSING heading: $h"; exit 1; }
    done

    # QUICK-03 outcome picture element present
    grep -q "quickstart-outcome" dist/quickstart/index.html
    grep -q "<picture" dist/quickstart/index.html

    # Natural Earth sample link present (research Q10)
    grep -q "naturalearth" dist/quickstart/index.html

    # Code blocks present
    grep -c "<pre" dist/quickstart/index.html  # should be ≥ 5 (one per Step + any inline)
    ```
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com && test -f src/pages/quickstart/index.astro && grep -q "import SiteLayout" src/pages/quickstart/index.astro && grep -q "import QuickstartOutcome" src/pages/quickstart/index.astro && ! grep -q "localhost:8000" src/pages/quickstart/index.astro && ! grep -q "5432/6379" src/pages/quickstart/index.astro && grep -q "5434" src/pages/quickstart/index.astro && grep -q "8001" src/pages/quickstart/index.astro && grep -q "naturalearth" src/pages/quickstart/index.astro && npm run build 2>&1 | tail -10 && test -f dist/quickstart/index.html && for h in "Prerequisites" "Step 1" "Step 2" "Step 3" "Step 4" "Step 5" "Troubleshooting" "Next steps"; do grep -q "$h" dist/quickstart/index.html || { echo "MISSING: $h"; exit 1; }; done && grep -q "5434" dist/quickstart/index.html && grep -q "8001" dist/quickstart/index.html && ! grep -q "localhost:8000" dist/quickstart/index.html && grep -q "quickstart-outcome" dist/quickstart/index.html && PC=$(grep -c "<pre" dist/quickstart/index.html) && test "$PC" -ge 5 && echo "QUICKSTART BUILD OK — corrected ports 5434/8001/8080, Natural Earth sample linked, 8 sections present"</automated>
  </verify>
  <done>
    - src/pages/quickstart/index.astro exists and imports SiteLayout + QuickstartOutcome
    - Page has 8 section headings per D-05 order
    - All port references use 5434 (Postgres), 8001 (API), 8080 (Frontend)
    - NO references to `localhost:8000` or `5432/6379`
    - Redis is NOT listed as a required prerequisite
    - Step 5 links to Natural Earth 1:110m countries per research Q10
    - All code blocks use consistent styling (plain `<pre><code>` with class or inline styles)
    - `npm run build` exits 0
    - dist/quickstart/index.html exists with all 8 section headings, corrected ports, outcome image, and ≥5 `<pre>` elements
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Quickstart guide → new users | Instructions are executed verbatim by developers on their local machines |
| External links → third-party hosts | Natural Earth CDN, GitHub, and docker.io are all referenced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-216-06-01 | Tampering | Wrong port numbers → user hits nonexistent service | mitigate | Verify step greps for 5434/8001/8080 present AND 5432/8000 absent. Research Q9 corrected these from the preliminary CONTEXT.md D-05. Getting ports wrong is a 10-minute-wasted moment for every new user — highest severity UX risk in the phase. |
| T-216-06-02 | Tampering | Wrong sample dataset URL causes download failure | mitigate | Use the verified Natural Earth S3 CDN URL (research Q10). Do NOT link to the internal e2e/fixtures/sample.geojson which is not a user-facing asset. |
| T-216-06-03 | Information Disclosure | Default dev credentials documented in public marketing | accept | `admin`/`admin` are explicit dev-only defaults baked into .env.example. Users are told to change them for production via the callout in Step 2. This matches existing README/quickstart conventions in geolens/README.md and is not a meaningful disclosure. |
| T-216-06-04 | Tampering | External link to a compromised Natural Earth CDN | accept | The Natural Earth S3 bucket is public domain US-based infrastructure used by GIS professionals for 15+ years. Compromise risk is near-zero and outside this phase's threat model. |
| T-216-06-05 | Spoofing | GitHub URL mistyped → developer clones a typosquat repo | mitigate | Use the exact verified URL `https://github.com/geolens-io/geolens.git` (matches STATE.md and homepage jsonLd). Automated grep during verify ensures the URL is exactly this. |
| T-216-06-06 | Elevation of Privilege | Instructions encourage running `docker compose up` on untrusted images | accept | Docker Compose v2 is the user's explicit tool choice. The geolens images are built from the open-source Dockerfile in the cloned repo (not pulled from docker.io), so the user reviews the source before running. |
| T-216-06-07 | Repudiation | Troubleshooting advice becomes stale | mitigate | Each troubleshooting item is tied to a specific env var or command that is stable across GeoLens versions. If .env.example renames a variable, the quickstart is updated by re-running this phase's planning (future phase 224+). |

T-216-06-01 is the highest-priority threat. Automated greps block a regression on the port list correction. Without these greps, a future edit could silently reintroduce 5432/8000 if someone reads the old CONTEXT.md D-05.
</threat_model>

<verification>
Phase-level verification for this plan:

1. **File existence**
   - src/components/quickstart/QuickstartOutcome.astro exists
   - src/pages/quickstart/index.astro exists

2. **Port correction check (research Q9 — CRITICAL)**
   - `grep -q "5434" dist/quickstart/index.html` → match
   - `grep -q "8001" dist/quickstart/index.html` → match
   - `grep -q "8080" dist/quickstart/index.html` → match
   - `! grep -q "localhost:8000" dist/quickstart/index.html` → no match
   - `! grep -q "5432/6379" dist/quickstart/index.html` → no match
   - No explicit "Redis required" claim anywhere in the page

3. **Section heading check (D-05 order)**
   - All of: Prerequisites, Step 1, Step 2, Step 3, Step 4, Step 5, What you'll see, Troubleshooting, Next steps present in built HTML

4. **Code block count**
   - `grep -c "<pre" dist/quickstart/index.html` → ≥ 5 (one per Step + troubleshooting snippets)

5. **Natural Earth sample link check (research Q10)**
   - `grep -q "naturalearth" dist/quickstart/index.html` → match

6. **QUICK-03 outcome check**
   - `grep -q "quickstart-outcome" dist/quickstart/index.html` → match (the Picture element generates a reference to the source filename)
   - `grep -q "<picture" dist/quickstart/index.html` → match

7. **Syntax check**
   - `npm run check` → 0 errors

8. **Build check**
   - `npm run build` → exit 0
</verification>

<success_criteria>
Plan 06 is complete when:

1. src/components/quickstart/QuickstartOutcome.astro exists and renders quickstart-outcome.png via Picture inside BrowserFrame
2. src/pages/quickstart/index.astro exists and composes the full D-05 structure (8 sections)
3. All port references use the CORRECTED defaults per research Q9: 5434 (Postgres), 8001 (API), 8080 (Frontend)
4. Page does NOT reference port 5432, 8000, or Redis as required (CONTEXT.md D-05 was wrong; research Q9 corrected)
5. Step 5 links to the public Natural Earth 1:110m countries dataset via the S3 CDN URL
6. All code blocks use plain `<pre><code>` with consistent styling (NOT Shiki, per D-09 — unless executor chose the Q11 `<Code>` alternative)
7. QUICK-03 outcome section renders the real screenshot via the QuickstartOutcome component
8. `npm run build` exits 0
9. dist/quickstart/index.html contains all 8 section headings, corrected ports, Natural Earth link, and the outcome `<picture>` element
10. Troubleshooting section addresses 4 common issues with port numbers that match the rest of the page (5434/8001/8080)

This plan is parallelizable with Plans 05 and 07 (all depend only on Plan 02 for screenshots — Plan 05/06 consume previews, Plan 07 is independent).
</success_criteria>

<output>
After completion, create `.planning/phases/216-features-and-quickstart-pages/216-06-SUMMARY.md` documenting:
- Line counts for quickstart/index.astro and QuickstartOutcome.astro
- Confirmation that all port references match research Q9 (5434/8001/8080, no Redis)
- Number of code blocks on the page and which styling approach was chosen (plain `<pre><code>` per D-09 or `<Code>` per Q11)
- The Natural Earth sample URL used
- Built dist/quickstart/index.html byte size
- Grep output confirming no stale port references remain
- Next step: Plan 07 (Nav amendment) can run now if not already complete
</output>
