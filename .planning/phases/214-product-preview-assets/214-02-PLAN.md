---
phase: 214-product-preview-assets
plan: 02
type: execute
wave: 2
depends_on:
  - 214-01
files_modified:
  - getgeolens.com/src/components/previews/MapBuilderPreview.astro
  - getgeolens.com/src/components/previews/DatasetDetailPreview.astro
  - getgeolens.com/src/pages/preview-test.astro
autonomous: false
requirements:
  - ASSET-02
  - ASSET-03
must_haves:
  truths:
    - "A MapBuilderPreview component renders a convincing map builder UI with a layer panel sidebar and a map canvas area"
    - "A DatasetDetailPreview component renders a convincing dataset detail page with metadata fields and a map thumbnail"
    - "All three preview components render side-by-side on a test page without layout errors"
    - "The full Astro site builds without errors"
  artifacts:
    - path: "getgeolens.com/src/components/previews/MapBuilderPreview.astro"
      provides: "CSS-rendered map builder UI preview with layer panel and map canvas"
      contains: "Nys Aquifers"
    - path: "getgeolens.com/src/components/previews/DatasetDetailPreview.astro"
      provides: "CSS-rendered dataset detail page preview with metadata table and map thumbnail"
      contains: "EPSG:4326"
    - path: "getgeolens.com/src/pages/preview-test.astro"
      provides: "Test page rendering all three preview components for visual verification"
      contains: "SearchPreview"
  key_links:
    - from: "MapBuilderPreview.astro"
      to: "BrowserFrame.astro"
      via: "Astro import + slot composition"
      pattern: "import BrowserFrame from.*BrowserFrame.astro"
    - from: "DatasetDetailPreview.astro"
      to: "BrowserFrame.astro"
      via: "Astro import + slot composition"
      pattern: "import BrowserFrame from.*BrowserFrame.astro"
    - from: "preview-test.astro"
      to: "all three preview components"
      via: "Astro import"
      pattern: "import.*Preview from"
---

<objective>
Create MapBuilderPreview.astro (ASSET-02) and DatasetDetailPreview.astro (ASSET-03), then wire all three previews into a test page for visual verification.

Purpose: Completes the product preview asset set. The test page enables the human review checkpoint identified in STATE.md as necessary before proceeding to the homepage phase.
Output: Two preview components + one test page. Full `astro build` passes. Visual review ready.
</objective>

<threat_model>
No threats identified. Same rationale as Plan 01: static decorative Astro components with zero JavaScript, no user input, no API calls, no authentication. The preview-test.astro page is a temporary development page — it has no sensitive content and can be removed after Phase 215/216 integration. No robots meta exclusion needed since the test page will be deleted.
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/214-product-preview-assets/214-CONTEXT.md
@.planning/phases/214-product-preview-assets/214-UI-SPEC.md
@.planning/phases/214-product-preview-assets/214-RESEARCH.md
@.planning/phases/214-product-preview-assets/214-01-SUMMARY.md

<interfaces>
<!-- BrowserFrame.astro (created in Plan 01) -->
import BrowserFrame from './BrowserFrame.astro';
<!-- Props: url: string, class?: string -->
<!-- Usage: <BrowserFrame url="app.geolens.io/builder"> content </BrowserFrame> -->
<!-- Provides: window chrome, perspective tilt, glow, slot for content -->

<!-- Design tokens from getgeolens.com/src/styles/global.css -->
:root {
  --primary-50:  oklch(0.97 0.02 250);
  --primary-100: oklch(0.93 0.05 250);
  --primary-500: oklch(0.55 0.18 250);
  --primary-700: oklch(0.46 0.16 250);
  --background: oklch(1 0 0);
  --foreground: oklch(0.145 0 0);
  --muted-foreground: oklch(0.45 0 0);
  --muted: oklch(0.90 0 0);
  --border: oklch(0.92 0 0);
  --surface-1: oklch(0.99 0 0);
  --surface-2: oklch(0.98 0 0);
  --surface-3: oklch(0.96 0 0);
}

<!-- SiteLayout.astro (from Phase 212) -->
import SiteLayout from '../components/layout/SiteLayout.astro';
<!-- Props: title: string, description?: string -->
<!-- Provides: html shell, nav, footer, main slot -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MapBuilderPreview.astro (ASSET-02) and DatasetDetailPreview.astro (ASSET-03)</name>
  <files>
    getgeolens.com/src/components/previews/MapBuilderPreview.astro
    getgeolens.com/src/components/previews/DatasetDetailPreview.astro
  </files>
  <read_first>
    - /Users/ishiland/Code/getgeolens.com/src/components/previews/BrowserFrame.astro (confirm slot pattern, import path, class prop)
    - /Users/ishiland/Code/getgeolens.com/src/components/previews/SearchPreview.astro (confirm established patterns — badge styling, SVG technique, color token usage)
    - /Users/ishiland/Code/geolens/.planning/phases/214-product-preview-assets/214-UI-SPEC.md (MapBuilderPreview spec lines 168-191, DatasetDetailPreview spec lines 195-212)
    - /Users/ishiland/Code/geolens/.planning/phases/214-product-preview-assets/214-RESEARCH.md (lines 200-261 — Patterns 3 and 4, lines 447-566 — code examples for both components)
    - /Users/ishiland/Code/getgeolens.com/src/styles/global.css (confirm token names for surface-1, muted, primary-50)
  </read_first>
  <action>
Create two files in `getgeolens.com/src/components/previews/`.

---

**File 1: MapBuilderPreview.astro** (per D-05, D-02, D-03)

**Frontmatter:**
```
import BrowserFrame from './BrowserFrame.astro';
```

**Structure:**
```astro
<BrowserFrame url="app.geolens.io/builder">
  <div class="flex" style="min-height: 280px;">
    <!-- Layer panel (left) -->
    <!-- Map canvas (right) -->
  </div>
</BrowserFrame>
```

**Layer panel (left, `w-52 shrink-0 border-r flex flex-col`):**
- Panel border: `style="border-color: var(--border); background: var(--surface-1);"`
- Header: `<div class="px-3 py-2.5 border-b text-[13px] font-semibold" style="border-color: var(--border); color: var(--foreground);">Layers</div>`
- Layer rows container: `<div class="flex-1">` with 3 rows, each `<div class="flex items-center gap-2 px-3 py-2.5" style="border-bottom: 1px solid oklch(0.92 0 0 / 30%);">`:

  **Row 1 — Nys Aquifers (visible):**
  - Eye icon: inline SVG `size-3.5 shrink-0` with `color: var(--muted-foreground)`, viewBox="0 0 24 24", fill="none", stroke="currentColor", stroke-width="2": `<path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/>`
  - Color swatch: `<div class="size-3 rounded-sm shrink-0" style="background: var(--primary-500);"></div>` — blue
  - Name: `<span class="text-[11px] truncate flex-1" style="color: var(--foreground);">Nys Aquifers</span>`
  - Drag handle: `<span class="text-[10px] shrink-0" style="color: var(--muted-foreground); opacity: 0.4;">⋮⋮</span>`

  **Row 2 — Nys Address Points (visible):**
  - Eye icon: same as Row 1
  - Color swatch: `style="background: oklch(0.59 0.22 25);"` — red-orange
  - Name: "Nys Address Points"
  - Drag handle: same

  **Row 3 — Base Map (visible, slightly muted):**
  - Eye icon: same as Row 1
  - Color swatch: `style="background: var(--border);"` — neutral gray
  - Name: "Base Map"
  - Drag handle: same

- Footer: `<div class="border-t px-3 py-2.5" style="border-color: var(--border);">` containing Add layer button:
  `<div class="flex items-center gap-1.5 text-[10px] font-semibold" style="color: var(--primary-700);"><span style="font-size: 14px; line-height: 1;">+</span> Add layer</div>`

**Map canvas (right, `flex-1 relative`):**
- Background: `style="background: linear-gradient(135deg, oklch(0.93 0.03 200) 0%, oklch(0.95 0.02 230) 40%, oklch(0.90 0.04 250) 100%);"` — tonal blue-green basemap gradient (Option B per RESEARCH)
- Polygon overlay SVG: `<svg class="absolute inset-0 w-full h-full" viewBox="0 0 400 280" fill="none" aria-hidden="true">` containing:
  - 2-3 polygon paths simulating aquifer shapes on the map: fill `var(--primary)` at 15% opacity, stroke `var(--primary)` at 50% opacity, stroke-width 1. Use paths:
    - `<path d="M 140 80 L 200 60 L 280 70 L 320 120 L 300 170 L 240 190 L 160 180 L 120 130 Z" fill="var(--primary)" fill-opacity="0.15" stroke="var(--primary)" stroke-opacity="0.5" stroke-width="1" />`
    - `<path d="M 180 100 L 260 85 L 310 130 L 280 165 L 200 160 L 160 130 Z" fill="var(--primary)" fill-opacity="0.10" stroke="var(--primary)" stroke-opacity="0.35" stroke-width="0.8" />`
  - 8-10 scattered point circles simulating address points: `<circle cx="..." cy="..." r="2.5" fill="oklch(0.59 0.22 25)" fill-opacity="0.6" />` at positions roughly scattered across the canvas: (100,90), (170,55), (250,110), (310,80), (190,150), (280,175), (130,165), (340,140), (220,195), (150,115).
- Zoom controls (top-right): `<div class="absolute top-2 right-2 flex flex-col gap-1">` with two boxes:
  - Plus: `<div class="size-6 rounded border flex items-center justify-center text-[10px] font-bold" style="background: var(--background); border-color: var(--border); color: var(--foreground);">+</div>`
  - Minus: same structure with text `−` (U+2212 minus sign, not hyphen)

---

**File 2: DatasetDetailPreview.astro** (per D-06, D-02, D-03)

**Frontmatter:**
```
import BrowserFrame from './BrowserFrame.astro';
```

**Structure:**
```astro
<BrowserFrame url="app.geolens.io/datasets/nys-aquifers">
  <div class="flex" style="min-height: 220px;">
    <!-- Metadata column (left) -->
    <!-- Map thumbnail column (right) -->
  </div>
</BrowserFrame>
```

**Metadata column (left, `flex-1 px-4 py-4`):**
- Type badge: Vector badge using same styling as SearchPreview Card 1 — `inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-[10px] font-semibold` with `background: color-mix(in oklch, var(--primary) 12%, transparent); color: var(--primary-700);` containing layer icon SVG + "Vector" text.
- Dataset title: `<p class="text-[13px] font-semibold mt-1 mb-2" style="color: var(--foreground);">Nys Aquifers</p>`
- Description: `<p class="text-[11px] mb-4" style="color: var(--muted-foreground);">Aquifer polygons in New York State</p>`
- Metadata table: 4 rows using `<div class="space-y-2">`, each row `<div class="flex gap-4">`:
  - Label: `<span class="text-[10px] font-semibold w-20 shrink-0" style="color: var(--muted-foreground);">Geometry</span>` — Value: `<span class="text-[10px]" style="color: var(--foreground);">MultiPolygon</span>`
  - Label: "Features" — Value: "907"
  - Label: "CRS" — Value: "EPSG:4326"
  - Label: "Updated" — Value: "Nov 2023"

**Map thumbnail column (right, `w-36 shrink-0 border-l p-3`):**
- Border: `style="border-color: var(--border);"`
- Thumbnail container: `<div class="aspect-square rounded-md overflow-hidden" style="background: var(--primary-50);">`
  - SVG: viewBox="0 0 100 100", fill="none", same NYS aquifer polygon shapes as SearchPreview quicklook but scaled larger to fill the thumbnail:
    - `<polygon points="15,20 35,10 60,15 75,35 70,60 50,70 25,65 12,45" fill="var(--primary)" fill-opacity="0.10" stroke="var(--primary)" stroke-opacity="0.45" stroke-width="0.8" />`
    - `<polygon points="30,28 50,18 68,30 65,52 48,62 25,55" fill="var(--primary)" fill-opacity="0.08" stroke="var(--primary)" stroke-opacity="0.35" stroke-width="0.7" />`
    - `<polygon points="20,40 38,28 55,38 52,55 35,62" fill="var(--primary)" fill-opacity="0.12" stroke="var(--primary)" stroke-opacity="0.40" stroke-width="0.7" />`
- Extent label: `<p class="text-[9px] text-center mt-1.5" style="color: var(--muted-foreground);">Extent</p>`

**Do NOT add** to either file: Any `transition`, `hover:`, `animation`, `<button>` (except decorative ones with tabindex="-1"), `<a>`, heading tags, or `client:*` directive.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com && npx astro check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at getgeolens.com/src/components/previews/MapBuilderPreview.astro
    - File exists at getgeolens.com/src/components/previews/DatasetDetailPreview.astro
    - MapBuilderPreview.astro contains `import BrowserFrame from './BrowserFrame.astro'`
    - MapBuilderPreview.astro contains `url="app.geolens.io/builder"`
    - MapBuilderPreview.astro contains "Layers" panel header
    - MapBuilderPreview.astro contains "Nys Aquifers", "Nys Address Points", "Base Map" layer names
    - MapBuilderPreview.astro contains `var(--primary-500)` (blue swatch)
    - MapBuilderPreview.astro contains `oklch(0.59 0.22 25)` (red-orange swatch)
    - MapBuilderPreview.astro contains `+ Add layer` text
    - MapBuilderPreview.astro contains `linear-gradient(135deg` for basemap canvas
    - DatasetDetailPreview.astro contains `import BrowserFrame from './BrowserFrame.astro'`
    - DatasetDetailPreview.astro contains `url="app.geolens.io/datasets/nys-aquifers"`
    - DatasetDetailPreview.astro contains "Geometry", "Features", "CRS", "Updated" metadata labels
    - DatasetDetailPreview.astro contains "MultiPolygon", "907", "EPSG:4326", "Nov 2023" metadata values
    - DatasetDetailPreview.astro contains "Extent" label under map thumbnail
    - DatasetDetailPreview.astro contains `var(--primary-50)` for thumbnail background
    - `npx astro check` exits without errors
  </acceptance_criteria>
  <done>MapBuilderPreview.astro renders a two-column layout with layer panel (3 layers, color swatches, visibility icons, add-layer button) and map canvas (gradient basemap, polygon/point overlays, zoom controls). DatasetDetailPreview.astro renders a two-column layout with metadata fields and map extent thumbnail with aquifer polygon SVGs.</done>
</task>

<task type="auto">
  <name>Task 2: Create preview-test.astro page for visual verification</name>
  <files>getgeolens.com/src/pages/preview-test.astro</files>
  <read_first>
    - /Users/ishiland/Code/getgeolens.com/src/components/previews/SearchPreview.astro (confirm import path)
    - /Users/ishiland/Code/getgeolens.com/src/components/previews/MapBuilderPreview.astro (confirm import path)
    - /Users/ishiland/Code/getgeolens.com/src/components/previews/DatasetDetailPreview.astro (confirm import path)
    - /Users/ishiland/Code/getgeolens.com/src/components/layout/SiteLayout.astro (confirm layout import and props)
  </read_first>
  <action>
Create `getgeolens.com/src/pages/preview-test.astro` — a temporary page for visual review of all three previews.

**Frontmatter:**
```astro
---
import SiteLayout from '../components/layout/SiteLayout.astro';
import SearchPreview from '../components/previews/SearchPreview.astro';
import MapBuilderPreview from '../components/previews/MapBuilderPreview.astro';
import DatasetDetailPreview from '../components/previews/DatasetDetailPreview.astro';
---
```

**Content:** Use SiteLayout wrapper. Render all three previews vertically with labels, centered on the page, with generous spacing.

```astro
<SiteLayout title="Preview Test — Product Assets">
  <div class="mx-auto max-w-3xl px-6 py-16 space-y-16">
    <h1 class="text-2xl font-bold" style="color: var(--foreground);">Product Preview Assets</h1>
    <p class="text-sm" style="color: var(--muted-foreground);">Visual review page for Phase 214 components. Delete after integration into homepage/features pages.</p>

    <section class="space-y-4">
      <h2 class="text-lg font-semibold" style="color: var(--foreground);">ASSET-01: Search / Catalog</h2>
      <SearchPreview />
    </section>

    <section class="space-y-4">
      <h2 class="text-lg font-semibold" style="color: var(--foreground);">ASSET-02: Map Builder</h2>
      <MapBuilderPreview />
    </section>

    <section class="space-y-4">
      <h2 class="text-lg font-semibold" style="color: var(--foreground);">ASSET-03: Dataset Detail</h2>
      <DatasetDetailPreview />
    </section>
  </div>
</SiteLayout>
```

This page is intentionally simple — its only purpose is visual verification of the three preview components.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File exists at getgeolens.com/src/pages/preview-test.astro
    - File contains imports for SiteLayout, SearchPreview, MapBuilderPreview, DatasetDetailPreview
    - File contains `<SearchPreview />`, `<MapBuilderPreview />`, `<DatasetDetailPreview />`
    - File contains "ASSET-01", "ASSET-02", "ASSET-03" labels
    - `npm run build` completes successfully (exit 0) — confirms all components render without errors in static build
    - dist/preview-test/index.html exists after build
  </acceptance_criteria>
  <done>A preview-test page exists at /preview-test that renders all three product preview components side-by-side for visual review. Full astro build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual review of all three product preview components</name>
  <what-built>Three CSS-rendered browser-frame product preview components (SearchPreview, MapBuilderPreview, DatasetDetailPreview) using the shared BrowserFrame wrapper, all visible on the preview test page.</what-built>
  <how-to-verify>
    1. Start the dev server: `cd /Users/ishiland/Code/getgeolens.com && npm run dev`
    2. Open http://localhost:4321/preview-test in your browser
    3. Verify ASSET-01 (Search/Catalog):
       - Browser frame with traffic-light dots and "app.geolens.io/search" URL
       - Search bar with "Search geospatial data..." placeholder
       - Filter tabs: All (207), Vector (204), Raster (3), Table (3)
       - Three dataset cards with type badges, names, descriptions, stats
       - Nys Aquifers card has polygon SVG quicklook
       - Nys Address Points card has scattered-circles SVG quicklook
       - Bulletin card has empty thumbnail (Table type)
    4. Verify ASSET-02 (Map Builder):
       - Browser frame with "app.geolens.io/builder" URL
       - Left panel with "Layers" header, 3 layer rows with color swatches and eye icons
       - Right canvas with blue-green gradient basemap and polygon/point overlays
       - Zoom +/- controls in top-right corner
       - "+ Add layer" button at bottom of panel
    5. Verify ASSET-03 (Dataset Detail):
       - Browser frame with "app.geolens.io/datasets/nys-aquifers" URL
       - Left side: Vector badge, "Nys Aquifers" title, description, metadata table (Geometry, Features, CRS, Updated)
       - Right side: Map thumbnail with polygon outlines on light blue background, "Extent" label
    6. Verify all three have subtle perspective tilt (slight 3D angle) and a soft blue glow behind them
    7. Resize browser to < 640px — tilt should flatten to no perspective
    8. Overall impression: Do the previews look like convincing product screenshots? Would a visitor understand what GeoLens does from these alone?
  </how-to-verify>
  <resume-signal>Type "approved" if previews look good, or describe specific issues to fix (e.g., "search card text too small", "map canvas gradient too dark", "tilt angle too extreme").</resume-signal>
</task>

</tasks>

<verification>
- `cd /Users/ishiland/Code/getgeolens.com && npm run build` exits 0 (full static build passes)
- `ls getgeolens.com/src/components/previews/` shows BrowserFrame.astro, SearchPreview.astro, MapBuilderPreview.astro, DatasetDetailPreview.astro
- `ls getgeolens.com/dist/preview-test/` shows index.html after build
- All three preview components import BrowserFrame and render inside its slot
- Human reviewer confirms visual fidelity at the checkpoint
</verification>

<success_criteria>
- MapBuilderPreview.astro exists with layer panel (3 layers) and map canvas (gradient basemap, overlays, zoom controls)
- DatasetDetailPreview.astro exists with metadata fields and map extent thumbnail
- preview-test.astro renders all three previews without build errors
- `npm run build` passes
- Human reviewer approves visual fidelity of all three previews
</success_criteria>

<output>
After completion, create `.planning/phases/214-product-preview-assets/214-02-SUMMARY.md`
</output>
