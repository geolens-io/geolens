---
phase: 214-product-preview-assets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - getgeolens.com/src/components/previews/BrowserFrame.astro
  - getgeolens.com/src/components/previews/SearchPreview.astro
autonomous: true
requirements:
  - ASSET-01
must_haves:
  truths:
    - "A BrowserFrame wrapper component exists that renders macOS-style window chrome with traffic-light dots, URL bar, perspective tilt, and primary glow"
    - "A SearchPreview component renders a convincing catalog UI with search bar, filter tabs with counts, and three dataset cards with type badges, descriptions, stats, and inline SVG quicklook thumbnails"
    - "Both components build without errors in the Astro static pipeline"
  artifacts:
    - path: "getgeolens.com/src/components/previews/BrowserFrame.astro"
      provides: "Shared browser-frame wrapper with window chrome, perspective tilt, primary glow, slot for content"
      contains: "aria-hidden"
    - path: "getgeolens.com/src/components/previews/SearchPreview.astro"
      provides: "CSS-rendered search/catalog UI preview with 3 dataset cards"
      contains: "Nys Aquifers"
  key_links:
    - from: "SearchPreview.astro"
      to: "BrowserFrame.astro"
      via: "Astro import + slot composition"
      pattern: "import BrowserFrame from.*BrowserFrame.astro"
---

<objective>
Create the shared BrowserFrame.astro wrapper and the SearchPreview.astro component (ASSET-01) — the most complex of the three product previews.

Purpose: BrowserFrame is the foundation all three previews share. SearchPreview is the hero asset that shows the GeoLens catalog UI to evaluators on the marketing site.
Output: Two Astro components in getgeolens.com/src/components/previews/ that build cleanly.
</objective>

<threat_model>
No threats identified. This plan creates static decorative Astro components with zero JavaScript, no user input, no API calls, no authentication, and no data processing. All content is hardcoded marketing copy. Components are aria-hidden and non-interactive.
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/214-product-preview-assets/214-CONTEXT.md
@.planning/phases/214-product-preview-assets/214-UI-SPEC.md
@.planning/phases/214-product-preview-assets/214-RESEARCH.md

<interfaces>
<!-- Design tokens available in getgeolens.com/src/styles/global.css -->
<!-- Use var(--primary-*), var(--border), var(--surface-*), var(--muted-foreground), var(--foreground), var(--background) -->
<!-- Font: var(--font-sans) = 'Inter Variable' -->
<!-- Tailwind 4: structural classes (flex, px-4, rounded-xl) are fine; use inline style="" for brand colors -->

From getgeolens.com/src/styles/global.css:
:root {
  --primary-50:  oklch(0.97 0.02 250);
  --primary-100: oklch(0.93 0.05 250);
  --primary-500: oklch(0.55 0.18 250);
  --primary-700: oklch(0.46 0.16 250);
  --background: oklch(1 0 0);
  --foreground: oklch(0.145 0 0);
  --muted-foreground: oklch(0.45 0 0);
  --border: oklch(0.92 0 0);
  --surface-2: oklch(0.98 0 0);
  --surface-3: oklch(0.96 0 0);
}

From geolens/frontend/src/pages/LandingPage.tsx (reference implementation, lines 41-138):
- ProductPreview function: CSS-only browser frame with window chrome dots, search bar, filter tabs, 3 dataset cards with inline SVG quicklooks
- Pattern: div wrapper with aria-hidden="true", rounded-xl border, chrome bar with dots, slot-like content area
- Cards: Nys Aquifers (Vector, polygon SVG), Nys Address Points (Vector, scatter circles), Bulletin (Table, empty thumbnail)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BrowserFrame.astro shared wrapper</name>
  <files>getgeolens.com/src/components/previews/BrowserFrame.astro</files>
  <read_first>
    - /Users/ishiland/Code/getgeolens.com/src/styles/global.css (design tokens — confirm var names)
    - /Users/ishiland/Code/getgeolens.com/src/components/layout/Nav.astro (established inline style pattern for token usage)
    - /Users/ishiland/Code/geolens/frontend/src/pages/LandingPage.tsx (lines 41-55 — reference window chrome pattern)
    - /Users/ishiland/Code/geolens/.planning/phases/214-product-preview-assets/214-UI-SPEC.md (BrowserFrame spec at lines 109-128)
    - /Users/ishiland/Code/geolens/.planning/phases/214-product-preview-assets/214-RESEARCH.md (lines 363-410 — BrowserFrame code example)
  </read_first>
  <action>
Create `getgeolens.com/src/components/previews/BrowserFrame.astro` per D-10.

**Interface (frontmatter):**
```
interface Props {
  url: string;
  class?: string;
}
const { url, class: className } = Astro.props;
```

**Structure (outer to inner):**

1. Outer wrapper: `<div aria-hidden="true" class:list={["relative inline-block", className]}>` — positions glow + frame together.

2. Glow layer: `<div class="pointer-events-none absolute inset-[-20%] -z-10 blur-3xl opacity-50 rounded-full" style="background: radial-gradient(ellipse at 50% 60%, var(--primary-100) 0%, transparent 70%);"></div>` — per D-09, matches LandingPage hero blob technique.

3. Browser chrome container: `<div class="browser-frame-inner w-full overflow-hidden rounded-xl border" style="border-color: var(--border); background-color: var(--background); box-shadow: 0 20px 50px oklch(0 0 0 / 14%), 0 4px 12px oklch(0 0 0 / 8%); transform: perspective(1200px) rotateY(2deg) rotateX(1deg); transform-origin: center center;">` — per D-07, perspective tilt with dual box-shadow for depth.

4. Chrome bar: `<div class="flex items-center gap-2 border-b px-3 py-2" style="border-color: var(--border); background-color: var(--surface-2);">` containing:
   - Three traffic-light dots: `size-3 rounded-full` with backgrounds `#ff5f57` (red), `#ffbd2e` (yellow), `#28ca41` (green) — standard macOS chrome colors, hex is intentional (not design-system colors).
   - URL pill: `<div class="ml-3 flex-1 max-w-60 rounded-md px-3 py-1 text-[11px] truncate" style="background: var(--surface-3); color: var(--muted-foreground);">{url}</div>`

5. `<slot />` — receives preview content.

**Responsive (per UI-SPEC and Pitfall 2):**
Add a `<style>` block at the bottom of the file:
```css
@media (max-width: 639px) {
  .browser-frame-inner {
    transform: none;
  }
}
@media (min-width: 640px) and (max-width: 1023px) {
  .browser-frame-inner {
    transform: perspective(1200px) rotateY(1.5deg) rotateX(0.8deg);
  }
}
```

**Do NOT add:** Any `transition`, `hover:`, or `animation` rules (D-08). Any `<button>`, `<a>`, or heading tags inside the chrome. Any `client:*` directive.
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com && cat src/components/previews/BrowserFrame.astro | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at getgeolens.com/src/components/previews/BrowserFrame.astro
    - File contains `aria-hidden="true"` on outer wrapper
    - File contains `interface Props` with `url: string`
    - File contains `<slot />` for content injection
    - File contains `perspective(1200px)` in transform
    - File contains `#ff5f57` (red dot), `#ffbd2e` (yellow dot), `#28ca41` (green dot)
    - File contains `radial-gradient` for glow layer
    - File contains `pointer-events-none` on glow div
    - File contains `@media (max-width: 639px)` with `transform: none`
    - File does NOT contain `transition` or `hover` or `animation` or `client:`
  </acceptance_criteria>
  <done>BrowserFrame.astro renders window chrome with traffic lights, URL bar, perspective tilt, primary glow, responsive breakpoints, and a slot for preview content.</done>
</task>

<task type="auto">
  <name>Task 2: Create SearchPreview.astro (ASSET-01)</name>
  <files>getgeolens.com/src/components/previews/SearchPreview.astro</files>
  <read_first>
    - /Users/ishiland/Code/getgeolens.com/src/components/previews/BrowserFrame.astro (just created — confirm slot pattern and import path)
    - /Users/ishiland/Code/geolens/frontend/src/pages/LandingPage.tsx (lines 41-138 — reference ProductPreview with all 3 cards and SVGs)
    - /Users/ishiland/Code/geolens/.planning/phases/214-product-preview-assets/214-UI-SPEC.md (SearchPreview spec at lines 132-164)
    - /Users/ishiland/Code/geolens/.planning/phases/214-product-preview-assets/214-RESEARCH.md (lines 162-199 — Pattern 2 code, lines 412-443 — badge code examples)
    - /Users/ishiland/Code/getgeolens.com/src/styles/global.css (confirm token names)
  </read_first>
  <action>
Create `getgeolens.com/src/components/previews/SearchPreview.astro` per D-04, D-02, D-03.

**Frontmatter:**
```
import BrowserFrame from './BrowserFrame.astro';
```

**Outer structure:**
```astro
<BrowserFrame url="app.geolens.io/search">
  <!-- inner content here -->
</BrowserFrame>
```

**Inner content (top to bottom):**

**1. Search bar row** — `<div class="px-4 py-3 border-b" style="border-color: var(--border);">`:
  - Inner rounded input container: `flex items-center gap-2 rounded-lg border px-3 py-2` with `border-color: var(--border)`
  - Search SVG icon: `size-3.5 shrink-0` with `color: var(--muted-foreground)`, viewBox="0 0 24 24", fill="none", stroke="currentColor", strokeWidth="2.5": `<circle cx="11" cy="11" r="8" /><path d="m21 21-4.3-4.3" />`
  - Placeholder text: `<span class="text-[11px]" style="color: var(--muted-foreground);">Search geospatial data...</span>`

**2. Filter tabs row** — `<div class="flex items-center gap-1 px-4 py-2 border-b" style="border-color: var(--border);">`:
  - Active tab "All (207)": `rounded-md px-2 py-1 text-[10px] font-semibold` with `background: var(--surface-3); color: var(--foreground);`
  - Inactive tabs: `px-2 py-1 text-[10px]` with `color: var(--muted-foreground);`
    - "Vector (204)"
    - "Raster (3)"
    - "Table (3)"
  - Results count: `ml-auto text-[9px]` with `color: var(--muted-foreground); opacity: 0.6;` — text "207 results"

**3. Dataset cards** — `<div class="divide-y" style="border-color: oklch(0.92 0 0 / 10%);">`:

**Card 1 — Nys Aquifers:** `<div class="flex gap-3 px-4 py-3">`
  - Left column (`min-w-0 flex-1 space-y-1`):
    - Vector badge: `inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-[10px] font-semibold` with `background: color-mix(in oklch, var(--primary) 12%, transparent); color: var(--primary-700);`
      - Badge icon SVG: `size-2` viewBox="0 0 24 24" fill="currentColor": `<path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5" />`
      - Text: "Vector"
    - Name: `<p class="text-[13px] font-semibold" style="color: var(--primary-700);">Nys Aquifers</p>`
    - Description: `<p class="text-[11px]" style="color: var(--muted-foreground);">Aquifer polygons in New York State</p>`
    - Stats: `<p class="text-[9px]" style="color: var(--muted-foreground); opacity: 0.6;">MultiPolygon · 907 features · EPSG:4326</p>`
  - Right column — quicklook thumbnail: `<div class="size-14 shrink-0 overflow-hidden rounded-md" style="background: color-mix(in oklch, var(--muted) 12%, transparent);">`
    - SVG: viewBox="0 0 60 60", fill="none" containing 3 polygon shapes approximating NYS aquifer outlines:
      - `<polygon points="12,18 28,10 42,14 48,28 44,42 32,48 18,44 10,32" fill="var(--primary)" fill-opacity="0.12" stroke="var(--primary)" stroke-opacity="0.4" stroke-width="0.6" />`
      - `<polygon points="22,22 34,16 46,24 44,38 34,44 20,38" fill="var(--primary)" fill-opacity="0.08" stroke="var(--primary)" stroke-opacity="0.3" stroke-width="0.5" />`
      - `<polygon points="16,30 26,22 38,28 36,40 24,44" fill="var(--primary)" fill-opacity="0.1" stroke="var(--primary)" stroke-opacity="0.35" stroke-width="0.5" />`

**Card 2 — Nys Address Points:** `<div class="flex gap-3 px-4 py-3">`
  - Left column: Same structure as Card 1 but:
    - Vector badge (same styling as Card 1)
    - Name: `<p class="text-[13px] font-semibold" style="color: var(--foreground);">Nys Address Points</p>` (foreground, not primary-700)
    - Description: "Addressed properties in New York State"
    - Stats: "Point · 202,000 features · EPSG:4326"
  - Right column — quicklook: SVG with 12 scattered circles simulating address points:
    - viewBox="0 0 60 60", 12 `<circle>` elements at varied positions (cx/cy) and radii (1.3-2.0), all `fill="var(--primary)"` with fill-opacity ranging 0.4-0.6. Use exact positions from LandingPage.tsx reference: cx/cy pairs (18,20), (30,15), (42,22), (24,32), (36,28), (14,40), (28,44), (44,38), (38,48), (20,48), (48,14), (10,28).

**Card 3 — Bulletin:** `<div class="flex gap-3 px-4 py-3">`
  - Left column:
    - Table badge: `inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-[10px] font-semibold` with `background: oklch(0.75 0.15 85 / 15%); color: oklch(0.50 0.12 60);` — amber treatment per UI-SPEC
    - Name: `<p class="text-[13px] font-semibold" style="color: var(--foreground);">Bulletin</p>`
    - Description: "Initial creation 11/6/23"
    - Stats: "29 rows"
  - Right column: Empty thumbnail placeholder `<div class="size-14 shrink-0 rounded-md" style="background: color-mix(in oklch, var(--muted) 12%, transparent);"></div>` (table has no geometry)

**Do NOT add:** Any `transition`, `hover:`, `animation`, `<button>`, `<a>`, heading tags, or `client:*` directive. Use inline `style=""` for all brand colors (not Tailwind color classes). Use Tailwind only for structural utilities (flex, px, rounded, etc.).
  </action>
  <verify>
    <automated>cd /Users/ishiland/Code/getgeolens.com && npx astro check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at getgeolens.com/src/components/previews/SearchPreview.astro
    - File contains `import BrowserFrame from './BrowserFrame.astro'`
    - File contains `url="app.geolens.io/search"` on BrowserFrame usage
    - File contains "Search geospatial data..." placeholder text
    - File contains "All (207)" active filter tab
    - File contains "Vector (204)", "Raster (3)", "Table (3)" inactive tabs
    - File contains "207 results" count text
    - File contains "Nys Aquifers" with `color: var(--primary-700)` on its name
    - File contains "Nys Address Points" dataset card
    - File contains "Bulletin" dataset card with amber badge styling `oklch(0.75 0.15 85`
    - File contains at least 3 `<polygon` elements in Nys Aquifers quicklook SVG
    - File contains at least 10 `<circle` elements in Nys Address Points quicklook SVG
    - File contains "MultiPolygon · 907 features · EPSG:4326" stats line
    - `npx astro check` exits without errors
  </acceptance_criteria>
  <done>SearchPreview.astro renders a marketing-grade catalog UI with search bar, filter tabs, and three dataset cards (Nys Aquifers, Nys Address Points, Bulletin) with type badges, descriptions, stats, and inline SVG quicklook thumbnails, all wrapped in BrowserFrame with the URL "app.geolens.io/search".</done>
</task>

</tasks>

<verification>
- `cd /Users/ishiland/Code/getgeolens.com && npx astro check` exits 0 (type/import validation)
- BrowserFrame.astro contains `<slot />`, `aria-hidden="true"`, perspective tilt CSS, responsive breakpoints
- SearchPreview.astro imports BrowserFrame and renders 3 dataset cards with correct copy per UI-SPEC copywriting contract
</verification>

<success_criteria>
- BrowserFrame.astro exists with window chrome, perspective tilt, glow, responsive breakpoints, and slot
- SearchPreview.astro exists with search bar, filter tabs, 3 cards, inline SVG quicklooks
- `npx astro check` passes
- No JavaScript runtime code (no client:* directives, no transition/animation CSS)
</success_criteria>

<output>
After completion, create `.planning/phases/214-product-preview-assets/214-01-SUMMARY.md`
</output>
