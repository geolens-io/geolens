---
phase: 1053-quickstart-docs-environment-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
autonomous: true
requirements:
  - EW-04

must_haves:
  truths:
    - "A new contributor reading `.env.example` understands which DATABASE_SSL_MODE value to set for local Docker Compose dev (`prefer` works after the BU-01 fix; `disable` is the explicit local-dev recommendation if BU-01 ever regresses)."
    - "The audit's BU-01 root cause (empty-string DATABASE_SSL_MODE → strict SSL → migrate rejection) is impossible to silently re-create by a fresh `cp .env.example .env` flow because the file documents the intended local-dev value inline."
  artifacts:
    - path: ".env.example"
      provides: "Documented DATABASE_SSL_MODE local-dev hint"
      contains: "DATABASE_SSL_MODE"
  key_links:
    - from: ".env.example"
      to: "docker-compose.yml x-db-ssl-env anchor"
      via: "DATABASE_SSL_MODE env var"
      pattern: "DATABASE_SSL_MODE"
---

<objective>
Add a defense-in-depth local-dev hint to `.env.example` for `DATABASE_SSL_MODE`. The BU-01 critical bug (fixed in this repo at commit `7b168bde`) traced to the compose anchor falling back to empty-string when this variable was commented out. The compose-side fallback is now `prefer`, so the literal bug is closed — but the `.env.example` line for `DATABASE_SSL_MODE` is still commented out, meaning a future contributor copying `.env.example → .env` and then explicitly setting `DATABASE_SSL_MODE=` (empty) would re-create the exact failure mode. This plan adds an inline comment explaining `prefer` vs `disable` vs `require` so future contributors understand the SSL stance for each deployment target.

Purpose: Close EW-04 — defense-in-depth against BU-01 regression by making the local-dev expected value visible inline.

Output: One edited file (`.env.example`) with an enhanced comment block above the `DATABASE_SSL_MODE` line.

Repo: this repo (`/Users/ishiland/Code/geolens/`). Cross-repo: no.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/1053-quickstart-docs-environment-hardening/1053-CONTEXT.md

# Source-of-truth: audit's EW-04 section + BU-01 root-cause analysis
@.planning/M001-7n8vpc-dry-run-audit.md

# File being modified
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance DATABASE_SSL_MODE comment block in .env.example</name>
  <files>.env.example</files>
  <action>Locate the existing `DATABASE_SSL_MODE` block in `.env.example` (currently around line 365-368, ending with the commented `# DATABASE_SSL_MODE=prefer` line). Replace the existing comment block with an expanded version that documents all three local-dev-relevant values and calls out the BU-01 root cause as a guard rail. The new block must:
  1. Keep the existing options summary (`disable, prefer, require, verify-full`) and the existing `Default: prefer` note.
  2. Add a "For local docker-compose dev" recommendation paragraph explaining that the bundled `postgres` image accepts `prefer` (asyncpg attempts SSL then falls back to plain — works post-BU-01 fix); managed Postgres (RDS, Cloud SQL) typically wants `require` or `verify-full`; local non-docker dev against a system postgres usually wants `disable`.
  3. Keep the commented `# DATABASE_SSL_MODE=prefer` example line at the end (active form, just commented out — matches the existing convention for other variables in this file).
  4. Stay under ~12 lines of comment prose; this is a hint, not an essay.

  Do not change the variable name, the default semantic, or any other lines. Do not add executable env-var assignments — only documentation. Do not touch `DATABASE_SSL_CA_CERT` or related lines.

  Acceptance shape:
  ```
  # [OPTIONAL] Database SSL mode
  # Type: string | Default: prefer | Options: disable, prefer, require, verify-full
  # Note: verify-full requires DATABASE_SSL_CA_CERT to be set.
  #
  # Recommended values per deployment target:
  #   - Local docker-compose dev (bundled postgres image): prefer
  #       asyncpg attempts SSL and transparently falls back to plain TCP if the
  #       postgres image isn't built with TLS. Empty-string was the BU-01 root
  #       cause — use prefer, not "".
  #   - Local non-docker dev (system postgres without TLS): disable
  #   - Managed Postgres (RDS, Cloud SQL, etc.): require or verify-full
  # DATABASE_SSL_MODE=prefer
  ```

  (Exact prose may vary slightly — the requirement is the recommendation table and the BU-01 mention, not verbatim wording.)
  </action>
  <verify>
    <automated>grep -c "DATABASE_SSL_MODE" .env.example | grep -E "^[3-9]|^[0-9][0-9]" && grep -q "Local docker-compose dev" .env.example && grep -q "Managed Postgres\|managed Postgres" .env.example</automated>
    <human-check>Open `.env.example`, locate the `DATABASE_SSL_MODE` block (search for the string). Verify the comment block explains `prefer` / `disable` / `require` for the three deployment-target rows and mentions BU-01 (or "empty-string") as the failure mode to avoid. The variable itself stays commented out.</human-check>
  </verify>
  <done>`.env.example` has an expanded comment block above the `DATABASE_SSL_MODE` line that explains the recommended value for local docker-compose dev (prefer), local non-docker dev (disable), and managed Postgres (require/verify-full); BU-01 root-cause is mentioned as a guard rail; the line itself stays commented out per the existing file convention.</done>
</task>

</tasks>

<verification>
File-level acceptance:
- `grep "DATABASE_SSL_MODE" .env.example` shows at least 3 occurrences (variable name appears in the heading line, in the recommendation prose, and in the commented assignment line).
- `grep -c "^# DATABASE_SSL_MODE=prefer$" .env.example` returns 1 (the commented active-form line is preserved).
- `grep -c "^DATABASE_SSL_MODE=" .env.example` returns 0 (no uncommented assignment was added — `.env.example` must stay safe to copy verbatim).
- Diff stays small (single-block edit, no other lines touched).

Smoke gate:
- No tests required — docs-only change to an example/template file. Backend doesn't load `.env.example` directly.
- Optional manual: run `docker compose config 2>&1 | grep -i error` to confirm the example file still parses cleanly when copied to `.env`. Not required for this plan.
</verification>

<success_criteria>
EW-04 closed:
1. `.env.example` documents the local-dev expected `DATABASE_SSL_MODE` value inline so future contributors don't have to grep the audit, the changelog, or the compose anchor to understand why `prefer` is the right default.
2. The BU-01 root cause is mentioned by name (or its failure shape — "empty-string") in the comment so the regression mode is documented at the file where the bug is most likely to recur.
3. No other lines in `.env.example` are touched.
</success_criteria>

<output>
Create `.planning/phases/1053-quickstart-docs-environment-hardening/1053-01-SUMMARY.md` when done — record:
- The before/after diff hunk for the `DATABASE_SSL_MODE` block.
- Commit SHA on `main`.
- One-line credit: "EW-04 closed — defense-in-depth against BU-01."
</output>
