---
phase: 1044
plan: "04"
type: execute
wave: 3
depends_on: ["1044-01", "1044-02", "1044-03"]
files_modified: []
autonomous: false
requirements:
  - POL-25
tags:
  - smoke
  - e2e
  - milestone-close
  - gate

must_haves:
  truths:
    - "`npm run e2e:smoke:builder` exits 0 with every spec passing under Chromium."
    - "`npm run test:i18n` exits 0 (parity gate green from Plan 01)."
    - "`npx vitest run src/components/builder/` reports 0 failures, 0 worker errors, count strictly greater than the post-Phase-1041 baseline of 764 (after Plan 02 added a11y tests)."
    - "`npx tsc -b --noEmit` reports 0 errors in the frontend package."
    - "Any failure surfaced by the smoke run is either fixed inline within this plan, or reported as a P0 blocker that defers milestone close — NOT silently skipped or ignored."
  artifacts:
    - path: .planning/phases/1044-cross-cutting-closeout/1044-04-SUMMARY.md
      provides: "Final smoke results table + milestone-close GO/NO-GO recommendation"
      contains: "e2e:smoke:builder"
  key_links:
    - from: package.json
      to: e2e/builder-v1-5.spec.ts
      via: "e2e:smoke:builder script (added by Plan 03)"
      pattern: "builder-v1-5"
    - from: .planning/phases/1044-cross-cutting-closeout/1044-04-SUMMARY.md
      to: ROADMAP.md / STATE.md
      via: "Milestone close trigger; orchestrator promotes v1009 → shipped if SUMMARY shows GO"
      pattern: "GO|NO-GO"
---

<objective>
Run the final milestone-close smoke gate: `npm run e2e:smoke:builder` (which now includes `builder-v1-5.spec.ts` from Plan 03), the i18n parity test (from Plan 01), and the builder vitest suite (which now includes the a11y tests from Plan 02). Address any failures inline OR explicitly mark as deferred with justification. Produce the final SUMMARY that the milestone-close orchestrator reads to decide GO/NO-GO.

Purpose: POL-25 is the final v1009 contract. The milestone cannot close until the smoke set is green. This plan exists as a separate wave because it depends on the artifacts produced by Plans 01-03 and because it has a checkpoint nature — if smoke fails, the human reviewer needs to see the failure and decide between fix-inline vs defer.

Output: Run logs (in the SUMMARY), GO/NO-GO recommendation, and any inline fixes to flake or trivially broken assertions discovered during the gate run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1044-cross-cutting-closeout/1044-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/1044-cross-cutting-closeout/1044-01-PLAN.md
@.planning/phases/1044-cross-cutting-closeout/1044-02-PLAN.md
@.planning/phases/1044-cross-cutting-closeout/1044-03-PLAN.md

@package.json
@e2e/builder-v1-5.spec.ts
@e2e/builder.spec.ts
@e2e/builder-styling.spec.ts
@frontend/src/i18n/resources.test.ts

<interfaces>
<!-- The smoke gate command set. Final output is GO/NO-GO. -->

Commands the gate runs (in order):
1. `cd frontend && npm run test:i18n`
2. `cd frontend && npx vitest run src/components/builder/ --reporter=basic`
3. `cd frontend && npx tsc -b --noEmit`
4. `npm run e2e:smoke:builder` (run from repo root; Playwright config at /Users/ishiland/Code/geolens/playwright.config.ts)

Required precondition for the e2e gate:
- Local dev stack must be UP (`docker compose up -d` per CLAUDE.md). The frontend at `http://localhost:8080` and backend `/api/*` must be reachable. Playwright auth state file at `playwright/.auth/user.json` must exist.
- If the stack is down or auth is missing, the executor MUST surface this as a precondition failure in the SUMMARY; do NOT skip the e2e gate to make the smoke "pass."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the i18n + vitest + typecheck gates and capture results</name>
  <files>(no files modified; gate run only)</files>
  <read_first>
    - .planning/phases/1044-cross-cutting-closeout/1044-01-PLAN.md (locale parity expectations)
    - .planning/phases/1044-cross-cutting-closeout/1044-02-PLAN.md (a11y test expectations)
  </read_first>
  <action>
    Run from `frontend/`:
    1. `npm run test:i18n` — expected: 2 tests pass (resources.test.ts).
    2. `npm run check:i18n:changed` — expected: pass (validates locale files are well-formed across changed namespaces).
    3. `npx vitest run src/components/builder/ --reporter=basic` — expected: 0 failures, 0 worker errors, test count strictly greater than 764 (post-Plan-02 should add ~14 tests → expect ~778).
    4. `npx tsc -b --noEmit` — expected: 0 errors in frontend package.
    5. `npx vitest run src/i18n/ --reporter=basic` — expected: 0 failures (full i18n suite, redundant with test:i18n but catches any side files).

    For each command, capture:
    - Pass/fail status and exit code.
    - Test counts (total / passed / failed).
    - First 3 failure lines (if any) with the test file path and assertion message.

    If ANY command fails:
    - DIAGNOSE the failure: is it a real regression in the v1.5 work, or a flake/environmental issue?
    - If a real regression originating in Plans 01/02: fix inline (the fix lives in this plan's commit chain). Do NOT silently revert Plans 01/02 work.
    - If a flake: re-run the same command up to 2 times. If it passes on re-run, note the flake in the SUMMARY but record the gate as GREEN; do NOT mask consistent failures.
    - If a pre-existing failure unrelated to v1009 (e.g., Phase 1039 test debt that POL-19/20/21 was supposed to close but slipped): record it in the SUMMARY and surface to the milestone-close orchestrator as a deferred item — do NOT fix in this plan.

    Hold all results until Task 4 — the SUMMARY captures everything as one composite report.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run test:i18n 2>&amp;1 | tail -5 | grep -E "(Tests\s+\d+ passed|Test Files\s+\d+ passed)" &amp;&amp; npx tsc -b --noEmit 2>&amp;1 | grep -c "error TS" | awk '{ if ($1 == 0) exit 0; else exit 1 }'</automated>
  </verify>
  <done>
    All four commands' results captured (pass/fail + counts + any failure lines). i18n gate green. Typecheck green. Builder vitest green (count > 764, 0 failures, 0 worker errors). Any flakes documented; any regressions either fixed inline or escalated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run e2e:smoke:builder and capture results</name>
  <files>(no files modified; e2e run only — unless inline fix is required)</files>
  <read_first>
    - package.json (the `e2e:smoke:builder` script value, post-Plan-03)
    - .planning/phases/1044-cross-cutting-closeout/1044-03-PLAN.md (Test 1-4 expectations)
    - playwright.config.ts (timeouts, baseURL, retry config)
  </read_first>
  <action>
    Precondition check FIRST (before running):
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/` — expect 200 or 30x. If non-2xx/3xx, the dev stack is down — STOP and surface as precondition failure.
    - `test -f playwright/.auth/user.json` — if missing, run the project's auth setup (`npx playwright test e2e/auth.setup.ts --project=chromium` per the playwright config) before the smoke run.

    Then run from repo root:
    `npm run e2e:smoke:builder 2>&amp;1 | tee /tmp/1044-04-smoke.log`

    Capture from the log:
    - Total test count expected (3 specs × N tests each — `builder.spec.ts` 16 + `builder-styling.spec.ts` 4 + `builder-v1-5.spec.ts` 4 = ~24 by spec count, but Playwright may count parametrized tests differently).
    - Actual passed / failed / skipped counts.
    - Names of any failing tests with the first failure line.
    - Total runtime.

    Failure handling (same rules as Task 1):
    - Real v1009 regression → fix inline; commit the fix.
    - Flake → re-run the failing spec ONLY (up to 2 times): `npx playwright test e2e/builder-v1-5.spec.ts --project=chromium --retries=2`. If consistently passes after retry, note flake in SUMMARY; if still failing, surface as a real failure.
    - Pre-existing failure in `builder.spec.ts` or `builder-styling.spec.ts` unrelated to v1009 → record and surface; do NOT fix in this plan (would expand scope).

    Common pre-existing flake categories the executor should recognize and not panic about:
    - Tile-loading timeouts on cold starts → re-run usually clears.
    - MapLibre `Unable to load glyph range` warnings → already filtered by `assertConsoleClean`.
    - Network race in beforeAll dataset listing → builder.spec.ts:86 has fallback dataset upload; verify it didn't crash the suite.

    The goal is HONEST reporting, not masking. A green smoke that hides a real regression is worse than a red smoke that gets surfaced.
  </action>
  <verify>
    <automated>npm run e2e:smoke:builder 2>&amp;1 | tail -10 | grep -E "([0-9]+ passed|passed \([0-9]+m?[0-9]+s\))" &amp;&amp; ! npm run e2e:smoke:builder 2>&amp;1 | tail -10 | grep -E "([0-9]+ failed)"</automated>
  </verify>
  <done>
    `npm run e2e:smoke:builder` exits 0; all included specs pass. The full log is saved to `/tmp/1044-04-smoke.log` and the key counts (passed / failed / skipped / runtime) are recorded for the SUMMARY. If any failure occurred, it is either fixed inline OR escalated to the SUMMARY's "Deferred / Escalated" section.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review of gate results before milestone close</name>
  <files>(no files modified; checkpoint only)</files>
  <action>
    PAUSE for human verification of the gate suite results captured in Tasks 1-2.

    The executor should paste the gate results into the agent transcript before this checkpoint pauses. No automated action is taken in this task — it is a hard stop awaiting reviewer approval.
  </action>
  <what-built>
    Plans 01-03 shipped i18n locale parity, a11y verification + walkthrough doc, and the new `e2e/builder-v1-5.spec.ts` UAT spec. Tasks 1+2 of THIS plan ran the full gate suite (i18n + vitest + typecheck + e2e:smoke:builder).
  </what-built>
  <how-to-verify>
    The executor will paste the gate results into the agent transcript before this checkpoint. The reviewer should:

    1. Read the i18n + vitest + typecheck output. All green? → continue.
    2. Read the `e2e:smoke:builder` output. All specs green? → continue.
    3. If any test was flaky (re-run to pass): is the flake category one we accept (tile-loading, glyph-range)? → approve. Or is it new/concerning? → request fix-up.
    4. Spot-check the `1044-A11Y-WALKTHROUGH.md` doc — does it look reusable as a manual UAT script for the next milestone reviewer? → approve.
    5. Spot-check the i18n locale files at 3 keys (e.g., `de.bulkActions.delete`, `es.a11y.dragPickup`, `fr.settings.terrainInactiveHint`) — are the translations native, not English passthrough? → approve.

    If any of the 5 spot-checks reveals a problem:
    - Reply with the specific finding (e.g., "es.a11y.dragPickup is still English: 'Picked up...'").
    - The executor will fix it and re-run the affected gate command.

    If all 5 spot-checks pass and the gate suite is green:
    - Reply `approved` to authorize Task 4 (write final SUMMARY + milestone-close handoff).
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint task — manual approval required; no automated verification."</automated>
  </verify>
  <done>
    Reviewer responded `approved` (or equivalent affirmative) after spot-checking the 5 items above.
  </done>
  <resume-signal>Type "approved" to authorize milestone-close SUMMARY, OR describe specific issues to fix before approval.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Write final SUMMARY + milestone-close handoff</name>
  <files>.planning/phases/1044-cross-cutting-closeout/1044-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/1044-cross-cutting-closeout/1044-01-SUMMARY.md (Plan 01 results — i18n)
    - .planning/phases/1044-cross-cutting-closeout/1044-02-SUMMARY.md (Plan 02 results — a11y)
    - .planning/phases/1044-cross-cutting-closeout/1044-03-SUMMARY.md (Plan 03 results — UAT spec)
    - /tmp/1044-04-smoke.log (Task 2 output)
    - .planning/REQUIREMENTS.md (POL-22..POL-25 contracts to verify)
    - .planning/STATE.md (current milestone progress)
  </read_first>
  <action>
    Create `.planning/phases/1044-cross-cutting-closeout/1044-04-SUMMARY.md` per the project's SUMMARY template (`@$HOME/.claude/get-shit-done/templates/summary.md`).

    Required sections:

    **Frontmatter:** standard SUMMARY frontmatter — phase: 1044, plan: "04", subsystem: testing/closeout, tags: [smoke, e2e, milestone-close], dependency_graph naming Plans 01-03 as `requires`, key_files unmodified (this plan ran gates, no source change unless inline fix occurred).

    **One-liner:** "Final v1009 milestone smoke gate — `npm run e2e:smoke:builder` green at N/N tests, i18n parity green, builder vitest at M tests with 0 failures, GO/NO-GO recommendation: GO."

    **Gate Results table:**
    | Gate | Command | Status | Counts | Runtime |
    |------|---------|--------|--------|---------|
    | i18n parity | `npm run test:i18n` | PASS | 2/2 | <1s |
    | i18n changed-namespace | `npm run check:i18n:changed` | PASS | n/a | <2s |
    | typecheck | `npx tsc -b --noEmit` | PASS | 0 errors | <30s |
    | builder vitest | `npx vitest run src/components/builder/` | PASS | M/M (0 fail, 0 worker) | <60s |
    | builder smoke (Playwright) | `npm run e2e:smoke:builder` | PASS | N/N | actual minutes:seconds |

    **POL Requirements Coverage table:**
    | Requirement | Plan | Status | Evidence |
    |---|---|---|---|
    | POL-22 (i18n locale fill) | 1044-01 | ✅ | resources.test.ts green; sampled native translations confirmed |
    | POL-23 (a11y verification) | 1044-02 | ✅ | UnifiedStackPanel.a11y.test.tsx + MapBuilderPage.a11y.test.tsx + walkthrough doc |
    | POL-24 (Playwright UAT spec) | 1044-03 | ✅ | e2e/builder-v1-5.spec.ts 4/4 pass |
    | POL-25 (smoke green at close) | 1044-04 | ✅ | e2e:smoke:builder green; vitest baseline maintained |

    **v1009 Milestone Coverage table** (cross-reference all POL-01..25 → status):
    Pull from REQUIREMENTS.md the full POL-01..25 list with current status. Note any items still showing `[ ]` and confirm whether they are now satisfied by Plans 01-03 outputs (mostly the [ ] items in the POL-19..25 range that were `not started` per ROADMAP — they should now flip to `[x]` once their plans' SUMMARYs are written).

    **Deferred / Escalated section:**
    Any test flake observed (re-run-to-pass), any pre-existing failures that were NOT v1009-introduced, any open follow-ups. Be honest — silent deferrals come back as P0s next milestone.

    **GO/NO-GO Recommendation:**
    State explicitly: "Recommendation: GO — milestone v1009 ready to close" or "Recommendation: NO-GO — blockers: [list]." If GO, suggest the next orchestrator step: `/gsd-complete-milestone v1009` (or whatever the project's close command is — check STATE.md or recent milestones for the convention).

    **Pattern lessons for v1010+:** anything from this plan worth carrying forward (e.g., "ship native i18n alongside English in the introducing phase, not as deferred debt"; "destructive-confirm autoFocus is a portable a11y pattern"; "Playwright keyboard sensor more reliable than pointer for cross-context drag in headless Chromium").
  </action>
  <verify>
    <automated>test -f .planning/phases/1044-cross-cutting-closeout/1044-04-SUMMARY.md &amp;&amp; grep -c "^## Gate Results\|^## POL Requirements\|^## v1009 Milestone\|^## GO/NO-GO\|^## Deferred" .planning/phases/1044-cross-cutting-closeout/1044-04-SUMMARY.md | awk '{ if ($1 &gt;= 4) exit 0; else { print "ERROR: only " $1 " required sections; expected at least 4"; exit 1 } }'</automated>
  </verify>
  <done>
    1044-04-SUMMARY.md exists with the four required sections (Gate Results, POL Coverage, GO/NO-GO Recommendation, Deferred/Escalated). The recommendation is explicit (GO or NO-GO with reasons). The pattern-lessons section captures at least 2 carry-forwards for v1010+. The orchestrator reading this SUMMARY can make a milestone-close decision without needing to re-run any gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke runner → milestone-close decision | A green smoke triggers milestone close in the orchestrator; a false-green ships broken code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1044-13 | Repudiation (false-green smoke) | re-run-to-pass tolerance | mitigate | Task 2 caps re-runs at 2; consistent failure surfaces. Task 3 checkpoint requires human review of the actual log, not just the exit code. |
| T-1044-14 | Tampering (silently masking failures) | Task 1+2 result reporting | mitigate | "Hold all results until Task 4" rule; SUMMARY's Deferred/Escalated section forces honest reporting. Pattern-lessons section creates institutional memory of past masking attempts. |
| T-1044-15 | DoS (gate run exhausts CI minutes) | Playwright e2e | accept | smoke set is intentionally bounded (3 specs × ~24 tests); runtime <10min on local stack. |
</threat_model>

<verification>
- All 4 gate commands (i18n, typecheck, vitest, e2e:smoke:builder) exit 0.
- Human checkpoint approved (Task 3 receives `approved`).
- 1044-04-SUMMARY.md exists with required sections + explicit GO/NO-GO.
- All POL-22..POL-25 marked ✅ with evidence.
- Any flake or pre-existing failure escalated, not masked.
</verification>

<success_criteria>
1. `npm run e2e:smoke:builder` green; all included specs pass.
2. i18n parity, builder vitest, typecheck all green.
3. Final SUMMARY recommends GO (or NO-GO with explicit blocker list).
4. POL-22, POL-23, POL-24, POL-25 all satisfied with traceable evidence.
5. Milestone v1009 ready for close.
</success_criteria>

<output>
After completion, the orchestrator reads `.planning/phases/1044-cross-cutting-closeout/1044-04-SUMMARY.md` and runs the project's milestone-close workflow (`/gsd-complete-milestone v1009` or equivalent). v1009 progresses from "in progress" → "shipped" in ROADMAP.md and STATE.md.
</output>
