# Phase 1031 Plan: Cluster Performance Browser QA Closeout

## Goal

Validate v1006 end to end against automated gates and a live large point dataset so server-side Cluster is proven beyond unit-level routing.

## Scope

- Run focused backend coverage for cluster tile SQL, auth, cache keys, empty tiles, embed-token access, and style JSON interop.
- Run focused frontend coverage for cluster eligibility, source routing, map sync, companion layers, viewer behavior, and tile URL construction.
- Run i18n, lint, build, ruff, and existing builder smoke gates.
- Use Playwright MCP against a synthetic 6,001-feature point dataset to verify live server-cluster map load, signed tile requests, console cleanliness, and cluster popup interaction.
- Fix any milestone-scoped UAT blockers found during browser testing.

## Acceptance

- QA-01..QA-06 are complete.
- Large point Cluster avoids full-table GeoJSON and uses `/tiles/clusters/...` MVT URLs.
- Private dataset cluster tile requests include existing HMAC vector-tile params.
- Browser UAT current-page console has zero warnings/errors.
- Phase and milestone archive docs capture verification evidence and residual caveats.
