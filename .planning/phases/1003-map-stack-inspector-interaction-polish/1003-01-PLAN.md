---
phase: 1003-map-stack-inspector-interaction-polish
plan: 01
type: standard
wave: 1
depends_on: []
autonomous: true
requirements: [STACK-02]
files_modified:
  - backend/app/modules/catalog/maps/service_layers.py
  - backend/tests/test_maps.py
---

<objective>
Make add-layer ordering deterministic when API callers omit `sort_order`, closing F-1002-01 for future maps without changing explicit import/order payload behavior.
</objective>

<context>
@.planning/phases/1003-map-stack-inspector-interaction-polish/1003-CONTEXT.md
@.planning/phases/1002-kepler-guided-builder-workflow-audit-and-triage/1002-IMPLEMENTATION-INVENTORY.md
@backend/app/modules/catalog/maps/service_layers.py
@backend/app/modules/catalog/maps/schemas.py
@backend/tests/test_maps.py
</context>

<tasks>
  <task type="auto">
    <name>Task 1: Assign next available layer order when omitted</name>
    <files>backend/app/modules/catalog/maps/service_layers.py</files>
    <action>
      Update `add_layer` so `MapLayerInput` payloads that do not include `sort_order` use the next available order for the map. Preserve explicit `sort_order`, including explicit zero.
    </action>
    <verify>
      `cd backend && uv run pytest tests/test_maps.py -k "add_layer"`
    </verify>
    <done>
      Repeated add-layer calls without `sort_order` produce unique sequential sort orders while explicit sort orders are unchanged.
    </done>
  </task>

  <task type="auto">
    <name>Task 2: Cover omitted sort order insertion</name>
    <files>backend/tests/test_maps.py</files>
    <action>
      Add endpoint coverage proving two omitted add-layer calls receive stable `sort_order` values and duplicate dataset insertions remain distinguishable by layer ID/order.
    </action>
    <verify>
      `cd backend && uv run pytest tests/test_maps.py -k "add_layer_assigns_next_sort_order_when_omitted or add_layer"`
    </verify>
    <done>
      Regression tests fail against the old duplicate-order behavior and pass after the service fix.
    </done>
  </task>
</tasks>

<verification>
- `cd backend && uv run pytest tests/test_maps.py -k "add_layer"`
- `cd backend && uv run ruff check app/modules/catalog/maps/service_layers.py tests/test_maps.py`
- `cd backend && uv run ruff format --check app/modules/catalog/maps/service_layers.py tests/test_maps.py`
</verification>

<success_criteria>
- [ ] Omitted sort orders no longer duplicate within a map.
- [ ] Explicit sort orders remain honored.
- [ ] Backend focused tests pass.
- [ ] STACK-02 has a stable persistence foundation for reorder/duplicate workflows.
</success_criteria>
