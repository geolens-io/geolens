---
phase: 1009-layer-row-and-dataset-rendering-sidebar
plan: 01
wave: 1
depends_on: []
autonomous: true
requirements: [STACK-01, STACK-02, STACK-03, STACK-04, STACK-05]
subsystem: frontend-builder
tags: [map-builder, sidebar, layer-row, dataset-rendering, tests]
files_modified:
  - frontend/src/components/builder/map-stack.ts
  - frontend/src/components/builder/MapStackPanel.tsx
  - frontend/src/components/builder/MapStackItem.tsx
  - frontend/src/pages/MapBuilderPage.tsx
  - frontend/src/components/builder/__tests__/MapStackPanel.test.tsx
  - .planning/phases/1009-layer-row-and-dataset-rendering-sidebar/1009-01-SUMMARY.md
---

<objective>
Ship the v1002 layer row and dataset-rendering sidebar presentation over the existing Map Stack model and existing layer write handlers.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/1009-layer-row-and-dataset-rendering-sidebar/1009-CONTEXT.md
@frontend/src/components/builder/renderAs.ts
@frontend/src/components/builder/map-stack.ts
@frontend/src/components/builder/MapStackPanel.tsx
@frontend/src/components/builder/MapStackItem.tsx
@frontend/src/components/builder/hooks/use-builder-layers.ts
@frontend/src/pages/MapBuilderPage.tsx
</context>

<tasks>
  <task type="auto">
    <name>Extend stack metadata for dataset headers</name>
    <files>frontend/src/components/builder/map-stack.ts</files>
    <action>Add current dataset metadata needed by headers, especially feature count, while keeping groups derived and preserving existing system group IDs.</action>
    <verify>Existing map-stack tests still pass.</verify>
    <done>STACK-01 and STACK-03 have a metadata foundation without schema changes.</done>
  </task>

  <task type="auto">
    <name>Render v1 primary layer row anatomy</name>
    <files>frontend/src/components/builder/MapStackItem.tsx, frontend/src/components/builder/MapStackPanel.tsx, frontend/src/pages/MapBuilderPage.tsx</files>
    <action>Add compact renderAs, opacity, and zoom-range controls to primary layer rows using existing UI primitives and existing opacity/layout handlers. Keep renderAs display-only in this phase.</action>
    <verify>Component tests can click opacity and zoom controls and observe existing callbacks only.</verify>
    <done>STACK-02 and STACK-05 are visible and wired to existing fields.</done>
  </task>

  <task type="auto">
    <name>Add data-group dataset-rendering headers</name>
    <files>frontend/src/components/builder/MapStackPanel.tsx</files>
    <action>Inside the `data` section, derive duplicate dataset groups by `dataset_id` and render a collapsible header above two-or-more renderings with current metadata only.</action>
    <verify>Tests prove headers appear for shared `dataset_id`, omit LIVE, and do not appear for single renderings.</verify>
    <done>STACK-03 and STACK-04 are covered.</done>
  </task>

  <task type="auto">
    <name>Cover row/header behavior</name>
    <files>frontend/src/components/builder/__tests__/MapStackPanel.test.tsx</files>
    <action>Add focused Testing Library coverage for row anatomy, dataset headers, opacity writes, and zoom-range writes.</action>
    <verify>`cd frontend && npm run test -- MapStackPanel map-stack renderAs --run` passes.</verify>
    <done>Phase 1009 can hand off to renderAs mutation work with row contracts locked.</done>
  </task>

  <task type="auto">
    <name>Record summary and verification</name>
    <files>.planning/phases/1009-layer-row-and-dataset-rendering-sidebar/1009-01-SUMMARY.md</files>
    <action>Document shipped behavior, requirements covered, and verification commands/results.</action>
    <verify>Summary exists and matches commands run.</verify>
    <done>Phase 1009 closeout is ready for commit.</done>
  </task>
</tasks>

<verification>
- `cd frontend && npm run test -- MapStackPanel map-stack renderAs --run`
- `cd frontend && npm run lint`
- `cd frontend && npm run build`
</verification>

<must_haves>
- No schema/API migration or persisted group shape.
- No renderAs mutation dispatch yet.
- Dataset headers use only existing API response metadata.
- Zoom controls write only existing custom layout fields `_minzoom` and `_maxzoom`.
- All UI uses existing `frontend/src/components/ui/*` primitives and lucide icons.
</must_haves>

<success_criteria>
- [x] Six system groups still render unchanged.
- [x] Primary rows expose drag, visibility, swatch, display name, renderAs, opacity, zoom range, and overflow actions.
- [x] Dataset-rendering headers appear only for duplicated dataset IDs.
- [x] Header metadata excludes unsupported LIVE/status claims.
- [x] Zoom controls call existing layout write paths with `_minzoom` / `_maxzoom`.
- [x] Focused tests, lint, and build pass.
</success_criteria>
