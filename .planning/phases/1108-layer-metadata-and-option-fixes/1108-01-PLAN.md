# Phase 1108 Plan: Layer Metadata and Option Fixes

**Requirements:** LAYER-01, LAYER-02, LAYER-03, LAYER-04

## Tasks

- Add frontend regression tests for render-mode-only and legacy nested render mode style configs.
- Patch `normalize-style-config.ts` to preserve/promote these render modes.
- Patch ADK composition to emit canonical label/render/outline metadata.
- Rerun the composition script to update the existing target map.
- Verify DEM editor and style JSON with Playwright MCP.
