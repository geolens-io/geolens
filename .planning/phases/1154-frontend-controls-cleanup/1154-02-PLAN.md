---
phase: 1154-frontend-controls-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 1154-01
files_modified:
  - frontend/src/components/builder/LayerStyleEditor/RasterEditor.tsx
  - frontend/src/components/builder/LayerStyleEditor/__tests__/RasterEditor.test.tsx
  - frontend/src/components/builder/DEMEditorScene.tsx
  - frontend/src/components/builder/__tests__/DEMEditorScene.test.tsx
  - frontend/src/i18n/locales/en/builder.json
  - frontend/src/i18n/locales/de/builder.json
  - frontend/src/i18n/locales/es/builder.json
  - frontend/src/i18n/locales/fr/builder.json
autonomous: true
requirements:
  - RASTER-STRETCH-03
  - RASTER-STRETCH-UI-01
  - RASTER-STRETCH-UI-02
  - CLEANUP-01
must_haves:
  truths:
    - "On a multi-band raster (band_count=3) the STRETCH select is present and the COLORMAP select is absent"
    - "On a single-band raster the COLORMAP select is present (unchanged behavior)"
    - "When stretch=percentile the Low%/High% inputs appear; client guards 0<=pmin<pmax<=100 (no out-of-range paint write)"
    - "When stretch=stddev the sigma segmented control (1/2/3, default 2) appears"
    - "The stretchColormapHint <p role=note> appears only when band_count===1 && stretch!==minmax && colormap!==gray"
    - "The unreachable hillshadeTerrainNote block is removed from DEMEditorScene and its 4 i18n keys are gone"
    - "test:i18n parity is green (equal key sets across en/de/es/fr)"
    - "onRenderModeChange remains absent from LayerStyleEditor/types.ts; the live LayerEditorPanel onRenderModeChange is untouched"
  artifacts:
    - path: "frontend/src/components/builder/LayerStyleEditor/RasterEditor.tsx"
      provides: "gate-split stretch (>=1) vs colormap (===1), pmin/pmax/sigma controls, stretch-colormap hint"
      contains: "_pmin"
    - path: "frontend/src/components/builder/DEMEditorScene.tsx"
      provides: "hillshade sub-section with the unreachable advisory note removed"
    - path: "frontend/src/i18n/locales/en/builder.json"
      provides: "stretchColormapHint + pmin/pmax/sigma labels added; hillshadeTerrainNote removed"
      contains: "stretchColormapHint"
  key_links:
    - from: "frontend/src/components/builder/LayerStyleEditor/RasterEditor.tsx"
      to: "_pmin / _pmax / _sigma paint keys"
      via: "onPaintProp writes consumed by buildColormapTileUrl (Plan 01)"
      pattern: "onPaintProp\\('_(pmin|pmax|sigma)'"
---

<objective>
Wire the RasterEditor UI to the stretch-bound URL contract delivered in Plan 01,
widen the stretch gate to multi-band rasters, add the single-band stretch↔colormap
hint, and complete CLEANUP-01 by removing the unreachable hillshade advisory note.

Purpose: Users can now configure percentile/sigma bounds and use stretch on
multi-band rasters; the v1033 dead-code tech debt is cleared. No behavior change
to the render-as flow or the hillshade error-suppression guard.

Output: Updated RasterEditor + DEMEditorScene + 4-locale i18n, with matching
vitest coverage and i18n parity preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/1154-frontend-controls-cleanup/1154-CONTEXT.md
@.planning/phases/1154-frontend-controls-cleanup/1154-UI-SPEC.md

<interfaces>
<!-- The URL contract from Plan 01 (1154-01-SUMMARY.md). The paint keys this editor writes. -->
buildColormapTileUrl (raster-adapter.ts) reads these builder-private paint keys:
  _colormap  (string, default 'gray')      → colormap_name (single-band only)
  _stretch   ('minmax'|'percentile'|'stddev', default 'minmax')
  _pmin      (number, default 2)  → pmin   (forwarded only when _stretch==='percentile' && !== 2)
  _pmax      (number, default 98) → pmax   (forwarded only when _stretch==='percentile' && !== 98)
  _sigma     (1|2|3, default 2)   → sigma  (forwarded only when _stretch==='stddev'   && !== 2)

From RasterEditor.tsx (BaseStyleEditorProps):
  onPaintProp: (key: string, value: unknown) => void   // single-key write, builder-alias routing
  layer.band_count: number | null | undefined
  paint['_stretch'] ?? 'minmax', paint['_colormap'] ?? 'gray'
Existing token classes to reuse: Select trigger `h-8 text-xs flex-1`, label `w-28 shrink-0 text-xs text-muted-foreground`.

CONFIRMED ABSENT (do NOT add): onRenderModeChange is NOT a member of
LayerStyleEditor/types.ts (44-line file, full read). The LIVE onRenderModeChange in
LayerEditorPanel.tsx:32/:265 (render-as confirm flow) is a SEPARATE wiring — do not touch it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split the band_count gate and add pmin/pmax/sigma controls + hint in RasterEditor</name>
  <files>frontend/src/components/builder/LayerStyleEditor/RasterEditor.tsx</files>
  <read_first>
    - frontend/src/components/builder/LayerStyleEditor/RasterEditor.tsx (lines 182-242 — the current single `band_count === 1` COLORMAP+STRETCH section)
    - .planning/phases/1154-frontend-controls-cleanup/1154-UI-SPEC.md (Components 1-4 — gating, percentile inputs, sigma segmented, hint)
    - .planning/phases/1154-frontend-controls-cleanup/1154-CONTEXT.md (RASTER-STRETCH-03 / -UI-01 / -UI-02 decisions, lines 22-27)
  </read_first>
  <behavior>
    - band_count=3 (multi-band): the stretch Select IS present; the colormap Select is ABSENT
    - band_count=1: both colormap Select and stretch Select present (current order: colormap above stretch)
    - band_count=null and band_count=undefined: colormap Select absent (current behavior preserved); stretch Select still rendered for band_count>=1 only — null/undefined are NOT >= 1, so for null/undefined neither colormap NOR stretch renders (matches existing band_count===1 tests for colormap; document stretch follows band_count>=1 meaning a numeric >=1)
    - stretch=percentile: Low% (_pmin default 2) and High% (_pmax default 98) numeric inputs appear; stddev sigma control absent
    - stretch=stddev: sigma segmented buttons (1/2/3, default 2, aria-pressed on active) appear; pmin/pmax inputs absent
    - stretch=minmax: neither pmin/pmax nor sigma controls present
    - pmin/pmax client validation: entering pmin=50 pmax=40 (or pmin<0, pmax>100) does NOT call onPaintProp with an out-of-range value (guard rejects/reverts; no write)
    - hint: band_count=1 && stretch=percentile && colormap=viridis → `<p role="note">` with stretchColormapHint text present
    - hint absent: band_count=1 && stretch=minmax (any colormap) → no note; band_count=1 && colormap=gray (any non-minmax stretch) → no note; band_count=3 → no note
  </behavior>
  <action>
    Restructure the single `{layer.band_count === 1 && ( ... )}` wrapper (lines 186-242) so
    the STRETCH controls render for multi-band too while COLORMAP stays single-band only.

    GATE-SPLIT: introduce `const isSingleBand = layer.band_count === 1;` and
    `const isStretchEligible = typeof layer.band_count === 'number' && layer.band_count >= 1;`.
    Render the section wrapper when `isStretchEligible`. Inside:
    - COLORMAP row: wrap in `{isSingleBand && ( ... )}` (the existing colormap Select unchanged).
    - STRETCH row: always rendered inside the eligible section (the existing stretch Select unchanged).
    Keep the existing section header / className anatomy. For multi-band, the section still
    renders with only the stretch row (no colormap). Reuse `paint['_stretch'] ?? 'minmax'`,
    `paint['_colormap'] ?? 'gray'` reads.

    PERCENTILE INPUTS (RASTER-STRETCH-UI-01) — render below the stretch Select when
    `String(paint['_stretch'] ?? 'minmax') === 'percentile'`:
    - Read `pmin = typeof paint['_pmin'] === 'number' ? paint['_pmin'] : 2`,
      `pmax = typeof paint['_pmax'] === 'number' ? paint['_pmax'] : 98`.
    - Two-column row `flex gap-2`. Each column: a `<Label>` (`style.raster.pminLabel` /
      `style.raster.pmaxLabel`) + `<input type="number" min={0} max={100} step={1}>` styled
      `h-8 text-xs`. On change: parse the candidate value, then validate the FULL pair:
      a write to `_pmin` is allowed only if `0 <= candidate < currentPmax`; a write to `_pmax`
      only if `currentPmin < candidate <= 100`. On invalid, do NOT call onPaintProp (leave the
      prior paint value). Use a local `handlePminChange`/`handlePmaxChange` that calls
      `onPaintProp('_pmin', next)` / `onPaintProp('_pmax', next)` only when valid.

    SIGMA SEGMENTED (RASTER-STRETCH-UI-01) — render below the stretch Select when
    `String(paint['_stretch'] ?? 'minmax') === 'stddev'`:
    - Read `sigma = typeof paint['_sigma'] === 'number' ? paint['_sigma'] : 2`.
    - A `<Label>` (`style.raster.sigmaLabel`) + a row of 3 `<button type="button">` for values
      1/2/3, each `h-8 text-xs` with `aria-pressed={sigma === value}` and the established
      selected-token treatment (mirror an existing builder segmented/pill pattern — e.g. the
      render-as pills). On click: `onPaintProp('_sigma', value)`.

    HINT (RASTER-STRETCH-UI-02) — render a `<p role="note" className="text-[11px] leading-snug
    text-muted-foreground">` with `t('style.raster.stretchColormapHint')` below the stretch control
    ONLY when `isSingleBand && String(paint['_stretch'] ?? 'minmax') !== 'minmax' &&
    String(paint['_colormap'] ?? 'gray') !== 'gray'`. Copy-only, no interactivity.

    Pitfall 6: `_pmin`/`_pmax`/`_sigma` are builder-private — written via onPaintProp only,
    never added to RASTER_OWNED_PAINT_PROPERTIES. No direct map setPaintProperty in this file.
  </action>
  <acceptance_criteria>
    - `cd frontend && npm run typecheck` exits 0
    - New/updated vitest cases in RasterEditor.test.tsx assert each behavior above; existing
      RasterEditor tests (sliders, reset, colormap===1 gating, stretch options) still pass
    - An out-of-range pmin/pmax entry asserts onPaintProp NOT called with the invalid value
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- src/components/builder/LayerStyleEditor/__tests__/RasterEditor.test.tsx --run</automated>
  </verify>
  <done>Multi-band shows stretch (no colormap); percentile shows guarded Low%/High%; stddev shows sigma 1/2/3; single-band non-minmax non-gray shows the hint; all RasterEditor tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Add RasterEditor vitest coverage for gate-split, controls, and hint</name>
  <files>frontend/src/components/builder/LayerStyleEditor/__tests__/RasterEditor.test.tsx</files>
  <read_first>
    - frontend/src/components/builder/LayerStyleEditor/__tests__/RasterEditor.test.tsx (lines 120-400 — the i18n mock `t` map, makeProps/makeRasterLayer helpers, existing colormap/stretch tests 9-18)
  </read_first>
  <action>
    Extend the test's i18n mock `t` map (around lines 130-142) with the new keys so labels resolve:
    `style.raster.stretchColormapHint`, `style.raster.pminLabel`, `style.raster.pmaxLabel`,
    `style.raster.sigmaLabel`.

    Add cases (reuse `makeProps`, `makeRasterLayer`, `fireEvent`, `within`):
    - 'multi-band (band_count=3): stretch Select present, colormap Select absent' — render with
      band_count:3; assert a stretch Select exists (option /min\/max/i reachable) and NO colormap
      Select (option /viridis/i absent).
    - 'percentile: Low%/High% number inputs appear' — set paint {_stretch:'percentile'} on a
      band_count:1 layer; assert two number inputs (by Label or role spinbutton) present.
    - 'percentile out-of-range guard: entering pmin > pmax does not call onPaintProp("_pmin", invalid)'
      — fire change to an invalid value; assert onPaintProp not called with that value.
    - 'stddev: sigma buttons 1/2/3 render with aria-pressed on default 2' — set {_stretch:'stddev'};
      assert 3 buttons; the value-2 button has aria-pressed=true.
    - 'sigma button click fires onPaintProp("_sigma", 3)'.
    - 'minmax: no pmin/pmax/sigma controls' — assert spinbuttons and sigma buttons absent.
    - 'hint present when band_count=1 && stretch!=minmax && colormap!=gray' — paint
      {_stretch:'percentile', _colormap:'viridis'}; assert getByRole('note') has the hint text.
    - 'hint absent for stretch=minmax', 'hint absent for colormap=gray', 'hint absent for
      band_count=3' — assert queryByRole('note') is null (scope to the raster section; if the
      editor has no other note, plain queryByRole('note') is fine).
  </action>
  <acceptance_criteria>
    - `npm run test -- src/components/builder/LayerStyleEditor/__tests__/RasterEditor.test.tsx --run` green including the new cases
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- src/components/builder/LayerStyleEditor/__tests__/RasterEditor.test.tsx --run</automated>
  </verify>
  <done>RasterEditor.test.tsx proves multi-band stretch visibility, colormap absence, percentile guard, sigma control, and hint conditionality.</done>
</task>

<task type="auto">
  <name>Task 3: CLEANUP-01 — remove hillshade note + i18n; add new keys; verify types.ts; i18n parity</name>
  <files>frontend/src/components/builder/DEMEditorScene.tsx, frontend/src/components/builder/__tests__/DEMEditorScene.test.tsx, frontend/src/components/builder/LayerStyleEditor/types.ts, frontend/src/i18n/locales/en/builder.json, frontend/src/i18n/locales/de/builder.json, frontend/src/i18n/locales/es/builder.json, frontend/src/i18n/locales/fr/builder.json</files>
  <read_first>
    - frontend/src/components/builder/DEMEditorScene.tsx (lines 277-289 — the `{isTerrainBound && ( ... )}` note block inside the `mode === 'hillshade'` branch)
    - frontend/src/components/builder/__tests__/DEMEditorScene.test.tsx (lines 680-735 — the `POLISH-02 ... advisory note` describe block, 5 tests asserting the note)
    - frontend/src/components/builder/LayerStyleEditor/types.ts (full file — confirm onRenderModeChange ABSENT)
    - frontend/src/i18n/locales/en/builder.json (the `demEditor.hillshadeTerrainNote` key ~line 994 and the `style.raster` object)
  </read_first>
  <action>
    PART A — types.ts no-op verification (DO NOT EDIT the live handler): confirm
    `LayerStyleEditor/types.ts` has NO `onRenderModeChange` member. It is already absent — this
    is a documentation/verification step. Record the confirmation in the SUMMARY. DO NOT touch
    `LayerEditorPanel.tsx` or `MapBuilderPage.tsx` onRenderModeChange wiring (live render-as flow,
    tested) — editing it would break the render-as switch.

    PART B — remove the unreachable hillshade note (DEMEditorScene.tsx ~lines 279-288): delete the
    entire `{/* POLISH-02 ... */}` comment + `{isTerrainBound && ( <p role="note" ...>
    hillshadeTerrainNote </p> )}` JSX block. Leave the surrounding `mode === 'hillshade'` SUN
    POSITION sub-section intact. KEEP the `isTerrainBound` prop on the component (still in the
    interface/signature) — only the note JSX is removed; do not change `map-sync.ts`
    `isHillshadeTerrainBound` (separate error-suppression guard).

    PART C — i18n: in ALL FOUR locales (en/de/es/fr `builder.json`):
    - REMOVE the `demEditor.hillshadeTerrainNote` key (~line 994 in en; matching key in others).
    - ADD under `style.raster`: `stretchColormapHint`, `pminLabel`, `pmaxLabel`, `sigmaLabel`.
      English copy: stretchColormapHint = "Stretch sets the input range for the colormap.",
      pminLabel = "Low %", pmaxLabel = "High %", sigmaLabel = "Sigma (σ)". Provide proper
      translations for de/es/fr (do not leave English placeholders — the parity check compares
      key SETS, but keep translations meaningful). The four locales MUST end with identical key sets.

    PART D — DEMEditorScene.test.tsx: remove (or convert) the 5 tests in the `POLISH-02 ... advisory
    note` describe block (lines 684-735) that assert the note renders/queries `getByRole('note')`.
    Since the note is gone, the simplest correct change is to DELETE the whole `describe('POLISH-02
    ... advisory note', ...)` block. Keep `isTerrainBound` references elsewhere in the test
    (the prop still exists). If any non-note test depended on that describe's local fixtures, hoist
    them as needed.
  </action>
  <acceptance_criteria>
    - `grep -rn "hillshadeTerrainNote" frontend/src` returns ZERO matches (filtered: `grep -rn "hillshadeTerrainNote" frontend/src | grep -v '^#' | wc -l` == 0)
    - `grep -n "onRenderModeChange" frontend/src/components/builder/LayerStyleEditor/types.ts` returns ZERO matches (still absent)
    - `grep -n "onRenderModeChange" frontend/src/components/builder/LayerEditorPanel.tsx` STILL returns matches (live handler untouched)
    - `cd frontend && npm run test:i18n` passes (4-locale key-set parity)
    - `cd frontend && npm run test -- src/components/builder/__tests__/DEMEditorScene.test.tsx --run` green (note tests removed)
    - `cd frontend && npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run typecheck && npm run test:i18n && npm run test -- src/components/builder/__tests__/DEMEditorScene.test.tsx --run</automated>
  </verify>
  <done>hillshadeTerrainNote and its 4 i18n keys are gone; 4 new style.raster keys added with parity green; types.ts confirmed clean; live render-as handler and map-sync guard untouched; DEM note tests removed and DEM test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RasterEditor pmin/pmax/sigma inputs → tile URL | user-entered numerics cross into a server-side query param via _pmin/_pmax/_sigma paint keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1154-03 | Tampering | RasterEditor percentile inputs | mitigate | Client guard rejects writes unless `0<=pmin<pmax<=100`; never emits out-of-range to onPaintProp. Backend 422 is defense in depth. |
| T-1154-04 | Information Disclosure | i18n key removal | accept | Removing hillshadeTerrainNote strings exposes nothing; UI note was unreachable. No secret data. |
| T-1154-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (source + json edits only). |
</threat_model>

<verification>
- `cd frontend && npm run typecheck` exits 0
- `cd frontend && npm run test -- src/components/builder/LayerStyleEditor/__tests__/RasterEditor.test.tsx --run` green
- `cd frontend && npm run test -- src/components/builder/__tests__/DEMEditorScene.test.tsx --run` green
- `cd frontend && npm run test:i18n` green (key-set parity en/de/es/fr)
- `grep -rn "hillshadeTerrainNote" frontend/src | grep -v '^#' | wc -l` == 0
- `e2e:smoke:builder` (phase-level gate; run by orchestrator/close-gate)
</verification>

<success_criteria>
- Multi-band rasters show the stretch selector; colormap stays single-band-only
- percentile → guarded Low%/High% inputs; stddev → sigma 1/2/3; both forward to the tile URL via Plan 01's builder
- single-band non-minmax non-gray shows the stretch↔colormap hint
- hillshade advisory note + its 4 i18n keys removed; types.ts confirmed clean; live render-as handler + map-sync guard untouched
- i18n parity green
</success_criteria>

<output>
Create `.planning/phases/1154-frontend-controls-cleanup/1154-02-SUMMARY.md` when done.
</output>
