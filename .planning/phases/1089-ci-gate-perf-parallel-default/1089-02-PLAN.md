---
phase: 1089-ci-gate-perf-parallel-default
plan: 02
type: execute
wave: 2
depends_on:
  - 1089-01
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - CI-01
must_haves:
  truths:
    - "A new GitHub Actions job named `pytest-parallel-isolation` exists in `.github/workflows/ci.yml`"
    - "The job runs `uv run pytest -n <VALUE> -v --tb=short -m 'not perf'` where `<VALUE>` is Plan 1089-01's Section 5 recommended default"
    - "The job triggers on `if: needs.changes.outputs.backend == 'true' || needs.changes.outputs.alembic == 'true' || github.event_name == 'push'`"
    - "The job mirrors the `backend-test` job shape for env, Postgres bring-up, Python+uv setup, and alembic upgrade — but replaces the coverage test step"
    - "The `e2e-test` job's `needs:` list at line 641 includes `pytest-parallel-isolation`"
    - "Sequential pytest baseline `failed == 0` re-verified before commit (3036/0/38 or higher)"
    - "No production code, Makefile, pyproject.toml, conftest.py, or frontend changes"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "pytest-parallel-isolation CI job"
      contains: "pytest-parallel-isolation"
  key_links:
    - from: ".github/workflows/ci.yml `pytest-parallel-isolation` job"
      to: ".planning/audits/PYTEST-XDIST-PERF-v1020.md Section 5"
      via: "the `-n <VALUE>` flag value comes verbatim from PERF-01"
      pattern: "uv run pytest -n "
    - from: ".github/workflows/ci.yml line ~641 `e2e-test` needs"
      to: "the new `pytest-parallel-isolation` job"
      via: "addition to needs: list"
      pattern: "pytest-parallel-isolation"
---

<objective>
Add the `pytest-parallel-isolation` job to `.github/workflows/ci.yml` after the `alembic-clean-db` block (insertion point at line 492, immediately before the `# ---------- frontend ----------` comment at line 493). The new job runs `uv run pytest -n <VALUE>` against the backend test suite, where `<VALUE>` is the recommended default from Plan 1089-01 Section 5.

Purpose: This is the CI gate that PREVENTS regressions to the Phase 1088 fixture-isolation work. Without this gate, a future developer can push a fixture change that re-breaks parallel execution and the regression silently lands on main. With this gate, exit-0 on `pytest -n <VALUE>` is a required check for merge.

The `e2e-test` job at line 641 currently lists `needs: [backend-lint, backend-test, frontend-lint, frontend-test, security-scan]`. Add `pytest-parallel-isolation` to that list so the (currently-disabled) e2e gate also waits on the parallel gate when re-enabled. This makes `pytest-parallel-isolation` part of the canonical required-check graph.

Output: Single edit to `.github/workflows/ci.yml` adding the new job + extending the `e2e-test` needs list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/1089-ci-gate-perf-parallel-default/1089-CONTEXT.md
@.planning/phases/1089-ci-gate-perf-parallel-default/1089-01-SUMMARY.md
@.planning/audits/PYTEST-XDIST-PERF-v1020.md

<interfaces>
<!-- Existing CI workflow shape — reuse verbatim for the new job. -->
<!-- See .github/workflows/ci.yml in tree; key reference blocks below. -->

`backend-test` job env block (lines 300-310) — copy verbatim:

```yaml
env:
  POSTGRES_USER: geolens
  POSTGRES_PASSWORD: geolens_test
  POSTGRES_HOST: localhost
  POSTGRES_PORT: 5432
  POSTGRES_DB: postgres
  # Must be ≥ 32 chars to satisfy the Settings validator (backend/app/config.py).
  JWT_SECRET_KEY: test-secret-key-for-ci-padding-32chars
  GEOLENS_ADMIN_USERNAME: admin
  GEOLENS_ADMIN_PASSWORD: admin
  PYTHONPATH: .
```

`backend-test` `Start PostgreSQL with PostGIS + pgvector` block (lines 332-354) — copy verbatim (builds custom postgis+pgvector image, runs container on port 5432, waits for pg_isready).

`backend-test` Python + uv setup (lines 356-370) — copy verbatim (setup-uv@v8.1.0 with caching, setup-python@v6 python 3.13, `sudo apt-get install -y gdal-bin xmlsec1`, `uv sync --locked --dev`).

`backend-test` DB extensions block (lines 389-406) — copy verbatim (CREATE EXTENSION postgis/pg_trgm/vector/unaccent, CREATE SCHEMA catalog/data, ALTER DATABASE search_path, geolens_reader role).

`backend-test` `Run migrations` step (lines 408-409) — copy verbatim (`uv run alembic upgrade head`).

`alembic-clean-db` job shape (lines 462-491) — top-level structure reference (smaller scope but same hat-shape: name + needs + if + runs-on + timeout-minutes + steps).

`e2e-test` job needs line (line 641):
```yaml
needs: [backend-lint, backend-test, frontend-lint, frontend-test, security-scan]
```

`changes` job filters (lines 32-52) — already defines `backend` and `alembic` outputs that cover `backend/**`, `docker-compose*.yml`, `Dockerfile*`, `backend/alembic/**`, `backend/scripts/test_alembic_upgrade_clean_db.sh`, `backend/app/models/**`, `db/**`. The new job's trigger uses `backend == 'true' || alembic == 'true' || github.event_name == 'push'` — covers REQUIREMENTS.md CI-01's "PRs touching `backend/**` or `pyproject.toml` or `db/**`" without needing to add a new filter output.

</interfaces>

<perf_input>
<!-- Plan 1089-01's recommended default — the load-bearing input for this plan. -->
<!-- Executor: READ Plan 1089-01-SUMMARY.md "Recommended default consumed downstream" section
     to get the exact `-n VALUE`. Then read the audit doc Section 5 to verify the value matches. -->

Source: `.planning/phases/1089-ci-gate-perf-parallel-default/1089-01-SUMMARY.md`
Authoritative: `.planning/audits/PYTEST-XDIST-PERF-v1020.md` Section 5

Expected form (executor reads, does NOT pick): `auto` | `8` | `4`
Failure mode: if the SUMMARY does not yet name a value, HALT — Plan 1089-01 must commit first.

</perf_input>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve PERF-01 recommended default + write the new CI job</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Add the `pytest-parallel-isolation` job to `.github/workflows/ci.yml`. Insertion point: line 492 (the blank line immediately before `# ---------- frontend ----------` at line 493). This places the new job immediately after the `alembic-clean-db` block at lines 462-491 — the "sister to alembic-clean-db" shape per REQUIREMENTS.md CI-01.

**Step 1 — Resolve the `-n` value:**
1. Read `.planning/phases/1089-ci-gate-perf-parallel-default/1089-01-SUMMARY.md`. Locate the "Recommended default consumed downstream" section. Extract the `-n <VALUE>` value (one of `auto`, `8`, `4`).
2. Cross-verify: `grep -E "Recommended default for CI-01 \+ CI-02: \`-n " .planning/audits/PYTEST-XDIST-PERF-v1020.md` should yield exactly one line that matches the SUMMARY's value.
3. If the values disagree or are missing, HALT — fix Plan 1089-01 before continuing. Both sources must agree (this is the contract that instruction invariant #5 enforces).
4. Bind the resolved value to a placeholder `<PERF_N>` used in Step 2's YAML.

**Step 2 — Edit `.github/workflows/ci.yml`:**

Insert this YAML block at line 492 (after the blank line that follows `alembic-clean-db`'s last step at line 491). DO NOT modify any existing lines; INSERT only:

```yaml
  # ---------- pytest parallel-isolation gate (v1020 CI-01) ----------
  # Phase 1089 CI-01: defend the Phase 1088 fixture-isolation fixes against regression.
  # Runs `pytest -n <PERF_N>` against the backend test suite (post-Phase-1088 HEAD
  # baseline: 76 residual failures, mostly the flake-class deferred to Phase 1090 HYG-02).
  # Recommended `-n` value drives from .planning/audits/PYTEST-XDIST-PERF-v1020.md Section 5
  # (PERF-01 baseline). Sister-shape to alembic-clean-db (line 462).
  pytest-parallel-isolation:
    name: Pytest Parallel Isolation
    needs: changes
    if: needs.changes.outputs.backend == 'true' || needs.changes.outputs.alembic == 'true' || github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: backend
    env:
      POSTGRES_USER: geolens
      POSTGRES_PASSWORD: geolens_test
      POSTGRES_HOST: localhost
      POSTGRES_PORT: 5432
      POSTGRES_DB: postgres
      # Must be ≥ 32 chars to satisfy the Settings validator (backend/app/config.py).
      JWT_SECRET_KEY: test-secret-key-for-ci-padding-32chars
      GEOLENS_ADMIN_USERNAME: admin
      GEOLENS_ADMIN_PASSWORD: admin
      PYTHONPATH: .
    steps:
      - uses: actions/checkout@v6

      - name: Start PostgreSQL with PostGIS + pgvector
        working-directory: .
        run: |
          docker build -t geolens-test-db -f - . <<'DOCKERFILE'
          FROM postgis/postgis:17-3.5
          RUN apt-get update \
              && apt-get install -y --no-install-recommends postgresql-17-pgvector \
              && rm -rf /var/lib/apt/lists/*
          DOCKERFILE
          docker run -d --name test-postgres \
            -e POSTGRES_DB=postgres \
            -e POSTGRES_USER=geolens \
            -e POSTGRES_PASSWORD=geolens_test \
            -p 5432:5432 \
            geolens-test-db
          for i in $(seq 1 30); do
            if docker exec test-postgres pg_isready -U geolens -d postgres >/dev/null 2>&1; then
              echo "PostgreSQL is ready"
              break
            fi
            echo "Waiting for PostgreSQL... ($i/30)"
            sleep 2
          done

      - uses: astral-sh/setup-uv@v8.1.0
        with:
          version: "0.10.2"
          enable-cache: true
          cache-dependency-glob: "backend/uv.lock"

      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y gdal-bin xmlsec1

      - name: Install Python dependencies
        run: uv sync --locked --dev

      - name: Set up database extensions, roles, and schemas
        run: |
          PGPASSWORD=geolens_test psql -h localhost -U geolens -d postgres -c "
            CREATE EXTENSION IF NOT EXISTS postgis;
            CREATE EXTENSION IF NOT EXISTS pg_trgm;
            CREATE EXTENSION IF NOT EXISTS vector;
            CREATE EXTENSION IF NOT EXISTS unaccent;
            CREATE SCHEMA IF NOT EXISTS catalog;
            CREATE SCHEMA IF NOT EXISTS data;
            ALTER DATABASE postgres SET search_path TO catalog, data, public;
            DO \$\$ BEGIN
              IF NOT EXISTS (SELECT FROM pg_roles WHERE rolname = 'geolens_reader') THEN
                CREATE ROLE geolens_reader NOLOGIN;
              END IF;
            END \$\$;
            GRANT USAGE ON SCHEMA data TO geolens_reader;
            ALTER DEFAULT PRIVILEGES IN SCHEMA data GRANT SELECT ON TABLES TO geolens_reader;
          "

      - name: Run migrations
        run: uv run alembic upgrade head

      - name: Run tests with -n <PERF_N>
        # Phase 1089 CI-01 gate: defends against regressions to Phase 1088 fixture-isolation work.
        # `-n` value sourced from .planning/audits/PYTEST-XDIST-PERF-v1020.md Section 5.
        # No coverage flags here — backend-test job already covers coverage; this job is a regression gate only.
        # Skips enterprise overlay (CONTEXT.md decision: simpler is better; overlay-specific fixture races
        # are tracked separately in Phase 1090 HYG-02 if they surface).
        run: uv run pytest -n <PERF_N> -v --tb=short -m 'not perf'
```

Substitute `<PERF_N>` (in the comment AND the run command) with the value resolved in Step 1.

**Step 3 — Add to e2e-test needs list:**

At what is currently line 641, the `e2e-test` job has:
```yaml
    needs: [backend-lint, backend-test, frontend-lint, frontend-test, security-scan]
```

Change to:
```yaml
    needs: [backend-lint, backend-test, pytest-parallel-isolation, frontend-lint, frontend-test, security-scan]
```

Place `pytest-parallel-isolation` between `backend-test` and `frontend-lint` — keeps the backend cluster together. The `e2e-test` job is currently `if: false` (disabled per the comment block at lines 632-636) so this is a forward-compatibility wiring; when e2e is re-enabled it will require the parallel gate.

**Step 4 — Lint the YAML:**

Run a YAML syntax check (this catches most CI workflow errors offline; the full CI run is only verifiable on push, per CONTEXT.md "CI live-verification note"):

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"
```

Should exit 0. If it errors, find and fix the YAML issue before commit.

**Hard constraints per instruction invariants:**
- This plan touches `.github/workflows/ci.yml` ONLY. No other files (instruction invariant #2).
- NO traceability flip — CI-01 row stays `Pending` until Plan 1089-03 (instruction invariant #3).
- NO modification to `backend/tests/conftest.py`, `backend/app/*`, `Makefile`, `backend/pyproject.toml`, `frontend/*` (instruction invariant #2).
- CI live-verification is deferred — the gate only proves green on push to GitHub. Plan 1089-03's SUMMARY will reference the first post-merge run as the live-verification artifact (instruction invariant #4).
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && grep -E "^  pytest-parallel-isolation:" .github/workflows/ci.yml && grep -E "uv run pytest -n (auto|[0-9]+) -v --tb=short -m 'not perf'$" .github/workflows/ci.yml && grep -E "needs: \[backend-lint, backend-test, pytest-parallel-isolation, frontend-lint, frontend-test, security-scan\]" .github/workflows/ci.yml && grep -E "timeout-minutes: 30" .github/workflows/ci.yml</automated>
  </verify>
  <done>`.github/workflows/ci.yml` parses as valid YAML. New `pytest-parallel-isolation:` job exists with the resolved `-n VALUE` from Plan 1089-01 in BOTH the step comment and the `run:` invocation. Job has `needs: changes`, the correct `if:` trigger, `timeout-minutes: 30`, mirrored env block, Postgres+Python+migrations setup, and the pytest invocation (no coverage). `e2e-test` `needs:` list includes `pytest-parallel-isolation` in second position. No other files modified.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify sequential baseline + commit + write SUMMARY</name>
  <files>.github/workflows/ci.yml, .planning/phases/1089-ci-gate-perf-parallel-default/1089-02-SUMMARY.md</files>
  <action>
Before commit: re-verify sequential pytest baseline + write SUMMARY + atomic commit.

**Step 1 — Sequential baseline re-verify (HARD GATE per CONTEXT.md):**
```bash
cd /Users/ishiland/Code/geolens/backend && env $(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) \
  uv run pytest tests/ 2>&1 | tail -5
```

The final summary line MUST show `failed == 0` and `passed >= 3036`. If not, halt — this plan's YAML edit did not regress sequential (it cannot — it touches CI YAML only), but the baseline-preservation gate is unconditional per CONTEXT.md "Sequential baseline preservation" + instruction invariant #1. Investigate any regression before commit.

**Step 2 — Write Plan SUMMARY:**

Write `.planning/phases/1089-ci-gate-perf-parallel-default/1089-02-SUMMARY.md` using standard SUMMARY template. Sections:
- **Header**: plan id, phase, requirement (CI-01), commit SHA (filled after commit), status (Complete for this plan; requirement flip deferred to Plan 1089-03)
- **What shipped**: new `pytest-parallel-isolation` job in `.github/workflows/ci.yml` running `uv run pytest -n <PERF_N>` where `<PERF_N>` is the value from Plan 1089-01 Section 5
- **Job specifics**: trigger (`backend == 'true' || alembic == 'true' || push`), shape mirror (`backend-test` env + Postgres setup + Python/uv + alembic upgrade), test invocation (`-n <PERF_N> -v --tb=short -m 'not perf'`), timeout (30 min)
- **e2e-test needs update**: `pytest-parallel-isolation` added between `backend-test` and `frontend-lint`
- **CI live-verification deferred**: live confirmation requires push-to-GitHub; Plan 1089-03's SUMMARY will reference the first post-merge run (instruction invariant #4) with `gh run watch` command
- **Sequential baseline preserved**: re-verified `failed == 0` immediately before commit (number: `<N>` passed)
- **Traceability note**: CI-01 flip deferred to Plan 1089-03's atomic TD-13 commit
- **Skip-overlay decision documented**: per CONTEXT.md, enterprise overlay path is skipped from the new job — overlay-specific fixture races are tracked separately in Phase 1090 HYG-02 if they surface

**Step 3 — Stage and commit (atomic):**
```bash
git add .github/workflows/ci.yml .planning/phases/1089-ci-gate-perf-parallel-default/1089-02-SUMMARY.md
git commit -m "ci(1089-02): add pytest-parallel-isolation gate for CI-01 (-n <PERF_N> from PERF-01)"
```

Substitute `<PERF_N>` in the commit message with the resolved value.

**Step 4 — Verify negative-shape REQUIREMENTS.md gate:**

```bash
grep -E "^\| CI-01 .* Pending" .planning/REQUIREMENTS.md
```

Must yield exactly one match. CI-01 stays `Pending` until Plan 1089-03 flips it atomically with CI-02 + PERF-01 per instruction invariant #3.

**Step 5 — Verify HEAD touched only the expected files:**

```bash
git diff-tree --no-commit-id --name-only -r HEAD | sort > /tmp/1089-02-files.txt
printf ".github/workflows/ci.yml\n.planning/phases/1089-ci-gate-perf-parallel-default/1089-02-SUMMARY.md\n" | sort > /tmp/1089-02-expected.txt
diff /tmp/1089-02-files.txt /tmp/1089-02-expected.txt
```

Must exit 0 (zero diff).
  </action>
  <verify>
    <automated>git diff-tree --no-commit-id --name-only -r HEAD | sort > /tmp/1089-02-files.txt && printf ".github/workflows/ci.yml\n.planning/phases/1089-ci-gate-perf-parallel-default/1089-02-SUMMARY.md\n" | sort > /tmp/1089-02-expected.txt && diff /tmp/1089-02-files.txt /tmp/1089-02-expected.txt && grep -E "^\| CI-01 .* Pending" .planning/REQUIREMENTS.md && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"</automated>
  </verify>
  <done>Sequential pytest re-verified `failed == 0` immediately before commit. HEAD commit touches exactly two files: `.github/workflows/ci.yml` + Plan 1089-02 SUMMARY. REQUIREMENTS.md `| CI-01 |` row is still `Pending` (atomic flip is in Plan 1089-03). YAML parses cleanly. Plan SUMMARY references the resolved `<PERF_N>` value + notes the deferred CI live-verification.</done>
</task>

</tasks>

<verification>
**Plan-level verification:**

1. `.github/workflows/ci.yml` contains the new `pytest-parallel-isolation:` job with the resolved `-n <PERF_N>` value (sourced from Plan 1089-01 SUMMARY + PERF-01 audit Section 5).
2. The job's `if:` trigger covers REQUIREMENTS.md CI-01's "PRs touching `backend/**` or `pyproject.toml` or `db/**`" via `backend == 'true' || alembic == 'true' || push`.
3. `e2e-test` `needs:` list at line ~641 includes `pytest-parallel-isolation`.
4. YAML parses cleanly: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0.
5. Sequential pytest baseline `failed == 0` re-verified immediately before commit.
6. HEAD commit touches exactly two files (`.github/workflows/ci.yml` + Plan SUMMARY).
7. REQUIREMENTS.md CI-01 row remains `Pending` — flip deferred to Plan 1089-03 (instruction invariant #3).
8. No changes to `backend/tests/conftest.py`, `backend/app/*`, `Makefile`, `backend/pyproject.toml`, `frontend/*` (instruction invariant #2).
9. CI live-verification deferred to post-merge — Plan 1089-03 SUMMARY will reference the first post-merge run with `gh run watch <run_id>` for the operator (instruction invariant #4).
</verification>

<success_criteria>
- New `pytest-parallel-isolation` CI job exists in `.github/workflows/ci.yml`, configured per CONTEXT.md "CI-01 shape (LOCKED)" and instruction invariants
- `-n` value matches Plan 1089-01 Section 5 recommended default verbatim (PERF-01-drives-CI-default contract per instruction invariant #5)
- `e2e-test` needs list updated to include the new job
- Sequential baseline preserved (3036/0/38 or higher)
- REQUIREMENTS.md CI-01 row STILL `Pending` — atomic flip is in Plan 1089-03
- No code changes outside `.github/workflows/ci.yml` + plan SUMMARY
</success_criteria>

<output>
Create `.planning/phases/1089-ci-gate-perf-parallel-default/1089-02-SUMMARY.md` when done.
</output>
