---
phase: 1089-ci-gate-perf-parallel-default
plan: 03
type: execute
wave: 3
depends_on:
  - 1089-01
  - 1089-02
files_modified:
  - Makefile
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - CI-02
  - CI-01
  - PERF-01
must_haves:
  truths:
    - "A fresh clone running `make test` (no args) uses parallel execution with `-n <PERF_N>` where `<PERF_N>` is Plan 1089-01 Section 5's recommended default"
    - "A separate `make test-sequential` target exists and runs pytest in sequential mode (no `-n` flag) for debugging"
    - "REQUIREMENTS.md PERF-01, CI-01, and CI-02 rows all flip from `Pending` to `Complete` in the SAME commit as the Plan 1089-03 SUMMARY write (TD-13 traceability flip per instruction invariant #3)"
    - "REQUIREMENTS.md `[ ]` checkboxes for CI-01, CI-02, PERF-01 all flip to `[x]` in the same atomic commit"
    - "ROADMAP.md Phase 1089 row is marked `[x]` (complete) in the same atomic commit"
    - "STATE.md `Current Position` advances to Phase 1090 in the same atomic commit"
    - "Sequential pytest baseline `failed == 0` re-verified before commit (3036/0/38 or higher)"
  artifacts:
    - path: "Makefile"
      provides: "Parallel-default `test` target + sequential opt-in `test-sequential` target"
      contains: "test-sequential:"
    - path: ".planning/phases/1089-ci-gate-perf-parallel-default/1089-03-SUMMARY.md"
      provides: "Phase 1089 close summary + atomic TD-13 commit + CI live-verification handoff"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability flip for CI-01, CI-02, PERF-01"
      contains: "| CI-01 | Phase 1089 | Complete |"
  key_links:
    - from: "Makefile:27 `test:` target"
      to: ".planning/audits/PYTEST-XDIST-PERF-v1020.md Section 5"
      via: "the `-n <VALUE>` flag value comes verbatim from PERF-01"
      pattern: "uv run pytest -n "
    - from: ".planning/REQUIREMENTS.md traceability table"
      to: "this plan's SUMMARY commit"
      via: "atomic TD-13 commit groups REQ flip + ROADMAP flip + SUMMARY in one SHA"
      pattern: "Phase 1089.*Complete"
---

<objective>
Final plan of Phase 1089. Three deliverables in ONE atomic commit:

1. **CI-02 implementation**: Update `Makefile:27` `test:` target to use `-n <PERF_N>` (Plan 1089-01 Section 5 recommended default). Add new `test-sequential:` target preserving the no-args sequential debugging path (REQUIREMENTS.md CI-02 acceptance requirement). Touch surface confined to `Makefile` per CONTEXT.md "Option A is recommended" — `pyproject.toml addopts` is NOT modified to avoid double-applying `-n` in CI-01's explicit invocation.

2. **TD-13 atomic traceability flip**: REQUIREMENTS.md `[ ]` → `[x]` + `Pending` → `Complete` for CI-01, CI-02, PERF-01 (three rows). ROADMAP.md Phase 1089 row `[ ]` → `[x]`. All flips land in the SAME commit as the SUMMARY write per `requirements_traceability_flip` (`gsd-executor.md` rule live since v1019 TD-13).

3. **Phase 1089 close**: Plan 1089-03 SUMMARY + STATE.md position advance to Phase 1090. CI live-verification deferred to post-merge — SUMMARY references the first post-merge CI run with a `gh run watch` command for the operator (instruction invariant #4).

Purpose: Closes Phase 1089. After this commit, `make test` defaults to parallel; the CI gate from Plan 1089-02 blocks future regressions; the audit doc from Plan 1089-01 documents the chosen default with reproducibility. The "PERF-01 recommended default drives CI-02" contract (instruction invariant #5) closes here: the same `<PERF_N>` value is used in BOTH the CI invocation (Plan 1089-02) and the Makefile default (Plan 1089-03).

Output: Single atomic commit with `Makefile`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `1089-03-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/1089-ci-gate-perf-parallel-default/1089-CONTEXT.md
@.planning/phases/1089-ci-gate-perf-parallel-default/1089-01-SUMMARY.md
@.planning/phases/1089-ci-gate-perf-parallel-default/1089-02-SUMMARY.md
@.planning/audits/PYTEST-XDIST-PERF-v1020.md
@.planning/retros/v1019-process.md

<interfaces>
<!-- Makefile target shape — current and post-edit. -->

Current Makefile (lines 27-31):
```makefile
test:
	docker compose exec api uv run pytest -v --tb=short

test-cov:
	docker compose exec api uv run pytest -v --tb=short --cov=app --cov-report=term-missing
```

Post-edit Makefile (lines 27-34 conceptually — exact lines depend on what insertion adds):
```makefile
# Phase 1089 CI-02: defaults to parallel execution per PYTEST-XDIST-PERF-v1020.md Section 5.
# Use `make test-sequential` to opt into sequential debugging mode.
test:
	docker compose exec api uv run pytest -n <PERF_N> -v --tb=short

test-sequential:
	docker compose exec api uv run pytest -v --tb=short

test-cov:
	docker compose exec api uv run pytest -v --tb=short --cov=app --cov-report=term-missing
```

Notes:
- Indentation is TAB (Makefile syntax; `\t` not spaces). Preserve verbatim.
- `test-cov:` target is preserved unchanged.
- The comment block above `test:` is a 2-line guidance note new with this plan.

<!-- REQUIREMENTS.md traceability table — current and post-edit. -->

Current table (lines 70-80):
```
| Requirement | Phase | Status |
|-------------|-------|--------|
| FI-01 | Phase 1087 | Complete |
| FI-02 | Phase 1088 | Complete |
| FI-03 | Phase 1088 | Complete |
| CI-01 | Phase 1089 | Pending |
| CI-02 | Phase 1089 | Pending |
| PERF-01 | Phase 1089 | Pending |
| HYG-01 | Phase 1090 | Pending |
| HYG-02 | Phase 1090 | Pending |
| HYG-03 | Phase 1090 | Pending |
```

Post-edit:
```
| CI-01 | Phase 1089 | Complete |
| CI-02 | Phase 1089 | Complete |
| PERF-01 | Phase 1089 | Complete |
```

(Three rows flipped; FI-* + HYG-* rows unchanged.)

<!-- REQUIREMENTS.md checkbox flips -->

Lines 26 + 28 + 32:
- `- [ ] **CI-01**:` → `- [x] **CI-01**:`
- `- [ ] **CI-02**:` → `- [x] **CI-02**:`
- `- [ ] **PERF-01**:` → `- [x] **PERF-01**:`

<!-- ROADMAP.md Phase 1089 row -->

Current (line 92):
```
- [ ] **Phase 1089: CI Gate + Perf Baseline + Parallel Default** — Add `pytest-parallel-isolation` GitHub Actions job (sister to v1017 `alembic-clean-db`); capture `-n 4`/`-n 8`/`-n auto` benchmark; switch `make test` default to parallel with sequential opt-in retained.
```

Post-edit:
```
- [x] **Phase 1089: CI Gate + Perf Baseline + Parallel Default** — Add `pytest-parallel-isolation` GitHub Actions job (sister to v1017 `alembic-clean-db`); capture `-n 4`/`-n 8`/`-n auto` benchmark; switch `make test` default to parallel with sequential opt-in retained. **Closed 2026-05-22:** CI-01 + CI-02 + PERF-01 satisfied; recommended default `-n <PERF_N>` per audit Section 5; CI live-verification deferred to first post-merge run.
```

</interfaces>

<perf_input>
<!-- Plan 1089-01's recommended default — same value used in Plan 1089-02. -->
<!-- Executor: VALIDATE that the value used here matches Plan 1089-02's CI job. -->

Source: `.planning/phases/1089-ci-gate-perf-parallel-default/1089-01-SUMMARY.md`
Authoritative: `.planning/audits/PYTEST-XDIST-PERF-v1020.md` Section 5
Cross-check: `.github/workflows/ci.yml pytest-parallel-isolation:` step `run:` line

The Makefile `test:` target's `-n <VALUE>` MUST match the `<VALUE>` already committed in Plan 1089-02's CI job. If they diverge, the PERF-01-drives-CI-default contract (instruction invariant #5) is violated. Cross-check via:

```bash
CI_N=$(grep -E "uv run pytest -n " .github/workflows/ci.yml | grep -oE "\-n (auto|[0-9]+)" | head -1)
echo "CI uses: $CI_N — Makefile MUST use the same"
```

</perf_input>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Makefile + final sequential baseline re-verify</name>
  <files>Makefile</files>
  <action>
**Step 1 — Resolve and cross-check `-n` value:**

1. Read `.planning/phases/1089-ci-gate-perf-parallel-default/1089-01-SUMMARY.md` "Recommended default consumed downstream" section. Extract the `-n <VALUE>` value.
2. Cross-check against Plan 1089-02's CI job:
   ```bash
   CI_N=$(grep -E "uv run pytest -n " .github/workflows/ci.yml | grep -oE "\-n (auto|[0-9]+)" | head -1)
   echo "CI uses: $CI_N"
   ```
3. Both sources MUST agree. If not, HALT — the contract that "PERF-01 recommended default drives CI-02" (instruction invariant #5) is violated.
4. Bind to `<PERF_N>` for use in Makefile edit.

**Step 2 — Edit Makefile (lines 27-31):**

Use the Edit tool to replace the existing `test:` target with the new shape. Replace:

```makefile
test:
	docker compose exec api uv run pytest -v --tb=short
```

With:

```makefile
# Phase 1089 CI-02: defaults to parallel execution per PYTEST-XDIST-PERF-v1020.md Section 5.
# Use `make test-sequential` to opt into sequential debugging mode.
test:
	docker compose exec api uv run pytest -n <PERF_N> -v --tb=short

test-sequential:
	docker compose exec api uv run pytest -v --tb=short
```

Substitute `<PERF_N>` with the resolved value. The recipe lines (`docker compose ...`) MUST use TAB indentation (not spaces) — `make` will fail with `*** missing separator` on space-indented recipes. The pre-existing `test:` recipe line at Makefile:28 is TAB-indented; preserve that shape.

**Step 3 — Verify Makefile parses (negative-shape gate):**

```bash
make -n test 2>&1 | head -3
make -n test-sequential 2>&1 | head -3
```

`make -n` (dry-run) prints what the targets WOULD execute. Both must succeed and print the docker exec command. If either errors with `*** missing separator` or `*** No rule to make target`, fix the Makefile indentation before continuing.

The dry-run output for `make -n test` must include `-n <PERF_N>`; the dry-run output for `make -n test-sequential` must NOT include `-n` (sequential opt-in).

**Step 4 — Final sequential baseline re-verify (HARD GATE):**

This is the LAST sequential re-verify of Phase 1089. Per CONTEXT.md "Sequential baseline preservation" + instruction invariant #1:

```bash
cd /Users/ishiland/Code/geolens/backend && env $(grep -v '^#' /Users/ishiland/Code/geolens/.env.test | xargs) \
  uv run pytest tests/ 2>&1 | tail -5
```

`failed == 0` and `passed >= 3036` MUST hold. This proves no plan in Phase 1089 regressed sequential mode (the v1088 close-gate baseline was 3047/0/38 — sequential mode should match or exceed 3036).

Record the exact number for the Plan SUMMARY.

DO NOT modify `backend/pyproject.toml addopts` (CONTEXT.md "Option A recommended" — Makefile-only switch, no pyproject change). DO NOT modify `backend/tests/conftest.py` (Phase 1088 owns it).
  </action>
  <verify>
    <automated>make -n test 2>&1 | grep -E "uv run pytest -n (auto|[0-9]+)" && make -n test-sequential 2>&1 | grep -E "uv run pytest -v" && ! (make -n test-sequential 2>&1 | grep -E "uv run pytest -n ") && grep -E "^test-sequential:" Makefile && grep -E "Phase 1089 CI-02" Makefile</automated>
  </verify>
  <done>Makefile parses cleanly. `make -n test` shows `-n <PERF_N>` invocation; `make -n test-sequential` does NOT include `-n` flag. Comment block above `test:` references PYTEST-XDIST-PERF-v1020.md Section 5. `test-sequential:` target exists at lines 30-31. `test-cov:` target unchanged. Sequential baseline `failed == 0` re-verified immediately before commit (number: `<N>` passed).</done>
</task>

<task type="auto">
  <name>Task 2: Atomic TD-13 traceability flip — REQUIREMENTS.md + ROADMAP.md + STATE.md + SUMMARY</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/1089-ci-gate-perf-parallel-default/1089-03-SUMMARY.md</files>
  <action>
TD-13 atomic flip: ALL of these edits land in ONE commit with the Makefile edit from Task 1 + the SUMMARY write from this task. Per `requirements_traceability_flip` (gsd-executor.md, established v1019 Phase 1086, retro `.planning/retros/v1019-process.md`):

> "Before staging the SUMMARY commit, the executor MUST flip the REQUIREMENTS.md checkbox `[ ]` → `[x]` and the traceability row `Pending` → `Complete` for every requirement ID closed by the plan, and both edits MUST land in the SAME commit as the SUMMARY.md write."

**Step 1 — REQUIREMENTS.md checkbox flips (3 lines):**

Use Edit tool 3× (one per line) on `.planning/REQUIREMENTS.md`. CI-01 + CI-02 are adjacent (lines 26 + 28); PERF-01 is at line 32:

- Line 26: `- [ ] **CI-01**:` → `- [x] **CI-01**:`
- Line 28: `- [ ] **CI-02**:` → `- [x] **CI-02**:`
- Line 32: `- [ ] **PERF-01**:` → `- [x] **PERF-01**:`

The trailing requirement body text after the colon is UNCHANGED.

**Step 2 — REQUIREMENTS.md traceability table flips (3 rows):**

Use Edit tool 3× on the traceability table (lines ~75-77):

- `| CI-01 | Phase 1089 | Pending |` → `| CI-01 | Phase 1089 | Complete |`
- `| CI-02 | Phase 1089 | Pending |` → `| CI-02 | Phase 1089 | Complete |`
- `| PERF-01 | Phase 1089 | Pending |` → `| PERF-01 | Phase 1089 | Complete |`

**Step 3 — ROADMAP.md Phase 1089 row flip (1 line):**

Edit `.planning/ROADMAP.md` Phase 1089 entry (line 92). Replace:

```
- [ ] **Phase 1089: CI Gate + Perf Baseline + Parallel Default** — Add `pytest-parallel-isolation` GitHub Actions job (sister to v1017 `alembic-clean-db`); capture `-n 4`/`-n 8`/`-n auto` benchmark; switch `make test` default to parallel with sequential opt-in retained.
```

With (substitute `<PERF_N>` and `<COMMIT_DATE>`):

```
- [x] **Phase 1089: CI Gate + Perf Baseline + Parallel Default** — Add `pytest-parallel-isolation` GitHub Actions job (sister to v1017 `alembic-clean-db`); capture `-n 4`/`-n 8`/`-n auto` benchmark; switch `make test` default to parallel with sequential opt-in retained. **Closed <COMMIT_DATE>:** CI-01 + CI-02 + PERF-01 satisfied; recommended default `-n <PERF_N>` per audit Section 5; CI live-verification deferred to first post-merge run.
```

ALSO update the ROADMAP.md `### Phase 1089:` detail subsection's `Plans:` list (the per-plan checkboxes — currently mirroring Phase 1088's stale labels). Fix the per-plan list to reflect the actual three plans of Phase 1089:

Find the `### Phase 1089: CI Gate + Perf Baseline + Parallel Default` subsection (around line 128). Its `Plans:` list (around lines 138-144) currently contains stale Phase 1088 labels (`1088-01-PLAN.md` through `1088-05-PLAN.md`). Replace the entire `**Plans**:` line + the bulleted plan list with:

```
**Plans**: 3/3 plans complete (1089-01 PERF-01 baseline / 1089-02 CI-01 gate / 1089-03 CI-02 default switch + close)
Plans:
- [x] 1089-01-PLAN.md — PERF-01 baseline measurement (audit doc `.planning/audits/PYTEST-XDIST-PERF-v1020.md`)
- [x] 1089-02-PLAN.md — CI-01 wiring (`pytest-parallel-isolation` job in `.github/workflows/ci.yml`)
- [x] 1089-03-PLAN.md — CI-02 default switch (`make test` → `-n <PERF_N>`) + atomic TD-13 flip + Phase 1089 close
```

This fixes the per-plan list stale-from-template issue (the `### Phase 1090:` subsection has the same stale labels — that's not in this plan's scope, leave for Phase 1090's planner to fix).

**Step 4 — STATE.md position advance:**

Edit `.planning/STATE.md` to reflect Phase 1089 close. Specifically:

- Frontmatter `status:` → `Phase 1089 closed; CI-01 + CI-02 + PERF-01 satisfied; Phase 1090 next`
- Frontmatter `stopped_at:` → `Phase 1089 (CI-01 + CI-02 + PERF-01) shipped — pytest-parallel-isolation CI gate live; make test defaults to -n <PERF_N>; PERF-01 audit doc committed; ready for /gsd:plan-phase 1090`
- Frontmatter `last_updated:` → current date/time ISO timestamp
- Frontmatter `last_activity:` → "<today's date> — Phase 1089 closed; CI-01 + CI-02 + PERF-01 traceability flipped in single TD-13 commit (`<commit_sha_filled_post_commit>`); CI live-verification deferred to first post-merge run"
- Frontmatter `progress.completed_phases:` → 7 (was 6)
- Frontmatter `progress.completed_plans:` → 14 (was 11; +3 for 1089-01 + 1089-02 + 1089-03)
- Frontmatter `progress.percent:` → 75 (was 50)
- Body `## Current Position` → "Phase: 1089 — CI Gate + Perf Baseline + Parallel Default — COMPLETE" + plan line + status + last activity
- Body `### Decisions` — APPEND (do NOT remove prior entries):
  - "**<today's date> (Phase 1089-01 close):** PERF-01 baseline shipped — three parallel runs measured; Section 5 recommends `-n <PERF_N>` as the documented default for CI-01 + CI-02 (rationale: `<one-line rationale from audit Section 5>`)."
  - "**<today's date> (Phase 1089-02 close):** CI-01 wired — `pytest-parallel-isolation` job added at `.github/workflows/ci.yml` after `alembic-clean-db` block. Trigger: `backend == 'true' || alembic == 'true' || push`. Test invocation: `uv run pytest -n <PERF_N> -v --tb=short -m 'not perf'`. Skip enterprise overlay path (simpler-is-better per CONTEXT.md). `e2e-test` job's `needs:` list extended to include the new job. CI live-verification deferred to first post-merge run."
  - "**<today's date> (Phase 1089-03 close):** CI-02 default switched — `Makefile:27` `test:` target now runs `-n <PERF_N>`; new `test-sequential:` target preserves no-args sequential debugging path. REQUIREMENTS.md CI-01 + CI-02 + PERF-01 + ROADMAP.md Phase 1089 + 1089-03-SUMMARY.md all flipped in SINGLE commit per TD-13 `requirements_traceability_flip` rule. Sequential baseline preserved (3036/0/38 or higher)."
- Body `### Blockers/Concerns` → "**Phase 1090 inheritance:** post-Phase-1089 HEAD state is the input HYG-01 (skip audit) and HYG-02 (flake hunt 3× consecutive runs) will work against. `pytest-parallel-isolation` CI gate is now live and defends against future regressions to the Phase 1088 fixture-isolation work. CI live-verification (the first push-to-GitHub run that exercises the new gate) is a Phase 1090 close-gate item — `gh run watch <run_id>` confirms green on first post-merge run."
- Body `## Operator Next Steps` → "Run `/gsd:plan-phase 1090` to begin Phase 1090: Skip Audit + Flake Hunt + Close-Gate — disposition 38 sequential-mode skips; run `pytest -n auto` 3× to surface non-deterministic flakes; paper-trail v1019 WR-01 `lint:sec-fu-03-no-false-positive` script; cut tags `v1020` + `v1.5.5`."
- Body `## Deferred Items` — strikethrough (`~~`) CI-01, CI-02, PERF-01 entries (mirrors how FI-01/FI-02/FI-03 are struck through in current STATE.md). Append the closing parenthetical for each.

**Step 5 — Write Plan 1089-03 SUMMARY:**

Write `.planning/phases/1089-ci-gate-perf-parallel-default/1089-03-SUMMARY.md`. Sections:

- **Header**: plan id, phase, requirements (CI-01 + CI-02 + PERF-01 — three requirements close here via atomic TD-13 flip), commit SHA (filled after commit), status (Complete)
- **What shipped**: Makefile parallel-default + sequential opt-in; atomic TD-13 flip of 3 REQ rows + 1 ROADMAP row + STATE.md advance
- **Phase 1089 close summary**:
  - PERF-01: audit doc `.planning/audits/PYTEST-XDIST-PERF-v1020.md` (Plan 1089-01) — recommended default `-n <PERF_N>` with rationale `<one line>`
  - CI-01: new `pytest-parallel-isolation` job in `.github/workflows/ci.yml` (Plan 1089-02) — runs `uv run pytest -n <PERF_N> -v --tb=short -m 'not perf'`
  - CI-02: `Makefile:27` `test:` target switched to parallel with `-n <PERF_N>`; `test-sequential:` opt-in preserved
- **Sequential baseline preserved**: re-verified `failed == 0` immediately before commit (number: `<N>` passed)
- **TD-13 atomic flip verification**: list the post-commit `git diff-tree --no-commit-id --name-only -r HEAD` output (must show exactly: `Makefile`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/phases/1089-ci-gate-perf-parallel-default/1089-03-SUMMARY.md` — five files)
- **CI live-verification handoff**: explicit pointer for operator:
  ```bash
  # After this commit merges to main, the pytest-parallel-isolation gate fires for the first time.
  # Watch the run:
  gh run list --workflow=ci.yml --limit=1
  gh run watch <run_id>
  # If green: gate is proven live. Note SHA + run URL in Phase 1090 close-gate doc.
  # If red: the gate caught a regression OR the gate itself is misconfigured — debug from there.
  ```
- **PERF-01-drives-CI-default contract closed**: state explicitly that both Plan 1089-02's CI job and Plan 1089-03's Makefile use the SAME `<PERF_N>` value sourced from `.planning/audits/PYTEST-XDIST-PERF-v1020.md` Section 5 (instruction invariant #5). Cross-check command:
  ```bash
  diff <(grep -E "uv run pytest -n " .github/workflows/ci.yml | head -1 | grep -oE "\-n (auto|[0-9]+)") \
       <(grep -E "^\tdocker compose exec api uv run pytest -n " Makefile | head -1 | grep -oE "\-n (auto|[0-9]+)")
  # Must exit 0 (identical -n value in both surfaces).
  ```
- **Out of scope reaffirmations**: enterprise overlay deferred to HYG-02 if it surfaces; `pyproject.toml addopts` unchanged (Option A per CONTEXT.md); `max_connections` bump rejected (out of scope per REQUIREMENTS.md).
- **Phase 1090 handoff**: next phase is the skip audit + flake hunt + paper-trail close-gate per ROADMAP.md.

**Step 6 — Atomic commit (the load-bearing TD-13 invariant):**

Stage and commit EVERYTHING in ONE commit:

```bash
git add Makefile .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/1089-ci-gate-perf-parallel-default/1089-03-SUMMARY.md
git commit -m "$(cat <<'EOF'
ci(1089-03): close Phase 1089 — Makefile parallel default + atomic TD-13 flip (CI-01 + CI-02 + PERF-01)

Closes v1020 milestone Phase 1089. Final TD-13 atomic commit:
- Makefile:27 test: target switched to -n <PERF_N> (Plan 1089-01 Section 5 recommended default)
- New test-sequential: target preserves no-args sequential debugging path
- REQUIREMENTS.md [ ] -> [x] and Pending -> Complete for CI-01, CI-02, PERF-01 (3 reqs)
- ROADMAP.md Phase 1089 [ ] -> [x] + per-plan list updated
- STATE.md position advances to Phase 1090

Sequential baseline preserved (3036/0/38 or higher).
CI live-verification (first post-merge gate firing) tracked via gh run watch in SUMMARY.
EOF
)"
```

Substitute `<PERF_N>` in the commit message with the resolved value.

**Step 7 — Post-commit TD-13 verification (per CONTEXT.md):**

```bash
git diff-tree --no-commit-id --name-only -r HEAD | sort > /tmp/1089-03-files.txt
printf "Makefile\n.planning/REQUIREMENTS.md\n.planning/ROADMAP.md\n.planning/STATE.md\n.planning/phases/1089-ci-gate-perf-parallel-default/1089-03-SUMMARY.md\n" | sort > /tmp/1089-03-expected.txt
diff /tmp/1089-03-files.txt /tmp/1089-03-expected.txt
```

Must exit 0 (zero diff — exactly five files in the atomic commit).

ALSO confirm the negative-shape gate did NOT trigger by accident — REQUIREMENTS.md flips must have actually applied:

```bash
grep -E "^\| CI-01 .* Complete" .planning/REQUIREMENTS.md
grep -E "^\| CI-02 .* Complete" .planning/REQUIREMENTS.md
grep -E "^\| PERF-01 .* Complete" .planning/REQUIREMENTS.md
grep -E "^- \[x\] \*\*CI-01\*\*:" .planning/REQUIREMENTS.md
grep -E "^- \[x\] \*\*CI-02\*\*:" .planning/REQUIREMENTS.md
grep -E "^- \[x\] \*\*PERF-01\*\*:" .planning/REQUIREMENTS.md
grep -E "^- \[x\] \*\*Phase 1089:" .planning/ROADMAP.md
```

Six grep matches must all succeed (3 table rows + 3 checkbox flips + 1 ROADMAP flip). If any fails, the atomic commit is incomplete — investigate.

**Step 8 — Cross-check PERF-01-drives-CI-default contract (instruction invariant #5):**

```bash
CI_N=$(grep -E "uv run pytest -n " .github/workflows/ci.yml | head -1 | grep -oE "\-n (auto|[0-9]+)")
MAKE_N=$(grep -E "uv run pytest -n " Makefile | head -1 | grep -oE "\-n (auto|[0-9]+)")
test "$CI_N" = "$MAKE_N" && echo "OK: CI and Makefile agree on $CI_N" || { echo "FAIL: CI=$CI_N Makefile=$MAKE_N"; exit 1; }
```

Both surfaces MUST report identical `-n VALUE`. This is the contract-close verification.
  </action>
  <verify>
    <automated>git diff-tree --no-commit-id --name-only -r HEAD | sort > /tmp/1089-03-files.txt && printf "Makefile\n.planning/REQUIREMENTS.md\n.planning/ROADMAP.md\n.planning/STATE.md\n.planning/phases/1089-ci-gate-perf-parallel-default/1089-03-SUMMARY.md\n" | sort > /tmp/1089-03-expected.txt && diff /tmp/1089-03-files.txt /tmp/1089-03-expected.txt && grep -E "^\| CI-01 .* Complete" .planning/REQUIREMENTS.md && grep -E "^\| CI-02 .* Complete" .planning/REQUIREMENTS.md && grep -E "^\| PERF-01 .* Complete" .planning/REQUIREMENTS.md && grep -E "^- \[x\] \*\*CI-01\*\*:" .planning/REQUIREMENTS.md && grep -E "^- \[x\] \*\*CI-02\*\*:" .planning/REQUIREMENTS.md && grep -E "^- \[x\] \*\*PERF-01\*\*:" .planning/REQUIREMENTS.md && grep -E "^- \[x\] \*\*Phase 1089:" .planning/ROADMAP.md && CI_N=$(grep -E "uv run pytest -n " .github/workflows/ci.yml | head -1 | grep -oE "\-n (auto|[0-9]+)") && MAKE_N=$(grep -E "uv run pytest -n " Makefile | head -1 | grep -oE "\-n (auto|[0-9]+)") && test "$CI_N" = "$MAKE_N"</automated>
  </verify>
  <done>Atomic commit lands with exactly 5 files: `Makefile`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/phases/1089-ci-gate-perf-parallel-default/1089-03-SUMMARY.md`. REQUIREMENTS.md shows 3 `Complete` rows + 3 `[x]` checkboxes for CI-01/CI-02/PERF-01. ROADMAP.md Phase 1089 row shows `[x]`. STATE.md position advances to Phase 1090. The `-n` value in `.github/workflows/ci.yml` matches the value in `Makefile` (PERF-01-drives-CI-default contract closed per instruction invariant #5). Plan 1089-03 SUMMARY includes the `gh run watch` post-merge live-verification handoff. Sequential pytest baseline preserved (`failed == 0`, `passed >= 3036`).</done>
</task>

</tasks>

<verification>
**Plan-level verification (TD-13 atomic-commit gate, verified post-commit):**

1. `git diff-tree --no-commit-id --name-only -r HEAD` returns EXACTLY 5 files (Makefile + REQUIREMENTS.md + ROADMAP.md + STATE.md + 1089-03-SUMMARY.md). Atomic-commit contract verified.
2. REQUIREMENTS.md: 3 `Complete` rows + 3 `[x]` checkboxes for CI-01 + CI-02 + PERF-01.
3. ROADMAP.md: Phase 1089 `[x]` (complete) + per-plan list reflects actual 1089-01/02/03 plans (not stale 1088-01..05 labels).
4. STATE.md: position advanced to Phase 1090; progress counters bumped (completed_phases 6→7, completed_plans 11→14, percent 50→75); CI-01/CI-02/PERF-01 strikethrough'd in Deferred Items.
5. Makefile: `test:` target uses `-n <PERF_N>` (matching CI-01 job). `test-sequential:` target exists without `-n`. `test-cov:` unchanged.
6. PERF-01-drives-CI-default contract closed: `-n` value in Makefile matches `-n` value in CI workflow (instruction invariant #5).
7. Sequential pytest baseline preserved: `failed == 0`, `passed >= 3036` (instruction invariant #1).
8. CI live-verification handoff: Plan 1089-03 SUMMARY includes the `gh run watch` command for the operator's first post-merge confirmation (instruction invariant #4).
9. No changes to `backend/tests/conftest.py`, `backend/app/*`, `backend/pyproject.toml`, `frontend/*` (instruction invariant #2).
</verification>

<success_criteria>
- `make test` (no args) uses `-n <PERF_N>` parallel execution per Plan 1089-01 Section 5
- `make test-sequential` runs pytest without `-n` flag (debugging opt-in preserved per REQUIREMENTS.md CI-02)
- All three requirements (CI-01, CI-02, PERF-01) flip from `Pending` to `Complete` in REQUIREMENTS.md in the SAME atomic commit as the SUMMARY write (TD-13 `requirements_traceability_flip`)
- ROADMAP.md Phase 1089 row marked `[x]`; per-plan list reflects actual 1089-* plans
- STATE.md position advances to Phase 1090; progress counters updated; CI-01/CI-02/PERF-01 entries strikethrough'd in Deferred Items
- Sequential pytest baseline preserved (3036/0/38 or higher)
- `-n` value identical across CI workflow + Makefile (PERF-01-drives-CI-default contract closed)
- Atomic commit touches exactly 5 files (instruction invariant #3)
- CI live-verification handoff documented for operator via `gh run watch` (instruction invariant #4)
</success_criteria>

<output>
Create `.planning/phases/1089-ci-gate-perf-parallel-default/1089-03-SUMMARY.md` when done.
</output>
