# GeoLens Post-Implementation Audit - 2026-04-13

## Scope

- Resolved scope: whole repo
- Trigger: `$geolens-post-impl` with no args

## Baseline

Status: blocked, baseline red

Per `docs/testing-and-ci.md`, the audit started with the documented local CI baseline. The run stopped after the first failing baseline gates.

### Commands run

From repo root:

```bash
docker compose up -d --wait db
```

From `backend/`:

```bash
uv run ruff check .
uv run ruff format --check .
uv run bandit -r app/ -c pyproject.toml --severity-level high --confidence-level high
uv run pip-audit --strict --desc
```

### Results

- `docker compose up -d --wait db`: passed
- `uv run ruff check .`: passed
- `uv run ruff format --check .`: failed
- `uv run bandit -r app/ -c pyproject.toml --severity-level high --confidence-level high`: passed
- `uv run pip-audit --strict --desc`: failed

### Blocking baseline failures

`uv run ruff format --check .`

- Would reformat:
  - `alembic/versions/2026_04_12_1102-989ae68d7859_tighten_pydantic_varchar_constraints_p1_.py`
  - `app/ai/service.py`
  - `app/auth/dependencies.py`
  - `app/datasets/router_data.py`
  - `app/datasets/router_reupload.py`
  - `app/datasets/schemas.py`
  - `app/features/service.py`
  - `app/ingest/metadata.py`
  - `app/ingest/router.py`
  - `app/ingest/schemas.py`
  - `app/ingest/tasks.py`
  - `app/services/router.py`
  - `tests/test_features_geojson_z.py`

`uv run pip-audit --strict --desc`

- `pillow 12.1.1` - `CVE-2026-40192` - fix in `12.2.0`
- `pytest 9.0.2` - `CVE-2025-71176` - fix in `9.0.3`

## Recent Change Surface

Recent commits within 14 days heavily touched:

- ingest metadata and staging pipeline
- maps service/router and public viewer flows
- builder and viewer synchronization
- search and standards endpoints
- dataset detail UI and attribute table

These were the intended priority areas for the audit once the baseline was green.

## Audit Outcome

No post-implementation findings were assessed because the required baseline was already red. Per the skill workflow, existing baseline failures are not treated as audit findings and the audit stops here.

## Next Step

Clear the baseline first, then rerun:

- backend full coverage command from `docs/testing-and-ci.md`
- frontend gate matrix from `docs/testing-and-ci.md`
- post-implementation audit buckets after the baseline is green
